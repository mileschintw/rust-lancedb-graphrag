# Evaluation configuration overlay (LANCET_ENV=eval)
# Provides isolated LanceDB directory for evaluation benchmarks.
# lancedb_path-only overlay restored per 06.3.1 D-13 and 06.3.4.1 D-66: the
# [engine.workflow] table this overlay previously carried was accidentally
# added by 33e774b and duplicated config.toml's own values with no effective
# difference, so the effective eval workflow budgets equal config.toml's.

[engine]
lancedb_path = "./data/lancedb-eval"
