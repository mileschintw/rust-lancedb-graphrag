# Plan: Update README with Project Blueprint and Backlog

## Goal
Update `README.md` with references to all GSD planning documents (creating a project blueprint) and details of the 6 newly added backlog items.

## Tasks
1. Correct the links in `README.md` that point to the old repo name (`shrag`) and update them to use `lancet` and point to the `.discussion/` directory.
2. Add descriptions of the active `.planning/` documents (`PROJECT.md`, `REQUIREMENTS.md`, `ROADMAP.md`, `STATE.md`) to `README.md` so readers understand how the project is being managed.
3. Update the roadmap section in `README.md` to align with the actual 6-phase roadmap defined in `.planning/ROADMAP.md`.
4. Add a dedicated section in `README.md` for the 6 new backlog items (v2/v3 extension points) as captured in `.planning/ROADMAP.md`.
5. Update `.planning/STATE.md` to record this quick task in a "Quick Tasks Completed" section.
