---
phase: 260831-az7-phase-6-3-is-currently-stuck-the-previou
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/src/client/mod.rs
  - engine/src/client/tests.rs
  - engine/src/config.rs
  - engine/src/generation/tests.rs
  - engine/src/bin/inspect_lancedb.rs
  - engine/src/bin/seed_rag_fixture.rs
  - config/config.toml
  - config/config.example.toml
  - gateway/main_test.go
  - scripts/engine-test-targets.sh
  - eval/corpora/multihop_rag.toml
  - eval/corpora/graphrag_bench.toml
  - eval/src/lancet_eval/cli.py
  - eval/src/lancet_eval/corpus.py
  - eval/src/lancet_eval/preflight.py
  - eval/src/lancet_eval/score.py
  - eval/README.md
  - eval/tests/test_schema.py
  - eval/tests/test_report.py
  - eval/tests/test_judge.py
  - eval/tests/test_score_judged.py
  - eval/tests/test_preflight.py
  - eval/corpora/multihop_rag/document_map.json
autonomous: true
requirements: []
user_setup:
  - service: openrouter
    why: "Paid-tier model calls are billed against the project API key; the key's own daily spend limit gates every call in Tasks 1 and 3 independently of account balance."
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "Existing project key (already provisioned). Must resolve to a key whose GET /api/v1/key reports limit > 0 and limit_remaining > 0."
    dashboard_config:
      - task: "Keep the key's daily spend limit raised to at least $5/day (raised from $0 during discussion). If the limit lapses or resets to 0, raise it again before running Task 1 or Task 3."
        location: "OpenRouter Dashboard -> Settings -> API Keys -> (this key) -> Limit"
estimate:
  tokens: 95000
  raw_tokens: 62000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - The engine's OpenRouter embedding request carries an explicit dimensions field equal to the configured expected dimension (2048), so voyageai/voyage-4-large returns 2048-float vectors instead of its 1024-float default
    - A live single embedding call through the production OpenRouterClient code path (shipped model pin, from_env constructor) returns exactly 2048 floats
    - No free-tier-suffixed OpenRouter model name remains in config/config.toml, config/config.example.toml, eval/corpora/*.toml, or any non-test default under eval/src/lancet_eval/
    - The judge model remains a distinct model family from the engine generation model, and preflight's model_differentiation check passes
    - eval/corpora/multihop_rag/document_map.json contains 346 entries, all seeded against voyageai/voyage-4-large with no free-tier residue from the prior 6-document partial run
    - Offline suites stay green after every task - pinned Rust engine targets, the Go gateway suite, and the eval pytest suite
  artifacts:
    - engine/src/client/mod.rs
    - config/config.toml
    - eval/corpora/multihop_rag.toml
    - eval/corpora/multihop_rag/document_map.json
    - .planning/quick/260831-az7-phase-6-3-is-currently-stuck-the-previou/260831-az7-SUMMARY.md
  key_links:
    - OpenRouterEmbeddingConfig.expected_dimension -> EmbeddingRequest.dimensions -> OpenRouter response -> the dimension-mismatch validator in engine/src/client/mod.rs (around line 309) -> the 2048-wide Arrow FixedSizeList in engine/src/db/mod.rs (around lines 173-175)
    - config/config.toml [openrouter].generation_model -> SHIPPED_GENERATION_MODEL drift anchor in engine/src/generation/tests.rs (around line 1860), which deserializes ../config/config at test time
    - config/config.toml [openrouter].embedding_model -> the real engine binary spawned by gateway/main_test.go (around line 2470, Dir = repoRoot, embedding model NOT env-overridden) -> the mock embeddings-endpoint model assertion (around line 2308) and the snapshot EmbeddingModel assertions (around lines 2642 and 2802)
    - client::EMBEDDING_MODEL -> the embedding-model-class allowlist in engine/src/bin/inspect_lancedb.rs (around lines 196-198) -> readability of the reseeded eval store
    - OPENROUTER_API_KEY key-level daily spend limit -> every paid-model call in Task 1's live verify and Task 3's full reseed
---

<objective>
Phase 6.3 Plan 06.3-10 Task 1 is stalled at 6 of 346 documents because the engine and eval harness are pinned to free-tier OpenRouter model names that are rate-limited independently of account credit. Replace those pins with a cost-efficient paid-tier setup, add the explicit embedding `dimensions` request field the new embedding model requires to reach 2048 dims, prove the change live against the real engine code path, and complete the full 346-document reseed so Plan 06.3-10 can resume at its Task 2.

Purpose: Unblock the phase's headline deliverable (GAP-06.3-SC8, GAP-06.3-SC6) - a real recorded evaluation run cannot be produced against a corpus that is 6/346 seeded.
Output: Repinned engine + eval configuration, a `dimensions`-carrying embedding request with offline and live proof, a fully seeded `multihop_rag` corpus with a 346-entry `document_map.json`, and a SUMMARY handing control back to Plan 06.3-10 Task 2.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260831-az7-phase-6-3-is-currently-stuck-the-previou/260831-az7-CONTEXT.md
@.planning/phases/06.3-python-evaluation-harness-benchmark-corpora-and-recorded-run/06.3-HANDOVER.md
@rust-guidelines.md
@go-guidelines.md
</context>

<!-- planner-discipline-allow: :free -->
<!-- planner-discipline-allow: nvidia/llama-nemotron-embed-vl-1b-v2 -->
<!-- planner-discipline-allow: dots-studio/dots-3-note-preview -->

<model_pins>
The three replacement pins, decided in CONTEXT.md and not open for re-litigation:

| Role | Old pin | New pin |
|------|---------|---------|
| Embedding | `nvidia/llama-nemotron-embed-vl-1b-v2:free` | `voyageai/voyage-4-large` (requires explicit `dimensions` 2048; defaults to 1024) |
| Generation | `dots-studio/dots-3-note-preview:free` | `deepseek/deepseek-v4-flash-0731` |
| Judge | `meta-llama/llama-3.3-70b-instruct:free` | `meta-llama/llama-3.3-70b-instruct` (same model, paid routing) |

`openai/text-embedding-3-small` is disqualified - its dimension ceiling is 1536, empirically confirmed to reject a 2048 request.
`google/gemini-2.0-flash-001` (the handover doc's suggestion) no longer exists in OpenRouter's catalog.
</model_pins>

<scope_lock>
- Config/pin swap plus the one additive Rust request field only. Do NOT add a guardrail, lint, CI check, or validation preventing free-tier model names from being re-selected later - CONTEXT.md locks this task to unblocking Phase 6.3, not hardening the config surface.
- Do NOT change `EMBEDDING_DIMENSION` / `EMBEDDING_DIMENSIONS` away from 2048 anywhere. The Arrow schema width and the response validator both depend on it staying 2048; keeping it fixed is what makes this a no-migration change.
- Do NOT retune `MAX_RETRIES` (6) or `INITIAL_BACKOFF` (2s) in `engine/src/client/mod.rs`. CONTEXT.md lists this as discretion; the ruling is leave them - they were tuned for free-tier 429 storms, are harmless on paid tier, and touching them adds risk to a plan whose only job is unblocking.
- Do NOT change `eval/src/lancet_eval/config.py`'s Pydantic `judge_model` default. CONTEXT.md lists this as discretion; the ruling is no change - it is already a paid model (`openai/gpt-4o-mini`) and is overridden by the corpus TOML and CLI on every real run.
- Do NOT touch `engine/src/db/mod.rs`, `engine/src/ingest.rs`, `eval/src/lancet_eval/seed.py`, or the eval driver/journal/scoring logic. The reseed path already works; the models were the only blocker.
- Do NOT start Plan 06.3-10 Task 2 (the recorded benchmark run) or Task 3 (scoring/report). This plan ends when the corpus is seeded.
- Do NOT edit `.planning/phases/06.3-*/06.3-10-PLAN.md` or any phase SUMMARY.
</scope_lock>

<tasks>

<task type="tracer">
  <name>Task 1: Engine pins and explicit embedding dimensions, proven live end-to-end</name>
  <files>engine/src/client/mod.rs, engine/src/client/tests.rs, engine/src/config.rs, engine/src/generation/tests.rs, engine/src/bin/inspect_lancedb.rs, engine/src/bin/seed_rag_fixture.rs, config/config.toml, config/config.example.toml, gateway/main_test.go, scripts/engine-test-targets.sh</files>
  <precondition>`OPENROUTER_API_KEY` is set and `GET https://openrouter.ai/api/v1/key` reports `limit` greater than 0 and `limit_remaining` greater than 0. If `limit` is 0, halt and tell the developer to raise the key's daily spend limit in the OpenRouter dashboard before continuing - this is the root blocker that made every paid-model call return 403 during discussion, and it is independent of account balance.</precondition>
  <read_first>
    - `rust-guidelines.md` in the repo root (project CLAUDE.md mandates this before writing Rust).
    - `engine/src/client/mod.rs` lines 1-20 (the `EMBEDDING_MODEL` and `EMBEDDING_DIMENSION` consts), 72-130 (`OpenRouterEmbeddingConfig`, `EmbeddingRequest`), and 259-317 (`send_embedding`, including the dimension-mismatch validator).
    - `engine/src/client/tests.rs` around lines 208-264 - `embedding_request_uses_effective_model` shows the `server.request_bodies` capture mechanism this task's new assertion reuses; `embedding_config_preserves_bounds_and_redaction` already pins `expected_dimension` at 2048.
    - `engine/src/generation/tests.rs` lines 1860-1894 - `SHIPPED_GENERATION_MODEL` and `load_shipped_generation_model()`, which deserialize `../config/config` at test time. This is a live drift anchor, not a fixture.
    - `engine/src/bin/inspect_lancedb.rs` lines 15 and 190-200 - the stored-model allowlist that returns an unknown-class error for any model outside the const.
    - `gateway/main_test.go` around lines 2290-2320 and 2440-2475 - the RAG integration test spawns the real engine binary with `Dir = repoRoot`, so the engine loads `config/config.toml`. It env-overrides the generation model but NOT the embedding model, and its mock embeddings endpoint rejects any request whose model does not match the hardcoded literal.
    - `scripts/engine-test-targets.sh` - the pinned per-target test counts that must be re-pinned after adding tests.
  </read_first>
  <behavior>
    - Serialized embedding request body contains a `dimensions` key whose value equals the configured expected dimension (2048), alongside the existing `model` and `input` keys.
    - The `dimensions` value is sourced from `OpenRouterEmbeddingConfig.expected_dimension`, not a literal, so the value sent and the value the response validator enforces cannot drift apart.
    - A live call through `OpenRouterClient::from_env()` (the shipped pin, the real endpoint) returns a vector of exactly 2048 floats.
    - Every existing offline assertion that binds to the old embedding model name resolves to the new one, on both the Rust and Go sides.
  </behavior>
  <action>
    Add a `dimensions` field to the `EmbeddingRequest` struct in `engine/src/client/mod.rs` and populate it at the `send_embedding` call site from `self.config.expected_dimension`. Do not hardcode the number at the call site. Keep the existing `model` and `input` fields and the existing response-dimension validator unchanged.

    Repin `EMBEDDING_MODEL` in `engine/src/client/mod.rs` and `default_embedding_model()` in `engine/src/config.rs` to the new embedding model from the model_pins table, and update the matching literal in `engine/src/config.rs`'s default-construction test expectation (around line 496).

    Repin `[openrouter].embedding_model` and `[openrouter].generation_model` in `config/config.toml`, and the embedding model line in `config/config.example.toml`, to the new pins.

    Update `SHIPPED_GENERATION_MODEL` in `engine/src/generation/tests.rs` to the new generation pin. This constant is compared against a live deserialization of `config/config.toml`, so it fails the moment the config line changes unless updated in the same commit. Leave the sibling assertion that forbids `openai/gpt-4o-mini` intact - the new pin satisfies it.

    Update the standalone model constants in `engine/src/bin/inspect_lancedb.rs` and `engine/src/bin/seed_rag_fixture.rs` to the new embedding pin. The `inspect_lancedb` constant is not cosmetic: it is an allowlist that rejects any store whose rows carry a model outside it, so leaving it stale makes the reseeded eval store unreadable by that binary. This is a required repin to keep an existing gate working, not a new guardrail, so it stays inside scope.

    Update the six hardcoded embedding-model literals in `gateway/main_test.go` (the mock embeddings-endpoint model check and the snapshot `EmbeddingModel` assertions) to the new embedding pin. These are load-bearing because that test spawns the real engine binary against the repo's own `config/config.toml`.

    Add one Rust test in `engine/src/client/tests.rs`, a sibling of `embedding_request_uses_effective_model`, that drives `get_embeddings` against the existing `MockServer` and asserts the captured request body's `dimensions` value equals 2048.

    Add one `#[ignore]`-marked live test in `engine/src/client/tests.rs` that constructs the client via `OpenRouterClient::from_env()` (so it exercises the shipped pin and real endpoint, not a mock), requests a single short embedding, and asserts the returned vector length is exactly 2048. Skip cleanly with an explanatory message if `OPENROUTER_API_KEY` is absent. Name it exactly `live_embedding_returns_shipped_dimension` - the verify command filters on that literal with an exact match, so a differently-named test would make the gate pass having run zero tests. This is the live proof CONTEXT.md requires against the actual engine code path rather than a raw HTTP call, and it must pass before Task 3 spends the full reseed.

    When running that live gate, confirm from the command's own output that it reports one test run, not zero filtered out. A gate that runs nothing and exits 0 is not evidence.

    Re-pin the per-target test counts in `scripts/engine-test-targets.sh` to match the new totals after the added tests, so the invariant script does not fail on a stale count.

    Grep the whole repo for the two old engine-side model literals afterwards and confirm the only survivors are historical planning documents and the Phase 02 evidence fixtures under `scripts/` - if any live config, source default, or currently-executing test still carries them, fix it before declaring the task done.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked && sh scripts/engine-test-targets.sh && cd gateway && go test ./...</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked --lib -- --ignored live_embedding_returns_shipped_dimension</automated>
    <automated>test "$(git grep -n 'llama-nemotron-embed\|dots-3-note-preview' -- config engine gateway | wc -l)" -eq 0</automated>
  </verify>
  <done>The full pinned Rust engine suite, the engine-test-targets invariant script, and the Go gateway suite all exit 0 with the new pins in place. The mock-server test proves the wire body carries a `dimensions` value of 2048. The live ignored test returns a 2048-float vector from the real endpoint using the shipped pin. No live config file, Rust source default, or executing test under `config/`, `engine/`, or `gateway/` still references either old engine-side model name.</done>
</task>

<task type="auto">
  <name>Task 2: Repin the eval harness judge and generation model defaults</name>
  <files>eval/corpora/multihop_rag.toml, eval/corpora/graphrag_bench.toml, eval/src/lancet_eval/cli.py, eval/src/lancet_eval/corpus.py, eval/src/lancet_eval/preflight.py, eval/src/lancet_eval/score.py, eval/README.md, eval/tests/test_schema.py, eval/tests/test_report.py, eval/tests/test_judge.py, eval/tests/test_score_judged.py, eval/tests/test_preflight.py</files>
  <read_first>
    - `eval/README.md` around line 90 - states the judge model must be pinned distinctly from the engine's generation model. This design intent constrains the pin choice and must stay true after the swap.
    - `eval/src/lancet_eval/preflight.py` around lines 260-334 - `check_model_differentiation` and `run_preflight_checks`'s `generation_model` default parameter.
    - `eval/src/lancet_eval/corpus.py` around line 143 and `eval/src/lancet_eval/score.py` around line 727 - the fallback judge and generation model literals.
    - `eval/src/lancet_eval/cli.py` around lines 683-697 - the probe path's hardcoded `RunMetadata` model fields.
  </read_first>
  <action>
    Repin `judge_model` in `eval/corpora/multihop_rag.toml` and `eval/corpora/graphrag_bench.toml` to the paid judge pin from the model_pins table.

    Repin the free-tier judge and generation literals in `eval/src/lancet_eval/cli.py` (the probe `RunMetadata` block), `eval/src/lancet_eval/corpus.py` (the judge fallback), `eval/src/lancet_eval/preflight.py` (the `generation_model` default parameter), and `eval/src/lancet_eval/score.py` (the generation-model fallback). While in `cli.py` and `score.py`, also correct the stale `embedding_model` literals in those metadata blocks to the new embedding pin so recorded run metadata reports what was actually used.

    Update the model names quoted in `eval/README.md`'s judge section to the new pins, preserving the sentence's stated design intent that the judge is pinned distinctly from the generation model.

    Update the free-tier model literals used as fixtures in the five affected `eval/tests/` files so the suite exercises the shipped pins. Preserve each test's existing intent - in particular the preflight differentiation test must still assert a judge/generation pair that is genuinely distinct.

    Leave `eval/src/lancet_eval/config.py`'s Pydantic `judge_model` default untouched per the scope lock.

    Confirm the resulting judge pin and the new engine generation pin belong to different model families so `check_model_differentiation` passes for a judged run.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests -q</automated>
    <automated>test "$(git grep -n 'llama-3.3-70b-instruct:free\|dots-3-note-preview' -- eval | wc -l)" -eq 0</automated>
  </verify>
  <done>The eval pytest suite passes with at least the 136 tests recorded in the handover doc, zero free-tier model references remain anywhere under `eval/`, and the corpus TOML judge pin is a different model family from `config/config.toml`'s generation pin.</done>
</task>

<task type="auto">
  <name>Task 3: Full 346-document clean reseed of the multihop_rag corpus</name>
  <files>eval/corpora/multihop_rag/document_map.json</files>
  <precondition>`GET https://openrouter.ai/api/v1/key` still reports `limit` greater than 0 and `limit_remaining` sufficient for the run (roughly 1M embedding tokens plus per-document graph extraction, estimated well under $1 total). Re-check immediately before starting - a mid-run limit lapse produces exactly the partial-seed state this plan exists to escape. Also confirm the Docker compose stack (PostgreSQL) is running and the gateway is reachable, since `reseed` drives ingestion through the real gateway ingestion path.</precondition>
  <action>
    Restart the engine and gateway so they pick up the repinned `config/config.toml`, with `LANCET_ENV` set to `eval` so the isolated evaluation LanceDB path is used. Confirm the engine logs the new embedding and generation model names at startup before proceeding.

    Run the eval harness reseed for the `multihop_rag` corpus with its confirmation flag. This deliberately wipes and recreates the PostgreSQL eval schema, removes the eval LanceDB directory, and deletes the existing `document_map.json` before seeding all 346 documents from the committed subset. The wipe is required, not incidental: the 6 documents already in the store were embedded with the old model, and a homogeneous vector space is the point. No data migration is needed or wanted.

    The store's Arrow schema pins the embedding column at 2048 floats and the engine self-heals missing tables on access, so the recreated tables match the new model's explicitly-requested 2048 dimensions with no schema change. If any document fails with a dimension-mismatch error, stop immediately - that means the `dimensions` field from Task 1 is not reaching the wire, and continuing would burn the remaining budget on a broken run.

    Monitor progress. If the run halts partway, the seeder is resumable and skips already-mapped documents, so re-running seed (not reseed) continues from where it stopped - do not re-run reseed after a partial run unless the vector space has been contaminated by a model change.

    After completion, confirm the document map has 346 entries and that a spot-checked LanceDB row carries the new embedding model name. Run the eval preflight checks for the corpus and confirm they pass, including the judge/generation model differentiation check from Task 2.
  </action>
  <verify>
    <automated>uv run --project eval python -c "import json,pathlib; d=json.loads(pathlib.Path('eval/corpora/multihop_rag/document_map.json').read_text()); n=len(d['entries']); print(n); assert n==346, f'expected 346 seeded documents, got {n}'"</automated>
    <automated>uv run --project eval lancet-eval preflight --corpus multihop_rag</automated>
    <human-check>Confirm the engine startup logs show the new embedding and generation model names, that the reseed completed without dimension-mismatch or rate-limit errors, and that total OpenRouter spend for the run is within the key's daily limit.</human-check>
  </verify>
  <done>`eval/corpora/multihop_rag/document_map.json` contains exactly 346 entries, the eval store holds only vectors produced by the new embedding model, preflight passes for the corpus, and Phase 6.3 Plan 06.3-10 is unblocked at its Task 2 (recorded benchmark run).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine -> OpenRouter API | Outbound requests carry the project API key; responses are untrusted input parsed into embedding vectors |
| eval harness -> gateway ingestion | 346 untrusted corpus documents cross into the ingestion pipeline |
| developer shell -> OpenRouter dashboard | The key's daily spend limit is set out-of-band and is the sole cost ceiling |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-az7-01 | Information Disclosure | `OPENROUTER_API_KEY` in shell history / logs / the new live test | high | mitigate | The live test reads the key from the environment via the existing `from_env` constructor and never accepts it as an argument or prints it; the existing redaction test that asserts provider errors never contain the credential stays green in Task 1's suite run |
| T-az7-02 | Denial of Service | Key daily spend limit exhausted mid-reseed | medium | mitigate | Runnable `<precondition>` on Tasks 1 and 3 asserts `limit` and `limit_remaining` before any billed call; Task 3's resumable-seed note prevents a wasteful full restart after a partial run |
| T-az7-03 | Tampering | Untrusted OpenRouter response with wrong vector width silently corrupting the store | medium | mitigate | The existing response validator rejects any embedding whose length differs from the configured expected dimension, and Task 1 sources the requested `dimensions` from that same field so the two cannot drift |
| T-az7-04 | Elevation of Privilege | Reseed wiping the wrong PostgreSQL schema or LanceDB directory | high | mitigate | Unchanged existing guards - the reseed path asserts schema isolation and that the admin DSN targets the eval database before dropping anything; this plan's scope lock forbids touching that code |
| T-az7-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable - no new package is added by any task; all model changes are string pins and one additive struct field on existing dependencies |
</threat_model>

<verification>
1. `cargo test --manifest-path engine/Cargo.toml --locked` exits 0 and `sh scripts/engine-test-targets.sh` verifies every pinned invariant against the re-pinned counts.
2. `cd gateway && go test ./...` exits 0, including the RAG integration test that spawns the real engine against the repinned `config/config.toml`.
3. `uv run --project eval pytest eval/tests -q` passes at or above the 136-test baseline.
4. The ignored live embedding test returns exactly 2048 floats through the production client path.
5. `document_map.json` reports 346 entries and `lancet-eval preflight --corpus multihop_rag` passes.
6. Repo-wide grep confirms no free-tier model literal survives in `config/`, `engine/`, `gateway/`, or `eval/` - only in `.planning/` history and the frozen Phase 02 evidence fixtures under `scripts/`.
</verification>

<success_criteria>
- The 346-document `multihop_rag` corpus is fully seeded against paid-tier models with a homogeneous 2048-dimension vector space.
- Phase 6.3 Plan 06.3-10 can resume directly at its Task 2 with no further configuration work.
- No offline test suite regressed, and no guardrail, retry retuning, or config-surface hardening was added beyond the pin swap and the single additive `dimensions` field.
</success_criteria>

<output>
Create `.planning/quick/260831-az7-phase-6-3-is-currently-stuck-the-previou/260831-az7-SUMMARY.md` when done. Record: the three final model pins, the observed live embedding dimension, the actual OpenRouter spend for the reseed, the final `document_map.json` entry count, and an explicit handoff line stating that Phase 6.3 Plan 06.3-10 resumes at Task 2.
</output>
