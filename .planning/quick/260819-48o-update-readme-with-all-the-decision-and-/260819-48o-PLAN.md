# Plan: Update README with Current Progress, AI-Collaboration Framing, and Key Decisions

## Goal

Rewrite `README.md` (the only file this plan touches) to reflect that Phases 1-5 of the roadmap (including inserted Phase 04.1) are complete — 89 executed plans, a working Go gateway + Rust RAG/GraphRAG engine — with Phase 6 (Observability, Evaluation & Polish) next and not yet started. Preserve the existing resume-oriented "Story & Motivation" framing and explicitly extend it to also showcase building effectively in cooperation with an AI agent workflow (this project's GSD planning/execution methodology with Claude Code), grounded in artifacts already in the repo (`.planning/` PLAN/SUMMARY/VERIFICATION/UAT trail, force-closed ADRs). Surface a concise set of the most discussable key decisions/trade-offs from `PROJECT.md`. Do not fabricate any technical detail, command, or setup instruction not backed by `.planning/STATE.md`, `.planning/PROJECT.md`, or `.planning/ROADMAP.md`.

## Key Facts to Use (verified — do not re-derive or contradict)

- **Phase status:** Phase 1 complete (2026-07-13). Phase 2 complete (2026-07-30, force-closed per ADR-02-004, residual debt deferred to Phase 6). Phase 3 complete (2026-08-05, force-closed per ADR-03-003, residual debt deferred to Phase 6). Phase 4 complete (2026-08-06) — this was a `lance-graph`/LanceDB compatibility **spike only**; full extraction/storage/traversal was deferred to and delivered by the inserted **Phase 04.1** (9/9 plans complete). Phase 5 complete (27/27 plans; UAT 10/10 passed, 0 issues; verified live end-to-end against real OpenRouter). **Phase 6 — Observability, Evaluation & Polish — is next and has not started** (0 plans, status "Ready to plan").
- **Do not write "6 of 6 phases complete."** The roadmap's 6 numbered phases (1-6) still have Phase 6 outstanding; the "6 completed phase-entries" figure only reconciles because it counts the inserted Phase 04.1 in place of a 6th slot. Spell it out by phase name/number, not as a bare "6/6" fraction.
- **Plan count:** 89 plans executed total (Phase 1: 1, Phase 2: 28, Phase 3: 23, Phase 4: 1, Phase 04.1: 9, Phase 5: 27 → sums to 89).
- **What's built:** Go API gateway (control plane) — HTTP API, document upload/ingestion, `/rag/query` endpoint, graph query, SSE streaming of workflow events, PostgreSQL-backed metadata/session/checkpoint persistence. Rust RAG engine (data plane) — gRPC server, custom chunking (fixed-size + structure-aware), LanceDB embedded vector+graph storage, hybrid retrieval (dense vector + BM25 lexical + metadata filtering + cross-variant RRF fusion), entity/relationship extraction into a knowledge graph (`entities`/`entity_edges` LanceDB tables), Cypher-style graph traversal via `lance-graph`, graph context compiled into RAG prompts alongside chunk evidence, a lightweight Rust state machine (5 nodes: reformulate query → retrieve hybrid → extract graph context → assemble prompt → generate answer) with streaming client-facing workflow events, node timeouts/retries/cancellation, and PostgreSQL-backed workflow checkpoints.
- **What's NOT built yet (Phase 6 scope):** OpenTelemetry tracing across Go/gRPC/Rust, the offline LLM-as-judge evaluation script, and the RAG-03 hardening/debt-closure work. Do not describe these as done.
- **Source code exists at:** `gateway/` (Go), `engine/` (Rust), `proto/` (Protobuf contracts) — confirmed present as top-level directories in this repo.
- **Local-only exposure constraint (`DEBT-CR-04`) is still open/accepted**, and is no longer Phase-02-scoped — it is a standing v1/local-first constraint under review at Phase 6 per ROADMAP.md's Phase 6 success criterion 6. Keep the constraint itself; fix only its stale "Phase 02" framing.

## Out of Scope for This Task

- No run/setup instructions, commands, env vars, or config paths — not backed by any `.planning/` source and would be fabrication. Phase 6 success criterion 3 owns that deliverable.
- No changes to any file other than `README.md`.
- No changes to the "Future Backlog & Extension Points" section (999.x items) — not part of this task's scope.

## Tasks

### 1. Fix the status banner, extend the Story & Motivation section, and correct the "Target Architecture" framing

- Replace the `> [!NOTE]` status banner (currently "Project Status: Planning & Pre-Start Phase" / "No source code has been written yet" / "Development will begin shortly") with an accurate banner stating: Phases 1-5 complete (89 plans executed across Phase 1, 2, 3, 4, the inserted Phase 04.1, and 5), a working Go gateway + Rust RAG/GraphRAG engine exists in `gateway/`, `engine/`, `proto/`, and Phase 6 (Observability, Evaluation & Polish) is next and not yet started.
- In "The Story & Motivation," keep every existing sentence — do not delete or shorten the existing framing about showing systems-engineering depth by building the data plane from scratch. Add a new paragraph (or two) extending that same motivation to explicitly cover building effectively in cooperation with an AI agent: state that the implementation was planned and executed using an AI-agent-assisted engineering workflow (the GSD planning/execution methodology with Claude Code — visible under `.planning/`, one PLAN/SUMMARY/VERIFICATION/UAT set per phase), and that operating that human+AI loop — directing the agent, reviewing its output, holding it to explicit verification and acceptance criteria, and making the judgment calls it can't (e.g. the ADR-02-004 and ADR-03-003 decisions to force-close phases and explicitly defer remaining gaps as tracked technical debt rather than silently drop them) — is itself part of the engineering ability this project demonstrates, alongside the systems/RAG work. Write this as an authentic continuation of the existing narrative voice, not a bolted-on marketing line, and do not overclaim (this is human-directed collaboration with a reviewed AI workflow, not autonomous/unsupervised AI authorship).
- In "Target Architecture & Core Components": reword the section intro ("Once implemented, the platform will utilize...") to present tense since the architecture is implemented. In "Key Technical Targets," update items 1-3 (Go API Gateway, Rust RAG Engine incl. chunker/hybrid retriever/GraphRAG traverser, gRPC interface) to reflect they are built and working, but leave item 4 (OpenTelemetry tracing) and item 5 (LLM-as-judge evaluation) explicitly worded as planned/upcoming in Phase 6 — do not mark those two as done.

### 2. Correct Repository Contents, Roadmap, Local-Only Exposure Constraint, and add a concise Key Decisions section

- In "Repository Contents & Planning Blueprint," add a source-code subsection (alongside the existing Design & Discussion Documents and GSD Planning Blueprint subsections) listing `gateway/` (Go control plane), `engine/` (Rust data plane), and `proto/` (Protobuf service contracts) as the actual implementation.
- In "Implementation Roadmap," mark Phase 1 through Phase 5 as complete (using the Key Facts above — do not just say "6 of 6 done"), add a Phase 04.1 entry (Knowledge Graph Extraction & Query — Full Implementation, inserted after Phase 4, 9/9 plans complete) between the current Phase 4 and Phase 5 entries, and mark Phase 6 (Observability, Evaluation & Polish) as next/not started. Keep the existing phase bullet descriptions but adjust verb tense/status per phase.
- Add a new concise section (e.g. "Key Decisions & Trade-offs") after the Roadmap section, before "Future Backlog." List at most 6-7 decisions pulled from `.planning/PROJECT.md`'s Key Decisions table, each as one line (decision + one-line rationale): Go gateway + Rust engine split (control/data plane separation); gRPC/Protobuf service contract; LanceDB for embedded local-first vector+graph storage; custom chunking and hybrid retrieval (dense + BM25 + RRF fusion) instead of a black-box framework; lightweight Rust state machine instead of adopting LangGraph/Dify (borrows orchestration concepts, keeps scope narrow); `lance-graph` + arrow-version IPC bridge for Cypher traversal, de-risked via a dedicated compatibility-spike phase (Phase 4) before committing to the full implementation. Optionally add one line on the explicit technical-debt ledger / force-close discipline (ADR-02-004, ADR-03-003) as a trade-off-management decision worth calling out. Do not dump the full PROJECT.md table — this section stays short.
- In "Local-Only Exposure Constraint & Debt Triggers," keep the constraint and its review triggers verbatim (this debt item is still open and accurate), but fix the stale scoping sentence "Phase 02 supports trusted local callers only" — reword to describe it as a standing v1/local-first constraint (not phase-scoped), consistent with ROADMAP.md's Phase 6 success criterion that reviews `DEBT-CR-04` if it hasn't triggered earlier.

## Acceptance Check

After editing, verify:

- `README.md` no longer contains the strings "Planning & Pre-Start", "No source code has been written yet", or "Once implemented, the platform will utilize" (grep returns 0 matches for each).
- The roadmap section shows Phase 1, 2, 3, 4, 04.1, and 5 as complete, and Phase 6 as next/not started.
- `README.md` references `gateway/`, `engine/`, and `proto/` as existing source directories.
- The Story & Motivation section still contains its original opening paragraphs plus a new AI-collaboration paragraph.
- A "Key Decisions" (or equivalently named) section exists with no more than ~7 bullet items.
