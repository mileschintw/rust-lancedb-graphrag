---
phase: 02-ingestion-chunking-vector-storage
plan: 26
type: execute
wave: 19
depends_on:
  - "02-24"
files_modified:
  - gateway/db/document_test.go
  - AGENTS.md
autonomous: true
gap_closure: true
requirements:
  - DATA-01
  - DATA-03
must_haves:
  truths:
    - "TestReconciliationIntentRecordAndClaim creates and claims every fixture through createIsolatedTestPool; no batch claim reaches the configured public schema (ADR-02-003 D-05)."
    - "Every public-table before/after snapshot error in the isolated lease tests fails the test immediately (ADR-02-003 D-05)."
    - "Repository review guidance requires isolated per-test schemas for claim/lease integration tests and adds no automated enforcement (ADR-02-003 D-05)."
  artifacts:
    - path: "gateway/db/document_test.go"
      provides: "Fully isolated claim/lease tests with fatal snapshot reads"
    - path: "AGENTS.md"
      provides: "Review checklist convention for destructive global-claim tests"
  key_links:
    - from: "TestReconciliationIntentRecordAndClaim"
      to: "createIsolatedTestPool"
      via: "all query objects and claimant connections use the returned claimant pool"
      pattern: "createIsolatedTestPool"
    - from: "public count snapshots"
      to: "test verdict"
      via: "every QueryRow Scan error calls t.Fatalf before comparison"
      pattern: "QueryRow.*public"
---

<objective>
**As a** Lancet maintainer, **I want to** run reconciliation lease tests without touching unrelated rows, **so that** integration evidence is safe and cannot false-pass.

Implement ADR-02-003 D-05 exactly: isolate the remaining claimant, make snapshot errors fatal, and record the review-only convention.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-REVIEW.md
@.discussion/decisions/phases/02/2026-07-30-ADR-02-003-staging-lifecycle-gap-closure.md
@gateway/db/document_test.go
@AGENTS.md
</context>

## Artifacts this phase produces

- One isolated `TestReconciliationIntentRecordAndClaim`.
- Fatal public-snapshot reads in every isolated lease test.
- A human code-review checklist entry for all claim/lease integration tests.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Claim reconciliation intents only inside a per-test schema</name>
  <files>gateway/db/document_test.go</files>
  <read_first>
    - `go-guidelines.md` and `gateway/go.mod` — detect and apply the supported Go version
    - `gateway/db/document_test.go` — `TestReconciliationIntentRecordAndClaim`, `createIsolatedTestPool`, both isolated lease tests, and public snapshots
    - `gateway/db/query.sql.go` — generated claim method signature
    - `ADR-02-003` D-05 — exact isolation and fatal-snapshot decisions
  </read_first>
  <behavior>
    - The existing named `TestReconciliationIntentRecordAndClaim` creates its query object and all fixtures through the isolated claimant pool.
    - Its batch claim can see only its own schema and returns its own due intent even when unrelated public intents exist.
    - Every initial/final public document or intent count read aborts on query/scan error before any numeric comparison.
    - All three isolated claim/lease tests drop only their unique schema through registered cleanup.
  </behavior>
  <action>
Implement ADR-02-003 D-05 without changing production SQL or claim behavior. Convert `TestReconciliationIntentRecordAndClaim` to call `createIsolatedTestPool`, construct `New(claimantPool)`, and create every document and reconciliation intent through that pool. Keep its record/claim assertions, but make public rows structurally unreachable.

For every before/after public count in the isolated lease tests, capture and check the `Scan` error immediately with a descriptive `t.Fatalf`; only compare counts after all reads succeed. Preserve immediate cleanup registration, claimant pool closure, and unique-schema drop. Do not add package serialization, fixture predicates to the global due query, or automated policy enforcement.
  </action>
  <verify>
    <automated>docker compose up -d db; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; Push-Location gateway; try { atlas schema apply --env local --auto-approve; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; $env:TEST_DATABASE_URL='postgres://postgres:postgres@127.0.0.1:5432/lancet?sslmode=disable'; $required=@('TestReconciliationIntentRecordAndClaim','TestReconciliationIntentClaimLeaseIsExclusive','TestReconciliationIntentClaimLeasePreservesUnrelatedDocumentAndIntent'); $listed=(go test -list '^TestReconciliationIntent(RecordAndClaim|ClaimLeaseIsExclusive|ClaimLeasePreservesUnrelatedDocumentAndIntent)$' ./db); if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; foreach($name in $required){if(-not ($listed -match [regex]::Escape($name))){throw "missing PostgreSQL regression: $name"}}; go test -count=1 -run '^TestReconciliationIntent(RecordAndClaim|ClaimLeaseIsExclusive|ClaimLeasePreservesUnrelatedDocumentAndIntent)$' ./db; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE} } finally { Remove-Item Env:TEST_DATABASE_URL -ErrorAction SilentlyContinue; Pop-Location }</automated>
  </verify>
  <acceptance_criteria>
    - The named record-and-claim test uses the isolated claimant pool for all fixtures and claims.
    - No configured public-schema row is leasable by any of the three isolated tests.
    - Snapshot read failure cannot become a zero-valued passing comparison.
    - Named test discovery precedes execution and every Go command checks `$LASTEXITCODE` immediately.
  </acceptance_criteria>
  <done>ADR-02-003 D-05 closes CR-01 and WR-02 with runnable isolated PostgreSQL tests.</done>
</task>

<task type="auto">
  <name>Task 2: Record the claim-and-lease review checklist</name>
  <files>AGENTS.md</files>
  <read_first>
    - `AGENTS.md` — current repository-wide agent and review guidance
    - `ADR-02-003` D-05 — review checklist decision and automation exclusion
  </read_first>
  <action>
Add a concise code-review checklist section to `AGENTS.md`: every integration test that globally claims, leases, dequeues, or batch-selects mutable rows must use a unique per-test schema/database before the query runs; reviewers must verify every fixture and claimant uses that isolated connection and every external snapshot error is fatal. State that this is a review convention. Add no hook, linter, workflow, generated check, or dependency.
  </action>
  <verify>
    <automated>$claimRule=Select-String -Path AGENTS.md -Pattern 'claim|lease' -CaseSensitive:$false; if(-not $claimRule){throw 'claim/lease review rule missing'}; $isolationRule=Select-String -Path AGENTS.md -Pattern 'isolated.*test.*schema|per-test schema' -CaseSensitive:$false; if(-not $isolationRule){throw 'per-test schema review rule missing'}; git diff --check -- AGENTS.md gateway/db/document_test.go; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}</automated>
  </verify>
  <acceptance_criteria>
    - Reviewers have an explicit isolated-schema checklist for global mutable claims.
    - The checklist covers fixture connections, claimant connections, and fatal snapshots.
    - No automated enforcement surface is added.
  </acceptance_criteria>
  <done>ADR-02-003 D-05's review-only convention is repository-visible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| integration test → configured PostgreSQL | Test queries can mutate durable rows outside their fixtures. |
| public snapshot → test assertion | Read failures can be mistaken for zero counts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-26-01 | Tampering | global reconciliation claim | critical | mitigate | Route every fixture and claimant through a unique isolated schema. |
| T-02-26-02 | Repudiation | public snapshot assertion | high | mitigate | Fail immediately on every before/after query error. |
| T-02-26-03 | Denial of Service | unrelated leased intents | high | mitigate | Make public rows structurally unreachable to the test query. |
</threat_model>

<verification>
- The three named PostgreSQL tests are discovered and pass against an enabled local database.
- Go commands check `$LASTEXITCODE` immediately.
- `git diff --check` passes for both modified files.
</verification>

<success_criteria>
- CR-01 and WR-02 meet ADR-02-003 D-05.
- The convention is review-only as locked.
- No deferred debt or production query behavior enters scope.
</success_criteria>

<output>
Create `.planning/phases/02-ingestion-chunking-vector-storage/02-26-SUMMARY.md` when done.
</output>
