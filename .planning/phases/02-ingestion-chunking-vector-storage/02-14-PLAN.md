---
phase: 02-ingestion-chunking-vector-storage
plan: 14
type: execute
wave: 11
depends_on:
  - "02-13"
files_modified:
  - engine/src/main.rs
  - engine/src/tests.rs
  - engine/src/client/tests.rs
autonomous: true
gap_closure: true
requirements:
  - DATA-03
  - DATA-07
  - DATA-08
  - RAG-06
must_haves:
  truths:
    - "Every schema-field lookup after canonical LanceDB versions are captured returns Result and reaches the existing rollback funnel instead of panicking (D-22, D-24; WR-01)."
    - "A deterministic missing-field fault preserves the prior documents/nodes/edges generation, records failed worker state, and a clean retry converges."
    - "The engine all-target warnings-as-errors gate is green, including the inspector target and OpenRouter client tests (WR-04)."
    - "No lint cleanup weakens schema drift, rollback, OpenRouter retry, or embedding behavior."
  artifacts:
    - path: "engine/src/main.rs"
      provides: "Rollback-safe schema-derived null-array lookup"
    - path: "engine/src/tests.rs"
      provides: "Schema lookup fault, rollback preservation, worker survival, and retry convergence regression"
    - path: "engine/src/client/tests.rs"
      provides: "Clippy-clean canonical repeated mock embedding construction"
  key_links:
    - from: "engine/src/main.rs:schema field lookup"
      to: "engine/src/main.rs:rollback_replacement"
      via: "propagated Result after version snapshot"
      pattern: "field_with_name.*ok_or_else"
    - from: "engine/src/tests.rs"
      to: "ReplacementMutationBoundary/worker state"
      via: "deterministic schema-field fault proves rollback and retry"
      pattern: "schema.*(rollback|retry)"
    - from: "engine/src/client/tests.rs"
      to: "cargo clippy --all-targets"
      via: "warning-free standard iterator construction"
      pattern: "repeat_n"
---

<objective>
Route post-snapshot schema failures through rollback and restore a green all-target Rust lint gate.

Purpose: A schema lookup must not panic the sole worker or bypass canonical replacement recovery, and every compiled target must pass warnings-as-errors.
Output: Fallible schema lookup, rollback/retry coverage, and all-target Clippy cleanup.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-CONTEXT.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-VERIFICATION.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-REVIEW.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-07-SUMMARY.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-13-SUMMARY.md
@engine/src/main.rs
@engine/src/tests.rs
@engine/src/client/tests.rs
</context>

<phase_goal>
**As a** Lancet API user, **I want to** ingest and safely replace text or Markdown documents, **so that** the last completed LanceDB index and PostgreSQL status remain trustworthy through failures and concurrent polling.
</phase_goal>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Roll back one post-snapshot schema lookup failure and retry cleanly</name>
  <files>engine/src/main.rs, engine/src/tests.rs</files>
  <read_first>
    - `AGENTS.md` — repository language-specific instruction triggers
    - `rust-guidelines.md` — mandatory Rust implementation and test conventions
    - `engine/src/main.rs` — canonical version capture, nullable array closures, add batches, error funnel, and `rollback_replacement`
    - `engine/src/tests.rs` — `ReplacementMutationBoundary`, persisted-state snapshots, all-boundary retry tests, and worker-state assertions
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-REVIEW.md` — WR-01 exact lookup sites
  </read_first>
  <behavior>
    - A missing node placeholder field after version capture returns an error rather than panicking.
    - The prior canonical documents/nodes/edges rows remain queryable after the failure.
    - Worker status becomes failed and the worker remains alive.
    - Retrying the same document ID with the valid schema converges to one current generation without stale rows.
  </behavior>
  <action>
Add a deterministic test seam for one schema-field lookup after canonical version capture, using the established replacement fault-injection style in `engine/src/tests.rs`. First write the persisted-state test proving prior-generation preservation, failed status, worker survival, and clean retry convergence.

Replace every post-snapshot `field_with_name(...).expect(...)` used to build node or edge arrays with a shared fallible schema-derived array helper. Include table/field context in errors, propagate through `Result`, and ensure the existing outer replacement error path invokes `rollback_replacement`. Keep startup D-22 fail-fast validation intact; this change applies to runtime drift/corruption after startup only.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml tests::schema_field_lookup_failure_rolls_back_and_retry_converges -- --exact --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - The named test exercises a real post-snapshot lookup failure.
    - No schema lookup in the replacement mutation region uses `expect` or `unwrap`.
    - The old generation survives and retry converges.
    - The sole worker remains available after the failed job.
  </acceptance_criteria>
  <done>Runtime schema-field drift is a recoverable replacement error, not a worker-killing panic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the exact all-target Clippy gate green</name>
  <files>engine/src/client/tests.rs, engine/src/main.rs, engine/src/tests.rs</files>
  <read_first>
    - `AGENTS.md` — repository language-specific instruction triggers
    - `rust-guidelines.md` — mandatory Rust implementation and test conventions
    - `engine/src/client/tests.rs` — mock 2048-value response builder flagged by `manual_repeat_n`
    - `engine/src/lib.rs` — shared database module established by Plan 02-13
    - `engine/src/bin/inspect_lancedb.rs` — shared-module import established by Plan 02-13
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-REVIEW.md` — WR-04 exact command and warnings
  </read_first>
  <behavior>
    - `cargo clippy --manifest-path engine/Cargo.toml --all-targets -- -D warnings` exits zero.
    - The mock response still contains exactly 2048 finite values.
    - The engine, inspector, library, and every test target compile and pass.
    - Production behavior and dependency versions are unchanged.
  </behavior>
  <action>
Replace the flagged manual repeat/take construction with the edition-supported standard-library equivalent while preserving exactly 2048 mock values. Resolve any residual warnings exposed by the Plan 02-13 library seam or Task 1 through precise code changes, not crate-wide lint allowances. Do not add `allow(dead_code)`, weaken `-D warnings`, skip targets, or alter dependencies.

Run format, the complete test suite, and the exact review command. If Clippy exposes a new warning caused by these scoped changes, correct the producing code and add/retain behavior coverage before rerunning the same gate.
  </action>
  <verify>
    <automated>cargo fmt --manifest-path engine/Cargo.toml -- --check; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; cargo test --manifest-path engine/Cargo.toml; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; cargo clippy --manifest-path engine/Cargo.toml --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - The exact all-target warnings-as-errors command exits zero.
    - Full Rust tests pass after formatting.
    - No broad lint suppression or target exclusion appears.
    - Locked OpenRouter and LanceDB behavior is unchanged.
  </acceptance_criteria>
  <done>Rollback-safe runtime schema handling and every Rust target are test-green and warning-free.</done>
</task>

</tasks>

<threat_model>
## Threat Model Configuration

ASVS Level: 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LanceDB runtime schema → replacement worker | Post-startup drift can trigger a panic after mutation begins. |
| Replacement error → rollback funnel | Errors must retain the canonical version across three tables. |
| Test/lint target → release confidence | Excluded or suppressed warnings can conceal a broken binary path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-14-01 | Denial of Service | schema-field panic | high | mitigate | Convert lookup to Result, test worker survival, and route through rollback. |
| T-02-14-02 | Tampering | partial replacement | high | mitigate | Prove old-generation preservation and retry convergence with persisted rows. |
| T-02-14-03 | Repudiation | lint target omission | medium | mitigate | Run exact `--all-targets -- -D warnings` command without allowances. |
| T-02-14-SC | Tampering | package installs | high | mitigate | No package install or manifest dependency change is planned. |
</threat_model>

## Artifacts this phase produces

- `engine/src/main.rs`: fallible post-snapshot schema lookup through rollback.
- `engine/src/tests.rs`: schema-fault rollback, worker-survival, and retry proof.
- `engine/src/client/tests.rs`: all-target Clippy-clean mock construction.

## Gap traceability

| Source | Finding | Task | Status |
|--------|---------|------|--------|
| REVIEW | WR-01 rollback-safe schema lookup | Task 1 | PLANNED |
| REVIEW | WR-04 green all-target Clippy | Task 2 | PLANNED |

<verification>
- Exact schema-fault rollback/retry test passes.
- Full engine test suite passes.
- Exact all-target Clippy warnings-as-errors command exits zero.
</verification>

<success_criteria>
- Runtime schema drift cannot panic the sole worker after version capture.
- Every such failure uses the canonical rollback funnel.
- Rust format, tests, and all-target lint are green.
</success_criteria>

<output>
Create `.planning/phases/02-ingestion-chunking-vector-storage/02-14-SUMMARY.md` when done.
</output>
