---
phase: 02-ingestion-chunking-vector-storage
plan: 12
type: execute
wave: 10
depends_on:
  - "02-10"
files_modified:
  - scripts/phase02_live_evidence.py
  - scripts/test_phase02_live_evidence.py
  - verify-ingestion.sh
  - .gitignore
autonomous: true
gap_closure: true
requirements:
  - DATA-01
  - DATA-03
must_haves:
  truths:
    - "A challenge older than the allowed gate age is rejected even when paired with otherwise fresh, valid evidence."
    - "The complete issued_at → run_started_at → generated_at window is bounded, ordered, and enforced under optimized isolated Python."
    - "verify-ingestion.sh deletes only a sample it created itself; a caller-owned positional input remains byte-for-byte unchanged on success and every early failure path (D-01)."
    - "The repository ignore rule for build output is anchored and no longer matches engine/src/bin while the exact private Phase 02 runtime paths remain ignored (WR-03)."
  artifacts:
    - path: "scripts/phase02_live_evidence.py"
      provides: "Bounded challenge age and complete run-window validation"
    - path: "scripts/test_phase02_live_evidence.py"
      provides: "Optimized stale-challenge and overlong-run regressions"
    - path: "verify-ingestion.sh"
      provides: "Explicit sample ownership tracking and safe cleanup"
    - path: ".gitignore"
      provides: "Root-anchored build-output ignore that preserves Rust source binaries"
  key_links:
    - from: "scripts/phase02_live_evidence.py:validate_challenge"
      to: "scripts/phase02_live_evidence.py:validate_gate"
      via: "one shared maximum challenge age plus full-run duration contract"
      pattern: "MAX_(CHALLENGE_AGE|RUN_WINDOW)"
    - from: "verify-ingestion.sh:argument parsing"
      to: "verify-ingestion.sh:cleanup"
      via: "sample ownership flag controls deletion"
      pattern: "sample_.*(owned|temporary)"
    - from: ".gitignore"
      to: "engine/src/bin/inspect_lancedb.rs"
      via: "root-anchored build directory pattern does not match nested Cargo source"
      pattern: "^/bin/"
---

<objective>
Close replay-window, caller-input ownership, and ignore-scope defects in the live tooling.

Purpose: The final provider gate must be fresh and non-destructive, and repository hygiene must not hide future Rust source binaries.
Output: Bounded challenge/run validation, safe sample cleanup, regressions, and an anchored ignore rule.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-CONTEXT.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-VERIFICATION.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-REVIEW.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-09-SUMMARY.md
@scripts/phase02_live_evidence.py
@scripts/test_phase02_live_evidence.py
@verify-ingestion.sh
@.gitignore
</context>

<phase_goal>
**As a** Lancet API user, **I want to** ingest and safely replace text or Markdown documents, **so that** the last completed LanceDB index and PostgreSQL status remain trustworthy through failures and concurrent polling.
</phase_goal>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject one stale challenge across the complete live-run window</name>
  <files>scripts/phase02_live_evidence.py, scripts/test_phase02_live_evidence.py</files>
  <read_first>
    - `scripts/phase02_live_evidence.py` — timestamp constants, `validate_challenge`, `validate_gate`, and `build_evidence`
    - `scripts/test_phase02_live_evidence.py` — `fixture_pair`, optimized subprocess runner, and structured-failure cases
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-REVIEW.md` — CR-03 reproduction
  </read_first>
  <behavior>
    - A challenge issued beyond the configured maximum age fails `parse-challenge`, `build-evidence`, and `validate-gate`.
    - Fresh evidence cannot make a stale challenge pass.
    - `generated_at - issued_at` and `run_started_at - issued_at` cannot exceed the complete gate run window.
    - Boundary-equal timestamps pass; one second beyond each bound fails.
    - Tests run with `PYTHONOPTIMIZE=1` and `python -O -I`.
  </behavior>
  <action>
Define one explicit maximum challenge age and one complete-run-window bound beside the existing evidence-age/future-skew constants. Apply the challenge bound in `validate_challenge` so every helper subcommand inherits it, then enforce the ordered issued/start/generated durations in evidence validation and construction. Use current UTC injected by existing test seams; avoid wall-clock sleeps.

Extend the optimized isolated suite with a one-year-old challenge paired with fresh valid evidence, an overlong run, and exact boundary/one-second-over cases. Keep error messages field-specific without echoing challenge values or content. Preserve the existing strict schema, replay, evidence freshness, and future-skew checks.
  </action>
  <verify>
    <automated>$env:PYTHONOPTIMIZE='1'; try { python -O -I scripts/test_phase02_live_evidence.py; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } finally { Remove-Item Env:PYTHONOPTIMIZE -ErrorAction SilentlyContinue }</automated>
  </verify>
  <acceptance_criteria>
    - The exact stale-challenge regression exits nonzero under optimized isolated Python.
    - Complete run duration is bounded independently of evidence freshness.
    - Boundary tests document and enforce the accepted window precisely.
    - No private runtime artifact or external service is touched.
  </acceptance_criteria>
  <done>An arbitrarily old challenge or overlong run can no longer authorize fresh-looking evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve caller input and anchor the build-output ignore</name>
  <files>verify-ingestion.sh, scripts/test_phase02_live_evidence.py, .gitignore</files>
  <read_first>
    - `verify-ingestion.sh` — positional sample parsing, credential check, `cleanup`, and `mktemp` sample creation
    - `scripts/test_phase02_live_evidence.py` — subprocess and temporary-file cleanup conventions
    - `.gitignore` — exact runtime paths and the generic `bin/` entry
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-REVIEW.md` — CR-04 and WR-03
  </read_first>
  <behavior>
    - Passing a caller-owned sample and forcing failure before service startup leaves its bytes, size, and path unchanged.
    - A script-created sample is removed by cleanup.
    - Cleanup remains safe for empty/unset variables and removes only script-owned temporary logs/evidence staging.
    - `git check-ignore --no-index engine/src/bin/future_inspector.rs` returns nonzero.
    - `git check-ignore` still matches `bin/placeholder` and both exact Phase 02 runtime JSON paths.
  </behavior>
  <action>
Introduce explicit ownership state for the sample path. Set it only when the runner creates the temporary sample, and gate cleanup deletion on that state. A positional caller path must never become script-owned. Add a shell-level regression launched from the existing Python suite: create a caller sample, force the safe pre-service missing-credential failure, and compare bytes plus existence afterward. Exercise script-owned cleanup through a test-only safe path that does not start services, perform network calls, or require a credential; keep test control in the test file rather than production-only fixture code.

Replace the unanchored `bin/` entry with `/bin/` per WR-03. Add exact `git check-ignore --no-index` assertions for nested Rust source, root build output, and both existing private runtime paths. Do not broaden ignores for `.planning`, `engine/src`, challenge, or evidence.
  </action>
  <verify>
    <automated>$env:PYTHONOPTIMIZE='1'; try { python -O -I scripts/test_phase02_live_evidence.py; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } finally { Remove-Item Env:PYTHONOPTIMIZE -ErrorAction SilentlyContinue }; bash -n verify-ingestion.sh; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; git check-ignore -q --no-index -- bin/placeholder; if ($LASTEXITCODE -ne 0) { throw "root /bin rule missing" }; git check-ignore -q --no-index -- engine/src/bin/future_inspector.rs; if ($LASTEXITCODE -eq 0) { throw "nested Rust source is still ignored" }</automated>
  </verify>
  <acceptance_criteria>
    - The early-failure regression proves caller data survives byte-for-byte.
    - Script-owned temporary input still cleans up.
    - Nested Cargo binary sources are not ignored.
    - Root build output and both exact private runtime artifacts remain ignored.
  </acceptance_criteria>
  <done>The live runner owns only its own temporary sample, and the build ignore cannot hide Rust source binaries.</done>
</task>

</tasks>

<threat_model>
## Threat Model Configuration

ASVS Level: 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime challenge/evidence → validator | Attacker-controlled timestamps may replay an old authorization. |
| Caller filesystem → live runner | A caller-owned document crosses a cleanup boundary. |
| Git ignore rules → source control | An overly broad pattern can conceal security-relevant source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-12-01 | Spoofing | stale challenge | high | mitigate | Enforce maximum challenge age and complete-run duration in shared validation. |
| T-02-12-02 | Tampering | caller-owned sample | high | mitigate | Track ownership explicitly and delete only script-created samples. |
| T-02-12-03 | Repudiation | optimized validation | medium | mitigate | Run stale/boundary regressions under optimized isolated Python. |
| T-02-12-04 | Information Disclosure | ignored Rust source | medium | mitigate | Anchor `/bin/` and prove nested `engine/src/bin` is visible to Git. |
| T-02-12-SC | Tampering | package installs | high | mitigate | No package install is planned; existing Python, Bash, and Git tooling is sufficient. |
</threat_model>

## Artifacts this phase produces

- `scripts/phase02_live_evidence.py`: stale-challenge and full-run-window enforcement.
- `scripts/test_phase02_live_evidence.py`: optimized freshness, ownership, and ignore-scope regressions.
- `verify-ingestion.sh`: caller-safe input cleanup.
- `.gitignore`: root-anchored build output rule.

## Gap traceability

| Source | Finding | Task | Status |
|--------|---------|------|--------|
| VERIFICATION | Stale challenge and bounded run window | Task 1 | PLANNED |
| VERIFICATION | Preserve caller-owned input | Task 2 | PLANNED |
| REVIEW | CR-03 | Task 1 | PLANNED |
| REVIEW | CR-04 | Task 2 | PLANNED |
| REVIEW | WR-03 anchored ignore rule | Task 2 | PLANNED |

<verification>
- Optimized isolated Python suite passes.
- `bash -n verify-ingestion.sh` passes.
- Caller-owned early-failure sample remains unchanged.
- Git ignore probes distinguish root `/bin/` from `engine/src/bin/`.
</verification>

<success_criteria>
- Old challenges and overlong runs fail closed.
- Caller files survive all cleanup paths.
- Future Rust binary sources cannot be hidden by the build-output ignore.
</success_criteria>

<output>
Create `.planning/phases/02-ingestion-chunking-vector-storage/02-12-SUMMARY.md` when done.
</output>
