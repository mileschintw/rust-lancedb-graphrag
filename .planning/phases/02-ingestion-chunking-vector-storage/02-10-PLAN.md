---
phase: 02-ingestion-chunking-vector-storage
plan: 10
type: execute
wave: 9
depends_on:
  - "02-09"
files_modified:
  - .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json
  - .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json
autonomous: false
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-06
  - DATA-07
  - DATA-08
  - DATA-09
  - RAG-06
user_setup:
  - service: openrouter
    why: "A fresh provider-backed ingestion after Plans 02-07 through 02-09 is the final Phase 02 evidence gate"
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "OpenRouter dashboard API keys; export it only in the user's private shell"
must_haves:
  truths:
    - "A fresh restrictive challenge is issued only after Plans 02-07 through 02-09 and their deterministic checks complete."
    - "One new production ingestion uses the real OpenRouter provider, locked model, 10-second request timeout, three-attempt policy, and concurrency cap five (D-17, D-18, D-19, D-20)."
    - "The evidence carries the exact fresh challenge/run identity and ordered timestamps; continuation text, a prior 02-06 pass, or an 02-09 summary cannot establish success."
    - "For the recorded UUID, PostgreSQL is completed and its positive chunk count equals the positive LanceDB node count; LanceDB has exactly one canonical document, zero staged rows, 2048-wide embeddings for the locked model, contiguous unique nodes, referentially valid unique edges, and one non-stale generation (D-21, D-23, D-24)."
    - "The final validator runs direct current PostgreSQL and LanceDB reinspection, exits zero, and only then removes both the challenge and evidence artifacts."
    - "Credentials, authorization headers, raw uploaded bytes, stored document text, and stored chunk content never appear in evidence, logs, summaries, staged files, or commits."
  artifacts:
    - path: ".planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json"
      provides: "Short-lived restrictive single-use challenge issued after all implementation changes"
    - path: ".planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json"
      provides: "Short-lived sanitized evidence from the exact post-change real-provider run"
    - path: "verify-ingestion.sh"
      provides: "Production OpenRouter live runner and sanitized evidence writer hardened by 02-09"
    - path: "verify-live-evidence.sh"
      provides: "Optimization-resistant final validator, direct store reinspection, and success-only dual-artifact cleanup hardened by 02-09"
  key_links:
    - from: ".planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json"
      to: ".planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json"
      via: "the live runner copies exact challenge/run identity and starts only after challenge issuance"
      pattern: "(challenge|run_id|issued_at|run_started_at)"
    - from: "verify-live-evidence.sh"
      to: "PostgreSQL and engine/src/bin/inspect_lancedb.rs"
      via: "validate-gate re-queries both durable stores for the recorded UUID before deleting either runtime artifact"
      pattern: "(validate-gate|inspect_lancedb|document-id)"
  prohibitions:
    - requirement_id: DATA-01
      category: privacy
      status: unresolved
      verification: test
      flagged: true
      flagged_unverified: true
      statement: "MUST NOT persist or emit the OpenRouter credential, authorization headers, raw uploaded bytes, stored document text, or stored chunk content in challenge/evidence artifacts, logs, summaries, staged files, or commits."
---

<objective>
Execute the fresh post-change Phase 02 live final gate.

Purpose: Prove the production ingestion behavior and hardened validator delivered by Plans 02-07 through 02-09 against a new real OpenRouter run and current PostgreSQL/LanceDB state.
Output: A just-issued restrictive challenge, sanitized evidence from one credentialed post-change ingestion, and exit-zero final validation followed by removal of both runtime artifacts.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-CONTEXT.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-VERIFICATION.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-07-SUMMARY.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-08-SUMMARY.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-09-SUMMARY.md
@verify-ingestion.sh
@verify-live-evidence.sh
@scripts/phase02_live_evidence.py
@engine/src/bin/inspect_lancedb.rs
@config/config.verify.toml
</context>

<phase_goal>
**As a** Lancet API user, **I want to** ingest and safely replace text or Markdown documents, **so that** the last completed LanceDB index and PostgreSQL status remain trustworthy through failures and concurrent polling.
</phase_goal>

<tasks>

<task type="auto">
  <name>Task 1: Issue a fresh restrictive post-change challenge</name>
  <files>.planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json, .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json</files>
  <read_first>
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-07-SUMMARY.md` — replacement, node-null, and canceled-compensation results that must precede the live run
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-08-SUMMARY.md` — timeout and durable-inspector results that must precede the live run
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-09-SUMMARY.md` — optimized validation, exact-ignore, derived-evidence, and dual-cleanup results that must precede challenge issuance
    - `verify-live-evidence.sh` — hardened `--prepare-gate` contract
  </read_first>
  <action>
Run all non-credentialed prerequisites automatically, including the full deterministic Rust, Go, optimized-Python, shell-syntax, and exact-ignore checks recorded by Plans 02-07 through 02-09. Stop before challenge creation on any failure. Then invoke `verify-live-evidence.sh --prepare-gate` with the exact phase-local challenge and evidence paths.

Preparation must remove stale prior artifacts, refuse tracked/staged paths and symlinks, generate independent unpredictable challenge and run identifiers from an OS cryptographic source, record a current RFC3339 UTC issue time, and atomically publish only the strict challenge schema under restrictive permissions. Challenge issuance is the final operation immediately before Task 2. Do not print the challenge, credential, uploaded content, stored content, headers, or environment.
  </action>
  <verify>
    <automated>bash ./verify-live-evidence.sh --prepare-gate --challenge .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json --evidence .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json</automated>
  </verify>
  <acceptance_criteria>
    - Plans 02-07 through 02-09 summaries and deterministic exit-zero checks exist before issuance.
    - No prior evidence or challenge survives preparation.
    - The new challenge is strict, unpredictable, fresh, restrictive, untracked, unstaged, and issued as the final pre-checkpoint action.
    - Preparation output reveals no private or content-bearing values.
  </acceptance_criteria>
  <done>A fresh single-use challenge issued after every production and validator change is ready for exactly one credentialed run.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Run one real OpenRouter ingestion against the fresh challenge</name>
  <files>.planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json</files>
  <read_first>
    - `verify-ingestion.sh` — production managed-services runner, exact challenge binding, direct reconciliation, and atomic sanitized evidence contract
    - `.planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json` — machine-consumed single-use challenge; do not inspect, paste, or edit it
  </read_first>
  <action>
Ask the user only for the private credential-dependent operation. In their private repository-root shell, they export the real `OPENROUTER_API_KEY` and run the exact wrapper command in the instructions. The command must use the production Rust OpenRouter client and all post-02-07 behavior, perform a new upload after reading the fresh challenge, reach terminal completion, directly reconcile PostgreSQL and LanceDB, and atomically write sanitized evidence only after success.

Missing credentials, a modified/missing challenge, provider failure, timeout exhaustion, service failure, or any status/count/model/width/generation/continuity/referential-integrity mismatch must exit nonzero without passing evidence. A response such as `done` is only a continuation to Task 3 and cannot authorize a summary or phase completion.
  </action>
  <instructions>
    1. In a private shell at the repository root, export the real `OPENROUTER_API_KEY`.
    2. Run exactly: `bash ./verify-ingestion.sh --managed-services --challenge-file .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json --evidence .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json`.
    3. If it reports `Ingestion validation: SUCCESS`, respond `done`. Otherwise provide only sanitized failing command/error context; never paste the key, environment, raw upload, or stored content.
  </instructions>
  <verification>
    <automated>test -s .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json &amp;&amp; test -s .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json</automated>
  </verification>
  <verify>
    <automated>test -s .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json &amp;&amp; test -s .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json</automated>
  </verify>
  <acceptance_criteria>
    - A new production OpenRouter request occurs after this plan's challenge issuance.
    - Evidence binds exactly to the issued challenge/run identity and ordered timestamps.
    - Evidence contains only sanitized identifiers, timestamps, counts, model/provider identity, and derived integrity facts.
    - Human continuation text cannot bypass Task 3.
  </acceptance_criteria>
  <done>The fresh real-provider run has produced untrusted sanitized evidence; acceptance remains pending Task 3.</done>
  <resume-signal>Type `done` only after the wrapper reports success, or provide sanitized failure context. Every response advances only to mechanical validation.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Reinspect PostgreSQL and LanceDB, validate mechanically, and clean both artifacts</name>
  <files>.planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json, .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json</files>
  <read_first>
    - `verify-live-evidence.sh` — hardened `--validate-gate` path with isolated explicit validation and success-only cleanup
    - `scripts/phase02_live_evidence.py` — strict schema, provenance, freshness, privacy, and live-state comparison contract
    - `engine/src/bin/inspect_lancedb.rs` — durable-row-derived provider/model/generation/continuity/uniqueness contract
    - `.planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json` — authoritative invocation identity
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json` — untrusted evidence to validate
  </read_first>
  <action>
Run the final validator against the exact phase-local paths. Do not infer success from Task 2 response text, the script sentinel by itself, a prior Phase 02 live pass, or any summary. Require explicit isolated-mode schema, provenance, freshness, privacy, provider/model, count, width, generation, continuity, and referential-integrity validation.

Using the evidence UUID, the validator must directly re-query current PostgreSQL and rerun the durable-row inspector against the current verification LanceDB store. Require PostgreSQL completed status with a positive count equal to the positive node count, exactly one canonical document, zero staged rows, one persisted locked model/provider and generation, width 2048, unique contiguous chunk identifiers/indexes, unique edges whose endpoints belong to current nodes, and a present non-negative edge count. Any missing service, command failure, mismatch, stale/replayed/malformed evidence, or secret/content-shaped field exits nonzero and preserves both artifacts for sanitized diagnosis.

Only after every check succeeds may `--validate-gate` remove both the challenge and evidence. Capture `$LASTEXITCODE` immediately after the Bash command in PowerShell before any follow-up check; fail on nonzero. Then require both exact runtime paths to be absent, untracked, and unstaged before creating the summary.
  </action>
  <verify>
    <automated>bash ./verify-live-evidence.sh --validate-gate --challenge .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json --evidence .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json; $gateExit = $LASTEXITCODE; if ($gateExit -ne 0) { throw "post-change live validator failed with exit code $gateExit" }; if ((Test-Path '.planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json') -or (Test-Path '.planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json')) { throw "successful validation did not remove both runtime artifacts" }; git diff --quiet --cached -- .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json; if ($LASTEXITCODE -ne 0) { throw "runtime artifacts are staged" }</automated>
  </verify>
  <acceptance_criteria>
    - `verify-live-evidence.sh --validate-gate` exits zero after direct current PostgreSQL/LanceDB reinspection.
    - The durable stores agree on UUID, completed status, positive equal counts, one document, zero staging rows, locked provider/model, 2048 width, one generation, continuity, uniqueness, and edge referential integrity.
    - Failure preserves both artifacts and cannot create the summary.
    - Success removes both artifacts; neither is tracked, staged, committed, or summarized.
  </acceptance_criteria>
  <done>Only an exit-zero validator plus absent runtime artifacts authorizes the Phase 02 final summary and completion.</done>
</task>

</tasks>

<threat_model>
## Threat Model Configuration

ASVS Level: 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Challenge issuer → private user shell | A single-use invocation identity crosses into the credentialed operation. |
| Private shell → Rust engine/OpenRouter | A private credential authorizes a real external embedding request. |
| Live runner → evidence file | Runtime results cross into an untrusted short-lived artifact. |
| Validator → PostgreSQL/LanceDB | Evidence identity is compared with current durable state. |
| Runtime artifacts → Git index | Private short-lived files could be disclosed if cleanup/ignore controls fail. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10-01 | Spoofing | challenge/evidence provenance | high | mitigate | Issue a fresh unpredictable post-change challenge and require exact run identity plus ordered timestamps. |
| T-02-10-02 | Tampering | live evidence and durable state | high | mitigate | Validate explicitly in isolated mode and re-query PostgreSQL plus row-derived LanceDB facts before acceptance. |
| T-02-10-03 | Information Disclosure | credential/content/runtime files | high | mitigate | Keep the credential private, reject content/secret fields, use exact ignores/restrictive writes, and remove both artifacts only after success. |
| T-02-10-04 | Repudiation | human continuation text | medium | mitigate | Treat it only as continuation; require validator exit zero and artifact absence before summary or phase completion. |
| T-02-10-05 | Denial of Service | provider or local services | low | accept | Missing dependencies visibly fail the gate and preserve artifacts for bounded retry. |
| T-02-10-SC | Tampering | package installs | high | mitigate | No package install occurs; the plan uses the committed Rust, Go, Bash, and Python-standard-library tooling. |
</threat_model>

## Spec-less probe disposition

All 16 unresolved edge-probe rows remain accounted for exactly as in Plans 02-07 and 02-09: five executable DATA-03/DATA-06/DATA-07 truths and eleven explicit `flagged_assumptions`. This live-only plan neither resolves nor drops any of them.

## Warning disposition

- WR-01 is implemented and tested by 02-07 before this live run.
- WR-02 remains explicitly unresolved because D-15 locks pending-job discard and no source defines restart reconciliation semantics.

## Multi-source coverage audit

| SOURCE | ID | Feature/Requirement | Plans | Status | Notes |
|--------|----|---------------------|-------|--------|-------|
| GOAL | — | Ingest text/markdown, chunk, and store in LanceDB | 02-01–02-10 | COVERED | This plan re-runs the integrated production path after all gap changes. |
| REQ | DATA-01, DATA-02, DATA-03 | Upload, chunk, and persist trustworthy metadata/vectors | 02-07–02-10 | COVERED | Tasks 2-3 prove the new run and direct durable count agreement. |
| REQ | DATA-06, DATA-07, DATA-08 | Placeholder/null schema integrity | 02-07, 02-10 | COVERED | Deterministic tests precede direct persisted-state inspection. |
| REQ | DATA-09 | ExactMatchResolver | prior 02-03, 02-10 regression path | COVERED | The production ingestion path retains the locked resolver. |
| REQ | RAG-06 | Async background worker | 02-07, 02-10 | COVERED | The post-change live run reaches terminal worker completion. |
| RESEARCH | §1.1–1.7 | Gateway, worker, OpenRouter, schemas, null placeholders | 02-07–02-10 | COVERED | The final live path exercises the established architecture without new dependencies. |
| RESEARCH | §2 | Unit, integration, and live UAT | 02-07–02-10 | COVERED | Deterministic checks plus a fresh provider-backed gate are executable. |
| CONTEXT | D-01–D-16 | Upload, metadata, chunking, queue, polling, worker contracts | 02-01–02-10 | COVERED | Tasks 2-3 exercise the integrated contracts without changing them. |
| CONTEXT | D-17–D-20 | OpenRouter-only model, timeout/retry, concurrency | 02-08, 02-10 | COVERED | A fresh production request occurs after the timeout fix. |
| CONTEXT | D-21–D-25 | LanceDB schema, nulls, replacement, token metadata | 02-07–02-10 | COVERED | Direct inspection checks the post-change canonical generation. |
| CONTEXT | D-26–D-30 | Shared configuration | prior plans, 02-10 | COVERED | The committed verification overlay selects the same configured store for runner and validator. |

<verification>
- `bash ./verify-live-evidence.sh --prepare-gate --challenge .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json --evidence .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json`
- In the user's private credentialed shell: `bash ./verify-ingestion.sh --managed-services --challenge-file .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json --evidence .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json`
- `bash ./verify-live-evidence.sh --validate-gate --challenge .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json --evidence .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json`; capture `$LASTEXITCODE` before any cleanup or follow-up command.
- Require validator exit zero, direct PostgreSQL/LanceDB agreement, and absence of both runtime files before the summary or phase completion.
</verification>

<success_criteria>
- A fresh post-change challenge and real OpenRouter ingestion exercise the production path delivered by Plans 02-07 through 02-09.
- Current PostgreSQL and LanceDB directly agree on UUID, terminal status, positive counts, one document, zero staging, locked model/provider, 2048 width, generation uniqueness, chunk continuity, and edge integrity.
- Optimized execution, replayed artifacts, stale evidence, and human continuation prose cannot bypass acceptance.
- Failure preserves both runtime artifacts; success removes both, and neither can be staged or committed.
- No credential, authorization header, raw upload, stored document text, stored chunk content, or environment dump appears in evidence, logs, summaries, or commits.
</success_criteria>

<output>
Create `.planning/phases/02-ingestion-chunking-vector-storage/02-10-SUMMARY.md` only after Task 3's validator exits zero and both runtime artifacts are absent, untracked, and unstaged. Record only sanitized provider/model/status/count/width/generation/continuity facts. Bare approval, done text, prior 02-06 evidence, and the 02-09 summary are non-authoritative and must never complete the phase.
</output>
