---
phase: 02-ingestion-chunking-vector-storage
plan: 07
type: execute
wave: 7
depends_on:
  - "02-06"
files_modified:
  - engine/src/main.rs
  - gateway/main.go
  - gateway/main_test.go
autonomous: true
gap_closure: true
requirements:
  - DATA-01
  - DATA-03
  - DATA-07
  - DATA-08
  - RAG-06
must_haves:
  truths:
    - "A real error at every documents/nodes/edges delete or add boundary, and at final staging cleanup, enters rollback_replacement after version capture (D-14, D-21, D-24)."
    - "After each deterministic boundary failure, the prior completed document, nodes, and edges remain queryable; retrying converges to one canonical generation with no staged, duplicate, or stale rows (DATA-03 idempotency/concurrency probes)."
    - "Persisted node summary placeholders are Arrow null values, not non-null empty text, while DATA-07 nullable schema initialization remains idempotent (D-23)."
    - "Canceled upload requests still receive bounded detached PostgreSQL compensation, preserving the original HTTP 429/502 response mapping and Go-only database boundary (D-02, D-05, D-11, D-12)."
    - "The communities table still initializes empty and repeatedly without schema drift (DATA-06 empty probe, regression coverage)."
  artifacts:
    - path: "engine/src/main.rs"
      provides: "Single rollback funnel for all post-snapshot replacement failures, named mutation boundaries, retry-convergence tests, and schema-derived node summary null persistence"
      contains:
        - "ReplacementMutationBoundary"
        - "rollback_replacement"
        - "new_null_array"
    - path: "gateway/main.go"
      provides: "Detached bounded compensation context for canceled ingestion requests"
      contains:
        - "compensateFailedIngest"
        - "context.WithTimeout"
    - path: "gateway/main_test.go"
      provides: "Canceled-request compensation behavior test"
  key_links:
    - from: "engine/src/main.rs"
      to: "rollback_replacement"
      via: "one result boundary wraps every operation after canonical versions are captured, including staging cleanup"
      pattern: "rollback_replacement"
    - from: "gateway/main.go"
      to: "documentStore.UpdateStatus"
      via: "compensation derives a bounded context independently of the canceled request"
      pattern: "context.WithTimeout"
  flagged_assumptions:
    - requirement_id: DATA-01
      category: empty
      status: unresolved
      flagged: true
      statement: "No source decision specifies whether a zero-byte upload is rejected or represented as a zero-chunk document."
    - requirement_id: DATA-01
      category: encoding
      status: unresolved
      flagged: true
      statement: "No source decision specifies whether invalid UTF-8 is rejected, lossily decoded, or normalized before character offsets are calculated."
    - requirement_id: DATA-02
      category: unclassified
      status: unresolved
      flagged: true
      statement: "The edge probe could not classify an additional chunking behavior; the locked structure-aware and fixed-size contracts remain authoritative."
    - requirement_id: DATA-06
      category: adjacency
      status: unresolved
      flagged: true
      statement: "No source decision defines merge or separation semantics for equal or adjacent community identifiers."
    - requirement_id: DATA-06
      category: ordering
      status: unresolved
      flagged: true
      statement: "No source decision defines stable ordering for equal community identifiers."
    - requirement_id: DATA-07
      category: adjacency
      status: unresolved
      flagged: true
      statement: "No source decision defines adjacency semantics for summary or unsummarized-reference values."
    - requirement_id: DATA-07
      category: ordering
      status: unresolved
      flagged: true
      statement: "No source decision defines stable ordering when unsummarized-reference values compare equal."
    - requirement_id: DATA-07
      category: concurrency
      status: unresolved
      flagged: true
      statement: "Concurrent schema initialization across multiple engine processes is unspecified; the locked single-engine startup model is retained."
    - requirement_id: DATA-08
      category: unclassified
      status: unresolved
      flagged: true
      statement: "The edge probe could not classify another edge-table behavior beyond the explicit separate-table and nullable-column contract."
    - requirement_id: DATA-09
      category: unclassified
      status: unresolved
      flagged: true
      statement: "The edge probe could not classify another EntityResolver behavior; ExactMatchResolver semantics remain unchanged."
    - requirement_id: RAG-06
      category: unclassified
      status: unresolved
      flagged: true
      statement: "Restart reconciliation for accepted pending jobs is unspecified. D-15 deliberately discards pending queue entries at shutdown, so changing restart behavior requires a new product decision rather than this gap fix."
---

<objective>
Close the canonical replacement integrity gap and the canceled-request compensation warning.

Purpose: A failed same-ID replacement or canceled upload must not destroy the last completed generation or strand gateway metadata.
Output: One rollback funnel covering every real post-snapshot storage error, deterministic boundary/retry tests, Arrow-null node summaries, and bounded detached gateway compensation.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-CONTEXT.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-VERIFICATION.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-05-SUMMARY.md
@engine/src/main.rs
@gateway/main.go
@gateway/main_test.go
</context>

<phase_goal>
**As a** Lancet API user, **I want to** ingest and safely replace text or Markdown documents, **so that** the last completed LanceDB index and PostgreSQL status remain trustworthy through failures and concurrent polling.
</phase_goal>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one real canonical mutation failure rolls back and retries end to end</name>
  <files>engine/src/main.rs</files>
  <read_first>
    - `engine/src/main.rs` — `replace_document_with_faults`, `rollback_replacement`, direct table deletes/adds, staging cleanup, and worker replacement tests
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-VERIFICATION.md` — actual-error rollback gap and retry behavior-unverified item
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-CONTEXT.md` — D-14 sequential writer, D-21 startup tables, D-22 drift policy, and D-24 replacement semantics
  </read_first>
  <behavior>
    - A deterministic failure returned from the production mutation abstraction at `DocumentsAdd` reaches `rollback_replacement`, not a direct `?` return.
    - The prior raw document, node IDs/count, edge IDs/count, and generation timestamp remain queryable after that failure.
    - Retrying the identical replacement without the failure leaves one document row, zero staged rows, contiguous unique chunk indexes, unique node/edge IDs, and only the replacement generation.
  </behavior>
  <action>
Start with a failing integration test named `replacement_documents_add_failure_rolls_back_and_retry_converges`. Replace the post-write-only `ReplacementBoundary` seam with a production-used `ReplacementMutationBoundary` contract whose named boundaries include `EdgesDelete`, `NodesDelete`, `DocumentsDelete`, `DocumentsAdd`, `NodesAdd`, `EdgesAdd`, and `StagingDelete`. The production implementation must perform the actual LanceDB `Table::delete` or `Table::add(...).execute()` operation; the deterministic test implementation delegates all operations except its selected boundary, where it returns an error.

Restructure `replace_document_with_faults` so canonical versions are captured before any mutation, then run every schema read, batch construction, canonical delete/add, edge construction, and final staging delete inside one `Result`-returning operation. On any error from that operation, call `rollback_replacement` for documents, nodes, and edges before returning. Do not leave any direct propagation path between the first canonical mutation and successful staging deletion. Preserve D-14's single consumer, D-22's fail-fast startup schema check, and D-24's durable staging row until the replacement fully succeeds.
  </action>
  <verify>
    <automated>$testList = cargo test --manifest-path engine/Cargo.toml -- --list 2&gt;&amp;1 | Out-String; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { throw "Rust test listing failed with exit code $listExit" }; if ($testList -notmatch '(?m)^tests::replacement_documents_add_failure_rolls_back_and_retry_converges: test$') { throw "required exact Rust test is absent: tests::replacement_documents_add_failure_rolls_back_and_retry_converges" }; cargo test --manifest-path engine/Cargo.toml tests::replacement_documents_add_failure_rolls_back_and_retry_converges -- --exact --nocapture; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { throw "replacement documents-add rollback test failed with exit code $testExit" }</automated>
  </verify>
  <acceptance_criteria>
    - The named tracer test fails before the implementation and passes afterward.
    - The test uses real temporary LanceDB tables and the same production mutation abstraction as `replace_document`.
    - It inspects persisted old-generation rows after failure and persisted replacement rows after retry; checking only an error string is insufficient.
    - No canonical or staging error after version capture can bypass the rollback funnel.
  </acceptance_criteria>
  <done>One actual mutation boundary is proven end to end: failure preserves the old generation and retry converges to exactly one new generation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand rollback coverage to every mutation boundary and persist node summary as Arrow null</name>
  <files>engine/src/main.rs</files>
  <read_first>
    - `engine/src/main.rs` — the tracer's `ReplacementMutationBoundary` seam, node RecordBatch construction, edge construction, and temporary-database test helpers
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-VERIFICATION.md` — all mutation boundaries plus persisted node-summary gap
  </read_first>
  <behavior>
    - A table-driven test selects each of the seven named boundaries and proves the old document/node/edge generation remains intact.
    - After every injected failure, a no-fault retry produces one canonical document, zero staging rows, unique contiguous nodes, unique edges, and one current generation timestamp.
    - Real errors from schema reads, RecordBatch creation, canonical mutation, or staging cleanup after version capture use the same rollback path.
    - The persisted `summary` column has null_count equal to the node row count; `summary_vector`, `community_ids`, and `unsummarized_refs` remain schema-derived null arrays.
    - Empty-input and repeated-initialization regression cases still pass for DATA-06/DATA-07.
  </behavior>
  <action>
Generalize the tracer fixture into a table-driven `replacement_failure_boundaries_preserve_prior_generation_and_retry_converges` test covering every named boundary. Seed a completed old generation with enough chunks to create edges, capture its raw bytes, node IDs/indexes/timestamps, and edge IDs, then inject one failure at a time. After failure, query all canonical tables and compare the captured values. Retry without injection and assert one document row, zero staged rows, one generation timestamp, indexes exactly `0..node_count-1`, unique chunk IDs, edge IDs, and edge endpoints referencing current nodes.

Replace the node `summary` array with `new_null_array` derived from `node_schema.field_with_name("summary").data_type()` per D-23/DATA-07. Add a persisted-row assertion that downcasts the stored summary column and compares its null count to row count. Keep the schema nullable and do not use an empty string or copied vector as a missing-summary sentinel.
  </action>
  <verify>
    <automated>$testList = cargo test --manifest-path engine/Cargo.toml -- --list 2&gt;&amp;1 | Out-String; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { throw "Rust test listing failed with exit code $listExit" }; if ($testList -notmatch '(?m)^tests::replacement_failure_boundaries_preserve_prior_generation_and_retry_converges: test$') { throw "required exact Rust test is absent: tests::replacement_failure_boundaries_preserve_prior_generation_and_retry_converges" }; cargo test --manifest-path engine/Cargo.toml tests::replacement_failure_boundaries_preserve_prior_generation_and_retry_converges -- --exact --nocapture; $boundaryExit = $LASTEXITCODE; if ($boundaryExit -ne 0) { throw "replacement boundary suite failed with exit code $boundaryExit" }; $testList = cargo test --manifest-path engine/Cargo.toml -- --list 2&gt;&amp;1 | Out-String; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { throw "Rust test listing failed with exit code $listExit" }; if ($testList -notmatch '(?m)^tests::persisted_node_summary_is_arrow_null: test$') { throw "required exact Rust test is absent: tests::persisted_node_summary_is_arrow_null" }; cargo test --manifest-path engine/Cargo.toml tests::persisted_node_summary_is_arrow_null -- --exact --nocapture; $summaryExit = $LASTEXITCODE; if ($summaryExit -ne 0) { throw "persisted node-summary test failed with exit code $summaryExit" }</automated>
  </verify>
  <acceptance_criteria>
    - Every documents/nodes/edges delete and add boundary plus staging deletion is enumerated and exercised.
    - Each boundary case proves both failure preservation and successful retry convergence from persisted rows.
    - Tests fail if any production mutation resumes using a direct error return outside the rollback funnel.
    - Persisted node summary values are Arrow nulls for every row.
  </acceptance_criteria>
  <done>All actual replacement boundaries are rollback-safe and retry-convergent, and nullable node summaries are null in durable rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compensate ingestion failure after request cancellation</name>
  <files>gateway/main.go, gateway/main_test.go</files>
  <read_first>
    - `gateway/main.go` — `createDocument` and `compensateFailedIngest`
    - `gateway/main_test.go` — `fakeStore`, upload compensation tests, and `engineFunc`
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-VERIFICATION.md` — WR-01 canceled-request warning
  </read_first>
  <behavior>
    - If `engine.Ingest` cancels the request context before returning an error, compensation still calls `UpdateStatus` with a live bounded context.
    - The compensation context has a deadline, is canceled after the update, and does not outlive the handler indefinitely.
    - The PostgreSQL row becomes `failed`, while ResourceExhausted still maps to 429 and other engine errors still map to 502.
    - Compensation errors remain sanitized operational logs and do not replace the original HTTP response class.
  </behavior>
  <action>
Add a handler test whose engine cancels the request context and returns an enqueue error, while the fake store rejects already-canceled contexts. Change `compensateFailedIngest` to create its own short bounded context from `context.Background()` using an explicit compensation timeout constant, defer its cancellation, and use it only for the transactional status update. Continue passing the request context to the engine call itself. Preserve D-02/D-05 metadata transactions, D-11 response mapping, and D-12 Go-only PostgreSQL access.
  </action>
  <verify>
    <automated>Push-Location gateway; try { go test -count=1 -run 'TestCreateDocument.*Cancel|TestCreateDocument.*Compensat|TestCreateDocumentMapsFullQueue' .; if ($LASTEXITCODE -ne 0) { throw "gateway cancellation compensation tests failed with exit code $LASTEXITCODE" } } finally { Pop-Location }</automated>
  </verify>
  <acceptance_criteria>
    - A canceled request cannot prevent the inserted queued row from being marked failed.
    - The detached context has an enforced finite deadline.
    - Existing 429/502 and compensation-error tests remain green.
  </acceptance_criteria>
  <done>Gateway compensation remains reliable after client cancellation without broadening PostgreSQL ownership or response semantics.</done>
</task>

</tasks>

<threat_model>
## Threat Model Configuration

ASVS Level: 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust replacement coordinator → LanceDB | A multi-table generation crosses into durable canonical state. |
| HTTP request → Go compensation transaction | A canceled untrusted request triggers a durable terminal metadata update. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07-01 | Tampering | `replace_document_with_faults` | high | mitigate | Funnel every post-snapshot error through three-table version rollback and prove every mutation boundary plus retry with persisted-row tests. |
| T-02-07-02 | Repudiation | failed replacement status | medium | mitigate | Retain deterministic boundary names and contextual errors while excluding content and credentials. |
| T-02-07-03 | Denial of Service | detached gateway compensation | medium | mitigate | Use a short explicit timeout and always cancel the detached context. |
| T-02-07-04 | Information Disclosure | rollback/test diagnostics | high | mitigate | Assert identifiers, counts, hashes, and timestamps only; do not print raw uploaded bytes or embedding inputs. |
</threat_model>

## Artifacts this phase produces

- `engine/src/main.rs`: production-used named mutation seam, single rollback funnel, seven-boundary persisted-state tests, retry-convergence proof, and Arrow-null node summary persistence.
- `gateway/main.go`: bounded detached compensation context.
- `gateway/main_test.go`: canceled-request compensation coverage.

## Warning disposition

- WR-01 is fixed in Task 3.
- WR-02 remains flagged-unresolved because D-15 explicitly discards pending queue entries during shutdown and the sources do not define whether restart should re-enqueue, fail, or tombstone acknowledged staging rows. This missing product contract is not silently replaced by planner discretion.

<verification>
- `cargo fmt --manifest-path engine/Cargo.toml -- --check`
- `cargo test --manifest-path engine/Cargo.toml`
- `Push-Location gateway; try { gofmt -w main.go main_test.go; if ($LASTEXITCODE -ne 0) { throw "gofmt failed with exit code $LASTEXITCODE" }; go test ./...; if ($LASTEXITCODE -ne 0) { throw "gateway tests failed with exit code $LASTEXITCODE" }; go vet ./...; if ($LASTEXITCODE -ne 0) { throw "go vet failed with exit code $LASTEXITCODE" } } finally { Pop-Location }`
</verification>

<success_criteria>
- Every real post-snapshot storage error restores the prior canonical generation.
- Retry after each deterministic boundary failure converges to one current generation with no staged/duplicate/stale rows.
- Node summary placeholders persist as Arrow null.
- Canceled requests cannot suppress bounded failed-ingest compensation.
</success_criteria>

<output>
Create `.planning/phases/02-ingestion-chunking-vector-storage/02-07-SUMMARY.md` after all three tasks and automated checks pass.
</output>
