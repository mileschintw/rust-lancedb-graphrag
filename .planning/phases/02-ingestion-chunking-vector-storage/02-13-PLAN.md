---
phase: 02-ingestion-chunking-vector-storage
plan: 13
type: execute
wave: 10
depends_on:
  - "02-10"
files_modified:
  - engine/src/lib.rs
  - engine/src/main.rs
  - engine/src/bin/inspect_lancedb.rs
  - engine/src/inspect_lancedb_tests.rs
autonomous: true
gap_closure: true
requirements:
  - DATA-03
  - DATA-06
  - DATA-07
  - DATA-08
  - DATA-09
must_haves:
  truths:
    - "inspect_lancedb --lancedb-path uses the explicit path without evaluating repository configuration (D-21, D-27)."
    - "A process-level inspector test launched from a config-less working directory reaches the explicit store and reports only that store's data invariant result."
    - "The inspector rejects every embedding vector containing a null Float32 child, NaN, positive infinity, or negative infinity (D-18, D-23)."
    - "The database module is compiled once through the engine library target and shared by both binaries, removing inspector-target dead-code duplication without weakening D-22 schema drift checks."
  artifacts:
    - path: "engine/src/lib.rs"
      provides: "Shared public database module for engine and inspector binaries"
    - path: "engine/src/main.rs"
      provides: "Engine binary importing the shared library database module"
    - path: "engine/src/bin/inspect_lancedb.rs"
      provides: "Lazy explicit-path selection and child-value embedding validation"
    - path: "engine/src/inspect_lancedb_tests.rs"
      provides: "Config-less process and corrupt-child real-LanceDB regressions"
  key_links:
    - from: "engine/src/bin/inspect_lancedb.rs"
      to: "engine/src/lib.rs"
      via: "engine::db::DatabaseManager import rather than path-including db/mod.rs"
      pattern: "engine::db::DatabaseManager"
    - from: "inspect_lancedb --lancedb-path"
      to: "DatabaseManager::initialize"
      via: "lazy match selects the explicit argument before settings lookup"
      pattern: "match.*path"
    - from: "FixedSizeListArray"
      to: "Float32Array child values"
      via: "child null-count and finite-value validation before integrity facts are emitted"
      pattern: "(Float32Array|is_finite)"
---

<objective>
Make the durable LanceDB inspector configuration-independent and fail closed on unusable embedding children.

Purpose: The final live gate must inspect the explicit verification store without repository config and must never attest null or non-finite vectors.
Output: A shared database library seam, lazy path selection, child validation, and real process/store regressions.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-CONTEXT.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-VERIFICATION.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-REVIEW.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-08-SUMMARY.md
@engine/src/main.rs
@engine/src/bin/inspect_lancedb.rs
@engine/src/inspect_lancedb_tests.rs
@engine/src/db/mod.rs
</context>

<phase_goal>
**As a** Lancet API user, **I want to** ingest and safely replace text or Markdown documents, **so that** the last completed LanceDB index and PostgreSQL status remain trustworthy through failures and concurrent polling.
</phase_goal>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Inspect one explicit LanceDB store from a config-less process</name>
  <files>engine/src/lib.rs, engine/src/main.rs, engine/src/bin/inspect_lancedb.rs, engine/src/inspect_lancedb_tests.rs</files>
  <read_first>
    - `AGENTS.md` — repository language-specific instruction triggers
    - `rust-guidelines.md` — mandatory Rust implementation and test conventions
    - `engine/src/bin/inspect_lancedb.rs` — private `db` path module, argument parsing, `settings_path`, and final initialization
    - `engine/src/inspect_lancedb_tests.rs` — real LanceDB fixture and inspector test inclusion pattern
    - `engine/src/main.rs` — current `mod db` and database imports
    - `engine/src/db/mod.rs` — public DatabaseManager/schema/resolver surface and test module
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-REVIEW.md` — CR-05 and WR-04 dead-code root cause
  </read_first>
  <behavior>
    - Running the built inspector from a temporary directory with no config and an explicit valid store succeeds.
    - Running it against an explicit corrupt/empty store fails for the store invariant, never for config lookup.
    - Omitting `--lancedb-path` retains the existing settings-based fallback.
    - Both binaries use the same `engine::db` module; the inspector no longer compiles `db/mod.rs` privately.
  </behavior>
  <action>
Expose the existing database module through `engine/src/lib.rs`, then switch the engine binary and inspector binary to import that shared module. Keep its schemas, resolver, and startup drift behavior unchanged per D-21, D-22, and DATA-06 through DATA-09. Remove the inspector's path-based private module inclusion that causes all-target dead-code warnings.

Select the explicit inspector path with a lazy match so `settings_path()` is called only when no argument is supplied. Add a process-level test that builds/locates the Cargo-provided inspector executable, changes to a config-less temporary working directory, passes `--lancedb-path`, and distinguishes successful valid-store inspection from data-invariant failure. Do not create or copy config into the temporary directory.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --bin inspect_lancedb tests::explicit_path_works_from_configless_working_directory -- --exact --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - The process test proves explicit path selection does not touch configuration.
    - The settings fallback remains available when the argument is omitted.
    - Engine and inspector share one database module definition.
    - Schema-drift and resolver tests remain green.
  </acceptance_criteria>
  <done>The explicit inspector path works from a config-less directory through the same validated database module used by the engine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject null and non-finite embedding children in real LanceDB fixtures</name>
  <files>engine/src/bin/inspect_lancedb.rs, engine/src/inspect_lancedb_tests.rs</files>
  <read_first>
    - `AGENTS.md` — repository language-specific instruction triggers
    - `rust-guidelines.md` — mandatory Rust implementation and test conventions
    - `engine/src/bin/inspect_lancedb.rs` — `fixed_size_list_column` and current parent-null/width checks
    - `engine/src/inspect_lancedb_tests.rs` — canonical node batches and real table fixture builders
    - `engine/src/db/mod.rs` — nullable Float32 child schema and 2048 dimension definition
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-REVIEW.md` — CR-06
  </read_first>
  <behavior>
    - A 2048-wide embedding with one null child fails closed.
    - NaN, positive infinity, and negative infinity each fail closed.
    - Finite 2048-wide embeddings continue to produce the same sanitized integrity JSON.
    - Errors name only the invalid class and never serialize vector values or document content.
  </behavior>
  <action>
After validating the non-null FixedSizeList parent and width, downcast its child values to Float32 and require zero child nulls plus finiteness for every child before accumulating durable facts. Keep validation before JSON serialization and preserve the existing no-content projection.

Extend real-LanceDB fixtures with one null-child vector and each non-finite class. Assert the inspector returns an error for each and retains the valid fixture result. Build fixtures using the canonical schema rather than bypassing LanceDB with an isolated array-only unit.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --bin inspect_lancedb embedding_children -- --nocapture; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; cargo test --manifest-path engine/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - All four invalid child classes are persisted into a real fixture and rejected.
    - Valid embeddings still inspect successfully.
    - No error or JSON output exposes embedding values or content.
    - Full Rust tests pass.
  </acceptance_criteria>
  <done>The durable inspector cannot attest a vector containing null or non-finite child values.</done>
</task>

</tasks>

<threat_model>
## Threat Model Configuration

ASVS Level: 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argument → filesystem | An explicit path selects the durable store to attest. |
| LanceDB Arrow batches → inspector | Persisted values are untrusted integrity inputs. |
| Inspector JSON → live gate | A passing verdict authorizes Phase 02 acceptance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-13-01 | Spoofing | explicit store selection | high | mitigate | Use the explicit path without unrelated config fallback and test from a config-less process. |
| T-02-13-02 | Tampering | embedding child values | high | mitigate | Reject null and non-finite Float32 children before producing integrity facts. |
| T-02-13-03 | Information Disclosure | inspector errors/JSON | high | mitigate | Keep sanitized projections and class-only validation errors. |
| T-02-13-04 | Denial of Service | schema/module refactor | medium | mitigate | Reuse the canonical database module and retain all startup drift tests. |
| T-02-13-SC | Tampering | package installs | high | mitigate | No dependency or package install is planned. |
</threat_model>

## Artifacts this phase produces

- `engine/src/lib.rs`: shared database-module library surface.
- `engine/src/main.rs`: engine binary wired to the shared module.
- `engine/src/bin/inspect_lancedb.rs`: lazy explicit path and fail-closed embedding validation.
- `engine/src/inspect_lancedb_tests.rs`: config-less process and corrupt-vector fixtures.

## Gap traceability

| Source | Finding | Task | Status |
|--------|---------|------|--------|
| VERIFICATION | Config-independent inspector explicit path | Task 1 | PLANNED |
| VERIFICATION | Reject null/non-finite embedding children | Task 2 | PLANNED |
| REVIEW | CR-05 | Task 1 | PLANNED |
| REVIEW | CR-06 | Task 2 | PLANNED |
| REVIEW | WR-04 private module dead-code root cause | Task 1 | PLANNED |

<verification>
- Exact config-less process test passes.
- Exact invalid-child fixture tests pass.
- `cargo test --manifest-path engine/Cargo.toml` passes.
</verification>

<success_criteria>
- Explicit-path diagnostics are independent of repository configuration.
- The inspector fails closed for every unusable embedding child class.
- Both binaries share the canonical database schema implementation.
</success_criteria>

<output>
Create `.planning/phases/02-ingestion-chunking-vector-storage/02-13-SUMMARY.md` when done.
</output>
