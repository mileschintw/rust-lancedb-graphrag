---
phase: 02-ingestion-chunking-vector-storage
plan: 28
type: execute
wave: 20
depends_on:
  - "02-25"
  - "02-26"
  - "02-27"
files_modified:
  - .gitignore
  - verify-live-evidence.sh
  - scripts/phase02_live_evidence.py
  - scripts/test_phase02_live_evidence.py
  - .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json
  - .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json
  - .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-ATTESTATION.json
autonomous: false
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-06
  - DATA-07
  - DATA-08
  - DATA-09
  - RAG-06
user_setup:
  - service: openrouter
    why: "The final Phase 02 acceptance requires one fresh private provider-backed cross-store ingestion after ADR-02-003 fixes"
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "OpenRouter dashboard; export only in the user's private shell and never paste it into the session"
must_haves:
  truths:
    - "All ADR-02-003 D-01 through D-07 deterministic regressions pass before a fresh challenge is issued."
    - "The full locked Rust suite, full Go suite/vet, the three named isolated PostgreSQL tests, and the full optimized isolated Python suite pass."
    - "One fresh OpenRouter run converges one document identity across HTTP, PostgreSQL, engine status, and the exact configured LanceDB store with zero staging rows and one canonical generation."
    - "A private human review confirms no credential, authorization header, raw upload, document text, chunk content, or attacker-controlled secret key appears in runtime output."
    - "Successful direct current-store validation atomically retains one ignored sanitized attestation before removing the private challenge/evidence pair; failure preserves the private pair for diagnosis."
  artifacts:
    - path: ".planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json"
      provides: "Short-lived post-fix single-use challenge"
    - path: ".planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json"
      provides: "Short-lived sanitized provider-backed cross-store evidence"
    - path: ".planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-ATTESTATION.json"
      provides: "Retained non-sensitive run identity, current-store facts, and human disclosure approval for independent reinspection"
    - path: ".planning/phases/02-ingestion-chunking-vector-storage/02-28-SUMMARY.md"
      provides: "Executor-created non-sensitive attestation digest and checkpoint judgment record consumed by the subsequent verifier"
    - path: "COVERAGE.md"
      provides: "Existing valid OpenRouter capability matrix, unchanged by this gap closure"
  key_links:
    - from: "02-25 through 02-27 summaries"
      to: "challenge issuance"
      via: "deterministic preflight"
      pattern: "02-(25|26|27)-SUMMARY"
    - from: "provider-backed ingestion"
      to: "PostgreSQL and LanceDB"
      via: "recorded UUID plus direct final reinspection"
      pattern: "compare-live-state"
    - from: "02-LIVE-ATTESTATION.json and 02-28-SUMMARY.md"
      to: "subsequent independent gsd-verifier"
      via: "run/document identity, store fingerprint, current PostgreSQL/LanceDB facts, and private-review approval without raw evidence"
      pattern: "reinspect-attestation|Live verification attestation"
  prohibitions:
    - requirement_id: DATA-01
      category: privacy
      status: unresolved
      verification: judgment
      flagged: true
      flagged_unverified: true
      statement: "MUST NOT disclose provider credentials, authorization headers, raw uploads, stored document/chunk content, or attacker-controlled secret-bearing keys in evidence, logs, summaries, staged files, or commits."
---

<objective>
**As a** Lancet API user, **I want to** see a fresh provider-backed ingestion converge across every durable store after the lifecycle fixes, **so that** Phase 02 closes on current behavioral evidence rather than historical summaries.

Run the complete post-ADR acceptance gate and retain the required private disclosure review.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-CONTEXT.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-VERIFICATION.md
@.planning/phases/02-ingestion-chunking-vector-storage/deferred-items.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-25-SUMMARY.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-26-SUMMARY.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-27-SUMMARY.md
@verify-ingestion.sh
@verify-live-evidence.sh
</context>

## Artifacts this phase produces

- Current deterministic suite evidence for every accepted fix.
- One private provider-backed cross-store run.
- One private disclosure review.
- Success-only cleanup of the private challenge/evidence artifacts after retaining a sanitized attestation.
- A summary-safe attestation digest and human approval record for subsequent independent verification.

<assumptions>
- [RESOLVED BACKSTOP — DATA-01] The full Rust/Go/Python suites preserve existing empty-input and encoding cases; this plan adds no new interpretation.
- [RESOLVED BACKSTOP — DATA-02] Both existing chunk strategies and their metadata contract remain covered by the full Rust/Go suites.
- [RESOLVED EDGE — DATA-03] ADR regressions prove initializer/replay idempotency and bounded single-worker concurrency; broader multi-process writes remain explicitly unclassified.
- [RESOLVED BACKSTOP — DATA-06/DATA-07/DATA-08] Existing schema suites preserve adjacency, null/empty placeholders, encoding, and field ordering; no schema mutation is introduced.
- [RESOLVED BACKSTOP — DATA-09] The full Rust suite preserves the async resolver and exact-match default.
- [RESOLVED EDGE — RAG-06] Worker capacity, startup ordering, failure, restart, and terminal convergence are directly exercised by Plan 02-25.
</assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: Prove deterministic closure and issue a fresh challenge</name>
  <files>.gitignore, verify-live-evidence.sh, scripts/phase02_live_evidence.py, scripts/test_phase02_live_evidence.py</files>
  <read_first>
    - `02-25-SUMMARY.md`, `02-26-SUMMARY.md`, `02-27-SUMMARY.md` — required completed fixes
    - `verify-live-evidence.sh` — prepare/validate commands and cleanup ordering
    - `verify-ingestion.sh` — managed-services provider runner and runtime path contract
    - `02-VERIFICATION.md` — named PostgreSQL, provider-backed, and human-review requirements
  </read_first>
  <action>
Extend the existing live-evidence tooling before issuing the challenge. Add `.planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-ATTESTATION.json` to `.gitignore`. In `scripts/phase02_live_evidence.py`, add strict `build-attestation`, `validate-attestation`, and `compare-attested-state` contracts. In `verify-live-evidence.sh`, add the canonical `--attestation` path, make `--validate-gate` write and validate the attestation atomically before private cleanup, and add `--reinspect-attestation` so a later verifier can query current PostgreSQL and the configured LanceDB using only the retained attestation. Add optimized Python/shell regressions proving success retains the attestation while deleting only the private pair, validation failure preserves the private pair and does not replace an earlier attestation, sensitive fields are rejected, and reinspection detects PostgreSQL, store-fingerprint, staging, count, embedding, or generation drift.

The attestation schema is exact and closed to extra keys: top level `schema_version`, `run_id`, `document_id`, `validated_at`, `source_evidence_sha256`, `store_path_sha256`, `gateway`, `postgresql`, `lancedb`, and `human_disclosure_review`. `schema_version` is `1`; IDs are UUIDv4; timestamps are UTC. `gateway` contains only `status` and `chunk_count`. `postgresql` contains only `status` and `chunk_count`. `lancedb` contains only `provider`, `embedding_model`, `document_rows`, `staged_document_rows`, `node_rows`, `edge_rows`, `embedding_width`, `generation_count`, `duplicate_generation`, `stale_generation`, and `chunk_indexes_contiguous`. `human_disclosure_review` contains only `approved: true`, `scope: "private runtime disclosure checklist"`, `approval_source: "02-28 Task 2 blocking-human checkpoint"`, and `recorded_at`. The file must contain no credential, authorization header, raw challenge, raw upload/document/chunk content, raw evidence, service log, absolute store path, or human identity. Bind it to current config with the SHA-256 of the resolved store path and to the cleaned evidence with its SHA-256.

Run the complete deterministic gate after Plans 02-25 through 02-27. Discover all six 02-25 Rust behavior regressions and both Go status/cross-runtime regressions by exact name before execution. Run the full locked Rust tests and lint, apply the existing local PostgreSQL schema, run only the three isolated claim/lease tests with `TEST_DATABASE_URL`, then the full Go suite and vet with immediate `$LASTEXITCODE` checks. Run the complete optimized isolated Python suite.

Only after every command is green, prepare a fresh canonical challenge with `verify-live-evidence.sh --prepare-gate`. Confirm challenge, evidence, and attestation paths are ignored, untracked, unstaged, and not symlinks. Remove any stale attestation during successful challenge preparation so an older run cannot satisfy the new gate. Treat the provider run as destructive until its configured PostgreSQL/LanceDB paths and sample ownership are confirmed; use the managed verification environment and script-owned sample.
  </action>
  <verify>
    <automated>$summaries=@('.planning/phases/02-ingestion-chunking-vector-storage/02-25-SUMMARY.md','.planning/phases/02-ingestion-chunking-vector-storage/02-26-SUMMARY.md','.planning/phases/02-ingestion-chunking-vector-storage/02-27-SUMMARY.md'); foreach($s in $summaries){if(!(Test-Path -LiteralPath $s -PathType Leaf)){throw "missing prerequisite summary: $s"}}; $rustRequired=@('startup_recovery_exceeds_queue_capacity_without_deadlock','startup_recovery_fails_when_worker_exits','initialize_is_idempotent_over_non_empty_staging','staging_read_error_is_unavailable','staging_delete_failure_remains_replayable','embedding_failure_restart_converges_cross_store'); $rustListed=(cargo test --manifest-path engine/Cargo.toml --locked -- --list); if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; foreach($name in $rustRequired){if(-not ($rustListed -match [regex]::Escape($name))){throw "missing 02-25 Rust regression: $name"}}; cargo fmt --manifest-path engine/Cargo.toml -- --check; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; cargo test --manifest-path engine/Cargo.toml --locked; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; cargo clippy --manifest-path engine/Cargo.toml --all-targets -- -D warnings; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; docker compose up -d db; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; Push-Location gateway; try { atlas schema apply --env local --auto-approve; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; $env:TEST_DATABASE_URL='postgres://postgres:postgres@127.0.0.1:5432/lancet?sslmode=disable'; $required=@('TestReconciliationIntentRecordAndClaim','TestReconciliationIntentClaimLeaseIsExclusive','TestReconciliationIntentClaimLeasePreservesUnrelatedDocumentAndIntent'); $listed=(go test -list '^TestReconciliationIntent(RecordAndClaim|ClaimLeaseIsExclusive|ClaimLeasePreservesUnrelatedDocumentAndIntent)$' ./db); if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; foreach($name in $required){if(-not ($listed -match [regex]::Escape($name))){throw "missing isolated PostgreSQL test: $name"}}; $gatewayRequired=@('TestGetDocumentLeavesTransientEngineFailureQueued','TestEmbeddingFailureRestartConvergesAcrossRuntime'); $gatewayListed=(go test -list '^Test(GetDocumentLeavesTransientEngineFailureQueued|EmbeddingFailureRestartConvergesAcrossRuntime)$' .); if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; foreach($name in $gatewayRequired){if(-not ($gatewayListed -match [regex]::Escape($name))){throw "missing gateway regression: $name"}}; go test -count=1 -run '^TestReconciliationIntent(RecordAndClaim|ClaimLeaseIsExclusive|ClaimLeasePreservesUnrelatedDocumentAndIntent)$' ./db; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; go test -count=1 ./...; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; go vet ./...; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE} } finally { Remove-Item Env:TEST_DATABASE_URL -ErrorAction SilentlyContinue; Pop-Location }; $env:PYTHONOPTIMIZE='1'; try { python -O -I scripts/test_phase02_live_evidence.py; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE} } finally { Remove-Item Env:PYTHONOPTIMIZE -ErrorAction SilentlyContinue }; bash ./verify-live-evidence.sh --prepare-gate --challenge .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json --evidence .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json --attestation .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-ATTESTATION.json; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}</automated>
  </verify>
  <acceptance_criteria>
    - Every deterministic suite is green before challenge issuance.
    - Named isolated PostgreSQL tests are discovered and executed against an enabled database.
    - The challenge is fresh, private, ignored, untracked, unstaged, and post-fix.
    - All six named Rust regressions and both named Go status/cross-runtime regressions are discovered before their suites execute.
    - Attestation tooling is strict, privacy-tested, ignored, and able to drive later current-store reinspection.
  </acceptance_criteria>
  <done>The final live run is authorized by current deterministic evidence.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Run one private OpenRouter ingestion and review disclosure surfaces</name>
  <files>.planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json</files>
  <read_first>
    - `verify-ingestion.sh` — exact managed-services command and script-owned input behavior
    - `02-VERIFICATION.md` — fresh cross-store and private disclosure expectations
  </read_first>
  <action>
In a private shell, export `OPENROUTER_API_KEY` without printing or pasting it into the session. Run `bash ./verify-ingestion.sh --managed-services` against the fresh canonical challenge. Observe the private gateway, engine, provider, and verifier output during the run and inspect the sanitized evidence before final cleanup.
  </action>
  <instructions>
1. Open a private shell at the repository root.
2. Export `OPENROUTER_API_KEY` from the OpenRouter dashboard without echoing it.
3. Run `bash ./verify-ingestion.sh --managed-services`.
4. Review the private runtime output and the sanitized evidence against the disclosure checklist.
  </instructions>
  <verify>
    <automated>if(!(Test-Path -LiteralPath '.planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json' -PathType Leaf)){throw 'provider-backed evidence was not produced'}; python -O -I scripts/phase02_live_evidence.py check-privacy --file .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}</automated>
    <human-check>Confirm the private output contains no credential, authorization header, raw upload, stored document text, chunk content, or attacker-controlled secret-bearing key; confirm one UUID and matching positive chunk count across HTTP, PostgreSQL, engine status, and LanceDB.</human-check>
  </verify>
  <verification>The evidence file exists, the Python privacy check exits zero, one UUID/count agrees across the four surfaces, and the private disclosure checklist passes.</verification>
  <acceptance_criteria>
    - A new provider-backed run produced sanitized evidence for the post-fix challenge.
    - The user completed the private disclosure review after the fixes.
    - No secret is copied into planning artifacts, summaries, commits, or chat.
  </acceptance_criteria>
  <done>The required fresh cross-store run and private judgment-tier review are complete.</done>
  <resume-signal>Type "provider run and private review approved" or describe the mismatch/disclosure found.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Retain a sanitized attestation, then clean private evidence on success</name>
  <files>.planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json, .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json, .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-ATTESTATION.json</files>
  <read_first>
    - `verify-live-evidence.sh` — current PostgreSQL/LanceDB comparison and success-only cleanup
    - `scripts/phase02_live_evidence.py` — evidence privacy and identity validation
  </read_first>
  <action>
After the Task 2 blocking-human approval, run the final validator against the exact challenge/evidence pair with the canonical attestation path and the explicit human-review-approved flag. Require direct current PostgreSQL and configured LanceDB reinspection for the recorded UUID, matching completed gateway/PostgreSQL status and counts, finite 2048-wide embeddings, one canonical generation, contiguous nodes, valid edges, and zero staging rows.

On success, write the exact sanitized schema from Task 1 to a mode-0600 temporary file, privacy-check and structurally validate it, then atomically replace `02-LIVE-ATTESTATION.json`. Only after that replacement succeeds may the validator remove `.02-LIVE-CHALLENGE.json` and `02-LIVE-EVIDENCE.json`. Preserve the private pair and any previously valid attestation on validation/build/write failure. Retain the new attestation after success. Prove it is ignored, untracked, unstaged, a regular file, and sufficient for a second `--reinspect-attestation` call that reads current PostgreSQL/LanceDB without the deleted private pair.

The executor-created `02-28-SUMMARY.md` must include a `Live verification attestation` section containing only attestation schema version, run ID, document ID, validated timestamp, source-evidence SHA-256, store-path SHA-256, gateway/PostgreSQL counts/status, LanceDB structural facts, and the Task 2 approval scope/source/time. Do not copy the raw challenge, evidence JSON, credentials, headers, uploads, document/chunk content, logs, absolute store path, or reviewer identity. State explicitly that Plan 02-28 completion is not Phase 02 verification: the subsequent independent gsd-verifier must read the summary plus retained attestation, execute `--reinspect-attestation`, and update `02-VERIFICATION.md` to `status: passed` before the phase may advance.
  </action>
  <verify>
    <automated>$challenge='.planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json'; $evidence='.planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json'; $attestation='.planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-ATTESTATION.json'; bash ./verify-live-evidence.sh --validate-gate --challenge $challenge --evidence $evidence --attestation $attestation --human-review-approved; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; if((Test-Path -LiteralPath $challenge) -or (Test-Path -LiteralPath $evidence)){throw 'successful validation did not remove the private challenge/evidence pair'}; if(!(Test-Path -LiteralPath $attestation -PathType Leaf)){throw 'sanitized attestation was not retained'}; python -O -I scripts/phase02_live_evidence.py validate-attestation --attestation $attestation; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; python -O -I scripts/phase02_live_evidence.py check-privacy --file $attestation; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; bash ./verify-live-evidence.sh --reinspect-attestation --attestation $attestation; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; git check-ignore --quiet -- $attestation; if($LASTEXITCODE -ne 0){throw 'attestation is not ignored'}; git ls-files --error-unmatch -- $attestation 2>$null; if($LASTEXITCODE -eq 0){throw 'attestation must remain untracked'}; git diff --quiet --cached -- $challenge $evidence $attestation; if($LASTEXITCODE -ne 0){throw 'live runtime artifact is staged'}</automated>
  </verify>
  <acceptance_criteria>
    - Current PostgreSQL and LanceDB match the evidence for one UUID.
    - Staging is empty only after trustworthy terminal completion.
    - The private challenge/evidence pair is absent after success and unstaged.
    - The sanitized attestation is retained, ignored, untracked, unstaged, privacy-clean, and current-store reinspectable.
    - Human disclosure approval is recorded as judgment evidence without copying private material.
    - Failure preserves the private pair and does not replace a previously valid attestation.
  </acceptance_criteria>
  <done>The post-ADR provider-backed gate retains privacy-safe evidence for the subsequent independent verifier; Phase 02 still requires a new `02-VERIFICATION.md` verdict of `passed`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private credential → provider runner | A secret authorizes real external embedding requests. |
| provider/gateway/engine → evidence | Private runtime output is reduced to sanitized structural facts. |
| evidence → attestation → verifier | Private artifacts authorize atomic sanitized attestation, cleanup, and later current-store reinspection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-28-01 | Information Disclosure | private provider run | critical | mitigate | Keep credential in private shell, run category-only privacy validation, and require human review of private output. |
| T-02-28-02 | Tampering | live evidence | high | mitigate | Bind evidence to a fresh challenge/run ID and reinspect both durable stores before cleanup. |
| T-02-28-03 | Repudiation | historical success reuse | high | mitigate | Require post-fix summaries and issue the challenge only after current deterministic gates pass. |
| T-02-28-04 | Denial of Service | provider-backed test | medium | mitigate | Use the managed verification environment, script-owned sample, loopback services, and existing bounded admission. |
| T-02-28-05 | Repudiation | post-cleanup verification | high | mitigate | Retain an ignored strict-schema attestation plus summary digest and require the independent verifier to reinspect current stores. |
</threat_model>

<api_coverage>
The existing `COVERAGE.md` remains valid. These plans add no OpenRouter capability; the final run re-verifies the already integrated embeddings surface.
</api_coverage>

<source_audit>

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Trustworthy ingestion/replacement through failures and concurrent polling | 02-25..02-28 | COVERED | Lifecycle, safe tests, privacy, and fresh cross-store evidence close all current gaps. |
| REQ | DATA-01 | Lightweight text-like ingestion | 02-25, 02-27, 02-28 | COVERED | Recovery and privacy preserve the upload path; empty/encoding cases use the full-suite backstop. |
| REQ | DATA-02 | Fixed-size and structure-aware chunking | 02-28 | COVERED | Existing implementation is re-run in the complete suite/provider gate; no chunk semantic change. |
| REQ | DATA-03 | LanceDB chunks/metadata persistence | 02-25, 02-26, 02-28 | COVERED | Idempotent staging, terminal ordering, isolated claims, and cross-store convergence. |
| REQ | DATA-06 | Community placeholder schema | 02-01..02-24, 02-28 | COVERED | Existing schema tests and final suite remain the adjacency/empty/ordering backstop. |
| REQ | DATA-07 | Nullable node synthesis fields | 02-01..02-24, 02-28 | COVERED | Existing schema/persistence tests remain green; no schema change. |
| REQ | DATA-08 | Separate node/edge schemas | 02-01..02-24, 02-28 | COVERED | Existing schema/persistence tests remain green; no schema change. |
| REQ | DATA-09 | Async resolver / exact-match default | 02-01..02-24, 02-28 | COVERED | Full Rust suite preserves the established contract. |
| REQ | RAG-06 | Async Rust worker | 02-25, 02-28 | COVERED | Capacity, ordering, replay, failure, and restart are explicit. |
| RESEARCH | 1.1 | Thin Go PostgreSQL/status boundary | 02-25, 02-26 | COVERED | Go production semantics stay unchanged; tests protect non-terminal/error behavior. |
| RESEARCH | 1.3 | Bounded sequential Rust worker | 02-25 | COVERED | Worker-first recovery retains the same production channel and consumer. |
| RESEARCH | 1.4-1.7 | Chunking, provider, schemas, resolver | 02-28 | COVERED | Full suites and provider run preserve implemented behavior; no new package. |
| RESEARCH | 2.1-2.3 | Unit, integration, and live validation | 02-25..02-28 | COVERED | Named regressions, isolated PostgreSQL, optimized Python, private live verification, and retained sanitized reinspection evidence. |
| CONTEXT | D-01..D-48 | Existing Phase 02 locked decisions | 02-01..02-24 | COVERED | Existing plans remain preserved; ADR-02-003 changes only the listed re-verification gaps. |
| CONTEXT | ADR-02-003 D-01 | Worker before replay plus over-capacity regression | 02-25 | COVERED | Production ordering and send failure are explicit. |
| CONTEXT | ADR-02-003 D-02 | Remove migration path; idempotent non-empty initialization | 02-25 | COVERED | One current-schema initializer and restart test. |
| CONTEXT | ADR-02-003 D-03 | Proven absence; read error is Unavailable | 02-25 | COVERED | Rust error injection plus Go non-terminal regression. |
| CONTEXT | ADR-02-003 D-04 | Delete before terminal; replayable failure and convergence | 02-25 | COVERED | Fail/delete/restart cross-store regression. |
| CONTEXT | ADR-02-003 D-05 | Isolated claimant, checklist, fatal snapshots | 02-26 | COVERED | Exact named test and review-only convention. |
| CONTEXT | ADR-02-003 D-06 | Category-only privacy diagnostics | 02-27 | COVERED | Safe structural tokens and inert secret-key subprocess test. |
| CONTEXT | ADR-02-003 D-07 | Per-process fixture ownership | 02-27 | COVERED | Exact-path cleanup and `shutil.rmtree` directories. |
| CONTEXT | DEBT-CR-04/05, DEBT-BU-01/02 | Accepted non-blocking debt | NONE | EXCLUDED | Rationale, constraints, triggers, target, and future acceptance remain unchanged. |

</source_audit>

<verification>
<automated>$challenge='.planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json'; $evidence='.planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json'; $attestation='.planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-ATTESTATION.json'; if((Test-Path -LiteralPath $challenge) -or (Test-Path -LiteralPath $evidence)){throw 'private challenge/evidence pair remains after successful cleanup'}; if(!(Test-Path -LiteralPath $attestation -PathType Leaf)){throw 'sanitized attestation was not retained'}; python -O -I scripts/phase02_live_evidence.py validate-attestation --attestation $attestation; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; python -O -I scripts/phase02_live_evidence.py check-privacy --file $attestation; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; $facts=Get-Content -LiteralPath $attestation -Raw | ConvertFrom-Json; if(($facts.schema_version -ne 1) -or (-not $facts.human_disclosure_review.approved) -or ($facts.human_disclosure_review.scope -ne 'private runtime disclosure checklist') -or ($facts.human_disclosure_review.approval_source -ne '02-28 Task 2 blocking-human checkpoint') -or ($facts.gateway.status -ne 'completed') -or ($facts.postgresql.status -ne 'completed') -or ([int]$facts.gateway.chunk_count -le 0) -or ([int]$facts.gateway.chunk_count -ne [int]$facts.postgresql.chunk_count) -or ([int]$facts.lancedb.staged_document_rows -ne 0) -or ([int]$facts.lancedb.generation_count -ne 1) -or $facts.lancedb.duplicate_generation -or $facts.lancedb.stale_generation -or (-not $facts.lancedb.chunk_indexes_contiguous)){throw 'sanitized attestation does not contain the required approved terminal facts'}; bash ./verify-live-evidence.sh --reinspect-attestation --attestation $attestation; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; git check-ignore --quiet -- $attestation; if($LASTEXITCODE -ne 0){throw 'attestation is not ignored'}; git ls-files --error-unmatch -- $attestation 2>$null; $trackedExit=$LASTEXITCODE; if($trackedExit -eq 0){throw 'attestation must remain untracked'}; if($trackedExit -ne 1){throw "git ls-files failed with exit code $trackedExit"}; git diff --quiet --cached -- $challenge $evidence $attestation; if($LASTEXITCODE -ne 0){throw 'private or attestation artifact is staged'}</automated>
<human-check>After fixes, complete the fresh provider-backed cross-store run and privately review all nondeterministic disclosure surfaces.</human-check>
</verification>

<success_criteria>
- ADR-02-003 D-01 through D-07 have deterministic regression coverage.
- Full Rust, Go/vet, named isolated PostgreSQL, and optimized Python suites pass.
- Fresh provider-backed evidence converges; private inputs are cleaned only after a sanitized attestation is atomically retained.
- Private disclosure review passes.
- A subsequent independent verifier reinspects current stores from the attestation/summary and records `status: passed` in `02-VERIFICATION.md`.
- Accepted debt stays visible and unimplemented.
</success_criteria>

<output>
Create `.planning/phases/02-ingestion-chunking-vector-storage/02-28-SUMMARY.md` with the non-sensitive `Live verification attestation` section specified in Task 3. Then run the independent gsd-verifier; Phase 02 must not advance until `.planning/phases/02-ingestion-chunking-vector-storage/02-VERIFICATION.md` is refreshed with `status: passed`.
</output>
