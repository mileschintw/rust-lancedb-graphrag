---
phase: 02-ingestion-chunking-vector-storage
plan: 24
type: execute
wave: 18
depends_on:
  - "02-22"
  - "02-23"
files_modified:
  - gateway/main.go
  - gateway/main_test.go
  - gateway/db/document_test.go
autonomous: true
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - RAG-06
must_haves:
  truths:
    - "The Go gateway rejects chunk_size above 1048576 with HTTP 400 before PostgreSQL insertion or gRPC streaming, while accepting and persisting 1048576 exactly (D-46, D-48)."
    - "Rust staging presence is the authoritative recoverability classification: staged work is returned as queued/recoverable, while NotFound is reserved for absence from both registry and durable staging and retains truthful terminal gateway handling (D-45, D-48)."
    - "The reconciliation lease integration test routes every claimant through a per-test temporary schema; unrelated document and intent rows inside that scope and all outside-schema rows remain unchanged (D-44)."
    - "The Rust, Go, and isolated Python exit commands pass; the rawContent probe fails first; no required placeholder remains; independent re-verification closes all five ADR-02-002 gaps."
  artifacts:
    - path: "gateway/main.go"
      provides: "Thin pre-persistence chunk ceiling mirror and polling behavior aligned to Rust's staging-aware status contract"
    - path: "gateway/main_test.go"
      provides: "HTTP/store/gRPC boundary and polling-status regressions"
    - path: "gateway/db/document_test.go"
      provides: "Per-test-schema reconciliation lease test with unrelated document/intent survival and isolated cleanup"
  key_links:
    - from: "createDocument"
      to: "InsertDocumentParams.ChunkSize"
      via: "bounded base-10 32-bit parse and maxChunkSize guard before the cast"
      pattern: "maxChunkSize"
    - from: "getDocument"
      to: "engine.IngestionStatus"
      via: "queued/recoverable reflects Rust-confirmed staging while NotFound reflects Rust-confirmed absence and follows the existing terminal transition"
      pattern: "codes.NotFound"
    - from: "TestReconciliationIntentClaimLeaseIsExclusive"
      to: "ClaimDueReconciliationIntents"
      via: "every claimant connection uses a unique-schema search_path before the global due-intent query"
      pattern: "search_path"
  prohibitions:
    - "MUST NOT persist a chunk size that Rust rejects or narrow an unbounded integer into PostgreSQL int32."
    - "MUST NOT treat a Rust queued/recoverable response as absent or terminal; Rust NotFound is valid only after registry and durable-staging absence are both confirmed."
    - "MUST NOT issue an unqualified DELETE FROM documents from any test."
    - "MUST NOT add Go-owned chunking, queue, recovery, RAG, or vector semantics."
---

<objective>
**As a** Lancet API user, **I want to** see truthful persisted settings and status after admission, **so that** a thin gateway cannot corrupt or contradict Rust execution.

Close the Go portions of CR-03 and WR-01, close CR-02, and execute the deterministic ADR exit gate per D-44, D-45, D-46, and D-48.

Purpose: The gateway currently narrows an unbounded integer and has a live database test whose global claim query and table-wide cleanup can touch unrelated rows; its polling behavior must align precisely with Rust's staging-aware status classification.
Output: The exact Go mirror of Rust's ceiling, conservative polling, fixture-scoped database cleanup, sentinel survival, and a full cross-runtime closure gate.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-CONTEXT.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-VERIFICATION.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-REVIEW.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-22-SUMMARY.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-23-SUMMARY.md
@.discussion/decisions/phases/02/2026-07-29-ADR-02-002-refreshed-review-disposition.md
@gateway/main.go
@gateway/main_test.go
@gateway/db/document_test.go

<interfaces>
Existing contracts the executor must preserve:

- `engine.Ingest(ctx, id, filename, strategy string, chunkSize, chunkOverlap int, src io.Reader) IngestOutcome`
- `engine.IngestionStatus(context.Context, string) (*pb.GetIngestionStatusResponse, error)`
- `documentStore.Insert`, `Get`, and conditional `UpdateStatus`
- `createDocument` persists settings before calling `engine.Ingest`
- `getDocument` polls Rust only while the PostgreSQL row is queued or processing
- `TestReconciliationIntentClaimLeaseIsExclusive` uses `TEST_DATABASE_URL`, a real `pgxpool.Pool`, unique UUIDs, and the generated sqlc query surface
</interfaces>
</context>

## Artifacts this phase produces

- A thin Go `maxChunkSize` mirror that prevents any narrowing mismatch.
- Polling regressions that preserve queued/recoverable state when Rust confirms staging and retain terminal handling only for Rust-confirmed absence.
- A live PostgreSQL reconciliation-lease regression structurally scoped to test-owned IDs.
- A deterministic five-gap exit-gate result ready for independent re-verification.

<assumptions>
- [FLAGGED ASSUMPTION — DATA-01] Empty and invalid-encoding ingestion remain unclassified without a Phase SPEC. Preserve current HTTP/upload behavior and do not claim those edges.
- [FLAGGED ASSUMPTION — DATA-02] The requirement has no edge classification beyond the accepted upper-bound decision. This plan closes only D-46's explicit size boundary.
- [FLAGGED ASSUMPTION — DATA-03] General idempotency/concurrency remains unclassified. The sentinel test proves fixture isolation, while recovery concurrency is limited to D-45's established single-worker contract.
- [FLAGGED ASSUMPTION — DATA-06/DATA-07] Adjacency, empty, ordering, idempotency, and concurrency edges remain unclassified; their existing schema implementation is unchanged.
- [FLAGGED ASSUMPTION — DATA-08/DATA-09/RAG-06] No further edge classification exists. Existing table/resolver/worker behavior remains in scope only to the degree exercised by the accepted five-gap exit gate.
</assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Keep one accepted document truthful across HTTP admission and restart-facing polling</name>
  <reversibility rating="costly">The chunk bound and polling response are shared HTTP/gRPC/PostgreSQL contracts; changing them requires coordinated Go/Rust and client-facing regression updates, but no data migration.</reversibility>
  <files>gateway/main.go, gateway/main_test.go</files>
  <read_first>
    - `go-guidelines.md` — detect the supported Go version from the module and apply project Go syntax/testing rules
    - `gateway/go.mod` — authoritative Go language version for this module; record the detected version before editing
    - `gateway/main.go` — constants, `createDocument` setting parsing/cast, `engine.Ingest`, and `getDocument` NotFound branch
    - `gateway/main_test.go` — `fakeStore`, `engineFunc`, `TestCreateDocumentChunkSettingsContract`, and `TestGetDocumentRepairsAuthoritativeEngineNotFound`
    - `02-22-SUMMARY.md` — Rust-authoritative maximum, staging-aware status, and startup replay contract
    - `02-VERIFICATION.md` — failed persisted-settings and queued-shutdown truths
    - `ADR-02-002` CR-03/WR-01 and D-45/D-46/D-48 — exact thin-interface scope
  </read_first>
  <behavior>
    - `TestCreateDocumentChunkSizeBoundaries`: 1048576 produces HTTP 202, is stored as positive int32 1048576, and is passed unchanged to `engine.Ingest`; 1048577 and 2147483648 produce HTTP 400 with zero store insert and zero engine call.
    - Existing default/custom overlap and strategy cases remain green.
    - `TestGetDocumentRecoverableStagingRemainsQueued`: an engine queued/recoverable response (the Rust registry-miss staging fallback) performs no terminal `UpdateStatus` and returns queued state.
    - `TestGetDocumentNotFoundMarksFailedAfterRustConfirmsAbsence`: engine NotFound is treated as Rust-confirmed absence from both registry and staging, performs the existing conditional failed transition, and rereads the terminal winner if another poll won first.
    - Completed/failed engine responses, document-ID mismatch handling, and conditional terminal-winner reread retain their existing behavior.
  </behavior>
  <action>
Read `gateway/go.mod` first, detect Go 1.25.0, and apply `go-guidelines.md` only up to that version for all Go edits and tests in this task.

Implement WR-01 per D-46 and keep the gateway thin per D-48. Define `maxChunkSize = 1048576` beside the existing Go chunk defaults, with a comment naming Rust `MAX_CHUNK_SIZE` as authoritative. Parse the multipart value with base 10 and 32-bit bounds, then reject values below one or above `maxChunkSize` before allocating a document ID, inserting PostgreSQL metadata, or invoking gRPC. Persist and forward the accepted integer unchanged; do not widen the schema, add configuration-driven chunk semantics, or duplicate Rust chunking decisions.

Implement only CR-03's interface contract per D-45/D-48. Rust Plan 02-22 makes `get_ingestion_status` staging-aware: a registry miss with matching recoverable staging returns queued, and NotFound is emitted only after both registry and durable staging are absent. Do not add a PostgreSQL staging lookup, recovery loop, or new Go classification. Preserve queued/recoverable responses without terminal mutation, and retain the existing conditional `NotFound → failed` transition for Rust-confirmed absence, including terminal-winner reread behavior. No protobuf/generated-code change is needed because the existing queued status and NotFound code already carry the two classifications; if implementation reveals that contract is insufficient, stop and amend the plan to include the `.proto`, generated Rust/Go files, and cross-language tests rather than inventing a gateway heuristic.

Add the three named handler regressions before production changes. Assert HTTP status, stored/forwarded values, fake-store call counts, engine call counts, queued no-mutation behavior, Rust-confirmed-absence terminal mutation, and conditional-winner reread rather than checking constants in source.
  </action>
  <verify>
    <automated>Push-Location gateway; try { $required=@('TestCreateDocumentChunkSizeBoundaries','TestGetDocumentRecoverableStagingRemainsQueued','TestGetDocumentNotFoundMarksFailedAfterRustConfirmsAbsence'); $listed=(go test -list 'Test(CreateDocumentChunkSizeBoundaries|GetDocumentRecoverableStagingRemainsQueued|GetDocumentNotFoundMarksFailedAfterRustConfirmsAbsence)$' .); if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; foreach($name in $required){if(-not ($listed -match [regex]::Escape($name))){throw "missing Go regression: $name"}}; go test -count=1 -run 'Test(CreateDocumentChunkSizeBoundaries|GetDocumentRecoverableStagingRemainsQueued|GetDocumentNotFoundMarksFailedAfterRustConfirmsAbsence)$' .; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE} } finally { Pop-Location }</automated>
  </verify>
  <acceptance_criteria>
    - The Go bound is exactly 1048576 and explicitly mirrors Rust authority.
    - Boundary acceptance reaches both PostgreSQL and gRPC unchanged.
    - Larger values fail before any durable write or engine call.
    - Rust queued/recoverable performs no terminal mutation; Rust-confirmed NotFound retains truthful failed handling and conditional-winner reread.
    - Go acquires no chunking, queue, or recovery semantics.
  </acceptance_criteria>
  <done>D-45/D-46 are closed at the thin Go boundary without contradicting Rust-owned execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scope reconciliation lease fixtures and run the five-gap exit gate</name>
  <reversibility rating="reversible">The database change is confined to test fixture ownership and can be reverted without changing production schema or behavior.</reversibility>
  <precondition>Docker Compose can start the repository PostgreSQL service and Atlas can apply the existing local schema.</precondition>
  <files>gateway/db/document_test.go</files>
  <read_first>
    - `go-guidelines.md` — detect the supported Go version from the module and apply project Go syntax/testing rules
    - `gateway/go.mod` — authoritative Go language version for this module; record the detected version before editing
    - `gateway/db/document_test.go` — `TestReconciliationIntentClaimLeaseIsExclusive`, existing unique-ID cleanup, and live PostgreSQL conventions
    - `gateway/db/query.sql` — generated parameterized document/intent operations available to the test
    - `gateway/db/schema.sql` — exact production table shapes cloned into the per-test isolated schema
    - `gateway/atlas.hcl` — local database target
    - `02-22-SUMMARY.md` and `02-23-SUMMARY.md` — prerequisites for the final Rust/Python gates
    - `ADR-02-002` CR-02, Exit Conditions, and D-44 — fixture-scoping and sentinel acceptance
  </read_first>
  <behavior>
    - `TestReconciliationIntentClaimLeaseIsExclusive`: create a unique temporary PostgreSQL schema, clone the production documents/intent table shapes into it, route every claimant connection through a pool whose `search_path` is that schema, and prove only one concurrent claimant receives the sole due test intent before lease expiry.
    - `TestReconciliationIntentClaimLeasePreservesUnrelatedDocumentAndIntent`: in the same isolated schema, snapshot an unrelated document plus a not-due reconciliation intent, run the exclusive claim, and prove every field in both rows remains unchanged.
    - Rows in `public` or another test schema are structurally unreachable by the claimant query; cleanup closes the isolated pool and drops only the unique schema, including on partial setup/failure.
    - Full Rust, Go, vet, Python, and rawContent gates pass together after both prerequisite plans.
  </behavior>
  <action>
Read `gateway/go.mod` first, detect Go 1.25.0, and apply `go-guidelines.md` only up to that version for all Go edits and tests in this task.

Implement CR-02 exactly per D-44 with structural isolation, not predicate-only cleanup. Add a test helper that opens an administrative pool on `TEST_DATABASE_URL`, creates a cryptographically unique schema name, clones the exact current `public.documents` and `public.document_reconciliation_intents` shapes into that schema, and creates a separate claimant `pgxpool.Pool` whose connection runtime parameters set `search_path` to only the quoted isolated schema. Route `New(pool)` and every concurrent claimant connection through that isolated pool before any production `ClaimDueReconciliationIntents` query runs. Register cleanup immediately: close the claimant pool, then use the administrative pool to `DROP SCHEMA <isolated> CASCADE`; never change `search_path` on a shared connection or rely on a transaction whose connection cannot be shared by the two claimers.

Remove the table-wide setup cleanup from `TestReconciliationIntentClaimLeaseIsExclusive`. Inside the isolated schema, insert one due test document/intent and one unrelated sentinel document/intent whose `next_attempt_at` is in the future. Snapshot all document and intent fields, run two concurrent `LIMIT 1` claimants, assert exactly one receives the due intent, then reread and byte/value-compare every unrelated field. Also take read-only public-table counts before and after to prove the isolated claim and schema drop cannot mutate outside rows. The test must fail if any claimant pool connection resolves `documents` or `document_reconciliation_intents` outside its per-test schema.

Keep the generated parameterized query layer and live `TEST_DATABASE_URL` gate. Do not weaken the concurrency assertion, serialize the whole package to hide interference, filter the production global-due query by fixture ID, or edit production SQL/schema for a test-only problem.
  </action>
  <verify>
    <automated>docker compose up -d db; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; Push-Location gateway; try { atlas schema apply --env local --auto-approve; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; $env:TEST_DATABASE_URL='postgres://postgres:postgres@127.0.0.1:5432/lancet?sslmode=disable'; $requiredTests=@('TestReconciliationIntentClaimLeaseIsExclusive','TestReconciliationIntentClaimLeasePreservesUnrelatedDocumentAndIntent'); $listed=(go test -list 'TestReconciliationIntentClaimLease(IsExclusive|PreservesUnrelatedDocumentAndIntent)$' ./db); if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; foreach($name in $requiredTests){if(-not ($listed -match [regex]::Escape($name))){throw "missing isolated database regression: $name"}}; go test -count=1 -run 'TestReconciliationIntentClaimLease(IsExclusive|PreservesUnrelatedDocumentAndIntent)$' ./db; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; $unsafe=(rg -n 'DELETE FROM documents\"' . -g '*_test.go'); if($LASTEXITCODE -eq 0){throw "unqualified destructive test SQL remains: $unsafe"}; if($LASTEXITCODE -ne 1){exit $LASTEXITCODE}; go test -count=1 ./...; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; go vet ./...; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE} } finally { Remove-Item Env:TEST_DATABASE_URL -ErrorAction SilentlyContinue; Pop-Location }; cargo test --manifest-path engine/Cargo.toml; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; python -O -I scripts/test_phase02_live_evidence.py; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; $probe=('{"rawContent":"do-not-publish"}' | python -O -I scripts/phase02_live_evidence.py check-privacy - 2>&1); if($LASTEXITCODE -eq 0){throw 'camel-case privacy probe unexpectedly passed'}; if(($probe -join "`n") -match 'do-not-publish'){throw 'privacy probe disclosed its submitted value'}; $required=@('engine/src/main.rs','engine/src/db/mod.rs','engine/src/tests.rs','gateway/main.go','gateway/main_test.go','gateway/db/document_test.go','scripts/phase02_live_evidence.py','scripts/test_phase02_live_evidence.py'); $markers=Select-String -LiteralPath $required -SimpleMatch '[TODO]'; if($markers){throw "required-path placeholder remains: $($markers.Path -join ', ')"}</automated>
  </verify>
  <acceptance_criteria>
    - No Go test contains an unqualified destructive documents-table statement.
    - Every claimant connection is confined to a unique temporary schema before the global due-intent query runs.
    - An unrelated document and not-due intent survive the real exclusive-claim integration scenario unchanged.
    - Public/outside-schema row counts remain unchanged and the isolated schema is dropped after the test.
    - `cargo test --manifest-path engine/Cargo.toml` passes.
    - From `gateway`, `go test -count=1 ./...` and `go vet ./...` pass with the integration database enabled.
    - `python -O -I scripts/test_phase02_live_evidence.py` passes without changing real runtime artifacts.
    - The rawContent probe exits nonzero and omits its value.
    - No required path contains a placeholder marker.
  </acceptance_criteria>
  <done>CR-02 and the combined ADR exit gate are green; independent Phase 02 re-verification can close all five recorded gaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| multipart caller → Go gateway | Caller-controlled numeric metadata reaches PostgreSQL and the Rust stream. |
| Rust status RPC → PostgreSQL polling row | A transient engine response can trigger a durable terminal mutation. |
| test process → configured PostgreSQL | Integration fixtures can affect unrelated persistent rows if not structurally scoped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-24-01 | Tampering | persisted chunk settings | high | mitigate | Parse with 32-bit bounds, enforce the Rust-owned maximum before insert/cast, and assert stored/forwarded equality. |
| T-02-24-02 | Tampering | polling terminal state | high | mitigate | Rust returns queued for registry-miss staging and reserves NotFound for confirmed absence; gateway tests preserve queued and retain conditional terminal handling only for confirmed absence. |
| T-02-24-03 | Denial of Service | stale queued polling | medium | mitigate | Keep recovery classification and replay in Rust; gateway reflects queued/terminal responses without adding a polling loop or heuristic. |
| T-02-24-04 | Tampering | database integration fixtures | critical | mitigate | Route every claimant through a unique-schema search path, snapshot unrelated document/intent rows, drop only that schema, and keep a structural gate against the dangerous statement. |
| T-02-24-05 | Information Disclosure | combined exit output | medium | mitigate | Raw-content probe requires nonzero exit and explicit omission of the submitted value; existing class-only diagnostics remain mandatory. |
</threat_model>

<api_coverage>
The gap closure adds no public endpoint, external provider operation, or new OpenRouter capability. It tightens validation and failure behavior on the existing `POST /documents` and `GET /documents/{id}` routes. The existing OpenRouter coverage matrix remains accurate, so `COVERAGE.md` is not modified.
</api_coverage>

<source_audit>

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Trustworthy ingestion/replacement through failures and concurrent polling | 02-22, 02-23, 02-24 | COVERED | Engine durability, truthful gateway state, privacy, and safe evidence close the five current blockers. |
| REQ | DATA-01 | Lightweight text-like ingestion | 02-22, 02-23, 02-24 | COVERED | Accepted work recovery, verification privacy, and gateway polling preserve the existing upload flow. Empty/encoding edges remain flagged, not falsely resolved. |
| REQ | DATA-02 | Fixed-size and structure-aware chunking | 02-22, 02-24 | COVERED | D-46 adds the exact accepted upper boundary without changing strategies. Other unclassified edges remain flagged. |
| REQ | DATA-03 | LanceDB persistence | 02-22, 02-23 | COVERED | Complete staging replay and non-destructive evidence protect durable state. General concurrency remains unclassified. |
| REQ | DATA-06 | Community placeholder schema | 02-03..02-21 | COVERED | Existing executed plans and verification satisfy it; the new gaps do not change its schema. |
| REQ | DATA-07 | Nullable node synthesis fields | 02-03..02-21 | COVERED | Existing executed plans and verification satisfy it; unresolved edge classifications remain flagged. |
| REQ | DATA-08 | Separate node/edge schemas | 02-03..02-21 | COVERED | Existing executed plans and verification satisfy it; no refreshed finding changes this surface. |
| REQ | DATA-09 | Rust EntityResolver / ExactMatchResolver | 02-03..02-21 | COVERED | Existing executed plans and verification satisfy it; D-48 preserves Rust ownership. |
| REQ | RAG-06 | Async Rust worker | 02-22, 02-24 | COVERED | D-45 closes acknowledged-work drain/replay while preserving the single-worker structure. |
| RESEARCH | 1.1 | Go PostgreSQL and polling boundary | 02-24 | COVERED | Reuses transactional store and HTTP handler patterns without new dependency. |
| RESEARCH | 1.3 | Rust bounded sequential worker | 02-22 | COVERED | Extends the established worker with accepted-work drain/replay. |
| RESEARCH | 1.4 | Rust chunk setting execution | 02-22, 02-24 | COVERED | Adds the ADR-locked ceiling at both authoritative and mirror boundaries. |
| RESEARCH | 1.5-1.7 | OpenRouter, Lance schemas, resolver hooks | 02-03..02-21 | COVERED | Existing phase implementation remains; no new provider/package work is introduced. |
| RESEARCH | 2.1-2.3 | Unit, integration, and live verification | 02-22, 02-23, 02-24 | COVERED | Adds deterministic Rust, real PostgreSQL, isolated Python, and final combined gates. |
| RESEARCH | 3-4 | Existing pgx/sqlc/tracing/dependency patterns | 02-22, 02-24 | COVERED | Reuses current patterns; no package install and no legitimacy audit required. |
| CONTEXT | D-01..D-14, D-16..D-30 | Existing Phase 02 ingestion/chunk/storage/config contracts | 02-01..02-21 | COVERED | Executed history remains authoritative and unchanged. |
| CONTEXT | D-15 | Pending-job discard | NONE | SUPERSEDED | D-45 replaces this behavior; it is not implemented. |
| CONTEXT | D-31..D-37, D-41 | Prior accepted closure and runtime boundary | 02-17..02-24 | COVERED | Existing closures remain; D-48 reinforces the responsibility split. |
| CONTEXT | D-38..D-40, D-42 | Accepted deferred debt and triggers | NONE | EXCLUDED | Explicitly deferred while triggers remain false; no new task implements it. |
| CONTEXT | D-43 | Camel-case privacy canonicalization | 02-23 | COVERED | Production CLI fail-first tracer plus all six aliases. |
| CONTEXT | D-44 | Fixture-scoped database cleanup | 02-24 | COVERED | Every claimant connection uses a per-test schema; unrelated document/intent rows and public counts remain unchanged. |
| CONTEXT | D-45 | Shutdown drain, durable startup recovery, polling guard | 02-22, 02-24 | COVERED | Rust owns staging/replay/status and returns queued for durable staging; Go retains terminal handling only for Rust-confirmed absence. |
| CONTEXT | D-46 | Rust-authoritative 1048576 plus thin Go mirror | 02-22, 02-24 | COVERED | Boundary, above-boundary, historical overflow, no-write, and gRPC class cases. |
| CONTEXT | D-47 | Parameterized live-evidence runtime paths | 02-23 | COVERED | Temporary fixture paths and byte-identical real sentinels. |
| CONTEXT | D-48 | Rust owns chunk/RAG/vector/queue/recovery semantics | 02-22, 02-24 | COVERED | Gateway changes are validation/reflection only. |

</source_audit>

<verification>
- `cargo test --manifest-path engine/Cargo.toml`
- From `gateway`: `go test -count=1 ./...` with `TEST_DATABASE_URL`, followed immediately by `$LASTEXITCODE` checking, then `go vet ./...`
- `python -O -I scripts/test_phase02_live_evidence.py`
- The rawContent fail-first probe
- Required-path placeholder scan
- Independent `02-VERIFICATION.md` re-verification reports `passed` and closes the five matching gap entries.
</verification>

<success_criteria>
- CR-01, CR-02, CR-03, WR-01, and WR-02 meet every ADR acceptance criterion.
- All five prior `gaps_remaining` entries are absent after independent re-verification.
- The phase requirement set remains 8/8 represented without claiming unresolved SPEC edges.
- No deferred idea, new external API capability, or Go-owned ingestion semantics enters scope.
</success_criteria>

<output>
Create `.planning/phases/02-ingestion-chunking-vector-storage/02-24-SUMMARY.md` when done.
</output>
