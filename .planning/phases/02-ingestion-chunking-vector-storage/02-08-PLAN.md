---
phase: 02-ingestion-chunking-vector-storage
plan: 08
type: execute
wave: 7
depends_on:
  - "02-06"
files_modified:
  - engine/src/client/mod.rs
  - engine/src/client/tests.rs
  - engine/src/bin/inspect_lancedb.rs
autonomous: true
gap_closure: true
requirements:
  - DATA-03
  - DATA-08
must_haves:
  truths:
    - "The production OpenRouter HTTP client enforces the locked 10-second per-call timeout and retains the three-retry exponential-backoff policy and concurrency cap five (D-17, D-18, D-19, D-20)."
    - "A behavior test reaches the same HTTP-client builder used by OpenRouterClient::new and observes a slow request time out at the production limit."
    - "The inspector reads persisted embedding_model, ingested_at, chunk_id, chunk_index, edge_id, source_node_id, and target_node_id values for the requested UUID instead of emitting fixed verdicts (D-21, D-23, D-24)."
    - "Provider, model, generation count, duplicate/stale generation, chunk-index continuity, and edge referential-integrity facts are derived from durable rows and fail closed when missing, mixed, duplicated, non-contiguous, or stale."
  artifacts:
    - path: "engine/src/client/mod.rs"
      provides: "Locked production timeout constant and shared HTTP client builder"
      contains:
        - "REQUEST_TIMEOUT"
        - "Duration::from_secs(10)"
    - path: "engine/src/client/tests.rs"
      provides: "Behavioral production-timeout test"
    - path: "engine/src/bin/inspect_lancedb.rs"
      provides: "Durable-row-derived provider/model/generation/uniqueness/continuity inspection"
      contains:
        - "duplicate_generation"
        - "chunk_indexes_contiguous"
        - "generation_count"
  key_links:
    - from: "engine/src/client/mod.rs"
      to: "reqwest::Client"
      via: "OpenRouterClient::new and the timeout behavior test use the same production builder and REQUEST_TIMEOUT"
      pattern: "REQUEST_TIMEOUT"
    - from: "engine/src/bin/inspect_lancedb.rs"
      to: "LanceDB nodes/edges rows"
      via: "direct filtered batch queries and Arrow downcasts derive all emitted facts"
      pattern: "(embedding_model|ingested_at|chunk_index|edge_id)"
---

<objective>
Correct the locked OpenRouter timeout and replace inspector attestations with durable-row-derived facts.

Purpose: The provider client and live evidence gate must report behavior and state that were actually enforced and inspected.
Output: A production 10-second timeout with behavioral coverage and a fail-closed direct LanceDB inspector whose provider/model/stale/duplicate/continuity verdicts come from persisted rows.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-CONTEXT.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-VERIFICATION.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-06-SUMMARY.md
@engine/src/client/mod.rs
@engine/src/client/tests.rs
@engine/src/bin/inspect_lancedb.rs
</context>

<phase_goal>
**As a** Lancet API user, **I want to** ingest and safely replace text or Markdown documents, **so that** the last completed LanceDB index and PostgreSQL status remain trustworthy through failures and concurrent polling.
</phase_goal>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Enforce and behaviorally prove the locked OpenRouter timeout</name>
  <files>engine/src/client/mod.rs, engine/src/client/tests.rs</files>
  <read_first>
    - `engine/src/client/mod.rs` — `OpenRouterClient::new`, `for_test`, retry loop, and concurrency constants
    - `engine/src/client/tests.rs` — local TCP mock server and timeout/retry tests
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-CONTEXT.md` — D-19 exact 10-second timeout and three-retry policy
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-VERIFICATION.md` — 30-second production timeout failure
  </read_first>
  <behavior>
    - `OpenRouterClient::new` builds reqwest with exactly `Duration::from_secs(10)`.
    - A slow local endpoint exercised through the same production builder returns a timeout error near the locked deadline.
    - The test uses zero retries or short backoff only through a test seam so it completes under 20 seconds; production keeps three retries and 1/2/4-second backoff.
    - Existing rate-limit, dimension, and maximum-five-concurrency tests remain green.
  </behavior>
  <action>
Write `production_client_times_out_at_locked_ten_seconds` first. Introduce `REQUEST_TIMEOUT: Duration = Duration::from_secs(10)` and one internal HTTP-client builder used by `OpenRouterClient::new` and the test endpoint constructor. The test constructor may override endpoint and retry count, but it must not override the HTTP timeout. Point it at the existing local TCP mock server with a response delay beyond the locked deadline, record elapsed monotonic time, and assert a timeout-class error occurs within a narrow tolerance around ten seconds. Keep D-19's three retries with exponential backoff in production and D-20's concurrency limit five.
  </action>
  <verify>
    <automated>$testList = cargo test --manifest-path engine/Cargo.toml -- --list 2&gt;&amp;1 | Out-String; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { throw "Rust test listing failed with exit code $listExit" }; if ($testList -notmatch '(?m)^client::tests::production_client_times_out_at_locked_ten_seconds: test$') { throw "required exact Rust test is absent: client::tests::production_client_times_out_at_locked_ten_seconds" }; cargo test --manifest-path engine/Cargo.toml client::tests::production_client_times_out_at_locked_ten_seconds -- --exact --nocapture; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { throw "production timeout test failed with exit code $testExit" }</automated>
  </verify>
  <acceptance_criteria>
    - The test fails against the prior 30-second production configuration.
    - The test reaches the builder used by `OpenRouterClient::new`; a separate arbitrary 10-millisecond test client is insufficient.
    - Focused execution completes in under 20 seconds and proves timeout behavior, not only source text.
  </acceptance_criteria>
  <done>The production OpenRouter client enforces and behaviorally demonstrates D-19's exact 10-second request timeout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive inspector identity and generation facts from durable rows</name>
  <files>engine/src/bin/inspect_lancedb.rs</files>
  <read_first>
    - `engine/src/bin/inspect_lancedb.rs` — current schema-only checks and fixed provider/model/stale values
    - `engine/src/db/mod.rs` — canonical nodes/edges schemas and table accessors
    - `engine/src/main.rs` — persisted `embedding_model`, `ingested_at`, chunk ID/index, edge ID/source/target construction
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-VERIFICATION.md` — durable derivation gap
  </read_first>
  <behavior>
    - The inspector filters by one UUID and reads node and edge batches directly without selecting raw document bytes or chunk content.
    - One non-null persisted model value equal to D-18 maps to provider `openrouter`; missing, null, mixed, or unknown model values fail nonzero.
    - Exactly one non-null `ingested_at` generation is required; multiple generations set stale/duplicate facts and fail nonzero.
    - Chunk IDs and indexes are unique; indexes equal the exact range `0..node_rows-1`; duplicate or non-contiguous values fail nonzero.
    - Edge IDs are unique and every source/target ID belongs to the current node ID set; duplicate or stale endpoint values fail nonzero.
    - Successful JSON emits only sanitized identity/count facts, including `generation_count`, `duplicate_generation`, `stale_generation`, and `chunk_indexes_contiguous`, all computed from rows.
  </behavior>
  <action>
Replace schema-only identity checks with filtered `query().only_if(...)` batch reads. Downcast the persisted node columns `embedding`, `embedding_model`, `ingested_at`, `chunk_id`, and `chunk_index`; downcast edge columns `edge_id`, `source_node_id`, and `target_node_id`. Derive the model set, generation timestamp set, node/index uniqueness, exact index continuity, edge uniqueness, and edge endpoint membership. Derive provider only from the persisted locked model mapping, never from a constant emitted without row evidence.

Factor pure aggregation/validation enough for focused tests, but include temporary real-LanceDB tests for a valid generation, mixed/missing models, two generation timestamps, duplicate chunk ID/index, non-contiguous index, duplicate edge ID, and stale edge endpoint. Fail nonzero before printing success JSON when any required fact cannot be derived. Do not select or serialize `raw_content`, node `content`, request headers, credentials, or embeddings themselves.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --bin inspect_lancedb -- --nocapture; if ($LASTEXITCODE -ne 0) { throw "inspector durable-row tests failed with exit code $LASTEXITCODE" }; cargo check --manifest-path engine/Cargo.toml --bin inspect_lancedb; if ($LASTEXITCODE -ne 0) { throw "inspector build failed with exit code $LASTEXITCODE" }</automated>
  </verify>
  <acceptance_criteria>
    - No successful inspector field for provider, model, generation, duplicate, stale, or continuity is a fixed attestation.
    - Invalid durable fixtures fail closed and valid durable fixtures emit the derived values.
    - JSON contains counts/identity/timestamps-as-count only and no sensitive content.
    - Existing exact document/staging count and 2048 persisted-width checks remain enforced.
  </acceptance_criteria>
  <done>The inspector's identity, uniqueness, continuity, and generation verdicts are direct durable-state facts suitable for the live gate.</done>
</task>

</tasks>

<threat_model>
## Threat Model Configuration

ASVS Level: 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust engine → OpenRouter | Text and a private API key cross to an external provider under a bounded request. |
| LanceDB → inspector JSON | Durable rows cross into a sanitized verification artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08-01 | Denial of Service | OpenRouter client | medium | mitigate | Enforce D-19's ten-second per-call timeout and bounded retries/concurrency. |
| T-02-08-02 | Spoofing | inspector provider/model verdict | high | mitigate | Derive the provider from one persisted locked model and reject missing/mixed/unknown values. |
| T-02-08-03 | Tampering | inspector generation verdict | high | mitigate | Derive generation, uniqueness, continuity, and edge membership from durable rows and fail closed. |
| T-02-08-04 | Information Disclosure | inspector output | high | mitigate | Query only sanitized columns and never serialize raw bytes, chunk content, embeddings, headers, or credentials. |
</threat_model>

## Artifacts this phase produces

- `engine/src/client/mod.rs`: shared locked request-timeout constant and production builder.
- `engine/src/client/tests.rs`: behavioral ten-second timeout test.
- `engine/src/bin/inspect_lancedb.rs`: durable-row-derived provider/model/generation/duplicate/stale/continuity inspector and adversarial fixtures.

<verification>
- `cargo fmt --manifest-path engine/Cargo.toml -- --check`
- `cargo test --manifest-path engine/Cargo.toml client::tests -- --nocapture`
- `cargo test --manifest-path engine/Cargo.toml --bin inspect_lancedb -- --nocapture`
- `cargo check --manifest-path engine/Cargo.toml --bin inspect_lancedb`
</verification>

<success_criteria>
- Production uses a behaviorally tested 10-second OpenRouter request timeout with the locked retry/concurrency policy.
- Inspector provider/model/stale/duplicate/continuity facts are derived from persisted rows and fail closed.
- No inspector output contains credentials or raw/stored content.
</success_criteria>

<output>
Create `.planning/phases/02-ingestion-chunking-vector-storage/02-08-SUMMARY.md` after both tasks and automated checks pass.
</output>
