---
phase: 02-ingestion-chunking-vector-storage
plan: 23
type: execute
wave: 17
depends_on:
  - "02-21"
files_modified:
  - scripts/phase02_live_evidence.py
  - scripts/test_phase02_live_evidence.py
autonomous: true
gap_closure: true
requirements:
  - DATA-01
  - DATA-03
must_haves:
  truths:
    - "Camel-case sensitive-field aliases map to the existing privacy classes and the rawContent fail-first probe exits nonzero without disclosing its value (D-43)."
    - "The live-evidence test harness injects temporary challenge/evidence paths and cannot overwrite or delete the real runtime artifacts (D-47)."
    - "Any pre-existing real runtime artifact remains byte-identical after the complete optimized isolated Python suite, proven only by read-only before/after snapshots (D-47)."
  artifacts:
    - path: "scripts/phase02_live_evidence.py"
      provides: "Canonical camel-case and separator-aware sensitive-field classification"
    - path: "scripts/test_phase02_live_evidence.py"
      provides: "Fail-first alias cases and temporary runtime-path isolation regressions"
  key_links:
    - from: "check-privacy"
      to: "classify_sensitive_field"
      via: "recursive inspect_privacy_prohibition traversal"
      pattern: "inspect_privacy_prohibition"
    - from: "test_captured_inspector_arguments_explicit_path"
      to: "verify-live-evidence.sh"
      via: "explicit temporary --challenge and --evidence arguments"
      pattern: "--challenge.*--evidence"
  prohibitions:
    - "MUST NOT accept camel-case aliases for credential, authorization-header, raw-upload, stored-document-text, or stored-chunk-content fields."
    - "MUST NOT print a prohibited field value while reporting a privacy violation."
    - "MUST NOT write, replace, truncate, or delete the real Phase 02 challenge/evidence paths from a test fixture."
---

<objective>
**As a** Lancet API user, **I want to** trust the retained verification result, **so that** sensitive content is rejected and concurrent live evidence is not destroyed by its own test suite.

Close CR-01 and WR-02 per D-43 and D-47.

Purpose: The current classifier misses camel-case boundaries, and the explicit-inspector-argument regression uses and unlinks the real challenge/evidence paths.
Output: One fail-closed canonicalization rule, independent alias regressions, injected temporary runtime paths, and read-only preservation proof for real artifacts.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-CONTEXT.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-VERIFICATION.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-REVIEW.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-21-SUMMARY.md
@.discussion/decisions/phases/02/2026-07-29-ADR-02-002-refreshed-review-disposition.md
@scripts/phase02_live_evidence.py
@scripts/test_phase02_live_evidence.py

<interfaces>
Existing contracts the executor must preserve:

- `classify_sensitive_field(name: str) -> str | None`
- `inspect_privacy_prohibition(value: Any, path: str = "root") -> None`
- `check-privacy` reads a JSON subject and exits nonzero through `ValidationError` on a prohibited class
- `test_captured_inspector_arguments_explicit_path` invokes the real shell entrypoint with a fake `cargo` executable and captured argv
- `verify-live-evidence.sh --validate-gate --challenge PATH --evidence PATH` already accepts explicit artifact paths
</interfaces>
</context>

## Artifacts this phase produces

- Camel-case-aware canonical privacy field classification.
- One fail-first regression for each ADR-listed camel-case alias.
- A temporary-path live-evidence subprocess harness with read-only byte-identity proof for any real runtime artifacts.

<assumptions>
- [FLAGGED ASSUMPTION — DATA-01] The phase has no SPEC classification for empty or invalid-encoding uploads. Privacy classification operates on parsed verification JSON only and does not resolve upload encoding behavior.
- [FLAGGED ASSUMPTION — DATA-03] General persistence idempotency/concurrency remains unclassified. This plan proves only that concurrent verification tests cannot mutate the two real runtime artifacts.
</assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject one rawContent subject through the production privacy CLI</name>
  <files>scripts/phase02_live_evidence.py, scripts/test_phase02_live_evidence.py</files>
  <read_first>
    - `scripts/phase02_live_evidence.py` — `SENSITIVE_FIELD_CATEGORIES`, `classify_sensitive_field`, `inspect_privacy_prohibition`, and `check-privacy`
    - `scripts/test_phase02_live_evidence.py` — existing privacy subprocess/control cases and value-omission assertions
    - `02-VERIFICATION.md` — failed privacy truth and prohibition result
    - `ADR-02-002` CR-01 and D-43 — exact aliases, canonicalization order, and fail-first acceptance
  </read_first>
  <behavior>
    - `classify_sensitive_field("rawContent")` returns `raw_content`.
    - `rawContent`, `storedDocumentText`, `authorizationHeader`, `bearerToken`, `chunkContent`, and `credentialValue` each fail independently under `python -O -I`.
    - The rawContent CLI subject exits nonzero, identifies only the prohibited class/location, and omits the submitted value from stdout/stderr.
    - Existing underscore, hyphen, mixed-case, nested-object, nested-list, and clean controls retain their prior results.
  </behavior>
  <action>
Implement CR-01 exactly per D-43. In `classify_sensitive_field`, split a lowercase-or-digit to uppercase boundary before lowercasing and applying the existing separator normalization; compare the separator-free canonical form against the existing prohibited categories. Keep one classifier and one recursive traversal—do not enumerate alternate classifier paths or reject all uppercase-containing keys.

Add independent fail-first tests for all six locked aliases, with the rawContent case also invoking the production `check-privacy` CLI through an optimized isolated Python subprocess. Assert return code, normalized class/location-only diagnostics, and omission of the submitted value. Retain the clean nested control and all previously covered naming conventions.
  </action>
  <verify>
    <automated>$env:PYTHONOPTIMIZE='1'; try { python -O -I scripts/test_phase02_live_evidence.py -k privacy; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; $probe=('{"rawContent":"do-not-publish"}' | python -O -I scripts/phase02_live_evidence.py check-privacy - 2>&1); if($LASTEXITCODE -eq 0){throw 'camel-case privacy probe unexpectedly passed'}; if(($probe -join "`n") -match 'do-not-publish'){throw 'privacy probe disclosed its submitted value'} } finally { Remove-Item Env:PYTHONOPTIMIZE -ErrorAction SilentlyContinue }</automated>
  </verify>
  <acceptance_criteria>
    - Every D-43 alias maps to its existing prohibited class.
    - The production CLI fails first for rawContent under optimized isolated Python.
    - Diagnostics omit the submitted value.
    - Existing clean and separator-based subjects remain correctly classified.
  </acceptance_criteria>
  <done>CR-01 is closed through the real privacy command, not a source-text assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inject temporary runtime paths and observe real artifacts read-only</name>
  <reversibility rating="reversible">The change is isolated to test fixture plumbing and can be reverted without changing runtime formats or public contracts.</reversibility>
  <files>scripts/test_phase02_live_evidence.py</files>
  <read_first>
    - `scripts/test_phase02_live_evidence.py` — runtime path constants, `to_bash_path`, `test_exact_runtime_paths_are_ignored`, and `test_captured_inspector_arguments_explicit_path`
    - `verify-live-evidence.sh` — existing explicit `--challenge` and `--evidence` arguments consumed by the subprocess harness
    - `ADR-02-002` WR-02 and D-47 — temporary-path isolation and byte-identical real-artifact acceptance
  </read_first>
  <behavior>
    - `test_captured_inspector_arguments_explicit_path` creates challenge/evidence only under its temporary fixture root and passes those exact absolute paths to the shell command.
    - All test-created sentinel bytes live under injected temporary challenge/evidence paths; no test setup, assertion, or cleanup writes either canonical real runtime path.
    - A read-only external before/after snapshot records existence, length, and content hash for each real runtime artifact and proves it is unchanged after the argument-capture test and complete Python suite.
    - No cleanup callback unlinks a real runtime path; only fixture-owned paths are removed.
    - The captured `inspect_lancedb` document ID and explicit LanceDB path assertions remain green.
  </behavior>
  <action>
Implement WR-02 exactly per D-47. Separate the two real-path constants used by the ignore contract from injectable test artifact paths. Refactor the argument-capture fixture to create its challenge and evidence inside a `TemporaryDirectory`, pass those explicit paths to `verify-live-evidence.sh`, and clean only that fixture root.

Add distinct test-created sentinel bytes only to the injected temporary challenge/evidence files and prove those exact fixture paths are the only artifact paths passed to the subprocess. Add a write-denial guard around Python fixture setup/cleanup that fails if any resolved mutation target equals a canonical real path. Keep the canonical paths as read-only contract inputs for `test_exact_runtime_paths_are_ignored`; do not create, replace, rename, truncate, delete, save, or restore either real artifact from the test process.

Keep the automated gate outside the Python suite responsible for real-path proof: snapshot each existing artifact's existence and bytes before the suite, run the full suite, then take a second read-only snapshot and require equality. Because the suite never writes a real path, this gate is safe beside a human run; if another process legitimately changes an artifact during the window, report concurrent external mutation rather than attempting restoration. Retain actual spawned-argv assertions for the exact document and LanceDB paths.
  </action>
  <verify>
    <automated>$real=@('.planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json','.planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json'); $before=@{}; foreach($path in $real){$before[$path]=if(Test-Path -LiteralPath $path){[Convert]::ToBase64String([IO.File]::ReadAllBytes((Resolve-Path -LiteralPath $path)))}else{$null}}; python -O -I scripts/test_phase02_live_evidence.py; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; foreach($path in $real){$after=if(Test-Path -LiteralPath $path){[Convert]::ToBase64String([IO.File]::ReadAllBytes((Resolve-Path -LiteralPath $path)))}else{$null}; if($after -ne $before[$path]){throw "runtime artifact changed: $path"}}</automated>
  </verify>
  <acceptance_criteria>
    - The shell harness receives temporary challenge/evidence paths.
    - The real runtime paths are never fixture output or fixture cleanup targets.
    - Existing real artifacts are byte-identical after the full suite according to read-only before/after snapshots.
    - Inspector argv capture still proves exact document and LanceDB paths.
  </acceptance_criteria>
  <done>WR-02 is closed and the full Python suite is safe to run beside a live verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| challenge/evidence JSON keys → privacy classifier | Attacker- or tool-controlled field naming can bypass disclosure controls. |
| test subprocess → live runtime artifact paths | Automated tests can interfere with a human verification using the same checkout. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-23-01 | Information Disclosure | privacy classifier | high | mitigate | Split camel-case boundaries before canonical matching and execute independent fail-first alias cases with value-omission assertions. |
| T-02-23-02 | Tampering | live challenge/evidence | high | mitigate | Inject temporary paths into the real shell command, reject fixture mutations targeting canonical paths, and compare real artifacts only through read-only before/after snapshots. |
| T-02-23-03 | Repudiation | privacy failure diagnostics | low | accept | Class and structural location provide sufficient local evidence; raw values remain intentionally absent. |
| T-02-23-04 | Denial of Service | temporary fixture cleanup | low | accept | Standard-library temporary directories and unconditional restoration bound residue to test-owned paths. |
</threat_model>

<api_coverage>
No new external API surface or provider capability is added. The plan changes local verification classification and fixture paths only; the existing OpenRouter API coverage declaration remains complete and `COVERAGE.md` is intentionally unchanged.
</api_coverage>

<verification>
- `python -O -I scripts/test_phase02_live_evidence.py`
- The rawContent fail-first CLI probe exits nonzero and omits the submitted value.
- Read-only before/after byte checks show no change to either real runtime path.
</verification>

<success_criteria>
- D-43 closes the camel-case privacy bypass without a parallel vocabulary.
- D-47 makes the complete optimized isolated test suite non-destructive to concurrent live evidence.
- Deferred complete-run-window and caller-owned-input proofs remain deferred and unclaimed.
</success_criteria>

<output>
Create `.planning/phases/02-ingestion-chunking-vector-storage/02-23-SUMMARY.md` when done.
</output>
