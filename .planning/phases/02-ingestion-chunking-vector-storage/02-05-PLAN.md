---
phase: 02-ingestion-chunking-vector-storage
plan: 05
type: execute
wave: 5
depends_on:
  - "02-04"
files_modified:
  - engine/src/db/mod.rs
  - engine/src/db/tests.rs
  - engine/src/main.rs
  - engine/src/bin/inspect_lancedb.rs
  - gateway/main.go
  - gateway/main_test.go
  - gateway/db/document_test.go
  - verify-ingestion.sh
  - verify-live-evidence.sh
autonomous: true
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-06
  - DATA-07
  - DATA-08
  - RAG-06
must_haves:
  truths:
    - "DATA-08: edges_schema() declares summary and summary_vector nullable, and persisted edge placeholders are Arrow nulls rather than empty text or copied embeddings."
    - "A failed same-ID replacement at any documents/nodes/edges write boundary restores all canonical tables to their captured LanceDB versions, so the prior completed index remains queryable."
    - "Retrying a failed same-ID replacement converges to exactly one canonical document generation without stale nodes or edges."
    - "If engine enqueue fails after PostgreSQL insertion, the gateway transactionally marks that row failed while preserving the original HTTP 429 or 502 response mapping."
    - "If terminal reconciliation loses the conditional-update race, the gateway re-reads and returns the winning terminal row instead of returning HTTP 500."
    - "The existing empty communities table remains valid after repeated initialization (DATA-06 empty-input probe)."
    - "Nullable node summary placeholders remain null and repeated schema initialization is idempotent (DATA-07 empty/idempotency probes)."
    - "Repeated or interrupted persistence of the same document ID is idempotent and preserves a valid committed generation (DATA-03 idempotency/concurrency probes)."
    - "Gateway and engine defaults are aligned to the locked 500-character size and 50-character overlap (D-09)."
    - "The committed live-gate tooling accepts only a pre-issued unpredictable challenge, generates sanitized evidence atomically after a real OpenRouter-backed run, and can directly re-inspect PostgreSQL and LanceDB for the recorded UUID (D-17/D-18)."
  artifacts:
    - path: "engine/src/db/mod.rs"
      provides: "Nullable edge schema plus internal durable staging-table access"
      exports:
        - "edges_schema"
        - "staged_documents_schema"
        - "DatabaseManager::staged_documents_table"
    - path: "engine/src/main.rs"
      provides: "Version-snapshotted recoverable replacement and deterministic write-boundary injection"
      contains:
        - "ReplacementBoundary"
        - "ReplacementFaultInjector"
        - "NoopReplacementFaultInjector"
        - "replace_document_with_faults"
    - path: "gateway/main.go"
      provides: "Failed-enqueue compensation and conditional-update race recovery"
      contains:
        - "compensateFailedIngest"
    - path: "gateway/db/document_test.go"
      provides: "Live PostgreSQL conditional-update race coverage"
      contains:
        - "TestConditionalTerminalUpdateRace"
    - path: "engine/src/bin/inspect_lancedb.rs"
      provides: "Machine-readable direct LanceDB inspection for one document ID"
      contains:
        - "document_id"
        - "embedding_width"
        - "embedding_model"
    - path: "verify-ingestion.sh"
      provides: "Challenge-bound live OpenRouter upload, managed service orchestration, three-store reconciliation, and atomic sanitized evidence generation"
      contains:
        - "OPENROUTER_API_KEY"
        - "inspect_lancedb"
        - "Ingestion validation: SUCCESS"
        - "EVIDENCE_OUT"
    - path: "verify-live-evidence.sh"
      provides: "Restrictive challenge issuance plus replay-resistant evidence validation and direct cross-store reinspection"
      contains:
        - "02-LIVE-CHALLENGE.json"
        - "02-LIVE-EVIDENCE.json"
        - "inspect_lancedb"
        - "Ingestion validation: SUCCESS"
  key_links:
    - from: "engine/src/main.rs"
      to: "engine/src/db/mod.rs"
      via: "ingest_document writes durable raw bytes to staged_documents; replace_document_with_faults snapshots and restores canonical table versions"
      pattern: "(staged_documents_table|version\\(|checkout\\(|restore\\()"
    - from: "engine/src/main.rs"
      to: "edges_schema()"
      via: "edge RecordBatch obtains both placeholder arrays from the validated edge schema through new_null_array"
      pattern: "new_null_array.*summary"
    - from: "gateway/main.go"
      to: "gateway/db/query.sql"
      via: "createDocument compensates through UpdateStatus; getDocument treats pgx.ErrNoRows as a lost terminal-update race and calls Get"
      pattern: "(UpdateStatus|ErrNoRows|\\.Get\\()"
    - from: "verify-ingestion.sh"
      to: "engine/src/bin/inspect_lancedb.rs"
      via: "the live verifier passes the uploaded UUID to the direct LanceDB inspector and reconciles its JSON with gateway and PostgreSQL state"
      pattern: "inspect_lancedb.*document-id"
    - from: "verify-live-evidence.sh"
      to: "verify-ingestion.sh"
      via: "the issuer creates a restrictive phase-local challenge immediately before the checkpoint; the live runner copies its exact challenge/run ID into atomic evidence and the validator compares both files before reinspection"
      pattern: "(challenge|issued_at|run_started_at|inspect_lancedb)"
  flagged_assumptions:
    - requirement_id: DATA-01
      category: empty
      status: unresolved
      flagged: true
      statement: "The requirements do not decide whether a zero-byte upload is accepted, rejected, or indexed as a zero-chunk document."
    - requirement_id: DATA-01
      category: encoding
      status: unresolved
      flagged: true
      statement: "The requirements do not decide whether invalid UTF-8 is rejected, lossily decoded, or normalized before character offsets are calculated."
    - requirement_id: DATA-02
      category: unclassified
      status: unresolved
      flagged: true
      statement: "The deterministic probe could not classify an additional chunking edge; existing chunking behavior is retained without inventing a new contract."
    - requirement_id: DATA-06
      category: adjacency
      status: unresolved
      flagged: true
      statement: "The requirement does not define merge/separation semantics for equal or adjacent community identifiers."
    - requirement_id: DATA-06
      category: ordering
      status: unresolved
      flagged: true
      statement: "The requirement does not define stable ordering for equal community identifiers."
    - requirement_id: DATA-07
      category: adjacency
      status: unresolved
      flagged: true
      statement: "The requirement does not define adjacency semantics for summary or unsummarized reference list values."
    - requirement_id: DATA-07
      category: ordering
      status: unresolved
      flagged: true
      statement: "The requirement does not define stable ordering when unsummarized reference values compare equal."
    - requirement_id: DATA-07
      category: concurrency
      status: unresolved
      flagged: true
      statement: "Concurrent schema initialization across multiple engine processes is not specified; this phase retains the single-engine startup model."
    - requirement_id: DATA-08
      category: unclassified
      status: unresolved
      flagged: true
      statement: "The deterministic probe could not classify an edge-table behavior beyond the explicit nullable-column contract."
    - requirement_id: DATA-09
      category: unclassified
      status: unresolved
      flagged: true
      statement: "The deterministic probe could not classify an additional EntityResolver edge; ExactMatchResolver semantics remain unchanged."
    - requirement_id: RAG-06
      category: unclassified
      status: unresolved
      flagged: true
      statement: "The deterministic probe could not classify an additional worker edge; the locked single-consumer and shutdown contracts remain authoritative."
---

<objective>
Implement the DATA-08 and ingestion-integrity fixes plus the instrumentation required for a replay-resistant live final-pass gate.

Purpose: A user can re-upload and poll a document without a failed replacement destroying the last valid index or a gateway race leaving misleading metadata.
Output: Nullable edge placeholders, recoverable LanceDB replacement, gateway compensation/race recovery, PostgreSQL integration coverage, direct LanceDB inspection, and challenge-bound live evidence generation/validation tooling. Plan 02-06 executes the credentialed gate.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-CONTEXT.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-RESEARCH.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-PATTERNS.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-VERIFICATION.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-04-SUMMARY.md
@engine/src/db/mod.rs
@engine/src/db/tests.rs
@engine/src/main.rs
@gateway/main.go
@gateway/main_test.go
@gateway/db/document_test.go
@verify-ingestion.sh
@verify-live-evidence.sh
</context>

<phase_goal>
**As a** Lancet API user, **I want to** ingest and safely replace text or Markdown documents, **so that** the last completed LanceDB index and PostgreSQL status remain trustworthy through failures and concurrent polling.
</phase_goal>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make edge placeholders nullable and make document replacement recoverable</name>
  <files>engine/src/db/mod.rs, engine/src/db/tests.rs, engine/src/main.rs</files>
  <read_first>
    - `engine/src/db/mod.rs` — `DatabaseManager`, `table_schemas()`, `nodes_schema()`, and `edges_schema()`
    - `engine/src/db/tests.rs` — initialization and drift-test conventions
    - `engine/src/main.rs` — `LancetServiceImpl::persist_raw`, `replace_document`, `process_job`, `spawn_worker`, and worker replacement tests
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-PATTERNS.md` — schema-driven null-array and deterministic failure-seam patterns
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-VERIFICATION.md` — DATA-08 blocker and destructive replacement warning
  </read_first>
  <behavior>
    - `edges_schema().field_with_name("summary").unwrap().is_nullable()` is true and the field type is Utf8.
    - `edges_schema().field_with_name("summary_vector").unwrap().is_nullable()` is true and the field type remains FixedSizeList(Float32, 2048); required edge identifiers remain non-nullable.
    - Persisted edge summary columns contain Arrow null values for every edge.
    - Injected failure after each canonical documents, nodes, and edges write boundary leaves the prior raw document, node set, and edge set queryable with their original counts/content.
    - A retry after every injected failure completes and leaves only the replacement generation, with no stale nodes or edges.
    - Empty communities registration and repeated nullable-schema initialization remain valid.
    - Missing chunk metadata uses 500 characters and 50 characters of overlap in both gateway metadata and the Rust worker.
  </behavior>
  <action>
First write failing schema and persistence tests. Change `edges_schema()` so `summary` and `summary_vector` are nullable per DATA-08 and D-21/D-23; keep the shared `vector()` type and D-22 fail-fast drift comparison unchanged. Add `staged_documents_schema()` and `DatabaseManager::staged_documents_table()` as an internal durable raw-upload staging surface so D-24 raw staging occurs after queue reservation without overwriting the currently committed `documents` row.

Refactor `LancetServiceImpl::persist_raw` to target `staged_documents`, and delete the staging row only after replacement commits. Before any canonical mutation, build all document/node/edge RecordBatches in memory, including edge `summary` and `summary_vector` arrays obtained with `new_null_array` from the validated edge schema. Introduce the exact internal test seam `ReplacementBoundary::{DocumentsWritten, NodesWritten, EdgesWritten}`, object-safe `ReplacementFaultInjector`, production `NoopReplacementFaultInjector`, and `replace_document_with_faults`; keep `replace_document` as the production wrapper. Capture `Table::version()` for canonical documents/nodes/edges before mutation. On any write or injected error, restore every changed table to its captured version with LanceDB `checkout(version)` plus `restore()` before returning failure; include rollback failures in the returned error. On success, remove the staged row only after all three canonical tables are valid. This preserves the prior valid index on every tested write-boundary failure while retaining D-14 single-consumer ordering and D-15 active-job shutdown behavior.

Align `DEFAULT_CHUNK_SIZE` and `DEFAULT_CHUNK_OVERLAP` with D-09 at 500 and 50, and update focused default-strategy assertions. Do not weaken schema drift validation, do not replace null summaries with sentinel values, and do not delete prior canonical rows before the replacement batches and rollback versions are ready.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml db::tests -- --nocapture &amp;&amp; cargo test --manifest-path engine/Cargo.toml replacement -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Explicit Arrow assertions cover edge summary types and nullability plus at least one required non-nullable edge field.
    - LanceDB persistence tests inspect edge arrays and prove both placeholder columns are null for every row.
    - Deterministic tests inject at all three named boundaries; each test proves prior canonical document/node/edge content remains intact, then proves retry convergence.
    - Existing incompatible edge schemas still fail startup with the schema-drift error required by D-22; no automatic wipe or silent schema migration is added.
    - The `staged_documents` table is registered and validated at startup, while the four roadmap-visible canonical tables retain their roles.
    - Rust missing-metadata defaults are asserted as 500/50 per D-09.
    - Focused Rust schema and replacement tests pass.
  </acceptance_criteria>
  <done>DATA-08 nullability is explicit in schema and stored rows, and every deterministic replacement-boundary failure preserves the prior valid index before a retry converges cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compensate enqueue failures and recover terminal-update races</name>
  <files>gateway/main.go, gateway/main_test.go, gateway/db/document_test.go</files>
  <read_first>
    - `gateway/main.go` — `documentStore`, `postgresStore.UpdateStatus`, `createDocument`, and `getDocument`
    - `gateway/main_test.go` — `fakeStore`, `engineFunc`, upload mapping tests, and polling tests
    - `gateway/db/document_test.go` — environment-gated transaction/rollback fixture
    - `gateway/db/query.sql` — conditional `UpdateDocumentStatus` contract; read only, no SQL change is expected
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-PATTERNS.md` — compensation and lost-race recovery patterns
  </read_first>
  <behavior>
    - ResourceExhausted after insert persists failed status/error and still returns HTTP 429.
    - Any other engine enqueue failure after insert persists failed status/error and still returns HTTP 502.
    - Compensation failure is logged with document ID and both errors while the original engine-derived HTTP status remains unchanged.
    - `UpdateStatus` returning `pgx.ErrNoRows` causes one `Get` re-read; a winning completed or failed row returns HTTP 200 unchanged.
    - A re-read that is missing/non-terminal or fails remains an HTTP 500 because it is not evidence of a winning terminal update.
    - Real PostgreSQL proves the second conditional terminal update returns `pgx.ErrNoRows` and the first terminal state remains intact.
    - Gateway inserts default chunk metadata as 500/50, matching D-09 and the Rust fallback.
  </behavior>
  <action>
Write handler tests before production changes. Extend `fakeStore` with narrowly scoped configurable insert/get/update errors and call counts. Add `compensateFailedIngest` in `gateway/main.go`: after `Insert` succeeds but `engine.Ingest` fails, call the existing transactional `UpdateStatus` with status `failed`, zero chunks, and a non-empty error message derived without secrets. Log a compensation failure with document ID and the original enqueue error, but preserve the externally required ResourceExhausted-to-429 and other-engine-error-to-502 mappings per D-02, D-05, D-11, and D-12.

In `getDocument`, treat only `pgx.ErrNoRows` from the conditional terminal `UpdateStatus` as a possible lost race. Re-read through `store.Get`; return HTTP 200 only when the winner is already `completed` or `failed`. Keep other update/read errors and an unexpectedly non-terminal reread as HTTP 500, and keep D-13 terminal-only PostgreSQL reconciliation.

Set the gateway's inserted default chunk metadata to 500/50 per D-09 and assert those exact values in the upload handler tests. Extend the rollback-isolated `TEST_DATABASE_URL` fixture with `TestConditionalTerminalUpdateRace`: insert queued, update terminal once, assert the second conditional update yields `pgx.ErrNoRows`, then re-read and assert the first terminal status/count/error remains intact. Do not make the SQL update unconditional.
  </action>
  <verify>
    <automated>Push-Location gateway; try { go test -count=1 -run 'TestCreateDocument|TestGetDocument' .; if ($LASTEXITCODE -ne 0) { throw "gateway handler tests failed with exit code $LASTEXITCODE" }; go test -count=1 -run 'TestDocumentQueries|TestConditionalTerminalUpdateRace' ./db; if ($LASTEXITCODE -ne 0) { throw "gateway database tests failed with exit code $LASTEXITCODE" } } finally { Pop-Location }</automated>
  </verify>
  <acceptance_criteria>
    - Handler tests cover queue-full compensation, general enqueue compensation, compensation logging/fallback response, successful lost-race reread, invalid reread, and non-race update failure.
    - Compensation uses the existing transactional store boundary and never changes the original 429/502 response class.
    - Lost-race recovery never overwrites the winner and returns only a verified terminal reread.
    - The PostgreSQL test is runnable with `TEST_DATABASE_URL`, transaction-isolated, and proves the real conditional-update contract.
    - Gateway upload tests prove 500/50 defaults match the Rust worker fallback.
    - Focused Go handler tests pass; the PostgreSQL suite passes when `TEST_DATABASE_URL` is configured and reports its existing explicit skip otherwise.
  </acceptance_criteria>
  <done>Failed engine admission cannot leave queued metadata, and a conditional terminal-update loser returns the winning terminal row without overwriting it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add direct LanceDB inspection and cross-store reconciliation</name>
  <files>engine/src/bin/inspect_lancedb.rs, verify-ingestion.sh, verify-live-evidence.sh</files>
  <read_first>
    - `engine/Cargo.toml` — existing lancedb, Arrow, futures, serde, serde_json, Tokio, and config dependencies; `src/bin` is auto-discovered without a manifest target
    - `engine/src/db/mod.rs` — `DatabaseManager` table accessors, canonical schemas, configured 2048 fixed-size vector type, and staged-documents accessor added by Task 1
    - `engine/src/main.rs` — `load_settings`, LanceDB predicates, persisted `embedding_model`, and the production OpenRouter engine wiring
    - `verify-ingestion.sh` — canonical live upload, poll, and PostgreSQL assertion to extend
    - `config/config.toml` and `config/config.dev.toml` — engine/gateway addresses and LanceDB path
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-CONTEXT.md` — D-17 sole-provider and D-18 model/dimension locks
  </read_first>
  <behavior>
    - The inspector requires a UUID document ID, resolves the same configured LanceDB path rules as the engine, opens LanceDB directly, and emits one JSON object without raw document or chunk content.
    - Inspection reports document, staged-document, node, and edge row counts; one distinct persisted embedding model; and the actual persisted node embedding width.
    - Inspection fails non-zero unless there is exactly one canonical document row, zero staged rows, at least one node row, 2048 values in every persisted embedding, one configured model identity, and unique/contiguous chunk identifiers and indexes with no duplicate edge IDs.
    - The live script exits before upload when `OPENROUTER_API_KEY` is absent, and missing/unreachable PostgreSQL, engine, gateway, LanceDB, or OpenRouter remains a hard failure.
    - The live script asserts gateway `ChunkCount` equals PostgreSQL `documents.chunk_count` equals the inspector node count, PostgreSQL status is `completed`, the LanceDB document count is one, persisted embedding width is 2048, and stale-generation checks pass.
    - Successful output is sanitized machine-readable evidence containing document ID, provider/model identity, PostgreSQL status/count, and LanceDB document/node/edge counts plus embedding width, never the API key, raw uploaded bytes, or stored chunk content.
    - The live runner requires a pre-issued challenge file, copies its exact cryptographically unpredictable challenge and run ID into evidence, records `run_started_at` no earlier than `issued_at`, and exposes evidence only after every assertion passes through an atomic same-directory rename.
    - The gate helper can remove old evidence and atomically issue a restrictive phase-local challenge immediately before a checkpoint; its validator rejects missing, malformed, stale, replayed, challenge-mismatched, or internally inconsistent evidence and rechecks the recorded document against PostgreSQL and direct LanceDB state.
  </behavior>
  <action>
First add focused tests around JSON assembly and invalid inspection results. Create the auto-discovered verification binary `engine/src/bin/inspect_lancedb.rs`; do not add a production HTTP or gRPC endpoint. Reuse the crate's existing dependencies and include the existing DB module by a path module declaration rather than introducing a library refactor solely for this verifier. Accept `--document-id` and an optional `--lancedb-path`; when no path override is supplied, load the same base TOML, `LANCET_ENV` overlay, and `LANCET_`-prefixed double-underscore environment overrides used by the engine. Reject non-UUID IDs before constructing the escaped predicate.

Open the configured LanceDB directly. Count matching `documents`, `staged_documents`, `nodes`, and `edges` rows. Query matching node batches to validate every actual `FixedSizeListArray` embedding has width 2048, collect a single distinct non-null `embedding_model`, and prove `chunk_id` uniqueness plus unique contiguous `chunk_index` values from zero through node_count minus one. Query edge identifiers to reject duplicates. Emit a single JSON object with `document_id`, `provider: "openrouter"`, `embedding_model`, `document_rows`, `staged_document_rows`, `node_rows`, `edge_rows`, `embedding_width`, and `stale_generation: false`. Exit non-zero instead of emitting success JSON if any invariant fails. Never select or serialize `raw_content` or node `content`.

Extend `verify-ingestion.sh` into the executable reconciliation gate. Fail fast if `OPENROUTER_API_KEY` is unset or empty, but never echo its value. Retain the real HTTP upload/poll flow and PostgreSQL query, changing the database result to structured status/count fields. Invoke `cargo run --quiet --manifest-path engine/Cargo.toml --bin inspect_lancedb -- --document-id "$document_id"` after terminal completion, parse its JSON, and assert gateway count equals PostgreSQL count equals `node_rows`; PostgreSQL status is completed; `document_rows` is one; `staged_document_rows` is zero; `embedding_width` is 2048; the model equals the configured D-18 model; and `stale_generation` is false. Treat any command, parse, or invariant failure as fatal.

Require `--challenge-file` (default `.planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json`) and allow `--evidence`/`EVIDENCE_OUT` (default `.planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json`). Read and validate the pre-issued `schema_version`, `challenge`, `run_id`, and RFC3339 UTC `issued_at` before starting any upload; never generate or replace the challenge inside the live runner. Capture `run_started_at` only after the challenge is read and reject clock ordering where it is earlier than `issued_at`. Support one credentialed `--managed-services` command that starts/checks PostgreSQL, launches the gateway and Rust engine with inherited environment, waits for readiness, performs the real upload/reconciliation, and cleans up only processes it started. Missing credentials or any service/provider failure remains fatal and the key is never printed.

Only after every live assertion succeeds, assemble deterministic JSON with `schema_version`, `success_sentinel: "Ingestion validation: SUCCESS"`, the exact `challenge` and `run_id`, `issued_at`, `run_started_at`, `generated_at`, document UUID, provider/model identity, gateway chunk count, PostgreSQL status/count, LanceDB document/staged/node/edge counts, embedding width, and duplicate/stale-generation booleans. Use `umask 077`, a same-directory temporary file, best-effort portable mode 600, and atomic rename to `EVIDENCE_OUT`; clean the temporary file on every exit. Never include the credential, raw uploaded bytes, stored document/chunk text, request headers, command environment, or service logs containing content. Printing the same sanitized JSON and sentinel is informational only; the files are the continuation contract.

Create committed source helper `verify-live-evidence.sh` with two explicit modes. `--prepare-gate` first removes the exact prior evidence path, generates challenge and run ID from an OS cryptographic source plus `issued_at`, writes only `schema_version`, `challenge`, `run_id`, and `issued_at` under `umask 077` to a same-directory temporary file, applies mode 600 where supported, and atomically renames it to the phase-local hidden challenge path. It must refuse a challenge path outside the phase directory and print only the subsequent wrapper command, never secret values. Preparation performs non-credentialed prerequisites such as required-command checks, PostgreSQL startup/readiness, inspector build, and script syntax checks before issuing the challenge as its final operation so issuance occurs immediately before the human checkpoint.

In `--validate-gate` mode, accept `--challenge` and `--evidence` with the phase-local defaults. Validate both strict JSON schemas and the exact sentinel; require exact equality of challenge, run ID, and issued timestamp, provider `openrouter`, model `nvidia/llama-nemotron-embed-vl-1b-v2:free`, a UUID, PostgreSQL `completed` with a positive chunk count, exactly one LanceDB document row, zero staged rows, equal positive gateway/PostgreSQL/LanceDB node counts, embedding width 2048, no duplicate node generation, no stale generation, and a present non-negative edge count. Require parseable UTC ordering `issued_at <= run_started_at <= generated_at`, freshness no older than 30 minutes, and reject unknown secret/content fields. Then re-query PostgreSQL and re-run `inspect_lancedb` for the recorded UUID; compare status/counts/model/width/duplicate/staging results with evidence and fail on any drift. Delete the challenge only after every validation and live reinspection succeeds; leave it intact on failure for diagnosis, and never read or print `OPENROUTER_API_KEY` or content fields.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --bin inspect_lancedb; if ($LASTEXITCODE -ne 0) { throw "LanceDB inspector tests failed with exit code $LASTEXITCODE" }; cargo check --manifest-path engine/Cargo.toml --bin inspect_lancedb; if ($LASTEXITCODE -ne 0) { throw "LanceDB inspector build failed with exit code $LASTEXITCODE" }; bash -n verify-ingestion.sh; if ($LASTEXITCODE -ne 0) { throw "verify-ingestion.sh syntax check failed with exit code $LASTEXITCODE" }; bash -n verify-live-evidence.sh; if ($LASTEXITCODE -ne 0) { throw "verify-live-evidence.sh syntax check failed with exit code $LASTEXITCODE" }; bash ./verify-live-evidence.sh --self-test; if ($LASTEXITCODE -ne 0) { throw "challenge/evidence helper self-test failed with exit code $LASTEXITCODE" }</automated>
  </verify>
  <acceptance_criteria>
    - `engine/src/bin/inspect_lancedb.rs` is a verification-only direct-store binary and no production network endpoint is added.
    - Inspector tests cover valid rows plus duplicate document, staged row, duplicate/non-contiguous chunk generation, duplicate edge ID, wrong embedding width, and mixed/missing model failures.
    - Inspector JSON is machine-readable and contains counts/identity only; no raw document bytes, chunk text, or credential field is selected or emitted.
    - `verify-ingestion.sh` hard-fails without `OPENROUTER_API_KEY` and reconciles all three counts plus completed status, one document row, zero staging rows, D-18 model/width, and stale-generation=false.
    - `verify-ingestion.sh` requires the pre-issued challenge, records its exact challenge/run ID and ordered timestamps, and exposes evidence only after success through a restrictive same-directory atomic rename.
    - `verify-live-evidence.sh --prepare-gate` deletes prior evidence before atomically issuing an unpredictable restrictive challenge as the final pre-checkpoint operation.
    - `verify-live-evidence.sh --validate-gate` rejects fixture-only, replayed, stale, malformed, challenge-mismatched, duplicate-generation, wrong-model, wrong-width, and missing-sentinel evidence; its PostgreSQL/LanceDB reinspection detects post-run drift and it consumes the challenge only after success.
    - The syntax, inspector test, and inspector build commands pass without adding dependencies or changing production protocols.
  </acceptance_criteria>
  <done>The repository has an automated, direct LanceDB inspection surface and a live script that can prove the OpenRouter-backed index agrees across gateway, PostgreSQL, and LanceDB.</done>
</task>

</tasks>

<threat_model>
## Threat Model Configuration

ASVS Level: 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → Go gateway | Untrusted multipart content and polling IDs enter the service. |
| Go gateway → PostgreSQL | Gateway status transitions cross into durable metadata. |
| Go gateway → Rust gRPC | Upload bytes and document identity cross process/runtime boundaries. |
| Rust worker → LanceDB | Replacement data crosses from staged computation into the canonical local index. |
| Rust engine → OpenRouter | Document chunks and an API credential cross to an external provider. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05-01 | Tampering | `replace_document_with_faults` | high | mitigate | Capture all canonical table versions before writes; on any boundary failure restore every changed table and prove prior content with deterministic tests. |
| T-02-05-02 | Denial of Service | `createDocument` enqueue failure | medium | mitigate | Compensate the inserted PostgreSQL row to terminal failed status while retaining HTTP 429 queue-pressure signaling. |
| T-02-05-03 | Repudiation | gateway compensation failure | medium | mitigate | Emit structured logs containing document ID plus original/compensation errors without credentials or raw document content. |
| T-02-05-04 | Tampering | conditional terminal update | medium | mitigate | Preserve the conditional SQL guard; recover a lost race only by re-reading and accepting the winning terminal row. |
| T-02-05-05 | Information Disclosure | OpenRouter live verification | high | mitigate | Read `OPENROUTER_API_KEY` from environment only; never print, persist, or include it in errors/test fixtures. |
| T-02-05-06 | Spoofing | document predicates | low | accept | Existing UUIDv4 validation and SQL-string escaping remain unchanged; this plan adds no new caller-controlled identifiers. |
| T-02-05-07 | Information Disclosure | direct LanceDB inspection evidence | high | mitigate | Select and emit only identifiers, provider/model identity, counts, embedding width, and boolean invariants; never select raw_content or node content, and require the checkpoint evidence to be sanitized. |
| T-02-05-08 | Tampering | generated live evidence | high | mitigate | Use a random run ID, current-run timestamps, restrictive permissions, same-directory atomic rename, strict schema/sentinel validation, and direct gateway/PostgreSQL/LanceDB reinspection before completion. |
</threat_model>

## Artifacts this phase produces

### New symbols

- `engine/src/db/mod.rs`: `staged_documents_schema()`, `DatabaseManager::staged_documents_table()`.
- `engine/src/main.rs`: `ReplacementBoundary`, `ReplacementFaultInjector`, `NoopReplacementFaultInjector`, `replace_document_with_faults`.
- `gateway/main.go`: `compensateFailedIngest`.
- `gateway/db/document_test.go`: `TestConditionalTerminalUpdateRace`.
- `engine/src/bin/inspect_lancedb.rs`: verification-only direct LanceDB inspector and sanitized JSON evidence schema.
- `engine/src/db/tests.rs`: explicit edge-nullability schema test.
- `engine/src/main.rs` tests: boundary-failure preservation and retry-convergence tests.
- `gateway/main_test.go`: enqueue-compensation and lost-race recovery tests.
- `verify-ingestion.sh`: live gateway/PostgreSQL/LanceDB reconciliation backed by a real OpenRouter engine request.
- `verify-live-evidence.sh`: committed autonomous validator for the generated live-run artifact and current cross-store state.

### New files

- `engine/src/bin/inspect_lancedb.rs`: machine-readable direct LanceDB inspection binary used only by verification.
- `verify-live-evidence.sh`: committed verification source; validates evidence and re-inspects live stores.
- `.planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json`: Plan 02-06 runtime challenge only; restrictive, short-lived, and never staged or committed.
- `.planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json`: Plan 02-06 runtime evidence only; sanitized, short-lived, and never staged or committed.
- `.planning/phases/02-ingestion-chunking-vector-storage/COVERAGE.md`: planning-time external API coverage decision artifact.

## Spec-less probe disposition

The deterministic fallback produced 16 unresolved items. Five are defensibly converted into testable truths in `must_haves` (DATA-03 idempotency/concurrency, DATA-06 empty, DATA-07 empty/idempotency); the remaining eleven are preserved one-for-one in `must_haves.flagged_assumptions`. No item is silently dismissed.

The adversarial prohibition recall/precision pass produced no bespoke values, fairness, privacy, transparency, or safety prohibition for these storage/worker utility requirements. Routine correctness items are handled by the edge truths and tests; canon security items are routed through this plan's ASVS threat model and later `$gsd-secure-phase`. Therefore no descriptor-less `must_haves.prohibitions` item is minted.

## Verification-gap coverage audit

| Source | Gap | Plan task | Status | Evidence required |
|--------|-----|-----------|--------|-------------------|
| VERIFICATION critical | DATA-08 edge summary nullability | Task 1 | COVERED | Arrow field assertions plus persisted null-array inspection |
| VERIFICATION warning | Destructive independent multi-table replacement | Task 1 | COVERED | Three boundary failures preserve prior index; retry converges |
| VERIFICATION warning | Enqueue failure leaves queued PostgreSQL row | Task 2 | COVERED | 429/502 compensation handler tests |
| VERIFICATION warning | Conditional terminal update race returns 500 | Task 2 | COVERED | Fake-store race test plus real PostgreSQL contract test |
| VERIFICATION behavior-unverified | Live gateway polling updates PostgreSQL | Task 3 + Plan 02-06 | COVERED | Challenge-bound `verify-ingestion.sh` artifact plus autonomous PostgreSQL/LanceDB reinspection |
| VERIFICATION behavior-unverified | No direct persisted-store evidence or live provider proof | Task 3 + Plan 02-06 | COVERED | Inspector JSON, credentialed real OpenRouter run, and mechanically validated invocation-bound evidence |

## Multi-source coverage audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Ingest text/markdown, chunk, and store in LanceDB | 02-01–02-06 | COVERED | Plan 02-06 re-verifies the complete shipped path; 02-05 Tasks 1-3 protect and instrument integrity. |
| REQ | DATA-01 | Lightweight text-like document ingestion | 02-01, 02-04, 02-05 | COVERED | Failed-enqueue metadata and live E2E are closed here. |
| REQ | DATA-02 | Fixed-size and structure-aware chunking | 02-02, 02-05 | COVERED | Task 1/2 align the locked 500/50 defaults across runtimes. |
| REQ | DATA-03 | LanceDB chunk/metadata persistence | 02-03, 02-04, 02-05 | COVERED | Task 1 adds recoverable replacement. |
| REQ | DATA-06 | Community placeholders | 02-03, 02-05 | COVERED | Regression truth preserves empty table initialization. |
| REQ | DATA-07 | Nullable node summaries/refs | 02-03, 02-05 | COVERED | Regression truth preserves null/idempotent schema behavior. |
| REQ | DATA-08 | Separate nodes/edges and nullable edge summaries | 02-03, 02-05 | COVERED | Task 1 closes the blocker. |
| REQ | DATA-09 | EntityResolver and ExactMatchResolver | 02-03 | COVERED | Passed verification; unchanged. |
| REQ | RAG-06 | Async background worker | 02-01, 02-02, 02-04, 02-05 | COVERED | Task 1 retains single-consumer/shutdown behavior. |
| RESEARCH | §1.1–1.3 | Transactional metadata, gRPC ingestion, bounded sequential worker | 02-01, 02-04, 02-05 | COVERED | Task 2 uses transactional store; Task 1 preserves D-14/D-15. |
| RESEARCH | §1.4–1.5 | Chunking, token estimates, OpenRouter concurrency/retry | 02-02, 02-03 | COVERED | Verified implementation is not broadened. |
| RESEARCH | §1.6–1.7 | Canonical schemas, null placeholders, drift detection, resolver | 02-03, 02-05 | COVERED | Task 1 uses schema-derived Arrow null arrays and keeps fail-fast drift. |
| RESEARCH | §2 | Unit, integration, and live UAT validation | 02-01–02-06 | COVERED | 02-05 adds missing failure, PostgreSQL, direct-store, and gate tooling; 02-06 executes the live and continuation-validation coverage. |
| CONTEXT | D-01–D-05 | Multipart upload, PostgreSQL metadata/transactions, global corpus, streaming | 02-01, 02-04, 02-05 | COVERED | Task 2 cites and preserves D-02/D-05 transaction behavior. |
| CONTEXT | D-06–D-09 | Per-document structure-aware/fixed chunking defaults | 02-02 | COVERED | Verified and unchanged. |
| CONTEXT | D-10–D-16 | Bounded async queue, Go-only PostgreSQL, polling, one worker, shutdown, tracing | 02-01, 02-04, 02-05 | COVERED | Tasks 1-2 preserve D-11–D-16. |
| CONTEXT | D-17–D-20 | OpenRouter-only 2048 embeddings, retry, concurrency five | 02-03, 02-04, 02-05, 02-06 | COVERED | 02-05 Task 3 instruments inspection; 02-06 exercises and mechanically validates the persisted provider/model/dimension. |
| CONTEXT | D-21–D-25 | LanceDB schema/drift, raw replacement, token metadata | 02-03, 02-04, 02-05 | COVERED | Task 1 directly implements D-21–D-24 integrity and DATA-08 nullability. |
| CONTEXT | D-26–D-30 | Shared TOML/env/Docker configuration | 02-01 | COVERED | Verified and unchanged. |

## Locked-decision fidelity ledger

- D-01: Preserve multipart/form-data uploads; this closure changes post-insert failure handling only.
- D-02: Preserve comprehensive PostgreSQL metadata and add a truthful failed terminal state after enqueue rejection.
- D-03: Preserve 64KB Go-to-Rust gRPC streaming.
- D-04: Preserve the shared global corpus model.
- D-05: Perform compensation through the existing transactional PostgreSQL store boundary.
- D-06: Preserve per-document chunk strategy, size, and overlap metadata.
- D-07: Preserve structure-aware chunking as the fallback strategy.
- D-08: Preserve pulldown-cmark AST structure recognition.
- D-09: Correct both runtime fallbacks to the locked 500-character size and 50-character overlap.
- D-10: Preserve capacity-100 asynchronous Tokio admission and the immediate queued response.
- D-11: Preserve RESOURCE_EXHAUSTED and HTTP 429 mapping while compensating metadata.
- D-12: Keep PostgreSQL access exclusively in the Go gateway.
- D-13: Keep gRPC status polling and terminal-only PostgreSQL reconciliation; re-read only after a lost terminal-update race.
- D-14: Keep one sequential background consumer; the replacement coordinator does not add parallel writers.
- D-15: Keep active-job-safe graceful shutdown and pending-job discard behavior.
- D-16: Keep document-scoped tracing and add rollback/compensation context without raw content or credentials.
- D-17: Keep OpenRouter as the sole production embedding provider.
- D-18: Keep the locked 2048-dimension OpenRouter model and validate list width.
- D-19: Preserve the 10-second, three-attempt exponential-backoff policy.
- D-20: Preserve at most five concurrent OpenRouter calls per document.
- D-21: Initialize and validate canonical plus internal staging LanceDB schemas at engine startup.
- D-22: Keep fail-fast schema drift with no automatic wipe or silent migration.
- D-23: Preserve the complete nodes schema and its nullable fields.
- D-24: Keep durable Binary raw-content staging after queue reservation; commit same-ID raw/chunks/edges recoverably.
- D-25: Preserve o200k_base token estimates and tracking columns.
- D-26: Preserve shared root TOML configuration files.
- D-27: Preserve LANCET_CONFIG_DIR lookup and fallback.
- D-28: Preserve LANCET_ENV environment overlays.
- D-29: Preserve Viper/config-rs and LANCET_ double-underscore overrides.
- D-30: Preserve read-only Docker Compose config mounts.

<verification>
- `cargo fmt --manifest-path engine/Cargo.toml -- --check`
- `cargo test --manifest-path engine/Cargo.toml`
- `Push-Location gateway; try { gofmt -w main.go main_test.go db/document_test.go; if ($LASTEXITCODE -ne 0) { throw "gofmt failed with exit code $LASTEXITCODE" }; go test ./...; if ($LASTEXITCODE -ne 0) { throw "gateway tests failed with exit code $LASTEXITCODE" }; go vet ./...; if ($LASTEXITCODE -ne 0) { throw "go vet failed with exit code $LASTEXITCODE" } } finally { Pop-Location }`
- With `TEST_DATABASE_URL`: `bash -lc 'export TEST_DATABASE_URL="postgres://postgres:postgres@127.0.0.1:5432/lancet?sslmode=disable"; cd gateway &amp;&amp; go test -count=1 ./db'`
- `cargo test --manifest-path engine/Cargo.toml --bin inspect_lancedb`
- `cargo check --manifest-path engine/Cargo.toml --bin inspect_lancedb`
- `bash -n verify-ingestion.sh`
- `bash -n verify-live-evidence.sh`
- Confirm `verify-live-evidence.sh` tests cover challenge issuance, replay/mismatch rejection, timestamp ordering, and consume-on-success semantics without contacting OpenRouter.
</verification>

<success_criteria>
- DATA-08 is satisfied by explicit Arrow nullability and persisted-null tests.
- Every injected canonical write-boundary failure leaves the previous document index valid.
- Replaying the replacement removes stale generations and reaches completed status.
- Gateway enqueue failures are terminally compensated without changing 429/502 mappings.
- Conditional-update races return the winning terminal row without unconditional overwrite.
- Direct LanceDB inspection proves one canonical document row, no staged row, unique current-generation nodes/edges, the configured OpenRouter model, and actual 2048-wide persisted embeddings.
- The committed live gate deletes old evidence before issuing an unpredictable restrictive challenge, requires exact challenge/run-ID equality and ordered timestamps, emits evidence atomically, and consumes the challenge only after successful validation.
- The one-command managed live runner uses the production OpenRouter client without printing/persisting the credential and treats missing credentials, services, or cross-store invariants as fatal.
- All 16 spec-less probe items are accounted for as five testable truths plus eleven flagged assumptions, and zero low-value prohibitions are fabricated.
</success_criteria>

<output>
Create `.planning/phases/02-ingestion-chunking-vector-storage/02-05-SUMMARY.md` after all three implementation tasks and their autonomous checks pass. Record the implemented integrity/gate contracts and test results; do not claim a live OpenRouter pass. Plan 02-06 exclusively owns challenge issuance, credentialed execution, live evidence validation, and the final phase gate.
</output>
