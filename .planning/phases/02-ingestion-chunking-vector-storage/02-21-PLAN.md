---
phase: 02-ingestion-chunking-vector-storage
plan: 21
type: execute
wave: 16
depends_on:
  - "02-17"
  - "02-19"
  - "02-20"
files_modified:
  - scripts/phase02_live_evidence.py
  - scripts/test_phase02_live_evidence.py
  - scripts/test_phase02_privacy_prohibition.cjs
  - verify-ingestion.sh
  - verify-live-evidence.sh
autonomous: true
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
must_haves:
  truths:
    - "Python is the only external verification runtime and one recursive normalized classifier covers every forbidden privacy field class; Node is absent from the gate (D-33, D-41)."
    - "Invalid/missing/empty/non-string verification LanceDB configuration fails before the inspector runs, and a valid relative path resolves from repository root from any caller CWD (D-35)."
    - "The explicit-path regression captures the actual spawned inspector command and exact path rather than searching source text (D-34)."
    - "All accepted Phase 02 ship findings pass the ADR exit commands while DEBT-CR-04, DEBT-CR-05, DEBT-BU-01, and DEBT-BU-02 remain discoverable and unimplemented unless triggered (D-31, D-38, D-39, D-40, D-42)."
  artifacts:
    - path: "scripts/phase02_live_evidence.py"
      provides: "Canonical privacy classifier and strict verification-store resolver"
    - path: "scripts/test_phase02_live_evidence.py"
      provides: "Per-category privacy, fail-closed config, and process-argument regressions"
    - path: "verify-ingestion.sh"
      provides: "Strict repository-root store resolution for ingestion inspection"
    - path: "verify-live-evidence.sh"
      provides: "Python-only privacy/config gate before current-store inspection"
  key_links:
    - from: "verify-live-evidence.sh --validate-gate"
      to: "phase02_live_evidence.py"
      via: "mandatory Python privacy validation and strict store-path resolution"
      pattern: "phase02_live_evidence.py"
    - from: "resolved config path"
      to: "inspect_lancedb"
      via: "captured explicit --lancedb-path argument"
      pattern: "--lancedb-path"
    - from: "privacy fixtures"
      to: "canonical forbidden classifier"
      via: "one isolated case for each normalized category"
      pattern: "(credential|secret|bearer|authorization_header|raw_content|document_text|chunk_content)"
---

<objective>
Close WR-01, WR-02, WR-03's explicit-argument gap, and WR-04, then run the deterministic accepted Phase 02 exit gate.

Purpose: A verification green result must not depend on Node, drift between privacy vocabularies, inspect a fallback store, or claim behavior from source text.
Output: Python-only privacy enforcement, strict store resolution, actual process-argument capture, and an exit-gate result consistent with D-31 through D-42.
</objective>

<artifacts_this_phase_produces>
- One canonical Python privacy classifier and per-category fail-first tests.
- One strict repository-root verification-store resolver shared by both shell entrypoints.
- Actual spawned-inspector argument capture.
- Deterministic final ship-finding gate without deferred implementation.
</artifacts_this_phase_produces>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-CONTEXT.md
@.planning/phases/02-ingestion-chunking-vector-storage/deferred-items.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-17-SUMMARY.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-18-SUMMARY.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-19-SUMMARY.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-20-SUMMARY.md
@.discussion/decisions/phase-02-verification-disposition.md
@scripts/phase02_live_evidence.py
@scripts/test_phase02_live_evidence.py
@scripts/test_phase02_privacy_prohibition.cjs
@verify-ingestion.sh
@verify-live-evidence.sh
@config/config.verify.toml
</context>

<phase_goal>
**As a** Lancet API user, **I want to** ingest and safely replace text or Markdown documents, **so that** the last completed LanceDB index and PostgreSQL status remain trustworthy through failures and concurrent polling.
</phase_goal>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Enforce every privacy class through one mandatory Python path</name>
  <files>scripts/phase02_live_evidence.py, scripts/test_phase02_live_evidence.py, scripts/test_phase02_privacy_prohibition.cjs, verify-live-evidence.sh</files>
  <read_first>
    - `scripts/phase02_live_evidence.py` — current recursive normalized forbidden-field classifier
    - `scripts/test_phase02_live_evidence.py` — optimized isolated subprocess and fixture conventions
    - `scripts/test_phase02_privacy_prohibition.cjs` — superseded narrower vocabulary to remove
    - `verify-live-evidence.sh` — conditional Node branch in `--validate-gate`
    - `.discussion/decisions/phase-02-verification-disposition.md` WR-01/WR-02 and runtime boundaries
  </read_first>
  <behavior>
    - Test 1: clean nested metadata passes under `python -O -I`.
    - Test 2: independent nested fixtures for `credential`, `secret`, `bearer`, `authorization_header`, `raw_content`, `document_text`, and `chunk_content` each fail with only the normalized field class in diagnostics.
    - Test 3: mixed case, separators, and recursive list/object placement cannot bypass classification.
    - Test 4: the final validation path returns nonzero when Python execution or privacy validation fails and never invokes Node.
  </behavior>
  <action>
Implement D-33 and D-41. Keep one canonical normalized forbidden-field classifier in `scripts/phase02_live_evidence.py`, recursively walking mappings and sequences case-insensitively. Move the fail-first/control fixture cases into the isolated Python test suite, with one independently executed bad subject for every ADR category and a clean control. Errors identify only the normalized field class and structural location class; they never serialize a value.

Make `verify-live-evidence.sh --validate-gate` invoke the Python privacy path unconditionally before inspection/cleanup and propagate inability to run it as nonzero. Remove all Node detection/invocation and delete `scripts/test_phase02_privacy_prohibition.cjs` after equivalent Python behavior is green. Keep Python verification-only; do not introduce it into Go/Rust startup, service, ingestion, polling, or recovery.
  </action>
  <verify>
    <automated>$env:PYTHONOPTIMIZE='1'; try { python -O -I scripts/test_phase02_live_evidence.py -k privacy; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } finally { Remove-Item Env:PYTHONOPTIMIZE -ErrorAction SilentlyContinue }; if (Test-Path -LiteralPath scripts/test_phase02_privacy_prohibition.cjs) { throw 'superseded Node privacy test still exists' }; if ((rg -n 'node|test_phase02_privacy_prohibition' verify-live-evidence.sh).Count -ne 0) { throw 'Node remains in final verification gate' }</automated>
  </verify>
  <acceptance_criteria>
    - Clean control passes and each forbidden category independently fails.
    - Recursive/case/separator variants are covered.
    - Python absence or validation failure is fatal to the gate.
    - Node is absent from verification and production runtime requirements.
    - Diagnostics contain field classes only.
  </acceptance_criteria>
  <done>WR-01 and WR-02 are closed with one mandatory Python implementation and no Node dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve the intended store strictly, capture actual inspector arguments, and run exit gates</name>
  <files>scripts/phase02_live_evidence.py, scripts/test_phase02_live_evidence.py, verify-ingestion.sh, verify-live-evidence.sh</files>
  <read_first>
    - `verify-ingestion.sh` — permissive inline TOML/regex fallback and inspector invocation
    - `verify-live-evidence.sh` — matching fallback and final cleanup ordering
    - `config/config.verify.toml` — committed authoritative `engine.lancedb_path`
    - `scripts/test_phase02_live_evidence.py` — current source-text explicit-path assertion to replace
    - `deferred-items.md` — four accepted records that must remain suppressed unless triggered
    - `.discussion/decisions/phase-02-verification-disposition.md` WR-03, WR-04, and Phase 02 Exit Conditions
  </read_first>
  <behavior>
    - Test 1: invalid TOML, missing engine table/key, empty value, non-string value, or missing expected store fails before fake inspector invocation.
    - Test 2: a valid relative path resolves identically from repository root and an unrelated temporary CWD.
    - Test 3: fake `cargo` captures `inspect_lancedb`, the expected document UUID, and exact absolute `--lancedb-path` for both scripts.
    - Test 4: the deterministic Rust, Go, Python, and shell exit commands pass after all ship findings are integrated.
  </behavior>
  <action>
Implement D-35/WR-04 with a dedicated Python helper/subcommand that parses committed TOML through `tomllib`, requires `engine.lancedb_path` as a non-empty string, resolves relative values against the repository root, validates the expected local verification-store contract, and prints only the absolute path. It must return nonzero on parse/key/type/empty/path-contract errors. Replace both shell scripts' inline parser/regex/hardcoded fallback with this one helper; stop before `inspect_lancedb` when resolution fails.

Replace WR-03's source-text check with a config-less subprocess harness. Put a fake `cargo` executable first on `PATH`, record argv to a temporary capture file, invoke each real shell path from an unrelated CWD with inert fixtures, and assert the actual command contains the inspector binary, exact document UUID, and exact resolved absolute path. Also assert invalid config leaves the capture file absent.

Run the accepted ADR exit commands plus optimized isolated Python and shell syntax checks. Confirm summaries 02-17 through 02-20 exist and that the four debt records remain in both `02-CONTEXT.md` and `deferred-items.md`. Per D-38 through D-42, do not add CR-05 resource controls, non-loopback auth/TLS/quotas, complete-run-window proof, or exhaustive caller-owned-input proof; report a trigger if current repository configuration contradicts the recorded local-only constraints.
  </action>
  <verify>
    <automated>$required=@('.planning/phases/02-ingestion-chunking-vector-storage/02-17-SUMMARY.md','.planning/phases/02-ingestion-chunking-vector-storage/02-18-SUMMARY.md','.planning/phases/02-ingestion-chunking-vector-storage/02-19-SUMMARY.md','.planning/phases/02-ingestion-chunking-vector-storage/02-20-SUMMARY.md'); foreach($path in $required){ if(!(Test-Path -LiteralPath $path -PathType Leaf)){ throw "missing prerequisite summary: $path" } }; cargo fmt --manifest-path engine/Cargo.toml -- --check; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; cargo test --manifest-path engine/Cargo.toml; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; cargo clippy --manifest-path engine/Cargo.toml --all-targets -- -D warnings; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; Push-Location gateway; try { go test -count=1 ./...; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; go vet ./...; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE} } finally { Pop-Location }; $env:PYTHONOPTIMIZE='1'; try { python -O -I scripts/test_phase02_live_evidence.py; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE} } finally { Remove-Item Env:PYTHONOPTIMIZE -ErrorAction SilentlyContinue }; bash -n verify-ingestion.sh; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; bash -n verify-live-evidence.sh; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; foreach($id in @('DEBT-CR-04','DEBT-CR-05','DEBT-BU-01','DEBT-BU-02')){ rg -q --fixed-strings $id .planning/phases/02-ingestion-chunking-vector-storage/02-CONTEXT.md; if($LASTEXITCODE -ne 0){throw "missing context debt: $id"}; rg -q --fixed-strings $id .planning/phases/02-ingestion-chunking-vector-storage/deferred-items.md; if($LASTEXITCODE -ne 0){throw "missing ledger debt: $id"} }</automated>
  </verify>
  <acceptance_criteria>
    - Every configuration error fails before inspector invocation.
    - Both scripts pass the exact repository-root-resolved absolute path.
    - Actual subprocess arguments, not source text, prove forwarding.
    - All ADR Phase 02 exit commands and Python/shell gates pass.
    - All four deferred records remain visible and no trigger is currently active.
  </acceptance_criteria>
  <done>WR-03/WR-04 are closed and every accepted ship finding has a deterministic green exit gate without implementing deferred debt.</done>
</task>

</tasks>

<finding_coverage>

| Finding | Disposition | Closure |
|---------|-------------|---------|
| CR-01 | Ship | 02-17 process-level `LANCET_CONFIG_DIR` startup |
| CR-02 | Ship | 02-17 persisted → gRPC → executed chunk contract |
| CR-03 | Ship | 02-18 durable intent contract + 02-19 restart-safe reconciler |
| CR-04 | Guardrail only | 02-17 loopback bind/documentation; `DEBT-CR-04` records the remainder |
| CR-05 | Defer | `DEBT-CR-05`; no Phase 02 implementation |
| CR-06 | Ship | 02-20 class-only inspector diagnostics |
| WR-01 + WR-02 | Ship | 02-21 Python-only canonical privacy enforcement |
| WR-03 | Ship in-scope exact proofs | 02-20 BU-03/BU-04 + 02-21 captured inspector argv; BU-01/BU-02 remain deferred |
| WR-04 | Ship | 02-21 strict fail-closed store selection |
| WR-05 | Ship | 02-20 read-only open-and-validate |
| BU-01 | Defer | `DEBT-BU-01`, Phase 6/v1 or immediate trigger |
| BU-02 | Defer | `DEBT-BU-02`, Phase 6/v1 or immediate trigger |
| BU-03 | Ship | 02-20 independent null/NaN/+∞/−∞ fixtures |
| BU-04 | Ship | 02-20 real schema-field fault, rollback, failed state, and worker survival |

</finding_coverage>

<source_audit>

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Trustworthy async ingestion, replacement, LanceDB generation, and PostgreSQL status | 02-17..02-21 | COVERED | New closures repair configuration, settings truth, durable status, inspection, and gates. |
| REQ | DATA-01 | Multi-format document ingestion | 02-17, 02-19, 02-21 | COVERED | Historical delivery preserved; settings/reconciliation/privacy gaps close. |
| REQ | DATA-02 | Fixed-size and structure-aware chunking | 02-17 | COVERED | Exact persisted-to-executed contract. |
| REQ | DATA-03 | LanceDB chunks and metadata | 02-20, 02-21 | COVERED | Read-only integrity and fail-closed path selection. |
| REQ | DATA-06 | Community placeholder schema | 02-20 | COVERED | Inspector requires/validates existing table without creating it. |
| REQ | DATA-07 | Nullable node synthesis columns | 02-20 | COVERED | Canonical schema validation preserved. |
| REQ | DATA-08 | Separate node/edge schemas | 02-20 | COVERED | Read-only required-table/schema validation. |
| REQ | DATA-09 | EntityResolver/ExactMatchResolver | 02-01..02-16 | COVERED | Shipped behavior unchanged. |
| REQ | RAG-06 | Background worker | 02-20 | COVERED | Worker survival after true schema fault. |
| RESEARCH | 1.1-1.7 | Go DB, proto, worker, chunking, OpenRouter, Lance schemas, resolver hooks | 02-01..02-20 | COVERED | Historical implementation plus ADR closures; no new dependency. |
| RESEARCH | 2.1-2.3 | Unit, integration, and UAT verification architecture | 02-17..02-21 | COVERED | Process, PostgreSQL, Rust durable, and Python verification tests. |
| RESEARCH | 3.1-3.2 | Transactional sqlc/pgx and tracing patterns | 02-18, 02-19 | COVERED | Reused for durable intent/reconciler. |
| RESEARCH | 4 | Existing Rust/Go dependencies | 02-17..02-21 | COVERED | No package installation task. |
| CONTEXT | D-01, D-02, D-03, D-04, D-05 | Upload, metadata, stream, corpus, transactions | 02-01..02-19 | COVERED | Existing behavior preserved; D-02/D-05 strengthened. |
| CONTEXT | D-06, D-07, D-08, D-09 | Per-document chunk settings/default/AST/size | 02-17 | COVERED | Exact strict cross-runtime contract. |
| CONTEXT | D-10, D-11, D-12, D-13, D-14, D-15, D-16 | Queue/poll/worker/shutdown/tracing | 02-19, 02-20 | COVERED | Durable gateway reconciliation and Rust liveness; historical queue behavior preserved. |
| CONTEXT | D-17, D-18, D-19, D-20 | Provider/model/retry/concurrency | 02-01..02-16 | COVERED | Shipped production behavior unchanged. |
| CONTEXT | D-21, D-22, D-23, D-24, D-25 | Lance initialization/drift/schema/replacement/token | 02-20 | COVERED | Startup path preserved; diagnostic path separated; rollback proof corrected. |
| CONTEXT | D-26, D-27, D-28, D-29, D-30 | Shared TOML/config directory/overlay/override/mount | 02-17 | COVERED | CR-01 closes Rust parity. |
| CONTEXT | D-31, D-32 | ADR authority and Phase 02 ship criticals | 02-17..02-21 | COVERED | All four critical ship findings planned. |
| CONTEXT | D-33, D-41 | Python-only verification; Node removed | 02-21 | COVERED | One canonical classifier and mandatory gate. |
| CONTEXT | D-34, D-36 | Exact behavior tests and read-only inspection | 02-20, 02-21 | COVERED | BU-03/BU-04 and actual argv capture. |
| CONTEXT | D-35 | Fail-closed verification configuration | 02-21 | COVERED | Strict TOML resolver. |
| CONTEXT | D-37 | CR-04 loopback-only guardrail | 02-17 | COVERED | Only authorized local guardrail. |
| CONTEXT | D-38, D-39, D-40, D-42 | Four accepted debt records and triggers | NONE | EXCLUDED | Explicitly deferred, documented, and non-blocking while triggers remain false. |

</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| challenge/evidence JSON → Python verifier | Structured artifacts are untrusted and may contain disclosure-bearing keys/values. |
| committed TOML → shell inspector invocation | Configuration selects which durable store is accepted as evidence. |
| shell script → spawned cargo inspector | Process argv must carry the exact intended store and document identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-21-01 | Information Disclosure | privacy validation | high | mitigate | One recursive class-only Python classifier with per-category fail-first fixtures and mandatory gate execution. |
| T-02-21-02 | Tampering | verification store selection | high | mitigate | Strict TOML parse/type/path contract, repository-root resolution, and failure before inspector spawn. |
| T-02-21-03 | Spoofing | inspector command wiring | medium | mitigate | Fake-process argv capture proves exact binary, document ID, and absolute store path. |
| T-02-21-04 | Denial of Service | missing verification runtime | low | accept | The gate returns nonzero with a setup diagnosis; Python is an explicit developer/CI prerequisite under D-41. |
</threat_model>

<verification>
- `python -O -I scripts/test_phase02_live_evidence.py`
- `bash -n verify-ingestion.sh` and `bash -n verify-live-evidence.sh`
- ADR Rust and Go exit commands
- Deferred-ledger presence and trigger audit
</verification>

<success_criteria>
- WR-01, WR-02, WR-03's in-scope argument test, and WR-04 satisfy the ADR.
- Node is removed from verification.
- All ship findings pass while all four accepted deferrals remain durable and unimplemented.
</success_criteria>

<output>
Create `.planning/phases/02-ingestion-chunking-vector-storage/02-21-SUMMARY.md` when done.
</output>
