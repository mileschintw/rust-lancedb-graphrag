---
phase: 02-ingestion-chunking-vector-storage
plan: 27
type: execute
wave: 19
depends_on:
  - "02-24"
files_modified:
  - scripts/phase02_live_evidence.py
  - scripts/test_phase02_live_evidence.py
autonomous: true
gap_closure: true
requirements:
  - DATA-01
  - DATA-03
must_haves:
  truths:
    - "Privacy diagnostics contain only the normalized prohibited class and structural locations made from trusted container labels and array indices; raw JSON keys never appear (ADR-02-003 D-06)."
    - "An inert secret-bearing key exits nonzero while stderr omits the raw key and its secret token (ADR-02-003 D-06)."
    - "The optimized live-evidence suite removes only fixtures owned by its current process through addCleanup/context ownership, and directories use shutil.rmtree (ADR-02-003 D-07)."
    - "No shared-directory fixture glob can delete another process's file or fail on a directory (ADR-02-003 D-07)."
  artifacts:
    - path: "scripts/phase02_live_evidence.py"
      provides: "Category-only, structurally safe privacy diagnostics"
    - path: "scripts/test_phase02_live_evidence.py"
      provides: "Secret-key non-disclosure and per-process fixture ownership regressions"
  key_links:
    - from: "inspect_privacy_prohibition"
      to: "ValidationError stderr"
      via: "normalized class plus trusted structural path"
      pattern: "classify_sensitive_field"
    - from: "fixture creation"
      to: "test cleanup"
      via: "per-test addCleanup/context manager ownership"
      pattern: "addCleanup|TemporaryDirectory"
---

<objective>
**As a** Lancet maintainer, **I want to** run privacy and live-evidence checks without disclosing attacker keys or deleting another process's fixtures, **so that** verification itself is non-destructive and private.

Implement ADR-02-003 D-06 and D-07 exactly.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-REVIEW.md
@.discussion/decisions/phases/02/2026-07-30-ADR-02-003-staging-lifecycle-gap-closure.md
@scripts/phase02_live_evidence.py
@scripts/test_phase02_live_evidence.py
</context>

## Artifacts this phase produces

- Category-only privacy failures with trusted structural locations.
- A subprocess regression for an attacker-controlled secret-bearing key.
- Per-process test fixture ownership with file/directory-safe cleanup.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject a secret-bearing key without echoing it</name>
  <files>scripts/phase02_live_evidence.py, scripts/test_phase02_live_evidence.py</files>
  <read_first>
    - `scripts/phase02_live_evidence.py` — classifier, recursive inspector, safe schema labels, and CLI error rendering
    - `scripts/test_phase02_live_evidence.py` — optimized subprocess helper and privacy cases
    - `ADR-02-003` D-06 — locked category-only diagnostic contract
  </read_first>
  <behavior>
    - `test_secret_bearing_key_is_rejected_without_disclosure`: submit an inert JSON key containing a unique secret token; subprocess exits nonzero, stderr contains the normalized field class, and neither the raw key nor token appears in stdout/stderr.
    - Nested objects use trusted container tokens and lists retain numeric indices, without interpolating dictionary keys.
    - Existing camel-case aliases and raw-value non-disclosure cases remain green under `python -O -I`.
  </behavior>
  <action>
Implement ADR-02-003 D-06 in the recursive privacy inspector. Separate classification from location rendering: classify each untrusted key, but build diagnostics only from the normalized prohibited class and a structural path composed of caller-supplied trusted root/container labels, a constant object-member token, and numeric array indices. Never append a raw dictionary key, redaction, or hash to the path.

Add the named subprocess regression first with an inert unique token embedded only in the key. Assert nonzero exit, the expected class, and omission of both the whole key and token from combined output. Preserve recursive checking, all locked alias classifications, optimized isolated execution, and fail-closed schema validation.
  </action>
  <verify>
    <automated>$env:PYTHONOPTIMIZE='1'; try { python -O -I scripts/test_phase02_live_evidence.py; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE} } finally { Remove-Item Env:PYTHONOPTIMIZE -ErrorAction SilentlyContinue }</automated>
  </verify>
  <acceptance_criteria>
    - Secret-bearing keys are rejected without raw-key/token disclosure.
    - Diagnostics retain normalized class and safe structural location.
    - Array indices remain locatable.
    - All existing privacy classifications remain green under optimization.
  </acceptance_criteria>
  <done>ADR-02-003 D-06 closes CR-06 with category-only diagnostics and an inert-key regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clean only fixtures owned by the current test process</name>
  <files>scripts/test_phase02_live_evidence.py</files>
  <read_first>
    - `scripts/test_phase02_live_evidence.py` — `write_json_fixtures`, `tearDownClass`, temporary path lists, and directory fixtures
    - `ADR-02-003` D-07 — exact ownership and cleanup primitives
  </read_first>
  <behavior>
    - A file matching the historical shared prefix but owned by a separate fixture context survives this suite's cleanup.
    - Current-process fixture files are registered immediately and removed through `addCleanup` or context ownership.
    - Current-process fixture directories are removed recursively with `shutil.rmtree` and do not raise an unlink error.
    - Parallel test processes cannot enumerate or delete each other's paths.
  </behavior>
  <action>
Implement ADR-02-003 D-07 by removing class-wide shared-directory sweeping. Make each helper/test register the exact file or directory it creates immediately through `self.addCleanup` or a context manager. Use file unlink only for owned files and `shutil.rmtree` for owned directories. Keep canonical challenge/evidence path guards and real-path byte-preservation assertions.

Add regressions for a foreign matching file and an owned directory. The foreign path must survive cleanup; all current-process paths must be gone even after an assertion failure. Do not enumerate a shared prefix and do not introduce a process-global cleanup registry.
  </action>
  <verify>
    <automated>$env:PYTHONOPTIMIZE='1'; try { python -O -I scripts/test_phase02_live_evidence.py; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE} } finally { Remove-Item Env:PYTHONOPTIMIZE -ErrorAction SilentlyContinue }; git diff --check -- scripts/phase02_live_evidence.py scripts/test_phase02_live_evidence.py; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}</automated>
  </verify>
  <acceptance_criteria>
    - Cleanup owns exact paths, not a shared name pattern.
    - Foreign matching fixtures survive.
    - Owned files and directories are removed with type-appropriate operations.
    - The complete optimized isolated Python suite passes.
  </acceptance_criteria>
  <done>ADR-02-003 D-07 closes WR-01 without cross-process deletion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted JSON key → diagnostic output | Attacker-controlled names can disclose secrets through stderr. |
| test process → shared filesystem | Broad cleanup can delete another process's fixtures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-27-01 | Information Disclosure | privacy diagnostics | high | mitigate | Emit normalized class and trusted structural tokens only; prove raw-key omission in subprocess output. |
| T-02-27-02 | Tampering | fixture cleanup | high | mitigate | Register exact owned paths and remove directories recursively. |
| T-02-27-03 | Denial of Service | suite teardown | medium | mitigate | Eliminate shared sweeping and prove directory cleanup cannot raise an unlink mismatch. |
</threat_model>

<verification>
- `python -O -I scripts/test_phase02_live_evidence.py` passes in full.
- The secret-bearing-key test is subprocess-based and inspects stdout/stderr.
- `git diff --check` passes.
</verification>

<success_criteria>
- CR-06 and WR-01 meet ADR-02-003 D-06/D-07.
- Python remains verification-only.
- Canonical runtime artifacts are neither touched nor disclosed by tests.
</success_criteria>

<output>
Create `.planning/phases/02-ingestion-chunking-vector-storage/02-27-SUMMARY.md` when done.
</output>
