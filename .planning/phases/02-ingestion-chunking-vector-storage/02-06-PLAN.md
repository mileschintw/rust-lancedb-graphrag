---
phase: 02-ingestion-chunking-vector-storage
plan: 06
type: execute
wave: 6
depends_on:
  - "02-05"
files_modified:
  - .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json
  - .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json
autonomous: false
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-06
  - DATA-07
  - DATA-08
  - RAG-06
user_setup:
  - service: openrouter
    why: "A real provider-backed ingestion run is the final Phase 2 evidence gate"
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "OpenRouter dashboard API keys; export it only in the user's private shell"
must_haves:
  truths:
    - "Old evidence is removed before an unpredictable challenge/run ID and issued_at are atomically written with restrictive permissions immediately before the blocking checkpoint."
    - "The only human action is one private-shell command whose production Rust engine uses OPENROUTER_API_KEY for a new real OpenRouter-backed ingestion run (D-17/D-18)."
    - "Evidence contains the exact pre-issued challenge, run ID, and issued_at, with run_started_at at or after issued_at; a continuation response alone never establishes success."
    - "The final validator rejects missing, stale, replayed, malformed, secret-bearing, or challenge-mismatched evidence and exits nonzero on every provider/model/count/store mismatch."
    - "For the recorded UUID, PostgreSQL is completed, its positive chunk_count equals the positive LanceDB node count, LanceDB has one document row and zero staged rows, embeddings are 2048-wide for the locked model, and no duplicate/stale generation remains; edge count is present and may be zero."
    - "The challenge is consumed only after exact evidence validation and direct PostgreSQL/LanceDB reinspection succeed, preventing reuse."
  artifacts:
    - path: ".planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json"
      provides: "Short-lived restrictive invocation challenge containing only schema_version, challenge, run_id, and issued_at"
    - path: ".planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json"
      provides: "Short-lived sanitized evidence from the exact challenged live run"
    - path: "verify-ingestion.sh"
      provides: "Credentialed managed live runner and atomic challenge-bound evidence writer implemented by 02-05"
    - path: "verify-live-evidence.sh"
      provides: "Non-secret gate preparation plus strict final validator implemented by 02-05"
  key_links:
    - from: ".planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json"
      to: ".planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json"
      via: "verify-ingestion.sh requires exact challenge/run ID/issued_at and records run_started_at only after reading the issued challenge"
      pattern: "(challenge|run_id|issued_at|run_started_at)"
    - from: "verify-live-evidence.sh"
      to: "engine/src/bin/inspect_lancedb.rs"
      via: "the final validator compares evidence with direct PostgreSQL and LanceDB reinspection for the recorded UUID before deleting the challenge"
      pattern: "(validate-gate|inspect_lancedb|document-id)"
---

<objective>
Execute the replay-resistant live final-pass gate for Phase 2.

Purpose: Prove that the implemented ingestion path performs a new real OpenRouter-backed run and that current PostgreSQL/LanceDB state is bound to the exact checkpoint invocation rather than old or fabricated evidence.
Output: A just-issued challenge, sanitized atomic evidence from one credentialed live run, and an exit-zero final validator result after direct store reinspection. No source implementation changes occur in this plan.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-CONTEXT.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-VERIFICATION.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-05-SUMMARY.md
@verify-ingestion.sh
@verify-live-evidence.sh
@engine/src/bin/inspect_lancedb.rs
@docker-compose.yml
@config/config.toml
@config/config.dev.toml
</context>

<phase_goal>
**As a** Lancet API user, **I want to** ingest and safely replace text or Markdown documents, **so that** the last completed LanceDB index and PostgreSQL status remain trustworthy through failures and concurrent polling.
</phase_goal>

<tasks>

<task type="auto">
  <name>Task 1: Prepare a single-use live-gate challenge immediately before the checkpoint</name>
  <files>.planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json, .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json</files>
  <read_first>
    - `verify-live-evidence.sh` — `--prepare-gate` contract implemented and tested by Plan 02-05
    - `verify-ingestion.sh` — exact challenge/evidence arguments printed by preparation
    - `docker-compose.yml` — non-secret PostgreSQL startup target
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-05-SUMMARY.md` — implementation and autonomous test results required before this gate
  </read_first>
  <action>
Run all non-credentialed preparation automatically. Invoke the committed `--prepare-gate` mode with the exact phase-local challenge and evidence paths. It must check required commands and script syntax, build/check the direct inspector, start and await PostgreSQL through Docker Compose, and verify that neither runtime JSON path is staged. Any prerequisite failure stops before checkpoint creation.

As its final operation immediately before yielding to Task 2, preparation must delete any prior evidence and prior challenge, generate `challenge` and `run_id` independently from an OS cryptographic source, record an RFC3339 UTC `issued_at`, and atomically publish a strict JSON object containing only `schema_version`, `challenge`, `run_id`, and `issued_at`. Use `umask 077`, a same-directory temporary file, and mode 600 where the platform supports it. Refuse symlinks, paths outside `.planning/phases/02-ingestion-chunking-vector-storage/`, predictable values, empty fields, or a challenge file already tracked/staged by Git. Do not print the challenge value; print only the exact private-shell wrapper command for Task 2. Neither runtime JSON file may be committed.
  </action>
  <verify>
    <automated>bash ./verify-live-evidence.sh --prepare-gate --challenge .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json --evidence .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json</automated>
  </verify>
  <acceptance_criteria>
    - Prior evidence is absent before the challenge is issued.
    - The challenge file is created only after every non-secret prerequisite passes and is the final automated operation before Task 2.
    - Strict parsing finds exactly `schema_version`, non-empty unpredictable `challenge`, non-empty unpredictable `run_id`, and parseable UTC `issued_at`.
    - File creation is atomic and restrictive (`umask 077`, plus mode 600 where supported); neither runtime path is staged or committed.
    - Preparation output contains no challenge, credential, raw document, stored chunk content, request header, or environment dump.
  </acceptance_criteria>
  <done>A single-use restrictive challenge is issued immediately before the credentialed checkpoint, and no prior evidence can satisfy it.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Run one private credential-dependent OpenRouter ingestion command</name>
  <files>.planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json</files>
  <read_first>
    - `verify-ingestion.sh` — `--managed-services`, challenge consumption, real upload, and atomic evidence contract
    - `.planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json` — machine-consumed challenge; do not paste or edit it
  </read_first>
  <action>
Ask the user to perform only the operation that requires their private OpenRouter credential. They must export `OPENROUTER_API_KEY` in their own shell and run the single wrapper command below. The wrapper inherits the key without echoing it, launches/checks the remaining managed services, uses the production Rust `OpenRouterClient`, performs a new upload after reading the pre-issued challenge, directly reconciles PostgreSQL and LanceDB, and atomically writes sanitized evidence only after success.

The live script must refuse a missing/empty credential, missing or changed challenge, service/provider failure, mock/pre-seeded-only path, or any invariant mismatch. Evidence must copy the challenge file's exact `challenge`, `run_id`, and `issued_at`; set `run_started_at` after challenge loading and require `run_started_at >= issued_at`; include `generated_at` after all assertions; and contain no key, raw/stored content, headers, environment, or secret-bearing logs. A response such as `done` is only a continuation signal to Task 3 and never establishes correctness.
  </action>
  <instructions>
    1. In a private shell at the repository root, export the real `OPENROUTER_API_KEY`.
    2. Run exactly: `bash ./verify-ingestion.sh --managed-services --challenge-file .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json --evidence .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json`.
    3. If it reports `Ingestion validation: SUCCESS`, respond `done`. Otherwise provide only sanitized failing command/error context; never paste the key, environment, raw upload, or stored content.
  </instructions>
  <verify>
    <automated>test -s .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json &amp;&amp; test -s .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json</automated>
  </verify>
  <acceptance_criteria>
    - The production Rust engine performs a new real OpenRouter embedding request under the just-issued challenge.
    - Evidence is published through a restrictive same-directory atomic rename only after the real upload and all live reconciliation checks pass.
    - Evidence carries exact challenge/run-ID/issued-at equality and ordered `issued_at <= run_started_at <= generated_at`.
    - Missing credentials/services or any provider/store mismatch leaves no passing evidence and blocks here.
    - Human response text cannot bypass or replace Task 3.
  </acceptance_criteria>
  <done>The challenged credentialed command has produced sanitized evidence for a new real OpenRouter-backed ingestion run; mechanical acceptance remains pending Task 3.</done>
  <resume-signal>Type `done` after the wrapper reports success, or provide sanitized failure context. Every continuation advances only to Task 3.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Validate exact challenge provenance and current live store state</name>
  <files>.planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json, .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json</files>
  <read_first>
    - `verify-live-evidence.sh` — strict `--validate-gate` implementation from Plan 02-05
    - `.planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json` — authoritative invocation identity and issue time
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json` — untrusted generated evidence
    - `engine/src/bin/inspect_lancedb.rs` — direct LanceDB count/model/width/uniqueness contract
  </read_first>
  <action>
Run the committed final validator against the exact challenge and evidence paths. Do not infer success from the checkpoint response or terminal prose. Strictly reject missing files, symlinks, malformed/extra schema fields, stale evidence, replayed evidence, changed challenge, challenge/run-ID/issued-at mismatch, unparseable timestamps, `run_started_at < issued_at`, `generated_at < run_started_at`, or evidence older than 30 minutes.

Require the exact sentinel, provider `openrouter`, locked model `nvidia/llama-nemotron-embed-vl-1b-v2:free` per D-17/D-18, a UUID document ID, PostgreSQL `completed` with positive chunk count, exactly one LanceDB documents row, zero staged rows, equal positive PostgreSQL/LanceDB node counts (and equal gateway count when recorded), embedding width 2048, no duplicate node/edge generation, no stale generation, and a present non-negative edge count that may be zero. Reject any API-key, raw-document, chunk-content, header, environment, or unknown secret/content field.

Using the recorded UUID, directly query live PostgreSQL and run `inspect_lancedb`; compare current status, counts, provider/model, width, uniqueness, staging, and stale-generation results with evidence. Any absent service, failed command, drift, or mismatch exits nonzero. Delete the challenge atomically only after every schema/provenance/freshness/live reinspection check succeeds so it cannot be reused; do not consume it on failure. Task 3 exit zero is mandatory before creating the summary or completing the phase.
  </action>
  <verify>
    <automated>bash ./verify-live-evidence.sh --validate-gate --challenge .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json --evidence .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json</automated>
  </verify>
  <acceptance_criteria>
    - The validator exits zero only when evidence exactly matches the just-issued challenge/run ID/timestamp and `run_started_at >= issued_at`.
    - Missing, stale, replayed, malformed, challenge-mismatched, secret-bearing, or fixture-only evidence exits nonzero.
    - Direct PostgreSQL and LanceDB reinspection for the recorded UUID agrees with evidence and all provider/model/status/count/width/uniqueness/staging invariants.
    - Edge count is required and non-negative but may be zero.
    - Successful validation consumes the challenge; failed validation preserves it and cannot create a summary.
  </acceptance_criteria>
  <done>The exact challenged live run and current PostgreSQL/LanceDB state pass mechanical validation; only this exit-zero result authorizes Plan 02-06 and Phase 2 completion.</done>
</task>

</tasks>

<threat_model>
## Threat Model Configuration

ASVS Level: 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Checkpoint issuer → user shell | A single-use challenge crosses into the credentialed operation. |
| User shell → Rust engine/OpenRouter | The private key authorizes a real external embedding request. |
| Live runner → evidence file | Runtime results cross into an untrusted continuation artifact. |
| Validator → PostgreSQL/LanceDB | Recorded identity is checked against current durable state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06-01 | Spoofing | checkpoint evidence provenance | high | mitigate | Issue an unpredictable challenge immediately before the checkpoint; require exact challenge/run-ID/issued-at equality and ordered timestamps. |
| T-02-06-02 | Tampering | live evidence artifact | high | mitigate | Delete old evidence, use strict schemas/restrictive atomic writes, reject replays/mismatches, and directly re-inspect PostgreSQL/LanceDB. |
| T-02-06-03 | Information Disclosure | OpenRouter credential and document content | high | mitigate | Keep the key in the user's private environment; exclude credentials/content/headers/environment from evidence, logs, summaries, and commits. |
| T-02-06-04 | Repudiation | checkpoint continuation text | medium | mitigate | Treat human response only as continuation; require Task 3 process exit zero before summary/phase completion. |
| T-02-06-05 | Denial of Service | unavailable live dependency | low | accept | Missing PostgreSQL, LanceDB, engine, gateway, or OpenRouter blocks the gate visibly and preserves the challenge for retry. |
</threat_model>

## Verification-gap coverage audit

| Source | Gap | Plan task | Status | Evidence required |
|--------|-----|-----------|--------|-------------------|
| VERIFICATION behavior-unverified | Live gateway polling updates PostgreSQL | Tasks 2-3 | COVERED | New challenged run plus direct PostgreSQL reinspection |
| VERIFICATION behavior-unverified | No direct persisted-store evidence or live provider proof | Tasks 2-3 | COVERED | Real OpenRouter request, direct LanceDB inspection, and exact challenge-bound evidence |
| Checker evidence_provenance | Evidence could predate checkpoint invocation | Tasks 1-3 | COVERED | Old-evidence deletion, unpredictable pre-issued challenge, exact match, ordered timestamps, consume-on-success |
| Checker checkpoint_semantics | Human setup exceeded private credential action | Tasks 1-2 | COVERED | Automated non-secret preparation and one credentialed wrapper command |

## Multi-source coverage audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Ingest text/markdown, chunk, and store in LanceDB | 02-01–02-06 | COVERED | This plan supplies the final live proof for the shipped path. |
| REQ | DATA-01, DATA-02, DATA-03 | Upload, chunk, and persist metadata/vectors | 02-01–02-06 | COVERED | Tasks 2-3 prove a new real run and matching durable counts. |
| REQ | DATA-06, DATA-07, DATA-08 | Placeholder/null schema integrity | 02-03, 02-05, 02-06 | COVERED | Plan 02-05 tests schema; Task 3 re-inspects the persisted generation. |
| REQ | DATA-09 | ExactMatchResolver behavior | 02-03 | COVERED | Passed verification and is outside this live gate. |
| REQ | RAG-06 | Async background worker | 02-01, 02-02, 02-04, 02-05, 02-06 | COVERED | Task 2 exercises the production worker through terminal completion. |
| RESEARCH | §2 | Unit, integration, and live UAT validation | 02-05, 02-06 | COVERED | Plan 02-05 builds tooling; this plan executes challenge-bound live validation. |
| CONTEXT | D-01–D-16 | Upload, metadata, chunking, queue, polling, worker contracts | 02-01–02-05, 02-06 | COVERED | Tasks 2-3 exercise the integrated contracts without changing them. |
| CONTEXT | D-17–D-20 | OpenRouter-only locked model/dimension/retry/concurrency | 02-03, 02-04, 02-05, 02-06 | COVERED | Tasks 2-3 require real provider/model identity and 2048 persisted width. |
| CONTEXT | D-21–D-30 | LanceDB schemas/replacement/configuration | 02-03–02-05, 02-06 | COVERED | Task 3 directly checks current canonical/staging state under existing configuration. |

<verification>
- `bash ./verify-live-evidence.sh --prepare-gate --challenge .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json --evidence .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json`
- In the user's private credentialed shell: `bash ./verify-ingestion.sh --managed-services --challenge-file .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json --evidence .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json`
- `bash ./verify-live-evidence.sh --validate-gate --challenge .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json --evidence .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json`
- Confirm the final command exits zero and the challenge file no longer exists; no summary or phase completion is allowed otherwise.
</verification>

<success_criteria>
- Old evidence is removed and an unpredictable restrictive challenge is issued immediately before the checkpoint.
- A new production OpenRouter-backed ingestion run starts no earlier than challenge issuance and atomically writes sanitized exact-match evidence.
- The final validator rejects stale/replayed/malformed/mismatched evidence and directly confirms current PostgreSQL/LanceDB state for the recorded UUID.
- PostgreSQL completed count equals the positive LanceDB node count, LanceDB contains one canonical document and zero staged rows, the model/provider and 2048 width are locked, no duplicate/stale generation exists, and edge count is present but may be zero.
- The challenge is consumed only after exit-zero validation; no human response, missing dependency, or old artifact can complete the plan.
- No credential, raw document, stored chunk content, request header, or environment dump appears in evidence, logs, summary, staged files, or commits.
</success_criteria>

<output>
Create `.planning/phases/02-ingestion-chunking-vector-storage/02-06-SUMMARY.md` only after Task 3 exits zero. Record the challenge-bound provenance result and sanitized provider/model/status/count/width facts, never the challenge value, API key, raw content, headers, or environment. After summary capture, remove `02-LIVE-EVIDENCE.json`; the validator has already consumed `.02-LIVE-CHALLENGE.json`. Never stage or commit either runtime JSON file.
</output>
