---
phase: 02-ingestion-chunking-vector-storage
plan: 09
type: execute
wave: 8
depends_on:
  - "02-08"
files_modified:
  - .gitignore
  - verify-ingestion.sh
  - verify-live-evidence.sh
  - scripts/phase02_live_evidence.py
  - scripts/test_phase02_live_evidence.py
autonomous: true
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-06
  - DATA-07
  - DATA-08
  - DATA-09
  - RAG-06
must_haves:
  truths:
    - "Challenge, evidence, durable-inspection, freshness, model, count, and cross-store validation use explicit fail-closed conditions invoked with Python isolated mode; optimized execution cannot elide a security decision."
    - "Negative self-tests run with PYTHONOPTIMIZE=1 and optimized isolated Python, and every malformed, replayed, stale, secret-bearing, mismatched, or store-inconsistent case exits nonzero."
    - "verify-ingestion.sh records duplicate/stale/continuity/provider/model facts only from the Plan 02-08 inspector; it does not hardcode a passing duplicate-generation value."
    - "The exact challenge and evidence paths are Git-ignored, never staged, and both are removed only after successful mechanical validation and direct PostgreSQL/LanceDB reconciliation."
    - "Bare approved/done text is never an acceptance signal; only the final validator's exit-zero result after current durable-store reinspection passes."
    - "The written final gate still requires one real production OpenRouter ingestion and direct PostgreSQL/LanceDB count, model, width, generation, and continuity reconciliation (D-17, D-18, D-19, D-20, D-21, D-24)."
  artifacts:
    - path: "scripts/phase02_live_evidence.py"
      provides: "Explicit fail-closed challenge/evidence/cross-store validation with no assert-based decisions"
    - path: "scripts/test_phase02_live_evidence.py"
      provides: "Malformed/replay/stale/secret/mismatch/optimization negative tests"
    - path: "verify-ingestion.sh"
      provides: "Isolated validator invocation and inspector-derived evidence generation"
    - path: "verify-live-evidence.sh"
      provides: "Isolated final validation, current store reconciliation, and success-only cleanup of both runtime artifacts"
    - path: ".gitignore"
      provides: "Exact ignore rules for the two private Phase 02 JSON artifacts"
  key_links:
    - from: "verify-ingestion.sh"
      to: "scripts/phase02_live_evidence.py"
      via: "python -I subcommands validate the issued challenge and assemble evidence from inspector/PostgreSQL/gateway inputs"
      pattern: "python.*-I.*phase02_live_evidence"
    - from: "verify-live-evidence.sh"
      to: "engine/src/bin/inspect_lancedb.rs"
      via: "final helper validation compares evidence with fresh PostgreSQL and inspector JSON before cleanup"
      pattern: "(inspect_lancedb|validate-live-state)"
    - from: ".gitignore"
      to: "runtime artifacts"
      via: "two literal phase-local ignore entries checked by git check-ignore"
      pattern: "02-LIVE"
  prohibitions:
    - requirement_id: DATA-01
      category: privacy
      status: unresolved
      verification: test
      flagged: true
      flagged_unverified: true
      statement: "MUST NOT persist or emit the OpenRouter credential, authorization headers, raw uploaded bytes, stored document text, or stored chunk content in challenge/evidence artifacts, logs, summaries, staged files, or commits."
---

<objective>
Make the Phase 02 live gate fail closed under Python optimization, consume only durable inspector facts, and clean both private runtime artifacts.

Purpose: The already-written real OpenRouter and PostgreSQL/LanceDB gate must remain trustworthy in every supported Python mode and leave no stageable private evidence behind.
Output: Explicit isolated-mode validation, optimization-resistant negative tests, inspector-derived evidence, exact ignore rules, and success-only cleanup of challenge plus evidence.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-CONTEXT.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-VERIFICATION.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-08-SUMMARY.md
@.planning/phases/02-ingestion-chunking-vector-storage/COVERAGE.md
@verify-ingestion.sh
@verify-live-evidence.sh
@engine/src/bin/inspect_lancedb.rs
@.gitignore
</context>

<phase_goal>
**As a** Lancet API user, **I want to** ingest and safely replace text or Markdown documents, **so that** the last completed LanceDB index and PostgreSQL status remain trustworthy through failures and concurrent polling.
</phase_goal>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make one malformed-evidence path fail closed under optimized isolated Python</name>
  <files>scripts/phase02_live_evidence.py, scripts/test_phase02_live_evidence.py, verify-live-evidence.sh</files>
  <read_first>
    - `verify-live-evidence.sh` — `parse_and_validate_gate`, inline Python assertions, `--self-test`, and success cleanup
    - `verify-ingestion.sh` — inline challenge and evidence assertions
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-VERIFICATION.md` — Python optimization bypass gap
    - `engine/src/bin/inspect_lancedb.rs` — Plan 02-08 derived JSON contract
  </read_first>
  <behavior>
    - A challenge/evidence pair with a wrong model exits nonzero through an explicit validation exception/return path when run with optimization enabled.
    - The helper contains no Python `assert` statement for schema, provenance, freshness, model, count, generation, continuity, privacy, or store comparisons.
    - Shell invokes the helper as `python -I` for normal validation; the negative test also runs with `PYTHONOPTIMIZE=1` and `python -O -I`.
    - Failure preserves both diagnostic runtime artifacts; no continuation text can convert failure to success.
  </behavior>
  <action>
Create `scripts/phase02_live_evidence.py` using only the Python standard library. Give it explicit subcommands for strict challenge parsing, evidence construction, gate validation, and live-state comparison. Implement a small `ValidationError` plus `require(condition, message)` that raises on false and a top-level nonzero exit handler; do not use Python assertions for acceptance logic.

Create `scripts/test_phase02_live_evidence.py` with temporary sanitized fixtures. Lead with a wrong-model test that invokes the helper in an optimized isolated subprocess and expects nonzero. Wire `verify-live-evidence.sh` to call the helper with isolated mode for that one gate path. Preserve challenge/evidence files on failure. The helper must reject unknown fields and secret/content-shaped fields without echoing their values.
  </action>
  <verify>
    <automated>$env:PYTHONOPTIMIZE='1'; try { python -O -I scripts/test_phase02_live_evidence.py; if ($LASTEXITCODE -ne 0) { throw "optimized isolated negative test failed with exit code $LASTEXITCODE" } } finally { Remove-Item Env:PYTHONOPTIMIZE -ErrorAction SilentlyContinue }; bash -n verify-live-evidence.sh; if ($LASTEXITCODE -ne 0) { throw "verify-live-evidence.sh syntax failed with exit code $LASTEXITCODE" }</automated>
  </verify>
  <acceptance_criteria>
    - The wrong-model fixture cannot pass when Python optimization is enabled.
    - Normal validation runs in isolated mode and uses explicit checks.
    - The test asserts optimization is active and still observes the expected nonzero rejection.
    - No failure path deletes challenge or evidence.
  </acceptance_criteria>
  <done>One representative malformed live-evidence path is optimization-resistant, isolated, fail closed, and non-destructive on failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand fail-closed validation, durable facts, exact ignores, and success cleanup</name>
  <files>.gitignore, verify-ingestion.sh, verify-live-evidence.sh, scripts/phase02_live_evidence.py, scripts/test_phase02_live_evidence.py</files>
  <read_first>
    - `scripts/phase02_live_evidence.py` — tracer validation contract
    - `scripts/test_phase02_live_evidence.py` — optimized subprocess fixture pattern
    - `verify-ingestion.sh` — challenge parsing, document UUID check, evidence assembly, and hardcoded duplicate verdict
    - `verify-live-evidence.sh` — prepare/validate/self-test modes and current cleanup behavior
    - `engine/src/bin/inspect_lancedb.rs` — Plan 02-08 provider/model/generation/duplicate/stale/continuity output
    - `.gitignore` — existing verification-store entries and missing exact runtime JSON rules
  </read_first>
  <behavior>
    - Strict schemas, UUIDs, timestamps, freshness, sentinel, provider/model, counts, width, generation, continuity, privacy, and current-store equality all use explicit validation.
    - Negative fixtures cover missing/extra fields, bad UUID/timestamp, replay/challenge mismatch, stale/future evidence, wrong provider/model/width/counts, duplicate/stale generation, broken continuity, unknown secret/content fields, and current PostgreSQL/LanceDB drift.
    - `verify-ingestion.sh` copies `duplicate_generation`, `stale_generation`, `chunk_indexes_contiguous`, provider, and model from inspector JSON and rejects any non-passing derived fact.
    - `.gitignore` contains the two literal phase-local runtime paths; `git check-ignore` succeeds for each.
    - Final validation re-queries PostgreSQL and re-runs the inspector, then removes both challenge and evidence only after every check exits successfully.
    - A failing validation preserves both files and returns nonzero.
  </behavior>
  <action>
Move all remaining inline Python acceptance logic from both shell scripts into explicit helper subcommands invoked with `python -I`. Evidence construction must accept the inspector JSON as untrusted input, require its derived provider/model/generation/duplicate/stale/continuity facts, and serialize those exact values; remove the hardcoded duplicate-generation success value. Final live comparison must require current PostgreSQL status/count and every inspector identity/count/generation/continuity field to equal the evidence.

Expand optimized negative tests for every structured failure class. Include a success-cleanup integration fixture and a failure-preservation fixture using temporary files; tests must not touch real credentials, live stores, or the canonical runtime artifacts. Add these exact `.gitignore` entries, without a broader `.planning` glob:
`.planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json`
`.planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json`

In `--validate-gate`, remove both exact runtime files only after helper validation, PostgreSQL query, fresh inspector execution, and helper live comparison all return zero. Preserve both on any error. Keep `--prepare-gate` deleting stale prior artifacts before issuing a new restrictive challenge. Keep the final written gate requiring the real `OPENROUTER_API_KEY` in a private shell, the production OpenRouter client, and direct PostgreSQL/LanceDB reconciliation; do not accept `approved`, `done`, a sentinel alone, or a summary as completion.
  </action>
  <verify>
    <automated>$env:PYTHONOPTIMIZE='1'; try { python -O -I scripts/test_phase02_live_evidence.py; if ($LASTEXITCODE -ne 0) { throw "optimized fail-closed suite failed with exit code $LASTEXITCODE" } } finally { Remove-Item Env:PYTHONOPTIMIZE -ErrorAction SilentlyContinue }; bash -n verify-ingestion.sh; if ($LASTEXITCODE -ne 0) { throw "verify-ingestion.sh syntax failed with exit code $LASTEXITCODE" }; bash -n verify-live-evidence.sh; if ($LASTEXITCODE -ne 0) { throw "verify-live-evidence.sh syntax failed with exit code $LASTEXITCODE" }; git check-ignore -q -- .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json; if ($LASTEXITCODE -ne 0) { throw "challenge path is not ignored" }; git check-ignore -q -- .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json; if ($LASTEXITCODE -ne 0) { throw "evidence path is not ignored" }</automated>
  </verify>
  <acceptance_criteria>
    - No security or integrity decision in either shell script depends on a Python assertion.
    - All negative cases remain rejected under optimized isolated execution.
    - Evidence duplicate/stale/continuity/provider/model facts originate from inspector rows.
    - Both exact runtime paths are ignored and successful validation removes both; failure removes neither.
    - Tests inspect only sanitized fixture data and preserve process exit codes.
  </acceptance_criteria>
  <done>The live gate is optimization-resistant, durable-fact-derived, replay-resistant, private by default, and cleans both runtime artifacts only after mechanical success.</done>
</task>

</tasks>

<threat_model>
## Threat Model Configuration

ASVS Level: 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime JSON → Python validator | Untrusted challenge/evidence enters acceptance logic. |
| Inspector/PostgreSQL → evidence gate | Current durable-state facts enter final reconciliation. |
| Private shell → OpenRouter | A credential authorizes the required real provider request. |
| Runtime artifacts → Git index | Short-lived verification files could otherwise be staged or disclosed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09-01 | Tampering | optimized Python validation | high | mitigate | Replace assertions with explicit checks and run negative suites under optimized isolated Python. |
| T-02-09-02 | Spoofing | hardcoded inspector verdicts in evidence | high | mitigate | Copy and revalidate only row-derived provider/model/generation/duplicate/stale/continuity facts. |
| T-02-09-03 | Information Disclosure | challenge/evidence files | high | mitigate | Exact Git ignores, restrictive atomic writes, secret-field rejection, and success-only removal of both artifacts. |
| T-02-09-04 | Repudiation | human continuation text | medium | mitigate | Require final validator exit zero after direct current-store reinspection. |
| T-02-09-SC | Tampering | package installs | high | mitigate | No package install is planned; use only existing Rust dependencies and the Python standard library. |
</threat_model>

## Artifacts this phase produces

- `scripts/phase02_live_evidence.py`: fail-closed isolated validation helper.
- `scripts/test_phase02_live_evidence.py`: optimized negative and cleanup tests.
- `verify-ingestion.sh`: inspector-derived evidence writer retaining the real OpenRouter gate.
- `verify-live-evidence.sh`: strict final validator and success-only dual-artifact cleanup.
- `.gitignore`: exact private runtime-artifact rules.

## Prohibition recall result

The adversarial recall/precision pass kept one bespoke privacy prohibition: runtime proof must never expose credentials or raw/stored content. It is authored descriptor-less and `flagged_unverified` in `must_haves.prohibitions`, so downstream verification cannot silently green it without a separately wired fail-first mechanical descriptor. Routine correctness negatives remain truths/tests, not fabricated values prohibitions.

## Spec-less probe disposition

All 16 unresolved edge-probe rows remain accounted for across the gap plan set: DATA-03 idempotency/concurrency, DATA-06 empty, and DATA-07 empty/idempotency are executable truths in 02-07; the other eleven remain explicit `flagged_assumptions` in 02-07. No unresolved row is silently dropped.

## Warning disposition

- WR-01 is implemented in 02-07 Task 3.
- WR-02 remains explicitly flagged because D-15 locks pending-job discard and no source specifies restart reconciliation semantics.

## Verification-gap coverage audit

| Structured gap | Plan/task | Status |
|----------------|-----------|--------|
| Locked 10-second timeout | 02-08 Task 1 | COVERED |
| Every real replacement error rolls back; retry converges | 02-07 Tasks 1-2 | COVERED |
| Optimizable assertions and isolated fail-closed validation | 02-09 Tasks 1-2 | COVERED |
| Durable provider/model/stale/duplicate/continuity facts | 02-08 Task 2, 02-09 Task 2 | COVERED |
| Exact ignores and cleanup of both private artifacts | 02-09 Task 2 | COVERED |
| Nullable node summary persisted as Arrow null | 02-07 Task 2 | COVERED |
| Canceled-request compensation warning | 02-07 Task 3 | COVERED |
| Pending restart recovery warning | 02-07 flagged assumption | EXPLICITLY UNRESOLVED — D-15 contract missing |

## Multi-source coverage audit

| SOURCE | ID | Feature/Requirement | Plans | Status | Notes |
|--------|----|---------------------|-------|--------|-------|
| GOAL | — | Ingest text/markdown, chunk, and store in LanceDB | 02-01–02-09 | COVERED | 02-07–02-09 close only current verification gaps without reopening the shipped architecture. |
| REQ | DATA-01 | Lightweight ingestion and trustworthy metadata | 02-07, 02-09 | COVERED | Canceled compensation and fail-closed live proof. |
| REQ | DATA-02 | Fixed/structure-aware chunking | prior plans, 02-09 gate | COVERED | No implementation gap; live gate remains intact. |
| REQ | DATA-03 | Durable LanceDB chunks/metadata | 02-07, 02-08, 02-09 | COVERED | Rollback/retry and derived durable inspection. |
| REQ | DATA-06 | Community placeholders | 02-07 regression | COVERED | Empty/idempotent initialization retained. |
| REQ | DATA-07 | Nullable node summary fields | 02-07 | COVERED | Persisted Arrow-null assertion. |
| REQ | DATA-08 | Separate node/edge tables | 02-07, 02-08 | COVERED | Replacement and edge-integrity inspection. |
| REQ | DATA-09 | ExactMatchResolver | prior 02-03, regression gate | COVERED | No current gap; explicit probe remainder retained. |
| REQ | RAG-06 | Async worker | 02-07, 02-09 | COVERED | Single-consumer behavior retained and live path revalidated. |
| RESEARCH | §1.1–1.7 | Gateway, worker, OpenRouter, schemas, null placeholders | 02-07–02-09 | COVERED | Gap plans follow established dependencies and schemas; no new package. |
| RESEARCH | §2 | Unit/integration/live UAT | 02-07–02-09 | COVERED | Boundary, timeout, durable-inspection, optimization, and final live gates are executable. |
| CONTEXT | D-01–D-05 | Multipart/metadata/streaming/global/transactions | prior plans, 02-07 | COVERED | 02-07 preserves transactions and truthful compensation. |
| CONTEXT | D-06–D-09 | Chunk strategy and 500/50 defaults | prior 02-02/02-05 | COVERED | Verified and unchanged. |
| CONTEXT | D-10–D-16 | Queue, Go-only PostgreSQL, polling, single worker, shutdown, tracing | 02-07 | COVERED | Single writer and D-15 discard remain authoritative. |
| CONTEXT | D-17–D-20 | OpenRouter-only model, 10-second retry, concurrency | 02-08, 02-09 | COVERED | Timeout is corrected and live provider gate preserved. |
| CONTEXT | D-21–D-25 | LanceDB schema/replacement/token metadata | 02-07–02-09 | COVERED | Rollback, null persistence, and durable inspection close the gaps. |
| CONTEXT | D-26–D-30 | Shared configuration | prior plans, 02-09 live gate | COVERED | Existing verification overlay and config rules remain unchanged. |
| COVERAGE | OpenRouter | All integrated/opt-out API capabilities | 02-08, 02-09 | COVERED | Existing COVERAGE.md remains sufficient; no new capability or opt-out is introduced. |

<verification>
- `cargo test --manifest-path engine/Cargo.toml`
- `Push-Location gateway; try { go test ./...; if ($LASTEXITCODE -ne 0) { throw "gateway tests failed with exit code $LASTEXITCODE" } } finally { Pop-Location }`
- `$env:PYTHONOPTIMIZE='1'; try { python -O -I scripts/test_phase02_live_evidence.py; if ($LASTEXITCODE -ne 0) { throw "optimized live-evidence suite failed with exit code $LASTEXITCODE" } } finally { Remove-Item Env:PYTHONOPTIMIZE -ErrorAction SilentlyContinue }`
- `bash -n verify-ingestion.sh`
- `bash -n verify-live-evidence.sh`
- `git check-ignore -q -- .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json`
- `git check-ignore -q -- .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json`
- Final written provider gate: prepare a fresh challenge, perform one private production OpenRouter ingestion, and accept only `verify-live-evidence.sh --validate-gate` exit zero after direct PostgreSQL/LanceDB reconciliation. Remove both runtime JSON artifacts afterward and never commit them.
</verification>

<success_criteria>
- Python optimization and continuation prose cannot bypass any live-gate decision.
- Evidence identity/generation facts are derived from current durable rows.
- Exact runtime paths are ignored, and successful validation removes both private artifacts while failure preserves them.
- The real OpenRouter and direct PostgreSQL/LanceDB reconciliation gate remains a written mandatory final check.
- The privacy prohibition remains visible and unverified until a fail-first descriptor is separately wired; it is never silently treated as passed.
</success_criteria>

<output>
Create `.planning/phases/02-ingestion-chunking-vector-storage/02-09-SUMMARY.md` only after deterministic tests pass. Phase completion still requires a fresh real OpenRouter run and final validator exit zero; bare approval or done text is insufficient. Runtime challenge/evidence files must be absent and uncommitted after successful validation.
</output>
