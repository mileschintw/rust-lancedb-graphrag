---
phase: 02-ingestion-chunking-vector-storage
plan: 16
type: execute
wave: 13
depends_on:
  - "02-11"
  - "02-12"
  - "02-13"
  - "02-14"
  - "02-15"
files_modified:
  - scripts/test_phase02_live_evidence.py
  - verify-ingestion.sh
  - verify-live-evidence.sh
  - .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json
  - .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json
autonomous: false
gap_closure: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-06
  - DATA-07
  - DATA-08
  - DATA-09
  - RAG-06
user_setup:
  - service: openrouter
    why: "A fresh provider-backed run after every deterministic Phase 02 blocker closes is required for authoritative final evidence"
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "OpenRouter dashboard API keys; export only in the user's private shell and never paste it into the session"
must_haves:
  truths:
    - "Plans 02-11 through 02-15 are complete and their deterministic Go, Rust, Python, Bash, Git, Clippy, and prohibition checks are green before a new challenge is issued."
    - "The challenge is fresh, bounded by the maximum challenge/run window, single-use, exact-ignored, untracked, unstaged, and created after all deterministic changes."
    - "One new production ingestion uses OpenRouter, the locked model, 2048 dimensions, ten-second call timeout, three retries after the initial request, 1/2/4-second backoff, and concurrency cap five (D-17, D-18, D-19, D-20)."
    - "The recorded UUID converges across HTTP, PostgreSQL, engine status, and explicit-path LanceDB inspection with matching identities and no ambiguous-admission divergence (D-02, D-10, D-13, D-21, D-24)."
    - "The final inspector rejects corrupt embedding children and verifies one canonical generation, zero staging rows, positive contiguous nodes, locked model/provider, and referentially valid edges."
    - "Only validator exit zero after direct current-store reinspection removes both runtime artifacts; continuation text, a prior run, or a summary cannot authorize completion."
    - "The structured privacy prohibition is machine-enforced and a human reviews the fresh run's nondeterministic credential/log/content surfaces."
  artifacts:
    - path: "verify-ingestion.sh"
      provides: "Caller-safe credentialed production runner with explicit verification LanceDB selection"
    - path: "verify-live-evidence.sh"
      provides: "Explicit-path current-store validator with privacy-before-cleanup ordering"
    - path: "scripts/test_phase02_live_evidence.py"
      provides: "Config-less script-wiring regressions for both explicit inspector calls"
    - path: "engine/src/bin/inspect_lancedb.rs"
      provides: "Config-independent fail-closed durable inspector"
    - path: "scripts/test_phase02_privacy_prohibition.cjs"
      provides: "Machine-wired structured privacy enforcement"
  key_links:
    - from: "02-11 through 02-15 summaries"
      to: "challenge issuance"
      via: "preflight refuses issuance until every deterministic closure result exists and passes"
      pattern: "02-(11|12|13|14|15)-SUMMARY"
    - from: "challenge"
      to: "credentialed ingestion evidence"
      via: "exact challenge/run identity and bounded ordered timestamps"
      pattern: "(challenge|run_id|issued_at|run_started_at|generated_at)"
    - from: "verify-live-evidence.sh"
      to: "PostgreSQL and explicit-path inspector"
      via: "direct current-state comparison against the resolved verification LanceDB path before cleanup"
      pattern: "(compare-live-state|--lancedb-path)"
    - from: "verify-ingestion.sh"
      to: "inspect_lancedb --lancedb-path"
      via: "credentialed runner forwards the resolved verification LanceDB path explicitly"
      pattern: "--lancedb-path.*verification_lancedb_path"
    - from: "verify-live-evidence.sh"
      to: "inspect_lancedb --lancedb-path"
      via: "final validator forwards the same resolved verification LanceDB path explicitly"
      pattern: "--lancedb-path.*verification_lancedb_path"
    - from: "scripts/test_phase02_live_evidence.py"
      to: "verify-ingestion.sh and verify-live-evidence.sh"
      via: "config-less harness proves both inspector invocations receive the exact explicit path"
      pattern: "(verify-ingestion|verify-live-evidence).*lancedb-path"
  prohibitions:
    - requirement_id: DATA-01
      category: privacy
      status: resolved
      verification: test
      statement: "MUST NOT accept credential, authorization-header, raw-upload, stored-document-text, or stored-chunk-content fields in Phase 02 challenge/evidence JSON."
      check_kind: node-test
      check_target: scripts/test_phase02_privacy_prohibition.cjs
      check_violation_fixture: scripts/fixtures/phase02_privacy_violation.json
      check_clean_fixture: scripts/fixtures/phase02_privacy_clean.json
    - requirement_id: DATA-01
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT expose credentials, authorization headers, raw upload bytes, stored document text, or stored chunk content through runtime/service logs, summaries, staged files, or commits."
---

<objective>
Run the authoritative post-closure OpenRouter ingestion and accept Phase 02 only after current-store convergence, privacy review, and success-only cleanup.

Purpose: Deterministic fixes prove individual failure modes; this dependent plan proves the integrated production path after all of them.
Output: Explicit-path live-script wiring, one fresh credentialed run, sanitized challenge-bound evidence, direct PostgreSQL/LanceDB reconciliation, human disclosure review, and removal of both private artifacts.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-CONTEXT.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-VERIFICATION.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-REVIEW.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-11-SUMMARY.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-12-SUMMARY.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-13-SUMMARY.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-14-SUMMARY.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-15-SUMMARY.md
@verify-ingestion.sh
@verify-live-evidence.sh
@scripts/phase02_live_evidence.py
@scripts/test_phase02_privacy_prohibition.cjs
@engine/src/bin/inspect_lancedb.rs
@config/config.verify.toml
</context>

<phase_goal>
**As a** Lancet API user, **I want to** ingest and safely replace text or Markdown documents, **so that** the last completed LanceDB index and PostgreSQL status remain trustworthy through failures and concurrent polling.
</phase_goal>

<tasks>

<task type="tracer">
  <name>Task 1: Prove deterministic closure and issue one bounded fresh challenge</name>
  <files>scripts/test_phase02_live_evidence.py, verify-ingestion.sh, verify-live-evidence.sh, .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json, .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json</files>
  <read_first>
    - `02-11-SUMMARY.md` — lost-ack, eventual reconciliation, NotFound repair, and identity results
    - `02-12-SUMMARY.md` — stale challenge, bounded run, caller input, and ignore results
    - `02-13-SUMMARY.md` — explicit inspector path and embedding-child results
    - `02-14-SUMMARY.md` — rollback-safe schema lookup and all-target Clippy results
    - `02-15-SUMMARY.md` — machine-wired privacy check and human review result
    - `config/config.verify.toml` — authoritative verification `engine.lancedb_path`, resolved relative to repository root
    - `verify-ingestion.sh` — current credentialed inspector invocation
    - `verify-live-evidence.sh` — `--prepare-gate` contract
    - `scripts/test_phase02_live_evidence.py` — optimized isolated shell-wiring test conventions
  </read_first>
  <action>
First wire both live scripts to the same explicit verification store. Resolve `engine.lancedb_path` from `config/config.verify.toml` relative to the repository root into `verification_lancedb_path`, validate that the resolved directory exists, and pass `--lancedb-path "$verification_lancedb_path"` on both `inspect_lancedb` invocations. The inspector process must receive its store solely through that argument; it must not evaluate repository settings or depend on its working directory. Extend the optimized isolated Python suite with a config-less harness that uses a fake `cargo` executable to capture arguments without starting services or making network calls, then prove each script forwards the exact resolved path and document UUID even when inspector settings lookup would fail.

Require non-empty 02-11 through 02-15 summaries, including completion of Plan 02-15's blocking-human privacy checkpoint. Then run the exact deterministic closure gates before creating any runtime artifact: full gateway tests and vet; Rust format, full tests, and all-target Clippy; optimized isolated Python tests including the two explicit-path regressions; Bash syntax checks; anchored/exact Git-ignore probes; and bad-RED/clean-GREEN/ordinary-GREEN privacy enforcement. Stop immediately on a missing summary or nonzero command.

Only after every preflight result is green, invoke `verify-live-evidence.sh --prepare-gate` with the two exact phase-local paths. Preparation must remove stale artifacts, reject symlinks/tracked/staged paths, use OS randomness, write restrictive permissions atomically, and issue a current challenge governed by Plan 02-12's maximum age and complete-run window. Challenge issuance is the final command before Task 2. Do not print artifact contents, environment, credentials, headers, or document content.
  </action>
  <verify>
    <automated>$summaries=@('.planning/phases/02-ingestion-chunking-vector-storage/02-11-SUMMARY.md','.planning/phases/02-ingestion-chunking-vector-storage/02-12-SUMMARY.md','.planning/phases/02-ingestion-chunking-vector-storage/02-13-SUMMARY.md','.planning/phases/02-ingestion-chunking-vector-storage/02-14-SUMMARY.md','.planning/phases/02-ingestion-chunking-vector-storage/02-15-SUMMARY.md'); foreach ($summary in $summaries) { if (!(Test-Path -LiteralPath $summary -PathType Leaf) -or (Get-Item -LiteralPath $summary).Length -eq 0) { throw "missing deterministic closure summary: $summary" } }; Push-Location gateway; try { go test -count=1 ./...; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; go vet ./...; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } finally { Pop-Location }; cargo fmt --manifest-path engine/Cargo.toml -- --check; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; cargo test --manifest-path engine/Cargo.toml; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; cargo clippy --manifest-path engine/Cargo.toml --all-targets -- -D warnings; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $env:PYTHONOPTIMIZE='1'; try { python -O -I scripts/test_phase02_live_evidence.py; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } finally { Remove-Item Env:PYTHONOPTIMIZE -ErrorAction SilentlyContinue }; bash -n verify-ingestion.sh; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; bash -n verify-live-evidence.sh; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; git check-ignore -q --no-index -- bin/placeholder; if ($LASTEXITCODE -ne 0) { throw "root /bin rule missing" }; git check-ignore -q --no-index -- engine/src/bin/future_inspector.rs; if ($LASTEXITCODE -eq 0) { throw "nested Rust source is ignored" }; git check-ignore -q --no-index -- .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json; if ($LASTEXITCODE -ne 0) { throw "challenge path is not ignored" }; git check-ignore -q --no-index -- .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json; if ($LASTEXITCODE -ne 0) { throw "evidence path is not ignored" }; $env:GSD_PROHIB_SUBJECT='scripts/fixtures/phase02_privacy_violation.json'; node --test scripts/test_phase02_privacy_prohibition.cjs *> $null; $badExit=$LASTEXITCODE; $env:GSD_PROHIB_SUBJECT='scripts/fixtures/phase02_privacy_clean.json'; node --test scripts/test_phase02_privacy_prohibition.cjs; $cleanExit=$LASTEXITCODE; Remove-Item Env:GSD_PROHIB_SUBJECT -ErrorAction SilentlyContinue; node --test scripts/test_phase02_privacy_prohibition.cjs; $normalExit=$LASTEXITCODE; if ($badExit -eq 0 -or $cleanExit -ne 0 -or $normalExit -ne 0) { throw "privacy fail-first/control contract failed: bad=$badExit clean=$cleanExit normal=$normalExit" }; bash ./verify-live-evidence.sh --prepare-gate --challenge .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json --evidence .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json</automated>
  </verify>
  <acceptance_criteria>
    - Every deterministic blocker and warning has an exit-zero gate before challenge issuance.
    - No stale challenge/evidence survives.
    - The fresh challenge is restrictive, ignored, untracked, unstaged, and bounded.
    - No sensitive or content-bearing value appears in output.
  </acceptance_criteria>
  <done>All deterministic closure is green and a single fresh bounded challenge is ready for the credentialed production path.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Run one private OpenRouter ingestion and review its disclosure surfaces</name>
  <precondition>OPENROUTER_API_KEY is set only in the user's private shell, and Task 1's fresh challenge file exists and is non-empty.</precondition>
  <files>.planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json</files>
  <read_first>
    - `verify-ingestion.sh` — caller ownership, managed services, challenge binding, sanitized evidence, and success sentinel
    - `.planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json` — machine-consumed challenge; do not open, paste, or edit
    - `scripts/test_phase02_privacy_prohibition.cjs` — deterministic structured-artifact boundary already proven by Plan 02-15
  </read_first>
  <action>
Ask the user only to perform the credential-dependent command in a private repository-root shell. The runner must use the production gateway/engine path, D-17 through D-20 OpenRouter contract, caller-safe sample handling, matching response identities, authoritative admission convergence, and the explicit-path fail-closed inspector. It must atomically write sanitized evidence only after terminal success and direct PostgreSQL/LanceDB reconciliation.

The integrated run must exercise without weakening the locked upload/metadata/streaming/corpus/transaction contracts (D-01, D-02, D-03, D-04, D-05), chunking configuration/default/AST behavior (D-06, D-07, D-08, D-09), queue/polling/worker/shutdown/tracing behavior (D-10, D-11, D-12, D-13, D-14, D-15, D-16), provider/model/retry/concurrency behavior (D-17, D-18, D-19, D-20), LanceDB initialization/drift/schema/replacement/token behavior (D-21, D-22, D-23, D-24, D-25), and shared configuration selection/overlays/mount behavior (D-26, D-27, D-28, D-29, D-30).

After the runner reports success, require the user to review the command's terminal output and managed service logs for credential values, authorization headers, raw uploaded bytes, stored document text, or stored chunk content. They must not paste logs or sensitive values into the session. A `done` response confirms only that the private run and judgment review occurred; it cannot bypass Task 3.
  </action>
  <instructions>
    1. In a private shell at the repository root, export the real `OPENROUTER_API_KEY`.
    2. Run exactly: `bash ./verify-ingestion.sh --managed-services --challenge-file .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json --evidence .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json`.
    3. Confirm the command reports `Ingestion validation: SUCCESS`.
    4. Review terminal/service logs locally and confirm they contain no credential, authorization header, raw upload, stored document text, or stored chunk content.
    5. Respond `done` only after both success and the local disclosure review; otherwise provide sanitized error context without values.
  </instructions>
  <verify>
    <automated>test -s .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json &amp;&amp; test -s .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json</automated>
  </verify>
  <acceptance_criteria>
    - The provider request begins after this plan's challenge issuance.
    - The runner reaches a terminal production ingestion and writes sanitized evidence.
    - Human review finds no disclosure in fresh terminal/service logs.
    - No secret, header, raw/stored content, or environment dump is pasted or persisted.
  </acceptance_criteria>
  <done>One fresh post-closure provider run produced untrusted sanitized evidence and passed local nondeterministic disclosure review; mechanical acceptance remains pending.</done>
  <resume-signal>Type `done` only after the private command succeeds and the local disclosure review is clean, or provide sanitized failure context.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Reinspect current stores, validate privacy enforcement, and remove runtime artifacts</name>
  <files>verify-live-evidence.sh, .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json, .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json</files>
  <read_first>
    - `verify-live-evidence.sh` — `--validate-gate` direct comparison and cleanup ordering
    - `scripts/phase02_live_evidence.py` — bounded timestamps, strict schemas, privacy, and live-state comparison
    - `engine/src/bin/inspect_lancedb.rs` — explicit path and child-value integrity checks
    - `scripts/test_phase02_privacy_prohibition.cjs` — descriptor-backed structured privacy check
    - both transient runtime JSON paths — untrusted machine inputs; never print their contents
  </read_first>
  <action>
Before invoking any command capable of cleanup, set `GSD_PROHIB_SUBJECT` to the exact evidence path and run the descriptor-backed privacy node test. Treat Task 2 continuation text and the runner sentinel as untrusted. A privacy failure must stop immediately with both runtime artifacts untouched.

Then run the final validator against the exact runtime paths. Require strict schema/provenance, maximum challenge age, complete-run window, provider/model, counts, width, generation, continuity, response identity, and privacy validation. Using the evidence UUID, directly re-query current PostgreSQL and invoke the inspector with `--lancedb-path "$verification_lancedb_path"` from a config-independent context. Require completed PostgreSQL status; positive equal PostgreSQL/node counts; one canonical document; zero staged rows; one locked OpenRouter model/provider and generation; 2048 finite non-null embeddings; contiguous unique chunks; and unique referentially valid edges. Any failure preserves both runtime files for private diagnosis.

Inside `verify-live-evidence.sh`, keep the evidence-targeted privacy test and every current PostgreSQL/LanceDB reconciliation check before the `rm` operation. Only after every check exits zero may the validator remove both files. Capture the Bash exit code before follow-up commands. Then require both paths absent, exact-ignored, untracked, and unstaged. The summary may record only sanitized status/count/model/width/generation/continuity facts and the human review disposition.
  </action>
  <verify>
    <automated>$env:GSD_PROHIB_SUBJECT='.planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json'; try { node --test scripts/test_phase02_privacy_prohibition.cjs; if ($LASTEXITCODE -ne 0) { throw "privacy prohibition check failed before cleanup" } } finally { Remove-Item Env:GSD_PROHIB_SUBJECT -ErrorAction SilentlyContinue }; bash ./verify-live-evidence.sh --validate-gate --challenge .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json --evidence .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json; $gateExit=$LASTEXITCODE; if ($gateExit -ne 0) { throw "final live gate failed with exit code $gateExit" }; if ((Test-Path '.planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json') -or (Test-Path '.planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json')) { throw "successful validation did not remove both artifacts" }; git diff --quiet --cached -- .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json</automated>
  </verify>
  <acceptance_criteria>
    - Final validator exits zero after direct current PostgreSQL and explicit-path LanceDB reinspection.
    - Current rows satisfy identity, terminal state, count, staging, provider/model, finite width, generation, continuity, and edge-integrity invariants.
    - Machine-wired privacy check passes and Task 2 human review is recorded.
    - Success removes both runtime artifacts; failure preserves them and cannot create the summary.
  </acceptance_criteria>
  <done>Fresh provider ingestion is authoritative only after direct current-store convergence, dual privacy disposition, and success-only removal of both private artifacts.</done>
</task>

</tasks>

<threat_model>
## Threat Model Configuration

ASVS Level: 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deterministic plans → challenge issuer | A live run must not precede closure of known blockers. |
| Private shell → OpenRouter | A credential authorizes the external embedding request. |
| Challenge/evidence → validator | Untrusted transient JSON attempts to prove provenance and integrity. |
| PostgreSQL/LanceDB → final acceptance | Current durable state must match the recorded UUID. |
| Runtime/log/Git surfaces → privacy review | Sensitive data could escape structured evidence controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-16-01 | Spoofing | pre-closure/prior run | high | mitigate | Depend on 02-11 through 02-15, rerun their gates, and issue a new challenge last. |
| T-02-16-02 | Tampering | challenge/evidence | high | mitigate | Enforce strict schema, provenance, bounded timestamps, and direct current-store comparison. |
| T-02-16-03 | Information Disclosure | credential/log/content | high | mitigate | Private-shell execution, descriptor-backed JSON check, human log review, exact ignores, and success-only cleanup. |
| T-02-16-04 | Spoofing | cross-document state | high | mitigate | Require matching UUID through gateway, engine, PostgreSQL, and inspector. |
| T-02-16-05 | Tampering | corrupt embeddings | high | mitigate | Explicit-path inspector rejects null/non-finite children before acceptance. |
| T-02-16-06 | Repudiation | continuation text | medium | mitigate | Require validator exit zero and artifact absence after direct reinspection. |
| T-02-16-07 | Denial of Service | provider/local services | low | accept | Fail visibly and preserve private artifacts for bounded retry; no false acceptance occurs. |
| T-02-16-SC | Tampering | package installs | high | mitigate | No package install occurs. |
</threat_model>

## Artifacts this phase produces

- `verify-ingestion.sh`: credentialed runner that passes the resolved verification store through `--lancedb-path`.
- `verify-live-evidence.sh`: final reconciler that uses the same explicit store and performs privacy validation before cleanup.
- `scripts/test_phase02_live_evidence.py`: config-less regression coverage for both live-script inspector invocations.
- `.02-LIVE-CHALLENGE.json`: transient single-use post-closure challenge, removed after success.
- `02-LIVE-EVIDENCE.json`: transient sanitized evidence, removed after success.
- `02-16-SUMMARY.md`: sanitized authoritative result only after validator exit zero.

## Multi-source coverage audit

| SOURCE | ID | Feature/Requirement | Plans | Status | Notes |
|--------|----|---------------------|-------|--------|-------|
| GOAL | — | Ingest text/markdown, chunk, and store in LanceDB | 02-01–02-16 | COVERED | Task 2 exercises the complete production path; Task 3 reinspects current durable state. |
| REQ | DATA-01 | Upload and trustworthy metadata convergence | 02-11, 02-12, 02-15, 02-16 | COVERED | Lost ack, compensation retry, caller safety, privacy, and final live convergence. |
| REQ | DATA-02 | Structure-aware/fixed chunking | prior plans, 02-16 | COVERED | Unchanged verified implementation is exercised by the fresh run. |
| REQ | DATA-03 | LanceDB persistence and integrity | 02-13, 02-14, 02-16 | COVERED | Explicit inspection, valid embeddings, rollback-safe errors, and current-store proof. |
| REQ | DATA-06 | Community placeholder schema | prior plans, 02-13, 02-16 | COVERED | Shared canonical schema retains drift validation and is exercised by startup/live run. |
| REQ | DATA-07 | Nullable node summary fields | prior plans, 02-14, 02-16 | COVERED | Fallible schema-derived arrays preserve runtime recovery. |
| REQ | DATA-08 | Edge schema and nullable summaries | prior plans, 02-14, 02-16 | COVERED | Runtime schema lookup/rollback and edge integrity remain in the final inspection. |
| REQ | DATA-09 | EntityResolver/ExactMatchResolver | prior plans, 02-13, 02-16 | COVERED | Shared library export retains the production resolver and tests. |
| REQ | RAG-06 | Background worker | 02-11, 02-14, 02-16 | COVERED | Admission reconciliation and worker survival are deterministic before the fresh run. |
| RESEARCH | §1.1–1.7 | Gateway, worker, OpenRouter, LanceDB schemas, resolver | 02-11–02-16 | COVERED | Gap plans harden the established architecture without new packages. |
| RESEARCH | §2 | Unit, integration, and live validation | 02-11–02-16 | COVERED | Deterministic suites precede the provider-backed gate. |
| CONTEXT | D-01–D-16 | Upload, metadata, streaming, queue, polling, worker contracts | prior plans, 02-11, 02-12, 02-16 | COVERED | Locked contracts are preserved while ambiguity and ownership gaps close. |
| CONTEXT | D-17–D-20 | OpenRouter provider/model/timeout/retry/concurrency | prior plans, 02-16 | COVERED | Fresh production request uses the exact locked contract. |
| CONTEXT | D-21–D-25 | LanceDB schema, replacement, token metadata | 02-13, 02-14, 02-16 | COVERED | Shared schemas, fail-closed inspection, rollback, and durable reconciliation. |
| CONTEXT | D-26–D-30 | Shared configuration | prior plans, 02-13, 02-16 | COVERED | Explicit inspector path avoids unrelated config while production still uses the locked overlay. |
| REVIEW | WR-01 | Rollback-safe schema lookup | 02-14 | COVERED | Deterministic failure/retry test. |
| REVIEW | WR-02 | Response identity checks | 02-11 | COVERED | Ingest and status mismatches rejected. |
| REVIEW | WR-03 | Anchored ignore rule | 02-12 | COVERED | Nested Rust source no longer ignored. |
| REVIEW | WR-04 | Green all-target Clippy | 02-13, 02-14 | COVERED | Shared library removes duplicate dead code; exact lint gate is required green. |

<verification>
- All dependent summaries exist and all deterministic commands pass before challenge issuance.
- Both live scripts pass the exact verification LanceDB path explicitly, and their config-less wiring regressions pass.
- One private `verify-ingestion.sh --managed-services` run succeeds after challenge issuance.
- Human reviews fresh terminal/service logs locally without exposing values.
- Evidence-targeted privacy validation passes before `verify-live-evidence.sh --validate-gate` performs final current-store reconciliation and cleanup.
- Both runtime artifacts are absent, ignored, untracked, and unstaged after success.
</verification>

<success_criteria>
- Every Verification gap and REVIEW WR-01 through WR-04 is deterministically closed before the live request.
- The fresh challenge-bound OpenRouter run converges across gateway, engine, PostgreSQL, and the explicitly selected verification LanceDB store.
- Structured privacy is machine-proven and nondeterministic disclosure surfaces receive explicit human review.
- Privacy failure preserves diagnostics; only exit-zero final reconciliation and success-only cleanup authorize the sanitized summary.
</success_criteria>

<output>
Create `.planning/phases/02-ingestion-chunking-vector-storage/02-16-SUMMARY.md` only after Task 3 exits zero and both runtime artifacts are absent. Never include credentials, headers, environment values, raw upload, stored document text, stored chunk content, or private artifact payloads.
</output>
