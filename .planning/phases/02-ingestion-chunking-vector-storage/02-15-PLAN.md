---
phase: 02-ingestion-chunking-vector-storage
plan: 15
type: execute
wave: 12
depends_on:
  - "02-11"
  - "02-12"
  - "02-13"
  - "02-14"
files_modified:
  - scripts/test_phase02_privacy_prohibition.cjs
  - scripts/fixtures/phase02_privacy_violation.json
  - scripts/fixtures/phase02_privacy_clean.json
autonomous: false
gap_closure: true
requirements:
  - DATA-01
  - DATA-03
must_haves:
  truths:
    - "The privacy prohibition has a complete flat test-tier enforcement descriptor with a known-bad fixture and a known-clean causation control."
    - "The node test reads GSD_PROHIB_SUBJECT, fails on forbidden credential/authorization/raw/stored-content fields, and passes the clean sanitized evidence shape."
    - "The deterministic check never prints forbidden field values."
    - "Nondeterministic credential handling, terminal/service logs, summaries, staged files, and commits remain subject to explicit human review rather than being falsely declared mechanically proven."
  artifacts:
    - path: "scripts/test_phase02_privacy_prohibition.cjs"
      provides: "Node test compatible with prohibition-enforcement subject injection"
    - path: "scripts/fixtures/phase02_privacy_violation.json"
      provides: "Known-bad sanitized fixture that proves fail-first"
    - path: "scripts/fixtures/phase02_privacy_clean.json"
      provides: "Known-clean control fixture that proves content-caused failure"
  key_links:
    - from: "must_haves.prohibitions.check_target"
      to: "scripts/test_phase02_privacy_prohibition.cjs"
      via: "deterministic node-test lookup"
      pattern: "test_phase02_privacy_prohibition"
    - from: "scripts/test_phase02_privacy_prohibition.cjs"
      to: "GSD_PROHIB_SUBJECT"
      via: "known-bad/known-clean subject injection"
      pattern: "GSD_PROHIB_SUBJECT"
  prohibitions:
    - requirement_id: DATA-01
      category: privacy
      status: resolved
      verification: test
      statement: "MUST NOT accept credential, authorization-header, raw-upload, stored-document-text, or stored-chunk-content fields in Phase 02 challenge/evidence JSON."
      check_kind: node-test
      check_target: scripts/test_phase02_privacy_prohibition.cjs
      check_violation_fixture: scripts/fixtures/phase02_privacy_violation.json
      check_clean_fixture: scripts/fixtures/phase02_privacy_clean.json
    - requirement_id: DATA-01
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT expose credentials, authorization headers, raw upload bytes, stored document text, or stored chunk content through runtime/service logs, summaries, staged files, or commits."
---

<objective>
Machine-wire the deterministic privacy prohibition and preserve human review for disclosure surfaces that fixtures cannot prove.

Purpose: Verification must be able to locate, fail-first prove, and pass the structured-artifact check without overstating coverage of live credentials or logs.
Output: Node-test enforcement, known-bad/known-clean fixtures, flat descriptor projection, and a human disclosure review.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-VERIFICATION.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-REVIEW.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-09-SUMMARY.md
@.planning/phases/02-ingestion-chunking-vector-storage/02-10-SUMMARY.md
@scripts/phase02_live_evidence.py
@scripts/test_phase02_live_evidence.py
@verify-ingestion.sh
@verify-live-evidence.sh
</context>

<phase_goal>
**As a** Lancet API user, **I want to** ingest and safely replace text or Markdown documents, **so that** the last completed LanceDB index and PostgreSQL status remain trustworthy through failures and concurrent polling.
</phase_goal>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove the privacy check fails on a known violation and passes its clean control</name>
  <files>scripts/test_phase02_privacy_prohibition.cjs, scripts/fixtures/phase02_privacy_violation.json, scripts/fixtures/phase02_privacy_clean.json</files>
  <read_first>
    - `.codex/gsd-core/references/prohibition-probe.md` — flat descriptor and `GSD_PROHIB_SUBJECT` fail-first contract
    - `scripts/phase02_live_evidence.py` — forbidden structured-field classes already enforced in production validation
    - `scripts/test_phase02_live_evidence.py` — sanitized secret/content negative cases
    - `.planning/phases/02-ingestion-chunking-vector-storage/02-VERIFICATION.md` — unverified-prohibition gap
  </read_first>
  <behavior>
    - With GSD_PROHIB_SUBJECT set to the violation fixture, `node --test` exits nonzero.
    - With GSD_PROHIB_SUBJECT set to the clean fixture, `node --test` exits zero.
    - Without subject injection, the check runs against the clean fixture and exits zero.
    - The bad fixture uses inert sanitized marker values; test output names only forbidden field classes, never values.
    - The frontmatter descriptor uses flat `check_kind`, `check_target`, `check_violation_fixture`, and `check_clean_fixture` scalars.
  </behavior>
  <action>
Create a standard-library `node:test` check that reads the JSON subject named by `GSD_PROHIB_SUBJECT`, defaulting to the clean control for an ordinary run. Inspect nested keys recursively using the same credential, authorization, raw upload, stored document text, and stored chunk content field classes enforced by the Python helper. Fail without including the offending value.

Create one strict-schema clean evidence fixture containing only sanitized identity/timestamp/count/integrity fields and one known-bad fixture that differs by a forbidden field carrying an inert marker. Keep both fixtures free of real credentials, headers, uploads, or stored content. Exercise the exact producer contract manually: bad subject RED, clean subject GREEN, ordinary check GREEN. Do not claim this structured-fixture check proves arbitrary service log or commit content.
  </action>
  <verify>
    <automated>$env:GSD_PROHIB_SUBJECT='scripts/fixtures/phase02_privacy_violation.json'; node --test scripts/test_phase02_privacy_prohibition.cjs *> $null; $badExit=$LASTEXITCODE; $env:GSD_PROHIB_SUBJECT='scripts/fixtures/phase02_privacy_clean.json'; node --test scripts/test_phase02_privacy_prohibition.cjs; $cleanExit=$LASTEXITCODE; Remove-Item Env:GSD_PROHIB_SUBJECT -ErrorAction SilentlyContinue; node --test scripts/test_phase02_privacy_prohibition.cjs; $normalExit=$LASTEXITCODE; if ($badExit -eq 0 -or $cleanExit -ne 0 -or $normalExit -ne 0) { throw "privacy fail-first/control contract failed: bad=$badExit clean=$cleanExit normal=$normalExit" }</automated>
  </verify>
  <acceptance_criteria>
    - Known-bad RED and known-clean GREEN are both machine-observed.
    - The descriptor is complete and points to committed paths.
    - Fixture values are inert and sanitized.
    - The test reports no forbidden values.
  </acceptance_criteria>
  <done>The verifier can deterministically locate and machine-prove a non-vacuous structured-artifact privacy prohibition.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Review nondeterministic credential, log, content, and Git surfaces</name>
  <files>.planning/phases/02-ingestion-chunking-vector-storage/02-11-SUMMARY.md, .planning/phases/02-ingestion-chunking-vector-storage/02-12-SUMMARY.md, .planning/phases/02-ingestion-chunking-vector-storage/02-13-SUMMARY.md, .planning/phases/02-ingestion-chunking-vector-storage/02-14-SUMMARY.md</files>
  <read_first>
    - `02-11-SUMMARY.md` through `02-14-SUMMARY.md` — sanitized deterministic closure records
    - current staged diff — exact files proposed for commit
    - retained test output — field-class errors only; never private runtime payloads
  </read_first>
  <what-built>The deterministic blocker plans and machine-wired structured-artifact privacy check are complete; arbitrary runtime logs, summaries, staged files, and commits remain a judgment-tier surface.</what-built>
  <action>
Perform the explicit local review below after automation has completed. Review only sanitized summaries, diffs, file names, and test output; do not open or paste any private runtime artifact, environment value, credential, header, raw upload, or stored content. Record approval as the judgment-tier disposition while leaving the descriptor-backed test result independent and mandatory.
  </action>
  <how-to-verify>
    1. Run `git diff --cached --name-only` and confirm neither Phase 02 runtime JSON path is staged.
    2. Review the pending diffs and completed 02-11 through 02-15 summaries for credential values, authorization headers, raw uploaded bytes, stored document text, or stored chunk content.
    3. Review any retained sanitized test output; confirm errors name field classes only and no environment dump or secret/content value appears.
    4. Confirm the violation fixture contains only inert marker data and is not copied from a real provider request or uploaded document.
    5. Approve only if the nondeterministic surfaces are clean; the later credentialed Plan 02-16 must repeat this review for its fresh run.
  </how-to-verify>
  <verify>
    <automated>git diff --cached --quiet -- .planning/phases/02-ingestion-chunking-vector-storage/.02-LIVE-CHALLENGE.json .planning/phases/02-ingestion-chunking-vector-storage/02-LIVE-EVIDENCE.json</automated>
  </verify>
  <acceptance_criteria>
    - No private runtime artifact is staged.
    - Human review finds no credential/header/raw/stored-content disclosure.
    - Approval is recorded as judgment, not substituted for the machine descriptor.
  </acceptance_criteria>
  <done>Deterministic structured privacy is machine-enforced and remaining nondeterministic disclosure surfaces have an explicit human disposition.</done>
  <resume-signal>Type `approved` only after completing the five checks, or describe the disclosure concern without pasting any sensitive value.</resume-signal>
</task>

</tasks>

<threat_model>
## Threat Model Configuration

ASVS Level: 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan frontmatter → verifier | Descriptor fields determine which mechanical check is trusted. |
| Fixture path → node test | A subject must genuinely cause RED or GREEN based on content. |
| Runtime/log/Git surfaces → human reviewer | Nondeterministic disclosure cannot be fully reduced to a fixture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-15-01 | Information Disclosure | structured challenge/evidence | high | mitigate | Machine-wire recursive forbidden-field checking with bad and clean fixtures. |
| T-02-15-02 | Tampering | vacuous prohibition test | high | mitigate | Require known-bad RED and known-clean GREEN through subject injection. |
| T-02-15-03 | Information Disclosure | logs/summaries/Git | high | mitigate | Keep a separate judgment-tier prohibition and blocking human review. |
| T-02-15-04 | Repudiation | human approval | medium | mitigate | Record exact review steps; approval cannot replace the test-tier check. |
| T-02-15-SC | Tampering | package installs | high | mitigate | Use the installed Node standard library; no package install is planned. |
</threat_model>

## Artifacts this phase produces

- `scripts/test_phase02_privacy_prohibition.cjs`: deterministic prohibition-enforcement target.
- `scripts/fixtures/phase02_privacy_violation.json`: known-bad fail-first subject.
- `scripts/fixtures/phase02_privacy_clean.json`: known-clean causation control.

## Gap traceability

| Source | Finding | Task | Status |
|--------|---------|------|--------|
| VERIFICATION | Machine-wire privacy must-NOT descriptor | Task 1 | PLANNED |
| VERIFICATION | Retain human review for nondeterministic surfaces | Task 2 | PLANNED |

<verification>
- Bad fixture makes the node test exit nonzero.
- Clean fixture and ordinary invocation exit zero.
- Human review disposes nondeterministic disclosure surfaces without exposing values.
</verification>

<success_criteria>
- Verification can reconstruct the check solely from flat plan frontmatter.
- Fail-first and clean-control causation are machine-proven.
- Human review remains explicit for credential/log/content surfaces beyond deterministic JSON fixtures.
</success_criteria>

<output>
Create `.planning/phases/02-ingestion-chunking-vector-storage/02-15-SUMMARY.md` only after the human review is approved.
</output>
