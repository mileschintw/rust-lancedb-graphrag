---
phase: 06.3.2
slug: eval-harness-diagnostics-scored-dimensions-and-paired-ablati
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: validated
nyquist_compliant: false
wave_0_complete: true
created: 2026-09-04
---

# Phase 06.3.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from `06.3.2-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (existing `eval/tests/`), stdlib-only assertions — **no scipy, no numpy** |
| **Config file** | `eval/pyproject.toml` (uv project config) |
| **Quick run command** | `uv run --project eval pytest tests/test_score_deterministic.py -x` |
| **Full suite command** | `uv run --project eval pytest` |
| **Estimated runtime** | ~10s quick / ~60s full (bootstrap measured at ~0.9s per delta for 500 pairs at B=10_000; 8–9 deltas ≈ 7–8s of the full run) |

---

## Sampling Rate

- **After every task commit:** Run `uv run --project eval pytest tests/test_score_deterministic.py -x`
- **After every plan wave:** Run `uv run --project eval pytest`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 06.3.2-01-01 | 01 | 1 | OBS-05 (D-01) | T-06.3.2-03 | SSE loop captures `NodeCompletedEvent.duration_ms` + `WorkflowCompletedEvent.metadata` instead of discarding them in the bare `pass` arm | unit | `uv run --project eval pytest tests/test_wire_capture.py -k node_timing -x` | ❌ W0 | ✅ green |
| 06.3.2-01-01 | 01 | 1 | OBS-05 (D-01) | — | Journal record carries the captured wire diagnostics | unit | `uv run --project eval pytest tests/test_wire_capture.py -k wire_diagnostics -x` | ❌ W0 | ✅ green |
| 06.3.2-01-01 | 01 | 1 | OBS-05 (D-27) | T-06.3.2-01 | Journal outcome is the client's status: the failure status maps to error, the degraded status does NOT — closes the `run.py:58` hardcode without over-correcting | unit | `uv run --project eval pytest tests/test_wire_capture.py -k outcome_routing -x` | ❌ W0 | ✅ green |
| 06.3.2-01-01 | 01 | 1 | OBS-05 (D-33) | T-06.3.2-02 | Snapshot precedence tests the answer's snapshot, not the answer object; failure-path partial snapshot never overwrites populated chunks | unit | `uv run --project eval pytest tests/test_wire_capture.py -k snapshot_precedence -x` | ❌ W0 | ✅ green |
| 06.3.2-01-01 | 01 | 1 | OBS-05 (D-34) | — | A retryable node failure followed by workflow success leaves the record usable — added in the cross-AI review pass; no such vector existed | unit | `uv run --project eval pytest tests/test_usability.py -k retryable -x` | ❌ W0 | ✅ green |
| 06.3.2-01-02 | 01 | 1 | OBS-05 (D-34) | — | `is_usable()` predicate identifies infrastructure failures and is the single filter feeding retrieval AND answer denominators | unit | `uv run --project eval pytest tests/test_denominators.py tests/test_usability.py -x` | ❌ W0 | ✅ green |
| 06.3.2-01-02 | 01 | 1 | OBS-05 (D-34) | — | Caveat 6 states the real convention and the `required_caveats` list in `test_report.py` is updated in the same commit | unit | `uv run --project eval pytest tests/test_report.py::test_all_normative_caveats_present_in_markdown -x` | ✅ exists | ✅ green |
| 06.3.2-02-01 | 02 | 2 | OBS-05 (D-09) | T-06.3.2-07 | `percentile([])` raises `ValueError`, not `IndexError`; latency dimensions skip rather than impute zero — added in the cross-AI review pass | unit | `uv run --project eval pytest tests/test_path_health.py -k percentile -x` | ❌ W0 | ✅ green |
| 06.3.2-02-02 | 02 | 2 | OBS-05 (D-02, D-09) | T-06.3.2-08 | Graph-health and per-path yield/latency dimensions report their numbers; graph emptiness never gates a run as bad | unit | `uv run --project eval pytest tests/test_path_health.py -k graph -x` | ❌ W0 | ✅ green |
| 06.3.2-02-02 | 02 | 2 | OBS-05 (D-02) | T-06.3.2-08 | A `GRAPH_TIMEOUT` notice on a normally-completed graph node counts as a graph attempt — added in the cross-AI review pass | unit | `uv run --project eval pytest tests/test_path_health.py -k graph_timeout_attempt -x` | ❌ W0 | ✅ green |
| 06.3.2-02-03 | 02 | 2 | OBS-05 (D-35) | T-06.3.2-09 | `wire_contract_conformance` counts self-contradiction; an honest engine failure with a null `error_type` is NOT a violation — the keying pinned in the cross-AI review pass | unit | `uv run --project eval pytest tests/test_wire_contract.py -x` | ❌ W0 | ✅ green |
| 06.3.2-03-01 | 03 | 2 | OBS-05 (D-07) | T-06.3.2-11, T-06.3.2-28 | Raw retention is bounded by named constants; the run-level cap is a backstop that cannot bind before per-unit truncation, so retention stays scheduling-independent — pinned in the cross-AI review pass | unit | `uv run --project eval pytest tests/test_raw_events.py -k cap -x` | ❌ W0 | ✅ green |
| 06.3.2-03-03 | 03 | 2 | OBS-05 (D-11) | T-06.3.2-14 | `canary_manifest` validates the committed file and its corpus agreement with NO gateway running — split out in the cross-AI review pass | unit | `uv run --project eval pytest tests/test_preflight_canary.py -k manifest -x` | ❌ W0 | ✅ green |
| 06.3.2-03-03 | 03 | 2 | OBS-05 (D-10, D-12, D-31) | T-06.3.2-16 | `canary_floors` probes the query path behind the reachability gate and fails loud below the locked floors, with budgets read live from config | integration | `uv run --project eval lancet-eval preflight --corpus multihop_rag` | ❌ W0 | ✅ green |
| 06.3.2-04-01 | 04 | 3 | OBS-05 (D-37–D-43) | T-06.3.2-29 | Pairing join is an inner join on `question_id` where both arms are usable AND the graph-off arm proves provenance; unpaired dropped in four distinct buckets; nulls excluded | unit | `uv run --project eval pytest tests/test_pairing.py -k join -x` | ❌ W0 | ✅ green |
| 06.3.2-04-01 | 04 | 3 | OBS-05 (D-38, D-43) | T-06.3.2-17, T-06.3.2-19 | Bootstrap CI is deterministic at the locked seed with sorted-pair input; `n_pairs=0` → error status; `n_pairs=1` → degenerate marker; a pair with an absent metric score is excluded and counted, never differenced | unit | `uv run --project eval pytest tests/test_pairing.py -k bootstrap -x` | ❌ W0 | ✅ green |
| 06.3.2-04-02 | 04 | 3 | OBS-05 (D-36) | T-06.3.2-18 | **WR-02 implementation:** deduplication is keyed on question AND arm and applied once journal-wide BEFORE the arm grouping, so judging, worksheet and pairing all read a collapsed list | unit | `uv run --project eval pytest tests/test_ablation.py -k wr02 -x` | ❌ W0 | ✅ green |
| 06.3.2-04-02 | 04 | 3 | OBS-05 (D-37) | T-06.3.2-29 | `test_graph_ablation_provenance_failure` rewritten for the paired contract: a provenance-failed graph-off record yields zero pairs and an error status, not a scored delta | unit | `uv run --project eval pytest tests/test_score_deterministic.py::test_graph_ablation_provenance_failure -x` | ✅ exists | ✅ green |
| 06.3.2-04-02 | 04 | 3 | OBS-05 (D-39, D-40) | — | Stratified deltas computed per gold `question_type` (comparison / inference / temporal) **and** in aggregate | unit | `uv run --project eval pytest tests/test_ablation.py -k stratified -x` | ❌ W0 | ✅ green |
| 06.3.2-05-01 | 05 | 4 | OBS-05 (D-36) | T-06.3.2-27 | CR-01 and WR-03 closed: the worksheet refuses a judge-disabled run and names the remedy as enabling judging; the primary arm is chosen from data | unit | `uv run --project eval pytest tests/test_review_findings.py -k "worksheet or primary_arm" -x` | ❌ W0 | ✅ green |
| 06.3.2-05-01 | 05 | 4 | OBS-05 (D-36) | T-06.3.2-18 | **WR-02 consumer assertion:** duplicate records for one question yield a judged population of one and one worksheet row — asserted here, not assumed from plan 04 | unit | `uv run --project eval pytest tests/test_review_findings.py -k wr02 -x` | ❌ W0 | ✅ green |
| 06.3.2-05-02 | 05 | 4 | OBS-05 (D-36) | T-06.3.2-22, T-06.3.2-23, T-06.3.2-25 | WR-04 and IN-02 closed: config-read failure warns without disclosure, BOTH generation and embedding model fall back to an unknown marker, dropped cache verdicts are counted | unit | `uv run --project eval pytest tests/test_review_findings.py tests/test_judge.py tests/test_secrets.py -x` | ❌ W0 | ✅ green |
| 06.3.2-05-03 | 05 | 4 | OBS-05 (D-02, D-34, D-35) | T-06.3.2-26 | Every registered dimension renders; absolute-magnitude dimensions do not render through the judged branch; integral detail floats render as integers — added in the cross-AI review pass | unit | `uv run --project eval pytest tests/test_report.py tests/test_schema.py -x` | ✅ exists | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Task IDs backfilled from the five committed plans. `06.3.2-01-02` supplies the shared `is_usable()` denominator predicate that plans 02/04/05 consume; the D-35 self-contradiction predicate is implemented by `06.3.2-02-03`. Canary file creation is `06.3.2-03-02` and carries no automated row of its own beyond the shape command in its `<verify>`; the preflight checks that consume it are `06.3.2-03-03`.*

*Review-pass revisions to this map (2026-09-05), from `06.3.2-REVIEWS.md`:*

- ***WR-02 re-pointed.*** *It previously sat only on `06.3.2-05-01`. The implementation is `06.3.2-04-02` — journal-wide deduplication before the arm grouping — and `06.3.2-05-01` now carries a separate consumer-side assertion row against the judged and worksheet paths the finding actually names. Both rows are required: the phase claim is only as strong as the assertion at the consumer.*
- *Seven rows added for tests introduced by the review pass: the retryable-usable vector (01-01), the `percentile` empty-input raise (02-01), the `GRAPH_TIMEOUT`-as-attempt case (02-02), the `error_type` keying of the conformance dimension (02-03), the raw-event byte-cap constants and backstop inequality (03-01), the ungated `canary_manifest` check (03-03), and type-aware score and detail rendering (05-03). The paired `None`-score guard and the provenance requirement in the join were folded into the existing 04-01 rows.*
- *Threat references filled in from the five plans' STRIDE registers; the column was empty in the seeded version.*
- *Two rows are marked `✅ exists` rather than `❌ W0` because they modify tests already in the suite — `test_all_normative_caveats_present_in_markdown`, `test_graph_ablation_provenance_failure` — and one because `test_report.py` and `test_schema.py` are existing modules gaining assertions.*

---

## Wave 0 Requirements

Module names corrected in the review pass to match the five plans' `artifacts_this_phase_produces`
inventories. The seeded list named `test_client.py`, `test_run.py` and `test_score.py`; none of the
three exists in `eval/tests/` and none is created by this phase.

- [x] `eval/tests/test_wire_capture.py` — SSE capture, snapshot precedence, outcome routing and the retryable-usable vector for D-01 / D-27 / D-33 (plan 01)
- [x] `eval/tests/test_usability.py` — the D-34 usable-record predicate, including the retryable clause (plan 01)
- [x] `eval/tests/test_denominators.py` — one denominator across retrieval and answer, the relaxed validator's surviving invariants (plan 01)
- [x] `eval/tests/test_path_health.py` — yield, latency and graph presence/influence dimensions, `percentile` empty-input contract, graph-attempt filter for D-02 / D-09 (plan 02)
- [x] `eval/tests/test_wire_contract.py` — the D-35 self-contradiction predicate and its `error_type` keying, scored against the committed 2026-09-03 journal (plan 02)
- [x] `eval/tests/test_raw_events.py` — bounded retention, the byte-cap constants and the deterministic baseline sample for D-07 (plan 03)
- [x] `eval/tests/test_preflight_canary.py` — the ungated manifest check and the gated floors check for D-10 / D-11 / D-12 / D-31 (plan 03)
- [x] `eval/tests/test_pairing.py` — the join, provenance exclusion, the `None`-score guard and bootstrap determinism for D-37–D-43 (plan 04)
- [x] `eval/tests/test_ablation.py` — the paired deltas, stratification and the WR-02 three-consumer test (plan 04)
- [x] `eval/tests/test_review_findings.py` — one named regression test per closed 06.3-REVIEW finding (plan 05)
- [x] `eval/corpora/multihop_rag/canary.jsonl` — the committed canary file (D-11, D-12). **Created in this phase** (AI-SPEC §5 line ~1089). The spec locks the *rule* — 6 unique questions / 7 invocations and the floors table — not the question ID strings.
- [x] Preflight canary probe integration test

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions | Resolution |
|----------|-------------|------------|-------------------|------------|
| Concrete canary question IDs are the right ones | OBS-05 (D-11, D-31) | The two graph-on canaries require gold entities **verified present in the `entities` table**, which is 06.3.3's D-05 inspection. Until that runs, ID selection cannot be automatically validated. | After 06.3.3 D-05 completes, confirm each graph-on canary's gold entities appear in the `entities` table, then pin the IDs. | **RESOLVED (deferred)** — `06.3.2-VERIFICATION.md` (2026-09-05, `e416cca`) ruled this a legitimate documented deferral to Phase 06.3.3, not a phase-level gap. |
| Report reads honestly end to end | OBS-05 (D-02, D-34) | Judging whether a rendered `report.md` communicates failure rather than hiding it is a human reading, not an assertion. | Render a report from a journal containing known-unusable records; confirm the unusable count and rate are visible and no dimension scores infrastructure failure as a quality zero. | **RESOLVED (performed, failed)** — `06.3.2-VERIFICATION.md` (2026-09-05, `e416cca`) performed this check directly against the real 2026-09-03 journal: usable-but-answerless records score a quality zero on the answer side while being excluded on the retrieval side, contradicting `report.md.j2:52`'s own caveat. Folded into the SC-7 gap (CR-01); not a pass. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter — **blocked**: none of the rows above cover the usable-but-answerless record shape that produces the SC-7 gap (CR-01). That shape was not anticipated when this contract was seeded; closing it needs a new row (or an amendment) once the gap-closure plan lands.

**Approval:** partial — all per-task rows verified green (244/244, `uv run --project eval pytest`); phase-level gap SC-7 open per `06.3.2-VERIFICATION.md` (`e416cca`). Full approval blocked on gap closure.

---

*Reconciled 2026-09-05 against `06.3.2-VERIFICATION.md` (`e416cca`): all pending rows confirmed green from the regression-gate run; both Manual-Only rows resolved per verification's rulings. `status` moved draft → validated; `nyquist_compliant` stays false pending the SC-7 fix.*
