---
phase: 06.3.2
slug: eval-harness-diagnostics-scored-dimensions-and-paired-ablati
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: true
created: 2026-09-04
---

# Phase 06.3.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from `06.3.2-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (existing `eval/tests/`), stdlib-only assertions — **no scipy, no numpy** |
| **Config file** | `eval/pyproject.toml` (uv project config) |
| **Quick run command** | `uv run --project eval pytest tests/test_score.py -x` |
| **Full suite command** | `uv run --project eval pytest` |
| **Estimated runtime** | ~10s quick / ~60s full (bootstrap measured at ~0.9s per delta for 500 pairs at B=10_000; 8–9 deltas ≈ 7–8s of the full run) |

---

## Sampling Rate

- **After every task commit:** Run `uv run --project eval pytest tests/test_score.py -x`
- **After every plan wave:** Run `uv run --project eval pytest`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 06.3.2-01-01 | 01 | 1 | OBS-05 (D-01) | — | SSE loop captures `NodeCompletedEvent.duration_ms` + `WorkflowCompletedEvent.metadata` instead of discarding them in the bare `pass` arm | unit | `uv run --project eval pytest tests/test_client.py::test_sse_captures_node_timing -x` | ❌ W0 | ⬜ pending |
| 06.3.2-01-01 | 01 | 1 | OBS-05 (D-01) | — | Journal record carries the captured wire diagnostics | unit | `uv run --project eval pytest tests/test_run.py::test_run_record_has_wire_diagnostics -x` | ❌ W0 | ⬜ pending |
| 06.3.2-01-01 | 01 | 1 | OBS-05 (D-27) | — | `outcome != "success"` when hard node failures are present — closes the `run.py:58` hardcode | unit | `uv run --project eval pytest tests/test_run.py::test_outcome_respects_node_failures -x` | ❌ W0 | ⬜ pending |
| 06.3.2-01-02 | 01 | 1 | OBS-05 (D-34, D-35) | — | `is_usable()` predicate identifies infrastructure failures; self-contradiction boolean matches the locked AI-SPEC predicate | unit | `uv run --project eval pytest tests/test_dimensions.py -k usable_record_predicate -x` | ❌ W0 | ⬜ pending |
| 06.3.2-04-01 | 04 | 3 | OBS-05 (D-37–D-43) | — | Pairing join is an inner join on `question_id` where both arms are usable; unpaired dropped; nulls excluded | unit | `uv run --project eval pytest tests/test_score.py::test_form_pairs_inner_join -x` | ❌ W0 | ⬜ pending |
| 06.3.2-04-01 | 04 | 3 | OBS-05 (D-38, D-43) | — | Bootstrap CI is deterministic across runs at `BOOTSTRAP_SEED=42` with sorted-pair input; `n_pairs=0` → `status=error`; `n_pairs=1` → `degenerate_ci=1.0` | unit | `uv run --project eval pytest tests/test_dimensions.py::test_bootstrap_determinism -x` | ❌ W0 | ⬜ pending |
| 06.3.2-04-02 | 04 | 3 | OBS-05 (D-39, D-40) | — | Stratified deltas computed per gold `question_type` (comparison / inference / temporal) **and** in aggregate | unit | `uv run --project eval pytest tests/test_score.py::test_stratified_delta_by_question_type -x` | ❌ W0 | ⬜ pending |
| 06.3.2-02-02 | 02 | 2 | OBS-05 (D-02, D-09) | — | Graph-health and per-path yield/latency dimensions report their numbers; graph emptiness never gates a run as bad | unit | `uv run --project eval pytest tests/test_dimensions.py -k graph_health -x` | ❌ W0 | ⬜ pending |
| 06.3.2-03-03 | 03 | 2 | OBS-05 (D-10, D-11, D-31) | — | Preflight canary probes the query path and fails loud below the locked floors | integration | `uv run --project eval lancet-eval preflight --corpus multihop_rag` | ❌ W0 | ⬜ pending |
| 06.3.2-05-01 | 05 | 4 | OBS-05 (D-36) | — | Four `06.3-REVIEW.md` findings closed: CR-01, WR-02, WR-03, WR-04 + IN-02 | unit | `uv run --project eval pytest tests/test_score_judged.py -k worksheet -x` | ⚠️ partial | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Task IDs backfilled from the five committed plans. `06.3.2-01-02` additionally supplies the shared `is_usable()` denominator predicate that plans 02/04/05 consume; the D-35 self-contradiction half of that row is implemented by `06.3.2-02-03`. Canary file creation is `06.3.2-03-02`; the preflight probe that consumes it is `06.3.2-03-03`. The remaining three review findings (WR-04, IN-02) land in `06.3.2-05-02`.*

---

## Wave 0 Requirements

- [x] `eval/tests/test_client.py` — SSE capture tests for D-01
- [x] `eval/tests/test_run.py` — wire-diagnostics and outcome-fix tests for D-01 / D-27
- [x] `eval/tests/test_dimensions.py` — usability predicate, bootstrap determinism, yield/latency/presence dimensions for D-02 / D-09 / D-34 / D-35
- [x] `eval/tests/test_score.py` — pairing join, stratification for D-37–D-43
- [x] `eval/corpora/multihop_rag/canary.jsonl` — the committed canary file (D-11, D-12). **Created in this phase** (AI-SPEC §5 line ~1089). The spec locks the *rule* — 6 unique questions / 7 invocations and the floors table — not the question ID strings.
- [x] Preflight canary probe integration test

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Concrete canary question IDs are the right ones | OBS-05 (D-11, D-31) | The two graph-on canaries require gold entities **verified present in the `entities` table**, which is 06.3.3's D-05 inspection. Until that runs, ID selection cannot be automatically validated. | After 06.3.3 D-05 completes, confirm each graph-on canary's gold entities appear in the `entities` table, then pin the IDs. |
| Report reads honestly end to end | OBS-05 (D-02, D-34) | Judging whether a rendered `report.md` communicates failure rather than hiding it is a human reading, not an assertion. | Render a report from a journal containing known-unusable records; confirm the unusable count and rate are visible and no dimension scores infrastructure failure as a quality zero. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
