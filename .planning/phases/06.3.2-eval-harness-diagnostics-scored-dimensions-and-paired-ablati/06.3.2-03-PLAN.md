---
phase: 06.3.2
plan: 03
type: execute
wave: 2
depends_on: [06.3.2-01]
files_modified:
  - eval/src/lancet_eval/raw_events.py
  - eval/src/lancet_eval/client.py
  - eval/src/lancet_eval/run.py
  - eval/src/lancet_eval/preflight.py
  - eval/corpora/multihop_rag/canary.jsonl
  - eval/tests/test_raw_events.py
  - eval/tests/test_preflight_canary.py
  - eval/tests/test_preflight.py
  - eval/tests/test_run_arms.py
  - eval/tests/test_client_contract.py
autonomous: true
requirements: [OBS-05]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every query whose stream carried a node failure keeps its raw SSE frames on disk, plus a fixed deterministic sample of successes as a baseline (D-07)."
    - "Raw retention is bounded by two NAMED constants — a per-unit byte cap and a run-level backstop — with any truncation recorded in the file rather than dropped silently."
    - "Which units are retained is a pure function of the journal and the question list: every failing unit plus the fixed baseline sample, never a function of thread scheduling. The run-level cap is sized so it cannot bind before per-unit truncation does, so it can never make the committed artifact depend on completion order."
    - "A committed, diffable canary set drives the real query path during preflight, so a dead retrieval path fails loud before a drive starts instead of passing five green checks (D-10, D-11)."
    - "Each canary asserts at least one retrieved chunk; the null canary asserts the retrieval node completed rather than asserting chunks; the two designated graph canaries assert at least one graph node; the graph-off twin asserts the ablation notice (D-31)."
    - "Every observed node duration is checked against that node's timeout read live from config/config.toml — no millisecond value is written into the harness, so the canary is what locks 06.3.3's derived budgets in (D-12)."
    - "The canary is the only place a missing graph hit may fail anything; no arbitrary corpus question is ever required to return graph nodes."
  artifacts:
    - "eval/src/lancet_eval/raw_events.py — the bounded raw-frame sink and the deterministic baseline sample rule"
    - "eval/corpora/multihop_rag/canary.jsonl — 6 unique questions across 7 invocations, with floors"
    - "eval/src/lancet_eval/preflight.py — check_canary_floors and the live node-timeout reader"
  key_links:
    - "The canary preflight drives the same client path a real run uses, so a broken query path cannot pass preflight and fail the drive"
    - "Node names map to timeout keys one to one: RetrieveHybrid to retrieve, ExtractGraphContext to graph node, AssemblePrompt to prompt, GenerateAnswer to generation node, ReformulateQuery to reformulate"
    - "Raw retention writes inside the run directory, which is git-tracked for multihop_rag runs — the byte cap is what keeps a committed artifact reviewable"
  prohibitions:
    - "No new Python dependency; uv.lock unchanged"
    - "No millisecond timeout value is hardcoded in the harness or in canary.jsonl"
    - "Raw retention must never persist HTTP headers, the request body, environment variables or any credential — only the SSE event name and its data payload"
    - "The canary must not assert a graph node count on any question other than the two designated graph canaries"
---

<objective>
Give the drive itself two pieces of integrity it lacked: forensic retention of the raw stream
where things went wrong, and a preflight that actually probes the query path.

Purpose: the 2026-09-03 preflight passed all five checks — store isolation, gateway reachable,
engine reachable, corpus generation, OpenRouter key — on a run where the very next 500 queries
would produce 904 hard retrieval failures. None of those checks touches retrieval. And when the
run did collapse, nothing but the parsed journal survived, so the forensic trail is a
reconstruction rather than a recording.

Output: a bounded raw-SSE retention sink (D-07), a committed canary set (D-11, D-31), and a
preflight canary check whose duration floors are read live from the engine's own configuration
so that the canary is what locks the budgets 06.3.3 derives (D-12).
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-CONTEXT.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-AI-SPEC.md
@.planning/phases/06.3.2-eval-harness-diagnostics-scored-dimensions-and-paired-ablati/06.3.2-PATTERNS.md
@.planning/phases/06.3.2-eval-harness-diagnostics-scored-dimensions-and-paired-ablati/06.3.2-RESEARCH.md
@.planning/phases/06.3.2-eval-harness-diagnostics-scored-dimensions-and-paired-ablati/06.3.2-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bounded raw-SSE forensics on failures plus a deterministic success baseline</name>
  <files>eval/src/lancet_eval/raw_events.py, eval/src/lancet_eval/client.py, eval/src/lancet_eval/run.py, eval/tests/test_raw_events.py, eval/tests/test_run_arms.py, eval/tests/test_client_contract.py</files>
  <read_first>
    - `eval/src/lancet_eval/client.py` — the SSE loop at 189-220 as landed by plan 06.3.2-01, and the four `QueryOutcome` return paths at 232-281.
    - `eval/src/lancet_eval/run.py:22-79` — `drive_one` and its never-raise contract; `run.py:82-163` — `drive`, the resume-key logic, the work-unit ordering and the thread pool that runs `drive_one` concurrently.
    - `eval/src/lancet_eval/journal.py:78-104` — the `Journal` append idiom, in particular that it opens and flushes per record rather than holding a handle, and that it takes no lock.
    - `.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-CONTEXT.md` D-07.
    - `.gitignore` lines 60-64 — `eval/runs/*` is ignored except `*-multihop_rag/` and `*-graphrag_bench/`, so anything written inside a multihop_rag run directory becomes a committed artifact.
  </read_first>
  <behavior>
    - **Failure retention:** a stream containing a `node_failed` frame produces a raw file whose
      lines replay the frames in arrival order with their event names and data payloads.
    - **Baseline retention:** a clean success whose question is in the baseline sample produces
      a raw file; a clean success outside the sample produces none.
    - **Deterministic sample:** the baseline set computed from a given question list is the same
      on every call and is independent of thread scheduling and of the order work units
      complete.
    - **Per-unit cap:** a stream whose frames exceed the byte cap produces a file that stops at
      the cap and ends with a truncation record naming the number of frames not written. No
      frame is dropped without that record.
    - **In-memory bound:** the buffer held during streaming is capped by the same limit, so a
      never-ending stream cannot exhaust memory before the deadline fires.
    - **No secrets:** the written records contain only the event name and its data; no HTTP
      header, no request body, no environment value, no base URL appears in any line.
    - **Off by default:** `run_query` called without the capture option behaves exactly as
      before and returns no raw payload — the preflight canary in task 3 relies on this.
    - **Never raises:** a sink whose directory cannot be created, or whose write fails, records
      the problem and lets the drive continue; `drive_one`'s never-raise contract is preserved
      and a sink failure does not become a fabricated error record.
    - **Concurrency:** two work units driven concurrently write to two distinct paths and
      neither interleaves into the other.
  </behavior>
  <action>
    Create `raw_events.py` with a sink that owns the retention policy and a pure function that
    computes the baseline sample.

    The baseline rule must be reproducible from committed inputs alone: sort the driven question
    identifiers ascending and take the first twenty-five. That set is a function of the question
    list, not of scheduling, so a reader can regenerate exactly which successes were kept. Do not
    use a random sample, a hash bucket, or a running counter — all three make the committed
    artifact unreproducible.

    The sink writes one file per work unit under a `raw_events` directory inside the run
    directory, partitioned by arm and named by question identifier, one JSON object per line
    carrying a sequence ordinal, the event name and the data payload. One file per unit rather
    than one shared file, because the driver runs units concurrently and the existing journal
    append idiom takes no lock — large multi-line payloads appended from several threads to one
    handle would interleave.

    Enforce two bounds, both as named module-level constants with the values pinned here rather
    than left to judgement. These files land inside a run directory that `.gitignore` lines 60-64
    explicitly un-ignore for `*-multihop_rag` runs, so every retained file is a committed
    artifact and the cap is the only thing keeping the commit reviewable. A 1000-question two-arm
    drive that fails the way the 2026-09-03 run did produces roughly 991 retained units; at a
    megabyte apiece that is a gigabyte-scale commit, and at four kilobytes apiece every real
    failure is truncated to uselessness.

    `RAW_EVENT_BYTES_PER_UNIT` is 32768. It caps what any single unit may write; when it is
    exhausted the sink stops and writes a final record stating that truncation occurred and how
    many frames were not written. Silence here would reproduce, in the forensic artifact, the
    exact class of silent loss this phase exists to remove. The same constant bounds the
    in-memory buffer the client accumulates while streaming, so a pathological stream cannot
    exhaust memory before the wall-clock deadline fires.

    `RAW_EVENT_BYTES_PER_RUN` is 67108864. It is a backstop against a pathological unit count,
    deliberately sized so it can never bind before per-unit truncation does: the worst case for
    the largest planned drive is every one of roughly two thousand work units retained at the
    per-unit cap, which is under sixty-three megabytes. This sizing is load-bearing, not
    arithmetic trivia. `drive` completes work units through a thread pool in completion order, so
    a run-level cap that could actually bind would make WHICH units survive depend on thread
    scheduling — turning a committed, reproducible forensic artifact into a nondeterministic one
    and contradicting this plan's own reproducibility guarantee. Sized as a backstop, retention
    stays a pure function of which units failed and which are in the baseline set. If the
    backstop is ever reached, stop writing and record that fact once in the run directory rather
    than failing the drive.

    In `client.py`, add an off-by-default capture option to `run_query`. When enabled, append the
    event name and data of each frame to a bounded buffer as it arrives, and return that buffer
    on the `QueryOutcome`. Capture the frame content only: never the response headers, never the
    request body, never the client's configured base URL. The retain decision cannot be made
    until the stream ends, because it depends on whether any node failure appeared, so buffering
    is required — but the buffer is capped, so the cost is bounded whether or not the unit is
    ultimately retained. Every existing caller that does not pass the option must be unaffected.

    In `run.py`, have `drive` compute the baseline set once from the question list it is about to
    drive, construct the sink rooted at the journal path's parent directory, and pass both into
    `drive_one`. `drive_one` enables capture, and after the record is built decides retention:
    keep when the record has any node failure, or when its question is in the baseline set.
    Guard the sink call so a filesystem problem is recorded and swallowed rather than escaping
    into the surrounding blanket handler, where it would be written into the journal as a
    fabricated query error.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_raw_events.py eval/tests/test_run_arms.py eval/tests/test_client_contract.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --project eval pytest eval/tests -q` exits 0 offline, no gateway, no network, no `OPENROUTER_API_KEY`.
    - `uv run --project eval ruff check --preview eval/src eval/tests` exits 0 and `uv lock --project eval --check` exits 0.
    - A named test drives a mocked failing stream through `drive_one` into a temporary run directory and asserts the raw file exists, is valid JSON Lines, and replays the frames in order.
    - A named test asserts a clean success outside the baseline set leaves no raw file, and one inside it does.
    - A named test calls the baseline function twice on a shuffled copy of the same question list and asserts both calls return the same set.
    - A named test drives a stream whose frames exceed the byte cap and asserts the file's final record states truncation and a non-zero count of unwritten frames.
    - `uv run --project eval python -c "from lancet_eval.raw_events import RAW_EVENT_BYTES_PER_UNIT as U, RAW_EVENT_BYTES_PER_RUN as R; assert U == 32768, U; assert R == 67108864, R; assert R > 2000 * U; print('caps-ok')"` prints `caps-ok`. The final assertion is the backstop property: the run cap exceeds the largest planned drive's worst case, so it cannot bind before per-unit truncation and cannot make retention scheduling-dependent.
    - A named test asserts the enforced cap is the constant rather than a separate literal, by monkeypatching the per-unit constant to a small value and asserting the written file honours the patched value.
    - A named test asserts no written line contains any HTTP header name, the client base URL, or an environment variable value, by driving a stream through a client configured with a recognisable base URL and asserting that string is absent from every written byte.
    - A named test asserts `run_query` called without the capture option returns an outcome carrying no raw payload and that its behaviour on the existing SSE fixtures is unchanged.
    - A named test makes the sink's target directory unwritable, drives one unit, and asserts the returned record is a normal record — not an error record — and that `drive_one` did not raise.
    - Two units driven concurrently in a test write to two distinct files whose contents do not interleave.
  </acceptance_criteria>
  <done>
    A run leaves a bounded, reproducible forensic trail: every failing query's raw stream and a
    fixed sample of healthy ones, with truncation stated in the file rather than hidden.
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit the canary set — six questions, seven invocations, explicit floors</name>
  <files>eval/corpora/multihop_rag/canary.jsonl</files>
  <read_first>
    - `eval/corpora/multihop_rag/questions.sample.jsonl` — the committed 500-question sample; use shell tools to inspect, do not read it whole. Fields are `question_id`, `query`, `question_type`, `evidence_list`, `answer`.
    - `eval/src/lancet_eval/corpus.py:24-60` — `GoldQuestion`, its `is_null` property (empty evidence list or empty gold facts), and the MultiHop-RAG adapter that produces it.
    - `.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-AI-SPEC.md` lines 975-996 — the locked canary composition rule and the floors table.
    - `.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-CONTEXT.md` D-11, D-12, D-31.
  </read_first>
  <action>
    Write the committed canary file. The AI-SPEC locks the rule — six unique questions across
    seven invocations, with the floors below — and leaves the identifiers to this phase.

    Four retrieval canaries, all on the graph arm that leaves the graph enabled, drawn from the
    committed 500-sample by a rule a reviewer can re-run: sort the sample by question identifier
    ascending and take the first question of each of the four types. Against the committed file
    that yields `mhr-0073ab564e55` (comparison), `mhr-01a03d465730` (inference),
    `mhr-00fc91a80765` (temporal) and `mhr-0279d4a349c3` (null). Re-derive them rather than
    trusting this list, and if the file has changed, record the divergence in the summary and use
    what the rule produces.

    Two graph canaries, selected by a second re-runnable rule: among the non-null questions, rank
    by the number of distinct source documents their gold evidence spans, descending, breaking
    ties by question identifier ascending, and take the top two. Against the committed file that
    yields `mhr-0d5e238015ef` and `mhr-12912d800c0c`, both spanning four distinct sources. The
    rationale is mechanical, not aesthetic: a question whose gold evidence is spread across the
    most documents is the one whose entities are most likely to have been extracted into the
    graph, which is what D-31's "entities known to be in the graph" asks for.

    One graph-off twin of the first graph canary, so the ablation notice itself is under test.

    Give each row the question identifier, the arm, the verbatim question text copied from the
    sample, the question type, and its floors as explicit fields: the minimum retrieved chunk
    count, whether a graph node is required, and whether the retrieval node is merely required to
    complete. Carry the question text so the file is self-describing and diffable, which is what
    D-11 asks for; task 3 checks that text against the corpus sample so drift is caught rather
    than silently tolerated.

    Set the floors per the AI-SPEC table. The three non-null retrieval canaries require at least
    one retrieved chunk and no graph node. The null canary requires the retrieval node to complete
    and permits zero chunks — a null question legitimately retrieves nothing, and requiring
    otherwise would make the canary fire on correct behaviour. The two graph canaries require at
    least one retrieved chunk and at least one graph node. The graph-off twin requires at least
    one retrieved chunk and the ablation notice.

    Put no millisecond value in this file. The per-node duration floor is read from the engine's
    live configuration in task 3, which is the mechanism that makes this canary lock in the
    budgets 06.3.3 derives.
  </action>
  <verify>
    <automated>uv run --project eval python -c "import json;from pathlib import Path;rows=[json.loads(l) for l in Path('eval/corpora/multihop_rag/canary.jsonl').read_text(encoding='utf-8').splitlines() if l.strip()];assert len(rows)==7, len(rows);assert len({r['question_id'] for r in rows})==6;print('canary-shape-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `eval/corpora/multihop_rag/canary.jsonl` exists, is valid JSON Lines, has exactly 7 rows over exactly 6 distinct question identifiers, and is committed.
    - Exactly one row has the graph-off arm and it is a twin of a graph-on row with the same question identifier.
    - Exactly two rows require a graph node; every other row does not.
    - Exactly one row is a null question, it permits zero chunks, and it is the only row that does.
    - This command confirms every canary identifier and its text exist verbatim in the corpus sample: `uv run --project eval python -c "import json;from pathlib import Path;s={json.loads(l)['question_id']:json.loads(l)['query'] for l in Path('eval/corpora/multihop_rag/questions.sample.jsonl').read_text(encoding='utf-8').splitlines() if l.strip()};rows=[json.loads(l) for l in Path('eval/corpora/multihop_rag/canary.jsonl').read_text(encoding='utf-8').splitlines() if l.strip()];assert all(r['question_id'] in s and r['question']==s[r['question_id']] for r in rows);print('canary-text-matches-corpus')"` prints `canary-text-matches-corpus`.
    - This command confirms no millisecond value was written into the canary: `grep -c "_ms" eval/corpora/multihop_rag/canary.jsonl` outputs `0`.
    - The four retrieval identifiers are re-derived by the stated sort rule and the two graph identifiers by the stated ranking rule, and both derivations are recorded in the summary alongside the identifiers chosen.
    - The summary records that the two graph canaries' gold entities have NOT yet been confirmed present in the entities table, and that the confirmation is 06.3.3's D-05 inspection — it is a recorded deferral, not a silent assumption.
    - The summary states explicitly what this task does and does not close: it closes "a committed, reproducibly-selected canary file exists". It does NOT close D-31's requirement that the graph canaries use entities *known* to be in the graph, because the gold-evidence document-span heuristic is a proxy for graph membership, not a verification of it. Until 06.3.3 records the store inspection, a green graph canary is provisional and a red one is ambiguous between a graph defect and a badly chosen canary — which is what the failure message in task 3 must say.
    - The summary names 06.3.3 D-05 as the successor that must record the entity inspection BEFORE anyone treats a graph-canary result as a go/no-go signal, and this deferral is carried in the canary file's own header or summary note so a reader of the committed file sees it without reading the planning tree.
  </acceptance_criteria>
  <done>
    A committed, reviewable, diffable canary set exists with explicit floors and no embedded
    timing constants, and the selection of every identifier is reproducible from the corpus.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preflight probes the real query path against the canary floors</name>
  <files>eval/src/lancet_eval/preflight.py, eval/tests/test_preflight_canary.py, eval/tests/test_preflight.py</files>
  <read_first>
    - `eval/src/lancet_eval/preflight.py` in full — `PreflightCheckResult` at 23-31, the check-function shape at 34-71, `check_corpus_generation` at 141-238 for the corpus-aware idiom, and the `run_preflight_checks` aggregator at 293-340 including its client lifecycle and the gateway-and-engine gating.
    - `eval/src/lancet_eval/client.py` — `run_query` and the `QueryOutcome` fields as landed by plan 06.3.2-01, in particular `node_timings`, `workflow_meta` and `partial_snapshot`.
    - `config/config.toml` — the `[engine.workflow]` block holding `reformulate_timeout_ms`, `query_embedding_timeout_ms`, `retrieve_timeout_ms`, `graph_operation_timeout_ms`, `graph_node_timeout_ms`, `prompt_timeout_ms`, `generation_node_timeout_ms`.
    - `engine/src/workflow/runner.rs:298-317` — the authoritative node-name to timeout mapping this check must mirror: ReformulateQuery to reformulate, ExtractGraphContext to graph node, RetrieveHybrid to retrieve, AssemblePrompt to prompt, GenerateAnswer to generation node.
    - `eval/src/lancet_eval/dimensions.py:10-11` — the notice-code constants for graph ablation and graph unavailable.
    - `eval/tests/test_preflight.py` — the mocking idiom used for the existing preflight checks.
    - `.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-AI-SPEC.md` lines 975-996.
  </read_first>
  <behavior>
    - **All floors met:** every canary returning chunks above its floor, the graph canaries
      returning a graph node, the twin carrying the ablation notice, and every node duration under
      its configured timeout yields a passing check whose detail names the number of canaries run.
    - **Retrieval floor missed:** a canary returning zero chunks yields a failing check whose
      message names that canary's identifier and the observed count.
    - **Null canary:** a null canary returning zero chunks passes; the same canary whose retrieval
      node failed rather than completed fails.
    - **Graph floor missed:** a designated graph canary returning zero graph nodes fails, and the
      message names the identifier and states that the remedy is either a graph defect or a
      wrongly chosen canary pending the store inspection.
    - **Non-graph canary with no graph node passes** — the graph floor applies only to the two
      designated rows.
    - **Twin:** the graph-off twin lacking the ablation notice fails.
    - **Duration floor:** a node duration at or above its configured timeout fails, and the
      message names the node, the observed duration and the configured budget. Changing the
      configured budget changes the threshold with no harness edit.
    - **Config unreadable:** an unreadable or malformed engine configuration yields a failing
      check that says so, and never a silently permissive pass.
    - **Missing canary file:** an absent canary file yields a failing `canary_manifest` check
      naming the expected path, and it does so with no gateway running — file validation is
      independent of reachability.
    - **Manifest drift:** a canary row whose question text no longer matches the corpus sample
      for that identifier fails `canary_manifest`, again without needing a gateway.
    - **Aggregation, manifest:** `canary_manifest` is present in the aggregated results on every
      invocation, whether or not the gateway and engine checks passed.
    - **Aggregation, floors:** `canary_floors` is present in the aggregated results only when the
      gateway and engine checks both passed, because it issues real queries and there is nothing
      to query otherwise. When it is present and failing, the preflight command exits non-zero
      and its output names `canary_floors`.
    - **Gateway down:** with no gateway running, the preflight command exits non-zero because
      `gateway_reachable` failed and names that check; `canary_floors` does not appear in the
      output at all, and `canary_manifest` still appears and still passes against the committed
      file. These three facts are one coherent contract: an unreachable gateway is reported by
      the reachability check that owns it, not by a query check that never ran.
  </behavior>
  <action>
    Add a canary loader, a live timeout reader and TWO canary checks to `preflight.py`, following
    the file's existing check-function shape: a result carrying a name, a pass flag, an actionable
    message and a diagnostic detail mapping.

    Split the concern into `check_canary_manifest` and `check_canary_floors`, because the two
    halves have different preconditions and collapsing them produces a contract that cannot be
    satisfied. Validating that the committed canary file exists, parses, has the right shape and
    still matches the corpus sample needs no gateway at all; probing the query path needs a
    reachable gateway and engine. If both live in one gated check, then an absent or drifted
    canary file goes unreported whenever the stack happens to be down — the check most likely to
    catch a repository-level mistake is silenced by an unrelated infrastructure condition. And a
    single gated check cannot simultaneously be absent from the results when the gateway is down
    and be named in the output as failing, which is the contradiction the cross-AI review found
    in this task's earlier acceptance criteria.

    `check_canary_manifest` runs unconditionally, outside the gateway-and-engine gate. It reads
    the committed canary file, asserts the shape task 2 locked, validates each row's question
    text against the corpus sample for that identifier, and fails loudly on an absent file, a
    parse failure, a shape violation or drift — rather than letting a stale canary silently probe
    a question the repository no longer contains.

    `check_canary_floors` runs inside the existing gate, alongside the corpus generation check,
    and does the query-path work described below. It consumes the rows the manifest check already
    validated.

    The timeout reader parses the engine's live configuration and returns the per-node budgets,
    mapping node names to configuration keys exactly as the engine's own runner does. Read the
    values at check time — do not copy them into the harness, and do not fall back to a default
    when the file is unreadable. A silent default here would let a canary pass against a budget
    nobody configured, which is the opposite of D-12's purpose: this check is what makes the
    budgets 06.3.3 derives actually binding. Read only the workflow timeout keys; the same file
    holds provider configuration, so neither the parsed document nor any part of its contents may
    appear in a message, a detail value or a log line.

    The check drives each canary row through the same client path a real run uses, with the graph
    disabled exactly on the graph-off twin, and evaluates that row's floors: the minimum retrieved
    chunk count, the graph node requirement where present, the completion requirement on the null
    row, the ablation notice on the twin, and for every row each observed node duration against
    that node's configured budget. Accumulate all failures across all rows and report them
    together — a single message naming one problem would send an operator round the loop once per
    canary.

    Message text on a graph floor miss must be actionable: name the canary, state the observed
    graph node count, and state that the two possible causes are a graph defect or a canary whose
    entities are not in fact in the store, the latter resolvable when the sibling measurement
    phase inspects it. That is the honest reading — the canary asserts a known-good question, not
    a graph capability floor, and this is the only place in the harness where a missing graph hit
    may fail anything.

    Name the configuration key that was read in every duration-floor message, because the node
    budgets are nested and a miss is otherwise misdiagnosed. Do not let this task's duration
    clause be mistaken for coverage of all six budgets: the node map ties `ExtractGraphContext`
    to the outer graph-node budget of fifteen seconds, but the graph deaths in the 2026-09-03 run
    were the inner graph-operation budget of four seconds. Because inner expiry is handled as
    non-fatal degradation that emits a notice and returns successfully, the outer budget can
    never fire for the graph operation at all — the engine configuration documents this as a
    known incoherence under D-17, and that run recorded 494 inner expiries and zero outer ones. A
    graph node degrading at roughly four seconds therefore has a duration well under the outer
    budget and passes the duration floor. What actually locks the inner budget is the graph-node
    count floor on the two designated graph canaries. State this in the summary so 06.3.3 does
    not believe D-12's duration clause covers all six budgets when it covers five.

    Register `check_canary_manifest` in `run_preflight_checks` unconditionally, and
    `check_canary_floors` inside the branch that already gates on the gateway and engine being
    reachable, alongside the corpus generation check. Adding a result to the returned list is
    safe: no existing test in `eval/tests/test_preflight.py` asserts the length of that list.

    Reuse the client the aggregator already manages for the floors check; do not open a second
    one. Note that the aggregator's client carries the gateway timeout as its client-level
    default while `run_query` sets its own per-request read timeout and deadline, and the
    per-request value is the one that governs — pass the client through to `run_query` rather
    than reconfiguring it. Drive each canary through `run_query` and nothing else. Do not
    "simplify" this to a plain POST in the manner of the corpus-generation check: that would
    discard the SSE parsing, and without parsed frames the chunk counts, the graph node counts,
    the ablation notice and every per-node duration are all unobservable, leaving the floors
    untestable and the check green for the same reason the 2026-09-03 preflight was green.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_preflight_canary.py eval/tests/test_preflight.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --project eval pytest eval/tests -q` exits 0 offline, with no gateway running — every canary test drives a mocked transport.
    - `uv run --project eval ruff check --preview eval/src eval/tests` exits 0 and `uv lock --project eval --check` exits 0.
    - A named test per behavior bullet, each asserting both the pass flag and that the message names the offending canary identifier where one exists.
    - A named test proves the duration floor is read live: it scores the same mocked responses twice against two different configured budget values and asserts the check passes under the larger and fails under the smaller, with no change to any harness constant.
    - A named test asserts every duration-floor failure message names the configuration key whose value was compared against, so an inner-budget miss cannot be misread as an outer-budget miss.
    - This command confirms no budget constant was baked into the preflight module: `grep -vE "^\s*#" eval/src/lancet_eval/preflight.py | grep -cE "timeout_ms[^\"']*[:=][^\"']*[0-9]"` outputs `0`. It reports `0` against the pre-change file, so it is a regression guard, not a pass-by-default.
    - A named test asserts a malformed engine configuration produces a failing result, not a pass.
    - A named test asserts neither the message nor the detail of any canary result contains a provider key name or any value read from outside the workflow timeout block, by pointing the reader at a fixture configuration containing a recognisable secret-shaped string and asserting that string appears in no result field.
    - A named test asserts `canary_floors` is absent from the aggregated results when the gateway check failed, and present when it passed.
    - A named test asserts `canary_manifest` is present in the aggregated results in BOTH conditions — gateway reachable and gateway down — proving file validation is not gated on reachability.
    - A named test asserts that with the gateway and engine mocked reachable but the query path returning zero retrieved chunks, `canary_floors` fails and its message names the offending canary identifier and the observed count. This is the case that proves the check probes the query path rather than merely reaching the gateway.
    - A named test points the manifest check at an absent canary file with no gateway running and asserts `canary_manifest` fails and names the expected path; a sibling test does the same for a drifted question text.
    - `uv run --project eval lancet-eval preflight --corpus multihop_rag` exits non-zero when no gateway is running, and its output names `gateway_reachable` as the failing check. `canary_floors` does not appear in that output, because it never ran; `canary_manifest` does appear and passes against the committed file. This is the human-observable end of the feature and does not require a live stack.
  </acceptance_criteria>
  <done>
    Preflight drives the committed canary set through the real query path and fails loud, with a
    named identifier and an actionable message, on a dead retrieval path, a missing graph hit on a
    designated canary, a missing ablation notice, or any node exceeding its configured budget.
  </done>
</task>

</tasks>

<review_incorporation>
## Cross-AI review findings folded into this plan

From `06.3.2-REVIEWS.md` (reviewers: cursor, antigravity). This plan drew one HIGH from each
reviewer and they are different findings; both are incorporated.

| Finding | Severity | Disposition |
|---|---|---|
| Per-unit byte cap is never given a number, and retained files are git-tracked | HIGH (cursor) | Incorporated. `RAW_EVENT_BYTES_PER_UNIT` and `RAW_EVENT_BYTES_PER_RUN` are named constants with values pinned in the action, plus the sizing rationale, an acceptance command asserting both values and the backstop inequality, and a monkeypatch test proving the constant is the enforced cap. |
| Task 3 acceptance criteria contradict each other on gateway-down canary behaviour | HIGH (antigravity) | Incorporated as a structural fix, not a wording patch. The check is split into an ungated `canary_manifest` and a gated `canary_floors`; the behavior block now states all three gateway-down facts as one coherent contract, and the two conflicting criteria are replaced by five that cannot disagree. The reviewer's line numbers (2788/2789) did not match this file, but the contradiction was real and was located at the criteria pair asserting absence-when-gateway-down and named-as-failing-when-gateway-down. Antigravity's suggested resolution — reachability failures reported by the reachability checks, canary probes skipped, and file validation decoupled from reachability — is the resolution adopted. |
| Graph canaries are selected by gold evidence span, not by entities known to be in the store | MEDIUM (cursor) | Incorporated. Task 2's acceptance criteria now state explicitly that the task closes "the file exists and is reproducibly selected" and does NOT close D-31, name 06.3.3 D-05 as the blocking successor that must record the inspection before any graph-canary result is treated as go/no-go, and require the deferral to be carried in the committed file itself. |
| Duration floors do not catch the inner graph-operation budget | MEDIUM (cursor) | Incorporated. The action states the nesting, cites the documented D-17 incoherence and the 494-versus-zero split from the 2026-09-03 run, identifies the graph-node-count floors as what actually locks the inner budget, and requires the summary to record that D-12's duration clause covers five of six budgets. A new acceptance criterion requires every duration message to name the configuration key it compared against. |
| Canary reuses the preflight client; confirm the per-request timeout wins and do not simplify to a plain POST | MEDIUM (cursor) | Incorporated. The action pins client reuse, states that `run_query`'s per-request read timeout and deadline govern over the client-level default, and forbids the plain-POST simplification with the consequence named. |
| `check_corpus_generation` already POSTs the query endpoint and must not be deleted or assumed sufficient | LOW (cursor) | Already correct in the plan; the canary is registered alongside that check rather than replacing it. No change. |
| Seven full generations per preflight invocation have a real API cost | MEDIUM (antigravity) / LOW (cursor) | **Accepted and documented, not mitigated with a flag.** Rationale recorded in `<out_of_scope>` below. |
</review_incorporation>

<out_of_scope>
| Item | Owner | Why not here |
|---|---|---|
| Confirming the two graph canaries' gold entities are present in the `entities` table | 06.3.3 (D-05) | Requires `inspect_lancedb` against the surviving store. Recorded in `06.3.2-VALIDATION.md` under Manual-Only Verifications; the canary ships with the deferral stated, not assumed away. |
| Setting or deriving any timeout value | 06.3.3 (D-13, D-14, D-15) | This plan reads whatever is configured; it never writes one. |
| Locking the inner graph-operation budget via a duration floor | 06.3.3 (D-13) | The outer node budget cannot fire for the inner operation, so no duration floor can observe it. The graph-node-count floors on the two designated canaries are the binding mechanism in this phase. |
| The staged 50-question drive and its coverage floor | 06.3.4 (D-44) | Drive sequencing belongs to the re-drive phase. |
| A mid-run abort threshold | Not planned — explicitly considered and not selected in D-10. | |
| An opt-out or cached mode for the canary's seven generations | 06.3.4, if the cost ever bites | **Considered and rejected for this phase.** Both reviewers raised the cost; it is accepted rather than mitigated. An opt-out defaulting to off would reinstate exactly the false-green preflight D-10 exists to remove — the 2026-09-03 preflight passed five checks precisely because none of them touched the query path — and an opt-out defaulting to on is a flag nobody sets. The cost is bounded and small in context: seven generations before a drive that issues a thousand. The check reports the number of canary invocations in its detail so an operator can see what it spends, and this deferral is the recorded escape hatch if that ever stops being acceptable. Adding a flag would also break the "New CLI flags: None" invariant asserted in two plans' artifact inventories. |
</out_of_scope>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This plan's contribution. The complete phase inventory is the `artifacts_this_phase_produces`
section of `06.3.2-01-PLAN.md`.

| Kind | Artifact |
|---|---|
| New source file | `eval/src/lancet_eval/raw_events.py` — `RawEventSink`, `baseline_sample_ids`, `RAW_EVENT_BYTES_PER_UNIT` (32768), `RAW_EVENT_BYTES_PER_RUN` (67108864) |
| New symbols in `client.py` | `QueryOutcome.raw_events`, the off-by-default raw-capture parameter on `run_query` |
| New symbols in `run.py` | the raw-sink parameter on `drive_one`, the baseline-set computation in `drive` |
| New symbols in `preflight.py` | `check_canary_manifest`, `check_canary_floors`, the canary loader, the live node-timeout reader |
| New committed data file | `eval/corpora/multihop_rag/canary.jsonl` — 6 unique questions, 7 invocations, explicit floors, no embedded timing constant |
| New run-directory artifact | `eval/runs/<dated-run>/raw_events/<arm>/<question_id>.jsonl` — git-tracked for `*-multihop_rag` runs |
| New preflight check names | `canary_manifest` (ungated file validation), `canary_floors` (gated query-path probe) |
| New test modules | `eval/tests/test_raw_events.py`, `eval/tests/test_preflight_canary.py` |
| New dimension names | None — this plan adds no scored dimension |
| New CLI flags | None — retention and the canary check are automatic |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway SSE stream → raw retention file | Unbounded external content is written to a git-tracked path. |
| `config/config.toml` → preflight | A local file holding provider configuration alongside the workflow timeouts. |
| `canary.jsonl` → live query path | A committed file that causes real queries to be issued against the configured gateway. |
| run directory → git | `eval/runs/*-multihop_rag/` is explicitly un-ignored, so retention output is committed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.2-11 | Denial of Service | raw retention sink and its in-memory buffer | high | mitigate | `RAW_EVENT_BYTES_PER_UNIT` (32768) bounds both the buffer and each file; `RAW_EVENT_BYTES_PER_RUN` (67108864) is a backstop sized above the largest planned drive's worst case so it cannot bind before per-unit truncation. The baseline sample is a fixed twenty-five units. Truncation is recorded rather than silent. A named test drives an over-cap stream and asserts the cap held; an acceptance command asserts both constant values and the backstop inequality. |
| T-06.3.2-28 | Repudiation | run-level cap binding on a concurrent drive | medium | mitigate | If the run-level cap could bind, which units survived would depend on thread completion order and the committed forensic artifact would stop being reproducible. It is sized as a backstop above the worst case so retention stays a pure function of which units failed plus the fixed baseline set; the sizing inequality is an acceptance criterion, and reaching the backstop is recorded once in the run directory rather than failing the drive. |
| T-06.3.2-12 | Information Disclosure | raw retention content in a git-tracked directory | high | mitigate | Only the SSE event name and data payload are captured; headers, request body, base URL and environment are never touched. A named test drives a client with a recognisable base URL and asserts that string appears in no written byte. |
| T-06.3.2-13 | Information Disclosure | preflight reading `config/config.toml` | high | mitigate | Only the workflow timeout keys are read; no parsed document, file content or provider key ever reaches a message, a detail value or a log line. Asserted against a fixture configuration carrying a secret-shaped string. |
| T-06.3.2-14 | Spoofing | canary drift from the corpus sample | medium | mitigate | Each canary row's question text is validated against the corpus sample for that identifier by `check_canary_manifest`, which runs unconditionally rather than behind the reachability gate; drift fails the check instead of silently probing a different question, and it fails even when the stack is down, so an unrelated infrastructure outage cannot mask a repository-level defect. |
| T-06.3.2-15 | Elevation of Privilege | canary as the only graph gate | medium | mitigate | The graph floor is applied only to rows explicitly marked as requiring it; a named test asserts a non-graph canary with zero graph nodes passes, so the check cannot broaden into a general graph requirement. |
| T-06.3.2-16 | Tampering | silent fallback on an unreadable engine configuration | high | mitigate | No default budget exists; an unreadable or malformed configuration fails the check, so a canary can never pass against a budget nobody set. |
| T-06.3.2-SC | Tampering | npm/pip/cargo/uv installs | high | accept | No package-manager install task exists: every addition is standard library or already-vendored. `uv lock --project eval --check` is an acceptance criterion on all three tasks. No `[ASSUMED]` or `[SUS]` package is introduced. |
</threat_model>

<verification>
- `uv run --project eval pytest eval/tests -q` exits 0 offline.
- `uv run --project eval ruff check --preview eval/src eval/tests` exits 0.
- `uv lock --project eval --check` exits 0.
- `eval/corpora/multihop_rag/canary.jsonl` is committed with 7 rows over 6 identifiers.
- `git status --short eval/runs/` reports no modification to the retained 2026-09-03 evidence.
</verification>

<success_criteria>
- ROADMAP SC2 satisfied: raw SSE events are retained for any query with non-empty node failures, plus a fixed sample of successes.
- ROADMAP SC9 satisfied: preflight gains query-path checks driven by a committed canary set with fixed floors, and the per-node duration floor is read from the engine's live configuration.
</success_criteria>

<output>
Create `.planning/phases/06.3.2-eval-harness-diagnostics-scored-dimensions-and-paired-ablati/06.3.2-03-SUMMARY.md` when done.
</output>
