# Phase 06.3.2 Plan 02: Path Health Scored Dimensions & Wire Contract Conformance Summary

## Execution Overview
- Implemented `percentile` helper in `eval/src/lancet_eval/stats.py` with linear interpolation and explicit `ValueError` on empty input.
- Added `attempted_graph` and `is_self_contradictory` predicates to `eval/src/lancet_eval/usability.py`.
- Created builders in `eval/src/lancet_eval/dimensions.py`:
  - `make_bm25_yield` (shared `RetrieveHybrid` latency clock with `make_vector_yield`).
  - `make_retrieve_latency_ms` (p50 and p95 over usable records with `missing_timing_n`).
  - `make_graph_presence_rate` (usable graph-on records, MultiHop-RAG 0.20 floor flag, NoMatchFound complement).
  - `make_graph_influence_rate` (usable graph-on records, skipped with reason if absent, 0.0 if explicit zero).
  - `make_graph_latency_ms` (records that attempted the graph, inner GRAPH_TIMEOUT included as an attempt).
  - Redefined `make_wire_contract_conformance` over the FULL journal using `is_self_contradictory` keyed on `error_type` for harness parsing/framing breaks.
- Registered all 5 new dimensions and updated `score.py` to append them alongside `vector_yield`.
- Verified historical 2026-09-03 journal scores at 0.034 for `wire_contract_conformance`.
- Added comprehensive unit tests in `eval/tests/test_path_health.py` and `eval/tests/test_wire_contract.py`.
- All 185 tests pass offline; ruff check passes clean.
