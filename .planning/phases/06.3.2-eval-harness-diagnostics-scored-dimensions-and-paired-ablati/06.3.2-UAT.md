---
status: resolved
phase: 06.3.2-eval-harness-diagnostics-scored-dimensions-and-paired-ablati
source: [06.3.2-VERIFICATION.md]
started: "2026-09-06T09:20:00.000Z"
updated: "2026-09-06T09:55:00.000Z"
---

## Current Test

[testing complete]

## Tests

### 1. Decide whether CR-01 (abstention_rate() called without the notices argument at score.py:285, permanently disabling the documented typed-notice abstention-detection path) must be fixed before this phase closes, or is deliberately deferred to a named follow-up.
expected: A recorded decision: either a follow-up plan/commit fixes the missing notices argument, or an explicit override/deferral is added to this VERIFICATION.md or the roadmap noting the abstention_on_unanswerable dimension relies on text-matching only, with an owner and target.
result: resolved — decision: fix now. `score.py:283-287` now passes `rec.notices` into `abstention_rate`. Regression test `test_abstention_credits_notice_based_no_evidence_over_text_match` added (`eval/tests/test_denominators.py`).
resolution:
  resolved_by: "fix(06.3.2): pass notices to abstention_rate and correct misleading skip reason (commit cc1e71d), test(06.3.2): add regression tests for CR-01/CR-02 abstention review findings (commit 0cc78b9)"
  verified_by: "06.3.2-VERIFICATION.md re-verification, 2026-09-06 — status passed, 10/10 must-haves"

### 2. Decide whether CR-02 (score.py:979-990 prints "Corpus contains no unanswerable questions" even when unanswerable_payload_excluded > 0, i.e., unanswerable questions existed but were payload-excluded, not absent) must be fixed before this phase closes.
expected: A recorded decision: either a follow-up plan/commit corrects the skip-reason string to distinguish the two cases, or an explicit override/deferral is recorded.
result: resolved — decision: fix now. `score.py`'s abstention skip branch now branches on `unanswerable_payload_excluded > 0` to select between two distinct reason strings. Regression test `test_abstention_skip_reason_distinguishes_payload_excluded_from_absent` added (`eval/tests/test_denominators.py`).
resolution:
  resolved_by: "fix(06.3.2): pass notices to abstention_rate and correct misleading skip reason (commit cc1e71d), test(06.3.2): add regression tests for CR-01/CR-02 abstention review findings (commit 0cc78b9)"
  verified_by: "06.3.2-VERIFICATION.md re-verification, 2026-09-06 — status passed, 10/10 must-haves"

## Summary

total: 2
passed: 2
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps
