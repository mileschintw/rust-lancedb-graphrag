---
phase: 06.3.2-eval-harness-diagnostics-scored-dimensions-and-paired-ablati
verified: 2026-09-06T00:00:00Z
status: human_needed
score: 10/10 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 9/10
  gaps_closed:
    - "SC-7: Infrastructure-failed records are excluded from all denominators with an explicit unusable-record count and rate, making the retrieval/answer denominator convention consistent"
  gaps_remaining: []
  regressions: []
deferred:
  - truth: "Concrete graph-on canary question IDs are verified to have gold entities present in the entities table (D-11, D-31)"
    addressed_in: "Phase 06.3.3"
    evidence: >
      06.3.2-03-SUMMARY.md records an explicit "Recorded Deferral": the two
      graph-on canaries' gold entities have not yet been confirmed present in
      the entities table; confirmation is deferred to Phase 06.3.3's D-05
      store inspection. 06.3.2-VALIDATION.md's own Manual-Only Verifications
      table states the same reason and defers the same way. Plan 06.3.2-06
      did not touch preflight.py or canary.jsonl, so this deferral carries
      forward unchanged from the 2026-09-05 verification.
human_verification:
  - test: "Decide whether 06.3.2-REVIEW.md's CR-01 (abstention_rate() called without the notices argument at score.py:285, permanently disabling the documented typed-notice abstention-detection path) must be fixed before this phase closes, or is deliberately deferred to a named follow-up."
    expected: "A recorded decision: either a follow-up plan/commit fixes the missing notices argument, or an explicit override/deferral is added to this VERIFICATION.md or the roadmap noting the abstention_on_unanswerable dimension relies on text-matching only, with an owner and target."
    why_human: "This is a confirmed-present correctness defect in code this phase's own gap-closure plan (06.3.2-06) deliberately took ownership of scoring (must_haves truth #2: 'The same rule governs abstention'). It does not fail any of the 10 numbered ROADMAP success criteria and does not affect SC-7's fix (the call site is only reached after the payload gate already passed), so it cannot be mechanically resolved to FAILED — but shipping a scored dimension whose documented primary detection path is dead is a judgment call about whether 'so the next run can be read honestly' is satisfied for this specific dimension, which belongs to the developer, not to a grep."
  - test: "Decide whether 06.3.2-REVIEW.md's CR-02 (score.py:979-990 prints 'Corpus contains no unanswerable questions' even when unanswerable_payload_excluded > 0, i.e., unanswerable questions existed but were payload-excluded, not absent) must be fixed before this phase closes."
    expected: "A recorded decision: either a follow-up plan/commit corrects the skip-reason string to distinguish the two cases, or an explicit override/deferral is recorded."
    why_human: "Confirmed present in code. The excluded_payload_records count itself is still correctly published in this branch's detail (verified at score.py:986), so SC-7's literal publication requirement holds — but the human-readable reason text a report reader sees is factually wrong in the case where exclusions exist, which is precisely the class of defect this phase was chartered to eliminate ('the next run can be read honestly'). Whether this rises to blocking or is an acceptable known issue for this phase is a judgment call, not a mechanical check."
---

# Phase 06.3.2: Eval harness diagnostics, scored dimensions and paired ablation Verification Report

**Phase Goal:** Stop the eval harness discarding the diagnostics the engine already emits, turn them into scored per-path and graph dimensions, replace the two-independently-averaged-lists ablation with a per-question paired comparison, fix the denominator convention that let infrastructure failure masquerade as a quality score, and close the four open Phase 6.3 review findings — so the next run can be read honestly.

**Verified:** 2026-09-06T00:00:00Z
**Status:** human_needed
**Re-verification:** Yes — after gap closure (plan 06.3.2-06)

## Goal Achievement

### Observable Truths (SC-1 through SC-10)

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | SC-1: duration_ms + WorkflowCompletedEvent.metadata persisted into every journal record (D-01) | ✓ VERIFIED (regression check — file untouched by gap-closure plan) | `eval/src/lancet_eval/client.py` and `run.py` not in the 06.3.2-06 diff (`git diff --stat fc082eb^..2bded29`); prior verification (2026-09-05) confirmed `client.py:280-284`, `WorkflowMetadata`, `run.py:84-98`, and `test_wire_capture.py` assertions hold. |
| 2 | SC-2: Raw SSE retained for non-empty node_failures + fixed success sample (D-07) | ✓ VERIFIED (regression) | `raw_events.py`/`run.py` untouched by this gap-closure plan; prior verification's `should_retain` trace stands. |
| 3 | SC-3: report.md gains scored graph/per-path dimensions symmetric across vector/BM25/graph; graph emptiness never gates a run as bad (D-02, D-09) | ✓ VERIFIED (regression) | `dimensions.py` untouched by 06.3.2-06; `vector_yield`, `bm25_yield`, `graph_presence_rate`, `graph_influence_rate`, `graph_latency_ms` calls still present at `score.py:634-637` (read directly this pass). |
| 4 | SC-4: graph_ablation_delta redefined as per-question paired comparison with CI, unpaired dropped, coverage reported (D-37, D-38, D-43) | ✓ VERIFIED (regression) | `pairing.py`'s `form_pairs` untouched by 06.3.2-06 (confirmed via diff stat); `score.py:787-815` still calls `form_pairs`/`compute_paired_delta` for `graph_ablation_delta` unchanged in shape; independently re-scored and confirmed 8 pairs, 0.016 coverage, populated CI (see Data-Flow Trace). |
| 5 | SC-5: Pairing covers retrieval_evidence_coverage + EM/F1/context precision/ranking quality, stratified by question_type; judged dims unpaired (D-39, D-40) | ✓ VERIFIED (regression) | Five paired dimensions (`graph_ablation_delta`, `_exact_match`, `_f1`, `_context_precision`, `_ranking_quality`) all present at `score.py:794-895`, each now additionally gated by `has_scorable_payload` (strengthens, does not weaken, this criterion). No judged (faithfulness/groundedness) paired dimension exists. |
| 6 | SC-6: Ablation reports paired latency delta and paired prompt-token delta (D-42) | ✓ VERIFIED (regression) | `graph_ablation_latency_delta` (`score.py:897-912`) and `graph_ablation_prompt_token_delta` (`score.py:914-959`) both still registered, unchanged by this plan. |
| 7 | **SC-7 (gap-closure target): Infrastructure-failed records excluded from ALL denominators with explicit unusable-record count/rate, convention consistent (D-34)** | ✓ VERIFIED | Directly re-checked in code, not from SUMMARY prose. `has_scorable_payload(record)` added in `usability.py:45-65`, requiring a non-`None` snapshot AND non-blank (stripped) answer, with the NO_EVIDENCE empty-chunks carve-out preserved. Applied as a single early-exit guard in `score.py`'s per-arm loop (`score.py:246-251`) before any metric computation — removes a payload-less record from recall, precision, ranking, nDCG, exact match, F1 **and** abstention in one place. The dead `retrieval_skipped` counter is gone (`grep -rn retrieval_skipped eval/` repo-wide → zero matches, checked beyond just `score.py`); replaced by population-scoped `payload_excluded_answerable`/`payload_excluded_unanswerable` accumulators (`score.py:231-232, 247-250, 294-295`). The count is published as `excluded_payload_records` on all 5 quality dimensions via `_build_mean_dim`'s `extra_detail` param, merged into the `ok`, `skipped`, and `error` branches alike (`score.py:641-684`), and on `abstention_on_unanswerable` in both its `ok` and `skipped` branches (`score.py:962-990`), using the population-appropriate accumulator in each case. Applied identically inside all 5 paired ablation score functions (`_score_evidence_coverage`, `_score_em`, `_score_f1`, `_score_cp`, `_score_mrr`, `score.py:795-882`), publishing `excluded_unscorable_pairs` via `dimensions.py:109`. `report.md.j2` caveat 6 rewritten with the three required claims and no numerals (confirmed by direct read). Behaviorally re-verified: ran `test_scorable_payload_exclusion_and_reconciliation` and `test_paired_ablation_payload_exclusion_across_all_five_metrics` directly — both pass, and their fixtures construct the exact record shape the prior gap identified (usable, `outcome="success"`, `answer=""`, `snapshot=None`), asserting `n==6`/`excluded_payload_records==2`/reconciliation-to-8 for the aggregate case and `n==3`/`excluded_unscorable_pairs==2` for the paired case — not merely asserting the predicate in isolation. Independently re-scored a fresh scratch copy of the committed `2026-09-03-multihop_rag` journal myself (not taken from SUMMARY): `retrieval_evidence_coverage.n=16`, `answer_exact_match.n=16`, `context_precision_at_k.n=16`, `ranking_quality.n=16`, all with `excluded_payload_records=2` — the exact numbers this closes the prior verifier's own live 16-vs-18 reproduction to. `git diff` also shows 3 pre-existing tests in `test_score_deterministic.py` needed an added `answer="test answer"` field to remain scorable under the new guard — independent evidence the fix is load-bearing, not cosmetic. |
| 8 | SC-8: wire_contract_conformance redefined to count self-contradiction (D-35) | ✓ VERIFIED (regression) | `usability.py`'s `is_self_contradictory` (lines 111-165) read in full this pass — unchanged from prior verification, all 5 clauses intact; only `has_scorable_payload` was added to the file, nothing in `is_self_contradictory` was touched. |
| 9 | SC-9: Preflight canary-driven query-path checks with fixed floors (D-10, D-11, D-31) | ✓ VERIFIED (regression) | `preflight.py` and `canary.jsonl` not in the 06.3.2-06 diff; prior verification's trace through `check_canary_floors` stands unchanged. |
| 10 | SC-10: All four open 06.3-REVIEW.md findings closed — CR-01, WR-02, WR-03, WR-04 + IN-02 (D-36) | ✓ VERIFIED (regression, re-confirmed at exact line anchors) | Re-grepped rather than assumed: `score.py:524` still raises `ScoreError` for `--emit-calibration-worksheet` + `--no-judge` (CR-01 of 06.3-REVIEW.md, distinct from 06.3.2-REVIEW.md's own unrelated CR-01 — see Known Open Issues below); `score.py:308-316` still selects `primary_arm` from whichever arm has records (WR-03); `score.py:1021` still falls back to the literal `"unknown-generation-model"` marker (WR-04); `pairing.py`'s `deduplicate_by_arm` and `judge.py`'s cache-drop logging were not touched by this plan and stood verified prior. |

**Score:** 10/10 truths verified (0 present-but-behavior-unverified)

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | Concrete graph-on canary question IDs verified against gold entities in the `entities` table (D-11, D-31) | Phase 06.3.3 | `06.3.2-03-SUMMARY.md`'s explicit "Recorded Deferral" to 06.3.3's D-05 store inspection; matches `06.3.2-VALIDATION.md`'s own Manual-Only Verifications row and the shared `06.3.1-CONTEXT.md` family plan, which scopes D-05 to 06.3.3. Not touched by plan 06.3.2-06 (which modified only `usability.py`, `score.py`, `report.md.j2`, and two test files) — carries forward unchanged from the 2026-09-05 verification. Legitimate, documented, not a gap. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `eval/src/lancet_eval/usability.py` | `has_scorable_payload` predicate, sibling of `is_usable` (D-34 family) | ✓ VERIFIED | Present, substantive (documents both boundaries — NO_EVIDENCE carve-out and None/""/whitespace equivalence), consumed by `score.py` at 7 call sites. |
| `eval/src/lancet_eval/score.py` | Single early-exit guard, retired dead counter, published detail on 5 dims + abstention, 5 paired score functions gated | ✓ VERIFIED | All present; `retrieval_skipped` fully removed (repo-wide `grep` confirms zero matches); `_build_mean_dim` extended with `extra_detail` merged across all 3 status branches. |
| `eval/src/lancet_eval/templates/report.md.j2` | Rewritten caveat 6 | ✓ VERIFIED | Caveat 6 states the rule in method language, no numerals, covers infra-failure, payload rule, single shared population, and the paired-delta extension. |
| `eval/tests/test_denominators.py` | Named regression tests building the exact CR-01 record shape | ✓ VERIFIED | `test_scorable_payload_exclusion_and_reconciliation`, `test_whitespace_only_answer_treated_as_payload_less`, `test_abstention_on_unanswerable_payload_exclusion_population_scoped`, `test_empty_retrieved_chunks_snapshot_is_scorable_zero_recall`, `test_all_payload_less_journal_non_ok_status_and_rendered_report`, `test_caveat_6_mentions_payload_rule_and_has_no_numerals` all present and pass. `test_mixed_denominators_gone_and_unusable_rate` passes unchanged. |
| `eval/tests/test_ablation.py` | Named regression test for the paired half | ✓ VERIFIED | `test_paired_ablation_payload_exclusion_across_all_five_metrics` present and passes, asserting `n==3`/`excluded_unscorable_pairs==2` across all 5 paired dimensions. |

### Prohibitions (plan 06.3.2-06 must_haves.prohibitions)

| # | Prohibition | Status | Evidence |
|---|---|---|---|
| 1 | No change to `is_usable` | ✓ RESOLVED | `usability.py` diff is additive only (+23 lines, the new `has_scorable_payload` function); `is_usable`'s body read in full this pass, byte-identical logic to prior verification. |
| 2 | No change to path-health or pairing populations (arm-metrics usable-record list, `form_pairs`) | ✓ RESOLVED | `pairing.py` absent from the 06.3.2-06 diff stat entirely. `arm_metrics[arm]["usable_records"]` (score.py:303-305) still filters solely on `is_usable`/`has_arm_provenance`, not on the new payload predicate. |
| 3 | A present snapshot with an empty retrieved-chunk list stays scorable (NO_EVIDENCE carve-out) | ✓ RESOLVED | `has_scorable_payload` checks `record.snapshot is None` only, never chunk-list length; `test_empty_retrieved_chunks_snapshot_is_scorable_zero_recall` passes (confirmed in this pass's test run). |
| 4 | No new Python dependency; `uv.lock` unchanged | ✓ RESOLVED | `uv.lock` absent from the diff stat; SUMMARY records `uv lock --project eval --check: Resolved 32 packages in 1ms`. |
| 5 | No modification to `eval/runs/` | ✓ RESOLVED | `git status --short eval/runs/` returned empty in this verification pass, independently re-checked after this pass's own re-score (run against a scratch copy outside the working tree). |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `has_scorable_payload` | per-arm scoring loop | single early exit at `score.py:246-251`, before any metric computed | ✓ WIRED | Confirmed by direct read; one predicate, one call site per record, feeding both population accumulators. |
| `payload_excluded_answerable`/`_unanswerable` | rendered report | `_build_mean_dim`'s `extra_detail` (score.py:680-739) and the inline abstention branches (score.py:962-990) | ✓ WIRED | The previously dead `retrieval_skipped` counter has no successor with the same defect — both new accumulators have readers confirmed by passing detail assertions in named tests and by this pass's own live re-score. |
| `has_scorable_payload` | 5 paired ablation score functions | direct call at the top of each of `_score_evidence_coverage/_score_em/_score_f1/_score_cp/_score_mrr` | ✓ WIRED | Confirmed by direct read (score.py:795-882) and by `test_paired_ablation_payload_exclusion_across_all_five_metrics` passing. |

### Data-Flow Trace (Level 4) — independently re-scored, not taken from SUMMARY

Copied `eval/runs/2026-09-03-multihop_rag` to a scratch directory outside the working tree and ran `uv run lancet-eval score --run <scratch-copy> --no-judge` myself with the current (HEAD) code:

| Dimension | n (found by prior verifier, before fix) | n (this pass, live) | excluded_payload_records | Status |
|---|---|---|---|---|
| `retrieval_evidence_coverage` | 16 | 16 | 2 | ✓ FLOWING, denominators now equal |
| `answer_exact_match` | 18 | 16 | 2 | ✓ FLOWING, denominators now equal |
| `context_precision_at_k` | — | 16 | 2 | ✓ FLOWING |
| `ranking_quality` | — | 16 | 2 | ✓ FLOWING |
| `graph_ablation_delta` | 8 pairs | 8 pairs | `excluded_unscorable_pairs: 0` | ✓ FLOWING, unchanged (no payload-less record among the 8 committed pairs — consistent with the plan's own prediction that this half of the fix is latent on real data and proven by the constructed fixture instead) |

These numbers were produced by a re-score I ran in this verification pass (not copied from `06.3.2-06-SUMMARY.md`), and match the SUMMARY's claims exactly. `git status --short eval/runs/` confirmed clean afterward — the committed evidence directory was not touched.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Named reconciliation tests pass | `uv run pytest eval/tests/test_denominators.py eval/tests/test_ablation.py -q` (run from `eval/`) | 23 passed | ✓ PASS |
| Full harness test suite is enumerable at the count SUMMARY claims | `uv run pytest -q --collect-only` | 251 tests collected | ✓ PASS (matches SUMMARY's "251 passed in 16.18s" and the task context's independently-run "251/251 passed") |
| Fix is load-bearing, not cosmetic | `git diff fc082eb^..2bded29 -- eval/tests/test_score_deterministic.py` | 3 pre-existing tests required adding `answer="test answer"` to stay scorable under the new guard | ✓ PASS — corroborates the guard actually changes behavior on previously-passing fixtures |
| Live re-score of real committed data | `uv run lancet-eval score --run <scratch-copy> --no-judge` | See Data-Flow Trace above | ✓ PASS |

Full suite was not re-run in this verification pass beyond the two named files above plus a collect-only enumeration and the live re-score, per the constraint against re-running the whole suite when the task context already reports a clean 251/251 orchestrator run this session.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| OBS-05 | 06.3.2-01 through 06.3.2-06 | Trustworthy evaluation measurement (shared across 06.3.1–06.3.4 by design, D-50) | Satisfied by this phase's 10/10 SCs (as scoped); 2 open code-review criticals flagged for human decision | All 10 ROADMAP success criteria for 06.3.2 now verified. OBS-05 as a whole remains split across 06.3.1/06.3.3/06.3.4 per REQUIREMENTS.md's traceability table — this phase's slice (harness capture, scored dimensions, paired ablation, denominator convention) is functionally complete, with two known-open correctness defects in the abstention dimension (see Known Open Issues) requiring an explicit accept-or-fix decision. |

No orphaned requirements found for this phase in `.planning/REQUIREMENTS.md`.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `eval/src/lancet_eval/score.py` | 285 | `abstention_rate(gold, rec.answer or "", snap_chunks)` called without the `notices` argument (06.3.2-REVIEW.md CR-01) | ⚠️ Warning | Confirmed present in code during this pass. Permanently disables the documented typed-notice (`NOTICE_CODE_NO_EVIDENCE`) abstention-detection path, forcing reliance on brittle text-matching in `abstention_on_unanswerable`. Does not fail any of the 10 numbered SCs and is orthogonal to the SC-7 payload-gate fix (only reached after a record already has a non-blank answer). Routed to Human Verification below rather than silently absorbed, because plan 06's own must_haves explicitly took ownership of abstention scoring ("The same rule governs abstention"). |
| `eval/src/lancet_eval/score.py` | 979-990 | Misleading skip reason `"Corpus contains no unanswerable questions"` printed even when `unanswerable_payload_excluded > 0` (06.3.2-REVIEW.md CR-02) | ⚠️ Warning | Confirmed present in code during this pass. `excluded_payload_records` is still correctly published in `detail` in this branch (satisfies SC-7's literal publication requirement), but the human-readable `reason` text is factually wrong when exclusions exist — directly adjacent to this phase's "read honestly" charter. Routed to Human Verification below. |
| `eval/src/lancet_eval/dimensions.py` | 83-90 | `OBS_04_PLACEHOLDER` constant | ℹ️ Info | Pre-existing (predates this phase), explicitly references a formal follow-up ("Deferred to Phase 999.1"). Not a debt-marker violation, not touched by plan 06.3.2-06. |

No debt markers (`TBD`/`FIXME`/`XXX`) or unreferenced `TODO`/`HACK`/`PLACEHOLDER` found in the files modified by plan 06.3.2-06 (`usability.py`, `score.py`, `report.md.j2`, `test_denominators.py`, `test_ablation.py`).

### Known Open Issues (see Human Verification below)

`06.3.2-REVIEW.md` (refreshed 2026-09-06, after plan 06.3.2-06 landed) found 2 new critical findings, both confirmed present in code during this verification pass and **not yet fixed**. Neither fails a numbered ROADMAP success criterion, and both are demonstrably orthogonal to the SC-7 fix and outside SC-10's four-item scope (SC-10 closes `06.3-REVIEW.md`'s four named findings — a different document with its own, unrelated CR-01 — not `06.3.2-REVIEW.md`'s self-review findings). They are classified as Warnings in the Anti-Patterns table above and escalated as human-verification items per the Escalation Gate pattern, rather than silently marked passing or silently marked failing: a real, confirmed-present correctness defect in a scored dimension this phase's own charter says must "be read honestly" is a developer accept/fix decision, not a mechanical one.

### Human Verification Required

1. **CR-01 — abstention notice-based detection disabled.** See frontmatter `human_verification[0]`. `score.py:285` omits the `notices` argument to `abstention_rate`, permanently disabling the typed-notice detection path documented as the preferred mechanism in `abstention_outcome`.
2. **CR-02 — misleading abstention skip-reason string.** See frontmatter `human_verification[1]`. `score.py:979-990` prints a reason that contradicts its own published `excluded_payload_records` detail when unanswerable records were payload-excluded rather than absent.

### Gaps Summary

The single gap from the prior verification (2026-09-05) — SC-7's usable-population payload asymmetry — is closed. I re-derived the fix from first principles against the codebase rather than trusting SUMMARY.md: read `has_scorable_payload`'s full definition and both call-site families (aggregate loop and paired score functions), confirmed the dead `retrieval_skipped` counter is gone via a repo-wide `grep`, ran the two named regression tests directly (23/23 pass) rather than accepting the SUMMARY's reported pass count, independently re-scored a fresh scratch copy of the committed 2026-09-03 journal myself and reproduced the SUMMARY's claimed 16/16/excluded-2 numbers exactly, and found corroborating evidence in `git diff` that three pre-existing tests needed a code change (`answer="test answer"`) to keep passing — a signal the guard actually altered scoring behavior rather than merely adding an unused predicate. The report caveat was read directly and confirms the three-claim, no-numerals contract the plan specified. All five of plan 06's `must_haves.prohibitions` were independently checked and hold. The one previously-recorded deferral (canary gold-entity confirmation, → Phase 06.3.3) is untouched by this plan and carries forward unchanged.

Two new critical findings from the phase's freshly-refreshed self-review (`06.3.2-REVIEW.md`) remain open in code (CR-01: abstention notices argument omitted; CR-02: misleading skip-reason string). Both were independently confirmed present at their cited line numbers during this verification pass. Neither is a numbered ROADMAP success criterion for this phase, so the status is not `gaps_found` — but both are real, confirmed-present correctness defects in a dimension this phase's own gap-closure plan explicitly took ownership of scoring, and are adjacent to this phase's "read honestly" charter. Per the Escalation Gate pattern, these are surfaced as human-verification items rather than silently resolved either way: the status is `human_needed`, not `passed`, pending a developer decision to fix, defer with a named follow-up, or explicitly accept.

---

_Verified: 2026-09-06T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
