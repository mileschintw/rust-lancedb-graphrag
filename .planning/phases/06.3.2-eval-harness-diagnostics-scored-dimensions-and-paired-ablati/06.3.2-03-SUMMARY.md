# Phase 06.3.2 Plan 03: Forensic Raw-SSE Stream Retention, Canary Set, and Preflight Floors Summary

## Execution Overview
- **Task 1: Bounded raw-SSE stream forensics and deterministic baseline**:
  - Implemented `RawEventSink` and `baseline_sample_ids` in `eval/src/lancet_eval/raw_events.py`.
  - Pinned constants: `RAW_EVENT_BYTES_PER_UNIT = 32768` (32 KB), `RAW_EVENT_BYTES_PER_RUN = 67108864` (64 MB), ensuring run cap > 2000 * unit cap so retention never depends on thread completion order.
  - Added off-by-default `capture_raw_events` option to `run_query` in `client.py` with in-memory buffering bounded by `RAW_EVENT_BYTES_PER_UNIT`.
  - Added raw fields to `QueryOutcome`: `raw_events`, `raw_events_truncated`, `raw_events_dropped_frames`.
  - Updated `drive_one` and `drive` in `run.py` to write raw events for failing records or questions in the deterministic baseline sample (first 25 question IDs sorted ascending).
  - Ensured sink errors never raise or convert successful queries into fabricated error records.
  - Created `eval/tests/test_raw_events.py` covering all behavior specifications.

- **Task 2: Canary set commitment**:
  - Generated `eval/corpora/multihop_rag/canary.jsonl` with 7 rows across 6 distinct question IDs matching `questions.sample.jsonl` verbatim with 0 `_ms` timing literals.
  - 4 retrieval canaries re-derived by taking first of each question type sorted ascending by ID:
    - `mhr-0073ab564e55` (comparison)
    - `mhr-01a03d465730` (inference)
    - `mhr-00fc91a80765` (temporal)
    - `mhr-0279d4a349c3` (null: permits 0 chunks, requires retrieval completion)
  - 2 graph canaries re-derived by ranking non-null questions by number of distinct source documents spanned (descending, tie-breaking by ID ascending):
    - `mhr-0d5e238015ef` (inference, 4 distinct sources)
    - `mhr-12912d800c0c` (inference, 4 distinct sources)
  - 1 graph-off twin of `mhr-0d5e238015ef` requiring ablation notice 18 (`GRAPH_ABLATION`).
  - *Recorded Deferral*: The two graph canaries' gold entities have NOT yet been confirmed present in the entities table. Confirmation is deferred to Phase 06.3.3 D-05 store inspection. Until then, a red graph canary is ambiguous between a graph defect and an entity absent from the store.

- **Task 3: Preflight canary manifest & live floors probes**:
  - Implemented `check_canary_manifest` in `eval/src/lancet_eval/preflight.py` running unconditionally outside reachability gates.
  - Implemented `read_workflow_timeouts` reading `[engine.workflow]` timeout keys live via `tomllib` without hardcoding milliseconds or exposing secrets.
  - Implemented `check_canary_floors` probing the query path via `run_query` against canary floors and live node duration budgets.
  - Gated `check_canary_floors` inside `run_preflight_checks` only when gateway and engine are reachable.
  - Created `eval/tests/test_preflight_canary.py` verifying manifest validation, live duration comparisons, and reachability gating.

## Verification
- All 204 tests pass offline with zero network calls and without `OPENROUTER_API_KEY`.
- `ruff check --preview` clean across all modified and newly created files.
- `uv lock --project eval --check` verified clean.
