---
phase: 06.3.2
plan: 05
type: execute
wave: 4
depends_on: [06.3.2-03, 06.3.2-04]
files_modified:
  - eval/src/lancet_eval/score.py
  - eval/src/lancet_eval/judge.py
  - eval/src/lancet_eval/report.py
  - eval/src/lancet_eval/templates/report.md.j2
  - eval/report.schema.json
  - eval/tests/test_review_findings.py
  - eval/tests/test_report.py
  - eval/tests/test_schema.py
  - eval/tests/test_score_judged.py
  - eval/tests/test_score_deterministic.py
  - eval/tests/test_judge.py
  - eval/tests/test_secrets.py
autonomous: true
requirements: [OBS-05]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Asking for a calibration worksheet without the judge fails loudly instead of silently emitting an unverified first-twenty selection (CR-01)."
    - "The primary scoring arm is chosen from the arm that actually has records, not from dictionary insertion order (WR-03)."
    - "A configuration read that fails is visible: the report never carries a plausible-looking model name the harness could not actually read, and a judging run refuses to proceed when it cannot verify the judge and generation models differ (WR-04)."
    - "A judge cache entry that cannot be loaded is counted and reported rather than silently discarded, so paid verdicts cannot vanish without a trace (IN-02)."
    - "Every registered dimension renders as a row in the report, and every diagnostic count a dimension publishes is legible in that row."
    - "The report's methodology text describes the conventions the code actually implements — the denominator rule, the redefined conformance dimension, and the fact that graph emptiness is a published number rather than a run failure."
  artifacts:
    - "eval/tests/test_review_findings.py — one named regression test per closed finding"
    - "eval/src/lancet_eval/templates/report.md.j2 — the completed methodology caveats"
  key_links:
    - "The four findings are the last open items from 06.3-REVIEW.md and all of them fabricate or discard provenance, which is the same failure class this phase exists to close"
    - "Report completeness is asserted against the registry, so a dimension added by an earlier plan in this phase cannot silently fail to render"
  prohibitions:
    - "No new Python dependency; uv.lock unchanged"
    - "A warning emitted on a configuration read failure must not include the file's contents or any parsed value"
    - "The calibration worksheet size stays as it is — resizing it is D-48 and belongs to Phase 06.3.4"
---

<objective>
Close the four open findings from the Phase 06.3 code review and finish the report surface, so
the next run can be read honestly end to end.

Purpose: all four findings are the same failure class this phase exists to close. A calibration
worksheet that silently degrades to an unverified selection, an arm chosen by dictionary order, a
configuration read that fails into a plausible-looking default, and a judge cache that drops
entries without a word — each of them fabricates or discards provenance quietly. The 2026-09-03
report was believable precisely because every one of its failure paths was silent.

Output: CR-01, WR-03 and WR-04 with IN-02 closed with named regression tests, plus a report whose
methodology text matches the conventions the code now implements.

WR-02 is implemented by plan 06.3.2-04, which applies deduplication once to the whole loaded
journal before the arm grouping, so the judged loop and the worksheet selection this plan owns
read a collapsed list. This plan does not re-implement it and it does not assume it either: task
1 carries its own regression test against the judging and worksheet consumers the finding
actually names, and a stated fallback if the upstream plan landed a narrower fix. The phase claim
that WR-02 is closed is only as good as an assertion made at the consumer, and the consumer is
here.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-CONTEXT.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-AI-SPEC.md
@.planning/phases/06.3-python-evaluation-harness-benchmark-corpora-and-recorded-run/06.3-REVIEW.md
@.planning/phases/06.3.2-eval-harness-diagnostics-scored-dimensions-and-paired-ablati/06.3.2-RESEARCH.md
@.planning/phases/06.3.2-eval-harness-diagnostics-scored-dimensions-and-paired-ablati/06.3.2-04-SUMMARY.md
@CLAUDE.md
</context>

<reversibility rating="costly">
D-35 redefined `wire_contract_conformance` in plan 06.3.2-02. The 2026-09-03 value of that
dimension becomes non-comparable and Phase 6.4's methodology must say so. This plan is where that
statement lands in the rendered report. Flagged, not gated — no decision checkpoint is inserted.
</reversibility>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-01 and WR-03 — the worksheet refuses to guess, the primary arm is chosen by data</name>
  <files>eval/src/lancet_eval/score.py, eval/tests/test_review_findings.py</files>
  <read_first>
    - `eval/src/lancet_eval/score.py:475-556` — the calibration worksheet block, in particular the cache-verified selection at 493-511 and the unconditional first-twenty fallback at 512-513 that is taken whenever the judge is off.
    - `eval/src/lancet_eval/score.py:274-280` — the primary arm choice and the insertion-order fallback.
    - `eval/src/lancet_eval/score.py:181-186` — where the arm dictionary is pre-seeded with both arm keys, which is why the fallback can never select the second one.
    - `eval/src/lancet_eval/score.py:56-58` — `ScoreError`, the loud-failure idiom this module already uses for refusing to produce a misleading artifact; `score.py:291-310` is a worked example of a message that explains the consequence rather than just the condition.
    - `eval/tests/test_score_judged.py:294-310` and its three sibling worksheet tests — all four pass the judge-enabled flag, so making the disabled combination raise breaks none of them.
    - `.planning/phases/06.3-python-evaluation-harness-benchmark-corpora-and-recorded-run/06.3-REVIEW.md` findings CR-01 and WR-03 in full.
  </read_first>
  <behavior>
    - Requesting a calibration worksheet with the judge disabled raises the module's own error
      type, with a message naming both flags and stating why the combination cannot produce a
      usable worksheet.
    - Requesting a calibration worksheet with the judge enabled behaves exactly as it does today,
      selecting only records whose cached verdict exists — the four existing worksheet tests
      continue to pass unchanged.
    - With records in the graph-on arm only, the primary arm is graph-on.
    - With records in the graph-off arm only, the primary arm is graph-off — this is the case the
      unreachable fallback never handled.
    - With records in both arms, the primary arm is graph-on.
    - With no records in either arm, the choice is deterministic and repeatable rather than
      dependent on dictionary ordering.
  </behavior>
  <action>
    Close CR-01 by refusing the combination rather than degrading into it. When a calibration
    worksheet is requested while the judge is disabled, raise the module's own error type with a
    message that names both flags and states the consequence: the selection logic that verifies a
    cached verdict exists cannot run without the judge, so what would be emitted is an unverified
    slice of the first records in the arm, which a human would then spend an hour scoring against
    verdicts that do not exist. That is exactly the failure Phase 06.3's own gap closure hit,
    where only one of twenty emitted rows resolved to a cached verdict.

    Phrase the remedy in that message as enabling the judge, not as removing a flag. The
    judge-disabled option is the CLI default (`cli.py:266`), so an operator hitting this error has
    almost certainly not passed anything to disable it; telling them to unset a flag they never
    set sends them looking for something that is not there. The message must tell them to re-run
    with judging enabled.

    Do not change the worksheet's size. Resizing it is a separate locked decision belonging to
    Phase 06.3.4, and touching it here would silently pre-empt that phase.

    Close WR-03 by choosing the primary arm from the arm that has records. The current fallback
    reads the first key of a dictionary that is pre-seeded with both arm names, so it is
    unreachable in its intended sense and always yields the same arm — a graph-off-only journal
    would be scored as though it were the graph-on arm, mislabelling every primary dimension.
    Prefer the graph-on arm when it has records, fall back to graph-off when it has records, and
    when neither does, pick deterministically by a stated rule rather than by iteration order.

    Verify WR-02 at its consumers rather than inheriting the claim. Plan 06.3.2-04 applies
    deduplication once to the whole loaded journal, before the arm grouping, precisely so the
    judged loop and the worksheet selection in this module read a collapsed list. Read that
    plan's summary first. If it landed as specified, add a regression test here asserting the
    consumer-side behaviour: a journal holding two records for one question in the primary arm
    yields a judged population of one for that question and one worksheet row for it. If the
    summary shows a narrower fix that only deduplicates the pairing join, apply the
    deduplication to the primary-arm record list before the judging block in this task instead,
    and record the divergence in the summary. Either way this plan finishes with the assertion,
    not with the assumption — the finding is about double-weighted judged scores and duplicated
    worksheet rows, and a phase cannot claim it closed while nothing tests either.

    Add one named regression test per finding, each asserting the behaviour the finding describes
    rather than the presence of the fix. Do not spend a test on the case where neither arm has
    records: `score_run` already refuses an empty journal before the arm choice is reached, so
    that branch is unreachable. Keep it as a deterministic belt-and-suspenders default, and say so
    in a comment rather than in a test.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_review_findings.py eval/tests/test_score_judged.py eval/tests/test_score_deterministic.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --project eval pytest eval/tests -q` exits 0 offline, no gateway, no network, no `OPENROUTER_API_KEY`.
    - `uv run --project eval ruff check --preview eval/src eval/tests` exits 0 and `uv lock --project eval --check` exits 0.
    - A named test asserts that requesting a worksheet with the judge disabled raises `ScoreError`, and that the message text mentions both the worksheet option and the judge option, and instructs the operator to re-run with judging enabled rather than to remove a flag.
    - A named test asserts the WR-02 consumer contract: a journal holding two records for one question in the primary arm yields a judged population of one for that question and exactly one worksheet row for it.
    - The four existing worksheet tests in `eval/tests/test_score_judged.py` pass unchanged, with no edit to their arguments.
    - A named test scores a journal containing graph-off records only and asserts the primary arm resolves to graph-off and the primary dimensions report that arm's record count.
    - A named test scores a journal containing both arms and asserts the primary arm resolves to graph-on.
    - `uv run --project eval python -c "import inspect;from lancet_eval import score;src=inspect.getsource(score.score_run);assert 'next(iter(' not in src;print('wr03-closed')"` prints `wr03-closed`.
    - The worksheet row limit is unchanged from the pre-change value, confirmed by diffing that constant in the summary.
  </acceptance_criteria>
  <done>
    The harness will not hand a human a worksheet it cannot verify, and the arm a report is
    primarily about is the arm that actually has data in it.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-04 and IN-02 — a read that fails says so, and a dropped verdict is counted</name>
  <files>eval/src/lancet_eval/score.py, eval/src/lancet_eval/judge.py, eval/tests/test_review_findings.py</files>
  <read_first>
    - `eval/src/lancet_eval/score.py:64-75` — the configuration reader whose broad handler returns an empty string on any failure.
    - `eval/src/lancet_eval/score.py:318-330` — the guard that refuses to judge with the same model the engine generates with, and the condition that skips it entirely when the model name came back empty.
    - `eval/src/lancet_eval/score.py:770-790` — the metadata assembly. Two fabrications live here, not one: the generation model falls back to a hardcoded provider model string when the configuration read returned nothing, and immediately below it the embedding model is seeded with a hardcoded provider string and overwritten only if some record's snapshot supplies one. Both are in this task's scope.
    - `eval/src/lancet_eval/report.py:23-70` — `RunMetadata` and its non-blank field validator, which constrains what may be substituted for an unknown value.
    - `eval/src/lancet_eval/judge.py:200-222` — the cache loader, its per-entry handler that continues past an unparseable entry and its outer handler that abandons the whole file.
    - `eval/src/lancet_eval/judge.py:222-245` — `save`, to confirm what a partially loaded cache would write back.
    - `.planning/phases/06.3-python-evaluation-harness-benchmark-corpora-and-recorded-run/06.3-REVIEW.md` findings WR-04 and IN-02 in full.
    - `eval/tests/test_secrets.py` — the existing convention for asserting a value never reaches an output.
  </read_first>
  <behavior>
    - An unreadable or malformed engine configuration produces a visible warning naming the path
      and the exception type, and the report's generation model is an explicit unknown marker
      rather than a provider model string the harness never read.
    - The warning contains no line of the configuration file and no parsed value.
    - A judging run whose configuration could not be read refuses to proceed rather than skipping
      the same-model guard, because that guard is the only thing standing between a judged score
      and a model grading its own output.
    - A scoring run with the judge disabled and an unreadable configuration still completes, with
      the unknown marker in the metadata — the guard only matters when judging.
    - A cache file containing one valid and one unparseable entry loads the valid one, counts the
      dropped one, and emits a warning naming the count.
    - A cache file that is not valid JSON at all emits a warning naming the path and the
      exception type, and does not present itself as an empty cache without comment.
    - An absent cache file is not a warning — it is the normal first-run state.
    - No warning message contains a verdict payload, an API key, or the cache file's contents.
  </behavior>
  <action>
    Close WR-04 in all three of its places, because the finding is one mechanism with three
    consequences.

    Make the configuration reader report its failure through the standard library's logging at
    warning level, naming the path it tried and the exception type. Do not include the file's
    contents, the parsed document, or any value read from it — that file holds provider
    configuration alongside the model names, and a warning that echoes it would turn a diagnostic
    into a disclosure.

    Distinguish "the file said nothing" from "the read failed" at the call sites. Where the
    metadata is assembled, an unread model name becomes an explicit unknown marker rather than a
    hardcoded provider string; the metadata validator forbids a blank value, so the marker must be
    a real non-blank token that a reader will recognise as an absence. A fabricated model name in
    a published benchmark's provenance is an audit failure of exactly the kind D-33 was written
    about.

    Apply the same treatment to the embedding model in the same metadata assembly. The finding's
    text names only the generation model, but `score.py:779-783` seeds the embedding model with a
    hardcoded provider string and overwrites it only if some record's snapshot happens to carry
    one — so a journal of snapshot-less records publishes a provider model name nobody read from
    anywhere. That is the identical mechanism with the identical consequence: a published
    benchmark asserting provenance it does not have. Fixing the generation model while leaving its
    sibling in place would close the finding's letter and leave its substance. Use the first
    snapshot that carries an embedding model, and the unknown marker when none does.

    Where the same-model guard runs, refuse to judge when the generation model could not be read.
    Today an empty name short-circuits the comparison and the guard silently does not apply, which
    means the one configuration state most likely to be broken is also the one where the safety
    check is skipped. A scoring run with the judge disabled must still complete, since the guard
    is irrelevant there.

    Close IN-02 in the cache loader. Count entries that fail to validate and warn once with the
    count rather than continuing past each in silence, and warn when the whole file fails to parse
    instead of returning as though the cache were simply empty. These are paid verdicts; losing
    them quietly means the next run re-spends for them and nobody knows why the bill moved. Keep
    an absent file silent — that is the normal first-run state, not a fault. No message may carry a
    verdict payload or the file's contents.

    Add one named regression test per behaviour bullet, using the test suite's existing convention
    for asserting a sensitive value never reaches an output.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_review_findings.py eval/tests/test_judge.py eval/tests/test_secrets.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --project eval pytest eval/tests -q` exits 0 offline.
    - `uv run --project eval ruff check --preview eval/src eval/tests` exits 0 and `uv lock --project eval --check` exits 0.
    - A named test points the configuration reader at a malformed file, captures the emitted log records, and asserts one warning naming the path and the exception type.
    - A named test asserts that warning contains no substring of the malformed file's body, using a fixture whose body includes a recognisable secret-shaped token.
    - A named test asserts the report metadata carries the unknown marker, and not any provider model string, when the configuration could not be read.
    - A named test asserts the report metadata's embedding model carries the unknown marker, and not any provider model string, when no record's snapshot supplies one; a sibling test asserts it carries the first snapshot's value when one does.
    - A named test asserts a judging run raises when the generation model could not be read, and a sibling test asserts a non-judging run completes in the same condition.
    - A named test loads a cache with one valid and one unparseable entry and asserts the valid entry is present, the dropped count is one, and a warning names that count.
    - A named test asserts a wholly unparseable cache file emits a warning, and a sibling test asserts an absent cache file emits none.
    - `uv run --project eval python -c "import inspect;from lancet_eval import judge;src=inspect.getsource(judge.JudgeCache._load);assert 'logging' in src or 'logger' in src or 'warn' in src;print('in02-closed')"` prints `in02-closed`.
  </acceptance_criteria>
  <done>
    Every silent failure path named in the two findings now says something: a failed configuration
    read is a warning and an unknown provenance marker, a judging run will not proceed without the
    model it must compare against, and a dropped verdict is counted out loud.
  </done>
</task>

<task type="auto">
  <name>Task 3: The report says what the code does — completeness, legibility, methodology</name>
  <files>eval/src/lancet_eval/report.py, eval/src/lancet_eval/templates/report.md.j2, eval/tests/test_report.py</files>
  <read_first>
    - `eval/src/lancet_eval/templates/report.md.j2` in full — the metadata table at 11-31, the dimension table at 33-43 including the non-ok branch plan 06.3.2-01 extended, and the methodology caveats at 45-53 including the denominator caveat plan 06.3.2-01 rewrote and the ablation caveat plan 06.3.2-04 added.
    - `eval/src/lancet_eval/report.py:82-100` — `format_score` and `format_details`, the two helpers the template is handed.
    - `eval/src/lancet_eval/report.py:102-110` — `compute_result_hash`, to confirm which fields the run's result hash covers before changing any rendering.
    - `eval/src/lancet_eval/dimensions.py` — the full `REGISTERED_DIMENSIONS` list after plans 06.3.2-01, 06.3.2-02 and 06.3.2-04 have added to it.
    - `eval/tests/test_report.py:97-135` — the existing test that renders every registered name; `eval/tests/test_schema.py:17-24` — the byte-identity assertion on the committed schema.
    - `.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-CONTEXT.md` D-02, D-29, D-34, D-35 — the four conventions the methodology text must state.
  </read_first>
  <action>
    Finish the report surface so a reader who has never seen this phase can read the numbers
    correctly.

    Make the score column type-aware before touching the detail cells, because this phase created
    the first dimensions the current heuristic gets wrong. `format_score` classifies a score as a
    judged scale when it exceeds 1.0 or when its name contains a judged keyword. That was sound
    while every non-judged dimension was a ratio. It is no longer: `retrieve_latency_ms`,
    `graph_latency_ms`, `graph_ablation_latency_delta` and `graph_ablation_prompt_token_delta`
    score in milliseconds and token counts, routinely exceed 1.0, and would be rendered through
    the judged branch. The numbers stay correct, but the methodology caveat promising three
    decimals for ratios and two for judged scales becomes a false statement about the table
    beneath it — and this plan's whole job is making the methodology text true. Branch on the
    dimension name: names ending in a millisecond suffix or containing a latency marker render as
    absolute magnitudes at one decimal place, names containing a judged keyword keep the judged
    precision, and everything else stays at ratio precision. A latency score above 1.0 must not
    reach the judged branch by magnitude alone.

    Make detail cells legible, with the same type awareness. Every new dimension publishes several
    diagnostic floats, and the helper that renders them currently formats raw float repetitions,
    so a bootstrap bound arrives with full binary precision in the middle of a table cell. Do not
    round everything to ratio precision either — the detail mapping is typed as floats but carries
    three different kinds of value. Counts are the common case in this phase: pair counts, drop
    counts, missing-metadata counts and collapsed-record counts are all integers stored as floats,
    and rendering a pair count of six as a three-decimal value is noise that makes the table
    harder to read, not more precise. Render float values that are exactly integral as integers,
    latency and token keys at one decimal place, and every remaining ratio at the existing ratio
    precision. Keep full precision in the JSON output where the value is machine-read.

    Confirm first that the run's result hash covers only dimension names, statuses, scores and
    counts and does not reach the detail mapping, so these rendering changes cannot move a
    published hash; record that check in the summary.

    Assert completeness rather than assuming it. Extend the existing test that renders every
    registered dimension so it also builds each one with a representative detail mapping and
    asserts the rendered row carries at least one detail key — a dimension that this phase added
    but that never reaches the table would otherwise pass silently.

    Complete the methodology caveats so the text matches the code. Five statements are still
    missing after the earlier plans in this phase. First, that graph presence and influence are
    published numbers and that an empty graph result never makes a run fail, with the single
    exception of the committed preflight canary, whose designated rows assert a graph hit for
    questions whose entities are known to be present. Second, that the conformance dimension now
    counts self-contradiction rather than parse success, that its previous value from the
    2026-09-03 run is therefore not comparable, and that the earlier run would score roughly 0.034
    under the current definition. Third, that a failing query's raw stream is retained alongside
    the journal, together with a fixed sample of successful ones, so a reader knows the forensic
    trail exists and is bounded. This third statement is why plan 06.3.2-03 is a declared
    dependency of this plan: the sentence describes an artifact that plan produces, and a
    methodology caveat asserting a forensic trail that does not yet exist would be the same class
    of unearned claim this phase exists to remove.

    Fourth, that the reported retrieval latency is the total elapsed duration of the hybrid
    retrieval node — dense vector search, lexical search and their fusion together — because there
    is one duration on the wire and no separate per-path timer, so a reader does not mistake it
    for a dense-only measurement. Fifth, that the rounding convention now has three cases rather
    than two: ratios, judged scales, and absolute magnitudes such as milliseconds and token
    counts.

    Keep every caveat a statement about method, not a claim about a result. Numbers belong in the
    dimension table.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_report.py eval/tests/test_schema.py -q</automated>
    <human-check>Render a report from a journal containing known-unusable records and read it end to end. Confirm the unusable count and rate are visible before the quality scores, that no dimension presents infrastructure failure as a quality zero, that the ablation row shows its pair count and coverage beside the delta, and that the methodology caveats describe what the code does. This is the second Manual-Only Verification recorded in `06.3.2-VALIDATION.md`; it is a human reading, not an assertion. `workflow.human_verify_mode` is end-of-phase, so this is performed at phase verification, not as a blocking checkpoint mid-plan.</human-check>
  </verify>
  <acceptance_criteria>
    - `uv run --project eval pytest eval/tests -q` exits 0 offline.
    - `uv run --project eval ruff check --preview eval/src eval/tests` exits 0 and `uv lock --project eval --check` exits 0.
    - `uv run --project eval pytest eval/tests/test_schema.py::test_schema_file_byte_identical -q` passes. If any Pydantic field was added, regenerate `eval/report.schema.json` in the same commit with `uv run --project eval python -c "from pathlib import Path; from lancet_eval.report import emit_schema; Path('eval/report.schema.json').write_text(emit_schema(), encoding='utf-8', newline='')"` and re-run.
    - A named test asserts every name in `REGISTERED_DIMENSIONS` renders as a row and that a dimension carrying a detail mapping renders at least one of its keys.
    - A named test asserts a detail float renders rounded, by giving a dimension a long-precision detail value and asserting the rendered cell does not contain the full-precision form.
    - A named test asserts an integral detail float renders without a decimal fraction, by giving a dimension a pair count of six and asserting the rendered cell contains the bare integer and not a three-decimal form.
    - A named test asserts a latency dimension scoring above 1.0 does not render through the judged branch: given a millisecond-named dimension with a score of `1234.5678`, the rendered score cell is the absolute magnitude at one decimal place, not the judged two-decimal form applied by magnitude.
    - A named test asserts a genuine judged dimension still renders at judged precision, so the branch change did not reclassify the dimensions the heuristic was written for.
    - The methodology caveat describing rounding is updated to state all three conventions — ratios, judged scales and absolute magnitudes — and matches what the renderer does, asserted by a named test.
    - A named test asserts the result hash is identical before and after the rendering change for the same dimension list, proving detail rendering is outside the hash.
    - `uv run --project eval python -c "from pathlib import Path;t=Path('eval/src/lancet_eval/templates/report.md.j2').read_text(encoding='utf-8').lower();missing=[k for k in ('canary','self-contradiction','raw','unusable') if k not in t];assert not missing, missing;print('methodology-complete')"` prints `methodology-complete`.
    - The rendered report for a fixture journal is inspected by hand and the finding recorded in the summary: the unusable count and rate are visible, no dimension presents infrastructure failure as a quality score, and the ablation row shows its pair count and interval.
  </acceptance_criteria>
  <done>
    Every dimension this phase created reaches the rendered table with legible diagnostics, and the
    methodology text states the denominator convention, the redefined conformance dimension, the
    graph-emptiness rule and the raw-retention artifact.
  </done>
</task>

</tasks>

<review_incorporation>
## Cross-AI review findings folded into this plan

From `06.3.2-REVIEWS.md` (reviewers: cursor, antigravity). Both reviewers rated this plan's own
changes low risk; cursor's HIGH is a cross-plan claim about WR-02 rather than a defect in the
work this plan does.

| Finding | Severity | Disposition |
|---|---|---|
| This plan claims WR-02 closed but does not implement it, leaving it a cross-plan assumption | HIGH (cursor) | Incorporated on both sides. Plan 06.3.2-04 now deduplicates the whole loaded journal before the arm grouping, so the judging and worksheet consumers this module owns read a collapsed list. This plan stops asserting the claim and starts testing it: the objective says so plainly, task 1's action instructs reading the upstream summary first and applying the deduplication here if the upstream fix landed narrower, and a named consumer-side acceptance test covers the judged population and the worksheet row count. |
| `depends_on` omits plan 06.3.2-03 while task 3 writes a caveat about raw retention | MEDIUM (cursor) | Incorporated. `depends_on` is now `[06.3.2-03, 06.3.2-04]`. The wave is unchanged at 4, since the maximum predecessor wave is still 3. Task 3's action states why the dependency exists: a methodology caveat describing a forensic trail must not ship before the trail does. |
| Embedding-model provenance is the same WR-04 mechanism and is left in place | MEDIUM (cursor) | Incorporated into task 2, in the same metadata assembly as the generation model, with `score.py:779-783` cited in `<read_first>` and two named acceptance tests. |
| `format_score` will mis-label latency dimensions; blanket ratio precision renders counts noisily | MEDIUM (cursor) / LOW (antigravity) | Incorporated into task 3, which owns `report.py`. Both halves are specified: the score column branches on the dimension name so a latency score above 1.0 cannot reach the judged branch by magnitude, and the detail renderer emits integral floats as integers, latency and token keys at one decimal, and ratios at ratio precision. Four named acceptance tests, plus an updated rounding caveat asserted against the renderer. |
| WR-03's "neither arm has records" branch is unreachable | LOW (cursor) | Incorporated as an instruction not to spend a test on it: the empty-journal refusal upstream makes the branch dead, so it stays as a deterministic default documented in a comment. |
| The CR-01 message should tell the operator to enable judging, not to unset a flag that is already the default | LOW (cursor) | Incorporated into the action and into the acceptance criterion on the message text. |
| Clarify that reported retrieval latency covers the whole hybrid node | Suggestion (antigravity) | Incorporated as the fourth methodology caveat. |
| `compute_result_hash` covers only name, status, score and count, so detail rendering cannot move the hash | Confirmation (cursor) | No change needed; the plan's "confirm first" check was already correct and remains, now stated to include the score-column change as well. |
</review_incorporation>

<out_of_scope>
| Item | Owner | Why not here |
|---|---|---|
| Resizing the calibration worksheet | 06.3.4 (D-48) | The worksheet size and the fresh human calibration belong to the corrected re-drive. CR-01 is about the bypassed verification, not the row count. |
| Performing human calibration | 06.3.4 (D-48) | No agent back-fill; the existing 16 scores judge answers that are being replaced. |
| Publishing corrected numbers or a root-cause document | 06.3.4 (D-54) | This phase makes the measurement trustworthy; it does not produce the corrected run. |
| The `get_or_create_table` fail-open finding | 06.3.1 (D-24, D-26) | Engine-side, and already planned in the sibling. It is not one of the four harness findings D-36 names. |
</out_of_scope>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This plan's contribution. The complete phase inventory is the `artifacts_this_phase_produces`
section of `06.3.2-01-PLAN.md`.

| Kind | Artifact |
|---|---|
| New behaviour in `score.py` | the worksheet refusal on a judge-disabled run, the data-driven primary-arm choice, the unknown-provenance marker for BOTH the generation model and the embedding model, the judging refusal on an unreadable configuration, and the consumer-side WR-02 regression assertion on the judged and worksheet paths |
| New behaviour in `judge.py` | the counted-and-warned cache entry drop and the whole-file parse warning |
| New behaviour in `report.py` | type-aware score formatting so absolute-magnitude dimensions do not render through the judged branch; type-aware detail rendering with integral floats as integers, latency and token keys at one decimal and ratios at ratio precision; full precision retained in the JSON output |
| New methodology caveats in `report.md.j2` | graph emptiness is a published number and only the canary may fail on a missing graph hit; the conformance dimension counts self-contradiction and its predecessor value is not comparable; a failing query's raw stream is retained alongside the journal; the reported retrieval latency covers the whole hybrid node rather than a dense-only timer; the rounding convention now has three cases |
| New test module | `eval/tests/test_review_findings.py` |
| New dimension names | None — this plan adds no scored dimension |
| New CLI flags | None. The calibration worksheet's existing options are unchanged in name and in size; only the invalid combination now fails. |
| Schema | `eval/report.schema.json` expected byte-identical; regenerate in the same commit if any Pydantic field is added. |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `config/config.toml` → scorer | A local file holding provider configuration read for two model names. |
| `judge_cache.json` → scorer | A file of paid model verdicts, re-read across runs. |
| report metadata → published artifact | The provenance a reader will cite. |
| log output → operator terminal and any capture | Warning text added by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.2-22 | Information Disclosure | the configuration-read failure warning | high | mitigate | The warning carries the path and the exception type only; a named test drives a malformed fixture containing a secret-shaped token and asserts that token appears in no emitted log record. |
| T-06.3.2-23 | Repudiation | fabricated generation-model provenance | high | mitigate | An unread model name becomes an explicit unknown marker rather than a hardcoded provider string, asserted by a named test on the assembled metadata. |
| T-06.3.2-24 | Spoofing | judging with the same model that generated | high | mitigate | The guard no longer short-circuits on an empty model name; a judging run refuses when the generation model could not be read, with a non-judging run still permitted. |
| T-06.3.2-25 | Tampering | silent judge-cache entry loss | medium | mitigate | Unparseable entries are counted and warned once; a wholly unparseable file warns rather than presenting as empty; an absent file stays silent. No message carries a verdict payload. |
| T-06.3.2-26 | Repudiation | an added dimension that never renders | medium | mitigate | Report completeness is asserted against the registry, so a dimension added by any plan in this phase cannot silently fail to reach the table. |
| T-06.3.2-27 | Information Disclosure | the calibration worksheet refusal message | low | accept | The message names two command-line flags and the consequence; it carries no file content and no credential. |
| T-06.3.2-SC | Tampering | npm/pip/cargo/uv installs | high | accept | No package-manager install task exists: logging is standard library and no new dependency is introduced. `uv lock --project eval --check` is an acceptance criterion on all three tasks. No `[ASSUMED]` or `[SUS]` package is introduced. |
</threat_model>

<verification>
- `uv run --project eval pytest eval/tests -q` exits 0 offline.
- `uv run --project eval ruff check --preview eval/src eval/tests` exits 0.
- `uv lock --project eval --check` exits 0.
- `eval/report.schema.json` is byte-identical to `emit_schema()`.
- `git status --short eval/runs/` reports no modification to the retained 2026-09-03 evidence.
</verification>

<success_criteria>
- ROADMAP SC10 satisfied together with plan 06.3.2-04: all four open `06.3-REVIEW.md` findings are closed — CR-01, WR-02, WR-03, WR-04 and IN-02 — each with a named regression test. WR-02 is implemented in plan 06.3.2-04 as a journal-wide deduplication applied before the arm grouping, and is asserted HERE at the judging and worksheet consumers the finding names, so the phase claim rests on a test rather than on a cross-plan assumption.
- ROADMAP SC3 completed on the reporting side: graph health and per-path dimensions render, and the methodology text states that graph emptiness never gates a run.
- The report's methodology matches the implemented conventions for denominators, conformance, graph emptiness and raw retention.
</success_criteria>

<output>
Create `.planning/phases/06.3.2-eval-harness-diagnostics-scored-dimensions-and-paired-ablati/06.3.2-05-SUMMARY.md` when done.
</output>
