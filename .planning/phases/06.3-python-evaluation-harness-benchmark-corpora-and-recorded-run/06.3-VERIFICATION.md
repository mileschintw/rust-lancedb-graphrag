---
phase: 06.3-python-evaluation-harness-benchmark-corpora-and-recorded-run
verified: 2026-09-04T23:30:00Z
status: human_needed
score: 11/11 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 11/11
  gaps_closed: []
  gaps_remaining: []
  regressions: []
deferred: []
coincidental_reliance_items: []
prohibitions:
  - must_have: "MUST NOT fabricate, simulate, infer, extrapolate or back-fill any calibration score (06.3-11-PLAN.md, category evidence-integrity, verification: manual)."
    status: flagged
    verification: manual
    evidence: "Independently confirmed commit 91c1f88 (`docs(06.3-11): reconcile calibration attestation after human discharge`, authored solely by `user39261580`, no `Co-Authored-By: Claude` line) rewrote 06.3-11-SUMMARY.md section 6 from the stale 'calibration pending' text to an explicit attestation: 'Status: calibration discharged (updated 2026-09-04, follow-up session)', quoting the scorer's own words verbatim ('I have reviewed all 16 items in the calibration scoring sheet and hand-scored each row's Groundedness and Faithfulness against the provided evidence.') and an explicit no-fabrication statement covering both the original halt and the later feed-back transcription step. This closes the specific paper-trail gap this verifier's prior round flagged (SUMMARY said 'pending' while HEAD said 'discharged') — the SUMMARY and HEAD are now internally consistent, and the attestation is now in the exact form the plan's acceptance criterion required. Residual uncertainty: this is still a documentation claim, not independently-sourced proof (e.g. a session transcript this verifier can read directly) of genuine per-row human authorship versus an agent transcribing a shorter or paraphrased human statement into a fuller one. Per fail-closed policy for judgment-tier prohibitions, a documentation attestation — however internally consistent and specific — is not treated as verifier-resolvable; it requires a human's own sign-off, not automated or LLM-judge confirmation. Non-authoritative LLM-judge assessment: the specificity (verbatim quote, two free-text notes in different registers/languages, non-uniform scores) makes fabrication substantially less likely than in the prior round, but this remains a flagged, human-decision item, not a resolved one."
  - must_have: "MUST NOT source calibration-candidate rows from the graph-off arm (06.3-11-PLAN.md, category evidence-integrity, verification: manual)."
    status: resolved
    verification: test
    evidence: "Original plan tier was `manual`; commit fdaa890 added `test_worksheet_never_sources_graph_off_records` to eval/tests/test_score_judged.py, upgrading enforcement from a circumstantial cache_key-membership argument to a wired regression test. Independently reproduced both RED and GREEN in this session: patched score.py's `p_records = records_by_arm.get(primary_arm, [])` line to `records_by_arm.get(\"graph-on\", []) + records_by_arm.get(\"graph-off\", [])` (a deliberately broken primary-arm scoping) and re-ran the test — it failed with `judge_cache.json` growing from the expected 5 entries to 10 (`assert 10 == 5`), an exact match to the commit message's stated RED symptom. Reverted the patch (`git checkout -- eval/src/lancet_eval/score.py`) and re-ran — the test passed. This is now a genuinely wired, independently-reproduced regression guard, not merely a manual/circumstantial argument — resolved, not flagged."
  - must_have: "MUST NOT change the calibration worksheet's row shape, header fields, or cache_key computation while fixing which records it selects (06.3-11-PLAN.md, category scope-fence, verification: test)."
    status: resolved
    verification: test
    evidence: "Unchanged since the prior round. `test_calibration_worksheet_emission_and_ingestion` exists, is unedited, and passes in this session's fresh full-suite run (140/140, up from 137/137 due to the three new tests added by the two follow-on commits below)."
  - must_have: "MUST NOT let the final --calibration-file score invocation in Task 2 omit --judge or use a narrower --sample scope than the judging invocation that preceded it (06.3-11-PLAN.md, category evidence-integrity, verification: test)."
    status: "resolved (partial)"
    verification: test
    evidence: "Commit 22d90a1 added a guard directly inside `score_run`, immediately after `judge_cache.json` loads. Independently reproduced RED and GREEN in this session: temporarily removed the guard block from score.py, re-ran `test_calibration_file_rejects_no_judge_when_cache_populated` and `test_calibration_file_rejects_narrower_sample_than_cache` — both failed with `Failed: DID NOT RAISE ScoreError`, an exact match to the commit message's stated RED symptom. Restored the guard (`git checkout --`) and re-ran the full suite — 140/140 passed. Confirmed CLI-reachable: `eval/src/lancet_eval/cli.py`'s `score` command passes `no_judge`, `sample`, and `calibration_file` straight through to `score_run(...)` with no intermediate transformation (cli.py:352-357), closing the prior round's specific 'CLI path not exercised' objection. HOWEVER, on independently re-reading the guard's actual predicate (score.py:296-314), it does not fully enforce the stated prohibition: the `--no-judge` half is fully enforced (any cached non-null verdict blocks `--no-judge`), but the `--sample` half compares the new invocation's `sample` against `cached_verdict_count` (the number of records that actually resolved to a cached verdict — 16 in this run), not against the *prior invocation's own `--sample` scope* (which was `None`/unrestricted, i.e. effectively 500, per `metadata.json`'s `sample_size_judged`). Concretely: `--calibration-file --judge --sample 200` would pass this guard (200 > 16) while still being narrower than the population the cache was actually built from (500) — exactly the silent-population-shrink hazard the prohibition names, just not caught by this specific check when the cited/citation rate is far below the sample size (which is this run's entire root cause, per 06.3-11-PLAN.md's own diagnosis). The guard's own error message reveals the approximation, calling the cached-verdict count 'a prior, larger judging invocation' rather than referencing the actual prior `--sample` value. This is a materially better state than the prior round's 'no enforcement at all,' and the specific committed run this phase produced is not exposed to the gap (it never used `--sample` for the calibration-producing invocation), but the enforcement does not fully match its own stated prohibition — recorded as resolved (partial), not fully resolved, and named as a follow-up."
  - must_have: "MUST NOT proceed to spend a human's time if Task 2's precondition reconfirms fewer than 10 cache-resolvable primary-arm records at execution time (06.3-11-PLAN.md, category fail-closed, verification: manual)."
    status: flagged (strongly corroborated)
    verification: manual
    evidence: "Unchanged since the prior round. 06.3-11-SUMMARY.md section 3 documents the precondition reconfirmation command and its output (16), and states '16 >= 10 ... execution proceeded without halting.' No code-level enforcement was added for this item by the intervening commits (they targeted items 2 and 4 above, not this one). Deterministic, artifact-backed, very low residual uncertainty, but its declared tier is `manual`, so it stays routed through the judgment-tier flag per policy rather than marked resolved outright — and per policy, a flagged manual-tier item requires an explicit human sign-off per item, not just strong verifier corroboration (see human_verification below, added this round)."
  - must_have: "MUST NOT re-plan, modify or supersede plans 06.3-01 through 06.3-10, and MUST NOT hand-edit 06.3-10-PLAN.md or 06.3-VERIFICATION.md's historical wording (06.3-11-PLAN.md, category scope-fence, verification: manual)."
    status: flagged (strongly corroborated)
    verification: manual
    evidence: "Re-confirmed in this session: `git diff --stat 8bac798..HEAD -- .../06.3-01-PLAN.md ... .../06.3-10-PLAN.md` (all ten plan files individually listed) returns empty — none of plans 01 through 10 was touched by the two intervening commits (fdaa890, 22d90a1) or by the SUMMARY reconciliation (91c1f88). The full `git diff --name-only bd580f7..HEAD` confirms exactly seven paths changed: `.planning/ROADMAP.md`, `.planning/STATE.md`, `06.3-11-SUMMARY.md`, `06.3-VERIFICATION.md`, `.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/.gitkeep` (a placeholder for the newly-inserted, not-yet-planned Phase 06.3.1 directory — identified by name, not left as an unidentified path), `score.py`, and `test_score_judged.py` — all either this phase's own gap-closure artifacts or the explicitly out-of-scope Phase 06.3.1 roadmap insertion. Declared tier is `manual`, so still routed as a flagged judgment item per policy, but with very strong corroborating evidence — and per policy still requires an explicit human sign-off per item (see human_verification below, added this round)."
requirements_coverage:
  OBS-02: "Truths-level mechanism SATISFIED (harness drives the live gateway/engine, scores deterministically and via LLM-judge over the full primary-arm population, a human has hand-scored the corrected 16-row calibration worksheet at the artifact level, and the committed report carries the resulting observed agreement figures, independently re-derived and matched exactly in a prior round). Of the six must_haves.prohibitions items: two are now independently confirmed resolved by wired, RED-then-GREEN-reproduced regression tests (up from one in the prior round); one is resolved but only partially enforces its own stated predicate (the --sample half of item 4 checks cached-verdict-count, not prior-invocation sample scope); two remain flagged-but-strongly-corroborated manual-tier items requiring explicit human sign-off (10-record floor, no re-planning of 06.3-01-10); one remains a flagged judgment-tier item with modest residual uncertainty (fabrication attestation) — though its supporting paper trail is now internally consistent, closing the specific gap (stale SUMMARY) the prior round identified. Held at human_needed pending human sign-off on all three flagged manual/judgment items; REQUIREMENTS.md checkbox remains unchecked, correctly."
  OBS-04: "SATISFIED — unchanged since prior rounds. community_summary_quality still registers, status=skipped, n=0, reason 'Deferred to Phase 999.1', confirmed present in the committed report.md/report.json; not touched by any commit in this session's diff range."
human_verification:
  - test: "Confirm with the human scorer (or direct evidence, e.g. session transcript/screen recording) that the 16 rows in eval/runs/2026-09-03-multihop_rag/calibration_completed.jsonl were genuinely hand-scored by a person reading each row's question/answer/evidence, and were not generated, inferred, simulated, or back-filled by an agent."
    expected: "A human confirms direct authorship of all 16 human_groundedness/human_faithfulness values. (The paper-trail inconsistency this item previously also covered — 06.3-11-SUMMARY.md contradicting HEAD — is now resolved: commit 91c1f88 rewrote the SUMMARY's attestation to match the actual discharge event, quoting the scorer's own words. What remains open is the inherently non-programmatically-verifiable question of genuine human authorship itself.)"
    why_human: "This is the exact class of claim (a human-authored measurement) that no code inspection, grep, or test can verify — it requires direct human attestation or independently-sourced evidence of the scoring session. A commit message or SUMMARY prose, however specific and internally consistent, is documentation, not proof, per this verification's own instructions."
  - test: "Confirm the 10-record floor precondition was genuinely reconfirmed at execution time, not merely asserted after the fact."
    expected: "A human accepts 06.3-11-SUMMARY.md section 3's documented precondition command and its printed output (16), and its stated conclusion ('16 >= 10 ... execution proceeded without halting') as sufficient, or requests independently-sourced evidence (e.g. a terminal transcript/screen recording) of the actual command run."
    why_human: "Declared verification tier is `manual` per 06.3-11-PLAN.md's own must_haves.prohibitions; per policy this requires explicit per-item human sign-off regardless of how strong the verifier's corroborating evidence is. This is a low-effort confirmation of already strongly-corroborated evidence, not an open investigation — a rubber-stamp item, not a suspected gap."
  - test: "Confirm no plan 06.3-01 through 06.3-10 was re-planned, modified, or superseded, and that 06.3-10-PLAN.md / 06.3-VERIFICATION.md's historical wording was not hand-edited outside the verifier's own permitted updates."
    expected: "A human accepts this verifier's independently-run `git diff --stat 8bac798..HEAD` across all ten named plan files (empty) as sufficient evidence, or requests a broader diff range/manual review."
    why_human: "Declared verification tier is `manual` per 06.3-11-PLAN.md's own must_haves.prohibitions; per policy this requires explicit per-item human sign-off regardless of how strong the verifier's corroborating evidence is. This is a low-effort confirmation of already strongly-corroborated evidence, not an open investigation — a rubber-stamp item, not a suspected gap."
---

# Phase 6.3: Evaluation Harness, Corpora and Recorded Run Verification Report (Re-Verification)

**Phase Goal:** Build the Python evaluation harness against MultiHop-RAG, run and commit a scored evaluation report with deterministic and LLM-judged metrics
**Verified:** 2026-09-04
**Status:** human_needed
**Re-verification:** Yes — third pass this session. Pass 1: gaps_found, 10/11 (Truth 11 undischarged). Pass 2 (after commit `bd580f7`, the human hand-scoring drive): human_needed, 11/11 truths, but flagged all six `06.3-11-PLAN.md` must_haves.prohibitions items as never independently evaluated (1 resolved, 1 unverified, 3 flagged-strongly-corroborated, 1 flagged with residual uncertainty). This pass independently re-verifies three concrete follow-on commits (`91c1f88`, `fdaa890`, `22d90a1`) claimed to close two of those gaps and reconcile a third, and additionally closes a gap in pass 2's own process: manual-tier flagged items 5 and 6 previously had no corresponding `human_verification` checklist entry, meaning they could have been silently absorbed into a future `passed` verdict without ever receiving the per-item human sign-off policy requires. Both now have explicit entries.

## Goal Achievement

### Observable Truths

Unchanged since pass 2 — none of this round's three commits touch `journal.jsonl`, `report.md`, `report.json`, `metadata.json`, `calibration_completed.jsonl`, or `calibration_worksheet.jsonl` (independently confirmed: `git diff --stat bd580f7..HEAD -- eval/runs/2026-09-03-multihop_rag/` is empty). All 9 ROADMAP Success Criteria and both prior gap-closure must-haves (Truths 10, 11) remain verified exactly as pass 2 recorded them.

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | MultiHop-RAG (~500 sampled questions, fixed committed seed) is the primary benchmark; GraphRAG-Bench optional | ✓ VERIFIED (regression) | Unchanged; not touched by this round's commits. |
| 2 | Python harness drives gateway `/rag/query` HTTP/SSE, dedicated `eval/` with `pyproject.toml` + uv lockfile, SSE client asserts wire contract | ✓ VERIFIED (regression) | `uv run --project eval pytest eval/tests -q` → `140 passed in 10.98s`, re-run fresh in this session (up from 137, three new tests added by this round's commits, all passing). |
| 3 | Deterministic metrics need no LLM/key; LLM-judge scores only groundedness/faithfulness, pinned distinct judge model, temp 0, cached by hash | ✓ VERIFIED (regression) | `judge_cache.json` unchanged at 16 entries, all non-null verdict, re-confirmed this session. Judge/generation models still distinct per `metadata.json`. |
| 4 | Graph capability measured by ablation, same question set graph-on/off via per-request flag on one engine, own dimension | ✓ VERIFIED (regression) | `report.json` unchanged since prior round; not touched by this round's commits. |
| 5 | OBS-04 placeholder registers, returns explicit `skipped` status with reason, never a fabricated number | ✓ VERIFIED (regression) | `report.json` dimension `community_summary_quality`: `status: skipped, n: 0`, unchanged. |
| 6 | Eval store fully isolated, seeded once through real ingestion path, reseed is explicit command | ✓ VERIFIED (regression) | Not touched by this round's commits. |
| 7 | Harness preflights gateway/engine reachability, eval-store seed state and generation, API key presence, fails fast with guidance | ✓ VERIFIED (regression) | `preflight.py` untouched. |
| 8 | Output is a committed Markdown report plus JSON, advisory only, per-corpus with run metadata, no cross-corpus aggregate | ✓ VERIFIED (regression) | `eval/runs/2026-09-03-multihop_rag/` unchanged since `bd580f7` (`git diff --stat` empty for that directory this round). |
| 9 | At least one automated test exercises the shipped generation model's structured-output preflight, closing WARN-NEW-01 | ✓ VERIFIED (regression) | Not touched by this round's commits. |
| 10 | The calibration-worksheet selector selects only records whose cache_key resolves to a `judge_cache.json` entry with a non-null verdict | ✓ VERIFIED (regression, as scoped) | `score.py`'s selector unchanged in this round's diff (the three new commits add a *different*, earlier guard for the calibration-file/cache-consistency prohibition and a *different* test for graph-arm scoping; the Truth-10 selector block itself, lines ~464-490, is untouched — confirmed by re-reading the file). CR-01 (the `no_judge=True`-default bypass) remains an unfixed, out-of-scope-for-this-phase follow-up, unchanged from pass 2. |
| 11 | Human verification item 06.3-06-03 is discharged: cache-resolvable primary-arm records (N=16) are hand-scored by a person, and the committed report carries an observed calibration agreement figure | ✓ VERIFIED (artifact/data level) | Unchanged since pass 2; independently re-confirmed this session that `eval/runs/2026-09-03-multihop_rag/` (including `calibration_completed.jsonl`) is byte-identical to its state at `bd580f7`. |

**Score:** 11/11 truths verified. Unchanged from pass 2 — this round's work targeted the `must_haves.prohibitions` block, not the truths.

### Prohibitions (must_haves.prohibitions, 06.3-11-PLAN.md) — the focus of this round

Full disposition and independently-reproduced evidence for each item is in the frontmatter `prohibitions:` list. Summary, with change markers relative to pass 2:

| # | Prohibition (abbreviated) | Tier | Pass 2 Disposition | This Pass |
|---|---|---|---|---|
| 1 | No agent-fabricated calibration score | manual | flagged (paper trail broken: SUMMARY said "pending", HEAD said discharged) | **flagged (paper trail fixed)** — commit `91c1f88` rewrote 06.3-11-SUMMARY.md's attestation to match HEAD, quoting the scorer's own words. Independently confirmed the commit is human-authored (no `Co-Authored-By: Claude` trailer) and content-matches HEAD. Still a judgment-tier item that cannot be self-certified by a verifier — genuine residual uncertainty, though now smaller than pass 2's (which also had to weigh an internally-contradictory paper trail). |
| 2 | No graph-off-arm calibration sourcing | manual → test | flagged (strongly corroborated, cache_key membership argument only) | **resolved** — commit `fdaa890` added `test_worksheet_never_sources_graph_off_records`. Independently reproduced RED (patched `p_records` to include both arms, cache grew 5→10, exact match to commit message) and GREEN (reverted, test passes). |
| 3 | No row-shape/header/cache_key change | test | resolved | **resolved** — unchanged, re-confirmed passing in this session's full-suite run. |
| 4 | Final `--calibration-file` invocation must not omit `--judge` or narrow `--sample` | test | **unverified** — no wired enforcement despite strong indirect corroboration | **resolved (partial)** — commit `22d90a1` added a guard inside `score_run` itself. Independently reproduced RED (removed guard, both new tests failed with "DID NOT RAISE ScoreError", exact match to commit message) and GREEN (restored guard, full suite 140/140). CLI-reachable, closing pass 2's "CLI path not exercised" objection. But the guard's `--sample` check compares against `cached_verdict_count` (16), not the prior invocation's actual `--sample` scope (unrestricted/500) — so a `--sample` value between the two (e.g. 200) would pass the guard while still narrowing the judged population relative to the preceding invocation, the exact hazard named. Enforces the `--no-judge` half fully; enforces the `--sample` half only when the new sample is at or below the cached-verdict count. Not exposed in the actual committed run (which never used `--sample` on the calibration-feeding invocation), but the code does not fully match its own stated prohibition. |
| 5 | Must not proceed below the 10-record floor | manual | flagged (strongly corroborated) | **unchanged** — flagged (strongly corroborated). No commit in this round targeted this item; SUMMARY-documented precondition check stands as before. Now has an explicit `human_verification` entry (previously missing). |
| 6 | Must not re-plan/modify plans 06.3-01–10 | manual | flagged (strongly corroborated) | **unchanged, re-confirmed** — flagged (strongly corroborated). `git diff --name-only bd580f7..HEAD` names exactly seven paths, all identified (including the `.gitkeep` for the new Phase 06.3.1 directory) and all either this phase's own artifacts or the out-of-scope roadmap insertion. Now has an explicit `human_verification` entry (previously missing). |

**Net change this round:** two items (2, 4) moved from weaker dispositions to test-backed, independently-reproduced status — though item 4's enforcement is partial, not complete, relative to its own stated predicate. One item's (1) paper-trail contradiction is fixed, though its core judgment-tier uncertainty is irreducible by a verifier. Two items (5, 6) are otherwise unchanged but now correctly carry their own `human_verification` entries, closing a process gap in how pass 2 routed strongly-corroborated manual-tier flags. None of the six represents a code defect against the phase's own committed run.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `eval/src/lancet_eval/score.py` (Truth-10 selector) | Cache-verdict-checked selection on the `not no_judge` branch | ✓ VERIFIED | Unchanged in this round's diff. |
| `eval/src/lancet_eval/score.py` (new calibration-file consistency guard) | Rejects `--no-judge`/narrower `--sample` combined with `--calibration-file` against a populated cache | ⚠️ VERIFIED (partial) | Added by `22d90a1`; independently RED/GREEN-reproduced this session. `--no-judge` half fully enforced; `--sample` half enforced only relative to cached-verdict-count, not the prior invocation's actual sample scope — see prohibition #4 above. |
| `eval/tests/test_score_judged.py` | Now 11 tests (was 8): adds `test_worksheet_never_sources_graph_off_records`, `test_calibration_file_rejects_no_judge_when_cache_populated`, `test_calibration_file_rejects_narrower_sample_than_cache` | ✓ VERIFIED | All three independently confirmed to exist, pass, and (where a RED-then-GREEN claim was made) actually fail against a deliberately reverted/broken version of the guarded code. |
| `06.3-11-SUMMARY.md` § Calibration Attestation | Reflects the actual discharge event, not a stale "pending" state | ✓ VERIFIED | Rewritten by `91c1f88`; content independently read and confirmed to match HEAD's `calibration_completed.jsonl` state. |
| `eval/README.md` | No documented example combines `--no-judge` or a narrower `--sample` with `--calibration-file` in a way the new guard would now reject | ✓ VERIFIED | Grepped for `--no-judge`/`--calibration-file`/`--sample`: the only calibration-feeding example (line 77) uses `--judge --sample 100 --calibration-file ...`, matching the same `--sample 100` used in the preceding judge pass on line 71 — consistent, not narrower. No doc/code inconsistency introduced by `22d90a1`. |
| `eval/runs/2026-09-03-multihop_rag/*` | Unchanged since pass 2 | ✓ VERIFIED | `git diff --stat bd580f7..HEAD -- eval/runs/2026-09-03-multihop_rag/` is empty. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| CLI `score --calibration-file --no-judge`/`--sample` | The new consistency guard in `score_run` | Direct pass-through of `no_judge`, `sample`, `calibration_file` from `cli.py`'s `score` command to `score_run(...)` (cli.py:352-357), no intermediate transform | ✓ WIRED (partial enforcement) | Independently confirmed by reading `cli.py` — the guard added in `score.py` is reachable via the actual CLI invocation path, not just the test's direct function call. The guard itself only partially covers its named prohibition, per prohibition #4 above. |
| Mixed graph-on/graph-off fixture | `judge_cache.json` / worksheet population | `records_by_arm.get(primary_arm, [])` (the same `p_records` scoping used throughout `score_run`) | ✓ WIRED | Independently RED/GREEN-reproduced — breaking this scoping line causes the graph-off regression test to fail exactly as the commit describes. |

### Data-Flow Trace (Level 4)

Unchanged since pass 2 — this round added enforcement/regression code, not new data-flow paths. See pass 2's trace (calibration figures independently re-derived and matched the committed report exactly; not re-run this session since the underlying files are byte-identical to `bd580f7`).

### Anti-Patterns Found

No `TBD`/`FIXME`/`XXX`/unreferenced `PLACEHOLDER` markers found in the diff introduced by this round's three commits (`git diff bd580f7..HEAD -- eval/src/lancet_eval/score.py eval/tests/test_score_judged.py`, grepped for debt markers — zero matches).

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `eval/src/lancet_eval/score.py:306` | — | The new calibration-file/`--sample` consistency guard's predicate (`sample < cached_verdict_count`) is a proxy for, not an exact match of, its stated prohibition ("narrower ... than the judging invocation that preceded it"). When the citation/judge-verdict rate is far below the sample size — this run's own root cause, 16 verdicts out of a 500-question population — a `--sample` value between the verdict count and the true prior scope silently passes the guard while still narrowing the judged population. | ⚠️ Warning — follow-up recommended, not blocking | Does not affect the phase's own committed run (its calibration-feeding invocation never used `--sample`). A future invocation that does use `--sample` in this specific range would not be caught. Recommend a follow-up that compares against the prior invocation's actual sample scope (e.g. persisted in `metadata.json` or a run-state file) rather than the cached-verdict count. |
| `.planning/ROADMAP.md:681` | — | `06.3-11-PLAN.md`'s checkbox is still `[ ]` (unchecked) despite having been executed and its SUMMARY existing since commit `263c04e`. | ℹ️ Info | Documentation-only drift in the roadmap's plan checklist; does not affect any truth or prohibition disposition. Recommend checking it off as part of closing this phase. |
| `.planning/STATE.md:7` | — | `stopped_at` still reads "...calibration pending" even after `91c1f88` corrected the SUMMARY itself. | ℹ️ Info | STATE.md is orchestration bookkeeping, not a plan/phase artifact; stale but not misleading in the same load-bearing way the SUMMARY was. |
| `eval/src/lancet_eval/score.py:465,489` | — | CR-01 (carried forward, unchanged): the Truth-10 selector's cache/verdict check is gated by `not no_judge`, and `no_judge` defaults to `True` in the CLI, so a *future* invocation that emits the worksheet while omitting `--judge` still bypasses the resolvability check. | ⚠️ Warning — follow-up recommended, not blocking | Unaffected by this round's commits (which added a *different* guard, for the calibration-file/cache-consistency prohibition, not this one). Does not affect the committed run. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full eval test suite passes offline | `uv run --project eval pytest eval/tests -q` | `140 passed in 10.98s` | ✓ PASS |
| Graph-off-sourcing regression test fails when primary-arm scoping is broken | Patched `p_records` to union both arms, ran `test_worksheet_never_sources_graph_off_records` | `assert 10 == 5` — FAILED (RED confirmed) | ✓ PASS (RED reproduced) |
| Graph-off-sourcing regression test passes against real code | Reverted patch, re-ran same test | PASSED | ✓ PASS (GREEN confirmed) |
| Calibration-file consistency guard fails when the guard is removed | Removed the guard block from `score.py`, ran both new tests | Both `Failed: DID NOT RAISE ScoreError` (RED confirmed) | ✓ PASS (RED reproduced) |
| Calibration-file consistency guard passes against real code | Restored the guard, re-ran full suite | `140 passed` (GREEN confirmed) | ✓ PASS (GREEN confirmed) |
| Calibration-file guard's `--sample` predicate matches its stated prohibition | Re-read score.py:296-314 against the prohibition's exact wording | Predicate is `sample < cached_verdict_count`, not `sample < prior_invocation_sample_scope` — a proxy, not an exact match | ⚠️ PARTIAL (see prohibition #4, anti-patterns) |
| No plan file 06.3-01–10 touched since pass 2's verification commit | `git diff --stat 8bac798..HEAD -- <all ten plan files>` | empty | ✓ PASS |
| `eval/runs/2026-09-03-multihop_rag/` unchanged since `bd580f7` | `git diff --stat bd580f7..HEAD -- eval/runs/2026-09-03-multihop_rag/` | empty | ✓ PASS |
| CLI passes `--judge`/`--sample`/`--calibration-file` straight through to `score_run` | `grep -n "no_judge\|sample\|calibration_file" eval/src/lancet_eval/cli.py` | direct pass-through at cli.py:352-357 | ✓ PASS |
| `91c1f88` is human-authored (no agent co-author trailer) | `git show 91c1f88` | Author `user39261580`, no `Co-Authored-By: Claude` line | ✓ PASS |
| `eval/README.md` has no doc example the new guard would now reject | `grep -n "no-judge\|calibration-file\|--sample" eval/README.md` | Only calibration example uses consistent `--sample 100` in both the judge pass and the feed-back invocation | ✓ PASS |

### Probe Execution

Not applicable — no `scripts/*/tests/probe-*.sh` conventions found; the phase's own verification mechanism is the `eval/tests/` pytest suite, run and independently manipulated (RED/GREEN) directly in this session.

### Requirements Coverage

See `requirements_coverage` in frontmatter. Both OBS-02 and OBS-04's underlying mechanisms remain satisfied; OBS-02 is held pending resolution of the remaining prohibition items (one genuine judgment-tier item, two strongly-corroborated manual-tier items, plus one partially-enforced test-tier item) rather than checked off. REQUIREMENTS.md's checkboxes remain unchecked as of this verification's timestamp — correctly, given the `human_needed` status.

No orphaned requirement IDs — only OBS-02, OBS-04 map to Phase 06.3 per `REQUIREMENTS.md`.

## Known Issue Outside Phase Scope (carried forward, unchanged)

`engine/src/db/mod.rs`'s `get_or_create_table` finding (silent fallback to `create_empty_table` on any `open_table()` error) remains flagged from prior rounds. Now formally tracked by the newly-inserted Phase 06.3.1 in ROADMAP.md (commit `c1d7faa`) — independently confirmed this insertion is purely additive (only ROADMAP.md and STATE.md changed for it) and does not alter Phase 6.3's own goal or Success Criteria text. Not part of this phase's own scope or status determination, per the task's explicit instruction.

## Follow-up Recommended (not a phase-blocking gap)

1. `06.3-REVIEW.md`'s CR-01 (the `no_judge=True`-default bypass of the Truth-10 worksheet's verdict-resolvability check) remains open, unaffected by this round's two new guards (which target the graph-off-sourcing and calibration-file-consistency prohibitions, a different code region). Still recommended before this workflow is relied on again with a different invocation pattern; still out of scope for closing this phase.
2. The new calibration-file/`--sample` consistency guard's predicate (`sample < cached_verdict_count`) should be tightened to compare against the prior invocation's actual `--sample` scope, not the cached-verdict count, to fully close prohibition #4 rather than partially closing it. Not blocking for this phase's own committed run (which never exercises the gap), but worth closing before the workflow is relied on with a `--sample` value in the affected range.

## Human Verification Required

See `human_verification` in frontmatter. Summary:

1. **Confirm human authorship of the 16 calibration scores.** The paper-trail inconsistency this verifier's prior round flagged (SUMMARY said "pending" while HEAD showed the calibration discharged) is now fixed — commit `91c1f88` rewrote the SUMMARY to carry an explicit, specific attestation quoting the scorer's own words. What remains open is the inherently non-programmatically-verifiable question underneath: genuine human authorship of the 16 scores, versus an agent paraphrasing or extending a shorter human statement. This is the one item in this round that a verifier cannot close by inspection, however consistent the documentation now is.
2. **Confirm the 10-record floor precondition.** Low-effort rubber-stamp of already strongly-corroborated SUMMARY evidence (16 >= 10, documented command and output).
3. **Confirm no plan 06.3-01–10 was re-planned or superseded.** Low-effort rubber-stamp of an independently-run, empty `git diff --stat` across all ten plan files.

### Gaps Summary

No truth-level gaps. No prohibition-level code defects against the phase's own committed run. Of the six `must_haves.prohibitions` items from `06.3-11-PLAN.md`: two are now resolved by independently-reproduced (RED-then-GREEN, by this verifier, in this session) wired regression tests; one is resolved but only partially — its enforcement predicate is a proxy for, not an exact match of, its own stated prohibition, and this is now named explicitly rather than accepted at face value. One item's supporting paper trail is now internally consistent (the SUMMARY matches HEAD), closing a documentation gap the prior round flagged, though its core judgment-tier uncertainty (genuine human authorship of the calibration scores) is not something a verifier can resolve and remains open. Two items are unchanged, strongly-corroborated, manual-tier flags that route to human sign-off per policy regardless of how strong their supporting evidence is — both now carry explicit `human_verification` entries, closing a process gap in how the prior round routed them (previously listed only in the prohibitions table, with no corresponding checklist entry, risking silent absorption into a future `passed` verdict). Per this verifier's own decision tree, any flagged judgment-tier prohibition routes the overall status to `human_needed` rather than `passed` — that is this round's status, materially improved in substance from the prior round but unchanged in overall classification.

---

_Verified: 2026-09-04_
_Verifier: Claude (gsd-verifier)_
