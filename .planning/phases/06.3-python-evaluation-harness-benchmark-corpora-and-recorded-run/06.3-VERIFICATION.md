---
phase: 06.3-python-evaluation-harness-benchmark-corpora-and-recorded-run
verified: 2026-09-04T22:30:00Z
status: human_needed
score: 11/11 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 10/11
  gaps_closed:
    - "Human verification item 06.3-06-03 (judge calibration) is now discharged at the artifact level. `eval/runs/2026-09-03-multihop_rag/calibration_completed.jsonl` exists, is git-tracked (commit `bd580f7`), and contains exactly 16 rows whose `cache_key` set is identical to `calibration_worksheet.jsonl`'s 16 rows (independently diffed as sets in this session -- equal). Every row's `human_groundedness`/`human_faithfulness` is a non-null integer in [1,5] (independently checked in this session -- zero nulls, zero out-of-range values, zero non-integers). One row (`mhr-2f6524fb519c`) is scored 3/3 with a candid note ('The answer and evidence were both unsufficient and uncompleted'); another carries a human note in Chinese critiquing the answer's relevance -- both consistent with genuine human authorship, not template/agent output. NOTE: this closes Truth 11's artifact/data requirements but does NOT by itself resolve the plan's must_haves.prohibitions fabrication attestation -- see prohibitions section below, which is the reason this round's status is human_needed rather than passed."
    - "report.md/report.json now carry calibration_exact_match/calibration_mad inside both judged dimensions' detail. Independently re-derived both figures from judge_cache.json + calibration_completed.jsonl in this session using the exact join logic read from score.py:382-447 (cache.get(cache_key), require cached.verdict truthy, compare human vs. cached verdict per question): answer_groundedness EM=0.9375 MAD=0.25, answer_faithfulness EM=1.0 MAD=0.0 -- both exactly match the committed report.json/report.md values, confirming the figures are computed, not fabricated."
    - "Full eval test suite re-run in this session: 137/137 passed, no regression from the 06.3-11 round (score.py/test_score_judged.py/journal.jsonl are byte-identical between commit 05302e6 and bd580f7, confirmed via `git diff --stat` returning empty)."
  gaps_remaining: []
  regressions: []
deferred: []
coincidental_reliance_items: []
prohibitions:
  - must_have: "MUST NOT fabricate, simulate, infer, extrapolate or back-fill any calibration score (06.3-11-PLAN.md, category evidence-integrity, verification: manual)."
    status: flagged
    verification: manual
    evidence: "Circumstantial evidence supports human authorship: 16 scores are non-uniform (15 rows at 5/5, one row at 3/3), two rows carry free-text notes in different registers (one candid English critique, one in Chinese), consistent with genuine hand-scoring rather than templated/generated output. However, the plan's own acceptance criterion required 'the SUMMARY carries an explicit attestation, in the same form 06.3-10 used, that no agent generated, simulated, inferred or back-filled any score' for THIS discharge event. No such SUMMARY exists or was updated: `06.3-11-SUMMARY.md` (the only SUMMARY on record) still reads 'Status: calibration pending ... awaiting a human scorer' -- the opposite of what HEAD now contains. The only textual attestation for the actual discharge event is the commit message of `bd580f7` itself, which per this verification's own instructions is not to be trusted as evidence on its own, and that commit carries `Co-Authored-By: Claude Sonnet 5`. This is a judgment-tier item this verifier cannot resolve from static evidence alone -- non-authoritative LLM-judge assessment leans toward genuine human authorship, but a human must confirm and the paper trail should be corrected."
  - must_have: "MUST NOT source calibration-candidate rows from the graph-off arm (06.3-11-PLAN.md, category evidence-integrity, verification: manual)."
    status: flagged (strongly corroborated)
    verification: manual
    evidence: "Strong programmatic evidence: all 16 `cache_key` values in `calibration_completed.jsonl` are present in `judge_cache.json`, and `judge_cache.json` entries are only ever written by the judge pass over `p_records` (the graph-on arm only) per `score.py`'s unchanged judge-pass block -- independently re-read and confirmed unaffected by this round's commits. Declared verification tier is `manual`, so this is still routed as a judgment-tier item per policy rather than silently marked resolved, but the evidence for compliance here is materially stronger than for the fabrication prohibition above -- effectively a rubber-stamp confirmation, not an open question."
  - must_have: "MUST NOT change the calibration worksheet's row shape, header fields, or cache_key computation while fixing which records it selects (06.3-11-PLAN.md, category scope-fence, verification: test)."
    status: resolved
    verification: test
    evidence: "`test_calibration_worksheet_emission_and_ingestion` exists, is unedited since before 06.3-11, and passes in this session's fresh full-suite run (137/137). This is a genuinely wired, automated regression guard -- resolved, not flagged."
  - must_have: "MUST NOT let the final --calibration-file score invocation in Task 2 omit --judge or use a narrower --sample scope than the judging invocation that preceded it (06.3-11-PLAN.md, category evidence-integrity, verification: test)."
    status: unverified
    verification: test
    evidence: "No wired automated test enforces this constraint at the CLI level -- `eval/tests/test_score_judged.py` calls `score_run(...)` directly with `calibration_file=` at the function level and never exercises the CLI's `--judge`/`--sample` argument-parsing/consistency path together with `--calibration-file`, confirmed by grep across the test suite. Per fail-closed policy for test-tier prohibitions, a well-formed item with no wired enforcement is UNVERIFIED even when substantively corroborated by other evidence. Substantive corroboration here IS strong: the human-provided command transcript names `--judge` explicitly on the completing invocation, `sample_size_judged` remained 500 (not narrowed), and this session's from-scratch recomputation of `calibration_exact_match`/`calibration_mad` from `judge_cache.json` + `calibration_completed.jsonl` exactly matches the committed report -- which would not be possible if a narrower/omitted-judge invocation had silently overwritten `report.json` with a different (non-calibration-bearing) result. But per policy this corroboration does not substitute for wired enforcement; recorded as unverified/flagged, not silently passed."
  - must_have: "MUST NOT proceed to spend a human's time if Task 2's precondition reconfirms fewer than 10 cache-resolvable primary-arm records at execution time (06.3-11-PLAN.md, category fail-closed, verification: manual)."
    status: flagged (strongly corroborated)
    verification: manual
    evidence: "06.3-11-SUMMARY.md section 3 documents the precondition reconfirmation command and its output (16), and states '16 >= 10 ... execution proceeded without halting.' This is a deterministic, artifact-backed check with very low residual uncertainty, but its declared tier is `manual`, so it is still routed through the judgment-tier flag per policy rather than marked resolved outright."
  - must_have: "MUST NOT re-plan, modify or supersede plans 06.3-01 through 06.3-10, and MUST NOT hand-edit 06.3-10-PLAN.md or 06.3-VERIFICATION.md's historical wording (06.3-11-PLAN.md, category scope-fence, verification: manual)."
    status: flagged (strongly corroborated)
    verification: manual
    evidence: "`git diff --stat 2ff9d40..HEAD -- .planning/phases/.../06.3-01-PLAN.md .planning/phases/.../06.3-10-PLAN.md .planning/phases/.../06.3-VERIFICATION.md` shows only `06.3-VERIFICATION.md` changed (by this verifier's own re-verification passes, which is the expected and permitted document to update) -- no plan file 01 through 10 was touched. Declared tier is `manual`, so still routed as a flagged judgment item per policy, but with very strong corroborating evidence."
requirements_coverage:
  OBS-02: "Truths-level mechanism SATISFIED (harness drives the live gateway/engine, scores deterministically and via LLM-judge over the full primary-arm population, a human has hand-scored the corrected 16-row calibration worksheet at the artifact level, and the committed report carries the resulting observed agreement figures, independently re-derived and matched exactly in this session). Held at human_needed pending resolution of the must_haves.prohibitions items above -- principally the fabrication-attestation paper-trail gap (SUMMARY says 'calibration pending', HEAD says discharged, with no SUMMARY documenting the actual discharge event). REQUIREMENTS.md checkbox remains unchecked, correctly, pending that resolution."
  OBS-04: "SATISFIED -- unchanged since the prior round. community_summary_quality still registers, status=skipped, n=0, reason 'Deferred to Phase 999.1', confirmed present in the committed report.md/report.json in this session."
human_verification:
  - test: "Confirm with the human scorer (or direct evidence, e.g. session transcript/screen recording) that the 16 rows in eval/runs/2026-09-03-multihop_rag/calibration_completed.jsonl were genuinely hand-scored by a person reading each row's question/answer/evidence, and were not generated, inferred, simulated, or back-filled by an agent."
    expected: "A human confirms direct authorship of all 16 human_groundedness/human_faithfulness values, OR the plan's required SUMMARY attestation (in the same explicit no-fabrication form 06.3-10-SUMMARY.md and 06.3-11-SUMMARY.md used) is added/updated to document the actual discharge event -- 06.3-11-SUMMARY.md currently still states 'calibration pending', which contradicts the calibration_completed.jsonl now present at HEAD."
    why_human: "This is the exact class of claim (a human-authored measurement) that no code inspection, grep, or test can verify -- it requires either direct human attestation or independently-sourced evidence of the scoring session (the commit message alone is explicitly excluded as sufficient evidence per this verification's own instructions)."
  - test: "Reconcile 06.3-11-SUMMARY.md's stale 'Calibration Attestation: Status: calibration pending' section against the discharge that actually happened in commit bd580f7."
    expected: "Either a new SUMMARY documenting the bd580f7 discharge event (command transcript, pairing/EM/MAD outputs, explicit no-fabrication attestation) is created, or 06.3-11-SUMMARY.md is amended to reflect the completed state, so the paper trail is internally consistent."
    why_human: "This is a documentation/process gap, not something this verifier can resolve by editing project artifacts on its own authority."
---

# Phase 6.3: Evaluation Harness, Corpora and Recorded Run Verification Report (Re-Verification)

**Phase Goal:** Build the Python evaluation harness against MultiHop-RAG, run and commit a scored evaluation report with deterministic and LLM-judged metrics
**Verified:** 2026-09-04
**Status:** human_needed
**Re-verification:** Yes — after the human hand-scoring pass (commit `bd580f7`) that discharged the sole remaining truth-level gap (Truth 11 / human-verification item 06.3-06-03) from the prior `10/11 gaps_found` round. This round finds all 11 observable truths verified, but surfaces unresolved `must_haves.prohibitions` items from `06.3-11-PLAN.md` that were not evaluated in the prior round and must not be silently absorbed into a `passed` verdict.

## Goal Achievement

### Observable Truths

Truths 1–9 are the ROADMAP's own Success Criteria (the goal contract), regression-checked in this session. Truth 10 is the 06.3-10/06.3-11 selector-mechanism must-have (previously VERIFIED, re-confirmed unchanged this session). Truth 11 is the human-discharge must-have that was the sole open gap — now independently confirmed closed at the artifact/data level (its prohibition-level attestation gap is tracked separately below, not as a failure of Truth 11 itself).

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | MultiHop-RAG (~500 sampled questions, fixed committed seed) is the primary benchmark; GraphRAG-Bench optional | ✓ VERIFIED (regression) | Unchanged since prior verification; not touched by commit `bd580f7` (files changed: `calibration_completed.jsonl`, `metadata.json`, `report.json`, `report.md` only, per `git show --stat bd580f7`). |
| 2 | Python harness drives gateway `/rag/query` HTTP/SSE, dedicated `eval/` with `pyproject.toml` + uv lockfile, SSE client asserts wire contract | ✓ VERIFIED (regression) | `uv run --project eval pytest eval/tests -q` → `137 passed in 10.15s`, re-run fresh in this session. No regression; `score.py`/`test_score_judged.py` are byte-identical between commit `05302e6` and `bd580f7` (`git diff --stat` empty). |
| 3 | Deterministic metrics need no LLM/key; LLM-judge scores only groundedness/faithfulness, pinned distinct judge model, temp 0, cached by hash | ✓ VERIFIED (regression) | `metadata.json`: `generation_model: deepseek/deepseek-v4-flash-0731`, `judge_model: meta-llama/llama-3.3-70b-instruct` (still distinct), `judge_temperature: 0.0`. `judge_cache.json` unchanged at 16 entries, all non-null verdict (independently counted this session). |
| 4 | Graph capability measured by ablation, same question set graph-on/off via per-request flag on one engine, own dimension | ✓ VERIFIED (regression) | `report.json` dimension 7 `graph_ablation_delta`: status ok, n=33 (16+17 from prior round), score present. `journal.jsonl` untouched by `bd580f7`. |
| 5 | OBS-04 placeholder registers, returns explicit `skipped` status with reason, never a fabricated number | ✓ VERIFIED (regression) | `report.json` dimension 10 `community_summary_quality`: `status: skipped, n: 0, score: None`; `report.md` renders `--`/`0`/`Deferred to Phase 999.1`, unchanged. |
| 6 | Eval store fully isolated (LanceDB path + PostgreSQL schema via Atlas), seeded once through real ingestion path over documented subset, reseed is explicit command | ✓ VERIFIED (regression) | Not touched by `bd580f7`; `document_map.json`/`seed.py` untouched. |
| 7 | Harness preflights gateway/engine reachability, eval-store seed state and generation, API key presence, fails fast with guidance | ✓ VERIFIED (regression) | `preflight.py` untouched. |
| 8 | Output is a committed Markdown report plus JSON, advisory only, per-corpus with run metadata, no cross-corpus aggregate | ✓ VERIFIED — re-committed | `git ls-files eval/runs/` shows 7 tracked files under `eval/runs/2026-09-03-multihop_rag/` (up from 6 — `calibration_completed.jsonl` added). `git log --oneline -- eval/runs/2026-09-03-multihop_rag/` shows commit `bd580f7` past the prior round's `a72dc27`. Leak check `grep -rn "sk-or-\|OPENROUTER_API_KEY" eval/runs/2026-09-03-multihop_rag/` → zero matches, re-run in this session. Single-corpus report only, no aggregate. |
| 9 | At least one automated test exercises the shipped generation model's structured-output preflight, closing WARN-NEW-01 | ✓ VERIFIED (regression) | Not touched by `bd580f7`, unaffected by this data-only commit. |
| 10 | The calibration-worksheet selector, on the `not no_judge and judged_qids` branch the 06.3-11 fix targets, selects only records whose cache_key resolves to a `judge_cache.json` entry with a non-null verdict | ✓ VERIFIED (regression, as scoped) | `score.py:465-489` unchanged since 06.3-11 (`git diff --stat 05302e6..bd580f7` empty for this file). Regression tests `test_worksheet_rows_are_drawn_from_the_judged_subset` and `test_worksheet_excludes_judge_errored_records` both pass in this session's full-suite run. **CR-01 re-confirmed, does not change this truth's status:** the `else: selected_records = p_records[:20]` branch (CLI default `--no-judge`) remains unguarded by design/scope, per `06.3-REVIEW.md`'s CR-01 finding (independently re-read this session, `cli.py:317-323` confirms `no_judge: bool = True` default). This is a latent defect for *other, future* invocations that omit `--judge`; it does not affect the committed run, whose sole calibration invocation used `--judge` explicitly, and this session's from-scratch recomputation of the committed report's calibration figures using the real join logic reproduces them exactly. Tracked as a follow-up item, not a phase-blocking gap (see Anti-Patterns). |
| 11 | Human verification item 06.3-06-03 is discharged: cache-resolvable primary-arm records (N=16) are hand-scored by a person, and the committed report carries an observed calibration agreement figure | ✓ VERIFIED (artifact/data level) | `calibration_completed.jsonl` exists, git-tracked at commit `bd580f7` (`git ls-files` confirms 7 files, up from 6). Independently loaded both files in this session: 16 rows in `calibration_completed.jsonl`, cache_key set identical to the 16-row `calibration_worksheet.jsonl` (set equality confirmed). All 16 `human_groundedness`/`human_faithfulness` values are non-null integers in [1,5] (zero nulls, zero out-of-range, confirmed by direct iteration). `report.json` dimensions 5 (`answer_faithfulness`) and 6 (`answer_groundedness`) both carry non-null `calibration_exact_match`/`calibration_mad` in their `detail` dict; `report.md` renders the same figures in its detail column. Independently re-derived the figures from `judge_cache.json` + `calibration_completed.jsonl` using the exact join formula read from `score.py:382-447` in this session and got an exact match to the committed values (groundedness EM=0.9375/MAD=0.25, faithfulness EM=1.0/MAD=0.0). **This truth's own wording (rows are hand-scored, report carries the figure) is satisfied.** The separate question of whether the plan's `must_haves.prohibitions` fabrication-attestation requirement is independently discharged is tracked in the `prohibitions` section below and is the reason overall status is `human_needed`, not a reason to downgrade this truth. |

**Score:** 11/11 truths verified. All 9 ROADMAP Success Criteria are met, the selector-mechanism must-have (Truth 10) remains verified as scoped, and the previously-open human-discharge must-have (Truth 11) is independently confirmed closed at the artifact/data level.

### Prohibitions (must_haves.prohibitions, 06.3-11-PLAN.md)

06.3-11-PLAN.md declares a six-item `must_haves.prohibitions` block that the prior verification round did not evaluate (it predates this verifier's prohibition-routing procedure being applied to this phase). Per policy, a flagged prohibition must never be silently absorbed into a `passed` verdict. Full disposition of each item is in the frontmatter `prohibitions:` list; summary:

| # | Prohibition (abbreviated) | Tier | Disposition |
|---|---|---|---|
| 1 | No agent-fabricated calibration score | manual | **flagged** — circumstantial evidence favors genuine human authorship, but the plan's own required SUMMARY attestation for this discharge event does not exist (06.3-11-SUMMARY.md still says "calibration pending"); commit message alone is not accepted as evidence per this verification's instructions |
| 2 | No graph-off-arm calibration sourcing | manual | flagged (strongly corroborated) — cache_key membership in the graph-on-only `judge_cache.json` confirms compliance, but tier is `manual` so routed as a flag, not silently resolved |
| 3 | No row-shape/header/cache_key change | test | **resolved** — wired regression test (`test_calibration_worksheet_emission_and_ingestion`) exists and passes |
| 4 | Final `--calibration-file` invocation must not omit `--judge` or narrow `--sample` | test | **unverified** — no wired automated test enforces this; fail-closed per policy despite strong substantive corroboration (transcript, unchanged `sample_size_judged=500`, exact-match recomputation of report figures) |
| 5 | Must not proceed below the 10-record floor | manual | flagged (strongly corroborated) — SUMMARY documents the precondition check (16 ≥ 10) |
| 6 | Must not re-plan/modify plans 06.3-01–10 | manual | flagged (strongly corroborated) — `git diff --stat` confirms no plan file 01–10 was touched since the prior round |

None of these represents a failed truth or a code defect — items 2, 5, 6 are effectively confirmed by hard evidence but held at "flagged" because their declared verification tier (`manual`) requires human sign-off rather than verifier self-certification. Item 4 is a genuine coverage gap (no automated enforcement exists) despite strong indirect evidence of compliance. Item 1 is the one item with real, if modest, residual uncertainty and is the primary driver of this round's `human_needed` status.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `eval/src/lancet_eval/score.py` (corrected selector) | Cache-verdict-checked selection on the `not no_judge` branch | ✓ VERIFIED | Unchanged since 06.3-11, re-confirmed present and correct. |
| `eval/tests/test_score_judged.py` | Mixed cited/uncited/judge-errored regression fixture, RED-then-GREEN | ✓ VERIFIED | Unchanged; both tests pass in this session's fresh full-suite run (137/137). |
| `eval/runs/2026-09-03-multihop_rag/judge_cache.json` | Enlarged with real verdicts for the full primary-arm population | ✓ VERIFIED | Unchanged, 16 entries, all non-null verdict (re-counted this session). |
| `eval/runs/2026-09-03-multihop_rag/calibration_worksheet.jsonl` | Re-emitted, every row cache-resolvable | ✓ VERIFIED | Unchanged, 16 data rows, strict pairing still holds. |
| `eval/runs/2026-09-03-multihop_rag/calibration_completed.jsonl` | Human-scored counterpart | ✓ VERIFIED (data level) | Exists, git-tracked at `bd580f7`. 16 rows, cache_key set matches the worksheet exactly, every human score is a valid non-null 1–5 integer. Free-text `notes` on 2 of 16 rows read as genuine human commentary. Authorship attestation gap tracked as a prohibition item, not an artifact defect. |
| `eval/runs/2026-09-03-multihop_rag/report.md` / `report.json` | Carries `calibration_exact_match`/`calibration_mad` | ✓ VERIFIED | Both files re-committed. `report.json` dimensions 5 and 6 (`answer_faithfulness`, `answer_groundedness`) both carry non-null `calibration_exact_match`/`calibration_mad` in `detail`. `report.md` renders the same figures inline. Values independently re-derived from source files in this session and matched exactly. |
| `eval/runs/2026-09-03-multihop_rag/metadata.json` | `sample_size_judged` reflects the full primary-arm population | ✓ VERIFIED | `sample_size_judged: 500` unchanged; `commit_sha` field now `53188d3...` (the review commit at generation time — expected, not a defect). |
| `eval/README.md` | Documents that worksheet candidate pool is capped by the preceding judge pass's `--sample` scope | ✓ VERIFIED | Unchanged since 06.3-11; not touched by `bd580f7`. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| Corrected selector in `score.py` | `judge_cache.json` | Per-candidate `cache.get(cache_key)` requiring `entry.verdict is not None` | ✓ WIRED (as scoped) | Unchanged since 06.3-11; re-confirmed. The `no_judge` (CLI default) branch remains unchanged by design — CR-01 follow-up, not a wiring failure of this link (see Anti-Patterns). |
| `calibration_completed.jsonl` | `report.md`/`report.json` dimension detail (`calibration_exact_match`, `calibration_mad`) | `score_run(calibration_file=...)` joined against `judge_cache.json` | ✓ WIRED — now exercised end-to-end | Previously flagged as "not wired — unreachable" because the upstream file did not exist. Now independently confirmed exercised: `score.py:382-447`'s join logic, re-implemented from scratch in this session against the committed `judge_cache.json`/`calibration_completed.jsonl`, reproduces the committed `report.json`/`report.md` figures exactly (groundedness EM=0.9375/MAD=0.25, faithfulness EM=1.0/MAD=0.0). This is a genuine end-to-end data-flow confirmation, not merely "the code path exists." |
| `score --judge` with no `--sample` restriction | `metadata.json.sample_size_judged` and headline `answer_groundedness`/`answer_faithfulness` n | `judged_qids = {r.question_id for r in p_records}` (sample-is-None branch) | ✓ WIRED | Unchanged since 06.3-11; `sample_size_judged=500`, judged n=16, re-confirmed by direct read this session. |
| Judge pass population | `p_records` (graph-on arm only) | `records_by_arm.get(primary_arm, [])`, `primary_arm` resolved before the judge block | ✓ VERIFIED | Unchanged; `score.py` byte-identical to the prior round for this region. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `report.json`/`report.md` | `calibration_exact_match`, `calibration_mad` (`answer_groundedness`/`answer_faithfulness` detail) | `score_run`'s calibration-ingestion block (`score.py:382-447`), joining `calibration_completed.jsonl` against `judge_cache.json` by `cache_key` | Yes — independently re-derived in this session from the two source files and matched the committed values exactly (not a hardcoded/static figure) | ✓ FLOWING |
| `calibration_completed.jsonl` | `human_groundedness`, `human_faithfulness` | A person's hand-scoring pass over `calibration_worksheet.jsonl` | Data flows and is non-uniform/plausible, but genuine human authorship (vs. agent generation) is not independently provable from the artifact alone — see prohibitions section | ⚠️ FLOWING, authorship unverified |

### Anti-Patterns Found

No `TBD`/`FIXME`/`XXX`/unreferenced `PLACEHOLDER` markers found in the files touched by commit `bd580f7` (`calibration_completed.jsonl`, `metadata.json`, `report.json`, `report.md`).

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `eval/src/lancet_eval/score.py:465,489` | — | CR-01 (carried forward, unchanged): the calibration-worksheet selector's cache/verdict check is gated by `not no_judge`, and `no_judge` defaults to `True` in the CLI (`cli.py:317-323`), so any *future* invocation that emits the worksheet while omitting `--judge` (with a stale, populated cache already present) bypasses the resolvability check entirely. | ⚠️ Warning — recommend dedicated follow-up (already queued separately per this task's own instructions), not blocking this phase | Does not affect the committed run: this run's sole calibration-producing invocation (`score --judge --calibration-file ...`) always passed `--judge`, and this session independently re-derived the exact committed `calibration_exact_match`/`calibration_mad` figures from source files using the real join logic, confirming the actual code path exercised for this run was correct end-to-end. Real risk is scoped to a different, not-yet-occurred invocation pattern. |
| `eval/src/lancet_eval/score.py:318-374,466-489` | — | WR-02 (pre-existing, carried forward): duplicate `(question_id, arm)` records from a non-`--resume` re-run are not de-duplicated before judging or worksheet selection. | ⚠️ Warning | Does not manifest in this committed run (journal record counts unchanged from prior round). Latent risk on a future re-run without `--resume`. |
| `eval/src/lancet_eval/score.py:464` | — | IN-01 (pre-existing): comment claims query-type stratification the code doesn't perform. | ℹ️ Info | Misleading to a future maintainer; no functional impact. |
| `06.3-11-SUMMARY.md:207-210` | — | Stale attestation: the SUMMARY's "Calibration Attestation" section states `Status: calibration pending`, which no longer reflects HEAD (commit `bd580f7` discharged the calibration). No SUMMARY documents the actual discharge event's command transcript or attestation. | ⚠️ Warning — drives this round's `human_needed` status | Not a code defect; a process/paper-trail gap. See prohibitions section and human_verification items. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full eval test suite passes offline | `uv run --project eval pytest eval/tests -q` | `137 passed in 10.15s` | ✓ PASS |
| `calibration_completed.jsonl` exists and is git-tracked | `git ls-files eval/runs/2026-09-03-multihop_rag/` | 7 files listed, incl. `calibration_completed.jsonl` | ✓ PASS |
| Completed-file cache_key set equals worksheet cache_key set | Python set-equality check, this session | `True` (16 == 16, identical sets) | ✓ PASS |
| All human scores valid, non-null 1–5 integers | Python iteration check, this session | zero nulls, zero out-of-range, zero non-integers | ✓ PASS |
| Report carries calibration figures | `grep -n -i calibration report.md`; JSON tree-walk of `report.json` | 2 lines in `report.md`; `dimensions[5]`/`dimensions[6]`.detail in `report.json` | ✓ PASS |
| Calibration figures independently re-derivable and match committed values exactly | Python re-implementation of `score.py:382-447`'s join, this session | groundedness EM=0.9375/MAD=0.25, faithfulness EM=1.0/MAD=0.0 — exact match to committed report | ✓ PASS |
| `judge_cache.json` unchanged, still 16/16 non-null verdicts | Python one-liner, this session | `entries: 16`, `non-null verdict entries: 16` | ✓ PASS |
| No API-key leakage in committed run directory | `grep -rn "sk-or-\|OPENROUTER_API_KEY" eval/runs/2026-09-03-multihop_rag/` | no matches | ✓ PASS |
| No unrelated regression in `score.py`/tests/journal between the two rounds | `git diff --stat 05302e6..bd580f7 -- eval/runs/.../journal.jsonl eval/src/lancet_eval/score.py eval/tests/test_score_judged.py` | empty diff | ✓ PASS |
| All 16 calibration cache_keys resolve only within the graph-on-only judge cache | Python cross-check, this session | 16/16 resolve; `judge_cache.json` is populated exclusively from `p_records` (graph-on) | ✓ PASS |
| No plan file 06.3-01–10 touched since prior round | `git diff --stat 2ff9d40..HEAD -- .../06.3-01-PLAN.md .../06.3-10-PLAN.md .../06.3-VERIFICATION.md` | only `06.3-VERIFICATION.md` changed | ✓ PASS |
| SUMMARY attestation matches HEAD state | `grep -n -i "calibration pending\|Attestation" 06.3-11-SUMMARY.md` | still reads "calibration pending" | ✗ FAIL (confirms prohibitions gap, drives human_needed) |

### Probe Execution

Not applicable — no `scripts/*/tests/probe-*.sh` conventions found in this phase; the phase's own verification mechanism is the `eval/tests/` pytest suite and the plan-specified one-liner checks above, both run directly in this session.

### Requirements Coverage

See `requirements_coverage` in frontmatter. Both OBS-02 and OBS-04's underlying mechanisms are satisfied; OBS-02 is held pending prohibition resolution rather than checked off. REQUIREMENTS.md's checkboxes remain unchecked as of this verification's timestamp — correctly, given the `human_needed` status.

No orphaned requirement IDs — only OBS-02, OBS-04 map to Phase 06.3 per `REQUIREMENTS.md`, and both appear across the phase's plan frontmatter (`06.3-01`, `06.3-07` list both; the rest list OBS-02 only).

## Known Issue Outside Phase Scope (carried forward, unchanged)

`engine/src/db/mod.rs`'s `get_or_create_table` finding (silent fallback to `create_empty_table` on any `open_table()` error) remains flagged from a prior verification round and `06.3-REVIEW.md`. Landed via an interleaved Quick Task, out of 06.3's scope. Not re-investigated in this session; still unresolved, still worth a dedicated fix — tracked separately, not a gate on this phase.

## Follow-up Recommended (not a phase-blocking gap)

`06.3-REVIEW.md`'s CR-01 (the `no_judge=True`-default bypass of the worksheet's verdict-resolvability check) is a real defect for a *different, not-yet-occurred* invocation pattern than the one this phase's committed run used. It does not falsify any must-have of this phase — both scoring invocations against `eval/runs/2026-09-03-multihop_rag/` explicitly passed `--judge`, and this session independently recomputed the committed calibration figures end-to-end from source files and got an exact match, confirming the actual exercised code path was correct. A dedicated follow-up (raising `ScoreError` when `--emit-calibration-worksheet` is combined with `--no-judge`, per CR-01's own suggested fix) is recommended before this workflow is relied on again with a different invocation pattern, but it is out of scope for closing this phase.

## Human Verification Required

See `human_verification` in frontmatter for full detail. Summary:

1. **Confirm human authorship of the 16 calibration scores.** Circumstantial evidence (non-uniform scores, candid free-text notes in two different registers/languages) is consistent with genuine hand-scoring, but the plan's own required written attestation for this specific discharge event does not exist. `06.3-11-SUMMARY.md` still says "calibration pending" while `calibration_completed.jsonl` exists at HEAD (`bd580f7`) — the paper trail is internally inconsistent. Recommend either a direct human confirmation in this session, or an updated/new SUMMARY carrying the explicit no-fabrication attestation in the same form prior rounds used.
2. **Reconcile the stale SUMMARY.** Update `06.3-11-SUMMARY.md` (or add a new SUMMARY) to reflect the actual discharge event that happened in commit `bd580f7`, so future readers are not misled by a document that says the opposite of what HEAD contains.

### Gaps Summary

No truth-level gaps remain. The prior round's sole open truth — Truth 11, the human-scored judge calibration discharge (06.3-06-03) — is independently confirmed closed at the artifact/data level: `calibration_completed.jsonl` exists, is git-tracked, contains 16 genuinely-plausible human-scored rows matching the worksheet's cache_key set exactly, and the committed `report.md`/`report.json` carry `calibration_exact_match`/`calibration_mad` figures that this session independently re-derived from the underlying source files and matched exactly. All 9 ROADMAP Success Criteria and both 06.3-10/06.3-11 truth-level must-haves are verified (11/11).

However, this round additionally evaluated `06.3-11-PLAN.md`'s `must_haves.prohibitions` block, which the prior round did not assess. One item (fabrication attestation) carries genuine, if modest, residual uncertainty because the plan's own required SUMMARY attestation for this specific discharge was never written — the only SUMMARY on record still says the calibration is pending, contradicting HEAD. One item (the `--judge`/`--sample` consistency prohibition) has no wired automated enforcement and is fail-closed per policy despite strong indirect corroboration. Three items are effectively confirmed by hard evidence but held as "flagged, strongly corroborated" because their declared tier requires human sign-off. One item (row-shape guard) is genuinely resolved by a wired, passing test. None of these represents a code defect or a failed truth; per this verifier's own decision tree, a flagged prohibition must route to `human_needed` rather than `passed`, so that is this round's status.

---

_Verified: 2026-09-04_
_Verifier: Claude (gsd-verifier)_
