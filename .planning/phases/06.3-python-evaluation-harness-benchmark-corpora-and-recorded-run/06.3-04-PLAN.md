---
phase: 06.3-python-evaluation-harness-benchmark-corpora-and-recorded-run
plan: 04
type: execute
wave: 3
depends_on: ["06.3-01", "06.3-03"]
files_modified:
  - gateway/atlas.hcl
  - config/config.eval.toml
  - eval/src/lancet_eval/seed.py
  - eval/src/lancet_eval/preflight.py
  - eval/src/lancet_eval/config.py
  - eval/src/lancet_eval/cli.py
  - eval/corpora/multihop_rag.toml
  - eval/corpora/multihop_rag/document_map.json
  - eval/tests/test_seed.py
  - eval/tests/test_preflight.py
  - eval/README.md
  - .gitignore
autonomous: true
requirements: [OBS-02]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

user_setup:
  - service: PostgreSQL (local, via docker compose)
    why: "The eval PostgreSQL schema is created by the existing Atlas migrations applied to a separate schema (D-57), and the gateway writes eval document metadata and checkpoints there."
    env_vars:
      - name: LANCET_GATEWAY__DATABASE_URL
        source: "The dev DSN with the eval schema appended, e.g. `postgres://postgres:postgres@127.0.0.1:5432/lancet?sslmode=disable&search_path=lancet_eval`. Never commit a real DSN — config/config.toml deliberately leaves database_url empty."
      - name: LANCET_ENV
        source: "Set to `eval` so config/config.eval.toml overlays the isolated LanceDB path."
      - name: OPENROUTER_API_KEY
        source: "https://openrouter.ai/keys — the engine needs it to embed during seeding; the harness itself does not read it for the deterministic path."
    dashboard_config:
      - task: "Start PostgreSQL with `docker compose up -d postgres`, then run the engine and gateway with LANCET_ENV=eval and the eval DSN before `lancet-eval seed`."
        location: "repo root / docker-compose.yml"

must_haves:
  truths:
    - "The eval PostgreSQL schema is created by the existing Atlas migration source `file://db/schema.hcl` — there is no second hand-maintained DDL definition that can drift from it."
    - "The eval store is isolated on both halves: its own PostgreSQL schema and its own LanceDB directory, neither equal to the dev configuration's."
    - "`lancet-eval preflight` refuses to run when the configured eval LanceDB path or eval PostgreSQL schema matches the dev configuration's, and says which one collided."
    - "`lancet-eval preflight` reports the gateway reachable, the engine reachable through the gateway, the eval store seeded at the corpus's expected index generation, the presence of an API key when judged dimensions are requested, and that the judge model differs from the generation model — each as a named check with its own pass or fail line."
    - "A corpus-generation mismatch fails preflight with a message telling the operator to reseed, before any question is driven."
    - "`lancet-eval preflight` probes the `/rag/query` request field names by sending one request carrying `disable_graph_context`, so a harness/gateway spelling skew surfaces as a named preflight failure rather than as a mid-run HTTP 400."
    - "Seeding goes through the real ingestion path — a multipart upload to the gateway's `/documents` endpoint — and not through a fixture loader or a direct vector insert."
    - "The seeder sends no `chunk_size` and no `chunk_overlap` form field, so the eval corpus is chunked at the shipped defaults."
    - "`document_map.json` is written at seed time mapping each gateway `document_id` to its corpus article identifier, is stamped with the index generation it was built at, and is committed alongside the corpus config."
    - "Scoring fails loudly on any `document_id` present in a response but absent from `document_map.json` — an unmapped id is never silently treated as a miss."
    - "Reseeding is an explicit command that drops the eval PostgreSQL schema and the eval LanceDB directory and replays, never an implicit per-run cost."
    - "Checkpoint persistence is not disabled for eval runs: no configuration written by this plan turns it off, so the checkpoint path stays exercised under load and its rows are dropped with the schema on reseed."
  artifacts:
    - gateway/atlas.hcl
    - config/config.eval.toml
    - eval/src/lancet_eval/seed.py
    - eval/src/lancet_eval/preflight.py
    - eval/corpora/multihop_rag/document_map.json
  key_links:
    - "config/config.eval.toml is a LANCET_ENV overlay following config/config.verify.toml's precedent, so both runtimes pick up the isolated LanceDB path with no code change."
    - "The eval PostgreSQL schema reaches the gateway through search_path on the DSN, so no gateway source change is required."
    - "document_map.json ties every scored response back to a corpus article and is the guardrail that stops a silently-wrong recall number."
  prohibitions:
    - "MUST NOT create a second hand-maintained DDL definition for the eval schema; the eval schema derives from gateway/db/schema.hcl."
    - "MUST NOT seed, reseed or run against a store whose LanceDB path or PostgreSQL schema equals the dev configuration's."
    - "MUST NOT treat a document_id absent from document_map.json as a scoring miss."
    - "MUST NOT override chunk_size or chunk_overlap when seeding the eval corpus."
---

<objective>
Give the evaluation its own store and its own front door: an isolated PostgreSQL schema created by the existing Atlas migrations, an isolated LanceDB directory, a seeder that uploads the reduced document subset through the real ingestion path, and a preflight that fails fast with guidance instead of forty minutes into a run.

Purpose: eval/dev store contamination invalidates every number and no metric surfaces it (AI-SPEC Critical Failure Mode #4), and the existing seeder is on record as non-idempotent (DEBT-P3-WARN-DX), so a partial reseed into a shared store is unrecoverable. The `document_id` → article map captured at seed time is the single highest-risk artifact in the phase: without it, recall is quietly wrong while looking plausible.
Output: an Atlas eval environment, `config/config.eval.toml`, `seed` / `reseed` / `preflight` commands, and the committed `document_map.json`.
</objective>

## User Story (MVP mode)

**As a** Lancet maintainer, **I want to** seed a dedicated eval store once and have the harness refuse to start when anything about it is wrong, **so that** I never spend a day of free-tier quota producing numbers against the wrong data.

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-observability-evaluation-polish/06-CONTEXT.md
@.planning/phases/06.3-python-evaluation-harness-benchmark-corpora-and-recorded-run/06.3-AI-SPEC.md
@.planning/phases/06.3-python-evaluation-harness-benchmark-corpora-and-recorded-run/06.3-RESEARCH.md
@.planning/phases/06.3-python-evaluation-harness-benchmark-corpora-and-recorded-run/06.3-PATTERNS.md
@.planning/phases/06.3-python-evaluation-harness-benchmark-corpora-and-recorded-run/06.3-01-SUMMARY.md
@.planning/phases/06.3-python-evaluation-harness-benchmark-corpora-and-recorded-run/06.3-03-SUMMARY.md
</context>

## Artifacts this phase produces

See the phase-wide inventory in `06.3-01-PLAN.md`. This plan delivers the `seed.py` and `preflight.py` rows, the `seed` / `reseed` / `preflight` CLI sub-commands, the `env "eval"` Atlas environment (or the pre-authorised fallback recorded in `gateway/atlas.hcl`), `config/config.eval.toml`, and the committed `eval/corpora/multihop_rag/document_map.json`.

## Decisions Implemented

Phase 6.3 has no local CONTEXT.md by design (06-CONTEXT.md D-77); coverage is asserted manually and the phase-wide map lives in `06.3-01-PLAN.md`.

**This plan implements:** D-55 (seed only the documents the sampled questions reference plus distractors, ingest once into the isolated store, reseed as an explicit command), D-56 (the eval store fully isolated — its own LanceDB path and its own PostgreSQL schema — seeded through the real ingestion path so the eval measures the actual system), D-57 (the eval schema created by the existing Atlas migrations applied to a different schema, identical DDL, no second definition to drift), D-64 (preflight of gateway and engine reachability, eval-store seed state and generation, and API-key presence, failing fast with guidance), D-84 (the new eval store knobs fail closed on present-but-invalid values).

**D-58 satisfied here as a side effect, with one acceptance criterion holding it true.** D-58 is a consequence note rather than an action item: a ~500-question run writes roughly 2,500 checkpoint rows carrying full accumulated snapshots (Phase 05 D-28), absorbed by the isolated eval schema and dropped with it on reseed. Nothing in this plan disables checkpoints, and an acceptance criterion on Task 1 asserts that, so the checkpoint path stays exercised under load. Phase 05 D-24's no-TTL decision for the real system is untouched.

<tasks>

<task type="auto">
  <name>Task 1: Isolate the eval store — Atlas eval schema and the LANCET_ENV overlay</name>
  <files>gateway/atlas.hcl, config/config.eval.toml, eval/src/lancet_eval/config.py, eval/README.md, .gitignore</files>
  <precondition>PostgreSQL is reachable at the dev DSN (`docker compose up -d postgres`) and the `atlas` CLI is on PATH. Both are required to run the schema-isolation probe below; without them this task cannot establish which mechanism works and must halt rather than guess.</precondition>
  <read_first>
    - `gateway/atlas.hcl` — the single `env "local"` block, its `src = "file://db/schema.hcl"`, the `db_url` variable and the `dev` URL.
    - `gateway/db/schema.hcl` — the leading `schema "public"` block and the tables that hang off it, including `documents` and the workflow-checkpoint table.
    - `gateway/internal/config/config.go` — the `LANCET_ENV` overlay via `v.SetConfigName("config." + environment)` and `MergeInConfig()`, the `LANCET_GATEWAY__DATABASE_URL` binding, and the fail-closed empty-DSN check.
    - `engine/src/config.rs` around the `LANCET_ENV` overlay and the `LANCET_ENGINE__LANCEDB_PATH` override — the symmetric Rust side.
    - `config/config.verify.toml` — the repo's existing isolated-store overlay, the exact precedent this task follows.
    - `gateway/db/document_test.go` around the `createIsolatedTestPool` helper and its `connConfig.ConnConfig.RuntimeParams["search_path"] = schemaName` line — the repo's proven way of pointing a pgx pool at a non-public schema.
  </read_first>
  <action>
**Probe first, then implement one of the pre-authorised branches. Do not assert a mechanism that has not been observed to work.**

Probe A — Atlas schema scoping. With PostgreSQL up, create the schema with `psql`-equivalent DDL (`CREATE SCHEMA IF NOT EXISTS lancet_eval`) and attempt `atlas schema apply --env local --url "<dev DSN>&search_path=lancet_eval" --dry-run`, driving the existing `file://db/schema.hcl`. Record verbatim whether Atlas accepts a schema-scoped URL against a source file whose only schema block is labelled `public`, and what it errors with if not.

Then take the highest-fidelity branch that the probe proved, in this order — the ordering is by D-57 fidelity, "no second schema definition to drift":

1. **Schema-scoped URL works.** Add an `env "eval"` block to `gateway/atlas.hcl` reusing `src = "file://db/schema.hcl"` verbatim with a `var.eval_db_url` whose default carries `search_path=lancet_eval`. One source file, one DDL, two environments. This is the target state.
2. **Atlas rejects the label mismatch but accepts a parameterised schema.** Introduce an Atlas input variable for the schema name and thread it through `db/schema.hcl`, keeping exactly one schema block, and add the `env "eval"` block that supplies it. Still one DDL definition.
3. **Neither works.** Generate the eval DDL from `db/schema.hcl` with a committed, idempotent generator command recorded in `eval/README.md`, write the output to `gateway/db/schema.eval.hcl`, mark that file generated in a header line, and add a check to the eval workflow that regenerates it and fails if the committed copy is stale. This keeps a single hand-maintained definition even though a second file exists. Do **not** hand-write a second schema.

Whichever branch is taken, record the probe output and the chosen branch in the SUMMARY, and leave a short comment in `gateway/atlas.hcl` naming the branch and why the higher-fidelity ones were unavailable, if they were.

Probe B — gateway `search_path`. The gateway builds its pool from `LANCET_GATEWAY__DATABASE_URL`. Determine empirically whether appending `search_path=lancet_eval` as a DSN query parameter reaches the pool as a runtime parameter (pgx forwards unrecognised query parameters as runtime parameters) by starting the gateway against the eval DSN and confirming an uploaded document row lands in `lancet_eval.documents` and not in `public.documents`. If the query-parameter form does not take effect, fall back to setting `PGOPTIONS` with a `search_path` setting in the gateway's environment — a zero-code-change alternative — and document whichever works in `eval/README.md`. Do not modify gateway source for this; both routes are configuration.

Write `config/config.eval.toml` as a `LANCET_ENV=eval` overlay in the shape of `config/config.verify.toml`: an `[engine]` block setting `lancedb_path` to a dedicated eval directory distinct from `./data/lancedb`, and nothing else unless a probe proves it necessary. Deliberately do **not** put a DSN in it — `config/config.toml` leaves `database_url` empty on purpose and the eval DSN arrives via `LANCET_GATEWAY__DATABASE_URL`. Deliberately do **not** disable checkpoints, alter workflow timeouts, or change `[engine.retrieval] excerpt_max_chars`: the eval must measure the shipped configuration, and D-58 requires the checkpoint path to stay exercised.

Extend `eval/src/lancet_eval/config.py` with `lancedb_path`, `database_url`, `dev_lancedb_path` and `dev_database_url` resolution, plus a `pg_schema_of()` helper that extracts the `search_path` value from a DSN. Per D-84 these fail closed: a DSN that parses to no schema, or an eval LanceDB path that is empty or equal to the dev path, raises `EvalConfigError` at load rather than defaulting.

Add the eval LanceDB directory to `.gitignore`, and document the full isolated-stack recipe in `eval/README.md`: the `CREATE SCHEMA` step, the Atlas invocation for the chosen branch, the `LANCET_ENV=eval` plus `LANCET_GATEWAY__DATABASE_URL` environment for both processes, and the reseed teardown.
  </action>
  <verify>
    <automated>uv run --project eval pytest tests/test_preflight.py -k isolation -v</automated>
    <human-check>With PostgreSQL running, execute the documented eval-schema creation and the Atlas invocation for the chosen branch, then start the engine and gateway with `LANCET_ENV=eval` and the eval DSN and upload one small text file to `/documents`. Confirm by direct query that the row landed in the `lancet_eval` schema's `documents` table and that `public.documents` did not gain a row, and that a query afterwards writes checkpoint rows into the `lancet_eval` schema rather than none at all. Paste the probe output, the chosen Atlas branch, and both query results into the SUMMARY.</human-check>
  </verify>
  <acceptance_criteria>
    - `gateway/atlas.hcl` gains an `env "eval"` block (branch 1 or 2) whose `src` is the same `file://db/schema.hcl` the `local` env uses, or — only if the probe ruled both out — branch 3's generated file exists with a generated-by header and a documented regeneration command, and the SUMMARY records the probe output that forced it.
    - `git diff gateway/db/schema.hcl` shows either no change (branch 1) or a single parameterised schema block (branch 2); under no branch does the repository gain a second hand-written schema definition.
    - `config/config.eval.toml` sets `[engine] lancedb_path` to a value different from `config/config.toml`'s, contains no `database_url`, and contains no key that disables checkpoints, alters workflow timeouts or changes `excerpt_max_chars` — asserted by reading the committed file's full key set.
    - `EvalSettings` raises `EvalConfigError` when the eval LanceDB path equals the dev LanceDB path, and when the eval DSN's `search_path` equals the dev DSN's; both asserted by unit test.
    - The human-check confirms an uploaded document row appears in the `lancet_eval` schema and not in `public`, and that checkpoint rows are written under `lancet_eval` during a query.
    - `eval/README.md` documents the schema creation, the Atlas command actually used, the environment variables for both processes, and the reseed teardown.
    - `.gitignore` excludes the eval LanceDB directory.
  </acceptance_criteria>
  <done>The eval store has its own PostgreSQL schema created from the one existing DDL source and its own LanceDB directory, both proven distinct from dev by direct observation, with checkpoints left enabled.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seed the reduced document subset through the real ingestion path and capture the document map</name>
  <files>eval/src/lancet_eval/seed.py, eval/src/lancet_eval/cli.py, eval/corpora/multihop_rag.toml, eval/corpora/multihop_rag/document_map.json, eval/tests/test_seed.py</files>
  <precondition>The eval PostgreSQL schema exists and the engine and gateway are running under `LANCET_ENV=eval` with the eval DSN, and `OPENROUTER_API_KEY` is set for the engine's embedding calls. The offline half of this task needs none of it; the live seeding half does.</precondition>
  <read_first>
    - `gateway/main.go`'s `createDocument` handler — the multipart `file` field, the optional `chunk_strategy` / `chunk_size` / `chunk_overlap` form fields, the `chunk_overlap >= chunk_size` rejection, the 10MB upload ceiling, the 202 response carrying the document JSON, and the `Location` header; plus `getDocument` for the polling surface at `GET /documents/{id}`.
    - `gateway/db/models.go` `Document` — the fields the 202 response body carries, notably the generated `id` the map keys on, and `status`.
    - `.planning/phases/06.3-python-evaluation-harness-benchmark-corpora-and-recorded-run/06.3-AI-SPEC.md` §4 "State Management" item 1 (the `document_map.json` rules: emitted by the seeder from the upload response, committed with the corpus config, stamped with its index generation, scoring fails loudly on an unmapped id, and a round-trip assertion in tests) and "Tool Use" (seeding goes through the real ingestion path, not a fixture loader).
    - `.planning/phases/06.3-python-evaluation-harness-benchmark-corpora-and-recorded-run/06.3-03-PLAN.md` § "Ruling: the evidence-match rule" — the binding decision that the eval corpus is seeded at the shipped chunking defaults with no per-upload override.
    - `eval/src/lancet_eval/corpus.py` from plan 06.3-03 — `documents.subset.jsonl` and `subset_selection.json`, the inputs the seeder consumes.
  </read_first>
  <behavior>
    - `seed --corpus multihop_rag` uploads each article in `documents.subset.jsonl` to `/documents` as a multipart file, one request per article, with no `chunk_size` and no `chunk_overlap` form field.
    - Each upload's 202 response yields a `document_id`, which is written into `document_map.json` against that article's corpus identifier, title and source url.
    - The seeder polls `GET /documents/{id}` until the status is terminal, and records a failed ingestion as a failure rather than continuing silently.
    - `seed` is resumable: an article already present in `document_map.json` is skipped, so an interrupted seed does not duplicate documents into a store the existing seeder cannot deduplicate.
    - `reseed --corpus multihop_rag` refuses to proceed without an explicit confirmation flag, drops the eval PostgreSQL schema and removes the eval LanceDB directory, then replays the full seed.
    - After seeding, the seeder issues one throwaway query, reads `snapshot.index_generation`, stamps it into `document_map.json` and writes it back into the corpus TOML's expected-generation field.
    - `load_document_map()` raises when asked to resolve a `document_id` that is not in the map.
  </behavior>
  <action>
Write `seed.py` implementing `seed_corpus()` and `reseed_corpus()` against the gateway's real HTTP surface. Each article is uploaded as a multipart request to `/documents` with a `file` part whose filename ends in `.txt` and whose body is the article text — the ingestion path this exercises is chunking, embedding and graph extraction, which is precisely what D-56 requires and what a fixture loader or a direct vector insert would bypass. Send **no** `chunk_size` and **no** `chunk_overlap` form field, so the corpus is chunked at the gateway's shipped defaults; the ruling in `06.3-03-PLAN.md` explains why raising overlap would break the matching rule it was meant to enable.

Respect the gateway's 10MB per-upload ceiling: an article that exceeds it is recorded as skipped with a reason in `subset_selection.json`'s companion output rather than silently truncated. Poll `GET /documents/{id}` with a bounded backoff until the status is terminal, and treat a failed ingestion as a seeding failure that aborts with the document id named — a half-seeded store produces numbers that look plausible and are wrong.

Write `document_map.json` incrementally as uploads succeed, so an interrupted seed resumes rather than duplicating: `seed` reads the existing map first and skips any article already present. Each entry carries the corpus article identifier, the gateway `document_id`, the article title and source url, and the map file carries a header object with the corpus name, the seeding timestamp and the `index_generation` observed after seeding completes. Implement `write_document_map()` atomically — write a temp file and replace — so a killed process cannot truncate the map.

After the last upload, issue one throwaway query through the gateway, read `snapshot.index_generation` off the response, stamp it into the map header, and write it into the corpus TOML's expected-generation field so preflight has something to compare against.

Extend `corpus.py`'s `load_document_map()` (or add it here if plan 06.3-03 left it out) so resolving an unknown `document_id` raises rather than returning a sentinel. "Not in the gold set" and "not in my map" are different facts, and conflating them is how recall goes quietly wrong while looking plausible.

Implement `reseed` to require an explicit confirmation flag, then drop the eval PostgreSQL schema with cascade, remove the eval LanceDB directory, delete `document_map.json`, and replay `seed`. It must refuse to act when the configured eval schema or LanceDB path matches the dev configuration's, before touching anything.

Write `tests/test_seed.py` against `pytest-httpx`: a successful multi-article seed producing a correct map; an upload that returns a non-2xx aborting with the article named; a resumed seed skipping already-mapped articles; an assertion that no request body carries a `chunk_size` or `chunk_overlap` form field, read back from `httpx_mock.get_requests()`; a `document_map.json` round-trip proving a response `document_id` resolves to a known article identifier; and an unmapped id raising.
  </action>
  <verify>
    <automated>uv run --project eval pytest tests/test_seed.py -v</automated>
    <human-check>With the isolated eval stack running, execute `uv run --project eval lancet-eval seed --corpus multihop_rag` to completion. Confirm: the command exits 0; `eval/corpora/multihop_rag/document_map.json` has one entry per article in `documents.subset.jsonl`; the map header carries a non-empty `index_generation`; and the eval `documents` table row count matches the map entry count. Then run `seed` a second time and confirm it uploads nothing and exits 0. Paste the counts and both command outputs into the SUMMARY. Note the wall-clock time and any rate-limit pressure observed — the engine embeds every chunk against the same free-tier quota the run will need.</human-check>
  </verify>
  <acceptance_criteria>
    - `uv run --project eval pytest tests/test_seed.py -v` exits 0 offline.
    - A test asserts that every recorded upload request's multipart body contains a `file` part and contains neither a `chunk_size` nor a `chunk_overlap` part, read from `httpx_mock.get_requests()`.
    - A test asserts a non-2xx upload response aborts the seed with the offending article identifier in the raised message, and that `document_map.json` on disk contains only the articles that succeeded before it.
    - A test asserts a second `seed` invocation against an existing complete map issues zero upload requests.
    - A round-trip test asserts `load_document_map()` resolves a `document_id` taken from a synthetic response to the expected corpus article identifier, and raises for an id not in the map.
    - `document_map.json` is committed, its header carries `corpus`, `seeded_at` and `index_generation`, and its entry count equals the line count of `documents.subset.jsonl` minus any entries recorded as skipped.
    - `reseed` without the confirmation flag exits non-zero and performs no destructive action, asserted by unit test.
    - `reseed` exits non-zero without acting when the eval LanceDB path or PostgreSQL schema equals the dev configuration's, asserted by unit test.
    - The human-check records the live seed completing, the map/table count match, and a no-op second seed.
  </acceptance_criteria>
  <done>The reduced document subset is ingested once through the real upload path into the isolated store, the document map is captured at seed time and committed, and reseeding is explicit, guarded and destructive only against the eval store.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preflight — fail fast with guidance, not forty minutes in</name>
  <files>eval/src/lancet_eval/preflight.py, eval/src/lancet_eval/cli.py, eval/tests/test_preflight.py, eval/README.md</files>
  <read_first>
    - `.planning/phases/06.3-python-evaluation-harness-benchmark-corpora-and-recorded-run/06.3-AI-SPEC.md` §4 "State Management" items 2 and 3 (store isolation assertion, corpus-generation tracking), §4b "Cost and Latency Budget" (the free-tier arithmetic: 20 requests per minute, 50 requests per day below the credit threshold and 1,000 at or above it, and the `X-RateLimit-Remaining` header), and §3 pitfall 6 (the DTO still rejects unknown fields, and why preflight should probe the field name).
    - `.planning/phases/06.3-python-evaluation-harness-benchmark-corpora-and-recorded-run/06.3-PATTERNS.md` — the fail-closed `PreflightError` pattern and its `scripts/phase02_live_evidence.py` analog.
    - `gateway/main.go` — the `/health` route and its response shape (it reports the engine's reachability and latency, which is how the harness checks the engine without speaking gRPC), and `ragQueryRequestBody` with `dec.DisallowUnknownFields()`.
    - `config/config.toml` `[openrouter] generation_model` — the value preflight compares the configured judge model against.
    - `eval/src/lancet_eval/config.py`, `eval/src/lancet_eval/corpus.py`, `eval/src/lancet_eval/seed.py` — the settings, expected generation and document map preflight reads.
  </read_first>
  <behavior>
    - `preflight` runs seven named checks and prints each with an explicit pass or fail line: gateway reachable, engine reachable through the gateway's health response, store isolation, corpus seeded with a document map present, corpus generation matches the expected value, request field names accepted, and judge configuration.
    - The API-key check runs only when judged dimensions are requested (`--judged`), and its absence is a pass with a note when they are not.
    - The judge check fails when the configured judge model equals the engine's configured generation model.
    - A corpus-generation mismatch fails with a message naming both the expected and observed generation and telling the operator to reseed.
    - A store-isolation collision fails naming which half collided, before any network call is made.
    - `preflight` exits 0 when every applicable check passes and non-zero on the first failure class, with all failures reported rather than only the first.
    - When the judge endpoint's rate-limit headers are visible, `preflight --judged` prints the remaining daily allowance and warns when the planned judged slice exceeds it.
  </behavior>
  <action>
Write `preflight.py` with `PreflightError` carrying a category and an actionable message, a `PreflightReport` collecting every check's outcome, and one function per check so each is independently testable and independently named in the output. Run the checks that need no network first — store isolation and judge distinctness — so an obviously misconfigured run fails before it touches anything.

`check_store_isolation()` compares the configured eval LanceDB path and eval PostgreSQL schema against the dev configuration's and refuses on either collision, naming which one. DEBT-P3-WARN-DX records the existing seeder as non-idempotent, so a partial re-seed into a shared store is unrecoverable and silently corrupting; this check is the only thing standing between the operator and that.

`check_gateway()` calls `GET /health` and `check_engine()` reads the engine block out of that same response rather than speaking gRPC, because the harness's contract is the HTTP surface. `check_corpus_generation()` issues one throwaway `/rag/query` and compares `snapshot.index_generation` against the corpus TOML's expected value, failing with a reseed instruction on mismatch — this is the control that stops a mid-run reseed from producing a silently mixed journal. `check_seeded()` asserts `document_map.json` exists, parses, and has a non-empty entry set.

`check_request_field_names()` sends one `/rag/query` request carrying `disable_graph_context` set to boolean true and treats an HTTP 400 as a named failure explaining that the harness and gateway have drifted on the field spelling. The field exists today, so this is not an existence probe; it is a version-skew detector, and the gateway's `DisallowUnknownFields` is what turns the skew into a loud 400 instead of a silently ignored field.

`check_judge_distinct()` reads the engine's configured generation model from `config/config.toml` and fails if the harness's configured judge model equals it — a judge grading its own generator's work is not a measurement, and this is D-62 enforced in code rather than in prose. `check_api_key()` runs only under `--judged` and asserts the environment variable is present and non-empty without ever echoing its value.

Under `--judged`, additionally attempt to read the judge provider's rate-limit headers and print the remaining daily allowance, warning when the planned judged slice exceeds it. Do not assume the higher daily tier: below the credit threshold the allowance is small enough to make a full drive a multi-week activity, and the operator needs to know that before starting, not after.

Collect every failure and report all of them, then exit non-zero — an operator fixing one misconfiguration at a time across seven round trips is the failure mode this command exists to prevent.

Document the preflight output and each failure's remedy in `eval/README.md`. Write `tests/test_preflight.py` covering every check in both outcomes against `pytest-httpx`, plus the isolation and judge checks with no network at all.
  </action>
  <verify>
    <automated>uv run --project eval pytest tests/test_preflight.py -v</automated>
    <automated>uv run --project eval pytest</automated>
    <automated>uv run --project eval ruff check --preview src tests</automated>
    <human-check>With the isolated eval stack running and the corpus seeded, run `uv run --project eval lancet-eval preflight --corpus multihop_rag` and then `... preflight --corpus multihop_rag --judged`. Confirm both print every named check with a pass line and exit 0, and that the judged form prints the remaining daily allowance. Then stop the gateway and re-run, confirming the failure names the gateway and gives an actionable remedy. Paste all three outputs into the SUMMARY.</human-check>
  </verify>
  <acceptance_criteria>
    - `uv run --project eval pytest` exits 0 offline with the whole suite green.
    - Each of the seven checks has both a passing and a failing test, asserted on the check's own name appearing in the report and on the failure message content.
    - The store-isolation and judge-distinctness tests run with `httpx_mock` configured to assert zero requests were made, proving those checks are network-free.
    - The corpus-generation mismatch test asserts the failure message contains both the expected and the observed generation values and the word instructing a reseed.
    - The field-name test asserts that a mocked HTTP 400 on the probe request produces a named failure rather than an unhandled exception.
    - The judge-distinctness test asserts failure when the judge model string equals the generation model string read from `config/config.toml`, and passes when they differ.
    - The API-key test asserts no assertion message, log line or report field contains the key's value, using a distinctive sentinel value in the environment for the test.
    - A test asserts that when three checks fail, all three appear in the report and the exit code is non-zero — not just the first.
    - `uv run --project eval ruff check --preview src tests` exits 0.
  </acceptance_criteria>
  <done>One command reports the state of every precondition a run depends on, refuses a contaminated or stale store, and gives the operator an actionable remedy before any quota is spent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval configuration → dev store | A misconfigured eval run can write into or destroy the developer's working store; this is the boundary the isolation checks defend. |
| harness → gateway `/documents` | Article bodies are untrusted third-party text uploaded through the production ingestion surface. |
| environment → committed artifacts | The eval DSN and the API key live in the environment and must never reach a committed file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3-13 | Tampering | `reseed` destructive path | high | mitigate | `reseed` requires an explicit confirmation flag and refuses before acting when the configured eval LanceDB path or PostgreSQL schema equals the dev configuration's; `preflight`'s isolation check runs first and is network-free. Both are asserted by unit test. |
| T-06.3-14 | Information Disclosure | eval DSN and API key | high | mitigate | `config/config.eval.toml` carries no DSN and no key — the DSN arrives via `LANCET_GATEWAY__DATABASE_URL` and the key via the environment, matching `config/config.toml`'s deliberate empty `database_url`. The API-key check asserts presence without echoing the value, and a test proves the value never appears in the report. |
| T-06.3-15 | Elevation of Privilege | Atlas eval-schema application | medium | mitigate | The eval environment reuses the existing `file://db/schema.hcl` source and is scoped to the `lancet_eval` schema, so an eval migration cannot alter the dev `public` schema. The human-check confirms rows land in `lancet_eval` and not in `public`. |
| T-06.3-16 | Denial of Service | seeding against the free-tier quota | medium | accept | Seeding embeds every chunk against the shared OpenRouter allowance and can exhaust it. Accepted as an operational cost inherent to D-56's real-ingestion-path requirement; mitigated only in that seeding is a once-per-corpus explicit command and the human-check records the observed quota pressure so the run can be budgeted around it. |
| T-06.3-17 | Spoofing | file upload through `/documents` | low | transfer | Upload validation, the 10MB ceiling and content-type handling are the gateway's existing controls (Phase 6 D-15 bad-input matrix); the harness adds no new upload surface and inherits them unchanged. |
</threat_model>

<verification>
- `uv run --project eval pytest` exits 0 offline.
- `uv run --project eval ruff check --preview src tests` exits 0.
- The Atlas probe output and chosen branch are recorded in the SUMMARY.
- The live human-checks confirm: eval rows in `lancet_eval` and not `public`; a complete seed with a matching document-map count; a no-op second seed; and preflight passing, then failing actionably with the gateway stopped.
</verification>

<success_criteria>
The eval store is isolated on both halves from one DDL source, the reduced document subset is seeded once through the real ingestion path with its document map captured and committed, reseed is explicit and guarded, and preflight reports every precondition with an actionable remedy before any quota is spent.
</success_criteria>

<output>
Create `.planning/phases/06.3-python-evaluation-harness-benchmark-corpora-and-recorded-run/06.3-04-SUMMARY.md` when done.
</output>
