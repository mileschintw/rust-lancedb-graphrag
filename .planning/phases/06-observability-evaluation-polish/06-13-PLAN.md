---
phase: 06-observability-evaluation-polish
plan: 13
type: execute
wave: 11
depends_on:
  - 06-10
files_modified:
  - engine/src/prompt.rs
  - engine/src/generation/mod.rs
  - engine/src/generation/openrouter.rs
  - engine/src/generation/tests.rs
  - engine/src/workflow/nodes/generate.rs
  - engine/src/workflow/mod.rs
  - engine/src/tests/workflow_phase5.rs
  - scripts/engine-test-targets.sh
autonomous: true
gap_closure: true
requirements:
  - RAG-03
estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low
  # Uncalibrated (no samples); factor 1. Keep as one plan.
must_haves:
  truths:
    - "SC3 / D-10 / D-11: when the caller has opted in and evidence is empty, the production generator packing path contacts the model and the workflow returns answer_basis MODEL_ONLY, NoticeCode::ModelOnly, and zero citations."
    - "SC3 production-shaped runner: one test constructs AssemblePromptNode and GenerateAnswerNode the way engine/src/service.rs build_production_workflow does (.with_settings, .with_citation_repair_enabled on GenerateAnswerNode) and drives the extracted packing helper, then asserts the MODEL_ONLY terminal from run_workflow."
    - "SC3 flag-off half remains the pre-phase fail-closed path: pack_evidence_and_graph_prompt still returns EmptyEvidence on an empty evidence slice; the existing test pack_evidence_and_graph_prompt_empty_evidence_still_errors_regardless_of_graph_facts stays passing and is not rewritten to succeed."
    - "D-10: OpenRouterGenerator::execute_one_call takes an empty-evidence branch that uses pack_model_only_prompt / model_only_system_policy and does not call pack_evidence_and_graph_prompt when request.evidence is empty and request.allow_model_only is true."
    - "D-10 packing helper: crate-visible free function taking question, evidence, graph_facts, graph_weight, evidence_budget, max_output_tokens, allow_model_only, and cancel; returning packed messages plus the evidence slice used for validation. Empty-evidence opt-in validation slice is the empty slice."
    - "D-10: GenerationRequest carries allow_model_only, set from ctx.allow_model_only in GenerateAnswerNode and in run_inline_prompt_generation_remainder, and execute_one_call validates ModelOutput with grounding_limits.with_allow_model_only(request.allow_model_only) against that validation slice so adapter-side Guard 1/2 cannot reject a legitimate opted-in model-only result."
    - "D-10 vs D-19 disposition: the outbound json_schema answer_basis enum admits the snake_case value model_only in addition to retrieval and mixed so a structured-output provider can return the D-10 basis. D-19 still forbids adding or removing schema properties and required keys; this is an enum-member admission of an already published proto value, governed by D-10, not a D-17 prompt-schema rewrite. No CONTEXT/ROADMAP amendment and no checkpoint:decision — D-10 already locked the published MODEL_ONLY contract."
    - "The model-only system policy does not tell the model to answer using only provided evidence blocks or to cite numbered markers that do not exist."
    - "Flagged assumption (specless probe): the unclassified RAG-03 probe edge from the EDGE_ABSENT/PROHIB_ABSENT report stays unresolved. This plan does not treat it as covered."
    - "Human gates from 06-VERIFICATION.md human_verification items 1 and 2 stay owner decisions. This plan does not record an owner adjudication for either."
    - "D-76: the model-only notice still goes through the single typed constructor. D-85: no CI configuration is added. D-16: no weak-evidence threshold is introduced."
  prohibitions:
    - statement: "MUST NOT treat the unclassified RAG-03 probe edge as covered or auto-resolved. No 06-SPEC.md exists; the edge remains unresolved pending manual review at phase verification."
      status: flagged-unverified
      verification: manual
      category: specless-probe
    - statement: "MUST NOT present a model-only answer as a grounded one. It must never carry a retrieval or mixed answer basis, must never carry citations, and must never be returned without its ModelOnly notice."
      status: resolved
      verification: test
      category: transparency
  artifacts:
    - path: "engine/src/generation/openrouter.rs"
      provides: "Empty-evidence packing branch, packing helper, and answer_basis enum that includes model_only."
      contains: "execute_one_call"
    - path: "engine/src/prompt.rs"
      provides: "Dedicated model-only system policy consumed by pack_model_only_prompt."
      contains: "model_only_system_policy"
    - path: "engine/src/generation/mod.rs"
      provides: "GenerationRequest.allow_model_only plumbed from the workflow opt-in."
    - path: "engine/src/workflow/nodes/generate.rs"
      provides: "GenerateAnswerNode copies ctx.allow_model_only onto GenerationRequest."
    - path: "engine/src/workflow/mod.rs"
      provides: "run_inline_prompt_generation_remainder copies ctx.allow_model_only onto GenerationRequest (D-11 tracer/production parity)."
    - path: "engine/src/tests/workflow_phase5.rs"
      provides: "Node packing-path test and production-shaped run_workflow SC3 test."
  key_links:
    - from: "engine/src/workflow/nodes/generate.rs"
      to: "engine/src/generation/openrouter.rs"
      via: "GenerationRequest.allow_model_only and empty evidence"
      pattern: "allow_model_only"
    - from: "engine/src/prompt.rs"
      to: "engine/src/generation/openrouter.rs"
      via: "model_only_system_policy / pack_model_only_prompt on the empty-evidence branch"
      pattern: "pack_model_only_prompt"
    - from: "engine/src/service.rs"
      to: "engine/src/tests/workflow_phase5.rs"
      via: "GenerateAnswerNode.with_settings and with_citation_repair_enabled construction shape"
      pattern: "with_settings"
---

## Phase User Story

**As a** client of Lancet's `/rag/query` endpoint, **I want to** receive an honest, machine-readable
answer even when the graph is unavailable, one retrieval path fails, evidence is absent, or the
model's citations do not resolve, **so that** I can tell a degraded answer from a healthy one — and a
grounded answer from a model-only one — without parsing prose.

> Gap closure for SC3 (failed): the opted-in empty-evidence path must produce MODEL_ONLY on the
> production packing path, not only against FakeGenerator.

<objective>
Close 06-VERIFICATION.md gap SC3: make AssemblePromptNode's model-only helper and the OpenRouter
packing path agree so an opted-in zero-evidence run reaches the model and returns MODEL_ONLY.
Addresses review concern: fail-closed named-test gates, production-shaped service.rs runner wiring,
packing-helper signature plus empty-slice validation, and D-11 remainder allow_model_only plumbing.

Purpose: Plan 06-10 wired the opt-in, runner gates, and FakeGenerator tests, but
OpenRouterGenerator::execute_one_call still always calls pack_evidence_and_graph_prompt, which
returns EmptyEvidence; that maps to non-retryable InvalidRequest and NODE_ERROR_KIND_LLM_GENERATION_FAILED
before the provider is contacted. The outbound schema also omits model_only, and pack_model_only_prompt
reuses the grounded evidence policy.
Output: empty-evidence packing branch, dedicated model-only policy, schema enum admission, remainder
flag copy, GenerateAnswerNode.with_settings packing test, and a production-shaped run_workflow test.
</objective>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-preconditions.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-reversibility.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-mvp-mode.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-observability-evaluation-polish/06-CONTEXT.md
@.planning/phases/06-observability-evaluation-polish/06-RESEARCH.md
@.planning/phases/06-observability-evaluation-polish/06-PATTERNS.md
@.planning/phases/06-observability-evaluation-polish/06-AI-SPEC.md
@.planning/phases/06-observability-evaluation-polish/06-VERIFICATION.md
@.planning/phases/06-observability-evaluation-polish/06-REVIEWS.md
@.planning/phases/06-observability-evaluation-polish/06-10-PLAN.md
@.planning/phases/06-observability-evaluation-polish/06-10-SUMMARY.md
@AGENTS.md
@rust-guidelines.md
@engine/src/generation/openrouter.rs
@engine/src/workflow/nodes/generate.rs
@engine/src/workflow/mod.rs
@engine/src/prompt.rs
@engine/src/service.rs

## Remaining human gates (not this plan)

06-VERIFICATION.md `human_verification` item 1 (unclassified RAG-03 probe edge) and item 2
(citation total-drop `effective_allow` contract) stay owner decisions. Re-verification after this
gap closure must still surface them. Do not implement either as a silent pass.

## Artifacts this phase produces

- OpenRouter empty-evidence packing branch that uses pack_model_only_prompt / model_only_system_policy.
- Crate-visible packing helper with an empty-slice validation branch for opted-in model-only.
- GenerationRequest.allow_model_only copied from WorkflowContext in GenerateAnswerNode and in
  run_inline_prompt_generation_remainder.
- Outbound answer_basis json_schema enum including model_only (D-10 enum admission; D-19 object-shape freeze).
- GenerateAnswerNode.with_settings packing-path test plus production-shaped run_workflow SC3 test.
- Updated scripts/engine-test-targets.sh library count for tests this plan adds.
</context>

<scope_rationale>
Wave 11 continues after executed waves 1–10. This plan depends on 06-10 (opt-in surface) and must
land before 06-14 because both edit generate.rs, prompt.rs, workflow_phase5.rs, and the test-count
gate. No new crates. COVERAGE.md records the OpenRouter surface as the existing chat endpoint with
enum-member admission only. Assumption-delta scan 6 reported a pluralization hit on D-14's locked
"no second provider call" wording — no-change; this plan does not add a second provider.
</scope_rationale>

## Review incorporation

| Finding | Disposition | Rationale |
|---------|-------------|-----------|
| Fail-closed `cargo test -- --list \| grep -qF` before every name-filtered cargo test | incorporated | Addresses review concern: name filters exit 0 when zero tests match. |
| Production-shaped SC3 runner matching `service.rs` `.with_settings` / packing path | incorporated | Task 1 named test `model_only_opt_in_empty_evidence_production_shaped_runner_returns_model_only`. |
| Packing helper signature and empty-branch validation slice | incorporated | Task 1 specifies the free-function parameters and empty-slice validation. |
| `run_inline_prompt_generation_remainder` omits `allow_model_only` | incorporated | Task 1 copies `ctx.allow_model_only` onto `GenerationRequest`; `engine/src/workflow/mod.rs` is in `files_modified`. |
| D-19 vs D-10 outbound `model_only` enum admission | incorporated | Explicit disposition in must_haves and Task 3 `<reversibility rating="costly">`; D-10 already locked; no new checkpoint. |
| SSE `degraded_mode: false` when metadata is nil (`gateway/internal/sse/sse.go`) | deferred | Phase 6.2 owns `WorkflowCompletedEvent.metadata` population (D-41). Flag only; not an SC3 blocker. |
| config-rs `prefix_separator` / `LANCET__` trap (`config.rs`) | deferred | Future cleanup; not a Phase 6 gap-closure blocker. |
| Systemic grep that every `GenerateAnswerNode::new` uses `.with_settings` | deferred | Out of SC3 closure scope; 34 existing construction sites; schedule with 6.1+ behavior plans. |
| ROADMAP summary-table rows 1–3 column misalignment | deferred | Phase 6.4 owns ROADMAP reconciliation (D-04 / D-78 / D-86). |
| Unclassified RAG-03 specless-probe edge | deferred (flagged) | Preserved as flagged-unverified; no 06-SPEC.md. |

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end opted-in empty evidence through production packing</name>
  <read_first>AGENTS.md; rust-guidelines.md; 06-VERIFICATION.md (gap SC3); 06-REVIEWS.md (SC3 production-shaped runner); 06-10-SUMMARY.md; engine/src/generation/openrouter.rs (execute_one_call packing and the post-response validate_grounding_with_limits call); engine/src/generation/mod.rs (GenerationRequest, GroundingLimits::with_allow_model_only); engine/src/workflow/nodes/generate.rs (GenerationRequest::new at the start of run, with_settings); engine/src/service.rs (build_production_workflow AssemblePromptNode.with_settings and GenerateAnswerNode.with_settings / with_citation_repair_enabled); engine/src/workflow/mod.rs (run_inline_prompt_generation_remainder GenerationRequest construction); engine/src/prompt.rs (pack_model_only_prompt, pack_evidence_and_graph_prompt EmptyEvidence); engine/src/tests/workflow_phase5.rs (model_only_opt_in_true_zero_evidence_runs_generation_and_emits_notice)</read_first>
  <files>engine/src/generation/mod.rs, engine/src/generation/openrouter.rs, engine/src/workflow/nodes/generate.rs, engine/src/workflow/mod.rs, engine/src/tests/workflow_phase5.rs</files>
  <action>
    Per D-10 and D-11, make the opted-in empty-evidence path survive OpenRouter packing instead of dying as EmptyEvidence mapped to InvalidRequest. Addresses review concern: packing-helper signature, empty-slice validation, remainder flag copy, and production-shaped runner.

    Add allow_model_only: bool to GenerationRequest, default false in GenerationRequest::new, and include it in PartialEq. In GenerateAnswerNode::run, after GenerationRequest::new(ctx.original_query, ctx.evidence_blocks), set req.allow_model_only from ctx.allow_model_only before the request snapshot is cloned for retry.

    In run_inline_prompt_generation_remainder, after GenerationRequest::new and the existing graph_facts / graph_weight / session / correlation / cancel copies, also set gen_req.allow_model_only from ctx.allow_model_only so the tracer remainder and GenerateAnswerNode share D-11 opt-in on the same request field.

    Extract the packing decision used by OpenRouterGenerator::execute_one_call into a crate-visible async free function in the same module (not a method on OpenRouterGenerator, so tests need no API key and no reqwest Client). Signature: question, evidence slice, graph_facts, graph_weight, evidence_budget, max_output_tokens, allow_model_only, and cancel. Return packed system and user message strings plus the evidence list execute_one_call must pass into validate_grounding_with_limits. execute_one_call reads evidence_token_budget and max_completion_tokens from self.config and forwards them into this helper.

    Helper behavior per D-10: when evidence is empty and allow_model_only is true, skip pack_evidence_and_graph_prompt; build messages from pack_model_only_prompt / model_only_system_policy (Task 2 may land the dedicated policy in the same commit if needed for the tracer to compile); the validation evidence list on this branch is empty. When evidence is empty and allow_model_only is false, keep today's fail-closed InvalidRequest / EmptyEvidence outcome. When evidence is non-empty, keep calling pack_evidence_and_graph_prompt unchanged and validate against that packed evidence list.

    After a successful pack, execute_one_call still returns a ModelOnly ModelOutput only when validation agrees: pass grounding_limits.with_allow_model_only(request.allow_model_only) into validate_grounding_with_limits with the helper's validation slice (empty on the opt-in empty-evidence branch). Keep existing token-usage checks on this path.

    Leave the runner flag-off short-circuit as shipped. Leave pack_evidence_and_graph_prompt's EmptyEvidence return as shipped. Covering tests must invoke the extracted helper, not only FakeGenerator.

    Canned ModelOutput answers used by these tests must contain no numbered citation markers, because validate_grounding_with_limits enforces set equality between cited_evidence_ids and inline markers and the opt-in path cites nothing.

    Add generate_answer_node_model_only_empty_evidence_uses_production_packing_path in engine/src/tests/workflow_phase5.rs: build GenerateAnswerNode::new(Some(packing_gen)).with_settings(GroundingLimits::new(8192, 2048), citation_excerpt_max_chars, graph_weight) with citation repair left at default; ctx.allow_model_only true; ctx.evidence_blocks empty. packing_gen must call the extracted OpenRouter packing helper on the GenerationRequest it receives, then return a canned ModelOutput with empty cited_evidence_ids and AnswerBasis::ModelOnly. Assert the node run is Ok, ctx.answer_basis is ModelOnly, structured_citations is empty, citations is empty, and a NoticeCode::ModelOnly notice is present. If the helper still called pack_evidence_and_graph_prompt on empty evidence, this test must fail.

    Add model_only_opt_in_empty_evidence_production_shaped_runner_returns_model_only in the same file, modeled on model_only_opt_in_true_zero_evidence_runs_generation_and_emits_notice but wiring nodes the way service.rs build_production_workflow does: AssemblePromptNode::with_settings using the same GroundingLimits budgets and graph_weight; GenerateAnswerNode::new(Some(packing_gen)).with_settings(those limits, citation_excerpt_max_chars, graph_weight).with_citation_repair_enabled(true). packing_gen again calls the extracted helper. Drive run_workflow with allow_model_only true and empty retrieval. Assert WorkflowCompleted final_response answer_basis MODEL_ONLY, empty citations, and a ModelOnly notice. Extracting a shared production-node builder from service.rs is allowed but not required; mirroring the construction shape is sufficient.
  </action>
  <acceptance_criteria>
    - OpenRouter packing on empty evidence with allow_model_only true does not return PromptAssemblyError::EmptyEvidence and does not map to NODE_ERROR_KIND_LLM_GENERATION_FAILED before a canned model result can be applied.
    - The packing helper is a free function with the parameters listed in the action; empty-evidence opt-in validation uses an empty evidence slice.
    - GenerateAnswerNode and run_inline_prompt_generation_remainder copy ctx.allow_model_only onto GenerationRequest; adapter validation uses with_allow_model_only(request.allow_model_only).
    - Named test generate_answer_node_model_only_empty_evidence_uses_production_packing_path exists, uses .with_settings, and drives the extracted packing helper rather than FakeGenerator.
    - Named test model_only_opt_in_empty_evidence_production_shaped_runner_returns_model_only exists, uses AssemblePromptNode.with_settings plus GenerateAnswerNode.with_settings and with_citation_repair_enabled, drives the packing helper, and asserts the MODEL_ONLY terminal from run_workflow.
    - pack_evidence_and_graph_prompt_empty_evidence_still_errors_regardless_of_graph_facts still passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list | grep -qF 'generate_answer_node_model_only_empty_evidence_uses_production_packing_path' && cargo test --lib --manifest-path engine/Cargo.toml --locked -- generate_answer_node_model_only_empty_evidence_uses_production_packing_path</automated>
    <automated>cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list | grep -qF 'model_only_opt_in_empty_evidence_production_shaped_runner_returns_model_only' && cargo test --lib --manifest-path engine/Cargo.toml --locked -- model_only_opt_in_empty_evidence_production_shaped_runner_returns_model_only</automated>
    <automated>cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list | grep -qF 'pack_evidence_and_graph_prompt_empty_evidence_still_errors_regardless_of_graph_facts' && cargo test --lib --manifest-path engine/Cargo.toml --locked -- pack_evidence_and_graph_prompt_empty_evidence_still_errors_regardless_of_graph_facts</automated>
  </verify>
  <done>Opted-in empty evidence is packable on the production path; GenerateAnswerNode.with_settings and a service-shaped run_workflow both prove it.</done>
  <reversibility rating="reversible">Additive request flag and packing branch; flag-off EmptyEvidence behavior is preserved.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dedicated model-only system policy</name>
  <read_first>rust-guidelines.md; engine/src/prompt.rs (base_system_policy, pack_model_only_prompt); engine/src/workflow/nodes/assemble_prompt.rs (model-only branch writing ctx.assembled_prompt); 06-CONTEXT.md D-10 D-17 D-19</read_first>
  <files>engine/src/prompt.rs, engine/src/generation/openrouter.rs, engine/src/tests/workflow_phase5.rs</files>
  <behavior>
    - pack_model_only_prompt(question) contains the question and the model-only policy string.
    - The model-only policy string is distinct from base_system_policy: it does not require numbered evidence markers and does not restrict answers to provided evidence blocks.
    - base_system_policy is unchanged, including the D-17 evidence-over-priors sentence.
    - OpenRouter empty-evidence system/user messages use the model-only policy, not base_system_policy.
  </behavior>
  <action>
    Per D-10, add model_only_system_policy as a documented helper in engine/src/prompt.rs. Rewrite pack_model_only_prompt to format that policy plus the question, matching today's concatenation shape so AssemblePromptNode checkpoint text stays a single string.

    Point the Task 1 empty-evidence OpenRouter branch at this policy for the system message and at the question body for the user message (or at pack_model_only_prompt if that is the single packed user blob — send the model-only policy on this branch, not the grounded base_system_policy).

    Leave the json_schema property set unchanged (D-19 object-shape freeze). Leave D-17's sentence inside base_system_policy unchanged. Add pack_model_only_prompt_uses_ungrounded_policy in engine/src/tests/workflow_phase5.rs (or prompt.rs tests if that is the existing analog) asserting the packed model-only string contains the question and does not contain the grounded-only-evidence / numbered-marker instructions that base_system_policy uses. Follow rust-guidelines.md M-CANONICAL-DOCS on the new public helper.
  </action>
  <acceptance_criteria>
    - pack_model_only_prompt no longer concatenates base_system_policy.
    - A named test proves the model-only packed string is question-bearing and is not the grounded citation policy.
    - Grounded packing still uses base_system_policy, including the D-17 precedence sentence.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list | grep -qF 'pack_model_only_prompt_uses_ungrounded_policy' && cargo test --lib --manifest-path engine/Cargo.toml --locked -- pack_model_only_prompt_uses_ungrounded_policy</automated>
  </verify>
  <done>Model-only prompts no longer instruct citing evidence that does not exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Admit model_only on the outbound answer_basis schema</name>
  <read_first>06-CONTEXT.md D-10 D-19; 06-REVIEWS.md (D-19 vs D-10 disposition); engine/src/generation/openrouter.rs (answer_basis enum in schema_json); engine/src/generation/tests.rs (assert on answer_basis enum); engine/src/generation/mod.rs (AnswerBasis serde snake_case); scripts/engine-test-targets.sh</read_first>
  <files>engine/src/generation/openrouter.rs, engine/src/generation/tests.rs, scripts/engine-test-targets.sh</files>
  <reversibility rating="costly">Admits model_only on the outbound OpenRouter json_schema enum that structured-output models must emit; undo means providers that already emit the member would fail schema validation. D-10 already locked MODEL_ONLY on the published Lancet contract, so this is not a new one-way door and needs no checkpoint:decision.</reversibility>
  <behavior>
    - Captured OpenRouter json_schema properties.answer_basis.enum contains retrieval, mixed, and model_only.
    - Required keys remain answer, cited_evidence_ids, answer_basis, notices, warnings.
    - Empty-evidence opted-in generate() against a mock chat endpoint reaches HTTP POST rather than returning InvalidRequest from EmptyEvidence.
  </behavior>
  <action>
    Per D-10, extend the execute_one_call schema_json answer_basis enum from retrieval and mixed to also include model_only. D-19 vs D-10 disposition: D-19 freezes the D-17 prompt change to prompt text and freezes json_schema object shape (properties and required keys). Admitting an enum member for the already published proto AnswerBasis::ModelOnly is governed by D-10, not a D-19 property-set rewrite. Do not add or remove properties or required keys. Update the existing assertion in engine/src/generation/tests.rs that currently expects only retrieval and mixed.

    Add openrouter_empty_evidence_opt_in_reaches_chat_with_model_only_schema mirroring the existing mock TcpListener OpenRouter tests: GenerationRequest with empty evidence and allow_model_only true must POST a chat body whose schema enum includes model_only, and must not fail with prompt assembly failed / EmptyEvidence before the mock accepts the request.

    Update scripts/engine-test-targets.sh library expected count by exactly the number of library tests this plan added (Tasks 1–3, including the production-shaped runner); leave inspect_lancedb, seed_rag_fixture, engine bin, and config_startup counts unchanged. Record before/after rows in the SUMMARY. No CI files (D-85). Leave COVERAGE.md's no-new-external-API declaration intact aside from the OpenRouter enum-admission footnote already recorded.
  </action>
  <acceptance_criteria>
    - Outbound answer_basis enum includes model_only; required property names are unchanged.
    - Named mock-HTTP test openrouter_empty_evidence_opt_in_reaches_chat_with_model_only_schema passes.
    - scripts/engine-test-targets.sh exits 0 with the new library count.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list | grep -qF 'openrouter_empty_evidence_opt_in_reaches_chat_with_model_only_schema' && cargo test --lib --manifest-path engine/Cargo.toml --locked -- openrouter_empty_evidence_opt_in_reaches_chat_with_model_only_schema</automated>
    <automated>cargo clippy --manifest-path engine/Cargo.toml -- -D warnings && cargo fmt --manifest-path engine/Cargo.toml --check && cargo test --manifest-path engine/Cargo.toml --locked && sh scripts/engine-test-targets.sh</automated>
  </verify>
  <done>A structured-output provider can return model_only, and the test-count gate matches the new library tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GenerateAnswerNode → OpenRouterGenerator | Empty evidence plus allow_model_only crosses into provider packing and schema. |
| OpenRouter JSON schema → model | Structured-output enum constrains what answer_basis values the provider may emit. |
| Model-only answer → client | Ungrounded text must be distinguishable from retrieval-backed answers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-13-01 | Spoofing | MODEL_ONLY response | high | mitigate | Keep AnswerBasis::ModelOnly, empty citations, and NoticeCode::ModelOnly on the opted-in empty-evidence path; GenerateAnswerNode.into_model_only / notice constructor from 06-10 stay in force. |
| T-06-13-02 | Elevation of privilege | OpenRouter schema enum | high | mitigate | Admit model_only only as an additional enum member. D-18 conservative-wins reconciliation still weakens false retrieval claims; a model_only self-report is never strengthened. |
| T-06-13-03 | Tampering | pack_evidence_and_graph_prompt | high | mitigate | EmptyEvidence on empty slices stays; the new branch is gated on empty evidence AND allow_model_only. Existing EmptyEvidence unit test remains a passing pin. |
| T-06-13-04 | Information disclosure | model-only policy | medium | mitigate | Dedicated policy that does not instruct citing nonexistent evidence; do not log raw provider bodies in new code. |
| T-06-13-SC | Tampering | cargo installs | high | accept | No new crates or cargo add in this plan; legitimacy gate does not apply. |
</threat_model>

<verification>
- generate_answer_node_model_only_empty_evidence_uses_production_packing_path passes with .with_settings and a packing-path generator.
- model_only_opt_in_empty_evidence_production_shaped_runner_returns_model_only passes with service.rs-shaped node construction.
- pack_model_only_prompt_uses_ungrounded_policy passes.
- openrouter_empty_evidence_opt_in_reaches_chat_with_model_only_schema passes.
- pack_evidence_and_graph_prompt_empty_evidence_still_errors_regardless_of_graph_facts still passes.
- cargo test --manifest-path engine/Cargo.toml --locked and sh scripts/engine-test-targets.sh exit 0.
- Each named cargo test verify is prefixed with cargo test -- --list | grep -qF so a missing test fails the gate.
</verification>

<success_criteria>
SC3 opt-in half is achievable on the production packing path and on a service-shaped run_workflow: MODEL_ONLY + notice + zero citations. Flag-off EmptyEvidence behavior is unchanged. The unclassified RAG-03 probe edge and the two human_verification items remain unresolved owner gates.
</success_criteria>

<output>
Create `.planning/phases/06-observability-evaluation-polish/06-13-SUMMARY.md` when done.
</output>
