---
phase: 06-observability-evaluation-polish
plan: 16
type: execute
wave: 14
depends_on:
  - 06-15
files_modified:
  - engine/src/workflow/nodes/generate.rs
  - engine/src/generation/openrouter.rs
  - engine/src/tests/workflow_phase5.rs
autonomous: true
gap_closure: true
gap_ids:
  - G-06-1
  - G-06-2
requirements:
  - RAG-03
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "When allow_model_only is false, D-18 total-drop must return LlmGenerationFailed rather than succeeding as AnswerBasis::ModelOnly (G-06-1)."
    - "When allow_model_only is true, D-18 total-drop succeeds as AnswerBasis::ModelOnly with CITATION_DROPPED and BASIS_RECONCILED notices (G-06-1)."
    - "A citation naming a retrieved-but-truncated block (not in the prompt-packed subset) is not resolved and ships no excerpt; it is dropped with CITATION_DROPPED notice (G-06-2)."
    - "If all citations are dropped due to being truncated, the allow_model_only rule is applied (fails closed if false, succeeds as ModelOnly if true) (G-06-2)."
  artifacts:
    - path: "engine/src/workflow/nodes/generate.rs"
      provides: "Flag-aware effective_allow computation on total-drop and packed-evidence bound marker/citation resolution"
    - path: "engine/src/tests/workflow_phase5.rs"
      provides: "Unit and workflow tests pinning flag-off total-drop failure and truncated block citation drop behavior"
  key_links:
    - from: "engine/src/workflow/nodes/generate.rs:258"
      to: "engine/src/generation/mod.rs:validate_grounding_with_limits"
      desc: "effective_allow passed to limits.with_allow_model_only matches ctx.allow_model_only rather than forcing true on total_drop"
---

<objective>
Close UAT gaps G-06-1 (D-18 total-drop flag-off fail-closed semantics) and G-06-2 (retrieved-but-truncated citations must be dropped, not resolved).

Purpose: Ensure fail-closed integrity when callers opt out of model-only generation, and ensure that citations only resolve against the actual prompt-packed evidence universe.
Output: Fixed generate.rs, openrouter.rs (if needed), and comprehensive regression tests in workflow_phase5.rs.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-observability-evaluation-polish/06-UAT.md
@.planning/debug/d18-total-drop-flag-off.md
@.planning/debug/truncated-citation-resolution.md
@engine/src/workflow/nodes/generate.rs
@engine/src/generation/openrouter.rs
@engine/src/generation/mod.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce flag-dependent allow_model_only on D-18 total-drop path in generate.rs</name>
  <files>engine/src/workflow/nodes/generate.rs, engine/src/tests/workflow_phase5.rs</files>
  <behavior>
    - Test 1: When allow_model_only is false and all citations are dropped (total-drop), GenerateNode::run fails closed with NodeErrorKind::LlmGenerationFailed.
    - Test 2: When allow_model_only is true and all citations are dropped (total-drop), GenerateNode::run succeeds with AnswerBasis::ModelOnly and emits CITATION_DROPPED + BASIS_RECONCILED notices.
  </behavior>
  <action>
    In `engine/src/workflow/nodes/generate.rs`:
    1. Update the grounding validation on the repair path (around line 258): do NOT OR `total_drop` into `effective_allow`. Set `effective_allow = ctx.allow_model_only;`.
    2. When `total_drop` occurs and `ctx.allow_model_only` is false, ensure validation with limits rejects the model-only view and returns `NodeError::new(NodeErrorKind::LlmGenerationFailed, ...)`.
    3. Add regression tests in `engine/src/tests/workflow_phase5.rs` covering both `allow_model_only = false` (fails closed) and `allow_model_only = true` (succeeds as ModelOnly) on the total citation drop path.
  </action>
  <verify>
    <automated>cargo test --package engine --test workflow_phase5 total_drop</automated>
  </verify>
  <done>
    Total citation drop path returns LlmGenerationFailed when allow_model_only is false and succeeds as ModelOnly when allow_model_only is true.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Treat citations to retrieved-but-truncated blocks as unresolvable (CITATION_DROPPED)</name>
  <files>engine/src/workflow/nodes/generate.rs, engine/src/generation/openrouter.rs, engine/src/tests/workflow_phase5.rs</files>
  <behavior>
    - Test 1: Construct an evidence set where block [N] is truncated out of the prompt due to evidence token budget. When the LLM cites [N], the marker is dropped with CITATION_DROPPED notice and does not appear in structured_citations (no excerpt shipped).
    - Test 2: If the LLM cites both a surviving block [1] and a truncated block [N], [1] resolves to a structured citation and [N] is dropped with CITATION_DROPPED notice.
  </behavior>
  <action>
    In `engine/src/workflow/nodes/generate.rs` and `engine/src/generation/openrouter.rs`:
    1. Ensure the known evidence ID universe for marker extraction/repair and citation resolution (`evidence_ids` in `generate.rs`) strictly reflects the packed prompt subset (`ctx.evidence_blocks` as packed by `AssemblePromptNode` or prompt packing).
    2. Ensure `resolve_citations` and `resolve_markers` drop citations referencing blocks truncated from the prompt, emitting `CITATION_DROPPED` notices.
    3. Verify that `openrouter.rs` does not restore the pre-split packed check that would break D-18 total drop.
    4. Add unit and workflow tests in `engine/src/tests/workflow_phase5.rs` verifying that citations to truncated blocks are dropped and ship no excerpt.
  </action>
  <verify>
    <automated>cargo test --package engine --test workflow_phase5 truncated</automated>
  </verify>
  <done>
    Citations naming truncated blocks are dropped with CITATION_DROPPED notices and do not resolve or ship excerpts.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generator Output → Citation Resolver | Model output containing hallucinated or truncated citation IDs crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-16-01 | Information Disclosure | GenerateAnswerNode citation resolution | medium | mitigate | Restrict citation resolution universe to prompt-packed evidence so truncated blocks are dropped rather than leaking ungrounded excerpts |
| T-06-16-02 | Tampering / Elevation | GenerateAnswerNode model-only gating | high | mitigate | Fail closed with LlmGenerationFailed when allow_model_only is false and total citation drop occurs |
</threat_model>

<verification>
cargo test --package engine --test workflow_phase5
cargo test --package engine --test bad_input_matrix
</verification>

<success_criteria>
1. All unit and integration tests pass.
2. G-06-1 is fixed: allow_model_only=false total-drop fails closed with LlmGenerationFailed.
3. G-06-2 is fixed: retrieved-but-truncated citations are dropped and not resolved.
</success_criteria>

<output>
Create `.planning/phases/06-observability-evaluation-polish/06-16-SUMMARY.md` when done
</output>
