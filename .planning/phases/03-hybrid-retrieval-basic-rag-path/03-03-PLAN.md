---
phase: 03
plan: "03"
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - proto/lancet/v1/lancet.proto
  - engine/src/pb/lancet/v1/lancet.v1.rs
  - engine/src/pb/lancet/v1/lancet.v1.tonic.rs
  - gateway/proto/lancet/v1/lancet.pb.go
  - gateway/proto/lancet/v1/lancet_grpc.pb.go
  - engine/src/main.rs
  - config/config.toml
  - config/config.example.toml
  - engine/tests/config_startup.rs
  - engine/src/tests.rs
autonomous: true
requirements: [RAG-02, RAG-04]
must_haves:
  truths:
    - "The additive QueryRAG gRPC contract carries typed filters, effective session identity, structured citations, answer basis, notices/warnings, and a retrieval snapshot without renumbering existing fields."
    - "A valid gRPC QueryRAG call reaches the Rust retrieval, NoOpReranker, evidence, and Generator path and returns the structured response."
    - "Rust builds the initial BM25 snapshot from the current completed LanceDB corpus before registering the service or emitting the serving-readiness signal."
    - "The committed TOML and environment-overlay settings expose the locked retrieval and generation bounds/defaults, while replacement/restart switching remains DEBT-RAG-04."
  artifacts:
    - path: "proto/lancet/v1/lancet.proto"
      provides: "Backward-aware QueryRAG request/response with typed filter and evidence contracts"
      contains: "rpc QueryRAG"
    - path: "engine/src/main.rs"
      provides: "Rust tonic QueryRAG handler and initial combined dense/BM25 query-ready state"
      contains: "Bm25Index"
    - path: "config/config.toml"
      provides: "Committed retrieval, generation, and provider endpoint defaults"
      contains: "embedding_endpoint"
    - path: "engine/tests/config_startup.rs"
      provides: "Process-level readiness ordering and initial BM25 failure proof"
      contains: "Rust RAG Engine serving"
  key_links:
    - from: "proto/lancet/v1/lancet.proto"
      to: "engine/src/pb/lancet/v1/lancet.v1.rs"
      via: "Buf regenerates the Rust request/response and tonic service bindings"
      pattern: "QueryRagRequest"
    - from: "engine/src/main.rs"
      to: "engine/src/retrieval/mod.rs"
      via: "The tonic handler forwards validated requests to the Rust Retriever"
      pattern: "Retriever"
    - from: "engine/src/main.rs"
      to: "engine/src/retrieval/bm25.rs"
      via: "Startup constructs the initial completed-corpus snapshot before readiness"
      pattern: "Bm25Index"
    - from: "config/config.toml"
      to: "engine/src/main.rs"
      via: "TOML plus LANCET_ENGINE__ and LANCET_OPENROUTER__ environment overlays supply explicit bounds and provider endpoints"
      pattern: "RetrievalSettings"

---

<objective>
Expose the Rust query core through the additive gRPC contract and make engine readiness mean that the initial dense/BM25 state is query-ready.

Purpose: Create the first real cross-layer handoff—generated wire contract to the Rust semantic owner—before the Go HTTP boundary is added.
Output: Regenerated Rust/Go bindings, a real Rust QueryRAG handler, startup BM25 construction/readiness ordering, explicit configuration, and process-level readiness tests.
</objective>

<scope_note>
The gRPC response preserves future-compatible typed capacity for D-11 through D-16 and D-24 plus the current valid-path fields for D-17, but this plan executes only the valid path. It does not implement degraded retrieval, model-only fallback, graph failure handling, citation repair/downgrade, provider retries/fallback, or dynamic re-ingestion/restart atomic switching; those targets remain in `deferred-items.md`.

The ten-file plan list is an ownership map, not one broad edit: Task 1 atomically owns the proto source plus the four generated binding outputs; Task 2 owns the Rust service/configuration seam and its service test module; Task 3 owns only process-level startup proof. The generated-output group and service/config group are each kept together so Buf regeneration and the Rust handoff remain coherent, while the existing five-wave order stays unchanged.
</scope_note>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-AI-SPEC.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-RESEARCH.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-PATTERNS.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/deferred-items.md
@proto/lancet/v1/lancet.proto
@buf.gen.yaml
@engine/src/main.rs
@engine/src/db/mod.rs
@engine/src/retrieval/mod.rs
@engine/src/retrieval/bm25.rs
@engine/src/generation/mod.rs
@engine/src/generation/openrouter.rs
@engine/tests/config_startup.rs
@config/config.toml
@config/config.example.toml

<interfaces>
From engine/src/retrieval/mod.rs: Retriever accepts QueryRequest plus a combined query-ready state, validates UUIDv4/session/filter bounds, and returns ordered typed candidates or InvalidArgument/internal errors.

From engine/src/generation/mod.rs: Generator, GenerationRequest, ModelOutput, AnswerBasis, and GenerationError are provider-neutral; OpenRouterGenerator is constructed from explicit endpoint/model/sampling/timeout settings.

From proto/lancet/v1/lancet.proto: Existing QueryRAG fields 1-3 remain wire-compatible; all new typed filters, structured evidence, basis, notices/warnings, and snapshot fields must use fresh field numbers.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace the additive QueryRAG contract through stable Rust and Go bindings</name>
  <files>proto/lancet/v1/lancet.proto, engine/src/pb/lancet/v1/lancet.v1.rs, engine/src/pb/lancet/v1/lancet.v1.tonic.rs, gateway/proto/lancet/v1/lancet.pb.go, gateway/proto/lancet/v1/lancet_grpc.pb.go</files>
  <read_first>
    - AGENTS.md
    - rust-guidelines.md
    - go-guidelines.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-AI-SPEC.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-RESEARCH.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-PATTERNS.md
    - proto/lancet/v1/lancet.proto
    - buf.gen.yaml
    - gateway/go.mod
    - engine/src/main.rs
    - engine/src/retrieval/mod.rs
    - engine/src/rerank/mod.rs
    - engine/src/prompt.rs
    - engine/src/generation/mod.rs
    - engine/src/generation/openrouter.rs
  </read_first>
  <action>Before regenerating any Go output, read the root go-guidelines.md and detect the target Go version from gateway/go.mod (currently Go 1.25.0); apply the guidance through that version. Extend QueryRAGRequest and QueryRAGResponse additively, preserving existing field numbers and adding typed document/content filters, the valid-path answer-basis enum, structured citation provenance, notices/warnings, effective session ID, and compact retrieval snapshot fields per D-07, D-14, D-18, D-19, D-20, D-21, D-22, D-23, D-25, D-28, and D-31. Regenerate all Rust and Go outputs from the repository Buf configuration and keep generation stable so the next handoff can compile one identical contract in both runtimes. Preserve typed independent retrieval outcomes and public warning/basis fields as future compatibility capacity for D-11 through D-16 and D-24; do not execute those deferred branches. Pattern alignment: preserve the additive same-file contract evolution in proto/lancet/v1/lancet.proto and the generated-output ownership in engine/src/pb/lancet/v1/lancet.v1.rs and gateway/proto/lancet/v1/lancet.pb.go, as mapped in 03-PATTERNS.md.</action>
  <verify>
    <automated>buf lint; $bufExit = $LASTEXITCODE; if ($bufExit -ne 0) { exit $bufExit }; $generated = @('engine/src/pb/lancet/v1/lancet.v1.rs','engine/src/pb/lancet/v1/lancet.v1.tonic.rs','gateway/proto/lancet/v1/lancet.pb.go','gateway/proto/lancet/v1/lancet_grpc.pb.go'); buf generate; $bufExit = $LASTEXITCODE; if ($bufExit -ne 0) { exit $bufExit }; $first = $generated | ForEach-Object { (Get-FileHash -Algorithm SHA256 -LiteralPath $_).Hash }; buf generate; $bufExit = $LASTEXITCODE; if ($bufExit -ne 0) { exit $bufExit }; $second = $generated | ForEach-Object { (Get-FileHash -Algorithm SHA256 -LiteralPath $_).Hash }; if ((@($first) -join ',') -ne (@($second) -join ',')) { throw 'Buf generation is not stable across consecutive runs' }; git diff --check -- $generated; $diffExit = $LASTEXITCODE; if ($diffExit -ne 0) { exit $diffExit }</automated>
  </verify>
  <acceptance_criteria>
    - Existing QueryRAG wire fields remain unchanged and all generated Rust/Go bindings are regenerated from the one additive proto source; compilation is verified by the next Rust-service task.
    - The generated request/response and tonic service types expose the locked filters, session, evidence, answer-basis, warning, and snapshot fields for the Rust-service handoff.
    - Decision traceability is explicit for current D-07, D-17 through D-25, D-28, and D-31 fields plus future D-11 through D-16/D-24 compatibility capacity; deferred failure behavior remains unexecuted.
  </acceptance_criteria>
  <done>The additive proto source produces stable Rust and Go QueryRAG bindings that carry the complete valid-path contract into the next Rust-service handoff.</done>
</task>

<task type="auto">
  <name>Task 2: Trace the generated contract through Rust retrieval, evidence, generation, and configured startup</name>
  <files>engine/src/main.rs, engine/src/tests.rs, config/config.toml, config/config.example.toml</files>
  <read_first>
    - AGENTS.md
    - rust-guidelines.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-PATTERNS.md
    - engine/src/main.rs
    - engine/src/tests.rs
    - engine/src/retrieval/mod.rs
    - engine/src/retrieval/bm25.rs
    - engine/tests/config_startup.rs
    - config/config.toml
    - config/config.example.toml
  </read_first>
  <action>Extend LancetServiceImpl in engine/src/main.rs with the combined query-ready state from the completed nodes table, initial Bm25Index, typed Retriever settings, NoOpReranker, and provider-neutral Generator. Implement one valid QueryRAG handler that validates and normalizes the request, preserves the original question for generation, applies identical typed filters to dense and BM25, assembles bounded evidence, performs one strict generation call, and maps typed caller/provider errors to tonic statuses per D-18, D-19, D-20, D-21, D-22, D-23, D-25, D-28, and D-31. Build the initial BM25 snapshot from the canonical completed LanceDB corpus before registering LancetServiceServer or logging readiness as the MVP safeguard for D-40; this is not the restart-specific D-42/D-43 target, and dynamic replacement/restart behavior remains in DEBT-RAG-04 per D-41 through D-43. Populate the committed TOML base and example overlays with the non-secret embedding/model-metadata/chat endpoints, generation bounds, query/filter/candidate/final/evidence limits, BM25 k1/b and field boosts, and vector/BM25 RRF weights/k consumed by this handler. Because these new settings are additive to the existing deserializer, give every newly introduced field or nested settings section an explicit safe serde default matching the committed values—candidate 32, final evidence 8, evidence budget 8192 tokens, excerpt 512 characters, vector/BM25 weights 1.0, RRF k 60, BM25 k1 1.2/b 0.75, content/title/section boosts 1.0/2.0/1.5, query 8 KiB, filter limits 100/16, generation timeout 30 seconds, temperature 0, top-p 1, and output 2,048 tokens—while keeping existing `grpc_addr` and `lancedb_path` required. Verify that all three existing `config_startup.rs` inline fixtures (config-directory base, repository fallback with environment overrides, and staging base-plus-overlay precedence) still deserialize and start without adding credentials or silently weakening the existing required engine settings. Preserve the shared LANCET_ENV plus double-underscore environment override convention and never add credentials; do not execute deferred failure branches. Name the service-level fake-provider/fixture assertion `query_rag_happy_path_service` in engine/src/tests.rs. Pattern alignment: extend the existing LancetServiceImpl/query_rag/startup seams in engine/src/main.rs and put service-level fake-provider/fixture assertions in the listed engine/src/tests.rs file, following the same-file fake-provider tests mapped in 03-PATTERNS.md.</action>
  <verify>
    <automated>$testList = cargo test --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; if ($testList -notmatch '(?m)query_rag_happy_path_service') { throw 'Expected Rust QueryRAG service test is not registered' }; $testOutput = cargo test --manifest-path engine/Cargo.toml --locked query_rag_happy_path_service; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }; if ($testOutput -notmatch '(?m)running\s+[1-9]\d*\s+tests?') { throw 'QueryRAG service filter matched zero tests' }; cargo check --manifest-path engine/Cargo.toml --locked; $cargoExit = $LASTEXITCODE; if ($cargoExit -ne 0) { exit $cargoExit }; cargo fmt --manifest-path engine/Cargo.toml -- --check; $fmtExit = $LASTEXITCODE; if ($fmtExit -ne 0) { exit $fmtExit }; $startupList = cargo test --manifest-path engine/Cargo.toml --locked --test config_startup -- --list; $startupListExit = $LASTEXITCODE; if ($startupListExit -ne 0) { exit $startupListExit }; foreach ($name in @('engine_starts_from_config_less_cwd_with_lancet_config_dir','engine_starts_with_repository_fallback_when_variable_absent','engine_honors_env_overlay_and_environment_override_precedence')) { if ($startupList -notmatch ('(?m)' + [regex]::Escape($name))) { throw "Existing config_startup fixture is not registered: $name" } }; $startupOutput = cargo test --manifest-path engine/Cargo.toml --locked --test config_startup -- --test-threads=1; $startupExit = $LASTEXITCODE; if ($startupExit -ne 0) { exit $startupExit }; if ($startupOutput -notmatch '(?m)running\s+[1-9]\d*\s+tests?') { throw 'Existing config_startup fixtures matched zero tests' }; git diff --check -- engine/src/main.rs config/config.toml config/config.example.toml; $diffExit = $LASTEXITCODE; if ($diffExit -ne 0) { exit $diffExit }</automated>
  </verify>
  <acceptance_criteria>
    - A valid generated QueryRAG request reaches Rust-owned dense/BM25 retrieval, deterministic fusion, NoOpReranker, bounded evidence, and one Generator call, returning effective session, citations, answer basis, notices/warnings capacity, and snapshot metadata.
    - Initial BM25 construction is performed before service registration/readiness, and the committed settings expose all locked non-secret retrieval/provider/generation bounds through TOML and environment overlays.
    - New settings default safely when absent from the three pre-existing config_startup base/overlay fixtures, while the committed base and example files expose the same explicit values and existing required engine settings remain required.
    - Decision traceability is explicit for current D-18 through D-23, D-25, D-28, D-31, and D-40; the initial build gate is documented as an MVP safeguard, while future D-41 through D-43 lifecycle behavior remains outside this task.
  </acceptance_criteria>
  <done>The generated contract reaches a configured Rust QueryRAG handler with a valid combined retrieval/generation path and readiness cannot precede the initial BM25 build.</done>
</task>

<task type="auto">
  <name>Task 3: Prove initial BM25 readiness ordering and startup failure isolation</name>
  <files>engine/tests/config_startup.rs</files>
  <read_first>
    - AGENTS.md
    - rust-guidelines.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-PATTERNS.md
    - engine/src/main.rs
    - engine/src/retrieval/mod.rs
    - engine/src/retrieval/bm25.rs
    - engine/src/retrieval/tests.rs
    - engine/tests/config_startup.rs
  </read_first>
  <action>Extend engine/tests/config_startup.rs with isolated temporary LanceDB setup that seeds a valid completed corpus and a malformed-but-schema-valid lexical fixture containing an otherwise valid unique row whose required `content` is empty or whitespace-only. Reuse the explicit BM25 build-validation contract introduced and unit-tested by 03-01 Task 1; do not use duplicate identities as the failure mechanism. Name the two focused tests `initial_bm25_ready_before_serving` and `initial_bm25_failure_blocks_readiness`. Assert the serving signal is emitted only after the valid build succeeds and is absent with the deterministic field/row BM25 validation error for the malformed fixture. Drop database handles before removing temporary paths, keep every fixture process/path isolated, and limit the test to the initial MVP safeguard for D-40; do not claim the future restart/re-ingestion targets D-41 through D-43. Pattern alignment: follow the existing process/readiness shape in engine/tests/config_startup.rs, the explicit empty-required-content contract in engine/src/retrieval/tests.rs, and the isolated LanceDB fixture conventions in engine/src/inspect_lancedb_tests.rs and engine/src/db/tests.rs, as mapped in 03-PATTERNS.md.</action>
  <verify>
    <automated>$testList = cargo test --manifest-path engine/Cargo.toml --locked --test config_startup -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; if ($testList -notmatch '(?m)initial_bm25_ready_before_serving') { throw 'Expected readiness-order test is not registered' }; if ($testList -notmatch '(?m)initial_bm25_failure_blocks_readiness') { throw 'Expected BM25 failure test is not registered' }; $testOutput = cargo test --manifest-path engine/Cargo.toml --locked --test config_startup -- --test-threads=1; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }; if ($testOutput -notmatch '(?m)running\s+[1-9]\d*\s+tests?') { throw 'config_startup test target matched zero tests' }; cargo fmt --manifest-path engine/Cargo.toml -- --check; $fmtExit = $LASTEXITCODE; if ($fmtExit -ne 0) { exit $fmtExit }; git diff --check -- engine/tests/config_startup.rs; $diffExit = $LASTEXITCODE; if ($diffExit -ne 0) { exit $diffExit }</automated>
  </verify>
  <acceptance_criteria>
    - A valid isolated completed corpus reaches the existing readiness signal only after initial BM25 construction; a schema-valid row with empty or whitespace-only required content prevents readiness and reports the named deterministic build-validation error.
    - Dynamic replacement/restart switching remains DEBT-RAG-04 and no deferred RAG-03 failure path is claimed.
  </acceptance_criteria>
  <done>The startup test proves initial query readiness is impossible without a successful completed-corpus BM25 build and keeps temporary fixture cleanup isolated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Protobuf caller to Rust service | Untrusted query/session/filter fields cross the generated gRPC boundary. |
| LanceDB completed corpus to query-ready service | Canonical rows become the combined dense/BM25 state used by every query. |
| Configuration/environment to provider | Endpoint/model/budget settings select provider behavior; secrets remain external. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-03-01 | Tampering | Additive protobuf contract | medium | mitigate | Preserve existing field numbers, regenerate all outputs with Buf, and compile both language bindings. |
| T-03-03-02 | Tampering | Rust QueryRAG input and typed predicates | high | mitigate | Validate query/session/filter bounds before retrieval and construct typed filters rather than interpolated predicates. |
| T-03-03-03 | Tampering | Cross-index readiness | high | mitigate | Build the initial BM25 snapshot from the canonical completed corpus before service registration/readiness and fail clearly on build errors. |
| T-03-03-04 | Information Disclosure | Provider configuration | high | mitigate | Keep credentials in environment variables, commit only non-secret endpoint/model defaults, and redact provider diagnostics. |
| T-03-03-05 | Denial of Service | Query and generation budgets | medium | mitigate | Enforce the configured UTF-8, filter, candidate, evidence, output, and timeout bounds before provider work. |
</threat_model>

<verification>
- Execute the task-level PowerShell wrappers; the Buf wrapper captures $LASTEXITCODE immediately after each native Buf invocation, and each focused Rust wrapper preflights names before testing.
- Confirm the serving log is downstream of initial BM25 build and that malformed lexical state fails startup.
- Confirm D-41 through D-43 replacement/restart behavior remains deferred in the ledger; only the initial MVP build/readiness safeguard is exercised.
</verification>

<success_criteria>
Rust exposes an additive, structured QueryRAG gRPC path and cannot report readiness until the initial completed-corpus dense/BM25 state is built successfully from explicit non-secret settings.
</success_criteria>

<output>
Create `.planning/phases/03-hybrid-retrieval-basic-rag-path/03-03-SUMMARY.md` when done.
</output>
