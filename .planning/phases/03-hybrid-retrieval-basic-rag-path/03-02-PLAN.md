---
phase: 03
plan: "02"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - engine/Cargo.toml
  - engine/Cargo.lock
  - engine/src/retrieval/mod.rs
  - engine/src/retrieval/bm25.rs
  - engine/src/retrieval/dense.rs
  - engine/src/retrieval/fusion.rs
  - engine/src/retrieval/tests.rs
  - engine/src/rerank/mod.rs
  - engine/src/rerank/tests.rs
  - engine/src/main.rs
  - engine/tests/config_startup.rs
autonomous: true
requirements: [RAG-02, RAG-04]
must_haves:
  truths:
    - "Unicode-aware BM25 indexing and querying use the same NFKC, full case-folding, word-boundary, and technical-identifier analysis while preserving original source text."
    - "Dense and BM25 candidates use one validated filter representation before candidate limits, with OR within each filter field, AND across fields, and global BM25 document frequency."
    - "Weighted RRF uses 1-based full-precision ranks, deduplicates by chunk ID while retaining both source ranks, and resolves equal scores deterministically."
    - "NoOpReranker preserves every fused candidate field and ordering through an object-safe async port."
    - "The engine cannot report query readiness before the initial completed-corpus BM25 snapshot exists; dynamic re-ingestion/restart switching remains DEBT-RAG-04."
  artifacts:
    - path: "engine/src/retrieval/bm25.rs"
      provides: "Approved Unicode analyzer, global-IDF BM25 snapshot, field boosts, and bounded filtered query"
      contains: "Bm25Index"
    - path: "engine/src/retrieval/fusion.rs"
      provides: "Pure weighted RRF, duplicate retention, and stable tie ordering"
      exports: ["FusedCandidate", "fuse_candidates"]
    - path: "engine/src/retrieval/tests.rs"
      provides: "Isolated retrieval fixtures covering analyzer, filters, scoring, fusion, and repeat-run determinism"
      contains: "unicode"
    - path: "engine/src/rerank/tests.rs"
      provides: "Async NoOpReranker pass-through proof"
      contains: "NoOpReranker"
    - path: "engine/tests/config_startup.rs"
      provides: "Readiness proof for the initial lexical snapshot"
      contains: "Rust RAG Engine serving"
  key_links:
    - from: "engine/src/retrieval/tests.rs"
      to: "engine/src/retrieval/bm25.rs"
      via: "The same analyzer builds and queries an isolated completed-corpus snapshot"
      pattern: "Bm25Index"
    - from: "engine/src/retrieval/tests.rs"
      to: "engine/src/retrieval/fusion.rs"
      via: "Candidate fixtures assert source-rank retention, deduplication, and D-51 ordering"
      pattern: "fuse_candidates"
    - from: "engine/src/main.rs"
      to: "engine/src/retrieval/bm25.rs"
      via: "Initial snapshot construction precedes tonic server registration and readiness"
      pattern: "build"
    - from: "engine/src/rerank/tests.rs"
      to: "engine/src/rerank/mod.rs"
      via: "NoOpReranker is invoked through the async trait and compared field-for-field"
      pattern: "NoOpReranker"
---

<objective>
**As a** chat service API user, **I want to** receive stable hybrid retrieval results from the completed corpus, **so that** the answer path is reproducible and filter-correct.

Purpose: Expand the proven tracer with the full Unicode BM25 contract, deterministic fusion invariants, isolated retrieval fixtures, the NoOpReranker proof, and the initial snapshot readiness gate.
Output: A complete Rust retrieval/reranker test matrix and a fail-fast readiness proof for the current completed corpus.
</objective>

<scope_note>
This plan completes initial lexical analysis and readiness for the valid query path. It does not implement replacement switching, dynamic restart/re-ingestion recovery, graph extraction/failure handling, degraded retrieval, citation repair/downgrade, provider retries, or alternate fallback; those remain in DEBT-RAG-01, DEBT-RAG-02, DEBT-RAG-03, DEBT-RAG-04, and DEBT-RAG-06.
</scope_note>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-RESEARCH.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-PATTERNS.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/deferred-items.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-01-SUMMARY.md
@engine/src/retrieval/mod.rs
@engine/src/retrieval/dense.rs
@engine/src/retrieval/bm25.rs
@engine/src/retrieval/fusion.rs
@engine/src/rerank/mod.rs
@engine/src/main.rs
@engine/src/db/mod.rs
@engine/tests/config_startup.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete the Unicode BM25 analyzer and snapshot contract</name>
  <files>engine/Cargo.toml, engine/Cargo.lock, engine/src/retrieval/mod.rs, engine/src/retrieval/bm25.rs</files>
  <read_first>
    - AGENTS.md
    - rust-guidelines.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-RESEARCH.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-PATTERNS.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-01-SUMMARY.md
    - engine/src/chunker/mod.rs
    - engine/src/retrieval/mod.rs
    - engine/src/retrieval/bm25.rs
    - engine/Cargo.toml
  </read_first>
  <action>Follow engine/src/chunker/mod.rs for deterministic source-preserving transforms and keep the public analyzer/index APIs small and testable. Add only the three packages approved by the Package Legitimacy Audit: unicode-normalization, unicode-casefold, and unicode-segmentation; preserve the repository's pinned Cargo style, update Cargo.lock through Cargo, and do not introduce a BM25/RAG framework. Complete D-44, D-45, and D-48 with NFKC normalization, full Unicode case folding, Unicode word boundaries, no stemming or stop-word removal, and whole plus camel-case/underscore/hyphen technical-identifier tokens while retaining the original content/title/section strings for evidence. Complete D-46 through D-50 with content/title/section boosts 1.0/2.0/1.5, any-term matching, cumulative multi-term relevance, k1 1.2, b 0.75, global completed-corpus document frequency, and filters that constrain candidates without redefining IDF. Keep the initial snapshot build/readiness contract from Plan 03-01 intact and make the analyzer/build/query views deterministic and reusable by both startup and request-time retrieval.</action>
  <verify>
    <automated>cargo check --manifest-path engine/Cargo.toml --locked</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked bm25</automated>
    <automated>cargo fmt --manifest-path engine/Cargo.toml -- --check</automated>
  </verify>
  <acceptance_criteria>
    - Cargo contains only the three audited Unicode analysis dependencies at locked versions and the lockfile is generated by Cargo.
    - BM25 build and query use the same NFKC/case-folding/segmentation and identifier-token analysis, preserve original source metadata, and use global IDF with the locked field boosts and scoring defaults.
    - The initial completed-corpus snapshot remains the lexical representation paired with dense retrieval before readiness; no dynamic replacement or restart recovery is added.
  </acceptance_criteria>
  <done>The BM25 implementation honors the full locked Unicode, identifier, scoring, and global-corpus contract over a query-ready completed snapshot.</done>
</task>

<task type="auto">
  <name>Task 2: Prove dense-filter parity, fusion determinism, and duplicate invariants</name>
  <files>engine/src/retrieval/dense.rs, engine/src/retrieval/fusion.rs, engine/src/retrieval/tests.rs</files>
  <read_first>
    - AGENTS.md
    - rust-guidelines.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-RESEARCH.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-PATTERNS.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-01-SUMMARY.md
    - engine/src/bin/inspect_lancedb.rs
    - engine/src/inspect_lancedb_tests.rs
    - engine/src/db/tests.rs
    - engine/src/retrieval/dense.rs
    - engine/src/retrieval/fusion.rs
  </read_first>
  <action>Follow engine/src/bin/inspect_lancedb.rs for schema-driven Arrow extraction and engine/src/inspect_lancedb_tests.rs plus engine/src/db/tests.rs for unique temporary LanceDB fixtures, null handling, and handle cleanup. Make dense and BM25 consume one validated filter representation before their candidate limits: absent filters search the global completed corpus, IDs OR within their field, content types OR within their field, and the two fields AND together; valid no-match filters return empty evidence and malformed values remain InvalidArgument boundary cases. Implement and test D-01, D-02, D-04, D-51, and D-52 as pure full-precision weighted RRF with 1-based ranks, chunk_id deduplication retaining both source ranks, and explicit best-rank/document/chunk-index/chunk-ID tie ordering independent of map iteration. Repeat identical normalized query/filter/snapshot/configuration runs and compare ordered IDs plus serialized diagnostics per D-53. Keep every mutable LanceDB fixture on a unique temporary path, use the same isolated path for all fixture connections, and drop all handles before cleanup.</action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked retrieval</automated>
    <automated>cargo fmt --manifest-path engine/Cargo.toml -- --check</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove analyzer parity, global IDF, field boosts, typed filter semantics, valid empty results, dense/BM25 pre-limit parity, weighted RRF, duplicate source-rank retention, locked tie-breaking, and repeat-run determinism.
    - Dense Arrow extraction is schema-checked and preserves nullable source metadata rather than fabricating values.
    - No result ordering depends on rounded scores or hash-map iteration, and all mutable fixtures are isolated and cleaned only after handles are dropped.
  </acceptance_criteria>
  <done>Hybrid retrieval semantics are reproducible, filter-correct, schema-safe, and proven against isolated completed-corpus fixtures.</done>
</task>

<task type="auto">
  <name>Task 3: Prove NoOpReranker and initial query-readiness ordering</name>
  <files>engine/src/rerank/mod.rs, engine/src/rerank/tests.rs, engine/src/main.rs, engine/tests/config_startup.rs</files>
  <read_first>
    - AGENTS.md
    - rust-guidelines.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-RESEARCH.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-PATTERNS.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-01-SUMMARY.md
    - engine/src/main.rs
    - engine/src/rerank/mod.rs
    - engine/src/db/mod.rs
    - engine/tests/config_startup.rs
  </read_first>
  <action>Follow the boxed async provider seam in engine/src/main.rs and the async EntityResolver convention in engine/src/db/mod.rs for an object-safe Send + Sync Reranker port without adding an async-trait dependency. Assert through the actual async port that NoOpReranker preserves order, fused score precision, source ranks, chunk/document identity, and provenance metadata per D-05 and RAG-04. Extend the existing isolated process readiness pattern in engine/tests/config_startup.rs with a temporary completed corpus and a failure case for the initial lexical build: the BM25 snapshot must exist before the existing Rust RAG Engine serving signal, and a current-snapshot build error must prevent serving. This task verifies the initial D-40/D-42/D-43 path introduced in Plan 03-01; keep replacement switching, dynamic restart recovery, and mixed-generation atomic visibility in DEBT-RAG-04.</action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked rerank</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked --test config_startup</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked</automated>
  </verify>
  <acceptance_criteria>
    - NoOpReranker is invoked through the async trait and returns byte-for-byte equivalent candidate fields and ordering.
    - A unique temporary completed corpus proves lexical initialization precedes the serving signal.
    - A failed initial BM25 build stops startup with a clear error; no test or implementation claims dynamic re-ingestion/restart atomic switching.
  </acceptance_criteria>
  <done>The async reranker port is proven and the engine cannot report query readiness before its initial dense/BM25 representations are both available.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Request filters to dense/BM25 | Validated caller values become typed candidate constraints. |
| Canonical LanceDB rows to BM25 memory | Completed source content is transformed into a derived searchable snapshot. |
| Startup lifecycle to query readiness | The process must not expose a query service with only one representation initialized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-02-01 | Tampering | Dense/BM25 filter parity | high | mitigate | Build one typed filter object, apply it before both candidate limits, and test OR-within/AND-across semantics. |
| T-03-02-02 | Tampering | RRF ordering and deduplication | high | mitigate | Use full-precision scores, 1-based ranks, explicit stable tie fields, and chunk-ID keyed accumulation. |
| T-03-02-03 | Information Disclosure | BM25 evidence metadata | medium | mitigate | Preserve provenance from canonical rows and expose only bounded excerpts through the prompt contract. |
| T-03-02-04 | Denial of Service | BM25 build and search | medium | mitigate | Bound candidate/final limits, use the configured analyzer, and fail startup instead of serving an incomplete index. |
| T-03-02-SC | Tampering | Cargo dependency installation | high | mitigate | Install only the three packages listed as Approved in the Phase 03 Package Legitimacy Audit and verify the lockfile with Cargo. |
</threat_model>

<verification>
- Run the focused BM25, retrieval, rerank, and startup tests, then the complete locked Rust suite.
- Confirm all retrieval fixtures use unique temporary LanceDB paths and drop handles before cleanup.
- Confirm initial BM25 readiness is covered while dynamic replacement/restart behavior remains in DEBT-RAG-04.
</verification>

<success_criteria>
The Rust retrieval data plane is Unicode-aware, deterministic, filter-correct, deduplicated, NoOp-reranked through an async port, and query-ready only after the initial completed-corpus BM25 snapshot is built.
</success_criteria>

<output>
Create `.planning/phases/03-hybrid-retrieval-basic-rag-path/03-02-SUMMARY.md` when done.
</output>
