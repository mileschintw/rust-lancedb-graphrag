---
phase: 03
plan: "04"
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - engine/src/client/mod.rs
  - engine/src/client/tests.rs
  - engine/src/main.rs
  - gateway/main.go
  - gateway/main_test.go
autonomous: true
requirements: [RAG-02]
must_haves:
  truths:
    - "The Go gateway accepts a bounded strict POST /rag/query envelope, forwards context and typed fields through the generated client, and maps caller validation failures to HTTP 400."
    - "The gateway preserves the effective session, structured answer basis, notices/warnings, citations, and retrieval snapshot without reimplementing retrieval or generation semantics."
    - "The existing Rust embedding client can target a configured localhost endpoint for deterministic integration tests while retaining the production OpenRouter default and existing retry/timeout behavior."
  artifacts:
    - path: "gateway/main.go"
      provides: "Thin /rag/query route, generated client forwarding, and stable status/JSON mapping"
      contains: "Post(\"/rag/query\""
    - path: "gateway/main_test.go"
      provides: "HTTP contract tests with a stateful engine fake and context/filter assertions"
      contains: "QueryRAG"
    - path: "engine/src/client/mod.rs"
      provides: "Production-default plus explicit endpoint-injectable embedding client constructor"
      contains: "embedding_endpoint"
    - path: "engine/src/client/tests.rs"
      provides: "Local endpoint override proof without credential disclosure"
      contains: "for_test"
  key_links:
    - from: "gateway/main.go"
      to: "gateway/proto/lancet/v1/lancet_grpc.pb.go"
      via: "grpcEngine forwards QueryRAG with r.Context and generated typed request"
      pattern: "QueryRAG"
    - from: "gateway/main.go"
      to: "engine/src/main.rs"
      via: "The public route remains a JSON decode/forward/response-map boundary"
      pattern: "r.Context"
    - from: "engine/src/main.rs"
      to: "engine/src/client/mod.rs"
      via: "OpenRouterClient receives the configured embedding endpoint for query embeddings"
      pattern: "from_env_with_endpoint"

---

<objective>
Expose the Rust gRPC path through a strict Go HTTP route and create the embedding-client endpoint seam required by the local cross-runtime harness.

Purpose: Complete the public boundary while keeping Rust as the sole owner of retrieval, evidence, and provider semantics.
Output: `/rag/query` route and contract tests, context/status preservation, and a production-safe localhost-targetable embedding client.
</objective>

<scope_note>
The route proves only the valid structured path and boundary validation. It preserves typed fields for deferred degraded/model-only behavior but does not add those branches, graph behavior, citation repair/downgrade, retries/fallback, or PostgreSQL dependence to the query path.
</scope_note>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-RESEARCH.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-PATTERNS.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/deferred-items.md
@gateway/main.go
@gateway/main_test.go
@gateway/go.mod
@gateway/proto/lancet/v1/lancet.pb.go
@gateway/proto/lancet/v1/lancet_grpc.pb.go
@engine/src/main.rs
@engine/src/client/mod.rs
@engine/src/client/tests.rs
@config/config.toml

<interfaces>
From gateway/main.go: app.routes owns chi registration, engine is the thin boundary interface, grpcEngine wraps pb.LancetServiceClient, writeJSON is the shared response encoder, and middleware.Timeout already bounds the request context.

From gateway/main_test.go: engineFunc supplies stateful fakes and existing tests invoke the real chi router through httptest; use t.Context() and capture the forwarded request values.

From engine/src/client/mod.rs: OpenRouterClient currently defaults to the OpenRouter embeddings URL, loads OPENROUTER_API_KEY, and retains the ten-second reqwest timeout, concurrency cap, and retry classification. Add an explicit endpoint constructor without weakening those production defaults.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace a strict POST /rag/query request through the generated Go client boundary</name>
  <files>gateway/main.go, gateway/main_test.go</files>
  <read_first>
    - AGENTS.md
    - go-guidelines.md
    - gateway/go.mod
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-PATTERNS.md
    - gateway/main.go
    - gateway/main_test.go
    - gateway/proto/lancet/v1/lancet.pb.go
    - gateway/proto/lancet/v1/lancet_grpc.pb.go
  </read_first>
  <action>Before changing Go, detect the gateway module target as Go 1.25.0 and apply go-guidelines.md through that target. Add QueryRAG to the engine interface and grpcEngine, register POST /rag/query, bound the JSON body, use json.Decoder.DisallowUnknownFields, reject malformed or trailing JSON, normalize the request envelope, forward query/session/document_ids/content_types with r.Context through the generated client, and map gRPC InvalidArgument to HTTP 400 while preserving structured upstream failures. Map every response field—effective session ID, answer, valid answer basis, notices, warnings, structured citation provenance/excerpt/truncation/retrieval metadata, and retrieval snapshot—without retrieval or provider logic in Go per D-18, D-19, D-20, D-21, D-22, D-23, D-25, D-54, D-55, D-56, and D-57. Name the engineFunc-based httptest cases `TestRAGQueryValidMapping`, `TestRAGQueryCallerSessionAndFilters`, `TestRAGQueryRejectsUnknownOrTrailingJSON`, and `TestRAGQueryInvalidArgumentStatus`; cover generated-session behavior, caller-supplied session/filter forwarding, request-context propagation, strict body/trailing-field rejection, and InvalidArgument→400. Keep future D-11 through D-16 and D-24 as typed response capacity only and do not add their deferred runtime branches. Pattern alignment: follow the existing chi route, grpcEngine, and writeJSON seams in gateway/main.go and the engineFunc/httptest assertions in gateway/main_test.go, as mapped in 03-PATTERNS.md.</action>
  <verify>
    <automated>Push-Location gateway; $goAllOutput = go test ./...; $goAllExit = $LASTEXITCODE; if ($goAllExit -ne 0) { Pop-Location; exit $goAllExit }; $goList = go test . -list '^TestRAGQuery'; $goListExit = $LASTEXITCODE; if ($goListExit -ne 0) { Pop-Location; exit $goListExit }; foreach ($name in @('TestRAGQueryValidMapping','TestRAGQueryCallerSessionAndFilters','TestRAGQueryRejectsUnknownOrTrailingJSON','TestRAGQueryInvalidArgumentStatus')) { if ($goList -notmatch ('(?m)^' + $name + '$')) { Pop-Location; throw "Expected Go test is not registered: $name" } }; $goOutput = go test . -run '^TestRAGQuery' -count=1 -v; $goExit = $LASTEXITCODE; if ($goExit -ne 0) { Pop-Location; exit $goExit }; if ($goOutput -notmatch '(?m)^--- PASS: TestRAGQuery') { Pop-Location; throw 'TestRAGQuery filter matched zero tests' }; Pop-Location</automated>
    <automated>git diff --check -- gateway/main.go gateway/main_test.go; $diffExit = $LASTEXITCODE; if ($diffExit -ne 0) { exit $diffExit }</automated>
  </verify>
  <acceptance_criteria>
    - The route accepts only the documented bounded JSON envelope and rejects unknown/trailing content before forwarding.
    - The fake engine receives the exact typed filters, session value, and request context; the response preserves all structured metadata.
    - InvalidArgument is HTTP 400, other upstream failures remain stable gateway errors, and no retrieval/provider semantics appear in Go.
    - Decision traceability is explicit for D-18 through D-23 and D-25 plus the D-54 through D-57 input-boundary rules.
  </acceptance_criteria>
  <done>A real Go HTTP request can traverse the public /rag/query boundary into the generated QueryRAG client with strict, lossless mapping and focused contract proof.</done>
</task>

<task type="auto">
  <name>Task 2: Make query embeddings endpoint-injectable for the local provider harness</name>
  <files>engine/src/client/mod.rs, engine/src/client/tests.rs, engine/src/main.rs</files>
  <read_first>
    - AGENTS.md
    - rust-guidelines.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-RESEARCH.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-PATTERNS.md
    - engine/src/client/mod.rs
    - engine/src/client/tests.rs
    - engine/src/main.rs
    - config/config.toml
    - config/config.example.toml
  </read_first>
  <action>Preserve OpenRouterClient::from_env as the production-default constructor, add a clearly named endpoint-injectable constructor used by engine startup, and have main.rs pass the configured embedding endpoint from the Phase 03 TOML/environment settings. Keep the existing ten-second request timeout, five-request concurrency cap, three-retry policy, 2,048-dimension validation, and credential checks unchanged per the established Phase 02 client contract; the seam only changes the URL so a local mock can receive embeddings. Name the focused local client assertion `client_embedding_endpoint_override` and prove the override targets the supplied endpoint and still parses the deterministic 2,048-wide response without exposing credentials. Keep provider retry/fallback decisions outside this phase per D-27, D-29, D-30, and D-31; keep the future D-41 through D-43 restart/re-ingestion lifecycle target in the debt ledger. This seam is for the provider-independent cross-runtime test; it must not introduce a second embedding client or a reduced production path. Pattern alignment: follow the constructor and credential-redaction conventions in engine/src/client/mod.rs and the deterministic local endpoint assertions in engine/src/client/tests.rs, as mapped in 03-PATTERNS.md.</action>
  <verify>
    <automated>$testList = cargo test --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; if ($testList -notmatch '(?m)client_embedding_endpoint_override') { throw 'Expected embedding endpoint test is not registered' }; $testOutput = cargo test --manifest-path engine/Cargo.toml --locked client; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }; if ($testOutput -notmatch '(?m)running\s+[1-9]\d*\s+tests?') { throw 'Client filter matched zero tests' }; cargo check --manifest-path engine/Cargo.toml --locked; $cargoExit = $LASTEXITCODE; if ($cargoExit -ne 0) { exit $cargoExit }; cargo fmt --manifest-path engine/Cargo.toml -- --check; $fmtExit = $LASTEXITCODE; if ($fmtExit -ne 0) { exit $fmtExit }</automated>
  </verify>
  <acceptance_criteria>
    - Engine startup can target either the configured OpenRouter embeddings endpoint or a supplied localhost endpoint through the same OpenRouterClient implementation.
    - Existing timeout, concurrency, retry, dimension, status, and credential behavior remains covered; no provider fallback or query semantic duplication is added.
    - The endpoint seam is ready for an isolated local mock and does not require PostgreSQL or a live provider.
  </acceptance_criteria>
  <done>The existing embedding client has a production-preserving endpoint seam, and engine startup can use it for deterministic local cross-runtime verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| HTTP caller to Go gateway | Untrusted JSON bytes and filter/session values enter the public route. |
| Go gateway to Rust gRPC | Generated request/response fields and status codes cross the runtime boundary. |
| Rust embedding client to configured endpoint | Query text and a bearer credential cross to OpenRouter or the local test server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-04-01 | Tampering | JSON request envelope | high | mitigate | Bound the body, reject unknown/trailing fields, normalize typed values, and let Rust remain authoritative for semantic validation. |
| T-03-04-02 | Spoofing | Session and response citation identity | medium | mitigate | Forward caller session values unchanged for Rust validation and map only engine-supplied effective IDs/citations. |
| T-03-04-03 | Information Disclosure | Embedding credential and endpoint errors | high | mitigate | Keep keys in environment configuration, preserve redacted client errors, and use localhost only through an explicit test endpoint setting. |
| T-03-04-04 | Denial of Service | HTTP body/query and embedding request | medium | mitigate | Enforce HTTP/body/query bounds and retain the existing client timeout, concurrency, and retry limits. |
| T-03-04-05 | Tampering | Go/Rust contract evolution | medium | mitigate | Use generated bindings from the additive proto and test exact field/status mapping. |
</threat_model>

<verification>
- Execute the task-level PowerShell wrappers; the Go wrapper preflights every named RAG test and rejects a zero-test filtered run, while the Rust wrapper preflights the endpoint test.
- Confirm the route is only a boundary adapter and the local endpoint override is the same client used by production startup.
</verification>

<success_criteria>
The public Go boundary is strict, lossless, context-preserving, and thin, and the Rust query embedding client can be redirected to a deterministic localhost mock without weakening its established provider safeguards.
</success_criteria>

<output>
Create `.planning/phases/03-hybrid-retrieval-basic-rag-path/03-04-SUMMARY.md` when done.
</output>
