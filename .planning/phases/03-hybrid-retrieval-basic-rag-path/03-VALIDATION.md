---
phase: 03
slug: hybrid-retrieval-basic-rag-path
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-31
---

# Phase 03 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in tests with `#[tokio::test]`, Go standard `testing`/`httptest`, Buf CLI checks |
| **Config file** | Rust: `engine/Cargo.toml`; Go: `gateway/go.mod`; API matrix: `COVERAGE.md` |
| **Quick run command** | `cargo test --manifest-path engine/Cargo.toml --locked retrieval` |
| **Full suite command** | `cargo test --manifest-path engine/Cargo.toml --locked`; `Push-Location gateway; go test ./...; $exitCode = $LASTEXITCODE; Pop-Location; if ($exitCode -ne 0) { exit $exitCode }`; `buf lint` |
| **Estimated runtime** | ~60 seconds locally, excluding the explicitly ignored live OpenRouter smoke |

## Sampling Rate

- **After every task commit:** Run the task's focused Rust, Go, Buf, or API-coverage command from the map below.
- **After every plan wave:** Run the full Rust suite, `go test ./...`, and `buf lint`; repeat `buf generate` stability checks after contract changes.
- **Before `/gsd-verify-work`:** Full suite must be green; the live provider smoke is run only when its user setup precondition is available.
- **Max feedback latency:** 60 seconds for local checks; external provider smoke is an explicit ignored check with network-dependent latency.

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 03-01-01 | 01 | 1 | RAG-02 | T-03-01-01 / T-03-01-02 / T-03-01-SC | Approved Unicode crates are locked before the first BM25 compile; the full analyzer preserves source metadata and bounded lexical candidates | Rust build + BM25 contract | `cargo check --manifest-path engine/Cargo.toml --locked`; `cargo test --manifest-path engine/Cargo.toml --locked bm25` | ❌ created by owning task | ⬜ pending |
| 03-01-02 | 01 | 1 | RAG-02/RAG-04 | T-03-01-01 / T-03-01-02 / T-03-01-03 | Dense and BM25 share filters before limits, fuse with stable weighted RRF, and preserve every field through NoOpReranker | Rust unit + retrieval integration | `cargo test --manifest-path engine/Cargo.toml --locked retrieval`; `cargo test --manifest-path engine/Cargo.toml --locked rerank` | ❌ created by owning task | ⬜ pending |
| 03-02-01 | 02 | 2 | RAG-03/RAG-04 | T-03-02-01 / T-03-02-03 / T-03-02-05 | Provider-neutral async generation consumes bounded whole-chunk evidence and returns strict valid output without deferred fallback branches | Rust prompt + generation contract | `cargo test --manifest-path engine/Cargo.toml --locked prompt`; `cargo test --manifest-path engine/Cargo.toml --locked generation` | ❌ created by owning task | ⬜ pending |
| 03-02-02 | 02 | 2 | RAG-03 | T-03-02-02 / T-03-02-03 / T-03-02-04 | Metadata capability preflight, exact sampling/budget, one strict chat call, cancellation, and typed provider errors | Rust local-provider contract | `cargo test --manifest-path engine/Cargo.toml --locked openrouter` | ❌ created by owning task | ⬜ pending |
| 03-03-01 | 03 | 3 | RAG-03/RAG-04 | T-03-03-01 | Additive generated QueryRAG contract carries typed filters, session, evidence, answer-basis, warning, and snapshot fields without renumbering existing fields | Buf contract generation | `buf lint`; stable `buf generate`; `git diff --check` on generated Rust/Go bindings | ❌ generated by owning task | ⬜ pending |
| 03-03-02 | 03 | 3 | RAG-02/RAG-03/RAG-04 | T-03-03-02 / T-03-03-03 / T-03-03-04 / T-03-03-05 | Rust handler applies shared filters, NoOpReranker, bounded evidence, one Generator call, configured limits, and pre-readiness BM25 construction | Rust service/config integration | `cargo check --manifest-path engine/Cargo.toml --locked`; `cargo fmt --manifest-path engine/Cargo.toml -- --check` | ❌ created by owning task | ⬜ pending |
| 03-03-03 | 03 | 3 | RAG-02 | T-03-03-03 / T-03-03-05 | Isolated valid and malformed completed-corpus fixtures prove readiness follows the initial BM25 build and never serves vector-only after a lexical build error | Rust process/startup integration | `cargo test --manifest-path engine/Cargo.toml --locked --test config_startup` | ❌ created by owning task | ⬜ pending |
| 03-04-01 | 04 | 4 | RAG-03/RAG-04 | T-03-04-01 / T-03-04-02 / T-03-04-05 | Strict unary `/rag/query` mapping forwards context, session, and filters and preserves structured Rust response fields | Go HTTP/gRPC contract | `Push-Location gateway; go test . -run '^TestRAGQuery' -count=1; $exitCode = $LASTEXITCODE; Pop-Location; if ($exitCode -ne 0) { exit $exitCode }` | ❌ created by owning task | ⬜ pending |
| 03-04-02 | 04 | 4 | RAG-02/RAG-03 | T-03-04-03 / T-03-04-04 | The production embedding client targets an explicit endpoint seam without weakening timeout, retries, concurrency, dimension checks, or redaction | Rust client unit + startup wiring | `cargo test --manifest-path engine/Cargo.toml --locked client`; `cargo check --manifest-path engine/Cargo.toml --locked` | ❌ created by owning task | ⬜ pending |
| 03-05-01 | 05 | 5 | RAG-02/RAG-03/RAG-04 | T-03-05-01 / T-03-05-02 / T-03-05-03 / T-03-05-04 / T-03-05-SC | Real Go-to-Rust process path uses isolated seeded LanceDB and deterministic embedding, supported-parameters metadata, and chat mocks with cleanup | Cross-runtime integration | `cargo check --manifest-path engine/Cargo.toml --locked --bin seed_rag_fixture`; `Push-Location gateway; go test . -run '^TestRAGQueryCrossRuntime$' -count=1; $exitCode = $LASTEXITCODE; Pop-Location; if ($exitCode -ne 0) { exit $exitCode }` | ❌ created by owning task | ⬜ pending |
| 03-05-02 | 05 | 5 | RAG-03 | T-03-05-01 / T-03-05-SC | Coverage matrix names the integrated unary/provider-independent surface and keeps manual live smoke plus DEBT-RAG-05 exhaustive negative-input coverage explicitly deferred | Documentation/API gate | `node D:\Repos\lancet\.codex\gsd-core\bin\gsd-tools.cjs check api-coverage.verify-pre D:\Repos\lancet\.planning\phases\03-hybrid-retrieval-basic-rag-path`; `git diff --check -- .planning/phases/03-hybrid-retrieval-basic-rag-path/COVERAGE.md` | ✅ existing artifact | ⬜ pending |

The ignored live smoke is mapped to 03-02-02 and runs only with the declared `OPENROUTER_API_KEY` setup: `cargo test --manifest-path engine/Cargo.toml --locked openrouter_structured_output_smoke -- --ignored`. It is separate from the automated local cross-runtime smoke in 03-05-01.

## Wave 0 Requirements

- [x] Existing Rust, Go, `httptest`, Buf, and Cargo infrastructure covers the phase; no new test framework installation is needed.
- [x] The three Unicode packages required by the BM25 contract have an approved Package Legitimacy Audit in `03-RESEARCH.md`.
- [ ] Test scaffolds listed in the verification map are created at the start of their owning task before production assertions are finalized; a separate standalone Wave 0 plan is not required because each of the five plans owns a vertical handoff.
- [ ] The cross-runtime harness uses only a test-owned temporary LanceDB path and deterministic localhost endpoints; no PostgreSQL or live provider setup is required for automated verification.

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Optional real OpenRouter structured-output response | RAG-03 | Requires a user-provided provider credential and network access; the deterministic local mock is the automated gate. | Set `OPENROUTER_API_KEY`, then run `cargo test --manifest-path engine/Cargo.toml --locked openrouter_structured_output_smoke -- --ignored`; confirm supported-parameters metadata, one strict response, and redacted diagnostics. Skipping it does not block Phase 03. |

## Validation Sign-Off

- [x] All revised tasks have an `<automated>` verify command.
- [x] Sampling continuity: no task lacks an automated command.
- [ ] Wave 0 test scaffolds are created before their task assertions run.
- [x] No watch-mode flags are used.
- [x] Local feedback commands target the 60-second budget; the ignored provider smoke is separately identified and non-blocking.
- [ ] `nyquist_compliant: true` set in frontmatter after execution and validation.

**Approval:** pending
