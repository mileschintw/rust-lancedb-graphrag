---
phase: 03-hybrid-retrieval-basic-rag-path
plan: "15"
type: execute
wave: 13
depends_on:
  - 03-14
files_modified:
  - engine/src/tests.rs
autonomous: true
requirements:
  - RAG-02
gap_closure: true
assumption_delta: false
must_haves:
  truths:
    - "The citation identity and notices QueryRAG test has stable fixture ordering and passes alone, in the parallel locked suite, and in the serial locked suite."
    - "Successful QueryRAG test doubles satisfy the fail-closed citation contract instead of relying on citationless retrieval output."
    - "The complete locked Rust regression gate is green in both required execution modes."
  artifacts:
    - "engine/src/tests.rs contains deterministic citation identity/notices fixtures and valid successful-generation doubles."
    - "03-15 execution evidence records focused, parallel, and serial locked Rust test commands and results."
  key_links:
    - "Deterministic fixture identifiers/content -> hybrid ranking -> citation identity assertions"
    - "Successful fake ModelOutput markers -> validate_grounding -> QueryRAG response assertions"
    - "Focused test isolation -> full parallel/serial cargo gates"
---

<objective>
Close the remaining full-gate blocker by making the QueryRAG citation identity fixture deterministic and aligning successful test doubles with the fail-closed grounding contract, then prove the complete locked Rust suite in both required modes.

Purpose: The current named test passes alone but fails under the full suite because its random fixture can change rank-two truncation behavior. The gate must measure the real Phase 03 contract rather than test-order or random-data luck.

Output: A deterministic citation/notices regression fixture and recorded focused, parallel, and serial locked Rust gate commands.
</objective>

<scope>
This plan closes only the current P09/P24 regression-gate blocker. It does not change production retrieval, provider, citation repair, degradation, model-only fallback, lifecycle recovery, graph fallback, or the deferred DEBT-RAG-01/03/04/05/06 behavior. It does not edit verification/state artifacts; the verifier will consume the execution evidence afterward.
</scope>

<assumptions>
- 03-13 has enforced nonempty retrieval citations and valid inline markers for successful Retrieval/Mixed outputs.
- 03-14 has completed shared production test-file changes before this plan runs.
- The locked Rust gate is the authoritative regression command for this blocker; formatting warnings are outside the five current gaps.
</assumptions>

## Artifacts this phase produces

- Stable fixture IDs, ordering inputs, and assertions for query_rag_citation_identity_and_notices.
- Updated successful QueryRAG fake outputs where the stricter grounding validator requires an inline marker.
- Evidence from the focused test and both full locked Rust suite modes.

## Source coverage audit

| Source | Current item | Plan coverage |
|---|---|---|
| GOAL | A completed-corpus hybrid RAG query returns stable, evidence-backed citation identity. | Tasks 1-2 make the assertion deterministic and gate it end to end. |
| REQ | RAG-02 retrieval and evidence remain the source of the cited result. | Tasks 1-2 test the existing path without changing retrieval semantics. |
| REQ | RAG-04 Reranker/NoOp behavior is baseline work from 03-01 through 03-12. | Preserved; no Reranker implementation is added. |
| RESEARCH | Deterministic ordering and stable ordered IDs are required for reproducible RAG behavior. | Task 1 fixes fixture inputs and explicit assertions. |
| CONTEXT | D-21 structured citations, D-22 inline markers, D-23 bounded excerpts, D-51 deterministic tie-breaking, D-53 stable ordered IDs. | Tasks 1-2 exercise these locked behaviors. |
| DEFERRED | Repair/downgrade, degradation, lifecycle, graph fallback, and exhaustive negative matrices. | Explicitly excluded. |

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-VERIFICATION.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-14-PLAN.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-PATTERNS.md
@engine/src/tests.rs
@engine/src/generation/mod.rs
@engine/src/retrieval/mod.rs
@engine/src/retrieval/fusion.rs
@engine/src/retrieval/bm25.rs
@engine/src/retrieval/dense.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Make citation identity and notices assertions deterministic</name>
  <read_first>
    AGENTS.md
    rust-guidelines.md
    .planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
    .planning/phases/03-hybrid-retrieval-basic-rag-path/03-VERIFICATION.md
    .planning/phases/03-hybrid-retrieval-basic-rag-path/03-PATTERNS.md
    .planning/phases/03-hybrid-retrieval-basic-rag-path/03-14-PLAN.md
    engine/src/tests.rs
    engine/src/generation/mod.rs
    engine/src/prompt.rs
    engine/src/retrieval/mod.rs
    engine/src/retrieval/fusion.rs
    engine/src/retrieval/bm25.rs
    engine/src/retrieval/dense.rs
    engine/src/retrieval/tests.rs
    engine/src/inspect_lancedb_tests.rs
    engine/src/db/tests.rs
  </read_first>
  <files>engine/src/tests.rs</files>
  <action>Following the isolated LanceDB fixture and deterministic Arrow/test patterns mapped in 03-PATTERNS.md, replace random document identifiers and rank-sensitive fixture inputs in `query_rag_citation_identity_and_notices` with fixed valid UUIDs, stable document metadata, and equal-score content that exercises the D-21/D-22/D-23 citation contract, D-51 tie-break, and D-53 stable ordered IDs. Keep the provider answer citing the intended marker, then assert the exact cited document identity, title, section path, content type, rank, excerpt bound, and truncation state without a branch based on whichever random document ranks second. Preserve unique temporary database paths and cleanup. Keep the fixture on the real retrieval, fusion, prompt, and grounding path so it exercises bounded evidence excerpts and notices rather than only asserting that a query returns.</action>
  <verify>
    <automated>Push-Location engine; cargo test --locked tests::query_rag_citation_identity_and_notices -- --exact --test-threads=1 2>&1; $testExit = $LASTEXITCODE; Pop-Location; if ($testExit -ne 0) { exit $testExit }</automated>
  </verify>
  <acceptance_criteria>
    - The named citation identity/notices test has no random document IDs or conditional expected metadata that can change with test ordering.
    - The test deterministically identifies the cited rank-two document and verifies its identity, notices, bounded excerpt, and truncation behavior.
    - The focused locked test passes in an isolated single-thread invocation.
  </acceptance_criteria>
  <done>The previously flaky citation fixture is a stable end-to-end regression test for D-21, D-22, D-23, D-51, and D-53.</done>
</task>

<task type="auto">
  <name>Task 2: Align successful QueryRAG doubles and run the complete locked gate</name>
  <read_first>
    AGENTS.md
    rust-guidelines.md
    .planning/phases/03-hybrid-retrieval-basic-rag-path/03-VERIFICATION.md
    .planning/phases/03-hybrid-retrieval-basic-rag-path/03-PATTERNS.md
    .planning/phases/03-hybrid-retrieval-basic-rag-path/03-14-PLAN.md
    engine/src/tests.rs
    engine/src/generation/mod.rs
    engine/src/main.rs
    engine/src/retrieval/mod.rs
    engine/src/retrieval/fusion.rs
    engine/src/retrieval/tests.rs
    engine/src/inspect_lancedb_tests.rs
    engine/src/db/tests.rs
  </read_first>
  <files>engine/src/tests.rs</files>
  <action>Audit every successful QueryRAG fake output reached after retrieval and update citation-bearing answers and cited_evidence_ids together so they satisfy the D-21/D-22/D-23 fail-closed marker contract from 03-13. Use the same-file fake-generator and grounding-validation analogs in 03-PATTERNS.md; in particular, update `service_index_generation_is_opaque_and_stable` to use the evidence ID and inline marker supplied by its request fixture, while retaining citationless fakes only in tests that intentionally fail before generation. Do not weaken `validate_grounding` or add citation repair. After the fixture and doubles are corrected, run the named test, the full locked Rust suite with default parallel execution, and the same suite with `--test-threads=1`. Capture each PowerShell native exit code immediately and fail the task on any nonzero result; do not substitute a smaller test selection for either full gate.</action>
  <verify>
    <automated>Push-Location engine; cargo test --locked tests::query_rag_citation_identity_and_notices -- --exact --test-threads=1 2>&1; $focusedExit = $LASTEXITCODE; if ($focusedExit -ne 0) { Pop-Location; exit $focusedExit }; cargo test --locked 2>&1; $parallelExit = $LASTEXITCODE; if ($parallelExit -ne 0) { Pop-Location; exit $parallelExit }; cargo test --locked -- --test-threads=1 2>&1; $serialExit = $LASTEXITCODE; Pop-Location; if ($serialExit -ne 0) { exit $serialExit }</automated>
  </verify>
  <acceptance_criteria>
    - All successful QueryRAG test doubles contain a nonempty cited ID set and matching inline markers when they reach generation validation.
    - The focused citation identity test, full locked parallel suite, and full locked serial suite all pass.
    - No production behavior or deferred Phase 03 branch is changed to make the tests pass.
  </acceptance_criteria>
  <done>The complete locked Rust regression gate is green in both execution modes, including the formerly order-sensitive citation identity/notices test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test fixture -> retrieval/evidence pipeline | Fixture values determine ranking, evidence identity, and truncation assertions. |
| Fake generator -> QueryRAG validator | Test-controlled provider output crosses the same grounding contract as a real provider response. |
| Cargo test process -> regression gate | Parallel scheduling and shared process state can otherwise mask or expose nondeterminism. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-15-01 | Tampering | Citation fixture identity and rank assertions | high | mitigate | Use fixed IDs and deterministic equal-score inputs, then assert exact cited metadata and truncation state. |
| T-03-15-02 | Tampering | Successful QueryRAG test doubles | high | mitigate | Keep cited IDs and inline markers synchronized under the fail-closed validator and isolate intentional pre-generation failures. |
| T-03-15-03 | Denial of Service | Full regression gate scheduling | medium | mitigate | Require both default parallel and single-thread locked cargo suites with immediate exit-code checks. |
| T-03-15-SC | Tampering | Cargo dependency/install boundary | low | accept | No package or dependency installation is introduced by this gap-closure plan. |
</threat_model>

<verification>
- Run the focused citation identity test with one test thread.
- Run cargo test --locked with default execution and with --test-threads=1 from engine.
- Confirm the three required commands each return exit code zero in PowerShell.
- Leave verification/state transition to the verifier workflow; this plan writes no verification or state artifact.
</verification>

<success_criteria>
- The fifth current blocker is closed by a deterministic fixture rather than by weakening an assertion.
- The complete locked Rust regression gate passes in parallel and serial modes.
- The accepted Phase 03 boundary remains unchanged: successful retrieval-backed output with valid citations, bounded evidence, and no fabricated fallback.
</success_criteria>

<output>
Create the 03-15-SUMMARY.md execution summary when this plan is executed.
</output>
