---
phase: 03-hybrid-retrieval-basic-rag-path
plan: 18
type: execute
wave: 16
depends_on:
  - "03-17"
files_modified:
  - engine/src/main.rs
  - engine/src/tests.rs
  - gateway/main.go
  - gateway/main_test.go
  - .planning/phases/03-hybrid-retrieval-basic-rag-path/deferred-items.md
autonomous: true
gap_closure: true
requirements:
  - RAG-02
  - RAG-04
must_haves:
  truths:
    - "D2: a syntactically valid query whose filters match zero completed rows returns success with an explicit no-results response and never invokes the provider."
    - "D2: the gRPC no-results shape is answer empty, citations empty, session effective, ANSWER_BASIS_UNSPECIFIED, one INFO NO_EVIDENCE notice with the locked message, and a populated retrieval snapshot."
    - "D2: the HTTP no-results shape includes the same fields with answer_basis 0 and empty arrays, and is HTTP 200 rather than HTTP 400."
    - "D2: malformed query/filter input remains InvalidArgument/HTTP 400, and D1 infrastructure failures remain error responses."
    - "D3, D4, and D5 remain explicitly deferred with their residual risks and triggers; strict citation validation, initial BM25 readiness, and chunk-only Phase 03 scope remain intact."
    - "RAG-04 remains covered by the locked Rust and cross-runtime regression gates; this gap closure does not claim RAG-03 delivery."
  artifacts:
    - path: "engine/src/main.rs"
      provides: "Success-path no-results branch before prompt packing and provider invocation"
      contains: "NO_EVIDENCE"
    - path: "engine/src/tests.rs"
      provides: "Rust gRPC no-results and provider-call suppression regression"
      contains: "query_rag_valid_zero_match"
    - path: "gateway/main.go"
      provides: "Explicit QueryRAG JSON response mapping that preserves zero values"
      contains: "structured_citations"
    - path: "gateway/main_test.go"
      provides: "HTTP 200 exact no-results body and malformed-input regression"
      contains: "NoResults"
    - path: ".planning/phases/03-hybrid-retrieval-basic-rag-path/deferred-items.md"
      provides: "ADR-03-001-aligned deferred ledger for DEBT-RAG-01/03/04/05/06"
      contains: "ADR-03-001 deferred-confirmed boundaries"
  key_links:
    - from: "successful empty final_candidates"
      to: "QueryRagResponse"
      via: "early no-results construction before pack_evidence_prompt and Generator::generate"
    - from: "QueryRagResponse no-results fields"
      to: "HTTP JSON body"
      via: "explicit gateway DTO with answer, citations, basis, notices, and snapshot fields"
    - from: "D3/D4/D5 ledger rows"
      to: "phase exit verification"
      via: "deferred boundaries and residual triggers remain auditable without implementation claims"
---

<objective>
Close D2 from ADR-03-001 by making valid zero-match retrieval a success-path no-results response, then align the deferred ledger for D3, D4, D5, and the remaining DEBT-RAG-01/03/04/05/06 boundaries.

Purpose: Clients must distinguish an empty completed-corpus match from malformed input or infrastructure failure, while Phase 03 remains chunk-grounded and does not claim RAG-03.
Output: Exact Rust gRPC and Go HTTP no-results behavior, provider-call suppression tests, explicit JSON zero-value mapping, and a deferred-confirmed ledger section.
</objective>

<execution_context>
@D:/Repos/lancet/.agents/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-VERIFICATION.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-REVIEW.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/deferred-items.md
@.discussion/decisions/phases/03/2026-08-04-ADR-03-001-verification-gap-disposition.md
@engine/src/main.rs
@engine/src/tests.rs
@gateway/main.go
@gateway/main_test.go
@proto/lancet/v1/lancet.proto
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace valid zero-match retrieval to a provider-free gRPC success</name>
  <reversibility rating="costly">The no-results basis and notice values become client-visible API semantics, so changing them later requires coordinated client behavior updates, although this choice uses existing protobuf fields and needs no data migration.</reversibility>
  <read_first>
    - rust-guidelines.md
    - engine/src/main.rs: query_rag final-candidate flow, snapshot construction, status mapping, and QueryRagResponse assembly
    - engine/src/prompt.rs: EmptyEvidence behavior and citation packing boundary
    - engine/src/tests.rs: query fixture helpers, FakeGenerator call counts, and malformed-grounding tests
    - proto/lancet/v1/lancet.proto: QueryRAGResponse, AnswerBasis, Notice, and RetrievalSnapshot fields
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md: D-10, D-54, D-56, and D-57
  </read_first>
  <files>engine/src/main.rs, engine/src/tests.rs</files>
  <action>
    Implement D2 and D-10 by branching immediately after successful reranking and final limiting when final_candidates is empty, before assemble_evidence_blocks, pack_evidence_prompt, or Generator::generate. Return tonic OK with answer empty, empty citations and structured_citations, the normalized effective session ID, AnswerBasis UNSPECIFIED, exactly one Notice with code NO_EVIDENCE, message No completed corpus evidence matched the requested filters., and INFO severity, plus the normal RetrievalSnapshot populated with the active normalized filter and empty-result hash. Use the existing protobuf response fields; do not add or renumber a proto field.

    Keep successful empty dense retrieval distinct from D1 errors, retain the existing InvalidArgument mapping for empty/oversized questions and malformed or unsupported filters, and leave NoEvidenceFits classification unchanged per ADR-03-001. Add a Rust service test with a valid UUID filter that matches no completed row, a FakeGenerator, and assertions that the response is exactly the stated shape and the generator call count is zero.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked query_rag_valid_zero_match -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Valid zero-match retrieval returns tonic OK with the locked no-results fields and a retrieval snapshot.
    - The provider is not invoked and no generated answer, citation, or model-only basis is fabricated.
    - Empty/oversized/malformed caller input remains tonic InvalidArgument.
  </acceptance_criteria>
  <done>D2's provider-free success branch is wired at the Rust query boundary and its exact gRPC behavior passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve the exact no-results shape at the Go HTTP boundary</name>
  <read_first>
    - go-guidelines.md
    - gateway/main.go: queryRAG, writeJSON, generated engine interface, and current response forwarding
    - gateway/main_test.go: TestRAGQueryValidMapping, strict-input tests, engineFunc, and HTTP recorder helpers
    - gateway/proto/lancet/v1/lancet.pb.go: JSON tags and AnswerBasis enum values
    - proto/lancet/v1/lancet.proto: confirm that D2 uses existing fields and requires no regeneration
  </read_first>
  <files>gateway/main.go, gateway/main_test.go</files>
  <behavior>
    - A successful no-results response serializes answer as an empty string, citations and structured_citations as empty arrays, the effective session ID, answer_basis as numeric zero, one NO_EVIDENCE notice with severity one, and a non-null snapshot.
    - The no-results response is HTTP 200 and does not expose an error body or call a provider through the gateway seam.
    - Existing grounded response mapping and InvalidArgument-to-400 behavior remain unchanged.
  </behavior>
  <action>
    Add a QueryRAG-specific JSON DTO in the gateway response path so zero values are explicit instead of being removed by the generated protobuf json tags; leave the generic writeJSON helper unchanged. Map every existing response field in order: answer, citations, session_id, answer_basis, structured_citations, notices, and snapshot. Keep enum serialization numeric to match the current encoding/json contract. Do not introduce a new proto message or enum value: D2 uses AnswerBasis UNSPECIFIED (numeric zero) plus the stable NO_EVIDENCE notice as the machine-readable discriminator.

    Add TestRAGQueryNoResults that returns the Rust-defined shape from the fake engine, asserts HTTP 200 and the exact decoded fields including empty arrays, notice code/message/severity, basis zero, and snapshot presence, and keeps the existing valid grounded mapping and malformed-input tests passing. The HTTP test proves boundary serialization; the Rust test remains the proof that the real query path suppresses provider work.
  </action>
  <verify>
    <automated>cd gateway && go test -count=1 -run 'TestRAGQuery(NoResults|ValidMapping|InvalidArgumentStatus|RejectsUnknownOrTrailingJSON)' ./...</automated>
  </verify>
  <acceptance_criteria>
    - HTTP no-results responses are 200 with explicit empty answer/citation arrays, numeric basis zero, NO_EVIDENCE notice, effective session, and snapshot.
    - Existing grounded response fields remain losslessly mapped.
    - The generic gateway JSON writer and gRPC InvalidArgument-to-400 classification are unchanged.
  </acceptance_criteria>
  <done>D2's exact public HTTP shape is executable and clients can distinguish no-results from malformed or infrastructure failure.</done>
</task>

<task type="auto">
  <name>Task 3: Record ADR-03-001 deferred-confirmed boundaries</name>
  <read_first>
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/deferred-items.md
    - .discussion/decisions/phases/03/2026-08-04-ADR-03-001-verification-gap-disposition.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-VERIFICATION.md
  </read_first>
  <files>.planning/phases/03-hybrid-retrieval-basic-rag-path/deferred-items.md</files>
  <action>
    Update the source-of-record ledger with a short heading named ADR-03-001 deferred-confirmed boundaries. Revise DEBT-RAG-01 to state that D1 infrastructure failures now fail closed with identity while disclosed surviving-path continuation and model-only behavior remain Phase 06. Revise DEBT-RAG-03 to state that D3 keeps strict invalid-marker rejection with no public success and reserves citation repair/downgrade for Phase 06. Revise DEBT-RAG-04 to preserve initial BM25 build-before-readiness and document that a long-running engine may require restart after in-process ingest because completed does not imply live BM25 visibility. Revise DEBT-RAG-05 to carve out only D2's valid zero-match success branch while retaining the full malformed, bound, unmatched, and combinatorial filter matrix as deferred. Revise DEBT-RAG-06 to confirm D5 is chunk-only in Phase 03, with the graph seam owned by Phase 04 and unavailable fallback reserved for Phase 06. Keep residual risks, escalation triggers, and future acceptance criteria for each row; do not add any runtime implementation claim for D3, D4, or D5.
  </action>
  <verify>
    <automated>rg -n "ADR-03-001 deferred-confirmed boundaries|DEBT-RAG-01|DEBT-RAG-03|DEBT-RAG-04|DEBT-RAG-05|DEBT-RAG-06|D3|D4|D5" .planning/phases/03-hybrid-retrieval-basic-rag-path/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - The ledger explicitly records the shipped D1 and D2 boundary changes and the still-deferred D3, D4, and D5 contracts.
    - DEBT-RAG-01, DEBT-RAG-03, DEBT-RAG-04, DEBT-RAG-05, and DEBT-RAG-06 retain residual risk, trigger, target, and future acceptance information.
    - The ledger does not claim citation repair, BM25 hot refresh/restart recovery, graph fallback, or exhaustive filter coverage as Phase 03 behavior.
  </acceptance_criteria>
  <done>The deferred ledger and plan-set boundary agree with the accepted ADR, with D3-D5 documented rather than implemented.</done>
</task>

</tasks>

<deferred-confirmed>
## Deferred-confirmed boundaries

- D3 / DEBT-RAG-03: strict citation-marker validation remains fail closed; repair and transparent downgrade remain Phase 06 work.
- D4 / DEBT-RAG-04: initial BM25 build/readiness remains the Phase 03 safeguard; hot refresh, coordinated replacement, and restart lifecycle remain deferred, with restart required for correctness-sensitive post-ingest hybrid queries.
- D5 / DEBT-RAG-06: Phase 03 remains source-chunk-only; graph context belongs to Phase 04 and graph-unavailable degraded behavior remains Phase 06 work.
- DEBT-RAG-05: only the single D2 valid zero-match success branch is added; the broader filter matrix remains deferred.

</deferred-confirmed>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| HTTP client -> Go QueryRAG handler | Caller-controlled JSON must be distinguished from a valid empty corpus match. |
| Rust retrieval outcome -> gRPC response | An empty successful result becomes a public response without provider text. |
| gRPC response -> HTTP JSON DTO | Enum, notices, empty arrays, and snapshot fields cross into the public API representation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-18-01 | Spoofing | no-results notice/basis contract | medium | mitigate | Use the fixed numeric UNSPECIFIED basis and exact NO_EVIDENCE notice only on a successful empty retrieval branch, never on D1 errors. |
| T-03-18-02 | Tampering | gateway JSON response mapping | medium | mitigate | Map all QueryRAG fields through a typed DTO and assert exact zero-value serialization in an HTTP test. |
| T-03-18-03 | Denial of Service | malformed query/filter boundary | high | mitigate | Preserve existing strict decoding, request bounds, and InvalidArgument-to-400 behavior before retrieval or provider work. |
| T-03-18-SC | Tampering | package-manager installs | low | accept | No package-manager install or dependency change is part of this plan; existing locked dependencies are reused. |
</threat_model>

<verification>
- cargo test --manifest-path engine/Cargo.toml --locked query_rag_valid_zero_match -- --test-threads=1
- cd gateway && go test -count=1 -run 'TestRAGQuery(NoResults|ValidMapping|InvalidArgumentStatus|RejectsUnknownOrTrailingJSON)' ./...
- cargo test --manifest-path engine/Cargo.toml --locked
- cd gateway && go test -count=1 ./...
- cd gateway && go test -count=1 -run '^TestRAGQueryCrossRuntime$' ./...
- cd gateway && go vet ./...
- buf lint
- Fresh Phase 03 verification must rerun the prior failed axis and report 65/65 with no regressions in grounding, initial BM25 readiness, RAG-04 NoOp reranking, or chunk-only scope.
</verification>

<success_criteria>
D2 is closed when valid zero-match queries return the exact provider-free HTTP/gRPC success shape, malformed inputs remain 400, D1 failures remain distinct, the deferred ledger is aligned for DEBT-RAG-01/03/04/05/06, RAG-04 remains green, and the fresh Phase 03 verification rerun reaches 65/65 on the prior failed axis without claiming RAG-03.
</success_criteria>

<output>
Create .planning/phases/03-hybrid-retrieval-basic-rag-path/03-18-SUMMARY.md when done.
</output>
