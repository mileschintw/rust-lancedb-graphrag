---
phase: 03
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/Cargo.toml
  - engine/Cargo.lock
  - engine/src/main.rs
  - engine/src/retrieval/mod.rs
  - engine/src/retrieval/dense.rs
  - engine/src/retrieval/bm25.rs
  - engine/src/retrieval/fusion.rs
  - engine/src/rerank/mod.rs
  - engine/src/retrieval/tests.rs
  - engine/src/rerank/tests.rs
autonomous: true
requirements: [RAG-02, RAG-04]
must_haves:
  truths:
    - "Rust builds a Unicode-aware BM25 snapshot from completed LanceDB rows without losing the original evidence metadata."
    - "Dense and BM25 candidates use one validated filter model, fuse with weighted full-precision RRF, and deduplicate deterministically by chunk ID."
    - "The NoOpReranker is an object-safe async pass-through that preserves every fused candidate field and ordering."
    - "The retrieval core is repeatable for the same normalized query, filters, snapshot generation, and settings; dynamic replacement and restart recovery remain recorded debt."
  artifacts:
    - path: "engine/Cargo.toml"
      provides: "Direct Unicode analysis dependencies required by the full BM25 contract"
      contains: "unicode-normalization"
    - path: "engine/Cargo.lock"
      provides: "Cargo-generated lockfile entries for the approved Unicode crates"
      contains: "name = \"unicode-casefold\""
    - path: "engine/src/retrieval/bm25.rs"
      provides: "Reusable analyzer, global-IDF BM25 snapshot, and filtered lexical query"
      exports: ["Bm25Index", "Bm25Config"]
    - path: "engine/src/retrieval/fusion.rs"
      provides: "Weighted RRF, source-rank retention, chunk-ID deduplication, and stable tie ordering"
      exports: ["FusedCandidate", "fuse_candidates"]
    - path: "engine/src/rerank/mod.rs"
      provides: "Async Reranker port and NoOpReranker implementation"
      exports: ["Reranker", "NoOpReranker"]
  key_links:
    - from: "engine/src/retrieval/mod.rs"
      to: "engine/src/retrieval/dense.rs"
      via: "One normalized QueryRequest and typed QueryFilters drive dense candidate selection"
      pattern: "QueryFilters"
    - from: "engine/src/retrieval/mod.rs"
      to: "engine/src/retrieval/bm25.rs"
      via: "The same completed snapshot and filter model drive lexical candidates"
      pattern: "Bm25Index"
    - from: "engine/src/retrieval/fusion.rs"
      to: "engine/src/rerank/mod.rs"
      via: "Fused candidates enter the async NoOpReranker without field loss"
      pattern: "NoOpReranker"

---

<objective>
Build the Rust retrieval data-plane slice that makes one normalized question produce deterministic dense-plus-BM25 candidates and a replaceable reranker result.

Purpose: Establish the semantic core before any cross-runtime contract or provider wiring so the later plans consume one tested retrieval interface.
Output: The approved Unicode dependencies and lockfile, reusable dense/BM25/fusion modules, isolated retrieval fixtures, and the async NoOpReranker port.
</objective>

<scope_note>
This plan implements successful dense and lexical retrieval, typed filter semantics, deterministic fusion, and the RAG-04 pass-through seam. It does not implement degraded retrieval, model-only fallback, citation repair/downgrade, graph extraction, provider retries/fallback, or dynamic re-ingestion/restart switching; those constraints remain explicit in deferred-items.md.
</scope_note>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-RESEARCH.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-PATTERNS.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/deferred-items.md
@engine/src/db/mod.rs
@engine/src/bin/inspect_lancedb.rs
@engine/src/inspect_lancedb_tests.rs
@engine/src/chunker/mod.rs
@engine/Cargo.toml

<interfaces>
From engine/src/db/mod.rs: DatabaseManager opens the canonical nodes table; its schema supplies document_id, chunk_id, chunk_index, content, embedding, embedding_model, ingested_at, title, section_path, and content_type, with nullable provenance fields preserved as absent.

From engine/src/chunker/mod.rs: source-preserving transforms and the cached o200k_base token estimator establish the project convention for deterministic derived data.

Handoff produced by this plan: retrieval/mod.rs exports QueryRequest, QueryFilters, RetrievalSettings, Candidate, Retriever, and typed retrieval errors; fusion.rs exports FusedCandidate and fuse_candidates; rerank/mod.rs exports the boxed-future Reranker trait and NoOpReranker.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace the first full-Unicode lexical query slice and lock its analyzer dependencies</name>
  <files>engine/Cargo.toml, engine/Cargo.lock, engine/src/main.rs, engine/src/retrieval/mod.rs, engine/src/retrieval/bm25.rs, engine/src/retrieval/tests.rs</files>
  <read_first>
    - AGENTS.md
    - rust-guidelines.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-RESEARCH.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-PATTERNS.md
    - engine/src/db/mod.rs
    - engine/src/bin/inspect_lancedb.rs
    - engine/src/chunker/mod.rs
    - engine/Cargo.toml
    - engine/src/main.rs
  </read_first>
  <action>Before compiling the analyzer, add only the three packages approved by the Phase 03 Package Legitimacy Audit—unicode-normalization 0.1.25, unicode-casefold 0.2.0, and unicode-segmentation 1.13.3—to engine/Cargo.toml using the existing ~major.minor style, update engine/Cargo.lock through Cargo, and prove the locked build resolves those exact direct dependencies. Wire the retrieval module into the existing Rust binary, then build the first executable lexical query slice that normalizes and validates a QueryRequest, derives the BM25 retrieval view, constructs a global completed-corpus Bm25Index, and returns bounded lexical candidates per D-44, D-45, D-46, D-47, D-48, D-49, D-50, D-54, D-55, D-56, and D-57. The analyzer must apply NFKC, full Unicode case folding, Unicode word boundaries, and whole plus camel-case/underscore/hyphen identifier tokens without stemming or stop-word removal; retain original source strings and nullable provenance; use global completed-corpus document frequency; apply typed filters before candidate limits; and return a clear build error if the lexical snapshot cannot be constructed. Keep settings in explicit Rust configuration structs for the next handoff, add module-local analyzer parity coverage in the listed engine/src/retrieval/tests.rs file, and do not introduce a reduced Unicode analyzer, a search framework, or any deferred failure-path behavior. Pattern alignment: follow the source-preserving transform and separate-test-module convention in engine/src/chunker/mod.rs, the schema-driven LanceDB/Arrow access in engine/src/bin/inspect_lancedb.rs, and the isolated fixture assertions in engine/src/inspect_lancedb_tests.rs, as mapped in 03-PATTERNS.md.</action>
  <verify>
    <automated>cargo check --manifest-path engine/Cargo.toml</automated>
    <automated>cargo check --manifest-path engine/Cargo.toml --locked</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked bm25</automated>
    <automated>cargo fmt --manifest-path engine/Cargo.toml -- --check</automated>
    <automated>$lock = Get-Content -Raw 'engine/Cargo.lock'; foreach ($name in @('unicode-normalization','unicode-casefold','unicode-segmentation')) { if ($lock -notmatch ('name = "' + [regex]::Escape($name) + '"')) { throw "Cargo.lock is missing $name" } }; git diff --check -- engine/Cargo.toml engine/Cargo.lock engine/src/retrieval</automated>
  </verify>
  <acceptance_criteria>
    - engine/Cargo.toml contains only the three approved Unicode analysis additions for this task, and Cargo-generated engine/Cargo.lock is current under --locked verification.
    - engine/src/main.rs can compile the full-Unicode lexical module as the first executable retrieval handoff, and Cargo.lock is current under --locked verification.
    - Bm25Index and query analysis share the full NFKC/full-case-fold/Unicode-boundary/technical-identifier pipeline, preserve original evidence fields, and calculate IDF from the global completed corpus.
    - Decision traceability is explicit for D-44 through D-50 and D-54 through D-57; dynamic recovery and all deferred RAG-03 branches remain outside this task.
  </acceptance_criteria>
  <done>The Rust binary compiles a runnable full-Unicode BM25 query slice, the approved analyzer dependencies are locked before any downstream plan consumes it, and the lexical path returns bounded candidates from the completed corpus.</done>
</task>

<task type="auto">
  <name>Task 2: Expand the lexical slice to hybrid fusion and the async NoOpReranker</name>
  <files>engine/src/main.rs, engine/src/retrieval/mod.rs, engine/src/retrieval/dense.rs, engine/src/retrieval/fusion.rs, engine/src/rerank/mod.rs, engine/src/retrieval/tests.rs, engine/src/rerank/tests.rs</files>
  <read_first>
    - AGENTS.md
    - rust-guidelines.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-PATTERNS.md
    - engine/src/retrieval/mod.rs
    - engine/src/retrieval/dense.rs
    - engine/src/retrieval/bm25.rs
    - engine/src/retrieval/fusion.rs
    - engine/src/inspect_lancedb_tests.rs
    - engine/src/db/tests.rs
  </read_first>
  <action>Extend the existing module wiring from Task 1 with dense LanceDB candidate selection, one typed QueryFilters representation shared with BM25, weighted full-precision RRF, chunk-ID deduplication retaining both source ranks, stable D-51 tie ordering, and the boxed-future Send + Sync Reranker port with NoOpReranker. Use module-local tests plus the existing unique temporary LanceDB fixture pattern and schema-driven Arrow extraction to prove absent filters search the global corpus, document IDs and content types OR within their own fields and AND across fields, valid no-match filters return no candidates, malformed values are rejected before querying, global IDF is unchanged by filters, and repeated normalized query/filter/snapshot/configuration runs return identical ordered IDs and serialized diagnostics per D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-47, D-50, D-51, D-52, and D-53. Prove byte-for-byte candidate-field and ordering preservation through NoOpReranker per RAG-04. Every fixture connection must use the same unique temporary path and all LanceDB handles must be dropped before cleanup; no test may exercise degraded retrieval, dynamic replacement/restart recovery, graph behavior, citation repair/downgrade, or provider retry/fallback. Put the retrieval fixture/candidate assertions in the listed engine/src/retrieval/tests.rs file, following engine/src/inspect_lancedb_tests.rs and engine/src/db/tests.rs, and put reranker assertions in the listed engine/src/rerank/tests.rs file, following engine/src/client/tests.rs, as mapped in 03-PATTERNS.md.</action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked retrieval</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked rerank</automated>
    <automated>cargo fmt --manifest-path engine/Cargo.toml -- --check</automated>
  </verify>
  <acceptance_criteria>
    - Module-local and isolated LanceDB fixtures prove Unicode analyzer parity, field boosts, global IDF, typed filter semantics, pre-limit parity, RRF precision, source-rank retention, stable ties, and repeat-run determinism.
    - NoOpReranker is called through the async trait and preserves score precision, source ranks, identity, provenance, and ordering.
    - Null metadata remains absent rather than fabricated, handles are dropped before temporary-path cleanup, and the deferred-items ledger remains the boundary for failure-path behavior.
  </acceptance_criteria>
  <done>Hybrid retrieval and RAG-04 pass-through behavior are isolated, deterministic, filter-correct, and covered by runnable Rust tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Caller values to Rust retrieval | Query text, UUID filters, content types, and bounds are untrusted before typed normalization. |
| Canonical LanceDB rows to derived BM25 memory | Completed source content and nullable metadata become searchable in-memory state. |
| Cargo registry to build output | Approved Unicode crates enter the executable retrieval path and the lockfile controls their versions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-01-01 | Tampering | Typed filters and LanceDB predicates | high | mitigate | Normalize UUID/content-type values into one typed QueryFilters object and apply it before both candidate limits; never accept arbitrary predicate text. |
| T-03-01-02 | Tampering | BM25 analyzer and global statistics | high | mitigate | Use one full Unicode analyzer for build/query, preserve original fields separately, and compute IDF over the complete snapshot rather than the filtered subset. |
| T-03-01-03 | Denial of Service | Candidate and evidence precursor limits | medium | mitigate | Enforce query/filter/candidate bounds in RetrievalSettings before scanning or fusing candidates. |
| T-03-01-04 | Information Disclosure | Derived BM25 metadata | medium | mitigate | Carry only canonical bounded provenance into candidates and leave absent nullable fields absent. |
| T-03-01-SC | Tampering | Cargo package installation | high | mitigate | Use only the three packages marked Approved in the Phase 03 Package Legitimacy Audit and verify Cargo.lock with --locked checks. |
</threat_model>

<verification>
- Run the locked retrieval and reranker suites, formatter, and Cargo manifest/lockfile checks.
- Confirm every mutable LanceDB fixture uses a unique temporary path and releases all handles before cleanup.
- Confirm the retrieval tests cover only the successful dense/BM25 path and preserve dynamic recovery and degraded behavior in deferred-items.md.
</verification>

<success_criteria>
The Rust retrieval core provides full-Unicode BM25, same-filter dense/BM25 selection, deterministic weighted RRF, duplicate/source-rank retention, and an async NoOpReranker without relying on a reduced analyzer or an external search framework.
</success_criteria>

<output>
Create `.planning/phases/03-hybrid-retrieval-basic-rag-path/03-01-SUMMARY.md` when done.
</output>
