---
phase: 03-hybrid-retrieval-basic-rag-path
plan: "14"
type: execute
wave: 12
depends_on:
  - 03-13
files_modified:
  - engine/src/generation/openrouter.rs
  - engine/src/generation/tests.rs
  - engine/src/main.rs
  - engine/src/tests.rs
  - engine/tests/config_startup.rs
  - gateway/main.go
  - gateway/main_test.go
autonomous: true
requirements:
  - RAG-02
gap_closure: true
assumption_delta: false
must_haves:
  truths:
    - "The production OpenRouter request uses the same validated EffectiveRagSettings evidence budget and generation limits as the retrieval path."
    - "A failed generation request carries the QueryRAG session and correlation identity in structured tonic error metadata and never returns fabricated answer content."
    - "The Go /rag/query error boundary exposes validated session, correlation, and error-kind identity headers while preserving the existing HTTP error classification and generic body."
    - "Production startup refuses to construct the provider when OPENROUTER_API_KEY is absent or blank."
  artifacts:
    - "engine/src/generation/openrouter.rs exposes a validated evidence-budget setting and applies it while packing the provider prompt."
    - "engine/src/main.rs wires EffectiveRagSettings, correlation identity, structured generation errors, and explicit provider credentials into the service path."
    - "engine/src/generation/tests.rs, engine/src/tests.rs, engine/tests/config_startup.rs, gateway/main.go, and gateway/main_test.go exercise the production adapter, startup, and public error contracts."
  key_links:
    - "EffectiveRagSettings.evidence_token_budget + max_output_tokens -> OpenRouterGenerationConfig -> pack_evidence_prompt + max_completion_tokens"
    - "QueryRAG session/correlation IDs -> GenerationRequest -> GenerationError -> tonic trailer metadata -> gateway HTTP identity headers"
    - "OPENROUTER_API_KEY validation -> provider construction -> startup readiness"
---

<objective>
Close the current P25/P41, D-31, and startup-credential gaps by making the production provider adapter consume the validated effective settings, preserving request identity on generation failure, and rejecting unconfigured OpenRouter startup.

Purpose: The accepted RAG path must have one authoritative runtime configuration and an auditable failure boundary. Provider calls must be bounded by the same settings used to build evidence, failures must remain attributable to the caller session, and production must never silently run with fabricated credentials.

Output: A production-wired OpenRouter evidence budget, deterministic adapter coverage, structured identity-preserving generation errors, and an explicit credential validation test.
</objective>

<scope>
This is gap-closure work only. It does not add one-path degradation, model-only fallback semantics, citation repair/downgrade, retries, lifecycle recovery, graph fallback, or a new gateway/protobuf error response. ModelOnly remains rejected by the fail-closed contract from 03-13; this plan only carries identity through the existing Rust error boundary.
</scope>

<assumptions>
- 03-13 has established the fail-closed grounding and bounded-provider-output contract before this plan runs.
- The existing tonic status metadata API is sufficient for the D-31 structured error envelope because the QueryRAG success response has no error payload branch.
- No package or dependency installation is required.
</assumptions>

## Artifacts this phase produces

- A configurable OpenRouterGenerationConfig evidence budget populated from EffectiveRagSettings.
- Production adapter tests proving runtime evidence limits are honored on the actual OpenRouter request path.
- A QueryRAG generation-error mapping that preserves session/correlation identity without exposing prompts, evidence, or credentials.
- Startup coverage proving a missing or blank OPENROUTER_API_KEY blocks readiness.

## Source coverage audit

| Source | Current item | Plan coverage |
|---|---|---|
| GOAL | A completed-corpus hybrid RAG query reaches a provider using bounded evidence and returns an attributable result or failure. | Tasks 1-2 wire the production provider and error boundary. |
| REQ | RAG-02 hybrid retrieval remains the source of provider evidence. | Task 1 preserves the RAG-02 effective-settings path. |
| REQ | RAG-04 retrieval/Reranker scope is already implemented by 03-01 through 03-12. | Preserved as baseline; no new RAG-04 behavior is introduced. |
| RESEARCH | Rust owns provider orchestration; OpenRouter is one bounded adapter; configuration is validated before use; no fabricated fallback. | Tasks 1-2 follow the responsibility map and existing adapter contract. |
| CONTEXT | D-26/D-27 provider-neutral generator and OpenRouter default; D-29 one attempt; D-30 timeout; D-31 structured identity; D-32 temporary provider; D-33 output bound; D-35/D-39 bounded untrusted evidence. | Tasks 1-2 cite and implement the applicable decisions. |
| DEFERRED | DEBT-RAG-01/03/04/05/06 and other deferred lifecycle/degradation/repair behavior. | Explicitly excluded from this plan. |

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-AI-SPEC.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-VERIFICATION.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-13-PLAN.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-PATTERNS.md
@engine/src/generation/openrouter.rs
@engine/src/generation/mod.rs
@engine/src/main.rs
@engine/src/generation/tests.rs
@engine/src/tests.rs
@engine/tests/config_startup.rs
@gateway/main.go
@gateway/main_test.go
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Route EffectiveRagSettings through the production OpenRouter evidence packer</name>
  <read_first>
    AGENTS.md
    rust-guidelines.md
    .planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
    .planning/phases/03-hybrid-retrieval-basic-rag-path/03-AI-SPEC.md
    .planning/phases/03-hybrid-retrieval-basic-rag-path/03-PATTERNS.md
    .planning/phases/03-hybrid-retrieval-basic-rag-path/03-13-PLAN.md
    engine/src/generation/openrouter.rs
    engine/src/generation/mod.rs
    engine/src/generation/tests.rs
    engine/src/main.rs
    engine/src/client/mod.rs
    engine/src/client/tests.rs
    config/config.toml
    config/config.example.toml
  </read_first>
  <files>engine/src/generation/openrouter.rs, engine/src/generation/tests.rs, engine/src/main.rs</files>
  <action>Extend `OpenRouterGenerationConfig` with a validated evidence-token-budget field and carry both effective limits through every constructor/caller without changing the provider-neutral `Generator` interface: `EffectiveRagSettings.evidence_token_budget` must become the adapter prompt limit, and `EffectiveRagSettings.max_output_tokens` must remain the adapter completion limit. In `execute_one_call`, pass `self.config.evidence_token_budget` and `self.config.max_completion_tokens` together to `pack_evidence_prompt`, and use the same pair for provider usage validation; the production adapter must not select a second prompt or completion budget. Reject zero or unusable values at configuration validation. In `main`, pass both validated effective values after settings validation. Follow the strong OpenRouter client and configured-settings test analogs in 03-PATTERNS.md. Extend `generation_request_uses_effective_settings` with a non-default evidence limit and completion limit, capture the actual user prompt and request JSON from the local HTTP adapter, and assert that packing changes at the non-default boundary while `max_completion_tokens` carries the configured value. Preserve the existing one-call, timeout, strict-schema, finish-reason, and provider-error behavior per D-27, D-29, D-30, D-32, D-33, D-35, and D-39.</action>
  <verify>
    <automated>Push-Location engine; rg -n -U 'pack_evidence_prompt\([^;]*(8192|2048)' src/generation/openrouter.rs; $rgExit = $LASTEXITCODE; if ($rgExit -eq 0) { Pop-Location; throw 'OpenRouter adapter still contains a literal prompt/output budget in the evidence-packing call' }; if ($rgExit -gt 1) { Pop-Location; exit $rgExit }; rg -n 'evidence_token_budget|max_completion_tokens' src/generation/openrouter.rs src/main.rs src/generation/tests.rs; $wiringExit = $LASTEXITCODE; if ($wiringExit -ne 0) { Pop-Location; exit $wiringExit }; cargo test --locked generation::tests::generation_request_uses_effective_settings -- --exact --test-threads=1 2>&1; $testExit = $LASTEXITCODE; Pop-Location; if ($testExit -ne 0) { exit $testExit }</automated>
  </verify>
  <acceptance_criteria>
    - OpenRouterGenerationConfig owns the validated effective evidence-token and completion-token limits, and the adapter passes both values to evidence packing and outbound request construction on every production request.
    - main supplies the validated EffectiveRagSettings.evidence_token_budget and max_output_tokens; no second runtime prompt or completion limit is selected in adapter wiring.
    - The focused adapter test observes non-default prompt packing on the OpenRouter HTTP path and the exact configured completion-token value, while source verification rejects retained literal budget arguments in that call.
  </acceptance_criteria>
  <done>The production OpenRouter adapter is controlled by the validated effective evidence settings, and its focused test proves the setting changes the actual outbound prompt.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve generation identity and reject fake-key startup</name>
  <read_first>
    AGENTS.md
    go-guidelines.md
    gateway/go.mod
    rust-guidelines.md
    .planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
    .planning/phases/03-hybrid-retrieval-basic-rag-path/03-AI-SPEC.md
    .planning/phases/03-hybrid-retrieval-basic-rag-path/03-PATTERNS.md
    .planning/phases/03-hybrid-retrieval-basic-rag-path/03-VERIFICATION.md
    engine/src/generation/mod.rs
    engine/src/main.rs
    engine/src/tests.rs
    engine/tests/config_startup.rs
    gateway/main.go
    gateway/main_test.go
  </read_first>
  <files>engine/src/main.rs, engine/src/tests.rs, engine/tests/config_startup.rs, gateway/main.go, gateway/main_test.go</files>
  <action>Before editing `gateway/main.go` or `gateway/main_test.go`, apply the detected Go 1.25.0 guidance from `go-guidelines.md` and the target declared in `gateway/go.mod`. Generate one correlation UUID at the QueryRAG boundary per D-31, set it on `GenerationRequest` alongside the effective session ID from D-20, and map `GenerationError` to tonic `Status` while preserving the current `InvalidRequest` versus internal/provider status classification. Attach validated ASCII trailing metadata with the exact lowercase keys `x-lancet-session-id`, `x-lancet-correlation-id`, and `x-lancet-error-kind`; use the UUID values for the first two and one of the stable snake_case codes `invalid_request`, `supported_parameters`, `provider_error`, `schema_validation`, `timeout`, `cancelled`, or `session_correlation` for the third. Keep the public tonic status message generic and exclude prompts, retrieved evidence, response bodies, and credentials; generation failure must produce no `QueryRAGResponse`. Add a focused Rust service regression using a failing `FakeGenerator` that asserts the status metadata contains both identities and the stable error kind and that no response is fabricated.

At the existing Go error boundary, make `grpcEngine.QueryRAG` capture the unary call trailers with `grpc.Trailer`, wrap the error while preserving `status.Code`, and have `queryRAG` copy only validated values for those three metadata keys to the exact HTTP headers `X-Lancet-Session-ID`, `X-Lancet-Correlation-ID`, and `X-Lancet-Error-Kind`, setting each header value exactly to its corresponding trailer value. Preserve HTTP 400 for `codes.InvalidArgument`, HTTP 502 with the existing generic `engine query failed` body for other engine errors, and all successful protobuf response messages; do not invent a fallback identity when trailers are absent and do not add a new protobuf error response. Follow the existing chi `RequestID`, `writeJSON`, gRPC status, and `engineFunc`/httptest analogs mapped in 03-PATTERNS.md. Add `TestRAGQueryProviderErrorPreservesIdentity` in `gateway/main_test.go` with a trailer-bearing fake error and assert the 502 status, unchanged generic body, and exact session/correlation/error-kind headers.

In startup, read `OPENROUTER_API_KEY` only after effective settings validation and before provider construction, reject missing, empty, and whitespace-only values with explicit configuration errors, and remove the production fallback credential. Extend `engine/tests/config_startup.rs` with `missing_openrouter_api_key_blocks_readiness` and `blank_openrouter_api_key_blocks_readiness`; the latter must cover both an empty string and a whitespace-only string. Each process test must assert a nonzero exit and no `Rust RAG Engine serving` readiness line using the existing isolated config/process helpers.</action>
  <verify>
    <automated>Push-Location engine; $tests = @('tests::query_rag_generation_error_preserves_identity'); foreach ($target in $tests) { cargo test --locked $target -- --exact --test-threads=1 2>&1; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { Pop-Location; exit $testExit } }; $startupTests = @('missing_openrouter_api_key_blocks_readiness','blank_openrouter_api_key_blocks_readiness'); foreach ($name in $startupTests) { cargo test --locked --test config_startup $name -- --exact --test-threads=1 2>&1; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { Pop-Location; exit $testExit } }; Pop-Location</automated>
    <automated>Push-Location gateway; go test -count=1 -run '^TestRAGQueryProviderErrorPreservesIdentity$' ./...; $goExit = $LASTEXITCODE; Pop-Location; if ($goExit -ne 0) { exit $goExit }</automated>
  </verify>
  <acceptance_criteria>
    - Every QueryRAG generation request has a nonempty correlation ID paired with its session ID, and a generation failure exposes both in structured tonic metadata.
    - The generation error path returns no fabricated QueryRAGResponse and does not leak untrusted provider content or credentials through the status message.
    - The Go `/rag/query` failure boundary exposes the exact validated identity headers while retaining HTTP 502 and the generic existing error body for provider/internal failures, and retains HTTP 400 for InvalidArgument.
    - Missing, empty, and whitespace-only OPENROUTER_API_KEY values stop startup before readiness with nonzero process exit; no fake credential is accepted.
  </acceptance_criteria>
  <done>D-31 identity is preserved through the existing tonic error boundary and P41 startup uses an explicit validated credential, with focused service and process tests covering both gaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| QueryRAG caller -> Rust service | Caller-controlled session context and question enter the request path. |
| Rust service -> provider adapter | Retrieved evidence and generation settings become an outbound provider request. |
| Process environment -> startup | OPENROUTER_API_KEY is an external secret required for provider construction. |
| Provider error -> tonic client | Provider failure details cross back through a public structured status boundary. |
| tonic error trailers -> Go HTTP response | Allowlisted session/correlation/error-kind identity crosses from gRPC metadata into public response headers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-14-01 | Tampering | Effective settings to OpenRouter request | high | mitigate | Carry the validated evidence budget through OpenRouterGenerationConfig and assert the outbound prompt boundary in the recording HTTP adapter test. |
| T-03-14-02 | Information Disclosure | GenerationError to tonic status | high | mitigate | Emit only stable identity/error-kind metadata and a bounded generic message; exclude prompts, evidence, response bodies, and credentials. |
| T-03-14-03 | Repudiation | QueryRAG session/correlation attribution | medium | mitigate | Generate and attach a correlation ID at the request boundary, then preserve both IDs in the structured provider error. |
| T-03-14-04 | Spoofing | Provider startup credential | high | mitigate | Reject absent or trim-empty OPENROUTER_API_KEY before provider construction and remove the fake-key path. |
| T-03-14-05 | Information Disclosure | Gateway provider-error headers | medium | mitigate | Copy only the three allowlisted ASCII trailer values to stable headers, retain the generic 502 body, and test that provider content and credentials do not cross the boundary. |
| T-03-14-SC | Tampering | Cargo dependency/install boundary | low | accept | No package or dependency installation is introduced by this gap-closure plan. |
</threat_model>

<verification>
- Run the focused adapter, QueryRAG generation-error, both credential startup tests, and the gateway identity-header test from the tasks with immediate PowerShell exit-code capture.
- Confirm both effective evidence and completion budgets control production prompt packing/request construction and the source gate rejects literal budget arguments in the adapter call.
- Confirm the Rust status metadata reaches the public Go HTTP boundary as the exact identity headers, with unchanged status classification/body and no fabricated response.
- Leave the full locked parallel and serial Rust gate to plan 15 after the deterministic citation fixture is repaired.
</verification>

<success_criteria>
- P25/P41 provider configuration coverage demonstrates that EffectiveRagSettings controls production evidence packing and generation limits.
- D-31 is observable in a focused QueryRAG failure test through stable identity metadata and no answer payload.
- D-31 is observable at the public Go HTTP boundary through stable session/correlation/error-kind headers without changing protobuf success messages.
- P41 startup fails closed for missing, empty, or whitespace-only OpenRouter credentials.
</success_criteria>

<output>
Create the 03-14-SUMMARY.md execution summary when this plan is executed.
</output>
