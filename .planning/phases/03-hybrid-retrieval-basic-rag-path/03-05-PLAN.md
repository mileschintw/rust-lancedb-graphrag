---
phase: 03
plan: "05"
type: execute
wave: 5
depends_on:
  - 03-04
files_modified:
  - engine/src/bin/seed_rag_fixture.rs
  - gateway/main_test.go
  - .planning/phases/03-hybrid-retrieval-basic-rag-path/COVERAGE.md
autonomous: true
requirements: [RAG-02, RAG-04]
must_haves:
  truths:
    - "A provider-independent process-level smoke starts the real Rust engine against an isolated temporary LanceDB corpus, sends query embeddings to a deterministic localhost mock, and returns a grounded structured answer through the real Go HTTP route."
    - "The deterministic localhost mock covers embedding vectors, model supported-parameters metadata, and strict chat completion, so dense retrieval, BM25 retrieval, fusion, evidence, and generation are all exercised without OpenRouter credentials or PostgreSQL."
    - "The serving log is only a startup milestone: a bounded generated-gRPC Ping to the configured 127.0.0.1 endpoint succeeds before the Go route issues `/rag/query`."
    - "The child environments contain only the exact test-scoped application variables, and teardown reaps the process tree and proves the isolated LanceDB path is released on Windows."
    - "The smoke fixture and every child process are isolated and cleaned up after the test, while the optional live-provider check remains manual and separate from automated verification."
  artifacts:
    - path: "engine/src/bin/seed_rag_fixture.rs"
      provides: "Reusable deterministic completed-corpus LanceDB fixture seeder"
      contains: "--lancedb-path"
    - path: "gateway/main_test.go"
      provides: "Real Go-to-Rust cross-runtime RAG smoke with local embedding, metadata, and chat mocks"
      contains: "TestRAGQueryCrossRuntime"
    - path: ".planning/phases/03-hybrid-retrieval-basic-rag-path/COVERAGE.md"
      provides: "Updated API and verification coverage map for the five-plan decomposition"
      contains: "TestRAGQueryCrossRuntime"
  key_links:
    - from: "engine/src/bin/seed_rag_fixture.rs"
      to: "engine/src/main.rs"
      via: "The seed binary writes the canonical completed nodes schema before the real engine opens the same temporary LanceDB path"
      pattern: "nodes"
    - from: "gateway/main_test.go"
      to: "engine/src/main.rs"
      via: "The test builds the engine and seeder once, launches their resolved direct binaries from the repository root, and uses a test-scoped loopback gRPC address and isolated LanceDB path"
      pattern: "LANCET_ENGINE__GRPC_ADDR"
    - from: "gateway/main_test.go"
      to: "engine/src/client/mod.rs"
      via: "LANCET_OPENROUTER__EMBEDDING_ENDPOINT redirects the real query embedding call to the deterministic localhost embedding handler"
      pattern: "LANCET_OPENROUTER__EMBEDDING_ENDPOINT"
    - from: "gateway/main_test.go"
      to: "engine/src/generation/openrouter.rs"
      via: "LANCET_OPENROUTER__MODEL_METADATA_ENDPOINT drives the supported-parameters preflight against the same localhost mock"
      pattern: "LANCET_OPENROUTER__MODEL_METADATA_ENDPOINT"
    - from: "gateway/main_test.go"
      to: "engine/src/generation/openrouter.rs"
      via: "LANCET_OPENROUTER__CHAT_ENDPOINT drives exactly one strict structured completion against the same localhost mock"
      pattern: "LANCET_OPENROUTER__CHAT_ENDPOINT"
    - from: "gateway/main_test.go"
      to: "gateway/main.go"
      via: "The test sends an HTTP request through the registered /rag/query route and a generated gRPC client to the real Rust process"
      pattern: "/rag/query"

---

<objective>
Prove the complete local happy path through the real Go gateway, Rust gRPC engine, dense-plus-BM25 retrieval, bounded evidence, and structured generation contract.

Purpose: Close the provider-independent acceptance loop and make the five-plan handoffs executable without introducing live-provider or deferred recovery scope.
Output: A reusable isolated LanceDB seeder, a deterministic three-endpoint local provider mock, a real cross-runtime HTTP smoke, and synchronized coverage documentation.
</objective>

<scope_note>
This is the accepted MVP happy-path tracer completion. It does not add degraded retrieval, model-only fallback, citation repair/downgrade, provider retry/fallback, dynamic restart/re-ingestion recovery, graph extraction, or alternate-provider behavior. It keeps the optional live OpenRouter check manual and separate from automated tests per DEBT-RAG-01, DEBT-RAG-02, DEBT-RAG-03, DEBT-RAG-04, DEBT-RAG-06, and future D-11 through D-16/D-24/D-41 through D-43.
</scope_note>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-VALIDATION.md
@engine/src/db/mod.rs
@engine/src/client/mod.rs
@engine/src/main.rs
@gateway/main.go
@gateway/main_test.go
@config/config.example.toml
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace the real local Go-to-Rust RAG request with seeded LanceDB and three provider mocks</name>
  <files>engine/src/bin/seed_rag_fixture.rs, gateway/main_test.go</files>
  <read_first>
    - AGENTS.md
    - rust-guidelines.md
    - go-guidelines.md
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-AI-SPEC.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-RESEARCH.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-PATTERNS.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/deferred-items.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-VALIDATION.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-SOURCE-AUDIT.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/COVERAGE.md
    - engine/src/db/mod.rs
    - engine/src/bin/inspect_lancedb.rs
    - engine/src/main.rs
    - engine/tests/config_startup.rs
    - gateway/main.go
    - gateway/main_test.go
    - gateway/go.mod
    - engine/Cargo.toml
  </read_first>
  <action>Create a real reusable `seed_rag_fixture` binary that accepts an explicit temporary LanceDB path, initializes the existing database manager, and writes deterministic completed rows using the canonical `nodes` schema: stable document/chunk identities, complete chunk text/title/section/content-type metadata, finite 2,048-dimensional embeddings matching the configured embedding model, deterministic token/hash metadata, and no staged or incomplete rows. Populate enough distinct chunks for one dense hit, one lexical identifier hit, and stable citation ordering; release all LanceDB handles before the process exits. In `gateway/main_test.go`, add `TestRAGQueryCrossRuntime` using `t.TempDir` and the existing process-test conventions: start one deterministic `httptest.Server` with separate handlers for the exact configured embedding endpoint (returning the expected 2,048-wide query vector), model metadata endpoint (returning the configured model and `supported_parameters` advertising structured output/response format), and chat-completion endpoint (asserting the strict request, then returning valid structured JSON with one answer marker, the first supplied evidence ID, usage, model metadata, empty notices/warnings, and no fabricated citation IDs). Allocate one free TCP port on `127.0.0.1` before starting the engine, close only the reservation, and set the test-scoped `LANCET_ENGINE__GRPC_ADDR=127.0.0.1:<port>` value used by both the engine process and generated gRPC client. Build both binaries exactly once from the resolved repository root with `cargo build --manifest-path <repoRoot>/engine/Cargo.toml --locked --target-dir <repoRoot>/engine/target --bin engine --bin seed_rag_fixture`; require and resolve `<repoRoot>/engine/target/debug/engine.exe` and `<repoRoot>/engine/target/debug/seed_rag_fixture.exe` on Windows, then start those direct executable paths with `cmd.Dir = repoRoot` rather than launching the long-lived engine through Cargo. Construct clean child environments before launch from a minimal process-safe OS baseline rather than copying `os.Environ` wholesale: remove every inherited key beginning with `LANCET_`, `OPENROUTER_`, or `CONFIG_`, plus `DATABASE_URL`, `TEST_DATABASE_URL`, and `RUST_LOG`; append only `LANCET_ENGINE__GRPC_ADDR=127.0.0.1:<port>`, `LANCET_ENGINE__LANCEDB_PATH=<temp>`, `LANCET_OPENROUTER__EMBEDDING_ENDPOINT=<mock>/...`, `LANCET_OPENROUTER__MODEL_METADATA_ENDPOINT=<mock>/...`, `LANCET_OPENROUTER__CHAT_ENDPOINT=<mock>/...`, and `OPENROUTER_API_KEY=test-key` to the engine environment, and give the seeder no inherited application configuration. Assert that `LANCET_CONFIG_DIR`, `LANCET_ENV`, arbitrary `LANCET_*` overlays, provider base/key variables, and database variables are absent from the launched child environment. Start the direct seeder and wait for a successful exit within a 30-second deadline, then start the direct Rust engine from the repository root. Treat the existing `Rust RAG Engine serving` line only as a post-build milestone; after observing it, create the generated gRPC client and poll `Ping` against the exact `127.0.0.1:<port>` endpoint under a bounded 30-second context/deadline with bounded backoff, and do not construct the gateway route or issue HTTP until Ping succeeds. Send an HTTP POST to `/rag/query` through the real generated client and route. Assert HTTP success, effective session identity, grounded answer marker/citation provenance, retrieval snapshot, usage/model metadata, and that both dense and lexical fixture content are represented in the Rust-owned result. Register cleanup while the localhost mock is still alive: close the gRPC connection, cancel the engine context, invoke a Windows process-tree termination for the engine PID (`taskkill /PID <pid> /T /F`), wait up to 10 seconds, then use bounded `Process.Kill`/`Wait` fallback and reap descendants without name-based killing. The seed process must exit zero before the engine starts; an engine exit before the query or an unexpected pre-teardown nonzero status fails the test, while intentional teardown is allowed. Before `t.TempDir` cleanup, retry a rename of the isolated LanceDB directory to a sibling release path within a bounded deadline, remove the renamed path, and fail if the rename/remove proves any open handle remains. Keep the test provider-independent, avoid PostgreSQL, and do not replace the real Rust RPC with an engineFunc mock in this test; preserve the separate focused unit tests from Plan 03-04. Pattern alignment: use engine/src/db/mod.rs for the canonical fixture schema, engine/src/bin/inspect_lancedb.rs for LanceDB/Arrow access, engine/tests/config_startup.rs for readiness/process cleanup, and gateway/main_test.go for the real route assertion, as mapped in 03-PATTERNS.md.</action>
  <verify>
    <automated>cargo build --manifest-path engine/Cargo.toml --locked --target-dir engine/target --bin engine --bin seed_rag_fixture; $cargoExit = $LASTEXITCODE; if ($cargoExit -ne 0) { exit $cargoExit }; if (-not (Test-Path -LiteralPath 'engine/target/debug/engine.exe')) { throw 'Resolved engine executable is missing' }; if (-not (Test-Path -LiteralPath 'engine/target/debug/seed_rag_fixture.exe')) { throw 'Resolved fixture seeder executable is missing' }; Push-Location gateway; $goList = go test . -list '^TestRAGQueryCrossRuntime$'; $goListExit = $LASTEXITCODE; if ($goListExit -ne 0) { Pop-Location; exit $goListExit }; if ($goList -notmatch '(?m)^TestRAGQueryCrossRuntime$') { Pop-Location; throw 'TestRAGQueryCrossRuntime is not registered' }; $goOutput = go test . -run '^TestRAGQueryCrossRuntime$' -count=1 -v; $goExit = $LASTEXITCODE; if ($goExit -ne 0) { Pop-Location; exit $goExit }; if ($goOutput -notmatch '(?m)^--- PASS: TestRAGQueryCrossRuntime$') { Pop-Location; throw 'TestRAGQueryCrossRuntime matched zero tests or did not pass' }; Pop-Location</automated>
  </verify>
  <acceptance_criteria>
    - The seed binary writes the canonical completed nodes fixture to the caller-provided temporary path and exits before the engine opens it.
    - One localhost server receives and validates query embeddings, supported-parameters metadata, and exactly one strict chat completion during the real smoke.
    - The HTTP request traverses the real gateway route, generated gRPC client, Rust retrieval/fusion/evidence/generation path, and returns structured grounded output.
    - The log is never the sole readiness proof: the exact loopback endpoint answers Ping within the bounded deadline before the real Go route is called.
    - The engine and seeder use resolved direct binaries from the repository root, clean application environments, bounded process-tree teardown, and a successful rename/remove release check for the isolated LanceDB path; no provider credential or raw corpus is retained.
  </acceptance_criteria>
  <done>The provider-independent cross-runtime happy path passes against an isolated seeded LanceDB store and deterministic local embedding/metadata/chat endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize the Phase 03 coverage and manual-provider boundary</name>
  <files>.planning/phases/03-hybrid-retrieval-basic-rag-path/COVERAGE.md</files>
  <read_first>
    - AGENTS.md
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-AI-SPEC.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-PATTERNS.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/deferred-items.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-VALIDATION.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-SOURCE-AUDIT.md
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/COVERAGE.md
    - gateway/main.go
    - gateway/main_test.go
    - engine/tests/config_startup.rs
  </read_first>
  <action>Update the phase coverage matrix to map retrieval and Unicode analyzer proof to Plan 03-01, provider-neutral evidence and strict OpenRouter contract tests to Plan 03-02, additive gRPC/startup readiness to Plan 03-03, Go boundary and embedding endpoint seam to Plan 03-04, and the real local three-endpoint cross-runtime smoke to Plan 03-05. Record that automated verification covers embeddings, model supported-parameters metadata, one structured chat completion, grounded citations, usage/model metadata, strict request validation, timeout/cancellation, the initial BM25 build/readiness safeguard, and the post-log generated-gRPC Ping probe against the exact loopback endpoint without PostgreSQL or live credentials. State that Plan 03-05 builds the engine and seeder once, starts resolved direct binaries from the repository root with scrubbed application environments, uses bounded Windows process-tree teardown, and proves the isolated LanceDB path can be renamed/removed after child shutdown. Keep the ignored live OpenRouter structured-output check explicitly optional/manual and list future degraded/fallback/repair/retry/re-ingestion/graph behavior as debt rather than silently counting it as automated coverage. Carry `DEBT-RAG-05` into the coverage artifact with the deferred boundary (valid happy-path inputs only; basic guards remain in scope), rationale, trigger, Phase 06 target, and future acceptance criteria for empty/oversized queries, malformed IDs, unsupported content types, and filter limits; do not count that exhaustive negative-input/filter suite as automated MVP coverage. Pattern alignment: keep documentation ownership consistent with the route assertions in gateway/main_test.go and initial-readiness evidence in engine/tests/config_startup.rs, as mapped in 03-PATTERNS.md.</action>
  <verify>
    <automated>rg -F -q -- "03-01" .planning/phases/03-hybrid-retrieval-basic-rag-path/COVERAGE.md; $rgExit = $LASTEXITCODE; if ($rgExit -ne 0) { throw "COVERAGE.md is missing fixed-string assertion: 03-01" }; rg -F -q -- "03-02" .planning/phases/03-hybrid-retrieval-basic-rag-path/COVERAGE.md; $rgExit = $LASTEXITCODE; if ($rgExit -ne 0) { throw "COVERAGE.md is missing fixed-string assertion: 03-02" }; rg -F -q -- "03-03" .planning/phases/03-hybrid-retrieval-basic-rag-path/COVERAGE.md; $rgExit = $LASTEXITCODE; if ($rgExit -ne 0) { throw "COVERAGE.md is missing fixed-string assertion: 03-03" }; rg -F -q -- "03-04" .planning/phases/03-hybrid-retrieval-basic-rag-path/COVERAGE.md; $rgExit = $LASTEXITCODE; if ($rgExit -ne 0) { throw "COVERAGE.md is missing fixed-string assertion: 03-04" }; rg -F -q -- "03-05" .planning/phases/03-hybrid-retrieval-basic-rag-path/COVERAGE.md; $rgExit = $LASTEXITCODE; if ($rgExit -ne 0) { throw "COVERAGE.md is missing fixed-string assertion: 03-05" }; rg -F -q -- "TestRAGQueryCrossRuntime" .planning/phases/03-hybrid-retrieval-basic-rag-path/COVERAGE.md; $rgExit = $LASTEXITCODE; if ($rgExit -ne 0) { throw "COVERAGE.md is missing fixed-string assertion: TestRAGQueryCrossRuntime" }; rg -F -q -- "supported_parameters" .planning/phases/03-hybrid-retrieval-basic-rag-path/COVERAGE.md; $rgExit = $LASTEXITCODE; if ($rgExit -ne 0) { throw "COVERAGE.md is missing fixed-string assertion: supported_parameters" }; rg -F -q -- "DEBT-RAG-04" .planning/phases/03-hybrid-retrieval-basic-rag-path/COVERAGE.md; $rgExit = $LASTEXITCODE; if ($rgExit -ne 0) { throw "COVERAGE.md is missing fixed-string assertion: DEBT-RAG-04" }; rg -F -q -- "DEBT-RAG-05" .planning/phases/03-hybrid-retrieval-basic-rag-path/COVERAGE.md; $rgExit = $LASTEXITCODE; if ($rgExit -ne 0) { throw "COVERAGE.md is missing fixed-string assertion: DEBT-RAG-05" }; rg -F -q -- "Deferred boundary" .planning/phases/03-hybrid-retrieval-basic-rag-path/COVERAGE.md; $rgExit = $LASTEXITCODE; if ($rgExit -ne 0) { throw "COVERAGE.md is missing fixed-string assertion: Deferred boundary" }; rg -F -q -- "Rationale" .planning/phases/03-hybrid-retrieval-basic-rag-path/COVERAGE.md; $rgExit = $LASTEXITCODE; if ($rgExit -ne 0) { throw "COVERAGE.md is missing fixed-string assertion: Rationale" }; rg -F -q -- "Trigger" .planning/phases/03-hybrid-retrieval-basic-rag-path/COVERAGE.md; $rgExit = $LASTEXITCODE; if ($rgExit -ne 0) { throw "COVERAGE.md is missing fixed-string assertion: Trigger" }; rg -F -q -- "Target" .planning/phases/03-hybrid-retrieval-basic-rag-path/COVERAGE.md; $rgExit = $LASTEXITCODE; if ($rgExit -ne 0) { throw "COVERAGE.md is missing fixed-string assertion: Target" }; rg -F -q -- "Future acceptance criteria" .planning/phases/03-hybrid-retrieval-basic-rag-path/COVERAGE.md; $rgExit = $LASTEXITCODE; if ($rgExit -ne 0) { throw "COVERAGE.md is missing fixed-string assertion: Future acceptance criteria" }; rg -F -q -- "manual" .planning/phases/03-hybrid-retrieval-basic-rag-path/COVERAGE.md; $rgExit = $LASTEXITCODE; if ($rgExit -ne 0) { throw "COVERAGE.md is missing fixed-string assertion: manual" }; git diff --check -- .planning/phases/03-hybrid-retrieval-basic-rag-path/COVERAGE.md; $diffExit = $LASTEXITCODE; if ($diffExit -ne 0) { exit $diffExit }</automated>
  </verify>
  <acceptance_criteria>
    - Every current RAG-02/RAG-04 acceptance surface has one current plan/task owner in the coverage matrix, while RAG-03 is explicitly marked deferred to Phase 06.
    - The cross-runtime row names the embedding, metadata, and chat mock responsibilities plus isolated LanceDB seeding and cleanup.
    - Manual live-provider verification and accepted deferred debt are visibly separate from automated coverage.
  </acceptance_criteria>
  <done>COVERAGE.md describes the revised five-plan evidence route and does not overclaim deferred or live-provider behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process -> local provider mock | The cross-runtime test sends query text, model metadata requests, and structured generation requests to a test-owned localhost server. |
| Test process -> Rust engine child | Test-controlled configuration and a temporary LanceDB path cross into the real engine process. |
| Go gateway -> Rust gRPC | The HTTP request and typed filters cross the public service boundary into the Rust semantic owner. |
| Fixture seeder -> LanceDB | Deterministic test data is written to a temporary store that must not overlap a developer or production corpus. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05-01 | Tampering | Local provider mock and structured response | high | mitigate | Assert exact endpoint, model, strict schema, evidence IDs, and one completion; reject unexpected paths or malformed response shapes. |
| T-03-05-02 | Information Disclosure | Test environment and provider credential | high | mitigate | Use a test-only credential, localhost endpoints, temporary paths, and assertions that logs/responses contain neither real keys nor raw corpus dumps. |
| T-03-05-03 | Denial of Service | Engine and mock child processes | medium | mitigate | Bound the test request, use context cancellation/timeouts, wait for child exit, and run each store under `t.TempDir`. |
| T-03-05-04 | Tampering | LanceDB fixture isolation | high | mitigate | Seed only an explicit per-test temporary path, use canonical schema validation, close handles before cleanup, and never point the smoke at configured shared data. |
| T-03-05-SC | Tampering | cargo/go dependency installs | high | mitigate | Run locked builds and retain the approved package-legitimacy audit; no new unreviewed package is introduced by this plan. |
</threat_model>

<verification>
- Execute the task-level PowerShell wrappers; Task 1 preflights TestRAGQueryCrossRuntime, captures the Cargo/Go exit codes immediately, and rejects a zero-test filtered run, while Task 2 checks every coverage/debt literal independently.
- Confirm the local smoke uses all three deterministic mock contracts and no live provider or PostgreSQL service.
- Confirm the optional ignored OpenRouter smoke remains a separate manual check.
</verification>

<success_criteria>
The complete local happy path returns a grounded structured RAG response through the real Go and Rust processes using an isolated seeded LanceDB fixture and deterministic embedding/metadata/chat mocks, while coverage documentation clearly separates automated MVP proof from manual provider checks and accepted debt.
</success_criteria>

<output>
Create `.planning/phases/03-hybrid-retrieval-basic-rag-path/03-05-SUMMARY.md` when done
</output>
