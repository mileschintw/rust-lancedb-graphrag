---
phase: 03-hybrid-retrieval-basic-rag-path
plan: 16
type: execute
wave: 14
depends_on:
  - "03-15"
files_modified:
  - engine/src/generation/mod.rs
  - engine/src/generation/openrouter.rs
  - engine/src/generation/tests.rs
  - engine/src/main.rs
  - engine/tests/config_startup.rs
autonomous: true
gap_closure: true
requirements:
  - RAG-02
must_haves:
  truths:
    - "G1: one GroundingLimits carrier derived from EffectiveRagSettings governs evidence packing, provider max output, derived total usage, shared grounding validation, and OpenRouter usage validation."
    - "G1: evidence budget 16,384 tokens, output budget 4,096 tokens, and derived total ceiling 20,480 tokens are enforced before readiness and during provider response validation."
    - "G1: usage inside a configured non-default limit is accepted even when it exceeds the former 8,192 or 2,048 defaults, while usage above any effective limit is rejected."
    - "RAG-02 remains grounded on the existing provider-neutral path; RAG-03 degraded/model-only product behavior is not claimed."
  artifacts:
    - path: "engine/src/generation/mod.rs"
      provides: "Shared GroundingLimits carrier and usage validation used by ModelOutput"
      contains: "GroundingLimits"
    - path: "engine/src/generation/openrouter.rs"
      provides: "OpenRouter adapter configuration and response checks using the shared carrier"
      contains: "grounding_limits"
    - path: "engine/src/main.rs"
      provides: "EffectiveRagSettings construction and startup wiring for service-safe limits"
      contains: "grounding_limits"
    - path: "engine/tests/config_startup.rs"
      provides: "Readiness regression for configurations above service ceilings"
      contains: "service_ceiling"
  key_links:
    - from: "EffectiveRagSettings::try_from_settings"
      to: "generation::GroundingLimits"
      via: "one validated carrier is built before database/provider construction"
    - from: "OpenRouterGenerator::execute_one_call"
      to: "ModelOutput::validate_grounding"
      via: "both receive the same carrier and shared usage rules"
    - from: "config startup"
      to: "readiness signal"
      via: "above-ceiling settings fail before the engine advertises service readiness"
---

<objective>
Close the G1 provider-usage gap from ADR-03-001 by making effective provider limits authoritative across the complete production generation path. Pin service-safe ceilings at 16,384 evidence tokens, 4,096 output tokens, and 20,480 total tokens, and reject configurations above those ceilings before readiness.

Purpose: A configured budget must be a real control plane for RAG-02 rather than a value used only while constructing the prompt.
Output: A shared limits carrier, production adapter wiring, non-default usage regressions, and startup ceiling coverage with no unresolved planning values.
</objective>

<execution_context>
@D:/Repos/lancet/.agents/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-VERIFICATION.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-AI-SPEC.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-13-SUMMARY.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-14-SUMMARY.md
@engine/src/generation/mod.rs
@engine/src/generation/openrouter.rs
@engine/src/main.rs
</context>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Valid hybrid retrieval produces a grounded structured answer | 03-01..03-18 | COVERED | Existing plans prove retrieval/generation; this gap plan restores configurable usage truthfulness. |
| REQ | RAG-02 | Dense plus BM25 retrieval, filtering, fusion, deduplication, and grounded answer path | 03-16..03-18 | COVERED | G1 closes the failed provider-budget must-have; D1/D2 preserve the truthful boundary. |
| REQ | RAG-04 | Async reranker port and NoOp pass-through | 03-18 | COVERED | Final locked Rust gate is required and no gap task changes the reranker contract. |
| RESEARCH | — | Effective settings must drive prompt, provider request, and usage acceptance | 03-16 | COVERED | One carrier is required at every consumer. |
| RESEARCH | — | Service-safe resource ceilings must be validated before readiness | 03-16 | COVERED | Concrete ceilings are pinned in this objective and task actions. |
| CONTEXT | D-28, D-30, D-33, D-39 | Strict one-shot generation, timeout, output budget, and reserved evidence budget | 03-16 | COVERED | Existing behavior remains; usage validation now uses effective values. |
| ADR | G1 | Effective usage validation plus service-safe ceilings | 03-16 | COVERED | Accepted ADR decision implemented exactly with concrete numbers. |
| ADR | D1 | Fail-closed embedding/dense errors and identity | 03-17 | COVERED | Retrieval infrastructure disposition is implemented in the dependent plan. |
| ADR | D2 | Valid zero-match success-empty wire shape | 03-18 | COVERED | Provider-free no-results response is implemented in the dependent plan. |
| CONTEXT | D3, D4, D5 | Deferred citation, lifecycle, and graph boundaries | 03-18 | COVERED | The gap set records these boundaries without implementing them. |
| CONTEXT | D-01..D-57 other than the rows above | Existing Phase 03 decisions | 03-01..03-15 | COVERED | Existing executed plans remain the implementation source for the rest of the canonical context. |
</source_audit>

<tasks>

<task type="tracer">
  <name>Task 1: Trace EffectiveRagSettings limits through provider usage validation</name>
  <reversibility rating="costly">The shared validation signature and adapter configuration are consumed by the production binary and focused tests, so changing them later requires coordinated call-site updates but no data migration.</reversibility>
  <read_first>
    - rust-guidelines.md
    - engine/src/generation/mod.rs: ModelUsage, ModelOutput::validate_grounding, and existing budget constants
    - engine/src/generation/openrouter.rs: OpenRouterGenerationConfig and execute_one_call
    - engine/src/main.rs: EffectiveRagSettings::try_from_settings, EffectiveRagSettings::validate, query_rag, and generation startup construction
    - .planning/phases/03-hybrid-retrieval-basic-rag-path/03-13-SUMMARY.md and 03-14-SUMMARY.md
  </read_first>
  <files>engine/src/generation/mod.rs, engine/src/generation/openrouter.rs, engine/src/generation/tests.rs, engine/src/main.rs</files>
  <action>
    Implement G1 and the existing generation decisions D-28, D-30, D-33, and D-39 with one shared generation::GroundingLimits value. Define exact service ceilings of evidence_token_budget 16,384, max_output_tokens 4,096, and derived total_usage 20,480; derive the total with checked addition and reject zero or above-ceiling values. Build the carrier once from EffectiveRagSettings during try_from_settings, before database, BM25, embedding, or generator construction.

    Change ModelOutput::validate_grounding and its usage helper to accept the carrier. Make OpenRouterGenerationConfig retain that carrier and use it for prompt packing, the outbound maximum completion value, response usage checks, and the final grounding validation. Keep legacy constructors source-compatible by delegating to the same carrier construction, while ensuring runtime usage checks no longer read default-only constants. Update the production query path to use the carrier for evidence and output conversions instead of duplicating independent budget values.

    Add focused local-mock adapter regressions: a configured 10,000-token evidence budget and 3,000-token output budget must accept provider usage of 9,000 prompt tokens plus 2,500 completion tokens and total 11,500, while a response exceeding any effective component or derived total must fail with schema validation before a public grounded result can be assembled. Do not add retries, fallback providers, or model-only continuation.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked openrouter_effective_usage_limits -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - GroundingLimits is the single production usage carrier with evidence 16,384, output 4,096, and total 20,480 ceilings.
    - The same effective carrier reaches prompt packing, OpenRouter request construction, ModelOutput::validate_grounding, and OpenRouter response usage validation.
    - The non-default in-limit mock response succeeds and the over-limit mock response fails without a grounded success body.
  </acceptance_criteria>
  <done>G1's effective provider usage path is wired end to end and the focused adapter regressions pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove service ceilings reject readiness-invalid configuration</name>
  <read_first>
    - rust-guidelines.md
    - engine/tests/config_startup.rs: process spawning, environment overrides, readiness observation, and cleanup helpers
    - engine/src/main.rs: Settings defaults and EffectiveRagSettings validation errors
    - config/config.example.toml: committed operator configuration shape
  </read_first>
  <files>engine/tests/config_startup.rs</files>
  <behavior>
    - A configuration with evidence_token_budget 16,385 exits before readiness and does not open its configured loopback listener.
    - A configuration with max_output_tokens 4,097 exits before readiness and does not open its configured loopback listener.
    - Defaults and values at the exact 16,384 / 4,096 ceilings remain accepted by the settings validator.
  </behavior>
  <action>
    Add process-level startup regressions for G1 that use isolated temporary config and LanceDB paths, set OPENROUTER_API_KEY=test-key, and assert non-readiness plus non-listening for each above-ceiling override. Keep the existing readiness log as only a milestone and preserve the current child cleanup pattern. Add a direct boundary assertion for the exact ceiling values so the tests prove the inclusive contract rather than only testing rejection.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked --test config_startup service_ceiling -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Settings above either service ceiling fail closed before the engine readiness signal.
    - Exact ceiling values are accepted, and the derived total is never above 20,480.
    - Startup tests use isolated paths and leave no child process or fixture behind.
  </acceptance_criteria>
  <done>G1 service-safe ceiling enforcement is executable at the startup boundary and has no unresolved numeric values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| operator configuration -> Rust settings | Untrusted deployment values control prompt and provider resource budgets. |
| OpenRouter response -> Rust generation validator | Provider-reported token usage is untrusted input crossing into the grounded response path. |
| Rust settings/adapter -> provider request | Effective limits determine the amount of evidence and output requested from an external provider. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-16-01 | Denial of Service | EffectiveRagSettings budget validation | high | mitigate | Reject evidence values above 16,384, output values above 4,096, and derived totals above 20,480 before readiness. |
| T-03-16-02 | Tampering | OpenRouter usage and grounding validation | high | mitigate | Validate provider prompt, completion, total, and arithmetic consistency with the shared effective carrier before response assembly. |
| T-03-16-03 | Information Disclosure | generation request construction | medium | mitigate | Preserve existing credential/prompt redaction rules and keep the limits carrier free of secrets or source content. |
| T-03-16-SC | Tampering | package-manager installs | low | accept | No package-manager install or dependency change is part of this plan; existing locked dependencies are reused. |
</threat_model>

<verification>
- cargo test --manifest-path engine/Cargo.toml --locked openrouter_effective_usage_limits -- --test-threads=1
- cargo test --manifest-path engine/Cargo.toml --locked --test config_startup service_ceiling -- --test-threads=1
- cargo test --manifest-path engine/Cargo.toml --locked
</verification>

<success_criteria>
G1 is closed when non-default in-limit provider usage succeeds, effective-limit violations fail closed, values above 16,384 / 4,096 / 20,480 prevent readiness, and the locked Rust suite remains green.
</success_criteria>

<output>
Create .planning/phases/03-hybrid-retrieval-basic-rag-path/03-16-SUMMARY.md when done.
</output>
