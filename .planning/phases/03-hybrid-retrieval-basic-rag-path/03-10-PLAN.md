---
phase: 03-hybrid-retrieval-basic-rag-path
plan: "10"
type: execute
wave: 8
depends_on:
  - 03-07
files_modified:
  - engine/src/client/mod.rs
  - engine/src/client/tests.rs
  - engine/src/generation/openrouter.rs
  - engine/src/generation/tests.rs
autonomous: true
requirements:
  - RAG-02
gap_closure: true
assumption_delta: false
must_haves:
  truths:
    - "D-25 / D-27: a non-default configured embedding model appears in the provider request and model_id reports the same persistence/snapshot identity."
    - "D-27 / D-30 / D-32 / D-33: the configured generation model, endpoints, common timeout, sampling, and max completion tokens govern the strict OpenRouter request."
  artifacts:
    - path: engine/src/client/mod.rs
      provides: "OpenRouterEmbeddingConfig and OpenRouterClient::model_id"
    - path: engine/src/client/tests.rs
      provides: "Embedding model/endpoint request-capture and provider identity regressions"
    - path: engine/src/generation/openrouter.rs
      provides: "OpenRouterGenerationConfig with request and dual-timeout controls"
    - path: engine/src/generation/tests.rs
      provides: "Generation model/endpoint/timeout/sampling/output request-capture regressions"
  key_links:
    - from: engine/src/client/mod.rs
      to: engine/src/client/tests.rs
      via: "configuration-based embedding constructor is proven against a local HTTP request capture"
    - from: engine/src/generation/openrouter.rs
      to: engine/src/generation/tests.rs
      via: "configuration-based generator is proven against strict request and timeout captures"
  prohibitions:
    - "Do not add retries, fallback providers, tools, streaming, or alternate generation calls; D-29 remains one attempt."
    - "Do not retain compile-time model, sampling, output, or timeout constants as active request values after a configured constructor is used."
    - "Do not introduce a package dependency or expose credentials through provider requests or error diagnostics."
---

## Phase Goal

**As a** chat service API user, **I want to** ask a question over indexed content and receive a grounded answer with trustworthy citations, **so that** I can verify every claim against the retrieved evidence.

<objective>
Turn Plan 03-07 validated provider settings into configuration-based embedding and generation adapters with exact request-capture tests per D-25, D-27, D-30, D-32, and D-33.

Purpose: The adapters must prove they use operator-selected identities, bounds, and timeouts before startup wires them in Plan 03-11.
Output: Configurable provider constructors, model identity reporting, and focused local mock tests.
</objective>

<scope_note>
This plan owns provider adapter behavior. Plan `03-11` owns production startup injection and the binary-target configuration-example contract. D-29 remains exactly one generation attempt; fallback/retry expansion and alternate providers remain deferred. All constructor choices are reversible and preserve the provider-neutral traits.
</scope_note>

<spec_edge_resolution>
No Phase 03 SPEC exists. The provider-configuration edge is resolved concretely: embedding and generation request values are immutable constructor state; embedding model_id is the persistence/snapshot identity; generation uses one configured timeout at both reqwest and Tokio layers; request captures prove every non-default value.
</spec_edge_resolution>

## Artifacts this phase produces

- `engine/src/client/mod.rs`
  - Creates `OpenRouterEmbeddingConfig` and `OpenRouterClient::model_id`.
  - Changes `OpenRouterClient`, `OpenRouterClient::new_with_endpoint`, `OpenRouterClient::new`, `OpenRouterClient::from_env`, `OpenRouterClient::from_env_with_endpoint`, `build_http_client`, `EmbeddingRequest`, `OpenRouterClient::send_embedding`, and the private test constructor so configured model/endpoint/timeout/retry/concurrency/dimension values are immutable request state.
- `engine/src/client/tests.rs`
  - Creates `embedding_request_uses_effective_model` and `embedding_config_preserves_bounds_and_redaction`.
- `engine/src/generation/openrouter.rs`
  - Creates `OpenRouterGenerationConfig`.
  - Changes `OpenRouterGenerator`, `OpenRouterGenerator::new`, `OpenRouterGenerator::from_env`, `OpenRouterGenerator::with_endpoints`, `OpenRouterGenerator::execute_one_call`, and the `Generator for OpenRouterGenerator::generate` implementation so configured model/endpoints/timeout/sampling/output bounds drive the strict request.
- `engine/src/generation/tests.rs`
  - Creates `generation_request_uses_effective_settings` and `generation_timeout_uses_one_effective_value`.
- `.planning/phases/03-hybrid-retrieval-basic-rag-path/03-10-SUMMARY.md`
  - Execution summary recording provider request-capture evidence.

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@rust-guidelines.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-CONTEXT.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-VERIFICATION.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-REVIEW.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-AI-SPEC.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-06-SUMMARY.md
@.planning/phases/03-hybrid-retrieval-basic-rag-path/03-07-SUMMARY.md

<interfaces>
- OpenRouterEmbeddingConfig consumes the validated embedding model/endpoint from EffectiveRagSettings and retains the established timeout/retry/concurrency/dimension bounds as explicit constructor defaults.
- OpenRouterClient::model_id returns the configured identity; Plan 03-11 exposes it through the service-local EmbeddingProvider trait with a compatibility default for test doubles.
- OpenRouterGenerationConfig consumes validated model, metadata/chat endpoints, timeout, temperature, top_p, and max_completion_tokens.
- Preserve the Plan 03-06 strict json_schema request, finish_reason validation, and provider-neutral Generator trait.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Send one configured embedding request with matching model identity</name>
  <files>engine/src/client/mod.rs, engine/src/client/tests.rs</files>
  <read_first>
    - AGENTS.md and rust-guidelines.md for repository and Rust constraints.
    - engine/src/client/mod.rs: EmbeddingProvider, OpenRouter DTO, EMBEDDING_MODEL, endpoint/timeout/retry/concurrency constants, dimension validation, and constructors.
    - engine/src/client/tests.rs: local HTTP mock, endpoint override, retry, dimension, and credential-redaction assertions.
    - engine/src/main.rs after Plan 03-07: validated embedding configuration fields and required constructor inputs.
  </read_first>
  <behavior>
    - "embedding_request_uses_effective_model: a non-default configured embedding model appears in the local HTTP body and model_id returns the same identity."
    - "embedding_config_preserves_bounds_and_redaction: configured endpoint/timeout/retry/concurrency/dimension controls remain bounded and provider errors never expose credentials."
  </behavior>
  <action>
Write the model-identity request capture first. Create OpenRouterEmbeddingConfig from the exact validated fields exposed by Plan 03-07 and add a constructor that makes model, endpoint, timeout, retry count, concurrency, and expected dimension immutable provider state. Change EmbeddingRequest to serialize the configured model rather than EMBEDDING_MODEL. Add OpenRouterClient::model_id returning the same configured identity used by the HTTP body; Plan 03-11 will surface that inherent method through the service-local EmbeddingProvider trait. Per D-25 and D-27, prove request and model_id are identical. Preserve credential checks/redaction, bounded retries/concurrency, dimension enforcement, and existing constructor compatibility; do not add dependencies.
  </action>
  <acceptance_criteria>
    - "D-25/D-27: one non-default model identity is identical in request capture and model_id."
    - "Configured endpoint and client bounds are active without weakening retries, concurrency, dimension validation, or redaction."
    - "Existing OpenRouterClient construction paths remain source-compatible or receive explicit equivalent configuration."
  </acceptance_criteria>
  <verify>
    <automated>$listed = cargo test --manifest-path engine/Cargo.toml -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; @('embedding_request_uses_effective_model','embedding_config_preserves_bounds_and_redaction') | ForEach-Object { if ($listed -notmatch [regex]::Escape($_)) { throw "missing focused test: $_" } }; cargo test --manifest-path engine/Cargo.toml embedding_request_uses_effective_model; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }; cargo test --manifest-path engine/Cargo.toml embedding_config_preserves_bounds_and_redaction; exit $LASTEXITCODE</automated>
  </verify>
  <done>A local provider capture proves the configured embedding identity and client bounds govern the real request without leaking credentials.</done>
  <reversibility rating="reversible">The configuration constructor extends the existing provider-neutral interface without data or wire migration.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Send one strict generation request with every configured bound</name>
  <files>engine/src/generation/openrouter.rs, engine/src/generation/tests.rs</files>
  <read_first>
    - AGENTS.md and rust-guidelines.md for repository and Rust constraints.
    - engine/src/generation/openrouter.rs after Plan 03-06: strict request/response DTOs, generator constructor, endpoint mutator, reqwest timeout, and Tokio timeout.
    - engine/src/generation/tests.rs after Plan 03-06: strict JSON Schema, finish_reason, one-call, cancellation, and local mock assertions.
    - engine/src/main.rs after Plan 03-07: validated generation configuration fields.
  </read_first>
  <behavior>
    - "generation_request_uses_effective_settings: non-default model, endpoint, temperature, top_p, and max_completion_tokens appear in the strict request body."
    - "generation_timeout_uses_one_effective_value: the same configured duration bounds reqwest construction and Tokio cancellation behavior."
  </behavior>
  <action>
Write the non-default strict request capture first. Create OpenRouterGenerationConfig from the exact validated Plan 03-07 fields and make the generator constructor retain model, metadata/chat endpoints, timeout, temperature, top_p, and max_completion_tokens. Per D-27, D-30, D-32, and D-33, source every outgoing request value from that configuration, apply one timeout duration to both reqwest and Tokio enforcement, and keep cancellation propagation. Preserve the Plan 03-06 strict json_schema structure and finish_reason/semantic validation. Preserve D-29 one attempt with no retry, fallback, classifier, tool, or alternate-provider call. Prove non-default finite values in the local request capture and a bounded timeout case.
  </action>
  <acceptance_criteria>
    - "D-27/D-32/D-33: captured request contains the configured model, sampling, and max_completion_tokens together with the strict schema."
    - "D-30: the configured timeout governs both client and async cancellation layers."
    - "D-29: the failure case makes one generation attempt and returns the typed error."
  </acceptance_criteria>
  <verify>
    <automated>$listed = cargo test --manifest-path engine/Cargo.toml -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; @('generation_request_uses_effective_settings','generation_timeout_uses_one_effective_value') | ForEach-Object { if ($listed -notmatch [regex]::Escape($_)) { throw "missing focused test: $_" } }; cargo test --manifest-path engine/Cargo.toml generation_request_uses_effective_settings; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }; cargo test --manifest-path engine/Cargo.toml generation_timeout_uses_one_effective_value; exit $LASTEXITCODE</automated>
  </verify>
  <done>The strict OpenRouter adapter demonstrably uses every configured request and timeout value while retaining one-attempt fail-closed behavior.</done>
  <reversibility rating="reversible">The adapter constructor remains behind the provider-neutral Generator trait.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validated settings to provider adapters | Model identities, endpoints, limits, and timeouts become outbound client state. |
| engine to external HTTP | Provider requests leave the process with credentials and corpus-derived prompt data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10-01 | Spoofing | provider model identity | high | mitigate | Use one configured embedding identity for request and model_id; capture it in tests. |
| T-03-10-02 | Denial of Service | provider timeouts and limits | high | mitigate | Apply validated finite limits and one timeout to both client and async layers. |
| T-03-10-03 | Information Disclosure | provider credentials/errors | high | mitigate | Retain request and error redaction tests so configured credentials cannot escape. |
| T-03-10-04 | Tampering | generation request shape | high | mitigate | Preserve strict JSON Schema and capture every configured non-default field locally. |
| T-03-10-SC | Tampering | dependency manifests | low | accept | No package installation or dependency-manifest change is permitted. |
</threat_model>

<verification>
Run all focused task commands, then run `$env:GOTELEMETRY='off'; cargo test --manifest-path engine/Cargo.toml client; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; cargo test --manifest-path engine/Cargo.toml generation; exit $LASTEXITCODE`. Confirm Cargo manifests, protobuf, and deferred ledger are unchanged.
</verification>

<success_criteria>
- Embedding request and model_id use the same configured non-default identity.
- Generation request and timeout layers use every configured non-default value while preserving strict output validation.
- Provider errors retain credential redaction and one-attempt policy.
</success_criteria>

<output>
Create `.planning/phases/03-hybrid-retrieval-basic-rag-path/03-10-SUMMARY.md` when done.
</output>
