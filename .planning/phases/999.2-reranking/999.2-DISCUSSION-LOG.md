# Phase 999.2: Reranking - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-18
**Phase:** 999.2-reranking
**Areas discussed:** Model Selection, Integration Architecture & Traits, Scoring & Candidate Selection, Fallback & Degraded Modes

---

## Model Selection & Traits

| Option | Description | Selected |
|--------|-------------|----------|
| Dual-Implementation | (Recommended) Define a clean abstraction trait in Rust, support both an external API (e.g. Cohere) and a local model (ONNX Runtime), defaulting to a Mock/NoOp in v1 | ✓ |
| Strictly local ONNX Runtime / Candle | Maximizes local-first systems alignment, but requires adding C-bindings and model file downloads to the repo | |
| Strictly external API | Simplest to implement, but introduces a runtime network dependency and requires API keys | |
| You decide | Agent will choose the best fit | |

**User's choice:** Dual-Implementation (D-01), Async Trait (D-02), Configuration-driven budgets (D-03), Soft Fallback (D-04).
**Notes:** Decided to write an async trait in Rust that will be a pass-through NoOp in v1. This leaves a clean port for v2 without compilation bloat in v1.

---

## Agent Discretion

- **Discretion-01:** The agent has discretion over the exact error types in `RerankError` and how the local ONNX runtime thread pool is configured.

## Deferred Ideas
- None.
