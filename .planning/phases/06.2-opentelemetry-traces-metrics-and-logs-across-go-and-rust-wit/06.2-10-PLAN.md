---
phase: 06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit
plan: 10
type: execute
wave: 8
depends_on:
  - 06.2-03
files_modified:
  - deploy/grafana/provisioning/datasources/datasources.yaml
  - engine/src/tests/telemetry_metrics.rs
  - scripts/engine-test-targets.sh
  - .planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-VALIDATION.md
autonomous: true
gap_closure: true
gap_ids:
  - G-06.2-1
requirements:
  - OBS-01
user_setup:
  - service: docker-compose observability profile (Grafana + Jaeger + Loki)
    why: "Whether Grafana renders the 'Logs for this span' action is browser UI behavior. The config assertion and the datasource-API poll prove the provisioned state; only a human click-through closes G-06.2-1 (UAT Test 1)."
    dashboard_config:
      - task: "Reprovision Grafana after the datasource edit: `docker compose --profile observability up -d --force-recreate grafana` (repo root, Git Bash)."
        location: "repo root — Git Bash"
      - task: "Issue one /rag/query, open Grafana → Explore → Jaeger, select a gateway span, confirm the 'Logs for this span' action now renders and lands on Loki filtered to that trace ID; then follow a Loki line's derived TraceID field back to Jaeger."
        location: "http://127.0.0.1:3000 (admin:admin)"
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "`tracesToLogsV2` on the provisioned Jaeger datasource declares no `tags` list; trace ID is intrinsic OTel span context and is resolved by `filterByTraceID: true` alone (G-06.2-1, D-34, D-40)."
    - "`filterByTraceID: true` and `filterBySpanID: false` are retained verbatim; the Loki→Jaeger `derivedFields` direction is untouched, so both halves of the round trip stay configured (D-40)."
    - "The correlation configuration remains fully file-provisioned — the fix is a committed-file edit plus a container reprovision, never a Grafana UI change (D-40: no manual UI state anywhere)."
    - "A committed Rust test asserts the shipped `datasources.yaml` shape, so a future edit that re-adds an unresolvable span-tag mapping fails the suite rather than silently suppressing the link again."
    - "`scripts/engine-test-targets.sh` is updated from a fresh `cargo test -- --list` measurement covering every assertion the new lib test moves, not from arithmetic."
    - "The live Grafana datasource poll inspects `jsonData.tracesToLogsV2` as parsed JSON, not a whole-response substring match on the word tags, so omitted fields serialized as empty or null arrays cannot false-fail the poll (06.2-REVIEWS.md Cursor HIGH)."
    - "Compose volume destruction (`docker compose --profile observability down -v`) runs only after structured evidence that `jsonData.tracesToLogsV2` still contains the invalid `key: trace_id` mapping; `--force-recreate grafana` is the default recovery (06.2-REVIEWS.md Cursor HIGH)."
    - statement: "OBS-01 specless-probe edge remains unclassified/unresolved at whole-requirement level (`coverage: {applicable: 1, resolved: 0, unresolved: 1}`); this plan does not force-resolve it."
      verification: backstop
  flagged_assumptions:
    - "OBS-01 specless edge-probe returned `category: unclassified`, `status: unresolved` (`coverage: {applicable: 1, resolved: 0, unresolved: 1}`). Carried forward, not force-resolved. Reasoning: OBS-01 at whole-requirement level is already covered by executed plans 01-09; this plan closes a specific defect inside that existing coverage rather than adding new OBS-01 surface, so the probe's unclassified verdict does not map onto new work here. 06.2-VALIDATION.md's Per-Requirement Verification Map is the concrete acceptance surface standing in for it."
    - "06.2-03-PLAN.md flagged assumption A2 predicted exactly this failure: 'Provisioning can load partially and silently omit links, which is why the correlation click-through is a human-check item rather than a config-file assertion.' This plan resolves the A2 assumption in the failing direction and adds the config-file assertion A2 said could not stand alone — the human click-through remains the closing evidence, not a substitute for it."
    - "ASSUMED: Grafana 11.5.2 updates an already-provisioned datasource's `jsonData` in place on restart when a key is REMOVED from the provisioning file. If, after `--force-recreate grafana`, parsed `jsonData.tracesToLogsV2` still contains a mapping whose key is `trace_id`, the last-resort fallback is `docker compose --profile observability down -v` then `up -d`; record which path was needed and the structured JSON that justified `-v`. Empty or null `tags` arrays are not that evidence (06.2-REVIEWS.md Cursor HIGH)."
  prohibitions:
    - statement: "Must not fix the trace-to-logs link by editing the datasource in the Grafana UI, or by any step that leaves live correlation state outside the committed provisioning files (D-40)."
      status: unverified
      flagged: true
    - statement: "Must not make the link resolve by broadening the Loki query beyond the single trace, which would surface unrelated requests' log lines inside a correlation pane presented as one request's logs."
      status: unverified
      flagged: true
    - statement: "Must not alter the Loki `derivedFields` (Loki→Jaeger) direction while fixing the Jaeger→Loki direction, leaving one half of the round trip broken behind a fix that appears complete."
      status: unverified
      flagged: true
    - statement: "Must not add alert rules or recording rules to any provisioning file while editing this directory (D-40 forbids both)."
      status: unverified
      flagged: true
    - statement: "Must not treat the Rust config-shape test, the datasource-API poll, or any other automated gate as proof that Grafana renders the Logs-for-this-span action; that UI action is UAT Test 1 and stays a human-check item."
      status: unverified
      flagged: true
    - statement: "Must not run `docker compose --profile observability down -v` because a whole-response body contains the four-character JSON key for tags, including empty or null arrays Grafana may emit when the mapping is already gone."
      status: unverified
      flagged: true
  artifacts:
    - path: "deploy/grafana/provisioning/datasources/datasources.yaml"
      provides: "Jaeger `tracesToLogsV2` with no span-tag mapping and `filterByTraceID: true` retained."
    - path: "engine/src/tests/telemetry_metrics.rs"
      provides: "`grafana_traces_to_logs_maps_no_span_tag` — committed regression assertion on the provisioned correlation shape."
    - path: "scripts/engine-test-targets.sh"
      provides: "Engine test-count pins re-measured for the added lib test."
    - path: ".planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-VALIDATION.md"
      provides: "Per-Requirement Verification Map row for D-34/D-40 correlation updated in place; Manual-Only Grafana click-through Re-run owner retargeted to 06.2-10/T2."
  key_links:
    - from: "deploy/grafana/provisioning/datasources/datasources.yaml"
      to: "engine/src/tests/telemetry_metrics.rs"
      via: "committed provisioning file is asserted by a lib test that scans the Jaeger jsonData block"
      pattern: "grafana_as_code"
---

## Phase User Story

**As an** operator looking at a slow `/rag/query` trace in Grafana, **I want to** click "Logs for this span" and land on that trace's Loki lines, **so that** trace, log and metric panes correlate on one `trace_id` without hand-copying IDs (G-06.2-1, D-34, D-40).

<objective>
Close G-06.2-1: remove the unresolvable `tags: [{key: trace_id, value: trace_id}]` mapping from the Jaeger datasource's `tracesToLogsV2` block so Grafana stops suppressing the trace-to-logs action, and lock the corrected shape with a committed assertion.

Purpose: trace ID is intrinsic OTel span context (`SpanContext.trace_id`), not a span attribute. Grafana evaluates `tags` against span attributes and hides the "Logs for this span" action when a configured tag variable cannot be interpolated. `filterByTraceID: true` is the built-in mechanism that queries Loki by trace ID with no `tags` entry at all — so the mapping was not merely redundant, it was the suppressor.
Output: a three-line deletion in one provisioning file, one Rust regression test, a re-measured engine pin, and one VALIDATION.md map row updated in place.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-observability-evaluation-polish/06-CONTEXT.md
@.planning/debug/grafana-trace-log-correlation.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-UAT.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-VALIDATION.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-03-PLAN.md
@rust-guidelines.md
@deploy/grafana/provisioning/datasources/datasources.yaml
@engine/src/tests/telemetry_metrics.rs
@scripts/engine-test-targets.sh
</context>

<scope_rationale>
Smallest possible surface for a `major` UAT gap: one YAML block, one test, one pin, one map row. No Grafana version bump, no datasource restructuring, no change to the Loki side.

**Serialization (load-bearing).** This plan is wave 8; 06.2-11 is wave 9 and 06.2-12 is wave 10. They are NOT file-disjoint: all three modify `engine/src/tests/telemetry_metrics.rs`, `scripts/engine-test-targets.sh`, and `06.2-VALIDATION.md`. Under execute-phase, same-wave plans run in parallel, which on these three shared files risks a lost edit or a merge conflict, and — worse — would make each plan's test-count pin bump land as an isolated `+1` from the pre-gap-closure baseline instead of the correct cumulative total. Serializing per `planner-gap-closure.md` §7 (a plan depending on another gap-closure plan takes `max(dependency waves) + 1`) makes each plan measure the count its predecessor actually left behind. This plan is the head of that chain and sets the first cumulative value.

`depends_on: 06.2-03` because that plan authored `datasources.yaml` and owns the reprovision/poll pattern this plan reuses. ASVS L1: no threat here rates above `medium`; block-on-`high` is not reached.
</scope_rationale>

<tasks>

<task type="tracer" tdd="true">
  <name>Remove the unresolvable span-tag mapping and lock the corrected correlation shape</name>
  <reversibility rating="reversible">Three deleted YAML lines in a provisioned file; revert restores the prior (broken) state exactly.</reversibility>
  <read_first>
    - `.planning/debug/grafana-trace-log-correlation.md` — full root cause with the datasources.yaml:18-26 excerpt and why `filterByTraceID` alone is the correct mechanism
    - `.planning/phases/06-observability-evaluation-polish/06-CONTEXT.md` D-34 (Collector fans out to Jaeger/Prometheus/Loki, Grafana as single pane correlating on `trace_id`) and D-40 (observability as code, no manual UI state anywhere, no alert or recording rules)
    - `deploy/grafana/provisioning/datasources/datasources.yaml` — all 37 lines; the Jaeger block is lines 13-25 and the Loki `derivedFields` block is lines 27-37
    - `engine/src/tests/telemetry_metrics.rs` — `collector_prometheus_exporter_has_no_namespace` (starts line 789) is the house pattern for asserting a committed deploy YAML: read to string, scan by indentation into a block, assert on the block. The engine has NO YAML crate (`engine/Cargo.toml` carries `serde` and `serde_json` only) — follow the indentation-scan pattern, do not add a dependency
    - `rust-guidelines.md` before editing the `.rs` file (per CLAUDE.md; this task touches Rust, not Go — do not read `go-guidelines.md`)
  </read_first>
  <files>deploy/grafana/provisioning/datasources/datasources.yaml, engine/src/tests/telemetry_metrics.rs</files>
  <behavior>
    - The Jaeger datasource's `jsonData.tracesToLogsV2` block declares `datasourceUid: loki-datasource`, `filterByTraceID: true`, `filterBySpanID: false`, and no `tags` key.
    - The Loki datasource's `derivedFields` block is byte-identical to its current state.
    - `grafana_traces_to_logs_maps_no_span_tag` fails if a `tags:` key is reintroduced anywhere under `tracesToLogsV2`, or if `filterByTraceID: true` is removed.
  </behavior>
  <action>
<!-- planner-discipline-allow: tags: -->
Edit `deploy/grafana/provisioning/datasources/datasources.yaml`: delete exactly lines 21-23 (the unresolvable span-attribute mapping under `tracesToLogsV2`) so the Jaeger entry reads:

```yaml
  - name: Jaeger
    type: jaeger
    access: proxy
    url: http://jaeger:16686
    uid: jaeger-datasource
    jsonData:
      tracesToLogsV2:
        datasourceUid: loki-datasource
        filterByTraceID: true
        filterBySpanID: false
```

Change nothing else in the file — the Prometheus entry, the Loki entry, and the Loki `derivedFields` (`matcherRegex: "trace_id=([0-9a-fA-F]+)"`, `name: TraceID`, `url: "$${__value.raw}"`) all stay verbatim. Do not substitute a different tag (`service.name` or otherwise); `tracesToLogsV2` needs no `tags` entry at all for trace-ID filtering.

Add `#[test] fn grafana_traces_to_logs_maps_no_span_tag()` to `engine/src/tests/telemetry_metrics.rs`, modeled on `collector_prometheus_exporter_has_no_namespace`: resolve the path as `Path::new(env!("CARGO_MANIFEST_DIR")).parent().unwrap().join("deploy").join("grafana").join("provisioning").join("datasources").join("datasources.yaml")`, assert it exists, read to string, then scan lines to extract the `tracesToLogsV2:` block (start at the line whose trimmed content is `tracesToLogsV2:`, record that line's indent width, and consume subsequent lines while they are blank or more-indented than that width). Assert on the extracted block:
  - it contains `filterByTraceID: true`
  - it contains `filterBySpanID: false`
  - no line's trimmed content starts with `tags:` — failure message must name the root cause, e.g. `"tracesToLogsV2 must not map a span tag: trace_id is intrinsic OTel span context, and an unresolvable tag makes Grafana suppress the 'Logs for this span' action (G-06.2-1)"`
Additionally assert, outside the block, that the file still contains `matcherRegex: "trace_id=([0-9a-fA-F]+)"` so the Loki→Jaeger return direction cannot be silently dropped by a later edit.

Follow `rust-guidelines.md` for the test body. Do not add a YAML parsing crate to `engine/Cargo.toml`.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- grafana_traces_to_logs_maps_no_span_tag</automated>
    <automated>sh -c '! grep -q "tags:" deploy/grafana/provisioning/datasources/datasources.yaml'</automated>
  </verify>
  <acceptance_criteria>
    - `sh -c '! grep -q "tags:" deploy/grafana/provisioning/datasources/datasources.yaml'` exits 0. Do not use `grep -c` for this zero-match gate: under `set -e` a zero count is a failing exit (06.2-REVIEWS.md Cursor LOW).
    - `grep -n "filterByTraceID: true" deploy/grafana/provisioning/datasources/datasources.yaml` returns exactly one line.
    - `grep -n "derivedFields" deploy/grafana/provisioning/datasources/datasources.yaml` returns exactly one line and `git diff -- deploy/grafana/provisioning/datasources/datasources.yaml` shows only deletions (three), no additions.
    - `cargo test --manifest-path engine/Cargo.toml --lib -- grafana_traces_to_logs_maps_no_span_tag` exits 0.
    - Fail-first is demonstrated in this exact order and recorded in the SUMMARY: (1) write `grafana_traces_to_logs_maps_no_span_tag` FIRST, against the still-unmodified `datasources.yaml`; (2) run it and observe it FAIL on the `tags:` assertion, capturing the failure message; (3) only then delete the three YAML lines; (4) re-run and observe it pass. Do not delete the YAML lines before the test exists.
    - No `alerting`, `alert_rules`, `recording` or `rules` key appears anywhere under `deploy/grafana/provisioning/` (D-40).
  </acceptance_criteria>
  <done>The provisioned Jaeger datasource no longer declares a tag variable Grafana cannot resolve, and a committed test holds that shape.</done>
</task>

<task type="auto">
  <name>Reprovision Grafana, prove live datasource state, re-measure the engine pin, update the validation map</name>
  <read_first>
    - `.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-03-PLAN.md` Task 1 `<precondition>` (all `<automated>` blocks run under Git Bash / `sh`, NOT PowerShell — `$(pwd)` malforms under PowerShell; Node.js is not assumed on `PATH`, `curl` is) and Task 2's datasource-API poll at line 199, which is the pattern this task extends
    - `scripts/engine-test-targets.sh` — note it asserts SEVEN invariants, of which THREE move when a lib test is added: `TOTAL` (currently 475), `LIB_BIN_SUM` (currently 435) and `LIB_COUNT` (currently 435). `BIN_MAIN_COUNT` (0), `BIN_INSPECT_COUNT` (18), `BIN_SEED_COUNT` (0) and `INTEG_CONFIG_COUNT` (22) do not move. Read the script's own header comment: the values are MEASURED, and lowering one to make a red gate pass is never correct
    - `.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-VALIDATION.md` — the "Per-Requirement Verification Map" table (starts line 50); the row this plan updates is the LAST row of the table, `OBS-01 / D-34, D-40 | Jaeger, Prometheus, Loki and Grafana are correlated by trace_id in one pane | ... | Manual-Only — see the table below`
    - `.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-UAT.md` gap `G-06.2-1` — the `missing:` list is the closure checklist
  </read_first>
  <files>scripts/engine-test-targets.sh, .planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-VALIDATION.md</files>
  <behavior>
    - A running Grafana, restarted against the edited file, serves a Jaeger datasource whose parsed `jsonData.tracesToLogsV2` retains `filterByTraceID: true` and does not contain an invalid `key: trace_id` mapping (empty/null tag arrays are allowed).
    - `sh scripts/engine-test-targets.sh` exits 0 with all seven invariants matching a fresh measurement.
    - The VALIDATION.md correlation row names the new automated test and still marks the click-through as Manual-Only.
  </behavior>
  <action>
Reprovision and prove the LIVE state, not just the file. From Git Bash at the repo root (not PowerShell; `$(pwd)` malforms under PowerShell):

`docker compose --profile observability up -d --force-recreate grafana`

Then poll UID `jaeger-datasource` and assert the served object, not the raw body text. Addresses review concern: Plan 10's whole-response `"tags"` poll plus `down -v` can false-fail and destroy volumes (06.2-REVIEWS.md Cursor HIGH). Python is the JSON parser (this repo already uses it for `scripts/test_*.py`; Node.js is not assumed on PATH). Save the helper as a one-shot Git Bash invocation:

`sh -c 'for i in $(seq 1 30); do body=$(curl --fail --silent --retry-connrefused --user admin:admin http://127.0.0.1:3000/api/datasources/uid/jaeger-datasource) || { sleep 2; continue; }; python -c "import json,sys; d=json.loads(sys.argv[1]); ttl=((d.get(\"jsonData\") or {}).get(\"tracesToLogsV2\") or {}); filt=ttl.get(\"filterByTraceID\") is True; tags=ttl.get(\"tags\", None); invalid=isinstance(tags, list) and any(isinstance(t, dict) and t.get(\"key\")==\"trace_id\" for t in tags); sys.exit(0 if (filt and not invalid) else 2)" "$body"; st=$?; if [ "$st" -eq 0 ]; then echo "OK: live jsonData.tracesToLogsV2 has filterByTraceID and no invalid trace_id mapping"; exit 0; fi; sleep 2; done; echo "Grafana datasource API never served a corrected tracesToLogsV2"; exit 1'`

Treat these as success, not as the invalid mapping: a missing `tags` key, `"tags": []`, or `"tags": null`. The only failing structured shape is a list that still contains an object with `key` equal to `trace_id`.

If `--force-recreate grafana` still serves that invalid mapping (python exit 2 after 30 tries), print the parsed `tracesToLogsV2` object into the SUMMARY, then and only then run `docker compose --profile observability down -v` followed by `docker compose --profile observability up -d`. Record which path was required and the JSON that justified `-v` (this resolves the third flagged assumption). Do not use `-v` because Grafana omitted a field as an empty array.

Re-measure the engine pin — do NOT compute it. Run `cargo test --manifest-path engine/Cargo.toml -- --list` and read off the per-target counts the way the script's `awk` blocks do, then write the measured numbers into `scripts/engine-test-targets.sh` for `TOTAL`, `LIB_BIN_SUM` and `LIB_COUNT` — all three, in the same commit as the new test. As a sanity cross-check only, one added lib test should move 475→476, 435→436 and 435→436; if the measurement disagrees with that cross-check, trust the measurement and say so in the SUMMARY. Leave `BIN_MAIN_COUNT`, `BIN_INSPECT_COUNT`, `BIN_SEED_COUNT` and `INTEG_CONFIG_COUNT` untouched. Never lower a value to make the gate green.

Edit `06.2-VALIDATION.md`'s Per-Requirement Verification Map IN PLACE — this file is not auto-maintained and must not be regenerated from a template. Update the existing last row (`OBS-01 / D-34, D-40 | Jaeger, Prometheus, Loki and Grafana are correlated by trace_id in one pane`) rather than appending a duplicate: set its Task ID to `06.2-03/T2, 06.2-03/T3, 06.2-10/T1, 06.2-10/T2`, Plan to `03, 10`, Wave to `2, 8`, and extend the Automated Command cell to name `cargo test --manifest-path engine/Cargo.toml --lib -- grafana_traces_to_logs_maps_no_span_tag` as the automated half while keeping "Manual-Only — see the table below" for the click-through. Then ADD one new row: `OBS-01 / D-34, D-40 | Provisioned Jaeger tracesToLogsV2 declares no unresolvable span tag and retains filterByTraceID | 06.2-10/T1 | 10 | 8 | Rust unit | cargo test --manifest-path engine/Cargo.toml --lib -- grafana_traces_to_logs_maps_no_span_tag | ✅ | ⬜ pending`. Append a short dated note below the table in the style of the existing "Review-incorporation pass" notes, recording that the G-06.2-1 closure added this row and why the click-through stays manual.

Also retarget the Manual-Only table (currently lines 123-128, Re-run owner column still `06.2-07/T3` for all four rows — 06.2-REVIEWS.md Cursor MEDIUM). This plan owns only the Grafana click-through row: set that row's Re-run owner cell to `06.2-10/T2` and add a dated note that UAT Test 1 is harvested from this task's `<human-check>`, not from executed Plan 07. Do not retarget the dashboard-render row (owned by 06.2-11/T2), the degrade-to-stdout row (owned by 06.2-12/T3), or the Windows bind-mount row: UAT Test 3 already passed, so leave its Re-run owner as historical `06.2-07/T3` and annotate the Why Manual / Test Instructions cell that it is already passed and must not be re-run as a `down -v` of the live stack during this plan.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- grafana_traces_to_logs_maps_no_span_tag</automated>
    <automated>sh scripts/engine-test-targets.sh</automated>
  </verify>
  <acceptance_criteria>
    - The datasource-API poll above prints `OK: live jsonData.tracesToLogsV2 has filterByTraceID and no invalid trace_id mapping` and exits 0. A body that contains the JSON key for tags as `[]` or `null` still counts as success if `filterByTraceID` is true and no mapping has `key` `trace_id`.
    - `git log -1 --format=%s` of any compose teardown that used `-v` (if one ran) is recorded in the SUMMARY together with the parsed `tracesToLogsV2` object that justified it; if `-v` was not needed, the SUMMARY says so explicitly.
    - Automated commands in this task do not close UAT Test 1; the human-check block below is the only evidence that the Logs-for-this-span action renders.
    - `sh scripts/engine-test-targets.sh` exits 0 and prints "All 7 Rust test target invariants verified successfully."
    - The three changed pin values in `scripts/engine-test-targets.sh` equal the values printed by `cargo test --manifest-path engine/Cargo.toml -- --list`; the four unchanged ones are untouched in `git diff`.
    - `06.2-VALIDATION.md` contains exactly one row mentioning `grafana_traces_to_logs_maps_no_span_tag` as a new row plus the reference in the updated correlation row, and the correlation row still contains the string `Manual-Only`.
    - `06.2-VALIDATION.md`'s table was edited in place: `git diff` on that file shows no wholesale rewrite of unrelated rows.
    - `06.2-VALIDATION.md`'s Manual-Only Grafana click-through row Re-run owner cell is `06.2-10/T2`. The bind-mount row still names historical `06.2-07/T3` and states UAT Test 3 already passed.
    - Human check below is recorded as the closing evidence for G-06.2-1.
  </acceptance_criteria>
  <human_check>
    <test>With the observability profile up and one `/rag/query` issued, open http://127.0.0.1:3000 (admin:admin) → Explore → Jaeger, select a `lancet-gateway` span, and confirm the "Logs for this span" action now renders. Follow it and confirm it lands on Loki filtered to that trace's ID. Then open a Loki log line from that result and follow its derived `TraceID` field back into Jaeger. Confirm the same `trace_id` appears in the Jaeger pane, the Loki pane, and that a Prometheus series exists over the same window.</test>
    <expect>The "Logs for this span" action is present and clickable (it was absent before this change); both directions of the round trip resolve to the same trace_id.</expect>
  </human_check>
  <done>Live Grafana serves the corrected correlation config, the engine pin matches a fresh measurement, and the validation map names the new automated assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Committed provisioning file → running Grafana | Grafana loads datasource configuration, including the Loki query template used by the trace-to-logs action, from this file at container start. |
| Grafana → Loki query | The correlation action issues a Loki query on the operator's behalf; its filter scope determines which log lines are surfaced next to one trace. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.2-10-01 | Information Disclosure | trace-to-logs query scope | medium | mitigate | Fix retains `filterByTraceID: true`, so the action queries by the selected trace's ID only. Removing `tags` narrows nothing and broadens nothing — it removes an unresolvable variable. The prohibition against broadening the Loki query is explicit; the acceptance criteria require deletions only. |
| T-06.2-10-02 | Tampering | provisioned datasource drifting from the committed file | medium | mitigate | The live datasource-API poll asserts served `jsonData` matches the file, and `grafana_traces_to_logs_maps_no_span_tag` fails the suite if the file regresses. D-40's no-manual-UI-state rule is a named prohibition. |
| T-06.2-10-03 | Repudiation | half-fixed round trip presented as complete | low | mitigate | The test additionally asserts the Loki `derivedFields` `matcherRegex` still exists, and the human check exercises both directions. |
| T-06.2-10-04 | Elevation of Privilege | Grafana admin credentials in verification commands | low | accept | `admin:admin` is the committed local demo credential on a loopback-only Compose stack (D-39). Hardening Grafana auth is out of scope for this gap and is not introduced or weakened here. |
| T-06.2-10-05 | Tampering | package/image installs | low | accept | No new images, modules or crates. The explicit prohibition on adding a YAML crate keeps the dependency graph unchanged. |
| T-06.2-10-06 | Denial of Service | `docker compose down -v` wiping Prometheus/Loki/Grafana volumes after a false-positive live poll | medium | mitigate | Addresses review concern: Plan 10 whole-response tag-key poll (06.2-REVIEWS.md Cursor HIGH). Poll parses `jsonData.tracesToLogsV2`; `-v` is last resort only when that object still contains `key: trace_id`. |

Generic provisioning-file secret handling and injection classes are canon — covered by `/gsd-secure-phase`; not minted as prohibitions here. No threat reaches the `high` blocking threshold (ASVS L1).
</threat_model>

## Review incorporation

Current Cursor/Grok findings against this unexecuted plan (06.2-REVIEWS.md). Claude findings against executed 07–09 are recorded here only when they still affect remaining UAT/verification; none require rewriting this plan's YAML fix.

| Finding | Severity | Disposition | Rationale |
|---------|----------|-------------|-----------|
| Whole-response `"tags"` poll plus `down -v` can false-fail and destroy volumes | HIGH | incorporated | Task 2 poll parses `jsonData.tracesToLogsV2`; `-v` only after structured evidence of `key: trace_id`; T-06.2-10-06; prohibition on empty/null false positives |
| `grep -c "tags:"` exits 1 on the successful zero-match case | LOW | incorporated | Task 1 verify/AC use `! grep -q` |
| XML `&amp;&amp;` in verify blocks depends on execute-plan unescaping | LOW | deferred | This plan's new poll uses `;` / `\|\|` / sequential commands; no new `&&` in verify |
| VALIDATION.md Manual-Only owners still name `06.2-07/T3` | MEDIUM | incorporated | Task 2 retargets the Grafana click-through row to `06.2-10/T2`; bind-mount left as already-passed UAT Test 3 |
| Keep Grafana click-through as required human evidence | MEDIUM | incorporated | Task 2 `<human-check>` remains UAT Test 1; automated gates explicitly do not close it |
| Plan 07 unit-suffix live probe / Collector 0.159.0 vs 0.120.0 / Plan 09 tautological exporter test / COVERAGE TLS row / wave-6 pin race | various (Claude on 07–09) | rejected as remaining work | Independently confirmed closed or residual: `scripts/verify-collector-prom-export.sh` live SUCCESS on all dashboard stems; Collector image recorded as 0.120.0 in 06.2-07-SUMMARY; CR-04 TLS row is already INTEGRATE in COVERAGE.md; TestOTLPExportersFollowEndpointSecurity weakness is advisory WR not a UAT gap |
| DEBT-P6.2-WR-01..06 / IN-01 | LOW (Claude) | deferred | Already recorded in STATE.md Known Issues; out of G-06.2-1 scope |

<verification>
- `cargo test --manifest-path engine/Cargo.toml --lib -- grafana_traces_to_logs_maps_no_span_tag` and `sh scripts/engine-test-targets.sh` both exit 0.
- `git diff -- deploy/grafana/provisioning/datasources/datasources.yaml` shows three deletions and zero additions.
- The live Grafana datasource-API poll reports parsed `jsonData.tracesToLogsV2` with `filterByTraceID` true and no invalid `trace_id` mapping.
- The human click-through in Task 2 is performed and its result recorded; G-06.2-1 / UAT Test 1 is not closed on the automated checks alone.
- No alert or recording rules were introduced anywhere under `deploy/grafana/provisioning/` (D-40).
</verification>

<success_criteria>
- Grafana renders the "Logs for this span" action on a `lancet-gateway` span and it resolves to that trace's Loki lines (G-06.2-1, UAT Test 1).
- The corrected correlation shape is committed, asserted, and reproducible from files with no manual UI state (D-34, D-40).
- The engine test-count gate is green against a fresh measurement, leaving a correct cumulative baseline for 06.2-11.
</success_criteria>

## Artifacts this phase produces

- `deploy/grafana/provisioning/datasources/datasources.yaml` — Jaeger `tracesToLogsV2` block with the `tags` list removed and `filterByTraceID: true` / `filterBySpanID: false` retained (3 lines deleted, 0 added)
- `engine/src/tests/telemetry_metrics.rs` — new lib test `grafana_traces_to_logs_maps_no_span_tag`
- `scripts/engine-test-targets.sh` — re-measured `TOTAL`, `LIB_BIN_SUM`, `LIB_COUNT` (three of the seven invariants)
- `.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-VALIDATION.md` — correlation row updated in place plus one added row and a dated closure note
- No new crates, Go modules, container images, dashboards, or alert/recording rules

<output>
Create `.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-10-SUMMARY.md` recording: the exact YAML lines removed; the fail-first evidence and failure message for `grafana_traces_to_logs_maps_no_span_tag`; whether `--force-recreate grafana` sufficed or a `down -v` was required, including the parsed `tracesToLogsV2` object that justified any `-v`; the measured engine counts written into the pin script and the cumulative baseline handed to 06.2-11; and the human click-through result for UAT Test 1 (automated gates do not close that test).
</output>
