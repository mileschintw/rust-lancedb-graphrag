---
phase: 06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit
plan: 06
type: execute
wave: 5
depends_on:
  - 06.2-01
  - 06.2-02
  - 06.2-03
  - 06.2-04
  - 06.2-05
files_modified:
  - gateway/main.go
  - gateway/main_test.go
  - gateway/internal/telemetry/telemetry.go
  - gateway/internal/telemetry/telemetry_test.go
  - gateway/internal/sse/sse_test.go
  - engine/src/telemetry/mod.rs
  - engine/src/workflow/mod.rs
  - engine/src/workflow/events.rs
  - engine/src/workflow/runner.rs
  - engine/src/workflow/nodes/generate.rs
  - engine/src/tests/telemetry_query.rs
  - scripts/engine-test-targets.sh
  - scripts/gateway-test-targets.sh
autonomous: true
requirements:
  - OBS-01
estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Logs are correlated with traces on both sides: every request-owned gateway log record carries the request's OTel context so its LogRecord reports the same trace ID as the request span, and Rust `tracing` events emitted inside node and leaf spans reach the OTel logger provider carrying that span's trace context (D-36). Grafana's trace-to-log link therefore resolves against real data rather than an empty result set."
    - "Background and startup logs do not fabricate an OTel context. A reconciler or shutdown log carries its correlation attribute and no trace context, which is honest — inventing a background trace ID would make the trace-to-log link return records that belong to no request."
    - "REVIEW-CORRECTED classification, published as a table rather than an escape hatch. Request-owned (gets the telemetry context field): `gateway/main.go:428` (`insert document`, inside `uploadDocument` with `r` in scope) and the `rag query stream opened` Info log Plan 01 added inside `queryRAG`. Threaded and then migrated by this plan: `:644` (`retain pending checkpoint failed`) via a new leading `ctx context.Context` parameter on the non-handler helper `writeWorkflowEvent`. Background (no OTel context, ever): `:217` (`compensate failed ingestion`), `:273` and `:330` (the durable reconciler), and `:725`, `:730`, `:736`, `:765`, `:770`, `:779`, `:785` (all `run()` startup and shutdown). The classification is closed — there is no residual 'if no reachable request context, leave it uncorrelated' branch for an executor to resolve at execution time."
    - "Console output survives the migration in both services: the Rust `fmt` layer and the zap console core still receive every record, so `cargo run` and `go run` remain fully usable with no Collector (D-38)."
    - "D-41 (substantive): Phase 05 D-30's workflow metadata lands in BOTH places — as attributes on the query span AND as the additive `WorkflowCompletedEvent.metadata` protobuf field. The proto field already exists from Phase 6's consolidated wire change and the gateway SSE surface already forwards it; today the engine sets it to none, so this phase populates it. All ten fields are populated: start and completion timestamps, reformulation-used, vector and bm25 counts, graph node and edge counts, prompt and completion tokens, and the derived degraded-mode flag."
    - "`degraded_mode` is DERIVED from the notice set and the answer basis by the single rule recorded in `06-AI-SPEC.md` section 4.2, and is never set by hand at a degrade site. It is true when the terminal notices contain any of the graph-unavailable, graph-degraded, graph-timeout, any retrieval-degraded, citation-dropped, model-only, basis-reconciled, index-rebuild-failed, index-stale or index-generation-mismatch codes, or when the answer basis is not retrieval. No-evidence, model-notice, model-warning, citation-repaired and graph-ablation are deliberately excluded — a repaired citation is not a degradation, a zero-match query over a healthy index is a correct answer, and an operator-requested ablation is a requested mode."
    - "That single derived value is what reaches the gateway root span as `lancet.degraded_mode`, completing D-31's terminal degraded-flag requirement from the same derivation that feeds the wire field rather than from a second gateway-local rule."
    - "Token counts come from the provider's reported usage, which `ModelOutput` already carries as an optional structure; when the provider reported no usage the fields stay zero and that absence is visible rather than fabricated."
  flagged_assumptions:
    - "`ModelUsage` on `ModelOutput` is optional and a provider may omit it. Zero token counts therefore mean either a genuinely empty generation or an absent usage report, and the wire field cannot distinguish them. Recorded rather than papered over; if this matters to Phase 6.3's evaluation harness it is a follow-on decision, not something to invent here."
    - "REVIEW-PINNED `reformulation_used` predicate (Cursor MEDIUM). There is no dedicated flag — the reformulate node is a v1 pass-through per ORCH-05, and `ReformulateQueryNode::new()` leaves `ctx.variants` empty, after which `RetrieveHybridNode` backfills it with `[original_query]` at `engine/src/workflow/nodes/retrieve.rs:75-77`. The predicate is therefore exactly: `reformulation_used` is true iff `ctx.variants` is non-empty AND `ctx.variants.as_slice()` is not equal to the single-element slice containing `ctx.original_query`. That yields false for the default pass-through node (variants backfilled to `[original_query]`) and false for a run that failed before retrieval (variants empty), both correct. A reformulator that legitimately returns exactly `[original_query]` also reads false, which is an accepted and stated limitation, not a bug. The pass-through case is pinned by a test, not left to inference."
    - "RESEARCH A4 [ASSUMED] closes or fails here: Plan 01 proved the `otelzap` context seam at one added site; this plan migrates the remaining request-owned sites and threads context into exactly one non-handler helper. REVIEW-CORRECTED: the prior escape hatch ('if a site turns out to have no reachable request context, leave it uncorrelated') is deleted, because the ambiguity it created is precisely the review's HIGH finding — the classification above is now decided per site, in advance, from the actual signatures."
  prohibitions:
    - statement: "Do not attach a fabricated or background OTel context to a log record that has no request context; an uncorrelated background log is correct, an invented correlation is not."
      flagged: true
    - statement: "Do not set `degraded_mode` at any degrade site; it is computed once from the terminal notice set and answer basis."
      flagged: true
    - statement: "Do not add, remove or renumber any protobuf field; the metadata message and its field already exist from Phase 6's consolidated wire change and this phase only populates it."
      flagged: true
    - statement: "Do not change the type of `app.logger` away from `*zap.Logger`. Do not alter the signature of any `http.HandlerFunc`-shaped method on `app` (`queryRAG`, `uploadDocument`, and every other route target) for any reason, and do not add a context parameter to `compensateFailedIngest`, which deliberately detaches from request lifetime. REVIEW-CLARIFIED: this does NOT bar the one sanctioned helper change this plan makes — `writeWorkflowEvent` is an internal, non-routed helper with exactly two call sites, both inside `queryRAG` where `r` is in scope, and it gains a leading `ctx context.Context` parameter. That is stated affirmatively in Task 1's action; the prohibition exists to stop signature churn spreading past it."
      flagged: true
    - statement: "Do not put query text, evidence text, prompt text or answer text into a log record body or attribute while migrating log sites."
      flagged: true
    - statement: "Do not replace the shipped `correlation_id` in any log, notice, checkpoint or response with the OTel trace ID; both identities coexist."
      flagged: true
  artifacts:
    - path: "engine/src/workflow/mod.rs"
      provides: "WorkflowContext start timestamp and prompt/completion token fields, and the single degraded-mode derivation function."
    - path: "engine/src/workflow/events.rs"
      provides: "workflow_completed populating the WorkflowMetadata message instead of leaving it unset."
    - path: "gateway/internal/telemetry/telemetry.go"
      provides: "The request-context logging seam used across the gateway's request-owned log sites."
  key_links:
    - from: "engine/src/workflow/runner.rs"
      to: "engine/src/workflow/events.rs"
      via: "the single terminal emitter builds the workflow metadata once and passes it into the terminal event"
      pattern: "single_terminal_owner"
    - from: "engine/src/workflow/mod.rs"
      to: "gateway/main.go"
      via: "the one derived degraded-mode value travels on the terminal frame's metadata and is recorded onto the gateway root span"
      pattern: "single_degradation_derivation"
---

<objective>
Close the phase: expand the log-correlation seam Plan 01 proved at one site to every request-owned site in both services, and populate D-41's workflow metadata in both of the two places the decision requires — span attributes and the additive protobuf field — from one derivation.

Purpose: SC8 of the phase, plus the half of SC2 that the single degradation derivation owns, plus the log leg of SC5. Without this plan Grafana's trace-to-log link resolves against almost no correlated records, and the metadata field the gateway already forwards stays permanently empty.
Output: correlated logs across both runtimes, a populated `WorkflowCompletedEvent.metadata`, matching span attributes, and the derived degraded flag on the root span.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
@D:/Repos/lancet/.claude/gsd-core/references/planner-reversibility.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-observability-evaluation-polish/06-CONTEXT.md
@.planning/phases/06-observability-evaluation-polish/06-AI-SPEC.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-RESEARCH.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-PATTERNS.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-VALIDATION.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-01-SUMMARY.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-02-SUMMARY.md
@CLAUDE.md
@rust-guidelines.md
@go-guidelines.md
@engine/src/workflow/mod.rs
@engine/src/workflow/events.rs
@engine/src/workflow/runner.rs
@gateway/main.go
@gateway/internal/sse/sse.go
</context>

<scope_rationale>
Three tasks over two independent risks. Tasks 1 and 2 expand the logging seam whose design risk Plan 01 already retired, one per runtime. Task 3 populates D-41 and is the phase's last substantive behaviour change, deliberately last because it consumes the observation fields Plan 02 added and the derivation it introduces is the single source the gateway root span reads. Wave 5 because every file here is owned by an earlier plan in an earlier wave.
</scope_rationale>

<tasks>

<task type="auto" tdd="true">
  <name>Gateway log correlation across every request-owned site</name>
  <reversibility rating="reversible">Each migration adds one field to an existing log call.</reversibility>
  <read_first>go-guidelines.md; .planning/phases/06-observability-evaluation-polish/06-CONTEXT.md decisions D-36 and D-38; 06.2-RESEARCH.md the audit row refuting automatic zap trace attachment, "Common Pitfalls 5" (the bridge core defaults to a background context and a plain trace-ID string field is an attribute, not OTel log trace context), "Open Questions" item 2 and its recommendation to prove one site before broad migration, and assumption A4; 06.2-PATTERNS.md section `gateway/internal/telemetry/telemetry.go`; 06.2-01-SUMMARY.md (the resolved `otelzap` context-field constructor and the one already-migrated site); gateway/main.go log call sites at 217 (compensate failed ingestion), 273 and 330 (the durable reconciler), 428 (insert document), 644 (retain pending checkpoint), and 725, 730, 736, 765, 770, 779, 785 (startup and shutdown); gateway/main.go:176-220 (`compensateFailedIngest` — read its DECLARATION at 179, `(id string, ingestErr error)`, and the five `context.WithTimeout(context.Background(), …)` calls inside; this is why line 217 is background, and the review disagreed about it) and 447 (its only caller, inside `uploadDocument`); gateway/main.go:633-651 (`writeWorkflowEvent` — read its declaration at 633, `(w, rc, ev)`, with no `*http.Request`) and its two call sites at 603 and 629, both inside `queryRAG` where `r` IS in scope; gateway/main.go:533-631 (`queryRAG`, including the `rag query stream opened` log Plan 01 added after `w.WriteHeader(http.StatusOK)`); gateway/main.go:711-788 (`run`, where the console core is teed and the reconciler goroutine is launched at 744); gateway/main_test.go (the roughly thirty `app{... logger: zap.NewNop()}` sites whose type must not change)</read_first>
  <files>gateway/main.go, gateway/internal/telemetry/telemetry.go, gateway/internal/telemetry/telemetry_test.go, gateway/main_test.go</files>
  <behavior>
    - `TestRequestOwnedLogsCarryRequestTrace`: a log emitted from a handler during a request whose span is active produces a LogRecord whose trace ID equals the request span's, for each migrated site.
    - `TestBackgroundLogsCarryNoTraceContext`: a log emitted from the reconciler goroutine or a startup path produces a LogRecord with no trace context, and specifically not a fabricated or background-derived one.
    - `TestBackgroundLogsRetainCorrelationAttribute`: a background log that has a correlation identifier available still carries it as an attribute, so a human can still join it manually.
    - `TestConsoleCoreStillReceivesEveryRecord`: with the OTel core teed on, the console core observes the same records it did before the migration, at the same levels.
    - `TestMigratedLogsCarryNoUserContent`: no migrated log record body or attribute contains query text, evidence text, prompt text or answer text.
    - `TestCheckpointRetentionLogCarriesRequestTrace`: with a dispatcher stub that returns a pending result and a retention call that fails, the `retain pending checkpoint failed` record emitted from `writeWorkflowEvent` during a `/rag/query` request carries the request span's trace ID — proving the threaded `ctx` reaches the log rather than falling back to `env.TraceID`, which is the wire correlation UUID and not OTel context.
    - `TestCompensationLogStaysBackground`: the `compensate failed ingestion` record carries `document_id` and NO OTel trace context, even though the compensation runs synchronously inside an active request — pinning the deliberate classification so a later change cannot quietly correlate it.
  </behavior>
  <action>Read go-guidelines.md first, detecting the Go version from gateway/go.mod.

Use this classification verbatim — REVIEW-CORRECTED, and it is decided here rather than derived at execution time. The prior version listed `:217` as request-owned and then offered an escape hatch for sites with no reachable context; that contradiction was the review's HIGH finding. Verify each row against the current tree as you go, and if any row is wrong, record it as a finding rather than silently reclassifying.

| Site | Function | Classification | Action |
|---|---|---|---|
| `gateway/main.go:428` `insert document` | `uploadDocument` | request-owned | add `telemetry.Ctx(r.Context())` |
| the `rag query stream opened` Info log Plan 01 added | `queryRAG` | request-owned | already carries the field — verify, change nothing |
| `gateway/main.go:644` `retain pending checkpoint failed` | `writeWorkflowEvent` | request-owned ONCE THREADED | thread `ctx`, then add `telemetry.Ctx(ctx)` |
| `gateway/main.go:217` `compensate failed ingestion` | `compensateFailedIngest` | background | no OTel context; keep `document_id` |
| `gateway/main.go:273`, `:330` | `durableReconciler` | background | no OTel context; keep `document_id` where present |
| `gateway/main.go:725`, `:730`, `:736`, `:765`, `:770`, `:779`, `:785` | `run()` | background | no OTel context |

`:217` is background even though `compensateFailedIngest` is called synchronously from `uploadDocument` at line 447. Its signature is `(id string, ingestErr error)` — no context parameter — and every database call inside it deliberately builds its own `context.WithTimeout(context.Background(), …)` precisely so compensation survives request cancellation. Correlating that log would mean adding a context parameter to a function whose whole design point is detachment from request lifetime, for the sake of one error log. Antigravity's review treated this site as request-owned; Cursor checked the signature and found it is not. Cursor is right and this plan follows Cursor: `:217` stays background and keeps its `document_id` field for manual joins.

`:644` is the one sanctioned signature change. `writeWorkflowEvent` (`gateway/main.go:633`) is an internal helper, not a routed handler, with exactly two call sites REPO-WIDE — `gateway/main.go:603` and `:629`, both inside `queryRAG` where `r` is in scope. Verified with `grep -rn "writeWorkflowEvent" gateway/`, which returns those two lines plus the declaration and nothing else; in particular `gateway/main_test.go` and `gateway/internal/sse` do NOT call it, so there is no test-side compile break to absorb. Re-run that grep before editing and record the hit count as a finding if it differs from three. Give it a leading `ctx context.Context` parameter, pass `r.Context()` from both call sites, and add `telemetry.Ctx(ctx)` to the log at 644. This is worth doing because 644 is the only pre-existing zap call on the `/rag/query` path and leaving it uncorrelated would mean the RAG path's checkpoint failures never appear under their own trace. Do not extend this pattern to any other function.

For every request-owned site, add the telemetry context field Plan 01 introduced, carrying the request context. Add nothing else to the call.

For every background site, change nothing about the trace context. Where a correlation identifier is already in scope, keep it as the existing string attribute so a human can still join manually. A background log with an invented trace ID is worse than an uncorrelated one, because Grafana's trace-to-log link would then return records belonging to no request.

Record the table above in the SUMMARY as the shipped classification, including the `:217` ruling and its reasoning, so Phase 6.4's documentation and any future reader inherit the decision rather than re-litigating it.

Extend `gateway/internal/telemetry/telemetry.go` only if the migration reveals a genuine seam gap — for example a helper that returns a logger already bound to a request context. Keep `app.logger` typed `*zap.Logger` and keep the console core teed, so the roughly thirty test construction sites in `gateway/main_test.go` compile untouched and console output is unaffected.

Write the five named tests, splitting them between `gateway/internal/telemetry/telemetry_test.go` for the seam-level behaviour and `gateway/main_test.go` for the per-handler assertions, using an in-memory log recorder.</action>
  <acceptance_criteria>
    - The shipped classification matches the table in this task's action row for row, and is reproduced in the SUMMARY including the `:217`-stays-background ruling and its reasoning.
    - Every request-owned site passes the telemetry context field carrying the request context; no other change is made to those calls.
    - `writeWorkflowEvent` gains a leading `ctx context.Context` parameter, and `grep -rn "writeWorkflowEvent" gateway/` returns exactly three lines repo-wide — the declaration plus the two call sites at `gateway/main.go:603` and `:629`, both of which pass `r.Context()`. No call site exists in `gateway/main_test.go` or `gateway/internal/sse`, so no test file needs editing for this change. `:644` carries `telemetry.Ctx(ctx)`. No other function in `gateway/main.go` gains a context parameter; `compensateFailedIngest` keeps its `(id string, ingestErr error)` signature and its `context.Background()` timeouts unchanged.
    - No background or startup site attaches an OTel context, and none fabricates a trace ID.
    - Background sites that already had a correlation identifier still carry it as an attribute.
    - `app.logger` is still `*zap.Logger`, no `http.HandlerFunc`-shaped method on `app` changed signature, and `gateway/main_test.go`'s existing construction sites compile untouched.
    - The console core still receives every record at the same levels.
    - All seven named tests pass.
    - `cd gateway && go vet ./...` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd gateway && go test ./internal/telemetry -run 'TestRequestOwnedLogsCarryRequestTrace|TestBackgroundLogsCarryNoTraceContext|TestBackgroundLogsRetainCorrelationAttribute|TestConsoleCoreStillReceivesEveryRecord'</automated>
    <automated>cd gateway && go test ./... -run 'TestMigratedLogsCarryNoUserContent|TestCheckpointRetentionLogCarriesRequestTrace|TestCompensationLogStaysBackground'</automated>
    <automated>cd gateway && go vet ./...</automated>
  </verify>
  <done>Every gateway log that belongs to a request is correlated to that request's trace, every background log is honestly uncorrelated, and console output is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Engine log correlation across the instrumented paths</name>
  <reversibility rating="reversible">The bridge is already installed; this task proves and adjusts which events reach it.</reversibility>
  <read_first>rust-guidelines.md; .planning/phases/06-observability-evaluation-polish/06-CONTEXT.md decisions D-36 and D-38; 06.2-RESEARCH.md "Pattern 1" (the log provider and processor shape), "Common Pitfalls 2" (shutdown from the wrong runtime context losing buffered records) and 3, and "Don't Hand-Roll" (the Rust log bridge row: never format trace IDs into log strings); 06.2-01-SUMMARY.md (the installed appender bridge and registry composition); engine/src/telemetry/mod.rs as produced by Plan 01; engine/src/workflow/runner.rs (the terminal warning at the dropped-checkpoint path and the node event flow); engine/src/ingest.rs (the graph-completeness warning and the extraction-failure error inside the document span Plan 04 created, and the rebuild error paths)</read_first>
  <files>engine/src/telemetry/mod.rs, engine/src/tests/telemetry_query.rs</files>
  <behavior>
    - `engine_log_inside_node_span_carries_trace_context`: a `tracing` event emitted while a node span is active produces an OTel LogRecord whose trace and span identifiers match that node span.
    - `engine_log_inside_leaf_span_carries_leaf_span_context`: an event emitted inside a leaf span reports the LEAF span's identifier, not merely the trace, so a log can be attributed to the specific external call it describes.
    - `engine_log_outside_any_span_has_no_trace_context`: a startup or background event emitted with no active span produces a LogRecord with no trace context rather than a fabricated one.
    - `engine_log_reaches_fmt_layer_regardless_of_provider`: with no logger provider installed, the same events still reach the console layer, proving the console path does not depend on OTLP health.
    - `engine_log_records_survive_bounded_shutdown`: events emitted immediately before the telemetry handle's shutdown are present in the exporter output afterwards, proving the batch processor drains rather than dropping them.
    - `engine_logs_carry_no_user_content`: no exported LogRecord body or attribute from an instrumented query run contains query text, evidence text, prompt text or answer text.
  </behavior>
  <action>Read rust-guidelines.md before editing.

This task is mostly proof rather than construction: Plan 01 installed the appender bridge, and the existing `tracing` sites throughout the engine are exactly what D-36's bridge architecture is supposed to pick up without a rewrite. The work is establishing that the bridge actually attaches span context at the sites this phase created, and fixing the composition if it does not.

Verify and, if necessary, correct the layer composition in `engine/src/telemetry/mod.rs` so that log records emitted inside a span carry that span's context. Confirm the ordering of the OTel trace layer and the appender bridge layer in the registry — the bridge reads the active OTel context, so it must observe the context the trace layer installed. If the ordering is wrong, the symptom is uncorrelated logs rather than a compile error, which is precisely why this is a test and not a review.

Confirm the shutdown path drains buffered log records under its bounded deadline. RESEARCH's second pitfall is explicit that the current batch processors use blocking shutdown methods that must not be called directly on a current-thread runtime; if a test needs a current-thread runtime, move the shutdown onto a blocking task rather than changing the production shutdown shape.

Do NOT rewrite existing `tracing` call sites to add trace identifiers as fields. Formatting a trace ID into a log string or a string field produces an attribute, not OTel log trace context, and Grafana's trace-to-log link will not resolve it. The bridge is the mechanism.

Do NOT relocate any existing `tracing` emission in this task, and do not edit `engine/src/ingest.rs` — it is deliberately absent from this plan's file list. The case the prior version anticipated (the debounce task's rebuild errors sitting outside any span) is already closed by Plan 04, which instruments the `rebuild_and_swap` call at `engine/src/ingest.rs:1601` with the `index_rebuild` span, so every `tracing::error!` inside that function is already emitted under it. `spawn_rebuild_debounce_task`'s own `tracing::debug!` shutdown messages are genuinely span-less background work and must stay uncorrelated. If you nevertheless find a log site that describes spanned work and sits outside its span, record it as a finding and add the owning file to this plan's file list explicitly rather than editing a file the plan does not claim.

Write the six named tests in `engine/src/tests/telemetry_query.rs` using an in-memory log exporter alongside the in-memory span exporter.</action>
  <acceptance_criteria>
    - Log records emitted inside a node span carry that node span's trace and span identifiers; records emitted inside a leaf span carry the leaf span's identifier.
    - Records emitted with no active span carry no trace context and none is fabricated.
    - With no logger provider installed, every event still reaches the console layer.
    - Records emitted immediately before shutdown are present in the exporter output afterwards.
    - No existing `tracing` call site was rewritten to format a trace identifier into its message or fields, and no existing emission was relocated; `git diff --exit-code -- engine/src/ingest.rs` reports no change from this task.
    - No exported LogRecord from an instrumented query run carries query, evidence, prompt or answer text.
    - All six named tests pass.
    - `cargo clippy --manifest-path engine/Cargo.toml -- -D warnings` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- engine_log_inside_node_span_carries_trace_context</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- engine_log_inside_leaf_span_carries_leaf_span_context</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- engine_log_outside_any_span_has_no_trace_context</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- engine_log_reaches_fmt_layer_regardless_of_provider</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- engine_log_records_survive_bounded_shutdown</automated>
    <automated>cargo clippy --manifest-path engine/Cargo.toml -- -D warnings</automated>
  </verify>
  <done>Engine logs carry the trace and span context of the work they describe, unspanned logs stay honestly uncorrelated, console output is independent of OTLP, and buffered records survive shutdown.</done>
</task>

<task type="auto" tdd="true">
  <name>D-41 workflow metadata in both places, from one degraded-mode derivation</name>
  <reversibility rating="costly">Populating a published protobuf field changes what every SSE client observes on the terminal frame; the field itself already exists, but its values become part of the observed contract.</reversibility>
  <read_first>rust-guidelines.md; .planning/phases/06-observability-evaluation-polish/06-CONTEXT.md decisions D-41, D-31, D-42 and D-74; .planning/phases/06-observability-evaluation-polish/06-AI-SPEC.md section 4.2 (the authoritative `degraded_mode` derivation rule, its included code set, its four deliberate exclusions, and the separate paragraph explaining why graph ablation is excluded) and section 4.3 (the observable SSE surface listing the ten metadata fields); proto/lancet/v1/lancet.proto:231-253 (the `WorkflowMetadata` message with its ten fields including the derived-never-set comment on the last one, and `WorkflowCompletedEvent.metadata` at tag 7) and the `NoticeCode` enum at lines 72-99 — note `NOTICE_CODE_UNSPECIFIED = 0` at line 73, which generates a Rust `Unspecified` variant that neither §4.2 list names, and `reserved 17` at line 93, which generates no variant; engine/src/workflow/nodes/reformulate.rs:29-60 (`ReformulateQueryNode::new()` sets `reformulator: None` — the v1 pass-through that leaves `ctx.variants` empty) and engine/src/workflow/nodes/retrieve.rs:74-77 (where `RetrieveHybridNode` backfills `ctx.variants` with `[ctx.original_query]`, which is why the `reformulation_used` predicate must compare against that exact single-element slice); engine/src/workflow/events.rs:357-374 (`workflow_completed` and the `metadata: None` it currently emits at 372); engine/src/workflow/runner.rs:497-543 (`emit_terminal_once`, the single terminal owner, and its two `workflow_completed` construction sites at 521 and 532 — the only two call sites in the codebase); engine/src/workflow/mod.rs:77-133 (`WorkflowContext` fields and `WorkflowContext::new`) and 162-215 (`update_from_model_output`, which already receives the full `ModelOutput`); engine/src/generation/mod.rs:48-70 (`ModelUsage` with its prompt, completion and total token counts, and `ModelOutput.usage` as an optional field); engine/src/workflow/nodes/generate.rs (where the generation attempt count Plan 02 added is set); gateway/internal/sse/sse.go:100-140 (the metadata map the gateway already builds from the frame, and the all-zero fallback it synthesizes when the field is unset); gateway/main.go terminal-frame handling as modified by Plan 02</read_first>
  <files>engine/src/workflow/mod.rs, engine/src/workflow/events.rs, engine/src/workflow/runner.rs, engine/src/workflow/nodes/generate.rs, engine/src/tests/telemetry_query.rs, gateway/main.go, gateway/main_test.go, gateway/internal/sse/sse_test.go, scripts/engine-test-targets.sh, scripts/gateway-test-targets.sh</files>
  <behavior>
    - `workflow_metadata_is_populated_on_success`: a successful run's terminal event carries a metadata message with all ten fields set, not the unset field the engine emits today.
    - `workflow_metadata_is_populated_on_failure`: a failed run also carries metadata, with the counts reflecting however far the run got.
    - `degraded_mode_true_for_each_included_notice_code`: for each notice code in the included set, a run carrying that code alone yields a true degraded flag — one case per code, so a future code added to the enum but omitted from the rule is visible.
    - `degraded_mode_false_for_each_excluded_notice_code`: a run carrying only no-evidence, model-notice, model-warning, citation-repaired, graph-ablation or the unspecified zero-value code yields a false degraded flag — six cases, one per false-arm variant.
    - `workflow_metadata_reformulation_used_is_false_for_pass_through_node`: a run whose reformulate node is the default `ReformulateQueryNode::new()` (no reformulator) reports `reformulation_used` false after `RetrieveHybridNode` has backfilled `ctx.variants` with `[original_query]`.
    - `workflow_metadata_timestamps_are_epoch_millis`: `started_at_ms` and `completed_at_ms` are both plausible unix epoch millisecond values (greater than the millisecond value of 2020-01-01) and `completed_at_ms >= started_at_ms`, which a monotonic `Instant`-derived value would fail.
    - `degraded_mode_true_when_basis_is_not_retrieval`: a run with a mixed or model-only basis and no notices at all still yields true.
    - `degraded_mode_has_exactly_one_derivation_site`: the derivation function is the only place the flag is computed, and no degrade site sets it.
    - `workflow_metadata_tokens_come_from_provider_usage`: a run whose provider reported usage carries those prompt and completion counts; a run whose provider reported none carries zeros.
    - `span_attributes_match_wire_metadata`: the values recorded as span attributes on the query span equal the values placed in the metadata message, field for field.
    - `TestRootSpanCarriesDerivedDegradedMode` (Go): the gateway root span records the degraded flag taken from the terminal frame's metadata, not recomputed locally.
    - `TestSSEMetadataForwardsRealValues` (Go): the SSE metadata map carries the engine's real values rather than the all-zero fallback, proving the fallback branch is no longer the live path.
  </behavior>
  <action>Read rust-guidelines.md and go-guidelines.md before editing.

Do not touch `proto/lancet/v1/lancet.proto`. The metadata message and its field already exist from Phase 6's consolidated additive wire change (D-74) and the gateway already forwards them; the only gap is that the engine emits the field unset. This task populates it. No field is added, removed or renumbered.

Add the two missing observation fields to `WorkflowContext` in `engine/src/workflow/mod.rs`: a start timestamp in unix milliseconds, set inside `WorkflowContext::new` so no construction site changes, and prompt and completion token counts. REVIEW-DRIVEN clock choice (Antigravity LOW): `WorkflowMetadata.started_at_ms` and `completed_at_ms` are `int64` UTC epoch milliseconds on the wire, so both must come from the SYSTEM clock — `std::time::SystemTime::now().duration_since(std::time::UNIX_EPOCH)`, unwrapped to a default on the pre-epoch error case, cast to `i64` milliseconds — and never from `Instant::now()`, which is monotonic and has no epoch meaning. Use the identical expression in `WorkflowContext::new` for the start value and in `emit_terminal_once` for the completion value, so the two are subtractable. This does NOT replace the existing `Instant`-based duration measurement the runner already uses for `duration_ms`; that stays as it is. Populate the token counts inside `update_from_model_output`, which already receives the full `ModelOutput` and therefore already has access to its optional usage structure — when usage is absent leave the counts at zero rather than estimating. Plan 02 already added the graph node count, graph edge count and generation attempt count; read those rather than recomputing, so the span attribute and the wire field cannot disagree.

Add ONE function in `engine/src/workflow/mod.rs` that derives the degraded flag from a notice slice and an answer basis, implementing the rule in `06-AI-SPEC.md` section 4.2 verbatim: true when any notice's typed code is in the included set — graph-unavailable, graph-degraded, graph-timeout, any retrieval-degraded variant, citation-dropped, model-only, basis-reconciled, index-rebuild-failed, index-stale, index-generation-mismatch — or when the answer basis is anything other than retrieval. The excluded codes are no-evidence, model-notice, model-warning, citation-repaired and graph-ablation, each excluded for a stated reason: a repaired citation is not a degradation, a zero-match query over a healthy index is a correct answer, and an operator-requested ablation is a requested mode rather than an unplanned failure. Match on the typed code enum, never on the string form, and write the match so that a future enum variant forces a compile-time decision rather than defaulting silently into either bucket.

`NOTICE_CODE_UNSPECIFIED` needs an explicit arm — REVIEW-DRIVEN (Cursor MEDIUM). `proto/lancet/v1/lancet.proto:73` declares `NOTICE_CODE_UNSPECIFIED = 0`, so the generated Rust enum has an `Unspecified` variant that neither the included nor the excluded list above names. Because a wildcard arm is prohibited, the match will not compile without it. Add `Unspecified` to the FALSE (non-degrading) arm alongside the five excluded codes: an unset code carries no evidence that anything degraded, and treating an absent value as degradation would make every default-constructed notice look like an outage. Note that `reserved 17` in the same enum generates no variant, so it needs no arm. Enumerate the arms explicitly rather than grouping the false set behind a catch-all, so a future variant added to the proto breaks this build.

Change `events::workflow_completed` in `engine/src/workflow/events.rs` to accept a metadata argument and place it on the message instead of the unset value at line 372. There are exactly two call sites, both inside `emit_terminal_once` in `engine/src/workflow/runner.rs` at lines 521 and 532, so this is a contained signature change.

In `emit_terminal_once`, build the metadata once — it is already the codebase's single terminal owner, which is what keeps this from becoming a second place terminal facts are assembled. Take the start timestamp from the context, the completion timestamp from the system clock using the same expression, the reformulation-used flag from the pinned predicate below, the vector and bm25 counts from the context's result vectors, the graph counts and attempt count from the observation fields Plan 02 added, the token counts from the fields added above, and the degraded flag from the derivation function applied to the terminal notice set and the terminal answer basis. Pass the same metadata into both the success and failure terminal events. Record the identical values as attributes on the query span before it closes, so D-41's "both places" requirement is satisfied from one computation rather than two.

Pin the `reformulation_used` predicate — REVIEW-DRIVEN (Cursor MEDIUM), because the prior wording ("whether the variant set grew beyond the original query") is a heuristic that reads wrong on the default node. `ReformulateQueryNode::new()` is the v1 pass-through and leaves `ctx.variants` EMPTY; `RetrieveHybridNode` then backfills it with `[ctx.original_query]` at `engine/src/workflow/nodes/retrieve.rs:75-77`. The predicate is exactly: true iff `ctx.variants` is non-empty AND `ctx.variants.as_slice()` is not equal to the one-element slice containing `ctx.original_query`. Evaluate it against the terminal `ctx`, not mid-run. Add `workflow_metadata_reformulation_used_is_false_for_pass_through_node` driving a workflow whose reformulate node is `ReformulateQueryNode::new()` (no reformulator) and asserting the flag is false after retrieval has backfilled the variant — testing it only against a configured reformulator would leave the default production path unproven, which is the exact failure the reviewer flagged.

In `gateway/main.go`, at the terminal-frame handling Plan 02 added, record `lancet.degraded_mode` on the request span from the frame's metadata. This completes D-31's degraded-flag requirement using the engine's single derived value; the gateway still computes nothing. Note the deliberate asymmetry with Plan 05: this flag is §4.2's notice-inclusive derivation, whereas `lancet.rag.answer.degraded` counts only `answer_basis != RETRIEVAL`. They are different, separately-scoped facts and neither is derived from the other.

Write the eight Rust tests in `engine/src/tests/telemetry_query.rs` and the two Go tests in `gateway/main_test.go` and `gateway/internal/sse/sse_test.go`. The per-code degraded tests must enumerate the included and excluded sets explicitly rather than sampling, because the whole risk in a derived flag is a code that silently falls into the wrong bucket.

Then re-pin both count gates: enumerate with `cargo test --manifest-path engine/Cargo.toml --list` and `cd gateway && go test -list '.*' ./...`, update the engine TOTAL and lib pins, and update the gateway EXPECTED_TOTAL together with the `gateway` and `gateway/internal/sse` package counts in EXPECTED_PACKAGES. Add one line to each script's invariant-history comment. Never lower a pinned count.</action>
  <acceptance_criteria>
    - `proto/lancet/v1/lancet.proto` is unmodified, and no generated binding is regenerated.
    - `WorkflowContext` carries a start timestamp set in `WorkflowContext::new` and prompt and completion token counts populated in `update_from_model_output` from the provider's optional usage, left at zero when absent.
    - Both `started_at_ms` and `completed_at_ms` are derived from `SystemTime::now().duration_since(UNIX_EPOCH)`; `grep -n "Instant::now" engine/src/workflow/mod.rs engine/src/workflow/runner.rs` shows no `Instant` feeding either metadata field, and the runner's pre-existing `duration_ms` measurement is unchanged.
    - Exactly one function derives the degraded flag, it matches on the typed notice-code enum rather than strings, and its match forces a compile-time decision for a future enum variant.
    - The included and excluded code sets match `06-AI-SPEC.md` section 4.2 exactly, including the graph-ablation exclusion, plus an explicit `Unspecified` arm in the false bucket; no wildcard `_` arm exists in the derivation.
    - `reformulation_used` is the pinned predicate (variants non-empty and not equal to `[original_query]`), and `workflow_metadata_reformulation_used_is_false_for_pass_through_node` proves it against the default `ReformulateQueryNode::new()` path.
    - `events::workflow_completed` places a populated metadata message on the event; both of its two call sites in `emit_terminal_once` pass the same metadata instance.
    - All ten metadata fields are populated on both the success and failure terminal paths.
    - The span attributes recorded on the query span equal the metadata values field for field.
    - The gateway records the degraded flag onto the request span from the frame's metadata and computes nothing itself.
    - All eleven Rust tests and both Go tests pass, with the degraded tests enumerating every included and excluded code — including `Unspecified` — rather than sampling.
    - Both count gate scripts match their enumerated counts with no pin reduced, and both exit 0.
    - `cargo clippy --manifest-path engine/Cargo.toml -- -D warnings` exits 0 and `cd gateway && go vet ./...` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- workflow_metadata_is_populated_on_success</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- degraded_mode_true_for_each_included_notice_code</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- degraded_mode_false_for_each_excluded_notice_code</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- workflow_metadata_tokens_come_from_provider_usage</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- span_attributes_match_wire_metadata</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- workflow_metadata_reformulation_used_is_false_for_pass_through_node</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- workflow_metadata_timestamps_are_epoch_millis</automated>
    <automated>cd gateway && go test ./... -run 'TestRootSpanCarriesDerivedDegradedMode|TestSSEMetadataForwardsRealValues'</automated>
    <automated>sh scripts/engine-test-targets.sh</automated>
    <automated>sh scripts/gateway-test-targets.sh</automated>
    <automated>cargo clippy --manifest-path engine/Cargo.toml -- -D warnings</automated>
  </verify>
  <done>Every terminal frame carries fully populated workflow metadata whose degraded flag comes from one enumerated derivation, the same values ride as span attributes, and the gateway root span reports that single derived flag.</done>
</task>

</tasks>

## Artifacts this phase produces

**Created by this plan**
- New `WorkflowContext` fields in `engine/src/workflow/mod.rs`: workflow start timestamp in unix milliseconds, prompt token count, completion token count.
- New function in `engine/src/workflow/mod.rs`: the single degraded-mode derivation over a notice slice and an answer basis.
- New span attribute: `lancet.degraded_mode` on both the engine query span and the gateway request span, plus the ten workflow-metadata values mirrored as query-span attributes.
- New tests in `engine/src/tests/telemetry_query.rs`, `gateway/internal/telemetry/telemetry_test.go`, `gateway/main_test.go` and `gateway/internal/sse/sse_test.go`.

**Modified by this plan**
- `engine/src/workflow/events.rs` (`workflow_completed` gains a metadata argument and populates the message), `engine/src/workflow/runner.rs` (metadata built once in the single terminal emitter), `engine/src/workflow/nodes/generate.rs` (attempt-count wiring consumed by the metadata), `gateway/main.go` (request-owned log migration and the degraded-flag span attribute), `gateway/internal/telemetry/telemetry.go`, `scripts/engine-test-targets.sh`, `scripts/gateway-test-targets.sh`.

**Explicitly NOT modified**
- `proto/lancet/v1/lancet.proto` and every generated Rust and Go binding: the metadata message and its field already shipped with Phase 6's consolidated additive wire change; this phase populates, never redefines.
- `engine/src/ingest.rs`: Task 2's prior "move the emission inside the relevant span" instruction is withdrawn — Plan 04's `index_rebuild` span already encloses `rebuild_and_swap`'s error logs, and the debounce task's own shutdown `debug!` messages are legitimately span-less.
- `compensateFailedIngest`'s signature and its `context.Background()` timeouts.

<review_dispositions>
Cross-AI review feedback from `06.2-REVIEWS.md`, resolved into this plan. Antigravity read the request/background logging classification as sound and rated the plan LOW; Cursor checked the two disputed signatures and rated it MEDIUM–HIGH. Ruling: **Cursor is correct on both sites, verified independently in-tree** — `compensateFailedIngest` is declared `(id string, ingestErr error)` at `gateway/main.go:179` with five `context.Background()` timeouts inside, and `writeWorkflowEvent` is declared `(w, rc, ev)` at `:633`. Neither has a request context at the log line. Antigravity's approval is not a rationale for leaving them, so both are decided here.

| Finding | Source | Disposition | Where it landed |
|---|---|---|---|
| HIGH — compensation log at `:217` classified request-owned but is not | Cursor (Antigravity: treated as sound) | INCORPORATED — reclassified BACKGROUND. The site is called synchronously from `uploadDocument` at `:447`, so a live request span does exist on the stack, but correlating it would mean adding a context parameter to a function whose design point is surviving request cancellation, for one error log. Keeping `document_id` preserves the manual join | Task 1 action's classification table and the paragraph explaining the ruling; new must_have truth; prohibition reworded; behavior `TestCompensationLogStaysBackground`; acceptance pins the unchanged signature and `context.Background()` timeouts |
| HIGH — checkpoint log at `:644` has no request in scope | Cursor (Antigravity: treated as sound) | INCORPORATED — resolved by THREADING, with the prohibition narrowed to permit it. `writeWorkflowEvent` is an internal helper with exactly two call sites, both in `queryRAG`; it gains a leading `ctx context.Context` and both call sites pass `r.Context()`. Worth the change because `:644` is the only pre-existing zap call on the RAG path | Task 1 action's classification table plus a dedicated paragraph; prohibition reworded to bar handler signatures and `compensateFailedIngest` while affirmatively permitting this one helper; behavior `TestCheckpointRetentionLogCarriesRequestTrace`; acceptance criterion |
| HIGH escape hatch — "if no reachable request context, leave uncorrelated" contradicts the enumerated classification | Cursor | INCORPORATED — the escape hatch is DELETED and replaced with a per-site decision table | Task 1 action; flagged assumption A4 rewritten |
| MEDIUM — exhaustive match must include `NOTICE_CODE_UNSPECIFIED` | Cursor | INCORPORATED (verified: `proto/lancet/v1/lancet.proto:73` declares it; `reserved 17` generates no variant) | Task 3 action adds a dedicated paragraph placing `Unspecified` in the false arm; behavior `degraded_mode_false_for_each_excluded_notice_code` widened to six cases; acceptance criterion |
| MEDIUM — `reformulation_used` heuristic is wrong on the default pass-through node | Cursor | INCORPORATED with the exact predicate and a pass-through test (verified: `ReformulateQueryNode::new()` leaves variants empty and `retrieve.rs:75-77` backfills `[original_query]`) | Flagged assumption rewritten as a pinned predicate; Task 3 action; behavior and `<verify>` entry `workflow_metadata_reformulation_used_is_false_for_pass_through_node`; acceptance criterion |
| LOW — epoch milliseconds must come from `SystemTime`, not `Instant` | Antigravity | INCORPORATED | Task 3 action names the exact expression for both fields; behavior `workflow_metadata_timestamps_are_epoch_millis`; acceptance adds a `grep` criterion |
| LOW — Task 1 `<verify>` splits `TestMigratedLogsCarryNoUserContent` into a second command that is easy to skip | Cursor | INCORPORATED | The second `<verify>` command now runs it together with the two new gateway tests in one invocation |
| Cross-plan #4 — `query.duration` degraded outcome vs this plan's single derivation | Cursor | RESOLVED IN PLAN 05 (the `degraded` outcome is removed there); this plan states the resulting asymmetry so nobody re-merges them | Task 3 action's closing note on `lancet.degraded_mode` vs `lancet.rag.answer.degraded` |
| Strengths — `metadata: None` at `events.rs:372`, exactly two `workflow_completed` call sites, §4.2 set match, `update_from_model_output` already sees usage, proto untouched | Both | NO CHANGE NEEDED — confirmed against the tree | n/a |
</review_dispositions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Request context → log records | Correlated log records are exported and become queryable in Loki. |
| Provider usage report → wire metadata | Token counts originate from an external provider and become client-visible. |
| Engine terminal frame → SSE clients | The metadata field becomes observable to every client, including Phase 6.3's evaluation harness. |
| Derived degraded flag → span, wire and dashboard | One derivation feeds three consumers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.2-06-01 | Information Disclosure | Migrated log records exported to Loki | high | mitigate | Migration adds only a context field to existing calls; no message or field content changes, and a dedicated test asserts no user content appears in migrated records. |
| T-06.2-06-02 | Repudiation | Fabricated trace context on background logs | medium | mitigate | Background sites attach no OTel context, asserted by explicit negative tests (`TestBackgroundLogsCarryNoTraceContext`, `TestCompensationLogStaysBackground`); correlation identifiers remain as plain attributes for manual joins. The classification is a fixed table in Task 1 rather than a per-site judgement call, which is what removes the "attach whatever context is nearest" failure mode. |
| T-06.2-06-07 | Tampering | Context threading spreading past the one sanctioned helper and reaching a function that deliberately detaches from request lifetime | medium | mitigate | Exactly one signature changes (`writeWorkflowEvent`, two call sites); `compensateFailedIngest` keeps its signature and its five `context.Background()` timeouts, asserted as an acceptance criterion, so compensation still survives request cancellation. |
| T-06.2-06-03 | Tampering | Degradation derived in more than one place | high | mitigate | One derivation function, one terminal owner, and a test asserting no degrade site sets the flag; the gateway consumes and never computes. |
| T-06.2-06-04 | Tampering | A future notice code silently falling into the wrong degraded bucket | medium | mitigate | The derivation matches on the typed enum with no catch-all default, and per-code tests enumerate both the included and excluded sets. |
| T-06.2-06-05 | Information Disclosure | Provider-reported token counts becoming client-visible | low | accept | Counts are aggregate numbers with no content; they are already bounded by the existing generation limit checks and are exactly what D-30 specified as a response-contract field. |
| T-06.2-06-06 | Tampering | Buffered log records lost on shutdown, hiding a failure | medium | mitigate | Bounded shutdown drains the batch processors, proven by a test asserting records emitted immediately before shutdown are present afterwards. |
</threat_model>

<verification>
- `cargo test --manifest-path engine/Cargo.toml --locked && (cd gateway && go test ./...)` exits 0.
- `cargo clippy --manifest-path engine/Cargo.toml -- -D warnings` exits 0 and `cd gateway && go vet ./...` exits 0.
- `sh scripts/engine-test-targets.sh` and `sh scripts/gateway-test-targets.sh` both exit 0.
- `git diff --exit-code -- proto/lancet/v1/lancet.proto` reports no change.
</verification>

<success_criteria>
- Request-owned logs in both services carry the trace context of the request that produced them; background logs honestly carry none.
- Console output in both services is unaffected by the migration.
- `WorkflowCompletedEvent.metadata` is populated on every terminal frame, with all ten fields.
- The degraded flag is derived once and consumed by the wire field, the engine span and the gateway span.
- The protobuf contract is unchanged.
</success_criteria>

<output>
Create `.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-06-SUMMARY.md`, recording: the shipped request-owned versus background classification table for every gateway log site, including the ruling that `gateway/main.go:217` stays background and why, and the one sanctioned `writeWorkflowEvent` signature change; the exact `reformulation_used` predicate as implemented and its false-on-pass-through result; the placement of `NoticeCode::Unspecified` in the false bucket; the resolution of assumption A4; the deliberate scope difference between `lancet.degraded_mode` (§4.2, notice-inclusive) and Plan 05's `lancet.rag.answer.degraded` (basis-only); and the fact that zero token counts are indistinguishable between an empty generation and an absent provider usage report.
</output>
