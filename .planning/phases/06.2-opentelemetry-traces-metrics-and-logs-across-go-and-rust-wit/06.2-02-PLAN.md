---
phase: 06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit
plan: 02
type: execute
wave: 2
depends_on:
  - 06.2-01
files_modified:
  - engine/src/workflow/runner.rs
  - engine/src/workflow/mod.rs
  - engine/src/workflow/nodes/generate.rs
  - engine/src/workflow/nodes/graph_context.rs
  - engine/src/service.rs
  - engine/src/tests/telemetry_query.rs
  - gateway/main.go
  - gateway/main_test.go
  - scripts/engine-test-targets.sh
  - scripts/gateway-test-targets.sh
autonomous: true
requirements:
  - OBS-01
estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every RAG query emits exactly five workflow node spans — `query_reformulation`, `hybrid_retrieval`, `graph_context_extraction`, `prompt_assembly`, `llm_generation` — created in exactly one place, `WorkflowRunner::run_node`, and never duplicated inside a node implementation (D-29)."
    - "Each real external call gets its own child span at the actual I/O boundary: `embedding_request`, `dense_search`, `bm25_search`, `graph_traversal`, and one `llm_attempt` per generation attempt, so D-12's single retry appears as two sibling `llm_attempt` spans carrying `attempt` 1 and 2 (D-29)."
    - "REVIEW-CLARIFIED span inventory (Cursor MEDIUM). The concrete tree for one normal, non-retried query is: 5 node spans + 4 leaf spans (`embedding_request`, `dense_search`, `bm25_search`, `graph_traversal`) + 1 `llm_attempt` = 10 engine spans created BY THIS PLAN, plus the pre-existing `query_rag` span (`engine/src/service.rs:844-849`) and the gateway `otelhttp` server span, for roughly 12 spans in a demo trace. ROADMAP SC2 and D-29 say `roughly 8-10`, and the 10 this plan creates is the number that claim is about. NO test may assert a total exported-span count — the only exact-count assertion in this plan is `query_span_hierarchy_emits_exactly_five_node_spans`, which is scoped to the five node-span names."
    - "REVIEW-DRIVEN: the existing `graph_augmentation` observation survives the introduction of node and leaf spans. `ProductionGraphQueryPort::query_graph` records it with `tracing::Span::current().record(\"graph_augmentation\", tag)` at `engine/src/service.rs:479`, and the field is declared today only on `query_rag` (`:846`). Once a node span and then a leaf span become `Span::current()` at that point, an undeclared field makes the record a silent no-op and the three `graph_augmentation_*_is_observable_end_to_end` tests in `engine/src/tests.rs:7036+` go dark. Whatever span is current at `:479` must declare the field; exactly one `.record` call exists repo-wide and it never moves."
    - "Leaf spans live on the production port adapters (`ProductionEmbeddingPort`, `ProductionDenseRetrievalPort`, `ProductionBm25RetrievalPort`, `ProductionGraphQueryPort`) and on the two `generator.generate` call sites in `GenerateAnswerNode`, so a span exists exactly where a real external call exists and test doubles do not manufacture phantom I/O spans (D-29)."
    - "The root span covers the whole SSE stream: the gateway's `otelhttp` server span spans the synchronous `queryRAG` handler from request receipt to the last frame written, so its duration equals user-perceived latency and every engine node span nests inside it (D-31)."
    - "The terminal outcome is recorded on the root span before it closes — `lancet.answer_basis`, `lancet.workflow.success`, the bounded `lancet.notice_codes` list naming which paths degraded, and an explicit span status of Ok on success or Error on failure (D-31). The single derived `lancet.degraded_mode` boolean is added by Plan 06 from the one Rust-side derivation, deliberately not re-derived here, so degradation is never recorded from two independent rules."
    - "Naming follows OTel semantic conventions where they exist and domain names elsewhere (D-42): `gen_ai.request.model` and the `gen_ai.usage.*` constants on generation spans, `db.system` and `db.operation` on datastore spans, and `lancet.*` domain names for retrieval fusion, graph traversal, index generation and degraded reasons."
    - "Instrumentation is additive only: node timeouts, the biased cancellation select, retry counts, notice sets, event ordering and answer content are byte-for-byte unchanged, proven by the pre-existing suite staying green at its raised pinned counts."
  flagged_assumptions:
    - "The exact `gen_ai.usage.*` attribute-key spellings differ between semantic-convention releases. The executor must take them from the pinned `opentelemetry-semantic-conventions` 0.32.1 constants rather than hand-writing strings, and record the resolved constant names in the SUMMARY."
    - "RESEARCH's audit row on the SSE root span is a correction, not a confirmation: `otelhttp.NewHandler` ends its span after the wrapped handler returns, and the current `queryRAG` handler streams synchronously, so the middleware genuinely covers the whole stream today. That property is load-bearing for D-31 and is asserted by a regression test that holds the stream open — if the handler ever becomes asynchronous, that test is what fails."
    - "Two of the five leaf spans (`dense_search`, `graph_traversal`) require a temporary LanceDB store fixture and `embedding_request` requires a mock HTTP server. All three fixture families already exist in `engine/src/tests.rs`; if a fixture turns out to be unavailable for one of them, that is a finding to record, not grounds to downgrade the span to a source-only assertion."
  prohibitions:
    - statement: "Do not create a node span inside any node implementation; the runner is the single owner of the five node spans."
      flagged: true
    - statement: "Do not put query text, query embeddings, evidence or document excerpts, assembled prompts, or answer text into any span attribute."
      flagged: true
    - statement: "Do not derive degraded state from the notice set in the gateway; the single derivation lives in the engine and is consumed, not recomputed."
      flagged: true
    - statement: "Do not let instrumentation change observable workflow behaviour — no altered timeouts, retry counts, cancellation ordering, notice sets, or event sequence ordinals."
      flagged: true
    - statement: "Do not instrument the workflow test doubles or fake ports with leaf spans; a leaf span must correspond to a real external call."
      flagged: true
    - statement: "Do not move, duplicate or delete the single `tracing::Span::current().record(\"graph_augmentation\", tag)` call at `engine/src/service.rs:479`, and do not modify any of the three `graph_augmentation_*_is_observable_end_to_end` tests in `engine/src/tests.rs`. The field is made to resolve by DECLARING it on whichever span is current at that line, never by adding a second record call — two record calls would make those tests capture two values and fail their `assert_eq!` on a one-element slice."
      flagged: true
    - statement: "Do not write any test that asserts a total exported-span count for a query; span totals are a demo-trace observation recorded in the SUMMARY, not a contract."
      flagged: true
    - statement: "Do not install a process-global tracing subscriber from any test in this plan; use Plan 01's install-free `build_providers_and_layers` seam with `tracing::subscriber::set_default`, as `engine/src/tests.rs:7038-7127` already does."
      flagged: true
  artifacts:
    - path: "engine/src/workflow/runner.rs"
      provides: "Per-NodeKind span construction in run_node with the five D-29 span names and their node-level attributes."
    - path: "engine/src/service.rs"
      provides: "embedding_request, dense_search, bm25_search and graph_traversal leaf spans on the four production port adapters, plus terminal attributes on the query_rag span."
    - path: "engine/src/workflow/nodes/generate.rs"
      provides: "One llm_attempt span per generation attempt with attempt number and gen_ai attributes."
    - path: "gateway/main.go"
      provides: "Terminal-outcome attributes and explicit span status recorded on the otelhttp server span at the workflow_completed frame."
  key_links:
    - from: "gateway/main.go"
      to: "engine/src/workflow/runner.rs"
      via: "otelhttp server span is the parent of the engine node spans through the propagated gRPC trace context"
      pattern: "root_span_nesting"
    - from: "engine/src/workflow/runner.rs"
      to: "engine/src/service.rs"
      via: "node spans created in run_node become the parents of the leaf spans opened inside the production port adapters they call"
      pattern: "node_leaf_nesting"
---

<objective>
Expand the proven tracer into the full query-path span surface: five node spans owned by the runner, one leaf span per real external call including one per LLM attempt, and the terminal outcome plus span status recorded on the SSE root span.

Purpose: SC2 of the phase. This is where a Jaeger trace stops being "a request happened" and becomes an explanation of the query — which retrieval path ran, how long the graph traversal took, whether the model needed a second attempt.
Output: five node spans, five leaf span families, terminal attributes on the root span, and span-hierarchy tests driven by in-memory exporters.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
@D:/Repos/lancet/.claude/gsd-core/references/planner-reversibility.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-observability-evaluation-polish/06-CONTEXT.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-RESEARCH.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-PATTERNS.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-VALIDATION.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-01-SUMMARY.md
@CLAUDE.md
@rust-guidelines.md
@go-guidelines.md
@engine/src/workflow/runner.rs
@engine/src/workflow/node.rs
@engine/src/workflow/nodes/generate.rs
@engine/src/service.rs
@gateway/main.go
</context>

<scope_rationale>
Three expansion tasks over one proven mechanism, split by span ownership rather than by file: the runner owns node spans (Task 1), the production adapters and the generation node own leaf spans (Task 2), and the gateway owns the root span's terminal facts (Task 3). Wave 2, parallel with Plan 03 which touches only deployment files. Everything here depends on Plan 01's telemetry module, the installed parent context, and the pre-declared `engine/src/tests/telemetry_query.rs` module.
</scope_rationale>

<tasks>

<task type="auto" tdd="true">
  <name>Five workflow node spans, owned solely by the runner</name>
  <reversibility rating="reversible">Span construction is additive inside one dispatch function; removal restores the current behaviour exactly.</reversibility>
  <read_first>rust-guidelines.md; .planning/phases/06-observability-evaluation-polish/06-CONTEXT.md decisions D-29 and D-42; 06.2-RESEARCH.md "Pattern 3: Node and Leaf Span Ownership" including its ownership table, and the audit row refuting the claim that five node spans already exist; 06.2-PATTERNS.md section `engine/src/workflow/runner.rs` and "Shared Patterns → Span Ownership and Nesting"; engine/src/workflow/node.rs:74-99 (`NodeKind` and its five variants plus `name()`); engine/src/workflow/runner.rs:294-316 (`timeout_for_kind` and the string dispatch) and 318-404 (`run_node`: the node-started event at 327-328, the `prepare()` select at 330-345, the timing plus `tokio::select!` executing `node.run` at 347-357, and the result handling at 359-404); engine/src/telemetry/mod.rs as produced by Plan 01 (specifically the install-free `build_providers_and_layers` seam and the reason global `.init()` is prohibited in tests); engine/src/service.rs:459-497 (`ProductionGraphQueryPort::query_graph`) and specifically the `tracing::Span::current().record("graph_augmentation", tag)` call at line 479, plus the `graph_augmentation = tracing::field::Empty` declaration on the `query_rag` span at line 846 — read BOTH before writing any span, because the node span you add becomes `Span::current()` at 479 and an undeclared field turns that record into a silent no-op; engine/src/tests.rs:7036-7135 (`GraphAugmentationCaptureLayer`, its visitor, and the three `graph_augmentation_*_is_observable_end_to_end` tests, each of which asserts `captured.as_slice() == ["<one value>"]` — these must pass unmodified)</read_first>
  <files>engine/src/workflow/runner.rs, engine/src/workflow/mod.rs, engine/src/workflow/nodes/graph_context.rs, engine/src/workflow/nodes/generate.rs, engine/src/tests/telemetry_query.rs</files>
  <behavior>
    - `query_span_hierarchy_emits_exactly_five_node_spans`: a full workflow run against fake ports, recorded by an in-memory span exporter, exports exactly five spans whose names are the set `query_reformulation`, `hybrid_retrieval`, `graph_context_extraction`, `prompt_assembly`, `llm_generation` — no duplicates, no sixth node span.
    - `node_spans_nest_under_the_installed_parent`: every one of the five spans has the workflow's parent span as an ancestor and shares its trace ID, rather than starting a new root.
    - `node_span_records_duration_and_outcome`: each node span carries the node outcome attribute, and a node that times out carries the error outcome with an Error span status while still emitting the existing node-failed event unchanged.
    - `node_span_cancellation_does_not_leak`: a cancelled run closes every opened node span; no span is left unended in the exporter output.
    - `graph_augmentation_record_still_resolves_under_node_span`: with the `graph_context_extraction` node span active, `tracing::Span::current().record("graph_augmentation", …)` at `engine/src/service.rs:479` still produces exactly one observed record — asserted through a `GraphAugmentationCaptureLayer`-shaped capture, the same mechanism `engine/src/tests.rs` already uses.
    - The three pre-existing tests `graph_augmentation_succeeded_is_observable_end_to_end`, `graph_augmentation_no_match_found_is_observable_end_to_end` and `graph_augmentation_attempted_and_failed_is_observable_end_to_end` still pass UNMODIFIED, each capturing exactly one value.
    - The existing workflow event sequence, checkpoint ordinals, timeouts and cancellation ordering are unchanged — asserted by the pre-existing runner tests staying green.
  </behavior>
  <action>Read rust-guidelines.md before editing.

In `engine/src/workflow/runner.rs::run_node`, construct one `tracing` span per `NodeKind` using an exhaustive match on `kind` (the enum is closed at five variants, so the match must be exhaustive with no wildcard arm — a future sixth variant must break the build here rather than silently lose a span). Span names are exactly the D-29 names: `query_reformulation` for `ReformulateQuery`, `hybrid_retrieval` for `RetrieveHybrid`, `graph_context_extraction` for `ExtractGraphContext`, `prompt_assembly` for `AssemblePrompt`, `llm_generation` for `GenerateAnswer`. Declare per-node deferred fields with the empty-field marker and record them after the run: `hybrid_retrieval` records `lancet.retrieval.dense_count` and `lancet.retrieval.bm25_count`; `graph_context_extraction` records `lancet.graph.node_count` and `lancet.graph.edge_count`; `prompt_assembly` records `lancet.prompt.evidence_block_count`; `llm_generation` records `lancet.generation.attempts`; `query_reformulation` records `lancet.reformulation.variant_count`. Read those counts from the mutated `WorkflowContext` after the node returns, not from inside the node.

Three of those five counts already exist on `WorkflowContext` and are read directly: the retrieval counts from the vector-results and bm25-results vectors, the evidence-block count from the evidence-blocks vector, and the variant count from the variants vector. The other two do NOT exist yet and this task adds them as public `u32` fields on `WorkflowContext` in `engine/src/workflow/mod.rs`, defaulted to zero in `WorkflowContext::new` so no construction site changes: a graph node count and a graph edge count set by `ExtractGraphContextNode` where it already computes them while building the graph facts, and a generation attempt count set by `GenerateAnswerNode` reflecting how many provider attempts it actually made. These are observation fields, read by the runner and later by Plan 06's workflow-metadata population — that shared consumption is deliberate, so both the span attribute and the wire field report the same number rather than two independently derived ones. Setting them must not change any node's control flow.

Instrument the node execution so the span is entered across awaits: the span must wrap the existing `tokio::select!` at lines 350-357 — including both the cancellation arm and the timeout arm — via the `Instrument` future combinator, not a synchronous entered guard, so the active context follows the await. Do not restructure the select, change `node_timeout`, reorder the biased arms, or move the `start_time` capture at line 347; the span must be strictly additive around them.

On the result, record `lancet.node.outcome` on the span as one of the bounded constants `ok`, `error`, `timeout` or `cancelled`, and set an Error span status for the non-ok cases. Leave every existing `sink.send_event_or_cancel` call, the answer-chunk emission for `GenerateAnswer`, and the checkpoint call untouched.

Preserve the existing `graph_augmentation` observation — REVIEW-DRIVEN, and it breaks in THIS task, not Task 2. `ProductionGraphQueryPort::query_graph` calls `tracing::Span::current().record("graph_augmentation", tag)` at `engine/src/service.rs:479`. `tracing` only records fields DECLARED at span construction, and today the only declaration is `graph_augmentation = tracing::field::Empty` on the `query_rag` span at `service.rs:846`, which is `Span::current()` at line 479 because nothing sits between them. The moment this task's `graph_context_extraction` node span becomes current at that line, the record silently does nothing and the three `graph_augmentation_*_is_observable_end_to_end` tests fail their `assert_eq!(captured.as_slice(), ["…"])` on an empty slice. Fix it by DECLARATION, not relocation: add `graph_augmentation = tracing::field::Empty` to the `graph_context_extraction` arm's `info_span!` in `run_node`. Leave the `.record` call at `service.rs:479` exactly where it is, add no second record call anywhere (two calls would make those tests capture two values and fail on length), and do not edit the three tests.

Do NOT add spans in any file under `engine/src/workflow/nodes/`; the runner is the sole owner of node spans. Leaf spans are Task 2's and live at the I/O boundary.

Write the named tests in `engine/src/tests/telemetry_query.rs`, driving the workflow through the existing fake-port fixtures with an in-memory span exporter. Install layers with Plan 01's install-free `build_providers_and_layers` seam plus `tracing::subscriber::set_default`, never a global `.init()` — `engine/src/tests.rs:7038-7127` installs scoped subscribers in the same test process and a global install would panic or fight them.</action>
  <acceptance_criteria>
    - `run_node` contains exactly one span-construction site, reached through an exhaustive `match` on `NodeKind` with no wildcard arm.
    - `WorkflowContext` gains graph node count, graph edge count and generation attempt count as public `u32` fields defaulted to zero in `WorkflowContext::new`, set by the graph-context and generation nodes without altering either node's control flow, and read by the runner when recording node-span attributes.
    - The five span names are exactly the D-29 names, and no file under `engine/src/workflow/nodes/` creates a span named any of them.
    - The node span wraps the existing `tokio::select!` through the async instrument combinator; `node_timeout`, the biased arm order, and the `start_time` capture are unmodified.
    - `lancet.node.outcome` is drawn from the four named constants only, and non-ok outcomes set an Error span status.
    - `query_span_hierarchy_emits_exactly_five_node_spans`, `node_spans_nest_under_the_installed_parent`, `node_span_records_duration_and_outcome`, `node_span_cancellation_does_not_leak` and `graph_augmentation_record_still_resolves_under_node_span` pass.
    - `grep -c 'record("graph_augmentation"' engine/src/` returns exactly 1, at `engine/src/service.rs:479`, unmoved; and `graph_augmentation_succeeded_is_observable_end_to_end`, `graph_augmentation_no_match_found_is_observable_end_to_end` and `graph_augmentation_attempted_and_failed_is_observable_end_to_end` all pass UNMODIFIED, each capturing exactly one value. This pair of checks — not "the field is declared on span X" — is the gate, because it catches the breakage regardless of which span turns out to be current.
    - No test added by this task asserts a total exported-span count, and no test installs a process-global subscriber.
    - The complete pre-existing engine suite still passes, proving the instrumentation is behaviour-neutral.
    - `cargo clippy --manifest-path engine/Cargo.toml -- -D warnings` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- query_span_hierarchy_emits_exactly_five_node_spans</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- node_spans_nest_under_the_installed_parent</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- node_span_cancellation_does_not_leak</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- graph_augmentation_</automated>
    <automated>cargo clippy --manifest-path engine/Cargo.toml -- -D warnings</automated>
  </verify>
  <done>Every query produces exactly five correctly named, correctly nested node spans from one owner, with bounded outcome attributes and no behavioural change.</done>
</task>

<task type="auto" tdd="true">
  <name>Leaf spans at every real external call, including one per LLM attempt</name>
  <reversibility rating="reversible">Each leaf span wraps an existing call at its adapter; removal is local.</reversibility>
  <read_first>rust-guidelines.md; .planning/phases/06-observability-evaluation-polish/06-CONTEXT.md decisions D-29 and D-42; 06.2-RESEARCH.md "Pattern 3" ownership table (which names each adapter by symbol) and "Anti-Patterns to Avoid"; 06.2-PATTERNS.md section `engine/src/workflow/nodes/*.rs`; engine/src/service.rs:240-340 (`attempt_graph_augmentation` and its `fetch_neighborhood` call at 327), 417-458 (`ProductionEmbeddingPort` and its `QueryEmbeddingPort` impl at 422), 459-505 (`ProductionGraphQueryPort::query_graph` at 460 wrapping `attempt_graph_augmentation` at 471), 506-563 (`ProductionDenseRetrievalPort::retrieve_dense` at 506), 564-600 (`ProductionBm25RetrievalPort::retrieve_bm25` at 564); engine/src/workflow/nodes/generate.rs:100-150 (the first `generator.generate` call at 114, the retryability check at 126-132, and the byte-identical second attempt at 140); engine/src/workflow/nodes/retrieve.rs:78-140 (the dense call at 83 and the per-variant BM25 call at 136, for context on which layer must NOT hold the span); engine/src/tests.rs (existing temporary LanceDB store, in-memory BM25 index and mock HTTP server fixtures)</read_first>
  <files>engine/src/service.rs, engine/src/workflow/nodes/generate.rs, engine/src/tests/telemetry_query.rs</files>
  <behavior>
    - `leaf_span_dense_search_wraps_real_lancedb_call`: a `ProductionDenseRetrievalPort::retrieve_dense` call against a temporary LanceDB store exports one `dense_search` span carrying `db.system`, `db.operation` and `lancet.index.generation`, nested under the caller's span.
    - `leaf_span_bm25_search_wraps_real_index_call`: a `ProductionBm25RetrievalPort::retrieve_bm25` call against an in-memory BM25 index exports one `bm25_search` span carrying `lancet.retrieval.path` set to the bm25 constant and `lancet.index.generation`.
    - `leaf_span_embedding_request_wraps_real_http_call`: a `ProductionEmbeddingPort` embedding call against a mock HTTP server exports one `embedding_request` span carrying the semantic-convention request-model attribute and no query text.
    - `leaf_span_graph_traversal_wraps_real_traversal`: a `ProductionGraphQueryPort::query_graph` call against a temporary store exports one `graph_traversal` span carrying `lancet.graph.node_count` and `lancet.graph.edge_count`.
    - `llm_attempt_spans_are_two_siblings_on_retry`: a generation run whose first attempt fails with a retryable error and whose second succeeds, driven by a real `OpenRouterGenerator` against a mock HTTP server, exports exactly two `llm_attempt` spans that are siblings under the single `llm_generation` node span, carrying `attempt` 1 and 2 and per-attempt outcome attributes.
    - `llm_attempt_span_is_single_without_retry`: a first-attempt success exports exactly one `llm_attempt` span with `attempt` 1.
    - `leaf_spans_absent_for_fake_ports`: a workflow run driven entirely by fake ports exports the five node spans and zero leaf spans, proving leaf spans track real I/O rather than the call shape.
    - `graph_augmentation_record_lands_on_graph_traversal_leaf`: with the `graph_traversal` leaf span active, the single `.record` at `engine/src/service.rs:479` produces exactly one observed value, and the three pre-existing `graph_augmentation_*_is_observable_end_to_end` tests still pass UNMODIFIED with exactly one captured value each.
  </behavior>
  <action>Read rust-guidelines.md before editing.

Add one leaf span per real external call, at the adapter that performs it, not at the node that calls the adapter. In `engine/src/service.rs`: wrap the embedding call inside the `QueryEmbeddingPort` impl for `ProductionEmbeddingPort` (impl at line 422) in a span named `embedding_request`; wrap the traversal inside `ProductionGraphQueryPort::query_graph` (line 460, around the `attempt_graph_augmentation` call at 471) in a span named `graph_traversal`; wrap the search inside `ProductionDenseRetrievalPort::retrieve_dense` (line 506) in a span named `dense_search`; wrap the retrieval inside `ProductionBm25RetrievalPort::retrieve_bm25` (line 564) in a span named `bm25_search`. Each of these returns a boxed future, so attach the span with the async instrument combinator on the returned future rather than an entered guard.

Attributes, per D-42 — semantic conventions where they exist, domain names elsewhere. `dense_search` carries `db.system`, `db.operation` and the domain `lancet.index.generation` taken from the pinned `CorpusSnapshot` generation the port already holds; `bm25_search` carries `lancet.retrieval.path` set to a `bm25` constant plus `lancet.index.generation`; `embedding_request` carries the semantic-convention request-model key; `graph_traversal` carries `lancet.graph.node_count` and `lancet.graph.edge_count` recorded after the call. Take every `gen_ai.*` and `db.*` key from the pinned `opentelemetry-semantic-conventions` 0.32.1 constants where the crate exposes one, and only hand-write a key when it has no constant. Record counts and identifiers only — never the query string, the embedding vector, or retrieved text.

In `engine/src/workflow/nodes/generate.rs`, wrap EACH of the two `generator.generate` call sites (line 114 and line 140) in its own span named `llm_attempt`, carrying `attempt` set to 1 and 2 respectively, the semantic-convention request-model key, and a per-attempt outcome attribute recorded after the call. Two sibling spans under the one `llm_generation` node span is the observable form of D-12's single retry; do not merge them into one span with a retry counter. Leave the retryability classification at lines 126-132, the byte-identical request snapshot reuse, and the error mapping exactly as they are — this task adds spans and records attributes, nothing else.

Hand the `graph_augmentation` field down to the leaf — REVIEW-DRIVEN, and it is a one-line declaration, not a code move. The `graph_traversal` leaf span you open inside `ProductionGraphQueryPort::query_graph` becomes `Span::current()` at line 479, taking that role over from the `graph_context_extraction` node span Task 1 declared it on. Declare `graph_augmentation = tracing::field::Empty` on the `graph_traversal` `info_span!` as well. Leave the single `.record` call at line 479 exactly where it is; add no second record call. Task 1's declaration on the node span may stay (it simply renders as an empty field once the leaf takes over) — leaving it is harmless and is NOT an acceptance criterion either way. The `graph_augmentation = tracing::field::Empty` declaration on the `query_rag` span at `service.rs:846` is likewise now permanently empty; leaving it is acceptable and removing it is acceptable, but if you remove it you must confirm the three capture tests still pass, because they are field-name-scoped and not span-scoped.

Record `gen_ai.usage.*` token attributes on the `llm_attempt` span when the provider returned usage. The `ModelOutput` type already carries an optional `ModelUsage` with prompt, completion and total token counts, so this needs no new plumbing; when usage is absent, omit the attributes rather than recording zero.

Write the seven named tests in `engine/src/tests/telemetry_query.rs`, reusing the temporary LanceDB store, in-memory BM25 index and mock HTTP server fixtures that already exist in `engine/src/tests.rs`. The retry test must drive a real `OpenRouterGenerator` against a mock server — a generation test double cannot prove that two real provider calls produce two sibling spans.</action>
  <acceptance_criteria>
    - Exactly four leaf-span construction sites exist in `engine/src/service.rs`, one inside each of the four production port adapters named above, and none in any file under `engine/src/workflow/nodes/` other than the two `llm_attempt` sites.
    - Every leaf span is attached to the adapter's returned `BoxFuture` with the async `Instrument` combinator (`.instrument(span)`), never with a synchronous `entered()`/`enter()` guard held across an `.await` — a `Span::current()` read taken after an await inside an `enter()`-guarded body is not guaranteed to be the leaf. `grep -n "\.enter()\|entered()" engine/src/service.rs` returns no hit inside the four adapters.
    - `grep -c 'record("graph_augmentation"' engine/src/` still returns exactly 1, unmoved from `engine/src/service.rs:479`, and the three `graph_augmentation_*_is_observable_end_to_end` tests pass UNMODIFIED with exactly one captured value each.
    - Exactly two `llm_attempt` span-construction sites exist, one per `generator.generate` call in `GenerateAnswerNode`.
    - Every `gen_ai.*` and `db.*` attribute key resolves from a pinned semantic-convention constant where the crate exposes one; hand-written keys use the `lancet.` domain prefix.
    - No leaf span records query text, an embedding vector, evidence text, an assembled prompt, or answer text.
    - `gen_ai.usage.*` attributes appear only when the provider reported usage, and are omitted rather than zeroed when it did not.
    - All seven named tests pass, and the retry test drives a real `OpenRouterGenerator` against a mock HTTP server.
    - `leaf_spans_absent_for_fake_ports` proves fake-port runs emit five node spans and zero leaf spans.
    - The generation retry classification, request-snapshot reuse and error mapping are unchanged, proven by the pre-existing generation tests staying green.
    - `cargo clippy --manifest-path engine/Cargo.toml -- -D warnings` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- llm_attempt_spans_are_two_siblings_on_retry</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- leaf_span_dense_search_wraps_real_lancedb_call</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- leaf_span_bm25_search_wraps_real_index_call</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- leaf_span_graph_traversal_wraps_real_traversal</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- leaf_spans_absent_for_fake_ports</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- graph_augmentation_</automated>
    <automated>cargo clippy --manifest-path engine/Cargo.toml -- -D warnings</automated>
  </verify>
  <done>Every real external call on the query path has its own child span with convention-correct attributes, a retried generation shows two sibling attempt spans, the pre-existing `graph_augmentation` observation still resolves under the new leaf, and no span carries user content.</done>
</task>

<task type="auto" tdd="true">
  <name>SSE root span lifetime and terminal outcome</name>
  <reversibility rating="reversible">Attribute recording and status setting are additive at one handler site.</reversibility>
  <read_first>go-guidelines.md; .planning/phases/06-observability-evaluation-polish/06-CONTEXT.md decisions D-31, D-37 and D-42; 06.2-RESEARCH.md the audit row "Manual SSE root-span lifecycle is required..." (which CORRECTS the candidate claim: the current handler streams synchronously so the middleware span already covers the stream, and a custom root span would risk a duplicate server span) and "Pattern 2"; 06.2-PATTERNS.md section `gateway/main.go`; gateway/main.go:533-631 (`queryRAG`: the engine stream open at 572, the first-frame receive at 578, header writes at 584-595, the first-frame terminal check at 599-601, the receive loop at 606-630 and its terminal check at 626-628) and 633-700 (`writeWorkflowEvent` and the metadata map it already builds); gateway/main_test.go (existing `app{...}.routes()` handler-test conventions and the roughly thirty logger construction sites that must keep compiling); gateway/internal/telemetry/telemetry.go as produced by Plan 01; proto/lancet/v1/lancet.proto:245-253 (`WorkflowCompletedEvent` and its `metadata` field) and its `NoticeCode` enum at line 72</read_first>
  <files>gateway/main.go, gateway/main_test.go, scripts/engine-test-targets.sh, scripts/gateway-test-targets.sh</files>
  <behavior>
    - `TestSSERootSpanLifecycle`: a request served through the `otelhttp`-wrapped handler against a slow engine stream produces exactly one server span whose duration is at least the stream duration and which ends only after the final frame is written — proving the middleware span covers the whole stream and that no second server span is created.
    - `TestRootSpanTerminalAttributesOnSuccess`: a stream ending in a successful `workflow_completed` frame leaves the server span carrying `lancet.answer_basis`, `lancet.workflow.success` true, a `lancet.notice_codes` attribute listing the terminal notice code names, and an Ok span status.
    - `TestRootSpanTerminalAttributesOnFailure`: a failed `workflow_completed` frame leaves `lancet.workflow.success` false and an Error span status.
    - `TestRootSpanTerminalAttributesOnEOFWithoutTerminal`: a stream that ends without a `workflow_completed` frame leaves an Error span status and an attribute naming that condition, matching the existing stream-error code the handler already writes.
    - `TestRootSpanNoticeCodesAreBounded`: the recorded `lancet.notice_codes` values are enum names from the published notice-code vocabulary, never free-form notice message text.
  </behavior>
  <action>Read go-guidelines.md, detecting the Go version from gateway/go.mod, before editing.

Do NOT create a manual root span. RESEARCH corrected the candidate design on this point: `otelhttp.NewHandler` ends its span after the wrapped handler returns and the current `queryRAG` handler receives and writes the whole SSE stream synchronously inside one call, so the middleware span already satisfies D-31's "root span covers the whole SSE stream". Adding a second root span would produce duplicate server spans. Instead, obtain the active span from the request context inside `queryRAG` and record onto it.

At the two places `queryRAG` already recognises the terminal frame — the first-frame check at lines 599-601 and the loop check at lines 626-628 — record the terminal outcome onto the request span before the handler returns: `lancet.answer_basis` from the terminal frame's final response, `lancet.workflow.success` from the frame's success flag, and `lancet.notice_codes` as a sorted, de-duplicated list of the typed notice-code enum NAMES on the terminal frame. Set the span status explicitly: Ok when the frame reports success, Error with the frame's error message otherwise. Also record the existing correlation identifier as `lancet.correlation_id`, so the shipped wire identity is discoverable from the trace without becoming the trace identity (D-27).

At the two early-return error paths the handler already has — the EOF-without-terminal branch at lines 611-616 and the receive-error branch at 617-622 — set an Error span status and record a bounded `lancet.stream_error_code` attribute reusing the stream-error code constants the handler already writes to the client. Do not invent a parallel error vocabulary.

Do NOT compute a degraded flag here. The single degradation derivation is an engine-side rule (the enumerated notice-code set) and Plan 06 owns populating it and forwarding it to this span as `lancet.degraded_mode`. Recording it independently here would create a second derivation that can drift from the wire value.

Do not modify `routes()` and do not wrap the handler anywhere except the `run()` server construction Plan 01 already changed, so `gateway/main_test.go`'s direct `routes()` calls keep working. `app.logger` stays `*zap.Logger`.

Add the five named tests to `gateway/main_test.go` using an in-memory span exporter and the existing engine-stub conventions in that file. For the lifecycle test, hold the stub stream open with a measurable delay between frames and assert the span duration against it.

How the tests obtain a wrapped handler — REVIEW-DRIVEN (Cursor MEDIUM), state it rather than leaving it implied. `gateway/main_test.go`'s existing tests call `app{...}.routes()` directly and get an UNWRAPPED handler, so a test that needs a server span must build its own wrapper: construct `otelhttp.NewHandler(app{...}.routes(), "lancet-gateway", otelhttp.WithSpanNameFormatter(...))` inside the test, mirroring exactly what `run()` does at the `newHTTPServer` call site, and serve that through `httptest`. This keeps `routes()` unmodified and leaves every existing handler test's behaviour untouched, which is the whole reason Plan 01 wraps only in `run()`. Do NOT add the wrapper to `routes()` to make the tests simpler.

Then re-pin the count gates: enumerate with `cargo test --manifest-path engine/Cargo.toml --list` and `cd gateway && go test -list '.*' ./...`, update `scripts/engine-test-targets.sh`'s TOTAL and lib pins for this plan's Rust tests, and update `scripts/gateway-test-targets.sh`'s `gateway` package count and EXPECTED_TOTAL for the Go tests added here. Add one line to each script's invariant-history comment. Never lower a pinned count.</action>
  <acceptance_criteria>
    - `gateway/main.go` creates no manual server span; terminal facts are recorded onto the span obtained from the request context.
    - `lancet.answer_basis`, `lancet.workflow.success`, `lancet.notice_codes` and `lancet.correlation_id` are recorded at the terminal frame, and span status is set explicitly to Ok or Error on every exit path including the two early-return error branches.
    - `lancet.notice_codes` values are typed enum names from the published vocabulary; no notice message text is recorded.
    - No degraded-mode value is computed in the gateway.
    - `routes()` is unmodified and `gateway/main_test.go`'s existing tests compile and pass without edits to their construction sites; the five new tests build their own `otelhttp.NewHandler` wrapper in the test body, matching `run()`'s wrapping.
    - `lancet.notice_codes` is written as a SORTED, DE-DUPLICATED list of typed enum NAMES (for example `RETRIEVAL_DEGRADED_DENSE`), never enum integers and never localized notice message text — sortedness and de-duplication are asserted by `TestRootSpanNoticeCodesAreBounded`.
    - All five named tests pass, and the lifecycle test asserts span duration against a real inter-frame delay.
    - `scripts/engine-test-targets.sh` and `scripts/gateway-test-targets.sh` pins match the enumerated counts, with no pin reduced, and both scripts exit 0.
    - `cd gateway && go vet ./...` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd gateway && go test ./... -run 'TestSSERootSpanLifecycle|TestRootSpanTerminal|TestRootSpanNoticeCodesAreBounded'</automated>
    <automated>cd gateway && go vet ./...</automated>
    <automated>sh scripts/gateway-test-targets.sh</automated>
    <automated>sh scripts/engine-test-targets.sh</automated>
  </verify>
  <done>One server span spans the entire SSE stream, carries the terminal answer basis, success flag, notice-code vocabulary and correlation identifier, and reports Ok or Error status on every exit path.</done>
</task>

</tasks>

## Artifacts this phase produces

**Created by this plan**
- New span names: `query_reformulation`, `hybrid_retrieval`, `graph_context_extraction`, `prompt_assembly`, `llm_generation` (node spans); `embedding_request`, `dense_search`, `bm25_search`, `graph_traversal`, `llm_attempt` (leaf spans).
- New span attributes: `lancet.node.outcome`, `lancet.retrieval.dense_count`, `lancet.retrieval.bm25_count`, `lancet.retrieval.path`, `lancet.index.generation`, `lancet.graph.node_count`, `lancet.graph.edge_count`, `lancet.prompt.evidence_block_count`, `lancet.generation.attempts`, `lancet.reformulation.variant_count`, `attempt`, `lancet.answer_basis`, `lancet.workflow.success`, `lancet.notice_codes`, `lancet.correlation_id`, `lancet.stream_error_code`, plus the pinned semantic-convention `gen_ai.request.model`, `gen_ai.usage.*`, `db.system` and `db.operation` keys.
- New bounded constant sets: node-outcome values `ok` / `error` / `timeout` / `cancelled`; retrieval-path value `bm25`.
- New tests in `engine/src/tests/telemetry_query.rs` and `gateway/main_test.go`.

**Modified by this plan**
- `engine/src/workflow/runner.rs`, `engine/src/workflow/mod.rs`, `engine/src/workflow/nodes/generate.rs`, `engine/src/workflow/nodes/graph_context.rs`, `engine/src/service.rs`, `gateway/main.go`, `gateway/main_test.go`, `scripts/engine-test-targets.sh`, `scripts/gateway-test-targets.sh`.

<review_dispositions>
Cross-AI review feedback from `06.2-REVIEWS.md`, resolved into this plan.

| Finding | Source | Disposition | Where it landed |
|---|---|---|---|
| HIGH — `graph_augmentation` is recorded on `Span::current()`, which today is `query_rag`; leaf spans make the record a no-op and existing capture tests go dark | Cursor + Agreed Concern | INCORPORATED, and the diagnosis SHARPENED: `tracing` binds a span's fields at construction, so the breakage begins in Task 1 (the node span), not Task 2 (the leaf span) as the review implies. The fix is a field DECLARATION on whichever span is current, in both tasks, with the single `.record` at `service.rs:479` never moving | Task 1 action + Task 2 action; new must_have truth; new prohibition; behaviors `graph_augmentation_record_still_resolves_under_node_span` and `graph_augmentation_record_lands_on_graph_traversal_leaf`; grep + unmodified-test acceptance criteria in both tasks; `<verify>` runs the three pre-existing tests |
| MEDIUM — tests that need an `otelhttp`-wrapped handler vs "wrap only in `run()`" | Cursor | INCORPORATED | Task 3 action spells out building the wrapper inside the test; acceptance criterion added |
| MEDIUM — D-29 "8–10 spans" vs the actual ~12-span tree | Cursor | INCORPORATED as a clarification plus a guard | must_have truth now states the exact inventory (10 engine spans from this plan + `query_rag` + the HTTP server span); new prohibition forbids any total-span-count assertion; only the five-node-span count stays exact |
| LOW — `BoxFuture` instrumentation must use `.instrument()`, not an entered guard | Antigravity | ALREADY IN PLAN, now made checkable rather than merely stated | Task 2 acceptance adds a `grep` criterion for `.enter()`/`entered()` inside the four adapters |
| Suggestion — `lancet.notice_codes` must be sorted, de-duplicated enum names, not integers or message text | Antigravity | ALREADY IN PLAN (Task 3 action), now promoted to an acceptance criterion | Task 3 acceptance |
| Agreed Concern — subscriber test isolation | Both | INCORPORATED (mechanism owned by Plan 01) | New prohibition here; Task 1 action requires `build_providers_and_layers` + `set_default` |
| LOW — `fetch_neighborhood` at `service.rs:327` matches the plan | Cursor | NO CHANGE NEEDED — reviewer confirmed the citation is accurate | n/a |
</review_dispositions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow context → span attributes | Everything recorded here leaves the process over OTLP. |
| Engine terminal frame → gateway root span | Terminal facts cross the gRPC boundary and are re-recorded on the server span. |
| Provider response → `gen_ai.usage.*` attributes | Values originate from an external provider. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.2-02-01 | Information Disclosure | Leaf and node span attributes | high | mitigate | Counts, identifiers and bounded enums only; query text, embeddings, evidence, prompts and answers are prohibited and the prohibition is an acceptance criterion on every span-construction site. |
| T-06.2-02-02 | Information Disclosure | `lancet.notice_codes` on the root span | medium | mitigate | Typed enum names only, never notice message text, which can embed model or document content. |
| T-06.2-02-03 | Tampering | Provider-supplied usage values recorded as attributes | low | accept | Usage counts are numeric telemetry with no authorization effect; the existing generation limit checks already bound them, and a wrong count misleads a dashboard rather than the system. |
| T-06.2-02-04 | Denial of Service | Span volume per query | medium | mitigate | D-29 fixes the surface at five node spans plus one leaf span per real external call; the exactly-five test and the fake-port zero-leaf test keep it from growing silently. The documented inventory (10 engine spans from this plan, plus `query_rag`, plus the gateway server span) is recorded in the SUMMARY, deliberately NOT asserted as a total, so the guard stays on the enumerated names rather than a brittle count. |
| T-06.2-02-07 | Repudiation | Existing `graph_augmentation` observation silently lost when a new span becomes `Span::current()` at `service.rs:479` | high | mitigate | The field is declared on whichever span is current in each task; exactly one `.record` call exists repo-wide and never moves; the three pre-existing `graph_augmentation_*_is_observable_end_to_end` tests must pass UNMODIFIED, which is what detects the loss (an undeclared field fails silently at runtime, not at compile time). |
| T-06.2-02-05 | Repudiation | Instrumentation silently altering workflow behaviour | high | mitigate | Spans are strictly additive around the existing select, timeout, retry and event code; the full pre-existing suite at raised pins is the regression evidence. |
| T-06.2-02-06 | Tampering | Degradation recorded from two independent derivations | medium | mitigate | The gateway consumes the engine's single derived value (Plan 06) and computes nothing; prohibited in `must_haves.prohibitions`. |
</threat_model>

<verification>
- `cargo test --manifest-path engine/Cargo.toml --locked && (cd gateway && go test ./...)` exits 0.
- `cargo clippy --manifest-path engine/Cargo.toml -- -D warnings` exits 0 and `cd gateway && go vet ./...` exits 0.
- `sh scripts/engine-test-targets.sh` and `sh scripts/gateway-test-targets.sh` both exit 0.
</verification>

<success_criteria>
- A single query produces exactly five node spans plus one leaf span per real external call — 10 engine spans created by this plan, sitting under the pre-existing `query_rag` span and the gateway server span. No test asserts a total.
- A retried generation shows two sibling `llm_attempt` spans.
- The pre-existing `graph_augmentation` observation still resolves, proven by the three `graph_augmentation_*_is_observable_end_to_end` tests passing unmodified.
- The gateway server span covers the entire SSE stream and carries the terminal outcome plus an explicit status.
- No span carries user query text, evidence text, prompts or answers.
</success_criteria>

<output>
Create `.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-02-SUMMARY.md`, recording the resolved semantic-convention constant names used for the `gen_ai.*` and `db.*` keys, the OBSERVED total span count for one normal query (an observation, not an asserted contract) against the "roughly 8-10" D-29 target, which spans ended up declaring `graph_augmentation` and whether the `query_rag:846` declaration was left in place or removed, and the raised test-count pins.
</output>
