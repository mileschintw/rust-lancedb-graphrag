---
phase: 06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit
plan: 09
type: execute
wave: 6
depends_on:
  - 06.2-01
files_modified:
  - gateway/internal/telemetry/telemetry.go
  - gateway/internal/telemetry/telemetry_test.go
  - scripts/gateway-test-targets.sh
  - .planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/COVERAGE.md
autonomous: true
gap_closure: true
requirements:
  - OBS-01
estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A configured `https://` OTLP endpoint builds trace, metric, and log gRPC exporters with TLS credentials and without insecure transport (CR-04, D-84 honoring the scheme config already validates)."
    - "A configured `http://` OTLP endpoint still uses insecure gRPC transport so the local Collector on 127.0.0.1:4317 keeps working (D-38, D-39)."
    - "`Init` still never returns an error; exporter construction failure remains one bounded stderr warning (D-38)."
    - "COVERAGE.md OTLP exporter TLS / client authentication row is INTEGRATE for client export scheme honoring; D-06 documented-only ingress TLS stays out of this row's reason text."
  flagged_assumptions:
    - "OBS-01 edge-probe returned category unclassified, status unresolved (coverage applicable 1, resolved 0, unresolved 1). Not auto-resolved. CR-04 is the concrete OBS-01 slice here."
    - "Honoring https on an already-validated otlp_endpoint is a bugfix of existing Init, not a new API integration and not DEBT-CR-04 ingress TLS. No in-process TLS gRPC listener is added (coarse granularity; 06.2-REVIEWS.md option a)."
  prohibitions:
    - statement: "Must not force insecure gRPC transport on an https OTLP endpoint (silent TLS downgrade)."
      flagged: true
    - statement: "Must not make Init fail the process when the Collector is unreachable (D-38)."
      flagged: true
    - statement: "Must not add gateway HTTP server TLS, auth, or quotas (D-06 documented-only)."
      flagged: true
    - statement: "Must not treat TestOTLPExportersFollowEndpointSecurity as proof that New() option slices omit insecure transport; that proof is the WithInsecure source region grep."
      flagged: true
  artifacts:
    - path: "gateway/internal/telemetry/telemetry.go"
      provides: "Shared endpoint security helper and shared dial-option helper used by all three OTLP gRPC exporters."
    - path: "gateway/internal/telemetry/telemetry_test.go"
      provides: "Table tests for scheme classification; coverage-only exporters-follow test."
    - path: ".planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/COVERAGE.md"
      provides: "OTLP TLS row updated after the bugfix."
  key_links:
    - from: "gateway/internal/config/config.go"
      to: "gateway/internal/telemetry/telemetry.go"
      via: "validated http/https otlp_endpoint scheme selects insecure vs TLS credentials"
      pattern: "fail_closed_config"
---

## Phase User Story

**As an** operator who set `otlp_endpoint` to an `https://` URL, **I want to** export OTLP over TLS, **so that** telemetry is not silently sent in cleartext (CR-04, D-84).

<objective>
Close CR-04: branch gateway OTLP gRPC exporters on the parsed URL scheme. http keeps insecure transport for the local Collector; https uses client TLS. Prove https does not select insecure transport via a named source grep plus scheme-helper tests, not a tautological option-introspect test (D-84, D-38, D-06).

Purpose: config validation already accepts https; Init must not strip that guarantee.
Output: shared helpers, three exporters wired through them, table-driven classification tests, coverage-only follow test, COVERAGE.md row update, pin script using go test -list.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-observability-evaluation-polish/06-CONTEXT.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-VERIFICATION.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-REVIEW.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-REVIEWS.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/COVERAGE.md
@go-guidelines.md
@gateway/go.mod
@gateway/internal/telemetry/telemetry.go
@gateway/internal/telemetry/telemetry_test.go
@gateway/internal/config/config.go
@scripts/gateway-test-targets.sh
</context>

<scope_rationale>
Security-relevant silent downgrade on a primary trust boundary (process → Collector). Discretion: extract `otlpEndpointSecurity` and `otlpGRPCOptions` so tests assert scheme→(target, useTLS) without a TLS server. Apply the same helpers to trace, metric, and log exporters. Named gate for insecure-not-on-https is the source region grep (06.2-REVIEWS.md option a). ASVS L1: mitigate high. File-disjoint from 06.2-08 (Go vs Rust); may run the same wave.
</scope_rationale>

<review_dispositions>
- MEDIUM tautological exporters-follow test — incorporated option (a): grep is the named gate; TestOTLPExportersFollowEndpointSecurity is coverage-only. Rejected option (b) in-process TLS listener (coarse granularity; no concrete listener design in sources).
- LOW `go test ./... -count=0` — incorporated: pin measurement uses `go test -list '.*' ./...` matching scripts/gateway-test-targets.sh.
- LOW COVERAGE.md stale OPT-OUT — incorporated: Task 2 one-line row edit to INTEGRATE for client export TLS.
- LOW WR/IN — deferred to 06.2-07/T3 STATE.md (this plan does not own STATE.md).
- LOW truths schema — incorporated: unclassified edge is flagged_assumption only.
</review_dispositions>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end https OTLP endpoint does not select insecure transport</name>
  <reversibility rating="reversible">Exporter option branch behind helpers; local http default unchanged.</reversibility>
  <read_first>go-guidelines.md (Go version from gateway/go.mod: `go 1.25.0`); 06-CONTEXT.md D-06 D-38 D-84; 06.2-REVIEW.md CR-04; gateway/internal/config/config.go otlp_endpoint http/https validation (~100-106); gateway/internal/telemetry/telemetry.go Init endpoint trim and the three exporter constructors (~115-174); gateway/internal/telemetry/telemetry_test.go conventions; google.golang.org/grpc/credentials already available via the gateway grpc module</read_first>
  <files>gateway/internal/telemetry/telemetry.go, gateway/internal/telemetry/telemetry_test.go</files>
  <behavior>
    - Exported or package-visible `otlpEndpointSecurity(endpoint string) (target string, useTLS bool)`: trims http:// and https:// prefixes for the gRPC target host:port; useTLS is true iff the original endpoint has https scheme (after trim/parse); useTLS is false for http.
    - `TestOTLPEndpointSecurity`: https://collector.example:4317 → target collector.example:4317, useTLS true; http://127.0.0.1:4317 → target 127.0.0.1:4317, useTLS false; https://127.0.0.1:4317 → useTLS true.
  </behavior>
  <action>Read go-guidelines.md and gateway/go.mod (1.25.0) before editing Go. Add otlpEndpointSecurity in telemetry.go. Add otlpGRPCOptions(useTLS bool) that returns the gRPC exporter option list used by all three constructors: TLS credentials via credentials.NewClientTLSFromCert(nil, "") when useTLS is true; the insecure option only when useTLS is false. Init must call otlpEndpointSecurity once, pass the target into all three WithEndpoint options, and pass otlpGRPCOptions(useTLS) into each constructor (otlptracegrpc, otlpmetricgrpc, otlploggrpc). Do not return an error from Init (D-38). Add TestOTLPEndpointSecurity in telemetry_test.go. Do not add new Go modules unless credentials import is missing (it should come from google.golang.org/grpc). Do not add HTTP server TLS (D-06).</action>
  <verify>
    <automated>cd gateway && go test ./internal/telemetry -run TestOTLPEndpointSecurity -count=1</automated>
  </verify>
  <acceptance_criteria>
    - TestOTLPEndpointSecurity passes for http (useTLS false) and https (useTLS true) cases including loopback https (D-84).
    - Init uses otlpEndpointSecurity and otlpGRPCOptions rather than always appending the insecure option beside WithEndpoint.
    - Init signature still never returns error (D-38).
  </acceptance_criteria>
  <done>https endpoints are classified as TLS at the helper; tests lock that classification (CR-04 tracer).</done>
</task>

<task type="auto" tdd="true">
  <name>Named grep gate for exporters; coverage-only follow test; pin and COVERAGE row</name>
  <read_first>go-guidelines.md; gateway/internal/telemetry/telemetry.go three New() call sites and otlpGRPCOptions; scripts/gateway-test-targets.sh EXPECTED_TOTAL and gateway package count (currently 107 total, gateway/internal/telemetry 9); COVERAGE.md OTLP exporter TLS row; existing TestCollectorUnavailable if present</read_first>
  <files>gateway/internal/telemetry/telemetry.go, gateway/internal/telemetry/telemetry_test.go, scripts/gateway-test-targets.sh, .planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/COVERAGE.md</files>
  <behavior>
    - `TestOTLPExportersFollowEndpointSecurity` is coverage-only: it calls otlpGRPCOptions(true) and otlpGRPCOptions(false) so both branches compile and run, and it documents in a test comment that constructor option slices are opaque. It must not re-implement otlpEndpointSecurity under another name and assert the same table as TestOTLPEndpointSecurity as if that proved New() wiring.
    - Existing Collector-unavailable / Init-never-errors tests still pass with http://127.0.0.1:1 or similar.
  </behavior>
  <action>Addresses review concern: exporters-follow test was tautological if it only re-asserted the helper. Keep TestOTLPExportersFollowEndpointSecurity as a coverage-only test that exercises otlpGRPCOptions both branches without claiming to introspect otlptracegrpc.Option values. The named gate is the source region: the insecure constructor identifier must appear only inside otlpGRPCOptions when useTLS is false, and all three New() sites must call otlpGRPCOptions (not inline the insecure option). Raise gateway-test-targets.sh EXPECTED_TOTAL and the telemetry package line using `cd gateway && go test -list '.*' ./...` measured totals in the same commit; never lower. Change the COVERAGE.md row currently `OTLP exporter — TLS / client authentication | OPT-OUT | explicitly out of scope — loopback-only...` to decision INTEGRATE with reason that client OTLP gRPC export honors https vs http on the already-validated otlp_endpoint (CR-04); state D-06 documented-only ingress TLS is unchanged. Record in 06.2-09-SUMMARY.md that TestOTLPExportersFollowEndpointSecurity is coverage-only and the grep is the wiring gate.</action>
  <verify>
    <automated>cd gateway && go test ./internal/telemetry -count=1; sh scripts/gateway-test-targets.sh</automated>
  </verify>
  <acceptance_criteria>
    - All three OTLP gRPC exporters share otlpGRPCOptions (D-84).
    - Named gate: `grep -n 'WithInsecure' gateway/internal/telemetry/telemetry.go` lists only lines inside otlpGRPCOptions (the !useTLS branch), not the three New() call sites.
    - TestOTLPExportersFollowEndpointSecurity exists and is described as coverage-only in 06.2-09-SUMMARY.md.
    - `cd gateway && go test ./internal/telemetry` exits 0.
    - gateway-test-targets.sh matches `go test -list '.*' ./...` package totals after the new tests (not `go test ./... -count=0`).
    - COVERAGE.md TLS row decision is INTEGRATE with a CR-04 / client-export reason; D-06 ingress remains described as not in this row.
    - No new auth/quotas/HTTP timeouts (D-06).
  </acceptance_criteria>
  <done>Validated https OTLP URLs get TLS; local http Collector export still works; Init remains non-fatal; coverage matrix matches the bugfix (CR-04, D-38).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gateway process → OTLP collector | Telemetry (trace/log content may include session/correlation ids) leaves the process here. Configured https implies TLS; http on loopback is the local demo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.2-09-01 | Information Disclosure | OTLP gRPC insecure transport on https URLs | high | mitigate | Branch on scheme; TLS credentials for https; TestOTLPEndpointSecurity plus WithInsecure region grep as the wiring gate. Block-on-high: this is the CR-04 silent downgrade. |
| T-06.2-09-02 | Spoofing | default TLS cert pool | medium | mitigate | NewClientTLSFromCert(nil, "") uses system roots for https collectors; local demo remains http. |
| T-06.2-09-03 | Denial of Service | Init failing on TLS handshake at construction | medium | mitigate | Keep Init non-erroring; construction failures stay one stderr warning (D-38). Handshake failures at export time remain bounded SDK errors. |
| T-06.2-09-SC | Tampering | package installs | high | accept | No new modules expected; grpc credentials already on the graph. |
</threat_model>

<verification>
- `cd gateway && go test ./internal/telemetry -count=1` and `sh scripts/gateway-test-targets.sh`.
- Named WithInsecure region grep on telemetry.go.
- Do not expand DEBT-CR-04 ingress TLS.
</verification>

<success_criteria>
- https OTLP endpoints are not forced plaintext; http local export still works; Init never fails the service (CR-04, D-38, D-84).
- COVERAGE.md TLS row matches shipped client-export behavior.
</success_criteria>

## Artifacts this phase produces

- New helpers/tests in `gateway/internal/telemetry/` (`otlpEndpointSecurity`, `otlpGRPCOptions`, `TestOTLPEndpointSecurity`, coverage-only exporters-follow test)
- Updated COVERAGE.md OTLP TLS row
- No new deploy surfaces; no in-process TLS listener

<output>
Create `.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-09-SUMMARY.md` recording the helper signatures, that TestOTLPExportersFollowEndpointSecurity is coverage-only, the WithInsecure grep as named gate, and the new Go test counts from `go test -list '.*' ./...`.
</output>
