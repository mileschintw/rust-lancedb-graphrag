---
phase: 06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit
plan: 05
type: execute
wave: 4
depends_on:
  - 06.2-01
  - 06.2-02
  - 06.2-03
  - 06.2-04
files_modified:
  - engine/src/telemetry/metrics.rs
  - engine/src/workflow/runner.rs
  - engine/src/workflow/nodes/retrieve.rs
  - engine/src/workflow/nodes/graph_context.rs
  - engine/src/workflow/nodes/generate.rs
  - engine/src/workflow/nodes/assemble_prompt.rs
  - engine/src/ingest.rs
  - engine/src/tests/telemetry_metrics.rs
  - scripts/engine-test-targets.sh
autonomous: true
requirements:
  - OBS-01
estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Metrics ship with a real backend, not as a tracing side effect — this is D-33's deliberate widening of OBS-01's tracing-only wording, and every instrument here reaches Prometheus through the Collector exposition endpoint Plan 03 scrapes."
    - "The metric set is RAG-quality operational rather than generic request-rate telemetry, and consists of exactly the ten instruments D-35 enumerates: `lancet.rag.query.duration` (histogram by outcome), `lancet.rag.retrieval.path_failures` (counter by path and kind), `lancet.rag.answer.degraded` (counter by answer basis), `lancet.rag.citation.repairs` (counter by action), `lancet.rag.generation.retries` (counter by outcome), `lancet.rag.evidence.set_size` (histogram), `lancet.ingest.documents` (counter by outcome), `lancet.ingest.chunks` (counter), `lancet.index.rebuild.duration` (histogram by outcome) and `lancet.index.corpus_generation` (gauge)."
    - "Every metric maps to a decision made in this milestone: the retrieval-path failure counter observes D-13's degrade-not-fail behaviour, the degraded-answer counter observes D-10 through D-12's model-only opt-in, the citation counter observes D-14's repair and drop paths, the retry counter observes D-12's single bounded retry, and the rebuild duration and corpus generation instruments observe D-20 through D-25's rebuild-and-swap (D-35)."
    - "Every metric dimension is a bounded enum drawn from a named constant set — `outcome`, `path`, `kind`, `answer_basis` and `action`. No identifier, no free string, no raw text and no arbitrary model name is ever used as a metric attribute (D-35, D-42)."
    - "The corpus generation gauge reports the NUMERIC `nodes_version`, not the string `lance-{version}` label. A string-valued gauge is not an OTel metric, and the string generation stays a span attribute where it belongs."
    - "Every panel query in Plan 03's committed dashboard resolves against an instrument defined here, so the dashboard-as-code artifact and the instrument implementation cannot silently drift apart."
    - "Recording a metric never changes program behaviour: no notice is added or suppressed, no degrade decision is made at a metric site, and every recording reads state that already exists rather than recomputing it."
  flagged_assumptions:
    - "The corpus generation gauge is an asynchronous observable instrument reading the current `CorpusSnapshot`, which means it needs a callback holding a handle to the corpus store. If that ownership proves awkward from the metrics module, the fallback is an explicit synchronous record at each successful swap; whichever shape is chosen must be recorded in the SUMMARY, and the value must remain the numeric nodes version either way."
    - "The exported Prometheus series names are produced by the Collector, not by the instrument names. Plan 03 recorded the observed transformation rule; this plan's dashboard cross-check must use that recorded rule rather than re-deriving it."
    - "`lancet.rag.query.duration` already exists from Plan 01 with a two-value outcome set. Widening it to include the degraded outcome changes the meaning of existing series rather than adding a new instrument; that is intended and must be noted in the SUMMARY."
  prohibitions:
    - statement: "Do not use any unbounded value as a metric attribute — no session identifier, document identifier, trace identifier, query text, notice message, or arbitrary model name."
      flagged: true
    - statement: "Do not define an eleventh instrument beyond the ten D-35 enumerates without a recorded decision; the metric surface is a fixed contract shared with the committed dashboard."
      flagged: true
    - statement: "Do not report the string corpus generation as a gauge value; the gauge carries the numeric nodes version."
      flagged: true
    - statement: "Do not let a metric recording site make or alter a degrade, notice, retry or citation decision; recording reads state, it never decides."
      flagged: true
    - statement: "Do not derive the degraded-answer count from a second, metrics-local rule; it reads the answer basis and notice state the workflow already produced."
      flagged: true
  artifacts:
    - path: "engine/src/telemetry/metrics.rs"
      provides: "The ten D-35 instruments, their bounded attribute constant sets, and one recording function per instrument."
  key_links:
    - from: "engine/src/telemetry/metrics.rs"
      to: "deploy/grafana/dashboards/lancet-rag-operations.json"
      via: "each dashboard panel queries the Prometheus series produced from one instrument defined here, cross-checked by an explicit test"
      pattern: "metric_dashboard_contract"
    - from: "engine/src/workflow/nodes/retrieve.rs"
      to: "engine/src/telemetry/metrics.rs"
      via: "each degrade notice emission site records the corresponding bounded path-failure counter increment"
      pattern: "metric_recording_site"
---

<objective>
Deliver the full D-35 operational metric set on the mechanism Plan 01 proved with a single instrument: ten instruments, bounded dimensions only, recorded at the sites where the decisions they observe already happen, and reaching the Prometheus backend Plan 03 provisioned.

Purpose: SC4 of the phase. Traces explain one query; these metrics explain the system's behaviour over time — how often retrieval degrades, how often the model needs a second attempt, how often a citation gets repaired instead of dropped.
Output: `engine/src/telemetry/metrics.rs` fully populated, recording sites across the query and ingestion paths, and a test proving every committed dashboard panel resolves.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
@D:/Repos/lancet/.claude/gsd-core/references/planner-reversibility.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-observability-evaluation-polish/06-CONTEXT.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-RESEARCH.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-PATTERNS.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-VALIDATION.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-01-SUMMARY.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-03-SUMMARY.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-04-SUMMARY.md
@CLAUDE.md
@rust-guidelines.md
@engine/src/telemetry/metrics.rs
@engine/src/workflow/nodes/retrieve.rs
@engine/src/workflow/nodes/graph_context.rs
@engine/src/workflow/nodes/generate.rs
@engine/src/ingest.rs
</context>

<scope_rationale>
Three tasks split by what can independently break: the instrument definitions and their cardinality discipline (Task 1), the query-path recording sites (Task 2), and the ingestion and index recording sites plus the dashboard contract cross-check (Task 3). Wave 4 because it edits the node files Plan 02 owns in wave 2 and `engine/src/ingest.rs` which Plan 04 owns in wave 3, and because the dashboard cross-check needs Plan 03's recorded series-name transformation rule.
</scope_rationale>

<tasks>

<task type="auto" tdd="true">
  <name>The ten D-35 instruments with bounded dimensions and a cardinality guard</name>
  <reversibility rating="costly">Instrument names and attribute keys become the query surface of a committed dashboard, so renaming one later means regenerating and reviewing that dashboard alongside it.</reversibility>
  <read_first>rust-guidelines.md; .planning/phases/06-observability-evaluation-polish/06-CONTEXT.md decisions D-33, D-35 and D-42, and the `### Claude's Discretion` list; 06.2-RESEARCH.md "Pattern 5: Metrics with Bounded Dimensions" (including its point that the current corpus generation is a STRING and a string-valued gauge is not an OTel metric), "Anti-Patterns to Avoid" (the metric-labelling row), and the "High-cardinality metric labels" row of the Known Threat Patterns table; 06.2-PATTERNS.md sections `engine/src/telemetry/metrics.rs` and "Shared Patterns → Bounded Metrics Dimensions"; engine/src/telemetry/metrics.rs as produced by Plan 01 (the existing `lancet.rag.query.duration` histogram and its two outcome constants); engine/src/workflow/ports.rs:13-24 (`corpus_generation_from_nodes_version` and the `CorpusSnapshot` generation / nodes_version / rebuild_degraded fields); .planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-03-SUMMARY.md — Plan 03 (wave 2) is the OWNER of the ten instrument names and of the observed OTel-to-Prometheus series-name transformation rule; read both from that summary and implement against them rather than re-deriving either</read_first>
  <files>engine/src/telemetry/metrics.rs, engine/src/tests/telemetry_metrics.rs</files>
  <behavior>
    - `telemetry_metrics_registry_defines_exactly_ten_instruments`: the module exposes exactly the ten named D-35 instruments, no more and no fewer, asserted against an explicit expected name list rather than a count alone.
    - `telemetry_metrics_attributes_are_bounded`: every recording function accepts its dimension as a typed value or a `&'static str` drawn from a named constant set, so a caller cannot pass a runtime-constructed string; the test enumerates the permitted values per instrument.
    - `telemetry_metrics_corpus_generation_gauge_is_numeric`: the corpus generation instrument reports the numeric nodes version; recording it produces an integer data point, and no code path passes the `lance-` prefixed string label to it.
    - `telemetry_metrics_query_duration_outcome_set_widened`: the existing query-duration outcome constant set now includes the degraded value alongside completed and failed.
    - `telemetry_metrics_no_instrument_carries_an_identifier_attribute`: an exhaustive check over the recorded data points from a synthetic exercise of every recording function shows no attribute key or value resembling a session identifier, document identifier, trace identifier or free text.
  </behavior>
  <action>Read rust-guidelines.md before editing.

Populate `engine/src/telemetry/metrics.rs` with exactly the ten D-35 instruments, extending Plan 01's single histogram rather than replacing it.

Instrument one, `lancet.rag.query.duration`: existing millisecond histogram, attribute `outcome`; widen its constant set from completed and failed to also include a degraded value. Instrument two, `lancet.rag.retrieval.path_failures`: counter with attributes `path` (constants for dense, bm25 and graph) and `kind` (constants for timeout, error and unavailable). Instrument three, `lancet.rag.answer.degraded`: counter with attribute `answer_basis` (constants for retrieval, mixed and model_only). Instrument four, `lancet.rag.citation.repairs`: counter with attribute `action` (constants for repaired and dropped). Instrument five, `lancet.rag.generation.retries`: counter with attribute `outcome` (constants for recovered and exhausted). Instrument six, `lancet.rag.evidence.set_size`: histogram of evidence block counts, no attributes. Instrument seven, `lancet.ingest.documents`: counter with attribute `outcome` (constants for completed and failed). Instrument eight, `lancet.ingest.chunks`: counter, no attributes. Instrument nine, `lancet.index.rebuild.duration`: millisecond histogram with attribute `outcome` (constants for completed and failed). Instrument ten, `lancet.index.corpus_generation`: gauge reporting the NUMERIC nodes version.

This ten-instrument list is a phase-level contract OWNED by Plan 03, which fixed the names in wave 2 and whose committed dashboard already queries the Prometheus series derived from them. Cross-check the names above against `06.2-03-SUMMARY.md` before writing them; if they disagree, Plan 03's summary wins and the disagreement is a finding to record. Do not add an eleventh instrument and do not rename one without regenerating that dashboard in the same commit.

Enforce bounded dimensions structurally rather than by convention. Each recording function takes its dimension as either a small enum defined in this module or a `&'static str` that callers may only obtain from the module's own public constants — never a `String`, a `&str` of runtime origin, or a formatted value. That is what makes the high-cardinality threat unreachable rather than merely discouraged, and it is why the cardinality test can be exhaustive.

For the corpus generation gauge, take the numeric `nodes_version` (a `u64`) from `CorpusSnapshot`, never the `lance-` prefixed generation string produced by `corpus_generation_from_nodes_version` — that string belongs on spans, and a string-valued gauge is not a metric at all. Implement it as an asynchronous observable gauge whose callback reads the current corpus snapshot if that ownership is workable from this module; otherwise record it synchronously at each successful swap. Record which shape was chosen, and why, in the SUMMARY.

Expose one recording function per instrument with a name that states what is being recorded, and keep all instrument construction lazy behind a single shared meter so the module works whether or not a meter provider is installed.

Write the five named tests in `engine/src/tests/telemetry_metrics.rs`, the module Plan 01 pre-declared, using an in-memory metric reader.</action>
  <acceptance_criteria>
    - `engine/src/telemetry/metrics.rs` defines exactly ten instruments with exactly the D-35 names listed above.
    - Every recording function's dimension parameter is a module-defined enum or a `&'static str` obtainable only from module constants; no recording function accepts a `String` or a runtime-constructed string.
    - The corpus generation instrument reports a numeric nodes version; no call site passes the `lance-` prefixed string to it.
    - The query-duration outcome constant set includes completed, failed and degraded.
    - Instrument construction is lazy and safe when no meter provider is installed.
    - All five named tests pass, including the exhaustive no-identifier-attribute check.
    - `cargo clippy --manifest-path engine/Cargo.toml -- -D warnings` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- telemetry_metrics_registry_defines_exactly_ten_instruments</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- telemetry_metrics_attributes_are_bounded</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- telemetry_metrics_corpus_generation_gauge_is_numeric</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- telemetry_metrics_no_instrument_carries_an_identifier_attribute</automated>
    <automated>cargo clippy --manifest-path engine/Cargo.toml -- -D warnings</automated>
  </verify>
  <done>Ten RAG-operational instruments exist with structurally bounded dimensions, the corpus generation gauge is numeric, and a caller cannot pass an unbounded label even by accident.</done>
</task>

<task type="auto" tdd="true">
  <name>Query-path metric recording at the decision sites they observe</name>
  <reversibility rating="reversible">Each recording is a single additive call adjacent to an existing decision site.</reversibility>
  <read_first>rust-guidelines.md; .planning/phases/06-observability-evaluation-polish/06-CONTEXT.md decisions D-33 and D-35, plus D-10 through D-14 (the behaviours these counters observe); 06.2-RESEARCH.md "Pattern 5"; engine/src/workflow/nodes/retrieve.rs:60-75 (the `IndexRebuildFailed` notice at 68), 95-115 (the `RetrievalDegradedDense` notice at 103), 148-165 (the `RetrievalDegradedBm25` notice at 156), 240-255 (the `NoEvidence` notice at 247); engine/src/workflow/nodes/graph_context.rs:95-105 (the `GraphAblation` notice at 100 — an operator-requested mode, not a failure), 120-180 (the `GraphUnavailable` notices at 129 and 175, the `GraphTimeout` notice at 154, the `GraphDegraded` notice at 163); engine/src/workflow/nodes/generate.rs:110-145 (the first generate call at 114, the retryability classification at 126-132, the second attempt at 140), 160-230 (the `ModelOnly` notice at 169, the `CitationRepaired` notice at 210, the `CitationDropped` notice at 221); engine/src/workflow/nodes/assemble_prompt.rs:65-100 (the empty-evidence branch at 70 and the packing call at 81-94 which overwrites `ctx.evidence_blocks` with the packed subset); engine/src/workflow/runner.rs terminal emitter and the existing query-duration recording site Plan 01 added; engine/src/workflow/mod.rs:162-215 (`update_from_model_output` and the `BasisReconciled` notice at 186)</read_first>
  <files>engine/src/workflow/nodes/retrieve.rs, engine/src/workflow/nodes/graph_context.rs, engine/src/workflow/nodes/generate.rs, engine/src/workflow/nodes/assemble_prompt.rs, engine/src/workflow/runner.rs, engine/src/tests/telemetry_metrics.rs</files>
  <behavior>
    - `metrics_dense_degrade_increments_path_failure`: a run where dense retrieval degrades records one path-failure increment with the dense path constant and the matching kind, alongside the unchanged existing degrade notice.
    - `metrics_bm25_degrade_increments_path_failure`: the same for the BM25 degrade path.
    - `metrics_graph_timeout_and_unavailable_increment_distinct_kinds`: a graph timeout and a graph-unavailable outcome record the graph path with different kind values, and an operator-requested graph ablation records NOTHING, because an ablation is a requested mode rather than a failure.
    - `metrics_degraded_answer_counted_by_basis`: a run terminating with a non-retrieval answer basis records one degraded-answer increment carrying that basis.
    - `metrics_citation_repair_and_drop_counted_separately`: a run that repairs one marker and drops another records one increment under each action constant.
    - `metrics_generation_retry_recorded_with_outcome`: a first-attempt failure followed by a successful second attempt records one retry increment with the recovered outcome; a two-attempt exhaustion records the exhausted outcome.
    - `metrics_evidence_set_size_recorded_after_packing`: the evidence-set histogram records the packed block count, taken after prompt assembly overwrites the context evidence set, not the pre-packing retrieved count.
    - `metrics_query_duration_outcome_degraded`: a query that completes with degradation records the query-duration histogram under the degraded outcome rather than the plain completed outcome.
  </behavior>
  <action>Read rust-guidelines.md before editing.

Add one metric recording adjacent to each decision site listed below. Every recording READS state the workflow already computed — it never recomputes a degrade rule, never adds or suppresses a notice, and never changes a branch.

In `engine/src/workflow/nodes/retrieve.rs`: record a path-failure increment with the dense path constant next to the `RetrievalDegradedDense` notice emission at line 103, and with the bm25 path constant next to the `RetrievalDegradedBm25` emission at line 156, choosing the kind constant from the same failure classification that already drives the notice. Do not record anything at the `NoEvidence` emission at line 247 — a zero-match query over a healthy index is a correct answer, not a retrieval-path failure, which is the same distinction the milestone's degraded-mode derivation rule already draws. Do not record at the `IndexRebuildFailed` emission at line 68; the rebuild instruments in Task 3 own that.

In `engine/src/workflow/nodes/graph_context.rs`: record a graph path-failure increment next to the `GraphUnavailable` emissions at lines 129 and 175 and the `GraphTimeout` emission at line 154 and the `GraphDegraded` emission at line 163, with distinct kind constants for the timeout case and the unavailable case. Record NOTHING at the `GraphAblation` emission at line 100 — a per-request ablation is an operator-requested mode and counting it as a failure would make every evaluation run look like an outage.

In `engine/src/workflow/nodes/generate.rs`: record a generation-retry increment once per run that took a second attempt, at the point after the second attempt at line 140 resolves, with the recovered or exhausted outcome constant according to that result. Record a citation increment with the repaired action next to the `CitationRepaired` notice at line 210 and with the dropped action next to the `CitationDropped` notice at line 221 — one increment per affected marker, matching the notice's own granularity.

In `engine/src/workflow/nodes/assemble_prompt.rs`: record the evidence-set size histogram after the packing call at lines 81-94 has overwritten the context evidence blocks with the packed subset, so the recorded value is the evidence the model actually saw and not the larger retrieved set. Record zero for the empty-evidence branch at line 70 rather than skipping the record, so the histogram's zero bucket is meaningful.

In `engine/src/workflow/runner.rs`: at the existing query-duration recording site Plan 01 added in the terminal emitter, widen the outcome selection to distinguish a degraded completion from a clean one, and record a degraded-answer increment carrying the terminal answer basis when the terminal basis is not plain retrieval. Read the basis from the workflow context the runner already holds; do not introduce a second degradation rule here.

Write the eight named tests in `engine/src/tests/telemetry_metrics.rs` using an in-memory metric reader and the existing fake-port and mock-server fixtures. The generation retry and citation tests must drive a real generator against a mock HTTP server, matching how Phase 6 proved those same paths — a generation test double cannot prove a real retry happened.</action>
  <acceptance_criteria>
    - Path-failure increments exist at the dense and BM25 degrade sites and at all four graph failure sites, with distinct kind constants for timeout and unavailable.
    - No increment is recorded at the no-evidence site or at the graph-ablation site, and a test asserts each of those absences explicitly.
    - A generation-retry increment is recorded exactly once per retried run with the correct outcome constant.
    - Citation increments are recorded at the repaired and dropped notice sites with matching granularity.
    - The evidence-set histogram records the packed block count after prompt assembly overwrites the context, and records zero on the empty-evidence branch.
    - The query-duration outcome distinguishes degraded from completed, and the degraded-answer counter reads the terminal basis rather than recomputing degradation.
    - No notice is added, removed or reordered, no branch condition changes, and the full pre-existing workflow suite passes unchanged.
    - All eight named tests pass, and the retry and citation tests drive a real generator against a mock HTTP server.
    - `cargo clippy --manifest-path engine/Cargo.toml -- -D warnings` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- metrics_dense_degrade_increments_path_failure</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- metrics_graph_timeout_and_unavailable_increment_distinct_kinds</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- metrics_citation_repair_and_drop_counted_separately</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- metrics_generation_retry_recorded_with_outcome</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- metrics_evidence_set_size_recorded_after_packing</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- metrics_query_duration_outcome_degraded</automated>
    <automated>cargo clippy --manifest-path engine/Cargo.toml -- -D warnings</automated>
  </verify>
  <done>Every query-path decision this milestone made — degrade instead of fail, model-only opt-in, citation repair versus drop, one bounded retry — has a counter recording it, with operator-requested modes correctly excluded and no behaviour changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Ingestion and index metrics, plus the dashboard contract cross-check</name>
  <reversibility rating="reversible">Additive recordings inside the ingestion worker and the rebuild function.</reversibility>
  <read_first>rust-guidelines.md; .planning/phases/06-observability-evaluation-polish/06-CONTEXT.md decisions D-33 and D-35, and D-20 through D-25 (the rebuild-and-swap behaviour these instruments observe); 06.2-RESEARCH.md "Pattern 5" (the numeric-versus-string generation point); engine/src/ingest.rs:1667-1760 as restructured by Plan 04 (the processing status insert at 1667, the completed status insert at 1716 with its chunk count, the rebuild notification at 1724 and the failure branch at 1727), 1441-1547 (`rebuild_and_swap`, its cfg(test) fault branch and its snapshot swap) and the `index_rebuild` span Plan 04 added there; engine/src/workflow/ports.rs:13-24 (`CorpusSnapshot` nodes_version); .planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-03-SUMMARY.md (the observed OTel-to-Prometheus series-name transformation rule); deploy/grafana/dashboards/lancet-rag-operations.json (the committed dashboard whose panel queries this task cross-checks); scripts/engine-test-targets.sh:32-88</read_first>
  <files>engine/src/ingest.rs, engine/src/telemetry/metrics.rs, engine/src/tests/telemetry_metrics.rs, scripts/engine-test-targets.sh</files>
  <behavior>
    - `metrics_ingest_document_counted_by_outcome`: a successfully indexed document records one document increment with the completed outcome; a failed one records the failed outcome and does not record a chunk count.
    - `metrics_ingest_chunks_counted_on_success`: a successfully indexed document records its chunk count on the chunk counter, taken from the same value written into the completed status.
    - `metrics_index_rebuild_duration_recorded_by_outcome`: a successful rebuild records a duration under the completed outcome; a rebuild driven to fail through the existing test fault hook records one under the failed outcome.
    - `metrics_corpus_generation_gauge_tracks_nodes_version`: after a successful swap the gauge reports the new snapshot's numeric nodes version, and never the `lance-` prefixed string.
    - `dashboard_panels_resolve_to_defined_instruments`: every metric name referenced by a panel query in the committed dashboard JSON maps, through the recorded Prometheus series-name transformation rule, to one of the ten instruments defined in the metrics module — and the reverse direction is reported so an unused instrument is visible rather than silent.
  </behavior>
  <action>Read rust-guidelines.md before editing.

In `engine/src/ingest.rs`, record the ingestion counters inside the worker branch Plan 04 restructured. At the completed status insert around line 1716, record one document increment with the completed outcome constant and record the chunk counter with the same `chunk_count` value that insert writes — reading the existing value rather than recounting. On the failure branch beginning at line 1727, record one document increment with the failed outcome and record no chunk count. Do not move, duplicate or condition the existing status inserts.

In `rebuild_and_swap`, capture a start instant at the top of the function alongside the existing rebuild counter increment, and record the rebuild-duration histogram on every exit path with the completed or failed outcome constant — including the cfg(test) fault branch, which must record the failed outcome before returning its error, and the real error paths. After a successful swap, update the corpus generation gauge from the new snapshot's numeric `nodes_version`. If Task 1 implemented the gauge as an asynchronous observable, wire its callback to the corpus store here instead of recording at the swap; either way the value is the numeric version and never the generation string.

Do not alter the rebuild counter, the fault hook, the build-off-the-lock ordering, the snapshot swap, or the `index_rebuild` span Plan 04 added — this task records alongside them.

Add the dashboard contract cross-check as a real test rather than a review note. It reads the committed dashboard JSON, extracts every metric name referenced by a panel query, applies the OTel-to-Prometheus series-name transformation rule recorded in Plan 03's summary, and asserts each one maps to an instrument name defined in the metrics module. It must also report, as an explicit assertion or a named failure message, any defined instrument that no panel queries — an unqueried instrument is a real finding, either a missing panel or a metric nobody needs. This test is what keeps dashboards-as-code and instruments-as-code from drifting apart between phases.

Then re-pin the engine count gate: enumerate with `cargo test --manifest-path engine/Cargo.toml --list`, update the TOTAL and lib pins in `scripts/engine-test-targets.sh` in the same commit, and add one line to its invariant-history comment. Never lower a pinned count.</action>
  <acceptance_criteria>
    - Document and chunk counters are recorded from the values the existing status inserts already carry, with no status-insert logic moved or duplicated.
    - The failure branch records a failed-outcome document increment and no chunk count.
    - The rebuild-duration histogram is recorded on every exit path of `rebuild_and_swap`, including the cfg(test) fault branch and the real error paths, with the correct outcome constant.
    - The corpus generation gauge reports the numeric nodes version after a successful swap and never the generation string.
    - The rebuild counter, fault hook, lock ordering, swap semantics and the `index_rebuild` span are unchanged, proven by Phase 6.1's and Plan 04's tests staying green.
    - `dashboard_panels_resolve_to_defined_instruments` passes, and it reports both directions — unresolved panel queries and unqueried instruments.
    - All five named tests pass.
    - `scripts/engine-test-targets.sh` pins match the enumerated counts with no pin reduced, and the script exits 0.
    - `cargo clippy --manifest-path engine/Cargo.toml -- -D warnings` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- metrics_ingest_document_counted_by_outcome</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- metrics_index_rebuild_duration_recorded_by_outcome</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- metrics_corpus_generation_gauge_tracks_nodes_version</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- dashboard_panels_resolve_to_defined_instruments</automated>
    <automated>sh scripts/engine-test-targets.sh</automated>
    <automated>cargo clippy --manifest-path engine/Cargo.toml -- -D warnings</automated>
  </verify>
  <done>Ingestion volume, rebuild duration and corpus generation are all measured, and a test proves the committed dashboard and the instrument registry describe the same metric surface.</done>
</task>

</tasks>

## Artifacts this phase produces

**Created by this plan**
- Ten metric instruments: `lancet.rag.query.duration`, `lancet.rag.retrieval.path_failures`, `lancet.rag.answer.degraded`, `lancet.rag.citation.repairs`, `lancet.rag.generation.retries`, `lancet.rag.evidence.set_size`, `lancet.ingest.documents`, `lancet.ingest.chunks`, `lancet.index.rebuild.duration`, `lancet.index.corpus_generation`.
- New bounded attribute constant sets in `engine/src/telemetry/metrics.rs`: `outcome` (completed, failed, degraded, recovered, exhausted per instrument), `path` (dense, bm25, graph), `kind` (timeout, error, unavailable), `answer_basis` (retrieval, mixed, model_only), `action` (repaired, dropped).
- One public recording function per instrument in `engine/src/telemetry/metrics.rs`.
- New tests in `engine/src/tests/telemetry_metrics.rs`, including the dashboard contract cross-check.

**Modified by this plan**
- `engine/src/telemetry/metrics.rs`, `engine/src/workflow/runner.rs`, `engine/src/workflow/nodes/retrieve.rs`, `engine/src/workflow/nodes/graph_context.rs`, `engine/src/workflow/nodes/generate.rs`, `engine/src/workflow/nodes/assemble_prompt.rs`, `engine/src/ingest.rs`, `scripts/engine-test-targets.sh`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow and ingestion state → metric attributes | Anything used as a dimension becomes a persistent Prometheus label set. |
| Engine → Collector exposition endpoint → Prometheus | Metric series are stored and queried by the committed dashboard. |
| Instrument registry → committed dashboard | A rename on either side silently empties a panel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.2-05-01 | Denial of Service | Metric label cardinality explosion | high | mitigate | Dimensions are structurally restricted to module-defined enums and `'static` constants, so a runtime string cannot become a label; an exhaustive test enumerates the permitted values per instrument. |
| T-06.2-05-02 | Information Disclosure | Identifiers or text leaking into labels | high | mitigate | Same structural restriction, plus an explicit test scanning every recorded data point for identifier-shaped or free-text attributes. |
| T-06.2-05-03 | Repudiation | A metric recording altering a degrade, retry or citation decision | high | mitigate | Every recording reads already-computed state adjacent to an existing notice or status site; the full pre-existing workflow and ingestion suites are the regression evidence. |
| T-06.2-05-04 | Tampering | Counting an operator-requested graph ablation as a failure | medium | mitigate | No increment at the ablation site, asserted by a dedicated absence test — the same distinction the milestone's degraded-mode derivation rule draws. |
| T-06.2-05-05 | Tampering | Instrument and dashboard drifting apart | medium | mitigate | A test resolves every committed panel query to a defined instrument through the recorded series-name transformation rule, and reports unqueried instruments in the reverse direction. |
| T-06.2-05-06 | Information Disclosure | Corpus generation string exposed as a metric label | low | mitigate | The gauge takes the numeric nodes version only; the string generation stays a span attribute, enforced by a dedicated test. |
</threat_model>

<verification>
- `cargo test --manifest-path engine/Cargo.toml --locked && (cd gateway && go test ./...)` exits 0.
- `cargo clippy --manifest-path engine/Cargo.toml -- -D warnings` exits 0.
- `sh scripts/engine-test-targets.sh` exits 0.
</verification>

<success_criteria>
- Exactly the ten D-35 instruments exist, each mapping to a decision this milestone made.
- Every dimension is a bounded enum; an unbounded label is structurally unreachable.
- The corpus generation gauge is numeric.
- Every committed dashboard panel resolves to a real instrument.
- No workflow or ingestion behaviour changed.
</success_criteria>

<output>
Create `.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-05-SUMMARY.md`, recording: whether the corpus generation gauge was implemented as an observable callback or a synchronous record at swap and why, the widened meaning of the existing query-duration outcome series, and the result of the dashboard cross-check in both directions.
</output>
