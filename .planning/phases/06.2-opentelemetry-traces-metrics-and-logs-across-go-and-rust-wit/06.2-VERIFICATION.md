---
phase: 06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit
verified: 2026-08-24T00:00:00Z
status: gaps_found
score: 4/8 roadmap success criteria verified
behavior_unverified: 2
overrides_applied: 0
gaps:
  - truth: "SC5 — Both services export OTLP to a Collector, fanning out to Jaeger (traces), Prometheus (metrics), Loki (logs), correlated in Grafana by trace_id (D-34)"
    status: failed
    reason: "The Collector's Prometheus exporter is configured with `namespace: lancet` (deploy/collector/otel-collector-config.yaml:27-29). Every engine/gateway metric name already carries a `lancet.` prefix at the instrument-definition source (engine/src/telemetry/metrics.rs, e.g. `lancet.rag.query.duration`), so the Collector double-prefixes on export (`lancet_lancet_rag_query_duration_ms_count`). This was independently reproduced by the orchestrator by pushing a synthetic OTLP metric through the live Collector (127.0.0.1:4318) and reading back `lancet_lancet_test_probe_total` from the Prometheus exporter (127.0.0.1:8889/metrics). The Prometheus fan-out therefore does not produce the series name any consumer (dashboard or ad hoc query) expects. 06.2-03-SUMMARY.md's 'Observed Prometheus Metric Transformation Rule' section documents the single-prefixed (wrong) transformation as if verified against a live scrape, which the plan's own must_haves.flagged_assumptions explicitly required NOT be predicted — this documented rule is itself evidence the live check was not correctly performed as claimed. Confirmed the `_ms`/`_total`/`_count`/`_bucket` suffixes the dashboard depends on ARE correctly produced independent of this bug (`.with_unit(\"ms\")` present on both duration histograms, engine/src/telemetry/metrics.rs:78,151), so the namespace removal alone — not a second, independent unit-suffix fix — is sufficient to close this gap."
    artifacts:
      - path: "deploy/collector/otel-collector-config.yaml"
        issue: "prometheus exporter sets `namespace: lancet` on top of already-namespaced `lancet.*` instrument names (lines 27-29)"
      - path: "deploy/grafana/dashboards/lancet-rag-operations.json"
        issue: "all 10 panel queries reference single-prefixed series names (e.g. `lancet_rag_query_duration_ms_count`) that will never match the double-prefixed series the collector actually exports"
    missing:
      - "Remove `namespace: lancet` from the prometheus exporter block in deploy/collector/otel-collector-config.yaml (or drop the source-side `lancet.` prefix, not both)"
      - "Re-verify against a live collector (docker compose --profile observability up; curl collector:8889/metrics) that exported names match what the dashboard/scrape config expect, and correct 06.2-03-SUMMARY.md's transformation-rule claim"
  - truth: "SC6 — docker-compose exposes core/observability profiles; Collector pipelines, scrape/log config, Grafana datasources/dashboards committed and auto-provisioned with no manual UI state, dashboards generated from typed code (D-39, D-40)"
    status: failed
    reason: "Compose profile split (PostgreSQL with no profile; Collector/Jaeger/Prometheus/Loki/Grafana under `observability`) and Grafana datasource/dashboard provisioning-from-files are correctly implemented and committed (verified directly in docker-compose.yml and deploy/grafana/provisioning/datasources/datasources.yaml). However the shipped, auto-provisioned dashboard cannot render real data because of the same CR-02 root cause as SC5 above — a dashboard that is generated from typed code and provisioned with zero manual UI state, but never resolves any panel against real data, does not meet the observable intent of D-40. Separately, a 3.3MB compiled Windows binary (deploy/grafana/dashboard_gen/dashboard_gen.exe) is committed to git (confirmed via `git ls-files`), is not referenced by scripts/generate-dashboards.sh or any other script, and directly contradicts the 'dashboards generated from typed code' claim by shipping a stray, platform-specific, unregenerated build artifact alongside the generator source."
    artifacts:
      - path: "deploy/grafana/dashboard_gen/dashboard_gen.exe"
        issue: "committed compiled binary, unreferenced by any script, introduced in commit 1dda665 and never removed"
      - path: "deploy/grafana/dashboards/lancet-rag-operations.json"
        issue: "byte-identical to its generator output (scripts/generate-dashboards.sh determinism gate is real and passes) but will render 'No data' on every panel against the live stack per the CR-02 defect above"
    missing:
      - "git rm deploy/grafana/dashboard_gen/dashboard_gen.exe and add *.exe to .gitignore under that directory"
      - "Fix CR-02 (see SC5 gap) so the committed dashboard actually resolves against real Prometheus data"
  - truth: "Anti-pattern / code-review blocker carryover — gen_ai.request.model span attributes must reflect the actually-configured model (D-42/D-43 accurate resource and span attributes, phase goal 'production-grade')"
    status: failed
    reason: "Three span-attribute sites hardcode a literal model name instead of reading the configured model: engine/src/service.rs:431 (`ProductionEmbeddingPort::embed_variant_zero`) hardcodes `nvidia/llama-nemotron-embed-vl-1b-v2:free` even though `self.embedder.model_id()` exists and is used correctly by the sibling ingestion site (engine/src/ingest.rs:1143); engine/src/workflow/nodes/generate.rs:122 and :178 (both `llm_attempt` spans) hardcode `google/gemini-2.5-flash`, which matches no configured default anywhere in the repo (config.example.toml defaults to `openai/gpt-4o-mini`; config.toml's dev default is `dots-studio/dots-3-note-preview:free`). Confirmed unresolved by direct grep against current source — identical to 06.2-REVIEW.md CR-03, no fix committed since the review."
    artifacts:
      - path: "engine/src/service.rs"
        issue: "line 431 hardcodes gen_ai.request.model instead of using embedder.model_id()"
      - path: "engine/src/workflow/nodes/generate.rs"
        issue: "lines 122 and 178 hardcode gen_ai.request.model to a value matching no configured default"
    missing:
      - "Use `self.embedder.model_id()` at service.rs:431"
      - "Thread the configured generation model into GenerateAnswerNode (or add a model_id()-style accessor to the Generator trait) and use it at generate.rs:122,178"
  - truth: "Anti-pattern / code-review blocker carryover — gateway OTLP exporter must honor a configured https:// endpoint with TLS, not silently force plaintext (security-relevant, 'production-grade' phase goal)"
    status: failed
    reason: "gateway/internal/config/config.go validates otlp_endpoint as accepting either http or https scheme, implying https is a legitimate secured configuration. gateway/internal/telemetry/telemetry.go strips both http:// and https:// prefixes and unconditionally calls WithInsecure() on all three exporters (trace, metric, log) with no TLS branch. Confirmed unresolved by direct grep against current source — identical to 06.2-REVIEW.md CR-04, no fix committed since the review."
    artifacts:
      - path: "gateway/internal/telemetry/telemetry.go"
        issue: "lines 123, 153, 173 call WithInsecure() unconditionally regardless of the parsed scheme"
    missing:
      - "Branch on the parsed scheme; use TLS credentials for https:// endpoints instead of forcing WithInsecure()"
      - "Add a test asserting an https:// endpoint does not set WithInsecure"
deferred: []
behavior_unverified_items:
  - truth: "SC1 — inbound W3C traceparent is honoured end-to-end across the HTTP -> gRPC -> engine hop with one continuous trace identity; absent/invalid parent starts a valid root"
    test: "Run the full observability stack, issue one /rag/query with and without a traceparent header, and confirm the same trace_id appears in the gateway's otelhttp span and the engine's query_rag span in Jaeger (or via exported span JSON)."
    expected: "One continuous trace ID across both processes for the propagated case; a fresh, valid root trace ID for the absent/invalid case."
    why_human: "The plan's own must_haves.flagged_assumptions explicitly documents this as an accepted residual risk: `TestGRPCTracePropagation` and `rust_traceparent_parents_workflow_span` share one pinned traceparent fixture but do not run HTTP -> otelgrpc -> extract_parent_context -> query_rag inside a single process. The continuity claim is a composition of two per-side unit assertions plus a shared literal, not one observed end-to-end trace. This is exactly VALIDATION.md's Manual-Only item (a), which is explicitly marked BLOCKING and has not been performed by any executor."
  - truth: "SC7 (runtime half) — a missing/unreachable Collector degrades silently to stdout in both services without failing startup or requests"
    test: "Stop the collector container (or never start the observability profile), issue a /rag/query, and confirm both services keep logging to stdout/console, the request still completes, and no unbounded export-error stream appears in either service's console output."
    expected: "Request completes normally; console/stdout logging is unaffected; at most one bounded startup warning is logged, no crash loop or unbounded error spam."
    why_human: "This is a live runtime behavior across a stopped container and streaming console output that no unit test with an in-memory exporter can exercise. This is VALIDATION.md's Manual-Only item (d), explicitly marked BLOCKING and confirmed (via grep across all 6 SUMMARY.md files) to not have been performed by any executor. The config-level fail-closed validation (malformed otlp_endpoint / sampler_ratio) IS code-verified and covered by automated tests (engine/tests/config_startup.rs, gateway/internal/config/config_test.go) — only the missing-but-well-formed-endpoint degrade-to-stdout runtime path is unverified."
human_verification:
  - test: "Grafana trace<->log<->metric correlation click-through (VALIDATION.md Manual-Only item a, OBS-01/D-34,D-40, task 06.2-03/T2)"
    expected: "Open Grafana, run one query, follow the Jaeger trace's trace-to-logs link into Loki, then follow a Loki log line's derived TraceID field back to Jaeger; the same trace_id appears in all three panes, and a non-empty Prometheus series exists over the same window."
    why_human: "Visual correlation across three backends through Grafana UI links is not scriptable, and Grafana silently ignores a misspelled provisioning key rather than erroring. Given the CR-02 finding above, expect the Prometheus-series half of this check to FAIL as currently shipped until the double-prefix defect is fixed."
  - test: "Operations dashboard renders against real data (VALIDATION.md Manual-Only item b, OBS-01/D-35,D-40, task 06.2-03/T3)"
    expected: "Run one ingestion and several queries including a degrading one; all ten panels render with non-empty series where work was performed, with no datasource or unknown-metric errors."
    why_human: "A panel querying a misspelled/mismatched metric name renders as an empty graph rather than an error, so this cannot be distinguished from success by any automated config check. Given the CR-02 finding, this check is expected to FAIL as currently shipped — the automated dashboard-instrument-contract test (`dashboard_panels_resolve_to_defined_instruments`) is purely textual (compares panel expr strings against a hardcoded single-prefixed stem list) and would pass even with the double-prefix bug present, which is exactly why it did not catch this defect."
  - test: "Windows Docker Desktop bind-mount smoke (VALIDATION.md Manual-Only item c, OBS-01/D-39,D-40, task 06.2-03/T1, RESEARCH A6)"
    expected: "From a clean `down -v`, bring the observability profile up from Git Bash; all five containers run without restart loops, and each mounted file under ./deploy/ is present and non-empty inside its container, with no config-parse or file-not-found errors in any container's logs."
    why_human: "A failed Windows bind mount does not fail `docker compose config` or usually `up -d` either — the container starts and silently falls back to defaults, so no automated check distinguishes a working mount from an empty one."
  - test: "Collector degrade-to-stdout on missing backend (VALIDATION.md Manual-Only item d, OBS-01/D-38, task 06.2-01/T2)"
    expected: "Stop the collector container, issue a query, confirm both services keep logging to stdout/console, the request still completes, and no unbounded export-error stream appears."
    why_human: "Requires stopping the Collector container mid-run and observing service console output live; also listed under behavior_unverified_items above since it maps directly to roadmap SC7's runtime half."
---

# Phase 06.2: OpenTelemetry Traces, Metrics and Logs Across Go and Rust — Verification Report

**Phase Goal:** Ship production-grade OTel traces, metrics and logs across Go and Rust, exported through a Collector to Jaeger/Prometheus/Loki with Grafana as the correlated pane, provisioned as code
**Verified:** 2026-08-24
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Roadmap Success Criteria)

| # | Truth (Roadmap SC) | Status | Evidence |
|---|---|---|---|
| 1 | SC1 — OTel trace ID authoritative, correlation_id retained as span attribute, W3C traceparent honoured, degrade-to-local-root on absent/malformed parent | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Code present: `engine/src/telemetry/propagation.rs` (MetadataExtractor, extract_parent_context), `gateway/main.go:888` (otelgrpc.NewClientHandler), `gateway/main.go:912` (otelhttp.NewHandler). Per-side unit tests exist (TestHTTPTraceParent, TestGRPCTracePropagation, rust_traceparent_parents_workflow_span) but the plan's own flagged_assumptions concede no single-process test proves the full HTTP→gRPC→engine hop shares one trace ID. Routed to human verification (VALIDATION.md Manual-Only item a). |
| 2 | SC2 — ~8-10 spans per query (5 node + child-per-external-call), root span covers SSE stream with terminal outcome attrs + status | ✓ VERIFIED | 5 node spans confirmed at `engine/src/workflow/runner.rs:349-372` (ReformulateQuery, RetrieveHybrid, ExtractGraphContext, AssemblePrompt, GenerateAnswer), each `.instrument()`-ed at :392; the named test `telemetry_query::query_span_hierarchy_emits_exactly_five_node_spans` was run directly in this verification session and PASSED (`cargo test --manifest-path engine/Cargo.toml --lib "telemetry_query::query_span_hierarchy_emits_exactly_five_node_spans" -- --exact` → 1 passed). All 5 leaf-span name literals confirmed directly by unfiltered grep: `graph_traversal` (service.rs:475), `dense_search` (service.rs:547), `bm25_search` (service.rs:613), `llm_attempt` x2 (generate.rs:120,176), `embedding_request` (service.rs:429). Root-span every-exit-path status test `TestRootSpanStatusSetOnEveryExitPath` was run directly (`cd gateway && go test ./... -run TestRootSpanStatusSetOnEveryExitPath -count=1`) and PASSED all 5 subtests (success_terminal, failed_terminal, early_eof, recv_error, invalid_json_body). |
| 3 | SC3 — Ingestion fully traced end to end (upload→admission→chunking→embedding→staging→graph extraction→index rebuild), Phase 6.1 rebuild-and-swap observable | ✓ VERIFIED | Directly read: `ingest_document` span (ingest.rs:1845) wraps `index_document` (ingest.rs:1856, nested via `.instrument()`) which wraps existing `chunk_document`/`embed_document`/`persist_document` (ingest.rs:866,879,885); `graph_extraction` span (ingest.rs:1869) with `graph_entity_lookup` (ingest.rs:980), `entity_name_embedding` (ingest.rs:1141, correctly uses `embedder.model_id()` — not hardcoded), and `extraction_attempt` (graph/extraction.rs:98) leaf spans nested inside it; `index_rebuild` span (ingest.rs:1737) present for the debounced rebuild. Trace-parent carried on `IngestionJob` and re-installed via `set_parent` (ingest.rs:1850-1852). The named test `telemetry_ingest::ingest_span_hierarchy_covers_document_stages` (which asserts `index_document.parent_span_id == ingest_document.span_id`, the exact nesting-mechanism must_have) was run directly and PASSED. |
| 4 | SC4 — Metrics ship with a real backend: 10 named RAG-quality operational instruments | ✓ VERIFIED | All 10 D-35 instruments confirmed defined in `engine/src/telemetry/metrics.rs`: `lancet.rag.query.duration`, `lancet.rag.retrieval.path_failures`, `lancet.rag.answer.degraded`, `lancet.rag.citation.repairs`, `lancet.rag.generation.retries`, `lancet.rag.evidence.set_size`, `lancet.ingest.documents`, `lancet.ingest.chunks`, `lancet.index.rebuild.duration`, `lancet.index.corpus_generation`, each with bounded-constant attribute sets and correct `.with_unit("ms")` on both duration histograms. Metrics are genuinely recorded and reach the OTLP pipeline; whether they are *consumable* as intended downstream is broken by the SC5 gap below (a Collector-side defect, not a metrics-definition defect). |
| 5 | SC5 — Both services export OTLP to a Collector fanning out to Jaeger/Prometheus/Loki, correlated in Grafana by trace_id | ✗ FAILED | See `gaps` in frontmatter (CR-02). Confirmed directly: `deploy/collector/otel-collector-config.yaml:27-29` sets `namespace: lancet` on the Prometheus exporter on top of already-`lancet.`-prefixed instrument names, producing double-prefixed series (`lancet_lancet_...`) that none of the 10 dashboard panel queries or ad hoc single-prefixed queries will match. Independently confirmed empirically by the orchestrator against the live Collector before this verification began. |
| 6 | SC6 — docker-compose core/observability profiles; Collector/scrape/log config and Grafana datasources/dashboards committed and auto-provisioned, dashboards generated from typed code | ✗ FAILED | Compose profile split and Grafana datasource/dashboard provisioning-from-files are correctly implemented (docker-compose.yml, deploy/grafana/provisioning/datasources/datasources.yaml — confirmed present with tracesToLogsV2 and derivedFields correlation config). BUT the committed, auto-provisioned dashboard cannot render real data (same CR-02 root cause), and a 3.3MB compiled `dashboard_gen.exe` is committed to git (confirmed via `git ls-files`), unreferenced by any script — contradicts "generated from typed code" and the code review's CR-01 finding, still unresolved. |
| 7 | SC7 — Missing Collector degrades silently to stdout, never fails the service; service identity via standard resource attributes | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Fail-closed config validation confirmed in both services and IS test-covered: `engine/src/config.rs` (TelemetryConfigSettings with defaults + malformed-value rejection tested by `engine/tests/config_startup.rs`) and `gateway/internal/config/config.go:80,96,104` (explicit error returns for invalid sampler_ratio/otlp_endpoint). Resource attributes confirmed set: `engine/src/telemetry/mod.rs:43-46` (service.name/version/deployment.environment), `gateway/internal/telemetry/telemetry.go:80-83` (semconv.ServiceName/Version/DeploymentEnvironment). The load-bearing half of this truth — actually stopping the Collector and observing silent stdout degrade with no unbounded error spam — is a live runtime behavior no unit test exercises; unverified, routed to human verification (VALIDATION.md Manual-Only item d). |
| 8 | SC8 — Phase 05 D-30 workflow metadata lands as span attributes AND additive `WorkflowCompletedEvent` protobuf fields | ✓ VERIFIED | `engine/src/workflow/events.rs:364` populates `WorkflowMetadata` on the terminal event; `engine/src/workflow/mod.rs:240` defines `derive_degraded_mode` as the single derivation rule consumed by both the wire field and the span attribute, matching the plan's "single derived value" must_have. |

**Score:** 4/8 roadmap success criteria cleanly VERIFIED (SC2, SC3, SC4, SC8). 2 PRESENT_BEHAVIOR_UNVERIFIED (SC1, SC7 — routed to human, not counted toward the verified score). 2 FAILED (SC5, SC6 — confirmed code-level defect, CR-02 double-prefix).

### Additional Code-Level Defects (Confirmed Still Present, from 06.2-REVIEW.md)

06.2-REVIEW.md (status: issues_found, reviewed same day, no commits since) reported 4 critical, 6 warning, 1 info findings. All 4 criticals were independently re-confirmed against current source during this verification — none has been fixed:

| ID | Finding | Confirmed Still Present |
|---|---|---|
| CR-01 | `dashboard_gen.exe` committed compiled binary, unused by any script | Yes — `git ls-files deploy/grafana/dashboard_gen/` still lists it |
| CR-02 | Collector `namespace: lancet` double-prefixes every metric, breaking dashboard/scrape contract | Yes — `deploy/collector/otel-collector-config.yaml:29` still has `namespace: lancet`; empirically reproduced live by the orchestrator |
| CR-03 | `gen_ai.request.model` hardcoded/wrong on 3 span sites | Yes — `engine/src/service.rs:431`, `engine/src/workflow/nodes/generate.rs:122,178` |
| CR-04 | Gateway OTLP exporter always forces insecure transport, ignoring configured `https://` | Yes — `gateway/internal/telemetry/telemetry.go:123,153,173` unconditional `WithInsecure()` |

CR-01 and CR-02 are folded into the SC6/SC5 gaps above (same underlying artifacts). CR-03 and CR-04 are recorded as standalone gaps in the frontmatter since they don't map to one specific roadmap SC bullet but directly undermine the phase goal's "production-grade" qualifier — CR-03 makes trace data systematically wrong (not merely missing), and CR-04 is a security-relevant silent-downgrade defect.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `engine/src/telemetry/mod.rs` | TelemetryHandle, init, build_providers_and_layers, resource builder | ✓ VERIFIED | Present, resource attrs confirmed at :43-46 |
| `engine/src/telemetry/propagation.rs` | MetadataExtractor, extract_parent_context | ✓ VERIFIED | Present |
| `engine/src/telemetry/metrics.rs` | 10 instruments + bounded constants | ✓ VERIFIED | All 10 confirmed present, `.with_unit("ms")` confirmed on both duration histograms |
| `engine/src/config.rs` | TelemetryConfigSettings, fail-closed overlays | ✓ VERIFIED | Present |
| `gateway/internal/telemetry/telemetry.go` | Config/Providers/Init/SetupPropagator/WrapCore | ⚠️ WIRED WITH DEFECT | Present and wired, but CR-04 (forced insecure transport) defect inside |
| `gateway/internal/config/config.go` | fail-closed validation | ✓ VERIFIED | Present |
| `deploy/collector/otel-collector-config.yaml` | 1 receiver, 3 pipelines, pinned image | ⚠️ HOLLOW (namespace bug) | Structurally present, functionally broken for Prometheus (CR-02) |
| `deploy/prometheus/prometheus.yml`, `deploy/loki/loki-config.yaml` | scrape/log config | ✓ VERIFIED | Present (not independently content-audited beyond scrape target) |
| `deploy/grafana/provisioning/datasources/datasources.yaml` | 3 datasources, correlation config | ✓ VERIFIED | Confirmed read directly — tracesToLogsV2, derivedFields present |
| `deploy/grafana/dashboards/lancet-rag-operations.json` + `dashboard_gen/main.go` | typed generator + deterministic committed JSON | ⚠️ HOLLOW (data won't render) + stray `.exe` committed | Determinism gate genuinely passes; data-rendering broken by CR-02; CR-01 binary still committed |
| `engine/src/workflow/mod.rs` / `events.rs` | degraded_mode derivation, WorkflowMetadata population | ✓ VERIFIED | Present |

### Data-Flow Trace (Level 4) — Metrics Pipeline

| Stage | Source | Produces Real Data | Status |
|---|---|---|---|
| Instrument definition → OTLP export | `engine/src/telemetry/metrics.rs` record_* functions | Yes, metrics genuinely recorded | ✓ FLOWING |
| Collector OTLP receipt → Prometheus exporter | `deploy/collector/otel-collector-config.yaml` | Yes, but under a double-prefixed series name | ⚠️ STATIC (wrong name, not absent) |
| Prometheus scrape → Grafana dashboard panel | `deploy/grafana/dashboards/lancet-rag-operations.json` panel `expr` fields | No — panel queries target the single-prefixed name that the double-prefixed series never matches | ✗ DISCONNECTED |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Exactly 5 node spans (behavior-dependent truth) | `cargo test --manifest-path engine/Cargo.toml --lib "telemetry_query::query_span_hierarchy_emits_exactly_five_node_spans" -- --exact` | 1 passed; 0 failed | ✓ PASS |
| Root span status set on every exit path (cleanup invariant) | `cd gateway && go test ./... -run TestRootSpanStatusSetOnEveryExitPath -count=1` | 5/5 subtests passed (success_terminal, failed_terminal, early_eof, recv_error, invalid_json_body) | ✓ PASS |
| Ingest span nesting is parent-ID equality, not just presence (behavior-dependent truth) | `cargo test --manifest-path engine/Cargo.toml --lib "telemetry_ingest::ingest_span_hierarchy_covers_document_stages" -- --exact` | 1 passed; 0 failed | ✓ PASS |
| 10 D-35 instruments defined | `grep -c "pub fn record_" engine/src/telemetry/metrics.rs` | 10 matches | ✓ PASS |
| Duration-histogram unit suffix (`_ms`) is correctly declared, ruling out a second CR-02-adjacent name mismatch | `grep -n "with_unit" engine/src/telemetry/metrics.rs` | `.with_unit("ms")` at lines 78, 151 | ✓ CONFIRMED |
| Dashboard-instrument contract test exists and is purely textual (does not catch CR-02) | Read `engine/src/tests/telemetry_metrics.rs:744-787` | Test compares panel `expr` strings against a hardcoded single-prefixed stem list — no live collector involved | ✓ CONFIRMED (explains why CR-02 shipped past this gate) |
| Full regression suites | Already run by orchestrator prior to this verification: `cargo test --manifest-path engine/Cargo.toml --locked` (474 total, 0 failed) and `cd gateway && go test ./...` (all packages ok) | Both green | ✓ PASS (does not cover CR-01/02/03/04 — none is exercised by these suites) |
| Collector config namespace / dashboard query mismatch | `grep namespace deploy/collector/otel-collector-config.yaml` + `grep -o '"expr":.*' deploy/grafana/dashboards/lancet-rag-operations.json` | `namespace: lancet` present; all 10 exprs single-prefixed | ✓ CONFIRMS CR-02 |
| Committed binary | `git ls-files deploy/grafana/dashboard_gen/` | `dashboard_gen.exe` listed | ✓ CONFIRMS CR-01 |
| Hardcoded model attrs | `grep -n "gen_ai.request.model" engine/src/service.rs engine/src/workflow/nodes/generate.rs` | Literal strings at 3 sites | ✓ CONFIRMS CR-03 |
| Forced insecure OTLP transport | `grep -n "WithInsecure" gateway/internal/telemetry/telemetry.go` | 3 unconditional calls | ✓ CONFIRMS CR-04 |

The full workspace test suite was not re-run wholesale in this verification session (already run and confirmed green by the orchestrator immediately prior, per the harness rule against re-running a full suite per must-have); three specific named tests were instead run individually as targeted behavioral evidence for SC2 and SC3's cleanup/nesting invariants, plus the defect-confirmation greps above for CR-01 through CR-04, none of which the automated suites exercise.

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|---|---|---|---|---|
| OBS-01 | 06.2-01 through 06.2-06 (all six) | OpenTelemetry-compatible tracing across Go, gRPC, Rust nodes, retrieval, graph queries, and LLM calls | ⚠️ PARTIALLY SATISFIED | Implementation substantively complete (spans, metrics, logs, provisioning-as-code all present, structurally correct, and behaviorally tested where automatable) but blocked from full "production-grade, correlated pane" delivery by CR-01/CR-02 (dashboard non-functional against real data), CR-03 (systematically wrong model attribution), CR-04 (silent TLS downgrade), and 3 of 4 explicitly-BLOCKING VALIDATION.md manual checks not yet performed. No orphaned requirements — REQUIREMENTS.md maps OBS-01 to Phase 06.2 exclusively, and all 6 plans declare `requirements: [OBS-01]`. |

**Deferral check (Step 9b):** Checked the milestone roadmap for later phases that might absorb these gaps. Phase 06.3 (offline evaluation harness / OBS-02) and Phase 06.4 (README/design-narrative / OBS-03) are the only later phases in this milestone; neither's goal or success criteria references the Collector config, the Grafana dashboard, span attribute accuracy, or the gateway's OTLP transport security. None of the 4 gaps above defer to a later phase — the `deferred` list is empty.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `deploy/grafana/dashboard_gen/dashboard_gen.exe` | n/a | Committed compiled binary, unreferenced by tooling | 🛑 Blocker | Repo bloat, platform-specific, contradicts "generated from typed code" |
| `deploy/collector/otel-collector-config.yaml` | 27-29 | Double-namespacing already-prefixed metric names | 🛑 Blocker | Breaks every Grafana panel and any Prometheus consumer of the shipped series names |
| `engine/src/service.rs` | 431 | Hardcoded `gen_ai.request.model` literal, diverges from `embedder.model_id()` available on the same struct | 🛑 Blocker | Every embedding span reports a fixed model regardless of actual configuration |
| `engine/src/workflow/nodes/generate.rs` | 122, 178 | Hardcoded `gen_ai.request.model` matching no configured default anywhere in the repo | 🛑 Blocker | Every generation span reports a wrong model in every environment |
| `gateway/internal/telemetry/telemetry.go` | 115-124, 151-154, 171-174 | Validated `https://` scheme silently downgraded to unconditional plaintext `WithInsecure()` | 🛑 Blocker | Configured TLS protection never applies; telemetry (potentially including query/session identifiers) travels in cleartext or export silently fails |
| `docker-compose.yml` | 89-90 | Hardcoded `admin`/`admin` Grafana credentials | ⚠️ Warning | Not addressed since review; low exposure (127.0.0.1-bound, profile-gated) but a known-default credential pattern |
| `engine/src/tests/telemetry_metrics.rs` | 744-787 | Dashboard-instrument contract test is textual only, never exercises a live Collector/Prometheus round-trip | ℹ️ Info | Explains why CR-02 shipped past all automated gates; not itself a defect but a coverage gap worth closing alongside the CR-02 fix |

No `TBD`/`FIXME`/`XXX` debt markers found in phase-modified files.

### Human Verification Required

See frontmatter `human_verification` — the 4 VALIDATION.md Manual-Only items, all explicitly marked BLOCKING by the phase's own validation plan, none of which any executor has performed (confirmed via grep across all 6 SUMMARY.md files for manual/human-check/click-through/bind-mount/degrade-to-stdout evidence — zero hits). Two of these (items a and d) are also recorded as `behavior_unverified_items` because they map directly to roadmap SC1 and SC7's runtime halves respectively. Items b and c are additionally noted: item (b) is expected to fail as currently shipped given the CR-02 defect confirmed above, and item (c) remains genuinely open (Windows bind-mount behavior was not part of this verification's evidence set).

### Gaps Summary

Four concrete, code-level defects block full goal achievement, all confirmed present in the current working tree (no commits since 06.2-06 addressed any of the code review's findings):

1. **CR-02 (Collector double-prefix)** — breaks roadmap SC5 and SC6 directly: the shipped, committed, auto-provisioned Grafana dashboard will render "No data" on every panel against a live stack, and this was independently reproduced empirically (not just inferred from code reading) by the orchestrator before this verification began. The plan's own SUMMARY documents an incorrect transformation rule as if verified live, which is itself evidence the required live-scrape check was not correctly performed. The `_ms` unit suffix the dashboard also depends on IS correctly declared in the instrument definitions, so removing the namespace key alone is sufficient — this is not a two-part fix.
2. **CR-01 (committed `.exe`)** — a 3.3MB stray, platform-specific compiled binary in version control, contradicting the "dashboards generated from typed code" success criterion's spirit and the code review's explicit blocker classification.
3. **CR-03 (hardcoded model attributes)** — three span sites report a fixed, often-wrong model name regardless of actual configuration, undermining the phase goal's "production-grade" qualifier by making trace data systematically incorrect rather than merely incomplete.
4. **CR-04 (forced insecure OTLP transport)** — a validated `https://` endpoint configuration is silently downgraded to plaintext, a security-relevant divergence between what config validation promises and what the exporter actually does.

Additionally, 3 of 4 explicitly-BLOCKING VALIDATION.md manual-only checks have not been performed by any executor (by design, per `workflow.human_verify_mode = end-of-phase`), and one of them (dashboard renders against real data) is expected to fail as currently shipped given gap #1.

The underlying instrumentation architecture (span ownership, node/leaf span hierarchy, ingestion tracing, metric instrument definitions, log correlation seam, workflow metadata derivation) is well-built and matches the plans' must_haves at the structural/wiring level, and this verification directly ran (not merely inherited from planning docs) named tests confirming the two behavior-dependent invariants that presence-checking alone cannot prove (five-node-span construction, root-span status on every exit path, and ingest span parent-ID nesting) — all three passed. The regression suites (474 Rust tests, full Go suite) are genuinely green. The gaps are narrow, well-isolated, and mostly one-line-to-few-line fixes (remove a YAML key, remove a committed binary, thread an existing accessor through, add a TLS branch) — but they are real, currently shipped, and directly contradict specific roadmap success criteria and the phase's own "production-grade" framing.

---

_Verified: 2026-08-24_
_Verifier: Claude (gsd-verifier)_
