---
phase: 6.2
slug: opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-23
---

# Phase 6.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded by `/gsd-plan-phase 6.2` from `06.2-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework (Rust)** | Built-in `cargo test` with Tokio |
| **Framework (Go)** | Built-in `go test` and `net/http/httptest` |
| **Config file** | none — targets defined by `engine/Cargo.toml`, Go module conventions, and test-target scripts |
| **Quick run command (Rust)** | `cargo test --manifest-path engine/Cargo.toml --lib -- telemetry` |
| **Quick run command (Go)** | `cd gateway && go test ./internal/telemetry` |
| **Full suite command** | `cargo test --manifest-path engine/Cargo.toml --locked && (cd gateway && go test ./...)` |
| **Build command** | `cargo build --manifest-path engine/Cargo.toml && (cd gateway && go build ./...)` |
| **Supplementary gates** | `bash scripts/engine-test-targets.sh`, `bash scripts/gateway-test-targets.sh`, `cargo clippy --manifest-path engine/Cargo.toml -- -D warnings`, `cargo fmt --check`, `go vet ./...` |
| **Estimated runtime** | Not yet measured — Wave 0 telemetry test targets do not exist yet (both `-- telemetry` and `./internal/telemetry` are currently empty/stub selectors) |

**Baseline (measured 2026-08-23, pre-phase):** Rust 370 library + 18 inspect + 19 config-startup = 407; Go `gateway` 65 + `gateway/db` 7 + `gateway/internal/sse` 8 = 80. [VERIFIED: scripts/engine-test-targets.sh:55-88; scripts/gateway-test-targets.sh:17-24,95-98]

---

## Sampling Rate

- **After every task commit:** Run the quick command matching the task's language (`cargo test --manifest-path engine/Cargo.toml --lib -- telemetry` for Rust work, `cd gateway && go test ./internal/telemetry` for Go work).
- **After every plan wave:** Run `cargo test --manifest-path engine/Cargo.toml --locked && (cd gateway && go test ./...)`.
- **After dependency/module changes:** `bash scripts/engine-test-targets.sh` and `bash scripts/gateway-test-targets.sh`.
- **Before `/gsd-verify-work`:** Full suite green, plus `cargo clippy -- -D warnings`, `cargo fmt --check`, `go vet ./...`, and Collector/Compose observability config validation.
- **Max feedback latency:** not yet measured (Wave 0 — no telemetry tests exist to time).

> **D-85: there is no CI.** These commands *are* the verification path.

---

## Per-Requirement Verification Map

Pre-plan draft from research — Task ID / Plan / Wave columns are placeholders until `gsd-planner` assigns real task numbers. Source: `06.2-RESEARCH.md` § Validation Architecture → Phase Requirements → Test Map.

| Req ID | Behavior | Test Type | Automated Command | File Exists | Status |
|---|---|---|---|---|---|
| OBS-01 / D-28 | Go HTTP extraction honors valid W3C `traceparent`; absent/invalid parent starts a valid root | unit/integration | `cd gateway && go test ./internal/telemetry -run TestHTTPTraceParent -count=1` | ❌ Wave 0 | ⬜ pending |
| OBS-01 / D-28 | Go `otelgrpc.NewClientHandler` injects the current SpanContext into engine metadata | unit/integration | `cd gateway && go test ./internal/telemetry -run TestGRPCTracePropagation -count=1` | ❌ Wave 0 | ⬜ pending |
| OBS-01 / D-28 | Rust tonic metadata extractor parents the workflow span and preserves wire correlation separately | unit/integration | `cargo test --manifest-path engine/Cargo.toml --lib rust_traceparent -- --exact` | ❌ Wave 0 | ⬜ pending |
| OBS-01 / D-31 | `/rag/query` root span covers the entire synchronous SSE stream and records terminal outcome/status | integration | `cd gateway && go test ./internal/telemetry -run TestSSERootSpanLifecycle -count=1` | ❌ Wave 0 | ⬜ pending |
| OBS-01 / D-29 | Exactly five workflow node spans plus leaf spans for embedding, dense, BM25, graph, and each LLM attempt | Rust integration | `cargo test --manifest-path engine/Cargo.toml --lib query_span_hierarchy -- --exact` | ❌ Wave 0 | ⬜ pending |
| OBS-01 / D-29 | Retry creates two sibling LLM attempt spans with attempt/outcome attributes | Rust unit/integration | `cargo test --manifest-path engine/Cargo.toml --lib llm_attempt_spans -- --exact` | ❌ Wave 0 | ⬜ pending |
| OBS-01 / D-30 | Ingestion spans cover upload/admission/chunk/embed/staging/graph/rebuild, linked to Phase 6.1's rebuild-and-swap | integration | `cargo test --manifest-path engine/Cargo.toml --lib ingest_span_hierarchy -- --exact` | ❌ Wave 0 | ⬜ pending |
| OBS-01 / D-33/D-35 | RAG operational counters/histograms/gauge use bounded dimensions and numeric generation gauge | Rust unit | `cargo test --manifest-path engine/Cargo.toml --lib telemetry_metrics -- --exact` | ❌ Wave 0 | ⬜ pending |
| OBS-01 | Rust logs reach an OTel logger provider with active trace context while fmt output remains available | Rust unit/integration | `cargo test --manifest-path engine/Cargo.toml --lib telemetry_logs -- --exact` | ❌ Wave 0 | ⬜ pending |
| OBS-01 | Go zap logs use the request context for OTel trace correlation; background logs do not fabricate OTel context | Go unit | `cd gateway && go test ./internal/telemetry -run TestZapTraceCorrelation -count=1` | ❌ Wave 0 | ⬜ pending |
| OBS-01 / D-38 | Invalid present telemetry config fails closed; missing Collector does not prevent service startup or console logs | unit/integration | `cargo test --manifest-path engine/Cargo.toml --test config_startup -- telemetry` and `cd gateway && go test ./internal/telemetry -run TestCollectorUnavailable` | ❌ Wave 0 | ⬜ pending |
| OBS-01 / D-34 | Collector config validates for the pinned image and routes all three signals | container smoke | `docker run --rm -v <collector-config>:/etc/otelcol/config.yaml otel/opentelemetry-collector-contrib:0.159.0 validate --config=/etc/otelcol/config.yaml` | ❌ Wave 0 | ⬜ pending |
| OBS-01 / D-39 | Compose profile starts PostgreSQL by default, optional `observability` stack, no 4317/4318 collision | container smoke | `docker compose config --profiles`, `docker compose --profile observability config` | ❌ Wave 0 | ⬜ pending |
| OBS-01 / D-34/D-40 | Jaeger, Prometheus, Loki, and Grafana are reachable and provisioned without manual UI edits | end-to-end/manual-assisted | Compose smoke with HTTP health/query checks and one trace/log correlation click | ❌ Wave 0 | ⬜ pending |

Cargo does not accept Go-style `-count=1`; Rust rows above use `-- --exact` (or an equivalent Cargo filter) instead — corrected from the research candidate's placeholder commands.

---

## Wave 0 Requirements

- [ ] Rust telemetry module + explicit `opentelemetry` 0.32 feature configuration in `engine/Cargo.toml`; update `engine/Cargo.lock`.
- [ ] Go provider/bridge initialization in the existing `gateway/internal/telemetry` stub; update `gateway/go.mod`/`go.sum` (including the gRPC `v1.83.0` graph change).
- [ ] Rust tonic `MetadataMap` extractor and parent-span tests.
- [ ] Go HTTP/gRPC propagation, SSE lifetime, and zap context-correlation tests.
- [ ] Rust node/leaf/ingestion span and metric/log tests using in-memory providers/exporters.
- [ ] Collector config, Loki config, Prometheus scrape config, Grafana datasource provisioning, dashboard source, and generated dashboard JSON.
- [ ] Explicit Go module layout and regeneration target for the Grafana Foundation SDK generator.
- [ ] Compose profile/port/healthcheck configuration and a pinned-image Collector validation command.

*None of the above exist yet — this phase IS Wave 0 for telemetry.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|---|---|---|---|
| Grafana trace↔log↔metric correlation click-through | OBS-01 / D-34 | Visual correlation across three backends via `trace_id`, not scriptable without a browser | Open Grafana, run one query, follow the trace panel's linked log/metric views, confirm `trace_id` matches across all three |
| Collector degrade-to-stdout on missing backend | OBS-01 / D-38 | Requires stopping the Collector container mid-run and observing service logs | Stop the `otel-collector` container, issue a query, confirm both services log to stdout/console and the request still completes |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** draft — pending plan creation (`gsd-planner` assigns real Task ID/Plan/Wave; a full re-check belongs to `/gsd-validate-phase` after planning).
