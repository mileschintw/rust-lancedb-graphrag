---
phase: 06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit
plan: 07
type: execute
wave: 7
depends_on:
  - 06.2-03
  - 06.2-05
  - 06.2-08
files_modified:
  - deploy/collector/otel-collector-config.yaml
  - scripts/verify-collector-prom-export.sh
  - deploy/grafana/dashboard_gen/main.go
  - deploy/grafana/dashboards/lancet-rag-operations.json
  - .gitignore
  - engine/src/tests/telemetry_metrics.rs
  - scripts/engine-test-targets.sh
  - .planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-03-SUMMARY.md
  - .planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-VALIDATION.md
  - .planning/STATE.md
autonomous: true
gap_closure: true
requirements:
  - OBS-01
user_setup:
  - service: docker-compose observability profile
    why: "Live Collector Prometheus exposition round-trip for CR-02 (SC5/SC6), including histogram unit suffixes. The textual dashboard-instrument test cannot see double-prefixing or unit translation."
    dashboard_config:
      - task: "Bring up the observability profile so host 127.0.0.1:4318 and 127.0.0.1:8889 are reachable"
        location: "repo root — docker compose --profile observability up -d (Git Bash)"
estimate:
  tokens: 82000
  raw_tokens: 82000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Collector Prometheus exposition publishes source-prefixed series that dashboard PromQL queries: `lancet_rag_*`, `lancet_ingest_*`, `lancet_index_*` — not a doubled collector prefix (D-34, D-35, D-40)."
    - "A live OTLP HTTP push of all ten D-35 instruments (including both `ms`-unit duration histograms) through 127.0.0.1:4318 is scraped from 127.0.0.1:8889 such that every PromQL metric stem parsed from deploy/grafana/dashboards/lancet-rag-operations.json `expr` fields is a substring of the scrape (CR-02 / SC5 / SC6)."
    - "Engine instrument names in engine/src/telemetry/metrics.rs keep their existing `lancet.` prefix; the Collector prometheus exporter does not add a second namespace (D-35, D-42)."
    - "In-image Collector `validate` runs against the image tag parsed from docker-compose.yml's collector service (not a hardcoded 0.159.0 pin) (D-39, D-40)."
    - "`dashboard_panels_resolve_to_defined_instruments` plus the live scrape gate together cover namespace and unit-suffix transformation; textual stem matching alone is not the only check."
    - "`deploy/grafana/dashboard_gen` contains generator source only; compiled Windows or Unix binaries of that CLI are not tracked in git (CR-01 / D-40 generated-from-typed-code)."
    - "06.2-03-SUMMARY.md's Observed Prometheus Metric Transformation Rule is transcribed from the Task 1 scrape (verbatim exported lines) and the resolved collector image tag, not predicted (D-40)."
    - "STATE.md Known Issues records DEBT-P6.2-WR-01 through DEBT-P6.2-WR-06 and DEBT-P6.2-IN-01 so 06.2-REVIEW.md warnings are not lost when the review artifact goes cold."
    - "06.2-VALIDATION.md Manual-Only table re-owns all four BLOCKING checks on 06.2-07/T3 for post-fix re-run (D-34, D-38, D-39, D-40)."
  flagged_assumptions:
    - "OBS-01 edge-probe returned category unclassified, status unresolved (coverage applicable 1, resolved 0, unresolved 1). Not auto-resolved. Concrete acceptance for this plan is the CR-01/CR-02 missing items in 06.2-VERIFICATION.md plus the live stem/unit scrape, not a probe-derived edge predicate."
    - "Live scrape requires the observability profile (user_setup). If 4318/8889 are down, the executor stops on the tracer precondition rather than predicting series names."
    - "Automated gate uses JSON-derived stems plus synthetic OTLP payloads matching D-35 names and histogram units. Driving a live /rag/query plus ingestion against OpenRouter is the Task 3 human-check (operations dashboard with real data), not the POSIX script."
  prohibitions:
    - statement: "Must not reintroduce a Collector prometheus-exporter extra prefix on top of source `lancet.` instrument names (the double-prefix that made every panel No data)."
      flagged: true
    - statement: "Must not drop or rename source-side `lancet.` prefixes on D-35 instruments to paper over the Collector extra prefix."
      flagged: true
    - statement: "Must not hand-edit deploy/grafana/dashboards/lancet-rag-operations.json; any PromQL change after a unit-suffix mismatch goes through dashboard_gen/main.go then scripts/generate-dashboards.sh."
      flagged: true
    - statement: "Must not drop `.with_unit(\"ms\")` on the two duration histograms while leaving committed `_ms` PromQL in place."
      flagged: true
    - statement: "Must not commit compiled dashboard_gen binaries after they are removed."
      flagged: true
    - statement: "Must not implement WR-01..WR-06 or IN-01 code fixes in this plan; only record DEBT ids in STATE.md."
      flagged: true
  artifacts:
    - path: "deploy/collector/otel-collector-config.yaml"
      provides: "Prometheus exporter without an extra prefix field so source `lancet.*` names survive sanitization as `lancet_*`."
    - path: "scripts/verify-collector-prom-export.sh"
      provides: "Live OTLP HTTP → Collector :8889 round-trip asserting every dashboard-JSON stem and rejecting doubled prefixes."
    - path: ".gitignore"
      provides: "Ignore rules for dashboard_gen compiled binaries."
  key_links:
    - from: "engine/src/telemetry/metrics.rs"
      to: "deploy/grafana/dashboards/lancet-rag-operations.json"
      via: "Collector prometheus exporter (no extra namespace) then Prometheus scrape of :8889, including histogram unit suffixes"
      pattern: "collector_fanout"
---

## Phase User Story

**As a** operator viewing the provisioned RAG operations dashboard, **I want to** query Prometheus series whose names match the typed dashboard PromQL including histogram unit suffixes, **so that** Grafana panels render real Collector-exported data instead of empty graphs (D-34, D-40).

<objective>
Close CR-02 (Collector prometheus-exporter extra prefix breaking SC5/SC6 dashboard data, including the unit-suffix half of the naming contract) and CR-01 (committed dashboard_gen.exe). Re-verify export names against a live scrape of all dashboard stems on the compose-pinned Collector image and correct the Plan 03 SUMMARY transformation rule from that scrape (D-34, D-35, D-40). Re-own the four BLOCKING manual checks and record 06.2-REVIEW.md WR/IN debt ids.

Purpose: SC5 fan-out and SC6 dashboards-as-code are structurally shipped but hollow until series names (prefix and unit suffix) match panel queries; the stray binary contradicts generated-from-typed-code.
Output: collector config without prometheus extra prefix, live all-stems export-name script, optional generator/JSON update if the running Collector expands `ms` to `milliseconds`, gitignored generator binaries, scrape-derived 06.2-03-SUMMARY.md, yaml-level regression test, VALIDATION re-schedule, STATE.md DEBT rows.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-observability-evaluation-polish/06-CONTEXT.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-VERIFICATION.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-REVIEW.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-REVIEWS.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-03-SUMMARY.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-03-PLAN.md
@deploy/collector/otel-collector-config.yaml
@deploy/grafana/dashboards/lancet-rag-operations.json
@docker-compose.yml
@engine/src/tests/telemetry_metrics.rs
@engine/src/telemetry/metrics.rs
@.gitignore
@scripts/generate-dashboards.sh
</context>

<scope_rationale>
Gap cluster is observability-as-code delivery (SC5/SC6), not instrumentation. Do not re-plan SC2/SC3/SC4/SC8 span or metric definition work. Wave 7 after 06.2-08 so `sh scripts/engine-test-targets.sh` does not compile the engine crate while Plan 08 still edits generation/service files (06.2-REVIEWS.md LOW parallel-suite finding). Discretion: live proof is a POSIX script next to generate-dashboards.sh rather than a Rust #[ignore] test, so the default cargo suite stays docker-free while the plan verify still requires the live hop. Addresses review concern: Plan 07 live probe was blind to histogram unit suffixes.
</scope_rationale>

<review_dispositions>
- HIGH unit-suffix-blind probe — incorporated: Task 1 parses all `expr` stems from lancet-rag-operations.json and POSTs all ten D-35 names including both `ms` histograms.
- HIGH fourth decision — incorporated: if scrape duration families use `milliseconds` instead of committed `_ms` stems, update dashboard_gen/main.go PromQL to the observed suffix, run generate-dashboards.sh, update the stem list in dashboard_panels_resolve_to_defined_instruments; do not remove `.with_unit("ms")` while `_ms` PromQL remains.
- MEDIUM collector image pin — incorporated: Task 1 `validate` uses the collector `image:` tag from docker-compose.yml; SUMMARY records the resolved tag.
- MEDIUM orphaned BLOCKING human checks — incorporated: Task 3 human-check plus VALIDATION.md re-schedule owned by 06.2-07/T3.
- LOW `grep -c namespace` exit 1 — incorporated: region check uses `! grep -q`.
- LOW parallel cargo vs Plan 08 — incorporated: `depends_on: [06.2-08]`, wave 7.
- LOW truths schema — incorporated: truths are plain strings; unclassified OBS-01 edge is a flagged_assumption (specless unresolved, not a backstop object).
- LOW WR-01..WR-06 / IN-01 — incorporated: Task 3 DEBT rows on STATE.md; no code fixes.
- Suggestion 1 real query/ingest half — deferred to Task 3 human-check (dashboard renders with real data); automated gate stays synthetic OTLP so OpenRouter is not a Task 1 precondition.
</review_dispositions>

<tasks>

<task type="tracer">
  <name>End-to-end Collector export names match every dashboard stem — including ms histograms</name>
  <reversibility rating="reversible">Removing the prometheus exporter extra prefix is a one-key Collector config change; undo is restoring the field. Dashboard generator PromQL edits are reversible via git.</reversibility>
  <read_first>.planning/phases/06-observability-evaluation-polish/06-CONTEXT.md decisions D-34, D-35, D-39, D-40, D-42; 06.2-VERIFICATION.md gaps CR-02/SC5/SC6; 06.2-REVIEW.md CR-02; 06.2-REVIEWS.md HIGH unit-suffix finding; 06.2-03-SUMMARY.md Observed Prometheus Metric Transformation Rule; deploy/collector/otel-collector-config.yaml exporters.prometheus block; engine/src/telemetry/metrics.rs ten D-35 names and `.with_unit("ms")` on lancet.rag.query.duration and lancet.index.rebuild.duration; deploy/grafana/dashboards/lancet-rag-operations.json all ten expr fields; docker-compose.yml collector service image and ports 4318/8889; scripts/generate-dashboards.sh; deploy/grafana/dashboard_gen/main.go PromQL strings</read_first>
  <files>deploy/collector/otel-collector-config.yaml, scripts/verify-collector-prom-export.sh, deploy/grafana/dashboard_gen/main.go, deploy/grafana/dashboards/lancet-rag-operations.json</files>
  <precondition>docker compose --profile observability is running with host 127.0.0.1:4318 (OTLP HTTP) and 127.0.0.1:8889 (Prometheus exposition) accepting connections. If either port is closed, start or recreate the collector from the repo root in Git Bash before this task; do not invent series names.</precondition>
  <action>Addresses review concern: live probe was blind to histogram unit suffixes and used a compose-mismatched Collector image.

In deploy/collector/otel-collector-config.yaml, delete the prometheus exporter's extra prefix key so that exporter only sets endpoint 0.0.0.0:8889. Leave otlp/jaeger and otlphttp/loki tls blocks unchanged. Do not edit engine/src/telemetry/metrics.rs instrument name strings; they already begin with lancet. (D-35, D-42). Do not remove `.with_unit("ms")` in this step.

Parse COLLECTOR_IMAGE from docker-compose.yml as the collector service `image:` value (today otel/opentelemetry-collector-contrib:0.120.0 — do not hardcode 0.159.0). Under Git Bash, run docker validate: docker run --rm with a read-only bind of deploy/collector/otel-collector-config.yaml at /etc/otelcol-contrib/config.yaml using that COLLECTOR_IMAGE, command validate --config=/etc/otelcol-contrib/config.yaml. Recreate the collector container so the mounted config reloads (force-recreate the collector service on the observability profile).

Create scripts/verify-collector-prom-export.sh (POSIX, same go/path discovery style as scripts/generate-dashboards.sh). Enforce a single wall-clock budget of 30 seconds for connect plus poll (Nyquist 8b). The script must: (1) fail if http://127.0.0.1:8889/metrics is unreachable within the budget; (2) parse every JSON "expr" value from deploy/grafana/dashboards/lancet-rag-operations.json and extract PromQL metric identifiers (the lancet_* tokens, stripping _count/_bucket/_sum/_total suffixes used only for matching families as needed so each panel's queried family is represented); (3) POST one OTLP HTTP metrics payload to http://127.0.0.1:4318/v1/metrics that includes all ten D-35 instruments with source names lancet.rag.query.duration (unit ms, histogram), lancet.rag.retrieval.path_failures (counter), lancet.rag.answer.degraded (counter), lancet.rag.citation.repairs (counter), lancet.rag.generation.retries (counter), lancet.rag.evidence.set_size (histogram, no ms unit), lancet.ingest.documents (counter), lancet.ingest.chunks (counter), lancet.index.rebuild.duration (unit ms, histogram), lancet.index.corpus_generation (gauge); (4) poll :8889/metrics until every parsed dashboard stem is a substring of the scrape body or the 30s budget ends; (5) exit 0 only if every parsed stem is present and no scrape line contains a doubled extra prefix of those stems. Use a unique datapoint attribute or start/end scrape snapshot if residual series remain. Print the observed exported lines for both duration histograms to stdout.

Fourth decision (record in 06.2-07-SUMMARY.md): if the scrape shows duration families whose unit token is milliseconds while the committed dashboard exprs still query the _ms form, update only the PromQL string literals in deploy/grafana/dashboard_gen/main.go to the observed unit token, run sh scripts/generate-dashboards.sh so the committed JSON matches, then re-run this script until stems parsed from the new JSON all appear. If the scrape already contains the committed _ms stems, leave generator and JSON unchanged. Do not remove `.with_unit("ms")` as a way to keep stale _ms PromQL.

Gap reason: Collector extra prefix stacked on source lancet. prefix produced series no dashboard query matches; a unit-less probe cannot settle Collector unit translation (06.2-REVIEWS.md HIGH).</action>
  <verify>
    <automated>COLLECTOR_IMAGE=$(awk '/^  collector:/{c=1} c&&/image:/{print $2; exit}' docker-compose.yml) && docker run --rm -v "$(pwd)/deploy/collector/otel-collector-config.yaml:/etc/otelcol-contrib/config.yaml:ro" "$COLLECTOR_IMAGE" validate --config=/etc/otelcol-contrib/config.yaml && sh scripts/verify-collector-prom-export.sh</automated>
  </verify>
  <acceptance_criteria>
    - The prometheus exporter mapping in deploy/collector/otel-collector-config.yaml has an endpoint and no extra prefix key.
    - `! awk '/^  prometheus:/{p=1} p&&/^  [a-z]/{if(!/^  prometheus:/)p=0} p' deploy/collector/otel-collector-config.yaml | grep -q namespace` (prometheus exporter region only; zero-match must exit 0).
    - Source files engine/src/telemetry/metrics.rs still declare the ten D-35 names beginning with lancet. (do not strip that prefix) and still call `.with_unit("ms")` on the two duration histograms unless the fourth-decision branch was regenerate-dashboard (unit stays either way).
    - `sh scripts/verify-collector-prom-export.sh` exits 0 against the live collector within 30s wall time; every stem parsed from the current lancet-rag-operations.json expr fields is present in the scrape; doubled extra-prefix families are absent. On connect or poll expiry the script exits non-zero before 30s of extra wait.
    - `grep -E 'max-time|connect-timeout|30' scripts/verify-collector-prom-export.sh` shows a documented 30s connect-plus-poll cap (curl flags and/or a deadline variable).
    - The script reads stems from the dashboard JSON file path, not from a hardcoded ten-name list in the shell source (JSON is the stem source of truth).
    - docker validate used COLLECTOR_IMAGE from docker-compose.yml and exited 0.
    - If fourth-decision regenerate ran: `sh scripts/generate-dashboards.sh` exits 0 and git diff of the JSON is only PromQL unit-token changes produced by the generator.
  </acceptance_criteria>
  <done>One live metric path from OTLP HTTP through Collector Prometheus exposition matches every operations-dashboard PromQL stem, including duration histogram unit suffixes, on the compose-pinned Collector image (D-34, D-40).</done>
</task>

<task type="auto">
  <name>Remove committed dashboard_gen binary and ignore rebuilds</name>
  <read_first>06.2-REVIEW.md CR-01; 06.2-VERIFICATION.md SC6 exe gap; scripts/generate-dashboards.sh (go run main.go, never an exe); .gitignore Go compiled-binaries section (gateway/gateway.exe pattern); git ls-files deploy/grafana/dashboard_gen/</read_first>
  <files>.gitignore, deploy/grafana/dashboard_gen/dashboard_gen.exe</files>
  <action>git rm deploy/grafana/dashboard_gen/dashboard_gen.exe if it is still tracked. Add ignore lines for deploy/grafana/dashboard_gen/*.exe and deploy/grafana/dashboard_gen/dashboard_gen next to the existing gateway binary ignores in .gitignore. Do not change scripts/generate-dashboards.sh; it already uses go run. Do not add the generator to CI (D-85).</action>
  <verify>
    <automated>git ls-files -- deploy/grafana/dashboard_gen/dashboard_gen.exe deploy/grafana/dashboard_gen/dashboard_gen; test ! -n "$(git ls-files -- deploy/grafana/dashboard_gen/dashboard_gen.exe)"; grep -n "dashboard_gen" .gitignore; sh scripts/generate-dashboards.sh</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files -- deploy/grafana/dashboard_gen/` lists source files (main.go, go.mod, go.sum) and no .exe and no file named dashboard_gen.
    - .gitignore contains deploy/grafana/dashboard_gen/*.exe and deploy/grafana/dashboard_gen/dashboard_gen.
    - `sh scripts/generate-dashboards.sh` still exits 0 (byte-identical committed JSON after any Task 1 generator update).
  </acceptance_criteria>
  <done>Dashboards remain generated from typed Go; compiled generator artifacts are untracked (CR-01, D-40).</done>
</task>

<task type="auto" tdd="true">
  <name>Lock export-name contract, scrape-derived SUMMARY, re-own manual checks, record WR debt</name>
  <read_first>engine/src/tests/telemetry_metrics.rs dashboard_panels_resolve_to_defined_instruments (approx lines 744-787) and stem list ~761-772; rust-guidelines.md; 06.2-03-SUMMARY.md Observed Prometheus Metric Transformation Rule; scripts/engine-test-targets.sh TOTAL and LIB_COUNT pins (currently 474 / 434); 06.2-VALIDATION.md Manual-Only Verifications table; .planning/STATE.md Known Issues Phase 06.2 bullets; 06.2-REVIEW.md WR-01..WR-06 and IN-01 headings</read_first>
  <files>engine/src/tests/telemetry_metrics.rs, scripts/engine-test-targets.sh, .planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-03-SUMMARY.md, .planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-VALIDATION.md, .planning/STATE.md</files>
  <behavior>
    - New test `collector_prometheus_exporter_has_no_namespace` reads deploy/collector/otel-collector-config.yaml relative to CARGO_MANIFEST_DIR parent, isolates the prometheus exporter mapping (the block under exporters whose key is prometheus), and asserts that region contains endpoint and does not contain an extra prefix key.
    - Existing `dashboard_panels_resolve_to_defined_instruments` still requires all ten panel exprs to reference the stems that match the live scrape (after any Task 1 fourth-decision JSON update) and still fails if a panel expr used a doubled extra-prefix stem.
    - 06.2-03-SUMMARY.md Observed Prometheus Metric Transformation Rule quotes verbatim exported lines from Task 1 (including both duration histogram families) and the resolved COLLECTOR_IMAGE tag; it does not claim a unit suffix that was not observed.
  </behavior>
  <action>Read rust-guidelines.md before editing .rs files. Add the yaml-region test in engine/src/tests/telemetry_metrics.rs using std filesystem reads only (no new crate). Keep dashboard_panels_resolve_to_defined_instruments; if Task 1 regenerated PromQL, update that test's stem literals to the new JSON. After adding tests, re-measure `sh scripts/engine-test-targets.sh` and raise TOTAL and LIB_COUNT (and LIB_BIN_SUM) to the new measured values in the same commit; do not lower pins. This plan is the sole writer of scripts/engine-test-targets.sh in the gap-closure waves.

Rewrite the Observed Prometheus Metric Transformation Rule section in 06.2-03-SUMMARY.md only after Task 1: transcribe observed :8889 lines verbatim, then derive dots→underscores / counter and histogram suffixes / unit token from those lines. Do not pre-write that millisecond duration units append `_ms` unless that exact token appears in the scrape. Note the prior extra-prefix wording was incorrect versus the orchestrator scrape. Record COLLECTOR_IMAGE in 06.2-07-SUMMARY.md.

Update 06.2-VALIDATION.md: set `plans` to include 06.2-07, 06.2-08, 06.2-09; add a Wave 7 re-run subsection stating all four BLOCKING Manual-Only rows are re-owned by 06.2-07/T3 against the post-CR-02 tree (do not rewrite executed 06.2-01 or 06.2-03 PLAN.md). Keep original Owning Task ids as historical; add a Re-run owner column or equivalent row note of 06.2-07/T3.

Append under STATE.md Known Issues (Phase 06.2 section) one bullet each: DEBT-P6.2-WR-01 (streamEndedNormally unreachable in gateway/main.go queryRAG), DEBT-P6.2-WR-02 (retrieval path_failures kind via error-message substring in retrieve.rs), DEBT-P6.2-WR-03 (REBUILD_TEST_MUTEX not held by rebuild_failure_degrades_not_fails and rebuild_checkout_latest_failure_degrades_not_fails), DEBT-P6.2-WR-04 (Grafana GF_SECURITY_ADMIN_USER/PASSWORD committed admin/admin), DEBT-P6.2-WR-05 (otelhttp span names use raw URL path), DEBT-P6.2-WR-06 (engine telemetry_handle.shutdown skipped on startup ? paths), DEBT-P6.2-IN-01 (per-call meter instrument construction). Do not change production code for those ids.</action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib telemetry_metrics::dashboard_panels_resolve_to_defined_instruments telemetry_metrics::collector_prometheus_exporter_has_no_namespace -- --exact; sh scripts/engine-test-targets.sh</automated>
    <human-check>After CR-02 fix and live scrape green: (1) operations dashboard — run one ingestion and several queries including a degrading one; all ten panels render non-empty series where work was performed, no datasource or unknown-metric errors (VALIDATION row originally 06.2-03/T3). (2) Grafana trace↔log↔metric click-through for one query (originally 06.2-03/T2). (3) Windows Docker Desktop bind-mount smoke from Git Bash (originally 06.2-03/T1). (4) Stop collector, issue a query, confirm stdout degrade (originally 06.2-01/T2). Harvested end-of-phase per human_verify_mode; all four BLOCKING.</human-check>
  </verify>
  <acceptance_criteria>
    - `collector_prometheus_exporter_has_no_namespace` exists and passes.
    - `dashboard_panels_resolve_to_defined_instruments` still passes and matches the stems in the committed dashboard JSON after any fourth-decision update.
    - 06.2-03-SUMMARY.md transformation section quotes live scrape lines and does not describe a prometheus exporter extra prefix; unit-suffix wording matches Task 1 stdout, not a predicted `_ms` rule.
    - engine-test-targets.sh pins equal the post-change `cargo test --list` counts (raised, not lowered).
    - 06.2-VALIDATION.md names 06.2-07/T3 as re-run owner of all four BLOCKING manual rows.
    - STATE.md contains the seven DEBT-P6.2-WR-01..06 and DEBT-P6.2-IN-01 identifiers.
  </acceptance_criteria>
  <done>Double-prefix cannot return via Collector config without a failing unit test, Plan 03's recorded transformation matches the live scrape including unit suffixes, manual checks are re-owned, and review warnings are debt-tracked (D-40).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engine/gateway OTLP → Collector | Metric names and units cross into Prometheus exposition here. |
| Collector :8889 → Prometheus/Grafana | Scraped series names are the only contract dashboards have. |
| Git tree → contributors | Committed binaries are a supply-chain and bloat surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.2-07-01 | Tampering | prometheus exporter extra prefix | high | mitigate | Remove extra prefix; yaml-region unit test plus live all-stems script so a reintroduced prefix fails both gates. |
| T-06.2-07-02 | Information Disclosure | synthetic OTLP probe | low | accept | Probe uses D-35 instrument names and no query text; local loopback only (D-06). |
| T-06.2-07-03 | Tampering | committed dashboard_gen.exe | medium | mitigate | git rm plus gitignore so the PE cannot return unnoticed via `git ls-files`. |
| T-06.2-07-04 | Tampering | histogram unit suffix vs dashboard PromQL | high | mitigate | Live scrape asserts every JSON-derived stem including duration histograms; fourth decision regenerates typed PromQL if the compose-pinned Collector expands `ms`. Block-on-high: panels otherwise stay empty after prefix-only fix. |
| T-06.2-07-SC | Tampering | package installs | high | accept | This plan adds no npm/pip/cargo packages. |
</threat_model>

<verification>
- docker validate against compose-parsed COLLECTOR_IMAGE exits 0.
- `sh scripts/verify-collector-prom-export.sh` exits 0 and finishes within 30s wall time (fail-closed on connect/poll budget); all dashboard stems present.
- `git ls-files -- deploy/grafana/dashboard_gen/` has no .exe.
- Named telemetry_metrics tests and `sh scripts/engine-test-targets.sh` exit 0.
- Do not re-run full instrumentation matrices for SC2/SC3/SC4/SC8.
</verification>

<success_criteria>
- Live :8889 series names match every dashboard PromQL stem including duration unit suffixes (SC5/SC6 data path).
- No tracked dashboard_gen binary (CR-01).
- Plan 03 SUMMARY transformation rule matches the live scrape and records the running Collector tag.
- Four BLOCKING manual checks re-owned; WR/IN debt ids recorded.
</success_criteria>

## Artifacts this phase produces

- New: `scripts/verify-collector-prom-export.sh`
- New test: `collector_prometheus_exporter_has_no_namespace` in `engine/src/tests/telemetry_metrics.rs`
- Updated: `06.2-03-SUMMARY.md` transformation rule (scrape-derived)
- Updated: `06.2-VALIDATION.md` wave-7 re-run ownership
- Updated: `.planning/STATE.md` DEBT-P6.2-WR-* / IN-01
- Conditional: dashboard_gen PromQL + regenerated JSON if unit translation mismatches `_ms`
- None new: D-35 instrument name strings, compose profile split

<output>
Create `.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-07-SUMMARY.md` recording the exact scraped metric lines from :8889 (especially both duration histograms), the resolved COLLECTOR_IMAGE tag, which fourth-decision branch ran, the collector recreate command used, and the new engine-test-targets counts.
</output>
