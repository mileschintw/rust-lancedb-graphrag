---
phase: 06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit
plan: 07
type: execute
wave: 6
depends_on:
  - 06.2-03
  - 06.2-05
files_modified:
  - deploy/collector/otel-collector-config.yaml
  - .gitignore
  - scripts/verify-collector-prom-export.sh
  - engine/src/tests/telemetry_metrics.rs
  - scripts/engine-test-targets.sh
  - .planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-03-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - OBS-01
user_setup:
  - service: docker-compose observability profile
    why: "Live Collector Prometheus exposition round-trip for CR-02 (SC5/SC6). The textual dashboard-instrument test cannot see double-prefixing."
    dashboard_config:
      - task: "Bring up the observability profile so host 127.0.0.1:4318 and 127.0.0.1:8889 are reachable"
        location: "repo root — docker compose --profile observability up -d"
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Collector Prometheus exposition publishes source-prefixed series that dashboard PromQL already queries: `lancet_rag_*`, `lancet_ingest_*`, `lancet_index_*` — not a doubled collector prefix (D-34, D-35, D-40)."
    - "A live OTLP HTTP push of an already-`lancet.`-prefixed instrument through 127.0.0.1:4318 is scraped from 127.0.0.1:8889 under the single-prefixed Prometheus name; the doubled form is absent (CR-02 / SC5 / SC6)."
    - "Engine instrument names in `engine/src/telemetry/metrics.rs` keep their existing `lancet.` prefix; the Collector prometheus exporter does not add a second namespace (D-35, D-42)."
    - "`dashboard_panels_resolve_to_defined_instruments` plus a live scrape gate together cover the transformation: textual stem matching alone is not the only check (VERIFICATION info coverage gap)."
    - "`deploy/grafana/dashboard_gen` contains generator source only; compiled Windows or Unix binaries of that CLI are not tracked in git (CR-01 / D-40 generated-from-typed-code)."
    - "06.2-03-SUMMARY.md's Observed Prometheus Metric Transformation Rule describes the live scrape after this fix, including that the prometheus exporter has no namespace field (D-40)."
    - statement: "OBS-01 unclassified edge remains unresolved by the deterministic probe; this gap plan closes CR-01/CR-02 only, not SC1 cross-process continuity or SC7 degrade-to-stdout (those stay end-of-phase human checks)."
      verification: backstop
  flagged_assumptions:
    - "OBS-01 edge-probe returned category unclassified, status unresolved (coverage applicable 1, resolved 0, unresolved 1). Not auto-resolved. Concrete acceptance for this plan is the CR-01/CR-02 missing items in 06.2-VERIFICATION.md, not a probe-derived edge predicate."
    - "Live scrape requires the observability profile (user_setup). If 4318/8889 are down, the executor stops on the tracer precondition rather than predicting series names."
  prohibitions:
    - statement: "Must not reintroduce a Collector prometheus-exporter namespace on top of source `lancet.` instrument names (the double-prefix that made every panel No data)."
      flagged: true
    - statement: "Must not drop or rename source-side `lancet.` prefixes on D-35 instruments to paper over the Collector namespace."
      flagged: true
    - statement: "Must not hand-edit deploy/grafana/dashboards/lancet-rag-operations.json; panel exprs already use the single-prefixed stems."
      flagged: true
    - statement: "Must not commit compiled dashboard_gen binaries after they are removed."
      flagged: true
  artifacts:
    - path: "deploy/collector/otel-collector-config.yaml"
      provides: "Prometheus exporter without a namespace field so source `lancet.*` names survive sanitization as `lancet_*`."
    - path: "scripts/verify-collector-prom-export.sh"
      provides: "Live OTLP HTTP → Collector :8889 round-trip asserting single-prefix series and rejecting doubled prefix."
    - path: ".gitignore"
      provides: "Ignore rules for dashboard_gen compiled binaries."
  key_links:
    - from: "engine/src/telemetry/metrics.rs"
      to: "deploy/grafana/dashboards/lancet-rag-operations.json"
      via: "Collector prometheus exporter (no extra namespace) then Prometheus scrape of :8889"
      pattern: "collector_fanout"
---

## Phase User Story

**As a** operator viewing the provisioned RAG operations dashboard, **I want to** query Prometheus series whose names match the typed dashboard PromQL, **so that** Grafana panels render real Collector-exported data instead of empty graphs (D-34, D-40).

<objective>
Close CR-02 (Collector prometheus-exporter namespace double-prefix breaking SC5/SC6 dashboard data) and CR-01 (committed dashboard_gen.exe). Re-verify export names against a live scrape and correct the Plan 03 SUMMARY transformation rule that documented the wrong live result.

Purpose: SC5 fan-out and SC6 dashboards-as-code are structurally shipped but hollow until series names match panel queries; the stray binary contradicts generated-from-typed-code.
Output: collector config without prometheus namespace, live export-name script, gitignored generator binaries, corrected 06.2-03-SUMMARY.md, a yaml-level regression test so the textual dashboard test cannot be the only gate.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-observability-evaluation-polish/06-CONTEXT.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-VERIFICATION.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-REVIEW.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-03-SUMMARY.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-03-PLAN.md
@deploy/collector/otel-collector-config.yaml
@deploy/grafana/dashboards/lancet-rag-operations.json
@engine/src/tests/telemetry_metrics.rs
@engine/src/telemetry/metrics.rs
@.gitignore
@scripts/generate-dashboards.sh
</context>

<scope_rationale>
Gap cluster is observability-as-code delivery (SC5/SC6), not instrumentation. Do not re-plan SC2/SC3/SC4/SC8 span or metric definition work. Discretion: live proof is a POSIX script next to generate-dashboards.sh rather than a Rust #[ignore] test, so the default cargo suite stays docker-free while the plan verify still requires the live hop.
</scope_rationale>

<tasks>

<task type="tracer">
  <name>End-to-end Collector export names match dashboard stems — one metric path</name>
  <reversibility rating="reversible">Removing the prometheus exporter namespace is a one-key Collector config change; undo is restoring the field.</reversibility>
  <read_first>.planning/phases/06-observability-evaluation-polish/06-CONTEXT.md decisions D-34, D-35, D-39, D-40, D-42; 06.2-VERIFICATION.md gaps CR-02/SC5/SC6; 06.2-REVIEW.md CR-02; 06.2-03-SUMMARY.md Observed Prometheus Metric Transformation Rule; deploy/collector/otel-collector-config.yaml exporters.prometheus block; engine/src/telemetry/metrics.rs instrument names (keep `lancet.` at source); deploy/grafana/dashboards/lancet-rag-operations.json panel expr stems; docker-compose.yml collector service ports 4318 and 8889</read_first>
  <files>deploy/collector/otel-collector-config.yaml, scripts/verify-collector-prom-export.sh</files>
  <precondition>docker compose --profile observability is running with host 127.0.0.1:4318 (OTLP HTTP) and 127.0.0.1:8889 (Prometheus exposition) accepting connections. If either port is closed, start or recreate the collector from the repo root before this task; do not invent series names.</precondition>
  <action>In deploy/collector/otel-collector-config.yaml, delete the prometheus exporter's namespace field so that exporter only sets endpoint 0.0.0.0:8889. Leave otlp/jaeger and otlphttp/loki tls blocks unchanged. Do not edit engine/src/telemetry/metrics.rs instrument names; they already begin with lancet. (D-35, D-42). Recreate the collector container so the mounted config reloads (force-recreate the collector service on the observability profile).

Create scripts/verify-collector-prom-export.sh (POSIX, same go/path discovery style as scripts/generate-dashboards.sh). Enforce a single wall-clock budget of 30 seconds for connect plus poll (Nyquist 8b): curl --connect-timeout and --max-time must fit inside that budget; poll :8889/metrics until the family appears or the remaining budget is exhausted; if the budget elapses without a matching scrape, exit non-zero (fail-closed). The script: (1) curl-fails if http://127.0.0.1:8889/metrics is unreachable within the budget; (2) POSTs one OTLP HTTP metrics payload to http://127.0.0.1:4318/v1/metrics whose instrument name is exactly lancet.ingest.documents (a D-35 name the dashboard already queries as lancet_ingest_documents_total); (3) polls :8889/metrics until that family appears or the 30s budget ends; (4) exits 0 only if the scrape contains lancet_ingest_documents and does not contain lancet_lancet_ingest_documents. Use a unique datapoint attribute or start/end scrape snapshot if residual series from a previous double-prefix scrape remain — after recreate they should be gone. Record the observed exported line in 06.2-07-SUMMARY.md.

Gap reason: Collector namespace stacked on source lancet. prefix produced series no dashboard query matches (orchestrator live repro on :8889).</action>
  <verify>
    <automated>sh scripts/verify-collector-prom-export.sh</automated>
  </verify>
  <acceptance_criteria>
    - The prometheus exporter mapping in deploy/collector/otel-collector-config.yaml has an endpoint and no namespace key.
    - `awk '/^  prometheus:/{p=1} p&&/^  [a-z]/{if(!/^  prometheus:/)p=0} p' deploy/collector/otel-collector-config.yaml | grep -c namespace` is 0 (prometheus exporter region only).
    - Source files engine/src/telemetry/metrics.rs still declare the ten D-35 names beginning with lancet. (do not strip that prefix).
    - `sh scripts/verify-collector-prom-export.sh` exits 0 against the live collector within 30s wall time; scrape shows lancet_ingest_documents and not lancet_lancet_ingest_documents. On connect or poll expiry the script exits non-zero before 30s of extra wait.
    - `grep -E 'max-time|connect-timeout|30' scripts/verify-collector-prom-export.sh` shows a documented 30s connect-plus-poll cap (curl flags and/or a deadline variable).
    - Dashboard JSON panel exprs still contain lancet_rag_query_duration_ms (unchanged committed JSON).
  </acceptance_criteria>
  <done>One live metric path from OTLP HTTP through Collector Prometheus exposition uses the same single-prefixed family the operations dashboard already queries (D-34, D-40).</done>
</task>

<task type="auto">
  <name>Remove committed dashboard_gen binary and ignore rebuilds</name>
  <read_first>06.2-REVIEW.md CR-01; 06.2-VERIFICATION.md SC6 exe gap; scripts/generate-dashboards.sh (go run main.go, never an exe); .gitignore Go compiled-binaries section (gateway/gateway.exe pattern); git ls-files deploy/grafana/dashboard_gen/</read_first>
  <files>.gitignore, deploy/grafana/dashboard_gen/dashboard_gen.exe</files>
  <action>git rm deploy/grafana/dashboard_gen/dashboard_gen.exe if it is still tracked. Add ignore lines for deploy/grafana/dashboard_gen/*.exe and deploy/grafana/dashboard_gen/dashboard_gen next to the existing gateway binary ignores in .gitignore. Do not change scripts/generate-dashboards.sh; it already uses go run. Do not add the generator to CI.</action>
  <verify>
    <automated>git ls-files -- deploy/grafana/dashboard_gen/dashboard_gen.exe deploy/grafana/dashboard_gen/dashboard_gen; test ! -n "$(git ls-files -- deploy/grafana/dashboard_gen/dashboard_gen.exe)"; grep -n "dashboard_gen" .gitignore; sh scripts/generate-dashboards.sh</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files -- deploy/grafana/dashboard_gen/` lists source files (main.go, go.mod, go.sum) and no .exe and no file named dashboard_gen.
    - .gitignore contains deploy/grafana/dashboard_gen/*.exe and deploy/grafana/dashboard_gen/dashboard_gen.
    - `sh scripts/generate-dashboards.sh` still exits 0 (byte-identical committed JSON).
  </acceptance_criteria>
  <done>Dashboards remain generated from typed Go; compiled generator artifacts are untracked (CR-01, D-40).</done>
</task>

<task type="auto" tdd="true">
  <name>Lock export-name contract in tests and correct Plan 03 SUMMARY</name>
  <read_first>engine/src/tests/telemetry_metrics.rs dashboard_panels_resolve_to_defined_instruments (approx lines 744-787); rust-guidelines.md; 06.2-03-SUMMARY.md Observed Prometheus Metric Transformation Rule; scripts/engine-test-targets.sh TOTAL and LIB_COUNT pins (currently 474 / 434)</read_first>
  <files>engine/src/tests/telemetry_metrics.rs, scripts/engine-test-targets.sh, .planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-03-SUMMARY.md</files>
  <behavior>
    - New test `collector_prometheus_exporter_has_no_namespace` reads deploy/collector/otel-collector-config.yaml relative to CARGO_MANIFEST_DIR parent, isolates the prometheus exporter mapping (the block under exporters whose key is prometheus), and asserts that region contains endpoint and does not contain a namespace key.
    - Existing `dashboard_panels_resolve_to_defined_instruments` still requires all ten panel exprs to reference the single-prefixed stems (lancet_rag_query_duration_ms, lancet_rag_retrieval_path_failures_total, lancet_rag_answer_degraded_total, lancet_rag_citation_repairs_total, lancet_rag_generation_retries_total, lancet_rag_evidence_set_size, lancet_ingest_documents_total, lancet_ingest_chunks_total, lancet_index_rebuild_duration_ms, lancet_index_corpus_generation) and still fails if a panel expr used a doubled stem.
    - 06.2-03-SUMMARY.md Observed Prometheus Metric Transformation Rule no longer claims the prometheus exporter uses a namespace; it states dots become underscores, histogram/counter suffixes, _ms from with_unit("ms"), and that the exporter adds no extra prefix because source names already start with lancet.
  </behavior>
  <action>Read rust-guidelines.md before editing .rs files. Add the yaml-region test in engine/src/tests/telemetry_metrics.rs using std filesystem reads only (no new crate). Keep dashboard_panels_resolve_to_defined_instruments. After adding tests, re-measure `sh scripts/engine-test-targets.sh` and raise TOTAL and LIB_COUNT (and LIB_BIN_SUM) to the new measured values in the same commit; do not lower pins. This plan is the sole writer of scripts/engine-test-targets.sh in wave 6. Rewrite the Observed Prometheus Metric Transformation Rule section in 06.2-03-SUMMARY.md to match the live script result from Task 1, and note that the prior namespace-inclusive wording was incorrect versus the orchestrator scrape.</action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib telemetry_metrics::dashboard_panels_resolve_to_defined_instruments telemetry_metrics::collector_prometheus_exporter_has_no_namespace -- --exact; sh scripts/engine-test-targets.sh</automated>
  </verify>
  <acceptance_criteria>
    - `collector_prometheus_exporter_has_no_namespace` exists and passes.
    - `dashboard_panels_resolve_to_defined_instruments` still passes and still matches single-prefixed stems.
    - 06.2-03-SUMMARY.md transformation section does not describe a prometheus exporter namespace; it cites single-prefixed examples consistent with Task 1's scrape.
    - engine-test-targets.sh pins equal the post-change `cargo test --list` counts (raised, not lowered).
  </acceptance_criteria>
  <done>Double-prefix cannot return via Collector config without a failing unit test, and Plan 03's recorded transformation matches the live scrape (D-40).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engine/gateway OTLP → Collector | Metric names cross into Prometheus exposition here. |
| Collector :8889 → Prometheus/Grafana | Scraped series names are the only contract dashboards have. |
| Git tree → contributors | Committed binaries are a supply-chain and bloat surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.2-07-01 | Tampering | prometheus exporter namespace | high | mitigate | Remove namespace; yaml-region unit test plus live script so a reintroduced prefix fails both gates. |
| T-06.2-07-02 | Information Disclosure | synthetic OTLP probe | low | accept | Probe uses a D-35 instrument name and no query text; local loopback only (D-06). |
| T-06.2-07-03 | Tampering | committed dashboard_gen.exe | medium | mitigate | git rm plus gitignore so the PE cannot return unnoticed via `git ls-files`. |
| T-06.2-07-SC | Tampering | package installs | high | accept | This plan adds no npm/pip/cargo packages. |
</threat_model>

<verification>
- `sh scripts/verify-collector-prom-export.sh` exits 0 and finishes within 30s wall time (fail-closed on connect/poll budget).
- `git ls-files -- deploy/grafana/dashboard_gen/` has no .exe.
- Named telemetry_metrics tests and `sh scripts/engine-test-targets.sh` exit 0.
- Do not re-run full instrumentation matrices for SC2/SC3/SC4/SC8.
</verification>

<success_criteria>
- Live :8889 series names match dashboard PromQL stems (SC5/SC6 data path).
- No tracked dashboard_gen binary (CR-01).
- Plan 03 SUMMARY transformation rule matches the live scrape.
</success_criteria>

## Artifacts this phase produces

- New: `scripts/verify-collector-prom-export.sh`
- New test: `collector_prometheus_exporter_has_no_namespace` in `engine/src/tests/telemetry_metrics.rs`
- Updated: `06.2-03-SUMMARY.md` transformation rule
- None new: Grafana JSON, D-35 instruments, compose profile split

<output>
Create `.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-07-SUMMARY.md` recording the exact scraped metric line from :8889, the collector recreate command used, and the new engine-test-targets counts.
</output>
