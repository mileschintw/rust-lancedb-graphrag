---
phase: 06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit
plan: 11
status: completed
date: 2026-08-27
files_modified:
  - deploy/grafana/dashboard_gen/main.go
  - deploy/grafana/dashboards/lancet-rag-operations.json
  - engine/src/tests/telemetry_metrics.rs
  - scripts/engine-test-targets.sh
  - .planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-VALIDATION.md
---

# Phase 06.2 Plan 11 Summary: RAG Operations Dashboard Duration Histogram Metrics (G-06.2-2)

## Executive Overview
Plan 11 closes G-06.2-2 by changing Panel 1 ("RAG Query Duration") PromQL expression from query throughput (`rate(..._count[5m])`) to latency distribution (`histogram_quantile(0.95, sum(rate(..._bucket[5m])) by (le, outcome))`) with legend `p95 - {{outcome}}`, adding `Unit("ms")` to both duration panels (Panel 1 and Panel 9), regenerating the dashboard deterministically via the typed generator, and locking the histogram contract with a committed Rust regression test.

### Key Actions Completed
1. **Generator and Dashboard Updates**:
   - Before: `sum by (outcome) (rate(lancet_rag_query_duration_milliseconds_count[5m]))` with legend `{{outcome}}`.
   - After: `histogram_quantile(0.95, sum(rate(lancet_rag_query_duration_milliseconds_bucket[5m])) by (le, outcome))` with legend `p95 - {{outcome}}`.
   - Added `Unit("ms")` to Panel 1 and Panel 9 in `deploy/grafana/dashboard_gen/main.go`.
   - Regenerated committed JSON via two-step command:
     1. `cd deploy/grafana/dashboard_gen && go run main.go -output ../dashboards/lancet-rag-operations.json`
     2. `sh scripts/generate-dashboards.sh` (verified byte-identical).
2. **Committed Rust Test & Fail-First Demonstration**:
   - Added `duration_panels_query_histogram_buckets` in `engine/src/tests/telemetry_metrics.rs` checking histogram stem `_bucket` usage + `histogram_quantile(` and duration title checks.
   - Fail-first demonstration via `cp`/`sed`/`cp`:
     - Temporarily injected `_count` expression on disk.
     - Observed failure:
       `panel query 'sum by (outcome) (rate(lancet_rag_query_duration_milliseconds_count[5m]))' reads histogram instrument 'lancet_rag_query_duration_milliseconds' but does not use histogram_quantile — G-06.2-2`
     - Restored fixed JSON and observed test pass.
3. **Cumulative Engine Target Measurement**:
   - Measured `cargo test --manifest-path engine/Cargo.toml -- --list`:
     - LIB_COUNT: 437 (cumulative baseline on top of Plan 10)
     - TOTAL: 477 (cumulative baseline on top of Plan 10)
     - LIB_BIN_SUM: 437
   - Updated `scripts/engine-test-targets.sh`; verified all 7 invariants pass.
4. **Validation Map Update**:
   - Updated `06.2-VALIDATION.md` in place with the new automated row, updated dashboard contract row, and retargeted Operations dashboard manual check Re-run owner to `06.2-11/T2`.

## Verification Results
- `sh scripts/generate-dashboards.sh`: Passed (byte-identical).
- `cargo test --manifest-path engine/Cargo.toml --lib -- duration_panels_query_histogram_buckets`: Passed.
- `cargo test --manifest-path engine/Cargo.toml --lib -- dashboard_panels_resolve_to_defined_instruments`: Passed.
- `sh scripts/engine-test-targets.sh`: Passed (7/7 invariants, TOTAL=477, LIB=437).