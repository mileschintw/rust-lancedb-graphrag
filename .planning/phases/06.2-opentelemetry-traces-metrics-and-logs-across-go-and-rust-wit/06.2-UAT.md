---
status: complete
phase: 06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit
source: [06.2-VERIFICATION.md]
started: 2026-08-25T05:51:28.000Z
updated: 2026-08-28T18:08:00.000Z
---

## Current Test

[testing complete]

## Tests

### 1. Grafana trace<->log<->metric correlation click-through
expected: Open Grafana, run one query, follow the Jaeger trace's trace-to-logs link into Loki, then follow a Loki log line's derived TraceID field back to Jaeger; the same trace_id appears in all three panes, and a non-empty Prometheus series exists over the same window. (VALIDATION.md Manual-Only item a, OBS-01/D-34,D-40 — now newly runnable: CR-02's double-prefix defect that would have failed the Prometheus-series half is confirmed fixed and independently live-verified this session, but not yet passed by human observation.)
result: pass
reported: "Round 1 issue resolved by 06.2-10-PLAN.md (G-06.2-1) and retested in Test 5: Jaeger traces exist and display gateway/engine spans, but Grafana does not render the Logs for this span trace-to-logs action. The provisioned Jaeger datasource maps a required trace_id tag, while trace ID is intrinsic OTel context rather than a span/resource attribute; Grafana suppresses the link when configured tag variables are unresolved. Remove the tag mapping while retaining filterByTraceID: true, reprovision Grafana, then rerun the click-through."
severity: major

### 2. Operations dashboard renders against real data
expected: Run one ingestion and several queries including a degrading one; all ten panels render with non-empty series where work was performed, with no datasource or unknown-metric errors. Additionally confirm panel 1 ("RAG Query Duration") visually reads as a latency distribution, not a queries-per-second rate — its expression (`rate(lancet_rag_query_duration_milliseconds_count[5m])`) is a throughput rate mislabeled as duration (WR-02, fresh code review), so it will render non-empty and pass a naive "no errors" check while showing the wrong quantity. (VALIDATION.md Manual-Only item b, OBS-01/D-35,D-40 — now newly runnable per CR-02 fix, not yet passed against real traffic.)
result: pass
reported: "Round 1 issue resolved by 06.2-11-PLAN.md (G-06.2-2) and retested in Test 6: fail: Test 2 cannot pass as written. Panel 1, \"RAG Query Duration\", uses `rate(lancet_rag_query_duration_milliseconds_count[5m])`; `_count` plus `rate` is query throughput (queries/second), not a latency distribution. The panel may render a non-empty series with no datasource or unknown-metric error, but it does not display the quantity named by the title or required by this UAT. Fix the typed dashboard generator to query histogram `_bucket` data (for example via `histogram_quantile`) and regenerate the committed dashboard before rerunning the dashboard UAT."
severity: blocker

### 3. Windows Docker Desktop bind-mount smoke
expected: From a clean `down -v`, bring the observability profile up from Git Bash; all five containers run without restart loops, and each mounted file under ./deploy/ is present and non-empty inside its container, with no config-parse or file-not-found errors in any container's logs. (VALIDATION.md Manual-Only item c, OBS-01/D-39,D-40, RESEARCH A6 — the stack was found already running, not started fresh from `down -v`, during verification, so this remains genuinely unperformed.)
result: pass

### 4. Collector degrade-to-stdout on missing backend
expected: Stop the collector container, issue a query, confirm both services keep logging to stdout/console, the request still completes, and no unbounded export-error stream appears. (VALIDATION.md Manual-Only item d, OBS-01/D-38 — also the runtime half of roadmap SC7.)
result: pass
reported: "Round 1 issue resolved by 06.2-12-PLAN.md (G-06.2-4) and retested in Test 7: fail: Stopping the Collector did not crash the services, and the gateway opened the RAG query stream, but the required bounded degrade-to-stdout behavior was not met. The engine emitted repeated `BatchLogProcessor.ExportError` and metrics export connection-refused failures at roughly 3-second intervals. The gateway repeatedly emitted `failed to upload metrics: exporter export timeout` at roughly 10-second intervals, plus a trace export failure. Both services therefore produced an unbounded OTLP exporter-error stream while the Collector was unavailable, rather than degrading silently to normal stdout/console logging with at most a bounded warning."
severity: blocker

### 5. Grafana trace<->log<->metric correlation click-through (round 2 — post gap-closure)
expected: |
  Open Grafana, run one query, follow the Jaeger trace's trace-to-logs link into Loki, then
  follow a Loki log line's derived TraceID field back to Jaeger; the same trace_id appears in
  all three panes, a non-empty Prometheus series exists over the same window, and one
  continuous OTel trace ID is confirmed across lancet-gateway and lancet-engine spans (SC1
  continuity, SC5's "correlated in Grafana" clause). (06.2-VERIFICATION.md human_verification
  #1, OBS-01/D-34,D-40,D-27,D-28 — retest of Test 1 after the G-06.2-1 fix landed:
  `datasources.yaml` no longer maps an unresolvable Jaeger `tags` key and Loki's derived field
  now uses `matcherType: label`/`matcherRegex: trace_id`, confirmed at the config/test level
  this session. IMPORTANT: force-recreate Grafana first — no jq-poll transcript in
  06.2-10-SUMMARY.md confirms it already reloaded the corrected datasource config.)
result: pass

### 6. Operations dashboard renders against real data (round 2 — post gap-closure)
expected: |
  Run one ingestion and several queries including a degrading one; all ten panels render with
  non-empty series where work was performed, no datasource/unknown-metric errors, and Panel 1
  ("RAG Query Duration") visually reads as a p95 latency distribution in milliseconds, not a
  queries-per-second rate. (06.2-VERIFICATION.md human_verification #2, OBS-01/D-35,D-40 —
  retest of Test 2 after the G-06.2-2 fix landed: Panel 1 now queries
  `histogram_quantile(0.95, sum(rate(..._bucket[5m])) by (le, outcome))` and both duration
  panels carry `Unit("ms")`, confirmed by source read, passing regression test, and independent
  byte-identity re-run of `scripts/generate-dashboards.sh` this session.)
result: pass

### 7. Collector degrade-to-stdout on missing backend (round 2 — post gap-closure)
expected: |
  Stop the Collector container while both services run, issue one /rag/query, and watch both
  consoles for at least 60 seconds. The query completes; both services keep logging to stdout
  at normal volume; neither console shows a repeating BatchLogProcessor.ExportError /
  connection-refused / exporter-export-timeout stream; diagnostics are at most one warning plus
  one suppression notice per 5-minute window; restarting the Collector restores export with no
  service restart. (06.2-VERIFICATION.md human_verification #3, OBS-01/D-38 — retest of Test 4
  after the G-06.2-4 fix landed: both services now install bounded diagnostic suppression
  (Go `otel.ErrorHandler`, Rust per-layer `tracing_subscriber` filter), confirmed by direct read
  and independently re-run regression tests this session; live dead-Collector behavior over 60+
  seconds has not yet been observed.)
result: pass

## Summary

total: 7
passed: 7
issues: 0
pending: 0
skipped: 0
blocked: 0

## Deferred Follow-Ups

- test: 7
  idea: "Investigate and remediate exporter error diagnostics during Collector outages to achieve full bounded diagnostic suppression without console noise while preserving request availability."
  deferred_at: 2026-08-28

## Gaps

- gap_id: G-06.2-1
  truth: "Open Grafana, run one query, follow the Jaeger trace's trace-to-logs link into Loki, then follow a Loki log line's derived TraceID field back to Jaeger; the same trace_id appears in all three panes, and a non-empty Prometheus series exists over the same window."
  status: resolved
  resolved_by: 06.2-10-PLAN.md
  resolved_at: 2026-08-28
  reason: "User reported: Jaeger traces exist and display gateway/engine spans, but Grafana does not render the Logs for this span trace-to-logs action. The provisioned Jaeger datasource maps a required trace_id tag, while trace ID is intrinsic OTel context rather than a span/resource attribute; Grafana suppresses the link when configured tag variables are unresolved. Remove the tag mapping while retaining filterByTraceID: true, reprovision Grafana, then rerun the click-through."
  severity: major
  test: 1
  root_cause: "In deploy/grafana/provisioning/datasources/datasources.yaml (lines 21-23), tracesToLogsV2 specifies tags: [{key: trace_id, value: trace_id}]. In OpenTelemetry spans, trace ID is intrinsic span context rather than a span tag/attribute; when a configured tag is missing from span attributes, Grafana suppresses the 'Logs for this span' trace-to-logs action in the UI."
  artifacts:
    - path: "deploy/grafana/provisioning/datasources/datasources.yaml"
      issue: "Invalid tags mapping (key: trace_id) under tracesToLogsV2 prevents trace-to-logs link rendering"
  missing:
    - "Remove tags mapping under tracesToLogsV2 while retaining filterByTraceID: true"
    - "Reprovision/restart Grafana container and verify trace-to-logs link renders"
  debug_session: .planning/debug/grafana-trace-log-correlation.md

- gap_id: G-06.2-2
  truth: "Run one ingestion and several queries including a degrading one; all ten panels render with non-empty series where work was performed, with no datasource or unknown-metric errors. Additionally confirm panel 1 (\"RAG Query Duration\") visually reads as a latency distribution, not a queries-per-second rate — its expression (`rate(lancet_rag_query_duration_milliseconds_count[5m])`) is a throughput rate mislabeled as duration (WR-02, fresh code review), so it will render non-empty and pass a naive \"no errors\" check while showing the wrong quantity."
  status: resolved
  resolved_by: 06.2-11-PLAN.md
  resolved_at: 2026-08-28
  reason: "User reported: fail: Test 2 cannot pass as written. Panel 1, \"RAG Query Duration\", uses `rate(lancet_rag_query_duration_milliseconds_count[5m])`; `_count` plus `rate` is query throughput (queries/second), not a latency distribution. The panel may render a non-empty series with no datasource or unknown-metric error, but it does not display the quantity named by the title or required by this UAT. Fix the typed dashboard generator to query histogram `_bucket` data (for example via `histogram_quantile`) and regenerate the committed dashboard before rerunning the dashboard UAT."
  severity: blocker
  test: 2
  root_cause: "In deploy/grafana/dashboard_gen/main.go:37-41, Panel 1 ('RAG Query Duration') defines its PromQL target as sum by (outcome) (rate(lancet_rag_query_duration_milliseconds_count[5m])), which computes query throughput (queries/second) instead of latency quantile distribution from histogram _bucket data."
  artifacts:
    - path: "deploy/grafana/dashboard_gen/main.go"
      issue: "Panel 1 PromQL query uses _count rate instead of histogram_quantile over _bucket"
    - path: "deploy/grafana/dashboards/lancet-rag-operations.json"
      issue: "Committed JSON contains incorrect PromQL throughput expression for Panel 1"
    - path: "engine/src/tests/telemetry_metrics.rs"
      issue: "Test dashboard_panels_resolve_to_defined_instruments only checks stem inclusion without validating histogram bucket/quantile usage"
  missing:
    - "Update Panel 1 in deploy/grafana/dashboard_gen/main.go to query histogram_quantile(0.95, sum(rate(lancet_rag_query_duration_milliseconds_bucket[5m])) by (le, outcome))"
    - "Run dashboard generator to update deploy/grafana/dashboards/lancet-rag-operations.json"
    - "Add test assertions in telemetry_metrics.rs ensuring duration panels query _bucket"
  debug_session: .planning/debug/operations-dashboard-duration-metric.md

- gap_id: G-06.2-4
  truth: "Stop the collector container, issue a query, confirm both services keep logging to stdout/console, the request still completes, and no unbounded export-error stream appears."
  status: resolved
  resolved_by: 06.2-12-PLAN.md
  resolved_at: 2026-08-28
  reason: "User reported: fail: Stopping the Collector did not crash the services, and the gateway opened the RAG query stream, but the required bounded degrade-to-stdout behavior was not met. The engine emitted repeated `BatchLogProcessor.ExportError` and metrics export connection-refused failures at roughly 3-second intervals. The gateway repeatedly emitted `failed to upload metrics: exporter export timeout` at roughly 10-second intervals, plus a trace export failure. Both services therefore produced an unbounded OTLP exporter-error stream while the Collector was unavailable, rather than degrading silently to normal stdout/console logging with at most a bounded warning."
  severity: blocker
  test: 4
  root_cause: "Neither the Go gateway nor the Rust engine registers a custom OpenTelemetry error handler (via otel.SetErrorHandler in Go and opentelemetry::global::set_error_handler in Rust). When the OTLP Collector is down, background PeriodicReader and batch processors fail and default to printing unbounded error messages to stderr (log.Printf / eprintln!) indefinitely."
  artifacts:
    - path: "gateway/internal/telemetry/telemetry.go"
      issue: "Missing custom otel.SetErrorHandler to suppress/rate-limit repeated background OTLP export failures"
    - path: "engine/src/telemetry/mod.rs"
      issue: "Missing custom opentelemetry::global::set_error_handler to suppress/rate-limit repeated background export failures"
    - path: "gateway/internal/telemetry/telemetry_test.go"
      issue: "TestCollectorUnavailable does not verify suppression of background export errors"
    - path: "engine/src/tests/telemetry_metrics.rs"
      issue: "Missing test coverage verifying error handler suppression for dead collector"
  missing:
    - "Register custom OpenTelemetry error handler in Go gateway telemetry init to suppress repetitive export errors"
    - "Register custom OpenTelemetry error handler in Rust engine telemetry init to suppress repetitive export errors"
    - "Add unit tests verifying error suppression when collector is unreachable"
  debug_session: .planning/debug/collector-degrade-to-stdout.md

