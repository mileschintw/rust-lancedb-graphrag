---
phase: 06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit
plan: 10
status: completed
date: 2026-08-27
files_modified:
  - deploy/grafana/provisioning/datasources/datasources.yaml
  - engine/src/tests/telemetry_metrics.rs
  - scripts/engine-test-targets.sh
  - .planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-VALIDATION.md
---

# Phase 06.2 Plan 10 Summary: Jaeger tracesToLogsV2 & Loki Label-Derived Correlation Fix (G-06.2-1)

## Executive Overview
Plan 10 closes G-06.2-1 by removing the unresolvable `tags: [{key: trace_id, value: trace_id}]` mapping from the Jaeger datasource's `tracesToLogsV2` block so Grafana stops suppressing the trace-to-logs action, and updating Loki `derivedFields` from body regex (`trace_id=([0-9a-fA-F]+)`) to structured metadata label matching (`matcherType: label` / `matcherRegex: trace_id`).

### Key Actions Completed
1. **Jaeger Datasource Configuration**:
   - Removed `tags:` mapping with `key: trace_id` from `tracesToLogsV2` in `deploy/grafana/provisioning/datasources/datasources.yaml`.
   - Retained `filterByTraceID: true` and `filterBySpanID: false`.
2. **Loki Datasource Configuration**:
   - Replaced body regex with `matcherType: label` and `matcherRegex: trace_id` under `derivedFields` in `datasources.yaml`.
3. **Fail-First Demonstration**:
   - Added `grafana_traces_to_logs_maps_no_span_tag` in `engine/src/tests/telemetry_metrics.rs`.
   - Executed against unmodified `datasources.yaml` and captured red failure:
     `tracesToLogsV2 must not have tags: mapping because trace_id is intrinsic span context (G-06.2-1): found line '        tags:'`
   - Modified `datasources.yaml` and re-executed: passed cleanly (1 passed).
4. **Test Target Pin Re-Measurement**:
   - Measured `cargo test --manifest-path engine/Cargo.toml -- --list`:
     - engine (lib): 436 (was 435)
     - TOTAL: 476 (was 475)
     - LIB_BIN_SUM: 436 (was 435)
   - Updated `scripts/engine-test-targets.sh` with the fresh measurement; verified all 7 invariants pass.
5. **Validation Map Update**:
   - Updated `06.2-VALIDATION.md` in place with the new automated test row and retargeted Grafana click-through Re-run owner to `06.2-10/T2` with SC1 continuous trace ID instructions.

## Verification Results
- `cargo test --manifest-path engine/Cargo.toml --lib -- grafana_traces_to_logs_maps_no_span_tag`: Passed.
- `sh scripts/engine-test-targets.sh`: Passed (All 7 Rust test target invariants verified successfully: TOTAL=476, LIB=436).
- `git grep "tags:" deploy/grafana/provisioning/datasources/datasources.yaml`: Zero matches.
- `git grep "matcherType: label" deploy/grafana/provisioning/datasources/datasources.yaml`: 1 match.
- `git grep "matcherRegex: trace_id" deploy/grafana/provisioning/datasources/datasources.yaml`: 1 match.