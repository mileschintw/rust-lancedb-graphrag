---
phase: 06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit
plan: 03
type: execute
wave: 2
depends_on:
  - 06.2-01
files_modified:
  - docker-compose.yml
  - deploy/collector/otel-collector-config.yaml
  - deploy/loki/loki-config.yaml
  - deploy/prometheus/prometheus.yml
  - deploy/grafana/provisioning/datasources/datasources.yaml
  - deploy/grafana/provisioning/dashboards/dashboards.yaml
  - deploy/grafana/dashboards/lancet-rag-operations.json
  - deploy/grafana/dashboard_gen/go.mod
  - deploy/grafana/dashboard_gen/go.sum
  - deploy/grafana/dashboard_gen/main.go
  - scripts/generate-dashboards.sh
autonomous: true
requirements:
  - OBS-01
estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Both services export OTLP to one Collector which fans out to Jaeger for traces, Prometheus for metrics and Loki for logs, with Grafana as the single correlated pane keyed on `trace_id` (D-34)."
    - "`docker compose` exposes a core surface of PostgreSQL alone with no profile flag, and an `observability` profile bringing up Collector, Jaeger, Prometheus, Loki and Grafana together (D-39)."
    - "Collector pipelines, the Prometheus scrape config, the Loki config, and Grafana datasources and dashboard provisioning are all committed to the repository and auto-provisioned on container start — there is no manual UI state anywhere in the stack (D-40)."
    - "The dashboard is generated from typed Go code using the Grafana Foundation SDK; both the generator source and the generated JSON are committed, and a regeneration command reproduces the committed JSON byte-for-byte (D-40)."
    - "No alert rules and no recording rules exist anywhere in the committed configuration — there is no on-call and nowhere to route them (D-40)."
    - "Logs reach Loki over its native OTLP endpoint with structured metadata enabled, not through the deprecated dedicated log exporter, and Prometheus scrapes the Collector's exposition endpoint rather than receiving OTLP directly."
    - "Every dashboard panel query names a metric from the fixed D-35 instrument set this phase defines, so Plan 05's instrument implementation and this dashboard cannot drift apart."
  flagged_assumptions:
    - "RESEARCH A1 [ASSUMED]: `grafana/loki:3.4.2` accepts the planned Collector OTLP HTTP payload and exposes log trace context in a form Grafana's trace-to-log link can query, given only `allow_structured_metadata`. RESEARCH open question 3 is explicit that the exact query or derived-field syntax is unresolved and must be settled by an end-to-end smoke, not by reading docs."
    - "RESEARCH A2 [ASSUMED]: `grafana/grafana:11.5.2` accepts the corrected `tracesToLogsV2` tag-object and `filterByTraceID` provisioning shape exactly as the current Grafana documentation describes. Provisioning can load partially and silently omit links, which is why the correlation click-through is a human-check item rather than a config-file assertion."
    - "RESEARCH A3 [ASSUMED]: `github.com/grafana/grafana-foundation-sdk/go v0.0.18` (public preview) produces dashboard JSON accepted by Grafana 11.5.2. If the generated schema is rejected, that is a finding to record and resolve, not grounds to hand-edit the JSON."
    - "RESEARCH A6 [ASSUMED]: this image-tag combination behaves identically on Windows Docker Desktop and WSL. Bind-mount path handling is the most likely divergence."
    - "The exact Prometheus series names produced from OTel instrument names by the Collector exposition exporter (dot-to-underscore conversion plus a unit suffix) must be read off a live scrape of the exposition endpoint before the dashboard queries are written down, not predicted."
  prohibitions:
    - statement: "Do not hand-edit the generated dashboard JSON; it is an output of the committed typed generator and a regeneration must reproduce it exactly."
      flagged: true
    - statement: "Do not create any Grafana state through the UI — no manually added datasource, dashboard, folder or explore-link may be required for the stack to work from a clean volume."
      flagged: true
    - statement: "Do not add alert rules or recording rules to Prometheus, the Collector, or Grafana."
      flagged: true
    - statement: "Do not introduce the deprecated dedicated Loki exporter; logs reach Loki through its native OTLP endpoint."
      flagged: true
    - statement: "Do not publish any backend container port on an interface other than 127.0.0.1, and do not add authentication-bypassing anonymous access beyond the local-only Grafana admin credentials."
      flagged: true
    - statement: "Do not put PostgreSQL into a profile; a bare compose start must continue to bring it up."
      flagged: true
  artifacts:
    - path: "deploy/prometheus/prometheus.yml"
      provides: "Scrape configuration targeting the Collector exposition endpoint."
    - path: "deploy/loki/loki-config.yaml"
      provides: "Single-binary Loki configuration with structured metadata enabled for native OTLP ingestion."
    - path: "deploy/grafana/provisioning/datasources/datasources.yaml"
      provides: "Jaeger, Prometheus and Loki datasources with stable UIDs plus the trace-to-log correlation configuration."
    - path: "deploy/grafana/provisioning/dashboards/dashboards.yaml"
      provides: "Dashboard provider pointing at the committed dashboard directory."
    - path: "deploy/grafana/dashboard_gen/main.go"
      provides: "Typed Foundation SDK dashboard generator in its own Go module."
    - path: "scripts/generate-dashboards.sh"
      provides: "Regeneration command and determinism check."
  key_links:
    - from: "deploy/collector/otel-collector-config.yaml"
      to: "deploy/loki/loki-config.yaml"
      via: "logs pipeline exports over OTLP HTTP to Loki's native base endpoint, which appends the logs path"
      pattern: "collector_fanout"
    - from: "deploy/prometheus/prometheus.yml"
      to: "deploy/collector/otel-collector-config.yaml"
      via: "Prometheus scrapes the Collector's exposition endpoint on the internal Compose network"
      pattern: "collector_fanout"
    - from: "deploy/grafana/provisioning/datasources/datasources.yaml"
      to: "deploy/grafana/dashboards/lancet-rag-operations.json"
      via: "dashboard panels reference the Prometheus datasource by its stable provisioned UID"
      pattern: "grafana_as_code"
---

<objective>
Complete the telemetry backend as committed code: Prometheus, Loki and Grafana join the `observability` profile, the Collector's logs leg moves from its placeholder to Loki's native OTLP endpoint, Grafana's datasources and trace-to-log correlation are provisioned from files, and the operations dashboard is generated from typed Go rather than hand-edited JSON.

Purpose: SC5 and SC6 of the phase. Plan 01 gave the tracer somewhere to export to; this plan gives a human somewhere to look, with zero manual UI state.
Output: three backend services, four committed configuration files, a typed dashboard generator in its own module, and a committed generated dashboard with a regeneration command.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
@D:/Repos/lancet/.claude/gsd-core/references/planner-preconditions.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-observability-evaluation-polish/06-CONTEXT.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-RESEARCH.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-PATTERNS.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-VALIDATION.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-01-SUMMARY.md
@CLAUDE.md
@go-guidelines.md
@docker-compose.yml
@jaeger-config.yaml
@deploy/collector/otel-collector-config.yaml
</context>

<scope_rationale>
Deployment-only plan, deliberately file-disjoint from Plan 02 so both can run in wave 2. Three tasks split by the thing that can independently be wrong: backend services and their own configs (Task 1), Grafana's correlation wiring (Task 2), and dashboards-as-code (Task 3). The Grafana correlation click-through and the Collector degrade-to-stdout behaviour are the phase's two genuinely manual verifications; per `workflow.human_verify_mode = end-of-phase` they ride as `<human-check>` blocks on the tasks that build them rather than halting execution mid-plan.

Claude's Discretion exercised (per 06-CONTEXT.md `### Claude's Discretion`): Grafana Foundation SDK is chosen over grafonnet, because the repository already has a Go toolchain and no Jsonnet one, so the generator needs no new language runtime. The generator lives in its own module at `deploy/grafana/dashboard_gen` rather than as a gateway subcommand, keeping a public-preview dependency out of the gateway's production module graph — RESEARCH open question 4 required this be an explicit choice.
</scope_rationale>

<tasks>

<task type="auto">
  <name>Prometheus, Loki and Grafana join the observability profile with committed configuration</name>
  <precondition>Docker Engine and Docker Compose are running locally, and Plan 01's `observability` profile with its Collector and Jaeger services is already committed. REVIEW-ADDED toolchain note: every `<automated>` block in this plan runs under Git Bash / `sh`, not PowerShell — `$(pwd)` in the bind-mount arguments resolves to a .NET `PathInfo` object under PowerShell and malforms the `-v` flag, and the polling loops below use `curl`, which is the tool guaranteed by the Docker Desktop / Git-for-Windows environment this repository already assumes. Node.js is NOT assumed to be on `PATH` and no verification command in this plan may depend on it.</precondition>
  <reversibility rating="reversible">Compose services and mounted configuration are committed text with named volumes; removal restores Plan 01's two-service profile.</reversibility>
  <read_first>.planning/phases/06-observability-evaluation-polish/06-CONTEXT.md decisions D-34, D-39 and D-40; 06.2-RESEARCH.md "Supporting Infrastructure" (the four pinned image tags), "Pattern 6: Collector and Backend Provisioning" (including the structured-metadata requirement), the audit rows on the Loki base endpoint and on Compose profiles, "Alternatives Considered" (which explains why the dedicated Loki exporter is out), "Common Pitfalls 6", and "Runtime State Inventory" (which records that no backend volume or UI state exists yet); 06.2-PATTERNS.md sections `docker-compose.yml`, `deploy/loki/loki-config.yaml` and `deploy/prometheus/prometheus.yml`; docker-compose.yml as produced by Plan 01 (the unprofiled `db` service, the profiled `jaeger` and `collector` services and their host port bindings); deploy/collector/otel-collector-config.yaml as produced by Plan 01 (the three pipelines and the placeholder logs exporter)</read_first>
  <files>docker-compose.yml, deploy/collector/otel-collector-config.yaml, deploy/loki/loki-config.yaml, deploy/prometheus/prometheus.yml</files>
  <behavior>
    - `docker compose config` with no profile still resolves PostgreSQL and nothing else.
    - `docker compose --profile observability config` resolves Collector, Jaeger, Prometheus, Loki and Grafana, and no two services declare the same host port.
    - The Collector configuration still validates inside its pinned image after the logs exporter is repointed at Loki.
    - After `docker compose --profile observability up -d`, Prometheus reports the Collector scrape target as up, and Loki's readiness endpoint returns success.
  </behavior>
  <action>Add three services to the `observability` profile in `docker-compose.yml`, all with host ports bound explicitly to `127.0.0.1` and none reachable on any other interface.

Prometheus: image `prom/prometheus:v3.2.1`, container name `lancet-prometheus`, host port 9090, mounting `./deploy/prometheus/prometheus.yml` read-only and a named `prometheus_data` volume, with the config-file flag in its command. Loki: image `grafana/loki:3.4.2`, container name `lancet-loki`, host port 3100, mounting `./deploy/loki/loki-config.yaml` read-only and a named `loki_data` volume, with the config-file flag in its command. Grafana: image `grafana/grafana:11.5.2`, container name `lancet-grafana`, host port 3000, mounting `./deploy/grafana/provisioning` read-only into the Grafana provisioning directory and `./deploy/grafana/dashboards` read-only into the directory the dashboard provider will reference, plus a named `grafana_data` volume, with local-only admin credentials supplied as environment variables and `depends_on` naming Prometheus, Loki and Jaeger. Declare the three new named volumes alongside the existing PostgreSQL volume. Add the Collector's `depends_on` entries for Loki and Prometheus. Do not touch the PostgreSQL service and do not give it a profile.

Write `deploy/prometheus/prometheus.yml` with a global scrape interval, a single scrape job targeting the Collector's exposition endpoint at `collector:8889` on the internal Compose network, and no rule files. Prometheus scrapes the Collector rather than enabling its own OTLP receiver — that is the topology RESEARCH verified against the exporter documentation. No alerting section, no recording rules.

Write `deploy/loki/loki-config.yaml` as a single-binary local configuration with authentication disabled, a filesystem object store, the current TSDB schema, an HTTP listen port matching the published one, and — the load-bearing line — `allow_structured_metadata` set to true under the limits section, which is what makes native OTLP log ingestion carry OTel attributes and trace context. No rule storage, no ruler.

Repoint the Collector's logs pipeline in `deploy/collector/otel-collector-config.yaml` from Plan 01's placeholder exporter to an OTLP HTTP exporter targeting Loki's native OTLP base endpoint on the internal network at `http://loki:3100/otlp` with insecure transport — the Collector appends the logs path itself, so the base endpoint is correct and a fully-qualified logs path is not. Use the canonical Collector 0.159.0 exporter identifiers documented in RESEARCH Pattern 6; the older identifiers listed under RESEARCH's "State of the Art" deprecation notes are aliases and must not be used. Leave the traces and metrics pipelines as Plan 01 built them.

After editing, bring the profile up and confirm the two runtime facts the configuration alone cannot prove: that Prometheus lists the Collector target as up, and that Loki reports ready.</action>
  <acceptance_criteria>
    - `docker compose config --profiles` lists `observability`, and a no-profile `docker compose config` resolves PostgreSQL only.
    - Prometheus, Loki and Grafana all carry `profiles: ["observability"]`; PostgreSQL carries no profile key.
    - Every host port published by any service in this file binds explicitly to `127.0.0.1`.
    - `deploy/loki/loki-config.yaml` sets `allow_structured_metadata` to true and declares no ruler or rule storage.
    - `prometheus_data`, `loki_data` and `grafana_data` are declared as ordinary named local volumes with NO retention or size limit configured anywhere, and that is recorded in the SUMMARY as a deliberate local-demo boundary (Antigravity LOW) rather than an oversight: telemetry storage grows until an operator runs `docker compose down -v`, and D-40 forbids the alert/recording-rule surface that would otherwise drive retention tuning.
    - `deploy/prometheus/prometheus.yml` declares exactly one scrape job pointing at the Collector exposition endpoint and no rule files, alerting section, or recording rules.
    - The Collector logs pipeline exports to Loki's native OTLP base endpoint using a canonical 0.159.0 exporter identifier, and the Collector configuration still validates inside the pinned image.
    - With the profile running, the Prometheus targets API reports the Collector job as up and Loki's readiness endpoint returns success.
  </acceptance_criteria>
  <verify>
    <automated>docker run --rm -v "$(pwd)/deploy/collector:/etc/otelcol:ro" otel/opentelemetry-collector-contrib:0.159.0 validate --config=/etc/otelcol/otel-collector-config.yaml</automated>
    <automated>docker compose --profile observability config</automated>
    <automated>docker compose config --profiles</automated>
    <automated>docker compose --profile observability up -d</automated>
    <automated>curl --fail --silent --show-error --retry 30 --retry-delay 2 --retry-all-errors --retry-connrefused http://127.0.0.1:3100/ready</automated>
    <automated>sh -c 'for i in $(seq 1 30); do curl --fail --silent --retry-connrefused http://127.0.0.1:9090/api/v1/targets | grep -q "\"health\":\"up\"" &amp;&amp; exit 0; sleep 2; done; echo "collector scrape target never reported up"; exit 1'</automated>
    <human-check>
      <test>On the Windows Docker Desktop host this repository is developed on, from a clean state (`docker compose --profile observability down -v` first), run `docker compose --profile observability up -d` from Git Bash and then inspect each of the five containers: `docker compose --profile observability ps`, and for Collector, Prometheus, Loki and Grafana run `docker logs` and check the mounted configuration was actually read (Collector prints its resolved pipelines; Prometheus prints the loaded config path; Grafana prints provisioning file loads).</test>
      <expected>All five services report running, none in a restart loop. Every mounted configuration file is visible inside its container with non-empty content — no empty file and no directory-instead-of-file, which is the classic Windows Docker Desktop bind-mount failure. No container logs a config-parse or file-not-found error for a path under `./deploy/`.</expected>
      <why_human>RESEARCH assumption A6 assumes this image set behaves identically on Windows Docker Desktop and WSL, and names bind-mount path handling as the most likely divergence. A failed bind mount does not fail `docker compose config` and often does not fail `up -d` either — the container starts and silently uses defaults — so no automated check in this plan distinguishes a working mount from an empty one. This is a BLOCKING end-of-phase human verification alongside the Grafana click-through, per the review's "provisioning assumptions need blocking verification" agreed concern.</why_human>
    </human-check>
  </verify>
  <done>The observability profile brings up a Collector fanning traces to Jaeger, metrics to a scraping Prometheus and logs to Loki over native OTLP, all from committed configuration, while a bare compose start still yields PostgreSQL alone.</done>
</task>

<task type="auto">
  <name>Grafana datasources and trace-to-log correlation, provisioned from files only</name>
  <reversibility rating="reversible">Provisioning files are committed text read at container start; deleting them returns Grafana to an empty install.</reversibility>
  <read_first>.planning/phases/06-observability-evaluation-polish/06-CONTEXT.md decisions D-34 and D-40; 06.2-RESEARCH.md "Pattern 7: Grafana Correlation Provisioning" (the corrected tag-object shape and the case-sensitive filter key), the audit row "Candidate Grafana trace/log provisioning is valid" which marks this Corrected and partly unresolved, "Common Pitfalls 7", "Open Questions" item 3 (the unresolved Loki trace-context query shape), and assumptions A1 and A2; 06.2-PATTERNS.md section `deploy/grafana/provisioning/datasources/datasources.yaml`; deploy/loki/loki-config.yaml and docker-compose.yml as produced by Task 1</read_first>
  <files>deploy/grafana/provisioning/datasources/datasources.yaml, deploy/grafana/provisioning/dashboards/dashboards.yaml</files>
  <behavior>
    - After a `docker compose --profile observability up -d` from an empty Grafana volume, Grafana's datasource API lists exactly three datasources with the stable provisioned UIDs and no manual step was performed.
    - Each datasource resolves its backend over the internal Compose network, not a host address.
    - The Jaeger datasource carries the trace-to-log configuration keyed to the Loki datasource UID.
    - A destroyed and recreated Grafana volume reproduces the identical datasource set.
  </behavior>
  <action>Write `deploy/grafana/provisioning/datasources/datasources.yaml` declaring three datasources with explicit, stable UIDs so dashboards and correlation links can reference them by identity rather than by name: a Jaeger datasource pointing at the Jaeger query service on the internal network, a Prometheus datasource pointing at the Prometheus service and marked default, and a Loki datasource pointing at the Loki service. All three use proxy access and internal Compose hostnames, never host addresses.

On the Jaeger datasource, configure trace-to-logs correlation using the current provisioning shape RESEARCH verified against the official documentation: the versioned trace-to-logs key, the Loki datasource UID, tags expressed as key OBJECTS rather than bare strings, and the trace-ID filter key in its exact documented casing. RESEARCH explicitly corrected a candidate that used bare string tags and the wrong casing on that filter key; Grafana silently ignores a misspelled provisioning key rather than failing, which is why the click-through below is a human-check rather than a file assertion.

Because RESEARCH open question 3 leaves the exact form in which Loki exposes native-OTLP log trace context unresolved, configure the Loki datasource with a derived-field mapping to the Jaeger datasource as a fallback path, and treat whichever mechanism actually produces a working link as the finding to record in the SUMMARY. Do not assert in the plan which one works — establish it empirically.

Write `deploy/grafana/provisioning/dashboards/dashboards.yaml` declaring a single file-based dashboard provider that reads the committed dashboard directory mounted in Task 1, with dashboard updates allowed from disk and UI deletion disabled so a dashboard cannot be removed out from under the committed source.

Add no alert rules, no notification policies, no contact points and no recording rules anywhere in the provisioning tree.</action>
  <acceptance_criteria>
    - Three datasources are declared with explicit stable UIDs, proxy access, and internal Compose hostnames only.
    - The Grafana admin credentials set in `docker-compose.yml` (Task 1) are `admin` / `admin`, which is what the automated datasource poll in this task's `<verify>` authenticates with. If Task 1 chooses different credentials, this task's poll must be updated in the same commit — otherwise the poll fails on authentication and reads as a provisioning failure it is not.
    - The Jaeger datasource carries the versioned trace-to-logs configuration referencing the Loki datasource UID, with tags as key objects and the trace-ID filter key in its documented casing.
    - The Loki datasource carries a derived-field mapping to the Jaeger datasource UID as the fallback correlation path.
    - A dashboard provider is declared pointing at the committed dashboard directory, with UI deletion disabled.
    - No alert rules, notification policies, contact points or recording rules exist anywhere under `deploy/grafana/provisioning`.
    - From an empty Grafana volume, the datasource API lists exactly the three provisioned datasources with the expected UIDs, with no manual UI step.
  </acceptance_criteria>
  <verify>
    <automated>docker compose --profile observability up -d</automated>
    <automated>sh -c 'for i in $(seq 1 30); do n=$(curl --fail --silent --retry-connrefused --user admin:admin http://127.0.0.1:3000/api/datasources | grep -o "\"uid\"" | wc -l); [ "$n" = "3" ] &amp;&amp; exit 0; sleep 2; done; echo "Grafana did not provision exactly 3 datasources"; exit 1'</automated>
    <human-check>
      <test>With the observability profile running and both services started against the Collector, issue one real RAG query so a trace, a metric and a log are produced. Open Grafana at http://127.0.0.1:3000, go to Explore, select the Jaeger datasource, open the trace for that query, and click the trace-to-logs link on the root span. Then open the Loki datasource in Explore and click the derived TraceID field on one of the returned log lines.</test>
      <expected>The Jaeger trace shows the gateway root span with the engine node spans nested beneath it. The trace-to-logs link opens Loki filtered to that trace and returns at least one log line. The derived TraceID field on a Loki log line links back to the same Jaeger trace. The Prometheus datasource returns a non-empty series for the query-duration metric over the same window. The same trace ID appears in all three panes.</expected>
      <why_human>Visual correlation across three backends through UI links is not scriptable without a browser, and Grafana silently ignores a misspelled provisioning key rather than erroring — so a file-level assertion cannot distinguish a working link from an ignored one. This is 06.2-VALIDATION.md's first Manual-Only row and RESEARCH open question 3's stated resolution path. It is BLOCKING, not advisory: both reviewers agreed that assumptions A1 and A2 stay unproven until this click-through is observed, and it is the only thing that can fail SC5 — a green datasource-count poll is not evidence of a working link, and this check must not be waived on the strength of one.</why_human>
    </human-check>
  </verify>
  <done>Grafana comes up from a clean volume with three correctly identified datasources and a file-provisioned dashboard folder, with trace-to-log correlation configured in both directions and zero manual UI state.</done>
</task>

<task type="auto">
  <name>Typed dashboard generator and the committed dashboard it reproduces</name>
  <reversibility rating="costly">The generated dashboard's panel queries bind to the D-35 metric names, so renaming an instrument later means regenerating and reviewing the dashboard alongside it.</reversibility>
  <read_first>go-guidelines.md; .planning/phases/06-observability-evaluation-polish/06-CONTEXT.md decisions D-33, D-35 and D-40; 06.2-RESEARCH.md "Supporting Infrastructure" row for the Foundation SDK, "Don't Hand-Roll" (the dashboard JSON row), "Open Questions" item 4 (the module-location question this task settles), the "Recommended Project Structure" note that there is no repository-root Go module, and assumption A3; 06.2-PATTERNS.md sections `deploy/grafana/provisioning/dashboards/lancet-rag-operations.json` and `deploy/grafana/dashboard_gen/go.mod`; gateway/go.mod (module naming and Go version conventions). The authoritative source for the metric names is `06-CONTEXT.md` D-35 plus the ten-name enumeration written out in this task's own action below — do NOT read a later plan's file for it, because this plan runs in wave 2 and Plan 05 runs in wave 4, so a forward reference would invert the dependency direction</read_first>
  <files>deploy/grafana/dashboard_gen/go.mod, deploy/grafana/dashboard_gen/go.sum, deploy/grafana/dashboard_gen/main.go, deploy/grafana/dashboards/lancet-rag-operations.json, scripts/generate-dashboards.sh</files>
  <behavior>
    - Running the generator writes the dashboard JSON; running it a second time produces a byte-identical file, so the committed artifact is reproducible.
    - The generated dashboard declares a stable UID and one panel per D-35 metric family.
    - Every panel query names a metric from the fixed instrument set, so a typo cannot silently produce an empty panel that looks provisioned.
    - Grafana loads the committed dashboard from the provisioned folder with no import step.
  </behavior>
  <action>Read go-guidelines.md first, detecting the Go version convention from gateway/go.mod.

Create `deploy/grafana/dashboard_gen` as its OWN Go module — this repository has no root Go module, so a bare repository-root go run against this directory would not resolve, and putting a public-preview dependency into the gateway's production module graph is the alternative worth avoiding. Declare the module, pin `github.com/grafana/grafana-foundation-sdk/go` at v0.0.18, and commit both the module file and its checksum file.

Write `deploy/grafana/dashboard_gen/main.go` as a generator that builds the dashboard through the SDK's typed builders and marshals it to a file path given on the command line. Set a stable dashboard UID, a browser timezone and a refresh interval. Build one panel per D-35 metric family, each querying the Prometheus datasource by the stable UID provisioned in Task 2. This plan OWNS the instrument-name list; it is fixed here, in wave 2, and Plan 05 implements against it in wave 4. The ten names, derived from D-35's enumeration, are: `lancet.rag.query.duration` (histogram, attribute `outcome`), `lancet.rag.retrieval.path_failures` (counter, attributes `path` and `kind`), `lancet.rag.answer.degraded` (counter, attribute `answer_basis`), `lancet.rag.citation.repairs` (counter, attribute `action`), `lancet.rag.generation.retries` (counter, attribute `outcome`), `lancet.rag.evidence.set_size` (histogram, no attributes), `lancet.ingest.documents` (counter, attribute `outcome`), `lancet.ingest.chunks` (counter, no attributes), `lancet.index.rebuild.duration` (histogram, attribute `outcome`) and `lancet.index.corpus_generation` (gauge, no attributes). One panel per name, ten panels. Record the list in the SUMMARY as the contract Plan 05 must satisfy.

The OTel-to-Prometheus name transformation is performed by the Collector's exposition exporter, not by this generator: instrument dots become underscores and a unit suffix may be appended. Do NOT predict those series names. Bring the observability profile up, send one metric through, scrape the Collector exposition endpoint, and read the actual exported series names off that scrape before writing the panel queries. Record the observed transformation rule in the SUMMARY so Plan 05 and Phase 6.4's documentation inherit it.

Add no alert rules, no thresholds that behave as alerts, and no recording rules to the dashboard.

Write `scripts/generate-dashboards.sh` as the regeneration command AND the determinism gate, because the script's own exit code — not a working-tree diff — is what has to carry the guarantee. On a clean checkout a working-tree diff passes trivially and proves nothing, so the script must do the real comparison itself: regenerate into a temporary path, byte-compare that temporary output against the committed file, and exit non-zero on any difference, printing which file differed. Only after that comparison passes does it write the committed path. Prove the gate has teeth before declaring the task done: perturb one byte of the committed JSON, confirm the script exits non-zero, then restore. That fail-first demonstration is what separates an enforceable determinism check from an aspirational one, and it must be recorded in the SUMMARY.

Commit both the generator source and the generated JSON, as D-40 requires.</action>
  <acceptance_criteria>
    - `deploy/grafana/dashboard_gen` is a self-contained Go module pinning the Foundation SDK at v0.0.18, with its checksum file committed; the gateway module gains no new dependency.
    - `deploy/grafana/dashboards/lancet-rag-operations.json` is committed, declares a stable UID, and contains one panel per D-35 metric family — ten families in total.
    - Every panel query references the Prometheus datasource by the stable UID from Task 2 and names a metric from the fixed D-35 instrument set.
    - Panel queries use the series names actually observed on a live scrape of the Collector exposition endpoint, and the observed transformation rule is recorded in the SUMMARY.
    - `sh scripts/generate-dashboards.sh` performs the temporary-path byte comparison itself and exits non-zero on any difference; the script's exit code, not a working-tree diff, is the determinism gate.
    - The gate is demonstrated fail-first: with one byte of the committed JSON perturbed the script exits non-zero, and the demonstration is recorded in the SUMMARY.
    - The ten instrument names are enumerated in this plan's SUMMARY as a VERBATIM, copy-pasteable list of ten backtick-quoted strings under a heading Plan 05's Task 1 can cite directly — not paraphrased, not embedded in prose — because Plan 05 reads this summary as the authoritative name source and a typo here becomes a wave-4 rewrite (Cursor MEDIUM: "a typo in 03 becomes a Plan 05 '03 wins' rewrite"). Alongside each name, record its attribute keys.
    - The `lancet.rag.query.duration` panel slices by the `outcome` LABEL and does not hardcode any particular outcome value as a series. Plan 05 ships that instrument with exactly two values, `completed` and `failed`; a panel that hardcoded a `degraded` series would render permanently empty.
    - The dashboard contains no alert rules and no recording rules.
    - Grafana loads the dashboard from the provisioned folder with no manual import.
  </acceptance_criteria>
  <verify>
    <automated>cd deploy/grafana/dashboard_gen && go build ./... && go vet ./...</automated>
    <automated>sh scripts/generate-dashboards.sh</automated>
    <automated>git diff --exit-code -- deploy/grafana/dashboards/lancet-rag-operations.json</automated>
    <human-check>
      <test>With the observability profile running, open Grafana at http://127.0.0.1:3000 and navigate to the provisioned Lancet RAG Operations dashboard. Run one ingestion and several RAG queries, including one that degrades a retrieval path, then look at the dashboard over the last fifteen minutes.</test>
      <expected>The dashboard appears without any manual import, all ten panels render, and the panels that correspond to work you just performed show non-empty series — query latency, evidence-set size, ingest counters and the corpus generation gauge in particular. No panel shows a datasource-not-found or unknown-metric error.</expected>
      <why_human>A panel that queries a misspelled metric renders as an empty graph rather than an error, so no automated file check distinguishes a correct dashboard from a plausible one; only looking at it against real data does. This is the dashboard half of 06.2-VALIDATION.md's Manual-Only correlation row.</why_human>
    </human-check>
  </verify>
  <done>A committed typed generator reproduces the committed dashboard byte-for-byte, Grafana provisions it from disk with no import, and every panel queries a metric name that is a phase-level contract with Plan 05.</done>
</task>

</tasks>

## Artifacts this phase produces

**Created by this plan**
- `deploy/loki/loki-config.yaml`, `deploy/prometheus/prometheus.yml`: new backend configuration files.
- `deploy/grafana/provisioning/datasources/datasources.yaml`: datasources with the stable UIDs `jaeger-ds`, `prometheus-ds`, `loki-ds` plus the trace-to-logs and derived-field correlation configuration.
- `deploy/grafana/provisioning/dashboards/dashboards.yaml`: file-based dashboard provider.
- `deploy/grafana/dashboards/lancet-rag-operations.json`: committed generated dashboard with a stable dashboard UID.
- `deploy/grafana/dashboard_gen/go.mod`, `go.sum`, `main.go`: new self-contained Go module and generator binary.
- `scripts/generate-dashboards.sh`: regeneration command with a determinism check.
- New Compose services `prometheus` (`lancet-prometheus`), `loki` (`lancet-loki`), `grafana` (`lancet-grafana`) and named volumes `prometheus_data`, `loki_data`, `grafana_data`.

**Modified by this plan**
- `docker-compose.yml` (three services, three volumes, Collector dependency edges), `deploy/collector/otel-collector-config.yaml` (logs leg repointed at Loki's native OTLP endpoint).

<review_dispositions>
Cross-AI review feedback from `06.2-REVIEWS.md`, resolved into this plan.

| Finding | Source | Disposition | Where it landed |
|---|---|---|---|
| MEDIUM — Node.js runtime dependency in the verification blocks | Antigravity (+ suggestion to replace with a guaranteed tool) | INCORPORATED | Both `node -e` polls replaced with `curl --retry`/`sh` loops; Task 1 precondition states Git Bash as the shell and that Node is not assumed on `PATH` |
| MEDIUM — RESEARCH A1/A2 remain assumed; keep the Loki click-through BLOCKING | Cursor + Agreed Concern | INCORPORATED | Task 2's `<human-check>` `why_human` now states it is blocking, that a green datasource-count poll is not evidence of a working link, and that it must not be waived |
| MEDIUM — Plan 03 commits panel queries before Plan 05's instruments exist; a typo here becomes a Plan 05 rewrite | Cursor (+ suggestion to freeze the ten names as an explicit string list) | INCORPORATED | Task 3 acceptance now requires the ten names in the SUMMARY as a verbatim, copy-pasteable backtick-quoted list with attribute keys, under a heading Plan 05 cites directly |
| LOW — Windows Docker Desktop bind mounts can fail the smoke independently of YAML validity (RESEARCH A6) | Cursor + Agreed Concern | INCORPORATED — this was a real gap: A6 existed only as a flagged assumption with no verification anywhere | New `<human-check>` on Task 1 for the clean-state bind-mount smoke on Windows Docker Desktop, marked blocking; a matching row added to `06.2-VALIDATION.md`'s Manual-Only table |
| LOW — ephemeral volume default; retention boundary should be noted | Antigravity | INCORPORATED | Task 1 acceptance criterion stating no retention or size limit is configured and requiring it in the SUMMARY as a deliberate boundary; `T-06.2-03-04` already accepts the risk |
| Cross-plan — Plan 05 drops the `degraded` outcome from `lancet.rag.query.duration` | derived from Cursor's Plan 05 MEDIUM | INCORPORATED as a guard, no regeneration needed | Task 3 acceptance requires the query-duration panel to slice by the `outcome` label rather than hardcoding a `degraded` series |
| Strengths — `db` unprofiled, `:8888` vs `:8889` distinction, dashboards-as-code, no alert rules, loopback-only | Both | NO CHANGE NEEDED | n/a |
</review_dispositions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host → backend container ports | Grafana, Prometheus, Loki and Jaeger UIs and APIs are reachable from the host. |
| Collector → backend services | Fanout traffic on the internal Compose network. |
| Committed configuration → running stack | Everything the stack does is determined by files in the repository. |
| Log records → Loki storage | Whatever the services logged becomes queryable and persisted in a volume. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.2-03-01 | Information Disclosure | Backend UIs and APIs reachable beyond loopback | high | mitigate | Every published port binds `127.0.0.1` explicitly; backend-to-backend traffic stays on the internal Compose network with no host publication. |
| T-06.2-03-02 | Information Disclosure | Log content persisted in the Loki volume | medium | mitigate | The content prohibition is enforced upstream at every span and log site (Plans 01, 02, 06); this plan adds no new content and the volume is local-only and disposable. |
| T-06.2-03-03 | Repudiation | Manual UI state drifting from committed configuration | medium | mitigate | D-40: all datasources and dashboards are file-provisioned, UI deletion of provisioned dashboards is disabled, and the generator determinism check fails on any hand-edit of the JSON. |
| T-06.2-03-04 | Denial of Service | Unbounded local disk growth from telemetry volumes | low | accept | Local demo topology with named, disposable volumes; retention tuning has no on-call consumer and D-40 forbids the rule surface that would drive it. |
| T-06.2-03-05 | Elevation of Privilege | Grafana default admin credentials | low | accept | Grafana is loopback-only on a local developer machine by the same local-only design that defers auth and TLS under D-06; the credential is a demo affordance, and this is recorded rather than silently assumed. |
| T-06.2-03-06 | Tampering | Deprecated Collector exporter identifiers silently changing behaviour on a future image | medium | mitigate | Canonical 0.159.0 identifiers only, one pinned image tag, and configuration validated inside that exact image as a gate. |
</threat_model>

<verification>
- The Collector configuration validates inside the pinned image.
- `docker compose config --profiles` lists `observability`; a no-profile config resolves PostgreSQL only.
- With the profile up, Prometheus reports the Collector scrape target up, Loki reports ready, and Grafana's datasource API lists the three provisioned UIDs.
- `sh scripts/generate-dashboards.sh` passes its determinism check and leaves the committed dashboard unchanged.
- `cd deploy/grafana/dashboard_gen && go build ./... && go vet ./...` exits 0.
</verification>

<success_criteria>
- One Collector fans traces, metrics and logs out to Jaeger, Prometheus and Loki from committed pipelines.
- Grafana provisions three correlated datasources and one generated dashboard from files, with no manual UI state.
- The dashboard is reproducible from typed Go source and both source and output are committed.
- No alert or recording rules exist anywhere in the stack.
- A bare compose start is still PostgreSQL only.
</success_criteria>

<output>
Create `.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-03-SUMMARY.md`, recording: which correlation mechanism actually produced a working Jaeger-to-Loki link (the versioned trace-to-logs configuration, the Loki derived field, or both), the observed OTel-to-Prometheus series-name transformation rule read off a live scrape, the ten instrument names and their attribute keys as a VERBATIM backtick-quoted list under a dedicated heading (this is the contract Plan 05 implements against and it must be copy-pasteable, not paraphrased), the fail-first demonstration of the determinism gate, the deliberate absence of any retention or size limit on the three named volumes, and the resolution of assumptions A1, A2, A3 and A6.
</output>
