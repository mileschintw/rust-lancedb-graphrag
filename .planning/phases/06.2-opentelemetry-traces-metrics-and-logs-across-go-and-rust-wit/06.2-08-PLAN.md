---
phase: 06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit
plan: 08
type: execute
wave: 6
depends_on:
  - 06.2-02
files_modified:
  - engine/src/service.rs
  - engine/src/workflow/nodes/generate.rs
  - engine/src/generation/mod.rs
  - engine/src/generation/openrouter.rs
  - engine/src/tests/telemetry_query.rs
  - scripts/engine-test-targets.sh
autonomous: true
gap_closure: true
requirements:
  - OBS-01
estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The `embedding_request` span's `gen_ai.request.model` equals `ProductionEmbeddingPort.embedder.model_id()` at record time, matching the ingest sibling at engine/src/ingest.rs entity_name_embedding (D-42)."
    - "Both `llm_attempt` spans (attempt 1 and retry attempt 2) set `gen_ai.request.model` from `Generator::model_id()` on the node’s generator, not from a compile-time model string (D-42)."
    - "`OpenRouterGenerator` reports `OpenRouterGenerationConfig.model` through `Generator::model_id()` so production generation traces follow the configured generation model (D-43 attribution)."
    - statement: "OBS-01 unclassified edge remains unresolved by the probe; this plan only closes CR-03 model-attribute accuracy."
      verification: backstop
  flagged_assumptions:
    - "OBS-01 edge-probe returned category unclassified, status unresolved. Not auto-resolved. CR-03 is the concrete OBS-01 slice here."
    - "Claude's Discretion: prefer `Generator::model_id()` (default method plus OpenRouter override) over stuffing generation_model onto `GenerateAnswerNode::with_settings`, so every generator carries its own id the way `EmbeddingProvider::model_id` already does."
  prohibitions:
    - statement: "Must not hardcode provider model identifiers onto gen_ai.request.model at the embedding_request or llm_attempt span sites."
      flagged: true
    - statement: "Must not leave llm_attempt model attributes pointing at a model that is not the Generator in use."
      flagged: true
  artifacts:
    - path: "engine/src/generation/mod.rs"
      provides: "Generator::model_id() default plus required override on OpenRouterGenerator."
    - path: "engine/src/service.rs"
      provides: "embedding_request span bound to embedder.model_id()."
    - path: "engine/src/workflow/nodes/generate.rs"
      provides: "llm_attempt spans bound to generator.model_id()."
  key_links:
    - from: "engine/src/workflow/nodes/generate.rs"
      to: "engine/src/generation/openrouter.rs"
      via: "Generator::model_id() reads OpenRouterGenerationConfig.model"
      pattern: "semantic_conventions"
---

## Phase User Story

**As a** developer reading Jaeger, **I want to** see the configured embedding and generation model on `gen_ai.request.model`, **so that** traces attribute cost and latency to the model that actually ran (D-42, D-43).

<objective>
Close CR-03: replace hardcoded `gen_ai.request.model` values on `ProductionEmbeddingPort::embed_variant_zero` and both `GenerateAnswerNode` `llm_attempt` spans with the live configured model accessors.

Purpose: production-grade traces cannot report a model the process is not using.
Output: accessor-backed span attributes plus tests that fail if a default-looking literal is wired instead of the test double's model_id.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-observability-evaluation-polish/06-CONTEXT.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-VERIFICATION.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-REVIEW.md
@rust-guidelines.md
@engine/src/service.rs
@engine/src/ingest.rs
@engine/src/workflow/nodes/generate.rs
@engine/src/generation/mod.rs
@engine/src/generation/openrouter.rs
@engine/src/tests/telemetry_query.rs
</context>

<scope_rationale>
Two call sites, one convention (D-42). Trait default keeps FakeGenerator and workflow_phase5 doubles compiling; OpenRouter and the telemetry test doubles override. Do not retouch leaf span topology from Plan 02.
</scope_rationale>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end embedding_request model attribute follows embedder.model_id()</name>
  <reversibility rating="reversible">Span field source swap; ingest.rs already uses embedder.model_id().</reversibility>
  <read_first>rust-guidelines.md; 06-CONTEXT.md D-42 D-43; 06.2-REVIEW.md CR-03; engine/src/ingest.rs EmbeddingProvider::model_id default and OpenRouterClient impl (~1415-1428) and entity_name_embedding span (~1143); engine/src/service.rs ProductionEmbeddingPort::embed_variant_zero info_span (~429-432); engine/src/tests/telemetry_query.rs TestEmbeddingProvider (~65-76) and leaf_span_embedding_request_wraps_real_http_call (~584-613); engine/src/client/mod.rs OpenRouterClient::model_id</read_first>
  <files>engine/src/service.rs, engine/src/tests/telemetry_query.rs, scripts/engine-test-targets.sh</files>
  <behavior>
    - `TestEmbeddingProvider` overrides `model_id()` to the distinctive string `test-embed-model-override` (not the EmbeddingProvider default).
    - `leaf_span_embedding_request_wraps_real_http_call` asserts the exported `embedding_request` span attribute `gen_ai.request.model` equals `test-embed-model-override` exactly — presence-only is insufficient.
  </behavior>
  <action>Read rust-guidelines.md before editing Rust. In ProductionEmbeddingPort::embed_variant_zero, set the embedding_request span's gen_ai.request.model from tracing's display of self.embedder.model_id(), the same pattern as ingest.rs entity_name_embedding. Override TestEmbeddingProvider::model_id to return test-embed-model-override. Tighten leaf_span_embedding_request_wraps_real_http_call to compare the attribute string to that override. If engine-test-targets counts move, raise pins in the same commit as later tasks; if only this test body changes, counts stay.</action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib telemetry_query::leaf_span_embedding_request_wraps_real_http_call -- --exact</automated>
  </verify>
  <acceptance_criteria>
    - ProductionEmbeddingPort embedding_request span uses embedder.model_id() (D-42).
    - leaf_span_embedding_request_wraps_real_http_call fails if the span still records the EmbeddingProvider default instead of test-embed-model-override.
    - ingest.rs entity_name_embedding site remains %embedder.model_id() (no regression).
  </acceptance_criteria>
  <done>Query-path embedding spans report the embedder in use (CR-03 embedding half).</done>
</task>

<task type="auto" tdd="true">
  <name>Thread Generator::model_id into both llm_attempt spans</name>
  <reversibility rating="costly">Adds a method on the Generator trait consumed by production and many test doubles; default body keeps existing impls compiling, but OpenRouter and telemetry doubles must override.</reversibility>
  <read_first>rust-guidelines.md; engine/src/generation/mod.rs Generator trait (~562-577) and FakeGenerator; engine/src/generation/openrouter.rs OpenRouterGenerator and OpenRouterGenerationConfig.model (private field, same module); engine/src/workflow/nodes/generate.rs llm_attempt spans (~117-126 and ~175-178); engine/src/tests/telemetry_query.rs RetryableGenerator, SingleSuccessGenerator, llm_attempt_spans_are_two_siblings_on_retry, llm_attempt_span_is_single_without_retry; scripts/engine-test-targets.sh</read_first>
  <files>engine/src/generation/mod.rs, engine/src/generation/openrouter.rs, engine/src/workflow/nodes/generate.rs, engine/src/tests/telemetry_query.rs, scripts/engine-test-targets.sh</files>
  <behavior>
    - `Generator::model_id(&self) -> &str` exists with a default of `unspecified` so existing test generators compile without edits unless they participate in model-attribute assertions.
    - `OpenRouterGenerator` implements `model_id` returning `self.config.model` (the configured generation model).
    - `RetryableGenerator` and `SingleSuccessGenerator` override `model_id` to `test-gen-model-override`.
    - `llm_attempt_spans_are_two_siblings_on_retry` asserts both exported llm_attempt spans have gen_ai.request.model equal to test-gen-model-override.
    - `llm_attempt_span_is_single_without_retry` asserts the single llm_attempt span has the same value.
  </behavior>
  <action>Read rust-guidelines.md. Add Generator::model_id with default unspecified. Implement it on OpenRouterGenerator from config.model (add a inherent method if useful for tests). In GenerateAnswerNode, after the generator binding, pass generator.model_id() into both llm_attempt info_span! fields via tracing display. Override RetryableGenerator and SingleSuccessGenerator. Extend the two llm_attempt tests to assert attribute equality, not mere presence. Grep generate.rs llm_attempt regions to ensure they do not contain google/ or nvidia/ model path literals. Raise scripts/engine-test-targets.sh TOTAL/LIB_COUNT/LIB_BIN_SUM if --list grows; never lower.</action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib telemetry_query::llm_attempt_spans_are_two_siblings_on_retry telemetry_query::llm_attempt_span_is_single_without_retry -- --exact; sh scripts/engine-test-targets.sh</automated>
  </verify>
  <acceptance_criteria>
    - Both llm_attempt info_span sites in generate.rs use generator.model_id() (D-42).
    - OpenRouterGenerator::model_id returns the config model string.
    - The two named llm_attempt tests require test-gen-model-override on every llm_attempt span.
    - Region check: `awk '/llm_attempt/,/;/' engine/src/workflow/nodes/generate.rs` does not contain google/ or nvidia/ model path literals.
    - engine-test-targets.sh matches measured counts after any new tests.
  </acceptance_criteria>
  <done>Generation traces report the Generator's configured model on every LLM attempt (CR-03 generation half).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Configured model → exported spans | Operators and backends trust gen_ai.request.model for attribution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.2-08-01 | Information Disclosure | wrong model on spans | medium | mitigate | Bind attributes to embedder/generator accessors; discriminating tests use override ids that are not production defaults. |
| T-06.2-08-02 | Repudiation | traces attributing the wrong model | medium | mitigate | Same accessor binding; OpenRouter reads config.model. |
| T-06.2-08-SC | Tampering | package installs | high | accept | No new crates. |
</threat_model>

<verification>
- Named telemetry_query tests above plus `sh scripts/engine-test-targets.sh`.
- Do not re-instrument SC2 hierarchy tests except as they already run in the locked suite if the executor runs the full lib target.
</verification>

<success_criteria>
- embedding_request and both llm_attempt spans carry the in-use model id (CR-03, D-42).
</success_criteria>

## Artifacts this phase produces

- New trait method: `Generator::model_id`
- No new files; tests extended in `engine/src/tests/telemetry_query.rs`

<output>
Create `.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-08-SUMMARY.md` recording the Generator::model_id default string and the two test override literals.
</output>
