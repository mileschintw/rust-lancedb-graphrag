---
phase: 06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit
plan: 04
type: execute
wave: 3
depends_on:
  - 06.2-01
  - 06.2-02
files_modified:
  - engine/src/ingest.rs
  - engine/src/service.rs
  - engine/src/main.rs
  - engine/src/tests.rs
  - engine/src/tests/telemetry_ingest.rs
  - scripts/engine-test-targets.sh
autonomous: true
requirements:
  - OBS-01
estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Ingestion is traced end to end as one trace per document: upload at the gateway, admission at the engine RPC, chunking, embedding, staging write, and graph extraction all appear under a single document-scoped span, with the existing `chunk_document`, `embed_document`, `persist_document` and `index_document` spans preserved and nested rather than replaced (D-30)."
    - "The admission trace context survives the ingestion queue: the job carried onto the bounded Tokio channel carries its W3C trace parent, and the worker re-installs it so the background document trace is a continuation of the upload rather than an orphan root (D-30)."
    - "Graph extraction is inside the document trace. Today `extract_and_persist_entities` runs after the existing `index_document` span has already closed, so a document-scoped span wraps the whole worker branch and the extraction gets its own `graph_extraction` child (D-30)."
    - "REVIEW-DRIVEN nesting mechanism: `index_document` is a CHILD of `ingest_document`, provable as `index_document.parent_span_id == ingest_document.span_id`. `tracing` binds a span's parent at CONSTRUCTION, not at await, so it is not enough to instrument the awaited future — the `tracing::info_span!(\"index_document\", …)` statement itself (currently `engine/src/ingest.rs:1676-1680`) must execute while `ingest_document` is the current span. Any restructure that leaves that construction outside the instrumented block produces two sibling roots and silently fails this must_have."
    - "Phase 6.1's rebuild-and-swap is observable: every rebuild attempt produces an `index_rebuild` span recording the corpus generation before and after, the number of coalesced triggering documents, and whether the outcome degraded — making the debounce behaviour and the degraded path visible instead of inferred (D-30, and the D-20 through D-25 machinery it observes)."
    - "Because the debounce deliberately coalesces a burst of documents into one rebuild, the rebuild span is not parented to a single document. It carries span LINKS to the trace contexts of the documents that triggered it, which is the correct OTel shape for a many-to-one causal relationship."
    - "Instrumentation is additive: the worker's shutdown arm, the status map transitions, the rollback branch on indexing failure, the rebuild notification, and the debounce coalescing behaviour are all unchanged, proven by the existing ingestion and rebuild tests staying green."
  flagged_assumptions:
    - "`IngestionJob` gains one optional field. Grep confirms four struct-literal construction sites (`engine/src/ingest.rs` in `read_staged_jobs`, `engine/src/service.rs` in the ingest RPC, and two in `engine/src/tests.rs`) plus 55 `IngestionJob::new` call sites which keep their signature. If the executor finds a fifth literal site, that is a finding to record, not a reason to change approach."
    - "Jobs recovered from staged rows at startup by `read_staged_jobs` have no live admission context and therefore no trace parent. Their document trace is a legitimate new root, not a bug; the test suite must assert that explicitly so a future reader does not treat it as a propagation failure."
    - "Span links are attached at span-construction time in the Rust SDK, so the trigger contexts must be drained before the rebuild span is created. That ordering constraint is what shapes the trigger-buffer design and is worth stating in the SUMMARY."
    - "REVIEW-CORRECTION to a review finding, recorded so a future reader does not act on the wrong version. Cursor's LOW claimed `engine/src/tests/telemetry_ingest.rs` cannot see helpers in `engine/src/tests.rs`. That is half wrong: `lib.rs` declares both as `#[cfg(test)]` modules of the SAME crate, so `pub(crate)` items ARE visible — verified for `database_path` (`engine/src/tests.rs:928`) and `configured_service` (`:1158`), both `pub(crate)`. What is genuinely unreachable is any item with no visibility modifier. The rule for this plan: build ingest telemetry tests only on `pub`/`pub(crate)` items from `engine::tests` and the public `engine::ingest` surface; if a needed helper turns out to be private, widen it to `pub(crate)` in `engine/src/tests.rs` (already in this plan's file list) rather than duplicating it."
    - "REVIEW-DRIVEN scope note: threading the trigger-link buffer changes three function signatures and therefore touches `engine/src/main.rs`, which the prior version of this plan omitted. The affected production call sites are `engine/src/main.rs:88` (`spawn_worker`) and `:99` (`spawn_rebuild_debounce_task`). `rebuild_and_swap`'s signature is deliberately NOT changed, which keeps its six existing test call sites (`engine/src/tests.rs:7918`, `:8007`, `:8044`, `:8084`, `:8130`, `:8284`) untouched."
  prohibitions:
    - statement: "Do not remove or rename the existing `index_document`, `chunk_document`, `embed_document` or `persist_document` spans; new spans wrap them."
      flagged: true
    - statement: "Do not parent the rebuild span to one triggering document; a coalesced rebuild has many causes and uses links."
      flagged: true
    - statement: "Do not put document filename, raw document bytes, chunk text or extracted entity text into any span attribute; counts, identifiers and byte sizes only."
      flagged: true
    - statement: "Do not change the BEHAVIOUR of the ingestion queue capacity, the worker shutdown arm, the rollback branch, or the debounce coalescing semantics while adding spans. REVIEW-CLARIFIED: this bars behavioural change, NOT the lexical relocation the nesting fix requires — moving the existing `index_document` construction, the match arms and the rollback branch verbatim into an `async` block that is `.instrument(ingest_span)`-ed is expected and required, and an executor must not refuse the restructure on the strength of this prohibition. Every statement inside must stay byte-identical apart from indentation."
      flagged: true
    - statement: "Do not make the trace-parent field required on `IngestionJob`; jobs recovered from staged rows legitimately have none."
      flagged: true
  artifacts:
    - path: "engine/src/ingest.rs"
      provides: "IngestionJob trace-parent field and builder, the document-scoped ingest span wrapping the worker branch, the graph_extraction span, the RebuildTriggerLinks buffer, and the index_rebuild span with generation attributes and links."
    - path: "engine/src/service.rs"
      provides: "ingest_admission span on the ingest RPC and injection of the active trace parent into the job placed on the queue."
  key_links:
    - from: "engine/src/service.rs"
      to: "engine/src/ingest.rs"
      via: "the admission span's W3C trace parent rides on IngestionJob across the bounded Tokio channel and is re-installed as the worker document span's parent"
      pattern: "queue_context_carry"
    - from: "engine/src/ingest.rs"
      to: "engine/src/ingest.rs"
      via: "documents that notify the rebuild watch push their span context into the trigger buffer, which the debounce task drains into links on the index_rebuild span"
      pattern: "rebuild_trigger_links"
---

<objective>
Extend the proven span mechanism to the ingestion path: one trace per document from upload through graph extraction, carried across the ingestion queue, plus an observable `index_rebuild` span linked to the documents that triggered it.

Purpose: SC3 of the phase, and the thing that makes Phase 6.1's debounced rebuild-and-swap visible. Today the two most interesting ingestion behaviours — that graph extraction happens after the document span has already closed, and that a burst of documents coalesces into one rebuild — are invisible.
Output: a document-scoped ingest span with the existing stage spans nested inside it, a graph-extraction span, queue-crossing trace context, and a linked rebuild span.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
@D:/Repos/lancet/.claude/gsd-core/references/planner-reversibility.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-observability-evaluation-polish/06-CONTEXT.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-RESEARCH.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-PATTERNS.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-VALIDATION.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-01-SUMMARY.md
@.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-02-SUMMARY.md
@CLAUDE.md
@rust-guidelines.md
@engine/src/ingest.rs
@engine/src/service.rs
</context>

<scope_rationale>
Two tasks split at the only real seam in this work: the synchronous document path (admission through graph extraction, one trace, context carried across a channel) and the asynchronous coalesced rebuild (a many-to-one relationship that needs links rather than parenting). Wave 3 because it edits `engine/src/service.rs`, which Plan 02 owns in wave 2, and `engine/src/ingest.rs`, which Plan 05 will extend in wave 4.
</scope_rationale>

<tasks>

<task type="auto" tdd="true">
  <name>One trace per ingested document, carried across the ingestion queue</name>
  <reversibility rating="costly">`IngestionJob` gains a field, so the four struct-literal construction sites and the queue contract move together; undo is a coordinated revert rather than a local one.</reversibility>
  <read_first>rust-guidelines.md; .planning/phases/06-observability-evaluation-polish/06-CONTEXT.md decisions D-30 and D-42; 06.2-RESEARCH.md "Pattern 4: Ingestion Stage Spans and Rebuild Context" (which states the exact problem: the existing `index_document` span ends when `process_job_with_boundary` returns, so graph extraction happens after it closes) and "Common Pitfalls 4"; 06.2-PATTERNS.md section `engine/src/ingest.rs`; engine/src/ingest.rs:122-140 (`IngestionJob` fields and the `new` constructor), 288-400 (`read_staged_jobs` and its struct-literal construction around line 377), 850-903 (`process_job_with_boundary` and the three existing stage spans at 856, 869 and 875), 904 (`extract_and_persist_entities`), 1606-1626 (`spawn_worker`), 1627-1666 (`spawn_worker_with_boundary` and its biased shutdown select), 1667-1760 (the worker branch: status insert at 1667, the `index_document` span at 1676-1680, the instrumented `process_job_with_boundary` call at 1681-1687, the graph-extraction call at 1689-1715 which sits OUTSIDE that span, the completed status insert at 1716, the rebuild notification at 1724, and the failure/rollback branch from 1727); engine/src/service.rs:640-700 (the ingest RPC, its stream handling and the `IngestionJob` struct literal at 683); engine/src/tests.rs:4402 and the second `IngestionJob` struct-literal site; engine/src/telemetry/propagation.rs as produced by Plan 01</read_first>
  <files>engine/src/ingest.rs, engine/src/service.rs, engine/src/tests.rs, engine/src/tests/telemetry_ingest.rs</files>
  <behavior>
    - `ingest_span_hierarchy_covers_document_stages`: processing one job through the worker exports, in one trace, a document-scoped span with `index_document` nested inside it, and `chunk_document`, `embed_document` and `persist_document` nested inside that — the three existing span names unchanged. The nesting assertion is on parent span IDs, not on span presence: `index_document.parent_span_id == ingest_document.span_id` and `chunk_document.parent_span_id == index_document.span_id`. A presence-only assertion passes against the sibling-root bug this task exists to fix.
    - `ingest_graph_extraction_is_inside_the_document_trace`: the `graph_extraction` span is a child of the document-scoped span and shares its trace ID, which is not true today because extraction runs after `index_document` closes.
    - `ingest_admission_context_crosses_the_queue`: a job admitted with a live admission span and processed by the worker produces a document span whose trace ID equals the admission span's trace ID.
    - `ingest_recovered_job_starts_a_new_root`: a job produced by `read_staged_jobs` with no trace parent produces a document span that is a valid new root, explicitly asserted so a future reader does not read it as a propagation failure.
    - `ingest_failure_branch_records_error_status`: a job whose indexing fails leaves the document span with an Error status and the existing rollback branch behaviour unchanged.
    - `ingest_spans_carry_no_document_content`: no exported ingestion span attribute contains the filename, raw bytes, chunk text or extracted entity text.
  </behavior>
  <action>Read rust-guidelines.md before editing.

Add `pub trace_parent: Option<String>` to `IngestionJob` at `engine/src/ingest.rs:122-128`, holding a serialized W3C trace parent rather than an OTel context object, so the type stays plain data that is cheap to clone, compare and debug across the channel. Keep `IngestionJob::new` at line 131 defaulting the field to `None` so all 55 call sites of that constructor compile unchanged, and add a small consuming builder that sets it. Update the four struct-literal construction sites grep identifies — `read_staged_jobs` around line 377, the ingest RPC at `engine/src/service.rs:683`, and the two sites in `engine/src/tests.rs` starting at 4402 — to supply `None` explicitly, except the RPC site which supplies the live value below.

In `engine/src/service.rs`, wrap the `ingest_document` RPC handler body in a span named `ingest_admission`, parented to the context extracted from the request metadata by Plan 01's `extract_parent_context`. REVIEW-DRIVEN ordering (Cursor MEDIUM): the extraction call must be the line immediately BEFORE `let mut stream = request.into_inner();` at line 643, because that call consumes the request wrapper — exactly the constraint `query_rag` already satisfies. The span therefore opens before the streaming body is read, so upload time is inside it rather than stranded on the gateway span. That means `lancet.document.id` and `lancet.document.bytes` are NOT known at construction: declare both as deferred `tracing::field::Empty` fields and `.record()` them after the read loop terminates — the id after the first frame sets `document_id`, the byte size after the loop from `raw.len()`. Do NOT record `filename`; it is a field on the job (`service.rs:684`) and is the easiest content leak on this path. Immediately before `permit.send(job)` at line 692, serialize the currently active OTel context into a W3C trace parent using the globally registered propagator and set it on the job's new `trace_parent` field. Do not hand-format the header value; the propagator's injector owns that format.

In `engine/src/ingest.rs`, restructure the worker branch so one span covers the whole document. This is the review's HIGH finding and the fix is a lexical relocation, not a change of instrumentation range — `tracing` binds a span's parent at CONSTRUCTION, so instrumenting the awaited future is not sufficient. Do it in three concrete steps:

Step one: immediately after the "processing" `statuses.insert(...)` that ends at line 1674 and after `let document_id = job.document_id.clone();`, construct `let ingest_span = tracing::info_span!("ingest_document", …)` carrying `lancet.document.id` and `lancet.document.bytes` (the byte size is available as `job.raw_data.len()`). When `job.trace_parent` is present, reconstruct the context with the propagator's extractor and install it as that span's parent with `set_parent`; otherwise let it start a local root.

Step two: move the ENTIRE remainder of the loop body — starting at the existing `let span = tracing::info_span!("index_document", …)` statement at line 1676 and running through the closing brace of the `Err(error)` arm at the end of the match (~line 1760) — verbatim into a single `async move { … }` block, and await it as `.instrument(ingest_span).await`. Nothing inside changes except indentation: the `index_document` span construction, its `.instrument(span)` on the `process_job_with_boundary` future, the graph-extraction call at 1689, the completed status insert at 1716, the rebuild notification at 1724 and the whole rollback branch at 1727 all stay byte-identical. Because the `index_document` construction now executes while `ingest_document` is current, `index_document` becomes its child. If instead you leave that construction outside the block — the reading the prior version of this plan invited — `index_document` stays a sibling root and this task's central must_have silently fails while every test that only checks span PRESENCE still passes.

Step three: leave the three stage spans inside `process_job_with_boundary` at lines 856, 869 and 875 completely untouched; they nest automatically once `index_document` is nested.

Attribute note (Cursor MEDIUM): `index_document` already carries `bytes = job.raw_data.len()` at line 1679. Keep that line as it is and add `lancet.document.bytes` on `ingest_document` — the duplication is deliberate (one is the pre-existing field name, one is the phase's `lancet.`-prefixed convention), and neither may be replaced by `filename` or `raw_data`.

Add a span named `graph_extraction` around the `extract_and_persist_entities` call at line 1689, carrying `lancet.document.id` and, recorded after the call, `lancet.graph.written_entity_edges` and `lancet.graph.prior_entity_edges` from the returned summary. On the error branch at 1708, set an Error span status while leaving the existing error log and the prior-graph-preserved behaviour unchanged.

On the failure branch at 1727, record an Error status on the `ingest_document` span. Do not modify the rollback logic, the staged-document delete, or the failure status insert.

Do not touch the worker's biased shutdown select at 1640-1660, the queue capacity, or the rebuild notification at 1724 — Task 2 adds to the latter, this task does not.

Attribute discipline: document identifier, byte size and counts only. No filename, no raw bytes, no chunk text, no entity text.

Write the six named tests in `engine/src/tests/telemetry_ingest.rs`, the module Plan 01 pre-declared, reusing the existing worker and temporary-store fixtures in `engine/src/tests.rs`.</action>
  <acceptance_criteria>
    - `IngestionJob` carries `trace_parent: Option<String>`; `IngestionJob::new` keeps its existing signature and defaults it to `None`; all four struct-literal sites compile.
    - The ingest RPC creates an `ingest_admission` span parented to the extracted request context and injects the active trace parent onto the job before it is queued, using the propagator's injector and no hand-written header formatting.
    - The `ingest_document` span wraps the entire worker branch from the `index_document` span CONSTRUCTION onward, including graph extraction, the completed status insert, the rebuild notification and the failure branch. Source check: the `tracing::info_span!("index_document", …)` statement is lexically inside the `async` block that carries `.instrument(ingest_span)`, not before it.
    - The existing `index_document`, `chunk_document`, `embed_document` and `persist_document` spans exist with unchanged names and nest under `ingest_document`, asserted by parent span ID equality rather than by span presence.
    - The `ingest_admission` span's `extract_parent_context` call is on the line strictly preceding `request.into_inner()` at `engine/src/service.rs:643`; `lancet.document.id` and `lancet.document.bytes` are declared `Empty` and recorded after the stream read loop; no span on this path records `filename`.
    - A `graph_extraction` span exists around `extract_and_persist_entities`, records the two edge counts, and sets an Error status on the extraction-failure branch without changing that branch's behaviour.
    - No ingestion span attribute carries filename, raw bytes, chunk text or entity text.
    - All six named tests pass, including the explicit assertion that a recovered staged job legitimately starts a new root.
    - The pre-existing ingestion, rollback and shutdown tests all still pass.
    - `cargo clippy --manifest-path engine/Cargo.toml -- -D warnings` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- ingest_span_hierarchy_covers_document_stages</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- ingest_graph_extraction_is_inside_the_document_trace</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- ingest_admission_context_crosses_the_queue</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- ingest_recovered_job_starts_a_new_root</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- ingest_spans_carry_no_document_content</automated>
    <automated>cargo clippy --manifest-path engine/Cargo.toml -- -D warnings</automated>
  </verify>
  <done>An uploaded document produces one trace spanning admission, chunking, embedding, staging write and graph extraction, continuous across the ingestion queue, with every pre-existing span preserved and no document content leaked into attributes.</done>
</task>

<task type="auto" tdd="true">
  <name>Observable index rebuild with links to the documents that triggered it</name>
  <reversibility rating="reversible">A trigger buffer and one span inside `rebuild_and_swap`; removal restores the current silent rebuild.</reversibility>
  <read_first>rust-guidelines.md; .planning/phases/06-observability-evaluation-polish/06-CONTEXT.md decisions D-30 (which names index rebuild as the final ingestion stage), D-23 and D-25 (the debounce and degrade behaviour being made observable), and D-42; 06.2-RESEARCH.md "Pattern 4" (specifically its statement that the rebuild is an independently spawned task needing an explicit context or link strategy rather than an assumed-active parent span); 06.2-PATTERNS.md section `engine/src/ingest.rs` rebuild-span subsection; engine/src/ingest.rs:1441-1547 (`rebuild_and_swap`: the rebuild counter, the cfg(test) fault hook, the nodes-table checkout, the BM25 build off the lock and the snapshot swap), 1548-1605 (`spawn_rebuild_debounce_task`, its coalescing select loop, and specifically the `let _ = rebuild_and_swap(&database, &corpus_store, bm25_settings.clone()).await;` at line 1601), 1606-1626 (`spawn_worker`, the thin wrapper that delegates to `spawn_worker_with_boundary` at 1615), 1724 (the worker's rebuild notification); engine/src/main.rs:84-105 (the `watch::channel(0u64)` construction at 85, the `spawn_worker(...)` call at 88 and the `spawn_rebuild_debounce_task(...)` call at 99 — both take a new parameter in this task); engine/src/workflow/ports.rs (`CorpusSnapshot` generation, nodes_version and rebuild_degraded fields); .planning/phases/06.1-index-rebuild-and-swap-bu-deterministic-proofs-cr-04-cr-05-d/06.1-01-PLAN.md (the debounce and degrade contract this span observes); scripts/engine-test-targets.sh:32-88</read_first>
  <files>engine/src/ingest.rs, engine/src/main.rs, engine/src/tests.rs, engine/src/tests/telemetry_ingest.rs, scripts/engine-test-targets.sh</files>
  <behavior>
    - `index_rebuild_span_records_generation_transition`: one successful rebuild exports one `index_rebuild` span carrying the corpus generation before and after and the numeric nodes version after, with an Ok status.
    - `index_rebuild_span_records_degraded_outcome`: a rebuild driven to fail through the existing test fault hook exports an `index_rebuild` span with an Error status and an attribute recording that the previous generation was retained.
    - `index_rebuild_span_links_all_triggering_documents`: five documents ingested inside one debounce window produce exactly one `index_rebuild` span carrying five links, one per triggering document trace, and a trigger-count attribute of five.
    - `index_rebuild_span_is_not_parented_to_a_document`: the rebuild span is a root span rather than a child of any single document span.
    - `index_rebuild_trigger_buffer_is_drained_per_rebuild`: a second rebuild after a further single ingestion carries exactly one link, proving the buffer does not accumulate across rebuilds.
    - The existing debounce coalescing, degrade-and-clear, and shutdown-termination tests remain green.
  </behavior>
  <action>Read rust-guidelines.md before editing.

Add a trigger-link buffer type `RebuildTriggerLinks` to `engine/src/ingest.rs`: a cloneable handle over `Arc<std::sync::Mutex<Vec<opentelemetry::trace::SpanContext>>>` with `Default`, a `push_current()` that reads the currently active span's OTel `SpanContext` and appends it, and a `drain()` that empties the buffer and returns its contents plus the overflow count. Use a plain `std::sync::Mutex`, never an async mutex, and never hold the guard across an `.await` — every critical section is one `push` or one `drain` — so a slow debounce drain can never block the ingestion worker (Antigravity's LOW). Cap the buffer at 1024 entries: once full, `push_current` drops the incoming context and increments an overflow counter, which `drain` returns and the span records as `lancet.index.rebuild.dropped_links`. That cap and that attribute name are what `T-06.2-04-04`'s mitigation promises, so they are stated here and not left implicit.

Because the rebuild watch channel carries only a counter, this buffer is the mechanism that carries causality; do not change the watch channel's payload type, which has thirteen construction sites established by Phase 6.1.

Threading — REVIEW-DRIVEN, this is the finding that `engine/src/main.rs` was missing from the file list. Add `RebuildTriggerLinks` as the LAST parameter of three functions: `spawn_worker` (`ingest.rs:1606`), `spawn_worker_with_boundary` (`:1627`) and `spawn_rebuild_debounce_task` (`:1548`); `spawn_worker` forwards its copy to `spawn_worker_with_boundary` at line 1615. Construct one instance in `engine/src/main.rs` next to the `watch::channel(0u64)` at line 85 and pass a clone to the `spawn_worker(...)` call at line 88 and another to the `spawn_rebuild_debounce_task(...)` call at line 99. Update every call site in `engine/src/tests.rs` to pass `RebuildTriggerLinks::default()`: the thirteen worker sites at lines 1523, 1560, 1676, 1782, 1940, 2039, 2094, 2216, 2307, 2353, 2429, 2468 and 7591, and the two debounce sites at 8167 and 8219. These edits are mechanical; if the count differs from fifteen, record the real number as a finding rather than adapting the design.

`rebuild_and_swap`'s signature is deliberately NOT changed, which is what keeps its six test call sites (`engine/src/tests.rs:7918`, `:8007`, `:8044`, `:8084`, `:8130`, `:8284`) compiling untouched.

At the worker's rebuild notification at line 1724, push the currently active span's context into the buffer immediately before the existing `rebuild_tx.send_modify` call. That context belongs to the `ingest_document` span Task 1 created, so each triggering document contributes exactly one entry.

Create the `index_rebuild` span in `spawn_rebuild_debounce_task`, immediately before the `rebuild_and_swap` call at line 1601 — NOT inside `rebuild_and_swap`, whose signature must stay stable. Drain the buffer FIRST, then construct the span with one link per drained context (the Rust SDK attaches links at construction, so draining must precede it), recording `lancet.index.rebuild.trigger_count` from the drained length and `lancet.index.rebuild.dropped_links` from the overflow count. Declare `lancet.index.generation_before`, `lancet.index.nodes_version_before`, `lancet.index.generation_after`, `lancet.index.nodes_version_after` and `lancet.index.rebuild.degraded` as deferred `tracing::field::Empty` fields on that span. The span is a ROOT span with links, never a child of a triggering document — a coalesced rebuild has many causes and parenting it to one would misattribute the rest.

Attach the span to the call with `.instrument(index_rebuild_span)` and change line 1601 from `let _ = rebuild_and_swap(...).await;` to a `match` on the result so an Error span status can be set on the `Err` arm and an Ok status on the `Ok` arm. This is behaviour-neutral — the discarded value was already unused and no branch is added that alters control flow — but it reads like a prohibited change unless stated, so it is stated: the `match` arms do nothing except set span status and record `lancet.index.rebuild.degraded`.

Inside `rebuild_and_swap`, record the four generation/version values onto the ambient span with `tracing::Span::current().record(...)`: the before-values from the snapshot read at the start of the function, the after-values following a successful swap. Recording onto a span that does not declare those fields is a silent no-op, which is exactly the right behaviour for the six tests that call `rebuild_and_swap` directly with no `index_rebuild` span in scope. Do not change the fault hook, the rebuild counter increment, the build-off-the-lock ordering, or the snapshot swap semantics; those are Phase 6.1's proven contract and this task observes them rather than altering them.

Leave `spawn_rebuild_debounce_task`'s coalescing select loop and its shutdown arm otherwise untouched.

Write the five named tests in `engine/src/tests/telemetry_ingest.rs`, driving the debounce task the way Phase 6.1's `debounce_coalesces_burst` test (`engine/src/tests.rs:8144`) does rather than calling `rebuild_and_swap` directly — a direct call produces no `index_rebuild` span at all, so a test written that way would assert against nothing. Build on `pub(crate)` helpers from `engine::tests` such as `database_path` (`:928`) and `configured_service` (`:1158`), which ARE visible from this sibling `#[cfg(test)]` module; if a helper you need is private, widen it to `pub(crate)` in `engine/src/tests.rs` rather than duplicating it.

Then re-pin the engine count gate: enumerate with `cargo test --manifest-path engine/Cargo.toml --list`, update the TOTAL and lib pins in `scripts/engine-test-targets.sh` in the same commit, and add one line to its invariant-history comment. Never lower a pinned count.</action>
  <acceptance_criteria>
    - `RebuildTriggerLinks` exists in `engine/src/ingest.rs` over a `std::sync::Mutex`, is capped at 1024 with an overflow counter surfaced as `lancet.index.rebuild.dropped_links`, holds no guard across an `.await`, and the rebuild watch channel's payload type is unchanged.
    - `spawn_worker`, `spawn_worker_with_boundary` and `spawn_rebuild_debounce_task` each take the buffer as their last parameter; `engine/src/main.rs` constructs one instance and passes clones at lines 88 and 99; all fifteen `engine/src/tests.rs` call sites compile. `rebuild_and_swap`'s signature is UNCHANGED and its six test call sites are untouched.
    - The worker pushes the active span context immediately before the existing rebuild notification, and does not otherwise change that line.
    - The debounce task drains the buffer before constructing the `index_rebuild` span, and the span carries one link per drained context plus a matching trigger-count attribute.
    - The `index_rebuild` span is a root span with links, not a child of any document span, and it is created in `spawn_rebuild_debounce_task` — not inside `rebuild_and_swap`.
    - `engine/src/ingest.rs:1601` is a `match` whose arms only set span status and record the degraded attribute; no control flow, return value handling or error propagation changed there.
    - Generation and nodes-version attributes are recorded for both before and after states on the success path via `Span::current().record`; the failure paths, including the cfg(test) fault branch, set an Error status and the degraded attribute.
    - The rebuild counter, the fault hook, the build-off-the-lock ordering and the snapshot swap semantics are unchanged, proven by Phase 6.1's rebuild tests staying green.
    - All five named tests pass, including the buffer-drain isolation test.
    - `scripts/engine-test-targets.sh` pins match the enumerated counts with no pin reduced, and the script exits 0.
    - `cargo clippy --manifest-path engine/Cargo.toml -- -D warnings` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- index_rebuild_span_records_generation_transition</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- index_rebuild_span_records_degraded_outcome</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- index_rebuild_span_links_all_triggering_documents</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- index_rebuild_trigger_buffer_is_drained_per_rebuild</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --lib -- debounce_coalesces_burst</automated>
    <automated>sh scripts/engine-test-targets.sh</automated>
    <automated>cargo clippy --manifest-path engine/Cargo.toml -- -D warnings</automated>
  </verify>
  <done>Every rebuild attempt produces one root `index_rebuild` span recording the generation transition and degrade outcome, linked to each document that triggered it, with Phase 6.1's rebuild contract untouched.</done>
</task>

</tasks>

## Artifacts this phase produces

**Created by this plan**
- New span names: `ingest_admission`, `ingest_document`, `graph_extraction`, `index_rebuild`.
- New span attributes: `lancet.document.id`, `lancet.document.bytes`, `lancet.graph.written_entity_edges`, `lancet.graph.prior_entity_edges`, `lancet.index.generation_before`, `lancet.index.generation_after`, `lancet.index.nodes_version_before`, `lancet.index.nodes_version_after`, `lancet.index.rebuild.trigger_count`, `lancet.index.rebuild.degraded`.
- New span attribute `lancet.index.rebuild.dropped_links` (trigger-buffer overflow count).
- New symbols in `engine/src/ingest.rs`: `IngestionJob::trace_parent` field plus its consuming builder, and `RebuildTriggerLinks` (cloneable handle, `Default`, `push_current`, `drain`, cap 1024).
- New last parameter on the existing public functions `spawn_worker`, `spawn_worker_with_boundary` and `spawn_rebuild_debounce_task`.
- New tests in `engine/src/tests/telemetry_ingest.rs`.

**Modified by this plan**
- `engine/src/ingest.rs` (worker branch span structure, rebuild span in the debounce task, trigger buffer and its threading), `engine/src/service.rs` (ingest RPC admission span and trace-parent injection), `engine/src/main.rs` (trigger-buffer construction and the two spawn call sites at lines 88 and 99), `engine/src/tests.rs` (two `IngestionJob` struct-literal sites plus fifteen spawn call sites), `scripts/engine-test-targets.sh`.

**Explicitly NOT modified**
- `rebuild_and_swap`'s signature, and therefore its six existing test call sites in `engine/src/tests.rs`.

<review_dispositions>
Cross-AI review feedback from `06.2-REVIEWS.md`, resolved into this plan. Antigravity accepted this plan's ingestion nesting as correctly diagnosed and rated it LOW; Cursor rated it HIGH and found two instruction bugs. Ruling: **Cursor is correct on both, verified independently in-tree.** Antigravity's read is understandable — the diagnosis of WHERE the gap is (graph extraction running after `index_document` closes) is exactly right — but the diagnosis being right does not make the repair instruction right, and the repair as previously written would have shipped two sibling roots plus a compile break. "Antigravity approved it" is not a reason to leave either alone.

| Finding | Source | Disposition | Where it landed |
|---|---|---|---|
| HIGH — `ingest_document` instrument range excludes `index_document` | Cursor (Antigravity: accepted as correct) | INCORPORATED, with the mechanism sharpened: the real cause is that `tracing` binds a parent at span CONSTRUCTION, so instrumenting the awaited future is insufficient — the `info_span!("index_document")` statement itself must run inside the instrumented block | Task 1 action rewritten as three explicit steps; new must_have truth naming the construction-time binding; acceptance now asserts `index_document.parent_span_id == ingest_document.span_id` and a source check on where the construction sits |
| HIGH — `engine/src/main.rs` missing from `files_modified` | Cursor (Antigravity: not raised) | INCORPORATED | `engine/src/main.rs` added to frontmatter and to Task 2 `<files>`; Task 2 action enumerates `main.rs:88`, `main.rs:99` and all fifteen `engine/src/tests.rs` call sites; `rebuild_and_swap` deliberately kept signature-stable so its six test sites stay untouched |
| MEDIUM — `bytes` already exists on `index_document`; do not add `raw_data` or `filename` | Cursor | INCORPORATED | Task 1 action's attribute note; acceptance criterion forbids `filename` on this path |
| MEDIUM — admission span vs streaming body | Cursor | INCORPORATED, resolved toward wrapping the whole handler | Task 1 action pins extraction to the line before `into_inner()` at `service.rs:643` and makes `lancet.document.id` / `lancet.document.bytes` deferred `Empty` fields recorded after the read loop — the prior text stated them as construction-time attributes, which would not compile |
| LOW — `telemetry_ingest.rs` cannot see private helpers in `tests.rs` | Cursor | PARTIALLY REJECTED with the correction recorded: `pub(crate)` helpers ARE visible from a sibling `#[cfg(test)]` module of the same crate (`database_path` at `tests.rs:928` and `configured_service` at `:1158` are both `pub(crate)`); only items with no visibility modifier are unreachable | New flagged assumption stating the correction and the rule; Task 2 action names the two helpers and the widen-don't-duplicate rule |
| LOW — trigger buffer concurrency; must not block worker indexing | Antigravity | INCORPORATED | Task 2 action: `std::sync::Mutex`, no guard held across an `.await`, one-operation critical sections |
| Suggestion — bounded buffer cap with a dropped/overflow counter | Antigravity | INCORPORATED with concrete values (this also closed an internal gap: `T-06.2-04-04` already promised a cap the action never specified) | Task 2 action: cap 1024, attribute `lancet.index.rebuild.dropped_links`; acceptance criterion; artifact list |
| Strengths — four `IngestionJob` literals, `into_inner()` at `:643`, stage spans, watch-channel counter, `debounce_coalesces_burst` harness, recovered jobs as new roots | Both | NO CHANGE NEEDED — both reviewers independently confirmed these citations against the tree | n/a |
</review_dispositions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gateway upload → engine ingest RPC | Untrusted document bytes and an untrusted inbound trace parent cross here. |
| Ingest RPC → bounded Tokio queue | The serialized trace parent crosses a process-internal async boundary. |
| Worker → rebuild trigger buffer | Span contexts accumulate in shared mutable state between the worker and the debounce task. |
| Document content → span attributes | Everything recorded leaves the process over OTLP. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.2-04-01 | Information Disclosure | Ingestion span attributes | high | mitigate | Document identifier, byte size and counts only; filename, raw bytes, chunk text and entity text are prohibited, with a dedicated test asserting no content appears in exported attributes. |
| T-06.2-04-02 | Spoofing | Caller-supplied trace parent on the ingest RPC | medium | mitigate | The standard propagator validates and falls back to a local root; the value is carried as data on the job and re-parsed by the same propagator, never interpreted as identity or authorization. |
| T-06.2-04-03 | Tampering | Malformed `trace_parent` value on a queued job | medium | mitigate | The field is optional and re-extracted through the propagator, which rejects invalid values and yields a new local root; the recovered-staged-job test pins that behaviour. |
| T-06.2-04-04 | Denial of Service | Unbounded growth of the rebuild trigger buffer | medium | mitigate | The buffer is drained on every rebuild and the drain-isolation test proves it does not accumulate across rebuilds; the cap is 1024 entries and the discarded count is recorded as `lancet.index.rebuild.dropped_links` — both values are stated in Task 2's action, not merely promised here. |
| T-06.2-04-07 | Denial of Service | Trigger-buffer lock contention stalling the ingestion worker | low | mitigate | A plain `std::sync::Mutex` with single-operation critical sections and no guard held across an `.await`, so a slow debounce-side drain cannot block worker indexing (Antigravity's concern). |
| T-06.2-04-05 | Repudiation | Instrumentation altering the rollback or degrade path | high | mitigate | Spans are strictly additive around the existing branches; Phase 6.1's rebuild, degrade and debounce tests plus the ingestion rollback tests are the regression evidence. |
| T-06.2-04-06 | Denial of Service | Span volume during a large ingestion batch | low | accept | Ingestion is operator-initiated and locally bounded by the existing queue capacity; the span count per document is fixed at six and the rebuild coalesces to one span per debounce window. |
</threat_model>

<verification>
- `cargo test --manifest-path engine/Cargo.toml --locked && (cd gateway && go test ./...)` exits 0.
- `cargo clippy --manifest-path engine/Cargo.toml -- -D warnings` exits 0.
- `sh scripts/engine-test-targets.sh` exits 0.
</verification>

<success_criteria>
- One uploaded document yields one continuous trace through admission, chunking, embedding, staging write and graph extraction.
- Graph extraction is inside the document trace, which it is not today.
- Every rebuild attempt is a visible span recording the generation transition and its degrade outcome.
- A coalesced rebuild links every triggering document rather than misattributing itself to one.
- No document content reaches any span attribute.
</success_criteria>

<output>
Create `.planning/phases/06.2-opentelemetry-traces-metrics-and-logs-across-go-and-rust-wit/06.2-04-SUMMARY.md`, recording the number of `IngestionJob` struct-literal sites actually found, the number of spawn call sites actually updated against the fifteen this plan enumerates, the observed `index_document.parent_span_id` / `ingest_document.span_id` equality that proves the nesting fix landed, the trigger-buffer cap and overflow handling as implemented, and the drain-before-construct ordering constraint that shaped the link design.
</output>
