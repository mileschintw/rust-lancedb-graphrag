---
phase: 06.3.3
plan: 03
type: execute
wave: 2
depends_on: [06.3.3-01, 06.3.3-02]
files_modified:
  - eval/src/lancet_eval/measure.py
  - eval/src/lancet_eval/decay.py
  - eval/src/lancet_eval/latency.py
  - eval/tests/test_measure.py
  - .planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-MEASUREMENT.md
autonomous: true
requirements: [OBS-05]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The measurement pass runs several hundred full end-to-end queries against a cheap generation model with no judge, starting from the surviving store and the existing PostgreSQL checkpoint rows as-is, with the recorded starting counts unchanged since 06.3.3-02 captured them."
    - "The pass is two-armed with each question's arms dispatched adjacently, and the record count is twice the question count; the sample-size unit is stated so a pass half the intended size cannot pass unnoticed."
    - "Every budget in force during the pass is raised far above any plausible value and is recorded, so the observed tail is a real tail rather than a value clipped at a ceiling."
    - "Both censoring ceilings are recorded — the engine's per-node budgets and the harness read timeout and question deadline — and the record states which of them, if either, could have clipped an observation."
    - "Every observation at or above the ceiling in force is counted and reported as censored per node; a percentile over a node with a non-zero censored count is reported as a lower bound and never as the true value."
    - "The pass includes one engine restart splitting it into exactly two segments, and the restart's wall-clock time and query ordinal are recorded so the segment boundary is unambiguous."
    - "Record ordering is well defined and stable across the two segments: ordinals are strictly increasing within each segment and continue across the boundary, and the analysis refuses a record set whose ordering does not hold rather than producing a corrupted trend (specless edge: ordering)."
    - "The decay question is answered against the criterion committed before the pass ran, and the answer names its statistic, its threshold and its verdict for each prong — a positive-slope prong and a late-versus-early window prong — plus their stated combination."
    - "A node with zero usable observations in either segment is reported as a stated-reason outcome carrying its observation count, and the decay verdict for that node is recorded as unanswerable rather than as no trend (specless edge: empty)."
    - "Every node that blew a budget, and every node whose observations were censored, carries a recorded investigation disposition naming it a defect or genuinely slow, with its evidence, before any budget is widened."
    - "Spend for the pass is capped per stage against the daily allowance and the actual observed figure is recorded — never estimated, and never seeded from the 2026-09-03 figure."
  artifacts:
    - "eval/runs/<date>-measure-multihop_rag/ — the measurement journal and run record, committed as evidence"
    - ".planning/phases/06.3.3-.../06.3.3-MEASUREMENT.md — pass configuration, censoring census, decay verdict, D-16 dispositions, observed spend"
  key_links:
    - "The recorded restart ordinal must fall inside the observed segment boundary in the journal; the discriminator refuses the record set otherwise, so the operator's claim and the data must agree"
    - "The disposition set produced by task 3 is the precondition 06.3.3-04 checks before widening any budget — the two are joined by node name"
    - "query_embedding and graph_operation are inner budgets with no per-node wire timing; they are decomposed from the ExtractGraphContext node total across the two arms, corroborated by the retained graph-path spans where available"
  prohibitions:
    - "No re-seed, no compaction, no checkpoint truncation, and no modification of eval/runs/2026-09-03-multihop_rag/ at any point during the pass"
    - "No budget may be widened in this plan; this plan measures and disposes, 06.3.3-04 writes"
    - "No raised budget may be written into any committed configuration file; raised values are supplied to the engine process only and the record names the set that was in force"
    - "No decay threshold, multiplier or window fraction may be changed after the data is seen; a change is a recorded amendment with its own rationale, never a silent edit"
    - "Spend must never be estimated from the 2026-09-03 figure of $0.0199 — that run was cheap because 966 of its queries generated nothing"
    - "No judge is involved in this pass"
---

<objective>
Run the pass that turns two competing hypotheses into one answer, and dispose of every node that
blew a budget as a defect or as genuinely slow before anything is widened.

Purpose: the 2026-09-03 run decayed monotonically — ten successes in the first eleven positions,
then thirteen, then four, then three, then zero across the final 436 — inside a single process
after a resume. A uniformly-too-tight budget fails uniformly from query one, so decay is not what
a tight budget looks like. But every observation in that run was clipped at the budgets, so its
latency distribution is unknown and the two hypotheses cannot be separated from it. This pass
runs the same system with the ceilings lifted out of the way, so the tail is observed rather than
clipped, and restarts the engine mid-run so that process state and accumulated store state give
different answers.

Output: a committed measurement run, and `06.3.3-MEASUREMENT.md` carrying the pass configuration,
the censoring census, the decay verdict against its pre-committed criterion, and one investigation
disposition per budget-blowing node.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-CONTEXT.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-AI-SPEC.md
@.planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-RESEARCH.md
@.planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-01-SUMMARY.md
@.planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-02-SUMMARY.md
@.planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-STORE-BASELINE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the two-segment measurement pass under lifted ceilings and a spend cap</name>
  <files>eval/src/lancet_eval/measure.py, eval/tests/test_measure.py, eval/runs/&lt;date&gt;-measure-multihop_rag/</files>
  <precondition>`06.3.3-STORE-BASELINE.md` exists with non-placeholder graph population counts and a recorded starting PostgreSQL checkpoint row count, and no re-seed or compaction has run since. A gateway and engine are reachable, `OPENROUTER_API_KEY` is set, and the preflight canary passes against the raised budgets. Halt and report if the baseline record is absent — the roadmap's first criterion requires population to be verified before this work runs.</precondition>
  <reversibility rating="costly">The pass consumes the very as-is condition it starts from: it appends PostgreSQL checkpoint rows and advances LanceDB versions, and re-seeding is out of scope, so the original starting state cannot be restored once spent. The next sibling's re-drive therefore begins from a post-pass store. This is accepted deliberately — measuring the original condition requires running against it.</reversibility>
  <read_first>
    - `eval/src/lancet_eval/measure.py`, `latency.py`, `thresholds.py` and the `measure` command as landed by 06.3.3-01, including the tracer's committed micro-pass record.
    - `.planning/phases/.../06.3.3-STORE-BASELINE.md` as landed by 06.3.3-02 — the graph population verdict and the starting row counts this pass must not have disturbed.
    - `engine/src/config.rs:659-731` — `load_settings` and the seven `LANCET_ENGINE__WORKFLOW__*_TIMEOUT_MS` overrides. This is how budgets are lifted without touching a committed file.
    - `engine/src/config.rs:294-345` — `validate` and `validate_against_provider`. A raised set must still satisfy both or the engine refuses to start; in particular the graph node budget must remain at least the sum of the query-embedding and graph-operation budgets, and the generation node budget at least twice the provider per-attempt timeout.
    - `eval/src/lancet_eval/config.py:53-80` — `gateway_timeout_secs` and `question_deadline_secs`, the harness-side ceilings, and their `LANCET_EVAL__` environment prefix.
    - `eval/src/lancet_eval/preflight.py` — `run_preflight_checks` and the canary check landed by 06.3.2-03 with the environment-aware timeout reader landed by 06.3.3-01.
    - `.planning/phases/06.3.1-.../06.3.1-CONTEXT.md` — D-20, D-21, D-22, D-47, and the `<code_context>` decay signature table.
  </read_first>
  <action>
    Run the pass. This task produces evidence; the code it runs already exists.

    Fix the pass configuration first and record it before driving anything. Choose a sample size
    large enough that the tail is estimable at the percentile the committed thresholds derive from,
    and state the reasoning. Note the unit: the sample size counts questions and the drive is
    two-armed, so N questions produce 2N records — the roadmap's several hundred end-to-end queries
    is the record count, not the question count, and confusing the two halves the pass. Choose the
    segment boundary ordinal so both segments carry enough observations for the boundary comparison
    at the committed segment sizes. Pin the cheap generation model and record it. No judge
    participates.

    The pass is two-armed and both arms are required. This is not a benchmark — no quality metric is
    scored here — but the graph-enabled and graph-disabled arms are the only wire-visible way to
    separate the two inner budgets from the node total that contains them, so a single-armed pass
    would leave two of the six budgets with no derivation input. The driver dispatches a question's
    two arms adjacently; preserve that rather than reordering work units for throughput.

    Lift every ceiling out of the way before driving. Supply the raised budget set to the engine
    process through the per-field environment overrides — never by editing a committed file, so
    nothing can leak into the repository and the record is the only place the raised set exists.
    Raise them far enough above the plausible maximum that the observed distribution is genuinely
    the system's own, not a value pressed against a wall. Check the raised set still satisfies the
    engine's own startup rules, because the engine will refuse to start otherwise and a refusal at
    this point looks like an environment problem rather than what it is.

    Then handle the second ceiling. The harness read timeout and the per-question deadline are
    ceilings too, and a raised engine budget whose worst-case sum exceeds the harness read timeout
    buys nothing — the harness clips first and the tail is censored anyway, silently, by a component
    nobody was thinking about. Compute the raised set's worst-case sum, compare it against both
    harness ceilings, and raise the harness ceilings for this pass if the sum exceeds either.
    Record both ceilings in the run record whatever the outcome, so a reader can tell which one
    could have clipped an observation.

    Run the preflight, including the canary, against the raised budgets. This is the first time the
    canary's environment-aware duration floor matters in anger: it reads the budgets the engine is
    actually running under rather than the committed file, so it can still fail loud on a dead
    retrieval path while the budgets are lifted. A failing canary stops the pass before it spends
    anything.

    Drive segment one under its stage spend cap. When the boundary ordinal is reached, stop, restart
    the engine process cleanly, and record the restart's wall-clock time and the ordinal it fell
    between. Restart the engine only — the harness keeps running across the boundary, which is what
    preserves ordinal continuity by construction; the recorded ordinal is the backstop that proves
    it if something goes wrong. After the restart, drive one probe query through the same client
    path before segment two begins and record its latency, so a segment-two head that is not lower
    than the segment-one tail can be attributed rather than guessed at. Then drive segment two.

    Watch the spend cap throughout. The engine spends, so the harness can only observe and halt.
    Record the observed figure at every stage boundary. Never seed a cap or an expectation from the
    2026-09-03 figure: that run cost almost nothing precisely because 966 of its queries generated
    nothing, which makes it an anti-estimate rather than a baseline.

    When the pass completes, verify against the baseline record that the store and checkpoint
    counts it captured were not disturbed before the pass started, and record the post-pass counts
    alongside them so the growth over the pass is visible. Then assert that no committed
    configuration file changed and that the engine's effective configuration once the raised
    environment is gone matches the committed file again — the lifted budgets must not survive the
    pass in any form.

    If the pass aborts partway — quota, transport, an operator interruption — do not silently
    resume into the same journal. Record where it stopped and why, and decide explicitly whether the
    partial record is analysable or whether the pass restarts; a resumed drive can reorder work
    units, and the ordering guarantee this analysis depends on is the one thing that must not be
    quietly compromised.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_measure.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --project eval pytest eval/tests -q` exits 0 offline and `uv run --project eval ruff check --preview eval/src eval/tests` exits 0.
    - A committed measurement run directory exists whose name ends with the corpus name and which is not matched by the benchmark resume glob.
    - A named post-hoc assertion over the produced record — not a re-run of the pass — checks all of the following and exits 0: every record carries an ordinal and a segment; ordinals are strictly increasing within each segment and continue across the boundary with no duplicate and no gap; exactly two distinct segment labels appear and the label changes exactly once; the recorded restart ordinal falls inside the observed boundary; per-node observation counts and per-node censored counts are present for every node; the raised budget set, both harness ceilings, the starting and ending spend figures and the stage cap are all present.
    - The same assertion checks the pass was two-armed: both arm labels appear, the record count is twice the question count, every question carries exactly one record per arm, and each question's two arms are adjacent within a stated small ordinal distance.
    - The record states whether the sample size was expressed in questions or records, and the record count matches twice the question count.
    - The run record names the raised budget set and states that it was supplied through the process environment, with no committed configuration file changed: `git status --short config/` reports no change.
    - The run record names the harness read timeout and question deadline in force, and states the raised set's worst-case sum against them.
    - The run record carries the observed spend at each stage boundary and the stage cap, and states that no figure was estimated.
    - The run record carries the post-restart probe query's latency alongside the segment-one tail, so the restart's effect is attributable.
    - The run record carries the post-pass store and checkpoint row counts alongside the pre-pass counts from `06.3.3-STORE-BASELINE.md`, and states the growth over the pass.
    - The record states that the engine's effective configuration, with the raised environment removed, matches `config/config.toml` again.
    - `git status --short eval/runs/2026-09-03-multihop_rag/` reports no modification to the retained evidence, and the eval store shows no re-seed or compaction.
    - No judge model appears anywhere in the pass configuration or in the run record's model fields.
  </acceptance_criteria>
  <done>
    Several hundred real queries have run end to end against the surviving store with every ceiling
    lifted and recorded, split into two segments by a recorded engine restart, under a spend cap
    whose observed figure is written down — and the resulting record is committed evidence.
  </done>
</task>

<task type="auto">
  <name>Task 2: Answer the decay question and publish the censoring census</name>
  <files>eval/src/lancet_eval/decay.py, eval/src/lancet_eval/latency.py, .planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-MEASUREMENT.md</files>
  <precondition>The measurement run from task 1 exists and passes its own ordering assertion — ordinals strictly increasing within each segment, exactly one segment change, the recorded restart ordinal inside the observed boundary. Halt if it does not; a trend read from a compromised ordering is worse than no trend.</precondition>
  <reversibility rating="reversible">Analysis over a recorded run plus a written record; the run itself is untouched.</reversibility>
  <read_first>
    - `eval/src/lancet_eval/decay.py`, `latency.py` and `thresholds.py` as landed by 06.3.3-01, in particular the committed significance threshold, materiality delta, window fractions and segment-boundary sizes.
    - Task 1's committed run record and journal.
    - `.planning/phases/06.3.1-.../06.3.1-CONTEXT.md` — D-18, D-19, D-21, and the `<code_context>` decay signature table with its four position ranges.
    - `.planning/phases/06.3.3-.../06.3.3-RESEARCH.md` — the decay-detection methodology section and its three named confounds: question difficulty varying across ordinal, arm imbalance across segments, and page-cache or server-side caching surviving the restart.
    - `engine/src/workflow/nodes/graph_context.rs:60-135` — the embedding prelude at 72-92 bounded by the query-embedding budget, the graph-off early return at 95-104, and the graph operation at 106-135 bounded by the graph-operation budget. This ordering is what makes the inner-budget decomposition possible.
    - `engine/src/workflow/runner.rs:379-396` — the node timer wraps the whole node body, so one duration is emitted per node and never per inner operation.
    - `engine/src/service.rs:425-440` and `:470-485` — the `embedding_request` and `graph_traversal` spans, the corroborating instrument for the two inner budgets where 06.3.1-03's span retention has landed.
  </read_first>
  <action>
    Answer the question the pass exists to answer, and publish what the pass could and could not
    observe.

    Start with the censoring census, because everything after it depends on whether the numbers are
    real. For every node, report the observation count, the count at or above the ceiling in force,
    and the resulting censored rate. Report the harness ceilings alongside the engine budgets, since
    either can clip. A node whose censored count is non-zero has its percentiles reported as lower
    bounds and marked as such, and it is flagged for the next task's investigation — a node that
    still hits a ceiling raised far above any plausible value is telling you something about the
    node, not about the budget.

    Then run the decay verdict against the criterion committed before the pass. Report both prongs
    separately and their stated combination: the ordinal-trend slope with its significance statistic
    against the committed threshold, and the late-window percentile against the early-window
    percentile with the delta against the committed materiality threshold. Name the statistic, the
    threshold and the outcome for each. Do not reduce it to a boolean and do not reduce it to a
    single prong; a reader must be able to see which prong carried the verdict and to recompute
    both from the record without re-running anything. An inconclusive result is a legitimate
    outcome here — the phase's own criterion admits that the hypothesis pair may stay open — and
    saying so is a better answer than picking a side.

    Stratify. Report trend and window results per arm and per gold question type as well as in
    aggregate, and cross-tabulate segment against arm and against question type. The research names
    two composition confounds and this is where they are caught: a segment-two elevation that
    coincides with a harder question mix or an arm imbalance is a composition artefact, not decay,
    and the only way to see it is to look within stratum. A trend that holds within stratum is a
    finding; one that appears only in aggregate is not.

    Run the restart discriminator at the committed segment sizes. Report the segment-one tail
    against the segment-two head per node, with the delta and its interval and the two sample sizes.
    Compare the post-restart probe latency from task 1 against the segment-one tail as well. State
    which hypothesis the comparison supports — latency resetting to baseline points at process
    state, latency staying elevated points at accumulated store state — and say inconclusive when
    the interval spans zero. Note the third confound explicitly whichever way it lands: operating
    system page cache and any provider-side caching survive a process restart, so a segment-two head
    that fails to reset does not by itself prove store state, and the record must say so rather than
    presenting one reading as the only one.

    Then decompose the two inner budgets, because they have no direct instrument. The node timer
    wraps a whole node, so the wire carries one duration for `ExtractGraphContext` and none for the
    query-embedding call or the graph traversal separately. But the node's own control flow supplies
    the decomposition: both arms pay the embedding, and only the graph-on arm continues past the
    early return to pay the traversal. So the graph-off arm's node duration is the query-embedding
    latency plus node overhead, and the per-question difference between the arms is the graph
    operation's latency. Compute it as a paired per-question difference over questions where both
    arms are usable and neither node timed out, report it with its interval, and label it an
    estimate with its method stated — it is a difference of two measurements, not a measurement.

    Guard the assumption the decomposition rests on. The embedding runs under an "if not already
    present" condition, so the graph-off node total equals the embedding cost only when the
    embedding actually ran inside that node. Confirm at execution time that nothing upstream
    populates the query embedding before this node — at planning time the only writer in the tree is
    this node itself, and the reformulation node writes only the variant list, so the condition is a
    defensive idempotency check rather than a live skip path. Verify it still holds against the code
    as landed, then include only records where the embedding demonstrably ran and count the excluded
    ones. If the condition has changed, the query-embedding estimate would be biased low with no
    visible symptom, which is exactly the class of silent error this phase exists to eliminate.

    Where 06.3.1-03's graph-path span retention has landed, read the `graph_traversal` and
    `embedding_request` span durations as a corroborating instrument and report both numbers side by
    side; agreement strengthens the estimate and disagreement is itself a finding. If the spans are
    unavailable, say so and carry the decomposition alone, with the weaker evidence stated.

    Flag the sharpest constraint in the phase explicitly. The graph-operation budget is the one that
    fired 494 times in the recorded run, so its true latency was fully censored there, and it is
    also the one with the weakest instrument here. Whatever this pass finds for it, the record must
    state the strength of the evidence behind it rather than presenting a number with the same
    confidence as a directly-measured node total.

    Finally, handle the empty case for every node and every stratum. A node with zero usable
    observations in a segment is reported as a stated-reason outcome carrying its count, and its
    decay verdict is recorded as unanswerable — not as no trend. Those are different findings and
    conflating them would let a node that never ran read as a node that ran steadily.

    Write all of it into `06.3.3-MEASUREMENT.md`.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_decay.py eval/tests/test_latency.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --project eval pytest eval/tests -q` exits 0 offline and `uv run --project eval ruff check --preview eval/src eval/tests` exits 0.
    - `06.3.3-MEASUREMENT.md` exists, is committed, and carries no unresolved placeholder in any field it claims to record.
    - The record carries a censoring census with, per node: observation count, count at or above the ceiling, censored rate, and the ceiling that applied. Every percentile for a node with a non-zero censored count is marked a lower bound.
    - The record states both censoring ceilings — the engine budgets and the harness read timeout and question deadline — and which could have clipped each node.
    - The decay verdict names, for each of the two prongs, the statistic value, the threshold it was compared against and the outcome, plus their stated combination; and the thresholds cited match the values committed in `thresholds.py` before the pass ran, asserted by comparing the record against the committed module.
    - The record carries trend and window results per arm and per gold question type as well as in aggregate, and a cross-tabulation of segment against arm and against question type.
    - The restart discriminator result names the segment-one tail and segment-two head sample sizes, the per-node delta with its interval, the post-restart probe latency, the hypothesis supported or an explicit inconclusive outcome, and the surviving-cache confound.
    - The inner-budget decomposition is present for the query-embedding and graph-operation budgets, states its method as a paired per-question arm difference, is labelled an estimate rather than a measurement, and states whether span corroboration was available and what it showed.
    - The record states that the single-writer condition on the query embedding was re-verified against the code as landed, names the result, and carries the count of records excluded from the decomposition because the embedding did not demonstrably run in the node.
    - The record states explicitly that the graph-operation budget carries the weakest evidence in the set and why.
    - Every node and stratum with zero usable observations appears as a stated-reason outcome carrying its count, with its decay verdict recorded as unanswerable rather than as no trend.
    - This command confirms the verdict was not reduced to a bare boolean: the record's decay section names a statistic value and a threshold value for both prongs, asserted by a named check that both appear and are numeric.
    - No threshold, multiplier or window fraction in `thresholds.py` was modified in this plan's commits: `git log --oneline -p -- eval/src/lancet_eval/thresholds.py` shows no change after the 06.3.3-01 commit, or any change is a recorded amendment carrying its own rationale in this plan's summary.
  </acceptance_criteria>
  <done>
    The phase's central question has an answer that names its own statistic and its own
    pre-committed threshold, the censoring census says exactly what could and could not be
    observed, and the two inner budgets have an estimate whose method and evidence strength are
    both stated.
  </done>
</task>

<task type="auto">
  <name>Task 3: Dispose of every budget-blowing node as a defect or as genuinely slow, before anything is widened</name>
  <files>.planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-MEASUREMENT.md</files>
  <precondition>Task 2's censoring census and decay verdict are recorded in `06.3.3-MEASUREMENT.md`, so the set of nodes requiring a disposition is derivable from the record rather than from memory.</precondition>
  <reversibility rating="reversible">A written investigation record; no code or configuration changes.</reversibility>
  <read_first>
    - `06.3.3-MEASUREMENT.md` as landed by task 2 — the censoring census and the per-node percentiles.
    - `engine/src/workflow/nodes/retrieve.rs` — the snapshot at 287 and `NO_EVIDENCE` at 305, both at the end of `run`, and whatever candidate-limit-dependent work sits between; the retrieval node is the one that failed 904 times.
    - `engine/src/workflow/nodes/graph_context.rs:96,119,127-148,160-180` — the graph-off early return, the graph-operation timeout, the node and edge count population, and the non-fatal degradation path.
    - `engine/src/service.rs:469-520` — `query_graph`, the `graph_traversal` span and `GraphAugmentationOutcome`.
    - `config/config.toml:41-52` — `candidate_limit`, `final_limit`, `evidence_token_budget` and the fusion weights, the parameters against which a latency that scales with candidate count would be diagnostic.
    - `.planning/phases/06.3.3-.../06.3.3-STORE-BASELINE.md` — the graph population verdict, which is what tells a slow traversal apart from a traversal over a store that was never populated.
    - `.planning/phases/06.3.1-.../06.3.1-CONTEXT.md` — D-16, D-03, D-04, D-46's back-edge rule.
  </read_first>
  <action>
    For every node that blew a budget during the pass, and every node whose observations were
    censored at a ceiling raised far above any plausible value, record an investigation and its
    conclusion. This is a gate, not a formality: the roadmap's fifth criterion says every such node
    is investigated as a possible defect before its budget is widened, and a widened budget with no
    disposition entry is a failure of this phase.

    Investigate before concluding. Look at what the node actually does and whether its latency has a
    shape that a budget can legitimately accommodate. A prompt-assembly node exceeding two seconds
    is doing something a prompt-assembly node should not be doing, and widening its budget would
    bury that. Retrieval latency that scales with the candidate limit points at work proportional to
    candidates where it should be proportional to results. Graph traversal that is slow against a
    store the baseline record shows is well populated is a different finding from graph traversal
    that is slow against a store with almost no edges, and the baseline record is what separates
    them. Use the decay verdict too: a node whose latency climbs with ordinal is not merely slow, it
    is accumulating something, and that is a defect signature rather than a sizing question.

    Record for each node: its observed percentiles with their censoring status, what was examined,
    what was found, and the conclusion — defect, or genuinely slow. Where the conclusion is defect,
    state whether the fix lands in this phase or the next, and route it forward. Under the back-edge
    rule, an engine or configuration fix that the diagnostics expose lands here or in the corrected
    re-drive; it never reopens a closed sibling. Where the conclusion is genuinely slow, state what
    makes it so, so the next task's widening has a reason attached rather than a number.

    Handle the graph case as the roadmap's seventh criterion directs. If traversal proves inherently
    too slow for any sane budget, that is not a defect and not a reason to ship a silently degraded
    graph — the budget is raised to what the measured requirement needs and the latency cost is
    recorded for the honest-limitations section of the documentation phase. Record the cost figure
    here so that section has a number to cite rather than a characterisation.

    Produce the disposition set in a shape the next plan can check mechanically: one entry per node
    name, each carrying its conclusion. The next plan's precondition is a containment check — every
    node whose budget the derivation widens must appear in this set — and a prose paragraph cannot
    satisfy a containment check.

    Where a node blew no budget and was not censored, say so explicitly rather than omitting it.
    An absent row is ambiguous between "clean" and "not looked at", and the containment check the
    next plan runs deserves an unambiguous input.
  </action>
  <verify>
    <automated>D=.planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva; grep -qiE "disposition" $D/06.3.3-MEASUREMENT.md &amp;&amp; for n in ReformulateQuery ExtractGraphContext RetrieveHybrid AssemblePrompt GenerateAnswer; do grep -q "$n" $D/06.3.3-MEASUREMENT.md || { echo "missing node: $n" >&amp;2; exit 1; }; done &amp;&amp; echo dispositions-cover-all-nodes</automated>
  </verify>
  <acceptance_criteria>
    - `06.3.3-MEASUREMENT.md` carries a disposition set with exactly one entry per workflow node name — `ReformulateQuery`, `ExtractGraphContext`, `RetrieveHybrid`, `AssemblePrompt`, `GenerateAnswer` — with no node omitted.
    - Each entry carries the node's observed percentiles with their censoring status, what was examined, what was found, and a conclusion of defect or genuinely slow, or an explicit statement that the node blew no budget and was not censored.
    - Every entry concluding defect states whether the fix lands in this phase or the corrected re-drive, and none routes work backwards into a closed sibling.
    - Every entry concluding genuinely slow states what makes it so, in terms of what the node does rather than in terms of the number.
    - The disposition set is machine-readable enough for a containment check by node name, demonstrated by the automated verify command above succeeding.
    - If graph traversal is concluded inherently slow, the record carries the latency cost figure the documentation phase's honest-limitations section will cite.
    - Where a node's disposition depends on whether the graph is populated, the entry cites the store-baseline record's verdict rather than assuming either way.
    - No budget value is written to any configuration file in this plan: `git status --short config/` reports no change.
  </acceptance_criteria>
  <done>
    Every node that hit a ceiling has a written investigation with a named conclusion, and the set
    is shaped so the next plan can mechanically refuse to widen a budget that has no disposition
    behind it.
  </done>
</task>

</tasks>

<out_of_scope>
| Item | Owner | Why not here |
|---|---|---|
| Writing any derived budget into `config/config.toml` | 06.3.3-04 (D-13, SC-6) | This plan measures and disposes; the write is a separate, production-facing act gated on the disposition set. |
| The go/no-go decision for paid spend | 06.3.3-04 (D-47, SC-8) | Deliberately placed on the phase boundary rather than mid-phase. |
| Fixing a defect this pass exposes in engine code | 06.3.3-04 or 06.3.4, per the D-46 back-edge rule | This plan diagnoses and routes; it does not reopen a closed sibling and it does not carry an engine fix itself. |
| Re-seeding or rebuilding the eval store | Not planned — out of scope in D-05. | The as-is store is the condition under test. |
| The corrected two-arm re-drive and human calibration | 06.3.4 (D-44, D-48) | This pass involves no judge and is not a benchmark run. |
| Graph traversal tuning — depth, fan-out, candidate limits, reranking | Not planned — deferred in D-03. | The bar for the graph is that it runs and outright bugs are fixed. |

</out_of_scope>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This plan's contribution. The complete phase inventory is the `artifacts_this_phase_produces`
section of `06.3.3-01-PLAN.md`.

| Kind | Artifact |
|---|---|
| New run-directory artifact | `eval/runs/<date>-measure-multihop_rag/journal.jsonl` — one record per query with ordinal, segment, warm-up marker and per-node durations |
| New run-directory artifact | `eval/runs/<date>-measure-multihop_rag/measurement.json` — raised budget set, both harness ceilings, restart wall-clock and ordinal, per-node observation and censored counts, stage spend figures and caps, pre- and post-pass store and checkpoint counts |
| New phase record | `06.3.3-MEASUREMENT.md` — pass configuration, censoring census, decay verdict with both prongs and their thresholds, stratified results, restart discriminator, inner-budget decomposition, D-16 disposition set |
| New analysis outputs | the inner-budget decomposition estimate for the query-embedding and graph-operation budgets, and its span corroboration where available |
| New config keys | None |
| New CLI flags | None — the `measure` command's surface is fixed by 06.3.3-01 |
| New dimension names | None |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raised environment budgets → live engine process | A deliberately non-production configuration drives a real engine for the duration of a paid pass. |
| provider API → paid measurement stage | Several hundred generation and embedding calls are issued against a metered account. |
| gateway SSE stream → committed measurement journal | Unbounded external content is written to a git-tracked path. |
| eval store and PostgreSQL eval schema → the pass | A store this phase must not re-seed is written to by the normal query path. |
| measurement record → the budget derivation that follows | A record whose numbers a later plan writes into production configuration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.3-13 | Tampering | raised budgets leaking beyond the pass | high | mitigate | Raised values are supplied through the process environment only; an acceptance criterion asserts no committed configuration file changed and that the engine's effective configuration matches the committed file once the raised environment is gone. |
| T-06.3.3-14 | Denial of Service | uncapped paid stage against the provider | high | mitigate | Each stage runs under a required cap with no default; spend is observed at every stage boundary, the stage halts at its cap, and an unreadable allowance halts before a query is issued. |
| T-06.3.3-15 | Spoofing | a percentile over clipped observations presented as the true tail | high | mitigate | Every node's censored count is published beside its percentiles; a node with a non-zero censored count has its percentiles marked lower bounds and is routed to the disposition set. |
| T-06.3.3-16 | Repudiation | a decay verdict whose threshold was chosen after the data | high | mitigate | Thresholds are committed in `thresholds.py` before the pass; the record cites them and an acceptance criterion compares the cited values against the committed module and against its commit history. |
| T-06.3.3-17 | Information Disclosure | measurement journal in a git-tracked directory | high | mitigate | The journal persists node names, durations, counts, flags and token totals over a public news corpus. No credential, header, request body or base URL is persisted — inherited from the 06.3.3-01 sink contract and re-asserted here. |
| T-06.3.3-18 | Tampering | the store's as-is condition altered before the pass | medium | mitigate | The baseline record carries a dated no-re-seed, no-compaction, no-truncation statement, and the pass re-verifies the recorded counts before starting and records the post-pass counts alongside them. |
| T-06.3.3-19 | Elevation of Privilege | a budget widened with no investigation behind it | high | mitigate | The disposition set is machine-readable by node name and is the containment precondition the next plan checks before writing any widened budget. |
| T-06.3.3-SC | Tampering | npm/pip/cargo/uv installs | high | accept | No package-manager install task exists: this plan runs already-committed code and writes records. `uv lock --project eval --check` is an acceptance criterion. No `[ASSUMED]` or `[SUS]` package is introduced. |
</threat_model>

<verification>
- `uv run --project eval pytest eval/tests -q` exits 0 offline.
- `uv run --project eval ruff check --preview eval/src eval/tests` exits 0 and `uv lock --project eval --check` exits 0.
- `git status --short config/` reports no change — this plan writes no production configuration.
- `git status --short eval/runs/2026-09-03-multihop_rag/` reports no modification to the retained evidence.
- The measurement run directory is committed, and its post-hoc assertion over ordering, segments, restart ordinal, censoring counts and ceilings exits 0.
- `06.3.3-MEASUREMENT.md` is committed with the censoring census, the two-pronged decay verdict citing the pre-committed thresholds, and one disposition entry per workflow node.

</verification>

<success_criteria>
- ROADMAP SC-2 satisfied: several hundred full end-to-end queries ran against a cheap generation model with budgets raised far above any plausible value, from the store and checkpoint rows as-is, with starting counts recorded and no judge involved.
- ROADMAP SC-3 satisfied: the pass includes a mid-run engine restart splitting it into two segments, with the restart's wall-clock time and ordinal recorded.
- ROADMAP SC-4 satisfied: the decay question is answered against a stated criterion committed before the data existed, reporting both the trend prong and the window prong with their statistics and thresholds.
- ROADMAP SC-5 satisfied: every node that blew a budget carries a recorded investigation disposition before any budget is widened, and any fix routes forward rather than into a closed sibling.
- ROADMAP SC-8 partly satisfied: the pass's stage cap was enforced and its actual spend recorded; the go/no-go decision itself closes the phase in 06.3.3-04.

</success_criteria>

<output>
Create `.planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-03-SUMMARY.md` when done.
</output>

## Revise-before-execute

Read this before executing. Each row is an input this plan assumed unavailable at planning time.
Confirm the producer has landed and the contract matches, then execute.

| Assumed-unavailable input | Produced by | Effect on this plan if still absent |
|---|---|---|
| Per-node `duration_ms` on every journal record — the instrument this phase measures with | **06.3.2** (D-01), plan `06.3.2-01`. Not executed at planning time. | Task 1's precondition halts. Without it only the total query duration is available, which degrades per-node derivation to aggregate-only and makes the inner-budget decomposition impossible. Do not substitute the total. |
| Notice tag 19 / `NOTICE_CODE_RETRIEVAL_FAILED`, and `WorkflowCompletedEvent.partial_snapshot` | **06.3.1** plans `06.3.1-01` and `06.3.1-04` (D-28, D-33) | Without them a retrieval node death is again invisible on the wire and censored observations cannot be attributed to a failed node rather than a slow one. Record the degradation if absent. |
| `WorkflowMetadata.graph_prompt_fact_count` | **06.3.1** plan `06.3.1-04` (D-06) | Not required by this plan; noted because the record's graph section reads more honestly with it. |
| Graph-node OTel span retention making `graph_traversal` and `embedding_request` usable | **06.3.1** plan `06.3.1-03` (D-08) | This is the corroborating instrument for the two inner budgets. If absent, task 2 carries the arm-difference decomposition alone and must state the weaker evidence explicitly. |
| Graph population counts, degree distribution, traversal result | **06.3.3-02** (D-05), this phase, wave 1 | Task 1's precondition halts without the baseline record. A zero-count verdict reclassifies the graph finding as ingest and suspends the graph-yield question. |
| Measured p95 and p99 per node | **This phase's own measurement pass** (task 1, ROADMAP SC-2) | Not available at planning time; every criterion here is procedural for that reason. |
| The set of nodes that blew a budget | Unknown until the pass runs (ROADMAP SC-5) | Task 3's disposition set is sized by the data, not pre-enumerated. Every node gets a row either way. |
| The multiplier used for derivation | Committed in `thresholds.py` by **06.3.3-01** before this pass runs (D-13, D-14, D-15) | If absent, derivation refuses. Do not substitute a default. |
| Whether graph traversal is inherently too slow for any sane budget | Unknown until measured (D-04, ROADMAP SC-7) | Task 3 records the answer and, if slow, the latency cost figure the documentation phase cites. |
| Whether the collapse was mis-sized budgets or progressive exhaustion | The mid-run-restart discriminator (D-18, D-21, ROADMAP SC-3) | An inconclusive verdict is a legitimate outcome; record it as such rather than forcing a side. |
| Actual spend for the pass | Recorded during the pass. The original `$0.0199` is **not** a usable estimate — it was cheap only because 966 queries generated nothing (D-47). | Never seed a cap or an expectation from that figure. |
| The preflight canary that locks these budgets in | **06.3.2** plan `06.3.2-03` (D-12), with the environment-aware reader from **06.3.3-01** | Without the environment-aware reader the canary compares against the committed file while the engine runs raised budgets, and every canary fails. Confirm 06.3.3-01 landed that extension before running the pass. |
| Whether `reformulate_timeout_ms` should be derived at all | **Open.** ROADMAP SC-6 names six budgets and does not name it. | Measure and report it in the census. Do not derive or rewrite it without an explicit decision. |
| Whether the harness read timeout must be raised for this pass | Computed in task 1 from the raised budget set | `gateway_timeout_secs` defaults to 300 s. If the raised worst-case sum exceeds it, the harness becomes a second censoring ceiling. Record both ceilings whatever the outcome. |
</content>
