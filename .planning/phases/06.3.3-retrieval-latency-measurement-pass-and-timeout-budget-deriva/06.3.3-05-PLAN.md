---
phase: 06.3.3
plan: 05
type: execute
wave: 4
depends_on: [06.3.3-04]
gap_closure: true
requirements: [OBS-05]
files_modified:
  - eval/src/lancet_eval/latency.py
  - eval/src/lancet_eval/measure.py
  - eval/src/lancet_eval/decay.py
  - eval/tests/conftest.py
  - eval/tests/test_latency.py
  - eval/tests/test_measure.py
  - eval/tests/test_decay.py
autonomous: true

estimate:
  tokens: 75000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A percentile derived over any node's observation set that contains censoring is never returned or recorded as if it were the true value: the censored count reaching `percentile_with_ci` for a node equals that node's at-ceiling drops PLUS its `CENSORED_NODE_TIMEOUT` count, so `is_lower_bound` becomes `True` and `ProposedBudgetRecord.censored_status` reads `censored_lower_bound(n)` rather than `clean`. Proven by a NEW fixture that drives genuinely ceiling-clipped and node-timer-expired observations through the derivation — not by re-running the 320-record clean dataset, which cannot distinguish a fixed instrument from the broken one (VERIFICATION.md gap 1)."
    - "Duration extraction is ceiling-aware by construction: `extract_node_durations` cannot return a bare `dict[str, list[float]]` that a caller can silently derive from without seeing the censoring count, because its return type carries both the surviving durations and the per-node at-ceiling drop count together. A future caller cannot repeat the gap-1 mistake by omitting an optional argument (VERIFICATION.md gap 1, `missing[0]`)."
    - "`decay.py`'s ordinal-to-latency pairing is positionally sound for every record disposition, not only for the two the current branches handle: a record that has neither a matching `error_kind == 1` timeout failure nor a completed timing for the analyzed node — a non-timeout node failure, an upstream failure that prevented the node from running, or an empty-timings transport exception — no longer desynchronizes every subsequent pair. Ordinals and latencies are built as ONE parallel structure appended only when a usable latency exists (VERIFICATION.md gap 2, `missing[0]` Option A), which structurally eliminates the hazard for the timeout branch as well, where `ordinals.append` also ran before its `continue`."
    - "The restart-ordinal boundary check still measures the boundary of the OBSERVED record set rather than of the surviving-latency subset, so filtering records out of the trend cannot narrow the accepted restart-ordinal range and manufacture a spurious `DecayAnalysisError` for an in-range restart."
    - "Every record dropped from the trend for a reason other than a classified node timeout is counted and reported on the verdict, so a future analysis over messier failure data reports how much it discarded instead of silently analysing a shrunken sample."
  artifacts:
    - "eval/src/lancet_eval/latency.py — `NodeDurationExtraction` dataclass and the ceiling-aware `extract_node_durations(records, ceilings_by_node)` that populates it"
    - "eval/src/lancet_eval/measure.py — `derive_budgets_from_records(...)`, the post-drive derivation block extracted from `run_measurement_pass` as a pure function so the censoring wiring is unit-testable without a live client"
    - "eval/src/lancet_eval/decay.py — `analyze_decay`'s single parallel `(ordinal, latency)` accumulation replacing the two independently-conditioned lists; `DecayVerdict.unusable_dropped_count`"
    - "eval/tests/conftest.py — `ceiling_censored_measurement_records` fixture returning MeasurementRecord OBJECTS (not dicts) that actually exercise the classifier"
    - "eval/tests/test_latency.py, eval/tests/test_measure.py, eval/tests/test_decay.py — the new regression tests named in each task's acceptance criteria"
  key_links:
    - "`compute_censoring_census`'s per-node `CENSORED_NODE_TIMEOUT` count and `extract_node_durations`'s per-node at-ceiling drop count are summed into the single `censored_count` argument of `percentile_with_ci` for that node, both computed against the SAME `ceilings` map built from `read_effective_workflow_config()` — this is the join that VERIFICATION.md recorded as NOT_WIRED"
    - "`derive_budget` reads `PercentileResult.is_lower_bound` to compute `censored_status` (latency.py:354-358), so wiring the count into `percentile_with_ci` is what makes the recorded budget honest end to end; no change to `derive_budget` is needed or permitted"
    - "`analyze_decay`'s unconditional full-ordinal list feeds the restart-boundary check at decay.py:531-532 while the filtered paired list feeds `compute_theil_sen_trend` — two different consumers, two different lists, which is why one list cannot serve both"
  prohibitions:
    - "No re-run of the existing clean 320-record dataset may be presented as evidence that gap 1 or gap 2 is closed; both bugs are invisible on that data by VERIFICATION.md's own finding, so a passing re-run proves nothing"
    - "No new Python dependency; `uv.lock` and `eval/pyproject.toml` unchanged; statistics stay stdlib-only per plan-01's standing prohibition"
    - "No modification of `eval/runs/2026-09-03-multihop_rag/` or `eval/runs/2026-09-06-measure-multihop_rag/` — both are committed forensic evidence"
    - "No edit to the already-executed `06.3.3-01..04-PLAN.md` or `-SUMMARY.md` files, including the orphaned LAT-0x citations noted in the objective"
    - "No fixture that returns plain dicts may be used to exercise the censoring classifier or the duration extractor — both read their inputs with `getattr`, so a dict silently yields the default and produces a green test that proves nothing"
---

<objective>
Close VERIFICATION.md gaps 1 and 2: the two instrument-correctness defects in the reusable Python
analysis library this phase built. Both are live defects in shipped code that this phase's own
must_haves claimed as properties, and both are invisible on the dataset this phase measured.

Purpose: the shipped budget values are not corrupted — 06.3.3-03's pass had zero node-level
censoring (320/320 OBSERVED on every node) and its derivation table was hand-reconstructed via
`drive_measurement_pass.py`. But `measure.py`/`latency.py`/`decay.py` are what 06.3.4 and every
future re-measurement will actually call, and as shipped they will silently report a censored
percentile as clean and silently mis-pair a decay trend. The 2026-09-03 collapse — 904 of 1000
retrieval observations sitting exactly at the 10000 ms `retrieve_timeout_ms` ceiling and being read
as "the p95 of retrieval" — is precisely the failure this instrument exists to make impossible, and
as shipped it would not catch it.

Output: a censoring-aware duration extractor whose return type makes the count impossible to ignore,
a pure derivation function extracted from the live driver so the wiring is unit-testable, a decay
analysis that pairs ordinals to latencies structurally rather than positionally, and NEW fixtures
that actually drive the previously-unexercised paths.

**Why no tracer task.** TRACER_MODE is on by default, and this plan opts out deliberately. A tracer
is the thinnest path through every layer a phase will modify, built first so an architectural
dead-end surfaces after one commit. These are three independent, already-diagnosed defects in code
that is already shipped, already wired end to end, and already has a proven verify command. There is
no shared spine to trace and no architecture at risk — a tracer here would be ceremony that adds a
commit without adding information. The tasks are ordered by data dependency instead (extractor
before its consumer), which is the only ordering constraint that exists.

**Noted, not tasked (recorded so the next reviewer sees a decision rather than a miss):**
- **WR-01** (`measure.py:426-473` — `query_embedding_timeout_ms` and `graph_operation_timeout_ms`
  are passed through from effective config rather than measured, and are reported in
  `proposed_budgets` without a `rule` tag distinguishing them from the four genuinely derived
  values) — **deferred**: code-quality/reporting clarity, not a must_have failure. It lives inside
  the same function task 2 edits, and folding it in would widen a closure round into a reporting
  redesign.
- **WR-02** (`measure.py:486-489` — the `calibration_note` string is emitted on every pass, including
  a production measurement pass, contradicting the CLI's own docstring) — **deferred** for the same
  reason: documentation-string accuracy, not a broken behavior, and same-function scope creep.
- **Orphaned `LAT-01`..`LAT-04` requirement IDs** in `06.3.3-01-SUMMARY.md:60` and
  `06.3.3-02-SUMMARY.md:32` — ad-hoc labels with no REQUIREMENTS.md, ROADMAP.md, or PLAN-frontmatter
  backing. VERIFICATION.md flags this as a WARNING, not a blocker. These live in committed execution
  records of already-executed plans; per this phase family's D-46 "does not reopen prior work"
  lineage, they are **left untouched**. Recorded here only so the finding stays visible.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-CONTEXT.md
@.planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-VERIFICATION.md
@.planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-REVIEW.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make duration extraction ceiling-aware by construction (gap 1, producer half)</name>
  <files>eval/src/lancet_eval/latency.py, eval/tests/conftest.py, eval/tests/test_latency.py</files>
  <reversibility rating="reversible">A return-type change on one pure function with exactly two call sites in the repository; plain git revert, no data migration and no published contract.</reversibility>
  <read_first>
    - `eval/src/lancet_eval/latency.py` in full, live — do not trust the line numbers below as static.
      In particular: `WORKFLOW_NODE_ORDER` (~line 21), `CensoringLabel` (~line 30),
      `classify_node_observation` (~line 53), `CensoringCensus` and `compute_censoring_census`
      (~lines 133-171), and `extract_node_durations` (~lines 174-185) which is the function this
      task changes.
    - `eval/src/lancet_eval/latency.py` `PercentileResult` (~line 188) and `percentile_with_ci`
      (~line 203). Note that the parameters needed to close gap 1 ALREADY EXIST — `censored_count`,
      `refuse_on_censored`, `is_lower_bound` — and that every non-empty return path already sets
      `is_lower_bound=censored_count > 0`. Do not add new parameters to this function; the defect is
      that no caller supplies the argument, not that the function lacks the capability.
    - `eval/tests/conftest.py` in full. Note that `censored_2026_09_03_sample` returns plain **dicts**
      and `synthetic_all_at_ceiling_latencies` returns plain **floats**. Neither can exercise
      `classify_node_observation` or `extract_node_durations`, because both read their inputs with
      `getattr`, and `getattr` on a dict returns the default rather than the mapping value. This is
      why a new fixture is required rather than a reuse of either.
    - `eval/src/lancet_eval/journal.py:20-26` — `NodeTiming(node_name, duration_ms)`, and lines
      ~44-67 for the base record's `node_timings` / `node_failures` / `notices` fields.
    - `eval/src/lancet_eval/client.py:80-88` — `NodeFailed(node_name, error_kind, error_message,
      retryable)`; and `client.py:33-41` — `Notice(code, message, severity, typed_code)`.
    - `eval/src/lancet_eval/measure.py` — the `MeasurementRecord` class definition, for the exact
      required field set the new fixture must supply.
    - `eval/tests/test_measure.py:62-75` (`test_reader_parses_with_measurement_record_type`) — the
      second and only other call site of `extract_node_durations` in the repository. Task 2 updates
      it; note here that it exists so the return-type change is understood as a two-call-site change.
    - `eval/tests/test_latency.py` — existing test naming and import conventions to match.
  </read_first>
  <behavior>
    - Given records and a `ceilings_by_node` map, a duration greater than or equal to that node's
      ceiling is EXCLUDED from the surviving duration list for that node and counted once in that
      node's at-ceiling drop count.
    - A duration strictly below the ceiling survives into the duration list and is not counted as a
      drop.
    - Boundary: a duration exactly equal to the ceiling is a drop, not a surviving tail sample —
      matching `classify_node_observation`'s existing `dur >= ceiling_ms` rule so the two functions
      cannot disagree about the same observation.
    - A node absent from `ceilings_by_node` has an effectively unbounded ceiling: every duration
      survives and its drop count is zero.
    - The at-ceiling drop count for a node equals that node's `CENSORED_AT_CEILING` count in
      `compute_censoring_census` when both are given the same records and the same ceilings map.
    - A node-timer expiry — a `node_failures` entry with `error_kind == 1` and NO `node_timings`
      entry for that node — contributes NOTHING to the surviving durations and NOTHING to the
      at-ceiling drop count, because there is no duration to drop. It is visible only through the
      census's `CENSORED_NODE_TIMEOUT` label. This is the reason task 2 must sum two sources.
  </behavior>
  <action>
    Change `extract_node_durations` in `eval/src/lancet_eval/latency.py` from
    `extract_node_durations(records: Sequence[Any]) -> dict[str, list[float]]` to
    `extract_node_durations(records: Sequence[Any], ceilings_by_node: dict[str, float] | None = None)`
    returning a new frozen dataclass `NodeDurationExtraction`, declared immediately above it, with
    exactly two fields: `by_node: dict[str, list[float]]` (the surviving durations, keyed by node
    name, initialised for every entry of `WORKFLOW_NODE_ORDER` exactly as the current implementation
    does) and `dropped_at_ceiling: dict[str, int]` (the per-node count of durations excluded for
    sitting at or above the ceiling, likewise initialised to zero for every node in
    `WORKFLOW_NODE_ORDER`).

    Return the dataclass rather than keeping the bare `dict` return with an extra out-parameter. The
    point is structural: VERIFICATION.md's gap 1 is that a caller obtained clean-looking durations
    and derived from them without ever consulting the census. If the only way to obtain durations is
    to receive the drop count in the same object, no future caller can repeat that mistake by
    forgetting an optional argument. Keep `ceilings_by_node` defaulted to `None` (meaning unbounded
    for every node) so the change is a return-type change only for the existing test call site.

    Inside the loop, keep the current traversal shape — iterate records, iterate
    `getattr(rec, "node_timings", []) or []`, read `node_name` and `duration_ms` with `getattr`, skip
    a name not in the results mapping and skip a `None` duration. Add exactly one branch: resolve
    `ceiling = (ceilings_by_node or {}).get(name, float("inf"))` and, when the float duration is
    greater than or equal to that ceiling, increment `dropped_at_ceiling[name]` and do not append;
    otherwise append as today. Use `>=`, not `>`, so this function and `classify_node_observation`
    (which already uses `dur >= ceiling_ms`) classify the identical observation identically — a
    strict comparison here would put the two counts permanently one apart at the boundary and break
    the cross-consistency property task 2 asserts.

    Add the fixture `ceiling_censored_measurement_records` to `eval/tests/conftest.py`. It must
    return a list of `MeasurementRecord` OBJECTS, imported from `lancet_eval.measure`, populated with
    `NodeTiming` objects from `lancet_eval.journal` and `NodeFailed` objects from
    `lancet_eval.client`. It must NOT return dicts: `classify_node_observation` and
    `extract_node_durations` both read their inputs with `getattr`, so a dict yields the default on
    every access and the record silently classifies as having no timings at all — a green test that
    proves nothing. Mirror the real 904-of-1000 shape recorded in the existing
    `censored_2026_09_03_sample` docstring at conftest.py:26-34, scaled down: build 50 records
    carrying a `RetrieveHybrid` timing, of which 40 have `duration_ms=10000.0` (exactly the
    `retrieve_timeout_ms` ceiling of that run, so the boundary case is the majority case) and 10 have
    genuinely observed sub-ceiling values spread below it; then 5 further records that carry NO
    `RetrieveHybrid` timing and instead carry a `NodeFailed(node_name="RetrieveHybrid",
    error_kind=1, ...)`, so the node-timer-expiry path is present in the same fixture and the
    two-source arithmetic in task 2 has data to exercise. Give every record a distinct sequential
    `ordinal` starting at 1, a `segment` of `"segment-1"`, a `graph_arm`, a `question_type` and the
    remaining fields `MeasurementRecord` requires. Expose the ceiling the fixture was built against
    so tests do not restate the literal independently — either return it alongside the records or
    add a sibling fixture supplying the `{"RetrieveHybrid": 10000.0}` ceilings map.

    Add the tests named in the acceptance criteria to `eval/tests/test_latency.py`.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_latency.py -q</automated>
    <fails_when>pytest exits non-zero, or its summary line reports any count under "failed" or "error" for the newly named tests below.</fails_when>
    <automated>uv run --project eval ruff check --preview eval/src eval/tests</automated>
    <fails_when>ruff exits non-zero and prints one or more diagnostic lines of the form `path:line:col:`.</fails_when>
  </verify>
  <acceptance_criteria>
    - `NodeDurationExtraction` exists in `eval/src/lancet_eval/latency.py` with fields `by_node` and
      `dropped_at_ceiling`, and `extract_node_durations` returns it. `uv run --project eval python -c
      "from lancet_eval.latency import NodeDurationExtraction, extract_node_durations; import
      inspect; assert 'ceilings_by_node' in inspect.signature(extract_node_durations).parameters;
      print('extract-signature-ok')"` prints `extract-signature-ok`.
    - `test_extract_durations_drops_and_counts_at_ceiling_values` asserts that, given the
      `ceiling_censored_measurement_records` fixture and its ceilings map,
      `result.by_node["RetrieveHybrid"]` has length 10, `result.dropped_at_ceiling["RetrieveHybrid"]`
      equals 40, and every surviving value is strictly less than the ceiling.
    - `test_extract_durations_ceiling_boundary_is_a_drop` asserts a record whose duration equals the
      ceiling exactly is counted in `dropped_at_ceiling` and absent from `by_node`, and that a
      duration one millisecond below the ceiling is present in `by_node` and not counted.
    - `test_extract_durations_without_ceilings_drops_nothing` asserts that calling with
      `ceilings_by_node=None` over the same fixture returns 50 surviving `RetrieveHybrid` values and
      a `dropped_at_ceiling` of 0 for every node — proving the default is genuinely unbounded and the
      change is opt-in for the existing call site.
    - `test_extract_drop_count_matches_census_at_ceiling_count` asserts
      `extract_node_durations(records, ceilings).dropped_at_ceiling["RetrieveHybrid"]` equals
      `compute_censoring_census(records, ceilings).node_counts["RetrieveHybrid"]["CENSORED_AT_CEILING"]`
      for the shared fixture — the cross-consistency guard that both functions read the same ceilings
      map with the same comparison operator.
    - `test_extract_durations_ignores_node_timer_expiry` asserts that the 5 fixture records carrying
      a `NodeFailed` with `error_kind == 1` and no timing contribute 0 to
      `by_node["RetrieveHybrid"]` and 0 to `dropped_at_ceiling["RetrieveHybrid"]`, while
      `compute_censoring_census(...).node_counts["RetrieveHybrid"]["CENSORED_NODE_TIMEOUT"]` equals 5
      — establishing in a test that the two censoring sources are disjoint and both are needed.
    - The `ceiling_censored_measurement_records` fixture returns objects, not mappings:
      `test_censored_fixture_yields_record_objects` asserts
      `not isinstance(records[0], dict)` and that `getattr(records[0], "node_timings")` is a non-empty
      list — the explicit guard against the conftest.py:44-57 dict-shaped fixture pattern.
    - `git diff --stat -- uv.lock eval/pyproject.toml` reports no change.
  </acceptance_criteria>
  <done>
    Duration extraction returns surviving durations and the per-node at-ceiling drop count together
    in one object, the drop count provably agrees with the census's own at-ceiling label, and a
    fixture of record objects exists that actually drives ceiling-clipped and node-timer-expired
    observations through the classifier.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the censored count into the percentile call and make the derivation testable (gap 1, consumer half)</name>
  <files>eval/src/lancet_eval/measure.py, eval/tests/test_measure.py</files>
  <reversibility rating="reversible">Extracts an existing inline block into a named pure function and adds one keyword argument to an existing call; no schema, no persisted format and no external contract changes.</reversibility>
  <read_first>
    - `eval/src/lancet_eval/measure.py` from `def run_measurement_pass` (~line 283) through the end of
      the summary construction (~line 520), live. The block this task extracts begins at the
      `# Post-drive analysis and summary record` comment (~line 421) and runs through the
      `check_harness_ceilings` call (~line 478). Note specifically: `census` and `durations` are
      computed on consecutive lines (~437-438) and never joined, and the `percentile_with_ci(vals,
      p=..., seed=...)` call inside the `for node, vals in durations.items()` loop (~line 457)
      supplies no censored count — this is the exact NOT_WIRED link VERIFICATION.md recorded.
    - `eval/src/lancet_eval/measure.py` imports at the top, including `extract_node_durations`
      (~line 30) — task 1 changed its return type, so this import site is the caller that must adapt.
    - `eval/src/lancet_eval/latency.py` as landed by task 1: `NodeDurationExtraction`,
      `CensoringLabel`, `compute_censoring_census`, `percentile_with_ci`, `derive_budget`
      (~line 329) and `derive_provider_contract_budget`.
    - `eval/src/lancet_eval/latency.py:352-358` specifically — `derive_budget` already computes
      `censored_status` as `f"censored_lower_bound({percentile_res.censored_count})"` when
      `percentile_res.is_lower_bound` is set, and `"clean"` otherwise. `derive_budget` needs NO
      change; supplying the count upstream is what makes its output honest.
    - `eval/tests/test_measure.py:62-75` — `test_reader_parses_with_measurement_record_type`, which
      calls `extract_node_durations([rec])` and asserts on a bare dict. This assertion must be
      updated to read `.by_node` after task 1's return-type change.
    - `eval/tests/conftest.py` as landed by task 1 — the `ceiling_censored_measurement_records`
      fixture and its ceilings map.
    - `eval/src/lancet_eval/thresholds.py` — `DecisionThresholds` and `COMMITTED_THRESHOLDS`, for the
      `thresholds` parameter of the extracted function.
  </read_first>
  <behavior>
    - Given a record set with censoring for a node, the derivation for that node produces a
      `ProposedBudgetRecord` whose `censored_status` names a censored lower bound and reports a
      non-zero count, never `clean`.
    - The count supplied for a node is the SUM of that node's at-ceiling drops and that node's
      `CENSORED_NODE_TIMEOUT` census count. Supplying only one of the two leaves the other class of
      censoring reading as clean.
    - The percentile a censored node derives from is computed over surviving durations only, so the
      reported value is genuinely a lower bound on the true percentile rather than a value pulled
      upward by clipped observations.
    - Given a record set with no censoring at all, the derived record's `censored_status` is `clean`
      and the proposed values are byte-identical to what the pre-change code produced — the fix does
      not perturb the clean path.
    - The derivation runs as a pure function of records, effective configuration, thresholds and the
      two harness ceilings: it issues no query, constructs no client and reads no environment.
  </behavior>
  <action>
    Extract the post-drive analysis block out of `run_measurement_pass` in
    `eval/src/lancet_eval/measure.py` into a new module-level pure function:

    `derive_budgets_from_records(analysis_records, effective_cfg, thresholds, sse_read_timeout_s,
    question_deadline_s)` returning a frozen dataclass `BudgetDerivation` carrying, as fields:
    `census`, `durations` (the `NodeDurationExtraction` from task 1), `censored_by_node`
    (`dict[str, int]`, the summed count actually supplied per node), `proposed_budgets`
    (`dict[str, int]`), `proposed_records` (`list[dict[str, Any]]`), `nesting_report` and
    `ceiling_report`. Move the `node_to_key` mapping, the `ceilings` construction, the census call,
    the duration extraction, the per-node derivation loop, the two inner-budget pass-through
    defaults, and the `check_nesting_invariants` / `check_harness_ceilings` calls into it verbatim
    except for the changes below. `run_measurement_pass` then calls it once and reads its fields when
    building `summary`, so every existing key of `summary` keeps its current value and shape.

    Extract rather than patch in place because the defect is currently only reachable through a
    function that drives live queries against a gateway. A test of the wiring must otherwise mock a
    client, a corpus loader and a spend reader to reach three lines of arithmetic. Extracting makes
    the previously-unexercised path directly callable, which is what makes the gap-1 regression test
    honest instead of ceremonial.

    Inside the extracted function make exactly two changes to behavior.

    First, pass the ceilings map into the extractor: `extraction = extract_node_durations(
    analysis_records, ceilings_by_node=ceilings)`, and iterate `extraction.by_node.items()` where the
    loop currently iterates `durations.items()`. The surviving durations are now the only values the
    percentile sees, so a clipped observation can no longer pull the percentile upward and present
    itself as a genuine tail sample.

    Second, compute the per-node censored total and supply it. For each node reaching the
    `percentile_with_ci` call, compute
    `censored = extraction.dropped_at_ceiling.get(node, 0) + census.node_counts.get(node,
    {}).get(CensoringLabel.CENSORED_NODE_TIMEOUT.value, 0)` and pass it as
    `percentile_with_ci(vals, p=thresholds.derivation_percentile, seed=thresholds.bootstrap_seed,
    censored_count=censored)`. Record the same value in `censored_by_node[node]` on the returned
    dataclass so the run record can report it.

    Both addends are required and neither is sufficient. `dropped_at_ceiling` covers only the
    observations that HAVE a duration at or above the ceiling. A node killed by its own timer has no
    duration at all — `NodeFailedEvent` carries no duration field — so it never enters the extractor
    and is visible only as the census's `CENSORED_NODE_TIMEOUT` label. Supplying only the drop count
    would leave every node-timer expiry reading as clean, which is half of gap 1 left open; supplying
    only the census label would leave every clipped duration reading as clean, which is the other
    half. Build both against the SAME `ceilings` map that the census call already uses — a second,
    independently-constructed map is how the two counts silently drift apart.

    Do not set `refuse_on_percentile` or enable `refuse_on_censored`. The must_have this closes reads
    "refused OR returned explicitly flagged"; the flagged path is the one already implemented — every
    non-empty return of `percentile_with_ci` sets `is_lower_bound=censored_count > 0`, and
    `derive_budget` already turns that into `censored_status`. Refusing outright would make a
    censored node produce no budget row at all, which is a behavior change beyond the gap and would
    break the existing derivation flow. Leave `derive_budget` and `percentile_with_ci` themselves
    untouched.

    Update `eval/tests/test_measure.py`'s `test_reader_parses_with_measurement_record_type` to read
    `extract_node_durations([rec]).by_node["RetrieveHybrid"]` — the only other call site in the
    repository and a mechanical change, not a weakening of the assertion.

    Add the tests named in the acceptance criteria to `eval/tests/test_measure.py`, driving the
    `ceiling_censored_measurement_records` fixture from task 1 through
    `derive_budgets_from_records` directly. A test that instead re-runs the recorded 320-record
    dataset from `eval/runs/2026-09-06-measure-multihop_rag/` does NOT close this gap and must not be
    written as if it did: VERIFICATION.md establishes that node-level censoring was 0/320 for every
    node in that pass, so it returns identical output before and after this change.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_measure.py eval/tests/test_latency.py -q</automated>
    <fails_when>pytest exits non-zero, or its summary line reports any count under "failed" or "error" for the newly named tests below.</fails_when>
    <automated>uv run --project eval ruff check --preview eval/src eval/tests</automated>
    <fails_when>ruff exits non-zero and prints one or more diagnostic lines of the form `path:line:col:`.</fails_when>
  </verify>
  <acceptance_criteria>
    - `derive_budgets_from_records` is importable and callable with no live client and no network:
      `uv run --project eval python -c "from lancet_eval.measure import
      derive_budgets_from_records, BudgetDerivation; print('derivation-importable')"` prints
      `derivation-importable`.
    - `test_derivation_flags_ceiling_censored_node_as_lower_bound` calls
      `derive_budgets_from_records` with the `ceiling_censored_measurement_records` fixture and its
      ceilings map, and asserts the `RetrieveHybrid` entry of `proposed_records` has a
      `censored_status` that is not the string `clean` and that reports a non-zero count. This is the
      direct assertion that VERIFICATION.md gap 1's observable property is restored.
    - `test_derivation_censored_count_sums_ceiling_drops_and_timer_expiries` asserts
      `result.censored_by_node["RetrieveHybrid"]` equals 45 for the task-1 fixture — 40 at-ceiling
      drops plus 5 node-timer expiries — proving both sources are summed rather than one being used
      alone.
    - `test_derivation_percentile_excludes_clipped_observations` asserts the `RetrieveHybrid`
      `percentile_value_ms` in `proposed_records` is strictly less than the fixture's 10000.0 ceiling,
      proving clipped values no longer enter the percentile computation.
    - `test_derivation_clean_records_report_clean_status` builds a record set with every duration
      strictly below its ceiling and no failures, and asserts every entry of `proposed_records` has
      `censored_status == "clean"` and that `censored_by_node` is zero for every node — proving the
      clean path is unperturbed and that the flag is not now stuck on.
    - `test_derivation_is_pure_and_needs_no_client` asserts `derive_budgets_from_records` completes
      with no gateway running and no `OPENROUTER_API_KEY` set, using only in-memory records.
    - `run_measurement_pass`'s `summary` dictionary retains every key it has today —
      `calibration_note`, `corpus`, `sample_size_questions`, `total_records_emitted`,
      `measured_records`, `raised_budgets`, `sse_read_timeout_s`, `question_deadline_s`,
      `spend_summary`, `censoring_census`, `proposed_budgets`, `nesting_report` — verified by a
      test asserting the key set is a superset of that list.
    - `uv run --project eval pytest eval/tests -q` exits 0 offline with no gateway, no network and no
      `OPENROUTER_API_KEY`, and reports no new failure relative to the single pre-existing unrelated
      failure recorded in `06.3.3-VERIFICATION.md:112`.
    - `git status --short eval/runs/` reports no modification to any committed run directory.
  </acceptance_criteria>
  <done>
    A percentile derived over a node with clipped or timer-expired observations is recorded as a
    censored lower bound carrying the summed count of both censoring sources, the derivation runs as
    a pure function testable without a live stack, and a clean record set still derives identically
    to before.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pair ordinals to latencies structurally in decay.py (gap 2)</name>
  <files>eval/src/lancet_eval/decay.py, eval/tests/test_decay.py</files>
  <reversibility rating="reversible">Local restructuring of one loop plus a defaulted dataclass field; no persisted format and no caller signature changes.</reversibility>
  <read_first>
    - `eval/src/lancet_eval/decay.py` `analyze_decay` (~line 425) in full, live. The loop this task
      restructures runs from `for r in sorted_records:` (~line 489) to the segment accumulation
      (~line 521), and its consumers are the restart-boundary check (~lines 531-532) and the
      `compute_theil_sen_trend` call (~lines 547-552).
    - `eval/src/lancet_eval/decay.py` `DecayVerdict` (~line 81) — note the trailing defaulted fields
      `stratified_results` and `provenance`, and that both construction sites (the empty-set early
      return ~line 463 and the final return ~line 584) pass every field by keyword, so an added
      defaulted field is safe.
    - `eval/src/lancet_eval/decay.py` `compute_theil_sen_trend` (~line 141) — confirm directly that
      it consumes `ordinals[j] - ordinals[i]` and `latencies[j] - latencies[i]` at the SAME index
      pair (~lines 184-186 and ~line 200). This is what makes the pairing positional and is why a
      one-element desync corrupts every subsequent pair rather than degrading gracefully.
    - `eval/src/lancet_eval/decay.py` `validate_and_sort_records` (~line 103) — the ordinal
      uniqueness and gap checks that run BEFORE this loop, so the loop can assume sorted, unique,
      gap-free ordinals over the full record set.
    - `eval/tests/test_decay.py` — existing test naming, the `MeasurementRecord` / `NodeTiming`
      import convention at lines 1-14, and the record-construction shape used by
      `test_ordering_validation_shuffled_lines_accepted`.
    - `eval/src/lancet_eval/client.py:80-88` — `NodeFailed`, for constructing a non-timeout failure
      (`error_kind` of something other than 1) in the new fixture.
  </read_first>
  <behavior>
    - A record with a usable timing for the analyzed node contributes exactly one `(ordinal,
      latency)` pair; the two lists handed to the trend function are therefore the same length by
      construction and cannot be truncated into misalignment.
    - A record whose analyzed node carries a `node_failures` entry with `error_kind == 1` contributes
      no pair, increments the censored count, and does NOT leave a dangling ordinal behind.
    - A record whose analyzed node has neither a matching timeout failure nor a completed timing —
      a non-timeout node failure, an upstream failure that stopped the node from running, or an
      empty-timings transport exception — contributes no pair, increments a separate
      unusable-dropped count, and does NOT leave a dangling ordinal behind.
    - Inserting an unusable record in the MIDDLE of an otherwise sound ascending sequence does not
      change the reported slope, because every remaining record keeps its own true ordinal rather
      than inheriting its neighbour's.
    - The restart-ordinal boundary is still measured over every observed record's ordinal, so a
      dropped first or last record does not narrow the accepted restart range.
    - A record set with zero usable latencies still returns a verdict via the existing
      unavailable/empty paths rather than raising on an unzip of an empty list.
  </behavior>
  <action>
    Restructure the accumulation loop in `analyze_decay` in `eval/src/lancet_eval/decay.py` to build
    ordinals and latencies as ONE parallel structure. This is VERIFICATION.md gap 2's stated Option A,
    and it is required rather than preferred: Option B ("extend censored_count to cover every reason a
    record is dropped") only makes the reporting count honest, it does not remove the positional
    hazard, and the hazard is not confined to the third case. Confirmed by direct source read:
    `ordinals.append(ord_val)` runs unconditionally at the top of the loop body, BEFORE the
    `error_kind == 1` branch performs its `continue` — so the classified-timeout path leaves a
    dangling ordinal too, and `compute_theil_sen_trend(ordinals[: len(latencies)], latencies, ...)`
    pairs a prefix positionally. Any timeout record that is not the last record in sort order already
    desynchronizes every later pair. Only building the pairs together eliminates both instances.

    Replace the two lists `ordinals` and `latencies` with three accumulators declared before the loop:
    `all_ordinals: list[int]` (every observed record's ordinal), `paired: list[tuple[int, float]]`
    (one entry per usable observation) and `unusable_dropped_count: int` starting at zero. Keep
    `censored_count`, `seg1_latencies`, `seg2_latencies`, `seen_segments` and `strata` exactly as
    they are.

    In the loop body, change the unconditional `ordinals.append(ord_val)` to
    `all_ordinals.append(ord_val)` and leave it at the top where it is — the full-ordinal list must
    stay unconditional because the restart-boundary check depends on it. In the timeout branch
    (`f is not None and getattr(f, "error_kind", None) == 1`) keep `censored_count += 1` and the
    `continue`. Change the usable-timing branch so that, instead of `latencies.append(dur)`, it
    appends `(ord_val, dur)` to `paired`; keep the `strata[strata_key].append(dur)` and the
    `seg1_latencies` / `seg2_latencies` appends unchanged, since those are order-independent
    accumulations. Add the branch the current structure lacks: when the record has neither a matching
    timeout failure nor a completed timing with a non-`None` duration, increment
    `unusable_dropped_count` and continue. Today that case simply falls off the end of the loop body
    having already grown the ordinal list — that fall-through is the defect.

    After the loop, derive the two aligned sequences from the single structure:
    `trend_ordinals = [o for o, _ in paired]` and `latencies = [d for _, d in paired]`. Prefer this
    list-comprehension form over `zip(*paired)` because `zip(*[])` raises on an empty pair list, and
    a record set with zero usable observations is a case this function must still return a verdict
    for rather than crash on.

    Change the `compute_theil_sen_trend` call to pass `trend_ordinals` with NO slice — remove the
    `ordinals[: len(latencies)]` prefix expression entirely. The slice exists only to paper over the
    length mismatch the old structure created; with the pairing fixed the two sequences are equal in
    length by construction, and leaving the slice in place would hide a future regression instead of
    surfacing it.

    Change the restart-boundary check to read `all_ordinals[0] if all_ordinals else 0` and
    `all_ordinals[-1] if all_ordinals else 0` where it currently reads `ordinals[0]` / `ordinals[-1]`.
    This matters and is not cosmetic: if that check reads the filtered list, a record dropped at
    either end of the sequence narrows the observed boundary, and an in-range recorded restart
    ordinal then raises `DecayAnalysisError` claiming it lies outside the observed boundary. Fixing
    the pairing without fixing this consumer would trade a silent wrong answer for a spurious hard
    failure.

    Add `unusable_dropped_count: int = 0` to the `DecayVerdict` dataclass, placed among the trailing
    defaulted fields alongside `stratified_results` and `provenance` so no positional construction
    breaks, and pass it at the final return site. Leave the empty-record-set early return to take the
    default of zero. Reporting the drop count separately from `censored_count` keeps the two
    dispositions distinguishable — a classified node timeout is a censored observation with a known
    lower bound, an unusable record is an observation that is simply absent — while making a future
    analysis over messier failure data state how much it discarded.

    Add the tests named in the acceptance criteria to `eval/tests/test_decay.py`. Note that the
    existing recorded pass cannot exercise any of this: `06.3.3-MEASUREMENT.md` section 3 records
    `RetrieveHybrid` at 320/320 OBSERVED with zero drops of any kind, so synthetic records are the
    only way to reach these branches.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_decay.py -q</automated>
    <fails_when>pytest exits non-zero, or its summary line reports any count under "failed" or "error" for the newly named tests below.</fails_when>
    <automated>uv run --project eval ruff check --preview eval/src eval/tests</automated>
    <fails_when>ruff exits non-zero and prints one or more diagnostic lines of the form `path:line:col:`.</fails_when>
  </verify>
  <acceptance_criteria>
    - The prefix slice is gone from the trend call, checked over CODE only so a comment mentioning
      the removed expression cannot fail the gate: `uv run --project eval python -c "import inspect,
      re; from lancet_eval import decay; src = '\n'.join(l for l in
      inspect.getsource(decay.analyze_decay).splitlines() if not l.strip().startswith('#')); assert
      not re.search(r'ordinals\s*\[\s*:\s*len\s*\(', src); print('no-prefix-slice')"` prints
      `no-prefix-slice`. It fails against the pre-change source, so it is a real regression guard
      rather than a pass-by-default. <!-- planner-discipline-allow: ordinals[: len( -->
      Do not leave the removed expression in a trailing (non-`#`-prefixed) inline comment, which the
      filter above cannot strip.
    - `test_decay_mid_sequence_unusable_record_does_not_shift_pairs` builds two record sets over the
      same ascending latency series — one fully usable, one identical except that a record in the
      middle (neither first nor last in sort order) carries a `NodeFailed` with an `error_kind` that
      is NOT 1 and no timing for the analyzed node — and asserts the reported `slope_statistic` of
      the two analyses agree to within a small stated tolerance, and that both report the same
      `verdict_decay_present`. Under the pre-change code the second set's pairs shift by one from the
      drop onward and the slopes diverge.
    - `test_decay_mid_sequence_node_timeout_does_not_shift_pairs` asserts the same invariant for a
      record whose analyzed node carries a `NodeFailed` with `error_kind == 1` placed mid-sequence —
      the instance VERIFICATION.md described as possibly safe and which direct source read shows is
      not, because `ordinals.append` ran before that branch's `continue`.
    - `test_decay_reports_unusable_dropped_count` asserts `DecayVerdict.unusable_dropped_count`
      equals the number of records dropped for a reason other than a classified node timeout, and
      that `censored_count` on the trend result counts only the `error_kind == 1` records — the two
      dispositions stay distinguishable.
    - `test_decay_restart_boundary_uses_full_observed_ordinal_range` builds a record set whose FIRST
      and LAST records are unusable for the analyzed node, supplies a `restart_ordinal` that lies
      inside the full observed ordinal range but outside the surviving-latency range, and asserts
      `analyze_decay` does NOT raise `DecayAnalysisError` and returns a `restart_result` that is not
      `None`.
    - `test_decay_zero_usable_latencies_returns_verdict` asserts a record set in which no record has
      a usable timing for the analyzed node returns a `DecayVerdict` rather than raising, and that
      its trend result is marked unavailable with a non-empty reason.
    - `test_decay_all_usable_records_unchanged` is the clean-path no-regression guard and must be
      SELF-CONTAINED — do not baseline it against values asserted elsewhere in `test_decay.py`, which
      would make the guard circular. Build one record set in which every record has a usable timing
      for the analyzed node and the latencies ascend, then assert three things: the reported
      `slope_statistic` is strictly positive, `verdict_decay_present` is `True`, and analysing the
      identical record set twice returns the identical `slope_statistic` and `window_delta_ms` — the
      bootstrap seed is fixed by `COMMITTED_THRESHOLDS`, so determinism is a real property to pin
      rather than an assumption.
    - `uv run --project eval pytest eval/tests -q` exits 0 offline and reports no new failure relative
      to the single pre-existing unrelated failure recorded in `06.3.3-VERIFICATION.md:112`.
  </acceptance_criteria>
  <done>
    Ordinals and latencies are built as one parallel structure, the trend call receives two sequences
    that are equal in length by construction rather than by slicing, the restart boundary still reads
    the full observed range, and a mid-sequence drop of either kind provably leaves the reported
    slope unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine SSE wire → harness record | Node timings, failures and notices cross from the engine into `MeasurementRecord`; the harness derives production timeout budgets from them. |
| harness derivation → `config/config.toml` | A proposed budget produced by this library becomes a production timeout value in a later phase; a wrong number here is a live production defect. |
| test fixture → analysis function | Synthetic records stand in for a live pass; a fixture shaped so it cannot reach the code under test yields a green result that certifies nothing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.3-05-01 | Information disclosure (of a false measurement) | `derive_budgets_from_records` / `percentile_with_ci` | high | mitigate | Sum both censoring sources into `censored_count`; task 2's `test_derivation_censored_count_sums_ceiling_drops_and_timer_expiries` pins the arithmetic so a partial fix cannot pass. |
| T-06.3.3-05-02 | Tampering (silent data corruption) | `analyze_decay` positional pairing | high | mitigate | Build `(ordinal, latency)` tuples in one structure and remove the prefix slice; `test_decay_mid_sequence_unusable_record_does_not_shift_pairs` fails on the pre-change code. |
| T-06.3.3-05-03 | Repudiation (a test that proves nothing) | `eval/tests/conftest.py` fixtures | medium | mitigate | New fixture returns record OBJECTS; `test_censored_fixture_yields_record_objects` asserts non-dict, because `getattr` on a dict silently returns the default and green-lights the unfixed code. |
| T-06.3.3-05-04 | Denial of service (spurious hard failure) | restart-boundary check | medium | mitigate | Keep `all_ordinals` unconditional so filtering cannot narrow the accepted restart range; `test_decay_restart_boundary_uses_full_observed_ordinal_range` guards it. |
| T-06.3.3-05-05 | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs in this plan. `uv.lock` and `eval/pyproject.toml` are asserted unchanged in task 1's acceptance criteria; the standing no-new-dependency prohibition from plan-01 carries forward. No `[ASSUMED]`/`[SUS]` package is introduced, so no legitimacy checkpoint applies. |
</threat_model>

<verification>
- `uv run --project eval pytest eval/tests -q` exits 0 offline (no gateway, no network, no
  `OPENROUTER_API_KEY`), with no new failure relative to the single pre-existing unrelated failure
  recorded at `06.3.3-VERIFICATION.md:112`.
- `uv run --project eval ruff check --preview eval/src eval/tests` exits 0.
- `uv lock --project eval --check` exits 0 and `git diff --stat -- uv.lock eval/pyproject.toml`
  reports no change.
- `git status --short eval/runs/` reports no modification to any committed run directory.
- Every test named in the three tasks' acceptance criteria exists and passes.
</verification>

<success_criteria>
- VERIFICATION.md gap 1 is closed: a percentile over a node with at-ceiling or timer-expired
  observations is recorded as a censored lower bound carrying the summed count, proven by a NEW
  fixture that drives both censoring kinds — not by re-running the clean 320-record dataset.
- VERIFICATION.md gap 2 is closed: ordinals and latencies are one parallel structure, the prefix
  slice is gone, the restart boundary reads the full observed range, and a mid-sequence drop of
  either kind provably leaves the slope unchanged.
- The clean path is unperturbed: a record set with no censoring derives the same budgets and reports
  `clean`, and a decay analysis with no drops reports the same slope and verdict as before.
- No new dependency, no committed run directory modified, no already-executed PLAN or SUMMARY edited.
</success_criteria>

<output>
Create `.planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-05-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

Symbols this plan creates or materially changes. The plan-review-convergence source-grounding pass
reads this list to exclude newly-created symbols from drift verification.

**New symbols:**
- `lancet_eval.latency.NodeDurationExtraction` — frozen dataclass, fields `by_node: dict[str, list[float]]`, `dropped_at_ceiling: dict[str, int]`
- `lancet_eval.measure.derive_budgets_from_records(analysis_records, effective_cfg, thresholds, sse_read_timeout_s, question_deadline_s) -> BudgetDerivation`
- `lancet_eval.measure.BudgetDerivation` — frozen dataclass, fields `census`, `durations`, `censored_by_node`, `proposed_budgets`, `proposed_records`, `nesting_report`, `ceiling_report`
- `lancet_eval.decay.DecayVerdict.unusable_dropped_count: int = 0` — new defaulted field
- `eval/tests/conftest.py::ceiling_censored_measurement_records` — new pytest fixture (returns `MeasurementRecord` objects) plus its sibling ceilings-map fixture

**New tests:**
- `test_latency.py`: `test_extract_durations_drops_and_counts_at_ceiling_values`, `test_extract_durations_ceiling_boundary_is_a_drop`, `test_extract_durations_without_ceilings_drops_nothing`, `test_extract_drop_count_matches_census_at_ceiling_count`, `test_extract_durations_ignores_node_timer_expiry`, `test_censored_fixture_yields_record_objects`
- `test_measure.py`: `test_derivation_flags_ceiling_censored_node_as_lower_bound`, `test_derivation_censored_count_sums_ceiling_drops_and_timer_expiries`, `test_derivation_percentile_excludes_clipped_observations`, `test_derivation_clean_records_report_clean_status`, `test_derivation_is_pure_and_needs_no_client`
- `test_decay.py`: `test_decay_mid_sequence_unusable_record_does_not_shift_pairs`, `test_decay_mid_sequence_node_timeout_does_not_shift_pairs`, `test_decay_reports_unusable_dropped_count`, `test_decay_restart_boundary_uses_full_observed_ordinal_range`, `test_decay_zero_usable_latencies_returns_verdict`, `test_decay_all_usable_records_unchanged`

**Materially changed signatures:**
- `lancet_eval.latency.extract_node_durations(records, ceilings_by_node=None) -> NodeDurationExtraction` — was `(records) -> dict[str, list[float]]`; call sites: `measure.py` and `eval/tests/test_measure.py`
- `lancet_eval.measure.run_measurement_pass` — post-drive block delegated to `derive_budgets_from_records`; public signature and every `summary` key unchanged

**Explicitly unchanged (do not edit):** `percentile_with_ci`, `derive_budget`,
`derive_provider_contract_budget`, `compute_censoring_census`, `classify_node_observation`,
`compute_theil_sen_trend`, `validate_and_sort_records`.
