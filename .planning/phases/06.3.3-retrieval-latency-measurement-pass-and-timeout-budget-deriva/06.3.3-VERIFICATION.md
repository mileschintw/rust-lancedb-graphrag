---
phase: 06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva
verified: 2026-09-08T13:15:00Z
status: passed
score: 11/11 must-haves verified
covered_files: [".planning/REQUIREMENTS.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-01-PLAN.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-01-SUMMARY.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-02-PLAN.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-02-SUMMARY.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-03-PLAN.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-03-SUMMARY.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-04-PLAN.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-04-SUMMARY.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-05-PLAN.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-05-SUMMARY.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-06-PLAN.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-06-SUMMARY.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-BUDGETS.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-MEASUREMENT.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-REVIEW-FIX.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-REVIEW.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-STORE-BASELINE.md", "config/config.example.toml", "config/config.toml", "config/config.verify.toml", "engine/src/bin/inspect_lancedb.rs", "engine/src/config.rs", "engine/src/tests.rs", "engine/src/tests/workflow_phase5_production.rs", "eval/scripts/drive_measurement_pass.py", "eval/src/lancet_eval/cli.py", "eval/src/lancet_eval/client.py", "eval/src/lancet_eval/decay.py", "eval/src/lancet_eval/latency.py", "eval/src/lancet_eval/measure.py", "eval/src/lancet_eval/preflight.py", "eval/src/lancet_eval/run.py", "eval/src/lancet_eval/thresholds.py", "eval/tests/conftest.py", "eval/tests/test_decay.py", "eval/tests/test_latency.py", "eval/tests/test_measure.py", "scripts/engine-test-targets.sh"]
covered_digest: "v1:sha256:93fba59abc4ee11cb35ae4408af16e2ce04ac010344dba97bf00b187e06e79c0"
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 10/11
  gaps_closed:
    - "Zero usable observations for a node is reported as an explicit stated-reason outcome carrying the observation count, never as a percentile of zero and never as a silently omitted row (PLAN-01 must_have; REVIEW CR-01)"
  gaps_remaining: []
  regressions: []
advisory:
  - finding: "Verify overlay generation_timeout_secs = 3 so a live provider attempt in (3s, 7s] under LANCET_ENV=verify now fails fast at the provider client instead of racing the 7000 ms GenerateAnswer node timer."
    category: other
    reason: "Carried forward from prior round; WR-04 documentation now applied (config/config.verify.toml and workflow_phase5_production.rs comments state verify-ingestion.sh is ingest/embeddings-only and this is expected). Informational only, not a functional gap."
    evidence_status: "config/config.verify.toml:9-14 comment; workflow_phase5_production.rs:640-641,752-754 comments corrected; named test workflow_phase5_config_verify_generation_timeout passing"
---

# Phase 06.3.3: Retrieval Latency Measurement Pass and Timeout Budget Derivation Verification Report

**Phase Goal:** Establish, by measurement, whether the 2026-09-03 collapse was mis-sized timeout budgets or progressive resource exhaustion — then derive all six production budgets from the observed latency. Closes at the go/no-go for paid spend, so that decision sits on a phase boundary rather than mid-phase.

**Verified:** 2026-09-08T13:15:00Z
**Status:** passed
**Re-verification:** Yes — targeted re-check of T-11 (REVIEW CR-01) on top of a full pass over all previously-verified truths (SC-1..SC-8, SC-2b, SC-6b).

## Goal Achievement

This round independently re-derives T-11's status from current source and a fresh test run in the main checkout — the prior `06.3.3-REVIEW-FIX.md` self-report is explicitly *not* trusted (it itself flags CR-01 "requires human verification" and ran its evidence in a torn-down worktree). Findings:

1. `derive_budgets_from_records` (`eval/src/lancet_eval/measure.py:299-417`) now appends an explicit refused `proposed_records` row (`proposed_ms: None`, `rule: "derivation_refused_empty_or_unavailable"`, `censored_status: "unavailable(<reason>; censored_count=<n>)"`) whenever `percentile_with_ci` returns `is_available=False`, and does **not** add that key to `proposed_budgets`. Verified for **both** fixture shapes named in the prior gap:
   - **Shape (a)** — untimed/empty-survivor node (`ExtractGraphContext`/`AssemblePrompt` on the `ceiling_censored_measurement_records` fixture, which only stamps `RetrieveHybrid`): confirmed via `eval/tests/test_measure.py::test_derivation_refuses_empty_survivors_without_nesting_fill_in` (passing, ran independently in the main checkout).
   - **Shape (b)** — 100%-at-ceiling node (the historical collapse shape): no committed regression test exercises this exact path through `derive_budgets_from_records`, but I independently reproduced it with a standalone script (20 `RetrieveHybrid` records all at the 10000ms ceiling) run against current source — see Behavioral Spot-Checks. Output: `retrieve_timeout_ms` absent from `proposed_budgets`, with a refused row (`rule: derivation_refused_empty_or_unavailable`, `censored_status: "unavailable(Empty observation set: 0 usable observations; censored_count=20)"`). No fabricated `10500` value. **Gap called out explicitly per instructions: this shape has no dedicated named regression test — coverage rests on manual reproduction plus the fact that `check_nesting_invariants`'s two nesting relationships (graph_node and retrieve) use the identical `if <key> in present` guard, so the fix is structurally symmetric, not shape (a)-specific.**
2. `check_nesting_invariants` (`eval/src/lancet_eval/latency.py:448-537`) now computes `present = set(budgets)` and gates **both** nesting relationships (`graph_node_timeout_ms` outer at line 471, `retrieve_timeout_ms` outer at line 504) behind `if <outer_key> in present`. A missing outer key is skipped entirely — no group is appended and `resolved.get(..., 0)` defaulting is gone from the outer-lookup path (only used for *inner* keys, which are legitimately pass-through-carried and always present via the two post-loop `if key not in proposed_budgets` blocks). Repo-wide grep for both symbol names (`derive_budgets_from_records`, `check_nesting_invariants`) across `eval/src/`, `eval/scripts/`, and every non-test file confirms exactly one call site each: `check_nesting_invariants` is called only from inside `derive_budgets_from_records` (measure.py:401), and `derive_budgets_from_records` is called only from `run_measurement_pass` (measure.py:561). `cli.py`, `preflight.py`, and `drive_measurement_pass.py` do not call either function and have no independent `.get(key, 0)` nesting-fill logic of their own — there is no bypass path.
3. Named tests re-run independently in the main checkout (not the torn-down worktree cited in REVIEW-FIX.md): `eval/tests/test_measure.py::test_derivation_refuses_empty_survivors_without_nesting_fill_in`, `test_derivation_flags_ceiling_censored_node_as_lower_bound`, `test_derivation_censored_count_sums_ceiling_drops_and_timer_expiries`, `test_inner_budgets_are_tagged_carried_forward_unmeasured`; `eval/tests/test_latency.py::test_nesting_skips_missing_outer_instead_of_fabricating`, `test_full_nesting_report_and_conflict_resolution` — all 6 passed. Full `eval/tests/test_measure.py eval/tests/test_latency.py eval/tests/test_decay.py` run: 64 passed, 0 failed.
4. **PLAN frontmatter harvest (all six plans, not just the ones cited in prior rounds' truth tables).** All six PLAN files (`06.3.3-01..06-PLAN.md`) declare `requirements: [OBS-05]` — no orphaned or extra requirement IDs. All six declare a `must_haves.prohibitions:` block, but every entry is a flat process-constraint string (e.g. "no re-seed of eval/runs/...", "no new Python dependency", "config.toml must not be edited") — none use the structured `{statement, status, verification}` schema Step 2b describes, so none carry a `judgment`/`test` tier requiring a separate human-resolution gate. Spot-checked the prohibitions against the actual review-fix diff scope (`git diff 68b70ae HEAD`, six files: `config/config.verify.toml`, `workflow_phase5_production.rs`, `latency.py`, `measure.py`, `test_latency.py`, `test_measure.py`): no re-seed of `eval/runs/`, no `uv.lock`/`pyproject.toml` change, no edit to `config.toml`/`config.example.toml`, no edit to already-executed `06.3.3-01..06-PLAN.md`/`-SUMMARY.md` files, and plan-06's specific prohibition ("`config.verify.toml`'s `generation_node_timeout_ms = 7000` must not be raised") still holds — none violated. Grepped every `<verify>` block across all six plans for `<human-check>` / `<why_human>` tags: zero matches — no plan in this phase defers a human-check item, so Step 8's PLAN-harvest step contributes no deferred human-verification items. `status: passed` is therefore legitimate — the human-verification section is genuinely empty, not merely unchecked.

T-11 is scored **VERIFIED**, not on REVIEW-FIX.md's self-report, but on: (a) direct reading of the current `measure.py`/`latency.py` source, (b) an independent test run in this checkout, and (c) an independent manual reproduction of shape (b) that the committed test suite does not cover.

### Observable Truths

| # | Truth (Success Criterion / must-have) | Status | Evidence |
|---|---------|--------|----------|
| SC-1 | Graph population verified before timeout work | ✓ VERIFIED | Unchanged: `06.3.3-STORE-BASELINE.md`; `graph_disposition: "populated"` in the measurement record. |
| SC-2a | Measurement pass drive mechanics | ✓ VERIFIED | Unchanged: `06.3.3-MEASUREMENT.md` §2 (160 questions / 320 records, cheap model, no judge, env-only raised ceilings). |
| SC-2b | Percentile over censored observations is refused or explicitly flagged, never reported as the true value | ✓ VERIFIED | Unchanged from prior round: `extract_node_durations` → `dropped_at_ceiling`; `derive_budgets_from_records` sums drops + `CENSORED_NODE_TIMEOUT` into `percentile_with_ci`. Regression-checked in this round's 64-test run. |
| SC-3 | Mid-run engine restart splits pass into two segments | ✓ VERIFIED | Unchanged: restart at ordinal 160 in `06.3.3-MEASUREMENT.md` §2/§4. |
| SC-4 | Decay question answered against a pre-committed criterion | ✓ VERIFIED | Unchanged recorded verdict; instrument pairing bug closed. `eval/tests/test_decay.py` unmodified since prior round, 0 diff, still passing. |
| SC-5 | Every budget-blowing node investigated before widening | ✓ VERIFIED | Unchanged disposition table in MEASUREMENT §6 / BUDGETS containment check. |
| SC-6a | Six budgets written, in step across toml + Rust defaults | ✓ VERIFIED | Grep re-confirmed: 645 / 16647 / 38595 / 45674 / 1070 / 65000 in `config/config.toml`, `config.example.toml`, and `default_*_timeout_ms()` in `engine/src/config.rs`. Production `config.toml` untouched by the review-fix commits (`git diff` scope: `config/config.verify.toml` only). |
| SC-6b | Written set satisfies startup validation and provider-attempt rule on the settings-load path | ✓ VERIFIED | `EffectiveRagSettings::validate()` calls `validate_against_provider` (`config.rs:592`). Re-ran named tests `tests::effective_settings_reject_provider_contract_violation_at_load`, `tests::effective_settings_accept_provider_contract_at_boundary` independently in this checkout — 2 passed. |
| SC-7 | Graph traversal cost recorded if inherently slow | ✓ VERIFIED | Unchanged ~25–30 s figure in BUDGETS. |
| SC-8 | Spend capped; `$0.0199` not used as estimate | ✓ VERIFIED | Unchanged MEASUREMENT spend $0.08316 vs $5 cap; BUDGETS go/no-go. |
| T-11 | Zero usable observations for a node is an explicit stated-reason outcome, never a silently omitted row (PLAN-01 must_have / REVIEW CR-01) | ✓ VERIFIED | See Goal Achievement above. Both fabricated-integer paths (`graph_node_timeout_ms=14500`, `retrieve_timeout_ms=10500`) reproduced as closed: refused rows emitted, `check_nesting_invariants` skips missing outers, no fabricated value reaches `proposed_budgets` or the persisted summary. |

**Score:** 11/11 truths verified (0 present, behavior-unverified)

### Deferred Items

None.

### Advisory (New Scope, Unevidenced)

| # | Finding | Category | Why Advisory |
|---|---------|----------|--------------|
| 1 | Verify overlay `generation_timeout_secs = 3` | other | Carried forward from prior round, now with WR-04 documentation applied (`config/config.verify.toml` and `workflow_phase5_production.rs` comments updated to state `verify-ingestion.sh` is ingest/embeddings-only and live generation in (3s,7s] is expected to fail fast). No longer flagged as unresolved — informational only. |

**Not filed here:** the shape-(b) test-coverage gap (below, Test Quality note) is in-scope and evidenced by my own reproduction — it belongs with the prior gap's `missing:` item 3 tracking, not under "new-scope, unevidenced."

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `eval/src/lancet_eval/thresholds.py` | Committed decision inputs | ✓ VERIFIED | Unchanged. |
| `eval/src/lancet_eval/latency.py` | Extractor + classifier + percentile + derivation + nesting | ✓ VERIFIED | `check_nesting_invariants` now gates both relationships on `in present`; no default-to-0 fabrication path remains. |
| `eval/src/lancet_eval/decay.py` | Parallel pairing, censoring-aware | ✓ VERIFIED | Unchanged since prior round (0-line diff). |
| `eval/src/lancet_eval/measure.py` | Driver + `derive_budgets_from_records` | ✓ VERIFIED | Refused-row emission for unavailable percentiles; carried-forward-unmeasured tagging on inner pass-through; `proposed_records`/`censored_by_node` now persisted to summary; calibration_note rewritten to match. |
| `eval/src/lancet_eval/client.py`, `run.py` | SSE timeout from settings | ✓ VERIFIED | Unchanged. |
| `engine/src/bin/inspect_lancedb.rs` | Store inspect | ✓ VERIFIED | Unchanged regression. |
| `06.3.3-STORE-BASELINE.md` / `MEASUREMENT.md` / `BUDGETS.md` | Records | ✓ VERIFIED | Unchanged; shipped six values are the real 320/160-record measurements, not the fabricated 14500/10500 figures from the closed bug. |
| `config/config.toml`, `config.example.toml` | Six derived values | ✓ VERIFIED | Byte-identical with Rust defaults; untouched by review-fix commits. |
| `config/config.verify.toml` | Coherent overlay | ✓ VERIFIED | `generation_timeout_secs = 3` with `generation_node_timeout_ms = 7000`; WR-04 comment added explaining verify-ingestion scope. |
| `eval/runs/2026-09-06-measure-multihop_rag/` | Evidence | ✓ VERIFIED | Present (not re-driven; prohibition). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `compute_censoring_census` + `dropped_at_ceiling` | `percentile_with_ci` in `derive_budgets_from_records` | summed `censored_count` | WIRED | Unchanged, re-confirmed. |
| `measure.py` ordinal/segment | `analyze_decay` trend | parallel `(ordinal, latency)` | WIRED | Unchanged. |
| `try_from_settings` → `validate()` | `validate_against_provider` | `config.rs:592` | WIRED | Unchanged, re-confirmed via test run. |
| `client.py` SSE timeout | `run.py` client timeout | `EvalSettings.gateway_timeout_secs` | WIRED | Unchanged. |
| `fetch_neighborhood` | `inspect_lancedb` | production graph helper | WIRED | Unchanged. |
| MEASUREMENT dispositions | BUDGETS containment | node name | WIRED | Unchanged. |
| Empty `PercentileResult` | `proposed_records` / nesting | refuse row, then skip nesting | **WIRED** | T-11 closed: `not pct.is_available` → refused row + no `proposed_budgets` entry → `check_nesting_invariants` skips the missing outer via `in present`. |
| `derivation.proposed_records` / `censored_by_node` | persisted `run_measurement_pass` summary | dict assignment | WIRED | WR-01 closed: both keys now present in the summary dict written to `run_record.json`/`measurement.json`. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `derive_budgets_from_records` | RetrieveHybrid `proposed_ms` (partial censoring) | surviving durations + censored_count | yes, flagged lower bound | ✓ FLOWING |
| `derive_budgets_from_records` | `graph_node_timeout_ms` on RetrieveHybrid-only fixture (shape a) | empty survivors → refused row, key absent from `proposed_budgets` | no fabrication; explicit refusal | ✓ FLOWING (refusal, not a value) |
| `derive_budgets_from_records` | `retrieve_timeout_ms` on 100%-at-ceiling retrieve (shape b) | empty survivors → refused row, key absent from `proposed_budgets` | no fabrication; explicit refusal (manually reproduced) | ✓ FLOWING (refusal, not a value) |
| `run_measurement_pass` summary | `proposed_budgets`, `proposed_records`, `censored_by_node` | `derivation.*` / `nesting_rep.resolved_budgets` | all three now persisted | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Shape (a): empty-survivor refusal, no nesting fill-in | `uv run --project eval pytest eval/tests/test_measure.py::test_derivation_refuses_empty_survivors_without_nesting_fill_in -q` | 1 passed | ✓ PASS |
| Ceiling-aware derivation + censored-count sum + inner tagging | `uv run --project eval pytest eval/tests/test_measure.py::test_derivation_flags_ceiling_censored_node_as_lower_bound eval/tests/test_measure.py::test_derivation_censored_count_sums_ceiling_drops_and_timer_expiries eval/tests/test_measure.py::test_inner_budgets_are_tagged_carried_forward_unmeasured -q` | 3 passed | ✓ PASS |
| Nesting skips missing outer / still resolves present nesting violations | `uv run --project eval pytest eval/tests/test_latency.py::test_nesting_skips_missing_outer_instead_of_fabricating eval/tests/test_latency.py::test_full_nesting_report_and_conflict_resolution -q` | 2 passed | ✓ PASS |
| Full eval regression (measure+latency+decay) | `uv run --project eval pytest eval/tests/test_measure.py eval/tests/test_latency.py eval/tests/test_decay.py -q` | 64 passed | ✓ PASS |
| Provider guard on `try_from_settings` | `cargo test --manifest-path engine/Cargo.toml --locked --lib -- tests::effective_settings_reject_provider_contract_violation_at_load tests::effective_settings_accept_provider_contract_at_boundary --exact` | 2 passed | ✓ PASS |
| WR-04 verify-overlay live generation timeout | `cargo test --manifest-path engine/Cargo.toml --locked --lib -- tests::workflow_phase5_production::workflow_phase5_config_verify_generation_timeout --exact` | 1 passed | ✓ PASS |
| Shape (b): 100%-at-ceiling `RetrieveHybrid` through `derive_budgets_from_records` (manual reproduction, no committed test) | Standalone script: 20 `RetrieveHybrid` records all at 10000ms ceiling → `derive_budgets_from_records(...)` | `retrieve_timeout_ms` absent from `proposed_budgets`; refused row present with `rule="derivation_refused_empty_or_unavailable"`, `censored_status="unavailable(Empty observation set: 0 usable observations; censored_count=20)"` | ✓ PASS (manual — see Test Quality Note) |
| Six production values identical | grep of six keys | 645 / 16647 / 38595 / 45674 / 1070 / 65000 | ✓ PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` declared or present — skipped.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| OBS-05 | Plans 01–06 | Evaluation fidelity; 06.3.3 slice is latency measurement and budget derivation | ✓ SATISFIED (this slice) | Pass ran, six production values written, go/no-go recorded, all prior instrument/startup/derivation gaps closed and re-confirmed independently. `OBS-05` is a family-level requirement spanning 06.3.1–06.3.4 and remains `[ ]` at `REQUIREMENTS.md:46`. Checked via `git log --oneline -- .planning/REQUIREMENTS.md`: commit `68b70ae` ("docs(phase-06.3.3): revert premature Complete requirements after gaps found") is the commit that unchecked it, immediately preceding the review-fix commits — a legitimate revert triggered by the first verification round's gap finding, not the cross-phase shared-ID footgun (that pattern un-checks a *different* phase's completed slice of a shared ID; here it is 06.3.3's own box being reverted for 06.3.3's own gap). Re-checking the box is outside verification scope — that belongs to the phase-completion step, not this report. |

**Orphaned IDs:** None newly introduced. SUMMARY-01/02's phantom `LAT-0x` citations (noted in the prior round) are pre-existing documentation drift, not a regression from the review-fix commits — WARNING only, not a blocker.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | None found in the review-fix diff (`git diff 68b70ae HEAD`) | — | Prior BLOCKER (measure.py 351-380 empty-survivor skip + nesting fabrication) and Warnings WR-01/02/03/04 are all closed in source, confirmed by direct read and test run, not by re-trusting REVIEW-FIX.md's narrative. |

No unreferenced `TBD`/`FIXME`/`XXX` in covered implementation files.

### Test Quality Note (T-11 closure, carried from prior gap's `missing:` item 3)

The prior gap's `missing:` list explicitly asked for "a regression test on the gap-1 fixture and a 100%-at-ceiling retrieve fixture." Only the first half landed as a named test (`test_derivation_refuses_empty_survivors_without_nesting_fill_in`, shape (a)). Shape (b) — a node that is 100% at-ceiling, the literal 2026-09-03 collapse shape — has no committed test that drives it through `derive_budgets_from_records`; `synthetic_all_at_ceiling_latencies` (the fixture built for exactly this shape) is wired only into `test_decay.py`, not into any `test_measure.py` derivation test. This verification round closes that evidence gap by manual reproduction against current source (see Behavioral Spot-Checks) rather than by trusting the symmetry argument alone. The manual repro passed and the code path is structurally identical for both nesting relationships (`in present` guard, same shape, same file), so T-11 is scored VERIFIED — but a future edit to relationship-2 handling in `check_nesting_invariants` (or to the retrieve branch of the derivation loop) could regress shape (b) without any committed test failing. Recommend a follow-up test closing this before or during 06.3.4.

Adjacent diagnostic-quality note, not a fabricated-integer regression and not part of this phase's verdict: `run_measurement_pass` feeds `nesting_rep.resolved_budgets` straight into `check_harness_ceilings` (measure.py:404). On a shape-(b)-shaped real pass, that map can legitimately be missing `retrieve_timeout_ms`/`graph_node_timeout_ms` (confirmed by the manual repro above — resolved output was `{generation_node_timeout_ms, query_embedding_timeout_ms, graph_operation_timeout_ms}` only). `check_harness_ceilings` did not error on the partial map in this repro, but a `largest_single_node_budget_ms` computed over a partial set under-reports and could return a falsely comfortable "no ceiling violation" on exactly the collapse-shaped data the instrument exists to catch. A test covering shape (b) end-to-end (the same follow-up recommended above) would surface whether this needs its own guard.

### Human Verification Required

None.

### Gaps Summary

No gaps remain. The single carried-forward gap (T-11 / REVIEW CR-01: empty-survivor nodes silently defaulted to 0 and fabricated through nesting) is closed:

- `derive_budgets_from_records` refuses (does not silently omit) any node whose percentile is unavailable, emitting an explicit `proposed_records` row with `rule: "derivation_refused_empty_or_unavailable"` and a reason string carrying the censored/observation count.
- `check_nesting_invariants` skips (does not default to 0) any outer budget key that was never present in the input map, for both nesting relationships (`graph_node_timeout_ms` and `retrieve_timeout_ms`), so a missing measurement can never be raised into a fabricated integer via inner-sum + slack.
- Both fixture shapes originally implicated (untimed node, and 100%-at-ceiling node) were checked against current source: shape (a) by a passing named regression test, shape (b) by an independent manual reproduction against current source (flagged in the Test Quality Note as a coverage gap worth closing with a named test, since it is not covered by the committed suite).
- The three prior-round gaps (SC-2b instrument wiring, decay pairing, provider-attempt startup validation) remain closed and were re-confirmed by re-running their named tests in this checkout — no regressions found.
- WR-01 (persisted `proposed_records`/`censored_by_node`), WR-02 (`carried_forward_unmeasured` tagging), WR-03 (calibration_note rewrite), and WR-04 (verify-overlay documentation) are all applied in source and match their commit descriptions.

Phase goal achieved: the instrument now correctly distinguishes a measured budget from a refused/unmeasured one at every point in the derivation pipeline, and the six production budgets shipped in `config.toml` were independently confirmed to be the real 320/160-record measurements, not artifacts of the closed fabrication bug.

---

_Verified: 2026-09-08T13:15:00Z_
_Verifier: Claude (gsd-verifier)_
