---
phase: 06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva
verified: 2026-09-07T15:30:00Z
status: gaps_found
score: 8/10 must-haves verified
covered_files: [".planning/REQUIREMENTS.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-01-PLAN.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-01-SUMMARY.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-02-PLAN.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-02-SUMMARY.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-03-PLAN.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-03-SUMMARY.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-04-PLAN.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-04-SUMMARY.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-BUDGETS.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-MEASUREMENT.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-REVIEW.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-STORE-BASELINE.md", "config/config.example.toml", "config/config.toml", "engine/src/bin/inspect_lancedb.rs", "engine/src/config.rs", "eval/scripts/drive_measurement_pass.py", "eval/src/lancet_eval/cli.py", "eval/src/lancet_eval/client.py", "eval/src/lancet_eval/decay.py", "eval/src/lancet_eval/latency.py", "eval/src/lancet_eval/measure.py", "eval/src/lancet_eval/preflight.py", "eval/src/lancet_eval/run.py", "eval/src/lancet_eval/thresholds.py", "scripts/engine-test-targets.sh"]
covered_digest: "v1:sha256:758f39b954ce75f4128530c550b1d1273f263725a797f7df694e6d781b8bace3"
gaps:
  - truth: "A percentile over an observation set containing any censoring of kinds (a)-(c) is refused or returned explicitly flagged, never as if it were the true value (06.3.3-01 must_have, part of roadmap SC-2's 'instrument')."
    status: failed
    reason: "run_measurement_pass() computes a correct 5-label censoring census via compute_censoring_census(), but extract_node_durations() pulls every duration off node_timings with no reference to that census, and the percentile_with_ci() call two lines later is never given the real censored count (it defaults censored_count=0), so is_lower_bound is always False and censored_status in the resulting ProposedBudgetRecord always reads 'clean' -- even when a meaningful fraction of the sample sat at the timeout ceiling. This is exactly the 904/1000-at-ceiling failure mode this phase's own test fixture (eval/tests/conftest.py:26-34) documents as having actually happened on 2026-09-03. Confirmed by direct source read, not deferred to the code reviewer's label. This breaks plan-01's own key_link: 'latency.py's censoring classifier reads three engine signals ... against the ceiling in force for each node ... so censoring is judged against the real ceiling' -- the classifier runs, its output is simply never wired into the derivation call (NOT_WIRED, not missing). The actual 06.3.3-03 pass was driven by eval/scripts/drive_measurement_pass.py, not by this buggy run_measurement_pass() path, and 06.3.3-BUDGETS.md's derivation table was hand-reconstructed with correct censoring treatment (the graph_operation lower bound), so the shipped budget values are not corrupted by this bug -- but the reusable library function this phase built (and lists as a required artifact for future re-measurement, e.g. by 06.3.4 or later) remains unsound."
    artifacts:
      - path: "eval/src/lancet_eval/measure.py"
        issue: "run_measurement_pass() (lines 437-463) computes census = compute_censoring_census(...) and durations = extract_node_durations(...) as two independent, never-joined values; percentile_with_ci(vals, ...) is called on the raw durations with no censored_count argument."
      - path: "eval/src/lancet_eval/latency.py"
        issue: "extract_node_durations() (lines 174-185) has no ceiling/census parameter at all -- it cannot exclude or count a censored duration even if a caller wanted it to."
    missing:
      - "Cross-reference the census with the raw durations before calling percentile_with_ci (e.g. extract_node_durations accepts the ceiling map and drops/counts any value >= ceiling_ms), then pass the resulting drop count into percentile_with_ci(..., censored_count=dropped) so is_lower_bound/censored_status reflect reality instead of unconditionally 'clean'."
  - truth: "eval/src/lancet_eval/decay.py -- trend statistic, early-versus-late windowing, restart-segment discriminator, all censoring-aware (06.3.3-01 must_have artifact claim)."
    status: failed
    reason: "Read decay.py:489-521 directly (not deferred to code review). ordinals.append(ord_val) runs unconditionally for every sorted record; latencies.append(dur) only runs when the analyzed node has a completed timing. A record is excluded from latencies AND counted via censored_count only when its own failure has error_kind == 1 (node timeout). Any other reason a timing is missing on the analyzed node -- a non-timeout failure on that node, an upstream failure that prevented the node from running (e.g. ReformulateQuery failing before RetrieveHybrid starts), or a transport exception in measure_one's except branch producing empty node_timings and empty node_failures -- silently drops the record from latencies while leaving censored_count unchanged and the ordinal still in ordinals. Downstream, compute_theil_sen_trend(ordinals[:len(latencies)], latencies, ...) pairs a truncated ordinal prefix against latencies positionally; once any such gap occurs before the last record, every subsequent (ordinal, latency) pair is mismatched, which can flip the sign/significance of the reported decay trend with no error or flag. This did not manifest in this phase's own 06.3.3-03 pass: the censoring census in 06.3.3-MEASUREMENT.md section 3 shows RetrieveHybrid (the node the decay verdict was computed for) at 320/320 OBSERVED, 0 in every other label, for all 320 records -- there were zero non-timeout drops in this specific dataset, so ordinals and latencies happened to stay 1:1 aligned and the reported Theil-Sen slope / Mann-Kendall p-value / window p95 delta in 06.3.3-MEASUREMENT.md section 4 are not corrupted by this bug. The failure is in the reusable instrument (an artifact this phase explicitly built and lists as a required deliverable for future re-measurement), not in this run's reported numbers."
    artifacts:
      - path: "eval/src/lancet_eval/decay.py"
        issue: "Lines 489-521: ordinals and latencies are built as two independently-conditioned lists, not one parallel structure; only error_kind==1 counts as censoring for alignment purposes."
    missing:
      - "Build ordinals and latencies as one parallel structure (append (ord_val, dur) tuples only when a usable latency exists, then unzip), or extend censored_count to cover every reason a record is dropped from latencies so the early-refusal path fires instead of silently misaligning pairs."
  - truth: "The written set satisfies the engine's own startup validation and its provider-attempt rule, proven by starting the engine against the committed configuration rather than by reasoning about the arithmetic (06.3.3-04 must_have)."
    status: failed
    reason: "engine/src/config.rs:590-649 EffectiveRagSettings::validate() calls self.workflow.validate() (nesting-invariant only, config.rs:296-331) and a series of unrelated field checks, but never calls WorkflowSettings::validate_against_provider() (config.rs:333-344), the guard that checks generation_node_timeout_ms >= 2 * generation_timeout_secs * 1000. try_from_settings() (config.rs:555-586), the only path main.rs's load_settings()-derived Settings flow through, calls effective.validate() and nothing else. Starting the engine against the committed config.toml therefore does NOT exercise validate_against_provider() at all -- confirmed by reading the source directly. The one test that does call validate_against_provider() (engine/src/tests.rs:320, config_workflow_timeout_overlays_match_contract; independently run and confirmed passing: `cargo test --manifest-path engine/Cargo.toml --locked --lib -- tests::config_workflow_timeout_overlays_match_contract --exact` -> ok, 1 passed) calls it directly on WorkflowSettings::default(), bypassing EffectiveRagSettings::validate()/try_from_settings() entirely -- it proves the arithmetic is correct in isolation, not that the engine enforces it at startup. The written value itself is not wrong today (generation_node_timeout_ms=65000 >= 2*30*1000=60000 holds), so no user-visible defect exists yet -- but the enforcement mechanism this phase's own design and BUDGETS.md's proof section rely on is dead code at runtime: if generation_timeout_secs is later raised past 32.5s (e.g. via LANCET_OPENROUTER__GENERATION_TIMEOUT_SECS), the engine will start successfully with a violated provider-attempt contract and no startup error, silently discarding otherwise-successful generations mid-retry in production."
    artifacts:
      - path: "engine/src/config.rs"
        issue: "validate() (lines 590-649) omits self.workflow.validate_against_provider(self.generation_timeout_secs); the check exists (lines 333-344) and is asserted only in tests (tests.rs:320, tests.rs:395/480 per code review)."
      - path: ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-BUDGETS.md"
        issue: "Lines 91-101 ('Engine startup proof') state the live-binary start 'reached Rust RAG Engine serving ... after load_settings and EffectiveRagSettings::try_from_settings, so both validate and validate_against_provider accepted the committed workflow set.' This is factually incorrect as written: validate_against_provider is not reachable from try_from_settings/validate at all, so the engine start proves nothing about the provider-attempt rule -- only about the nesting invariant and the other unrelated validate() checks."
    missing:
      - "Call self.workflow.validate_against_provider(self.generation_timeout_secs)? from inside EffectiveRagSettings::validate() (or from try_from_settings right after constructing effective), so the guard is enforced on every settings load, matching what BUDGETS.md already claims was proven."
      - "Correct 06.3.3-BUDGETS.md's 'Engine startup proof' section once the wiring is fixed and re-verified, or clearly scope the claim down to the nesting invariant only."
---

# Phase 06.3.3: Retrieval Latency Measurement Pass and Timeout Budget Derivation Verification Report

**Phase Goal:** Establish, by measurement, whether the 2026-09-03 collapse was mis-sized timeout budgets or progressive resource exhaustion — then derive all six production budgets from the observed latency. Closes at the go/no-go for paid spend, so that decision sits on a phase boundary rather than mid-phase.

**Verified:** 2026-09-07T15:30:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification (resumed session per #2868; code review had just completed with `issues_found`)

## Goal Achievement

This phase's operational goal — separate the two collapse hypotheses, run a real measurement pass, dispose of every budget-blowing node, derive and write six coherent budgets, and close on a go/no-go — **was achieved and the resulting numbers are, on the evidence available, defensible.** But the code review's three Critical findings are not stylistic: they are failures of specific must_haves this phase's own PLAN frontmatter wrote for itself, verified independently against source below (not deferred to the reviewer's severity labels, per this task's instructions). Two are instrument-correctness failures in reusable library code (`measure.py`/`latency.py`, `decay.py`) that this phase built and will be relied on by 06.3.4 and any future re-measurement; one is a false claim in the phase's own closing derivation record (`06.3.3-BUDGETS.md`) about what starting the engine actually proved.

### Observable Truths (scored against ROADMAP.md Success Criteria 1-8, the authoritative contract; SC-2 and SC-6 split into their operational and instrument/proof halves per plan-01/04 must_haves)

| # | Truth (Success Criterion) | Status | Evidence |
|---|---------|--------|----------|
| SC-1 | Graph population verified before timeout work (node/edge/entity_edge counts, degree distribution, hand-run traversal) | VERIFIED | `06.3.3-STORE-BASELINE.md` exists; `06.3.3-BUDGETS.md` cites 27,971 entities, 46,092 entity-edges, both canary entities present/connected; `graph_disposition: "populated"` recorded in `eval/runs/2026-09-06-measure-multihop_rag/measurement.json`. |
| SC-2a | Measurement pass drive mechanics: several hundred queries, cheap model, budgets raised, starting rows recorded, no judge | VERIFIED | `06.3.3-MEASUREMENT.md` §2: 160 questions / 320 records, `deepseek-v4-flash-0731`, `judge_model: null`, all 7 timeout keys raised via process env only (`git status --short config/` clean), checkpoint reconciliation table 2,262→4,152 fully attributed. |
| SC-2b | The instrument itself refuses/flags a percentile over any censored observation, never reporting it as the true value | **FAILED** | See gap 1: `run_measurement_pass()`/`extract_node_durations()` never cross-reference the census before deriving a percentile; `percentile_with_ci()` always receives `censored_count=0`. Did not corrupt this run's node-level percentiles only because node-level censoring happened to be 0/320 everywhere in this dataset (see `06.3.3-MEASUREMENT.md` §3) — the property is false of the code, not incidentally true. |
| SC-3 | Mid-run engine restart splits pass into two segments | VERIFIED | `06.3.3-MEASUREMENT.md` §2: restart at ordinal 160, `2026-09-07T00:04:03.593886+00:00`, post-restart probe query and latency recorded, segment discriminator in §4 (`process_state` hypothesis, CI excludes zero). |
| SC-4 | Decay question answered against a stated pre-committed criterion (positive slope, or late-window p95 materially above early-window p95) | VERIFIED | `06.3.3-MEASUREMENT.md` §4: both prongs evaluated against `thresholds.py`'s committed `decay_significance_threshold=0.05` / `materiality_delta_ms=500.0`; `decay_present: False`. Numerically unaffected by the decay.py bug in gap 2 for this specific dataset (RetrieveHybrid had 0 non-timeout drops, confirmed via the censoring census), but see gap 2 for the instrument-level defect underneath it. |
| SC-5 | Every budget-blowing node investigated as possible defect before widening | VERIFIED | `06.3.3-MEASUREMENT.md` §6: disposition table for all 5 workflow nodes (clean / genuinely slow), each with implementation citation and evidence; `06.3.3-BUDGETS.md` "Containment check" maps every widened budget to a disposition row before the write. |
| SC-6a | Six budgets re-derived together and written to production `config/config.toml`, kept in step with `config.example.toml` and Rust `default_*` (SC-6 value + sync requirement) | VERIFIED | Directly grepped: `query_embedding_timeout_ms=645`, `retrieve_timeout_ms=16647`, `graph_operation_timeout_ms=38595`, `graph_node_timeout_ms=45674`, `prompt_timeout_ms=1070`, `generation_node_timeout_ms=65000` — byte-identical across `config/config.toml`, `config/config.example.toml`, and `engine/src/config.rs` `default_*_timeout_ms()`. `reformulate_timeout_ms` unchanged at 5000 in both TOML files and `default_reformulate_timeout_ms()`. Generation budget derived from provider contract (`2×30×1000+5000=65000`, matches `derive_provider_contract_budget()`), not this pass's 20,889ms measured p95. |
| SC-6b | The written set satisfies the engine's own startup validation and provider-attempt rule, **proven by starting the engine** | **FAILED** | See gap 3: `EffectiveRagSettings::validate()` never calls `validate_against_provider()`; starting the engine against the committed config does not exercise that guard at all. `06.3.3-BUDGETS.md`'s own "Engine startup proof" section (lines 91-101) claims otherwise. The specific numbers happen to satisfy the invariant today (60000 ≤ 65000), but the proof mechanism is broken. |
| SC-7 | If graph traversal is inherently too slow, budget raised and cost recorded for 6.4's honest-limitations section | VERIFIED | `06.3.3-BUDGETS.md` "Graph question" section: traversal genuinely slow (not a defect), cost ~25-30s cited explicitly for 6.4, `graph_node`/`graph_operation` both widened. |
| SC-8 | Spend capped per stage against daily limit; `$0.0199` not usable as an estimate | VERIFIED | `06.3.3-MEASUREMENT.md` §1/§2: stage cap $5.00 (Task 1 authorization), observed spend $0.08316 (includes embeddings), anti-estimate note present. Go/no-go in `06.3.3-BUDGETS.md`: per-stage caps for 06.3.4 recorded (staged $2, full $5, judging $5). |

**Score:** 8/10 (SC-1, 2a, 3, 4, 5, 6a, 7, 8 verified; SC-2b, SC-6b failed).

### Deferred Items

None. All roadmap Success Criteria and the phase-closing go/no-go belong to this phase; no later milestone phase's goal or success criteria address the censoring-instrument or startup-validation gaps found here (06.3.4 consumes the written budgets and the graph disposition, it does not re-derive or re-validate them).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `eval/src/lancet_eval/thresholds.py` | Committed decision inputs, no post-hoc default | VERIFIED | Single commit (`c460715`), never modified since; all decision fields (multiplier, percentiles, thresholds, seed) have `__post_init__` validation, no silent defaults for the required ones. |
| `eval/src/lancet_eval/latency.py` | Duration extraction, 4-way censoring classifier, percentile+CI, derivation rule, nesting report, two-ceiling report | ⚠️ VERIFIED with defect — `compute_censoring_census`, `percentile_with_ci`, `derive_budget` all present and individually correct; `extract_node_durations` is the unwired link (gap 1). |
| `eval/src/lancet_eval/decay.py` | Trend statistic, windowing, restart discriminator, censoring-aware | ⚠️ VERIFIED with defect — alignment bug (gap 2), did not trigger on this dataset. |
| `eval/src/lancet_eval/measure.py` | Measurement driver: warm-up, ordinal/segment/question_type stamping, spend stop-rule, run-record emission | VERIFIED for drive mechanics; ⚠️ `run_measurement_pass()`'s derivation half inherits gap 1. |
| `eval/src/lancet_eval/client.py`, `run.py` | SSE read timeout threaded from `EvalSettings.gateway_timeout_secs`, no hardcoded `read=300.0` | VERIFIED — grepped both files; no `300.0` literal on the live query path, `read=effective_read_timeout`/`read=eval_settings.gateway_timeout_secs` confirmed. |
| `engine/src/bin/inspect_lancedb.rs` | Store-wide graph-population mode, bounded neighbourhood mode, name lookup | VERIFIED — file modified per SUMMARY-02, delegates to `graph::fetch_neighborhood` (line 566), `06.3.3-STORE-BASELINE.md` cites its output directly. |
| `.planning/.../06.3.3-STORE-BASELINE.md` | D-05/D-22 record with machine-readable graph disposition | VERIFIED — exists, `graph_disposition: "populated"` also mirrored into the pass's own `measurement.json`. |
| `.planning/.../06.3.3-MEASUREMENT.md` | Pass configuration, censoring census, decay verdict, dispositions, spend | VERIFIED — all sections present and internally consistent with `eval/runs/2026-09-06-measure-multihop_rag/measurement.json`. |
| `.planning/.../06.3.3-BUDGETS.md` | Derivation trail, invariants, non-derivable census, go/no-go | ⚠️ VERIFIED with defect — derivation table, invariants and non-derived census are sound; "Engine startup proof" section makes the false claim in gap 3. |
| `config/config.toml`, `config/config.example.toml` | Six derived values, refreshed nesting docs, kept in step | VERIFIED — byte-identical values confirmed by direct grep (see SC-6a). |
| `eval/runs/<date>-measure-multihop_rag/` | Measurement journal + run record committed as evidence | VERIFIED — `eval/runs/2026-09-06-measure-multihop_rag/{journal.jsonl (320 lines), measurement.json, run_record.json}` present. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `compute_censoring_census()` output | `percentile_with_ci()` call in `run_measurement_pass()` | "censoring is judged against the real ceiling" (06.3.3-01 key_link) | **NOT_WIRED** | Census is computed, discarded; `percentile_with_ci` called with default `censored_count=0`. Gap 1. |
| `measure.py`'s recorded `ordinal`/`segment` fields | `decay.py`'s trend/window computation | "decay.py reads those recorded fields after sorting by ordinal, never a line index" (06.3.3-01 key_link) | PARTIAL | Sorting by ordinal is correctly implemented; the *pairing* of sorted ordinals to filtered latencies is the break (gap 2), a narrower defect than the key_link as stated but within its scope. |
| `client.py:207`'s per-request `httpx.Timeout` | `run.py:207`'s client-level timeout | "client.py is the authoritative SSE read ceiling; both threaded from the same setting" | WIRED | Both resolve to `EvalSettings().gateway_timeout_secs` / `eval_settings.gateway_timeout_secs`; no literal divergence found. |
| `graph::fetch_neighborhood` (production path, `service.rs:329`) | `inspect_lancedb.rs` neighbourhood mode | "delegates ... rather than implementing a second breadth-first expansion" | **WIRED** | Directly confirmed: `engine/src/bin/inspect_lancedb.rs:566` calls `engine::graph::fetch_neighborhood(database, seed_entity_id, hop_bound, true)`. |
| `EffectiveRagSettings::try_from_settings()` → `validate()` | `WorkflowSettings::validate_against_provider()` | "written set satisfies ... provider-attempt rule, proven by starting the engine" (06.3.3-04 must_have) | **NOT_WIRED** | `validate()` (config.rs:590-649) never calls `validate_against_provider()` (config.rs:333-344). Gap 3. |
| `06.3.3-MEASUREMENT.md` §6 disposition set | `06.3.3-BUDGETS.md` containment check | "joined by node name; the containment check is the gate on the write" | WIRED | All 3 widened budgets (`retrieve`, `graph_node`, `graph_operation`) map to a disposition row; verified by direct comparison of both documents. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `validate_against_provider` arithmetic is correct in isolation | `cargo test --manifest-path engine/Cargo.toml --locked --lib -- tests::config_workflow_timeout_overlays_match_contract --exact` | `test tests::config_workflow_timeout_overlays_match_contract ... ok` (1 passed) | ✓ PASS (but proves only the isolated arithmetic, not startup wiring — see gap 3) |
| Six budget values are byte-identical across the three sources | `grep` of all six `*_timeout_ms` keys in `config/config.toml`, `config/config.example.toml`, `engine/src/config.rs` | 645 / 16647 / 38595 / 45674 / 1070 / 65000 identical everywhere | ✓ PASS |
| No hardcoded `read=300.0` remains on the live query path | `grep -n "300\.0\|gateway_timeout_secs" eval/src/lancet_eval/client.py eval/src/lancet_eval/run.py` | Both use `EvalSettings().gateway_timeout_secs` / `eval_settings.gateway_timeout_secs` | ✓ PASS |
| `eval/runs/2026-09-03-multihop_rag/` untouched during this phase | `git log --oneline -- eval/runs/2026-09-03-multihop_rag/` | 3 commits, all pre-dating this phase (`bd580f7`, `a72dc27`, `2ff9d40`) | ✓ PASS |
| `uv.lock` unchanged (no new Python dependency) | `git log --oneline -- uv.lock` | Single commit from `06.3-01` scaffold, none since | ✓ PASS |
| `config/config.eval.toml` still overrides only `lancedb_path` | `grep -n . config/config.eval.toml` | Single key: `lancedb_path = "./data/lancedb-eval"` | ✓ PASS |
| `graph::fetch_neighborhood` delegation | `grep -n "fetch_neighborhood" engine/src/bin/inspect_lancedb.rs` | Line 566 calls `engine::graph::fetch_neighborhood(...)` | ✓ PASS |

Full-suite runs referenced from the resume context (`cargo test --manifest-path engine/Cargo.toml --locked && go test ./...` all green; `uv run --project eval pytest` 320 passed / 42 subtests passed / 1 pre-existing unrelated failure) were not re-run in full here (per the "run the full suite at most once" constraint) — accepted as already-executed evidence per the task's supplied context. The code reviewer's statement that `test_measure.py`/`test_decay.py`/`tests.rs` do not exercise the CR-01/CR-02/CR-03 paths was independently corroborated by reading the affected source (gaps 1-3 above), not merely trusted.

### Probe Execution

No `scripts/*/tests/probe-*.sh` probes are declared by this phase's PLANs or referenced by its success criteria — skipped (no conventional or phase-declared probes found).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| OBS-05 | All four plans (01-04) | Evaluation fidelity; this phase's slice: "latency measurement and budget derivation" | ⚠️ SATISFIED WITH GAPS | The pass ran, dispositions were made, and six values were written and are internally coherent — but the measurement/derivation *instrument* (`measure.py`, `decay.py`) is not trustworthy in general (gaps 1-2), and the provider-contract enforcement this phase's budget depends on is not wired at runtime (gap 3). `REQUIREMENTS.md` line 46 marks OBS-05 `[x]` complete at the requirement level (shared across 06.3.1-06.3.4); per this task's instructions, this is **not** treated as a revert/checkbox action — 06.3.3's specific slice is reported here as short on the censoring-awareness and provider-enforcement clauses. |

**Orphaned/anomalous requirement IDs:** `06.3.3-01-SUMMARY.md:60` and `06.3.3-02-SUMMARY.md:32` declare `requirements-completed: [LAT-01, LAT-02, LAT-03]` and `[LAT-04]` respectively. These IDs do **not** exist anywhere in `.planning/REQUIREMENTS.md`, in any PLAN's `requirements:` frontmatter (all four cite only `[OBS-05]`), or in `ROADMAP.md`'s phase requirement mapping (`Requirements: OBS-05`, Success Criteria numbered SC-1..SC-8, not LAT-01..04). This contradicts the verification dispatch's premise that these are real requirement IDs cited in PLAN frontmatter — they are not; they appear to be ad-hoc labels invented in the SUMMARY files during execution with no corresponding REQUIREMENTS.md entry. No requirement ID is actually unaccounted for (OBS-05 is the only one any PLAN declares, and it is tracked and checked off), but the phantom `LAT-0x` citations are a traceability defect worth correcting. **WARNING, not a blocker.**

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `eval/src/lancet_eval/latency.py` | 174-185 | `extract_node_durations` silently accepts uncensored data with no ceiling awareness | 🛑 Blocker | Feeds gap 1 — instrument can silently under-derive a budget under real censoring. |
| `eval/src/lancet_eval/decay.py` | 489-521 | Positional list misalignment on non-timeout drop | 🛑 Blocker | Feeds gap 2 — can flip decay verdict sign with no error. |
| `engine/src/config.rs` | 590-649 | Missing call to already-implemented validator | 🛑 Blocker | Feeds gap 3 — provider-attempt invariant is dead code at runtime. |
| `.planning/.../06.3.3-BUDGETS.md` | 91-101 | False claim ("both validate and validate_against_provider accepted") | 🛑 Blocker (documentation) | The phase's own closing derivation record misstates what was proven; corrective edit required once gap 3 is fixed. |

No `TBD`/`FIXME`/`XXX` unreferenced debt markers found in the covered files (debt-marker gate: clean).

### Human Verification Required

None. All findings in this report are objectively determinable from source code, committed records, and a single named regression test — no visual, UX, or external-service judgment calls are outstanding.

### Gaps Summary

The phase's *operational* execution (drive the pass, restart mid-run, dispose of every budget-blowing node, derive and write six coherent, nesting-valid, harness-safe budgets, and close on a recorded go/no-go) is real and the resulting numbers are defensible on the evidence in `06.3.3-MEASUREMENT.md` and `06.3.3-BUDGETS.md`. Three Critical code-review findings, independently verified against source rather than taken on the reviewer's word, are genuine failures of must_haves this phase's own plans committed to, not code-quality notes:

1. **The reusable censoring-safe percentile instrument (plan-01's central deliverable) is not actually censoring-safe.** `run_measurement_pass()`/`extract_node_durations()` never apply the census before deriving a percentile. It happened not to corrupt this run's *node-level* percentiles because node-level censoring was 0/320 for every node in this specific 320-record pass (raised ceilings absorbed everything), and the actual shipped values were hand-reconstructed via `drive_measurement_pass.py` plus manual analysis rather than through this buggy path — but that is a property of this dataset and this workaround, not of the code, and the code is what 06.3.4 or any future re-measurement will actually call.
2. **`decay.py`'s claimed "censoring-aware" property is false in general** — a record dropped for any reason other than a classified node-timeout desynchronizes `ordinals` from `latencies`, which can silently flip a decay verdict's sign. It did not fire on this run's `RetrieveHybrid` data (zero non-timeout drops, confirmed via the censoring census), so the reported `decay_present: False` verdict stands, but the underlying defect remains live for any future analysis with messier failure data.
3. **The provider-attempt invariant this phase's `generation_node_timeout_ms` derivation depends on is not enforced at engine startup**, and `06.3.3-BUDGETS.md`'s own "Engine startup proof" section makes a specific, checkable claim about this that is false as written. The current written value (65000ms) happens to satisfy the invariant (60000ms required), so no user-visible defect exists today — but the enforcement mechanism the phase's design and its own closing record rely on is dead code, and a later env-override of `generation_timeout_secs` past 32.5s would start the engine successfully in violation of the provider contract with no error.

None of these three reverse the go/no-go decision itself (its rationale — no disposition concluded "defect", populated graph store, adequate remaining allowance — does not depend on the broken code paths), but they are exactly the class of finding the phase's stated purpose ("establish, by measurement... the collapse was mis-sized timeout budgets") exists to prevent from recurring silently. Recommended: a small closure plan against `eval/src/lancet_eval/latency.py`/`measure.py` (wire the census into the percentile call), `eval/src/lancet_eval/decay.py` (parallel-structure the ordinal/latency pairing), `engine/src/config.rs` (call `validate_against_provider` from `validate()`), and a corrective edit to `06.3.3-BUDGETS.md`'s "Engine startup proof" section.

---

_Verified: 2026-09-07T15:30:00Z_
_Verifier: Claude (gsd-verifier)_
