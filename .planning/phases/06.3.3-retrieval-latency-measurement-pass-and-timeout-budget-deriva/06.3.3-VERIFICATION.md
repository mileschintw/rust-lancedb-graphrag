---
phase: 06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva
verified: 2026-09-08T09:50:16Z
status: gaps_found
score: 10/11 must-haves verified
covered_files: [".planning/REQUIREMENTS.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-01-PLAN.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-01-SUMMARY.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-02-PLAN.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-02-SUMMARY.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-03-PLAN.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-03-SUMMARY.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-04-PLAN.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-04-SUMMARY.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-05-PLAN.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-05-SUMMARY.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-06-PLAN.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-06-SUMMARY.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-BUDGETS.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-MEASUREMENT.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-REVIEW.md", ".planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-STORE-BASELINE.md", "config/config.example.toml", "config/config.toml", "config/config.verify.toml", "engine/src/bin/inspect_lancedb.rs", "engine/src/config.rs", "engine/src/tests.rs", "engine/src/tests/workflow_phase5_production.rs", "eval/scripts/drive_measurement_pass.py", "eval/src/lancet_eval/cli.py", "eval/src/lancet_eval/client.py", "eval/src/lancet_eval/decay.py", "eval/src/lancet_eval/latency.py", "eval/src/lancet_eval/measure.py", "eval/src/lancet_eval/preflight.py", "eval/src/lancet_eval/run.py", "eval/src/lancet_eval/thresholds.py", "eval/tests/conftest.py", "eval/tests/test_decay.py", "eval/tests/test_latency.py", "eval/tests/test_measure.py", "scripts/engine-test-targets.sh"]
covered_digest: "v1:sha256:d584c733237f05f3cfc3d98bc7608580db02fa23581e536723e49a8e58165c92"
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 8/10
  gaps_closed:
    - "A percentile over an observation set containing any censoring of kinds (a)-(c) is refused or returned explicitly flagged, never as if it were the true value (prior gap 1 / SC-2b instrument wiring)"
    - "eval/src/lancet_eval/decay.py ordinal-to-latency pairing is one parallel structure (prior gap 2)"
    - "The written set satisfies the engine's own startup validation and its provider-attempt rule on every settings load (prior gap 3 / SC-6b)"
  gaps_remaining:
    - "Zero usable observations for a node is reported as an explicit stated-reason outcome, never as a silently omitted row (PLAN-01 must_have; REVIEW CR-01 empty-survivor nesting fill-in)"
  regressions:
    - "derive_budgets_from_records omits nodes with empty survivor lists, then check_nesting_invariants fills missing outers from 0 — introduced/exposed by the gap-1 ceiling-aware extractor on the same function gap-closure added"
gaps:
  - truth: "Zero usable observations for a node is reported as an explicit stated-reason outcome carrying the observation count, never as a percentile of zero and never as a silently omitted row (06.3.3-01 must_have, specless edge: empty). Maps to 06.3.3-REVIEW.md CR-01."
    status: failed
    reason: "percentile_with_ci on an empty survivor list correctly returns is_available=False with reason 'Empty observation set: 0 usable observations', and derive_budget would refuse that result. derive_budgets_from_records then skips the node when not pct.is_available, so no proposed_records row is emitted. Pass-through still injects query_embedding_timeout_ms / graph_operation_timeout_ms from effective_cfg. check_nesting_invariants treats missing graph_node_timeout_ms and retrieve_timeout_ms as 0 and raises them to inner-sum + slack, which become BudgetDerivation.proposed_budgets / the persisted summary integers. Reproduced (not taken from the reviewer): the gap-1 fixture yields proposed_budgets graph_node_timeout_ms=14500 with no graph_node proposed_records row; a 100% at-ceiling RetrieveHybrid sample yields retrieve_timeout_ms=10500 with no retrieve proposed_records row. Gap-1 tests only assert the RetrieveHybrid proposed_records row."
    artifacts:
      - path: "eval/src/lancet_eval/measure.py"
        issue: "derive_budgets_from_records (lines 327-380): if not pct.is_available: continue with no refused row; then nesting_rep.resolved_budgets is what is persisted."
      - path: "eval/src/lancet_eval/latency.py"
        issue: "check_nesting_invariants defaults missing outers to 0 (lines 464, 498) and raises them when slack is insufficient."
      - path: "eval/tests/test_measure.py"
        issue: "test_derivation_flags_ceiling_censored_node_as_lower_bound does not assert proposed_budgets['graph_node_timeout_ms'] or empty-survivor rows."
    missing:
      - "When pct.is_available is False, append an explicit refused proposed_records row (observation count, reason, censored_count) and do not let nesting invent that key from 0."
      - "Run nesting only over keys that were derived or explicitly carried forward; never persist an invariant-raised value computed from a missing measurement."
      - "Add a regression test on the gap-1 fixture and a 100%-at-ceiling retrieve fixture that fails if graph_node/retrieve integers appear without a matching proposed_records row."
advisory:
  - finding: "config.verify.toml generation_timeout_secs is now 3 so two attempts fit in the 7000 ms overlay node budget (plan 06). Live LANCET_ENV=verify generation in (3s, 7s] will fail that previously could run until the node timer."
    category: other
    reason: "Intentional coherence repair, not a carried-forward gap. Phase 06.3.3 production config.toml is unchanged. Confirm verify-ingestion only needs ingest/embeddings if live generation in that band matters."
    evidence_status: "config/config.verify.toml generation_timeout_secs = 3; workflow_phase5_production.rs asserts 3"
---

# Phase 06.3.3: Retrieval Latency Measurement Pass and Timeout Budget Derivation Verification Report

**Phase Goal:** Establish, by measurement, whether the 2026-09-03 collapse was mis-sized timeout budgets or progressive resource exhaustion — then derive all six production budgets from the observed latency. Closes at the go/no-go for paid spend, so that decision sits on a phase boundary rather than mid-phase.

**Verified:** 2026-09-08T09:50:16Z
**Status:** gaps_found
**Re-verification:** Yes — after gap-closure plans 06.3.3-05 and 06.3.3-06

## Goal Achievement

The three previously FAILED gaps are closed in source and by named tests. The operational pass, written six budgets, and go/no-go record still stand. The fresh `06.3.3-REVIEW.md` BLOCKER is **not** a restatement of those three gaps and **is** an original PLAN-01 must-have that the first verification did not score as its own truth: empty/unusable node samples must be an explicit stated-reason row, never a silently omitted row. Gap-1's ceiling-aware extractor now produces empty survivor lists for untimed nodes (and for 100% at-ceiling nodes); `derive_budgets_from_records` omits them; nesting then writes unmeasured outer integers into `proposed_budgets`.

### Observable Truths

| # | Truth (Success Criterion / must-have) | Status | Evidence |
|---|---------|--------|----------|
| SC-1 | Graph population verified before timeout work | ✓ VERIFIED | Unchanged: `06.3.3-STORE-BASELINE.md`; `graph_disposition: "populated"` in the measurement record. |
| SC-2a | Measurement pass drive mechanics | ✓ VERIFIED | Unchanged: `06.3.3-MEASUREMENT.md` §2 (160 questions / 320 records, cheap model, no judge, env-only raised ceilings). |
| SC-2b | Percentile over censored observations is refused or explicitly flagged, never reported as the true value (prior gap 1) | ✓ VERIFIED | `extract_node_durations` returns `NodeDurationExtraction` with `dropped_at_ceiling`. `derive_budgets_from_records` sums drops + `CENSORED_NODE_TIMEOUT` into `percentile_with_ci(..., censored_count=censored)`. Named tests: `test_derivation_flags_ceiling_censored_node_as_lower_bound`, `test_derivation_censored_count_sums_ceiling_drops_and_timer_expiries` (2 passed). Residual empty-survivor hole is scored as T-11, not as a reopen of this wiring gap. |
| SC-3 | Mid-run engine restart splits pass into two segments | ✓ VERIFIED | Unchanged: restart at ordinal 160 in `06.3.3-MEASUREMENT.md` §2/§4. |
| SC-4 | Decay question answered against a pre-committed criterion | ✓ VERIFIED | Unchanged recorded verdict; instrument pairing bug closed (see decay artifact). |
| SC-5 | Every budget-blowing node investigated before widening | ✓ VERIFIED | Unchanged disposition table in MEASUREMENT §6 / BUDGETS containment check. |
| SC-6a | Six budgets written, in step across toml + Rust defaults | ✓ VERIFIED | Grep: 645 / 16647 / 38595 / 45674 / 1070 / 65000 in `config/config.toml`, `config.example.toml`, and `default_*_timeout_ms()` in `engine/src/config.rs`. Production toml not edited by plan 06. |
| SC-6b | Written set satisfies startup validation and provider-attempt rule on the settings-load path (prior gap 3) | ✓ VERIFIED | `EffectiveRagSettings::validate()` calls `self.workflow.validate_against_provider(self.generation_timeout_secs)?` (`config.rs:592`). `try_from_settings` calls `effective.validate()`. Named tests: `effective_settings_reject_provider_contract_violation_at_load`, `effective_settings_accept_provider_contract_at_boundary` (2 passed). `06.3.3-BUDGETS.md` Engine startup proof amended: no longer claims the 2026-09-07 live start proved the provider rule. |
| SC-7 | Graph traversal cost recorded if inherently slow | ✓ VERIFIED | Unchanged ~25–30 s figure in BUDGETS. |
| SC-8 | Spend capped; `$0.0199` not used as estimate | ✓ VERIFIED | Unchanged MEASUREMENT spend $0.08316 vs $5 cap; BUDGETS go/no-go. |
| T-11 | Zero usable observations for a node is an explicit stated-reason outcome, never a silently omitted row (PLAN-01 must_have). Fresh REVIEW CR-01. | ✗ FAILED | See Gaps. `percentile_with_ci` already returns a stated reason on empty data; the derivation consumer drops the node and nesting fabricates outers. |

**Score:** 10/11 truths verified (0 present, behavior-unverified)

### Why the REVIEW BLOCKER is in-contract

It is **not** a fourth invented must-have. PLAN-01 frontmatter already required the empty-sample row. The first verification scored only roadmap SC-1..8 (with SC-2/SC-6 split) and therefore never failed this clause. After plan 05, at-ceiling durations are dropped from `by_node`, so untimed nodes and 100%-clipped nodes become empty survivor lists — the case PLAN-01 named. Evidence gate: `measure.py` was modified after the prior `verified:` timestamp; the defect is reproducible (command output below), so it is not an unevidenced new-scope opinion.

It is **not** a reopen of prior gap 1. Gap 1's prescribed missing work (join census + at-ceiling drops into `percentile_with_ci` so `censored_status` is not unconditionally `clean`) is done and tested on RetrieveHybrid survivors.

### Deferred Items

None. Phase 06.3.4 consumes written production budgets; it does not fix derivation of empty survivor sets.

### Advisory (New Scope, Unevidenced)

| # | Finding | Category | Why Advisory |
|---|---------|----------|--------------|
| 1 | Verify overlay `generation_timeout_secs = 3` | other | Intentional plan-06 coherence repair; not a prior gap. Flagged so verify-env generation latency is not assumed unchanged. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `eval/src/lancet_eval/thresholds.py` | Committed decision inputs | ✓ VERIFIED | Unchanged. |
| `eval/src/lancet_eval/latency.py` | Extractor + classifier + percentile + derivation + nesting | ✓ VERIFIED (extractor/percentile); nesting default-0 is the T-11 consumer | `NodeDurationExtraction` present; empty-set `PercentileResult` has stated reason. |
| `eval/src/lancet_eval/decay.py` | Parallel pairing, censoring-aware | ✓ VERIFIED | `paired` tuples; `trend_ordinals` unzipped; `all_ordinals` for restart bound; `unusable_dropped_count` on verdict. Tests passed. |
| `eval/src/lancet_eval/measure.py` | Driver + `derive_budgets_from_records` | ⚠️ HOLLOW on empty survivors | Wiring of census→percentile is correct; empty `is_available` path omits rows then persists nesting-filled integers. |
| `eval/src/lancet_eval/client.py`, `run.py` | SSE timeout from settings | ✓ VERIFIED | `read=effective_read_timeout` / `read=eval_settings.gateway_timeout_secs`. |
| `engine/src/bin/inspect_lancedb.rs` | Store inspect | ✓ VERIFIED | Unchanged regression. |
| `06.3.3-STORE-BASELINE.md` / `MEASUREMENT.md` / `BUDGETS.md` | Records | ✓ VERIFIED | BUDGETS startup section corrected. |
| `config/config.toml`, `config.example.toml` | Six derived values | ✓ VERIFIED | Byte-identical with Rust defaults. |
| `config/config.verify.toml` | Coherent overlay | ✓ VERIFIED | `generation_timeout_secs = 3` with `generation_node_timeout_ms = 7000`. |
| `eval/runs/2026-09-06-measure-multihop_rag/` | Evidence | ✓ VERIFIED | Present (not re-driven; prohibition). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `compute_censoring_census` + `dropped_at_ceiling` | `percentile_with_ci` in `derive_budgets_from_records` | summed `censored_count` | WIRED | Prior NOT_WIRED closed. |
| `measure.py` ordinal/segment | `analyze_decay` trend | parallel `(ordinal, latency)` | WIRED | Prior PARTIAL closed; restart uses `all_ordinals`. |
| `try_from_settings` → `validate()` | `validate_against_provider` | `config.rs:592` | WIRED | Prior NOT_WIRED closed. |
| `client.py` SSE timeout | `run.py` client timeout | `EvalSettings.gateway_timeout_secs` | WIRED | Unchanged. |
| `fetch_neighborhood` | `inspect_lancedb` | production graph helper | WIRED | Unchanged. |
| MEASUREMENT dispositions | BUDGETS containment | node name | WIRED | Unchanged. |
| Empty `PercentileResult` | `proposed_records` / nesting | refuse row then nest | NOT_WIRED | T-11: skip + default 0. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `derive_budgets_from_records` | RetrieveHybrid `proposed_ms` (partial censoring) | surviving durations + censored_count | yes, flagged lower bound | ✓ FLOWING |
| `derive_budgets_from_records` | `graph_node_timeout_ms` on RetrieveHybrid-only fixture | missing key → 0 → inner sum + slack | fabricated, not measured | ✗ DISCONNECTED |
| `derive_budgets_from_records` | `retrieve_timeout_ms` on 100% at-ceiling retrieve | missing key → 0 → embedding + slack | fabricated, not measured | ✗ DISCONNECTED |
| `run_measurement_pass` summary | `proposed_budgets` | `nesting_rep.resolved_budgets` | integers only; `proposed_records` / `censored_by_node` dropped (REVIEW WR-01) | ⚠️ STATIC flags |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Ceiling-aware extract + derivation flags lower bound | `uv run --project eval pytest eval/tests/test_latency.py::test_extract_durations_drops_and_counts_at_ceiling_values eval/tests/test_measure.py::test_derivation_flags_ceiling_censored_node_as_lower_bound eval/tests/test_measure.py::test_derivation_censored_count_sums_ceiling_drops_and_timer_expiries -q` | 3 passed | ✓ PASS |
| Decay pairing + unusable count | `uv run --project eval pytest eval/tests/test_decay.py::test_decay_mid_sequence_unusable_record_does_not_shift_pairs eval/tests/test_decay.py::test_decay_reports_unusable_dropped_count -q` | 2 passed | ✓ PASS |
| Provider guard on `try_from_settings` | `cargo test --manifest-path engine/Cargo.toml --locked --lib -- tests::effective_settings_reject_provider_contract_violation_at_load tests::effective_settings_accept_provider_contract_at_boundary -- --exact` | 2 passed | ✓ PASS |
| Empty-survivor nesting fill-in | inline `derive_budgets_from_records` on gap-1-shaped records and 20 all-at-ceiling RetrieveHybrid records | `graph_node_timeout_ms=14500` absent from `proposed_records`; all-clip `retrieve_timeout_ms=10500` with records `['generation_node_timeout_ms']` only | ✗ FAIL (T-11) |
| Six production values identical | grep of six keys | 645 / 16647 / 38595 / 45674 / 1070 / 65000 | ✓ PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` declared or present — skipped.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| OBS-05 | Plans 01–06 | Evaluation fidelity; 06.3.3 slice is latency measurement and budget derivation | ⚠️ SATISFIED WITH GAPS | Pass ran, six production values written, go/no-go recorded, prior instrument/startup gaps closed. Reusable derivation still persists unmeasured nested integers when a node has zero survivors. OBS-05 remains `[x]` at REQUIREMENTS.md (family-level); this slice is short on the PLAN-01 empty-sample clause. |

**Orphaned IDs:** SUMMARY-01/02 still cite phantom `LAT-0x` IDs (not in REQUIREMENTS.md or PLAN `requirements:`). WARNING, not a blocker. Plans 05–06 correctly cite only `[OBS-05]`.

### Test Quality Audit

| Test File | Linked Req | Active | Skipped | Circular | Assertion Level | Verdict |
|-----------|-----------|--------|---------|----------|-----------------|---------|
| `eval/tests/test_measure.py` | OBS-05 / gap 1 | gap-1 tests active | live-journal helpers skip if run dir missing | no | value (`censored_lower_bound`, count==45) | OK for survivors; INSUFFICIENT for empty-survivor / nesting fill-in |
| `eval/tests/test_latency.py` | OBS-05 / extractor | active | none on new tests | no | value | OK |
| `eval/tests/test_decay.py` | OBS-05 / gap 2 | active | none on new tests | no | behavioral pairing | OK |
| `engine/src/tests.rs` | OBS-05 / gap 3 | active | none | no | value (reject 40s/33s, accept 32s) | OK |

**Disabled tests on requirements:** 0 blockers (journal skips are not the only coverage).
**Circular patterns detected:** 0.
**Insufficient assertions:** 1 — gap-1 tests cannot see fabricated `graph_node_timeout_ms`.

### Decision Coverage

No `*-CONTEXT.md` in this phase directory (shared `06.3.1-CONTEXT.md`). Gate skipped.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `eval/src/lancet_eval/measure.py` | 351-380 | Empty `is_available` skipped; nesting fills missing keys | 🛑 Blocker | T-11 / REVIEW CR-01. File modified since prior verification; reproduced. |
| `eval/src/lancet_eval/measure.py` | 538-571 | `proposed_records` / `censored_by_node` not copied into summary | ⚠️ Warning | REVIEW WR-01; plan 05 stored them on `BudgetDerivation` but persistence still drops them. |
| `eval/src/lancet_eval/measure.py` | 356-363 | Inner budgets pass through untagged | ⚠️ Warning | REVIEW WR-02; plan 05 deferred. |
| `eval/src/lancet_eval/measure.py` | 539-542 | `calibration_note` claims not an input to derived budgets | ⚠️ Warning | REVIEW WR-03; plan 05 deferred. |

No unreferenced `TBD`/`FIXME`/`XXX` in covered implementation files.

### Human Verification Required

N/A — infrastructure/measurement-instrument phase. T-11 is a FAILED source-and-command truth, not a ⚠️ PRESENT_BEHAVIOR_UNVERIFIED item.

### Gaps Summary

Prior gaps 1–3 are closed:

1. Censoring census and at-ceiling drops reach `percentile_with_ci`; RetrieveHybrid mixed-censoring records `censored_lower_bound(n)` not `clean`.
2. Decay ordinals and latencies are one parallel structure; unusable drops are counted; restart bound uses the full observed ordinal range.
3. `validate_against_provider` runs from `EffectiveRagSettings::validate` / `try_from_settings`; BUDGETS.md no longer claims the 2026-09-07 binary start proved that rule.

Remaining gap: PLAN-01 empty-sample contract / REVIEW CR-01. Do not call nesting on a partial map that defaults missing outers to 0. Refuse or carry forward every measured node key when survivors are empty, and persist that refusal — otherwise a future `lancet-eval measure` pass under collapse-shaped data can write a retrieve/graph_node integer that was never measured.

Shipped production `config.toml` values for this phase were reconstructed in BUDGETS.md from the 320-record pass (every runner node 320/320 OBSERVED at node level) and are not themselves the fabricated 14500/10500 figures.

---

_Verified: 2026-09-08T09:50:16Z_
_Verifier: Claude (gsd-verifier)_
