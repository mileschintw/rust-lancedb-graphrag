---
phase: 06.3.3
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/src/bin/inspect_lancedb.rs
  - engine/src/inspect_lancedb_tests.rs
  - scripts/engine-test-targets.sh
  - .planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-STORE-BASELINE.md
autonomous: true
requirements: [OBS-05]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Graph population in the surviving eval store is inspected and its counts recorded before any budget is derived, so query-time behaviour is never blamed for a store that was never populated."
    - "A store whose node, edge or entity counts are zero reclassifies the finding as ingest rather than query yield: the graph-yield question is recorded as suspended, not answered, and no graph budget is derived from a measurement of an empty store (specless edge: empty)."
    - "An entity with no incident edges is counted and reported as an isolated entity rather than omitted from the degree distribution, so an empty or single-element neighbourhood is a stated number and not a missing row (specless edge: empty)."
    - "A traversal from a named entity returns its bounded neighbourhood with a stated hop bound, and a seed with no incident edges returns an explicitly empty result with the seed echoed — never an error that loses which seed was tried (specless edge: empty)."
    - "Neighbourhood results are returned in a stated, stable order so that two runs against an unchanged store produce byte-identical output and a recorded traversal is reproducible (specless edge: ordering)."
    - "Every count and every traversal result is captured into a committed phase record, so the state of the store before the measurement pass is durable evidence rather than a transcript in someone's terminal."
    - "The starting store row counts and the starting PostgreSQL checkpoint row count are recorded before the pass, and the record states that no re-seed and no compaction ran between the recording and the pass."
    - "The inspection is strictly read-only: it opens tables, counts and reads, and writes nothing to the store."
  artifacts:
    - "engine/src/bin/inspect_lancedb.rs — a store-wide graph-population mode and a bounded entity-neighbourhood mode, alongside the existing document-scoped mode"
    - ".planning/phases/06.3.3-.../06.3.3-STORE-BASELINE.md — the committed D-05 and D-22 record"
  key_links:
    - "entity_edges.source_node_id and entity_edges.target_node_id reference entities.entity_id; the degree distribution is computed over the union of both columns, and entities appearing in neither are the isolated set"
    - "The two designated graph canaries committed by 06.3.2-03 shipped with their gold entities unconfirmed; this inspection is the named producer of that confirmation and closes the deferral here"
    - "A zero-count finding suspends the graph-presence investigation floor the AI-SPEC states for graph_presence_rate — the finding becomes ingest, and 06.3.4 inherits the suspension rather than a missed floor"
  prohibitions:
    - "The inspection must not write, create, compact, delete or re-seed anything in the store; re-seeding is explicitly out of scope and would destroy the condition under test"
    - "No modification of eval/runs/2026-09-03-multihop_rag/"
    - "A pinned test count in scripts/engine-test-targets.sh may only be raised in the same commit as the tests that moved it; lowering a count to clear a red gate is never the correct response"
    - "The inspection must not print or persist chunk content, entity summaries, document raw content or any embedding vector — counts, identifiers, types and structural facts only"
---

<objective>
Establish what is actually in the surviving eval store before anything measures it, and record the
starting state the measurement pass will consume.

Purpose: the graph arm of the 2026-09-03 run produced 494 timeouts at the inner graph budget and
essentially no facts. That is consistent with a slow graph, and equally consistent with a graph
that was never populated — and those two findings send the phase in opposite directions. One is a
budget to derive; the other is an ingest defect and the graph-yield question is not a query
question at all. The roadmap makes this ordering explicit: population is verified before any
timeout work. Separately, the measurement pass consumes the store's "as-is" condition by running
against it, so the starting row counts must be captured while they still describe the original
state.

Output: a store-wide graph-population mode and a bounded neighbourhood mode on the existing
`inspect_lancedb` tool, and `06.3.3-STORE-BASELINE.md` recording population, degree distribution,
a hand-run traversal, the starting PostgreSQL checkpoint rows, and the canary-entity confirmation
06.3.2-03 deferred to this phase.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-CONTEXT.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-AI-SPEC.md
@.planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-RESEARCH.md
@.planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-PATTERNS.md
@CLAUDE.md
@rust-guidelines.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Store-wide graph population and bounded neighbourhood modes on inspect_lancedb</name>
  <files>engine/src/bin/inspect_lancedb.rs, engine/src/inspect_lancedb_tests.rs, scripts/engine-test-targets.sh</files>
  <reversibility rating="reversible">Additive read-only modes on an existing diagnostic binary; the document-scoped mode is untouched.</reversibility>
  <read_first>
    - `rust-guidelines.md` — read before writing any Rust in this repository, per `CLAUDE.md`.
    - `engine/src/bin/inspect_lancedb.rs` in full (373 lines) — the column-accessor helpers at 46-84, `query_columns` at 85-101, `row_count` at 102-105, `derive_durable_facts` at 106-256, `settings_path` at 257-276, `predicate` at 277-280, `inspect_document` at 281-334, the `#[cfg(test)] #[path = "../inspect_lancedb_tests.rs"]` module attachment at 335-338, and `main`'s hand-rolled argument loop at 342-373. Note that `main` currently rejects any unrecognised argument and requires `--document-id`; both change.
    - `engine/src/db/mod.rs:140-160` — `nodes_table`, `edges_table`, `entities_table`, `entity_edges_table` and `get_or_create_table`; and `:216-256` — `edges_schema`, `entities_schema` and `entity_edges_schema`. The entity graph is `entities` joined by `entity_edges`, whose `source_node_id` and `target_node_id` hold `entity_id` values.
    - `engine/src/inspect_lancedb_tests.rs` — the existing 18 tests and their fixture idiom, so new tests follow it rather than inventing a second style.
    - `scripts/engine-test-targets.sh` — its header explaining that pinned counts are measured values updated in the same commit as the tests that move them, and the `inspect_lancedb` pin at line 77.
    - `engine/src/service.rs:469-520` — `query_graph`, the `graph_traversal` span and `GraphAugmentationOutcome`, so the neighbourhood mode walks the same relation production traverses.
    - `.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-CONTEXT.md` — D-05, D-22, D-03.
  </read_first>
  <behavior>
    - **Population counts:** invoked in graph-population mode against a store, the tool emits row counts for the nodes, edges, entities, entity-edges, documents and staged-documents tables in one machine-readable object.
    - **Degree distribution:** the same output carries the entity degree distribution — minimum, median, upper percentile and maximum degree — computed over the union of the entity-edge source and target columns.
    - **Isolated entities counted:** entities appearing in neither entity-edge column are counted and reported as an explicit isolated-entity count, not dropped from the distribution.
    - **Empty store:** run against a store whose entity and entity-edge tables are empty, the tool emits zero counts, an explicitly empty distribution and a stated marker that the graph is unpopulated — it does not error and does not omit the fields.
    - **Neighbourhood:** invoked with a seed entity identifier, the tool emits that seed's incident edges out to the stated hop bound, with the relation type and the neighbour identifier on each.
    - **Seed with no edges:** a seed present in the entities table with no incident edge emits an explicitly empty neighbourhood echoing the seed, not an error.
    - **Seed absent:** a seed not present in the entities table is reported as absent, naming the seed, distinctly from a seed that is present but isolated.
    - **Stable order:** two invocations against an unchanged store emit byte-identical output, because results are ordered by a stated key.
    - **Hop bound honoured:** raising the hop bound cannot return fewer edges, and the emitted result states the bound that produced it.
    - **Read-only:** every mode opens tables, counts and reads; no invocation creates, writes, compacts or deletes.
    - **Existing mode unchanged:** the document-scoped invocation produces exactly the output it produces today.
    - **Usage:** an invocation naming no mode, or naming two, exits non-zero with a usage message listing every mode.
  </behavior>
  <action>
    Extend `inspect_lancedb` with two additional read-only modes. Keep the existing document-scoped
    mode exactly as it is; this is additive, and the tool is the sanctioned path for this inspection
    because it is the one D-05 names and the one that already opens the store through the validated
    database manager.

    Rework `main`'s argument loop so it selects among modes rather than requiring a document
    identifier. Today an unrecognised argument returns a usage error and `--document-id` is
    mandatory; both need to change, and the usage message must list every mode. Exactly one mode
    per invocation — reject zero and reject two, so an operator never has to guess which one ran.

    The graph-population mode takes no target and reports the whole store. Emit row counts for the
    six tables, the entity degree distribution, and the isolated-entity count, in one serialisable
    object printed as the existing mode prints its inspection. Compute degree over the union of the
    entity-edge source and target columns, since the relation is stored once per edge and an
    entity's degree is its appearances on either side. Count entities that appear on neither side
    explicitly: an isolated entity is a real structural fact about a graph built from sparse
    extraction, and dropping it from the distribution would make a mostly-disconnected graph read
    like a healthy one. When the entity or entity-edge table is empty, emit zeros, an empty
    distribution and an explicit marker that the graph is unpopulated — the caller needs the shape
    of the answer to be the same whether or not the graph exists, because an empty result here is a
    finding rather than an error.

    The neighbourhood mode takes a seed entity identifier and an optional hop bound with a stated
    default. Expand outward from the seed over the entity-edge relation to the bound, emitting each
    edge with its relation type and the neighbour it reaches. Distinguish three outcomes clearly: a
    seed that is absent from the entities table, a seed that is present with no incident edges, and
    a seed with a neighbourhood. The middle case is not an error — a sparsely-extracted entity
    legitimately has no edges — and conflating it with the first would make an inspection unable to
    tell "the entity was never extracted" from "the entity was extracted but connects to nothing",
    which is exactly the distinction this phase needs.

    Order every emitted collection by a stated key so two runs against an unchanged store produce
    identical bytes. A recorded traversal that cannot be reproduced is not evidence.

    Bound the expansion. Give the hop bound a small default and a hard ceiling, and cap the total
    edges returned, so a hub entity in a dense region cannot make the tool run unbounded against a
    store this phase is not allowed to modify.

    Emit structural facts only: identifiers, types, counts, relation types and degrees. Never chunk
    content, entity or edge summaries, document raw content, or any embedding vector. The output
    goes into a committed phase record.

    Add tests in the existing test module following its fixture idiom, one per behavior bullet, and
    update the pinned `inspect_lancedb` count in `scripts/engine-test-targets.sh` in the same commit
    as the tests that moved it. The script's own header states the rule: lowering a value to make
    the gate pass is never the correct response to a red gate.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked --bin inspect_lancedb</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --manifest-path engine/Cargo.toml --locked` exits 0 with zero failures.
    - `cargo clippy --manifest-path engine/Cargo.toml --all-targets --locked -- -D warnings` exits 0 and `cargo fmt --manifest-path engine/Cargo.toml -- --check` exits 0.
    - `sh scripts/engine-test-targets.sh` exits 0 and its `inspect_lancedb` pin equals the new measured count, raised in this commit, with the delta stated in the summary.
    - A named test per behavior bullet above.
    - A named test builds a fixture store whose entity and entity-edge tables are empty and asserts the emitted object carries zero counts, an empty distribution and the unpopulated marker — and asserts the process exit status is success, so an empty graph is a finding rather than a failure.
    - A named test builds a fixture with one entity carrying no incident edge and asserts it is counted in the isolated-entity total and that the total is non-zero.
    - A named test asserts a seed absent from the entities table and a seed present but isolated produce two distinguishable outcomes, and asserts each names the seed it was given.
    - A named test invokes the neighbourhood mode twice against one unchanged fixture and asserts the two outputs are byte-identical.
    - A named test asserts raising the hop bound never returns fewer edges than a lower bound on the same fixture, and asserts the emitted result states the bound used.
    - A named test asserts the existing document-scoped invocation's output is unchanged against an existing fixture.
    - A named test asserts an invocation naming no mode and one naming two modes both exit non-zero, and that the message lists every mode.
    - This command confirms no write path was introduced into the inspector: `grep -v '^[[:space:]]*//' engine/src/bin/inspect_lancedb.rs | grep -cE "\.(add|delete|merge_insert|update|create_table|optimize|compact)\(" ` outputs `0`. The pattern matches a method call, not a mention, and it reports `0` against the pre-change file, so it is a regression guard rather than a pass-by-default. <!-- planner-discipline-allow: compact --> <!-- planner-discipline-allow: create_table -->
    - A named test asserts no content or vector column reaches the new modes' output: the **serialised** graph-population and neighbourhood objects contain none of the substrings `raw_content`, `summary_vector`, `name_vector` or `embedding`. The assertion runs over emitted output, never over source, so prose in the implementation naming what must be excluded cannot invalidate it. <!-- planner-discipline-allow: embedding -->
  </acceptance_criteria>
  <done>
    A single read-only command reports what is actually in the graph — counts, degree distribution,
    isolated entities — and a second walks a bounded neighbourhood from a named seed, both
    reproducible byte for byte, with an empty graph reported as a number rather than an error.
  </done>
</task>

<task type="auto">
  <name>Task 2: Record the store baseline — population, traversal, checkpoint rows, canary entities</name>
  <files>.planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-STORE-BASELINE.md</files>
  <precondition>The eval store at the path `config/config.eval.toml` pins is present with its tables intact, and the PostgreSQL eval schema named in `EvalSettings.database_url` is reachable. If either is missing, halt and report — re-seeding is out of scope and would destroy the condition under test.</precondition>
  <reversibility rating="reversible">A committed record of observations; nothing is changed in the store.</reversibility>
  <read_first>
    - Task 1's output: the two new modes and their emitted object shapes.
    - `config/config.eval.toml` — the overlay pins only `lancedb_path`, which is the store to inspect.
    - `eval/src/lancet_eval/config.py:53-80` — `EvalSettings.database_url` and its `search_path` query parameter; `eval/src/lancet_eval/seed.py:35-70` — `assert_schema_isolated` and `pg_schema_of`, the sanctioned way to resolve the eval schema name without touching the dev schema.
    - `gateway/db/schema.eval.hcl:138-170` — the `workflow_checkpoints` table in the eval schema, whose row count is the D-22 baseline for the checkpoint-growth hypothesis.
    - `eval/corpora/multihop_rag/canary.jsonl` and `06.3.2-03-SUMMARY.md` — the two designated graph canaries and the recorded deferral of their entity confirmation to this phase.
    - `.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-AI-SPEC.md` — the `graph_presence_rate` investigation floor and its explicit suspension clause when this inspection finds zero counts.
    - `.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-CONTEXT.md` — D-05, D-22, D-32, D-31.
  </read_first>
  <action>
    Run the inspection and write the record. This task produces evidence, not code.

    Run the graph-population mode against the eval store and capture its output verbatim into the
    record. Then choose a seed entity by a rule a reviewer can re-run rather than by eye — take the
    highest-degree entity from the distribution, breaking ties by identifier ascending — and run the
    neighbourhood mode from it, capturing that output verbatim too. State the rule alongside the
    result so the traversal is reproducible.

    Record the starting PostgreSQL checkpoint row count in the eval schema, resolved through the
    existing schema-isolation helper rather than by naming a schema literally, so the count cannot
    accidentally be taken from the development schema. Record the LanceDB directory's version and
    fragment counts alongside it if the store exposes them. These two numbers are the baseline for
    the accumulated-store-state hypothesis the restart discriminator tests, and they are only
    obtainable now: the measurement pass adds checkpoint rows and advances store versions, so
    running it consumes the very "as-is" condition being recorded. Re-seeding is out of scope, so
    the condition cannot be restored once spent. State that consequence in the record explicitly —
    it is also why the corrected re-drive in the next sibling starts from a post-pass store rather
    than this one.

    State, as a signed observation with the wall-clock time it was taken, that no re-seed, no
    compaction and no checkpoint truncation ran between this recording and the measurement pass.
    That sentence is the whole of D-22's guarantee; without it the baseline is a number with no
    claim attached.

    Close the deferral 06.3.2-03 recorded. That plan committed two designated graph canaries whose
    gold entities were explicitly not yet confirmed present in the entities table, and named this
    inspection as the producer of the confirmation. Look each canary's gold entities up and record
    the result. If they are present, say so and the canary floor stands. If they are absent, say so
    plainly and record which of the two readings applies — a wrongly chosen canary, or an extraction
    gap — with the evidence for the choice. Do not quietly relax the canary; the whole value of that
    check is that it is the only place a missing graph hit may fail anything.

    Then state the reclassification rule and apply it. If the node, edge or entity counts are zero,
    the finding is ingest, not query yield: record that the graph-yield question is suspended rather
    than answered, that the graph-presence investigation floor the AI-SPEC states is suspended with
    it, and that no graph budget may be derived from a measurement taken against an empty store.
    Carry that suspension forward explicitly so the next sibling inherits a stated suspension rather
    than a silently missed floor. If the counts are non-zero, record that the graph-yield question
    remains open and is answered by measurement, not by this inspection.

    Finally, record what this inspection does not establish. It shows what is in the store; it does
    not show how long traversing it takes. Latency is the measurement pass's subject, and a reader
    of this record must not be able to mistake a healthy population for a healthy graph budget.
  </action>
  <verify>
    <automated>D=.planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva; test -s $D/06.3.3-STORE-BASELINE.md &amp;&amp; grep -qiE "isolated" $D/06.3.3-STORE-BASELINE.md &amp;&amp; ! grep -qiE "^[^|]*\bTBD\b" $D/06.3.3-STORE-BASELINE.md &amp;&amp; echo store-baseline-complete</automated>
  </verify>
  <acceptance_criteria>
    - `06.3.3-STORE-BASELINE.md` exists, is committed, and carries no unresolved placeholder in any field it claims to record.
    - The record carries the graph-population output verbatim, including the six table row counts, the degree distribution and the isolated-entity count.
    - The record names the seed-selection rule, states the seed it produced, and carries the neighbourhood output verbatim, so a reviewer can re-run the rule and reproduce the traversal.
    - The record carries the starting PostgreSQL checkpoint row count for the eval schema, and states that the schema was resolved through the existing isolation helper rather than named literally.
    - The record carries a dated statement that no re-seed, no compaction and no checkpoint truncation ran between the recording and the measurement pass.
    - The record states that running the measurement pass consumes the as-is condition — checkpoint rows are added and store versions advance — and that the condition cannot be restored because re-seeding is out of scope.
    - The record resolves 06.3.2-03's deferred canary-entity confirmation for both designated graph canaries, with a present-or-absent verdict per canary and, on absence, which of the two readings applies and the evidence for it.
    - The record applies the zero-count reclassification rule explicitly: either it states that counts are non-zero and the graph-yield question stays open to measurement, or it states that counts are zero, the finding is ingest, the graph-yield question and the graph-presence investigation floor are both suspended, and no graph budget may be derived from this store.
    - The record states what it does not establish — that population is not latency.
    - `git status --short` shows the eval store directory unchanged, and this command confirms the store was not written to during the inspection: the store path reported by `config/config.eval.toml` has no file whose modification time is newer than the recorded inspection start, asserted and stated in the summary.
    - `git status --short eval/runs/2026-09-03-multihop_rag/` reports no modification to the retained evidence.
  </acceptance_criteria>
  <done>
    The state of the graph before anything measures it is durable, committed evidence — counts,
    degree distribution, a reproducible traversal, the starting checkpoint rows — and the
    zero-count reclassification has been applied one way or the other rather than left implicit.
  </done>
</task>

</tasks>

<out_of_scope>
| Item | Owner | Why not here |
|---|---|---|
| Re-seeding or rebuilding the eval store | Not planned — explicitly out of scope in D-05. | The surviving store is the condition under test; rebuilding it destroys the phase's subject. |
| Any graph latency measurement | 06.3.3-03 | This plan establishes what is in the store, never how long traversing it takes. |
| Deriving `graph_operation_timeout_ms` or `graph_node_timeout_ms` | 06.3.3-04 (D-13, D-15) | Budget values are derived from measurement, and only after this record exists. |
| Fixing an extraction or ingest defect this inspection exposes | 06.3.4, or a follow-on phase | D-03 bounds graph scope to "make it run and fix outright bugs"; an ingest gap discovered here is recorded and routed forward under the D-46 back-edge rule, never backwards into a closed sibling. |
| Tuning traversal depth, fan-out or candidate limits | Not planned — deferred in D-03. | Graph-RAG quality work is explicitly out of the family's scope. |

</out_of_scope>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This plan's contribution. The complete phase inventory is the `artifacts_this_phase_produces`
section of `06.3.3-01-PLAN.md`.

| Kind | Artifact |
|---|---|
| New Rust CLI mode | `inspect_lancedb --graph-population` — six table row counts, entity degree distribution, isolated-entity count, unpopulated marker |
| New Rust CLI mode | `inspect_lancedb --entity <id> [--max-hops N]` — bounded neighbourhood expansion with absent, isolated and populated outcomes distinguished |
| Modified Rust CLI surface | `main`'s argument loop becomes mode-selecting; the document-scoped mode is unchanged |
| New Rust tests | additions to `engine/src/inspect_lancedb_tests.rs`, with the pinned count in `scripts/engine-test-targets.sh` raised in the same commit |
| New phase record | `06.3.3-STORE-BASELINE.md` — D-05 population and traversal, D-22 starting store and checkpoint rows, the canary-entity confirmation, the zero-count reclassification verdict |
| New config keys | None |
| New dimension names | None |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval LanceDB store → inspector process | A store this phase is forbidden to modify is opened by a tool that must remain read-only. |
| store contents → committed phase record | Corpus-derived content could reach a git-tracked document. |
| PostgreSQL eval schema → baseline record | A schema name resolved from a connection string containing credentials. |
| operator arguments → inspector | A seed identifier and a hop bound supplied on the command line drive expansion over a real store. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.3-08 | Tampering | inspector writing to the store under test | high | mitigate | Every mode opens, counts and reads only. A grep gate asserts no add, delete, merge, update, create, optimize or compact call exists in the inspector, and it reports zero against the pre-change file so it is a regression guard. A modification-time check on the store path is stated in the summary. |
| T-06.3.3-09 | Information Disclosure | corpus content reaching a committed phase record | high | mitigate | The new modes emit identifiers, types, counts, relation types and degrees only. A named test asserts the serialised output contains no raw-content, summary-vector, name-vector or embedding field. |
| T-06.3.3-10 | Information Disclosure | PostgreSQL connection string in the baseline record | high | mitigate | The schema name is resolved through the existing isolation helper and only the schema name and a row count are recorded; no connection string, host, user or password is written to the record. |
| T-06.3.3-11 | Denial of Service | unbounded neighbourhood expansion from a hub entity | medium | mitigate | The hop bound has a small default and a hard ceiling, and the total edges returned is capped. A named test asserts the ceiling holds against a dense fixture. |
| T-06.3.3-12 | Repudiation | an unreproducible traversal presented as evidence | medium | mitigate | Results are ordered by a stated key and a named test asserts two runs against an unchanged store are byte-identical; the seed is chosen by a stated re-runnable rule recorded alongside the output. |
| T-06.3.3-SC | Tampering | npm/pip/cargo/uv installs | high | accept | No package-manager install task exists: the change is additive to an existing binary using already-vendored crates. `cargo test --locked` is an acceptance criterion. No `[ASSUMED]` or `[SUS]` package is introduced. |
</threat_model>

<verification>
- `cargo test --manifest-path engine/Cargo.toml --locked` exits 0 with zero failures.
- `cargo clippy --manifest-path engine/Cargo.toml --all-targets --locked -- -D warnings` exits 0.
- `cargo fmt --manifest-path engine/Cargo.toml -- --check` exits 0.
- `sh scripts/engine-test-targets.sh` exits 0 with the `inspect_lancedb` pin raised in this commit.
- `cd gateway && go test ./...` exits 0 — no cross-runtime regression.
- `06.3.3-STORE-BASELINE.md` is committed and carries no unresolved placeholder.
- `git status --short eval/runs/2026-09-03-multihop_rag/` reports no modification to the retained evidence.

</verification>

<success_criteria>
- ROADMAP SC-1 satisfied: graph population in the surviving eval store is verified before any timeout work — node, edge and entity-edge counts, degree distribution and a hand-run traversal from a known entity, using `engine/src/bin/inspect_lancedb.rs`, with no re-seeding.
- ROADMAP SC-2's starting-condition requirement is half satisfied here: the starting store and PostgreSQL checkpoint row counts are recorded before the pass, with the no-re-seed and no-compaction claim attached.
</success_criteria>

<output>
Create `.planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-02-SUMMARY.md` when done.
</output>

## Revise-before-execute

Read this before executing. Each row is an input this plan assumed unavailable at planning time.
Confirm the producer has landed and the contract matches, then execute.

| Assumed-unavailable input | Produced by | Effect on this plan if still absent |
|---|---|---|
| The committed canary set `eval/corpora/multihop_rag/canary.jsonl` and its two designated graph canaries | **06.3.2** plan `06.3.2-03` (D-11, D-31). Not executed at planning time. | Task 2's canary-entity confirmation has nothing to confirm. Record the deferral as still open and name 06.3.2-03 as its producer; do not invent canary identifiers. |
| `06.3.2-03-SUMMARY.md`'s recorded deferral of the canary-entity confirmation to this phase | **06.3.2** plan `06.3.2-03`, acceptance criterion on its task 2 | If the summary does not carry the deferral, still perform the confirmation — the AI-SPEC's canary composition rule states the entities must be verified present after this inspection. |
| `get_or_create_table` failing closed on a non-missing-table error | **06.3.1** plan `06.3.1-02` (D-24, D-25) | Until it lands, an inspection against a store with a genuinely damaged table could silently see an empty recreated table and record a false zero. If 06.3.1-02 has not landed, state that caveat in the record — a zero count would be ambiguous rather than conclusive. |
| Graph population counts, degree distribution, traversal result | **This plan.** Nothing upstream produces them. | These are the phase's own first outputs; every later sibling reads them from this record. |
| Whether the graph is populated at all | Unknown until this task runs | The record must handle both outcomes. Do not pre-write a conclusion in either direction. |
| Whether the two graph canaries' entities are in the store | Unknown until this task runs | If absent, record which reading applies with its evidence; do not relax the canary floor. |
| The current measured `inspect_lancedb` test count | Measured by running the suite in this task | The pin at `scripts/engine-test-targets.sh:77` is `18` at planning time. Raise it to the new measured value in the same commit; never lower it to clear a gate. |