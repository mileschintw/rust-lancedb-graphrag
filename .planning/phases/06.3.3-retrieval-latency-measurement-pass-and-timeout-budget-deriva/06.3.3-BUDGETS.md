# Phase 06.3.3 production timeout budget derivation

**Date written:** 2026-09-07  
**Inputs:** `06.3.3-MEASUREMENT.md`, `06.3.3-STORE-BASELINE.md`, `eval/src/lancet_eval/thresholds.py` (`COMMITTED_THRESHOLDS`), `eval/src/lancet_eval/latency.py` (`derive_budget`, `derive_provider_contract_budget`, `check_nesting_invariants`, `check_harness_ceilings`).  
**Committed rule:** `proposed_ms = ceil(p95 × multiplier + allowance)` with `multiplier = 1.5`, `derivation_percentile = 0.95`, `allowance_ms = 0`, `slack_ms = 500` (nesting). Generation is **not** on this rule.

This document is the reconstruction trail for the six production `[engine.workflow]` budgets written to `config/config.toml`, `config/config.example.toml`, and `engine/src/config.rs` serde defaults in the same commit. `config/config.eval.toml` still overrides only `lancedb_path`. `config/config.verify.toml` is unchanged (test fixture).

## Containment check (ran before the write)

Disposition set in `06.3.3-MEASUREMENT.md` §6 (by workflow node name): `ReformulateQuery`, `ExtractGraphContext`, `RetrieveHybrid`, `AssemblePrompt`, `GenerateAnswer`.

| Budget field | Direction vs previous production value | Mapped disposition node | Contained? |
|---|---|---|---|
| `retrieve_timeout_ms` | widened 10000 → 16647 | `RetrieveHybrid` | yes |
| `graph_node_timeout_ms` | widened 15000 → 45674 | `ExtractGraphContext` | yes |
| `graph_operation_timeout_ms` | widened 4000 → 38595 | `ExtractGraphContext` (inner Cypher; no separate disposition row; covered by that node's investigation) | yes |
| `query_embedding_timeout_ms` | narrowed 10000 → 645 | `ExtractGraphContext` (inner embedding; arm-isolated) | n/a (not widened) |
| `prompt_timeout_ms` | narrowed 2000 → 1070 | `AssemblePrompt` | n/a (not widened) |
| `generation_node_timeout_ms` | unchanged 65000 | `GenerateAnswer` | n/a (not widened) |

**Result:** every widened budget is contained in the disposition set. Halt was not required.

`AssemblePrompt` disposition text suggested informal widening to 3–4s. The committed rule is p95 × 1.5, which yields 1070 ms. The multiplier was not amended after seeing the data. Observed max duration 2221 ms therefore sits **above** the written prompt budget; that tail is not covered by the p95 rule and is recorded here rather than used to change the multiplier.

## Derivation table

Recompute: `ceil(percentile_value_ms × 1.5)` unless the rule column says otherwise.

| Budget | Previous | Percentile source | p95 (ms) | n | Censoring status | Multiplier | Additive allowance | Rule | Written (ms) | Direction | Invariant-driven? |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `query_embedding_timeout_ms` | 10000 | `graph-off` arm duration (inner embedding; no per-node wire instrument) | 430.0 | 160 | clean at arm (0 node timeouts); **arm-difference / arm-isolated estimate**; span corroboration: **none** (`embedding_request` omitted from `RETAINED_GRAPH_SPAN_NAMES`) | 1.5 | 0 | p95 × 1.5 (ceil) | 645 | narrowed | no |
| `retrieve_timeout_ms` | 10000 | `RetrieveHybrid` node timings, all records | 11098.0 | 320 | clean (0/320 censored at 60s ceiling) | 1.5 | 0 | p95 × 1.5 (ceil) | 16647 | widened | no |
| `graph_operation_timeout_ms` | 4000 | paired difference `graph-on − graph-off` over **surviving** pairs | ≥ 25730.0 | 139 (21 excluded) | **censored-from-above lower bound**: graph-on `GRAPH_TIMEOUT` rate 13.125% (21/160) > `max_tolerated_graph_timeout_rate` 0.10. Not a measured population percentile. | 1.5 | 0 | p95 × 1.5 (ceil) applied to the lower-bound p95 | 38595 | widened | no |
| `graph_node_timeout_ms` | 15000 | `ExtractGraphContext` on `graph-on` arm | 30449.0 | 160 | node envelope OBSERVED (0/160 node-level timeouts); 21 inner `GRAPH_TIMEOUT` notices in the same stratum | 1.5 | 0 | p95 × 1.5 (ceil) | 45674 | widened | no |
| `prompt_timeout_ms` | 2000 | `AssemblePrompt` node timings, all records | 713.0 | 320 | clean (0/320 censored at 10s ceiling); max observed 2221 ms | 1.5 | 0 | p95 × 1.5 (ceil) | 1070 | narrowed | no |
| `generation_node_timeout_ms` | 65000 | **not a percentile** | (measured p95 20889.0, n=320, **non-derivable from this pass**) | 320 | clean under 65s ceiling | 2 attempts | 5000 ms slack | provider contract: `2 × generation_timeout_secs(30) × 1000 + 5000` | 65000 | unchanged | no |

Checks:

- `ceil(430.0 × 1.5) = 645`
- `ceil(11098.0 × 1.5) = 16647`
- `ceil(25730.0 × 1.5) = 38595` (lower bound × multiplier)
- `ceil(30449.0 × 1.5) = 45674`
- `ceil(713.0 × 1.5) = 1070`
- `2 × 30 × 1000 + 5000 = 65000`

`reformulate_timeout_ms` is **not a derived row**. It remains 5000. Measured p95 on this pass: 0.0 ms (n=320, reformulation disabled in eval). Recorded as an **open input**.

## Invariants

Passing `WorkflowSettings::validate` (graph node ≥ sum of inners) and `validate_against_provider` is **necessary but not sufficient**. The engine floor admits **equality**, in which an enclosing budget can never fire. That equality case is the incoherence D-17 documented and the 2026-09-03 run confirmed (494 inner graph-operation timeouts, zero outer graph-node timeouts). This record requires **strict inequality with slack ≥ 500 ms** (`COMMITTED_THRESHOLDS.slack_ms`).

### Nesting group 1 — `graph_node` contains `query_embedding` + `graph_operation`

- Inner sum: 645 + 38595 = **39240 ms**
- Outer: **45674 ms**
- Slack: **6434 ms** (≥ 500)
- Fires first: **`graph_operation_timeout_ms`** on traversal overrun (non-fatal `GRAPH_TIMEOUT`); the outer envelope can still fire if the whole `ExtractGraphContext` node exceeds 45674 ms.
- Conflict policy: **did not fire**. Independently derived outer p95×1.5 already exceeds inner sum + slack. Inner values were not shaved. The outer row is **not** labelled invariant-driven.

### Nesting group 2 — `retrieve` vs `query_embedding` (latency.py relationship)

- Inner: 645 ms
- Outer: 16647 ms
- Slack: **16002 ms**
- Fires first: **`query_embedding_timeout_ms`** if the inner embedding call overruns (that call actually lives inside `ExtractGraphContext`; this group is the committed nesting report shape, not a claim that RetrieveHybrid embeds).
- Conflict policy: **did not fire**.

### Harness ceilings (two different quantities)

Eval defaults: `gateway_timeout_secs = 300` (SSE per-read-gap), `question_deadline_secs = 600` (wall-clock).

**1. Largest single written runner-level node budget vs SSE read timeout**

- Largest: `generation_node_timeout_ms` = **65000 ms**
- SSE read timeout: **300000 ms**
- `65000 < 300000` — **pass**
- This check is **not** a sum.

**2. Worst-case sum of written runner-level node budgets vs question deadline**

Runner-level keys (same set `check_harness_ceilings` uses): reformulate 5000 + retrieve 16647 + graph_node 45674 + prompt 1070 + generation 65000 = **133391 ms**.

- Question deadline: **600000 ms**
- `133391 < 600000` — **pass**
- Inner budgets are not added on top of `graph_node` (that would double-count the graph path).

No check in this record compares a sum against the SSE read timeout.

## Engine startup proof

**Amendment (2026-09-08, 06.3.3 gap-closure round):** this section was rewritten after `06.3.3-VERIFICATION.md` gap 3 found that the original closing sentence claimed the recorded engine start had proven the provider-attempt rule, which `EffectiveRagSettings::validate()` did not yet call.

`WorkflowSettings::default().validate()` and `validate_against_provider(30)` are asserted in `config_workflow_timeout_overlays_match_contract`.

Live binary (after those tests): from repo root,

`LANCET_ENGINE__GRPC_ADDR=127.0.0.1:50151` and `LANCET_ENGINE__LANCEDB_PATH` pointing at an empty throwaway directory (so the start does not touch the eval store), `LANCET_ENV` unset,

`cargo run --manifest-path engine/Cargo.toml --locked --bin engine`

reached `Rust RAG Engine serving addr=127.0.0.1:50151` at `2026-09-07T13:07:16.011619Z`. That log is after `load_settings` and `EffectiveRagSettings::try_from_settings`. The process was then stopped. Startup success is that serving line, not the subsequent SIGTERM exit code.

What that 2026-09-07 start actually demonstrated: `load_settings` and `EffectiveRagSettings::try_from_settings` accepted the committed workflow values against the nesting invariant and the other checks `validate()` performed at that date. The provider-attempt rule was **not** among them. `EffectiveRagSettings::validate()` did not reach `WorkflowSettings::validate_against_provider` on 2026-09-07 — that is `06.3.3-VERIFICATION.md` gap 3.

What changed in plan 06: `validate_against_provider(self.generation_timeout_secs)` is now called from `EffectiveRagSettings::validate()`, so from that commit forward the rule is enforced on every settings load. The automated proof of that wiring is `effective_settings_reject_provider_contract_violation_at_load` (goes through `try_from_settings`). The pre-existing `generation_node_timeout_below_retry_budget_is_rejected` still proves the arithmetic in isolation by calling `validate_against_provider` directly; it does not prove startup wiring. This closure did not re-run the live binary; the new settings-path test covers the missing hop offline.

Coherence repair: `config/config.verify.toml` lowered `[openrouter] generation_timeout_secs` from 30 to 3 so two provider attempts fit inside the overlay's unchanged `generation_node_timeout_ms = 7000`. Production `config/config.toml` was unaffected: its `generation_node_timeout_ms = 65000` already satisfied the 60000 ms requirement against `generation_timeout_secs = 30`, so this round did not reopen SC-6a's byte-identical three-source guarantee.

`runner.timeout_for_node` in `workflow_phase5_production.rs` resolves through `EffectiveRagSettings::default()` → `WorkflowSettings::default()` → `config.rs` `default_*_timeout_ms`, **not** any TOML file. That is why the production-test literals moved with the Rust defaults in the same commit as the TOML.

## Non-derived census

1. **Generation node.** Provenance: provider attempt contract (`GenerateAnswerNode` up to 2 attempts × `generation_timeout_secs = 30` + 5 s inter-attempt slack). Measured generation p95 **20889 ms** (max tail 41300 ms class) is reported **beside** the budget and marked **non-derivable from this pass**: the cheap eval model is not the production generation model. No derivation input for this budget cites a measured percentile.

2. **Reformulate.** Measured p95 **0.0 ms** (n=320). Retained **5000 ms**. Open input — SC-6 names six budgets and does not name this one.

3. **Empty-sample nodes.** None. Every runner node in the census has 320 OBSERVED timings.

4. **Arm-difference / estimate budgets.**
   - `query_embedding_timeout_ms`: arm-isolated `graph-off` duration; **no** span corroboration.
   - `graph_operation_timeout_ms`: paired difference; **weakest evidence in the phase**; censored-from-above lower bound; 13% of graph-on queries clipped at the 30 s measurement ceiling.

## Graph question (roadmap criterion 7)

Store baseline verdict: **`populated`** (`06.3.3-STORE-BASELINE.md`: 27,971 entities, 46,092 entity-edges, canaries present and connected). The latency question is **answerable**.

Traversal is **genuinely slow**, not an engine defect: hub expansion over this graph costs on the order of **25–30 seconds** (outer graph-on p95 30.4 s; inner lower-bound p95 ≥ 25.7 s). Documentation phase honest-limitations should cite **~25–30 s** as the measured graph-context cost figure, not a tuning opportunity. Graph quality work is out of family scope; the decision is to **raise the budget and state the cost**.

## Decay consequence

`decay_present: False` on `RetrieveHybrid` (Theil-Sen slope −6.73 ms/query, late−early p95 delta −4785 ms; neither prong fired against α=0.05 and +500 ms materiality). Horizon of the sample: **160 questions / 320 records**, two segments split by a mid-run engine restart. Budgets derived from this distribution are **not** time-limited by a measured upward drift. The original collapse therefore still wants an explanation other than exhaustion (process-state reader-handle buildup is the restart-supported hypothesis in the measurement record, not a budget-derivation input).

## Downstream dependants

- Phase **06.3.4** corrected re-drive (staged then 500×2) runs under these production defaults; eval overlay still changes only the store path, so the canary reads these values live from `config.toml`.
- Preflight canary locks the written set as the standard the next drive is held to.
- Phase 6.4 evaluation-methodology / honest-limitations cite the graph cost figure above.

## Go/no-go for paid spend

**Decision:** **Go** — proceed to the Phase 06.3.4 corrected re-drive under the per-stage caps below.

| Field | Value |
|---|---|
| Date | 2026-09-07T07:47:00-07:00 |
| Decided by | User / Operator |
| Option | `go` |

### Measured basis

| Item | Value |
|---|---|
| Measurement-pass observed spend | **$0.08316 USD** (generation $0.07855 + embedding $0.00461) on 160 questions / 320 records |
| Implied per-query cost | **$0.00051975 / question** ($0.000259875 / record) |
| Intended full-drive size | **500 questions × 2 arms** (1000 records), after a smaller staged prefix (D-44) |
| Same-model scale of the pass to 500×2 | ~**$0.26 USD** for 1000 records at the cheap generation model used in the pass |
| Remaining daily allowance at decision time | OpenRouter `GET /api/v1/auth/key`: usage **$1.351363526**, limit **$15.00**, **limit_remaining $14.954613576** |

The 2026-09-03 figure of **$0.0199** is **not** a usable comparison. That run cost almost nothing because **966 of 1000 queries generated nothing**. A corrected drive in which retrieval and generation actually run will cost materially more. This decision uses the measurement pass’s observed spend, not that anti-estimate.

### Why go (plan no-go triggers)

- No D-16 disposition concluded **defect**; all five workflow nodes are clean or genuinely slow.
- Graph store verdict is **populated**; the latency question is answerable (cite **~25–30 s** graph-context cost).
- Remaining daily allowance (~$14.95) supports the intended size at the scaled pass cost, with headroom for judging.

Known residual risks (caught by the staged drive, not treated as no-go): `graph_operation_timeout_ms` rests on a censored-from-above lower bound; `prompt_timeout_ms` 1070 sits below the observed max 2221 ms; long-drive tails may still grow from process-state handle buildup.

### Per-stage caps for 06.3.4 (D-47)

No overall phase ceiling. Each stage is capped against the daily remainder. 06.3.4 must enforce these figures:

| Stage | Cap (USD) | Consumer |
|---|---|---|
| Staged drive | **$2.00** | 06.3.4-03 (D-44 prefix; pairing / graph-yield gate before full spend) |
| Full two-arm drive | **$5.00** | 06.3.4-04 (500 × 2 into a new dated run directory) |
| Judging | **$5.00** | 06.3.4-05 (D-48; separate paid stage, fresh human calibration) |

Enforcement: each 06.3.4 paid driver must require `--stage-cap` at these values (or equivalent fail-closed config) with no silent default. A later stage may revise a cap only with a new dated decision; this entry is the starting bound.
