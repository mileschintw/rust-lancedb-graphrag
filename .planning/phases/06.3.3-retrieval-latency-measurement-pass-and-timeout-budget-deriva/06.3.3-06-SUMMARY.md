---
phase: 06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva
plan: 06
subsystem: engine
tags: [config, timeouts, validate_against_provider, settings-load, gap-closure]
requires:
  - phase: 06.3.3-04
    provides: written production budgets and Engine startup proof record
provides:
  - provider-attempt invariant enforced on every EffectiveRagSettings::validate / try_from_settings load
  - coherent LANCET_ENV=verify overlay (generation_timeout_secs=3 inside 7000ms node budget)
  - corrected 06.3.3-BUDGETS.md Engine startup proof (per-evidence account of gap 3)
affects: [06.3.4, verify-ingestion.sh boot path]
actuals:
  tokens: 2437
  tasks: 3
  commits: 3
plan_head_before: 91b7e34c18f673e4cd4cc607a0a6324c8a312492
tech-stack:
  added: []
  patterns:
    - guard lives in validate() so file, overlay, and env provenances share one check
    - make violating fixtures coherent rather than weakening the invariant
key-files:
  created: []
  modified:
    - engine/src/config.rs
    - config/config.verify.toml
    - engine/src/tests.rs
    - engine/src/tests/workflow_phase5_production.rs
    - .planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-BUDGETS.md
key-decisions:
  - "Lower verify overlay generation_timeout_secs to 3 instead of raising generation_node_timeout_ms past SlowLiveProvider's 30s stall"
  - "Raise env-override generation node marker 8888 to 68888 rather than parsing LANCET_OPENROUTER__GENERATION_TIMEOUT_SECS"
  - "Do not re-run the live engine binary; the new try_from_settings test is the wiring proof"
patterns-established:
  - "Arithmetic tests call validate_against_provider directly; wiring tests go through try_from_settings"
  - "Post-hoc edits to recorded BUDGETS.md claims are dated amendments citing VERIFICATION.md"
requirements-completed: [OBS-05]
coverage:
  - id: D1
    description: EffectiveRagSettings::validate calls validate_against_provider so every settings load enforces generation_node_timeout_ms >= 2 * generation_timeout_secs * 1000
    requirement: OBS-05
    verification:
      - kind: unit
        ref: engine/src/tests.rs#effective_settings_reject_provider_contract_violation_at_load
        status: pass
      - kind: other
        ref: cargo test --manifest-path engine/Cargo.toml --locked
        status: pass
    human_judgment: false
  - id: D2
    description: config.verify.toml is internally coherent (3s provider × 2 attempts = 6000ms ≤ 7000ms node) without raising the 7000ms generation node budget
    requirement: OBS-05
    verification:
      - kind: unit
        ref: engine/src/tests.rs#config_workflow_timeout_overlays_match_contract
        status: pass
      - kind: unit
        ref: engine/src/tests/workflow_phase5_production.rs#workflow_phase5_config_verify_generation_timeout
        status: pass
    human_judgment: false
  - id: D3
    description: Boundary test accepts 32s (64000 required) and rejects 33s (66000 required) against the default 65000ms node budget
    requirement: OBS-05
    verification:
      - kind: unit
        ref: engine/src/tests.rs#effective_settings_accept_provider_contract_at_boundary
        status: pass
    human_judgment: false
  - id: D4
    description: 06.3.3-BUDGETS.md Engine startup proof no longer claims the 2026-09-07 binary start proved the provider-attempt rule
    requirement: OBS-05
    verification:
      - kind: other
        ref: grep -c 'accepted the committed workflow set' 06.3.3-BUDGETS.md == 0
        status: pass
    human_judgment: false
duration: 16min
completed: 2026-09-08
status: complete
---

# Phase 06.3.3 Plan 06: Provider-attempt guard at settings load Summary

**`EffectiveRagSettings::validate()` now calls `validate_against_provider`, the verify overlay is coherent at 3s/7000ms, and BUDGETS.md no longer claims the 2026-09-07 engine start proved that rule**

## Performance

- **Duration:** 16 min
- **Started:** 2026-09-08T09:12:59Z
- **Completed:** 2026-09-08T09:29:16Z
- **Tasks:** 3
- **Files modified:** 5

## Accomplishments

- Wired `self.workflow.validate_against_provider(self.generation_timeout_secs)?` into `EffectiveRagSettings::validate()` immediately after `self.workflow.validate()?`, so `try_from_settings` (the `main.rs` `load_settings` path) refuses a violating configuration and the engine does not start.
- Made `config/config.verify.toml` coherent by lowering `[openrouter] generation_timeout_secs` from 30 to 3, leaving `generation_node_timeout_ms = 7000` unchanged so `workflow_phase5_config_verify_generation_timeout` still measures a 7000 ms node timer against `SlowLiveProvider`'s hardcoded 30 s stall. This is a coherence repair: the previous pair could not contain even one 30 s provider attempt. That test is the automated boot-safety guard for the `LANCET_ENV=verify` path used by `verify-ingestion.sh`.
- Added `effective_settings_reject_provider_contract_violation_at_load` (Settings default, `generation_timeout_secs=40`, `try_from_settings` → `Err` naming `generation_node_timeout_ms` and `80000`) and `effective_settings_accept_provider_contract_at_boundary` (32 accepted / 33 rejected against default 65000 ms).
- Corrected `06.3.3-BUDGETS.md` `## Engine startup proof` with a dated amendment marker citing `06.3.3-VERIFICATION.md` gap 3. Production `config.toml` / `config.example.toml` untouched (65000 already ≥ 60000).

## Task Commits

1. **Task 1: Enforce the provider-attempt invariant at settings load** - `9d8a66c` (feat)
2. **Task 2: Add a startup-path regression test** - `a554fc2` (test)
3. **Task 3: Correct the false claim in BUDGETS.md Engine startup proof** - `a6ad3dd` (docs)

## Files Created/Modified

- `engine/src/config.rs` — one call in `EffectiveRagSettings::validate()`
- `config/config.verify.toml` — `generation_timeout_secs` 30 → 3
- `engine/src/tests.rs` — overlay/env assertion updates plus two new settings-path tests
- `engine/src/tests/workflow_phase5_production.rs` — verify overlay `generation_timeout_secs` assertion 30 → 3
- `.planning/phases/06.3.3-.../06.3.3-BUDGETS.md` — Engine startup proof section only

## Decisions Made

- Lower the verify overlay's provider budget rather than raise the 7000 ms node budget: raising it would stop the phase5 test from observing the node timer fire against a 30 s stall. `SlowLiveProvider` never reads `generation_timeout_secs`.
- Raise the nested env-override marker from `8888` to `68888` instead of setting `LANCET_OPENROUTER__GENERATION_TIMEOUT_SECS`, which has no explicit `load_settings` boundary parser.
- Do not re-run the live binary as part of this closure; the new `try_from_settings` test covers the previously unwired hop deterministically and offline.

## Deviations from Plan

None - plan executed exactly as written.

**Total deviations:** 0 auto-fixed
**Impact on plan:** None

## Issues Encountered

None

## Authentication Gates

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Gap 3 closed on both halves: the provider-attempt rule is enforced on every settings load, and the derivation record no longer claims the 2026-09-07 start proved it. `config.toml` / `config.example.toml` / `Cargo.lock` unchanged. Ready for phase 06.3.3 remaining gates (code review / re-verification) and then 06.3.4.

Noted, not tasked: orphaned `LAT-01`..`LAT-04` citations in 06.3.3-01/02 SUMMARYs remain a VERIFICATION.md WARNING per D-46.

## Verification evidence

- `cargo test --manifest-path engine/Cargo.toml --locked` — 0 failed (Task 1 after wiring+fixtures; Task 2 after new tests). Lib 458 passed / 2 ignored; inspect_lancedb 31; config_startup 22.
- Wiring-removed demonstration (Task 2 acceptance): temporarily commented out `validate_against_provider` in `EffectiveRagSettings::validate()`. `tests::effective_settings_reject_provider_contract_violation_at_load --exact` reported **FAILED** (`try_from_settings` returned `Ok` with `generation_timeout_secs: 40`). Restored the call; same `--exact` filter reported **ok. 1 passed**.
- `workflow_phase5_config_verify_generation_timeout` passed (7000 ms node timer still fires well below 30 s).
- `git diff --stat -- config/config.toml config/config.example.toml` — no change.

## Self-Check: PASSED

- FOUND: `engine/src/config.rs`
- FOUND: `config/config.verify.toml`
- FOUND: `engine/src/tests.rs`
- FOUND: `engine/src/tests/workflow_phase5_production.rs`
- FOUND: `.planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-BUDGETS.md`
- FOUND: `9d8a66c`, `a554fc2`, `a6ad3dd`

---
*Phase: 06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva*
*Completed: 2026-09-08*
