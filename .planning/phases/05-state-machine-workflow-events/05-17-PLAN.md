---
phase: 05-state-machine-workflow-events
plan: 17
type: execute
wave: 11
depends_on:
  - 05-14
files_modified:
  - proto/lancet/v1/lancet.proto
  - buf.gen.yaml
  - engine/src/pb/mod.rs
  - engine/src/pb/lancet/v1/lancet.v1.rs
  - engine/src/pb/lancet/v1/lancet.v1.tonic.rs
  - gateway/proto/lancet/v1/lancet.pb.go
  - gateway/proto/lancet/v1/lancet_grpc.pb.go
  - engine/src/retrieval/tests.rs
autonomous: true
gap_closure: true
requirements:
  - ORCH-01
  - ORCH-02
  - ORCH-04
must_haves:
  truths:
    - "The checked-in protobuf contract exposes RetrievalSnapshot.variant_count at field tag 10 and repeated RetrievalSnapshot.variant_identities at field tag 11 without changing tags 1 through 9."
    - "The checked-in protobuf contract exposes typed WorkflowCompletedEvent.notices at field tag 6 while preserving success, duration, error, and final_response fields and tags."
    - "Buf generation produces synchronized Rust prost/tonic and Go protobuf/grpc bindings that downstream production literals and gateway fixtures can consume without changing published tags."
    - "The generated retrieval contract supports the accepted count and ordered query identities without introducing D-30 workflow metadata such as vector or BM25 counts."
  artifacts:
    - path: "proto/lancet/v1/lancet.proto"
      provides: "Wire-compatible retrieval provenance and failure-terminal notice fields"
    - path: "engine/src/pb/lancet/v1/lancet.v1.rs"
      provides: "Checked-in Rust message bindings for variant fields and terminal notices"
    - path: "gateway/proto/lancet/v1/lancet.pb.go"
      provides: "Checked-in Go message bindings for variant fields and terminal notices"
    - path: "engine/src/pb/mod.rs"
      provides: "Protected hand-written Rust module glue preserved across Buf generation; no competing plan owns this file"
    - path: "engine/src/retrieval/tests.rs"
      provides: "Rust RetrievalSnapshot wire-contract field/tag regression"
  key_links:
    - "proto/lancet/v1/lancet.proto -> buf.gen.yaml -> checked-in Rust and Go generated bindings"
    - "buf.gen.yaml clean=false -> engine/src/pb/mod.rs preservation guard -> Rust module imports"
    - "WorkflowContext.variants -> RetrievalSnapshot.variant_count/variant_identities"
    - "WorkflowCompletedEvent.notices -> 05-19 Rust terminal construction -> gateway raw SSE mapping"
    - "D-07/D-08 retrieval provenance fields -> WR-09 auditability without adding D-30 workflow metadata"
---

<objective>
Restore the shared wire fields required by the graph/retrieval and failure-terminal closure plans.

Purpose: 05-16 consumes retrieval provenance fields that are absent from the checked-in protobuf contract, and WR-07 requires a typed terminal notice carrier for failed workflows. This plan owns the source schema, configured Buf generation, checked-in bindings, and a Rust field/tag wire-contract regression; downstream plans own runtime construction sites.
Output: Wire-compatible protobuf additions, regenerated Rust/Go artifacts, protected module glue, and a Rust field/tag wire-contract regression; 05-16 owns production Rust literal population and 05-11 owns the Go cross-runtime fixture.
</objective>

## Artifacts this phase produces

- Updated lancet.proto RetrievalSnapshot and WorkflowCompletedEvent fields with additive tags.
- Buf-generated Rust and Go bindings consumed by the downstream Rust and Go construction-site updates.
- Protected hand-written `engine/src/pb/mod.rs` module glue with a pre/post generation existence and content guard.
- Cross-runtime wire-contract tests for variant provenance and terminal notices.

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-preconditions.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-reversibility.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-PATTERNS.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEWS.md
@.planning/phases/05-state-machine-workflow-events/05-16-PLAN.md
@AGENTS.md
@rust-guidelines.md
@go-guidelines.md
@proto/lancet/v1/lancet.proto
@buf.gen.yaml
@engine/src/pb/mod.rs
@engine/src/pb/lancet/v1/lancet.v1.rs
@engine/src/pb/lancet/v1/lancet.v1.tonic.rs
@gateway/proto/lancet/v1/lancet.pb.go
@gateway/proto/lancet/v1/lancet_grpc.pb.go
@engine/src/main.rs
@engine/src/workflow/nodes/retrieve.rs
@gateway/main_test.go

</context>

<scope_rationale>
The eight-file inventory is kept atomic around the protobuf source, protected module glue, and synchronized generated outputs: Task 1 owns the schema, Buf template, four generated Rust/Go bindings, and the read-only preservation guard for hand-written `engine/src/pb/mod.rs`; Task 2 owns only the Rust wire-contract regression. 05-16 owns production Rust retrieval literals and provenance assertions, while 05-11 owns the Go gateway wire fixture, each in files they already modify for adjacent runtime concerns. This keeps generated binding safety atomic without claiming unrelated construction sites or expanding the plan's file scope.
</scope_rationale>

<tasks>

<task type="tracer">
  <name>Task 1: Extend and regenerate the shared protobuf contract without wire drift</name>
  <read_first>AGENTS.md; rust-guidelines.md; go-guidelines.md; 05-PATTERNS.md; 05-CONTEXT.md; 05-REVIEWS.md; proto/lancet/v1/lancet.proto; buf.gen.yaml; engine/src/pb/mod.rs; engine/src/pb/lancet/v1/lancet.v1.rs; engine/src/pb/lancet/v1/lancet.v1.tonic.rs; gateway/proto/lancet/v1/lancet.pb.go; gateway/proto/lancet/v1/lancet_grpc.pb.go</read_first>
  <files>proto/lancet/v1/lancet.proto, buf.gen.yaml, engine/src/pb/mod.rs, engine/src/pb/lancet/v1/lancet.v1.rs, engine/src/pb/lancet/v1/lancet.v1.tonic.rs, gateway/proto/lancet/v1/lancet.pb.go, gateway/proto/lancet/v1/lancet_grpc.pb.go</files>
  <action>Add uint32 variant_count = 10 and repeated string variant_identities = 11 to RetrievalSnapshot, preserving every existing field name and tag 1 through 9. Add repeated Notice notices = 6 to WorkflowCompletedEvent, preserving tags 1 through 5 and the existing oneof event tags. Keep `engine/src/pb/mod.rs` as hand-written module glue and change `buf.gen.yaml` to `clean: false` so generation cannot remove that file from the `engine/src/pb` output root; generated bindings remain source-derived and no generated file is hand-edited. Before generation, capture the complete mod.rs contents and require its existing `include!("lancet/v1/lancet.v1.rs")` glue; run Buf generation from the repository root; after generation, require the file still exists and its contents are byte-for-byte unchanged. If a generated-module path must change, update the glue intentionally and make the pre/post guard compare against that checked-in expected content. Do not renumber, reuse, remove, or rename a published field. Treat the two retrieval fields as WR-09 provenance required by D-07/D-08, not as the deferred D-30 workflow metadata set, and treat terminal notices as the typed D-05 failure diagnostic carrier. Keep all four checked-in generated Rust/Go outputs synchronized with the source schema and the protected module glue. Follow the 05-PATTERNS.md protobuf streaming source-of-truth and generated-artifact ordering analogs; no new RPC, provider, or transport is introduced.</action>
  <acceptance_criteria>
    - The proto source contains the exact RetrievalSnapshot fields and tags 10/11 and the exact WorkflowCompletedEvent notices field and tag 6.
    - Existing RetrievalSnapshot tags 1 through 9, WorkflowCompletedEvent tags 1 through 5, and WorkflowEvent oneof tags remain unchanged.
    - buf generate succeeds and the checked-in Rust and Go bindings expose the new fields with the expected prost/protobuf types.
    - Generated output is source-derived and git diff --check is clean after generation.
    - `buf.gen.yaml` disables cleanup of the shared Rust output root, and a pre/post generation guard proves `engine/src/pb/mod.rs` remains present with the expected module include and unchanged contents.
  </acceptance_criteria>
 <verify>
    <automated>$proto = Get-Content proto/lancet/v1/lancet.proto -Raw; $template = Get-Content buf.gen.yaml -Raw; $gluePath = 'engine/src/pb/mod.rs'; if ($template -notmatch 'clean:\s*false') { throw 'Buf cleanup is still enabled for the hand-written Rust module output root' }; if (-not (Test-Path $gluePath)) { throw 'Rust module glue is missing before generation' }; $glueBefore = Get-Content $gluePath -Raw; if ($glueBefore -notmatch 'include!\("lancet/v1/lancet.v1.rs"\)') { throw 'Rust module glue include is missing before generation' }; foreach ($expected in @('uint32 variant_count = 10;', 'repeated string variant_identities = 11;', 'repeated Notice notices = 6;', 'string index_generation = 1;', 'string result_hash = 9;')) { if ($proto -notmatch [regex]::Escape($expected)) { throw ('Proto source contract missing: ' + $expected) } }; $generated = buf generate; $generateExit = $LASTEXITCODE; if ($generateExit -ne 0) { exit $generateExit }; if (-not (Test-Path $gluePath)) { throw 'Rust module glue was removed by generation' }; $glueAfter = Get-Content $gluePath -Raw; if ($glueAfter -cne $glueBefore) { throw 'Rust module glue changed during generation' }; foreach ($path in @('engine/src/pb/lancet/v1/lancet.v1.rs', 'engine/src/pb/lancet/v1/lancet.v1.tonic.rs', 'gateway/proto/lancet/v1/lancet.pb.go', 'gateway/proto/lancet/v1/lancet_grpc.pb.go')) { if (-not (Test-Path $path)) { throw ('Generated binding missing: ' + $path) } }; $rust = Get-Content engine/src/pb/lancet/v1/lancet.v1.rs -Raw; $go = Get-Content gateway/proto/lancet/v1/lancet.pb.go -Raw; foreach ($expected in @('variant_count', 'variant_identities', 'notices')) { if ($rust -notmatch [regex]::Escape($expected)) { throw ('Rust generated binding missing: ' + $expected) } }; foreach ($expected in @('VariantCount', 'VariantIdentities', 'Notices')) { if ($go -notmatch [regex]::Escape($expected)) { throw ('Go generated binding missing: ' + $expected) } }; $diff = git diff --check; $diffExit = $LASTEXITCODE; if ($diffExit -ne 0) { exit $diffExit }</automated>
 </verify>
  <done>The source schema and all checked-in generated bindings agree on the additive fields, with old wire tags preserved and no manual generated-code drift.</done>
  <must_haves>
    - D-02, D-05, D-07, D-08, D-17, D-18, and D-30 remain explicit at the wire boundary.
    - The new fields are additive and do not create a second RPC or a deferred workflow metadata contract.
  </must_haves>
  <reversibility rating="costly">Protobuf field numbers are a client-visible wire contract; only additive fields with unused tags are permitted.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Prove the Rust retrieval snapshot wire contract</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; 05-CONTEXT.md; 05-REVIEWS.md; 05-16-PLAN.md; proto/lancet/v1/lancet.proto; engine/src/pb/lancet/v1/lancet.v1.rs; engine/src/retrieval/tests.rs</read_first>
  <files>engine/src/retrieval/tests.rs</files>
  <action>Add the Rust library test `retrieval_snapshot_variant_provenance_wire_contract` using the generated `RetrievalSnapshot` message with explicit `variant_count` and ordered `variant_identities` values. Encode and decode a populated snapshot, then assert descriptor field numbers 10 and 11, the preserved old fields/tags 1 through 9, the count, and ordered identities. This is a generated-contract test only: production literal population is owned by 05-16 and the Go reflection/marshal round-trip fixture is owned by 05-11 Task 1. Follow the 05-PATTERNS.md retrieval/fusion and generated-contract test analogs; preserve variant-zero embedding and D-30's exclusion of vector_count, bm25_count, graph counts, and token metadata.</action>
  <acceptance_criteria>
    - The Rust wire test compiles with explicit variant_count and variant_identities values and proves field numbers 10/11 plus round-trip values without changing tags 1 through 9.
    - The generated contract remains available for 05-16 production Rust literals and 05-11's Go cross-runtime fixture.
  </acceptance_criteria>
  <verify>
    <automated>$rustName = 'retrieval_snapshot_variant_provenance_wire_contract'; $rustList = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $rustListExit = $LASTEXITCODE; if ($rustListExit -ne 0) { exit $rustListExit }; $rustMatches = @($rustList | Select-String -Pattern ('(^|::)' + [regex]::Escape($rustName) + ': test$')); if ($rustMatches.Count -ne 1) { throw 'Rust test registration count mismatch' }; $rustRun = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $rustName --nocapture; $rustRunExit = $LASTEXITCODE; if ($rustRunExit -ne 0) { exit $rustRunExit }</automated>
  </verify>
  <done>The Rust wire-contract test proves the generated additive provenance fields survive encoding and decoding with exact tags; production Rust literal population remains owned by 05-16 and the Go cross-runtime fixture remains owned by 05-11.</done>
</task>

</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust/Go runtime to protobuf wire contract | Generated Rust and Go types serialize data exchanged across the engine/gateway boundary. |
| Source schema to generated artifacts | Buf code generation turns the checked-in schema into runtime bindings consumed by both modules. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-17-01 | Tampering | RetrievalSnapshot protobuf tags | critical | mitigate | Use only unused additive tags 10 and 11, assert descriptor numbers and preserve tags 1 through 9 in Rust and Go wire tests. |
| T-05-17-02 | Information disclosure | variant_identities payload | medium | mitigate | Serialize only the accepted ordered query identities already allowed by D-30; exclude vector, BM25, graph, token, and other generic workflow metadata. |
| T-05-17-03 | Tampering | Buf-generated Rust/Go bindings and hand-written Rust module glue | high | mitigate | Disable output-root cleanup, capture and compare engine/src/pb/mod.rs before and after generation, inspect all checked-in generated outputs, and require cross-runtime round-trip tests before accepting the plan. |
| T-05-17-SC | Tampering | npm/pip/cargo installs | high | accept | This plan adds no package-manager dependency or install task; the standard slopcheck register is not applicable to this source-only contract change. |
</threat_model>

<verification>
- Buf generation completes successfully and produces the checked-in Rust and Go bindings from the updated schema.
- The Rust wire-contract test is registered exactly once in the library target and passes with the additive field numbers and values; 05-11 owns the Go gateway reflection/marshal fixture after this generated contract lands.
- Downstream 05-16 and 05-11 ownership checks cover every Rust production or Go test construction site that consumes the new fields.
- git diff --check passes and the generated diff contains no tag changes to RetrievalSnapshot fields 1 through 9.
</verification>

<success_criteria>
The shared RetrievalSnapshot contract exposes variant_count at field 10 and ordered variant_identities at field 11 in both generated runtimes, with Rust production literals owned by 05-16 and the Go round-trip fixture owned by 05-11 after this plan's generated artifacts land.
</success_criteria>

<output>
Create .planning/phases/05-state-machine-workflow-events/05-17-SUMMARY.md when done.
</output>
