---
phase: 05-state-machine-workflow-events
plan: 17
type: execute
wave: 11
depends_on:
  - 05-14
files_modified:
  - proto/lancet/v1/lancet.proto
  - buf.gen.yaml
  - engine/src/pb/lancet/v1/lancet.v1.rs
  - engine/src/pb/lancet/v1/lancet.v1.tonic.rs
  - gateway/proto/lancet/v1/lancet.pb.go
  - gateway/proto/lancet/v1/lancet_grpc.pb.go
autonomous: true
gap_closure: true
requirements:
  - ORCH-01
  - ORCH-02
  - ORCH-04
must_haves:
  truths:
    - "RetrievalSnapshot exposes additive variant_count tag 10 and ordered variant_identities tag 11 without changing tags 1 through 9."
    - "WorkflowCompletedEvent exposes additive notices tag 6 without changing tags 1 through 5 or the existing WorkflowEvent oneof tags."
    - "Buf generation produces synchronized checked-in Rust and Go bindings while preserving the hand-written Rust module glue."
    - "The generated-field compile repair and Rust wire round-trip proof have explicit ownership in 05-23 before target-aware downstream plans execute."
  artifacts:
    - path: "proto/lancet/v1/lancet.proto"
      provides: "Source-of-truth additive RetrievalSnapshot and WorkflowCompletedEvent fields."
    - path: "buf.gen.yaml"
      provides: "Generation configuration that preserves the hand-written Rust module glue."
    - path: "engine/src/pb/lancet/v1/lancet.v1.rs"
      provides: "Rust generated message fields and tags."
    - path: "gateway/proto/lancet/v1/lancet.pb.go"
      provides: "Go generated message fields and tags."
  key_links:
    - "lancet.proto -> buf generate -> checked-in Rust and Go bindings"
    - "Generated RetrievalSnapshot/WorkflowCompletedEvent fields -> 05-23 compile repair and wire proof -> 05-16/05-19 runtime population"

---

<objective>
Land the additive protobuf fields and synchronized generated bindings that later workflow plans consume.

Purpose: The wire contract must be source-derived and backward-compatible before Rust production literals, terminal notices, and target-aware test seams consume the new fields.
Output: Updated schema, protected generation configuration, and synchronized checked-in Rust/Go bindings; compile-repair literals and wire tests are owned by 05-23.
</objective>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-preconditions.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-reversibility.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-PATTERNS.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEWS.md
@.planning/phases/05-state-machine-workflow-events/05-23-PLAN.md
@proto/lancet/v1/lancet.proto
@buf.gen.yaml
@engine/src/pb/mod.rs
@engine/src/pb/lancet/v1/lancet.v1.rs
@engine/src/pb/lancet/v1/lancet.v1.tonic.rs
@gateway/proto/lancet/v1/lancet.pb.go
@gateway/proto/lancet/v1/lancet_grpc.pb.go
@AGENTS.md
@rust-guidelines.md
@go-guidelines.md
</context>

<scope_rationale>
The six-file inventory is the smallest source-to-generated boundary: one schema, one generation template, and the four checked-in generated Rust/Go outputs. `engine/src/pb/mod.rs` is hand-written module glue and is intentionally read-and-guarded, not modified; Rust construction-site compile repair and the Rust wire round-trip test are separated into 05-23 so this plan does not leave an unverified partially compiling target hidden behind a generated-artifact diff. 05-16 owns production RetrievalSnapshot population, 05-19 owns terminal notice population, and 05-11 owns the Go cross-runtime fixture.
</scope_rationale>

<tasks>

<task type="tracer">
  <name>Task 1: Generate the additive Rust/Go wire contract without drift</name>
  <precondition>Buf CLI and the four configured remote plugins are available for this source-to-generated-artifact update; downstream plans consume checked-in bindings and do not require another remote generation invocation.</precondition>
  <read_first>AGENTS.md; rust-guidelines.md; go-guidelines.md; 05-PATTERNS.md; 05-CONTEXT.md; 05-REVIEWS.md; proto/lancet/v1/lancet.proto; buf.gen.yaml; engine/src/pb/mod.rs; engine/src/pb/lancet/v1/lancet.v1.rs; engine/src/pb/lancet/v1/lancet.v1.tonic.rs; gateway/proto/lancet/v1/lancet.pb.go; gateway/proto/lancet/v1/lancet_grpc.pb.go</read_first>
  <files>proto/lancet/v1/lancet.proto, buf.gen.yaml, engine/src/pb/lancet/v1/lancet.v1.rs, engine/src/pb/lancet/v1/lancet.v1.tonic.rs, gateway/proto/lancet/v1/lancet.pb.go, gateway/proto/lancet/v1/lancet_grpc.pb.go</files>
  <action>Add `uint32 variant_count = 10` and `repeated string variant_identities = 11` to RetrievalSnapshot, preserving every existing field name and tag 1 through 9. Add `repeated Notice notices = 6` to WorkflowCompletedEvent, preserving tags 1 through 5 and the existing WorkflowEvent oneof tags. Keep D-05 terminal diagnostics typed and keep D-07/D-08 provenance additive; do not add D-30 generic workflow metadata. Set `clean: false` in `buf.gen.yaml` because the hand-written `engine/src/pb/mod.rs` lives in the Rust output root. Capture the complete mod.rs contents before generation, run `buf generate` from the repository root, require mod.rs to remain present and byte-for-byte unchanged, and inspect the exact allowlist of the four generated Rust/Go output files. Commit source-derived generated bindings without hand-editing them. Run `buf lint` and a second generation/hash comparison to prove deterministic output. Leave all exhaustive Rust message literals and the Rust wire round-trip test to 05-23, and leave the Go cross-runtime fixture to 05-11; this task owns schema and generated artifacts only. Preserve the existing public wire contract and do not add a new RPC or package dependency.
  <acceptance_criteria>
    - The proto source contains the exact additive fields and tags 10/11 and 6, with all historical fields and oneof tags unchanged.
    - `buf generate` and `buf lint` succeed, the four checked-in bindings contain the new fields, and a repeated generation produces byte-identical generated outputs.
    - `engine/src/pb/mod.rs` remains present, retains its expected Rust include glue, and is byte-for-byte unchanged across generation.
    - The generated-output inventory contains only the two configured Rust generated files, the two configured Go generated files, and the protected hand-written module glue; no stale output is silently accepted.
    - 05-23 is the explicit owner of Rust construction-site repair and the Rust wire round-trip test, while 05-16, 05-19, and 05-11 retain their declared runtime fixture ownership.
  </acceptance_criteria>
  <verify>
    <automated>$proto = Get-Content 'proto/lancet/v1/lancet.proto' -Raw; $template = Get-Content 'buf.gen.yaml' -Raw; $gluePath = 'engine/src/pb/mod.rs'; if ($proto -notmatch 'uint32\s+variant_count\s*=\s*10;') { throw 'variant_count field/tag missing from proto source' }; if ($proto -notmatch 'repeated\s+string\s+variant_identities\s*=\s*11;') { throw 'variant_identities field/tag missing from proto source' }; if ($proto -notmatch 'repeated\s+Notice\s+notices\s*=\s*6;') { throw 'terminal notices field/tag missing from proto source' }; if ($template -notmatch 'clean:\s*false') { throw 'Buf cleanup protection is missing' }; if (-not (Test-Path -LiteralPath $gluePath)) { throw 'hand-written Rust module glue is missing' }; $glueBefore = Get-Content -LiteralPath $gluePath -Raw; if ($glueBefore -notmatch 'include!\("lancet/v1/lancet.v1.rs"\)') { throw 'expected Rust binding include is missing' }; $generated = buf generate; $generateExit = $LASTEXITCODE; if ($generateExit -ne 0) { exit $generateExit }; if ((Get-Content -LiteralPath $gluePath -Raw) -cne $glueBefore) { throw 'hand-written Rust module glue changed during generation' }; foreach ($path in @('engine/src/pb/lancet/v1/lancet.v1.rs','engine/src/pb/lancet/v1/lancet.v1.tonic.rs','gateway/proto/lancet/v1/lancet.pb.go','gateway/proto/lancet/v1/lancet_grpc.pb.go')) { if (-not (Test-Path -LiteralPath $path)) { throw "generated binding missing: $path" } }; $rust = Get-Content 'engine/src/pb/lancet/v1/lancet.v1.rs' -Raw; $go = Get-Content 'gateway/proto/lancet/v1/lancet.pb.go' -Raw; foreach ($needle in @('variant_count','variant_identities','notices')) { if ($rust -notmatch [regex]::Escape($needle)) { throw "Rust generated field missing: $needle" } }; foreach ($needle in @('VariantCount','VariantIdentities','Notices')) { if ($go -notmatch [regex]::Escape($needle)) { throw "Go generated field missing: $needle" } }; $lint = buf lint; $lintExit = $LASTEXITCODE; if ($lintExit -ne 0) { exit $lintExit }; $diff = git diff --check; $diffExit = $LASTEXITCODE; if ($diffExit -ne 0) { exit $diffExit }</automated>
    <automated>$paths = @('engine/src/pb/lancet/v1/lancet.v1.rs','engine/src/pb/lancet/v1/lancet.v1.tonic.rs','gateway/proto/lancet/v1/lancet.pb.go','gateway/proto/lancet/v1/lancet_grpc.pb.go'); $before = @{}; foreach ($path in $paths) { $before[$path] = (Get-FileHash -Algorithm SHA256 -LiteralPath $path).Hash }; $again = buf generate; $againExit = $LASTEXITCODE; if ($againExit -ne 0) { exit $againExit }; foreach ($path in $paths) { $after = (Get-FileHash -Algorithm SHA256 -LiteralPath $path).Hash; if ($after -ne $before[$path]) { throw "repeated generation changed generated output: $path" } }</automated>
  </verify>
  <done>The additive protobuf schema and all four checked-in generated bindings are synchronized, wire-compatible, deterministic, and protected from deletion of the hand-written Rust module glue.</done>
  <reversibility rating="costly">Published protobuf tags cannot be renumbered without a coordinated wire migration; use only unused additive tags and keep generated artifacts source-derived.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Protobuf schema to generated artifacts | Buf transforms the checked-in schema into Rust and Go runtime types. |
| Rust engine to Go gateway | Generated messages cross the runtime boundary with published field numbers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-17-01 | Tampering | RetrievalSnapshot and WorkflowCompletedEvent tags | critical | mitigate | Use only additive tags 10/11 and 6, preserve existing tags, and verify source plus generated field names. |
| T-05-17-02 | Tampering | Buf-generated bindings and hand-written module glue | high | mitigate | Guard mod.rs before/after generation, require exact output inventory, run lint, and compare repeated-generation hashes. |
| T-05-17-03 | Information disclosure | variant_identities payload | medium | mitigate | Carry only D-07/D-08 accepted ordered identities and exclude D-30 generic metadata. |
| T-05-17-SC | Tampering | npm/pip/cargo installs | high | accept | This plan adds no package-manager dependency or install task; the slopcheck register is not applicable. |
</threat_model>

<verification>
- Buf generation and lint pass, protected module glue remains unchanged, and the exact generated output inventory is clean.
- The source schema and generated Rust/Go bindings expose only the additive fields required by D-05, D-07, and D-08.
- 05-23 repairs exhaustive Rust literals and proves the Rust wire round trip before 05-18, 05-16, and 05-19 consume the generated fields.
</verification>

<success_criteria>
The shared protobuf source and checked-in Rust/Go bindings provide additive RetrievalSnapshot provenance and failure-terminal notices with preserved historical tags and deterministic generation.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-17-SUMMARY.md` when done.
</output>
