---
phase: 05-state-machine-workflow-events
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - engine/src/tests.rs
autonomous: true
requirements: [ORCH-01, ORCH-02, ORCH-03]

must_haves:
  truths:
    - "Tier 1 Rust tests exercise the actual LancetServiceImpl workflow with injected fakes, not an unconnected runner-only imitation."
    - "The full successful event sequence proves graph-before-retrieval, coarse per-node cardinality, one AnswerChunk, one FinalAnswer, terminal success, common correlation trace_id, and independent session_id (D-06, D-20, D-29)."
    - "Zero evidence is a successful short circuit, while dense/BM25 hard errors are typed retrieval failures and do not look like the short circuit (D-03, D-22)."
    - "Sub-second settings make every node timeout test deterministic, including a focused actual AssemblePromptNode timeout test after the cooperative packing change (D-17, review MEDIUM)."
    - "Cancellation is proven during a running node and at the named retry boundary, and the WorkflowHandle outcome remains observable after the event receiver is dropped (D-16, review HIGH/MEDIUM)."
    - "Two concurrent workflow calls use independent context, cancellation, event channels, and trace identities."
  artifacts:
    - path: "engine/src/tests.rs"
      provides: "Tier 1 fakes, configured WorkflowDependencies construction, and deterministic Phase 5 reference scenarios"
  key_links:
    - from: "engine/src/tests.rs::configured_service"
      to: "LancetServiceImpl::workflow_dependencies"
      via: "test-only graph/dense/BM25/reformulator/prompt/generator fakes are injected through the same service field production uses"
    - from: "test stream receiver drop"
      to: "WorkflowHandle::outcome"
      via: "the test consumes the terminal outcome channel rather than expecting a terminal event after the receiver is closed"
  prohibitions:
    - requirement_id: ORCH-01
      category: test-integrity
      statement: "MUST NOT make a test pass by asserting only the event count or by using a fake path that bypasses LancetServiceImpl dependency construction."
      verification: test
    - requirement_id: ORCH-02
      category: test-integrity
      statement: "MUST NOT equate trace_id with session_id or accept extra sub-step/retry events in the full-sequence assertion."
      verification: test
    - requirement_id: ORCH-03
      category: test-integrity
      statement: "MUST NOT use sleeps tied to five-, ten-, fifteen-, or thirty-second production defaults; timeout scenarios must override settings below one second."
      verification: test
---

<objective>
Build the deterministic Tier 1 evaluation harness that proves the Rust state machine's ordering, failure categories, retry/cancellation edges, and actual node timeout behavior.

**As a** Lancet engineer, **I want to** reproduce orchestration failures without provider or storage timing, **so that** workflow behavior remains debuggable as the pipeline evolves.

Purpose: turn every operative review concern about testability into a runnable Rust assertion before cross-runtime verification.
Output: service-level fakes and named deterministic scenarios covering the AI-SPEC reference dimensions that do not require Go or PostgreSQL.
</objective>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-state-machine-workflow-events/05-02-SUMMARY.md
@.planning/phases/05-state-machine-workflow-events/05-03-SUMMARY.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-AI-SPEC.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEWS.md
@AGENTS.md
@rust-guidelines.md
</context>

## Artifacts this phase produces

- `engine/src/tests.rs`: `FakeQueryReformulator`, `FakeGraphQueryPort`, `FakeDenseRetrievalPort`, `FakeBm25RetrievalPort`, `FakePromptAssembler`, `RetryGate` test double, service factory updates, and named workflow tests.
- Tier 1 scenario coverage for happy path, zero evidence, graph degrade/timeout, retrieval error, empty reformulation, node timeout, retry success/exhaustion, cancellation, trace/session identity, and concurrent-call isolation.

<tasks>

<task type="tracer">
  <name>Task 1: Trace a service-level happy path with real workflow nodes and injected fakes</name>
  <read_first>engine/src/tests.rs, engine/src/main.rs, engine/src/workflow/ports.rs, engine/src/workflow/node.rs, engine/src/workflow/runner.rs, engine/src/workflow/events.rs, engine/src/workflow/nodes/assemble_prompt.rs, engine/src/workflow/nodes/generate.rs, 05-AI-SPEC.md, 05-REVIEWS.md, AGENTS.md, rust-guidelines.md</read_first>
  <files>engine/src/tests.rs</files>
  <behavior>
    - `LancetServiceImpl::query_rag` with fake ports -> all real workflow nodes execute and produce the ordered happy-path event stream.
    - Event assertions -> trace_id equals correlation_id for every event; WorkflowCompleted.session_id equals the requested session id and differs from trace_id.
    - Service factory -> no concrete graph, dense, or BM25 query is invoked when the corresponding fake is installed.
  </behavior>
  <action>Extend the existing `configured_service` factory in `engine/src/tests.rs` to populate the production `WorkflowDependencies` field with deterministic `FakeQueryReformulator`, `FakeGraphQueryPort`, `FakeDenseRetrievalPort`, and `FakeBm25RetrievalPort` instances while preserving the existing isolated LanceDB setup required to construct `LancetServiceImpl`. Add a fake generator response that contains valid cited evidence and a fake prompt assembler only where the production node's explicit port requires it; call the actual tonic `LancetServiceImpl::query_rag` implementation and drain its returned stream through the same Rust event conversion path used by tonic. Add `TestWorkflowHappyPathPreservesOrderAndIdentity` covering D-06 graph-before-retrieval order, one NodeStarted/terminal pair per executed node, one AnswerChunk, one FinalAnswer, successful WorkflowCompleted, common correlation-derived trace_id, and independent session_id. Add `TestWorkflowConcurrentCallsHaveIndependentCancellationAndTrace` with two simultaneous requests and separate event/outcome drains. Keep this as deterministic Tier 1 coverage: no OpenRouter, PostgreSQL, process spawn, wall-clock production timeout, or fake runner shortcut.</action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked --bin engine -- TestWorkflowHappyPathPreservesOrderAndIdentity; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; cargo test --manifest-path engine/Cargo.toml --locked --bin engine -- TestWorkflowConcurrentCallsHaveIndependentCancellationAndTrace</automated>
  </verify>
  <acceptance_criteria>
    - The test invokes `LancetServiceImpl::query_rag` with the real production node vector and injected dependencies; it does not instantiate a separate toy runner.
    - The exact event sequence proves graph-before-retrieval and contains no dense/BM25 sub-step or retrying event.
    - All event trace IDs equal the request correlation ID, and the completed event's session ID is asserted separately.
    - Two concurrent requests have distinct traces and cancellation tokens, and completing or dropping one stream does not alter the other.
  </acceptance_criteria>
  <done>A real service-level happy path and concurrent isolation proof exercise the same Rust workflow architecture that production uses.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic failure, timeout, retry, and cancellation scenarios</name>
  <read_first>engine/src/tests.rs, engine/src/workflow/ports.rs, engine/src/workflow/node.rs, engine/src/workflow/runner.rs, engine/src/workflow/nodes/graph_context.rs, engine/src/workflow/nodes/retrieve.rs, engine/src/workflow/nodes/assemble_prompt.rs, engine/src/workflow/nodes/generate.rs, engine/src/workflow/events.rs, 05-AI-SPEC.md, 05-REVIEWS.md, AGENTS.md, rust-guidelines.md</read_first>
  <files>engine/src/tests.rs</files>
  <behavior>
    - Empty reformulator -> typed internal failure; dense failure and BM25 failure -> RetrievalFailed; zero candidates -> successful D-03 short circuit.
    - Graph no-match/failure/timeout -> graph node completes and retrieval continues with empty graph context.
    - Reformulate/Retrieve/Prompt node timeout -> Timeout; prompt assertion uses the actual AssemblePromptNode with a stalled injected assembler.
    - Generation first failure -> retry gate then equal request; double failure -> honest failed terminal; cancellation at gate -> one generator call and Cancelled outcome.
    - Receiver drop during a node -> closed stream plus WorkflowHandle outcome, with no hanging supervisor or fabricated terminal event.
  </behavior>
  <action>Define deterministic fakes in `engine/src/tests.rs` that can return configured results, block on a `Notify`, and record calls/requests: `FakeQueryReformulator` must be able to return an empty vector; graph, dense, and BM25 fakes must fail or stall independently; `FakePromptAssembler` must stall inside the actual `AssemblePromptNode`; and the generator/retry-gate pair must expose call count, exact `GenerationRequest` values, and a gate immediately before attempt two. Set all workflow timeout fields to sub-second values in each test's `EffectiveRagSettings` so no case waits on production defaults. Add named tests for D-03 zero evidence, empty reformulation, graph degrade and graph timeout, dense failure, BM25 failure, reformulate/retrieve/prompt timeout, one-retry success, double-generation failure with zero answer events, citation-resolution failure without provider retry, cancellation during a node, cancellation at the retry gate, stream receiver closure with observable `WorkflowHandle` outcome, and event-channel/supervisor cleanup. Assert typed NodeErrorCategory values and sanitized messages rather than raw provider/storage details. Use fatal assertions for every fixture setup and snapshot read; never convert a fake configuration error into a passing empty result. This explicitly addresses the review findings about uninjectable service fakes, empty variants, non-preemptive prompt timeout, retry rendezvous timing, and cancellation outcome observability.</action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked --bin engine -- 'TestWorkflow|workflow_'; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; cargo test --manifest-path engine/Cargo.toml --locked --bin engine</automated>
  </verify>
  <acceptance_criteria>
    - Every named scenario runs with sub-second settings and deterministic fake gates; no test relies on provider/network timing.
    - Zero evidence produces success with no AssemblePrompt/GenerateAnswer start; retrieval errors produce RetrievalFailed; graph timeout degrades and continues.
    - Actual AssemblePromptNode timeout behavior is tested through its injected assembler boundary after cooperative production packing was implemented.
    - Retry tests prove one equal-request retry, no third call, no retrying event, no fabrication, and cancellation between attempts.
    - Stream-drop tests retrieve WorkflowHandle outcome separately and prove the supervisor terminates on natural completion and client closure.
    - The unfiltered engine test suite passes, so targeted names are not the only evidence of coverage.
  </acceptance_criteria>
  <done>Tier 1 covers the full deterministic failure matrix and closes the Rust-side execution risks identified by both reviewers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test fixture → workflow port | Fault-injection inputs simulate storage/provider failures and must not weaken production validation. |
| Workflow event receiver → test harness | Receiver closure is the same cancellation signal as a client disconnect. |
| Test configuration → timeout runner | Sub-second overrides control deterministic resource-bound assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-14 | Tampering | Test-only dependency injection | medium | mitigate | Use the same `WorkflowDependencies` field as production and assert fake calls/results, preventing tests from bypassing the service wiring. |
| T-05-15 | Denial of Service | Cancellation/supervisor tests | high | mitigate | Use bounded test deadlines, Notify-based release paths, and explicit cleanup assertions for every stalled fake and spawned task. |
| T-05-16 | Repudiation | Failure-category assertions | medium | mitigate | Assert exact D-22 categories, correlation-linked trace IDs, and sanitized messages across every failure scenario. |
| T-05-17 | Information Disclosure | Fault fixture messages | low | accept | Fixtures use synthetic identifiers and the production sanitizer is exercised; no external or user data enters Tier 1 tests. |
</threat_model>

<verification>
- Run the focused named Tier 1 scenarios, then the full locked Rust suite and clippy.
- Confirm the test list contains every named scenario before executing the focused subset.
- Confirm no PostgreSQL or provider environment is required for this plan; those cross-runtime claims are explicitly environment-gated in 05-05.
</verification>

<success_criteria>
The Rust workflow's complete event, failure, timeout, retry, cancellation, and concurrency behavior is proven through real service construction and deterministic fakes, with no false confidence from a disconnected unit-only runner.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-04-SUMMARY.md` when done.
</output>
