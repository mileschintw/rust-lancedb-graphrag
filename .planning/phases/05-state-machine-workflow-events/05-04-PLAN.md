---
phase: 05-state-machine-workflow-events
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - engine/src/tests.rs
autonomous: true
requirements: [ORCH-03]

must_haves:
  truths:
    - "Every per-node timeout (ReformulateQuery, RetrieveHybrid, ExtractGraphContext, AssemblePrompt) is independently provable via a genuine stall, parametrized across all four — not a single hand-verified case (AI-SPEC reference dataset scenario 8). AssemblePrompt has no I/O boundary to inject a stall into via a production fake (it is pure in-process computation, per 05-03's design) — proven instead via a runner-level test that constructs a WorkflowRunner directly with a test-only stalling Node standing in for the assemble_prompt position, exercising the SAME generic tokio::select! timeout machinery every node is subject to (REVIEWS.md Codex MEDIUM — AssemblePrompt was previously silently excluded from this parametrization with no documented reason beyond 'no I/O to stall')."
    - "Every WorkflowEvent emitted during one full 5-node happy-path query shares exactly one trace_id, established from the first event and checked against every subsequent event including AnswerChunk/FinalAnswer/WorkflowCompleted (D-29, AI-SPEC reference dataset scenario 13)."
  artifacts: []
  key_links: []
  prohibitions: []
---

<objective>
Complete the deterministic Tier 1 fault-injection coverage AI-SPEC Section 5's reference dataset requires, now that 05-02 has already closed the port-seam gap (`GraphQueryPort`/`DenseRetrievalPort`, moved forward from an earlier draft's original 05-04 scope per REVIEWS.md Codex HIGH #6) and proven the graph-timeout-degrade scenario deterministically. What remains after 05-01/05-02/05-03 is: (a) the one node — `AssemblePrompt` — that was never covered by any per-node timeout scenario because it has no I/O boundary to stall via a production-shaped fake, and (b) a whole-pipeline `trace_id` consistency check that needs all five nodes wired together, which only became possible once 05-03 landed.

Purpose: ORCH-03 names "cancellation, timeouts, and retry/fallback behavior for node execution" for ALL nodes, not just the ones with an obvious I/O boundary. Leaving `AssemblePrompt`'s timeout enforcement structurally untested — even though it uses the identical generic runner machinery every other node does — would be a silent gap in exactly the property ORCH-03 requires. This plan closes it with a runner-level test that doesn't require inventing fake I/O for a node that has none.
Output: two new in-process Tier 1 scenarios in `engine/src/tests.rs` — parametrized per-node timeout enforcement across all four non-generation nodes (extending 05-02's/05-01's existing per-node fakes with a runner-level direct-construction test for `AssemblePrompt`), and full-pipeline `trace_id` consistency.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-AI-SPEC.md
@rust-guidelines.md

<interfaces>
<!-- No new production types this plan — read_first for the exact seams already established. -->

Per-node fakes already available by this wave (do not recreate — reuse directly): `FakeEmbeddingProvider` (05-01, stall via `tokio::time::sleep`), `FakeGraphQueryPort`/`FakeDenseRetrievalPort` (05-02, same stall shape). `WorkflowRunner`'s public constructor and `run(ctx, cancel)` method (05-01, `engine/src/workflow/runner.rs`) are directly callable from a test without going through `LancetServiceImpl::query_rag` — this plan's `AssemblePrompt` timeout scenario constructs a `WorkflowRunner` directly with a minimal `Vec<Box<dyn Node>>` (a fast no-op node standing in for earlier pipeline stages if needed to seed `ctx`, plus one test-only stalling `Node` named `"assemble_prompt"`) rather than routing through the full `LancetServiceImpl` service construction — read `engine/src/workflow/runner.rs` and `engine/src/workflow/node.rs` (both as 05-01/05-02/05-03 left them) before writing this test to confirm the exact public surface available.
</interfaces>
</context>

## Artifacts this phase produces (this plan's share)

No new production symbols this plan — test-only additions to `engine/src/tests.rs`: a test-only `StallingTestNode` (implements `Node`, used only inside the AssemblePrompt-timeout scenario, never referenced from production code).

<tasks>

<task type="auto">
  <name>Task 1: Parametrized per-node timeout coverage including AssemblePrompt</name>
  <files>engine/src/tests.rs</files>
  <read_first>engine/src/tests.rs, engine/src/workflow/runner.rs, engine/src/workflow/node.rs</read_first>
  <action>
    In engine/src/tests.rs: confirm (do not re-derive) that `ReformulateQuery`, `RetrieveHybrid`, and `ExtractGraphContext` already each have a passing per-node timeout scenario from 05-01/05-02 (grep for the test function names) — this task does not duplicate those, it only ADDS the fourth. Define a test-only `struct StallingTestNode { stall: Duration }` implementing the `Node` trait (`name()` returns `"assemble_prompt"`, `run()` does `tokio::time::sleep(self.stall).await; Ok(())`, never a busy-loop) — this is a harness node, never wired into `main.rs`'s production `Vec`. Write a test that constructs a `WorkflowRunner` directly (not via `LancetServiceImpl::query_rag`) with a `Vec<Box<dyn Node>>` containing just enough real/minimal nodes to reach a populated `WorkflowContext` state plus `StallingTestNode` positioned where `AssemblePrompt` normally sits, with the runner's timeout list entry for that position set to a test-shortened value (e.g. 50ms) the `StallingTestNode`'s `stall` duration exceeds; assert `NodeFailed{category: Timeout, node_name: "assemble_prompt"}` then `WorkflowCompleted{success: false}`. Document in a comment why this differs from the other three nodes' timeout tests (no I/O boundary to fake, so the test exercises the generic runner machinery directly rather than a production-shaped double).
  </action>
  <verify>
    <automated>cd D:/Repos/lancet/engine &amp;&amp; cargo test --locked --bin engine -- assemble_prompt_timeout</automated>
  </verify>
  <acceptance_criteria>
    - engine/src/tests.rs contains a test-only StallingTestNode implementing the Node trait, never referenced outside test code
    - A test constructing WorkflowRunner directly asserts NodeFailed{category: Timeout, node_name: "assemble_prompt"} then WorkflowCompleted{success: false} against a genuinely stalling test node
    - All four non-generation nodes (ReformulateQuery, RetrieveHybrid, ExtractGraphContext, AssemblePrompt) now have an independently passing timeout-enforcement test somewhere in engine/src/tests.rs
  </acceptance_criteria>
  <done>AssemblePrompt's timeout enforcement is proven via the generic runner machinery, closing the one per-node timeout gap left after 05-01/05-02/05-03.</done>
</task>

<task type="auto">
  <name>Task 2: Full-pipeline trace_id consistency</name>
  <files>engine/src/tests.rs</files>
  <read_first>engine/src/tests.rs, engine/src/workflow/events.rs</read_first>
  <action>
    In engine/src/tests.rs: add one in-process Tier 1 scenario (calling `service.query_rag(...)` directly, per the established 05-01/05-02/05-03 pattern) that runs a full happy-path query through all 5 nodes (reuse the existing happy-path fixture construction from 05-03's scenarios — `FakeGenerator` configured to succeed on the first call, real `FakeEmbeddingProvider`/`FakeGraphQueryPort`/`FakeDenseRetrievalPort` or the existing simple fakes where a stall isn't needed), drain the full event stream, capture the first event's `trace_id` as ground truth, and assert every subsequent event — including `AnswerChunk`, `FinalAnswer`, and the terminal `WorkflowCompleted` — carries the identical value, and that this value equals the `session_id`/`correlation_id` minted at the D-04 boundary (consistent with 05-01's must_haves on the single-node case, now proven across the complete 5-node pipeline).
  </action>
  <verify>
    <automated>cd D:/Repos/lancet/engine &amp;&amp; cargo test --locked --bin engine -- trace_id_consistency</automated>
  </verify>
  <acceptance_criteria>
    - A Tier 1 test runs a full 5-node happy-path query and asserts every emitted WorkflowEvent (including AnswerChunk/FinalAnswer/WorkflowCompleted) shares one trace_id
    - The asserted trace_id equals the correlation_id minted at the D-04 boundary
  </acceptance_criteria>
  <done>trace_id consistency is proven across the complete 5-node pipeline, not just the single-node case 05-01 originally covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine/src/tests.rs test-only StallingTestNode vs. production nodes | Internal only — StallingTestNode is never constructed outside test code and is never part of main.rs's production Vec |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09 | Tampering | engine/src/tests.rs test-only node construction | low | accept | StallingTestNode lives entirely in `#[cfg(test)]` code (engine/src/tests.rs, compiled only for `cargo test`) and is never reachable from the production binary's node-construction call sites in main.rs — same isolation guarantee 05-02's Fake* ports already established |

</threat_model>

<verification>
1. `cargo test --locked --bin engine` (full crate, binary target — `engine/src/tests.rs` compiles into the `engine` binary, not the lib target; `--lib` would silently run zero of these tests) exits 0, including the new AssemblePrompt-timeout and trace_id-consistency scenarios.
2. Every one of AI-SPEC Section 5's 15 reference-dataset scenarios that does not require the Go/Postgres boundary is now proven somewhere across 05-01 through 05-04 — scenario 4 (05-02), scenario 8 fully parametrized across all 4 non-generation nodes (05-01/05-02/this plan), scenario 13 (this plan), scenario 15 (05-02). Scenario 14 (checkpoint/event backpressure isolation) is relocated to 05-05's Tier 2, since it is now, correctly, about the Go-side Postgres write path rather than the Rust event channel (REVIEWS.md Codex+Antigravity consensus HIGH on try_send).
</verification>

<success_criteria>
- Per-node timeout enforcement (ORCH-03) is proven across all four non-generation nodes, including the one (AssemblePrompt) with no I/O boundary to fake.
- trace_id consistency is proven end-to-end across the complete 5-node pipeline.
- No AI-SPEC reference-dataset scenario is silently dropped — each is either proven in this plan, proven in an earlier plan, or explicitly relocated with a stated reason.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-04-SUMMARY.md` when done
</output>

## Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|----------------|
| 05-02/05-04 dependency mismatch — port seam introduced too late for 05-02's own must-haves | HIGH (Codex) | Port seam (`GraphQueryPort`/`DenseRetrievalPort`) moved into 05-02; this plan's original Task 1 is deleted entirely |
| AssemblePrompt excluded from timeout parametrization with no fake to inject a stall | MEDIUM (Codex) | Task 1: runner-level test with a test-only `StallingTestNode`, proving the generic timeout machinery without needing production-shaped I/O fakes |
| `try_send`-based "never-draining receiver" scenario 14 cannot coexist with reliable event delivery | Consequence of HIGH (Codex+Antigravity consensus) | Scenario 14 relocated to 05-05 Tier 2 as a Go-side test (documentStore fake that blocks/sleeps on InsertWorkflowCheckpoint) — the only place the fire-and-forget property actually applies after 05-01's fix |
| RetrieveHybrid hard-error-vs-zero-evidence (scenario 15) | Already resolved | Confirmed already covered by 05-02 Task 2's own scenario (c) — not duplicated here |

## Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| None — all findings relevant to this plan's original scope were either fixed here or relocated with a stated reason above | — |

## Round 3 Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|----------------|
| Wave 4 file conflict — this plan and 05-05 are both scheduled parallel in Wave 4 but both listed `engine/src/tests.rs` in `files_modified` | HIGH (Codex, ROADMAP.md:339) | Resolved entirely on the 05-05 side, not here: 05-05's two Rust checkpoint tests (`build_checkpoint_snapshot` fixtures — pure-function assertions with zero dependency on this file's `configured_service`/`Fake*` harness) moved into an inline `#[cfg(test)] mod tests` at the bottom of `engine/src/workflow/events.rs`, which was already in 05-05's `files_modified`. `engine/src/tests.rs` is removed from 05-05's `files_modified` entirely — this plan remains its sole owner. Both plans stay Wave 4, genuinely parallel, with zero `files_modified` overlap; no ROADMAP.md change was needed. This plan's own `StallingTestNode` and its two new tests (Task 1/Task 2 above) are unaffected and unchanged. |