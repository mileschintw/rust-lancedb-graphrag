---
phase: 05-state-machine-workflow-events
plan: 04
type: execute
wave: 5
depends_on:
  - 05-03
files_modified:
  - engine/src/tests.rs
  - engine/src/tests/workflow_phase5.rs
autonomous: true
requirements: [ORCH-01, ORCH-02, ORCH-03]

must_haves:
  truths:
    - "Tier 1 Rust tests exercise the actual LancetServiceImpl workflow with injected fakes, not an unconnected runner-only imitation."
    - "The full successful event sequence proves graph-before-retrieval, coarse per-node cardinality, one AnswerChunk, one FinalAnswer, terminal success, common correlation trace_id, and independent session_id (D-06, D-20, D-29)."
    - "Zero evidence is a successful short circuit, while dense/BM25 hard errors are typed retrieval failures and do not look like the short circuit (D-03, D-22)."
    - "Sub-second settings make every node timeout test deterministic, including a focused actual AssemblePromptNode timeout test after the cooperative packing change (D-17, review MEDIUM)."
    - "Cancellation is proven during a running node and at the named retry boundary, and the WorkflowHandle outcome remains observable after the event receiver is dropped (D-16, review HIGH/MEDIUM)."
    - "A never-draining checkpoint side-channel receiver cannot delay client-visible workflow events or FinalAnswer because checkpoint admission is bounded and nonblocking, while every checkpoint is retained in owned FIFO overflow and later drained or reported as pending (D-27, review BLOCKER)."
    - "Two concurrent workflow calls use independent context, cancellation, event channels, and trace identities."
    - "Phase 5 scenarios are organized in engine/src/tests/workflow_phase5.rs behind a narrow module declaration in engine/src/tests.rs, so the existing approximately 6,870-line host file does not absorb another large block of workflow fakes and cases."
    - "Every successful spawned-task join explicitly calls AbortOnDropGuard::disarm before assertions or cleanup; failed or timed-out joins leave the guard armed so the task is aborted rather than leaked."
    - "The checkpoint tests consume the exact 05-01/05-06 handoff contract: primary capacity 1, FIFO overflow capacity 5, Pending/transport-closed status, and explicit overflow rather than a locally invented queue policy."
  artifacts:
    - path: "engine/src/tests.rs"
      provides: "The explicit workflow_phase5 module declaration and narrowly scoped shared fixture visibility for the dedicated Phase 5 test module"
    - path: "engine/src/tests/workflow_phase5.rs"
      provides: "All Phase 5 Tier 1 fakes, configured WorkflowDependencies construction, RecordingCheckpointSink, and named service-level workflow scenarios without further expanding the large tests.rs host"
    - path: "engine/src/tests.rs::workflow_phase5"
      provides: "The explicit module declaration connecting the dedicated Phase 5 test file to the existing test crate"
    - path: "engine/src/tests/workflow_phase5.rs::AbortOnDropGuard"
      provides: "A task cleanup guard whose disarm method is called after every successful join"
  key_links:
    - from: "engine/src/tests.rs::configured_service"
      to: "LancetServiceImpl::workflow_dependencies"
      via: "test-only graph/dense/BM25/reformulator/prompt/generator fakes are injected through the same service field production uses"
    - from: "test stream receiver drop"
      to: "WorkflowHandle::outcome"
      via: "the test consumes the terminal outcome channel rather than expecting a terminal event after the receiver is closed"
    - from: "checkpoint handoff primary queue"
      to: "engine/src/tests/workflow_phase5.rs::RecordingCheckpointSink"
      via: "the test fills the bounded primary queue, verifies owned FIFO overflow, releases the sink, and asserts every full snapshot is drained in order"
  prohibitions:
    - requirement_id: ORCH-01
      category: test-integrity
      statement: "MUST NOT make a test pass by asserting only the event count or by using a fake path that bypasses LancetServiceImpl dependency construction."
      verification: test
    - requirement_id: ORCH-02
      category: test-integrity
      statement: "MUST NOT equate trace_id with session_id or accept extra sub-step/retry events in the full-sequence assertion."
      verification: test
    - requirement_id: ORCH-03
      category: test-integrity
      statement: "MUST NOT use sleeps tied to five-, ten-, fifteen-, or thirty-second production defaults; timeout scenarios must override settings below one second."
      verification: test
    - requirement_id: ORCH-02
      category: reliability
      statement: "MUST NOT count a checkpoint as delivered merely because the primary queue was full or closed; the recording sink must receive it after drain or the terminal outcome must expose it as pending."
      verification: test
---

<objective>
Build the deterministic Tier 1 evaluation harness that proves the Rust state machine's ordering, failure categories, retry/cancellation edges, and actual node timeout behavior.

**As a** Lancet engineer, **I want to** reproduce orchestration failures without provider or storage timing, **so that** workflow behavior remains debuggable as the pipeline evolves.

Purpose: turn every operative review concern about testability into a runnable Rust assertion before cross-runtime verification.
Output: service-level fakes and named deterministic scenarios covering the AI-SPEC reference dimensions that do not require Go or PostgreSQL.
</objective>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-state-machine-workflow-events/05-02-SUMMARY.md
@.planning/phases/05-state-machine-workflow-events/05-03-SUMMARY.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-AI-SPEC.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEWS.md
@AGENTS.md
@rust-guidelines.md
</context>

## Artifacts this phase produces

- `engine/src/tests.rs`: the explicit `workflow_phase5` module declaration and narrowly scoped shared fixture visibility needed by the dedicated Phase 5 test module.
- `engine/src/tests/workflow_phase5.rs`: all Phase 5 Tier 1 fakes, configured WorkflowDependencies construction, `RecordingCheckpointSink`, and named workflow scenarios covering happy path, zero evidence, graph degrade/timeout, retrieval error, empty reformulation, node timeout, retry success/exhaustion, cancellation, trace/session identity, and concurrent-call isolation.

<tasks>

<task type="tracer">
  <name>Task 1: Trace a service-level happy path with real workflow nodes and injected fakes</name>
  <action_extension>Make the service happy-path assertions executable for D-20's coarse event granularity, D-16's native receiver-close cancellation and sibling isolation, and D-11's rule that only `GenerateAnswer` owns automatic retry. Use `05-AI-SPEC.md`/`05-RESEARCH.md` as the approved workflow-test contract when no existing Phase 5 service analog exists.</action_extension>
  <read_first>engine/src/tests.rs, engine/src/main.rs, engine/src/workflow/ports.rs, engine/src/workflow/node.rs, engine/src/workflow/runner.rs, engine/src/workflow/events.rs, engine/src/workflow/nodes/assemble_prompt.rs, engine/src/workflow/nodes/generate.rs, 05-01-PLAN.md, 05-06-PLAN.md, 05-AI-SPEC.md, 05-REVIEWS.md, AGENTS.md, rust-guidelines.md</read_first>
  <files>engine/src/tests.rs, engine/src/tests/workflow_phase5.rs</files>
  <behavior>
    - `LancetServiceImpl::query_rag` with fake ports -> all real workflow nodes execute and produce the ordered happy-path event stream.
    - Event assertions -> trace_id equals correlation_id for every event; WorkflowCompleted.session_id equals the requested session id and differs from trace_id.
    - Service factory -> no concrete graph, dense, or BM25 query is invoked when the corresponding fake is installed.
    - Named exact tests in `engine/src/tests/workflow_phase5.rs` -> `test_workflow_happy_path_preserves_order_and_identity` and `test_workflow_concurrent_calls_have_independent_cancellation_and_trace` are each registered and pass.
  </behavior>
  <action>Keep `engine/src/tests.rs` as the shared test host and add an explicit `#[path = "tests/workflow_phase5.rs"] mod workflow_phase5;` declaration, then place the new Phase 5 fakes and named scenarios in `engine/src/tests/workflow_phase5.rs`; expose only the shared `configured_service`/fixture helpers needed by the child module instead of duplicating them. Populate the production `WorkflowDependencies` field with deterministic `FakeQueryReformulator`, `FakeGraphQueryPort`, `FakeDenseRetrievalPort`, and `FakeBm25RetrievalPort` instances while preserving the existing isolated LanceDB setup required to construct `LancetServiceImpl`. Add a fake generator response that contains valid cited evidence and a fake prompt assembler only where the production node's explicit port requires it; call the actual tonic `LancetServiceImpl::query_rag` implementation and drain its returned stream through the same Rust event conversion path used by tonic. Add `test_workflow_happy_path_preserves_order_and_identity` covering D-06 graph-before-retrieval order, one NodeStarted/terminal pair per executed node, one AnswerChunk, one FinalAnswer, successful WorkflowCompleted, common correlation-derived trace_id, and independent session_id. Add `test_workflow_concurrent_calls_have_independent_cancellation_and_trace` with two simultaneous requests and separate event/outcome drains. Keep this as deterministic Tier 1 coverage: no OpenRouter, PostgreSQL, process spawn, wall-clock production timeout, or fake runner shortcut.</action>
  <verify>
    <automated>$rustList = @(cargo test --manifest-path engine/Cargo.toml --locked --bin engine -- --list 2>&amp;1)
$rustListExit = $LASTEXITCODE
if ($rustListExit -ne 0) { exit $rustListExit }
$expectedRustTests = @('test_workflow_happy_path_preserves_order_and_identity', 'test_workflow_concurrent_calls_have_independent_cancellation_and_trace')
foreach ($leaf in $expectedRustTests) {
  $escapedLeaf = [regex]::Escape($leaf)
  $registered = @($rustList | ForEach-Object { $line = $_.ToString(); if ($line -match ("^\s*(?<full>[A-Za-z0-9_:]*" + $escapedLeaf + "):\s+test\s*$")) { $Matches['full'] } })
  if ($registered.Count -ne 1) { throw "Expected one exact Rust registration ending in $leaf, found $($registered.Count)" }
  $fullName = $registered[0]
  $run = @(cargo test --manifest-path engine/Cargo.toml --locked --bin engine -- --exact $fullName --nocapture 2>&amp;1)
  $runExit = $LASTEXITCODE
  if ($runExit -ne 0) { exit $runExit }
  if (@($run | Where-Object { $_.ToString() -match ("^test\s+" + [regex]::Escape($fullName) + "\s+\.\.\.\s+ok\s*$") }).Count -lt 1) { throw "Exact Rust test did not report PASS: $fullName" }
}</automated>
  </verify>
  <acceptance_criteria>
    - The test invokes `LancetServiceImpl::query_rag` with the real production node vector and injected dependencies; it does not instantiate a separate toy runner.
    - `engine/src/tests.rs` contains only the explicit `workflow_phase5` module declaration and shared fixture changes; the new Phase 5 fakes/scenarios reside in `engine/src/tests/workflow_phase5.rs`.
    - The exact event sequence proves graph-before-retrieval and contains no dense/BM25 sub-step or retrying event.
    - All event trace IDs equal the request correlation ID, and the completed event's session ID is asserted separately.
    - Two concurrent requests have distinct traces and cancellation tokens, and completing or dropping one stream does not alter the other.
  </acceptance_criteria>
  <done>A real service-level happy path and concurrent isolation proof exercise the same Rust workflow architecture that production uses.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic failure, timeout, retry, and cancellation scenarios</name>
  <action_extension>In the complete 24-test matrix, directly assert D-11 generation-only retry scope, D-16 cancellation at the client receiver boundary with no hanging supervisor, and D-20 coarse node/answer/terminal event cardinality. Use `05-AI-SPEC.md`/`05-RESEARCH.md` as the approved source for the timeout, cancellation, retry-gate, and checkpoint-handoff scenarios while retaining the shared test-host organization.</action_extension>
  <read_first>engine/src/tests.rs, engine/src/tests/workflow_phase5.rs, engine/src/workflow/ports.rs, engine/src/workflow/node.rs, engine/src/workflow/runner.rs, engine/src/workflow/nodes/graph_context.rs, engine/src/workflow/nodes/retrieve.rs, engine/src/workflow/nodes/assemble_prompt.rs, engine/src/workflow/nodes/generate.rs, engine/src/workflow/events.rs, 05-01-PLAN.md, 05-06-PLAN.md, 05-AI-SPEC.md, 05-REVIEWS.md, AGENTS.md, rust-guidelines.md</read_first>
  <files>engine/src/tests.rs, engine/src/tests/workflow_phase5.rs</files>
  <behavior>
    - Empty reformulator -> typed internal failure; dense failure and BM25 failure -> RetrievalFailed; zero candidates -> successful D-03 short circuit.
    - Graph no-match/failure/timeout -> graph node completes and retrieval continues with empty graph context.
    - Reformulate/Retrieve/Prompt node timeout -> Timeout; prompt assertion uses the actual AssemblePromptNode with a stalled injected assembler.
    - Generation first failure -> retry gate then equal request; double failure -> honest failed terminal; cancellation at gate -> one generator call and Cancelled outcome.
    - Receiver drop during a node -> closed stream plus WorkflowHandle outcome, with no hanging supervisor or fabricated terminal event.
    - Checkpoint persistence under a stalled receiver -> all full snapshots enter owned bounded FIFO overflow, then reach the recording sink in order after release; a closed receiver exposes exact pending records.
    - Named exact tests in `engine/src/tests/workflow_phase5.rs` -> `test_workflow_checkpoint_persistence_under_backpressure`, `test_workflow_timeout_matrix_uses_subsecond_settings`, and `test_workflow_cancellation_at_retry_gate` are each registered and pass.
  </behavior>
  <failure_matrix>
    The complete service-level matrix is the following exact Rust test-name list; every name must be registered, executed with `--exact`, and report one passing test: `test_workflow_happy_path_preserves_order_and_identity`, `test_workflow_concurrent_calls_have_independent_cancellation_and_trace`, `test_workflow_zero_evidence_short_circuit`, `test_workflow_empty_reformulation_is_internal_failure`, `test_workflow_graph_no_match_degrades_and_continues`, `test_workflow_graph_failure_degrades_and_continues`, `test_workflow_graph_timeout_degrades_and_continues`, `test_workflow_dense_failure_is_retrieval_failed`, `test_workflow_bm25_failure_is_retrieval_failed`, `test_workflow_reformulate_timeout_is_timeout`, `test_workflow_retrieve_timeout_is_timeout`, `test_workflow_prompt_timeout_uses_actual_assemble_node`, `test_workflow_prompt_failure_is_prompt_assembly_failed`, `test_workflow_timeout_matrix_uses_subsecond_settings`, `test_workflow_generation_retry_succeeds_once`, `test_workflow_generation_double_failure_is_honest`, `test_workflow_citation_failure_does_not_retry`, `test_workflow_cancellation_during_node`, `test_workflow_cancellation_at_retry_gate`, `test_workflow_receiver_drop_reports_handle_outcome`, `test_workflow_checkpoint_persistence_under_backpressure`, `test_workflow_checkpoint_closed_receiver_reports_pending`, `test_workflow_checkpoint_sixth_record_reports_overflow`, and `test_workflow_event_channel_and_supervisor_cleanup`.
    This list explicitly covers event ordering, timeout/cancellation distinctions, graph no-match and logical/timeout degradation, dense/BM25 retrieval failures, prompt failure and actual prompt-node timeout, one-retry success and retry exhaustion, citation failure without retry, client receiver disconnect, checkpoint FIFO/closed/overflow behavior, and bounded Notify/task cleanup. No scenario in this list may be represented only by an unlisted helper or inferred from an unfiltered-suite pass.
  </failure_matrix>
  <action>Implement every function named in the failure-matrix list above in `engine/src/tests/workflow_phase5.rs`; the list is exhaustive for the service-level matrix and no scenario may be represented only by an unlisted helper. Define the deterministic fakes and scenarios using the shared host factory from `engine/src/tests.rs`, so the approximately 6,870-line existing test host gains only the module declaration and narrowly scoped helper visibility. Fakes can return configured results, block on a `Notify`, and record calls/requests: `FakeQueryReformulator` must be able to return an empty vector; graph, dense, and BM25 fakes must fail or stall independently; `FakePromptAssembler` must stall inside the actual `AssemblePromptNode`; and the generator/retry-gate pair must expose call count, exact `GenerationRequest` values, and a gate immediately before attempt two. Add a `RecordingCheckpointSink`/handoff harness that consumes the exact 05-01/05-06 `CheckpointHandoff` contract (`CHECKPOINT_PRIMARY_CAPACITY = 1`, `CHECKPOINT_OVERFLOW_CAPACITY = 5`, FIFO order, `Pending` records, and transport-closed status); it pauses consumption, fills the bounded primary queue, records the full snapshots held in owned FIFO overflow, releases the sink, and asserts every expected record is delivered in order with an empty pending set. Add a closed-sink case that asserts `WorkflowOutcome.pending_checkpoints` contains the exact undelivered records and explicit transport-closed status, and an attempted sixth record is an explicit overflow/pending result rather than silent success. Wrap every `Notify::notified()` rendezvous wait in `tokio::time::timeout` with a short test deadline. Define `AbortOnDropGuard::disarm(&mut self)` and call it immediately after every successful spawned-task `join.await`, before assertions or cleanup; a failed or timed-out join leaves the guard armed so its Drop implementation aborts the task. In the normal path notify all release waiters, await task shutdown under another bounded timeout, disarm only after that join succeeds, and then assert results; this cleanup runs even when an earlier assertion fails, so no stalled rendezvous survives the test. Set all workflow timeout fields to sub-second values in each test's `EffectiveRagSettings` so no case waits on production defaults. Assert typed NodeErrorCategory values and sanitized messages rather than raw provider/storage details. Use fatal assertions for every fixture setup and snapshot read; never convert a fake configuration error into a passing empty result. This explicitly addresses the review findings about uninjectable service fakes, empty variants, non-preemptive prompt timeout, retry rendezvous timing, checkpoint backpressure isolation, cancellation outcome observability, large-test-file organization, and abort-guard cleanup.</action>
  <verify>
    <automated>$phase5TestFile = Get-Content -LiteralPath 'engine/src/tests/workflow_phase5.rs' -Raw
$matrixNames = @('test_workflow_happy_path_preserves_order_and_identity', 'test_workflow_concurrent_calls_have_independent_cancellation_and_trace', 'test_workflow_zero_evidence_short_circuit', 'test_workflow_empty_reformulation_is_internal_failure', 'test_workflow_graph_no_match_degrades_and_continues', 'test_workflow_graph_failure_degrades_and_continues', 'test_workflow_graph_timeout_degrades_and_continues', 'test_workflow_dense_failure_is_retrieval_failed', 'test_workflow_bm25_failure_is_retrieval_failed', 'test_workflow_reformulate_timeout_is_timeout', 'test_workflow_retrieve_timeout_is_timeout', 'test_workflow_prompt_timeout_uses_actual_assemble_node', 'test_workflow_prompt_failure_is_prompt_assembly_failed', 'test_workflow_timeout_matrix_uses_subsecond_settings', 'test_workflow_generation_retry_succeeds_once', 'test_workflow_generation_double_failure_is_honest', 'test_workflow_citation_failure_does_not_retry', 'test_workflow_cancellation_during_node', 'test_workflow_cancellation_at_retry_gate', 'test_workflow_receiver_drop_reports_handle_outcome', 'test_workflow_checkpoint_persistence_under_backpressure', 'test_workflow_checkpoint_closed_receiver_reports_pending', 'test_workflow_checkpoint_sixth_record_reports_overflow', 'test_workflow_event_channel_and_supervisor_cleanup')
foreach ($name in $matrixNames) {
  if ($phase5TestFile -notmatch ("(?m)^\s*(?:pub\s+)?(?:async\s+)?fn\s+" + [regex]::Escape($name) + "\b")) { throw "Phase 5 matrix test must be defined in engine/src/tests/workflow_phase5.rs: $name" }
}
$rustList = @(cargo test --manifest-path engine/Cargo.toml --locked --bin engine -- --list 2>&amp;1)
$rustListExit = $LASTEXITCODE
if ($rustListExit -ne 0) { exit $rustListExit }
$expectedRustTests = @('test_workflow_happy_path_preserves_order_and_identity', 'test_workflow_concurrent_calls_have_independent_cancellation_and_trace', 'test_workflow_zero_evidence_short_circuit', 'test_workflow_empty_reformulation_is_internal_failure', 'test_workflow_graph_no_match_degrades_and_continues', 'test_workflow_graph_failure_degrades_and_continues', 'test_workflow_graph_timeout_degrades_and_continues', 'test_workflow_dense_failure_is_retrieval_failed', 'test_workflow_bm25_failure_is_retrieval_failed', 'test_workflow_reformulate_timeout_is_timeout', 'test_workflow_retrieve_timeout_is_timeout', 'test_workflow_prompt_timeout_uses_actual_assemble_node', 'test_workflow_prompt_failure_is_prompt_assembly_failed', 'test_workflow_timeout_matrix_uses_subsecond_settings', 'test_workflow_generation_retry_succeeds_once', 'test_workflow_generation_double_failure_is_honest', 'test_workflow_citation_failure_does_not_retry', 'test_workflow_cancellation_during_node', 'test_workflow_cancellation_at_retry_gate', 'test_workflow_receiver_drop_reports_handle_outcome', 'test_workflow_checkpoint_persistence_under_backpressure', 'test_workflow_checkpoint_closed_receiver_reports_pending', 'test_workflow_checkpoint_sixth_record_reports_overflow', 'test_workflow_event_channel_and_supervisor_cleanup')
foreach ($leaf in $expectedRustTests) {
  $escapedLeaf = [regex]::Escape($leaf)
  $registered = @($rustList | ForEach-Object { $line = $_.ToString(); if ($line -match ("^\s*(?<full>[A-Za-z0-9_:]*" + $escapedLeaf + "):\s+test\s*$")) { $Matches['full'] } })
  if ($registered.Count -ne 1) { throw "Expected one exact Rust registration ending in $leaf, found $($registered.Count)" }
  $fullName = $registered[0]
  $run = @(cargo test --manifest-path engine/Cargo.toml --locked --bin engine -- --exact $fullName --nocapture 2>&amp;1)
  $runExit = $LASTEXITCODE
  if ($runExit -ne 0) { exit $runExit }
  if (@($run | Where-Object { $_.ToString() -match '^running 1 test$' }).Count -ne 1) { throw "Exact Rust test did not execute exactly one test: $fullName" }
  if (@($run | Where-Object { $_.ToString() -match ("^test\s+" + [regex]::Escape($fullName) + "\s+\.\.\.\s+ok\s*$") }).Count -ne 1) { throw "Exact Rust test did not report exactly one PASS: $fullName" }
  if (@($run | Where-Object { $_.ToString() -match ("^test\s+" + [regex]::Escape($fullName) + "\s+\.\.\.\s+ignored\s*$") }).Count -gt 0) { throw "Exact Rust test was ignored: $fullName" }
  if (@($run | Where-Object { $_.ToString() -match '^test result: ok\.\s+1 passed;\s+0 failed;\s+0 ignored;' }).Count -ne 1) { throw "Exact Rust test did not report one passed and zero ignored tests: $fullName" }
}
cargo test --manifest-path engine/Cargo.toml --locked --bin engine
$fullRustExit = $LASTEXITCODE
if ($fullRustExit -ne 0) { exit $fullRustExit }</automated>
  </verify>
  <acceptance_criteria>
    - Every named scenario runs with sub-second settings and deterministic fake gates; no test relies on provider/network timing.
    - Zero evidence produces success with no AssemblePrompt/GenerateAnswer start; retrieval errors produce RetrievalFailed; graph timeout degrades and continues.
    - Actual AssemblePromptNode timeout behavior is tested through its injected assembler boundary after cooperative production packing was implemented.
    - Retry tests prove one equal-request retry, no third call, no retrying event, no fabrication, and cancellation between attempts.
    - Stream-drop tests retrieve WorkflowHandle outcome separately and prove the supervisor terminates on natural completion and client closure.
    - `test_workflow_checkpoint_persistence_under_backpressure`, `test_workflow_checkpoint_closed_receiver_reports_pending`, and `test_workflow_checkpoint_sixth_record_reports_overflow` are implemented in `engine/src/tests/workflow_phase5.rs`, fill the primary handoff, prove client progress continues, then verify every checkpoint snapshot is delivered FIFO after release; closed/overflow cases expose exact pending records instead of reporting success.
    - The backpressure tests assert primary capacity 1, overflow capacity 5, the exact Pending/transport-closed fields, and explicit handling of a sixth attempted record using the shared 05-01/05-06 contract rather than a second queue implementation.
    - Every successful task join disarms `AbortOnDropGuard`; a timeout or failed join leaves it armed and the full focused suite completes without leaked fake tasks.
    - The unfiltered engine test suite passes, so targeted names are not the only evidence of coverage.
  </acceptance_criteria>
  <done>Tier 1 covers the full deterministic failure matrix and closes the Rust-side execution risks identified by both reviewers.</done>
</task>

</tasks>

<threat_model>
ASVS Level: 1. Blocking threshold: high. High-severity threats block plan acceptance/execution.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Test fixture → workflow port | Fault-injection inputs simulate storage/provider failures and must not weaken production validation. |
| Workflow event receiver → test harness | Receiver closure is the same cancellation signal as a client disconnect. |
| Test configuration → timeout runner | Sub-second overrides control deterministic resource-bound assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-14 | Tampering | Test-only dependency injection | medium | mitigate | Use the same `WorkflowDependencies` field as production and assert fake calls/results, preventing tests from bypassing the service wiring. |
| T-05-15 | Denial of Service | Cancellation/supervisor tests | high | mitigate | Use bounded test deadlines, Notify-based release paths, and explicit cleanup assertions for every stalled fake and spawned task. |
| T-05-16 | Repudiation | Failure-category assertions | medium | mitigate | Assert exact D-22 categories, correlation-linked trace IDs, and sanitized messages across every failure scenario. |
| T-05-17 | Information Disclosure | Fault fixture messages | low | accept | Fixtures use synthetic identifiers and the production sanitizer is exercised; no external or user data enters Tier 1 tests. |
| T-05-27 | Denial of Service | Checkpoint handoff test harness | high | mitigate | Saturate only the bounded primary queue, assert owned FIFO overflow retains every expected record, release the recording sink under a bounded timeout, and verify closed-sink records remain observable as pending. |
</threat_model>

<verification>
- Run the focused named Tier 1 scenarios, then the full locked Rust suite and clippy.
- Confirm the test list contains every named scenario before executing the focused subset.
- Confirm no PostgreSQL or provider environment is required for this plan; those cross-runtime claims are explicitly environment-gated in 05-05.
</verification>

<success_criteria>
The Rust workflow's complete event, failure, timeout, retry, cancellation, and concurrency behavior is proven through real service construction and deterministic fakes, with no false confidence from a disconnected unit-only runner.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-04-SUMMARY.md` when done.
</output>
