---
phase: 05-state-machine-workflow-events
plan: 23
type: execute
wave: 12
depends_on:
  - 05-08
  - 05-14
  - 05-17
files_modified:
  - engine/src/workflow/nodes/retrieve.rs
  - engine/src/workflow/events.rs
  - engine/src/main.rs
  - engine/src/retrieval/tests.rs
autonomous: true
gap_closure: true
requirements:
  - ORCH-01
  - ORCH-02
  - ORCH-03
  - ORCH-04
must_haves:
  truths:
    - "Every exhaustive Rust RetrievalSnapshot and WorkflowCompletedEvent construction site compiles immediately after the additive generated fields land."
    - "The Rust wire-contract regression proves RetrievalSnapshot tags 1 through 9 remain intact while tags 10 and 11 round-trip count and ordered identities."
    - "05-18 receives a complete Rust source and library target that is ready for its introducing binary test-target no-run gate; no accepted compile-break window is used."
  artifacts:
    - path: "engine/src/workflow/nodes/retrieve.rs"
      provides: "Compile-safe RetrievalSnapshot construction with explicit additive fields before 05-16 semantic enrichment."
    - path: "engine/src/workflow/events.rs"
      provides: "Compile-safe WorkflowCompletedEvent construction with explicit notices before 05-19 terminal population."
    - path: "engine/src/main.rs"
      provides: "Compile-safe production RetrievalSnapshot construction at both existing production sites."
    - path: "engine/src/retrieval/tests.rs"
      provides: "Rust generated-contract round-trip and field-number regression."
  key_links:
    - "05-17 generated fields -> retrieve.rs/events.rs/main.rs explicit literals -> 05-18 target handoff"
    - "RetrievalSnapshot descriptor tags -> encode/decode test -> 05-16 production provenance population"

---

<objective>
Repair every Rust message construction site and prove the additive RetrievalSnapshot wire contract before target-aware downstream changes.

Purpose: Generated protobuf fields must not leave the Rust crate in a hidden compile-break state. This plan is the explicit handoff between 05-17's schema/bindings and 05-18's complete binary test-target gate.
Output: Explicit zero/default initialization at every current Rust construction site and a focused Rust wire round-trip test; 05-16 and 05-19 later replace those defaults with semantic runtime values.
</objective>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-preconditions.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-reversibility.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-PATTERNS.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEWS.md
@.planning/phases/05-state-machine-workflow-events/05-17-PLAN.md
@engine/src/workflow/nodes/retrieve.rs
@engine/src/workflow/events.rs
@engine/src/main.rs
@engine/src/retrieval/tests.rs
@AGENTS.md
@rust-guidelines.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Repair all exhaustive Rust message literals after generation</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; 05-CONTEXT.md; 05-17-PLAN.md; engine/src/workflow/nodes/retrieve.rs; engine/src/workflow/events.rs; engine/src/main.rs; engine/src/pb/lancet/v1/lancet.v1.rs</read_first>
  <files>engine/src/workflow/nodes/retrieve.rs, engine/src/workflow/events.rs, engine/src/main.rs</files>
  <action>No useful analog applies to generated-field construction repairs; use the explicit `05-PATTERNS.md` `## No Analog Found` exception and the 05-17 generated contract as the source of truth. Enumerate every Rust `RetrievalSnapshot {` literal in `engine/src/workflow/nodes/retrieve.rs` and both production regions of `engine/src/main.rs`, and every `WorkflowCompletedEvent {` literal in `engine/src/workflow/events.rs`. Add explicit `variant_count: 0` and `variant_identities: Vec::new()` to each RetrievalSnapshot literal and `notices: Vec::new()` to each WorkflowCompletedEvent literal so the generated additive fields compile immediately. Preserve existing fields and D-05/D-07/D-08 semantics; 05-16 owns later production provenance enrichment and 05-19 owns later terminal-notice enrichment. Do not use default-spread or unkeyed construction to hide the new fields, do not alter protobuf tags, and do not add D-30/D-31 metadata. Keep the four construction-site ownership boundaries explicit for downstream source guards.
  <acceptance_criteria>
    - Every current Rust RetrievalSnapshot literal names both additive fields, and every WorkflowCompletedEvent literal names notices.
    - Existing values and wire-field names remain unchanged; the defaults are compile repair only and are explicitly replaced by 05-16/05-19 semantic owners.
    - `engine/src/main.rs`, `engine/src/workflow/events.rs`, and `engine/src/workflow/nodes/retrieve.rs` compile against the generated bindings without an unassigned literal.
  </acceptance_criteria>
  <verify>
    <automated>$snapshotPaths = @(rg -l 'RetrievalSnapshot\s*\{' engine/src/main.rs engine/src/workflow | Sort-Object); $snapshotExpected = @('engine/src/main.rs','engine/src/workflow/nodes/retrieve.rs'); if ((Compare-Object -ReferenceObject ($snapshotExpected | Sort-Object) -DifferenceObject $snapshotPaths)) { throw "RetrievalSnapshot ownership mismatch: $($snapshotPaths -join ', ')" }; $terminalPaths = @(rg -l 'WorkflowCompletedEvent\s*\{' engine/src/main.rs engine/src/workflow | Sort-Object); $terminalExpected = @('engine/src/workflow/events.rs'); if ((Compare-Object -ReferenceObject ($terminalExpected | Sort-Object) -DifferenceObject $terminalPaths)) { throw "WorkflowCompletedEvent ownership mismatch: $($terminalPaths -join ', ')" }; foreach ($path in $snapshotExpected) { $source = Get-Content $path -Raw; if ($source -notmatch 'variant_count\s*:') { throw "variant_count literal missing from $path" }; if ($source -notmatch 'variant_identities\s*:') { throw "variant_identities literal missing from $path" } }; $events = Get-Content 'engine/src/workflow/events.rs' -Raw; if ($events -notmatch 'WorkflowCompletedEvent\s*\{[\s\S]*notices\s*:') { throw 'terminal notices literal missing from events.rs' }; $lib = cargo check --lib --manifest-path engine/Cargo.toml --locked; $libExit = $LASTEXITCODE; if ($libExit -ne 0) { exit $libExit }; $bin = cargo check --bin engine --manifest-path engine/Cargo.toml --locked; $binExit = $LASTEXITCODE; if ($binExit -ne 0) { exit $binExit }</automated>
  </verify>
  <done>All current Rust construction sites explicitly initialize the additive generated fields, leaving 05-18 with a compile-safe schema handoff and 05-16/05-19 with clear semantic ownership.</done>
</task>

<task type="auto">
  <name>Task 2: Prove RetrievalSnapshot additive tags and round-trip values</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; 05-CONTEXT.md; 05-17-PLAN.md; engine/src/pb/lancet/v1/lancet.v1.rs; engine/src/retrieval/tests.rs</read_first>
  <files>engine/src/retrieval/tests.rs</files>
  <action>No useful analog applies to the generated-wire repair proof; use the explicit `05-PATTERNS.md` `## No Analog Found` exception and keep the test scoped to the 05-17 generated contract. Add `retrieval_snapshot_variant_provenance_wire_contract` as a Rust library test using explicit `variant_count` and ordered `variant_identities` values. Encode and decode a populated generated RetrievalSnapshot, assert descriptor field numbers 10 and 11, assert historical field numbers 1 through 9 remain present, and compare the decoded count and identity order. Keep this as a generated-contract proof: 05-16 owns production literal population and 05-11 owns the Go cross-runtime fixture. Preserve D-07/D-08 provenance semantics and D-30's exclusion of vector counts, BM25 counts, graph counts, and token metadata.
  <acceptance_criteria>
    - The Rust test proves additive field numbers 10 and 11 and round-trips count plus ordered identities.
    - Historical RetrievalSnapshot fields 1 through 9 remain represented and the test does not create a second production population path.
    - The library test is registered exactly once and is runnable before 05-18's target-aware migration.
  </acceptance_criteria>
  <verify>
    <automated>$name = 'retrieval_snapshot_variant_provenance_wire_contract'; $listed = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $pattern = '(^|::)' + [regex]::Escape($name) + ': test$'; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -match $pattern }); if ($registered.Count -ne 1) { throw 'Rust wire-contract registration count mismatch' }; $run = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $name --nocapture; $runExit = $LASTEXITCODE; if ($runExit -ne 0) { exit $runExit }</automated>
  </verify>
  <done>The Rust generated RetrievalSnapshot contract proves exact additive tags and ordered provenance values, while production and Go fixture ownership remain separated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated protobuf fields to Rust literals | New fields must be initialized at every exhaustive construction site. |
| Rust generated message to encoded wire bytes | Descriptor tags and round-trip values cross the serialization boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-23-01 | Tampering | Rust message construction sites | high | mitigate | Enumerate every literal, require explicit new fields, and compile both library and binary targets. |
| T-05-23-02 | Tampering | RetrievalSnapshot wire encoding | critical | mitigate | Assert tags 1 through 11, encode/decode populated values, and require exact test registration. |
| T-05-23-03 | Information disclosure | variant_identities | medium | mitigate | Restrict the wire test to D-07/D-08 ordered identities and exclude generic metadata under D-30. |
| T-05-23-SC | Tampering | npm/pip/cargo installs | high | accept | This plan adds no package-manager dependency or install task; the slopcheck register is not applicable. |
</threat_model>

<verification>
- All current Rust message literals compile after 05-17 generation and are assigned to explicit semantic owners.
- The focused Rust wire test lists once and passes with exact field numbers and ordered values.
- The 05-18 introducing binary test-target no-run gate is the next target-aware proof after this compile-repair boundary; no intermediate compile-break exception is accepted.
</verification>

<success_criteria>
The generated additive protobuf fields are initialized across the Rust crate and the RetrievalSnapshot wire contract is proven before the library/binary test-target split and later production population plans execute.</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-23-SUMMARY.md` when done.
</output>
