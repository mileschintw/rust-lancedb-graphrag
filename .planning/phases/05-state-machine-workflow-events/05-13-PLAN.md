---
phase: 05-state-machine-workflow-events
plan: 13
type: execute
wave: 9
depends_on:
  - 05-09
files_modified:
  - engine/src/generation/openrouter.rs
  - engine/src/generation/tests.rs
  - engine/src/workflow/node.rs
  - engine/src/workflow/nodes/generate.rs
  - engine/src/prompt.rs
  - engine/src/tests/workflow_phase5.rs
  - engine/src/tests/workflow_phase5_production.rs
autonomous: true
gap_closure: true
requirements:
  - ORCH-02
  - ORCH-03
must_haves:
  truths:
    - "OpenRouter capability preflight has its own short deadline and successful-only cache, independent of the 30-second provider-attempt timeout and the 65-second production generation-node budget."
    - "Concurrent callers for one composite models-endpoint/model key share one in-flight capability preflight, so a bounded concurrent regression observes exactly one /models request while preserving cancellation and retry behavior."
    - "Preflight timeout, connect, reset, and equivalent transient transport failures become typed retryable provider failures, while unsupported-parameter responses remain typed non-retryable capability failures."
    - "GenerateAnswer performs exactly one immediate retry for the locked transient generation classes with a byte-identical GenerationRequest, and a second failure emits an honest typed failure without a fabricated answer, alternate provider, or third attempt."
    - "NodeFailed.retryable is derived from typed generation error state and retry exhaustion rather than a hardcoded event literal, and no retrying event is emitted."
  artifacts:
    - path: "engine/src/generation/openrouter.rs"
      provides: "Dedicated capability-preflight timeout, successful-only single-flight capability cache, and typed transport/capability error mapping"
    - path: "engine/src/workflow/nodes/generate.rs"
      provides: "D-11/D-12 single-retry loop with exact request replay and D-13 honest failure"
    - path: "engine/src/workflow/node.rs"
      provides: "Typed retryability decision carried into NodeFailed event construction"
    - path: "engine/src/generation/tests.rs"
      provides: "Focused preflight transport and successful-only cache regression tests"
    - path: "engine/src/tests/workflow_phase5.rs"
      provides: "Production-quality generation retry tracer and exhausted-retry regression"
  key_links:
    - "config/config.verify.toml generation_timeout_secs=30 -> one OpenRouter provider attempt -> GenerateAnswer node-level budget owned by 05-09"
    - "capability preflight outcome -> GenerationErrorKind -> GenerateAnswer retry predicate -> typed NodeError retryability -> NodeFailed.retryable"
    - "Capability-preflight request -> dedicated deadline and successful-only cache; GenerationRequest snapshot -> attempt one and attempt two request bytes -> exact retry identity"
    - "successful capability response -> model-scoped cache; transient preflight failure -> no cache entry -> next call retries preflight"
---

<objective>
Separate the OpenRouter capability-preflight and generation-retry contract from the typed event and snapshot plan.

Purpose: The existing provider preflight runs inside every generation attempt, shares the provider timeout, and maps transient transport failures to a non-retryable capability error. The generation node also needs an explicit owner for its single-retry policy so 05-10 can focus on exhaustive NodeKind dispatch, reliable event delivery, and snapshots.
Output: A real adapter-to-node retry tracer, dedicated preflight timeout/cache behavior, typed retryability, and focused coverage for transient success and exhausted failure.
</objective>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-preconditions.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-reversibility.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-AI-SPEC.md
@.planning/phases/05-state-machine-workflow-events/05-VERIFICATION.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEW.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEWS.md
@.planning/phases/05-state-machine-workflow-events/05-PATTERNS.md
@.planning/phases/05-state-machine-workflow-events/05-09-PLAN.md
@AGENTS.md
@rust-guidelines.md
@engine/src/generation/openrouter.rs
@engine/src/generation/mod.rs
@engine/src/generation/tests.rs
@engine/src/workflow/node.rs
@engine/src/workflow/nodes/generate.rs

## Timing handoff to the continuation plan

05-13 owns the adapter's dedicated capability-preflight operation, successful-only cache, typed retry classification, and cancellation-token plumbing. It leaves the operation available but does not invoke it from `execute_one_call` or delete the capability check. 05-20 owns the `Node::prepare`/runner bootstrap seam that invokes that operation exactly once before the `GenerateAnswer` node timer starts; the generation attempt body is then chat-only. There is one preparation implementation/cache owner and one invocation owner: no intermediate plan may add a second preflight call or cache. 05-13's adapter implementation is not complete until 05-20's worst-case regression proves the 5000ms preflight plus two 30000ms attempts fits the derived configured overall workflow budget while the 65000ms node timer covers the two generation attempts only.

## Artifacts this phase produces

- A dedicated OpenRouter capability-preflight timeout and successful-only model capability cache.
- A typed GenerateAnswer retry contract that preserves the 30-second per-attempt provider budget separately from the 05-09-owned node-level budget.
- Regression coverage for transient preflight classification, cache behavior, byte-identical retry, and honest exhausted failure.

## Ownership boundary

05-09 owns `config/config.verify.toml`, including `openrouter.generation_timeout_secs = 30` and the live `generation_node_timeout_ms = 7000` proof overlay. This plan consumes that contract; it does not edit the overlay or redefine the seven `[engine.workflow]` settings. 05-10 consumes this plan's typed retryability result while owning NodeKind dispatch, event delivery, and full snapshots.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace one real generation retry through preflight and GenerateAnswer</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; 05-CONTEXT.md; 05-RESEARCH.md; 05-AI-SPEC.md; 05-09-PLAN.md; engine/src/generation/openrouter.rs; engine/src/generation/mod.rs; engine/src/workflow/node.rs; engine/src/workflow/nodes/generate.rs; engine/src/tests/workflow_phase5_production.rs</read_first>
  <files>engine/src/generation/openrouter.rs, engine/src/workflow/node.rs, engine/src/workflow/nodes/generate.rs, engine/src/prompt.rs, engine/src/tests/workflow_phase5_production.rs</files>
<action>Move capability preflight out of the per-attempt request body and expose it as a dedicated adapter preparation operation with a short timeout, using a 5-second production default with a bounded test override so it is independent of both `generation_timeout_secs` and `generation_node_timeout_ms`. Do not invoke the preparation operation from `execute_one_call`; do not remove the capability check. 05-20's `Node::prepare` path is the sole invocation owner and calls this operation before the node timer, while `execute_one_call` performs only one chat attempt. Implement the instance cache with a `tokio::sync::Mutex<HashMap<CapabilityKey, Arc<tokio::sync::OnceCell<ModelCapabilities>>>>`: lock only to obtain or create the cell for the composite `(models_endpoint, model)` key, then use `get_or_try_init` so concurrent callers share one in-flight `/models` request. Cache only successful capability responses; timeout, transport failure, or cancellation leaves the cell uninitialized so a later caller retries, and waiting callers retain their own cancellation behavior. A different endpoint or model uses a different key. Map timeout, connect, connection-reset, EOF, and equivalent transient transport failures to `GenerationErrorKind::ProviderError`, while an unsupported-parameter response remains `GenerationErrorKind::SupportedParameters`. Preserve config/config.verify.toml's 30-second provider-attempt budget from 05-09 and never use the 65-second node budget as the adapter attempt timeout. Make GenerateAnswer retry exactly once with the unchanged serialized GenerationRequest snapshot for the locked transient classes (Timeout and transient ProviderError, including the provider's 5xx/rate-limit responses), check cancellation before the second attempt, and map the exhausted result to NodeErrorKind::LlmGenerationFailed without an answer or alternate provider. Thread the one WorkflowRunner/node CancellationToken through GenerationRequest into pack_evidence_and_graph_prompt and the reqwest provider future; the OpenRouter adapter must not create a fresh CancellationToken for prompt packing or replace the propagated token, and a stalled in-flight provider request must observe cancellation before any retry. Add the production-quality tracer workflow_phase5_generation_retry_tracer in the binary-owned production test module with a local request-capturing provider harness: inspect the capability preflight independently by asserting its request method/endpoint, dedicated deadline, and successful-only cache behavior; then serialize only GenerationRequest attempt 1 and attempt 2 and assert those two generation payloads are byte-identical. Prove the first transient failure is followed by exactly one immediate retry and the successful output reaches the node context. Add `workflow_phase5_openrouter_cancellation_propagates` with a stalled provider, cancel the same workflow token during the request, and assert the provider future ends with cancellation, no second chat attempt occurs, and no fabricated answer/event is produced. Expose the retryability decision through typed NodeError state for 05-10's NodeFailed builder; do not accept a caller-supplied hardcoded boolean. Preserve D-11, D-12, D-13, D-14, D-15, D-17, and the unchanged structured-output validation contract from D-01. Because 05-18 owns `engine/src/tests.rs` registration, this wave verifies the production tracer body and both Rust targets compile; its binary list/exact filters are explicitly non-authoritative pre-handoff semantic checks, while 05-20-02 owns the authoritative post-handoff binary list/exact evidence after the ordered target gates. Pattern reference: no existing `05-PATTERNS.md` section covers capability preflight/cache; follow `05-RESEARCH.md` `Pattern 1: Rust Node/WorkflowRunner` and `Don't Hand-Roll`'s generation retry/error-classification constraint so provider attempts stay distinct from node orchestration. Addresses the review concerns about preflight lifecycle ambiguity, endpoint/cache collisions, concurrent single-flight, and cancellation propagation.</action>
  <acceptance_criteria>
    - A local provider harness observes the capability preflight under its own request/deadline/cache assertions and exactly two generation attempts for the transient-success tracer; only the two serialized GenerationRequest values are identical, with no third attempt.
    - The preflight timeout is a separate bounded value, and the provider attempt remains 30 seconds while the generation node budget remains owned by 05-09.
    - The successful-only cache is instance-scoped and keyed by `(models_endpoint, model)`; the first successful preparation is reused by repeated calls on that generator, while a failed preparation and a different endpoint/model issue a fresh request.
    - Concurrent preparation callers for one key share one in-flight `/models` request; a bounded request counter observes exactly one preflight, all callers receive the same successful capabilities, and cancellation or timeout does not publish a success.
    - The retry path emits no retrying event, does not select another model/provider, and commits the validated second-attempt output to WorkflowContext.
    - The typed retryability decision is available to NodeFailed construction without a hardcoded event flag.
    - Canceling the same propagated workflow token terminates the stalled OpenRouter request before a second chat attempt and produces no fabricated answer or terminal success.
  </acceptance_criteria>
  <verify>
    <automated>$production = Get-Content 'engine/src/tests/workflow_phase5_production.rs' -Raw; foreach ($name in @('workflow_phase5_generation_retry_tracer','workflow_phase5_openrouter_cancellation_propagates')) { if (-not $production.Contains($name)) { throw "production generation behavior missing: $name" } }; $lib = cargo check --lib --manifest-path engine/Cargo.toml --locked; $libExit = $LASTEXITCODE; if ($libExit -ne 0) { exit $libExit }; $bin = cargo check --bin engine --manifest-path engine/Cargo.toml --locked; $binExit = $LASTEXITCODE; if ($binExit -ne 0) { exit $binExit }; cargo test --bin engine --manifest-path engine/Cargo.toml --locked --no-run; $testCompileExit = $LASTEXITCODE; if ($testCompileExit -ne 0) { exit $testCompileExit }; foreach ($name in @('workflow_phase5_generation_retry_tracer','workflow_phase5_openrouter_cancellation_propagates')) { cargo test --bin engine --manifest-path engine/Cargo.toml --locked -- --exact $name --nocapture; if ($LASTEXITCODE -ne 0) { throw "non-authoritative pre-handoff generation behavior failed: $name" } }</automated>
  </verify>
  <done>A real OpenRouter adapter and GenerateAnswer path prove preflight separation, typed transient retry, exact request replay, and successful second-attempt completion.</done>
  <reversibility rating="costly">Provider preflight and retry behavior controls external request volume and client-visible failure classification; retain the named adapter and node boundaries with focused tests.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Verify preflight classification, cache invalidation, and exhausted retry</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; 05-CONTEXT.md; 05-RESEARCH.md; 05-AI-SPEC.md; engine/src/generation/openrouter.rs; engine/src/generation/tests.rs; engine/src/generation/mod.rs; engine/src/workflow/node.rs; engine/src/workflow/nodes/generate.rs; engine/src/tests/workflow_phase5.rs; 05-09-PLAN.md; 05-VALIDATION.md</read_first>
  <files>engine/src/generation/openrouter.rs, engine/src/generation/tests.rs, engine/src/workflow/node.rs, engine/src/workflow/nodes/generate.rs, engine/src/prompt.rs, engine/src/tests/workflow_phase5.rs</files>
 <action>Add openrouter_preflight_transport_is_retryable using a short test override to prove a preflight timeout or connection reset maps to ProviderError/retryable and is not cached. Add openrouter_capabilities_cache_success_only with request counters proving a successful model-capability response is reused on the same generator instance and composite endpoint/model key, while a transient failure or a different endpoint/model causes the next call to issue a fresh preflight. Update every existing models-first `TcpListener` fixture in `engine/src/generation/tests.rs` that previously accepted `/models` before `/chat` (including `openrouter_effective_usage_limits` at its four-connection sequence and the other existing preflight-then-chat cases) to call the new preparation owner explicitly and to expect the instance-scoped cache sequence; bound each mock accept/read with a test timeout so a missing request fails red instead of parking a `server_handle.join()`. Add workflow_phase5_generation_retry_exhausted to prove a transient first and second generation failure yields one NodeFailed-ready LlmGenerationFailed result, no AnswerChunk/FinalAnswer payload, no fabricated answer, and exactly two provider attempts. Assert that invalid request, unsupported parameters, cancellation, and non-transient provider responses do not receive the automatic second attempt; preserve the locked no-backup-provider boundary. Add a source guard that rejects `CancellationToken::new()` in the OpenRouter prompt-packing path and a cancellation test that aborts during prompt packing before the provider request; pair it with the production stalled-provider propagation test from Task 1 so both pre-request and in-flight cancellation are covered. Keep generation_timeout_secs = 30 as the per-attempt contract and leave the 05-09 live 7000ms node-timeout check as the wall-clock proof; these focused cases use local harnesses or bounded test overrides for semantic evidence only. Extend the existing Rust generation/workflow tests and the production retry tracer without creating an alternate orchestration protocol, and make the typed retryability result consumable by 05-10's event builder. Because 05-18 owns `engine/src/tests.rs` registration, this task verifies the production exhausted-retry body and binary compilation here; its binary `--bin engine` exact filter is explicitly non-authoritative pre-handoff semantic evidence, while 05-20-02 reruns the exact filter as authoritative post-handoff evidence after the bootstrap/timing changes and ordered target gates. Pattern reference: mirror the `engine/src/workflow/nodes/generate.rs` section and `Shared Patterns — Error taxonomy / typed error structs (Rust)` in `05-PATTERNS.md` for typed failure classification and bounded retry evidence. Addresses the review concerns about existing preflight mock sequencing, hanging listener joins, and cancellation token propagation.</action>
  <acceptance_criteria>
    - Transient capability transport failure is retryable and never poisons the successful-only cache.
    - A successful capability response is fetched once for repeated calls with the same configured model/endpoint, while a failed preflight is attempted again on the next call.
    - The named `openrouter_effective_usage_limits` mock and every other existing models-first generation fixture have an executable bounded accept sequence for the new prepare-before-chat lifecycle; a cache regression fails promptly rather than hanging a listener join.
    - Exhausted generation emits a typed failure after exactly two attempts, with no answer-shaped event or fallback provider.
    - Focused local tests are explicitly semantic evidence and do not claim to replace the 05-09 config.verify wall-clock timeout proof.
    - The OpenRouter prompt/provider path contains no fresh cancellation-token construction and the in-flight cancellation regression observes the propagated token terminating the request.
  </acceptance_criteria>
  <verify>
    <automated>$test = 'openrouter_preflight_transport_is_retryable'; $listed = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $pattern = '(^|::)' + [regex]::Escape($test) + ': test$'; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -match $pattern }); if ($registered.Count -ne 1) { throw "exact Cargo registration missing or duplicated for $test" }; $output = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $test --nocapture; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
    <automated>$test = 'openrouter_capabilities_cache_success_only'; $listed = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $pattern = '(^|::)' + [regex]::Escape($test) + ': test$'; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -match $pattern }); if ($registered.Count -ne 1) { throw "exact Cargo registration missing or duplicated for $test" }; $output = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $test --nocapture; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
    <automated>$production = Get-Content 'engine/src/tests/workflow_phase5_production.rs' -Raw; if (-not $production.Contains('workflow_phase5_generation_retry_exhausted')) { throw 'production exhausted-retry test body missing' }; $generationTests = Get-Content 'engine/src/generation/tests.rs' -Raw; foreach ($name in @('openrouter_effective_usage_limits','openrouter_capabilities_cache_single_flight')) { if (-not $generationTests.Contains($name)) { throw "preflight sequencing test is not covered: $name" } }; $bin = cargo check --bin engine --manifest-path engine/Cargo.toml --locked; $binExit = $LASTEXITCODE; if ($binExit -ne 0) { exit $binExit }; cargo test --bin engine --manifest-path engine/Cargo.toml --locked --no-run; $testCompileExit = $LASTEXITCODE; if ($testCompileExit -ne 0) { exit $testCompileExit }; cargo test --bin engine --manifest-path engine/Cargo.toml --locked -- --exact workflow_phase5_generation_retry_exhausted --nocapture; if ($LASTEXITCODE -ne 0) { throw 'non-authoritative pre-handoff exhausted-retry behavior failed' }</automated>
    <automated>$test = 'openrouter_capabilities_cache_single_flight'; $listed = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $pattern = '(^|::)' + [regex]::Escape($test) + ': test$'; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -match $pattern }); if ($registered.Count -ne 1) { throw "exact Cargo registration missing or duplicated for $test" }; $output = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $test --nocapture; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
  </verify>
  <done>Focused Rust evidence proves successful-only capability caching, retryable preflight transport failures, exact two-attempt generation behavior, typed retryability, and honest exhausted failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust adapter to OpenRouter capability endpoint | Provider responses and transport failures determine whether generation may proceed or retry. |
| Rust GenerateAnswer node to provider attempt loop | A transient failure can cause an additional paid request and must remain bounded. |
| Generation error classification to workflow event | Internal provider details become a safe typed category and retryability signal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-13-01 | Denial of service | Capability preflight | high | mitigate | Give preflight a dedicated short timeout and cache only successful capability responses so a slow or failed endpoint cannot consume the generation attempt budget or poison future calls. |
| T-05-13-02 | Denial of service | GenerateAnswer retry loop | high | mitigate | Enforce one immediate retry, preserve the exact request, check cancellation before retry, and prohibit a third attempt or alternate provider. |
| T-05-13-03 | Tampering | Provider error classification | high | mitigate | Map transport, capability, and generation outcomes through explicit typed error kinds and test retryability from the resulting state rather than a caller literal. |
| T-05-13-04 | Information disclosure | Provider failure messages | medium | mitigate | Emit safe typed NodeError messages without credentials, raw request bodies, or provider secrets. |
| T-05-13-05 | Repudiation | Generation attempt history | medium | mitigate | Capture request identity and exact attempt counts in focused local harness tests while retaining D-15's absence of a retrying event. |
</threat_model>

<verification>
- The production tracer/body guards, binary compile checks, and non-authoritative pre-handoff exact `--bin engine` filters for retry success, in-flight cancellation, and exhausted failure pass in this plan; 05-20 Task 2 reruns the filters after the bootstrap/timing changes and supplies the authoritative post-handoff binary evidence.
- The library-focused registration guards pass for `openrouter_preflight_transport_is_retryable`, `openrouter_capabilities_cache_success_only`, and `openrouter_capabilities_cache_single_flight`.
- Provider preflight runs under its own timeout, successful capabilities cache, concurrent callers single-flight per composite key, transient/canceled failures do not cache, and the 30-second provider attempt remains distinct from the 05-09-owned node deadline.
- GenerateAnswer retries once with identical request data and reports an honest typed failure after the second transient failure.
</verification>

<success_criteria>
ORCH-02 and ORCH-03 have an executable provider/retry contract: retryability is typed, bounded, cancellation-aware, and separated from event delivery and the live configuration timeout proof.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-13-SUMMARY.md` when done.
</output>
