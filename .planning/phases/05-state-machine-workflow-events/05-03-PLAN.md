---
phase: 05-state-machine-workflow-events
plan: 03
type: execute
wave: 4
depends_on:
  - 05-02
files_modified:
  - engine/src/prompt.rs
  - engine/src/workflow/nodes/assemble_prompt.rs
  - engine/src/workflow/nodes/generate.rs
  - engine/src/workflow/nodes/mod.rs
  - engine/src/workflow/events.rs
  - engine/src/workflow/runner.rs
  - engine/src/main.rs
  - engine/src/tests.rs
autonomous: true
requirements: [ORCH-01, ORCH-02, ORCH-03]

must_haves:
  truths:
    - "AssemblePrompt preserves the existing evidence/graph packing semantics while processing exactly one PackCandidate per cooperative unit, checking cancellation before that unit and yielding after it, so its configured timeout can preempt the actual node rather than a wrapper around non-yielding CPU work (review HIGH)."
    - "GenerateAnswer sends the unchanged structured-output request under D-17's exact 30-second LLMGeneration node deadline, retries exactly once for a retryable result that returns before that deadline, and checks cancellation at a named boundary before attempt two (D-01, D-11, D-12, D-15, D-17)."
    - "Two failed generation attempts emit no AnswerChunk or FinalAnswer and end with NodeFailed{LlmGenerationFailed} plus WorkflowCompleted(failed); no alternate provider/model or fabricated answer is introduced (D-13, D-14)."
    - "Citation-resolution failure after a successful model response is a non-retryable LlmGenerationFailed workflow error; schema/grounding failure before resolution follows the defined generation retry classification (review MEDIUM)."
    - "Every successful or failed node boundary produces a centralized full accumulated checkpoint snapshot through the separate nonblocking CheckpointHandoff; the Full payload retains vector_results and bm25_results plus additive final_candidates, and the explicit Full/Fallback payload shape is always valid JSON no larger than 262,144 UTF-8 bytes while Full/Closed admission remains retained or observable as pending (D-23, D-27, D-28, review HIGH)."
    - "Checkpoint truncation deterministically replaces an assembled_prompt field that alone exceeds the 262,144-byte budget before considering candidate fields, checks serialized UTF-8 bytes after every truncation stage, and when all stages still exceed the bound emits a fixed bounded fallback marker rather than an invalid or partial Full payload."
    - "D-03 zero evidence produces WorkflowCompleted success with the existing NO_EVIDENCE notice and retrieval snapshot, without entering prompt/generation or emitting AnswerChunk/FinalAnswer."
    - "The trace/session contract is preserved: checkpoint/event trace_id is correlation_id, WorkflowCompleted.session_id is session_id, and no D-30 workflow metadata or D-31 per-node tracing spans are added."
  artifacts:
    - path: "engine/src/prompt.rs"
      provides: "Cancellation-aware cooperative prompt packing that retains existing token, evidence, graph, and citation semantics"
    - path: "engine/src/workflow/nodes/assemble_prompt.rs"
      provides: "AssemblePromptNode and injectable prompt-assembly boundary"
    - path: "engine/src/workflow/nodes/generate.rs"
      provides: "GenerateAnswerNode, retry classification, RetryGate, and final answer event mapping"
    - path: "engine/src/workflow/nodes/mod.rs"
      provides: "Parent node registry declaring assemble_prompt and generate and exporting their concrete node types to the production runner"
    - path: "engine/src/workflow/events.rs"
      provides: "Central retrieval response builder, CheckpointSnapshotV1, CheckpointFallbackV1, CheckpointPayloadV1, and size-bounded serializer"
    - path: "engine/src/main.rs"
      provides: "Production QueryRAG runner construction using the complete fixed node vector"
    - path: "engine/src/tests.rs"
      provides: "Coordinated service-constructor/stream migration and exact real-runner event-order reachability test"
    - path: "engine/src/prompt.rs::PROMPT_YIELD_GRANULARITY"
      provides: "The concrete cooperative unit size of exactly one evidence block or graph fact between cancellation check and tokio::task::yield_now"
    - path: "engine/src/workflow/events.rs::CHECKPOINT_MAX_BYTES"
      provides: "The exact 262,144-byte serialized checkpoint bound and deterministic truncation/fallback markers"
    - path: "engine/src/workflow/events.rs::CHECKPOINT_FALLBACK_MARKER/ASSEMBLED_PROMPT_TRUNCATION_MARKER"
      provides: "Stable marker values for serialization fallback and the single-field assembled_prompt truncation stage"
  key_links:
    - from: "engine/src/workflow/nodes/mod.rs"
      to: "engine/src/main.rs::QueryRAG production runner"
      via: "declared modules and re-exported concrete AssemblePromptNode/GenerateAnswerNode types are installed in the fixed production vector"
    - from: "AssemblePromptNode"
      to: "prompt::pack_evidence_and_graph_prompt_cooperative"
      via: "yield/check cancellation points inside the real packing loop, not only a timeout around a synchronous future"
    - from: "GenerateAnswerNode"
      to: "generation::Generator::generate"
      via: "cloned byte-identical GenerationRequest and one RetryGate boundary before the second call"
    - from: "WorkflowRunner"
      to: "build_checkpoint_snapshot"
      via: "central builder invoked for both NodeCompleted and NodeFailed, then admitted through the independent nonblocking CheckpointHandoff with owned overflow/drain state"
  prohibitions:
    - requirement_id: ORCH-01
      category: semantics
      statement: "MUST NOT change the existing structured-output, evidence-grounding, citation, or prompt-packing semantics while extracting nodes."
      verification: test
    - requirement_id: ORCH-02
      category: cardinality
      statement: "MUST NOT emit token-level AnswerChunk events, a retrying event, or a second AnswerChunk for one validated answer."
      verification: test
    - requirement_id: ORCH-03
      category: honesty
      statement: "MUST NOT retry permanent generation configuration errors, retry citation resolution after model success, or fabricate a response after retry exhaustion."
      verification: test
---

<objective>
Complete the Rust workflow path through prompt assembly and generation, add cancellation-correct retry behavior, and centralize the durable snapshot contract before the Go persistence wave.

**As a** Lancet engineer, **I want to** see prompt and generation failures as typed, bounded workflow events, **so that** a debugging trace never masks a broken answer path.

Purpose: close the phase's core reliability invariants before cross-runtime checkpoint persistence is connected.
Output: cooperative prompt assembly, generation retry/failure nodes, one complete AnswerChunk/FinalAnswer path, and an infallible bounded checkpoint serializer.
</objective>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-state-machine-workflow-events/05-01-SUMMARY.md
@.planning/phases/05-state-machine-workflow-events/05-02-SUMMARY.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-PATTERNS.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEWS.md
@.planning/phases/05-state-machine-workflow-events/05-AI-SPEC.md
@AGENTS.md
@rust-guidelines.md
</context>

## Artifacts this phase produces

- `engine/src/prompt.rs`: `pack_evidence_and_graph_prompt_cooperative` and cancellation-aware bounded packing.
- `engine/src/workflow/nodes/assemble_prompt.rs`: `AssemblePromptNode` and prompt-assembly seam.
- `engine/src/workflow/nodes/generate.rs`: `GenerateAnswerNode`, `RetryGate`, and typed retry/failure mapping.
- `engine/src/workflow/nodes/mod.rs`: parent registry declaring `assemble_prompt` and `generate`, exporting their concrete node types, and keeping the complete production node set reachable.
- `engine/src/workflow/events.rs`: `GeneratedAnswer`, shared retrieval response construction, `CheckpointSnapshotV1`, `CheckpointFallbackV1`, `CheckpointPayloadV1`, and `build_checkpoint_snapshot`.
- `engine/src/workflow/runner.rs`: reliable client-event ordering plus nonblocking CheckpointHandoff admission and owned overflow/drain state on successful and failed node boundaries.
- `engine/src/main.rs`: production `QueryRAG` runner construction wired to the complete fixed node vector after the existing graph-before-retrieval expansion.
- `engine/src/tests.rs`: coordinated constructor/stream migration and a real production-runner event-order regression covering prompt assembly and generation reachability.

<tasks>

<task type="tracer">
  <name>Task 1: Trace one grounded answer through cooperative prompt assembly and generation</name>
  <action_extension>Use `engine/src/main.rs:1549-1559` as the existing prompt-path analog and `05-PATTERNS.md`/`05-RESEARCH.md` as the approved cooperative assembly guidance. Use the `Generator`/`GenerationRequest` contract in `engine/src/generation/mod.rs` and the provider timeout behavior in `engine/src/generation/openrouter.rs`; thread D-16 native client-close cancellation through both nodes and keep D-20's event set coarse at node and answer boundaries. Addresses the checker blocker by extending the real production registry and runner construction, not only isolated node tests.</action_extension>
  <read_first>engine/src/prompt.rs, engine/src/workflow/node.rs, engine/src/workflow/runner.rs, engine/src/workflow/events.rs, engine/src/workflow/nodes/mod.rs, engine/src/workflow/nodes/retrieve.rs, engine/src/main.rs, engine/src/tests.rs, engine/src/generation/mod.rs, engine/src/generation/openrouter.rs, 05-01-PLAN.md, 05-02-PLAN.md, 05-06-PLAN.md, 05-CONTEXT.md, 05-REVIEWS.md, rust-guidelines.md</read_first>
  <files>engine/src/prompt.rs, engine/src/workflow/nodes/assemble_prompt.rs, engine/src/workflow/nodes/generate.rs, engine/src/workflow/nodes/mod.rs, engine/src/workflow/events.rs, engine/src/workflow/runner.rs, engine/src/main.rs, engine/src/tests.rs</files>
  <behavior>
    - Non-empty retrieval -> AssemblePrompt NodeStarted/NodeCompleted -> one validated AnswerChunk -> FinalAnswer -> WorkflowCompleted(success).
    - Prompt cancellation/timeout -> PromptAssemblyFailed or Cancelled with no generation call.
    - Successful node boundary -> full checkpoint record on the independent side channel; failed node boundary -> accumulated checkpoint record even when the client-event channel remains open.
    - The generation node uses the locked 30-second node deadline; a fast retryable first result reaches exactly one retry within that deadline without extending it.
    - The registry/runner reachability test -> `test_production_runner_reaches_assemble_prompt_and_generate_answer` is registered and passes against the real service path.
  </behavior>
  <action>First update `engine/src/workflow/nodes/mod.rs` to declare `pub mod assemble_prompt;` and `pub mod generate;`, export the concrete `AssemblePromptNode` and `GenerateAnswerNode` types alongside the existing reformulation, graph, and retrieval exports, and keep their injectable port types reachable from the parent module. Update the production `QueryRAG` construction in `engine/src/main.rs` and the fixed node execution vector owned by `engine/src/workflow/runner.rs` to be exactly `[ReformulateQueryNode, ExtractGraphContextNode, RetrieveHybridNode, AssemblePromptNode, GenerateAnswerNode]`: append prompt assembly and generation after `RetrieveHybrid`, preserve D-06's graph-before-retrieval order, and retain D-20's coarse node/answer event boundaries. The production vector must use these exported concrete types, so the new modules are reachable from the real runner rather than only from isolated tests. Keep `engine/src/tests.rs` constructor and `drain_query_rag_stream` migrations coordinated with this registry/runner change; update every affected `LancetServiceImpl` fixture before compiling or registering focused tests, and preserve the existing streaming callers and assertions. Add `test_production_runner_reaches_assemble_prompt_and_generate_answer` through the real `LancetServiceImpl::query_rag`/production runner with deterministic injected dependencies, drain the returned stream, and project out transport-only checkpoints to assert the exact client-visible order `ReformulateQuery Started/Completed`, `ExtractGraphContext Started/Completed`, `RetrieveHybrid Started/Completed`, `AssemblePrompt Started/Completed`, `GenerateAnswer Started/Completed`, `AnswerChunk`, `FinalAnswer`, `WorkflowCompleted(success)`; also assert one checkpoint boundary for each completed node and that `trace_id` remains the request correlation ID while the terminal session identity remains separate (D-20, D-28, D-29). Implement `AssemblePromptNode` with an object-safe prompt assembler boundary whose production implementation calls a new async `pack_evidence_and_graph_prompt_cooperative` in `engine/src/prompt.rs`. Preserve `pack_evidence_and_graph_prompt`'s reserved first evidence block, graph-weight zero exclusion, token budget, delimiter escaping, candidate ordering, and citation inputs. Define `PROMPT_YIELD_GRANULARITY: usize = 1` as one `PackCandidate` (one evidence block or one graph fact): check the `CancellationToken` immediately before encoding/rendering/tokenizing that unit, process no second candidate, then call `tokio::task::yield_now().await` before the next unit. Apply this bounded check/yield discipline to the scoring and final packing loops, including the reserved first block, and rely on the existing candidate/graph settings limits for the finite input. Do not wrap the existing synchronous function in a bare `spawn_blocking` and call that preemptible: a dropped blocking handle would leave CPU work running after timeout. Map cooperative cancellation to `NodeErrorKind::Cancelled` and packing errors to `PromptAssemblyFailed`. Implement `GenerateAnswerNode` from the existing `generation::Generator` and `GenerationRequest` contract. Build the request from the packed evidence and graph facts, preserve session_id/correlation_id, model, temperature zero, top-p one, and structured JSON-schema generation unchanged (D-01). Use the locked CONTEXT.md D-17 interpretation as authoritative over the alternative retry-budget interpretation in 05-AI-SPEC.md: run the generator under the exact 30-second LLMGeneration node deadline, while retaining `openrouter.generation_timeout_secs` only as the provider's existing per-attempt cap. A retryable error that returns before the node deadline passes through the named retry gate and gets exactly one cloned request using the remaining node budget; the retry never extends the 30-second deadline, and cancellation or the deadline wins before a second call when no budget remains. On success validate grounding, resolve citations, and place a `GeneratedAnswer` plus the complete terminal `QueryRAGResponse` data in `WorkflowContext`; emit exactly one full-answer `AnswerChunk`, then a distinct `FinalAnswer`, then WorkflowCompleted(success) (D-02). On an empty final candidate list, retain the D-03 short circuit and its NO_EVIDENCE notice/retrieval snapshot without entering either node. In `events.rs`, centralize retrieval snapshot construction from `index_generation`, embedding model identity, filters, weights, limits, and result hash so both D-03 and the final response use the same complete input set; no node constructs a partial snapshot. Add `CheckpointSnapshotV1` with the full D-28 accumulated fields (`schema_version`, original_query, reformulated_query, vector_results, bm25_results, final_candidates, graph_context, assembled_prompt, answer, truncated), using serializable DTO copies and never serializing `WorkflowContext` directly. Keep vector_results and bm25_results populated from the retrieval node's source-result DTOs; final_candidates is additive fused output. Add explicit `CheckpointFallbackV1` and `CheckpointPayloadV1::Full/Fallback` representations so a fallback containing a marker and candidate count is not misrepresented as the full struct. Make `build_checkpoint_snapshot(ctx, node_name)` infallible, pass its result to `CheckpointHandoff::emit_checkpoint` for both NodeCompleted and NodeFailed boundaries, and keep client-event sends independent; Full/Closed admission transfers the exact record into the owned FIFO overflow and the builder must never turn a checkpoint problem into a query failure. Do not add D-30 workflow metadata or D-31 named per-node spans; preserve the captured existing `query_rag` span only.</action>
  <verify>
    <automated>cargo check --manifest-path engine/Cargo.toml --locked
$checkExit = $LASTEXITCODE
if ($checkExit -ne 0) { exit $checkExit }
$rustList = @(cargo test --manifest-path engine/Cargo.toml --locked --bin engine -- --list 2>&amp;1)
$rustListExit = $LASTEXITCODE
if ($rustListExit -ne 0) { exit $rustListExit }
$registryText = Get-Content -LiteralPath 'engine/src/workflow/nodes/mod.rs' -Raw
foreach ($required in @('mod assemble_prompt', 'mod generate', 'AssemblePromptNode', 'GenerateAnswerNode')) {
  if ($registryText -notmatch [regex]::Escape($required)) { throw "Workflow node registry is missing $required" }
}
$runnerText = (Get-Content -LiteralPath 'engine/src/workflow/runner.rs' -Raw) + (Get-Content -LiteralPath 'engine/src/main.rs' -Raw)
foreach ($required in @('ReformulateQueryNode', 'ExtractGraphContextNode', 'RetrieveHybridNode', 'AssemblePromptNode', 'GenerateAnswerNode')) {
  if ($runnerText -notmatch [regex]::Escape($required)) { throw "Production runner construction is missing $required" }
}
$expectedRustTests = @('test_production_runner_reaches_assemble_prompt_and_generate_answer', 'test_prompt_cooperative_cancellation_and_yield_granularity', 'test_workflow_answer_happy_path_and_checkpoint_boundaries')
foreach ($leaf in $expectedRustTests) {
  $escapedLeaf = [regex]::Escape($leaf)
  $registered = @($rustList | ForEach-Object { $line = $_.ToString(); if ($line -match ("^\s*(?<full>[A-Za-z0-9_:]*" + $escapedLeaf + "):\s+test\s*$")) { $Matches['full'] } })
  if ($registered.Count -ne 1) { throw "Expected one exact Rust registration ending in $leaf, found $($registered.Count)" }
  $fullName = $registered[0]
  $run = @(cargo test --manifest-path engine/Cargo.toml --locked --bin engine -- --exact $fullName --nocapture 2>&amp;1)
  $runExit = $LASTEXITCODE
  if ($runExit -ne 0) { exit $runExit }
  if (@($run | Where-Object { $_.ToString() -match ("^test\s+" + [regex]::Escape($fullName) + "\s+\.\.\.\s+ok\s*$") }).Count -lt 1) { throw "Exact Rust test did not report PASS: $fullName" }
}
cargo fmt --manifest-path engine/Cargo.toml -- --check
$fmtExit = $LASTEXITCODE
if ($fmtExit -ne 0) { exit $fmtExit }</automated>
  </verify>
  <acceptance_criteria>
    - `engine/src/workflow/nodes/mod.rs` declares `assemble_prompt` and `generate` and exports `AssemblePromptNode` and `GenerateAnswerNode`; the production runner and handler use those exports rather than test-only constructors.
    - The fixed production vector is exactly `ReformulateQueryNode`, `ExtractGraphContextNode`, `RetrieveHybridNode`, `AssemblePromptNode`, `GenerateAnswerNode`, preserving D-06 graph-before-retrieval and placing prompt assembly/generation after retrieval.
    - `test_production_runner_reaches_assemble_prompt_and_generate_answer` invokes the real `LancetServiceImpl::query_rag` production path, asserts the exact client-visible node/answer/terminal order, and proves both new modules are reachable; transport-only checkpoints are accounted for separately rather than treated as client progress nodes.
    - All `engine/src/tests.rs` constructor and streaming-helper migrations required by the registry change compile before the focused test list, exact tests, formatter check, or full Rust gates run.
    - The real AssemblePromptNode invokes the cooperative packing implementation and contains cancellation/yield points in the candidate loop; no bare blocking task is treated as preemptible.
    - The cooperative packing test proves `PROMPT_YIELD_GRANULARITY` is one evidence block or graph fact: cancellation is checked before each unit and `yield_now` occurs after each unit, with no batch processing between yield points.
    - The happy path emits exactly one AnswerChunk carrying the complete validated answer, one FinalAnswer, and one successful WorkflowCompleted in that order.
    - The shared retrieval snapshot builder is used for both zero-evidence and final-answer response construction and includes index generation, embedding model, filters, result hash, limits, and weights.
    - NodeCompleted and NodeFailed boundaries enqueue standalone checkpoint records built from the accumulated context through the independent side channel, and checkpoint construction is infallible to the caller.
    - The checkpoint DTO is separate from WorkflowContext and the fallback is a distinct serializable shape, not a partially populated full snapshot.
  </acceptance_criteria>
  <done>The fixed path reaches a grounded answer with cooperative prompt execution and a centralized checkpointable context without changing Phase 3 generation semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Close retry, citation, cancellation-boundary, and snapshot-size invariants</name>
  <action_extension>The retry action is the concrete D-11 generation-only boundary: classify and retry only inside `GenerateAnswer`, exactly once, with no retry policy in prompt/retrieval/graph/gateway. Use `engine/src/generation/openrouter.rs` plus `05-AI-SPEC.md`/`05-RESEARCH.md` for the distinct per-attempt/provider and node deadlines; no existing multi-step runner analog supplies this loop. Race the retry gate with D-16 cancellation and assert D-20's coarse event contract so attempt two produces no sub-step or retrying event.</action_extension>
  <read_first>engine/src/workflow/nodes/generate.rs, engine/src/workflow/events.rs, engine/src/workflow/runner.rs, engine/src/generation/mod.rs, engine/src/prompt.rs, 05-01-PLAN.md, 05-06-PLAN.md, 05-REVIEWS.md, 05-AI-SPEC.md, rust-guidelines.md</read_first>
  <files>engine/src/workflow/nodes/generate.rs, engine/src/workflow/events.rs, engine/src/workflow/runner.rs, engine/src/prompt.rs</files>
  <behavior>
    - ProviderError/Timeout/SchemaValidation -> attempt two with an equal GenerationRequest; permanent configuration/cancellation errors -> no retry.
    - The retry gate pauses immediately after attempt-one error is received and immediately before attempt two; cancellation there prevents attempt two.
    - Citation resolution failure after model success -> LlmGenerationFailed with no provider retry.
    - Oversized, normal, a single assembled_prompt field larger than the budget, a fixture that survives every truncation stage, and non-finite-score snapshots -> bounded valid JSON with the correct full/fallback/truncated state.
    - Named exact tests -> `test_generate_retry_gate_retries_once_with_equal_request`, `test_generate_double_failure_emits_no_answer`, `test_generate_cancellation_before_retry`, `test_citation_resolution_failure_no_retry`, `test_checkpoint_single_assembled_prompt_truncation`, `test_checkpoint_all_truncation_stages_exceed_uses_bounded_fallback`, and `test_checkpoint_nonfinite_score_uses_bounded_fallback` are each registered and pass.
  </behavior>
  <action>Define the retryable `GenerationErrorKind` set explicitly as ProviderError, Timeout, and SchemaValidation; InvalidRequest, SupportedParameters, SessionCorrelation, and Cancelled fail without a retry. Add a `RetryGate` trait with a no-op production implementation and a deterministic test implementation, and invoke `before_retry().await` after the first error has been classified but immediately before the second `Generator::generate` call. Race that gate against the cancellation token so the cancellation test proves the loop is interrupted between attempts, not during attempt one (review MEDIUM). Retry exactly once with a cloned, byte-identical `GenerationRequest`, emit no retrying event (D-12, D-15), and map a second failure to sanitized `LlmGenerationFailed` plus WorkflowCompleted(failed) with no answer event (D-13); the retry remains inside D-17's 30-second node deadline and never creates a third call or a longer envelope. Treat `ModelOutput::validate_grounding_with_limits`/structured-output failures as retryable schema failures; treat an unresolved citation after model output has passed validation as a non-retryable `LlmGenerationFailed` finalization error because the provider call already succeeded. In `build_checkpoint_snapshot`, define `CHECKPOINT_MAX_BYTES: usize = 262_144`, `CHECKPOINT_FALLBACK_MARKER = "checkpoint_fallback_v1"`, and `ASSEMBLED_PROMPT_TRUNCATION_MARKER = "[truncated:assembled_prompt]"`, then implement this deterministic size ladder: first serialize all full DTO fields; next replace `assembled_prompt` with the exact marker, then replace every evidence text and graph-fact text with `[truncated:evidence_text]`/`[truncated:graph_fact_text]`, then replace every fused candidate raw content and all `encoded_blocks` with `[truncated:candidate_content]`/`[truncated:encoded_blocks]`. After every stage, serialize and measure UTF-8 bytes and refuse to return an oversized representation. After the final candidate/encoded-block stage, run a terminal serialized-byte guard; if the representation still exceeds `CHECKPOINT_MAX_BYTES`, emit a fixed ASCII-safe `CheckpointFallbackV1` containing only schema_version, the exact fallback marker, bounded candidate_count, and truncated=true, serialize it, parse it back as JSON, and assert its byte length is at most the bound. Include a fixture where `assembled_prompt` alone is `CHECKPOINT_MAX_BYTES + 1` bytes while every other field is small, and require that the first truncation stage yields valid Full-shaped JSON at or below the bound with `truncated=true` and the exact assembled-prompt marker. Add a second fixture where deliberately oversized surviving metadata/text makes every Full truncation stage exceed the bound and require the fixed fallback rather than a failed serialization or an oversized payload. On any serialization error at any stage, including a non-finite score, return the same distinct fixed-shape fallback; its bounded primitive fields and final byte guard must make the fallback valid JSON at or below `CHECKPOINT_MAX_BYTES` without unwrap/propagation that can fail the user query. Parse the selected JSON before returning it. Keep provider/model fallback, answer_basis degraded/model-only, citation repair, and new workflow metadata outside this phase per D-14, D-30, and the deferred ledger.</action>
  <verify>
    <automated>$rustList = @(cargo test --manifest-path engine/Cargo.toml --locked --bin engine -- --list 2>&amp;1)
$rustListExit = $LASTEXITCODE
if ($rustListExit -ne 0) { exit $rustListExit }
$expectedRustTests = @('test_generate_retry_gate_retries_once_with_equal_request', 'test_generate_double_failure_emits_no_answer', 'test_generate_cancellation_before_retry', 'test_citation_resolution_failure_no_retry', 'test_checkpoint_single_assembled_prompt_truncation', 'test_checkpoint_all_truncation_stages_exceed_uses_bounded_fallback', 'test_checkpoint_nonfinite_score_uses_bounded_fallback')
foreach ($leaf in $expectedRustTests) {
  $escapedLeaf = [regex]::Escape($leaf)
  $registered = @($rustList | ForEach-Object { $line = $_.ToString(); if ($line -match ("^\s*(?<full>[A-Za-z0-9_:]*" + $escapedLeaf + "):\s+test\s*$")) { $Matches['full'] } })
  if ($registered.Count -ne 1) { throw "Expected one exact Rust registration ending in $leaf, found $($registered.Count)" }
  $fullName = $registered[0]
  $run = @(cargo test --manifest-path engine/Cargo.toml --locked --bin engine -- --exact $fullName --nocapture 2>&amp;1)
  $runExit = $LASTEXITCODE
  if ($runExit -ne 0) { exit $runExit }
  if (@($run | Where-Object { $_.ToString() -match ("^test\s+" + [regex]::Escape($fullName) + "\s+\.\.\.\s+ok\s*$") }).Count -lt 1) { throw "Exact Rust test did not report PASS: $fullName" }
}
cargo clippy --manifest-path engine/Cargo.toml --all-targets -- -D warnings
$clippyExit = $LASTEXITCODE
if ($clippyExit -ne 0) { exit $clippyExit }
cargo test --manifest-path engine/Cargo.toml --locked
$fullRustExit = $LASTEXITCODE
if ($fullRustExit -ne 0) { exit $fullRustExit }
Push-Location gateway
try { go test ./...; $goExit = $LASTEXITCODE; if ($goExit -ne 0) { exit $goExit } } finally { Pop-Location }</automated>
  </verify>
  <acceptance_criteria>
    - A retry test observes exactly two generator calls, equal `GenerationRequest` values, no retry event, and a successful answer only when attempt two succeeds.
    - A double-failure test observes exactly two calls, NodeFailed{LlmGenerationFailed}, WorkflowCompleted(failed), and zero AnswerChunk/FinalAnswer events.
    - A cancellation test releases the retry gate, cancels before attempt two, and observes one generator call plus a Cancelled outcome.
    - A citation-resolution failure test is categorized as LlmGenerationFailed and proves no second provider call is made for that local finalization error.
    - Normal, oversized, single-assembled_prompt-over-budget, all-stages-over-budget, and NaN/Infinity-score checkpoint fixtures prove all eight accumulated D-28 content fields are present in a normal Full payload (including vector_results and bm25_results, with final_candidates additive), the single-field fixture takes the exact assembled-prompt truncation stage, every stage performs a serialized-byte check, and the all-stages/non-finite cases produce the distinct valid `checkpoint_fallback_v1` Fallback payload at or below 262144 bytes.
  </acceptance_criteria>
  <done>Retry and cancellation arithmetic is executable, citation failures are honest, and every checkpoint payload is bounded and structurally valid without weakening the RAG answer contract.</done>
</task>

</tasks>

<threat_model>
ASVS Level: 1. Blocking threshold: high. High-severity threats block plan acceptance/execution.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Corpus/evidence → prompt builder | Untrusted document fields enter the model prompt and checkpoint DTO. |
| Rust → external generation provider | Provider response and transport failures enter the retry/error taxonomy. |
| Rust workflow → event/checkpoint payload | Generated answers and raw context become client-visible or durable data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-09 | Information Disclosure | Prompt/checkpoint serialization | high | mitigate | Preserve existing evidence delimiter escaping, use a dedicated DTO, bound all text fields, and never serialize the raw WorkflowContext. |
| T-05-10 | Tampering | Generation retry request | high | mitigate | Clone and compare the full GenerationRequest, retain provider/model/temperature/top-p/prompt identity, and cap the loop at two calls. |
| T-05-11 | Denial of Service | Prompt packing and node timeout | high | mitigate | Use bounded candidate inputs, cooperative yield/cancellation checks, configured node budgets, and no non-preemptive blocking wrapper. |
| T-05-12 | Repudiation | Failure/citation classification | medium | mitigate | Emit typed D-22 categories, sanitized messages, correlation-linked logs, and deterministic tests for citation resolution and retry exhaustion. |
| T-05-13 | Information Disclosure | Raw checkpoint corpus content | medium | accept | D-23 and 04.1 D-33 explicitly accept raw local-demo checkpoint content without a new redaction system; payload size and localhost scope remain enforced. |
</threat_model>

<verification>
- Focused prompt, generation, retry, cancellation-gate, and checkpoint tests pass.
- Full engine test and clippy suites pass after the node extraction.
- The generated answer contract still validates citations and structured output exactly as the prior pipeline did.
</verification>

<success_criteria>
The Rust state machine completes a grounded happy path, reports honest generation/citation failures, permits the locked retry exactly once, distinguishes cancellation at the retry boundary, and emits bounded full snapshots on both success and failure boundaries.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-03-SUMMARY.md` when done.
</output>
