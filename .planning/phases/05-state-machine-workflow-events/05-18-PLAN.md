---
phase: 05-state-machine-workflow-events
plan: 18
type: execute
wave: 12
depends_on: [05-08, 05-09, 05-14, 05-17]
files_modified:
  - engine/src/lib.rs
  - engine/src/tests.rs
  - engine/src/tests/workflow_phase5.rs
  - engine/src/tests/workflow_phase5_production.rs
  - engine/src/workflow/ports.rs
  - engine/src/main.rs
autonomous: true
gap_closure: true
requirements: [ORCH-01, ORCH-02, ORCH-03]

must_haves:
  truths:
    - Phase 5 workflow tests compile and execute in the library unit-test target where cfg(test) workflow fakes are available.
    - The binary target retains exactly one production-owned workflow test module and no longer registers the generic module that depends on library-only test fakes.
    - Every current BM25 test-service construction and access in engine/src/tests.rs uses the shared O(1) Arc snapshot ownership shape introduced at this boundary, so the binary test target remains compilable before later adapter behavior changes.
    - All binary-test call sites that reference workflow::ports::Fake* are relocated into the library-owned workflow test module before 05-15 gates those definitions behind cfg(test); no binary test depends on a library-only fake.
    - The target seam has an explicit compile owner for every target: this plan lands the shared BM25 type/field plus fixture and fake-call-site migration and runs the first binary test-target no-run gate, while later plans add behavior without reopening a compile-break window.
  artifacts:
    - engine/src/lib.rs registers workflow_phase5 under cfg(test) using the existing test source file.
    - engine/src/tests.rs contains no second workflow_phase5 registration and all 18 BM25 fixture constructions use the migrated Arc snapshot shape.
    - engine/src/tests/workflow_phase5.rs contains a direct fake-port compile probe and target-correct imports.
    - engine/src/tests/workflow_phase5_production.rs remains registered only in the binary target for real service-field and production-builder tests.
    - engine/src/workflow/ports.rs exposes the production-visible BM25 ownership alias while fake implementations remain independently gateable.
  key_links:
    - lib.rs cfg(test) registration -> workflow_phase5.rs -> ports.rs cfg(test) fakes
    - cargo test --lib registration -> phase test execution
    - Bm25IndexStore alias + RagEngineService field -> all binary fixture constructions and access expressions
    - relocated fake-dependent binary tests -> library cfg(test) registration -> 05-15 fake-port cfg(test) gates
    - cargo test --bin engine --manifest-path engine/Cargo.toml --locked --no-run -> binary test target remains compile-clean before downstream behavior changes
---

<objective>
Split the Phase 5 workflow tests by target: move generic fake-port tests into the library unit-test target while retaining production-builder tests in a binary-owned module.

Purpose: Close the target-aware test seam identified by the checker and keep both generic ORCH-01 through ORCH-03 coverage and real production reachability executable after fake ports become cfg(test)-only.
Output: One library-owned generic registration, one binary-owned production registration, target-correct imports, and exact library/binary execution evidence.
</objective>

## Artifacts this phase produces

- Library-owned workflow_phase5 registration and target-correct imports.
- A library-target fake-port compile probe and binary compile evidence.
- A binary-owned workflow_phase5_production registration for production-builder tests.

## Review resolution

HIGH-2's suggested direct dependency on 05-16 is rejected because 05-16 depends transitively on 05-18 through 05-15; adding it here would create a dependency cycle. The collision is resolved structurally by the generic/binary module split, and 05-16 plus 05-20 explicitly rerun the production-target tests after their adapter changes.

Review-mode ownership correction: 05-08 registers `workflow_phase5_production` in `engine/src/tests.rs` and owns the production-handler assertion strengthening plus GraphQueryPort call-site migration needed to compile that wave. This plan remains the owner of the final generic-library registration, removal of the duplicate generic binary registration, relocation of every workflow-fake-dependent binary test block, the shared `Bm25IndexStore` alias and service-field shape, all 18 BM25 fixture constructions plus their non-construction accesses, and the exact cross-target seam guard; preserve the single production registration created by 05-08 rather than deferring or duplicating it. The alias and service-field shape land in this wave before the fixture migration, so the first `cargo test --bin engine --manifest-path engine/Cargo.toml --locked --no-run` gate proves the complete binary test target before 05-15 removes workflow fakes from non-test production compilation. There is no planned Waves 11–13 compile-break exception and no use of `cargo check --bin engine` as a substitute for the binary test-target gate.

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-PATTERNS.md
@.planning/phases/05-state-machine-workflow-events/05-14-PLAN.md
@.planning/phases/05-state-machine-workflow-events/05-15-PLAN.md
@.planning/phases/05-state-machine-workflow-events/05-08-PLAN.md
@engine/src/lib.rs
@engine/src/tests.rs
@engine/src/tests/workflow_phase5.rs
@engine/src/workflow/ports.rs
</context>
<tasks>

<task type="tracer">
  <name>Task 1: Route fake-dependent tests and the BM25 ownership shape before cfg(test) gating</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; 05-CONTEXT.md; 05-08-PLAN.md; 05-14-PLAN.md; 05-15-PLAN.md; engine/src/lib.rs; engine/src/tests.rs; engine/src/tests/workflow_phase5.rs; engine/src/workflow/ports.rs</read_first>
  <files>engine/src/lib.rs, engine/src/tests.rs, engine/src/tests/workflow_phase5.rs, engine/src/tests/workflow_phase5_production.rs, engine/src/workflow/ports.rs, engine/src/main.rs</files>
  <action>Register generic workflow_phase5 in engine/src/lib.rs with the existing workflow_phase5.rs path under cfg(test), remove only the duplicate generic pub mod workflow_phase5 registration from the binary-side engine/src/tests.rs, and retain exactly the single workflow_phase5_production registration created by 05-08. Before 05-15 gates workflow fakes, move the enclosing binary-test blocks containing all 25 current crate::workflow::ports::Fake* or engine::workflow::ports::Fake* references from engine/src/tests.rs lines 7103-7807 into the library-owned workflow_phase5.rs module, normalize their crate-self imports, and preserve their test names, fixtures, stall behavior, and assertions; engine/src/tests.rs must retain only production-service tests and no workflow-fake call sites. Introduce a production-visible `Bm25IndexStore` alias for `Arc<RwLock<Arc<Bm25Index>>>` in workflow/ports.rs, change the RagEngineService BM25 field to that alias before migrating fixtures, and update all 37 current bm25_index references in engine/src/tests.rs, including all 18 constructions and non-construction accesses, to the same ownership shape without changing fixture behavior. Do not duplicate or take back 05-08's production-handler test bodies, production registration, or GraphQueryPort call-site migration. This is the target-aware seam consumed by 05-15: the six workflow fakes remain available when the library unit-test target compiles the generic module, while the binary target has no workflow-fake dependency and already compiles against the new BM25 field shape. Pattern reference: no `05-PATTERNS.md` analog covers Cargo library-vs-binary registration or the shared BM25 ownership alias; follow `05-RESEARCH.md` Validation Architecture and Current Validation Authority for target-specific registration and compile evidence. Addresses review concerns HIGH fake-port call-site loss and HIGH BM25 compile-order break by assigning both to the introducing boundary.</action>
  <acceptance_criteria>
    - lib.rs has exactly one cfg(test) registration for workflow_phase5 using engine/src/tests/workflow_phase5.rs.
    - engine/src/tests.rs has no generic workflow_phase5 module declaration and has exactly one workflow_phase5_production declaration.
    - engine/src/tests.rs contains zero workflow::ports::Fake* call sites after relocation, and the library-owned module contains the relocated fake-dependent test bodies.
    - The production-visible Bm25IndexStore alias and RagEngineService field exist before all 18 BM25 constructions and their 37 references are migrated.
    - workflow_phase5.rs compiles in library scope with crate-correct imports and retains the existing happy-path registration.
  </acceptance_criteria>
  <verify>
    <automated>$name = 'workflow_phase5_happy_path'; $list = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $matches = @($list | Select-String -Pattern ('(^|::)' + [regex]::Escape($name) + ': test$')); if ($matches.Count -ne 1) { throw 'Library workflow phase test registration count mismatch' }; $run = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $name --nocapture; $runExit = $LASTEXITCODE; if ($runExit -ne 0) { exit $runExit }</automated>
    <automated>$tests = Get-Content engine/src/tests.rs -Raw; if ($tests -match 'workflow::ports::Fake(?:Query|Graph|Dense|Bm25|Reranker)') { throw 'workflow fake call site remains in binary tests.rs after relocation' }; $ports = Get-Content engine/src/workflow/ports.rs -Raw; if ($ports -notmatch 'Bm25IndexStore') { throw 'shared BM25 ownership alias is missing' }; $main = Get-Content engine/src/main.rs -Raw; if ($main -notmatch 'Bm25IndexStore') { throw 'RagEngineService does not use the shared BM25 ownership alias' }; $bm25References = @(rg -n 'bm25_index' engine/src/tests.rs); if ($bm25References.Count -ne 37) { throw "expected 37 migrated bm25_index references, found $($bm25References.Count)" }; $constructions = @(rg -n 'RwLock::new\(Arc::new\(bm25_index(?:1|2)?\)\)' engine/src/tests.rs); if ($constructions.Count -ne 18) { throw "expected 18 migrated BM25 constructions, found $($constructions.Count)" }; $compile = cargo test --bin engine --manifest-path engine/Cargo.toml --locked --no-run; $compileExit = $LASTEXITCODE; if ($compileExit -ne 0) { exit $compileExit }</automated>
  </verify>
  <done>The phase workflow suite is owned by the library test target, and one exact happy-path test proves the moved registration is executable.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the cfg(test) fake-port seam after the introducing compile gate</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; 05-15-PLAN.md; engine/src/tests/workflow_phase5.rs; engine/src/tests/workflow_phase5_production.rs; engine/src/workflow/ports.rs; engine/src/generation/mod.rs; engine/Cargo.toml</read_first>
  <files>engine/src/tests/workflow_phase5.rs, engine/src/lib.rs, engine/src/tests.rs, engine/src/tests/workflow_phase5_production.rs, engine/src/workflow/ports.rs, engine/src/main.rs</files>
  <action>Add workflow_phase5_library_target_fake_ports_compile as a focused unit test that references FakeQueryReformulator, FakeQueryEmbeddingPort, FakeGraphQueryPort, FakeDenseRetrievalPort, FakeBm25RetrievalPort, and FakeReranker through the library target. Keep the compile probe side-effect free and keep all fake definitions behind the cfg(test) seam implemented by 05-15. Verify that the relocated binary-test blocks no longer import workflow fakes, that the production-visible Bm25IndexStore alias and RagEngineService field match the 18 migrated constructions and all 37 references, and that the single production module remains binary-owned. The introducing `cargo test --bin engine --manifest-path engine/Cargo.toml --locked --no-run` gate from Task 1 is the compile proof for the complete binary test target; this task must not weaken it to `cargo check --bin engine` or defer it to 05-16. Pattern reference: no `05-PATTERNS.md` analog covers test-only fake-port compilation or BM25 fixture ownership; follow `05-RESEARCH.md` Validation Architecture and Current Validation Authority for target-specific execution and named ownership checks. Addresses the review requirement that the binary test target remain green before 05-15 removes workflow fakes from non-test compilation.</action>
  <acceptance_criteria>
    - The compile probe names all six workflow fake ports and is registered in the library target.
    - The library target resolves every fake import while the binary test target has already passed its complete `--no-run` compile gate with no workflow-fake call sites and the shared BM25 ownership shape.
    - The 18 current BM25 test-service constructions and all 37 references in engine/src/tests.rs use the shared Arc snapshot shape and compile against the production service field.
    - The seam is machine-checkable by exact generic and production registration, source registration counts, library execution, and the introducing binary test-target compile; downstream plans add behavior without reopening ownership migration.
  </acceptance_criteria>
  <verify>
    <automated>$lib = Get-Content engine/src/lib.rs -Raw; $binaryTests = Get-Content engine/src/tests.rs -Raw; if (@([regex]::Matches($lib, '(?m)^\s*(?:pub\s+)?mod\s+workflow_phase5\s*;')).Count -ne 1) { throw 'Library workflow_phase5 registration count mismatch' }; if (@([regex]::Matches($binaryTests, '(?m)^\s*(?:pub\s+)?mod\s+workflow_phase5_production\s*;')).Count -ne 1) { throw 'Binary production registration count mismatch' }; if ($binaryTests -match '(?m)^\s*(?:pub\s+)?mod\s+workflow_phase5\s*;') { throw 'Binary-side generic workflow_phase5 registration remains' }; if ($binaryTests -match 'workflow::ports::Fake(?:Query|Graph|Dense|Bm25|Reranker)') { throw 'workflow fake call site remains in binary tests.rs' }; $ports = Get-Content engine/src/workflow/ports.rs -Raw; if ($ports -notmatch 'Bm25IndexStore') { throw 'shared BM25 ownership alias missing' }; $main = Get-Content engine/src/main.rs -Raw; if ($main -notmatch 'Bm25IndexStore') { throw 'RagEngineService does not use shared BM25 ownership alias' }; $references = @(rg -n 'bm25_index' engine/src/tests.rs); if ($references.Count -ne 37) { throw "expected 37 migrated bm25_index references, found $($references.Count)" }; $new = @(rg -n 'RwLock::new\(Arc::new\(bm25_index(?:1|2)?\)\)' engine/src/tests.rs); if ($new.Count -ne 18) { throw "expected 18 migrated BM25 fixture constructions, found $($new.Count)" }; $name = 'workflow_phase5_library_target_fake_ports_compile'; $list = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $matches = @($list | Select-String -Pattern ('(^|::)' + [regex]::Escape($name) + ': test$')); if ($matches.Count -ne 1) { throw 'Fake-port compile probe registration count mismatch' }; $run = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $name --nocapture; $runExit = $LASTEXITCODE; if ($runExit -ne 0) { exit $runExit }</automated>
  </verify>
  <done>Library-only fake ports compile and the binary target remains production-clean, eliminating the cfg(test) target mismatch before 05-15 gates the definitions.</done>
</task>

</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cargo library test target to cfg(test) workflow fakes | Test-only code crosses the compilation boundary while production binary code must not depend on it. |
| Phase test module to workflow ports | Tests import fake implementations that can affect event and state assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-18-01 | Tampering | Target registration and cfg(test) seam | high | mitigate | Assert one library registration, no binary registration, exact library test execution, and binary compilation. |
| T-05-18-02 | Denial of service | Phase workflow test harness | medium | mitigate | Keep the compile probe side-effect free and require exact named test execution so a skipped module cannot satisfy the gate. |
| T-05-18-03 | Repudiation | Fake workflow ports | low | mitigate | Reference every named fake in a registered library test and preserve the existing concrete fixture behavior. |
| T-05-18-SC | Tampering | npm/pip/cargo installs | high | accept | This plan adds no package-manager dependency or install task; the standard slopcheck register is not applicable to this target-registration change. |
</threat_model>

<verification>
- The library list contains exactly one registration for workflow_phase5_happy_path and exactly one registration for workflow_phase5_library_target_fake_ports_compile.
- Both exact library filters pass.
- The binary test target passes `cargo test --bin engine --manifest-path engine/Cargo.toml --locked --no-run` at this plan's introducing boundary, before 05-15 gates workflow fakes behind cfg(test); later plans may rerun this same gate after their owned production changes but do not own the initial compile proof.
- Source inspection confirms the module declaration exists only in lib.rs and the six fake-port references remain test-scoped.
</verification>

<success_criteria>
The Phase 5 suite executes in library scope with all cfg(test) workflow fakes, the binary target compiles without those fakes, and the exact registration checks prevent a silently skipped test module.
</success_criteria>

<output>
Create .planning/phases/05-state-machine-workflow-events/05-18-SUMMARY.md when done.
</output>
