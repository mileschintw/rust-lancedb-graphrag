---
phase: 05-state-machine-workflow-events
plan: 16
type: execute
wave: 13
depends_on:
  - 05-14
  - 05-15
  - 05-17
files_modified:
  - engine/src/main.rs
  - engine/src/workflow/mod.rs
  - engine/src/workflow/nodes/graph_context.rs
  - engine/src/workflow/nodes/retrieve.rs
  - engine/src/tests/workflow_phase5.rs
autonomous: true
gap_closure: true
requirements:
  - ORCH-01
  - ORCH-03
  - ORCH-04
must_haves:
  truths:
    - "Graph outcomes use exact machine-readable GRAPH_TIMEOUT and GRAPH_DEGRADED notice codes, and notice extension/merge preserves earlier notices through later success or terminal failure."
    - "RetrievalSnapshot makes multi-variant behavior auditable with explicit variant_count and ordered variant identities; the chosen representation is tested."
    - "BM25 retrieval uses an owned immutable index snapshot before any await, so a stalled retrieval cannot hold the live RwLock and wedge ingestion."
    - "Graph augmentation remains mandatory but non-required per D-09, while retrieval and production adapter behavior remain on the real five-node path established by 05-08 and 05-14."
  artifacts:
    - path: "engine/src/workflow/mod.rs"
      provides: "Notice extension/merge operation that retains prior ordered entries"
    - path: "engine/src/workflow/nodes/graph_context.rs"
      provides: "Exact graph timeout/degradation codes and preserved graph outcome semantics"
    - path: "engine/src/workflow/nodes/retrieve.rs"
      provides: "Auditable multi-variant RetrievalSnapshot and lock-free-across-await BM25 consumption"
    - path: "engine/src/main.rs"
      provides: "Production BM25 adapter that snapshots the index before asynchronous retrieval"
    - path: "engine/src/tests/workflow_phase5.rs"
      provides: "Graph notice, variant snapshot, and ingestion-not-wedged regressions"
  key_links:
    - "ExtractGraphContext -> WorkflowContext::merge_notices -> terminal/checkpoint preservation in 05-10"
    - "QueryReformulator variants -> RetrievalSnapshot.variant_count/variant_identities"
    - "BM25 RwLock read -> owned immutable snapshot -> async retrieve -> independent ingestion write"
---

<objective>
Make graph degradation, notice accumulation, retrieval provenance, and BM25 concurrency behavior auditable and safe.

Purpose: The checker called out stringly graph notices, overwritten notices, unauditable multi-variant BM25 weighting, and an RwLock guard held across await. This plan closes those findings before 05-10 serializes the final snapshot and 05-11 relays it.
Output: Exact graph notice codes, merge semantics, explicit variant provenance, immutable BM25 snapshots, and focused regression tests.
</objective>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-preconditions.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-reversibility.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-PATTERNS.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEW.md
@.planning/phases/05-state-machine-workflow-events/05-VERIFICATION.md
@.planning/phases/05-state-machine-workflow-events/05-14-PLAN.md
@.planning/phases/05-state-machine-workflow-events/05-15-PLAN.md
@.planning/phases/05-state-machine-workflow-events/05-17-PLAN.md
@AGENTS.md
@engine/src/main.rs
@engine/src/workflow/mod.rs
@engine/src/workflow/nodes/graph_context.rs
@engine/src/workflow/nodes/retrieve.rs
@engine/src/tests/workflow_phase5.rs

<scope_rationale>
The binary-owned 05-08 production regression module remains the exclusive owner of `workflow_phase5_production_five_node`, `workflow_phase5_production_dependencies_are_real`, `workflow_phase5_production_context_population`, and `workflow_phase5_production_reachability`. This plan owns only the retrieval/graph adapter changes and its two new production-target tests; the four 05-08 tests are re-run as post-change regression checks, not reimplemented or moved. Execution ordering is already supplied transitively by `05-16 -> 05-15 -> 05-18 -> 05-08`; this plan deliberately adds no direct 05-18 dependency, so the rejected cycle is not introduced. The generic fake-port tests remain library-owned by 05-18, while all production checks stay on `--bin engine`.
</scope_rationale>

## Artifacts this phase produces

- Exact GRAPH_TIMEOUT and GRAPH_DEGRADED notice codes with ordered merge/extend behavior.
- RetrievalSnapshot variant_count and ordered variant identities for auditable cross-variant retrieval.
- An owned immutable BM25 index snapshot whose read guard is released before async retrieval.
- Regression coverage for notice survival, provenance, and ingestion progress during stalled retrieval.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace graph degradation notices through accumulated WorkflowContext</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; 05-CONTEXT.md; 05-REVIEW.md; 05-17-PLAN.md; engine/src/workflow/mod.rs; engine/src/workflow/nodes/graph_context.rs; engine/src/tests/workflow_phase5.rs</read_first>
  <files>engine/src/workflow/mod.rs, engine/src/workflow/nodes/graph_context.rs, engine/src/tests/workflow_phase5.rs</files>
  <action>Define the exact machine-readable graph notice codes `GRAPH_TIMEOUT` and `GRAPH_DEGRADED` at the workflow boundary. Add a `WorkflowContext` notice extension/merge operation that appends new notices in occurrence order, preserves earlier entries, and deduplicates only an identical code/message pair when the same outcome is reported twice; it must never replace the existing notice vector. Make ExtractGraphContext use GRAPH_TIMEOUT for its bounded inner timeout and GRAPH_DEGRADED for no-match or logical graph failure, while retaining an empty graph_context and successful continuation per D-09. Add `workflow_phase5_graph_notice_merge` to prove earlier retrieval/graph notices survive later graph degradation and a later terminal failure, and that both exact codes reach the accumulated context consumed by 05-10. Follow the `05-PATTERNS.md` graph outcome and context-state analogs; do not introduce deferred degraded-mode metadata from D-30.</action>
  <acceptance_criteria>
    - Graph timeout notices have code GRAPH_TIMEOUT and graph no-match/logical degradation notices have code GRAPH_DEGRADED.
    - Notice extension preserves earlier entries and order across later success, degradation, and terminal failure.
    - A graph timeout/degradation leaves graph_context empty and does not fabricate graph evidence or force a hard workflow failure.
  </acceptance_criteria>
  <verify>
    <automated>$test = 'workflow_phase5_graph_notice_merge'; $listed = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $pattern = '(^|::)' + [regex]::Escape($test) + ': test$'; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -match $pattern }); if ($registered.Count -ne 1) { throw "exact Cargo registration missing or duplicated for $test" }; $output = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $test --nocapture; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
  </verify>
  <done>Graph notice codes and merge semantics are observable on the accumulated context and ready for terminal/checkpoint preservation.</done>
  <must_haves>
    - D-06, D-09, D-22, D-28, and D-30 scope fences remain intact.
  </must_haves>
</task>

<task type="auto">
  <name>Task 2: Make variant provenance and BM25 snapshotting testable</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; 05-CONTEXT.md; 05-RESEARCH.md; 05-REVIEW.md; 05-08-PLAN.md; 05-17-PLAN.md; 05-18-PLAN.md; engine/src/main.rs; engine/src/workflow/nodes/retrieve.rs; engine/src/workflow/ports.rs; engine/src/tests/workflow_phase5_production.rs; engine/src/pb/lancet/v1/lancet.v1.rs</read_first>
  <files>engine/src/main.rs, engine/src/workflow/nodes/retrieve.rs, engine/src/tests/workflow_phase5_production.rs</files>
  <action>Consume the generated `RetrievalSnapshot.variant_count` and ordered `RetrievalSnapshot.variant_identities` fields owned by 05-17; do not create a local shadow DTO or assume fields absent from the checked-in prost/go bindings. Set the count equal to the accepted reformulation count and the identities to the corresponding reformulated query strings in order. Preserve D-07 cross-variant retrieval and make the snapshot state explain which variants contributed without changing the locked retrieval protocol or adding D-30 workflow metadata. Add `workflow_phase5_retrieval_snapshot_variants` to the binary-owned production suite and rerun the 05-08 production reachability test after these adapter changes. Introduce an owned immutable BM25 index snapshot at the production adapter boundary using an O(1) Arc handle snapshot (for example Arc<RwLock<Arc<Bm25Index>>> or an equivalent existing ownership pattern): acquire the RwLock only long enough to clone the Arc, drop the guard before invoking any async retrieval, and never deep-copy corpus-sized index data. Add `workflow_phase5_bm25_snapshot_releases_lock` in the binary-owned production suite with a stalled retrieval and a concurrent ingestion/write assertion proving the writer progresses before retrieval is released; add a source guard that fails if a BM25 RwLock guard is held across an await or if the snapshot path performs a corpus deep copy. Follow the 05-PATTERNS.md retrieval/fusion analogs and keep embedding variant zero per D-08.</action>
  <acceptance_criteria>
    - RetrievalSnapshot contains auditable variant_count and ordered variant_identities, and the focused test proves their values.
    - BM25 retrieval never holds an RwLock guard across await; the production path obtains an O(1) Arc handle snapshot and does not deep-copy the corpus.
    - A stalled retrieval does not wedge a concurrent BM25 ingestion/write operation.
  </acceptance_criteria>
  <verify>
    <automated>$test = 'workflow_phase5_retrieval_snapshot_variants'; $listed = cargo test --bin engine --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $pattern = '(^|::)' + [regex]::Escape($test) + ': test$'; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -match $pattern }); if ($registered.Count -ne 1) { throw "exact Cargo registration missing or duplicated for $test" }; $output = cargo test --bin engine --manifest-path engine/Cargo.toml --locked -- --exact $test --nocapture; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
    <automated>$test = 'workflow_phase5_bm25_snapshot_releases_lock'; $listed = cargo test --bin engine --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $pattern = '(^|::)' + [regex]::Escape($test) + ': test$'; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -match $pattern }); if ($registered.Count -ne 1) { throw "exact Cargo registration missing or duplicated for $test" }; $output = cargo test --bin engine --manifest-path engine/Cargo.toml --locked -- --exact $test --nocapture; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
    <automated>cargo test --bin engine --manifest-path engine/Cargo.toml --locked -- --exact workflow_phase5_production_five_node --nocapture; if ($LASTEXITCODE -ne 0) { throw '05-08 production five-node regression failed after 05-16 adapter changes' }</automated>
    <automated>cargo test --bin engine --manifest-path engine/Cargo.toml --locked -- --exact workflow_phase5_production_dependencies_are_real --nocapture; if ($LASTEXITCODE -ne 0) { throw '05-08 production dependency regression failed after 05-16 adapter changes' }</automated>
    <automated>cargo test --bin engine --manifest-path engine/Cargo.toml --locked -- --exact workflow_phase5_production_context_population --nocapture; if ($LASTEXITCODE -ne 0) { throw '05-08 production context regression failed after 05-16 adapter changes' }</automated>
    <automated>cargo test --bin engine --manifest-path engine/Cargo.toml --locked -- --exact workflow_phase5_production_reachability --nocapture; if ($LASTEXITCODE -ne 0) { throw '05-08 production reachability regression failed after 05-16 adapter changes' }</automated>
  </verify>
  <done>Variant provenance is explicit and BM25 snapshotting prevents stalled retrieval from blocking ingestion.</done>
  <must_haves>
    - D-07 and D-08 remain true for every accepted variant.
    - WR-09 and WR-14 have focused executable regressions rather than accepted prose debt.
  </must_haves>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Graph adapter outcome to workflow notice | Timeout and degradation must not be confused or silently erase prior diagnostics. |
| Reformulation variants to retrieval snapshot | Multiple variants affect result provenance and must remain auditable. |
| BM25 index lock to async retrieval | A read guard held across await can block ingestion and create resource denial. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-16-01 | Tampering | Graph notice codes | high | mitigate | Use exact GRAPH_TIMEOUT/GRAPH_DEGRADED codes and typed merge tests. |
| T-05-16-02 | Repudiation | Workflow notices | medium | mitigate | Append and preserve earlier notice entries through later outcomes and checkpoints. |
| T-05-16-03 | Tampering | RetrievalSnapshot provenance | medium | mitigate | Persist variant_count and ordered identities beside result provenance. |
| T-05-16-04 | Denial of service | BM25 RwLock | high | mitigate | Snapshot owned index data before await and test concurrent ingestion during a stalled retrieval. |
</threat_model>

<verification>
- The graph notice, variant snapshot, and BM25 lock registration guards pass with one exact filter per test.
- Exact graph codes, notice merge behavior, variant provenance, and lock-release behavior are all asserted by executable tests.
- The plan does not add workflow metadata, tracing spans, resume behavior, or other deferred ideas.
</verification>

<success_criteria>
ORCH-01, ORCH-03, and ORCH-04 have auditable graph degradation, preserved notice history, variant-aware retrieval snapshots, and ingestion-safe BM25 concurrency.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-16-SUMMARY.md` when done.
</output>
