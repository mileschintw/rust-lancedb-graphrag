---
phase: 05-state-machine-workflow-events
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - buf.gen.yaml
  - proto/lancet/v1/lancet.proto
  - engine/src/pb/lancet/v1/lancet.v1.rs
  - engine/src/pb/lancet/v1/lancet.v1.tonic.rs
  - engine/Cargo.toml
  - engine/Cargo.lock
  - engine/src/workflow/mod.rs
  - engine/src/workflow/node.rs
  - engine/src/workflow/runner.rs
  - engine/src/workflow/events.rs
  - engine/src/workflow/nodes/mod.rs
  - engine/src/workflow/nodes/reformulate.rs
  - engine/src/main.rs
  - engine/src/tests.rs
autonomous: true
requirements: [ORCH-01, ORCH-02, ORCH-03, ORCH-05]

must_haves:
  truths:
    - "A valid QueryRAG request performs synchronous ReceiveQuery validation and ID minting before any stream opens; invalid session or query input remains a trailer-bearing gRPC error and becomes HTTP 4xx without an SSE 200 response (D-04, D-05)."
    - "The Rust tracer emits ReformulateQuery NodeStarted/NodeCompleted and WorkflowCompleted through a server-streaming gRPC response, while checkpoint records use a separate bounded handoff with nonblocking admission, an owned FIFO overflow/drain contract for full or closed queues, and an observable pending outcome through WorkflowHandle (D-16, D-27)."
    - "CheckpointHandoff uses a primary capacity of exactly 1 and an overflow capacity of exactly 5, derived from the five fixed checkpoint boundaries in this phase; terminal and answer events are not checkpoint records, and a sixth record is an explicit overflow/pending outcome rather than silent loss."
    - "The event trace_id is the request correlation_id and remains distinct from WorkflowCompleted.session_id (D-29)."
    - "The reformulation tracer returns exactly the original query without a model call, while the embedding boundary is prepared to consume only variant zero (D-08, ORCH-05)."
    - "The protobuf source and regenerated Rust tonic service define a server-streaming QueryRAG contract with distinct NodeStarted, NodeCompleted, NodeFailed, AnswerChunk, FinalAnswer, WorkflowCompleted, and transport-only Checkpoint event shapes; 05-06 regenerates the coordinated Go client (D-01, D-02, D-18, D-20, D-26)."
    - "During the interval after this plan and before 05-06, the checked-in buf.gen.yaml is a Rust-only template, so an ordinary buf generate cannot emit streaming Go stubs while gateway/main.go is still unary; 05-06 restores the full four-plugin template in the same coordinated change that updates the gateway."
  artifacts:
    - path: "proto/lancet/v1/lancet.proto"
      provides: "Server-streaming QueryRAG RPC, WorkflowEvent oneof, typed node-failure enum, and standalone checkpoint transport event source"
    - path: "engine/src/workflow/node.rs"
      provides: "Object-safe Node trait, WorkflowContext, NodeError, NodeErrorKind, and sanitized client-message helper"
    - path: "engine/src/workflow/runner.rs"
      provides: "WorkflowRunner, bounded client-event channel, CheckpointHandoff with owned bounded overflow/drain state, timeout/cancellation supervisor, WorkflowHandle, and WorkflowOutcome"
    - path: "engine/src/workflow/events.rs"
      provides: "Rust WorkflowEvent enum and the single into_proto conversion boundary"
    - path: "engine/src/workflow/nodes/reformulate.rs"
      provides: "QueryReformulator port, NoOpQueryReformulator, and ReformulateQueryNode"
    - path: "engine/src/workflow/runner.rs"
      provides: "Reliable client-event sender, nonblocking reliable checkpoint handoff, timeout/cancellation supervisor, WorkflowHandle, and WorkflowOutcome pending-checkpoint diagnostics"
    - path: "engine/src/workflow/runner.rs::CHECKPOINT_PRIMARY_CAPACITY/CHECKPOINT_OVERFLOW_CAPACITY/MAX_WORKFLOW_CHECKPOINTS"
      provides: "The exact 1-slot primary queue, 5-record FIFO overflow, and fixed workflow checkpoint bound consumed by later plans"
    - path: "buf.gen.yaml"
      provides: "An executable Rust-only code-generation guard for the unary-Go/streaming-Rust transition; 05-06 restores the coordinated four-plugin template"
  key_links:
    - from: "engine/src/main.rs::query_rag"
      to: "engine/src/workflow/runner.rs::spawn_workflow"
      via: "ReceiveQuery validation creates WorkflowContext, then a bounded event receiver is converted once to the tonic stream"
    - from: "WorkflowRunner::CheckpointHandoff"
      to: "QueryRAG response stream"
      via: "Nonblocking try_send admission moves Full/Closed records into an owned bounded FIFO; a detached FIFO drain maps accepted records into standalone transport-only Checkpoint events without awaiting from node execution"
    - from: "WorkflowRunner"
      to: "WorkflowHandle"
      via: "a oneshot terminal outcome and a cancellation supervisor race receiver closure against runner completion; pending checkpoint records and transport-closed status remain observable"
  prohibitions:
    - requirement_id: ORCH-01
      category: boundary
      statement: "MUST NOT move ReceiveQuery validation into the asynchronous node runner or wrap the standalone QueryGraph RPC in workflow events."
      verification: test
    - requirement_id: ORCH-02
      category: transparency
      statement: "MUST NOT convert the Rust WorkflowEvent enum inside nodes or use a lossy send for client-visible protocol events."
      verification: test
    - requirement_id: ORCH-03
      category: failure-handling
      statement: "MUST NOT report a closed client stream as a fabricated terminal success or confuse cancellation with timeout."
      verification: test
    - requirement_id: ORCH-05
      category: scope
      statement: "MUST NOT add LLM-backed, HyDE, or multi-query reformulation logic; NoOpQueryReformulator remains the only production implementation in this phase."
      verification: test
    - requirement_id: ORCH-02
      category: reliability
      statement: "MUST NOT treat a full or closed checkpoint queue as a successful delivery; every record must be retained in the bounded owned overflow/drain contract or reported as an explicit pending/overflow outcome."
      verification: test
---

<objective>
Establish the Phase 5 Rust workflow tracer and streaming contract, including the independent checkpoint side channel and observable cancellation outcome that the gateway/configuration plan will consume.

**As a** Lancet engineer, **I want to** formalize RAG orchestration into a Rust state machine, **so that** I can debug and extend the pipeline with predictable failure handling.

Purpose: prove the Rust orchestration and wire contract before adding the remaining pipeline nodes or gateway persistence.
Output: server-streaming protobuf and generated clients, a production-quality one-node Rust runner, a nonblocking reliable checkpoint handoff with owned overflow/drain state, a named cancellation handle, and a Rust tonic stream ready for the gateway plan.
</objective>

<scope_justification>
05-01 intentionally keeps its 14-file surface consolidated: the protobuf contract, generated Rust outputs, runner handoff, node registry, tonic handler, existing Rust QueryRAG test migration, and the checked-in Rust-only Buf transition guard must compile together before 05-06 regenerates the coordinated Go client and before 05-02 extends the same runner. Splitting generated contract files from the runtime handoff would create an intermediate wave with no compile-safe Rust stream and would force the same generated artifacts through multiple owners. The surface remains below the 15-file hard cap.
</scope_justification>

<execution_sub_boundaries>
The 14-file surface is intentionally atomic, but execution is reviewable at these concrete checkpoints: (1) assert the unary-state Buf predicates and hash the existing Go generated client plus `gateway/main.go`; (2) update the protobuf/Cargo contract, regenerate only Rust outputs, and require `cargo check --locked` before proceeding; (3) implement the node/event/runner/handler path, migrate and compile every existing Rust QueryRAG test caller through the streaming helper before the first Rust test-list gate, and run the exact named Rust lifecycle tests; (4) rerun Rust-only generation, compare both unary-state hashes, run the gateway build and full Rust suite, and leave the checked-in template Rust-only. A failed checkpoint halts the task before the next sub-boundary; no full four-plugin generation is permitted until 05-06's coordinated transition.
</execution_sub_boundaries>

<scope_exception>
Exact file count: 14 files in `files_modified`.
Coupling rationale: these files are coupled by the atomic Rust/Go/protobuf transition and cannot be split without breaking the server-streaming cardinality or the generated-client transition: the protobuf source, generated Rust outputs, Rust runner/event handoff, tonic handler, migrated Rust stream tests, and Rust-only Buf guard must compile and be verified together before 05-06 regenerates the coordinated Go client and before 05-02 extends the runner.
Ordered execution sub-boundaries/checkpoints: (1) assert the unary-state Buf predicates and hash both checked-in Go generated files plus `gateway/main.go`; (2) switch the checked-in template to Rust-only, regenerate Rust outputs, and pass `cargo check --locked`; (3) implement the tracer, migrate every Rust QueryRAG caller through `drain_query_rag_stream`, and pass the exact lifecycle-test gate; (4) rerun Rust-only generation, compare both Go-file hashes, then run gateway build, formatting, and the full Rust suite while leaving the template Rust-only.
Accepted review basis: this scope exception is accepted on the basis of the Phase 5 cross-AI review and the current operator's explicit instruction to continue revisions until all checks pass.
</scope_exception>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-PATTERNS.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEWS.md
@.planning/phases/05-state-machine-workflow-events/05-AI-SPEC.md
@.planning/phases/05-state-machine-workflow-events/05-VALIDATION.md
@.planning/phases/05-state-machine-workflow-events/05-SOURCE-AUDIT.md
@.planning/phases/05-state-machine-workflow-events/COVERAGE.md
@AGENTS.md
@rust-guidelines.md
@go-guidelines.md
</context>

## Artifacts this phase produces

- `proto/lancet/v1/lancet.proto`: `WorkflowEvent`, `NodeErrorCategory`, event payload messages, `Checkpoint`, and streaming `QueryRAG`.
  - `engine/src/pb/lancet/v1/lancet.v1.rs` and `engine/src/pb/lancet/v1/lancet.v1.tonic.rs`: regenerated Rust prost/tonic messages and service trait.
- `engine/Cargo.toml` and `engine/Cargo.lock`: reviewed Tokio time/sync plus cancellation/stream dependencies and locked versions.
- `engine/src/workflow/{mod.rs,node.rs,runner.rs,events.rs}`: `Node`, `WorkflowContext`, `WorkflowRunner`, `WorkflowHandle`, `WorkflowOutcome`, `NodeErrorKind`, and Rust-to-protobuf event conversion.
- `engine/src/workflow/nodes/mod.rs` and `engine/src/workflow/nodes/reformulate.rs`: node registry, `QueryReformulator`, `NoOpQueryReformulator`, and `ReformulateQueryNode`.
- `engine/src/main.rs`: synchronous ReceiveQuery validation followed by the Rust workflow stream and detached checkpoint forwarder.
- `engine/src/tests.rs`: `drain_query_rag_stream` terminal-payload helper and complete migration of the existing unary QueryRAG test callers and direct answer/notice assertions to the Rust server stream.
- `buf.gen.yaml`: the checked-in Rust-only transition template that prevents accidental Go streaming-stub generation before 05-06.

<checkpoint_handoff_contract>
`CheckpointHandoff` is the single source of truth for 05-01, 05-04, 05-05, and 05-06. Set `MAX_WORKFLOW_CHECKPOINTS = 5`, `CHECKPOINT_PRIMARY_CAPACITY = 1`, and `CHECKPOINT_OVERFLOW_CAPACITY = 5` (`MAX_WORKFLOW_CHECKPOINTS`): five is the exact FIFO slot bound and the maximum legitimate checkpoint sequence because the fixed workflow has exactly five checkpoint boundaries: ReformulateQuery, ExtractGraphContext, RetrieveHybrid, AssemblePrompt, and GenerateAnswer. The primary slot is admission capacity, not an additional workflow boundary. `NodeStarted`, `NodeCompleted`, `AnswerChunk`, `FinalAnswer`, and `WorkflowCompleted` are client events, not checkpoint records. `emit_checkpoint` uses `try_send` only; Full or Closed moves the exact record into the runner-owned FIFO. The FIFO drains in insertion order, `Pending` reports the exact remaining records plus transport-closed state, and the runner rejects an attempted sixth checkpoint with an explicit `Overflow` status retained in the terminal outcome rather than treating it as successful delivery. No later plan may invent another capacity, queue state, or loss policy; it must consume these constants and statuses (D-27, D-28).
</checkpoint_handoff_contract>

<tasks>

<task type="tracer">
  <name>Task 1: Trace one reformulation event through the Rust streaming contract</name>
  <action_extension>D-18 is implemented at this contract boundary: the protobuf source and regenerated Rust tonic service expose server-streaming `QueryRAG` with the single `WorkflowEvent` envelope, while the coordinated Go client remains owned by 05-06.</action_extension>
  <read_first>proto/lancet/v1/lancet.proto, buf.gen.yaml, engine/Cargo.toml, engine/src/main.rs, engine/src/tests.rs, engine/src/generation/mod.rs, 05-CONTEXT.md, 05-REVIEWS.md, rust-guidelines.md</read_first>
  <files>buf.gen.yaml, proto/lancet/v1/lancet.proto, engine/src/pb/lancet/v1/lancet.v1.rs, engine/src/pb/lancet/v1/lancet.v1.tonic.rs, engine/Cargo.toml, engine/Cargo.lock, engine/src/workflow/mod.rs, engine/src/workflow/node.rs, engine/src/workflow/runner.rs, engine/src/workflow/events.rs, engine/src/workflow/nodes/mod.rs, engine/src/workflow/nodes/reformulate.rs, engine/src/main.rs, engine/src/tests.rs</files>
  <reversibility rating="costly">D-18 changes the coordinated Go/Rust QueryRAG cardinality; reverting it requires restoring the unary protobuf contract and every generated client, gateway handler, and integration test, but no persisted data migration.</reversibility>
  <behavior>
    - Valid request -> ReceiveQuery -> ReformulateQuery NodeStarted -> NodeCompleted -> WorkflowCompleted over one server stream.
    - Invalid request -> synchronous gRPC status with existing trailer identity and no WorkflowEvent.
    - Client receiver closes -> WorkflowHandle reports Cancelled or ReceiverClosed independently of the closed event channel.
    - First Rust gRPC event -> available to a downstream server-streaming client without changing the synchronous validation boundary.
    - Existing Rust QueryRAG tests -> every unary caller is migrated to drain the server stream before any Rust test registration, focused execution, or full-suite gate.
  </behavior>
  <timeout_contract>Keep the configured GraphExtraction outer node timeout exactly 15 seconds by default per D-17 and define the shared `GRAPH_TIMEOUT_DEGRADE_MARGIN` as `Duration::from_millis(100)` for the runner/node boundary. Derive `inner_deadline = max(Duration::from_millis(1), outer_duration.saturating_sub(GRAPH_TIMEOUT_DEGRADE_MARGIN))` for the graph adapter, so normal configured values resolve before the outer runner backstop and tiny test values cannot underflow; cancellation remains distinct.</timeout_contract>
  <action>First make the checked-in `buf.gen.yaml` an executable safe transition state: retain `version: v2`, set `clean: false` so existing unary Go outputs are preserved, and keep only the two Rust prost/tonic plugins with no `gateway/proto` outputs until 05-06 coordinates the Go client. Run `buf generate` from that checked-in template and assert that the existing unary Go generated client and `gateway/main.go` remain unchanged. In the same task, change `proto/lancet/v1/lancet.proto` so `LancetService.QueryRAG` returns `stream WorkflowEvent` while retaining `QueryRAGResponse` as the terminal payload shape. Define one `WorkflowEvent` envelope with `trace_id` and a `oneof` containing `NodeStarted`, `NodeCompleted`, `NodeFailed`, `AnswerChunk`, `FinalAnswer`, `WorkflowCompleted`, and a transport-only `Checkpoint` event. Keep checkpoint data out of NodeCompleted and NodeFailed payloads: `WorkflowRunner` records a full snapshot in a separate bounded `CheckpointRecord` handoff, and a detached forwarder maps records into standalone Checkpoint events for the tonic stream. Include the D-22 closed failure categories and keep the terminal payload able to carry session identity, notices, and retrieval snapshot. Add the package-legitimacy-approved `tokio-util` and `tokio-stream` dependencies and explicit Tokio `time`/`sync` features in `engine/Cargo.toml`, then regenerate `engine/Cargo.lock` and the Rust prost/tonic outputs with the checked-in Rust-only Buf template; do not install any unreviewed package (R-13). Create `engine/src/workflow/{mod.rs,node.rs,runner.rs,events.rs}` and `engine/src/workflow/nodes/{mod.rs,reformulate.rs}`. Implement an object-safe `Node::run` returning the existing `BoxFuture` style, a bounded client-event `mpsc::Sender<WorkflowEvent>` owned by `WorkflowRunner`, and `send_event` using awaited `send` for primary protocol events. Implement `CheckpointHandoff` with `CHECKPOINT_PRIMARY_CAPACITY = 1` and `CHECKPOINT_OVERFLOW_CAPACITY = MAX_WORKFLOW_CHECKPOINTS = 5`, derived from the five fixed checkpoint boundaries `ReformulateQuery`, `ExtractGraphContext`, `RetrieveHybrid`, `AssemblePrompt`, and `GenerateAnswer`; terminal and answer events are excluded. `emit_checkpoint` may perform only nonblocking `try_send` admission; when the primary queue is Full or Closed, transfer the exact record into the owned FIFO and return an explicit queued/pending status. A detached FIFO drain retries delivery, closes only after runner completion and both queues are drained, and leaves records in `WorkflowOutcome.pending_checkpoints` with an explicit transport-closed status when the stream receiver is gone; an attempted sixth record is retained in the terminal overflow outcome rather than lost. Implement `WorkflowHandle` with a `CancellationToken`, a terminal `oneshot::Receiver<WorkflowOutcome>`, and a read-only outcome method. Implement `spawn_workflow(runner, ctx)` so a supervisor uses `tokio::select! { biased; _ = cancellation_token.cancelled() => Cancelled; result = timeout(...) => Timeout }` for node supervision, races receiver closure against runner completion, cancels the runner on a closed stream, closes the checkpoint handoff after the runner ends, and lets the detached checkpoint forwarder finish or stop on stream cancellation while preserving pending records for observation. Cancellation is sourced from token cancellation or failed event send; a token that remains open until the deadline maps to Timeout, while cancellation before the deadline maps to Cancelled, with the biased branch resolving simultaneous readiness as cancellation. The runner records the outcome even when it cannot emit terminal events. Capture and instrument the existing `query_rag` span on the detached runner rather than creating per-node spans, preserving D-31. Implement the NoOp `QueryReformulator` and one `ReformulateQueryNode` that returns the original query without an LLM call and leaves embedding injection ready for variant zero (D-08, ORCH-05). Keep ReceiveQuery validation, UUIDv4 session/correlation handling, and `d1_status` before channel creation (D-04); use the runner only after validation. Keep the standalone `QueryGraph` method untouched (D-10); the gateway SSE adapter, route timeout isolation, and timeout configuration are implemented by 05-06.
  At the Rust stream-adapter boundary, import `tokio_stream::StreamExt as TokioStreamExt` and use that alias for Tokio-stream adapters (or a fully qualified `tokio_stream::StreamExt` call) so it cannot conflict with the existing `futures::StreamExt` import.
  Before the first Rust test registration/list, focused test execution, or full-suite gate, migrate `engine/src/tests.rs` completely from unary QueryRAG calls to the new server stream. Define `async fn drain_query_rag_stream&lt;S&gt;(call: Result&lt;tonic::Response&lt;S&gt;, tonic::Status&gt;) -> Result&lt;QueryRAGResponse, tonic::Status&gt;` (using the generated stream bound or its equivalent): return a pre-stream `Err(Status)` unchanged so trailer metadata remains inspectable, drain every `Result&lt;WorkflowEvent, Status&gt;` item, surface any mid-stream tonic status, require one terminal `WorkflowCompleted` carrying the `QueryRAGResponse` payload, and return that payload only after the stream closes. Use the helper for every existing valid caller — `query_rag_happy_path_service`, `configured_provider_settings_reach_query_requests`, `configured_embedding_identity_persists_and_reports`, `configured_bm25_and_evidence_settings_reach_query`, `configured_rag_settings_drive_service`, `configured_evidence_token_budget_is_exact`, all three calls in `service_index_generation_is_opaque_and_stable`, `query_rag_citation_identity_and_notices`, `query_rag_invokes_recording_reranker_once`, `query_rag_grounding_uses_reranked_identity`, `query_rag_noop_reranker_preserves_fused_order`, `query_rag_valid_zero_match`, `query_rag_span_and_request_threading`, `capture_chat_request_body`, `graph_augmentation_succeeded_is_observable_end_to_end`, `graph_augmentation_no_match_found_is_observable_end_to_end`, and `graph_augmentation_attempted_and_failed_is_observable_end_to_end`. Migrate the expected post-open failures in `query_rag_rejects_unknown_marker_without_response`, `query_rag_rejects_invalid_provider_grounding`, `query_rag_generation_error_preserves_identity`, `query_rag_reranker_failure_skips_generation`, `query_rag_fail_closed_embedding_transport`, `query_rag_fail_closed_embedding_empty_payload`, `query_rag_fail_closed_embedding_multi_vector`, `query_rag_fail_closed_embedding_wrong_dimension`, `query_rag_fail_closed_embedding_non_finite`, and `query_rag_fail_closed_dense_snapshot` to drain events and assert their typed `NodeFailed` plus failed `WorkflowCompleted`; retain direct pre-stream `Status`/trailer assertions only for malformed ReceiveQuery inputs. Preserve every direct `response.answer`, `resp.answer`, `response.notices`, and `resp.notices` assertion in the existing tests — specifically the happy path, citation/notices, zero-match, mock-provider, and three graph-augmentation scenarios — against the extracted terminal payload rather than replacing them with event-count-only checks. This migration is a prerequisite: `cargo test -- --list`, every focused Rust test command, and the full `cargo test --locked` gate are invalid until `engine/src/tests.rs` compiles against the streaming generated client. Keep the Rust-only Buf transition guard intact: Rust generated streaming code may be used in this task, Go generated clients remain unary until 05-06, and no standard unified four-plugin `buf generate` may run during the interim.
  </action>
  <action_extension>Executable traceability: mirror the object-safe `BoxFuture`/node shape from `engine/src/generation/mod.rs` and the corresponding `05-PATTERNS.md` analog. Keep D-11's automatic retry boundary out of the generic runner so only `GenerateAnswer` owns it; enforce D-16's native client-close cancellation through the request token and failed event send; and enforce D-20's coarse one-pair-per-node event set without dense/BM25 sub-step or retry-attempt events.</action_extension>
  <verify>
    <automated>$bufText = Get-Content -LiteralPath 'buf.gen.yaml' -Raw; if ($bufText -notmatch 'clean:\s*false') { throw '05-01 Rust-only transition must keep clean=false' }; $rustPlugins = @([regex]::Matches($bufText, 'buf\.build/community/neoeinstein-(prost|tonic)')); if ($rustPlugins.Count -ne 2) { throw "Expected exactly two Rust plugins, found $($rustPlugins.Count)" }; if ($bufText -match 'buf\.build/protocolbuffers/go|buf\.build/grpc/go|out:\s*gateway/proto') { throw '05-01 Rust-only template must contain no Go plugin or gateway/proto output' }; $rustOutputs = @([regex]::Matches($bufText, 'out:\s*engine/src/pb')); if ($rustOutputs.Count -ne 2) { throw "Expected exactly two engine/src/pb outputs, found $($rustOutputs.Count)" }; $goGeneratedFiles = @('gateway/proto/lancet/v1/lancet.pb.go', 'gateway/proto/lancet/v1/lancet_grpc.pb.go'); $beforeGo = @{}; foreach ($path in $goGeneratedFiles) { $hash = git hash-object -- $path; $hashExit = $LASTEXITCODE; if ($hashExit -ne 0) { exit $hashExit }; $beforeGo[$path] = $hash }; $mainBefore = git hash-object -- gateway/main.go; $mainBeforeExit = $LASTEXITCODE; if ($mainBeforeExit -ne 0) { exit $mainBeforeExit }; buf generate; $bufExit = $LASTEXITCODE; if ($bufExit -ne 0) { exit $bufExit }; foreach ($path in $goGeneratedFiles) { $hash = git hash-object -- $path; $hashExit = $LASTEXITCODE; if ($hashExit -ne 0) { exit $hashExit }; if ($beforeGo[$path] -ne $hash) { throw "Rust-only generation changed $path" } }; $mainAfter = git hash-object -- gateway/main.go; $mainAfterExit = $LASTEXITCODE; if ($mainAfterExit -ne 0) { exit $mainAfterExit }; if ($mainBefore -ne $mainAfter) { throw 'Rust-only generation changed the unary gateway' }; cargo check --manifest-path engine/Cargo.toml --locked; $checkExit = $LASTEXITCODE; if ($checkExit -ne 0) { exit $checkExit }; $rustList = @(cargo test --manifest-path engine/Cargo.toml --locked --bin engine -- --list 2>&amp;1); $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; Push-Location gateway; try { go build ./...; $goExit = $LASTEXITCODE; if ($goExit -ne 0) { exit $goExit } } finally { Pop-Location }</automated>
  </verify>
  <acceptance_criteria>
    - The regenerated Rust tonic service accepts `Response&lt;Stream&lt;Item = Result&lt;pb::WorkflowEvent, tonic::Status&gt;&gt;&gt;` for QueryRAG; 05-06 owns regeneration of the Go server-streaming client after the gateway adapter is ready.
    - `WorkflowRunner::send_event` is the only client-event send path and awaits `mpsc::Sender::send`; checkpoint admission uses a separate bounded `try_send` path, transfers Full/Closed records into owned FIFO overflow, and never shares that await.
    - `spawn_workflow` owns a named cancellation supervisor that terminates on natural runner completion as well as receiver closure; two concurrent calls do not share a token or channel.
    - The runner preserves the D-17 GraphExtraction outer timeout at exactly 15 seconds by default and exposes one named `GRAPH_TIMEOUT_DEGRADE_MARGIN` contract for 05-02; `inner_deadline = max(1ms, outer_duration.saturating_sub(GRAPH_TIMEOUT_DEGRADE_MARGIN))` is strictly earlier for normal configured values and safely positive for tiny test values.
    - The checked-in buf template is Rust-only throughout the 05-01-to-05-06 interval; a plain `buf generate` cannot create Go streaming stubs until 05-06 restores the full template in its coordinated gateway change.
    - A valid tracer request produces ordered ReformulateQuery and WorkflowCompleted events with one common trace_id; WorkflowCompleted.session_id is the request session value and is not compared to trace_id.
    - Invalid pre-stream validation still uses `d1_status` trailers, while a post-open node failure uses NodeFailed plus WorkflowCompleted(failed) in-band.
    - The Rust response stream carries standalone Checkpoint events for Go-side persistence while NodeCompleted and NodeFailed remain client-event payloads without embedded snapshot bytes; every checkpoint either reaches the detached drain or remains visible as pending in the terminal outcome.
    - `CHECKPOINT_PRIMARY_CAPACITY` is 1 and `CHECKPOINT_OVERFLOW_CAPACITY` is 5; tests exercise the exact constants, FIFO order, explicit sixth-record overflow/pending behavior, and the distinct open-token timeout versus pre-deadline cancellation mappings.
  </acceptance_criteria>
  <done>The Rust QueryRAG tracer is server-streamed with locked validation/error boundaries, separate checkpoint delivery, and an independently observable cancellation outcome.</done>
</task>

<task type="auto">
  <name>Task 2: Close Rust stream lifecycle and cancellation invariants</name>
  <action_extension>Exercise the concrete lifecycle rules from `05-AI-SPEC.md`/`05-RESEARCH.md` and the existing per-attempt timeout precedent in `engine/src/generation/openrouter.rs`; there is no existing multi-step runner analog. The receiver-close test must prove D-16 native cancellation and `Cancelled` observability, D-20 coarse event cardinality, and D-11 retry ownership remaining in `GenerateAnswer`.</action_extension>
  <read_first>engine/src/main.rs, engine/src/tests.rs, engine/src/workflow/node.rs, engine/src/workflow/runner.rs, engine/src/workflow/events.rs, engine/src/workflow/nodes/reformulate.rs, 05-AI-SPEC.md, 05-REVIEWS.md, AGENTS.md, rust-guidelines.md</read_first>
  <files>engine/src/main.rs, engine/src/workflow/runner.rs, engine/src/workflow/events.rs, engine/src/workflow/node.rs</files>
  <behavior>
    - A client receiver closes -> the runner records a Cancelled or ReceiverClosed WorkflowOutcome and the supervisor terminates without terminal sends into the closed client channel.
    - A checkpoint receiver never drains -> client-visible NodeStarted, NodeCompleted, and WorkflowCompleted events remain deliverable through the reliable event channel while each checkpoint is retained in bounded owned overflow/pending state.
    - GraphExtraction has a distinct inner degradation deadline before its configured outer node timeout, so a stalled graph can complete as an empty-context success before the runner backstop while cancellation remains a separate outcome.
    - Two concurrent requests -> independent client-event channels, checkpoint queues, cancellation tokens, and trace identities.
    - `test_workflow_runner_timeout_with_open_token` -> an un-cancelled token at the deadline maps to Timeout.
    - `test_workflow_runner_cancellation_before_timeout` -> cancellation before the deadline maps to Cancelled.
    - `test_checkpoint_handoff_capacity_and_pending` -> exact primary-1/FIFO-overflow-5 registration, FIFO delivery, and explicit sixth-record overflow/pending behavior.
  </behavior>
  <action>Exercise the Rust tonic handler and runner lifecycle with deterministic in-process event receivers. Keep ReceiveQuery and its d1_status trailers synchronous before stream creation (D-04), preserve the captured query_rag span on the detached task without adding per-node spans (D-31), and expose WorkflowHandle::outcome as the only terminal observation after a receiver closes. Have the supervisor cancel the runner from client-channel closure, distinguish cancellation from timeout, drop all sender clones on natural completion, and stop the standalone checkpoint forwarder when the response stream is canceled while retaining pending records and an explicit transport-closed outcome. Prove the primary client-event path remains awaited/reliable while checkpoint admission remains nonblocking and every record is either drained FIFO or exposed through the owned overflow/pending contract required by D-27. Keep the event conversion in `events.rs` as the single Rust-to-protobuf boundary, retain separate trace_id/session_id identity (D-29), and leave QueryGraph untouched (D-10); configuration loading, Go SSE framing, and transport regression tests belong to 05-06.</action>
  <verify>
    <automated>$rustList = @(cargo test --manifest-path engine/Cargo.toml --locked --bin engine -- --list 2>&amp;1)
$rustListExit = $LASTEXITCODE
if ($rustListExit -ne 0) { exit $rustListExit }
$expectedRustTests = @('test_workflow_runner_timeout_with_open_token', 'test_workflow_runner_cancellation_before_timeout', 'test_checkpoint_handoff_capacity_and_pending')
foreach ($leaf in $expectedRustTests) {
  $escapedLeaf = [regex]::Escape($leaf)
  $registered = @($rustList | ForEach-Object { $line = $_.ToString(); if ($line -match ("^\s*(?<full>[A-Za-z0-9_:]*" + $escapedLeaf + "):\s+test\s*$")) { $Matches['full'] } })
  if ($registered.Count -ne 1) { throw "Expected one exact Rust registration ending in $leaf, found $($registered.Count)" }
  $fullName = $registered[0]
  $run = @(cargo test --manifest-path engine/Cargo.toml --locked --bin engine -- --exact $fullName --nocapture 2>&amp;1)
  $runExit = $LASTEXITCODE
  if ($runExit -ne 0) { exit $runExit }
  $pass = @($run | Where-Object { $_.ToString() -match ("^test\s+" + [regex]::Escape($fullName) + "\s+\.\.\.\s+ok\s*$") })
  if ($pass.Count -lt 1) { throw "Exact Rust test did not report PASS: $fullName" }
}
cargo fmt --manifest-path engine/Cargo.toml -- --check
 $fmtExit = $LASTEXITCODE
if ($fmtExit -ne 0) { exit $fmtExit }
cargo test --manifest-path engine/Cargo.toml --locked
$fullRustExit = $LASTEXITCODE
if ($fullRustExit -ne 0) { exit $fullRustExit }
Push-Location gateway
try { go test ./...; $goExit = $LASTEXITCODE; if ($goExit -ne 0) { exit $goExit } } finally { Pop-Location }</automated>
  </verify>
  <acceptance_criteria>
    - Rust tests prove receiver closure is observable through WorkflowHandle, no terminal send is attempted after closure, and the detached checkpoint forwarder has a bounded cleanup path.
    - Rust tests prove a starved checkpoint queue cannot delay the reliable client-event path, while checkpoint overflow is bounded and observable through diagnostics.
    - Concurrent Rust workflows keep independent channels/tokens and preserve correlation-derived trace IDs distinct from session IDs.
    - The test-list gate names and finds the open-token timeout, pre-deadline cancellation, and exact checkpoint-capacity tests individually; a generic substring match alone is not accepted as coverage evidence.
  </acceptance_criteria>
  <done>Rust stream lifecycle, cancellation outcome reporting, and independent checkpoint backpressure behavior are deterministic and ready for the gateway/configuration plan.</done>
</task>

</tasks>

<threat_model>
ASVS Level: 1. Blocking threshold: high. High-severity threats block plan acceptance/execution.

## Trust Boundaries

| Boundary | Description |
|---|---|
| HTTP client → Go `/rag/query` | Untrusted JSON body and connection lifetime enter the gateway. |
| Go gateway → Rust QueryRAG | Internal gRPC request and streaming event contract crosses a process boundary. |
| Rust event producer → client stream | A slow or disconnected consumer can affect event delivery and cancellation. |
| Cargo registry → engine build | Reviewed Rust dependencies enter the build graph. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-01 | Tampering | QueryRAG/SSE contract | high | mitigate | Keep pre-stream validation before channel creation, preserve trailer identity, and serialize only typed generated WorkflowEvent payloads. |
| T-05-02 | Denial of Service | Rust event channel and detached runner | high | mitigate | Use a bounded channel, awaited client-event sends, receiver-closure cancellation, and a supervisor that terminates on both natural completion and disconnect. |
| T-05-03 | Information Disclosure | NodeFailed messages | medium | mitigate | Map internal errors to the closed D-22 taxonomy and sanitize client messages while retaining detail only in existing structured logs. |
| T-05-04 | Spoofing | trace_id/session_id identity | medium | mitigate | Mint correlation_id server-side, copy it to every event trace_id, and assert it remains distinct from session_id. |
| T-05-05 | Denial of Service | CheckpointHandoff overflow/drain | high | mitigate | Keep primary admission nonblocking, retain Full/Closed records in a bounded owned FIFO sized to the fixed workflow maximum, expose pending/overflow outcomes, and verify FIFO drain under a stalled consumer. |
| T-05-SC | Tampering | Cargo dependency installation | high | mitigate | Use only the RESEARCH.md package-legitimacy-approved `tokio-util` and `tokio-stream` entries, regenerate the lockfile, and run all builds with `--locked`; no [ASSUMED] or [SUS] package is introduced. |
</threat_model>

<verification>
- The temporary Rust-only Buf generation succeeds, the Rust code compiles, and the existing Go suite remains green until 05-06 performs coordinated client regeneration.
- Focused Rust workflow and cancellation tests pass.
- The unfiltered Rust suite and the current unfiltered Go suite pass at the wave boundary; 05-06 then replaces the Go transport/configuration surface with its streaming regression suite.
- `cargo fmt --manifest-path engine/Cargo.toml -- --check` passes after implementation.
</verification>

<success_criteria>
One valid request traverses the new Rust streaming contract, one invalid request preserves the old pre-stream error identity, and a disconnected client has a deterministic runner outcome without a leaked supervisor or fixed gateway timeout conflict; 05-06 completes the Go/SSE hop.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-01-SUMMARY.md` when done.
</output>
