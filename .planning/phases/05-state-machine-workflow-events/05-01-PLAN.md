---
phase: 05-state-machine-workflow-events
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - proto/lancet/v1/lancet.proto
  - gateway/proto/lancet/v1/lancet.pb.go
  - gateway/proto/lancet/v1/lancet_grpc.pb.go
  - engine/src/pb/lancet/v1/lancet.v1.rs
  - engine/src/pb/lancet/v1/lancet.v1.tonic.rs
  - engine/Cargo.toml
  - engine/Cargo.lock
  - config/config.toml
  - config/config.example.toml
  - engine/src/workflow/mod.rs
  - engine/src/workflow/node.rs
  - engine/src/workflow/runner.rs
  - engine/src/workflow/events.rs
  - engine/src/workflow/nodes/mod.rs
  - engine/src/workflow/nodes/reformulate.rs
  - engine/src/main.rs
  - engine/src/tests.rs
  - gateway/main.go
  - gateway/main_test.go
autonomous: true
requirements: [ORCH-01, ORCH-02, ORCH-03, ORCH-05]

must_haves:
  truths:
    - "A valid QueryRAG request performs synchronous ReceiveQuery validation and ID minting before any stream opens; invalid session or query input remains a trailer-bearing gRPC error and becomes HTTP 4xx without an SSE 200 response (D-04, D-05)."
    - "The tracer path emits ReformulateQuery NodeStarted/NodeCompleted and WorkflowCompleted through a server-streaming gRPC response, then Go forwards those frames as SSE data frames."
    - "Every client-visible Rust event uses the bounded Rust WorkflowEvent channel and reliable send; a closed receiver produces a separately observable WorkflowOutcome through WorkflowHandle rather than attempting terminal sends into a closed stream (D-16)."
    - "The event trace_id is the request correlation_id and remains distinct from WorkflowCompleted.session_id (D-29)."
    - "The reformulation tracer returns exactly the original query without a model call, while the embedding boundary is prepared to consume only variant zero (D-08, ORCH-05)."
    - "The /rag/query route does not inherit the blanket 60-second timeout; Rust node budgets remain the source of bounded execution, so a configurable provider timeout cannot be invalidated by a stale fixed gateway budget."
    - "The generated protobuf and Go client use a server-streaming QueryRAG contract with distinct NodeStarted, NodeCompleted, NodeFailed, AnswerChunk, FinalAnswer, and WorkflowCompleted event shapes (D-01, D-02, D-18, D-20)."
  artifacts:
    - path: "proto/lancet/v1/lancet.proto"
      provides: "Server-streaming QueryRAG RPC, WorkflowEvent oneof, typed node-failure enum, and checkpoint sidecar contract"
    - path: "engine/src/workflow/node.rs"
      provides: "Object-safe Node trait, WorkflowContext, NodeError, NodeErrorKind, and sanitized client-message helper"
    - path: "engine/src/workflow/runner.rs"
      provides: "WorkflowRunner, bounded event channel, timeout/cancellation supervisor, WorkflowHandle, and WorkflowOutcome"
    - path: "engine/src/workflow/events.rs"
      provides: "Rust WorkflowEvent enum and the single into_proto conversion boundary"
    - path: "engine/src/workflow/nodes/reformulate.rs"
      provides: "QueryReformulator port, NoOpQueryReformulator, and ReformulateQueryNode"
    - path: "gateway/main.go"
      provides: "Server-streaming gRPC client adapter, first-frame-prefetch SSE handler, and route timeout isolation"
  key_links:
    - from: "engine/src/main.rs::query_rag"
      to: "engine/src/workflow/runner.rs::spawn_workflow"
      via: "ReceiveQuery validation creates WorkflowContext, then a bounded event receiver is converted once to the tonic stream"
    - from: "gateway/main.go::queryRAG"
      to: "gateway/main.go::grpcEngine.QueryRAG"
      via: "stream.Recv() prefetches the first event before headers, then every event is encoded as an SSE data frame"
    - from: "WorkflowRunner"
      to: "WorkflowHandle"
      via: "a oneshot terminal outcome and a cancellation supervisor race receiver closure against runner completion"
  prohibitions:
    - requirement_id: ORCH-01
      category: boundary
      statement: "MUST NOT move ReceiveQuery validation into the asynchronous node runner or wrap the standalone QueryGraph RPC in workflow events."
      verification: test
    - requirement_id: ORCH-02
      category: transparency
      statement: "MUST NOT convert the Rust WorkflowEvent enum inside nodes or use a lossy send for client-visible protocol events."
      verification: test
    - requirement_id: ORCH-03
      category: failure-handling
      statement: "MUST NOT report a closed client stream as a fabricated terminal success or confuse cancellation with timeout."
      verification: test
    - requirement_id: ORCH-05
      category: scope
      statement: "MUST NOT add LLM-backed, HyDE, or multi-query reformulation logic; NoOpQueryReformulator remains the only production implementation in this phase."
      verification: test
---

<objective>
Establish the Phase 5 vertical tracer from the internal streaming contract through one Rust workflow node to Go SSE delivery, with an observable cancellation outcome and the complete timeout/config contract available to later waves.

**As a** Lancet engineer, **I want to** formalize RAG orchestration into a Rust state machine, **so that** I can debug and extend the pipeline with predictable failure handling.

Purpose: prove the cross-runtime architecture before adding the remaining pipeline nodes.
Output: server-streaming protobuf and generated clients, a production-quality one-node Rust runner, a first-frame-safe SSE gateway, a named cancellation handle, and validated workflow timeout settings.
</objective>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-PATTERNS.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEWS.md
@.planning/phases/05-state-machine-workflow-events/05-AI-SPEC.md
@.planning/phases/05-state-machine-workflow-events/05-VALIDATION.md
@.planning/phases/05-state-machine-workflow-events/05-SOURCE-AUDIT.md
@.planning/phases/05-state-machine-workflow-events/COVERAGE.md
@AGENTS.md
@rust-guidelines.md
@go-guidelines.md
</context>

## Artifacts this phase produces

- `proto/lancet/v1/lancet.proto`: `WorkflowEvent`, `NodeErrorCategory`, event payload messages, `Checkpoint`, and streaming `QueryRAG`.
- `gateway/proto/lancet/v1/lancet.pb.go` and `gateway/proto/lancet/v1/lancet_grpc.pb.go`: regenerated Go messages and server-streaming client/server contract.
- `engine/src/pb/lancet/v1/lancet.v1.rs` and `engine/src/pb/lancet/v1/lancet.v1.tonic.rs`: regenerated Rust prost/tonic messages and service trait.
- `engine/Cargo.toml` and `engine/Cargo.lock`: reviewed Tokio time/sync plus cancellation/stream dependencies and locked versions.
- `engine/src/workflow/{mod.rs,node.rs,runner.rs,events.rs}`: `Node`, `WorkflowContext`, `WorkflowRunner`, `WorkflowHandle`, `WorkflowOutcome`, `NodeErrorKind`, and Rust-to-protobuf event conversion.
- `engine/src/workflow/nodes/mod.rs` and `engine/src/workflow/nodes/reformulate.rs`: node registry, `QueryReformulator`, `NoOpQueryReformulator`, and `ReformulateQueryNode`.
- `engine/src/main.rs` and `engine/src/tests.rs`: service wiring, timeout validation, workflow settings, and transport/config regression tests.
- `gateway/main.go` and `gateway/main_test.go`: streaming `engine.QueryRAG`, SSE forwarding, first-frame prefetch, and route registration without inherited `middleware.Timeout(60*time.Second)`.
- `config/config.toml` and `config/config.example.toml`: `WorkflowConfigSettings` and the five workflow timeout keys.

<tasks>

<task type="tracer">
  <name>Task 1: Trace one reformulation event from Rust through gRPC to SSE</name>
  <read_first>proto/lancet/v1/lancet.proto, buf.gen.yaml, engine/Cargo.toml, engine/src/main.rs, engine/src/generation/mod.rs, gateway/main.go, gateway/main_test.go, 05-CONTEXT.md, 05-REVIEWS.md, rust-guidelines.md, go-guidelines.md</read_first>
  <files>proto/lancet/v1/lancet.proto, gateway/proto/lancet/v1/lancet.pb.go, gateway/proto/lancet/v1/lancet_grpc.pb.go, engine/src/pb/lancet/v1/lancet.v1.rs, engine/src/pb/lancet/v1/lancet.v1.tonic.rs, engine/Cargo.toml, engine/Cargo.lock, engine/src/workflow/mod.rs, engine/src/workflow/node.rs, engine/src/workflow/runner.rs, engine/src/workflow/events.rs, engine/src/workflow/nodes/mod.rs, engine/src/workflow/nodes/reformulate.rs, engine/src/main.rs, gateway/main.go</files>
  <reversibility rating="costly">D-18 changes the coordinated Go/Rust QueryRAG cardinality; reverting it requires restoring the unary protobuf contract and every generated client, gateway handler, and integration test, but no persisted data migration.</reversibility>
  <behavior>
    - Valid request -> ReceiveQuery -> ReformulateQuery NodeStarted -> NodeCompleted -> WorkflowCompleted over one server stream.
    - Invalid request -> synchronous gRPC status with existing trailer identity and no WorkflowEvent.
    - Client receiver closes -> WorkflowHandle reports Cancelled or ReceiverClosed independently of the closed event channel.
    - First gRPC event -> prefetched before Go commits SSE headers; all subsequent events -> flushed `data: {json}` frames.
  </behavior>
  <action>Change `proto/lancet/v1/lancet.proto` so `LancetService.QueryRAG` returns `stream WorkflowEvent` while retaining `QueryRAGResponse` as the terminal payload shape. Define one `WorkflowEvent` envelope with `trace_id`, a `oneof` containing `NodeStarted`, `NodeCompleted`, `NodeFailed`, `AnswerChunk`, `FinalAnswer`, and `WorkflowCompleted`, and a separate optional `Checkpoint` sidecar. Include the D-22 closed failure categories and keep the terminal payload able to carry session identity, notices, and retrieval snapshot. Add the package-legitimacy-approved `tokio-util` and `tokio-stream` dependencies and explicit Tokio `time`/`sync` features in `engine/Cargo.toml`, then regenerate `engine/Cargo.lock` and protobuf output with the repository's `buf generate` command; do not install any unreviewed package (R-13). Create `engine/src/workflow/{mod.rs,node.rs,runner.rs,events.rs}` and `engine/src/workflow/nodes/{mod.rs,reformulate.rs}`. Implement an object-safe `Node::run` returning the existing `BoxFuture` style, a bounded `mpsc::Sender<WorkflowEvent>` owned by `WorkflowRunner`, and `send_event` using awaited `send` for primary protocol events. Implement `WorkflowHandle` with a `CancellationToken`, a terminal `oneshot::Receiver<WorkflowOutcome>`, and a read-only outcome method. Implement `spawn_workflow(runner, ctx)` so a supervisor races receiver closure against runner completion, cancels the runner on a closed stream, and drops its sender clone when either branch completes; the runner records the outcome even when it cannot emit terminal events. Capture and instrument the existing `query_rag` span on the detached runner rather than creating per-node spans, preserving D-31. Implement the NoOp `QueryReformulator` and one `ReformulateQueryNode` that returns the original query without an LLM call and leaves embedding injection ready for variant zero (D-08, ORCH-05). Keep ReceiveQuery validation, UUIDv4 session/correlation handling, and `d1_status` before channel creation (D-04); use the runner only after validation. Update `gateway/main.go`'s `engine` interface and `grpcEngine.QueryRAG` to return the generated server-streaming client and preserve `trailerError` for pre-stream errors. Rewrite `queryRAG` to prefetch one event before setting `Content-Type: text/event-stream`, `Cache-Control: no-cache`, and status 200; then serialize each event as an SSE frame and flush it. Forward in-stream failures as payloads and close normally (D-05), with no reconnect/resume state or Last-Event-ID handling (D-21). Register `/rag/query` in a router group that receives common middleware but not the root `middleware.Timeout(60*time.Second)`; do not add a fixed gateway budget that can undercut configurable Rust/provider timing. Keep the standalone `QueryGraph` method untouched (D-10).</action>
  <verify>
    <automated>buf generate; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; cargo check --manifest-path engine/Cargo.toml --locked; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; Push-Location gateway; try { go build ./...; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } finally { Pop-Location }</automated>
  </verify>
  <acceptance_criteria>
    - The generated Rust tonic service accepts `Response&lt;Stream&lt;Item = Result&lt;pb::WorkflowEvent, tonic::Status&gt;&gt;&gt;` for QueryRAG, and the generated Go client exposes the server-streaming QueryRAG client.
    - `WorkflowRunner::send_event` is the only client-event send path and awaits `mpsc::Sender::send`; a receiver-closed result completes the `WorkflowHandle` outcome without attempting more terminal sends.
    - `spawn_workflow` owns a named cancellation supervisor that terminates on natural runner completion as well as receiver closure; two concurrent calls do not share a token or channel.
    - A valid tracer request produces ordered ReformulateQuery and WorkflowCompleted events with one common trace_id; WorkflowCompleted.session_id is the request session value and is not compared to trace_id.
    - Invalid pre-stream validation still uses `d1_status` trailers, while a post-open node failure uses NodeFailed plus WorkflowCompleted(failed) in-band.
    - The gateway does not place `/rag/query` under the root 60-second timeout middleware and does not implement SSE resume/reconnect.
  </acceptance_criteria>
  <done>The thinnest real QueryRAG path is streamed from Rust to Go SSE, with locked validation/error boundaries and an independently observable cancellation outcome.</done>
</task>

<task type="auto">
  <name>Task 2: Make workflow timeout configuration and transport regressions executable</name>
  <read_first>engine/src/main.rs, engine/src/tests.rs, config/config.toml, config/config.example.toml, gateway/main.go, gateway/main_test.go, 05-AI-SPEC.md, 05-REVIEWS.md, AGENTS.md, rust-guidelines.md, go-guidelines.md</read_first>
  <files>engine/src/main.rs, engine/src/tests.rs, config/config.toml, config/config.example.toml, gateway/main_test.go</files>
  <behavior>
    - Every workflow timeout is configurable through TOML and `LANCET_ENGINE__WORKFLOW__...` environment overrides, including sub-second test values.
    - A generation node budget is validated against two effective provider attempts plus explicit slack, rather than the constructor's 30-second convenience constant alone.
    - The Go fake stream tests first-frame prefetch, trailer-to-header mapping for pre-stream rejection, SSE framing, trace/session identity, and route timeout isolation.
  </behavior>
  <action>In `engine/src/main.rs`, add `WorkflowConfigSettings` under `[engine.workflow]` with `reformulate_timeout_ms=5000`, `retrieve_timeout_ms=10000`, `graph_timeout_ms=15000`, `prompt_timeout_ms=2000`, and `generation_node_timeout_ms=65000`. Preserve D-17's `openrouter.generation_timeout_secs` as the per-attempt provider budget, and validate the outer generation-node budget with checked arithmetic so it is at least two effective provider budgets plus one second of slack; reject zero, overflow, or undersized values before the engine listens. Add explicit environment bindings for all five workflow keys alongside the existing manual overrides so casing and separator behavior cannot silently fall back to file defaults (addresses the Antigravity env-drift finding). Update `config/config.toml`, `config/config.example.toml`, and the exact config-contract arrays/section allowlist/adjacent annotation checks in `engine/src/tests.rs`; update every `Settings`/`EffectiveRagSettings` literal required by compilation. In `gateway/main_test.go`, replace the unary `engineFunc.QueryRAG` fake with a deterministic server-streaming fake and add focused tests for first-frame prefetch, malformed pre-stream status, in-band NodeFailed/WorkflowCompleted failure, SSE frame flushing, route exclusion from the 60-second middleware, and the fact that trace_id matches correlation identity while session_id remains independent. Keep the test assertions independent of provider latency and do not add a client-side resume contract (D-19 through D-21).</action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked --bin engine -- config; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; Push-Location gateway; try { go test ./... -run 'QueryRAG|SSE|RouteTimeout'; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } finally { Pop-Location }</automated>
  </verify>
  <acceptance_criteria>
    - Both committed config files contain one annotated `[engine.workflow]` section with all five timeout keys and no credential values.
    - The config tests recognize the workflow section and each new key exactly once, and a sub-second environment override is observable in the deserialized settings.
    - `EffectiveRagSettings::validate` rejects an outer generation budget that cannot contain two effective provider attempts plus slack, including when `generation_timeout_secs` is raised above its default.
    - Go tests prove a pre-stream invalid request remains HTTP 4xx with trailer-derived identity and that a valid stream is SSE-only after the first event is prefetched.
    - The transport tests do not assert trace_id equals session_id and do not rely on the global 60-second middleware for `/rag/query` completion.
  </acceptance_criteria>
  <done>Workflow timeout configuration is explicit, overrideable, and arithmetically consistent with the locked retry contract; the streaming transport has deterministic boundary tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| HTTP client → Go `/rag/query` | Untrusted JSON body and connection lifetime enter the gateway. |
| Go gateway → Rust QueryRAG | Internal gRPC request and streaming event contract crosses a process boundary. |
| Rust event producer → client stream | A slow or disconnected consumer can affect event delivery and cancellation. |
| Cargo registry → engine build | Reviewed Rust dependencies enter the build graph. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-01 | Tampering | QueryRAG/SSE contract | high | mitigate | Keep pre-stream validation before channel creation, preserve trailer identity, and serialize only typed generated WorkflowEvent payloads. |
| T-05-02 | Denial of Service | Rust event channel and detached runner | high | mitigate | Use a bounded channel, awaited client-event sends, receiver-closure cancellation, and a supervisor that terminates on both natural completion and disconnect. |
| T-05-03 | Information Disclosure | NodeFailed messages | medium | mitigate | Map internal errors to the closed D-22 taxonomy and sanitize client messages while retaining detail only in existing structured logs. |
| T-05-04 | Spoofing | trace_id/session_id identity | medium | mitigate | Mint correlation_id server-side, copy it to every event trace_id, and assert it remains distinct from session_id. |
| T-05-SC | Tampering | Cargo dependency installation | high | mitigate | Use only the RESEARCH.md package-legitimacy-approved `tokio-util` and `tokio-stream` entries, regenerate the lockfile, and run all builds with `--locked`; no [ASSUMED] or [SUS] package is introduced. |
</threat_model>

<verification>
- `buf generate` succeeds and generated Rust/Go code compiles.
- Focused Rust configuration/tracer tests and focused Go QueryRAG/SSE tests pass.
- `cargo fmt --manifest-path engine/Cargo.toml -- --check` passes after implementation.
</verification>

<success_criteria>
One valid request traverses the new streaming contract end to end, one invalid request preserves the old pre-stream error identity, and a disconnected client has a deterministic runner outcome without a leaked supervisor or fixed gateway timeout conflict.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-01-SUMMARY.md` when done.
</output>
