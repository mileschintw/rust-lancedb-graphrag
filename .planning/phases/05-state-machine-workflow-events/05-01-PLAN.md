---
phase: 05-state-machine-workflow-events
plan: 01
type: execute
wave: 1
depends_on: []
coordination_group: 05-wave-1-atomic-streaming-landing
coordination_mode: atomic_landing
coordination_peers: [05-06]
files_modified:
  - buf.gen.yaml
  - proto/lancet/v1/lancet.proto
  - engine/src/pb/lancet/v1/lancet.v1.rs
  - engine/src/pb/lancet/v1/lancet.v1.tonic.rs
  - engine/Cargo.toml
  - engine/Cargo.lock
  - engine/src/workflow/mod.rs
  - engine/src/workflow/node.rs
  - engine/src/workflow/events.rs
  - engine/src/workflow/runner.rs
  - engine/src/workflow/nodes/mod.rs
  - engine/src/workflow/nodes/reformulate.rs
  - engine/src/main.rs
  - engine/src/tests.rs
autonomous: false
requirements: [ORCH-01, ORCH-02, ORCH-03, ORCH-05]
must_haves:
  truths:
    - "The Rust QueryRAG entry point validates ReceiveQuery before opening the stream and then runs one observable reformulation-to-terminal workflow path with the D-06 order preserved for the eventual node expansion."
    - "The stream has typed NodeStarted, NodeCompleted, AnswerChunk, NodeFailed, Checkpoint, and WorkflowCompleted events, with trace_id equal to the request correlation ID per D-29 and no fabricated answer on failure per D-13."
    - "The successful evidence-bearing terminal payload preserves the complete existing QueryRAGResponse contract: populated answer, citations, session_id, non-unspecified answer_basis, structured_citations, a correctly mapped notices field that may be empty, and retrieval snapshot; the durable checkpoint context_snapshot remains a separate field, while the valid zero-evidence path may retain an empty answer/citation set and unspecified answer_basis per D-03."
    - "Cancellation is cooperative at the runner boundary and closes the workflow without leaving a task or channel receiver alive."
    - "Every accepted checkpoint is handed off through an explicit reliable/pending contract; a bounded drop-only try_send path is not an accepted implementation."
    - "[FLAGGED ASSUMPTION — spec-less edge probe ORCH-01/unclassified] The fixed state machine and transition order are treated as an executable must-have because the fallback supplied no resolved edge coverage."
    - "[FLAGGED ASSUMPTION — spec-less edge probe ORCH-02/unclassified] Client event names, ordering, terminal uniqueness, and transport boundaries are explicitly tested because the fallback supplied no resolved edge coverage."
    - "[FLAGGED ASSUMPTION — spec-less edge probe ORCH-03/idempotency] Repeating the same input creates an independent trace and at most one generation-attempt budget per runner until the generation node adds its locked retry; no cross-run state is shared."
    - "[FLAGGED ASSUMPTION — spec-less edge probe ORCH-03/concurrency] Parallel runners own separate workflow contexts, cancellation tokens, event channels, embedding dependencies, and checkpoint sequence counters."
  artifacts:
    - path: "proto/lancet/v1/lancet.proto"
      provides: "Server-streaming QueryRAG contract and typed workflow event envelope."
    - path: "engine/src/workflow/runner.rs"
      provides: "Cancellable runner seam, stream ownership, and the inline-remainder handoff contract."
    - path: "engine/src/workflow/node.rs"
      provides: "Node trait, typed workflow context, request-local QueryEmbeddingPort contract, cancellation input, and node error contract."
    - path: "engine/src/workflow/events.rs"
      provides: "Typed lifecycle, failure taxonomy, terminal, and full-checkpoint transport values."
    - path: "engine/src/main.rs"
      provides: "Synchronous pre-stream validation and one tracer service implementation."
  key_links:
    - "ReceiveQuery validation -> no stream on malformed request -> runner spawn only after validation"
    - "runner cancellation select -> node future cancellation -> terminal stream closure"
    - "workflow event envelope -> generated Rust service stream -> Go migration in 05-06"
    - "validated QueryRAGResponse fields -> Rust FinalAnswer/WorkflowCompleted terminal payload -> Go DTO -> SSE data frame"
    - "checkpoint event sequence_ordinal -> Go dispatcher reliability/order work in 05-06 and 05-05"
  prohibitions:
    - statement: "The workflow stream must not expose raw provider token streaming or a JSON fallback transport."
      status: unverified
      flagged: true
      rationale: "Spec-less prohibition fallback: no descriptor-bearing prohibition check was available; execution must verify this boundary."
    - statement: "Rust must not open PostgreSQL connections and QueryGraph's standalone API must remain unchanged."
      status: unverified
      flagged: true
      rationale: "Spec-less prohibition fallback: retain the boundary from D-10 and D-26, but do not claim a wired prohibition descriptor."
---

**As a** Lancet engineer, **I want to** formalize RAG orchestration into a Rust state machine, **so that** I can debug and extend the pipeline with predictable failure handling.

<objective>
Create the production-quality tracer slice for the Phase 5 Rust workflow: a validated server-streaming QueryRAG request enters a cancellable runner, emits typed reformulation/lifecycle events, invokes the explicitly named inline remainder bridge, and ends in one terminal event. Establish the protobuf and Rust ownership boundaries that later plans expand.

Purpose: Prove the state-machine-to-stream architecture before the graph, retrieval, prompt, generation, Go SSE, and checkpoint persistence expansions land.
Output: Rust workflow modules, typed stream contract, Rust-only transition codegen, and tracer tests.
</objective>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-PATTERNS.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEWS.md
@.planning/phases/05-state-machine-workflow-events/05-AI-SPEC.md
@AGENTS.md
@rust-guidelines.md

<scope_rationale>
The 14-file inventory is intentionally atomic for the Wave 1 Rust-only contract transition. The protobuf definition, Rust generated service surface, Cargo transition, runner/event modules, service entry point, and `engine/src/tests.rs` migration must land together or the intermediate build would expose a partial streaming API or fail before test discovery; no non-atomic file can move out without breaking that Rust-only generated-code seam. Task 1 owns all 14 files for the transition, including the mechanical migration of every existing Rust `.query_rag(` call site in `engine/src/tests.rs` and the `query_rag_tracer` registration. Task 2 may modify the same five workflow/test seam files only to add the named deterministic regressions after Task 1's stream-drain helper is complete (about 25% of executor context). No task may modify an unlisted file or take ownership of the other task's generated/Cargo files. No Go generated artifact is included in this landing; 05-06 owns that separate coordinated code-generation boundary. `coordination_group: 05-wave-1-atomic-streaming-landing` is the executable landing contract: 05-01 and 05-06 are dispatched and validated as one Wave 1 group in the shared execution workspace, with no sequential DAG edge between them; neither plan is independently landed. The Go suite is not a gate during the Rust-only transition, and the coordinated Go landing must follow before cross-runtime claims.
Executor-context budget is explicit and narrow: Task 1 is capped at approximately 40% of one executor context for the atomic Rust contract landing, and Task 2 at approximately 25% for deterministic regressions. The executor must keep both tasks within those budgets and the listed file boundary; the file count reflects generated/Cargo seam ownership, not permission to broaden implementation scope.
</scope_rationale>

## Review dispositions carried into this plan

- The 05-01/05-06 generated-API landing dependency is deliberate: this plan regenerates only the Rust side while the existing Go unary output remains until 05-06 performs the coordinated full-template generation and Go migration. Therefore this plan does not claim an unfiltered Go runtime suite pass.
- The Antigravity concern about `clean: true` deleting preserved Go output is explicitly rejected as a confirmed defect: with the Rust-only template's Go plugins absent, `gateway/proto` is not a configured output, and the before/after hash guard remains the executable protection against accidental mutation. The final full-template generation remains owned by 05-06.
- Query embedding has an explicit owner before graph/retrieval expansion. Task 1 defines `QueryEmbeddingPort::embed_variant_zero` and the optional `WorkflowContext.query_embedding` field for the tracer bridge; 05-02 moves the same call into the `ExtractGraphContext` prelude, where it receives the separate configured 10000ms budget and is reused by `RetrieveHybrid`. It is not charged to `ReformulateQuery`'s 5000ms budget and is not duplicated inside `RetrieveHybrid`.
- The tracer defines `run_inline_query_rag_remainder(&mut WorkflowContext, &WorkflowDependencies, &WorkflowEventSink, &CancellationToken) -> Result<(), NodeError>` as the compatibility bridge. Its input preserves `session_id`, `trace_id` (equal to `correlation_id`), `original_query`, ordered `variants`, and the variant-0 embedding; it may mutate only `graph_context`, `vector_results`, `bm25_results`, `final_candidates`, `assembled_prompt`, and `answer` as its inline nodes run. It owns `NodeStarted`/`NodeCompleted`/`NodeFailed` and one full snapshot for each inline node it actually invokes, stops at the first typed error, and returns that error. It never emits `WorkflowCompleted`: `WorkflowRunner::emit_terminal_once` is the sole terminal emitter. 05-02 replaces the graph/retrieve portion with runner-owned nodes and hands the same context to `run_inline_prompt_generation_remainder`; 05-03 removes that second bridge when prompt/generation become explicit nodes.
- Checkpoint delivery is represented as a reliable workflow event plus a pending/overflow contract at the Go dispatcher boundary. This Rust plan verifies that the envelope retains full snapshot bytes and explicit pending ownership without constructing `CheckpointDispatcher`; the sixth-envelope dispatcher handoff test belongs to the Go-owned 05-06 task. The valid service path has five or fewer checkpoint boundaries because D-03 can short-circuit before prompt/generation.
- D-12 is retained exactly: generation retry has no backoff. A reviewer suggestion for jitter/backoff is rejected because it conflicts with the locked decision.
- The spec-less edge probe remains unresolved. The flagged assumptions in `must_haves.truths` are intentional executable coverage obligations, not silently accepted behavior.

## Artifacts this phase produces

- `proto/lancet/v1/lancet.proto`: server-streaming QueryRAG and typed workflow event contract.
- `engine/src/pb/lancet/v1/lancet.v1.rs`, `engine/src/pb/lancet/v1/lancet.v1.tonic.rs`: Rust transition outputs only; the coordinated Go outputs belong to 05-06.
- `engine/src/workflow/{mod.rs,node.rs,events.rs,runner.rs}` and `engine/src/workflow/nodes/{mod.rs,reformulate.rs}`: state-machine types, cancellation boundary, event builders, and reformulation node.
- `engine/src/main.rs`: pre-stream validation and tracer service implementation.
- `engine/src/tests.rs`: tracer, event, cancellation, terminal-uniqueness, and same-input/concurrency regression coverage.
- `buf.gen.yaml`, `engine/Cargo.toml`, `engine/Cargo.lock`: transition generator/dependency configuration.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: confirm the locked Rust-only streaming transition</name>
  <read_first>05-CONTEXT.md D-04 and D-18; 05-REVIEWS.md findings on generated-code landing and intermediate Go-suite drift; buf.gen.yaml; proto/lancet/v1/lancet.proto</read_first>
  <decision>Proceed with the D-18 unary-to-server-streaming QueryRAG contract and the D-04 pre-stream validation boundary while keeping Go generated outputs unchanged until 05-06.</decision>
  <context>This is a confirmation of locked context decisions, not a new product choice. The Rust-only landing is the required intermediate state that lets the tracer compile before the coordinated Go generation/migration task.</context>
  <options>
    <option id="continue-locked-transition">
      <name>Continue locked transition</name>
      <pros>Preserves D-04, D-18, and the explicit 05-01/05-06 code-generation dependency.</pros>
      <cons>Leaves the existing Go generated API unchanged until the dependent plan executes.</cons>
    </option>
  </options>
  <acceptance_criteria>The executor confirms the already-locked Rust-only transition, preserves the existing Go generated files until 05-06, and does not treat a pre-migration Go suite result as Phase 05-01 evidence.</acceptance_criteria>
  <resume-signal>Confirm: continue-locked-transition</resume-signal>
</task>

<task type="tracer">
  <name>Task 1: Establish the streaming workflow contract and one end-to-end Rust path</name>
  <files>buf.gen.yaml, proto/lancet/v1/lancet.proto, engine/src/pb/lancet/v1/lancet.v1.rs, engine/src/pb/lancet/v1/lancet.v1.tonic.rs, engine/Cargo.toml, engine/Cargo.lock, engine/src/workflow/mod.rs, engine/src/workflow/node.rs, engine/src/workflow/events.rs, engine/src/workflow/runner.rs, engine/src/workflow/nodes/mod.rs, engine/src/workflow/nodes/reformulate.rs, engine/src/main.rs, engine/src/tests.rs</files>
  <read_first>05-CONTEXT.md sections D-01 through D-18 and D-27 through D-31; 05-RESEARCH.md sections on the Rust runner, protobuf transition, timeout/cancellation, embedding ownership, and checkpoint transport; 05-PATTERNS.md sections for generation node errors and the existing QueryRAG handler; proto/lancet/v1/lancet.proto; engine/src/main.rs; engine/src/client/mod.rs; engine/src/generation/mod.rs; engine/src/tests.rs; engine/Cargo.toml; buf.gen.yaml; AGENTS.md; rust-guidelines.md</read_first>
  <action>Change QueryRAG to a server-streaming `WorkflowEvent` contract per D-18 and D-01/D-02. Define the successful evidence-bearing terminal payload as a field-for-field equivalent of the existing `QueryRAGResponse`: `session_id` comes from validated ReceiveQuery identity; `answer` is the complete validated `ModelOutput.answer`; `citations` is the ordered resolved marker-ID list; `answer_basis` is the protobuf enum mapped from `ModelOutput.answer_basis`; `structured_citations` maps every resolved citation's `chunk_id`, `document_id`, `title`, `section_path`, `excerpt`, `is_truncated`, `score`, `rank`, and `content_type`; `notices` maps model notices and warnings to the existing Notice code/message/severity shape even when the list is empty; and `snapshot` is the existing RetrievalSnapshot built from retrieval settings, filters, and result hash. Keep `snapshot` distinct from `notices`, and keep that response snapshot distinct from the full checkpoint `context_snapshot`. `FinalAnswer` and successful `WorkflowCompleted` must carry this complete payload, while `AnswerChunk` remains a distinct single complete-answer event per D-01/D-02. Include typed node lifecycle, failure taxonomy from D-22, checkpoint envelope, trace/session identity, and a monotonically increasing checkpoint `sequence_ordinal`; per D-05, an opened stream reports failures in-band as NodeFailed followed by failed WorkflowCompleted, while malformed validation remains pre-stream Status. Use coarse per-public-node events with no embedding sub-step, raw provider token, or retrying event per D-20 and D-15, preserve no-reconnect/no-resume behavior per D-21, and do not add workflow metadata or per-node tracing spans per D-30/D-31. Transition `buf.gen.yaml` to the Rust-only template used by this tracer. Use the verification wrapper as the only `buf generate` invocation for this task: it compares hashes of the existing Go generated files immediately before and after generation so unrelated pre-existing worktree edits are preserved and this task cannot overwrite them before 05-06. The template's plugin/output list must contain the Rust outputs only; its preservation behavior must not rely on `clean: false`. Add only the approved existing-stack async dependencies and follow Rust error, cancellation, Send-future, logging, and mockable-I/O guidance. Use the generated Rust identifiers actually produced by protobuf (`QueryRagRequest`/`QueryRagResponse` where applicable), not invented acronym-cased names.</action>
  <action>Implement the tracer runner and reformulation node as production code. Parse/mint/validate the request synchronously before constructing the stream per D-04; malformed input returns a normal tonic Status and never opens a stream. After validation, create an independent `WorkflowContext`, trace_id equal to correlation_id per D-29, cancellation token, event channel, and checkpoint sequence counter. Make the runner's timeout table executable rather than a config-only declaration: `ReformulateQuery` selects `engine.workflow.reformulate_timeout_ms` with a 5000ms default, `ExtractGraphContext` selects `engine.graph.graph_node_timeout_ms` with a 15000ms outer budget containing its 10000ms embedding prelude and 4000ms graph operation, `RetrieveHybrid` selects `engine.workflow.retrieve_timeout_ms` with a 10000ms default, `AssemblePrompt` selects `engine.workflow.prompt_timeout_ms` with a 2000ms default, and `GenerateAnswer` selects `engine.workflow.generation_node_timeout_ms` with a 65000ms default; each node name must select its own budget in the runner, with no generic timeout fallback. Enable only Tokio's existing test-time support in `engine/Cargo.toml` if the deterministic paused-clock deadline tests require it; do not add a new package. Define `WorkflowContext.query_embedding: Option<Vec<f32>>` and `QueryEmbeddingPort::embed_variant_zero(&self, variant: &str, cancel: &CancellationToken) -> BoxFuture<'_, Result<Vec<f32>, NodeError>>` in the workflow seam, but keep the tracer's public event stream at coarse node granularity. Emit one Reformulate start/completed pair, preserve ordered variants, leave `query_embedding` unset until the graph/retrieval bridge owns the embedding prelude, then call `run_inline_query_rag_remainder(&mut WorkflowContext, &WorkflowDependencies, &WorkflowEventSink, &CancellationToken) -> Result<(), NodeError>`. The bridge uses the request-local embedding port once for variant zero before its still-inline graph/retrieval path; embedding transport/invalid-payload errors map to `RetrievalFailed`, embedding timeout maps to `Timeout`, and the same context field is reused by 05-02 rather than embedding again inside `RetrieveHybrid`. Register the focused tracer test exactly as `query_rag_tracer`. The bridge receives the validated identity/query/reformulation fields, may mutate only the graph/retrieval/prompt/answer fields as each still-inline node completes, owns only those partial-node lifecycle events and full snapshots, stops and returns the first typed D-22 error, and never emits a terminal. `WorkflowRunner::emit_terminal_once` handles the returned success or failure exactly once, so the reformulation boundary and later bridge boundaries cannot duplicate `WorkflowCompleted`. Select cancellation before node completion, propagate cancellation as D-22 `Cancelled`, and keep checkpoint admission fire-and-forget at the Go boundary without allowing a dropped bounded try_send implementation. Before the focused list gate, migrate every existing `.query_rag(` call in `engine/src/tests.rs`—all 29 call sites across `query_rag_happy_path_service`, `configured_provider_settings_reach_query_requests`, `configured_embedding_identity_persists_and_reports`, `configured_bm25_and_evidence_settings_reach_query`, `configured_rag_settings_drive_service`, `configured_evidence_token_budget_is_exact`, `service_index_generation_is_opaque_and_stable` (three calls), `query_rag_citation_identity_and_notices`, `query_rag_rejects_unknown_marker_without_response`, `query_rag_rejects_invalid_provider_grounding`, `query_rag_generation_error_preserves_identity`, `query_rag_invokes_recording_reranker_once`, `query_rag_grounding_uses_reranked_identity`, `query_rag_noop_reranker_preserves_fused_order`, `query_rag_reranker_failure_skips_generation`, the five fail-closed embedding tests, `query_rag_fail_closed_dense_snapshot`, `query_rag_valid_zero_match`, `query_rag_span_and_request_threading`, `capture_chat_request_body`, and the three `graph_augmentation_*_is_observable_end_to_end` tests—to a named `collect_query_rag_stream`/terminal-extraction helper that drains the server stream and preserves each test's pre-stream error assertions. Add the new `query_rag_tracer` test in this same module; do not leave any direct unary `.into_inner()` assumption in the Rust test suite.</action>
  <verify>
    <automated>$list = &amp; cargo test --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $name = 'query_rag_tracer'; $pattern = '(?:^|::)' + [regex]::Escape($name) + ': test$'; $hits = @($list | Select-String -Pattern $pattern); if ($hits.Count -ne 1) { throw "Missing exact focused Rust registration: $name" }</automated>
    <automated>$goGenerated = @('gateway/proto/lancet/v1/lancet.pb.go','gateway/proto/lancet/v1/lancet_grpc.pb.go'); $before = @{}; foreach ($path in $goGenerated) { if (-not (Test-Path -LiteralPath $path)) { throw "Missing pre-transition generated file: $path" }; $before[$path] = (Get-FileHash -Algorithm SHA256 -LiteralPath $path).Hash }; $bufOutput = &amp; buf generate; $bufExit = $LASTEXITCODE; if ($bufExit -ne 0) { exit $bufExit }; foreach ($path in $goGenerated) { $after = (Get-FileHash -Algorithm SHA256 -LiteralPath $path).Hash; if ($after -ne $before[$path]) { throw "Rust-only generation changed existing Go output: $path" } }; $rustText = Get-Content -Raw 'engine/src/pb/lancet/v1/lancet.v1.tonic.rs'; if ($rustText -notmatch 'QueryRagStream') { throw 'Rust streaming service output is missing' }; $bufText = Get-Content -Raw 'buf.gen.yaml'; if ($bufText -match 'gateway/proto') { throw 'Rust-only generation template names a Go output' }</automated>
    <automated>cargo check --manifest-path engine/Cargo.toml --locked</automated>
    <automated>$output = &amp; cargo test --manifest-path engine/Cargo.toml --locked query_rag_tracer -- --nocapture; $exit = $LASTEXITCODE; if ($exit -ne 0) { exit $exit }</automated>
  </verify>
  <acceptance_criteria>Rust compilation succeeds against the new stream contract; the Rust-only `buf generate` step succeeds while the existing Go generated files remain byte-for-byte unchanged and the generated Rust service exposes the streaming method; the runner selects the explicit 5000ms ReformulateQuery budget rather than a generic fallback; an evidence-bearing valid request reaches reformulation and the inline remainder through one typed event stream whose FinalAnswer and successful WorkflowCompleted preserve populated answer, citations, session_id, non-unspecified answer_basis, structured_citations, a correctly mapped notices field that may be empty, and retrieval snapshot fields with snapshot distinct from notices; a valid zero-evidence request remains allowed to complete with empty answer/citations and unspecified answer_basis plus its NO_EVIDENCE notice; malformed requests return before stream creation; cancellation and terminal failure use the typed taxonomy; the Go generated client and Go runtime test are explicitly left for the coordinated 05-06 landing.</acceptance_criteria>
  <done>The Phase 5 tracer is a runnable end-to-end Rust slice with a real validation boundary, reformulation node, inline-remainder handoff, cancellation path, and exactly one terminal event.</done>
  <reversibility rating="one-way">The protobuf RPC shape and generated Rust service surface are a cross-runtime contract; the Rust-only transition is isolated so the existing Go contract remains recoverable until 05-06.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Lock tracer lifecycle, edge, concurrency, and handoff regressions</name>
  <files>engine/src/workflow/events.rs, engine/src/workflow/runner.rs, engine/src/workflow/nodes/reformulate.rs, engine/src/main.rs, engine/src/tests.rs</files>
  <read_first>Task 1 outputs; 05-REVIEWS.md findings on trailer identity, cancellation, checkpoint overflow, generated API landing, zero-evidence success, and notices cardinality; 05-AI-SPEC.md evaluation scenarios for malformed input, cancellation, terminal uniqueness, checkpoint backpressure, and same-input behavior; engine/src/tests.rs test helpers and migrated stream collector</read_first>
  <action>Add focused Rust tests with exact names `query_rag_tracer_happy_path`, `workflow_tracer_cancellation`, `checkpoint_envelope_preserves_pending_ownership`, and `workflow_tracer_concurrent_same_input`, plus any small production seam needed to make them deterministic. Cover malformed request returns a plain pre-stream Status; an evidence-bearing valid request event ordering and one terminal; pre-open cancellation; native client-disconnect cancellation propagated through the runner per D-16; cancellation while the inline remainder is pending; task/channel cleanup; two identical inputs get independent trace/sequence state and do not cross-talk when run concurrently; five or fewer valid checkpoint boundaries retain full accumulated snapshot payloads per D-28 and sequence order; and the evidence-bearing terminal fixture asserts populated `answer`, `citations`, `session_id`, non-unspecified `answer_basis`, `structured_citations`, a correctly mapped `notices` field that may be empty, and `snapshot` values with `snapshot` not merged into `notices`. Add an explicit zero-evidence assertion for the valid empty-answer/empty-citation/unspecified-basis path. The Rust envelope adapter retains an explicit pending/overflow ownership result without claiming Go `CheckpointDispatcher` behavior. Keep transport identity semantics precise: d1-style trailer metadata is only used by existing pre-stream infrastructure paths that actually emit it, never as a claim that malformed validation carries trailers. Record D-12's no-backoff retry disposition in the test contract for later generation tests.</action>
  <verify>
    <automated>$list = &amp; cargo test --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $required = @('query_rag_tracer_happy_path','workflow_tracer_cancellation','checkpoint_envelope_preserves_pending_ownership','workflow_tracer_concurrent_same_input'); foreach ($name in $required) { $pattern = '(?:^|::)' + [regex]::Escape($name) + ': test$'; $hits = @($list | Select-String -Pattern $pattern); if ($hits.Count -ne 1) { throw "Missing exact focused Rust registration: $name" } }</automated>
    <automated>$required = @('query_rag_tracer_happy_path','workflow_tracer_cancellation','checkpoint_envelope_preserves_pending_ownership','workflow_tracer_concurrent_same_input'); foreach ($name in $required) { $output = &amp; cargo test --manifest-path engine/Cargo.toml --locked $name -- --nocapture; $exit = $LASTEXITCODE; if ($exit -ne 0) { exit $exit } }</automated>
  </verify>
  <acceptance_criteria>Focused tests are registered with nonzero matches and prove pre-stream validation, typed event order, one terminal, cooperative cancellation, same-input isolation, concurrent context isolation, ordered full checkpoint envelope handoff, and explicit Rust-side pending/overflow ownership semantics. No test depends on the not-yet-migrated Go generated API.</acceptance_criteria>
  <done>The tracer's observable and edge contracts are executable and provide a stable vertical slice for every later expansion plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> Rust tonic stream | Untrusted request fields and client cancellation cross into the workflow runner. |
| Rust workflow -> generated protobuf | Internal state and failure details are serialized into a client-visible event envelope. |
| Rust checkpoint event -> Go dispatcher | Full snapshot payloads cross the runtime boundary and must remain bounded, ordered, and non-blocking to query progress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | QueryRAG request validation | high | mitigate | Validate and mint identity before stream creation; malformed input is a plain typed Status. |
| T-05-02 | Information disclosure | WorkflowEvent failure/checkpoint payload | medium | mitigate | Use D-22 safe human messages, preserve typed kinds, and keep provider/raw secrets out of event fields. |
| T-05-03 | Denial of service | runner/event/checkpoint channels | high | mitigate | Bound channels, select cancellation first, and require pending/overflow delivery semantics instead of silent loss. |
| T-05-04 | Spoofing | trace_id/session_id | medium | mitigate | Derive trace_id from validated request correlation_id and keep session_id distinct per D-29. |
| T-05-SC | Tampering | Cargo dependency transition | high | mitigate | Use the approved package-legitimacy audit for existing `tokio-util`/`tokio-stream` dependencies and run locked generation/build checks. |

The spec-less prohibition fallback is flagged-unverified in `must_haves.prohibitions`; no descriptor-bearing prohibition check is claimed.
</threat_model>

<verification>
Run the focused Rust workflow tests, the locked engine compile, and the focused test-registration guard. Do not use the pre-migration Go runtime suite as a Phase 5-01 completion signal; 05-06 owns the generated Go landing and complete Go migration.

`05-VALIDATION.md` is intentionally a populated pre-execution draft with `nyquist_compliant: false`; the executor must run the exact task map and `/gsd-validate-phase 05` must record evidence before changing that lifecycle state.
</verification>

<success_criteria>
The validated Rust server-streaming tracer runs one reformulation-to-inline-remainder path, emits typed lifecycle/terminal events, handles cancellation and malformed input predictably, preserves independent concurrent workflow state, and exposes a reliable checkpoint handoff contract for the next runtime boundary.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-01-SUMMARY.md` when done.
</output>
