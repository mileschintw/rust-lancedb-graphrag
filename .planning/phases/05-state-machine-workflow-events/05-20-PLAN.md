---
phase: 05-state-machine-workflow-events
plan: 20
type: execute
wave: 18
depends_on: [05-13, 05-14, 05-16, 05-18, 05-19, 05-22]
files_modified:
  - engine/src/generation/mod.rs
  - engine/src/generation/openrouter.rs
  - engine/src/workflow/node.rs
  - engine/src/workflow/runner.rs
  - engine/src/workflow/nodes/generate.rs
  - engine/src/tests/workflow_phase5.rs
autonomous: true
gap_closure: true
requirements: [ORCH-02, ORCH-03]

must_haves:
  truths:
    - Capability preflight runs once as a bootstrap operation before GenerateAnswer's 65000ms node timer starts.
    - Two 30000ms provider attempts can complete after a 5000ms preflight without the node timer cutting off the mandated retry.
    - A 4999ms pre-deadline operation is not classified as a timeout and the happy-path receiver cannot wait forever.
    - The nine production binary filters are listed and executed only after the ordered 05-18 introducing, 05-15 post-cfg(test), and 05-16 post-production-BM25 `cargo test --bin engine --manifest-path engine/Cargo.toml --locked --no-run` gates; no binary `--list` or `--exact` command in this downstream task precedes those gates.
    - The derived configured workflow budget accounts for the five workflow node timers plus the separate preflight bootstrap, and the 102000ms figure is explicitly recorded as a non-enforced whole-workflow bound.
  artifacts:
    - engine/src/generation/mod.rs exposes a default preparation hook that does not break FakeGenerator or other Generator implementations.
    - engine/src/generation/openrouter.rs performs the 05-13 capability preflight through the preparation hook and leaves chat attempts at the 30-second provider boundary.
    - engine/src/workflow/node.rs and engine/src/workflow/runner.rs run preparation before the timed node body.
    - engine/src/tests/workflow_phase5.rs contains bootstrap, worst-case, pre-deadline, and bounded-drain regressions.
  key_links:
    - OpenRouterGenerator::prepare -> GenerateAnswerNode preparation -> WorkflowRunner bootstrap -> timed GenerateAnswer run
    - 97s pre-preflight node-budget arithmetic + 5000ms additive capability preflight -> a derived, non-enforced 102000ms whole-workflow bound; the 65000ms generation-node timer is `2 x 30000ms` attempts plus 5000ms inter-attempt slack, not the separate preflight.
    - 4999ms regression -> runner timeout boundary
    - happy-path spawned task -> bounded receiver drain -> AbortOnDrop cleanup
---

<objective>
Separate capability preflight from the GenerateAnswer node timer and prove the complete two-attempt path fits the configured node timers while recording the derived whole-workflow bound.

Purpose: Close the verification derivation gap where a 5-second preflight inside a 65-second node timer could consume the slack required for the locked retry, and close the receiver-drain liveness gap.
Output: A preparation/bootstrap seam, worst-case paused-clock timing proof, exact 4999ms boundary proof, and bounded happy-path test cleanup.
</objective>

## Artifacts this phase produces

- Generator and Node preparation hooks with runner bootstrap timing.
- Paused-clock worst-case, 4999ms boundary, and bounded happy-path receiver tests.

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/config.json
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-PATTERNS.md
@.planning/phases/05-state-machine-workflow-events/05-09-PLAN.md
@.planning/phases/05-state-machine-workflow-events/05-13-PLAN.md
@.planning/phases/05-state-machine-workflow-events/05-19-PLAN.md
@engine/src/generation/mod.rs
@engine/src/generation/openrouter.rs
@engine/src/workflow/node.rs
@engine/src/workflow/runner.rs
@engine/src/workflow/nodes/generate.rs
@engine/src/tests/workflow_phase5.rs
</context>
<tasks>

<task type="tracer">
  <name>Task 1: Bootstrap generation capability before the node timer</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; 05-CONTEXT.md; 05-08-PLAN.md; 05-09-PLAN.md; 05-13-PLAN.md; 05-18-PLAN.md; engine/src/generation/mod.rs; engine/src/generation/openrouter.rs; engine/src/workflow/node.rs; engine/src/workflow/runner.rs; engine/src/workflow/nodes/generate.rs; engine/src/tests/workflow_phase5.rs; engine/src/tests/workflow_phase5_production.rs</read_first>
  <files>engine/src/generation/mod.rs, engine/src/generation/openrouter.rs, engine/src/workflow/node.rs, engine/src/workflow/runner.rs, engine/src/workflow/nodes/generate.rs, engine/src/tests/workflow_phase5.rs</files>
  <action>Add a default asynchronous Generator preparation hook returning success so FakeGenerator and all existing generators remain source-compatible. Implement OpenRouterGenerator preparation as the sole invocation of 05-13's dedicated 5000ms successful-only capability-preflight operation; call it once per generator instance before the node timer, and remove only the preflight request from `execute_one_call`, leaving that method responsible for one 30000ms chat attempt. Do not delete or silently bypass capability validation. Add a default Node preparation hook returning success, make GenerateAnswerNode delegate it to its generator, and make WorkflowRunner run preparation after NodeStarted but before starting the node elapsed timer and before applying the 65000ms GenerateAnswer timeout; preparation failures must use the existing typed NodeError/retryability mapping and cancellation path. Add workflow_phase5_generation_preflight_bootstrap_tracer with a deterministic local generator that records preparation before the first generation attempt and proves the generated output reaches context through the runner. Preserve D-11, D-12, D-13, D-15, D-17, and the 05-09-owned configuration values; do not add a second provider, a retry event, or a new workflow metadata field. Pattern reference: follow the `engine/src/workflow/runner.rs` and `engine/src/workflow/nodes/generate.rs` sections of `05-PATTERNS.md` for per-attempt timeout wrapping, generation-node ownership, and typed provider error mapping. This resolves the review ambiguity by making 05-20's Node::prepare path the only preflight caller and 05-13's adapter operation the only preflight implementation.</action>
  <acceptance_criteria>
    - Generator and Node preparation hooks compile for existing fake and production implementations.
    - Node::prepare has a default no-op success implementation for the other four nodes, while GenerateAnswerNode is the only workflow node that delegates preparation to its generator.
    - OpenRouter capability preflight runs through preparation, not inside either generation attempt, with its 5000ms adapter deadline and successful-only cache intact.
    - The GenerateAnswer node timer covers the node body and two provider attempts, while preparation occurs before that timer starts.
    - The tracer proves preparation precedes generation and successful output reaches WorkflowContext.
  </acceptance_criteria>
  <verify>
    <automated>$name = 'workflow_phase5_generation_preflight_bootstrap_tracer'; $list = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $matches = @($list | Select-String -Pattern ('(^|::)' + [regex]::Escape($name) + ': test$')); if ($matches.Count -ne 1) { throw 'Generation bootstrap tracer registration count mismatch' }; $run = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $name --nocapture; $runExit = $LASTEXITCODE; if ($runExit -ne 0) { exit $runExit }</automated>
  </verify>
  <done>One production-quality generation path prepares provider capabilities before the timed node body and completes through the existing GenerateAnswer event path.</done>
</task>

<task type="auto">
  <name>Task 2: Prove worst-case timing, boundary safety, and bounded test cleanup</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; 05-CONTEXT.md; 05-08-PLAN.md; 05-09-PLAN.md; 05-13-PLAN.md; 05-18-PLAN.md; 05-19-PLAN.md; engine/src/generation/mod.rs; engine/src/workflow/node.rs; engine/src/workflow/runner.rs; engine/src/workflow/nodes/generate.rs; engine/src/tests/workflow_phase5.rs; engine/src/tests/workflow_phase5_production.rs</read_first>
  <files>engine/src/workflow/runner.rs, engine/src/workflow/node.rs, engine/src/workflow/nodes/generate.rs, engine/src/tests/workflow_phase5.rs</files>
  <precondition>The 05-18 introducing binary test-target compile gate, the 05-15 post-cfg(test) rerun, and the 05-16 post-handoff rerun `cargo test --bin engine --manifest-path engine/Cargo.toml --locked --no-run` have passed in that order after their owned migrations; this task's binary `--list` and `--exact` commands run only after all three gates.</precondition>
  <action>Keep the four binary-owned 05-08 production regression tests in engine/src/tests/workflow_phase5_production.rs; this plan only re-runs them after the generation/timing changes and does not move their registration into the library target. Because 05-18 owns `engine/src/tests.rs` registration and 05-13/05-16 already prove their owned generation and BM25 behavior, this downstream task performs the nine exact production-binary runs for `workflow_phase5_production_five_node`, `workflow_phase5_production_dependencies_are_real`, `workflow_phase5_generation_retry_tracer`, `workflow_phase5_generation_retry_exhausted`, `workflow_phase5_nodekind_tracer`, `workflow_phase5_nodekind_dispatch`, `workflow_phase5_nodekind_exhaustive`, `workflow_phase5_production_context_population`, and `workflow_phase5_production_reachability` only after both no-run gates. Add workflow_phase5_generation_preflight_worst_case_budget with a paused Tokio clock and a local generator whose preparation consumes 5000ms, attempt one consumes 30000ms and returns a transient timeout, and attempt two consumes 30000ms and succeeds. Advance the clock in explicit stages, assert the preflight completes before the generation timer starts, assert exactly two attempts and a successful terminal node result, and assert the 65000ms generation-node budget is `30000 + 30000 + 5000` where the 5000ms is inter-attempt slack. Record that the 97-second pre-preflight workflow arithmetic plus the separate 5000ms preflight yields a derived, non-enforced 102000ms whole-workflow bound unless a real global workflow deadline is added; the test proves component arithmetic only and must not claim that `runner.rs` enforces the sum. Per D-17, D-18, D-19, and D-21, explicitly accept the residual lifetime risk: gateway `/rag/query` remains outside the legacy chi 60-second middleware, the request context/client disconnect is the only whole-request cancellation signal, node timers remain independent, and no new global workflow deadline or resume mechanism is introduced in this phase. Add workflow_phase5_reformulate_predeadline_4999ms_no_timeout with a deterministic 4999ms operation and an explicit no-timeout assertion, and add workflow_phase5_retrieve_predeadline_9999ms_no_timeout with a deterministic 9999ms retrieval operation and an explicit no-timeout assertion so both boundaries are covered. Update workflow_phase5_happy_path to drain its event receiver under a five-second Tokio timeout while retaining AbortOnDrop cleanup, so a missing close cannot hang the test indefinitely. Keep the existing 05-09 live 7000ms proof separate from these paused-clock semantic tests and document that distinction in the test names/comments; the worst-case timing test must use paused Tokio time or equivalent deterministic advancement and must not sleep for real 65-second intervals. Pattern reference: follow the `engine/src/workflow/runner.rs` timeout-dispatch section and `Shared Patterns — TOML + env-var config override convention` in `05-PATTERNS.md` for consuming the 05-09 values without introducing a second timeout source.</action>
  <acceptance_criteria>
    - The worst-case regression proves the additive 5000ms preflight plus the 97-second pre-preflight node-budget arithmetic and records 102000ms as a derived, non-enforced whole-workflow bound; it does not claim a global workflow deadline, and it does not hit the 65000ms generation-node timeout because the node budget's 5000ms slack is distinct from the preflight.
    - The plan records the accepted risk that independent node timers and the uncapped SSE route do not enforce the derived 102000ms sum as one whole-workflow wall-clock ceiling unless a real global deadline is added.
    - The worst-case regression observes exactly two generation attempts, no third attempt, and a successful second attempt.
    - The 4999ms reformulation operation and the 9999ms retrieval operation complete without timeout classification at their 5000ms and 10000ms boundaries respectively; these are semantic paused-clock checks, not the live overlay proof.
    - workflow_phase5_happy_path has a five-second bounded receiver drain and still aborts a leaked task on test teardown.
    - The nine binary registration/exact-filter checks are downstream of the already-passed 05-18 introducing, 05-15 post-cfg(test), and 05-16 post-handoff `--no-run` gates; this task does not establish or weaken those compile proofs.
  </acceptance_criteria>
  <verify>
    <automated>$name = 'workflow_phase5_generation_preflight_worst_case_budget'; $list = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $matches = @($list | Select-String -Pattern ('(^|::)' + [regex]::Escape($name) + ': test$')); if ($matches.Count -ne 1) { throw 'Worst-case timing registration count mismatch' }; $run = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $name --nocapture; $runExit = $LASTEXITCODE; if ($runExit -ne 0) { exit $runExit }</automated>
    <automated>$name = 'workflow_phase5_reformulate_predeadline_4999ms_no_timeout'; $list = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $matches = @($list | Select-String -Pattern ('(^|::)' + [regex]::Escape($name) + ': test$')); if ($matches.Count -ne 1) { throw 'Pre-deadline timing registration count mismatch' }; $run = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $name --nocapture; $runExit = $LASTEXITCODE; if ($runExit -ne 0) { exit $runExit }</automated>
    <automated>$name = 'workflow_phase5_happy_path'; $list = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $matches = @($list | Select-String -Pattern ('(^|::)' + [regex]::Escape($name) + ': test$')); if ($matches.Count -ne 1) { throw 'Happy-path registration count mismatch' }; $run = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $name --nocapture; $runExit = $LASTEXITCODE; if ($runExit -ne 0) { exit $runExit }</automated>
    <automated>$name = 'workflow_phase5_retrieve_predeadline_9999ms_no_timeout'; $list = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $matches = @($list | Select-String -Pattern ('(^|::)' + [regex]::Escape($name) + ': test$')); if ($matches.Count -ne 1) { throw 'Retrieval pre-deadline timing registration count mismatch' }; $run = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $name --nocapture; $runExit = $LASTEXITCODE; if ($runExit -ne 0) { exit $runExit }</automated>
    <automated>$handoff = cargo test --bin engine --manifest-path engine/Cargo.toml --locked --no-run; $handoffExit = $LASTEXITCODE; if ($handoffExit -ne 0) { exit $handoffExit }</automated>
    <automated>$names = @('workflow_phase5_production_five_node','workflow_phase5_production_dependencies_are_real','workflow_phase5_generation_retry_tracer','workflow_phase5_generation_retry_exhausted','workflow_phase5_nodekind_tracer','workflow_phase5_nodekind_dispatch','workflow_phase5_nodekind_exhaustive','workflow_phase5_production_context_population','workflow_phase5_production_reachability'); $listed = cargo test --bin engine --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; foreach ($name in $names) { $pattern = '(^|::)' + [regex]::Escape($name) + ': test$'; $matches = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -match $pattern }); if ($matches.Count -ne 1) { throw "exact binary registration count mismatch for $name" }; $run = cargo test --bin engine --manifest-path engine/Cargo.toml --locked -- --exact $name --nocapture; $runExit = $LASTEXITCODE; if ($runExit -ne 0) { exit $runExit } }</automated>
  </verify>
  <done>Paused-clock timing proves the mandated retry survives preflight, both reformulation/retrieval boundaries are safe, and the existing happy-path test cannot block forever on receiver closure.</done>
</task>

</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow runner to provider preparation | A capability request can delay or fail before generation starts. |
| GenerateAnswer to provider attempts | Retry timing controls external request volume and node completion. |
| Tokio timer to test harness | Paused-clock tests can falsely pass or hang if scheduling and receiver cleanup are not bounded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-20-01 | Denial of service | Capability bootstrap | critical | mitigate | Run the dedicated preflight deadline before the node timer, preserve cancellation, and assert the preflight cannot consume the generation-node retry window. |
| T-05-20-02 | Denial of service | GenerateAnswer retry loop | critical | mitigate | Bound provider attempts at 30000ms, allow exactly one retry, assert two attempts in the paused-clock test, and reject a third attempt. |
| T-05-20-03 | Tampering | Workflow timeout configuration | high | mitigate | Consume the 05-09-owned seven-key configuration, assert the 65000ms generation-node budget with inter-attempt slack, and record the 102000ms figure as a derived, non-enforced whole-workflow bound from the 97-second pre-preflight total plus the additive 5000ms capability preflight; explicitly accept that no global deadline enforces this sum unless one is added, without introducing a competing timeout value. |
| T-05-20-04 | Denial of service | workflow_phase5_happy_path receiver | medium | mitigate | Wrap receiver draining in a five-second timeout and retain AbortOnDrop cleanup for the spawned runner. |
| T-05-20-05 | Denial of service | Gateway request and workflow lifetime | medium | accept | Per D-17, D-18, D-19, and D-21, `/rag/query` remains outside the legacy 60-second chi middleware and has no new global workflow deadline or resume mechanism; request-context/client-disconnect cancellation and independent node timers are the accepted whole-request lifetime boundary for this phase, while 05-20 proves only the derived component arithmetic. |
| T-05-20-SC | Tampering | npm/pip/cargo installs | high | accept | This plan adds no package-manager dependency or install task; the standard slopcheck register is not applicable to timer-boundary changes. |
</threat_model>

<verification>
- The bootstrap tracer, worst-case budget test, 4999ms pre-deadline test, and bounded happy-path test each have exact library registration guards and pass individually.
- The paused-clock worst-case path proves the additive preflight plus the 97-second pre-preflight workflow arithmetic and records the resulting 102000ms derived, non-enforced bound; it does not report a 65000ms generation-node timeout and does not claim global whole-workflow deadline enforcement.
- OpenRouter's preflight is not invoked from execute_one_call, 05-13 remains the sole preparation implementation/cache owner, this plan is the sole Node::prepare invocation owner, and each chat attempt remains independently bounded at 30000ms.
- The existing live 05-09 7000ms proof remains the authority for the production overlay; this plan adds semantic timing evidence only.
</verification>

<success_criteria>
The locked preflight, retry, and timeout contracts are executable without deadline interference: preparation occurs outside GenerateAnswer's node timer, the 97-second pre-preflight workflow arithmetic plus additive 5000ms preflight is recorded as a derived, non-enforced 102-second whole-workflow bound, two worst-case attempts retain the 65000ms node budget with inter-attempt slack, the 4999ms boundary is safe, and the happy-path test is liveness-bounded. The accepted risk is explicit: independent node timers and the uncapped SSE route do not enforce that derived sum as one global wall-clock deadline unless a real whole-workflow deadline is added.
</success_criteria>

<output>
Create .planning/phases/05-state-machine-workflow-events/05-20-SUMMARY.md when done.
</output>
