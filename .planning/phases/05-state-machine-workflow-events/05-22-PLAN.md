---
phase: 05-state-machine-workflow-events
plan: 22
type: execute
wave: 17
depends_on:
  - 05-08
  - 05-10
  - 05-13
  - 05-14
  - 05-16
  - 05-18
  - 05-23
files_modified:
  - engine/src/workflow/nodes/assemble_prompt.rs
  - engine/src/workflow/nodes/generate.rs
  - engine/src/workflow/mod.rs
  - engine/src/tests.rs
  - engine/src/tests/workflow_phase5_production.rs
autonomous: true
gap_closure: true
requirements:
  - ORCH-01
  - ORCH-02
  - ORCH-03
  - ORCH-04
  - ORCH-05
must_haves:
  truths:
    - "The typed GraphFactBlock vector produced by the production graph node reaches both prompt assembly and the GenerationRequest used by the provider adapter." 
    - "The production regression invokes the same five-node builder as query_rag and proves D-06 lifecycle order, D-01/D-02 answer cardinality, D-03 zero-evidence short-circuiting, and D-09 graph degradation." 
    - "Production handler assertions cannot pass on the retired one-node or sink-less path because they require the exact lifecycle and terminal event contract." 
    - "The ORCH-05 pass-through reformulator remains reachable through the production state-machine entry point." 
  artifacts:
    - path: "engine/src/workflow/nodes/assemble_prompt.rs"
      provides: "Prompt assembly consumes WorkflowContext.graph_facts rather than an empty graph-fact argument."
    - path: "engine/src/workflow/nodes/generate.rs"
      provides: "GenerationRequest carries the same typed graph facts that prompt assembly received."
    - path: "engine/src/tests/workflow_phase5_production.rs"
      provides: "Binary-target production-builder reachability and provider-request context regressions."
    - path: "engine/src/tests.rs"
      provides: "Direct production-handler assertions for exact node/event behavior."
  key_links:
    - "ExtractGraphContext -> WorkflowContext.graph_facts -> AssemblePrompt -> pack_evidence_and_graph_prompt"
    - "WorkflowContext.graph_facts -> GenerateAnswer -> GenerationRequest -> provider boundary"
    - "query_rag -> build_production_workflow -> five NodeStarted events -> AnswerChunk -> FinalAnswer -> WorkflowCompleted"

---

<objective>
Complete the production typed graph-fact handoff and prove the real query_rag path reaches every locked workflow behavior.

Purpose: 05-08 establishes the production builder and authoritative graph facts, while 05-18 establishes the target-aware test seam. This plan owns the prompt/generation boundary and the production-builder regression assertions that must observe the actual provider request and terminal event stream.
Output: Typed graph facts in prompt and generation requests, exact production handler assertions, and a binary-target reachability regression.
</objective>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-preconditions.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-reversibility.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-PATTERNS.md
@.planning/phases/05-state-machine-workflow-events/05-VERIFICATION.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEWS.md
@.planning/phases/05-state-machine-workflow-events/05-08-PLAN.md
@.planning/phases/05-state-machine-workflow-events/05-18-PLAN.md
@engine/src/workflow/nodes/assemble_prompt.rs
@engine/src/workflow/nodes/generate.rs
@engine/src/tests.rs
@engine/src/tests/workflow_phase5_production.rs
@AGENTS.md
@rust-guidelines.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace typed graph facts through prompt assembly and generation</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; 05-CONTEXT.md; 05-08-PLAN.md; 05-10-PLAN.md; 05-18-PLAN.md; engine/src/workflow/nodes/assemble_prompt.rs; engine/src/workflow/nodes/generate.rs; engine/src/tests/workflow_phase5_production.rs</read_first>
  <files>engine/src/workflow/nodes/assemble_prompt.rs, engine/src/workflow/nodes/generate.rs, engine/src/tests/workflow_phase5_production.rs</files>
  <action>Complete the D-06/D-09 graph-fact handoff by passing the authoritative `WorkflowContext.graph_facts` vector into `pack_evidence_and_graph_prompt` from AssemblePrompt, then copy the same typed vector into `GenerationRequest` in GenerateAnswer before the provider adapter runs. Preserve D-01 full-answer AnswerChunk and D-02 distinct FinalAnswer semantics, D-03 zero-evidence short-circuiting, D-09 graph degradation notices, D-30 exclusion of generic workflow metadata, and D-31 exclusion of extra tracing spans. Remove response-shaped fabrication text from the prompt/generation production path so prompt and answer content come only from real evidence and provider output. Extend `workflow_phase5_production_context_population` to invoke the same `build_production_workflow` used by `query_rag`, capture the actual GenerationRequest at the local provider boundary, and assert that the seeded graph marker is present in the typed facts/request messages rather than only in an intermediate assembled prompt. Return the existing typed prompt failure when required evidence is absent; do not fabricate evidence, answer, or citation content. Keep the production adapter path real and retain the ORCH-05 pass-through reformulator.</action>
  <action>Consume the 05-10 event-delivery and snapshot/context contract after its Wave 16 landing; keep this task limited to the provider-facing graph-fact transfer and production reachability expansion, and do not reopen or duplicate 05-10's terminal, ordinal, or snapshot implementation.</action>
  <acceptance_criteria>
    - AssemblePrompt passes `WorkflowContext.graph_facts` to prompt packing and does not supply a literal empty graph-fact vector on the production path.
    - GenerateAnswer places the same typed graph-fact vector in GenerationRequest before the provider call.
    - `workflow_phase5_production_context_population` observes the graph marker at the actual provider-request boundary through the production builder.
    - Prompt and generation sources contain no response-shaped fabrication text; missing evidence and failed generation remain typed failures.
    - Zero evidence exits before prompt/generation, graph degradation continues with its typed notice, and no fabricated response-shaped success is introduced.
  </acceptance_criteria>
  <verify>
    <automated>$assemble = Get-Content 'engine/src/workflow/nodes/assemble_prompt.rs' -Raw; $generate = Get-Content 'engine/src/workflow/nodes/generate.rs' -Raw; $production = Get-Content 'engine/src/tests/workflow_phase5_production.rs' -Raw; if ($assemble -notmatch 'pack_evidence_and_graph_prompt[\s\S]{0,1000}graph_facts') { throw 'AssemblePrompt does not pass typed graph facts to prompt packing' }; if ($generate -notmatch 'GenerationRequest[\s\S]{0,1600}graph_facts\s*=') { throw 'GenerateAnswer does not transfer graph facts into GenerationRequest' }; if ($assemble -match 'pack_evidence_and_graph_prompt\([^)]*&\[\]') { throw 'production prompt packing still receives an empty graph-fact vector' }; foreach ($needle in @('Content of chunk','Answer for {')) { if ($assemble.Contains($needle) -or $generate.Contains($needle)) { throw "fabrication text remains in prompt/generation production sources: $needle" } }; if ($production -notmatch 'workflow_phase5_production_context_population[\s\S]{0,5000}(GraphFactBlock|GenerationRequest|graph_facts)') { throw 'production context test does not inspect the provider-request graph facts' }</automated>
    <automated>$binaryNoRun = cargo test --bin engine --manifest-path engine/Cargo.toml --locked --no-run; $binaryNoRunExit = $LASTEXITCODE; if ($binaryNoRunExit -ne 0) { exit $binaryNoRunExit }</automated>
    <automated>$name = 'workflow_phase5_production_context_population'; $listed = cargo test --bin engine --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $pattern = '(^|::)' + [regex]::Escape($name) + ': test$'; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -match $pattern }); if ($registered.Count -ne 1) { throw 'production context registration count mismatch' }; $run = cargo test --bin engine --manifest-path engine/Cargo.toml --locked -- --exact $name --nocapture; $runExit = $LASTEXITCODE; if ($runExit -ne 0) { exit $runExit }</automated>
  </verify>
  <done>The real production provider request receives the typed graph facts emitted by the graph node, with prompt and generation using one authoritative vector.</done>
  <reversibility rating="costly">The provider-request shape is a cross-layer production contract; keep the graph fact carrier typed and the boundary test attached to the real builder so reverting it does not leave a hidden string-only path.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Expand exact production reachability and terminal assertions</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; 05-CONTEXT.md; 05-08-PLAN.md; 05-10-PLAN.md; 05-14-PLAN.md; 05-18-PLAN.md; engine/src/main.rs; engine/src/workflow/runner.rs; engine/src/tests.rs; engine/src/tests/workflow_phase5_production.rs</read_first>
  <files>engine/src/workflow/mod.rs, engine/src/tests.rs, engine/src/tests/workflow_phase5_production.rs</files>
  <action>Add `workflow_phase5_production_reachability` to the binary-owned production suite and strengthen the existing direct `service.query_rag` assertions (`query_rag_stream`, `query_rag_tracer`, and their companion call sites) so they require the exact D-06 lifecycle order, one full validated AnswerChunk, one distinct FinalAnswer, and one successful WorkflowCompleted event. Assert evidence-bearing input, D-03 zero evidence, graph degradation with an explicit notice, typed retrieval failure, and the ORCH-05 pass-through reformulator through the same production builder used by `query_rag`. Finalize the inline compatibility helper's no-generator branch as typed `LlmGenerationFailed` and update its existing `engine/src/tests.rs` callers to assert that failure, with no fabricated success event or response-shaped fallback. Add source guards for the retired inline remainder, the named builder, a non-empty `self.nodes` dense adapter, the absence of an empty graph-fact argument in the production prompt/generation handoff, and the absence of fabrication text across the helper/prompt/generation production sources. Keep all assertions target-correct: these cases remain binary-owned and do not re-register or move generic fake-port tests from the library target.</action>
  <acceptance_criteria>
    - The production reachability test fails when any required node is absent or out of D-06 order.
    - Evidence-bearing input emits exactly one full AnswerChunk, then a distinct FinalAnswer and successful completion; failure paths do not fabricate those events.
    - Zero evidence skips prompt and generation, graph degradation remains successful with a typed notice, and a hard retrieval error remains a typed failure.
    - The inline compatibility helper and its existing callers use typed `LlmGenerationFailed` on missing generation capability, with no fabricated answer-shaped success or fabrication literal in the helper/prompt/generation sources.
    - Production handler assertions prove the real builder path rather than a direct library-only runner, and the ORCH-05 pass-through remains reachable.
  </acceptance_criteria>
  <verify>
    <automated>$main = Get-Content 'engine/src/main.rs' -Raw; $query = [regex]::Match($main, '(?s)async fn query_rag\b.*?(?=\n\s*(?:pub\s+)?(?:async\s+)?fn\b|\z)').Value; if ([string]::IsNullOrWhiteSpace($query)) { throw 'query_rag production region missing' }; if ($main.Contains('execute_inline_query_rag_remainder') -or $query -match 'run_inline_prompt_generation_remainder') { throw 'retired inline production remainder remains reachable' }; $builder = [regex]::Match($main, '(?s)fn build_production_workflow\b.*?(?=\n\s*(?:pub\s+)?(?:async\s+)?fn\b|\z)').Value; foreach ($needle in @('self.nodes','ReformulateQueryNode','ExtractGraphContextNode','RetrieveHybridNode','AssemblePromptNode','GenerateAnswerNode')) { if (-not $builder.Contains($needle)) { throw "production builder guard missing $needle" } }; $workflow = Get-Content 'engine/src/workflow/mod.rs' -Raw; $assemble = Get-Content 'engine/src/workflow/nodes/assemble_prompt.rs' -Raw; $generate = Get-Content 'engine/src/workflow/nodes/generate.rs' -Raw; if ($workflow -notmatch 'LlmGenerationFailed') { throw 'inline compatibility helper does not return typed generation failure' }; foreach ($needle in @('Content of chunk','Answer for {')) { if ($workflow.Contains($needle) -or $assemble.Contains($needle) -or $generate.Contains($needle)) { throw "fabrication text remains in production workflow sources: $needle" } }; $tests = Get-Content 'engine/src/tests.rs' -Raw; foreach ($needle in @('query_rag_stream','query_rag_tracer','WorkflowCompleted','AnswerChunk','ExtractGraphContext','RetrieveHybrid','AssemblePrompt','GenerateAnswer','LlmGenerationFailed')) { if (-not $tests.Contains($needle)) { throw "production-handler assertion guard missing $needle" } }; $production = Get-Content 'engine/src/tests/workflow_phase5_production.rs' -Raw; if ($production -notmatch 'workflow_phase5_production_reachability') { throw 'production reachability test is missing' }</automated>
    <automated>$binaryNoRun = cargo test --bin engine --manifest-path engine/Cargo.toml --locked --no-run; $binaryNoRunExit = $LASTEXITCODE; if ($binaryNoRunExit -ne 0) { exit $binaryNoRunExit }; $names = @('workflow_phase5_production_five_node','workflow_phase5_production_dependencies_are_real','workflow_phase5_production_context_population','workflow_phase5_production_reachability'); $listed = cargo test --bin engine --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; foreach ($name in $names) { $pattern = '(^|::)' + [regex]::Escape($name) + ': test$'; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -match $pattern }); if ($registered.Count -ne 1) { throw "production registration count mismatch for $name" }; $run = cargo test --bin engine --manifest-path engine/Cargo.toml --locked -- --exact $name --nocapture; $runExit = $LASTEXITCODE; if ($runExit -ne 0) { exit $runExit } }</automated>
  </verify>
  <done>The binary-target production suite proves the real five-node path, event cardinality, zero-evidence and degradation behavior, typed failures, and pass-through reformulation without relying on the retired inline path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WorkflowContext to prompt/generation adapters | Typed graph facts and evidence cross into provider-facing request construction. |
| Production query handler to binary test harness | Untrusted request validation, node ordering, event cardinality, and failure behavior cross the production test boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-22-01 | Tampering | GraphFactBlock prompt/generation handoff | high | mitigate | Pass one typed WorkflowContext vector into both prompt packing and GenerationRequest, then assert the marker at the actual provider boundary. |
| T-05-22-02 | Spoofing | Production node/event assertions | high | mitigate | Invoke the named production builder and require exact D-06 NodeStarted order plus D-01/D-02 event cardinality in the binary target. |
| T-05-22-03 | Information disclosure | Provider-facing workflow context | medium | mitigate | Preserve D-30/D-31 exclusions and assert typed facts without adding generic metadata or tracing spans. |
| T-05-22-04 | Denial of service | Zero-evidence and graph-degradation branches | medium | mitigate | Require zero-evidence short-circuiting and bounded typed degradation instead of entering prompt/generation or fabricating output. |
| T-05-22-SC | Tampering | npm/pip/cargo installs | high | accept | This plan adds no package-manager dependency or install task; the slopcheck register is not applicable. |
</threat_model>

<verification>
- Source guards prove one production builder, five D-06 nodes, typed graph-fact transfer, and no reachable inline remainder.
- The binary test target passes its no-run gate before exact production filters, and all four named production regressions execute in the binary target.
- The production request and event assertions cover D-01, D-02, D-03, D-06, D-09, D-18, D-29, D-30, and D-31 without moving generic fake-port tests across targets.
</verification>

<success_criteria>
The production query path carries graph facts from ExtractGraphContext through prompt and generation, and the binary-target regression suite proves the exact five-node workflow, terminal event contract, degradation behavior, zero-evidence short-circuit, typed failure behavior, and ORCH-05 pass-through.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-22-SUMMARY.md` when done.
</output>
