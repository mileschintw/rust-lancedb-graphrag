---
phase: 05-state-machine-workflow-events
plan: 10
type: execute
wave: 14
depends_on:
  - 05-14
  - 05-15
  - 05-16
files_modified:
  - engine/src/workflow/runner.rs
  - engine/src/workflow/events.rs
  - engine/src/workflow/mod.rs
  - engine/src/tests/workflow_phase5.rs
autonomous: true
gap_closure: true
requirements:
  - ORCH-02
  - ORCH-03
  - ORCH-04
must_haves:
  truths:
    - "Workflow event delivery handles every send outcome: client-visible lifecycle, answer, and terminal events are delivered or cancellation-terminated, while checkpoint events retain explicit pending ownership without blocking node execution."
    - "Terminal emission is atomic and idempotent, so a successful completion, failed completion, stream cancellation, or duplicate terminal call produces exactly one terminal event."
    - "Each outer WorkflowEvent receives one sequence ordinal and the checkpoint payload carries that same ordinal without gaps or double increments."
    - "Every checkpoint serializes the complete accumulated WorkflowContext, including graph facts, graph context, evidence, prompt, answer, citations, notices, and retrieval snapshot fields, while the client response remains checkpoint-free."
    - "Earlier graph and retrieval notices survive later successful degradation and terminal failure through the notice merge contract supplied by 05-16."
  artifacts:
    - path: "engine/src/workflow/runner.rs"
      provides: "Cancellation-aware event delivery, one-source sequence allocation, and atomic terminal emission"
    - path: "engine/src/workflow/events.rs"
      provides: "Full WorkflowContext checkpoint serialization and event envelope construction"
    - path: "engine/src/workflow/mod.rs"
      provides: "Checkpoint-free response mapping and accumulated notice/snapshot state"
    - path: "engine/src/tests/workflow_phase5.rs"
      provides: "Focused delivery, terminal-idempotence, sequence, and full-snapshot regressions"
  key_links:
    - "NodeKind dispatch from 05-14 -> typed lifecycle labels and event sink"
    - "one EventSequence allocation -> outer WorkflowEvent ordinal -> matching checkpoint inner ordinal"
    - "WorkflowContext::merge_notices from 05-16 -> failure and terminal event payloads without notice replacement"
    - "WorkflowContext -> checkpoint JSON -> Go dispatcher, while to_query_rag_response excludes checkpoint-only fields"
    - "WorkflowContext.graph_facts -> full checkpoint snapshot and the GenerateAnswer request carrier established by 05-08"
---

<objective>
Make Rust workflow event delivery and checkpoint snapshots reliable after typed dispatch is established by 05-14.

Purpose: The checker identified ignored send outcomes, a non-idempotent terminal helper, double sequence increments, overwritten notices, and incomplete checkpoint JSON. This plan owns the event/snapshot boundary only; typed NodeKind definitions and exhaustive dispatch live in 05-14, and graph/retrieval notice semantics live in 05-16.
Output: Cancellation-aware event delivery, atomic terminal emission, one-source ordinals, full accumulated snapshots, and focused event-contract coverage.
</objective>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-preconditions.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-reversibility.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-PATTERNS.md
@.planning/phases/05-state-machine-workflow-events/05-VERIFICATION.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEWS.md
@.planning/phases/05-state-machine-workflow-events/05-14-PLAN.md
@.planning/phases/05-state-machine-workflow-events/05-16-PLAN.md
@AGENTS.md
@engine/src/workflow/runner.rs
@engine/src/workflow/events.rs
@engine/src/workflow/mod.rs
@engine/src/tests/workflow_phase5.rs

## Artifacts this phase produces

- A bounded event sink with explicit send outcomes and cancellation-aware client delivery.
- An atomic/idempotent terminal-emitted guard and one-source event/checkpoint sequence ordinals.
- Full accumulated WorkflowContext checkpoint JSON that remains separate from the response DTO.
- Focused tests for backpressure, terminal duplication, notice preservation, ordinal integrity, and snapshot completeness.
- An explicit D-24/D-28 storage-cost decision retaining the complete logical snapshot, including the full embedding and evidence text, with serialized-byte evidence in the focused test.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace one event-bearing workflow through reliable delivery and terminal emission</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; 05-CONTEXT.md; 05-14-PLAN.md; 05-16-PLAN.md; engine/src/workflow/runner.rs; engine/src/workflow/events.rs; engine/src/workflow/mod.rs; engine/src/tests/workflow_phase5.rs</read_first>
  <files>engine/src/workflow/runner.rs, engine/src/workflow/events.rs, engine/src/tests/workflow_phase5.rs</files>
  <action>Refactor WorkflowEventSink so client-visible events use a cancellation-raced awaited send and every result is handled as Sent, Closed, or a typed cancellation/backpressure outcome; checkpoint events retain owned pending envelopes in a bounded nonblocking path and never silently disappear. Allocate the EventSequence ordinal once while constructing the outer WorkflowEvent and pass that exact value into checkpoint serialization. Replace the terminal helper with an atomic compare-and-set guard shared by all success, failure, cancellation, and duplicate-terminal paths, and make the guard observable in the dedicated `workflow_phase5_event_delivery_tracer` test. Preserve D-01 full validated AnswerChunk, D-02 distinct FinalAnswer, D-05 in-band failure ordering, D-15 absence of a retrying event, D-16 cancellation-first behavior, D-20 coarse lifecycle events, D-22 typed failure categories, D-27 fire-and-forget checkpoint ownership, and D-29 trace identity. Use the `05-PATTERNS.md` runner/event and nonblocking checkpoint analogs; do not add another transport protocol or put checkpoint JSON into the response DTO.</action>
  <acceptance_criteria>
    - No event-send result is ignored; client-visible delivery distinguishes Sent from Closed/cancellation and checkpoint delivery distinguishes Sent from owned Pending.
    - A full successful path emits exactly one terminal event even if completion, failure cleanup, cancellation cleanup, or a duplicate caller races to emit another terminal.
    - The tracer proves one outer sequence allocation per event and matching checkpoint ordinal on the same path.
    - Client disconnect cancellation cannot leave the runner waiting forever on a closed event receiver, and checkpoint handoff does not await database persistence.
  </acceptance_criteria>
  <verify>
    <automated>$test = 'workflow_phase5_event_delivery_tracer'; $listed = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $pattern = '(^|::)' + [regex]::Escape($test) + ': test$'; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -match $pattern }); if ($registered.Count -ne 1) { throw "exact Cargo registration missing or duplicated for $test" }; $output = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $test --nocapture; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
  </verify>
  <done>A real workflow event path delivers typed progress and terminal events with explicit ownership, cancellation handling, one-source ordinals, and exactly-one terminal behavior.</done>
  <must_haves>
    - D-01, D-02, D-05, D-15, D-16, D-20, D-22, D-27, and D-29 remain observable at the Rust event boundary.
    - Terminal-event duplication is impossible even when failure cleanup and success cleanup both attempt emission.
  </must_haves>
  <reversibility rating="costly">Event ordering and terminal cardinality are shared with the gateway wire contract; preserve existing event names while isolating ownership and sequence changes behind the sink.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Prove full snapshots, notice survival, and terminal idempotence</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; 05-CONTEXT.md; 05-16-PLAN.md; engine/src/workflow/events.rs; engine/src/workflow/mod.rs; engine/src/workflow/runner.rs; engine/src/tests/workflow_phase5.rs</read_first>
  <files>engine/src/workflow/events.rs, engine/src/workflow/mod.rs, engine/src/workflow/runner.rs, engine/src/tests/workflow_phase5.rs</files>
  <action>Extend the owned Rust regression suite with `workflow_phase5_checkpoint_full_snapshot` and `workflow_phase5_terminal_idempotence`. Serialize every accumulated WorkflowContext field at a checkpoint: session_id, trace_id, original_query, filter, variants, query_embedding, graph_context, graph_facts, vector_results, bm25_results, final_candidates, evidence_blocks, assembled_prompt, answer, citations, answer_basis, structured_citations, notices, and snapshot. Apply the explicit D-24/D-28 storage-cost decision: retain the complete logical snapshot, including every element of the 2048-dimensional query embedding and full evidence/prompt text, with no field truncation, sampling, or omission; the accepted cost is indefinite checkpoint retention for debugging and no new fetch or compaction API is added in this phase. Have the focused test record the serialized byte length and assert that full embedding cardinality and representative evidence/prompt content are present. Assert that the payload round-trips as valid JSON, preserves D-28 accumulated state and D-29 identity, and remains separate from `to_query_rag_response`. Exercise notices produced before graph degradation and before a later terminal failure, asserting the earlier entries survive and the exact `GRAPH_TIMEOUT`/`GRAPH_DEGRADED` codes supplied by 05-16 are not replaced. Assert duplicate terminal calls produce exactly one terminal event and no answer-shaped event after failure, while preserving D-03 zero-evidence success, D-24 indefinite retention, D-25 no fetch API, and D-26 Rust-free persistence ownership. Use the 05-PATTERNS.md checkpoint fidelity and event-matrix analogs; a zero-registered focused test is a failure.</action>
  <acceptance_criteria>
    - The checkpoint JSON contains all nineteen listed WorkflowContext fields, including graph_facts, with trace and session identity intact.
    - The checkpoint test records payload bytes and proves the complete 2048-value embedding and evidence/prompt content are retained; the plan explicitly accepts the resulting D-24 storage cost and adds no truncation or compaction behavior.
    - The query response DTO contains no checkpoint-only snapshot JSON.
    - Earlier notices remain after graph degradation and later terminal failure, with machine-readable code values preserved in order.
    - Duplicate terminal attempts produce one terminal event, and failed workflows produce no AnswerChunk or FinalAnswer.
  </acceptance_criteria>
  <verify>
    <automated>$source = Get-Content 'engine/src/workflow/events.rs' -Raw; $tests = Get-Content 'engine/src/tests/workflow_phase5.rs' -Raw; foreach ($field in @('session_id','trace_id','original_query','filter','variants','query_embedding','graph_context','graph_facts','vector_results','bm25_results','final_candidates','evidence_blocks','assembled_prompt','answer','citations','answer_basis','structured_citations','notices','snapshot')) { if ($source -notmatch [regex]::Escape($field) -and $tests -notmatch [regex]::Escape($field)) { throw "checkpoint field missing from implementation/test contract: $field" } }; if ($tests -notmatch 'serialized|byte|len\(\)') { throw 'checkpoint test does not record serialized payload size' }; $test = 'workflow_phase5_checkpoint_full_snapshot'; $listed = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $pattern = '(^|::)' + [regex]::Escape($test) + ': test$'; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -match $pattern }); if ($registered.Count -ne 1) { throw "exact Cargo registration missing or duplicated for $test" }; $output = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $test --nocapture; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
    <automated>$test = 'workflow_phase5_terminal_idempotence'; $listed = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $pattern = '(^|::)' + [regex]::Escape($test) + ': test$'; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -match $pattern }); if ($registered.Count -ne 1) { throw "exact Cargo registration missing or duplicated for $test" }; $output = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $test --nocapture; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
  </verify>
  <done>Rust tests prove complete checkpoint snapshots, preserved notices, separated response data, exact terminal cardinality, and no fabricated success after failure.</done>
  <must_haves>
    - D-03, D-24, D-25, D-26, D-28, and D-29 are covered without adding deferred metadata, tracing, resume, or fetch behavior.
    - WR-13 is closed by an executable duplicate-terminal assertion, not by a prose statement.
  </must_haves>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Rust workflow to event receiver | A closed or saturated receiver can otherwise lose lifecycle or terminal evidence or stall the query. |
| WorkflowContext to checkpoint JSON | Full accumulated state crosses a persistence boundary and must retain identity and field separation. |
| Terminal state to client | Duplicate or fabricated terminal events could misrepresent workflow success. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-10-01 | Denial of service | WorkflowEventSink | high | mitigate | Use cancellation-aware client sends and bounded owned checkpoint pending state; surface closed/saturated outcomes. |
| T-05-10-02 | Tampering | Event and checkpoint ordinals | high | mitigate | Allocate one ordinal per outer event and serialize the same value inside the checkpoint payload. |
| T-05-10-03 | Tampering | Terminal emission | high | mitigate | Guard all terminal paths with one atomic compare-and-set and test duplicate completion. |
| T-05-10-04 | Information disclosure | Checkpoint serializer | medium | mitigate | Keep full snapshot JSON in checkpoint events only and keep it out of the response DTO. |
| T-05-10-05 | Repudiation | Workflow history | medium | mitigate | Preserve notices, trace/session identity, complete context, and ordered lifecycle events for reconstruction. |
| T-05-10-06 | Denial of service | Indefinitely retained checkpoint payload | medium | accept | D-24/D-28 require the complete logical snapshot for debugging; record serialized size and retain it only in the existing checkpoint sink, with no new public fetch or compaction surface in this phase. |
</threat_model>

<verification>
- The tracer registration guard passes for `workflow_phase5_event_delivery_tracer` with one exact filter.
- Full-snapshot and terminal-idempotence registration guards pass with one exact filter per focused test.
- All event send outcomes are handled, terminal emission is atomic/idempotent, ordinals are single-source, and every listed context field round-trips.
</verification>

<success_criteria>
ORCH-02, ORCH-03, and ORCH-04 have reliable Rust event delivery, exact terminal cardinality, full accumulated checkpoints, preserved notices, and no silent sequence or snapshot loss.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-10-SUMMARY.md` when done.
</output>
