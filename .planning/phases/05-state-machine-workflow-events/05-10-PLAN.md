---
phase: 05-state-machine-workflow-events
plan: 10
type: execute
wave: 10
depends_on:
  - 05-09
  - 05-13
files_modified:
  - engine/src/workflow/node.rs
  - engine/src/workflow/runner.rs
  - engine/src/workflow/events.rs
  - engine/src/workflow/mod.rs
  - engine/src/workflow/nodes/reformulate.rs
  - engine/src/workflow/nodes/graph_context.rs
  - engine/src/workflow/nodes/retrieve.rs
  - engine/src/workflow/nodes/assemble_prompt.rs
  - engine/src/workflow/nodes/generate.rs
  - engine/src/tests.rs
  - engine/src/tests/workflow_phase5.rs
autonomous: true
gap_closure: true
requirements:
  - ORCH-01
  - ORCH-02
  - ORCH-03
  - ORCH-04
must_haves:
  truths:
    - "Workflow event delivery never ignores a send outcome: progress, checkpoint, answer, and terminal events are either delivered, retained as explicit pending ownership, or cause a typed cancellation/backpressure failure."
    - "Each of ReformulateQuery, ExtractGraphContext, RetrieveHybrid, AssemblePrompt, and GenerateAnswer implements the exact typed NodeKind value, and dispatch, timeout selection, answer-chunk emission, and checkpoint naming are exhaustive over those five values rather than string comparisons or fallback names."
    - "Sequence ordinals are assigned once per outer WorkflowEvent and the checkpoint's inner ordinal matches that same value without gaps or double increments."
    - "NodeFailed.retryable is carried from the typed error classification supplied by 05-13, and no separate retrying event is emitted."
    - "A reformulation that produces nine variants fails before successful completion or downstream adapter calls; provider preflight and generation-attempt policy are supplied by 05-13 rather than duplicated in this event plan."
    - "Every checkpoint serializes the complete accumulated WorkflowContext, including graph, evidence, prompt, answer, citations, notices, and retrieval snapshot fields."
  artifacts:
    - path: "engine/src/workflow/node.rs"
      provides: "Typed NodeKind contract exposed by every workflow node"
    - path: "engine/src/workflow/runner.rs"
      provides: "Exhaustive node dispatch, reliable event outcomes, terminal delivery, and single ordinal assignment"
    - path: "engine/src/workflow/events.rs"
      provides: "Full WorkflowContext checkpoint serialization and typed event builders"
    - path: "engine/src/workflow/nodes/reformulate.rs"
      provides: "Pre-completion maximum-variant admission"
    - path: "engine/src/workflow/nodes/graph_context.rs"
      provides: "Exact ExtractGraphContext NodeKind implementation used by exhaustive runner dispatch"
    - path: "engine/src/workflow/nodes/retrieve.rs"
      provides: "Exact RetrieveHybrid NodeKind implementation used by exhaustive runner dispatch"
    - path: "engine/src/workflow/nodes/assemble_prompt.rs"
      provides: "Exact AssemblePrompt NodeKind implementation used by exhaustive runner dispatch"
    - path: "engine/src/workflow/nodes/generate.rs"
      provides: "Exact GenerateAnswer NodeKind implementation and event-facing typed failure state"
    - path: "engine/src/tests/workflow_phase5.rs"
      provides: "Backpressure, sequence, NodeKind, admission, and snapshot regression coverage"
  key_links:
    - "NodeKind -> timeout selection, lifecycle event names, checkpoint type, and AnswerChunk emission"
    - "Typed generation outcome from 05-13 -> NodeError classification -> NodeFailed.retryable -> Go SSE retryable field"
    - "one EventSequence allocation -> outer event ordinal and checkpoint inner ordinal -> durable checkpoint ordering"
    - "WorkflowContext -> checkpoint JSON -> gateway dispatcher, without placing snapshot JSON in SSE response DTOs"
---

<objective>
Make workflow event and checkpoint delivery reliable and make all five node implementations dispatch through one exhaustive typed contract.

Purpose: Review and verification identified ignored event-send results, double-incremented ordinals, stringly-typed dispatch, late nine-variant failure, and incomplete snapshots. This plan closes the event/snapshot contract while 05-13 owns OpenRouter preflight and generation-attempt policy.
Output: Exhaustive five-node typed dispatch, reliable event delivery, full context checkpoints, early variant admission, and deterministic Rust coverage for the event failure matrix.
</objective>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-preconditions.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-reversibility.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-AI-SPEC.md
@.planning/phases/05-state-machine-workflow-events/05-VALIDATION.md
@.planning/phases/05-state-machine-workflow-events/05-VERIFICATION.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEW.md
@.planning/phases/05-state-machine-workflow-events/05-PATTERNS.md
@.planning/phases/05-state-machine-workflow-events/05-13-PLAN.md
@AGENTS.md
@rust-guidelines.md
@engine/src/workflow/node.rs
@engine/src/workflow/runner.rs
@engine/src/workflow/events.rs
@engine/src/workflow/mod.rs
@engine/src/workflow/nodes/reformulate.rs
@engine/src/workflow/nodes/graph_context.rs
@engine/src/workflow/nodes/retrieve.rs
@engine/src/workflow/nodes/assemble_prompt.rs
@engine/src/workflow/nodes/generate.rs

## Artifacts this phase produces

- A typed event-delivery contract with explicit pending/backpressure ownership and terminal-event protection.
- Exact `NodeKind` implementations for ReformulateQuery, ExtractGraphContext, RetrieveHybrid, AssemblePrompt, and GenerateAnswer with exhaustive dispatch.
- Full-fidelity checkpoint JSON and one-source sequence ordinals.
- Regression coverage for the event, admission, snapshot, and backpressure edge matrix; generation preflight/retry coverage is supplied by 05-13.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Replace stringly and lossy workflow event handling with typed reliable delivery</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; engine/src/workflow/node.rs; engine/src/workflow/runner.rs; engine/src/workflow/events.rs; engine/src/workflow/mod.rs; engine/src/workflow/nodes/reformulate.rs; engine/src/workflow/nodes/graph_context.rs; engine/src/workflow/nodes/retrieve.rs; engine/src/workflow/nodes/assemble_prompt.rs; engine/src/workflow/nodes/generate.rs; 05-13-PLAN.md; 05-CONTEXT.md; 05-AI-SPEC.md; 05-REVIEW.md; 05-VERIFICATION.md</read_first>
  <files>engine/src/workflow/node.rs, engine/src/workflow/runner.rs, engine/src/workflow/events.rs, engine/src/workflow/mod.rs, engine/src/workflow/nodes/reformulate.rs, engine/src/workflow/nodes/graph_context.rs, engine/src/workflow/nodes/retrieve.rs, engine/src/workflow/nodes/assemble_prompt.rs, engine/src/workflow/nodes/generate.rs, engine/src/tests/workflow_phase5.rs</files>
  <action>Introduce a NodeKind enum covering exactly ReformulateQuery, ExtractGraphContext, RetrieveHybrid, AssemblePrompt, and GenerateAnswer, expose it from Node, and implement the matching exact NodeKind value in every one of those five node files. Make timeout selection, lifecycle labels, checkpoint labels, answer-chunk emission, and zero-evidence branching exhaustive over NodeKind with no string comparison or fallback branch. Add the dedicated production tracer test `workflow_phase5_event_delivery_tracer` and assert that the tracer traverses the same typed dispatch used by the production runner. Keep NodeFailed.retryable as an event-facing field sourced from the typed error classification supplied by 05-13; this plan does not duplicate the OpenRouter preflight or generation-attempt policy. Redesign WorkflowEventSink delivery so every send result is handled: client-visible lifecycle, AnswerChunk, FinalAnswer, and WorkflowCompleted events use a cancellation-raced awaited permit; checkpoint events use a bounded nonblocking forwarder that retains an owned pending event and drains it without waiting for database persistence, with saturation surfaced as an explicit typed backpressure failure rather than silent loss. Assign the sequence ordinal once when wrapping the outer event, pass that same value into the checkpoint payload, and remove public call sites that allocate a second ordinal. Preserve D-01 full validated answer chunks, D-02 distinct FinalAnswer, D-15 no retrying event, D-20 coarse public-node lifecycle, D-22 typed safe failure categories, D-27 fire-and-forget checkpoint behavior, and D-31 absence of per-node OTel spans or new workflow metadata. Follow the `05-PATTERNS.md` BoxFuture and runner/event analogs; record any justified deviation at the typed dispatch or sink boundary.</action>
  <acceptance_criteria>
    - Runner dispatch contains no unknown-name timeout fallback, and each of the five NodeKind values has an explicit timeout, event label, and checkpoint label.
    - No event send result is discarded; pending ownership and closed-channel cancellation are observable in the sink API and exercised by tests.
    - Terminal events cannot be silently lost when the client is present, and a disconnected client cancels rather than blocking the workflow indefinitely.
    - Each checkpoint inner sequence ordinal equals its enclosing WorkflowEvent ordinal, and the observed event sequence is contiguous for a successful query.
    - NodeFailed.retryable is forwarded from typed error state, with no retrying event emitted by the event contract.
    - Each of the five node files returns its exact NodeKind and the runner's dispatch and timeout/event matches are exhaustive.
    - Nine variants fail before ReformulateQuery completion and before any graph, retrieval, prompt, or generation call.
  </acceptance_criteria>
  <verify>
    <automated>$test = 'workflow_phase5_event_delivery_tracer'; $listed = cargo test --manifest-path engine/Cargo.toml -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -eq ($test + ': test') -or $_.EndsWith('::' + $test + ': test') }); if ($registered.Count -ne 1) { throw "exact Cargo registration missing or duplicated for $test" }; cargo test --manifest-path engine/Cargo.toml $test -- --nocapture; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
  </verify>
  <done>One production runner path emits typed, ordered, explicitly-owned events under backpressure, dispatches all five exact NodeKind values, and applies the locked variant-admission semantics.</done>
  <must_haves>
    - ORCH-02 event vocabulary remains node_started, node_completed, node_failed, answer_chunk, final_answer, checkpoint, and workflow_completed only.
    - D-01, D-02, D-15, D-20, D-22, D-27, and D-31 are enforced by the Rust event and runner code; D-11 through D-14 are supplied by 05-13.
    - No raw model token stream, fabricated answer, or retrying event is introduced by the event contract.
  </must_haves>
  <reversibility rating="costly">Event delivery and sequence behavior are cross-runtime contracts; keep typed outcomes and compatibility-preserving labels explicit so the gateway plan can validate the same contract.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Verify the typed event, admission, and snapshot edge matrix</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; engine/src/tests.rs; engine/src/tests/workflow_phase5.rs; engine/src/workflow/events.rs; engine/src/workflow/runner.rs; engine/src/workflow/node.rs; engine/src/workflow/nodes/reformulate.rs; engine/src/workflow/nodes/graph_context.rs; engine/src/workflow/nodes/retrieve.rs; engine/src/workflow/nodes/assemble_prompt.rs; engine/src/workflow/nodes/generate.rs; 05-13-PLAN.md; 05-AI-SPEC.md; 05-VALIDATION.md; 05-VERIFICATION.md; 05-REVIEW.md</read_first>
  <files>engine/src/workflow/nodes/reformulate.rs, engine/src/workflow/nodes/graph_context.rs, engine/src/workflow/nodes/retrieve.rs, engine/src/workflow/nodes/assemble_prompt.rs, engine/src/workflow/nodes/generate.rs, engine/src/tests.rs, engine/src/tests/workflow_phase5.rs</files>
  <action>Move the maximum-variant admission into ReformulateQueryNode before it returns success so a nine-variant result has a started and failed lifecycle only, no completed checkpoint, and no downstream work. Add the named exact-dispatch test `workflow_phase5_nodekind_dispatch` covering all five mappings — ReformulateQuery, ExtractGraphContext, RetrieveHybrid, AssemblePrompt, and GenerateAnswer — and the exhaustive runner match sites. Add the named event-matrix test `workflow_phase5_event_edge_matrix` in the existing Rust test locations. Extend Rust regression coverage for the AI-SPEC scenarios and corroborated review findings. Fill the bounded event path until it reports explicit pending ownership, then prove the query does not claim a successful terminal result while a required event is unowned. Assert contiguous outer ordinals and matching checkpoint inner ordinals, typed NodeKind dispatch, typed retryable-field forwarding from the 05-13 generation contract, no retrying event, and no silent loss. Assert the exact nine-variant lifecycle and zero downstream calls. Serialize a fully populated WorkflowContext and inspect every field, including session_id, trace_id, original_query, filter, variants, query_embedding, graph_context, vector_results, bm25_results, final_candidates, evidence_blocks, assembled_prompt, answer, citations, answer_basis, structured_citations, notices, and snapshot. Keep the checkpoint payload separate from the client response contract so no snapshot JSON is placed in SSE data, and preserve indefinite retention per D-24 and Rust-free persistence ownership per D-23 and D-26. Follow the `05-PATTERNS.md` workflow-regression patterns; a missing or zero-registered focused test is a failure, not an acceptable skip.</action>
  <acceptance_criteria>
    - Backpressure tests show explicit Sent, Pending, or Closed outcomes and never a silently vanished event.
    - Sequence tests prove no skipped or duplicated ordinals and exact checkpoint/event ordinal equality.
    - NodeKind tests cover all five exact node mappings and the runner has no stringly fallback dispatch.
    - Snapshot tests prove all listed WorkflowContext fields are present and round-trip as valid JSON with trace and session identity.
    - The tests prove no checkpoint fields appear in the query response DTO and no per-node span or workflow-metadata field is required.
  </acceptance_criteria>
  <verify>
    <automated>$test = 'workflow_phase5_nodekind_dispatch'; $listed = cargo test --manifest-path engine/Cargo.toml -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -eq ($test + ': test') -or $_.EndsWith('::' + $test + ': test') }); if ($registered.Count -ne 1) { throw "exact Cargo registration missing or duplicated for $test" }; cargo test --manifest-path engine/Cargo.toml $test -- --nocapture; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
    <automated>$test = 'workflow_phase5_event_edge_matrix'; $listed = cargo test --manifest-path engine/Cargo.toml -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -eq ($test + ': test') -or $_.EndsWith('::' + $test + ': test') }); if ($registered.Count -ne 1) { throw "exact Cargo registration missing or duplicated for $test" }; cargo test --manifest-path engine/Cargo.toml $test -- --nocapture; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
  </verify>
  <done>The event and checkpoint contracts have executable coverage for backpressure, sequence integrity, typed NodeKind dispatch, nine-variant admission, full snapshots, and scope fences.</done>
  <must_haves>
    - AI-SPEC scenarios 1, 2, 5, 6, 7, 8, 9, 10, 12, 13, and 14 are represented in automated assertions.
    - D-23, D-24, D-26, D-28, and D-29 remain visible in snapshot and persistence-boundary tests.
  </must_haves>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust workflow to bounded event channel | Backpressure can otherwise drop lifecycle, terminal, or checkpoint evidence. |
| Node errors to wire-level failure categories | Internal failures must become typed safe NodeFailed payloads without misleading retry advice. |
| WorkflowContext to checkpoint JSON | Full debugging state crosses a persistence boundary and must retain identity and field separation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-10-01 | Denial of service | Workflow event backpressure | high | mitigate | Use explicit bounded pending ownership, cancellation-aware terminal delivery, and typed saturation failure instead of an ignored send result. |
| T-05-10-02 | Tampering | Sequence and event identity | high | mitigate | Allocate ordinals once and bind session and trace identity to every outer event and checkpoint. |
| T-05-10-03 | Information disclosure | Checkpoint serializer | medium | mitigate | Serialize only the defined WorkflowContext snapshot into checkpoint events; keep it out of the client response DTO and use safe failure messages. |
| T-05-10-04 | Spoofing | Retryable failure flag | medium | mitigate | Derive retryability from typed error and retry state rather than accepting a caller-provided or hardcoded value. |
| T-05-10-05 | Repudiation | Workflow lifecycle history | medium | mitigate | Preserve coarse node lifecycle events, terminal event ordering, and full context checkpoints for reconstruction. |
</threat_model>

<verification>
- The Task 1 Cargo registration guard passes for `workflow_phase5_event_delivery_tracer`.
- Task 2 registration guards pass for `workflow_phase5_nodekind_dispatch` and `workflow_phase5_event_edge_matrix`; each exact test name is listed once before its single-filter run.
- Event send outcomes, terminal delivery, sequence ordinals, retryability, and snapshot serialization are all covered by focused regression cases.
- The Rust event contract preserves D-01, D-02, D-15, D-20, D-22, D-23, D-24, D-26, D-27, D-28, D-29, and D-31.
</verification>

<success_criteria>
ORCH-01, ORCH-02, ORCH-03, and ORCH-04 have an exhaustive five-node typed dispatch contract, ordered backpressure-aware event delivery, full WorkflowContext checkpoints, and no silent event loss; OpenRouter preflight and generation retry are owned by 05-13.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-10-SUMMARY.md` when done.
</output>
