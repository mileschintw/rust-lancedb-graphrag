---
phase: 05-state-machine-workflow-events
plan: 25
type: execute
wave: 1
depends_on: []
files_modified: ["data/lancedb/", "engine/src/db/mod.rs", "engine/src/db/tests.rs"]
autonomous: true
requirements: [ORCH-01, ORCH-02]
gap_closure: true
gap_ids: [G-05-1]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Engine boots against the local ./data/lancedb store with no LanceDB schema drift error."
    - "The local nodes table matches the current 19-field nodes_schema() code contract, and documents/edges/entities/entity_edges carry fresh, internally-consistent fixture rows a live query can retrieve and cite."
    - "A developer hitting this exact schema-drift failure again gets an actionable remediation hint in the error message itself, not just a raw field dump."
    - "The strict fail-closed validate_schema design (no auto-migration) remains intentional and self-documented, matching the Phase 02-03 decision recorded in STATE.md."
  artifacts:
    - "data/lancedb/ (regenerated documents/nodes/edges/entities/entity_edges/communities/staged_documents_v2 tables via seed_rag_fixture)"
    - "data/lancedb.pre-05-25.bak/ (preserved prior stale store, not deleted)"
    - "engine/src/db/mod.rs (validate_schema error message carries a remediation clause)"
    - "engine/src/db/tests.rs (schema_drift_fails_database_initialization asserts the remediation clause is present)"
  key_links:
    - "seed_rag_fixture binary --lancedb-path -> DatabaseManager::initialize -> ./data/lancedb on-disk tables"
    - "engine main() -> DatabaseManager::initialize(&settings.engine.lancedb_path) -> validate_schema (startup gate) -> 'Rust RAG Engine serving' milestone log line"
---

<objective>
Closes UAT gap G-05-1's Blocker A: the local `./data/lancedb` `nodes` table predates the Phase 04.1-01 schema restructure (commit 2302f79, 2026-08-07) and still carries the pre-restructure 23-field shape (it still has `community_ids`, `summary`, `summary_vector`, `unsummarized_refs`, which that commit moved onto a new `entities` table). `validate_schema`'s strict fail-closed equality check (`engine/src/db/mod.rs:161-174`) has no migration path, so the engine exits with code 1 on startup before the gateway or `/rag/query` can even be reached. This is confirmed stale local dev data (`.planning/debug/g-05-1-engine-startup-blockers.md` root-cause section), not a code bug — the fix is operational (regenerate the store) plus a small error-message improvement so the same failure is instantly diagnosable if it recurs on another developer's machine.

Design call made here (per the debug session's request for a planner decision): `validate_schema` stays strict-only. It is a deliberate Phase 02-03 safety property ("Fail startup on any LanceDB schema field drift — indexing must not proceed against incompatible persisted storage"), it is invisible to automated tests (they use isolated temp paths), and it only ever surfaces against a long-lived, manually-run local store. Building a generic reconciliation framework for arbitrary column removals would weaken that guarantee for all 7 tables to fix what is, in practice, a one-time local artifact. The existing precedent for schema evolution (the additive `generation` column special-case for `staged_documents_v2`) is reserved for genuinely additive, backward-compatible changes — not for tables where fields moved to a different table entirely, as happened here.

Purpose: Unblock UAT Test 1 (live end-to-end `/rag/query` run) by giving the engine a schema-current local store to boot against, and make the strict-validation decision self-documenting in code rather than only living in STATE.md.
Output: A regenerated `./data/lancedb` store with fresh fixture data (old store preserved as a backup, not deleted), and a `validate_schema` error message that tells the next person exactly what to do.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-state-machine-workflow-events/05-UAT.md
@.planning/debug/g-05-1-engine-startup-blockers.md
@engine/src/db/mod.rs
@engine/src/db/tests.rs
@engine/src/bin/seed_rag_fixture.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild and reseed the stale local LanceDB store</name>
  <files>data/lancedb/</files>
  <action>
    Preserve the existing stale store instead of deleting it: if `data/lancedb` exists and `data/lancedb.pre-05-25.bak` does not already exist, rename it with `mv data/lancedb data/lancedb.pre-05-25.bak` (single explicit path, no glob). If `data/lancedb` is already absent (e.g. a prior run of this task already handled it) or the backup already exists, skip the rename and proceed — this task must be safely re-runnable.

    Build the engine and seed_rag_fixture binaries: `cargo build --manifest-path engine/Cargo.toml --locked --bin engine --bin seed_rag_fixture`.

    From the repository root, run `cargo run --manifest-path engine/Cargo.toml --bin seed_rag_fixture -- --lancedb-path ./data/lancedb`. This calls `DatabaseManager::initialize`, which creates all 7 tables fresh against the current schemas (including the 19-field `nodes_schema()`), then seeds deterministic fixture rows and asserts read-back integrity internally (entity/edge counts, dense-score sanity), exiting non-zero on any mismatch.

    Non-idempotence warning: if the seeder fails partway through (non-zero exit), do NOT re-run it against the same `./data/lancedb` directory — its internal assertions expect an empty store (exactly 2 entities, 1 entity edge) and will fail differently against a partially-seeded one. On any seeder failure, remove `./data/lancedb` entirely (the backup at `data/lancedb.pre-05-25.bak` is untouched) and re-run the build+seed steps from a clean directory.

    Confirm the engine now boots cleanly against this store: run the built `engine` binary once with `LANCET_ENGINE__GRPC_ADDR` overridden to an unused loopback port (avoids colliding with any already-running instance on the default `[::1]:50051`) and `LANCET_ENGINE__LANCEDB_PATH=./data/lancedb`, capture stdout/stderr for several seconds, then terminate the process. The captured output must show the "Rust RAG Engine serving" milestone line and must not show a schema drift failure.
  </action>
  <verify>
    <automated>
      cargo run --manifest-path engine/Cargo.toml --bin seed_rag_fixture -- --lancedb-path ./data/lancedb
      # exit code 0 required (internal entity/edge/dense_score assertions already passed)

      LANCET_ENGINE__GRPC_ADDR=127.0.0.1:59991 LANCET_ENGINE__LANCEDB_PATH=./data/lancedb ./engine/target/debug/engine.exe > engine-boot-check.log 2>&1 &
      ENGINE_PID=$!
      sleep 8
      grep -q "Rust RAG Engine serving" engine-boot-check.log && ! grep -q "schema drift detected" engine-boot-check.log
      kill $ENGINE_PID 2>/dev/null || true
    </automated>
    <human-check>
      <test>Export a real OPENROUTER_API_KEY. Start the gateway (dials the engine over gRPC; ensure LANCET_ENGINE__LANCEDB_PATH points at the regenerated ./data/lancedb) and the engine, then `curl -N -X POST http://localhost:8080/rag/query` with a query body and watch the raw SSE frame sequence — this is the exact procedure from 05-UAT.md Test 1.</test>
      <expected>node_started/node_completed events for all five nodes (ReformulateQuery, ExtractGraphContext, RetrieveHybrid, AssemblePrompt, GenerateAnswer), exactly one answer_chunk, one final_answer, one workflow_completed, and no stream_error. The answer is grounded with real citations drawn from the reseeded fixture content (the dense/lexical/graph fixture markers seed_rag_fixture writes).</expected>
      <why_human>Requires a live OPENROUTER_API_KEY and a real OpenRouter round-trip; every automated proof of this pipeline substitutes an httptest mock for OpenRouter's /embeddings, /models, and /chat/completions endpoints (05-UAT.md Test 1's own why_human) — no automated command can close this truth. This task only proves the store-side blocker (schema drift) is gone; the live end-to-end run itself stays an open human-verification item.</why_human>
    </human-check>
  </verify>
  <done>seed_rag_fixture exits 0 against a freshly (re)created ./data/lancedb; the short-lived engine boot check shows the serving milestone with no schema-drift failure; the prior stale store survives at data/lancedb.pre-05-25.bak rather than being destroyed; the live end-to-end run remains an open human-check item pending an available OPENROUTER_API_KEY.</done>
  <reversibility rating="reversible">Renames (not deletes) the stale local LanceDB store before reseeding, so the prior local dev data remains recoverable from data/lancedb.pre-05-25.bak; the operation only touches a gitignored, disposable local dev data directory.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Give validate_schema's fail-closed error a remediation hint</name>
  <files>engine/src/db/mod.rs, engine/src/db/tests.rs</files>
  <behavior>
    - Test: the existing schema_drift_fails_database_initialization test's error string continues to contain "schema drift detected for documents" AND now also contains a remediation-guidance substring pointing the reader at regenerating the local store.
  </behavior>
  <action>
    In `validate_schema` (engine/src/db/mod.rs, ~lines 161-174), extend the returned error string so that, alongside the existing "LanceDB schema drift detected for {name}: expected {...}, found {...}" field dump, it also names the concrete remediation path: this validator has no automatic migration/reconciliation for column removals by design (Phase 02-03 decision: fail startup on any schema field drift), so a mismatched on-disk table must be regenerated operationally — rename or delete the stale LanceDB store directory and repopulate it (e.g. via the seed_rag_fixture binary for local dev, or a real ingestion re-run for a production-shaped store).

    Do not add a migration/reconciliation code path to validate_schema itself — keep it strict-only. This is an intentional fail-closed safety property (indexing must not proceed against incompatible persisted storage), not an oversight; only the diagnostic message improves.

    Extend the existing schema_drift_fails_database_initialization test in engine/src/db/tests.rs (~lines 85-108) with one additional assertion, alongside its current check that the error contains "schema drift detected for documents", proving the returned error also contains the new remediation clause.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked schema_drift_fails_database_initialization -- --exact</automated>
  </verify>
  <done>validate_schema's error message carries both the field-mismatch dump and a remediation clause; the extended test asserts both are present and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local filesystem (developer machine) -> engine process | `LANCET_ENGINE__LANCEDB_PATH` / `seed_rag_fixture --lancedb-path` accept an operator-supplied filesystem path; the engine reads/writes LanceDB tables at that path with process-level permissions. |
| Engine startup log output -> operator terminal | The improved error message is diagnostic text only, surfaced to whoever runs the binary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.25-01 | Tampering | `LANCET_ENGINE__LANCEDB_PATH` / `seed_rag_fixture --lancedb-path` | low | accept | Pre-existing, unchanged behavior: both already accept an operator-controlled path (config.toml default + explicit override); this plan does not widen that surface, and the tool is a local dev/test utility, not a network-facing service. |
| T-05.25-02 | Information Disclosure | validate_schema's error message | low | accept | The added remediation clause is generic operational guidance (rename/regenerate the store); it does not reveal secrets, credentials, or row-level data — only static field-schema mismatches already present in the pre-existing error format. |

</threat_model>

<verification>
- `cargo build --manifest-path engine/Cargo.toml --locked --bin engine --bin seed_rag_fixture` compiles cleanly.
- `cargo run --manifest-path engine/Cargo.toml --bin seed_rag_fixture -- --lancedb-path ./data/lancedb` exits 0.
- `cargo test --manifest-path engine/Cargo.toml --locked schema_drift_fails_database_initialization -- --exact` passes.
- Short-lived engine boot check against ./data/lancedb shows "Rust RAG Engine serving" with no schema-drift failure.
- `cargo test --manifest-path engine/Cargo.toml --locked` full suite still green (no regression from the error-message change).
</verification>

<success_criteria>
Blocker A (engine startup failure due to a stale local LanceDB nodes table) is closed: the local store boots cleanly, carries fresh fixture data a live query can cite, the prior store is preserved rather than destroyed, and a recurrence of this exact class of failure is self-diagnosing from the error message alone. The live end-to-end UAT Test 1 run itself remains pending human execution with a real OPENROUTER_API_KEY (captured as a human-check item, not silently closed).
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-25-SUMMARY.md` when done
</output>
