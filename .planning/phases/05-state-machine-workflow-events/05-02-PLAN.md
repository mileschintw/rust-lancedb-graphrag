---
phase: 05-state-machine-workflow-events
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - engine/src/workflow/mod.rs
  - engine/src/workflow/runner.rs
  - engine/src/workflow/ports.rs
  - engine/src/workflow/nodes/mod.rs
  - engine/src/workflow/nodes/graph_context.rs
  - engine/src/workflow/nodes/retrieve.rs
  - engine/src/retrieval/fusion.rs
  - engine/src/main.rs
  - engine/src/tests.rs
  - config/config.toml
autonomous: true
requirements: [ORCH-01, ORCH-03]

must_haves:
  truths:
    - "ExtractGraphContext always runs as a real pipeline node for every query and its own success is never required for the query to complete — a logical graph failure or no-match degrades the query to chunk-only evidence and still reaches WorkflowCompleted{success: true} (D-09)."
    - "ExtractGraphContext's own configured timeout also degrades exactly like a logical failure (empty graph_context, WorkflowCompleted{success: true}) rather than failing the whole query — this is a flagged interpretation of D-09's 'mandatory but non-required' framing extended to the timeout sub-case, not an explicitly locked CONTEXT.md decision (see 05-RESEARCH.md Common Pitfall #2 / Open Question 1, 05-AI-SPEC.md dimension 4). Proven deterministically this plan (not deferred to 05-04) via a GraphQueryPort test double that genuinely stalls, not a 0ms-timeout race — REVIEWS.md Codex HIGH #6: the port seam moves into THIS plan specifically so this must-have has a real deterministic test the moment it's written, instead of 05-04's later plan depending on a port this plan's own node doesn't yet have."
    - "ExtractGraphContextNode's own inner timeout (graph_timeout_ms, D-17's 15s default) is strictly SMALLER than the WorkflowRunner's outer per-node timeout entry for this node (graph_outer_timeout_ms, sized with headroom above the inner value) — so the inner degrade-to-empty-context path always wins the race in the normal case, and the outer timeout is a backstop that only fires if the inner race itself hangs (e.g. a bug in the inner select!), never the expected path (REVIEWS.md Codex HIGH #6, second half: an earlier draft of this plan let both budgets read the same configured value, letting the outer timeout occasionally win and hard-fail a query D-09 says should degrade)."
    - "RetrieveHybrid loops over QueryReformulator's Vec<String> variants and RRF-merges results across variants, rather than hard-indexing [0] — proven by a FakeQueryReformulator returning 2+ variants whose per-variant rank orderings are both reflected in the final merged order, not just behaviorally inert against the NoOp's single variant (D-07). The cross-variant merge's exact output SCORE (not just relative order) is asserted for at least one candidate, computed from the documented formula below — proving the merge is the real two-pass RRF-of-RRF-lists, not an approximation that happens to preserve ordering (REVIEWS.md Codex MEDIUM)."
    - "Each variant's dense+BM25 retrieval reuses the SAME ctx.query_embedding for dense search (D-08 — only variant [0] is ever embedded) but a per-variant retrieval::QueryRequest cloned from ctx.query_request with only its query field replaced by that variant's text (filters/document_ids/content_types carried through unchanged from the D-04-validated request) for the BM25 call — REVIEWS.md Codex HIGH #4: fixes an earlier draft that had no validated-filter field on WorkflowContext at all and risked BM25 running against raw, unnormalized query text."
    - "When RetrieveHybrid's final candidate set is empty, the workflow short-circuits straight to WorkflowCompleted{success: true, session_id, notices: [NO_EVIDENCE notice], snapshot: Some(retrieval snapshot)} without AssemblePrompt or GenerateAnswer ever receiving a NodeStarted (D-03) — this remains true regardless of how many more nodes exist later in the Vec, verified this plan with exactly 4 nodes in the Vec. The notice/snapshot population on this specific terminal event restores the exact data today's synchronous zero-match QueryRAGResponse carries (main.rs:1508-1547) — REVIEWS.md Codex HIGH #3, resolved via the WorkflowCompleted fields 05-01 added rather than FinalAnswer, so 05-03's zero-AnswerChunk/FinalAnswer must-have is untouched."
    - "A genuine RetrieveHybrid infrastructure error (not zero results) emits NodeFailed{category: RetrievalFailed} then WorkflowCompleted{success: false}, distinguishably different from the D-03 zero-evidence success path (D-22, AI-SPEC Critical Failure Mode #5) — proven this plan via a FakeDenseRetrievalPort configured to return Err on its first call, not merely an empty-results Ok(vec![])."
    - "ExtractGraphContext runs before RetrieveHybrid in the executed node order — this plan corrects a self-inconsistency in 05-CONTEXT.md's own domain-boundary summary line and 05-AI-SPEC.md Section 4's prose (both list 'ReformulateQuery -> RetrieveHybrid -> ExtractGraphContext'), which contradicts D-06's own explicit decision text ('graph augmentation runs before hybrid retrieval') and the verified live code order (engine/src/main.rs: embed at line 1395, attempt_graph_augmentation at line 1426, DenseRetriever::query at line 1450). Follow D-06's decision text and the verified code order, not the two inconsistent summary lines."
    - "Edge-probe ORCH-03 concurrency (closed by planner review — the deterministic engine returned this row unresolved with the probe question 'if interrupted or run in parallel, what is guaranteed?'): two truly concurrent QueryRAG calls share no mutable retrieval/graph state — each call constructs its own WorkflowContext, GraphQueryPort/DenseRetrievalPort Arc handles, and CancellationToken this plan introduces; the underlying LancetServiceImpl fields they wrap (nodes table handle, bm25_index RwLock) are the pre-existing Phase 3 concurrency contract, unchanged by this plan's port seam."
  artifacts:
    - path: "engine/src/workflow/ports.rs"
      provides: "GraphQueryPort/DenseRetrievalPort traits + production wrapper implementations + FakeGraphQueryPort/FakeDenseRetrievalPort test doubles (moved into this plan from 05-04 per REVIEWS.md Codex HIGH #6, so the deterministic graph-timeout/retrieval-error tests this plan's own must-haves require have a real seam to use immediately)"
    - path: "engine/src/workflow/nodes/graph_context.rs"
      provides: "ExtractGraphContextNode wrapping GraphQueryPort with its own inner timeout race, distinct from the runner's outer backstop"
    - path: "engine/src/workflow/nodes/retrieve.rs"
      provides: "RetrieveHybridNode wrapping DenseRetrievalPort with D-07 two-pass RRF-across-variants merge and per-variant filtered QueryRequest"
    - path: "engine/src/retrieval/fusion.rs"
      provides: "a new cross-variant RRF merge function reusing fuse_candidates's existing rrf_k/D-51 tie-break convention, with an explicit documented scoring formula"
  key_links:
    - from: "engine/src/main.rs query_rag handler"
      to: "engine/src/workflow/runner.rs WorkflowRunner (extended Vec)"
      via: "vec![reformulate, graph_context, retrieve_hybrid] node ordering per D-06"
      pattern: "GraphContextNode|RetrieveHybridNode"
    - from: "engine/src/workflow/nodes/retrieve.rs"
      to: "engine/src/retrieval/fusion.rs fuse_candidates"
      via: "one fuse_candidates call per QueryReformulator variant (each using a per-variant QueryRequest for BM25, the shared ctx.query_embedding for dense), then a second RRF merge pass across the per-variant FusedCandidate lists"
      pattern: "fuse_candidates"
    - from: "engine/src/workflow/nodes/graph_context.rs / retrieve.rs"
      to: "engine/src/workflow/ports.rs GraphQueryPort / DenseRetrievalPort"
      via: "Arc<dyn Trait> injected field, replacing direct DatabaseManager/DenseRetriever construction"
      pattern: "GraphQueryPort|DenseRetrievalPort"
  prohibitions:
    - requirement_id: ORCH-01
      category: transparency
      statement: "MUST NOT misreport the D-03 zero-evidence valid-success completion as a failure, or a genuine RetrieveHybrid error as the benign zero-evidence success path (D-03, D-22, AI-SPEC Critical Failure Mode #5)."
      verification: test
---

<objective>
Extend the proven single-node pipeline from 05-01 with the next two nodes in D-06's real execution order — `ExtractGraphContext` then `RetrieveHybrid` — appended to the same `WorkflowRunner`'s `Vec<Box<dyn Node>>`. This plan also introduces `engine/src/workflow/ports.rs` (`GraphQueryPort`/`DenseRetrievalPort`, moved forward from what an earlier draft scoped to 05-04 — REVIEWS.md Codex HIGH #6) so both new nodes are built against an injectable seam from the start, not a direct `DatabaseManager`/`DenseRetriever` dependency retrofitted later. No architectural change to the runner, proto, or Go transport beyond that: this plan is pure node expansion inside `engine/src/workflow/nodes/` plus the D-03 short-circuit, D-07 cross-variant merge, and the port seam these two nodes require.

Purpose: retire the phase's two remaining Rust-side open design questions — whether a graph *timeout* degrades like a logical graph failure (D-09) or hard-fails the query, and how RRF-merging across N reformulation variants (D-07) actually composes with the existing single-pass `fuse_candidates` — before 05-03 adds generation and 05-04 builds the remaining fault-injection coverage. Moving the port seam here (rather than 05-04) means this plan's own must-haves about deterministic graph-timeout-degrade and genuine-retrieval-error-vs-zero-evidence can be proven with a real stalling/failing test double in the SAME wave the node is written, not asserted-then-revisited two waves later.
Output: `engine/src/workflow/ports.rs` (`GraphQueryPort`/`DenseRetrievalPort` traits + production wrappers + fakes), `ExtractGraphContextNode`, `RetrieveHybridNode`, a new cross-variant RRF merge function in `engine/src/retrieval/fusion.rs`, the D-03 short-circuit wired generically into `WorkflowRunner` (now also populating `WorkflowCompleted`'s session_id/notices/snapshot on that path), `main.rs`'s node `Vec` extended to 3 entries, and `FakeQueryReformulator`/`FakeGraphQueryPort`/`FakeDenseRetrievalPort` test doubles proving all of the above land on deterministic, asserted behavior.

Neither new node adds a per-node `tracing::info_span!` beyond the existing `query_rag` span carried over from 05-01 — new span instrumentation (`hybrid_retrieval`, `graph_context_extraction`, etc.) is explicitly deferred to Phase 6's OpenTelemetry work (D-31); this plan only mirrors the existing `Span::current().record("graph_augmentation", ...)` field-recording pattern main.rs already uses, it does not open new spans.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-AI-SPEC.md
@rust-guidelines.md

<interfaces>
<!-- Verified live this session (Read against engine/src/retrieval/fusion.rs and engine/src/main.rs — NOT RESEARCH.md's paraphrase, which named an internal helper "rrf_add" that does not actually exist under that name). -->

From engine/src/retrieval/fusion.rs:1-110 (VERIFIED live, full read this session):
```rust
pub struct FusedCandidate {
    pub candidate: Candidate,
    pub fused_score: f64,
    pub vector_rank: Option<usize>,
    pub bm25_rank: Option<usize>,
    pub vector_score: Option<f64>,
    pub bm25_score: Option<f64>,
}
pub fn fuse_candidates(
    vector_candidates: Vec<Candidate>,
    bm25_candidates: Vec<Candidate>,
    settings: &RetrievalSettings,
) -> Result<Vec<FusedCandidate>, RetrievalError> { /* dedups by candidate.chunk_id, RRF-scores via a
    PRIVATE add_candidate() helper (NOT "rrf_add" — that name does not exist in this file) using
    settings.rrf_k and the D-51 tie-break: best source rank, document ID, chunk index, chunk ID */ }
```
`Candidate`, `RetrievalError`, `RetrievalErrorKind`, `RetrievalSettings` are imported from `super::` (the parent `retrieval` module) — grep `engine/src/retrieval/mod.rs` for their exact field shapes before writing `RetrieveHybridNode`. The module's private `add_candidate`/`Accumulator`/`Source` helpers are NOT `pub` — a new cross-variant merge function must either live inside `fusion.rs` itself (to reuse them directly) or re-derive the same `weight / (rrf_k + rank)` formula against `Vec<Vec<FusedCandidate>>` input; do not guess a public helper name that was not verified above.

From engine/src/main.rs:1426-1448 (VERIFIED live, read this session — the exact call + outcome match to wrap):
```rust
let graph_outcome = attempt_graph_augmentation(&self.database, &query_embedding, &self.effective_settings.graph).await;
tracing::Span::current().record("graph_augmentation", match &graph_outcome {
    GraphAugmentationOutcome::Succeeded { .. } => "succeeded",
    GraphAugmentationOutcome::NoMatchFound => "no_match_found",
    GraphAugmentationOutcome::AttemptedAndFailed { .. } => "attempted_and_failed",
});
let graph_facts: Vec<prompt::GraphFactBlock> = match graph_outcome {
    GraphAugmentationOutcome::Succeeded { facts } => facts.into_iter().map(|fact| prompt::GraphFactBlock { fact }).collect(),
    _ => vec![],
};
```
`attempt_graph_augmentation` returns `GraphAugmentationOutcome` (an outcome enum, never `Err`) — this session did NOT verify the exact field name inside `AttemptedAndFailed { .. }` (05-RESEARCH.md calls it `reason` but this was not independently re-checked here) — grep `engine/src/` for `enum GraphAugmentationOutcome` and confirm the field name before referencing it in code.

From engine/src/main.rs:1450-1476 (VERIFIED live, read this session — the exact dense-retrieval + error-mapping call to wrap):
```rust
let dense_retriever = DenseRetriever::new(self.nodes.clone());
let dense_candidates = match dense_retriever.query(&query_embedding, &query_request, &self.effective_settings.retrieval).await {
    Ok(candidates) => candidates,
    Err(err) => { /* maps RetrievalErrorKind::Snapshot -> Unavailable "dense_retrieval",
                     RetrievalErrorKind::NonFiniteScore -> Internal "non_finite_score",
                     _ -> Internal "dense_retrieval_internal", via d1_status */ }
};
let bm25_guard = self.bm25_index.read().await;
let bm25_candidates = bm25_guard.retrieve(&query_request, &self.effective_settings.retrieval).await.map_err(...)?;
drop(bm25_guard);
let fused = retrieval::fusion::fuse_candidates(dense_candidates, bm25_candidates, &self.effective_settings.retrieval)?;
let reranked = self.reranker.rerank(fused).await?;
let final_candidates: Vec<_> = reranked.into_iter().take(self.effective_settings.retrieval.final_limit).collect();
if final_candidates.is_empty() { /* D-03: build+return a zero-evidence success response */ }
```
This whole block (dense retrieve, BM25 retrieve, per-variant fuse, rerank, take(final_limit), the D-03 empty check) is `RetrieveHybridNode`'s scope — the rerank step (`self.reranker.rerank`) runs ONCE against the final cross-variant-merged candidate list, not once per variant (rerank is a post-fusion step in the existing code and this plan does not change that ordering).

Port seam shape to mirror (not independently re-verified this plan session beyond what 05-01 already established — `engine::generation::Generator`/`FakeGenerator`, `generation/mod.rs:459-512`, cited fully in 05-01's Interfaces and RESEARCH.md Code Examples): both `GraphQueryPort` and `DenseRetrievalPort` follow the exact same `BoxFuture`-returning, object-safe trait shape `Node`/`Generator` already use — `pub trait GraphQueryPort: Send + Sync { fn augment<'a>(&'a self, query_embedding: &'a [f32], settings: &'a GraphSettings) -> BoxFuture<'a, GraphAugmentationOutcome>; }` (production impl delegates to `attempt_graph_augmentation`) and `pub trait DenseRetrievalPort: Send + Sync { fn query<'a>(&'a self, embedding: &'a [f32], request: &'a QueryRequest, settings: &'a RetrievalSettings) -> BoxFuture<'a, Result<Vec<Candidate>, RetrievalError>>; }` (production impl delegates to `DenseRetriever::new(...).query(...)`). Each fake mirrors `FakeGenerator`'s `call_count: AtomicUsize` + `Mutex<Vec<...configured outcomes...>>` shape, with a stall variant achieved via `tokio::time::sleep` for a configured `Duration` before returning (never a busy-loop), so the node's own inner timeout has something real to race against.
</interfaces>
</context>

## Artifacts this phase produces (this plan's share)

New symbols this plan creates, in addition to 05-01's — later plans extend these, do not recreate them:
- Rust: `engine::workflow::ports::{GraphQueryPort, DenseRetrievalPort}` traits + `DatabaseManagerGraphPort`/`TableDenseRetrievalPort` production wrappers, `engine::workflow::nodes::graph_context::ExtractGraphContextNode`, `engine::workflow::nodes::retrieve::RetrieveHybridNode`, a new cross-variant merge function in `engine::retrieval::fusion` (public, exact name executor's choice)
- Rust test doubles: `FakeQueryReformulator`, `FakeGraphQueryPort`, `FakeDenseRetrievalPort` (new, in engine/src/tests.rs — in-process, mirroring 05-01's pattern, not a spawned-binary test)
- `WorkflowContext.short_circuit: bool` field (new)
- Config: `graph_timeout_ms` (inner node budget, D-17 default 15000), `graph_outer_timeout_ms` (runner backstop, sized strictly larger, e.g. 18000), `retrieve_timeout_ms` keys under `[engine.workflow]`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GraphQueryPort seam + ExtractGraphContextNode — always-runs, degrade-on-failure-or-timeout</name>
  <reversibility rating="costly">D-09 says the graph node "always runs... success is not required" but does not explicitly say whether its own D-17 TIMEOUT also degrades (vs. hard-failing like every other node's timeout). This plan resolves it as: timeout degrades exactly like a logical failure, per 05-RESEARCH.md's explicit recommendation. If this reading is wrong, undoing it means changing the node's failure-policy semantics after tests/checkpoints/clients have come to depend on "graph timeout = silent degrade" — a coordinated change across the node, its tests, and any consumer expectation, not a wire-format or schema migration.</reversibility>
  <files>engine/src/workflow/ports.rs, engine/src/workflow/nodes/graph_context.rs, engine/src/workflow/nodes/mod.rs, engine/src/main.rs, config/config.toml, engine/src/tests.rs</files>
  <read_first>engine/src/main.rs, engine/src/workflow/node.rs, engine/src/workflow/runner.rs, engine/src/db/mod.rs, engine/src/generation/mod.rs</read_first>
  <behavior>
    - Logical failure or no-match (GraphQueryPort::augment returns NoMatchFound or AttemptedAndFailed): ctx.graph_context stays empty, node returns Ok(()) to the runner — query proceeds and completes successfully.
    - Graph query exceeds its own configured graph_timeout_ms (the node's INNER budget): treated identically to the AttemptedAndFailed case above — ctx.graph_context stays empty, node returns Ok(()).
    - Success (Succeeded { facts }): ctx.graph_context populated from facts, node returns Ok(()).
    - Cancellation (client disconnect mid-graph-query): node returns Err(NodeError{kind: Cancelled}) — cancellation is NOT degraded silently; it propagates as a real failure exactly like every other node, since D-09 only concerns graph-specific failure/timeout, not the cross-cutting cancellation contract.
    - The WorkflowRunner's own outer per-node timeout entry for this node (graph_outer_timeout_ms) is a BACKSTOP only, sized strictly larger than graph_timeout_ms — in the normal case the node's inner select! always resolves first, so the outer timeout firing at all is itself a symptom the inner race is broken, not an expected code path (REVIEWS.md Codex HIGH #6).
  </behavior>
  <action>
    Create engine/src/workflow/ports.rs: `pub trait GraphQueryPort: Send + Sync { fn augment<'a>(&'a self, query_embedding: &'a [f32], settings: &'a GraphSettings) -> BoxFuture<'a, GraphAugmentationOutcome>; }` with `pub struct DatabaseManagerGraphPort(pub DatabaseManager);` as the production implementation (`augment` is a one-line delegation to `attempt_graph_augmentation(&self.0, query_embedding, settings)`), and `pub struct FakeGraphQueryPort { call_count: AtomicUsize, responses: Mutex<Vec<GraphOutcomeFixture>> }` where each configured fixture is either an immediate `GraphAugmentationOutcome` or a `(Duration, GraphAugmentationOutcome)` stall-then-return pair (via `tokio::time::sleep`, never a busy-loop) — this plan's Interfaces section above documents the exact trait/fake shape to mirror from `Generator`/`FakeGenerator`. Also define `pub trait DenseRetrievalPort` and its production/fake pair here even though Task 2 is what wires it into `RetrieveHybridNode` — both traits belong in the same file since they're introduced together this plan.
    Create engine/src/workflow/nodes/graph_context.rs: `ExtractGraphContextNode { graph_port: Arc<dyn GraphQueryPort>, graph_settings: GraphSettings (or whatever the existing self.effective_settings.graph field type is — confirm via read_first), inner_timeout: Duration }`. Wrap the call to `self.graph_port.augment(&ctx.query_embedding, &self.graph_settings)` in the node's own inner `tokio::select! { biased; _ = cancel.cancelled() => return Err(NodeError::cancelled()), res = tokio::time::timeout(self.inner_timeout, self.graph_port.augment(...)) => match res { ... } }` (per this task's Behavior block): a timeout OR a logical AttemptedAndFailed/NoMatchFound outcome both leave ctx.graph_context empty and return Ok(()); Succeeded populates ctx.graph_context from facts (reuse the exact `prompt::GraphFactBlock` mapping from 05-01/this plan's Interfaces citation) and returns Ok(()); a cancellation signal returns Err(NodeError{kind: Cancelled}). Record the same `tracing::Span::current().record("graph_augmentation", ...)` categorization main.rs currently performs (succeeded/no_match_found/attempted_and_failed/timed_out — add a 4th category string for the new timeout sub-case so this signal remains distinguishable in logs even though it doesn't fail the query).
    In config/config.toml's `[engine.workflow]` section, add `graph_timeout_ms = 15000` (D-17 default, the node's own inner budget) and `graph_outer_timeout_ms = 18000` (the runner's outer backstop entry for this node — strictly greater than `graph_timeout_ms`, never equal; document in a code comment that the outer value firing at all is itself a bug signal, not a normal path).
    In engine/src/main.rs: at the graph-node construction call site, build `Arc::new(DatabaseManagerGraphPort(self.database.clone()))` as the production port and pass it into `ExtractGraphContextNode`. Extend the `Vec<Box<dyn Node>>` constructed in query_rag to `vec![reformulate_node, graph_context_node, ...]` (graph_context_node constructed with the port, the effective graph settings, and `graph_timeout_ms` read from config as its `inner_timeout`; the runner's own timeouts list for this node position uses `graph_outer_timeout_ms`) — insert it as the SECOND node, immediately after ReformulateQueryNode and before any retrieval node (D-06 order, corrected per this plan's must_haves note above). Delete the now-superseded inline graph-augmentation block from main.rs (verified above) since ExtractGraphContextNode now owns this logic.
    In engine/src/tests.rs: un-ignore and rewrite any of the 5 previously-deferred query_rag tests whose behavior is now buildable against this node (grep for "pending 05-02" markers left by 05-01) — if none target graph behavior specifically, leave them ignored for 05-03. Add the graph-timeout-degrade Tier 1 scenario using `FakeGraphQueryPort` configured to stall past a test-shortened `graph_timeout_ms` — this is the deterministic proof AI-SPEC reference dataset scenario 4 requires; it is proven THIS plan, not deferred to 05-04.
  </action>
  <verify>
    <automated>cd D:/Repos/lancet/engine &amp;&amp; cargo test --locked --bin engine -- graph_context</automated>
  </verify>
  <acceptance_criteria>
    - engine/src/workflow/ports.rs defines GraphQueryPort and DenseRetrievalPort traits plus one production and one fake implementation each
    - engine/src/workflow/nodes/graph_context.rs contains ExtractGraphContextNode depending on Arc&lt;dyn GraphQueryPort&gt;, not a direct DatabaseManager field, with an inner tokio::select! wrapping the port call
    - A Tier 1 test using FakeGraphQueryPort asserts: graph logical failure -> ctx.graph_context empty -> WorkflowCompleted{success: true}
    - A Tier 1 test using FakeGraphQueryPort configured to genuinely stall (tokio::time::sleep) past a test-shortened graph_timeout_ms asserts: ctx.graph_context empty -> WorkflowCompleted{success: true}, NOT WorkflowCompleted{success: false}
    - A Tier 1 test asserts: graph success -> ctx.graph_context populated -> WorkflowCompleted{success: true}
    - config/config.toml contains graph_timeout_ms = 15000 and graph_outer_timeout_ms = 18000 under [engine.workflow], with graph_outer_timeout_ms strictly greater
  </acceptance_criteria>
  <done>ExtractGraphContextNode depends on the new GraphQueryPort seam, always runs, degrades on both logical failure and a genuinely-stalled timeout per the flagged D-09 interpretation (proven deterministically, not by a 0ms-timeout race), and only cancellation propagates as a real node failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RetrieveHybridNode — D-07 cross-variant RRF merge + D-03 short-circuit</name>
  <files>engine/src/retrieval/fusion.rs, engine/src/workflow/nodes/retrieve.rs, engine/src/workflow/nodes/mod.rs, engine/src/workflow/mod.rs, engine/src/workflow/runner.rs, engine/src/workflow/ports.rs, engine/src/main.rs, config/config.toml, engine/src/tests.rs</files>
  <read_first>engine/src/main.rs, engine/src/retrieval/fusion.rs, engine/src/retrieval/mod.rs, engine/src/workflow/runner.rs, engine/src/workflow/mod.rs</read_first>
  <behavior>
    - For each variant in ctx.reformulated_query, build a per-variant `retrieval::QueryRequest` cloned from `ctx.query_request` with only `.query` replaced by the variant text (document_ids/content_types filters carried through UNCHANGED from the D-04-validated request — REVIEWS.md Codex HIGH #4), then run the existing dense-retrieve + BM25-retrieve + fuse_candidates sequence — dense retrieval uses the SHARED `ctx.query_embedding` for every variant (D-08 — only variant [0] is ever embedded), BM25 uses that variant's own cloned QueryRequest — producing one Vec<FusedCandidate> per variant.
    - Merge the N per-variant Vec<FusedCandidate> lists into one final ranked list using this EXPLICIT cross-variant formula (REVIEWS.md Codex MEDIUM — an earlier draft left this underspecified): each per-variant list's own `fused_score`-descending order is that variant's rank (1-indexed); every variant contributes EQUALLY (weight 1.0 per variant — the existing vector_weight/bm25_weight were already applied once inside each variant's own fuse_candidates call, so they are NOT reapplied here); each candidate's cross-variant score is `sum over variants where it appears of 1.0 / (settings.rrf_k + variant_rank)`; a candidate absent from a variant's list contributes 0 for that variant (not a penalty term); the winning FusedCandidate's `vector_rank`/`bm25_rank`/`vector_score`/`bm25_score` provenance fields are copied from whichever single variant-appearance has the best (lowest) `vector_rank`, ties broken by lowest `bm25_rank` (D-51's "best source rank" extended across variants, not blended/averaged); `fused_score` on the output is overwritten with the cross-variant accumulated score; final sort is descending cross-variant score, D-51 tie-break for ties (document ID, chunk index, chunk ID). Do NOT concatenate/flatten the lists first (that corrupts rank-position-based scoring, since a rank-1 result from variant 2 would sit behind a rank-20 result from variant 1 in a flattened list).
    - Rerank the merged list once (not once per variant), take(final_limit).
    - If the final candidate set is empty: set ctx.short_circuit = true, populate WorkflowCompleted's notices with the existing NO_EVIDENCE notice and its snapshot with the existing RetrievalSnapshot construction (main.rs:1508-1547's exact fields — REVIEWS.md Codex HIGH #3), return Ok(()) — do NOT treat this as an error.
    - If dense retrieval, BM25 retrieval, or fusion returns a genuine infrastructure error (not zero results): return Err(NodeError{kind: RetrievalFailed}).
    - With a FakeQueryReformulator returning 2 variants whose dense/BM25 candidate sets differ, the merged output's EXACT cross-variant score for at least one candidate is asserted against the documented formula above (not just relative order) — proves the merge is the real two-pass computation, not an approximation that happens to preserve ordering.
  </behavior>
  <action>
    In engine/src/retrieval/fusion.rs, add a new public function (name it clearly, e.g. `merge_fused_candidate_lists` or similar — executor's choice) taking `Vec<Vec<FusedCandidate>>` (one list per reformulation variant, each already sorted by that variant's own `fused_score` descending) and `&RetrievalSettings`, returning `Result<Vec<FusedCandidate>, RetrievalError>`, implementing exactly the formula documented in this task's Behavior block above. Document the formula in a doc comment on the function itself (equal per-variant weight of 1.0, `1.0 / (rrf_k + variant_rank)` per appearance, best-source-rank provenance, D-51 tie-break) so a future reader does not have to reverse-engineer it from the implementation.
    Create engine/src/workflow/nodes/retrieve.rs: `RetrieveHybridNode { dense_port: Arc&lt;dyn DenseRetrievalPort&gt;, bm25_index: Arc&lt;tokio::sync::RwLock&lt;Bm25Index&gt;&gt;, reranker: Arc&lt;dyn rerank::Reranker&gt;, retrieval_settings: RetrievalSettings, timeout: Duration }` implementing the Behavior block above: loop `ctx.reformulated_query`, for each variant build the per-variant `QueryRequest` (clone `ctx.query_request`, replace `.query`), run `self.dense_port.query(&ctx.query_embedding, &variant_request, ...)` + `self.bm25_index.read().await.retrieve(&variant_request, ...)` + `retrieval::fusion::fuse_candidates(...)` (mapping infra errors to `NodeError{kind: RetrievalFailed}`, mirroring the existing error-kind mapping from this plan's Interfaces excerpts), collect the per-variant `Vec&lt;FusedCandidate&gt;` results, call the new cross-variant merge function, rerank once, `take(final_limit)`, store into `ctx.vector_results`/`ctx.bm25_results` (or a single merged-candidates field — match whatever field names `WorkflowContext` already declares from 05-01; if 05-01's field shape doesn't cleanly fit a merged-candidate list, extend `WorkflowContext` with the minimal additional field needed and document why), on empty final candidates set `ctx.short_circuit = true` AND populate two new `WorkflowContext` fields `ctx.short_circuit_notices: Vec&lt;lancet::v1::Notice&gt;` and `ctx.short_circuit_snapshot: Option&lt;lancet::v1::RetrievalSnapshot&gt;` with the exact NO_EVIDENCE notice + `RetrievalSnapshot` construction main.rs:1508-1547 already performs (reuse that logic verbatim, relocated).
    In engine/src/workflow/mod.rs: add `pub short_circuit: bool`, `pub short_circuit_notices: Vec<lancet::v1::Notice>`, `pub short_circuit_snapshot: Option<lancet::v1::RetrievalSnapshot>` to `WorkflowContext` (defaults false/empty/None).
    In engine/src/workflow/runner.rs: after a node completes successfully (`Ok(())`), check `ctx.short_circuit` — if true, send `WorkflowCompleted{success: true, session_id: ctx.session_id.clone(), notices: ctx.short_circuit_notices.clone(), snapshot: ctx.short_circuit_snapshot.clone()}` immediately and return, skipping every remaining node in the `Vec` without sending any further `NodeStarted` for them (this generic check requires no per-node-name special-casing and remains correct as 05-03 appends `AssemblePrompt`/`GenerateAnswer`; on the non-short-circuit success path, `WorkflowCompleted`'s notices/snapshot stay empty/None here, matching 05-01's existing terminal-send behavior — 05-03's `GenerateAnswerNode` populates `FinalAnswer`'s own notices/snapshot on the normal happy path instead).
    In config/config.toml's `[engine.workflow]` section, add `retrieve_timeout_ms = 10000` (D-17 default).
    In engine/src/main.rs: at the retrieval-node construction call site, build `Arc::new(TableDenseRetrievalPort(self.nodes.clone()))` as the production port. Extend the node `Vec` to `vec![reformulate_node, graph_context_node, retrieve_hybrid_node]`, constructing `RetrieveHybridNode` from the port, `self.bm25_index`/`self.reranker`/`self.effective_settings.retrieval` fields plus `retrieve_timeout_ms`. Delete the now-superseded inline dense/BM25/fuse/rerank/D-03-check block from `query_rag` (verified above).
    In engine/src/tests.rs: un-ignore and rewrite any "pending 05-02" tests whose behavior this node now covers (zero-match, dense-snapshot-failure tests per the test names listed in 05-01's Interfaces excerpts — `query_rag_fail_closed_dense_snapshot`, `query_rag_valid_zero_match`); leave rerank/generation/citation-identity tests ignored for 05-03. Add a `FakeQueryReformulator` test double (mirrors `NoOpQueryReformulator`'s shape but returns 2+ configured variants) and a `FakeDenseRetrievalPort` (per this plan's Interfaces/Task 1 shape) and write three new in-process Tier 1 scenarios (no spawned process, calling `service.query_rag(...)` directly per 05-01's established pattern): (a) cross-variant merge — 2-variant `FakeQueryReformulator` with `FakeDenseRetrievalPort` configured so the two variants' dense candidate sets differ, assert the merged output's exact score for at least one candidate matches the documented formula; (b) D-03 zero-evidence — all variants return zero candidates, assert `WorkflowCompleted{success: true}` carries the NO_EVIDENCE notice and a populated snapshot, and zero `NodeStarted` events fire for any node after `retrieve_hybrid`; (c) RetrieveHybrid infrastructure error — `FakeDenseRetrievalPort` returns `Err(...)` on its first call (not empty `Ok(vec![])`), assert `NodeFailed{category: RetrievalFailed}` then `WorkflowCompleted{success: false}`, structurally distinguishable from scenario (b).
  </action>
  <verify>
    <automated>cd D:/Repos/lancet/engine &amp;&amp; cargo test --locked --bin engine -- retrieve_hybrid</automated>
  </verify>
  <acceptance_criteria>
    - engine/src/retrieval/fusion.rs contains a new pub function merging Vec<Vec<FusedCandidate>> into one Vec<FusedCandidate> with a doc comment stating the equal-per-variant-weight, 1.0/(rrf_k+rank), best-source-rank-provenance formula
    - engine/src/workflow/nodes/retrieve.rs contains RetrieveHybridNode depending on Arc&lt;dyn DenseRetrievalPort&gt;, implementing the loop-per-variant (with per-variant cloned QueryRequest) + merge + rerank-once sequence
    - A Tier 1 test with a 2-variant FakeQueryReformulator + FakeDenseRetrievalPort asserts an EXACT cross-variant score for at least one candidate, matching the documented formula (not just relative order)
    - A Tier 1 test asserts the D-03 zero-evidence path reaches WorkflowCompleted{success: true, notices: [NO_EVIDENCE], snapshot: Some(...)} with zero NodeStarted events for any node after retrieve_hybrid
    - A Tier 1 test asserts a genuine RetrieveHybrid infrastructure error (FakeDenseRetrievalPort Err on first call) reaches NodeFailed{category: RetrievalFailed} then WorkflowCompleted{success: false}
    - config/config.toml contains retrieve_timeout_ms = 10000 under [engine.workflow]
    - engine/src/workflow/mod.rs's WorkflowContext contains short_circuit: bool, short_circuit_notices, and short_circuit_snapshot fields
  </acceptance_criteria>
  <done>RetrieveHybridNode depends on DenseRetrievalPort, correctly RRF-merges across N reformulation variants using an explicit documented formula proven by an exact-score assertion, per-variant BM25 calls use the validated filters (not raw query text), D-03's short-circuit restores session_id/notices/snapshot on WorkflowCompleted, and a genuine retrieval error is distinguishable from the zero-evidence success path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine/src/workflow/nodes/retrieve.rs -> LanceDB/BM25 index (via DenseRetrievalPort) | Internal, trusted — but an infrastructure error here must not be mis-signaled as the benign zero-evidence path (integrity of the failure trail, not a classic attacker boundary) |
| engine/src/workflow/nodes/graph_context.rs -> DatabaseManager (graph tables, via GraphQueryPort) | Internal, trusted — same integrity-of-signal concern as above for D-09's degrade path |
| engine/src/workflow/ports.rs test doubles vs. production wrappers | Internal only — FakeGraphQueryPort/FakeDenseRetrievalPort must never be reachable from main.rs's production construction path; only DatabaseManagerGraphPort/TableDenseRetrievalPort are wired into the real service (moved forward from 05-04's original threat register per REVIEWS.md Codex HIGH #6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Repudiation | engine/src/workflow/nodes/graph_context.rs D-09 degrade path | medium | mitigate | The node's own inner timeout race treats timeout identically to a logical failure and both leave a distinguishable `attempted_and_failed`/`timed_out` tracing category, so a developer can still tell what happened from logs even though the query itself succeeds; the inner budget is strictly smaller than the runner's outer backstop so the degrade path always wins the race in the normal case |
| T-05-06 | Repudiation | engine/src/workflow/nodes/retrieve.rs zero-evidence vs. infrastructure-error | high | mitigate | D-03's short-circuit (`ctx.short_circuit`) is set ONLY on a genuinely empty final candidate list; any infrastructure error path returns `Err(NodeError{RetrievalFailed})` through the normal node-failure channel instead — the two paths are structurally distinct in code, not distinguished by a fragile string check, verified by the dedicated Tier 1 scenario in Task 2, and disambiguated with a FakeDenseRetrievalPort Err (not merely empty results) |
| T-05-09 | Tampering | engine/src/main.rs port construction | medium | mitigate | main.rs only ever constructs DatabaseManagerGraphPort/TableDenseRetrievalPort (the production wrappers) at the node-construction call sites in Tasks 1/2 — the Fake* doubles live only in engine/src/tests.rs, a test-only compilation unit never linked into the production binary |

</threat_model>

<verification>
1. `cargo test --locked --bin engine -- workflow` passes all scenarios from 05-01 plus this plan's graph-degrade, graph-timeout-degrade, cross-variant-merge (exact score), zero-evidence, and infrastructure-error scenarios, in-process. (`--lib` would run zero of these tests — `engine/src/tests.rs` compiles into the `engine` binary target.)
2. `cargo build --locked` succeeds with the 3-node Vec wired into query_rag and the port seam wired into production.
3. Every previously-`#[ignore]`d test in engine/src/tests.rs whose target behavior this plan builds is un-ignored and passing; every test still deferred still carries an accurate `#[ignore = "pending 05-0N"]` pointing at the correct future plan.
</verification>

<success_criteria>
- ExtractGraphContext and RetrieveHybrid execute in the corrected D-06 order (graph before retrieve), both real nodes with real timeout/degrade/failure semantics, not stubs.
- D-07's cross-variant RRF merge is proven with an actual multi-variant fake, not merely "passes because NoOp only returns one variant."
- D-03's short-circuit is generic runner-level logic, correct regardless of how many more nodes 05-03 appends.
- D-09's graph-timeout interpretation is explicitly flagged, tested, and rated for its true undo cost — not silently assumed.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-02-SUMMARY.md` when done
</output>

## Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|----------------|
| 05-02/05-04 dependency mismatch — graph-timeout test needs GraphQueryPort, introduced too late | HIGH (Codex) | Task 1: `GraphQueryPort`/`DenseRetrievalPort` (and their fakes) moved into this plan; 05-04 no longer introduces them |
| Graph node's inner and outer timeout read the same configured budget | HIGH (Codex, part of #6) | Task 1: split into `graph_timeout_ms` (inner) and `graph_outer_timeout_ms` (outer backstop, strictly larger) |
| Cross-variant RRF semantics underspecified (weight reapplication, provenance) | MEDIUM (Codex) | Task 2 Behavior block: explicit equal-per-variant-weight formula, best-source-rank provenance rule, exact-score test requirement |
| `WorkflowContext` has no validated filters/normalized query for per-variant BM25 | HIGH (Codex, carried from 05-01) | Task 2: per-variant `QueryRequest` cloned from `ctx.query_request` with `.query` replaced, filters preserved |
| New event contract drops session_id/no-evidence notice+snapshot | HIGH (Codex, carried from 05-01) | Task 2: D-03 short-circuit now populates `WorkflowCompleted`'s session_id/notices/snapshot with the exact pre-existing NO_EVIDENCE data |
| Pre-allocate `Vec<Vec<FusedCandidate>>` buffer capacity | LOW (Antigravity suggestion) | Left to executor discretion during implementation — not a plan-level acceptance criterion; noted here so it isn't lost |

## Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| `try_send`/reliable-delivery fix | Resolved entirely in 05-01 (the runner is shared code); this plan inherits the fix, no separate action needed here |
| Checkpoint payload size/exposure | Belongs to 05-05 where the checkpoint sidecar is populated; this plan's short-circuit fields are proto-level Notice/Snapshot data, not the raw checkpoint blob |
