---
phase: 05-state-machine-workflow-events
plan: 02
type: execute
wave: 3
depends_on:
  - 05-06
files_modified:
  - engine/src/workflow/mod.rs
  - engine/src/workflow/ports.rs
  - engine/src/workflow/nodes/mod.rs
  - engine/src/workflow/nodes/reformulate.rs
  - engine/src/workflow/nodes/graph_context.rs
  - engine/src/workflow/nodes/retrieve.rs
  - engine/src/workflow/runner.rs
  - engine/src/retrieval/fusion.rs
  - engine/src/main.rs
  - engine/src/tests.rs
autonomous: true
requirements: [ORCH-01, ORCH-03, ORCH-05]

must_haves:
  truths:
    - "The production runner executes ReformulateQuery, ExtractGraphContext, and RetrieveHybrid in that order, with graph augmentation before hybrid retrieval (D-06)."
    - "ReformulateQuery embeds only the first reformulated variant, while RetrieveHybrid processes every non-empty variant and performs a deterministic second RRF merge across per-variant results (D-07, D-08)."
    - "Graph logical failure, no match, and timeout leave graph_context empty but do not fail the query; the graph node always runs and preserves the existing silent-degrade behavior (D-09)."
    - "Dense, BM25, and graph operations are injected through WorkflowDependencies, so service-level tests can use deterministic fakes instead of accidentally constructing concrete LanceDB paths (review HIGH)."
    - "The production graph adapter owns a cloned DatabaseManager captured by value for its 'static port object; it does not borrow the service database across the spawned workflow boundary, and its source adapter covers the live attempt_graph_augmentation implementation at engine/src/main.rs:1056-1236 (about 181 lines)."
    - "Cross-variant RRF accepts at most 8 non-empty variants and accumulates at most variant_count * candidate_limit unique chunk IDs, with a hard maximum of 8 * 500 = 4,000; it processes one variant at a time rather than concatenating unbounded lists."
    - "WorkflowContext retains the accumulated vector_results and bm25_results source lists alongside additive final_candidates, so later checkpoints can reproduce both retrieval inputs and the fused output (D-28)."
    - "An empty or overlong reformulator result is rejected as a bounded internal workflow error rather than indexing variant zero or silently returning zero evidence (review MEDIUM)."
    - "The standalone QueryGraph RPC and its existing generated contract are not changed or routed through the workflow runner (D-10)."
    - "No new workflow metadata or degraded_mode field is persisted; the bounded graph outcome is retained only for sanitized diagnostics and the checkpoint's explicit empty graph_context remains the Phase 5 degradation signal (D-30 and AI-SPEC)."
  artifacts:
    - path: "engine/src/workflow/ports.rs"
      provides: "WorkflowDependencies plus object-safe graph, dense, and BM25 ports and production adapters"
    - path: "engine/src/workflow/nodes/graph_context.rs"
      provides: "ExtractGraphContextNode with inner timeout/degrade handling"
    - path: "engine/src/workflow/nodes/retrieve.rs"
      provides: "RetrieveHybridNode with empty-variant validation and per-variant retrieval"
    - path: "engine/src/retrieval/fusion.rs"
      provides: "Deterministic cross-variant RRF merge preserving candidate provenance"
    - path: "engine/src/main.rs"
      provides: "A production graph adapter that owns a cloned DatabaseManager while delegating the live attempt_graph_augmentation span"
    - path: "engine/src/retrieval/fusion.rs::MAX_REFORMULATION_VARIANTS"
      provides: "The exact accepted variant bound of 8 and MAX_CROSS_VARIANT_CANDIDATES formula of 8 * 500 = 4,000"
    - path: "engine/src/workflow/ports.rs::DatabaseManagerGraphQueryAdapter"
      provides: "The owned cloned-DatabaseManager graph port implementing the live graph augmentation delegation"
    - path: "engine/src/retrieval/fusion.rs::fuse_across_variants"
      provides: "Bounded one-variant-at-a-time deduplicated RRF accumulation with provenance and deterministic tie ordering"
    - path: "engine/src/tests.rs"
      provides: "Constructor migration for every LancetServiceImpl literal, preservation of the 05-01 drain_query_rag_stream caller/assertion migration, and service-level graph-before-retrieval fixture seams"
  key_links:
    - from: "WorkflowDependencies"
      to: "LancetServiceImpl::query_rag"
      via: "service construction supplies production adapters while configured test services supply fakes"
    - from: "ReformulateQueryNode"
      to: "ExtractGraphContextNode"
      via: "variant-zero embedding stored in WorkflowContext before graph augmentation"
    - from: "RetrieveHybridNode"
      to: "retrieval::fusion::fuse_candidates"
      via: "one dense/BM25 fusion per reformulated variant followed by cross-variant RRF"
    - from: "test_workflow_dependencies"
      to: "LancetServiceImpl::query_rag"
      via: "the dependency-order test injects GraphQueryPort, DenseRetrievalPort, and an isolated Bm25Index-backed seam, then observes the real service event sequence"
  prohibitions:
    - requirement_id: ORCH-01
      category: ordering
      statement: "MUST NOT restore the lightweight plan's RetrieveHybrid-before-ExtractGraphContext order; D-06's shipped graph-before-retrieval order is authoritative."
      verification: test
    - requirement_id: ORCH-03
      category: resilience
      statement: "MUST NOT add automatic retries to graph, dense, BM25, or embedding nodes; generation-only retry is implemented in 05-03 per D-11."
      verification: test
    - requirement_id: ORCH-05
      category: scope
      statement: "MUST NOT turn NoOpQueryReformulator into a real reformulation strategy or make RetrieveHybrid depend on a single hard-coded variant index."
      verification: test
---

<objective>
Expand the tracer into the graph-before-retrieval portion of the fixed Rust pipeline, with injectable ports, deterministic cross-variant fusion, and honest graph degradation.

**As a** Lancet engineer, **I want to** formalize the graph and retrieval stages as typed workflow nodes, **so that** I can debug and extend retrieval without changing the Go boundary.

Purpose: make the highest-risk testability and ordering decisions explicit before prompt assembly and generation are added.
Output: `WorkflowDependencies`, production/fake-compatible ports, graph and hybrid-retrieval nodes, and a tested cross-variant RRF primitive.
</objective>

<scope_justification>
05-02 intentionally keeps its exact 10-file surface consolidated: `WorkflowDependencies` DI ports, every `LancetServiceImpl` and test constructor, the graph/retrieval node wrappers, cross-variant bounded RRF, runner wiring, and the service test-host migration share one typed contract. These files must compile together; splitting DI/service wiring from graph/retrieval fusion would create an unbuildable, incoherent intermediate contract in which constructors and nodes disagree about the dependency map and the fusion tests lack a complete service host. This is an intentionally accepted measured scope exception.
</scope_justification>

<execution_sub_boundaries>
The exact 10-file surface is intentionally atomic, but execution is reviewable at these ordered checkpoints: (1) add the DI ports and update every `LancetServiceImpl` and test constructor, including the preserved streaming-test migration; (2) wrap graph and retrieval using the cited source spans — `engine/src/main.rs:1056-1236` for the live graph attempt, `engine/src/main.rs:1426-1448` for its outcome boundary, `engine/src/retrieval/fusion.rs::fuse_candidates` for the existing fusion primitive, and `engine/src/main.rs:1450-1495` for the retrieval call site; (3) add cross-variant bounded RRF with provenance, deterministic scoring, and focused bound tests; (4) only after `engine/src/tests.rs` compiles, run exact test registration, focused Rust, and full Rust gates. A failed checkpoint halts the task before the next sub-boundary.
</execution_sub_boundaries>

<scope_exception>
Exact file count: 10 files in `files_modified`.
Coupling rationale: these files are coupled across `WorkflowDependencies`, graph/retrieval node wrappers, cross-variant RRF, and the service test-host migration. Splitting DI/service wiring from graph/retrieval fusion would create an unbuildable, incoherent intermediate contract: constructors would not satisfy the node ports, the production runner could not be assembled, and cross-variant fusion tests would not have the migrated service host needed to exercise the real path. The 10-file scope is intentionally accepted.
Ordered execution sub-boundaries/checkpoints: (1) add DI ports and update every `LancetServiceImpl`/test constructor; (2) wrap graph and retrieval using the cited `engine/src/main.rs:1056-1236`, `engine/src/main.rs:1426-1448`, `engine/src/retrieval/fusion.rs::fuse_candidates`, and `engine/src/main.rs:1450-1495` source spans; (3) add cross-variant bounded RRF and its exact/provenance/bound tests; (4) run exact registration, focused Rust, and full Rust gates only after `engine/src/tests.rs` compiles against the streaming client and new dependency field.
Accepted review basis: this measured scope exception is accepted on the basis of the Phase 5 cross-AI review and the operator's explicit instruction to continue revisions until all checks pass.
</scope_exception>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-state-machine-workflow-events/05-01-SUMMARY.md
@.planning/phases/05-state-machine-workflow-events/05-06-SUMMARY.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-PATTERNS.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEWS.md
@.planning/phases/05-state-machine-workflow-events/05-AI-SPEC.md
@AGENTS.md
@rust-guidelines.md
</context>

## Artifacts this phase produces

- `engine/src/workflow/ports.rs`: `WorkflowDependencies`, `GraphQueryPort`, `DenseRetrievalPort`, `Bm25RetrievalPort`, and production adapters.
- `engine/src/workflow/mod.rs` and `engine/src/workflow/nodes/mod.rs`: workflow context/port exports and the production node registry.
- `engine/src/workflow/nodes/graph_context.rs`: `ExtractGraphContextNode` and bounded graph outcome mapping.
- `engine/src/workflow/nodes/retrieve.rs`: `RetrieveHybridNode` and retrieval failure mapping.
- `engine/src/retrieval/fusion.rs`: cross-variant RRF merge and exact-score tests.
- `engine/src/main.rs`: `LancetServiceImpl` wiring so production dependencies are explicit and test constructors can replace all retrieval ports.
- `engine/src/tests.rs`: every existing `LancetServiceImpl` literal initializes `workflow_dependencies` while preserving the existing fake Generator/EmbeddingProvider/Reranker behavior; the named `test_workflow_dependencies` fixture injects GraphQueryPort, DenseRetrievalPort, and an isolated Bm25Index-backed seam while exercising the real service ordering path.

<tasks>

<task type="tracer">
  <name>Task 1: Wire injectable workflow dependencies through graph and retrieval</name>
  <action_extension>Use `05-PATTERNS.md` and `05-RESEARCH.md` as the approved graph/retrieval design sources: the graph adapter delegates the actual `engine/src/main.rs:1056-1236` `attempt_graph_augmentation` path, while retrieval follows `engine/src/retrieval/fusion.rs::fuse_candidates` and the existing `engine/src/main.rs:1450-1495` call site. Preserve D-11 retry ownership outside these nodes, thread D-16 request cancellation into every injected port, and keep D-20 event boundaries at coarse node granularity.</action_extension>
  <action_extension>Use the concrete `LancetServiceImpl.workflow_dependencies` field and name the production factory `WorkflowDependencies::from_service_parts`. In `engine/src/tests.rs`, extend `configured_service` and update every existing `LancetServiceImpl { ... }` literal, including `query_graph_service_with_db`, to initialize that field while preserving every existing fake Generator, EmbeddingProvider, and Reranker instance, response sequence, and assertion. Add the named `test_workflow_dependencies` helper for `test_workflow_dependencies_preserve_graph_before_retrieval`; inject `Arc&lt;dyn GraphQueryPort&gt;`, `Arc&lt;dyn DenseRetrievalPort&gt;`, and an isolated Bm25Index-backed `Arc&lt;dyn Bm25RetrievalPort&gt;` seam, then exercise the real `LancetServiceImpl::query_rag` path rather than bypassing service construction. Coordinate this additive constructor migration with 05-01's `drain_query_rag_stream` helper and all migrated streaming callers/assertions in the same file; do not replace that helper or revert any caller to unary QueryRAG. Complete and compile this migration before any `cargo test -- --list`, focused Rust registration/execution, or full Rust-suite gate; those gates are invalid until `engine/src/tests.rs` compiles against the streaming client and new dependency field.</action_extension>
  <read_first>engine/src/main.rs, engine/src/tests.rs, 05-01-PLAN.md, engine/src/db/mod.rs, engine/src/workflow/mod.rs, engine/src/workflow/node.rs, engine/src/workflow/runner.rs, engine/src/workflow/nodes/reformulate.rs, engine/src/retrieval/fusion.rs, engine/src/retrieval/mod.rs, engine/src/graph/context_strategy.rs, 05-CONTEXT.md, 05-REVIEWS.md, rust-guidelines.md</read_first>
  <files>engine/src/workflow/mod.rs, engine/src/workflow/ports.rs, engine/src/workflow/nodes/mod.rs, engine/src/workflow/nodes/graph_context.rs, engine/src/workflow/nodes/retrieve.rs, engine/src/main.rs, engine/src/tests.rs</files>
  <behavior>
    - Production service -> WorkflowDependencies -> ReformulateQuery -> ExtractGraphContext -> RetrieveHybrid -> emitted completion.
    - Test construction -> same node graph with fake graph/dense/BM25 ports and no concrete LanceDB query in the fake path.
    - Graph port failure/timeout -> graph node completes with empty facts and the runner proceeds to retrieval.
    - Named exact test -> `test_workflow_dependencies_preserve_graph_before_retrieval` is registered and passes.
    - Every existing `LancetServiceImpl` constructor/literal in `engine/src/tests.rs`, including `configured_service` and `query_graph_service_with_db`, initializes the `workflow_dependencies` field before any focused test registration or execution; the existing fake Generator, EmbeddingProvider, and Reranker instances and response behavior remain unchanged.
    - The named `test_workflow_dependencies` helper builds the injected dependency map from GraphQueryPort, DenseRetrievalPort, and an isolated Bm25Index-backed seam, and `test_workflow_dependencies_preserve_graph_before_retrieval` calls the real `LancetServiceImpl::query_rag` instead of bypassing service construction.
  </behavior>
  <action>Define `engine::workflow::ports::WorkflowDependencies` with `Arc&lt;dyn QueryReformulator&gt;`, `Arc&lt;dyn GraphQueryPort&gt;`, `Arc&lt;dyn DenseRetrievalPort&gt;`, and `Arc&lt;dyn Bm25RetrievalPort&gt;` fields, plus production adapter constructors that wrap `attempt_graph_augmentation`, `DenseRetriever`, and the existing `Arc&lt;tokio::sync::RwLock&lt;Bm25Index&gt;&gt;`. Name the production graph adapter `DatabaseManagerGraphQueryAdapter`. It must be an owned `'static` object: accept a `DatabaseManager` by value or clone `LancetServiceImpl.database` at construction, capture that clone inside the adapter, and delegate the live `attempt_graph_augmentation` implementation spanning `engine/src/main.rs:1056-1236` (about 181 lines) rather than reimplementing a shortened graph path or borrowing the service field. This is safe because `DatabaseManager` is `Clone` and wraps the LanceDB connection. Add these dependencies to `LancetServiceImpl` and to every production/test service constructor, while keeping the existing generator, embedder, reranker, ingestion worker, and `DatabaseManager` ownership intact. Give each port the existing `BoxFuture` object-safe shape and typed error/result aliases; do not create a Rust PostgreSQL dependency or alter the Go boundary (D-26). Update the workflow module and node registry so the runner's production vector is `[ReformulateQueryNode, ExtractGraphContextNode, RetrieveHybridNode]`, explicitly satisfying the D-06 graph-before-retrieval correction. `ReformulateQueryNode` calls the injected NoOp reformulator, validates the returned variants, embeds only variant zero through the existing `EmbeddingProvider`, and stores that embedding in `WorkflowContext`; it does not add an LLM call or a second embedding fan-out (D-07, D-08, ORCH-05). Preserve `WorkflowContext.vector_results` and `WorkflowContext.bm25_results` as accumulated source-result DTOs for every retrieval variant, and retain `final_candidates` as an additional fused/reranked field rather than substituting for either source list. Construct the adapters in `main` from the already-created `DatabaseManager`, nodes table, BM25 lock, settings, and embedder; do not instantiate concrete retrievers inside individual nodes. This is the service-level dependency-injection correction for the Codex HIGH review finding, not an optional test-only indirection.</action>
  <verify>
    <automated>$rustList = @(cargo test --manifest-path engine/Cargo.toml --locked --bin engine -- --list 2>&amp;1)
$rustListExit = $LASTEXITCODE
if ($rustListExit -ne 0) { exit $rustListExit }
$expectedRustTests = @('test_workflow_dependencies_preserve_graph_before_retrieval')
foreach ($leaf in $expectedRustTests) {
  $escapedLeaf = [regex]::Escape($leaf)
  $registered = @($rustList | ForEach-Object { $line = $_.ToString(); if ($line -match ("^\s*(?<full>[A-Za-z0-9_:]*" + $escapedLeaf + "):\s+test\s*$")) { $Matches['full'] } })
  if ($registered.Count -ne 1) { throw "Expected one exact Rust registration ending in $leaf, found $($registered.Count)" }
  $fullName = $registered[0]
  $run = @(cargo test --manifest-path engine/Cargo.toml --locked --bin engine -- --exact $fullName --nocapture 2>&amp;1)
  $runExit = $LASTEXITCODE
  if ($runExit -ne 0) { exit $runExit }
  if (@($run | Where-Object { $_.ToString() -match ("^test\s+" + [regex]::Escape($fullName) + "\s+\.\.\.\s+ok\s*$") }).Count -lt 1) { throw "Exact Rust test did not report PASS: $fullName" }
}
cargo check --manifest-path engine/Cargo.toml --locked
$checkExit = $LASTEXITCODE
if ($checkExit -ne 0) { exit $checkExit }</automated>
  </verify>
  <acceptance_criteria>
    - `LancetServiceImpl` has one explicit `WorkflowDependencies` field and production construction supplies real adapters for reformulation, graph, dense, and BM25.
    - The node vector is ordered ReformulateQuery, ExtractGraphContext, RetrieveHybrid, and the order is asserted by an event-sequence test.
    - `QueryGraph` implementation and protobuf messages are absent from the plan's modified files and continue to compile unchanged.
    - A test service can provide fake graph/dense/BM25 ports without constructing a real `DenseRetriever` or reading a shared BM25 index.
    - No node adds an automatic retry; each port error reaches the runner as a typed node error or the graph-specific degrade path.
  </acceptance_criteria>
  <done>The tracer reaches graph and hybrid retrieval through a stable, injectable Rust dependency map in the locked execution order.</done>
</task>

<task type="auto">
  <name>Task 2: Implement graph degradation and cross-variant hybrid retrieval</name>
  <action_extension>For graph timeout behavior, follow the `05-PATTERNS.md` outcome-match analog at `engine/src/main.rs:1426-1448` and the resolved inner-timeout guidance in `05-RESEARCH.md`; for fusion, retain the bounded RRF/provenance contract of `engine/src/retrieval/fusion.rs`. The node must preserve D-16 cancellation as `Cancelled`, leave D-11's sole retry in `GenerateAnswer`, and emit no D-20 retrieval sub-step or retry-attempt events.</action_extension>
  <action_extension>Run Task 2 only after Task 1 has migrated and compiled `engine/src/tests.rs`, including all `workflow_dependencies` initializers and the preserved 05-01 streaming helper/callers. Its focused test-list/registration, exact focused execution, and full `cargo test --locked` gates are invalid if that migration is incomplete.</action_extension>
  <read_first>engine/src/workflow/ports.rs, engine/src/workflow/node.rs, engine/src/workflow/runner.rs, engine/src/workflow/nodes/reformulate.rs, engine/src/retrieval/fusion.rs, engine/src/main.rs, engine/src/db/mod.rs, engine/src/graph/context_strategy.rs, 05-REVIEWS.md, 05-AI-SPEC.md, AGENTS.md, rust-guidelines.md, 05-01-PLAN.md, 05-06-PLAN.md</read_first>
  <files>engine/src/workflow/nodes/reformulate.rs, engine/src/workflow/nodes/graph_context.rs, engine/src/workflow/nodes/retrieve.rs, engine/src/retrieval/fusion.rs, engine/src/workflow/runner.rs</files>
  <behavior>
    - Empty reformulator output -> typed internal failure with no indexing panic.
    - Two reformulation variants -> two dense/BM25 fusion passes and one deterministic second RRF merge with exact score assertions.
    - Graph no-match, logical error, and timeout -> successful node completion with empty graph context and a bounded diagnostic reason kept outside the checkpoint metadata contract; the stalled-graph test reaches the inner degradation deadline before the 15-second outer backstop and emits neither `NodeFailed` nor `WorkflowCompleted(failed)`.
    - Dense or BM25 error -> NodeFailed{RetrievalFailed} and WorkflowCompleted(failed), distinguishable from zero-evidence success.
    - Named exact tests -> `test_fuse_across_variants_exact_scores`, `test_retrieve_rejects_empty_or_overlong_variants`, `test_graph_timeout_degrades_and_continues`, and `test_reformulate_embeds_only_variant_zero` are each registered and pass.
  </behavior>
  <timeout_contract>Keep the configured GraphExtraction outer node timeout exactly 15 seconds by default per D-17. Define and consume the shared `GRAPH_TIMEOUT_DEGRADE_MARGIN` as `Duration::from_millis(100)`; derive `inner_deadline = max(Duration::from_millis(1), outer_duration.saturating_sub(GRAPH_TIMEOUT_DEGRADE_MARGIN))`, map only that inner elapsed result to successful empty-context degradation, and preserve cancellation as a distinct `Cancelled` mapping. Make `test_graph_timeout_degrades_and_continues` stall beyond the inner deadline but before the outer runner backstop.</timeout_contract>
  <action_extension>In the graph node implementation, pass the configured outer GraphExtraction duration into the node, compute `max(Duration::from_millis(1), outer_duration.saturating_sub(GRAPH_TIMEOUT_DEGRADE_MARGIN))` before starting the adapter future, and use a test outer value above the margin so the stalled graph resolves before the runner backstop. The focused stalled-graph test must collect the emitted event sequence and assert successful `ExtractGraphContext` completion with empty context, downstream terminal success, no `NodeFailed`, and no failed `WorkflowCompleted`; a cancellation fixture must still assert `Cancelled` rather than timeout degradation.</action_extension>
  <action>Implement `ExtractGraphContextNode` around the existing `GraphAugmentationOutcome` path. Always execute it for every query (D-09); run the graph adapter under the configured graph timeout and treat `NoMatchFound`, `AttemptedAndFailed`, and a genuine timeout as an empty `ctx.graph_context` plus a bounded internal outcome for the existing query span/log, then return `Ok(())` so the next node runs. Do not add a new durable `degraded_mode` or graph-reason metadata field: D-30 defers workflow metadata and the AI-SPEC defines the explicit empty graph_context checkpoint diff as the Phase 5 degradation signal; record this review disposition in the task summary. Implement `RetrieveHybridNode` to reject a zero-variant or blank/overlong variant with a sanitized `NodeErrorKind::Internal`, loop over every normalized reformulation variant (D-07), preserve each source result in `ctx.vector_results` or `ctx.bm25_results`, invoke existing `fuse_candidates` once per variant, and then call a new bounded `fuse_across_variants` function whose output is the additive `ctx.final_candidates`. Set `MAX_REFORMULATION_VARIANTS: usize = 8`; reject a ninth variant before any storage query, and retain the existing per-variant `candidate_limit` maximum of 500. The second pass must process one variant-local fused list at a time, deduplicate by chunk identity in a bounded accumulator with capacity `variant_count * settings.candidate_limit` and hard ceiling `MAX_CROSS_VARIANT_CANDIDATES = 8 * 500 = 4_000`, retain each candidate's source rank/score provenance, apply the existing final-limit/reranker path, and resolve ties with the repository's established identity key. Do not concatenate all variant lists or allocate beyond that deduplicated bound; return a typed internal error if an input violates the bound. Sum deterministic `1/(rrf_k + rank)` contributions across variant-local fused ranks using the existing one-based rank convention (`rank_index + 1`) from `fuse_candidates`. A dense or BM25 error becomes `NodeErrorKind::RetrievalFailed`; an empty result set remains the D-03 zero-evidence success that the runner short-circuits before prompt/generation. Add inline fusion tests using genuinely different rankings (variant one `A,B,C,D`; variant two `C,A,D,B`) so duplicate `C` and non-duplicate ranks exercise the exact formula, plus one-variant equivalence, duplicate chunk identity, deterministic ties, non-finite rejection, ninth-variant rejection, and the 4,000-entry bound. Keep `vector_results` and `bm25_results` available for the full D-28 snapshot while retaining `final_candidates` as the additive fused field (D-28).</action>
  <verify>
    <automated>$rustList = @(cargo test --manifest-path engine/Cargo.toml --locked --bin engine -- --list 2>&amp;1)
$rustListExit = $LASTEXITCODE
if ($rustListExit -ne 0) { exit $rustListExit }
$expectedRustTests = @('test_fuse_across_variants_exact_scores', 'test_retrieve_rejects_empty_or_overlong_variants', 'test_graph_timeout_degrades_and_continues', 'test_reformulate_embeds_only_variant_zero')
foreach ($leaf in $expectedRustTests) {
  $escapedLeaf = [regex]::Escape($leaf)
  $registered = @($rustList | ForEach-Object { $line = $_.ToString(); if ($line -match ("^\s*(?<full>[A-Za-z0-9_:]*" + $escapedLeaf + "):\s+test\s*$")) { $Matches['full'] } })
  if ($registered.Count -ne 1) { throw "Expected one exact Rust registration ending in $leaf, found $($registered.Count)" }
  $fullName = $registered[0]
  $run = @(cargo test --manifest-path engine/Cargo.toml --locked --bin engine -- --exact $fullName --nocapture 2>&amp;1)
  $runExit = $LASTEXITCODE
  if ($runExit -ne 0) { exit $runExit }
  if (@($run | Where-Object { $_.ToString() -match ("^test\s+" + [regex]::Escape($fullName) + "\s+\.\.\.\s+ok\s*$") }).Count -lt 1) { throw "Exact Rust test did not report PASS: $fullName" }
}
cargo fmt --manifest-path engine/Cargo.toml -- --check
 $fmtExit = $LASTEXITCODE
if ($fmtExit -ne 0) { exit $fmtExit }
cargo test --manifest-path engine/Cargo.toml --locked
 $fullRustExit = $LASTEXITCODE
if ($fullRustExit -ne 0) { exit $fullRustExit }
Push-Location gateway
try { go test ./...; $goExit = $LASTEXITCODE; if ($goExit -ne 0) { exit $goExit } } finally { Pop-Location }</automated>
  </verify>
  <acceptance_criteria>
    - `RetrieveHybridNode` never indexes an empty reformulation vector and every variant is bounded and normalized before retrieval.
    - `WorkflowContext` retains vector_results and bm25_results source lists, while final_candidates remains an additive fused result used by later nodes and snapshots.
    - Cross-variant fusion has an exact documented formula, preserves candidate source provenance, deduplicates chunk IDs, and has a two-variant exact-score test with rankings A,B,C,D and C,A,D,B rather than indistinguishable variant inputs.
    - Cross-variant fusion rejects more than 8 variants and bounds the deduplicated accumulator at variant_count * candidate_limit, never exceeding 4,000 entries when candidate_limit is 500.
    - The graph adapter captures a cloned DatabaseManager by value and delegates the full live attempt_graph_augmentation implementation rather than a borrowed or shortened substitute.
    - `test_graph_timeout_degrades_and_continues` stalls the graph beyond the shared inner deadline but before the D-17 outer deadline, asserts `ExtractGraphContext` emits successful completion with empty `graph_context`, downstream retrieval/terminal success, zero `NodeFailed` events, and zero `WorkflowCompleted(failed)` events; cancellation remains a distinct `Cancelled` mapping.
    - Dense/BM25 hard errors emit `RetrievalFailed`, while empty candidates trigger D-03's successful short circuit and no prompt/generation start.
    - The existing standalone QueryGraph RPC remains byte-for-byte behaviorally covered by its existing tests; no QueryGraph source or proto file is modified.
  </acceptance_criteria>
  <done>Graph augmentation and hybrid retrieval are real typed nodes with deterministic multi-variant fusion, injectable failure seams, and the locked D-03/D-06/D-09 behavior.</done>
</task>

</tasks>

<threat_model>
ASVS Level: 1. Blocking threshold: high. High-severity threats block plan acceptance/execution.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Query request → Rust workflow | Query text, filters, and reformulator output are bounded data entering typed nodes. |
| Rust workflow → LanceDB/graph/BM25 adapters | Retrieval inputs cross into local storage and graph query implementations. |
| Rust node outcome → client event stream | Internal retrieval failures are converted into client-visible categories and messages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-05 | Tampering | Dense/BM25/graph port inputs | high | mitigate | Reuse `QueryRequest::from_values`, validated retrieval settings, and typed adapter parameters; do not build predicates from unvalidated reformulator text. |
| T-05-06 | Denial of Service | Cross-variant retrieval loop | high | mitigate | Bound variant count/content by query limits, retain candidate limits at each source, and reject empty or oversized variants before storage queries. |
| T-05-07 | Information Disclosure | Graph/retrieval error messages | medium | mitigate | Keep adapter detail in logs and map client-visible failures through `NodeError::sanitized_message` and D-22 categories. |
| T-05-08 | Elevation of Privilege | QueryGraph boundary | low | accept | No new authorization surface exists in the local single-user engine; the plan leaves the existing standalone RPC contract unchanged per D-10. |
</threat_model>

<verification>
- Focused graph/retrieval/fusion tests pass with deterministic fakes and exact score assertions.
- The Rust event sequence proves graph-before-retrieval and the zero-evidence short circuit.
- `cargo fmt --manifest-path engine/Cargo.toml -- --check` and `cargo test --manifest-path engine/Cargo.toml --locked --bin engine` pass.
</verification>

<success_criteria>
The runner reaches retrieval through explicit ports, graph failure/timeout degrades predictably, multi-variant retrieval has deterministic RRF semantics, and a genuine dense/BM25 error remains distinguishable from D-03 zero evidence.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-02-SUMMARY.md` when done.
</output>
