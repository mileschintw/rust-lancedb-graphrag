---
phase: 05-state-machine-workflow-events
plan: 21
type: execute
wave: 16
depends_on: [05-16, 05-23]
files_modified:
  - engine/src/retrieval/fusion.rs
  - engine/src/retrieval/tests.rs
autonomous: true
gap_closure: true
requirements: [ORCH-03, ORCH-04]

must_haves:
  truths:
    - Fusion provenance source values are typed at construction and filtering boundaries.
    - Serialized provenance remains the stable lowercase vector or bm25 value through one serde enum serialization path; no parallel manual string serializer exists.
    - The provenance field has no ineffective serde default and the typed test prevents a return to String-based filtering.
  artifacts:
    - engine/src/retrieval/fusion.rs defines the serializable provenance-source enum and uses it in VariantProvenance.
    - engine/src/retrieval/tests.rs contains fusion_variant_provenance_source_is_typed.
  key_links:
    - dense/BM25 merge source -> VariantProvenanceSource -> candidate source filtering
    - VariantProvenanceSource serialization -> existing retrieval response JSON
    - typed source test -> production source guard
---

<objective>
Make fusion provenance type-safe and remove the dead serialization default identified by review.

Purpose: Close IN-02 and IN-03 without changing the wire-visible lowercase provenance values or the locked retrieval/fusion behavior.
Output: A typed provenance source, enum-based filtering, cleanup of the ineffective serde attribute, and focused regression coverage.
</objective>

## Artifacts this phase produces

- Typed serializable fusion provenance source enum and enum-based filters.
- Focused serialization and source-cleanup regression coverage.

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEW.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEWS.md
@.planning/phases/05-state-machine-workflow-events/05-PATTERNS.md
@.planning/phases/05-state-machine-workflow-events/05-16-PLAN.md
@engine/src/retrieval/fusion.rs
@engine/src/retrieval/tests.rs
</context>
<tasks>

<task type="tracer">
  <name>Task 1: Carry typed provenance through fusion filters</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; 05-CONTEXT.md; 05-16-PLAN.md; engine/src/retrieval/fusion.rs; engine/src/retrieval/tests.rs</read_first>
  <files>engine/src/retrieval/fusion.rs, engine/src/retrieval/tests.rs</files>
  <action>Replace VariantProvenance.source's free-form string boundary with a serializable, equality-comparable provenance-source enum whose serde serialization is the single path for lowercase vector and bm25 values. Use the enum in dense/BM25 provenance construction and in every source filter; do not add a parallel manual string conversion or JSON serializer. Remove the ineffective serde default attached to the Serialize-only provenance field. Add fusion_variant_provenance_source_tracer over one vector-plus-BM25 merge that asserts both typed source values and the unchanged provenance rank/score data. Preserve D-07 variant fusion, D-08 variant-zero embedding, and the existing retrieval JSON contract. Pattern reference: follow the `engine/src/workflow/nodes/retrieve.rs` assignment in `05-PATTERNS.md`, specifically its `engine/src/retrieval/fusion.rs::fuse_candidates` analog, for preserving source construction and RRF output while typing provenance.</action>
  <acceptance_criteria>
    - VariantProvenance stores the typed source enum and all source filters compare enum values.
    - Serialization still emits vector and bm25 as lowercase strings.
    - The tracer proves one merged vector/BM25 path without altering RRF scores, ranks, or candidate ordering.
  </acceptance_criteria>
  <verify>
    <automated>$name = 'fusion_variant_provenance_source_tracer'; $list = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $matches = @($list | Select-String -Pattern ('(^|::)' + [regex]::Escape($name) + ': test$')); if ($matches.Count -ne 1) { throw 'Fusion provenance tracer registration count mismatch' }; $run = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $name --nocapture; $runExit = $LASTEXITCODE; if ($runExit -ne 0) { exit $runExit }</automated>
  </verify>
  <done>The fusion path carries typed provenance values from source construction through filtering and remains wire-compatible in serialized output.</done>
</task>

<task type="auto">
  <name>Task 2: Prove provenance serialization and cleanup guards</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; 05-REVIEW.md; 05-REVIEWS.md; engine/src/retrieval/fusion.rs; engine/src/retrieval/tests.rs</read_first>
  <files>engine/src/retrieval/fusion.rs, engine/src/retrieval/tests.rs</files>
  <action>Add fusion_variant_provenance_source_is_typed to the existing retrieval test module. Assert enum identity, JSON lowercase serialization for vector and bm25, exact provenance rank/score values, and both source-specific candidate filters. Add source guards to the focused verification so a free-form source comparison or ineffective serde default cannot return without failing the plan. Keep this cleanup owned by fusion.rs and retrieval/tests.rs; do not defer it to a later phase. Pattern reference: follow the `engine/src/workflow/nodes/retrieve.rs` assignment in `05-PATTERNS.md`, specifically its `engine/src/retrieval/fusion.rs::fuse_candidates` analog, as the closest existing source-filter and wire-compatible fusion pattern; no separate pattern section covers the dead serde-default guard.</action>
  <acceptance_criteria>
    - The focused test proves typed identity, lowercase JSON serialization, and source-specific filtering.
    - The production source has no free-form string comparison at the provenance filter boundary and one serde enum path owns wire serialization.
    - The production source has no ineffective serde default on the Serialize-only provenance field.
  </acceptance_criteria>
  <verify>
    <automated>$source = Get-Content engine/src/retrieval/fusion.rs -Raw; if ($source -match 'source\s*==\s*"(vector|bm25)"') { throw 'String-based provenance filter remains' }; if ($source -match '#\[serde\(default\)\][\r\n]+\s*pub\s+variant_provenance') { throw 'Dead serde default remains on provenance' }; $name = 'fusion_variant_provenance_source_is_typed'; $list = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $matches = @($list | Select-String -Pattern ('(^|::)' + [regex]::Escape($name) + ': test$')); if ($matches.Count -ne 1) { throw 'Typed provenance registration count mismatch' }; $run = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $name --nocapture; $runExit = $LASTEXITCODE; if ($runExit -ne 0) { exit $runExit }</automated>
  </verify>
  <done>Review cleanup is executable: typed provenance behavior passes and source guards prevent the dead attribute and String filter from returning.</done>
</task>

</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Retrieval ports to fusion provenance | Dense and BM25 source labels influence which candidates are included in source-specific calculations. |
| Fusion model to serialized response | Provenance values cross the Rust response serialization boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-21-01 | Tampering | Provenance source filter | high | mitigate | Use an equality-comparable enum at construction and filter sites and assert vector/BM25 source behavior in the retrieval test module. |
| T-05-21-02 | Repudiation | Candidate provenance serialization | medium | mitigate | Preserve lowercase serialized values and exact rank/score assertions so source attribution remains inspectable. |
| T-05-21-03 | Information disclosure | Serde field behavior | low | mitigate | Remove the ineffective default and keep the existing serialized field shape without adding unrelated metadata. |
| T-05-21-SC | Tampering | npm/pip/cargo installs | high | accept | This plan adds no package-manager dependency or install task; the standard slopcheck register is not applicable to fusion type cleanup. |
</threat_model>

<verification>
- Both exact fusion test registrations pass in the library target.
- Serialization assertions preserve vector and bm25 values and existing rank/score behavior.
- The source guards reject free-form provenance filtering and the ineffective serde default.
</verification>

<success_criteria>
Fusion provenance is type-safe at construction, filtering, and serialization boundaries, with executable review cleanup and no deferred IN-02 or IN-03 item.
</success_criteria>

<output>
Create .planning/phases/05-state-machine-workflow-events/05-21-SUMMARY.md when done.
</output>
