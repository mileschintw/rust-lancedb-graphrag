---
phase: 05-state-machine-workflow-events
plan: 19
type: execute
wave: 15
depends_on: [05-10, 05-16, 05-17, 05-18]
files_modified:
  - engine/src/workflow/events.rs
  - engine/src/workflow/runner.rs
  - engine/src/tests/workflow_phase5.rs
  - gateway/main.go
  - gateway/main_test.go
autonomous: true
gap_closure: true
requirements: [ORCH-02, ORCH-03, ORCH-04]

must_haves:
  truths:
    - A failed workflow terminal event carries accumulated notices in order even though final_response is absent.
    - Failure terminals contain no AnswerChunk or FinalAnswer event and remain in-band SSE events with HTTP 200.
    - The gateway emits failure notices in the workflow_completed SSE payload without inventing a final response or answer.
  artifacts:
    - engine/src/workflow/events.rs accepts typed terminal notices and populates WorkflowCompletedEvent.notices.
    - engine/src/workflow/runner.rs passes the accumulated context notices on both success and failure terminal paths.
    - engine/src/tests/workflow_phase5.rs asserts failure ordering, notice preservation, and answer-event exclusion.
    - gateway/main.go maps WorkflowCompletedEvent.notices to the existing notice DTO shape.
    - gateway/main_test.go proves raw failure-terminal SSE notice visibility.
  key_links:
    - WorkflowContext.notices -> events::workflow_completed -> generated WorkflowCompletedEvent.notices
    - WorkflowCompletedEvent.notices -> writeWorkflowEventSSE -> workflow_completed JSON
    - terminal failure -> no FinalAnswer or AnswerChunk -> one in-band workflow_completed frame
    - 05-17 generated Notice field -> Rust and Go terminal implementations
---

<objective>
Preserve accumulated notices on failure terminal events from the Rust runner through the Go raw SSE response while keeping failure responses answer-free.

Purpose: Close WR-07 so degraded context and checkpoint notices remain client-visible when generation or a later node fails, without violating D-05, D-13, D-18, D-19, or D-30.
Output: Typed failure-terminal notices, Rust event-order assertions, and a Go raw SSE regression test.
</objective>

## Artifacts this phase produces

- Typed terminal notices on Rust success and failure WorkflowCompleted events.
- Rust event-order assertions and gateway raw SSE failure-terminal notice coverage.

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEW.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEWS.md
@.planning/phases/05-state-machine-workflow-events/05-PATTERNS.md
@.planning/phases/05-state-machine-workflow-events/05-17-PLAN.md
@.planning/phases/05-state-machine-workflow-events/05-18-PLAN.md
@engine/src/workflow/events.rs
@engine/src/workflow/runner.rs
@engine/src/tests/workflow_phase5.rs
@gateway/main.go
@gateway/main_test.go
</context>
<tasks>

<task type="tracer">
  <name>Task 1: Wire failure notices from runner to raw SSE</name>
  <read_first>AGENTS.md; rust-guidelines.md; go-guidelines.md; 05-PATTERNS.md; 05-CONTEXT.md; 05-17-PLAN.md; 05-18-PLAN.md; engine/src/pb/lancet/v1/lancet.v1.rs; engine/src/workflow/events.rs; engine/src/workflow/runner.rs; engine/src/workflow/ports.rs; engine/src/tests/workflow_phase5.rs; gateway/main.go; gateway/main_test.go; gateway/proto/lancet/v1/lancet.pb.go</read_first>
  <files>engine/src/workflow/events.rs, engine/src/workflow/runner.rs, engine/src/tests/workflow_phase5.rs, gateway/main.go, gateway/main_test.go</files>
  <action>Consume the generated WorkflowCompletedEvent.notices field from 05-17. Extend events::workflow_completed with typed Notice values, pass ctx.notices.clone() on both success and failure terminal paths, and keep failure final_response as None. In gateway writeWorkflowEventSSE, map the typed notices through the existing noticeDTO shape while leaving final_response absent when nil. Add one narrow Rust failure-terminal notice tracer and one raw SSE gateway tracer using a deterministic NodeFailed followed by a failed WorkflowCompleted containing an ordered GRAPH_TIMEOUT then GRAPH_DEGRADED notice sequence. Preserve D-05 in-band failure, D-13 no fabricated answer, D-18 streaming, D-19 SSE, and D-30's prohibition on generic workflow metadata.</action>
  <acceptance_criteria>
    - Rust WorkflowCompletedEvent construction carries typed notices on success and failure, with no failure final response.
    - Go workflow_completed SSE data contains ordered notices and omits final_response for a failed terminal.
    - The tracer path emits no AnswerChunk or FinalAnswer for the failed workflow and still returns HTTP 200 with a normally closing SSE stream.
  </acceptance_criteria>
  <verify>
    <automated>$rustName = 'workflow_phase5_failure_terminal_notices_tracer'; $rustList = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $rustListExit = $LASTEXITCODE; if ($rustListExit -ne 0) { exit $rustListExit }; $rustMatches = @($rustList | Select-String -Pattern ('(^|::)' + [regex]::Escape($rustName) + ': test$')); if ($rustMatches.Count -ne 1) { throw 'Rust tracer registration count mismatch' }; $rustRun = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $rustName --nocapture; $rustRunExit = $LASTEXITCODE; if ($rustRunExit -ne 0) { exit $rustRunExit }; $goName = 'TestRAGQueryFailureTerminalNoticesSSE'; $goList = go -C gateway test -list ('^' + $goName + '$'); $goListExit = $LASTEXITCODE; if ($goListExit -ne 0) { exit $goListExit }; if (@($goList | Select-String -SimpleMatch $goName).Count -ne 1) { throw 'Go tracer registration count mismatch' }; $goRun = go -C gateway test -run ('^' + $goName + '$') -count=1; $goRunExit = $LASTEXITCODE; if ($goRunExit -ne 0) { exit $goRunExit }</automated>
  </verify>
  <done>A failed workflow's accumulated notices travel through the Rust terminal event and gateway raw SSE without an answer payload.</done>
</task>

<task type="auto">
  <name>Task 2: Assert Rust failure terminal ordering and notice retention</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; 05-CONTEXT.md; 05-17-PLAN.md; 05-18-PLAN.md; engine/src/workflow/events.rs; engine/src/workflow/runner.rs; engine/src/tests/workflow_phase5.rs</read_first>
  <files>engine/src/workflow/events.rs, engine/src/workflow/runner.rs, engine/src/tests/workflow_phase5.rs</files>
  <action>Add workflow_phase5_failure_terminal_preserves_notices_without_answer_events using the existing library-target workflow fixtures. Drive a deterministic failure after at least two notices have accumulated, then assert NodeFailed precedes exactly one failed WorkflowCompleted, the terminal notices preserve code/message/severity order, final_response is absent, and no AnswerChunk or FinalAnswer event exists anywhere in the failed stream. Also assert the successful path still retains its existing final answer and response notices so the new typed terminal field does not erase success behavior.</action>
  <acceptance_criteria>
    - Rust failure terminal assertions cover ordering, cardinality, notice values, and absence of answer events.
    - The failed WorkflowCompleted event has success false and no final_response while the successful path remains unchanged.
    - The test runs in the library target established by 05-18 and does not rely on binary-only registration.
  </acceptance_criteria>
  <verify>
    <automated>$name = 'workflow_phase5_failure_terminal_preserves_notices_without_answer_events'; $list = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $matches = @($list | Select-String -Pattern ('(^|::)' + [regex]::Escape($name) + ': test$')); if ($matches.Count -ne 1) { throw 'Rust failure-terminal registration count mismatch' }; $run = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $name --nocapture; $runExit = $LASTEXITCODE; if ($runExit -ne 0) { exit $runExit }</automated>
  </verify>
  <done>Rust proves a failed terminal is notice-bearing and answer-free, with event order and success behavior explicitly covered.</done>
</task>

<task type="auto">
  <name>Task 3: Strengthen gateway raw SSE failure-terminal assertions</name>
  <read_first>AGENTS.md; go-guidelines.md; 05-PATTERNS.md; 05-CONTEXT.md; 05-17-PLAN.md; gateway/main.go; gateway/main_test.go; gateway/proto/lancet/v1/lancet.pb.go</read_first>
  <files>gateway/main.go, gateway/main_test.go</files>
  <action>Complete TestRAGQueryFailureTerminalNoticesSSE against the existing fake query stream and SSE parser. Assert HTTP 200, event-stream content type, NodeFailed before one workflow_completed frame, ordered notice DTO fields, no final_response key, no answer_chunk event, no final_answer event, and normal stream termination. Use the existing noticeDTO conversion convention so terminal notices have the same code, message, and severity JSON shape as query response notices.</action>
  <acceptance_criteria>
    - The raw SSE test fails if notices are dropped, reordered, or serialized under a different shape.
    - The raw SSE test fails if a failed terminal emits final_response, AnswerChunk, or FinalAnswer content.
    - The Go test is registered in the gateway module and uses fatal errors for setup, HTTP, JSON, and stream assertions.
  </acceptance_criteria>
  <verify>
    <automated>$name = 'TestRAGQueryFailureTerminalNoticesSSE'; $list = go -C gateway test -list ('^' + $name + '$'); $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; if (@($list | Select-String -SimpleMatch $name).Count -ne 1) { throw 'Go failure-terminal SSE registration count mismatch' }; $run = go -C gateway test -run ('^' + $name + '$') -count=1; $runExit = $LASTEXITCODE; if ($runExit -ne 0) { exit $runExit }</automated>
  </verify>
  <done>Gateway raw SSE exposes failure notices as an in-band terminal payload while proving the failure path emits no answer event.</done>
</task>

</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust workflow runner to protobuf event sink | Untrusted or degraded node outcomes become client-visible terminal events. |
| Protobuf event stream to gateway SSE | Typed event data is mapped into browser/client JSON over an HTTP stream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-19-01 | Tampering | Failed WorkflowCompleted terminal | critical | mitigate | Pass cloned accumulated notices, keep success false and final_response absent, and assert event order and answer-event exclusion. |
| T-05-19-02 | Repudiation | Degraded notices on failure | high | mitigate | Preserve code, message, severity, and order in typed protobuf notices and assert the same values in raw SSE. |
| T-05-19-03 | Information disclosure | Gateway terminal JSON | medium | mitigate | Serialize only the typed Notice fields and omit final_response for failed terminals; do not add generic workflow metadata. |
| T-05-19-SC | Tampering | npm/pip/cargo installs | high | accept | This plan adds no package-manager dependency or install task; the standard slopcheck register is not applicable to terminal-event wiring. |
</threat_model>

<verification>
- Rust exact filters prove failed terminal notice retention, event ordering, and absence of AnswerChunk and FinalAnswer.
- Go exact raw-SSE filter proves HTTP 200, ordered terminal notices, absent final_response, and normal stream closure.
- Existing successful terminal assertions continue to pass with the typed notices field populated.
- git diff --check passes and no generic workflow metadata fields are introduced.
</verification>

<success_criteria>
Clients receive accumulated notices on a failed workflow terminal through the typed protobuf notices field and raw SSE, while the failure remains answer-free, in-band, ordered, and compatible with the existing successful response path.
</success_criteria>

<output>
Create .planning/phases/05-state-machine-workflow-events/05-19-SUMMARY.md when done.
</output>
