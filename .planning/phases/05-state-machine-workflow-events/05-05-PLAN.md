---
phase: 05-state-machine-workflow-events
plan: 05
type: execute
wave: 6
depends_on:
  - 05-03
  - 05-06
  - 05-04
files_modified:
  - gateway/db/schema.hcl
  - gateway/db/schema.sql
  - gateway/db/query.sql
  - gateway/db/models.go
  - gateway/db/query.sql.go
  - gateway/checkpoint_sink.go
  - gateway/main.go
  - gateway/main_test.go
autonomous: false
requirements: [ORCH-02, ORCH-04]

must_haves:
  truths:
    - "Every standalone checkpoint event received by Go is inserted into PostgreSQL through the existing Go-owned sqlc store, and Rust never opens a PostgreSQL connection (D-23, D-26)."
    - "Checkpoint rows contain a Go-generated UUID string primary key, correlation-derived trace_id, node_name, valid jsonb context_snapshot, created_at, and a trace/time index; no serial sequence is introduced."
    - "The persisted context is a full accumulated snapshot, including vector_results and bm25_results plus additive final_candidates, not a per-node diff, and the Go SSE DTO never exposes context_snapshot_json to the client (D-27, D-28)."
    - "Checkpoint persistence is detached and bounded: the Wave 2 CheckpointDispatcher admits events without blocking SSE, retains backpressured or failed writes in its owned FIFO/drain contract, and a slow or canceled database write cannot delay the next SSE frame or prevent FinalAnswer (D-27, review MEDIUM)."
    - "The Go dispatcher and PostgreSQL sink consume the exact 05-01/05-06 CheckpointHandoff contract: primary capacity 1, FIFO overflow capacity 5, explicit Pending/transport-closed state, and explicit overflow rather than silent loss; 05-05 does not define a second queue policy."
    - "After PostgreSQL wiring lands, the in-memory checkpoint sink remains a test/development seam only, the PostgreSQL sink is the production default constructed by main, and CheckpointDispatcher remains the admission/drain coordinator rather than becoming a sink implementation."
    - "Standalone checkpoint events emitted after both NodeCompleted and NodeFailed boundaries are accepted and persisted, so failed/canceled workflow boundaries remain debuggable (review MEDIUM)."
    - "PostgreSQL integration tests use unique per-test schemas and the canonical TEST_DATABASE_URL; Atlas, Docker service db, and tests are explicitly aligned, and database query errors are fatal."
    - "Checkpoint rows have no TTL/cleanup job and there is no checkpoint fetch API/RPC; direct SQL inspection remains the only access path (D-24, D-25)."
  artifacts:
    - path: "gateway/db/schema.hcl"
      provides: "Atlas workflow_checkpoints table and trace/created_at index"
    - path: "gateway/db/schema.sql"
      provides: "sqlc schema source synchronized with Atlas"
    - path: "gateway/db/query.sql"
      provides: "Parameterized InsertWorkflowCheckpoint query"
    - path: "gateway/db/models.go"
      provides: "Generated WorkflowCheckpoint sqlc model (review HIGH omission corrected)"
    - path: "gateway/db/query.sql.go"
      provides: "Generated InsertWorkflowCheckpoint params/function"
    - path: "gateway/main.go"
      provides: "documentStore checkpoint method, PostgreSQL CheckpointSink binding, and bounded detached persistCheckpoint"
    - path: "gateway/checkpoint_sink.go"
      provides: "PostgreSQL-backed CheckpointSink extension over the Wave 2 dispatcher, with explicit retry/drain and pending delivery state"
    - path: "gateway/checkpoint_sink.go::checkpointPrimaryCapacity/checkpointOverflowCapacity"
      provides: "Go constants mirroring the locked primary capacity 1 and FIFO overflow capacity 5 from the 05-01/05-06 handoff contract"
    - path: "gateway/checkpoint_sink.go::postgresCheckpointSink/memoryCheckpointSink"
      provides: "Production PostgreSQL sink ownership and the explicitly test/development-only in-memory sink seam"
    - path: "gateway/main_test.go"
      provides: "fake-store lifecycle, isolated-schema PostgreSQL helper, real stream/disconnect and checkpoint tests"
  key_links:
    - from: "Rust standalone WorkflowEvent.Checkpoint"
      to: "gateway/checkpoint_sink.go::CheckpointDispatcher"
      via: "event.TraceId is passed explicitly into a transport envelope; the dispatcher owns nonblocking admission and FIFO overflow before invoking the PostgreSQL sink"
    - from: "gateway/checkpoint_sink.go::postgresCheckpointSink"
      to: "gateway/main.go::persistCheckpoint"
      via: "the Wave 5 sink implements the Wave 2 interface and supplies the existing five-second context-bounded SQL write"
    - from: "gateway/main.go::persistCheckpoint"
      to: "gateway/db/query.sql::InsertWorkflowCheckpoint"
      via: "parameterized sqlc call under context.WithTimeout(context.Background(), ingestCompensationTimeout)"
    - from: "gateway/main_test.go"
      to: "isolated workflow_checkpoints schema"
      via: "per-test schema clone, search_path-bound pool, bounded polling, fatal snapshot query errors"
  prohibitions:
    - requirement_id: ORCH-02
      category: disclosure
      statement: "MUST NOT include checkpoint JSON or raw accumulated corpus context in the SSE JSON DTO; the sidecar is a Go persistence concern."
      verification: test
    - requirement_id: ORCH-04
      category: boundary
      statement: "MUST NOT add a Rust PostgreSQL client, checkpoint fetch RPC, TTL cleanup job, serial checkpoint ID, or shared-schema claimant test."
      verification: test
---

<objective>
Persist the Rust workflow's full standalone checkpoint events through Go into an Atlas/sqlc PostgreSQL table, with detached writes, schema push, and real cross-runtime verification.

**As a** Lancet engineer, **I want to** inspect durable workflow snapshots without stalling a query, **so that** failed and slow RAG runs remain diagnosable at the Go/PostgreSQL boundary.

Purpose: close ORCH-04 while preserving the Rust-owned data plane and the thin Go control-plane boundary.
Output: synchronized Atlas/sqlc schema and generated files, a PostgreSQL-backed extension of the Wave 2 nonblocking checkpoint sink, and environment-explicit Tier 2 tests.
</objective>

<dependency_note>
This plan is Wave 6 and depends on 05-04 as well as 05-03/05-06. The explicit dependency removes the nominal Wave 5 coordination gap: Tier 1 Rust tests and the shared 05-01/05-06 CheckpointHandoff/Pending/overflow contract are complete before Go duplicates the protocol constants and makes PostgreSQL the production sink.
</dependency_note>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-state-machine-workflow-events/05-03-SUMMARY.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-PATTERNS.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEWS.md
@.planning/phases/05-state-machine-workflow-events/05-AI-SPEC.md
@.planning/phases/05-state-machine-workflow-events/COVERAGE.md
@AGENTS.md
@go-guidelines.md
</context>

## Artifacts this phase produces

- `gateway/db/schema.hcl` and `gateway/db/schema.sql`: synchronized `workflow_checkpoints` table and `(trace_id, created_at)` index.
- `gateway/db/query.sql`, `gateway/db/models.go`, and `gateway/db/query.sql.go`: `InsertWorkflowCheckpoint` source, generated model, params, and method.
- `gateway/main.go`: `documentStore.InsertWorkflowCheckpoint`, `persistCheckpoint`, checkpoint-aware SSE loop, and no snapshot leakage in the DTO.
- `gateway/checkpoint_sink.go`: PostgreSQL sink binding that extends the Wave 2 bounded dispatcher without moving persistence into the SSE loop.
- `gateway/main_test.go`: isolated per-test schema helper, lifecycle-safe fake store, checkpoint fidelity, cancellation atomicity, backpressure, invalid-request, and real disconnect tests.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: confirm the durable workflow checkpoint schema contract</name>
  <action_extension>Use the existing `gateway/db/schema.hcl` `documents`/`document_reconciliation_intents` conventions and `gateway/db/query.sql:10-30` `InsertDocument` `INSERT ... RETURNING *` pattern, as mapped by `05-PATTERNS.md` and `05-RESEARCH.md`, to review the new checkpoint schema; use the approved AI-SPEC contract where no existing checkpoint-table analog exists.</action_extension>
  <read_first>05-CONTEXT.md, 05-RESEARCH.md, 05-AI-SPEC.md, 05-REVIEWS.md, 05-01-PLAN.md, 05-04-PLAN.md, 05-06-PLAN.md, gateway/db/schema.hcl, gateway/db/schema.sql, gateway/atlas.hcl</read_first>
  <files>gateway/db/schema.hcl, gateway/db/schema.sql, gateway/db/query.sql, gateway/db/models.go, gateway/db/query.sql.go</files>
  <decision>The executor will create the new durable `workflow_checkpoints` table with a Go-generated UUID string key, JSONB full-snapshot payload, explicit trace/node columns, and a trace/time index.</decision>
  <context>D-23, D-24, D-26, and D-28 lock durable Go-owned full snapshots, while the exact new table shape is a planner decision whose later change requires coordinated migration and codegen work.</context>
  <options>
    <option id="proceed-versioned-snapshot">
      <name>Proceed with versioned snapshot contract</name>
      <pros>Matches the locked full-snapshot and indefinite-retention requirements, keeps checkpoint payloads queryable, and gives later migrations an explicit schema_version boundary.</pros>
      <cons>The chosen key/JSON shape becomes a durable migration surface after rows exist.</cons>
    </option>
  </options>
  <resume-signal>Select: proceed-versioned-snapshot</resume-signal>
  <action>At execution time, confirm the single available schema option against the locked D-23/D-24/D-26/D-28 contract and the shared 05-01/05-04/05-06 checkpoint handoff contract, then record the approval in the plan summary before Task 1 changes Atlas, sqlc, or Go persistence artifacts. The selected schema task must not reopen the Rust-owned data-plane, Go-owned PostgreSQL boundary, or the exact primary-1/FIFO-overflow-5 Pending/transport-closed semantics already fixed by those plans.</action>
  <verify>
    <automated>if (-not (Test-Path -LiteralPath 'gateway/db/schema.hcl')) { exit 1 }; if (-not (Test-Path -LiteralPath 'gateway/db/schema.sql')) { exit 1 }</automated>
    <human-check>Confirm the selected option before applying the schema task.</human-check>
  </verify>
  <acceptance_criteria>The executor records approval of the exact schema contract before modifying the durable table and generated query surface.</acceptance_criteria>
  <done>The one-way schema decision is explicitly acknowledged without reopening the locked Go/PostgreSQL ownership or full-snapshot requirements.</done>
</task>

<task type="tracer">
  <name>Task 1: Trace one checkpoint from a Rust event into generated Go/PostgreSQL code</name>
  <action_extension>There is no existing SSE/runner persistence analog, so follow `05-AI-SPEC.md`/`05-RESEARCH.md` for the transport-only checkpoint design. Apply D-16 native client-close propagation through the request context and gRPC stream while detached persistence continues independently, and apply D-20 coarse client event granularity so checkpoint snapshots never become retrieval sub-step or retry-attempt SSE frames.</action_extension>
  <read_first>gateway/db/schema.hcl, gateway/db/schema.sql, gateway/db/query.sql, gateway/db/models.go, gateway/db/query.sql.go, gateway/sqlc.yaml, gateway/atlas.hcl, docker-compose.yml, gateway/checkpoint_sink.go, gateway/main.go, gateway/main_test.go, 05-01-PLAN.md, 05-04-PLAN.md, 05-06-PLAN.md, 05-CONTEXT.md, 05-REVIEWS.md, AGENTS.md, go-guidelines.md</read_first>
  <files>gateway/db/schema.hcl, gateway/db/schema.sql, gateway/db/query.sql, gateway/db/models.go, gateway/db/query.sql.go, gateway/checkpoint_sink.go, gateway/main.go</files>
  <reversibility rating="one-way">The workflow_checkpoints table and generated query/model surface are durable schema artifacts; changing their JSON shape or primary-key strategy after rows exist requires coordinated migration and codegen changes.</reversibility>
  <precondition>The executor can run `sqlc generate` and `go build ./...` from `gateway`; when `TEST_DATABASE_URL` is present, it is the only database URL permitted for Atlas and `psql` inspection, while its absence follows the explicit schema-verification skip in this task.</precondition>
  <behavior>
    - Schema source -> sqlc generation -> WorkflowCheckpoint model and InsertWorkflowCheckpoint method.
    - Standalone WorkflowEvent.Checkpoint -> detached Go persist call -> parameterized insert with explicit event trace_id.
    - Wave 2 CheckpointDispatcher -> PostgreSQL CheckpointSink -> persistCheckpoint -> sqlc insert, with blocked/failed writes retained for explicit drain or pending observation.
    - SSE frame -> public event fields only; checkpoint JSON remains internal to persistence.
  </behavior>
  <action>In `gateway/db/schema.hcl`, add `workflow_checkpoints` under `schema.public` with `id varchar(36) NOT NULL` as the primary key, `trace_id varchar(255) NOT NULL`, `node_name varchar(100) NOT NULL`, `context_snapshot jsonb NOT NULL`, and `created_at timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP`; add a non-unique index named `workflow_checkpoints_trace_id_created_at_idx` over exactly `(trace_id, created_at)`. Mirror the exact columns/index name in `gateway/db/schema.sql`, then add a parameterized `InsertWorkflowCheckpoint :one` query to `gateway/db/query.sql` that accepts the Go-generated id, trace_id, node_name, and JSON bytes and returns the inserted row. Run `sqlc generate` from `gateway`; do not hand-edit generated output. Require `gateway/db/models.go` to contain the generated `WorkflowCheckpoint` model and `gateway/db/query.sql.go` to contain `InsertWorkflowCheckpointParams` and `InsertWorkflowCheckpoint`, explicitly correcting the review's missing-model-file finding. In `gateway/main.go`, extend `documentStore` and `postgresStore` with `InsertWorkflowCheckpoint(ctx, id, traceID, nodeName string, snapshot []byte) error`, and add `persistCheckpoint` with an explicit `CheckpointEnvelope` argument that creates `context.WithTimeout(context.Background(), ingestCompensationTimeout)`, generates `uuid.NewString()`, calls the sqlc-backed store, and logs bounded operational detail on error. In `gateway/checkpoint_sink.go`, consume rather than redefine the 05-01/05-06 handoff contract: set `checkpointPrimaryCapacity = 1` and `checkpointOverflowCapacity = 5`, retain FIFO admission/drain and exact Pending/transport-closed/overflow results, and keep `CheckpointDispatcher` as the coordinator. Implement `postgresCheckpointSink` as the production sink wrapper that invokes `persistCheckpoint`; retain `memoryCheckpointSink` only for tests/development and do not select it from production `main` after this plan. Requeue failed writes through the existing bounded FIFO and leave them visible to the explicit `Drain`/`Pending` contract. In the streaming `queryRAG` loop, recognize standalone transport-only Checkpoint events, build an envelope using `event.TraceId` plus node name and full JSON bytes, and call `a.checkpointDispatcher.Dispatch(envelope)` without awaiting it or invoking the SQL sink inline; continue without writing a checkpoint-only event as an SSE frame, while client-visible events continue through the normal flush path. Never await persistence from `Recv`/Flush, never read trace identity from the checkpoint message, and never put its JSON into the client DTO. Keep Go as the PostgreSQL owner and Rust as the LanceDB/RAG owner (D-26), retain raw local-demo checkpoint content under D-23's accepted-risk framing, and add no TTL or fetch endpoint (D-24, D-25). After defining the schema and query, run `sqlc generate` and `go build ./...` from `gateway`, capturing each native exit code immediately. Use `$database = $env:TEST_DATABASE_URL` as the only canonical database variable for the remainder of the gate. If that variable is absent or whitespace-only, emit the explicit labeled schema-inspection skip required below and do not claim schema verification passed. When it is present, require `psql`; missing `psql` is a hard failure. Start the root Docker `db` service and capture its exit code immediately, then run the `psql` connectivity preflight with `ON_ERROR_STOP=1` and `SELECT 1`, capturing its native exit code immediately. Only clear connection-refused, timeout, no-route, or server-unavailable diagnostics may produce the explicit environment-gated skip; authentication, permission, missing-database, malformed-query, schema-access, and every other error must fail hard. After a successful preflight, run the blocking Atlas apply against `$database`, capture its exit code immediately, and run strict read-only assertions grounded in the exact schema sources: `information_schema.columns` must return exactly the five ordered rows `id|character varying|NO|36|<NULL>|<NULL>|<NULL>|<NULL>`, `trace_id|character varying|NO|255|<NULL>|<NULL>|<NULL>|<NULL>`, `node_name|character varying|NO|100|<NULL>|<NULL>|<NULL>|<NULL>`, `context_snapshot|jsonb|NO|<NULL>|<NULL>|<NULL>|<NULL>`, and `created_at|timestamp without time zone|NO|<NULL>|<NULL>|<NULL>|6|CURRENT_TIMESTAMP` for column name, data type, nullability, character length, numeric precision, numeric scale, datetime precision, and default; `pg_constraint` must return exactly `workflow_checkpoints_pkey|p|PRIMARY KEY (id)`; and the sole non-primary index must be `workflow_checkpoints_trace_id_created_at_idx`, non-unique, with `trace_id,created_at` in that order and the exact `pg_indexes.indexdef` ordered-column expression. Capture each `psql` exit code immediately; any nonzero exit or any extra/missing/reordered/typed/nullability/default/length/precision/primary-key/index mismatch is a hard failure. Do not substitute another database, add TTL or a fetch endpoint, or move PostgreSQL ownership from Go (D-24, D-25, D-26).</action>
  <schema_inspection_contract>Use `$database = $env:TEST_DATABASE_URL` for every Atlas and `psql` operation. If `TEST_DATABASE_URL` is absent or whitespace-only, print `SKIP: workflow_checkpoints schema verification skipped because canonical TEST_DATABASE_URL is absent; schema verification did not pass` and stop the schema inspection portion; this is the only absence-based skip. If the variable is present, `psql` must be installed or the task fails. Run a connectivity preflight with `psql --set=ON_ERROR_STOP=1 --command "SELECT 1"` and capture its native exit code immediately. A nonzero preflight exit may be classified as an environment-gated skip only when its captured diagnostics clearly match connection refused, connection timeout, no route/network unreachable, or an unavailable/not-accepting server; authentication failure, permission failure, missing database, malformed SQL, schema access failure, and every other diagnostic fail hard. If preflight succeeds, run `information_schema.columns`, `pg_constraint`, `pg_indexes`, and `pg_index` assertions with `ON_ERROR_STOP=1`, capture each `psql` exit code immediately after its invocation, and hard-fail on any nonzero query exit. Ground the exact expected rows in the planned `gateway/db/schema.sql` and `gateway/db/schema.hcl`: the ordered column metadata must be exactly `id|character varying|NO|36|<NULL>|<NULL>|<NULL>|<NULL>`, `trace_id|character varying|NO|255|<NULL>|<NULL>|<NULL>|<NULL>`, `node_name|character varying|NO|100|<NULL>|<NULL>|<NULL>|<NULL>`, `context_snapshot|jsonb|NO|<NULL>|<NULL>|<NULL>|<NULL>|<NULL>`, and `created_at|timestamp without time zone|NO|<NULL>|<NULL>|<NULL>|6|CURRENT_TIMESTAMP`, covering name, data type, nullability, character length, numeric precision, numeric scale, datetime precision, and default. The primary-key constraint result must be exactly `workflow_checkpoints_pkey|p|PRIMARY KEY (id)`. The non-primary index result must contain exactly one row named `workflow_checkpoints_trace_id_created_at_idx`, report `false` for `pg_index.indisunique`, report the exact ordered key list `trace_id,created_at`, and show a `pg_indexes.indexdef` whose ordered columns are exactly `(trace_id, created_at)`; missing, extra, reordered, or otherwise mismatched metadata fails.</schema_inspection_contract>
  <verify>
    <automated>
Push-Location gateway
try {
  sqlc generate
  $sqlcExit = $LASTEXITCODE
  if ($sqlcExit -ne 0) { exit $sqlcExit }
  go build ./...
  $buildExit = $LASTEXITCODE
  if ($buildExit -ne 0) { exit $buildExit }
} finally { Pop-Location }

$database = $env:TEST_DATABASE_URL
if ([string]::IsNullOrWhiteSpace($database)) {
  Write-Output 'SKIP: workflow_checkpoints schema verification skipped because canonical TEST_DATABASE_URL is absent; schema verification did not pass'
  exit 0
}
if ($null -eq (Get-Command psql -ErrorAction SilentlyContinue)) {
  throw 'psql is required when TEST_DATABASE_URL is present; schema inspection cannot be skipped'
}

docker compose up -d db
$composeExit = $LASTEXITCODE
if ($composeExit -ne 0) { exit $composeExit }

$preflightOutput = @(psql --dbname $database --set=ON_ERROR_STOP=1 --tuples-only --no-align --command 'SELECT 1' 2>&amp;1)
$preflightExit = $LASTEXITCODE
if ($preflightExit -ne 0) {
  $preflightText = ($preflightOutput | ForEach-Object { $_.ToString() }) -join "`n"
  $unreachablePattern = '(?i)(connection refused|connection timed out|connect(?:ion)? timeout|timeout expired|no route to host|network is unreachable|server is unavailable|database system is (?:starting up|not accepting connections|shutting down))'
  $hardFailurePattern = '(?i)(authentication failed|password authentication failed|permission denied|database .* does not exist|syntax error|malformed)'
  if ($preflightText -match $hardFailurePattern -or $preflightText -notmatch $unreachablePattern) {
    throw "PostgreSQL connectivity preflight failed: $preflightText"
  }
  Write-Output 'SKIP: workflow_checkpoints schema verification skipped because the canonical TEST_DATABASE_URL database is unreachable; schema verification did not pass'
  exit 0
}

Push-Location gateway
try {
  atlas schema apply --env local --var "db_url=$database" --auto-approve
  $atlasExit = $LASTEXITCODE
  if ($atlasExit -ne 0) { exit $atlasExit }

  $columnRows = @(psql --dbname $database --set=ON_ERROR_STOP=1 --tuples-only --no-align --command "SELECT c.column_name || '|' || c.data_type || '|' || c.is_nullable || '|' || COALESCE(c.character_maximum_length::text, '<NULL>') || '|' || COALESCE(c.numeric_precision::text, '<NULL>') || '|' || COALESCE(c.numeric_scale::text, '<NULL>') || '|' || COALESCE(c.datetime_precision::text, '<NULL>') || '|' || COALESCE(c.column_default, '<NULL>') FROM information_schema.columns AS c WHERE c.table_schema = 'public' AND c.table_name = 'workflow_checkpoints' ORDER BY c.ordinal_position" 2>&amp;1)
  $columnExit = $LASTEXITCODE
  if ($columnExit -ne 0) {
    $columnError = ($columnRows | ForEach-Object { $_.ToString() }) -join "`n"
    throw "workflow_checkpoints information_schema query failed: $columnError"
  }
  $expectedColumns = @(
    'id|character varying|NO|36|<NULL>|<NULL>|<NULL>|<NULL>',
    'trace_id|character varying|NO|255|<NULL>|<NULL>|<NULL>|<NULL>',
    'node_name|character varying|NO|100|<NULL>|<NULL>|<NULL>|<NULL>',
    'context_snapshot|jsonb|NO|<NULL>|<NULL>|<NULL>|<NULL>|<NULL>',
    'created_at|timestamp without time zone|NO|<NULL>|<NULL>|<NULL>|6|CURRENT_TIMESTAMP'
  )
  $actualColumns = @($columnRows | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ })
  if ($actualColumns.Count -ne $expectedColumns.Count) { throw "workflow_checkpoints columns mismatch: $($actualColumns -join ', ')" }
  for ($i = 0; $i -lt $expectedColumns.Count; $i++) {
    if ($actualColumns[$i] -ne $expectedColumns[$i]) { throw "workflow_checkpoints columns mismatch: $($actualColumns -join ', ')" }
  }

  $constraintRows = @(psql --dbname $database --set=ON_ERROR_STOP=1 --tuples-only --no-align --command "SELECT c.conname || '|' || c.contype || '|' || pg_get_constraintdef(c.oid, true) FROM pg_constraint AS c JOIN pg_class AS rel ON rel.oid = c.conrelid JOIN pg_namespace AS ns ON ns.oid = rel.relnamespace WHERE ns.nspname = 'public' AND rel.relname = 'workflow_checkpoints' AND c.contype = 'p' ORDER BY c.conname" 2>&amp;1)
  $constraintExit = $LASTEXITCODE
  if ($constraintExit -ne 0) {
    $constraintError = ($constraintRows | ForEach-Object { $_.ToString() }) -join "`n"
    throw "workflow_checkpoints primary-key query failed: $constraintError"
  }
  $expectedConstraints = @('workflow_checkpoints_pkey|p|PRIMARY KEY (id)')
  $actualConstraints = @($constraintRows | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ })
  if ($actualConstraints.Count -ne $expectedConstraints.Count -or $actualConstraints[0] -ne $expectedConstraints[0]) { throw "workflow_checkpoints primary-key mismatch: $($actualConstraints -join ', ')" }

  $indexRows = @(psql --dbname $database --set=ON_ERROR_STOP=1 --tuples-only --no-align --command "SELECT i.indexname || '|' || CASE WHEN ix.indisunique THEN 'true' ELSE 'false' END || '|' || COALESCE((SELECT string_agg(att.attname, ',' ORDER BY idx_key.ordinal) FROM unnest(ix.indkey) WITH ORDINALITY AS idx_key(attnum, ordinal) JOIN pg_attribute AS att ON att.attrelid = ix.indrelid AND att.attnum = idx_key.attnum), '<NULL>') || '|' || i.indexdef FROM pg_indexes AS i JOIN pg_class AS index_rel ON index_rel.relname = i.indexname JOIN pg_namespace AS index_ns ON index_ns.oid = index_rel.relnamespace AND index_ns.nspname = i.schemaname JOIN pg_index AS ix ON ix.indexrelid = index_rel.oid WHERE i.schemaname = 'public' AND i.tablename = 'workflow_checkpoints' AND i.indexname <> 'workflow_checkpoints_pkey' ORDER BY i.indexname" 2>&amp;1)
  $indexExit = $LASTEXITCODE
  if ($indexExit -ne 0) {
    $indexError = ($indexRows | ForEach-Object { $_.ToString() }) -join "`n"
    throw "workflow_checkpoints pg_indexes/pg_index query failed: $indexError"
  }
  $expectedIndexPrefix = 'workflow_checkpoints_trace_id_created_at_idx|false|trace_id,created_at|'
  $actualIndex = @($indexRows | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ })
  if ($actualIndex.Count -ne 1 -or -not $actualIndex[0].StartsWith($expectedIndexPrefix)) { throw "workflow_checkpoints index name/uniqueness/order mismatch: $($actualIndex -join ', ')" }
  $indexDefinition = $actualIndex[0].Substring($expectedIndexPrefix.Length)
  if ($indexDefinition -notmatch '(?i)^CREATE INDEX\s+workflow_checkpoints_trace_id_created_at_idx\s+ON\s+public\.workflow_checkpoints\s+USING\s+btree\s*\(\s*trace_id\s*,\s*created_at\s*\)$') { throw "workflow_checkpoints index definition mismatch: $indexDefinition" }
} finally { Pop-Location }
    </automated>
  </verify>
  <acceptance_criteria>
    - Atlas HCL and sqlc SQL contain matching workflow_checkpoints columns and `(trace_id, created_at)` index; the table uses a Go-supplied UUID string and no serial sequence.
    - The reachable PostgreSQL schema gate compares all five `information_schema.columns` rows for exact data type, `NO` nullability, `varchar` lengths, timestamp precision, and default values; asserts exactly `workflow_checkpoints_pkey|p|PRIMARY KEY (id)`; and proves through `pg_indexes` joined to `pg_index` that the sole non-primary index is non-unique with the exact ordered keys `trace_id,created_at` and matching `indexdef`.
    - `gateway/db/models.go` contains `WorkflowCheckpoint`, and generated query code contains `InsertWorkflowCheckpointParams` plus `InsertWorkflowCheckpoint`.
    - `persistCheckpoint` takes a `CheckpointEnvelope` whose traceID came from the standalone event, uses the existing five-second detached-write timeout, generates the row ID, and logs errors without panicking.
    - The SSE loop dispatches standalone checkpoint envelopes into the bounded dispatcher for successful and failed node boundaries without blocking `Recv`/Flush; the PostgreSQL sink is invoked only by the detached worker, and the DTO omits context_snapshot_json.
    - A blocked or temporarily failing PostgreSQL sink leaves every accepted envelope in FIFO drain/pending state; the backpressure test proves FinalAnswer still arrives and the released sink receives every checkpoint in order.
    - When `TEST_DATABASE_URL` is present, the blocking Atlas command runs against that canonical database after schema/codegen changes and before the live verification task; missing `psql`, failed Atlas/apply or connectivity checks outside the classified unreachable set, any reachable query/auth/permission/malformed/schema mismatch, and any metadata/primary-key/index mismatch fail hard. When the URL is absent, the automated gate emits an explicit environment-gated schema-verification skip and does not claim the schema inspection passed.
    - No Rust file, checkpoint fetch endpoint, cleanup job, or alternative database owner is introduced.
  </acceptance_criteria>
<done>One real standalone checkpoint event can flow through the Wave 2 dispatcher, the PostgreSQL sink, generated Go SQL code, and a correctly shaped PostgreSQL row without entering the SSE payload when the canonical database environment is available; backpressure remains observable, and an absent or classified-unreachable environment records an explicit schema-verification skip without claiming inspection success.</done>
</task>

<task type="auto">
  <name>Task 2: Prove detached persistence, full snapshots, isolation, and real boundary cancellation</name>
  <action_extension>Use `05-AI-SPEC.md` and `05-RESEARCH.md` as the approved checkpoint persistence and cancellation contract, with the existing `gateway/main_test.go` isolated-schema helper as the test analog; no separate checkpoint-table implementation or transport policy is introduced here.</action_extension>
  <action_extension>Make the real disconnect test the D-16 boundary proof: a closed request context cancels stream consumption and exposes undelivered checkpoint envelopes. Keep D-20 assertions on client-visible event cardinality, separate from full snapshot contents stored in PostgreSQL.</action_extension>
  <read_first>gateway/main.go, gateway/checkpoint_sink.go, gateway/main_test.go, gateway/db/models.go, gateway/db/query.sql.go, gateway/db/schema.hcl, docker-compose.yml, 05-01-PLAN.md, 05-04-PLAN.md, 05-06-PLAN.md, 05-REVIEWS.md, AGENTS.md, go-guidelines.md</read_first>
  <precondition>When `TEST_DATABASE_URL` is present it points at the schema pushed by Task 1; tests that spawn the real Rust engine can build and run the existing local provider fixture without external credentials.</precondition>
  <files>gateway/checkpoint_sink.go, gateway/main.go, gateway/main_test.go</files>
  <behavior>
    - Isolated schema + real SSE happy path -> one full generate-boundary snapshot row with original_query, reformulated_query, vector_results, bm25_results, final_candidates, graph_context, assembled_prompt, answer, and correlation-derived trace.
    - Genuine client disconnect -> no partially written checkpoint JSON for that trace; a complete row or no row is acceptable under detached timing.
    - Blocking InsertWorkflowCheckpoint fake -> FinalAnswer still arrives and fake exits on context cancellation before cleanup.
    - Blocking CheckpointSink fake -> dispatcher retains every envelope, then drains all envelopes in FIFO order after release; closed sink -> exact pending envelope set and explicit delivery error.
    - Real invalid request -> HTTP 4xx before SSE headers; real Go→gRPC→Rust disconnect -> Rust cancellation path is exercised.
    - Named exact Go tests -> `TestWorkflowCheckpointPersistence`, `TestWorkflowCheckpointCancellationAtomicity`, `TestWorkflowCheckpointBackpressureDoesNotStallSSE`, and `TestQueryRAGRealInvalidRequestAndDisconnect` are registered; the fake-store/dispatcher backpressure and real boundary tests are always runnable and must report an exact PASS, while only the two PostgreSQL-backed persistence tests may report an explicit `TEST_DATABASE_URL is not set` skip when the environment gate is absent.
  </behavior>
  <skip_policy>The fake-store/dispatcher `TestWorkflowCheckpointBackpressureDoesNotStallSSE` and real boundary `TestQueryRAGRealInvalidRequestAndDisconnect` paths are always runnable and have no `TEST_DATABASE_URL` skip branch; both must register and report `--- PASS`. Only `TestWorkflowCheckpointPersistence` and `TestWorkflowCheckpointCancellationAtomicity` may call `t.Skip("TEST_DATABASE_URL is not set")` when the canonical database URL is absent.</skip_policy>
  <action_extension>Implement the skip policy in the test bodies and fixtures: the fake-store/dispatcher backpressure and real invalid/disconnect tests must not inspect `TEST_DATABASE_URL` or call `t.Skip`, and their exact verification paths must require `--- PASS`. Keep the explicit environment-gated skip only in the two PostgreSQL-backed tests named by `<skip_policy>`, with the exact message and no silent skip.</action_extension>
  <action>Update every `documentStore` fake, including the existing `fakeStore` and any engine-function test fixture, with a recording `InsertWorkflowCheckpoint` implementation so the full Go suite compiles. Add `newWorkflowCheckpointsIsolatedPostgres(t, databaseURL)` following `newD04IsolatedPostgres`: create a unique schema name, clone `public.workflow_checkpoints` with `LIKE ... INCLUDING ALL`, set each claimant pool's `search_path` to only that schema before queries, close pools before dropping the schema, and make every external snapshot/count query fatal with `t.Fatalf` on error per AGENTS.md. Use `TEST_DATABASE_URL` as the single test variable; the blocking schema gate in Task 1 receives that same value with `--var db_url=...`, and Docker's service name remains `db`. Add a recording/blocking `CheckpointSink` fake for the Wave 2 dispatcher: block its persistence method, prove `FinalAnswer` still arrives, release it, call the explicit drain contract, and assert every full envelope is delivered in FIFO order; close the fake and assert the exact retained envelope set is exposed as pending. Assert the Go dispatcher uses the exact primary-1/overflow-5 contract from 05-01/05-06 and that `memoryCheckpointSink` remains test/development-only while `postgresCheckpointSink` is the production default after Task 1. At the start of each database-dependent test, call `t.Skip("TEST_DATABASE_URL is not set")` when the variable is absent; the focused command still executes and reports skips, while the always-runnable fake-store and dispatcher backpressure tests remain active. Add environment-gated Tier 2 tests with explicit names and bounded polling: `TestWorkflowCheckpointPersistence` runs the real Go route and Rust stream, then waits for the generate_answer row and validates trace_id, node_name, valid JSON, and the full accumulated D-28 fields (original_query, reformulated_query, vector_results, bm25_results, final_candidates, graph_context, assembled_prompt, answer); `TestWorkflowCheckpointCancellationAtomicity` cancels a genuine SSE request mid-query and asserts the isolated table has either no row or complete valid JSON for that trace, never a partial write; `TestWorkflowCheckpointBackpressureDoesNotStallSSE` uses the blocking sink/store and proves FinalAnswer arrives within the normal test envelope; and `TestQueryRAGRealInvalidRequestAndDisconnect` uses the existing real-engine process conventions to prove pre-stream HTTP 4xx/trailers and client-close propagation across Go→gRPC→Rust. Add a real checkpoint-bearing event DTO assertion that no `checkpoint` or context_snapshot_json field appears in SSE data. Run the complete unfiltered Go suite and vet after the focused tests; distinguish local Tier 1 coverage from these environment-gated Tier 2 results in the summary.</action>
  <verify>
    <automated>Push-Location gateway
try {
  $databaseBackedTests = @('TestWorkflowCheckpointPersistence', 'TestWorkflowCheckpointCancellationAtomicity')
  $alwaysRunnableTests = @('TestWorkflowCheckpointBackpressureDoesNotStallSSE', 'TestQueryRAGRealInvalidRequestAndDisconnect')
  $expectedGoTests = $databaseBackedTests + $alwaysRunnableTests
  foreach ($name in $expectedGoTests) {
    $escapedName = [regex]::Escape($name)
    $list = @(go test ./... -list ("^" + $escapedName + "$") 2>&amp;1)
    $listExit = $LASTEXITCODE
    if ($listExit -ne 0) { exit $listExit }
    $registered = @($list | Where-Object { $_.ToString() -match ("^" + $escapedName + "$") })
    if ($registered.Count -ne 1) { throw "Expected exactly one exact Go test registration for $name, found $($registered.Count)" }
    $run = @(go test -v -count=1 ./... -run ("^" + $escapedName + "$") 2>&amp;1)
    $runExit = $LASTEXITCODE
    if ($runExit -ne 0) { exit $runExit }
    $pass = @($run | Where-Object { $_.ToString() -match ("^--- PASS:\s+" + $escapedName + "(?:\s|$)") })
    $skip = @($run | Where-Object { $_.ToString() -match ("^--- SKIP:\s+" + $escapedName + "(?:\s|$)") })
    if ($pass.Count -gt 0 -and $skip.Count -gt 0) { throw "Exact Go test reported both PASS and SKIP: $name" }
    if ($alwaysRunnableTests -contains $name) {
      if ($skip.Count -gt 0 -or $pass.Count -lt 1) { throw "Always-runnable Go test must report PASS without SKIP: $name" }
    } else {
      if ($pass.Count -lt 1 -and $skip.Count -lt 1) { throw "Exact database-backed Go test did not report PASS or SKIP: $name" }
      if ($skip.Count -gt 0 -and -not ($run | Where-Object { $_.ToString().Contains('TEST_DATABASE_URL is not set') })) { throw "Unlabeled environment skip for $name" }
      if ($skip.Count -gt 0 -and $env:TEST_DATABASE_URL) { throw "Database-backed Go test skipped even though TEST_DATABASE_URL is set: $name" }
    }
  }
  go test -count=1 ./...
  $fullGoExit = $LASTEXITCODE
  if ($fullGoExit -ne 0) { exit $fullGoExit }
  go vet ./...
  $vetExit = $LASTEXITCODE
  if ($vetExit -ne 0) { exit $vetExit }
} finally { Pop-Location }
cargo test --manifest-path engine/Cargo.toml --locked
$rustExit = $LASTEXITCODE
if ($rustExit -ne 0) { exit $rustExit }</automated>
  </verify>
  <acceptance_criteria>
    - All checkpoint integration tests use the unique per-test schema for every claimant, fixture, insert, and read; no shared mutable table is globally claimed.
    - Snapshot query/setup errors call `t.Fatalf` and cannot become false-passing before/after comparisons.
    - The generate-boundary row contains the full accumulated snapshot, not only the node-local delta, and the JSON is valid and within the Rust-produced size bound.
    - Detached writes do not block SSE forwarding, and blocking fakes terminate on context cancellation before isolated schema cleanup.
    - Dispatcher backpressure tests prove a blocked or closed PostgreSQL sink retains exact envelopes, later drains them in order after release, and never reports a retained envelope as persisted.
    - `TestWorkflowCheckpointBackpressureDoesNotStallSSE` and `TestQueryRAGRealInvalidRequestAndDisconnect` are always runnable and must report `--- PASS` with no skip branch; only the two PostgreSQL-backed tests may use the exact `TEST_DATABASE_URL is not set` skip when that environment gate is absent.
    - The unfiltered Go suite and `go vet ./...` pass after all fake-store and generated-model changes.
  </acceptance_criteria>
  <done>ORCH-04 is proven across Rust event production, Go detached persistence, generated SQL, live PostgreSQL, isolated test schemas, and real stream cancellation without leaking checkpoint payloads to SSE clients.</done>
</task>

</tasks>

<threat_model>
ASVS Level: 1. Blocking threshold: high. High-severity threats block plan acceptance/execution.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Rust WorkflowEvent → Go gateway | Checkpoint payload and correlation identity cross the internal streaming boundary. |
| Go gateway → PostgreSQL | Untrusted/provider-derived snapshot bytes enter a parameterized JSONB insert. |
| Test process → PostgreSQL schema | Integration fixtures create and drop per-test schemas against a local database. |
| Client connection → detached write lifecycle | Client disconnect can cancel the stream while a bounded background insert is in flight. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-18 | Tampering | Checkpoint SQL insert | high | mitigate | Use generated parameterized sqlc queries, UUID row IDs, explicit trace/node fields, and no dynamic values in SQL. |
| T-05-19 | Denial of Service | Detached persistence | high | mitigate | Use a five-second context timeout, never await persistence in the SSE loop, and require blocking fakes to return on cancellation. |
| T-05-20 | Information Disclosure | JSONB checkpoint content | medium | accept | D-23 explicitly accepts raw corpus content in local PostgreSQL for debugging; the service is localhost-scoped, no new redaction system is introduced, and SSE excludes the payload. |
| T-05-21 | Tampering | Integration schema fixtures | high | mitigate | Use unique per-test schemas, clone the exact public table, bind every pool's search_path before queries, and drop only the owned schema after pools close. |
| T-05-22 | Repudiation | Trace/session checkpoint identity | medium | mitigate | Pass event.TraceId explicitly, assert it equals correlation_id and remains separate from session_id, and persist node_name with every row. |
</threat_model>

<verification>
- Run the blocking Atlas schema push using the canonical `TEST_DATABASE_URL` before any live checkpoint test when the URL is present; record the explicit labeled schema-verification skip without claiming success when it is absent or the connectivity preflight classifies the database as unreachable.
- Run focused environment-gated checkpoint and real-boundary tests, then the full Go suite and vet.
- Run the full Rust suite from the previous wave and confirm generated model/query files are committed artifacts.
</verification>

<success_criteria>
Checkpoint rows are durable, queryable, full-snapshot, trace-correct, size-bounded, and isolated from the SSE client; slow or canceled writes do not stall a real stream, and the Go/Rust/PostgreSQL boundary has live evidence.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-05-SUMMARY.md` when done.
</output>
