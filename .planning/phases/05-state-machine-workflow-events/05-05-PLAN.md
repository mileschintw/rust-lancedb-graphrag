---
phase: 05-state-machine-workflow-events
plan: 05
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - gateway/db/schema.hcl
  - gateway/db/schema.sql
  - gateway/db/query.sql
  - gateway/db/query.sql.go
  - gateway/main.go
  - gateway/main_test.go
  - engine/src/workflow/runner.rs
  - engine/src/workflow/events.rs
  - engine/src/workflow/mod.rs
autonomous: true
requirements: [ORCH-02, ORCH-04]
user_setup: []

must_haves:
  truths:
    - "Checkpoint rows are persisted to PostgreSQL — durable and queryable via psql, not memory-only or JSON files (D-23, ORCH-04)."
    - "Each checkpoint row's context_snapshot is a full accumulated WorkflowContext snapshot as of that node boundary (every field populated by any prior node restated, not just the current node's new field) — not an incremental diff (D-28), proven by asserting the checkpoint taken after GenerateAnswer still includes original_query, reformulated_query, vector/bm25 results, etc."
    - "Rust never opens its own Postgres connection — checkpoint payloads travel to Go over the existing streaming gRPC connection as a WorkflowEvent sidecar field (delivered via the same reliable .send(...).await 05-01 established for every WorkflowEvent — the checkpoint sidecar rides on an already-reliably-delivered NodeCompleted, it is not a separately lossy send), and Go's existing pgxpool-backed store persists them (D-26)."
    - "Checkpoint WRITES (the Postgres INSERT specifically) are fire-and-forget on the Go side — the query pipeline does not wait for Go's Postgres write to confirm before proceeding to the next node; a dropped/delayed checkpoint write never stalls or fails the user's actual query (D-27). This is the ONLY fire-and-forget leg in the whole checkpoint path — REVIEWS.md Codex+Antigravity consensus HIGH corrected an earlier draft that also applied non-blocking try_send to the Rust->gRPC event channel itself; that channel is reliable per 05-01, this plan's Go-side goroutine dispatch is where D-27 actually lives."
    - "trace_id (the same value as correlation_id) is present on every persisted checkpoint row, joinable back to the WorkflowEvent stream of the same query (D-29)."
    - "No TTL/retention cleanup job exists for checkpoint rows in v1, and no new fetch API/RPC is added — direct DB inspection (psql or a local script) is the only v1 access path (D-24, D-25)."
    - "A cancellation triggered by an actual SSE client disconnect through Go leaves no partially-written checkpoint row in Postgres — a checkpoint write either fully completes or is cleanly absent (AI-SPEC dimension 3's Postgres-atomicity half, Section 3 pitfall #2)."
    - "The client-facing SSE frame Go writes for a NodeCompleted event carrying a checkpoint sidecar NEVER includes the checkpoint/context_snapshot_json payload — 05-01's SSE-facing DTO already excludes this field by construction (this plan's must_haves.prohibitions below); this plan confirms the invariant holds once the field starts carrying real, non-empty data instead of always-unset placeholder content."
    - "A never-draining/slow InsertWorkflowCheckpoint write (simulated via a documentStore fake that blocks or sleeps) does not measurably delay FinalAnswer's arrival at the SSE client or inflate any node's measured duration — the relocated AI-SPEC reference dataset scenario 14 (moved here from an earlier draft of 05-04 per REVIEWS.md Codex+Antigravity consensus HIGH, since D-27's fire-and-forget property genuinely lives on this Go-side write, not the Rust event channel), proven as a Tier 2 Go test."
    - "gateway/db/schema.sql (the file gateway/sqlc.yaml actually reads for codegen, per its `schema: \"db/schema.sql\"` directive) carries the identical workflow_checkpoints table definition as gateway/db/schema.hcl (the Atlas source) — sqlc generate and Atlas apply both target the same schema, not two silently diverging sources (REVIEWS.md Codex HIGH — an earlier draft only updated schema.hcl, which sqlc never reads)."
    - "Edge-probe ORCH-04 (unclassified, closed by planner review): a full 5-node happy-path query produces exactly 5 workflow_checkpoints rows (one per node boundary), all sharing one trace_id, independently verifiable via direct psql inspection with no fetch API — the complete ORCH-04 acceptance surface demonstrated in one manual/scripted check, not just the per-property must-haves above taken in isolation."
  artifacts:
    - path: "gateway/db/schema.hcl"
      provides: "workflow_checkpoints table (trace_id, node_name, context_snapshot jsonb, created_at) — Atlas migration source"
    - path: "gateway/db/schema.sql"
      provides: "the identical workflow_checkpoints table definition — sqlc's actual codegen source per gateway/sqlc.yaml"
    - path: "gateway/db/query.sql"
      provides: "InsertWorkflowCheckpoint :one query"
  key_links:
    - from: "engine/src/workflow/runner.rs"
      to: "engine/src/workflow/events.rs checkpoint snapshot builder"
      via: "after each node's successful completion, serialize the current WorkflowContext to JSON and attach it as the NodeCompleted event's checkpoint sidecar field, delivered via the same reliable .send(...).await as every other event"
      pattern: "checkpoint"
    - from: "gateway/main.go queryRAG SSE loop"
      to: "gateway/db postgresStore.InsertWorkflowCheckpoint"
      via: "go a.persistCheckpoint(cp) fire-and-forget goroutine using a bounded context.WithTimeout(context.Background(), ingestCompensationTimeout) (the existing 5s constant, gateway/main.go:45), dispatched only when event.GetCheckpoint() != nil, never blocking the stream.Recv()/Flush() loop"
      pattern: "persistCheckpoint|InsertWorkflowCheckpoint"
  prohibitions:
    - requirement_id: ORCH-04
      category: safety
      statement: "MUST NOT leave a checkpoint write half-committed when a node future is dropped mid-poll on cancellation or timeout — a Postgres row is either fully written or cleanly absent, never a partial row (05-AI-SPEC.md Section 3 pitfall #2, Critical Failure Mode #3)."
      verification: test
    - requirement_id: ORCH-04
      category: availability
      statement: "MUST NOT let a backpressured or slow Postgres write block or stall the SSE forwarding loop or the underlying query pipeline (D-27, Critical Failure Mode #4)."
      verification: test
    - requirement_id: ORCH-02
      category: privacy
      statement: "MUST NOT forward the raw checkpoint context_snapshot payload (D-28's full corpus/prompt/evidence/embedding snapshot) to the SSE client — it is for Go's own Postgres persistence only; the SSE-facing DTO strips this field unconditionally (prohibition-probe recall item, carried forward from 05-01, reaffirmed here where the field first carries real data)."
      verification: test
---

<objective>
Complete ORCH-04 by giving the checkpoint payloads 05-01 through 05-04 have been carrying inside `WorkflowContext` an actual durable home: a new `workflow_checkpoints` Postgres table, written by Go (never by Rust directly, per D-26) as a fire-and-forget side effect of the same SSE-forwarding loop 05-01 built, sourced from a full-snapshot JSON payload the `WorkflowRunner` attaches to each `NodeCompleted` event.

Purpose: every other plan in this phase produced in-memory/in-stream correctness; this plan is what makes that correctness durable and inspectable after the fact, which is the entire point of ORCH-04 ("checkpoints... for workflow state during development and debugging"). It also closes the AI-SPEC's Tier 2 half of the cancellation-atomicity dimension — the one property Tier 1 (engine-only) tests structurally cannot prove, since it requires a real cross-process Postgres write racing a real cross-process disconnect.
Output: `gateway/db/schema.hcl`'s new `workflow_checkpoints` table, `gateway/db/query.sql`'s `InsertWorkflowCheckpoint` query (+ regenerated `query.sql.go`), `engine/src/workflow/runner.rs`'s checkpoint-snapshot attachment on every `NodeCompleted`, `gateway/main.go`'s fire-and-forget persistence dispatch, and Tier 2 Go+Postgres tests proving payload fidelity and cancellation atomicity.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-AI-SPEC.md
@go-guidelines.md
@rust-guidelines.md

<interfaces>
<!-- Verified live this session against gateway/db/schema.hcl, gateway/db/query.sql, gateway/main.go. -->

From gateway/db/schema.hcl (VERIFIED live — the exact table-block convention to follow, `documents`/`users` tables use varchar(255)/varchar(50) ids and a `default = sql("CURRENT_TIMESTAMP")` timestamp column):
Match this convention exactly for the new `workflow_checkpoints` table: `trace_id varchar(255)` (= correlation_id, D-29 — not unique per row, multiple checkpoint rows share one trace_id across the 5 node boundaries of one query), `node_name varchar(100)`, `context_snapshot jsonb` (D-28 full snapshot, `pgx/v5` supports `jsonb` natively via `pgtype.JSONB` — no new Go dependency), `created_at timestamp default CURRENT_TIMESTAMP`. Use a surrogate serial/uuid `id` primary key column following the `users` table's precedent (schema.hcl lines 5-27) since no natural single-column or composite key cleanly identifies one checkpoint row.

From gateway/db/query.sql:10-30 (VERIFIED live — the exact single-row insert convention `InsertWorkflowCheckpoint` must follow):
```sql
-- name: InsertDocument :one
INSERT INTO documents (id, filename, file_size, status, chunk_count, chunk_strategy, chunk_size, chunk_overlap)
VALUES ($1, $2, $3, 'queued', 0, $4, $5, $6)
RETURNING *;
```
Mirror this shape: `-- name: InsertWorkflowCheckpoint :one` / `INSERT INTO workflow_checkpoints (trace_id, node_name, context_snapshot) VALUES ($1, $2, $3) RETURNING *;` — `created_at` stays defaulted, not passed. No `ON CONFLICT` — 05-28 appends one row per node boundary, never updates an existing row.

From gateway/main.go:289-294, 99-117 (VERIFIED live — the exact app/store wiring to extend):
```go
type app struct { store documentStore; engine engine; logger *zap.Logger; retrySleep func(int) }
type postgresStore struct{ pool *pgxpool.Pool }
// existing simple-read pattern (GetDocument, GetReconciliationIntent) uses db.New(s.pool).SomeQuery(ctx, ...)
// directly, no explicit transaction — the checkpoint write should follow this simpler pattern (a single
// independent INSERT, not coordinated with any other write), NOT the BeginTx pattern CreateDocument/
// CreateReconciliationIntent use for multi-statement transactional writes.
```
Add `InsertWorkflowCheckpoint(ctx context.Context, traceID, nodeName string, snapshot []byte) error` to the existing `documentStore` interface (grep gateway/main.go for its current method list) and implement it on `postgresStore` via `db.New(s.pool).InsertWorkflowCheckpoint(ctx, db.InsertWorkflowCheckpointParams{...})` (the exact generated params struct name comes from sqlc after this plan's Task 1 regenerates `query.sql.go`).

From 05-01-PLAN.md's own prior action (already committed, read gateway/main.go's current `queryRAG` handler as it now stands post-05-01 before editing): the SSE-forwarding loop already contains a documented placeholder decision point for a Checkpoint sidecar field ("skip forwarding an event to SSE only if it carries a non-nil Checkpoint sidecar with no oneof event set... document the skip condition for 05-05's benefit"). This plan resolves that: in the FINAL design, `checkpoint` is always a sidecar field attached to a real oneof-bearing event (specifically `NodeCompleted`, never sent standalone) — so NO skip logic is needed at all. Every event is forwarded to SSE exactly as 05-01 already does; this plan only ADDS a fire-and-forget persistence dispatch alongside the existing forward, gated on `event.GetCheckpoint() != nil`. Remove any now-dead "skip if checkpoint-only" branch 05-01 may have stubbed if the executor finds one; do not leave unreachable code.
</interfaces>
</context>

## Artifacts this phase produces (this plan's share)

New symbols this plan creates:
- Postgres: `workflow_checkpoints` table
- Go: `db.InsertWorkflowCheckpoint` (sqlc-generated), `documentStore.InsertWorkflowCheckpoint` interface method, `postgresStore.InsertWorkflowCheckpoint` implementation, `app.persistCheckpoint` helper
- Rust: checkpoint-snapshot JSON builder in `engine::workflow::events`, `WorkflowRunner`'s per-node-boundary checkpoint attachment

<tasks>

<task type="auto">
  <name>Task 1: workflow_checkpoints schema + sqlc query</name>
  <precondition>PostgreSQL is reachable at the configured DATABASE_URL (docker compose service `db`; verify with `docker compose ps db` showing a running/healthy state, or an equivalent `pg_isready`-style read-only check) — Atlas migration apply and any Task 1 verification depend on a live database.</precondition>
  <precondition>The Atlas CLI (`atlas version`) and sqlc CLI (`sqlc version`) are available on PATH. If either is unavailable, fall back to hand-writing the migration as raw SQL against the schema (per 05-RESEARCH.md's named fallback: "migration can be hand-written as raw SQL... and reconciled with Atlas later") rather than blocking the task — document which path was used in the SUMMARY.</precondition>
  <files>gateway/db/schema.hcl, gateway/db/schema.sql, gateway/db/query.sql, gateway/db/query.sql.go</files>
  <read_first>gateway/db/schema.hcl, gateway/db/schema.sql, gateway/db/query.sql, gateway/sqlc.yaml, gateway/atlas.hcl</read_first>
  <action>
    In gateway/db/schema.hcl, add a new `table "workflow_checkpoints"` block following the `users`/`documents` table conventions verified in this plan's Interfaces excerpts: surrogate primary-key `id` column (serial or uuid, match the `users` table's exact precedent), `trace_id varchar(255) not null`, `node_name varchar(100) not null`, `context_snapshot jsonb not null`, `created_at timestamp not null default sql("CURRENT_TIMESTAMP")`. Apply the migration via the existing Atlas workflow this repo already uses for schema changes (check gateway/atlas.hcl / any existing migration-apply script/Makefile target before inventing a new command).
    In gateway/db/schema.sql (REVIEWS.md Codex HIGH — VERIFIED live this session: `gateway/sqlc.yaml` line 3 reads `schema: "db/schema.sql"`, NOT `schema.hcl` — sqlc will not know about `workflow_checkpoints` unless this file is also updated): add the IDENTICAL `workflow_checkpoints` table as a raw `CREATE TABLE "public"."workflow_checkpoints" (...)` statement, following this file's own existing style (read the `documents`/`users` table blocks already in this file and match their exact column-type/quoting conventions — e.g. `character varying(255)` not `varchar(255)`, `PRIMARY KEY (...)` placement) — the two files (`schema.hcl` for Atlas, `schema.sql` for sqlc) must describe the same table; keep them synchronized, do not let one drift from the other.
    In gateway/db/query.sql, add `-- name: InsertWorkflowCheckpoint :one` following the exact `InsertDocument` shape verified in this plan's Interfaces excerpts (no `ON CONFLICT`, `RETURNING *`). Regenerate `gateway/db/query.sql.go` via the existing sqlc convention (`sqlc generate` from wherever `gateway/sqlc.yaml` is rooted — confirmed this session to read `db/schema.sql`, so `sqlc generate` will only pick up the new table once `schema.sql` above is also updated).
  </action>
  <verify>
    <automated>cd D:/Repos/lancet/gateway &amp;&amp; sqlc generate &amp;&amp; go build ./... &amp;&amp; grep -c "InsertWorkflowCheckpoint" db/query.sql.go</automated>
  </verify>
  <acceptance_criteria>
    - gateway/db/schema.hcl contains a workflow_checkpoints table block with trace_id, node_name, context_snapshot (jsonb), created_at columns
    - gateway/db/schema.sql contains an equivalent CREATE TABLE "public"."workflow_checkpoints" statement with the same columns
    - `sqlc generate` exits 0 and gateway/db/query.sql.go contains a generated InsertWorkflowCheckpoint function and params struct
    - `go build ./...` succeeds in gateway/ with the regenerated sqlc code
    - The workflow_checkpoints table exists in the live PostgreSQL database after migration apply (verified via a direct psql \d workflow_checkpoints or equivalent, documented in the SUMMARY)
  </acceptance_criteria>
  <done>The workflow_checkpoints table exists in both schema.hcl (Atlas source) and schema.sql (sqlc's actual codegen source, kept in sync) and its sqlc-generated insert query compiles against a live PostgreSQL instance.</done>
</task>

<task type="auto">
  <name>Task 2: Rust checkpoint attachment + Go fire-and-forget persistence + Tier 2 tests</name>
  <files>engine/src/workflow/runner.rs, engine/src/workflow/events.rs, engine/src/workflow/mod.rs, gateway/main.go, gateway/main_test.go</files>
  <read_first>engine/src/workflow/runner.rs, engine/src/workflow/events.rs, engine/src/workflow/mod.rs, gateway/main.go, gateway/main_test.go, gateway/db/query.sql.go</read_first>
  <action>
    In engine/src/workflow/mod.rs: derive `serde::Serialize` on `WorkflowContext` and confirm (grep, do not assume) every field type it contains already derives `Serialize` too — `FusedCandidate` already does (verified in 05-02-PLAN.md's Interfaces excerpts: `#[derive(..., Serialize)] pub struct FusedCandidate`); check `GraphFactBlock`, the packed-evidence type, and `generation::ModelOutput` (05-AI-SPEC.md Section 4b cites it as already `#[derive(..., Serialize, Deserialize)]`) — add `#[derive(Serialize)]` to any field type still missing it, following the existing derive style in that type's own file rather than wrapping it in a manual serializer.
    In engine/src/workflow/events.rs: add a checkpoint-snapshot builder function taking `&WorkflowContext` and `node_name: &str`, returning a `Checkpoint { node_name: String, context_snapshot_json: String }` (or the exact prost-generated `Checkpoint` type from 05-01) via `serde_json::to_string(ctx)`.
    In engine/src/workflow/runner.rs: after a node's successful completion (immediately before or alongside sending its `NodeCompleted` event, per D-28 "after each node boundary"), call the new checkpoint-snapshot builder and attach the result as that `NodeCompleted` event's `checkpoint` sidecar field (D-28: full accumulated snapshot, not a diff — the WHOLE `WorkflowContext` as of this point, not just the fields this node itself populated). This applies uniformly to every node, including `generate_answer` (whose `NodeCompleted` now carries the fullest snapshot of the whole query). The `NodeCompleted` event (checkpoint sidecar included) is sent via the SAME reliable `.send(...).await` 05-01 already established for every WorkflowEvent — REVIEWS.md Codex+Antigravity consensus HIGH corrected an earlier draft that described this send as `try_send`; there is no lossy send anywhere on the Rust side. D-27's fire-and-forget property is entirely a Go-side property of the Postgres INSERT below, not of this Rust channel send.
    In gateway/main.go: add `InsertWorkflowCheckpoint(ctx context.Context, traceID, nodeName string, snapshot []byte) error` to the `documentStore` interface and implement it on `postgresStore` per this plan's Interfaces excerpts (the simple `db.New(s.pool).InsertWorkflowCheckpoint(...)` pattern, not a transaction). Add an `app.persistCheckpoint(cp *pb.Checkpoint)` method that builds `ctx, cancel := context.WithTimeout(context.Background(), ingestCompensationTimeout)` (REVIEWS.md Codex MEDIUM — reuse the existing 5-second `ingestCompensationTimeout` constant already defined at `gateway/main.go:45` and already used for exactly this "must survive request cancellation, but still bounded" pattern at lines 308/325/329/335/339, rather than an unbounded `context.Background()`; `defer cancel()`), then calls `a.store.InsertWorkflowCheckpoint(ctx, cp.TraceId, cp.NodeName, []byte(cp.ContextSnapshotJson))` and logs (does not panic) on error, including a timeout. In the `queryRAG` SSE-forwarding loop (built in 05-01), after writing each event's SSE frame — using the DTO 05-01 already defined that unconditionally excludes the checkpoint/context_snapshot_json field (confirm this still holds now that the field carries real data, not just always-unset placeholder content; do not accidentally widen the DTO to include it) — check `if cp := event.GetCheckpoint(); cp != nil { go a.persistCheckpoint(cp) }`, dispatched as a detached goroutine, never awaited, never blocking the next `stream.Recv()`/`Flush()` iteration. Remove any dead "skip forwarding" branch 05-01 may have stubbed for this case per this plan's Interfaces excerpts note.
    In gateway/main_test.go: add Tier 2 tests (using the real spawned engine binary + a real/test PostgreSQL connection, following the existing ~line 2120-2174 real-binary-spawn precedent) asserting: (a) checkpoint payload fidelity — after a full happy-path query, POLL the workflow_checkpoints table (bounded retry loop with a short sleep between attempts, e.g. up to 2s total — REVIEWS.md Codex MEDIUM: the write is dispatched from a detached goroutine, so a test that queries immediately after the SSE stream completes can race the insert; do not assert on the first query attempt) and assert the row taken at the generate_answer node boundary has a context_snapshot containing original_query, reformulated_query, vector/bm25-derived fields, AND the answer — i.e. every field populated by any earlier node, not just this node's own contribution; (b) cancellation atomicity — trigger a genuine SSE client disconnect mid-query (close the HTTP response body/cancel the request context before the stream completes) and, after the same bounded poll window (to let any in-flight detached write either complete or be legitimately still-running-within-its-5s-bound), assert no partially-written checkpoint row exists for that trace_id (either a row is fully present with valid jsonb, or absent — never a row with truncated/invalid JSON); (c) the SSE JSON frame for a NodeCompleted event carrying a real (non-empty) checkpoint sidecar never contains a checkpoint/context_snapshot_json key — reuses/extends 05-01's Task 3 DTO test with a real payload instead of an always-empty one; (d) relocated AI-SPEC reference dataset scenario 14 (backpressure isolation, moved here from an earlier draft of 05-04 per REVIEWS.md Codex+Antigravity consensus HIGH) — construct a `documentStore` fake whose `InsertWorkflowCheckpoint` blocks or sleeps far longer than a normal write, wire it into the app under test, run a full happy-path query through the real SSE loop, and assert the SSE response still receives every frame including `FinalAnswer` within the normal latency envelope (not stalled waiting on the slow/blocked Postgres write) — this is the deterministic proof that D-27's fire-and-forget property genuinely isolates the query pipeline from a slow checkpoint write, at the one point in the whole path where that property actually lives.
  </action>
  <verify>
    <automated>cd D:/Repos/lancet/gateway &amp;&amp; go test ./... -run "Checkpoint|Cancellation|Backpressure"</automated>
  </verify>
  <acceptance_criteria>
    - engine/src/workflow/runner.rs attaches a checkpoint sidecar to every NodeCompleted event, built from the full WorkflowContext at that point, sent via .send(...).await (zero try_send occurrences anywhere in the file)
    - gateway/main.go's persistCheckpoint uses context.WithTimeout(context.Background(), ingestCompensationTimeout), not a bare context.Background()
    - gateway/main.go's queryRAG loop dispatches a.persistCheckpoint as a detached goroutine gated on event.GetCheckpoint() != nil, never blocking the SSE forward loop
    - A Tier 2 test polls (not a single immediate query) and asserts the checkpoint row at the generate_answer boundary contains fields populated by every earlier node (full-snapshot fidelity, not a diff)
    - A Tier 2 test polls and asserts a genuine client-disconnect cancellation leaves no partially-written checkpoint row
    - A Tier 2 test asserts the SSE JSON frame for a NodeCompleted event with a real checkpoint sidecar never contains a checkpoint/context_snapshot_json key
    - A Tier 2 test with a blocking/slow documentStore fake asserts FinalAnswer still arrives within the normal latency envelope over SSE (relocated scenario 14)
    - `go test ./... -run "Checkpoint|Cancellation|Backpressure"` exits 0 against a live PostgreSQL instance
  </acceptance_criteria>
  <done>Checkpoint payloads are durably persisted to PostgreSQL by Go, fire-and-forget, with full-snapshot fidelity and no partial-write risk on cancellation — ORCH-04 is fully satisfied end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway/main.go persistCheckpoint -> PostgreSQL | Internal, trusted — but content-sensitivity (raw corpus text) is a real information-disclosure consideration if the DB were ever exposed |
| engine checkpoint payload -> Go -> Postgres | Same trust boundary as the existing WorkflowEvent stream; no new untrusted-input crossing, only a new persistence side effect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Information Disclosure | gateway/db workflow_checkpoints table (raw corpus content) | medium | accept | Explicitly accepted per D-23, extending 04.1 D-33's established "no redaction system, single-user local demo" framing — docker-compose.yml already binds Postgres to 127.0.0.1 only; not a new mitigation gap introduced by this plan |
| T-05-06 | Information Disclosure | gateway/main.go SSE JSON DTO (checkpoint field exclusion) | medium | mitigate | The SSE-facing DTO 05-01 defined unconditionally excludes the checkpoint/context_snapshot_json field; this plan's Task 2 test (c) reaffirms the invariant now that the field carries real, non-empty data instead of always-unset placeholder content (carried forward from 05-01's T-05-06) |
| T-05-10 | Denial of Service | gateway/main.go persistCheckpoint goroutine | medium | mitigate | Dispatched via `go a.persistCheckpoint(cp)` as a detached, unawaited goroutine using a BOUNDED `context.WithTimeout(context.Background(), ingestCompensationTimeout)` (revised from an earlier draft's unbounded `context.Background()` per REVIEWS.md Codex MEDIUM) — a slow/failing Postgres write can never block the SSE forwarding loop or the underlying query, and cannot leak an unbounded goroutine during a sustained DB outage either; proven by Task 2's Tier 2 cancellation-atomicity test and the relocated backpressure-isolation test (d) |
| T-05-11 | Repudiation | gateway/db workflow_checkpoints row atomicity on cancellation | high | mitigate | `InsertWorkflowCheckpoint` is a single-statement `:one` insert (no multi-step transaction to leave half-committed) — Postgres itself guarantees a single INSERT is atomic; the Tier 2 test proves no truncated/invalid-JSON row can result from a mid-stream cancellation, using a bounded poll rather than an immediate query to avoid racing the detached write |

</threat_model>

<verification>
1. `go build ./...` and `cargo build --locked` both succeed with the new schema (both schema.hcl and schema.sql), query, and Rust checkpoint-attachment code.
2. `sqlc generate` picks up workflow_checkpoints from gateway/db/schema.sql (the file gateway/sqlc.yaml actually reads) without manual edits.
3. `go test ./... -run "Checkpoint|Cancellation|Backpressure"` passes against a live PostgreSQL instance, proving full-snapshot fidelity, cancellation atomicity, checkpoint/SSE isolation, and backpressure isolation (relocated scenario 14).
4. A full happy-path query, run manually against the real stack, produces one workflow_checkpoints row per node boundary (5 rows for a 5-node happy path), all sharing one trace_id, directly inspectable via psql.
5. engine/src/workflow/runner.rs contains zero occurrences of try_send.
</verification>

<success_criteria>
- ORCH-04 is fully and durably satisfied: checkpoints exist in PostgreSQL, are full-snapshot (not diff), are written fire-and-forget by Go (never by Rust directly, and never by an unbounded Rust channel send either), and survive a request cancellation without corruption.
- The checkpoint payload never reaches the SSE client, and a slow/blocked Postgres write never delays the client's actual answer — both proven by dedicated Tier 2 tests, not just documented as an intent.
- This is the final plan of Phase 5 — combined with 05-01 through 05-04, every ROADMAP.md Phase 5 success criterion (state machine, streaming events, timeouts/retries, checkpoints, QueryReformulator pass-through) is now delivered.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-05-SUMMARY.md` when done
</output>

## Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|----------------|
| sqlc reads db/schema.sql, plan only updated schema.hcl | HIGH (Codex) | Task 1: gateway/db/schema.sql added to files_modified and action, kept in sync with schema.hcl; verify command runs sqlc generate explicitly |
| try_send on the checkpoint-carrying NodeCompleted send | HIGH (Codex+Antigravity, carried from 05-01) | Task 2: reaffirmed the reliable `.send(...).await` fix from 05-01 applies here too; acceptance criteria assert zero try_send occurrences |
| Fire-and-forget checkpoint writes use unbounded context.Background() | MEDIUM (Codex) | Task 2: switched to `context.WithTimeout(context.Background(), ingestCompensationTimeout)`, reusing the existing 5s constant and pattern already used for compensation writes |
| Tests query immediately after SSE completion, racing the detached insert | MEDIUM (Codex) | Task 2: Tier 2 tests (a) and (b) now poll with a bounded retry window instead of a single immediate query |
| Full checkpoint snapshot may be oversized/over-exposed via SSE | MEDIUM/LOW (both reviewers) | Task 2 test (c): explicit assertion the SSE JSON frame never contains the checkpoint field, now with real (non-empty) payload data — resolves the "over-exposure" half; a hard payload-size CAP is left as a documented, not-yet-triggered concern (see deferred below) since D-24 already accepts unbounded row growth for v1 |
| Checkpoint/event backpressure isolation (scenario 14) written against an architecture that no longer exists | HIGH (consequence of the try_send fix) | Relocated here as Task 2 test (d): a slow/blocking documentStore fake, proving D-27's fire-and-forget property at the point it actually lives (the Go Postgres write) |

## Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| Hard payload-size cap / redaction on the checkpoint context_snapshot | D-24 already accepts unbounded row growth with no TTL/retention job as an explicit v1 scope decision (05-CONTEXT.md); adding a size cap now would be scope not requested by any locked decision — left as a natural Phase 6 hardening candidate if storage growth becomes a real problem, not a gap this plan silently drops |
