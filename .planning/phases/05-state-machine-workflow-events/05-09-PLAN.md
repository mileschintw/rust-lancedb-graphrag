---
phase: 05-state-machine-workflow-events
plan: 09
type: execute
wave: 8
depends_on:
  - 05-08
files_modified:
  - engine/src/main.rs
  - engine/src/workflow/runner.rs
  - engine/src/workflow/nodes/graph_context.rs
  - engine/src/workflow/nodes/generate.rs
  - engine/src/tests.rs
  - engine/src/tests/workflow_phase5.rs
autonomous: true
gap_closure: true
requirements:
  - ORCH-01
  - ORCH-02
  - ORCH-03
must_haves:
  truths:
    - "Every [engine.workflow] timeout key is parsed into typed Rust settings, unknown workflow fields are rejected, and positive values are applied to the production runner and node adapters."
    - "The configured deadlines are distinct: reformulation 5s, query embedding 10s, retrieval 10s, graph operation 4s inside a 15s graph node, prompt 2s, and generation node 65s around two 30s provider attempts with slack per D-17."
    - "A client disconnect cancels the Rust workflow, drops the in-flight node future, and prevents later nodes or paid provider attempts from continuing."
    - "Timeout and cancellation produce distinct typed outcomes, and the /rag/query route remains outside the gateway's blanket 60-second timeout."
  artifacts:
    - path: "engine/src/main.rs"
      provides: "Typed workflow settings parsing, validation, effective settings propagation, and stream cancellation ownership"
    - path: "engine/src/workflow/runner.rs"
      provides: "Production timeout application and cancellation-first node execution"
    - path: "engine/src/workflow/nodes/graph_context.rs"
      provides: "Configured embedding prelude and graph-operation deadlines"
    - path: "engine/src/tests.rs"
      provides: "Config overlay and unknown-field regression assertions"
    - path: "engine/src/tests/workflow_phase5.rs"
      provides: "Deadline, cancellation, and prompt/generation teardown regressions"
  key_links:
    - "config [engine.workflow] -> WorkflowSettings -> EffectiveRagSettings -> WorkflowRunner and ExtractGraphContextNode"
    - "ReceiverStream drop or sender closure -> CancellationToken.cancel -> biased cancellation select -> dropped node future"
    - "generation provider attempt timeout -> GenerateAnswer node outer deadline -> typed Timeout or retryable failure"
---

<objective>
Make the locked workflow timeout and cancellation contract effective on production I/O instead of only on test runners.

Purpose: Verification found that all seven workflow settings are dead configuration, the real inline work is unbounded, and the production CancellationToken is never cancelled when the stream disappears. This plan closes ORCH-03 while preserving the five-node path from 05-08 and the existing gateway route boundary.
Output: Typed workflow configuration, applied per-node and nested deadlines, stream-drop cancellation, and deterministic regression coverage for timeout arithmetic and teardown.
</objective>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-preconditions.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-reversibility.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-VERIFICATION.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEW.md
@.planning/phases/05-state-machine-workflow-events/05-AI-SPEC.md
@AGENTS.md
@rust-guidelines.md
@engine/src/main.rs
@engine/src/workflow/runner.rs
@engine/src/workflow/nodes/graph_context.rs
@engine/src/workflow/nodes/generate.rs
@config/config.toml
@config/config.example.toml
@config/config.verify.toml

## Artifacts this phase produces

- A deny-unknown typed `WorkflowSettings` section with the seven existing millisecond keys and locked defaults.
- A production runner and graph/generation path that applies configured deadlines to real I/O.
- A stream ownership guard and tests proving disconnect cancellation, timeout classification, and the configured seven-second verification timeout.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Apply workflow settings and stream-owned cancellation to production execution</name>
  <read_first>AGENTS.md; rust-guidelines.md; engine/src/main.rs; engine/src/workflow/runner.rs; engine/src/workflow/nodes/graph_context.rs; engine/src/workflow/nodes/generate.rs; config/config.toml; config/config.example.toml; config/config.verify.toml; 05-CONTEXT.md; 05-RESEARCH.md; 05-VERIFICATION.md; 05-REVIEW.md</read_first>
  <files>engine/src/main.rs, engine/src/workflow/runner.rs, engine/src/workflow/nodes/graph_context.rs, engine/src/workflow/nodes/generate.rs</files>
  <action>Define a typed WorkflowSettings value for exactly reformulate_timeout_ms, query_embedding_timeout_ms, retrieve_timeout_ms, graph_operation_timeout_ms, graph_node_timeout_ms, prompt_timeout_ms, and generation_node_timeout_ms. Apply deny-unknown-fields to this workflow table, validate every value as greater than zero, preserve the documented defaults, and carry the parsed value into EffectiveRagSettings and the production runner built in 05-08. Thread reformulation, retrieval, prompt, and generation deadlines through WorkflowRunner, and thread query-embedding and graph-operation deadlines through ExtractGraphContextNode with the graph-operation deadline nested inside the graph-node backstop. Preserve the D-17 arithmetic: one 30-second provider deadline per attempt, one retry at most, and a 65-second generation node deadline. Wrap the returned QueryRAG ReceiverStream in a drop guard that calls CancellationToken.cancel, cancel when the event sender closes, and keep cancellation first in every node select so dropping a node future tears down the real I/O. Distinguish cancellation from timeout and preserve D-04 pre-stream validation, D-05 in-band failure semantics, D-16 native disconnect cancellation, D-18 streaming, and D-19 exclusion from the gateway blanket timeout. Do not add a CancelQuery RPC or a second cancellation protocol.</action>
  <acceptance_criteria>
    - All seven existing workflow keys are represented by typed fields, accepted from the three committed configuration overlays, and rejected when an unknown field is added to [engine.workflow].
    - The configured values are visible in the production runner and graph node rather than only in test constructors.
    - Real embedding, graph, retrieval, prompt, and generation calls are enclosed by the intended node or nested operation deadlines.
    - Dropping the server stream or observing a closed event receiver cancels the token and prevents later workflow nodes and additional provider attempts.
    - A timeout is reported as Timeout and a disconnect as Cancelled; neither is rewritten as a successful answer.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml workflow_phase5_config workflow_phase5_disconnect -- --nocapture</automated>
  </verify>
  <done>Production configuration and stream ownership enforce the locked timeout and native cancellation contract around real workflow I/O.</done>
  <must_haves>
    - D-04, D-05, D-16, D-17, D-18, and D-19 are explicit in the production control flow.
    - The provider's per-attempt 30-second timeout remains separate from the 65-second generation node budget.
    - Cancellation drops in-flight work instead of waiting for the provider to finish.
  </must_haves>
  <reversibility rating="costly">Changing timeout and stream ownership semantics affects paid provider calls and request teardown; keep the settings object and cancellation guard as named boundaries with focused tests.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Prove configured deadlines and cancellation teardown with deterministic faults</name>
  <read_first>AGENTS.md; rust-guidelines.md; engine/src/tests.rs; engine/src/tests/workflow_phase5.rs; engine/src/main.rs; engine/src/workflow/runner.rs; engine/src/workflow/nodes/graph_context.rs; engine/src/workflow/nodes/generate.rs; config/config.verify.toml; 05-AI-SPEC.md; 05-VALIDATION.md</read_first>
  <files>engine/src/tests.rs, engine/src/tests/workflow_phase5.rs</files>
  <action>Add deterministic regression cases that load the committed workflow overlays and exercise the production builder from 05-08. Verify unknown workflow fields fail during configuration loading, zero or overflow timeout values fail validation, a fast node completes before its deadline, and a deliberately slow provider under config.verify.toml fails at approximately the configured 7000ms generation-node deadline. Add cancellation cases during a fast node and during an in-flight generation attempt; assert the cancellation category, no later node lifecycle events, no additional provider work after teardown, and no fabricated answer. Keep the malformed-request no-stream assertion and the successful D-03 zero-evidence path in the same matrix so cancellation and timeout changes cannot bypass D-04 or D-03. Generation retry classification and the between-attempt cancellation case are verified after this settings and cancellation contract by 05-10.</action>
  <acceptance_criteria>
    - The seven workflow settings and their units are asserted by name, including the 5000/10000/10000/4000/15000/2000/65000 production values.
    - The config.verify generation-node test observes Timeout near 7000ms rather than the provider's uncontrolled completion time.
    - Cancellation tests prove no later node runs and no provider work continues after stream teardown.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml workflow_phase5_config workflow_phase5_timeout workflow_phase5_cancellation -- --nocapture</automated>
  </verify>
  <done>Regression coverage turns the verification runtime checks into automated evidence for settings, deadline arithmetic, and cancellation teardown.</done>
  <must_haves>
    - AI-SPEC timeout and cancellation scenarios are covered without relying on wall-clock sleeps longer than the configured test values.
    - D-03 zero-evidence and D-04 pre-stream validation remain true while timeout and cancellation controls are applied.
  </must_haves>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Configuration file to Rust settings | Operator-controlled TOML can contain unknown or unsafe timeout values. |
| HTTP and gRPC stream lifetime to Rust task | Client disconnect is an asynchronous teardown signal that controls paid and resource-intensive work. |
| Rust workflow to external providers and stores | Embedding, graph, retrieval, and generation calls may stall or fail independently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09-01 | Tampering | `[engine.workflow]` parser | high | mitigate | Deny unknown workflow fields and reject non-positive values before the engine starts serving requests. |
| T-05-09-02 | Denial of service | Node deadlines | high | mitigate | Apply per-node and nested operation timeouts to the real adapters, including the 65-second generation outer budget. |
| T-05-09-03 | Denial of service | Client disconnect cancellation | high | mitigate | Tie stream drop and sender closure to CancellationToken cancellation and cancellation-first selects. |
| T-05-09-04 | Information disclosure | Provider error handling | medium | mitigate | Preserve typed safe NodeError categories and do not expose provider credentials or raw transport details in stream failures. |
| T-05-09-05 | Repudiation | Timeout versus cancellation classification | medium | mitigate | Add deterministic tests that distinguish Timeout from Cancelled and retain trace/session identity on terminal events. |
</threat_model>

<verification>
- `cargo test --manifest-path engine/Cargo.toml workflow_phase5_config workflow_phase5_timeout workflow_phase5_cancellation -- --nocapture` passes.
- Unknown workflow settings fail loudly, all seven values apply to production execution, and config.verify produces the expected generation-node timeout.
- Disconnect tests demonstrate prompt Rust teardown without later nodes or provider attempts.
</verification>

<success_criteria>
ORCH-03 is enforced for production I/O: settings are live, deadlines are bounded and distinct, exactly-one generation retry remains predictable, and native stream cancellation terminates work.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-09-SUMMARY.md` when done.
</output>
