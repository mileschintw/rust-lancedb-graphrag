---
phase: 05-state-machine-workflow-events
plan: 09
type: execute
wave: 8
depends_on:
  - 05-08
files_modified:
  - engine/src/main.rs
  - engine/src/workflow/runner.rs
  - engine/src/workflow/nodes/graph_context.rs
  - engine/src/workflow/nodes/generate.rs
  - engine/src/tests.rs
  - engine/src/tests/workflow_phase5.rs
  - engine/src/tests/workflow_phase5_production.rs
  - config/config.verify.toml
autonomous: true
gap_closure: true
requirements:
  - ORCH-01
  - ORCH-02
  - ORCH-03
must_haves:
  truths:
    - "Every [engine.workflow] timeout key is parsed into typed Rust settings, unknown workflow fields are rejected, and positive values are applied to the production runner and node adapters."
    - "The production defaults remain distinct: reformulation 5s, query embedding 10s, retrieval 10s, graph operation 4s inside a 15s graph node, prompt 2s, and generation node 65s around two 30s provider attempts with slack per D-17; config/config.verify.toml deliberately uses a 7000ms generation-node deadline for the live timeout proof."
    - "Fast sub-second in-process deterministic tests using fake ports or paused Tokio time prove semantic timeout and cancellation behavior only; they do not serve as evidence that the live verification overlay is consumed."
    - "The live config/config.verify.toml harness uses a 7000ms generation-node deadline while the provider attempt budget is 30 seconds, so the node timeout is observed before the provider attempt can expire."
    - "A client disconnect cancels the Rust workflow, drops the in-flight node future, and prevents later nodes or paid provider attempts from continuing."
    - "Timeout and cancellation produce distinct typed outcomes, and the /rag/query route remains outside the gateway's blanket 60-second timeout."
  artifacts:
    - path: "engine/src/main.rs"
      provides: "Typed workflow settings parsing, validation, effective settings propagation, and stream cancellation ownership"
    - path: "engine/src/workflow/runner.rs"
      provides: "Production timeout application and cancellation-first node execution"
    - path: "engine/src/workflow/nodes/graph_context.rs"
      provides: "Configured embedding prelude and graph-operation deadlines"
    - path: "engine/src/tests/workflow_phase5.rs"
      provides: "Library-target deadline, cancellation, and prompt/generation teardown regressions"
    - path: "engine/src/tests/workflow_phase5_production.rs"
      provides: "Binary-target production settings, live overlay, and stream teardown regressions"
    - path: "config/config.verify.toml"
      provides: "Committed live verification overlay with a 7000ms generation-node deadline and 30-second provider-attempt budget"
  key_links:
    - "config [engine.workflow] -> WorkflowSettings -> EffectiveRagSettings -> WorkflowRunner and ExtractGraphContextNode"
    - "ReceiverStream drop or sender closure -> CancellationToken.cancel -> biased cancellation select -> dropped node future"
    - "config/config.verify.toml provider-attempt budget -> GenerateAnswer node 7000ms live deadline -> typed Timeout before the 30-second provider attempt budget"
    - "fast fake/paused-time semantic tests -> in-process behavior evidence, kept separate from the wall-clock overlay harness"
---

<objective>
Make the locked workflow timeout and cancellation contract effective on production I/O instead of only on test runners.

Purpose: Verification found that all seven workflow settings are dead configuration, the real inline work is unbounded, and the production CancellationToken is never cancelled when the stream disappears. This plan closes ORCH-03 while preserving the five-node path from 05-08 and the existing gateway route boundary.
Output: Typed workflow configuration, applied per-node and nested deadlines, stream-drop cancellation, and deterministic regression coverage for timeout arithmetic and teardown.
</objective>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-preconditions.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-reversibility.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-VERIFICATION.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEWS.md
@.planning/phases/05-state-machine-workflow-events/05-AI-SPEC.md
@AGENTS.md
@rust-guidelines.md
@engine/src/main.rs
@engine/src/workflow/runner.rs
@engine/src/workflow/nodes/graph_context.rs
@engine/src/workflow/nodes/generate.rs
@config/config.toml
@config/config.example.toml
@config/config.verify.toml

## Artifacts this phase produces

- A deny-unknown typed `WorkflowSettings` section with the seven existing millisecond keys and locked defaults.
- A production runner and graph/generation path that applies configured deadlines to real I/O.
- A stream ownership guard and tests proving disconnect cancellation, timeout classification, and the configured seven-second verification timeout.
- In-process production-builder cancellation semantics that provide the Rust handoff contract for the real HTTP client-disconnect proof owned by 05-11.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Apply workflow settings and stream-owned cancellation to production execution</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; engine/src/main.rs; engine/src/tests/workflow_phase5_production.rs; engine/src/workflow/runner.rs; engine/src/workflow/nodes/graph_context.rs; engine/src/workflow/nodes/generate.rs; config/config.toml; config/config.example.toml; config/config.verify.toml; 05-CONTEXT.md; 05-RESEARCH.md; 05-VERIFICATION.md; 05-REVIEWS.md</read_first>
  <files>engine/src/main.rs, engine/src/workflow/runner.rs, engine/src/workflow/nodes/graph_context.rs, engine/src/workflow/nodes/generate.rs, engine/src/tests.rs, config/config.verify.toml, engine/src/tests/workflow_phase5_production.rs</files>
<review_resolution>Addresses review concerns HIGH stream-drop detection, MEDIUM runner-select cancellation bias, MEDIUM-4, and LOW-1 at the introducing settings task: the stream guard owns the returned ReceiverStream lifetime, sender closure cancels the same token, and every node select observes cancellation before its deadline; extend `config_workflow_timeout_overlays_match_contract` in engine/src/tests.rs with exact values and timeout inequalities, and add the shared Phase 02/Phase 05 consumer comment to config/config.verify.toml. The production module is already registered by 05-08; 05-18 owns only the final target split, fake-call-site relocation, and complete binary test-target compile gate, while 05-13/05-14 own their exact production behavior filters and later plans rerun them. The later Task 2 remains the live-versus-semantic timing proof.</review_resolution>
  <action>Define a typed WorkflowSettings value for exactly reformulate_timeout_ms, query_embedding_timeout_ms, retrieve_timeout_ms, graph_operation_timeout_ms, graph_node_timeout_ms, prompt_timeout_ms, and generation_node_timeout_ms. Add the dedicated regression test body `workflow_phase5_settings_applied_to_production` in the binary-owned engine/src/tests/workflow_phase5_production.rs module; 05-08 registers that production module at the introducing wave, while 05-18 owns the later target-aware fixture handoff and complete binary test-target compile gate, and downstream plans own their exact behavior reruns. Extend `config_workflow_timeout_overlays_match_contract` in engine/src/tests.rs with exact overlay values and timeout inequalities, and add the shared consumer comment to config/config.verify.toml. Apply deny-unknown-fields to this workflow table, validate every value as greater than zero, preserve the documented defaults, and carry the parsed value into EffectiveRagSettings and the production runner built in 05-08. Thread reformulation, retrieval, prompt, and generation deadlines through WorkflowRunner, and thread query-embedding and graph-operation deadlines through ExtractGraphContextNode with the graph-operation deadline nested inside the graph-node backstop. Preserve the D-17 arithmetic: production uses one 30-second provider deadline per attempt, one retry at most, and a 65-second generation node deadline. Own config/config.verify.toml in this task: set [openrouter].generation_timeout_secs = 30, set reformulate/query_embedding/retrieve/graph_operation/graph_node/prompt workflow overlay values to 5000/10000/10000/4000/15000/2000 respectively so the live harness reaches GenerateAnswer, and retain [engine.workflow].generation_node_timeout_ms = 7000, making the provider attempt budget strictly longer than the live node deadline. Keep fast semantic fixtures separate from this wall-clock overlay proof. Wrap the returned QueryRAG ReceiverStream in a stream-owned drop guard that calls CancellationToken.cancel when the client drops the stream, cancel when the event sender closes, and keep cancellation first in every node select; the runner must not rely only on a detached task or a child future voluntarily yielding to notice disconnect, so dropping a node future tears down the real I/O. Follow the 05-PATTERNS.md runner timeout/select pattern and the engine/src/generation/openrouter.rs per-attempt timeout analog without merging their budgets. Distinguish cancellation from timeout and preserve D-04 pre-stream validation, D-05 in-band failure semantics, D-16 native disconnect cancellation, D-18 streaming, and D-19 exclusion from the gateway blanket timeout. Do not add a CancelQuery RPC or a second cancellation protocol. Addresses review concerns HIGH stream-drop detection, MEDIUM runner-select bias, MEDIUM-4, and LOW-1.</action>
  <action>Make timeout teardown explicit in the runner: structure the timeout arm of `run_node` so that when its deadline expires it calls `cancel.cancel()` on the same cancellation token before constructing or emitting `NodeFailed(Timeout)`. The timeout path must not rely on dropping the node future alone; the stalled provider or retrieval operation must observe that token and stop before terminal failure is delivered, while the independent stream-drop and closed-sender cancellation paths remain intact.</action>
  <action>The override matrix must name and exercise `LANCET_ENGINE__WORKFLOW__REFORMULATE_TIMEOUT_MS`, `LANCET_ENGINE__WORKFLOW__QUERY_EMBEDDING_TIMEOUT_MS`, `LANCET_ENGINE__WORKFLOW__RETRIEVE_TIMEOUT_MS`, `LANCET_ENGINE__WORKFLOW__GRAPH_OPERATION_TIMEOUT_MS`, `LANCET_ENGINE__WORKFLOW__GRAPH_NODE_TIMEOUT_MS`, `LANCET_ENGINE__WORKFLOW__PROMPT_TIMEOUT_MS`, and `LANCET_ENGINE__WORKFLOW__GENERATION_NODE_TIMEOUT_MS`, with each value asserted on the corresponding typed field.</action>
  <acceptance_criteria>
    - All seven existing workflow keys are represented by typed fields, accepted from the three committed configuration overlays, and rejected when an unknown field is added to [engine.workflow].
    - `config/config.verify.toml` contains `openrouter.generation_timeout_secs = 30`, upstream workflow values 5000/10000/10000/4000/15000/2000, and `engine.workflow.generation_node_timeout_ms = 7000`, so the live harness reaches GenerateAnswer and proves the provider-attempt budget is longer than the live node deadline.
    - The configured values are visible in the production runner and graph node rather than only in test constructors.
    - Real embedding, graph, retrieval, prompt, and generation calls are enclosed by the intended node or nested operation deadlines.
    - Dropping the server stream or observing a closed event receiver cancels the token and prevents later workflow nodes and additional provider attempts.
    - A node timeout calls `cancel.cancel()` before emitting `NodeFailed(Timeout)`, and the stalled operation observes cancellation before the bounded timeout regression completes; stream-drop cancellation remains independently covered.
    - A timeout is reported as Timeout and a disconnect as Cancelled; neither is rewritten as a successful answer.
    - The named production settings test body is present in workflow_phase5_production.rs, while its registration is compiled by 05-08 and final exact target execution is rerun by 05-18/05-20 after the target split.
  </acceptance_criteria>
  <verify>
    <automated>$source = Get-Content 'engine/src/tests/workflow_phase5_production.rs' -Raw; if ($source -notmatch 'workflow_phase5_settings_applied_to_production') { throw 'production settings regression body is missing' }; $main = Get-Content 'engine/src/main.rs' -Raw; foreach ($key in @('reformulate_timeout_ms','query_embedding_timeout_ms','retrieve_timeout_ms','graph_operation_timeout_ms','graph_node_timeout_ms','prompt_timeout_ms','generation_node_timeout_ms')) { if ($main -notmatch [regex]::Escape($key)) { throw "production settings key is not wired: $key" } }; $overlay = Get-Content 'config/config.verify.toml' -Raw; foreach ($value in @('generation_timeout_secs = 30','reformulate_timeout_ms = 5000','query_embedding_timeout_ms = 10000','retrieve_timeout_ms = 10000','graph_operation_timeout_ms = 4000','graph_node_timeout_ms = 15000','prompt_timeout_ms = 2000','generation_node_timeout_ms = 7000')) { if ($overlay -notmatch [regex]::Escape($value)) { throw "verification overlay value is missing: $value" } }; Write-Output 'production settings implementation and test-body handoff validated'</automated>
    <automated>cargo check --bin engine --manifest-path engine/Cargo.toml --locked; $checkExit = $LASTEXITCODE; if ($checkExit -ne 0) { exit $checkExit }</automated>
    <automated>cargo test --bin engine --manifest-path engine/Cargo.toml --locked --no-run; $testCompileExit = $LASTEXITCODE; if ($testCompileExit -ne 0) { exit $testCompileExit }</automated>
    <automated>$tests = Get-Content 'engine/src/tests.rs' -Raw; foreach ($needle in @('generation_timeout_secs = 30','reformulate_timeout_ms = 5000','query_embedding_timeout_ms = 10000','retrieve_timeout_ms = 10000','graph_operation_timeout_ms = 4000','graph_node_timeout_ms = 15000','prompt_timeout_ms = 2000','generation_node_timeout_ms = 7000','10000 + 4000','65000')) { if (-not $tests.Contains($needle)) { throw "exact timeout overlay regression assertion missing $needle" } }; $overlay = Get-Content 'config/config.verify.toml' -Raw; if ($overlay -notmatch 'phase02_live_evidence\.py') { throw 'shared config consumer comment is missing' }</automated>
  </verify>
  <done>Production configuration and stream ownership enforce the locked timeout and native cancellation contract around real workflow I/O.</done>
  <must_haves>
    - D-04, D-05, D-16, D-17, D-18, and D-19 are explicit in the production control flow.
    - The provider's per-attempt 30-second timeout remains separate from the 65-second generation node budget.
    - Cancellation drops in-flight work instead of waiting for the provider to finish.
  </must_haves>
  <reversibility rating="costly">Changing timeout and stream ownership semantics affects paid provider calls and request teardown; keep the settings object and cancellation guard as named boundaries with focused tests.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Prove configured deadlines and cancellation teardown with deterministic faults</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; engine/src/tests/workflow_phase5.rs; engine/src/tests/workflow_phase5_production.rs; engine/src/main.rs; engine/src/workflow/runner.rs; engine/src/workflow/nodes/graph_context.rs; engine/src/workflow/nodes/generate.rs; config/config.verify.toml; 05-AI-SPEC.md; 05-VALIDATION.md; 05-REVIEWS.md</read_first>
  <files>engine/src/tests.rs, engine/src/tests/workflow_phase5_production.rs, engine/src/tests/workflow_phase5.rs, config/config.verify.toml</files>
  <review_resolution>Addresses review concerns MEDIUM-4 and LOW-1: strengthen the durable overlay regression in engine/src/tests.rs with exact values and timeout inequalities, and add a shared-consumer comment to config/config.verify.toml. The live 7000ms harness remains the wall-clock authority; paused/fake cases remain semantic evidence only.</review_resolution>
  <action>Add the dedicated live runtime regression test body `workflow_phase5_config_verify_generation_timeout` plus fast semantic cases that exercise the production builder from 05-08 only in `engine/src/tests/workflow_phase5_production.rs`. Reserve `engine/src/tests/workflow_phase5.rs` for library-target fake-port and paused-clock semantics that do not construct `build_production_workflow` or `LancetServiceImpl`; 05-18 owns the corresponding library/binary target split and registration. The fast class uses fake ports or paused Tokio time with sub-second test values to prove semantic timeout/cancellation behavior; it must not be described as proof that the committed overlay controls wall-clock execution. The live class must load the committed config/config.verify.toml, whose reformulate/query_embedding/retrieve/graph_operation/graph_node/prompt values are 5000/10000/10000/4000/15000/2000 and whose generation_node_timeout_ms is 7000, start a running slow provider/engine harness, observe NodeStarted for GenerateAnswer before the stall, and prove that GenerateAnswer returns Timeout before the provider's generation_timeout_secs = 30 attempt budget expires. Record elapsed wall-clock evidence in the live test and assert it is near 7000ms, materially below 30000ms, rather than replacing this check with paused time or an in-process fake. Add a graph-operation stall and dense/retrieval stall control case proving the live timeout is owned by GenerateAnswer rather than an upstream node. Verify unknown workflow fields fail during configuration loading, zero or overflow timeout values fail validation, and a fast semantic node completes before its configured deadline. Add in-process cancellation cases during a fast node and during an in-flight generation attempt; assert the cancellation category, no later node lifecycle events, no additional provider work after teardown, and no fabricated answer. When preserving the shared config/config.verify.toml overlay, retain the existing `engine.lancedb_path` entry because `scripts/phase02_live_evidence.py` reads it as a cross-phase store contract. The authoritative client-disconnect proof is a named real-server/client test in 05-11, which must consume this cancellation-first production contract. Keep the malformed-request no-stream assertion and the successful D-03 zero-evidence path in the same matrix so cancellation and timeout changes cannot bypass D-04 or D-03. Follow the 05-PATTERNS.md config-overlay and deterministic-fault patterns. Generation retry classification and the between-attempt cancellation case are verified by 05-13 after this settings and cancellation contract.</action>
  <action>Add `workflow_phase5_timeout_cancels_stalled_provider` to the library-target workflow tests. Use a bounded stalled-I/O provider with an explicit cancellation observation, await the typed timeout path, assert cancellation is triggered before `NodeFailed(Timeout)`, and drain the provider with a bounded `tokio::time::timeout` to prove no provider progress continues after cancellation and no retry starts. Add `config_workflow_nested_env_overrides_match_contract` to the configuration test module: set all seven `LANCET_ENGINE__WORKFLOW__*` variables, invoke the actual settings loader, assert every typed workflow field receives its override, and restore the environment under the test's serialization guard.</action>
  <acceptance_criteria>
    - The repository regression test asserts every exact overlay value, preserves the existing `engine.lancedb_path` entry, and the inequalities `10000 + 4000 < 15000`, `65000 >= 2 * 30000 + 5000`, and `generation_timeout_secs(30s) > generation_node_timeout_ms(7000ms)`; the overlay comment names its Phase 02 and Phase 05 consumers.
    - The seven workflow settings and their units are asserted by name, including the 5000/10000/10000/4000/15000/2000/65000 production values.
    - Fast sub-second fake/paused-time cases are identified as semantic evidence only, while the live config.verify test reaches GenerateAnswer, uses a running slow provider, observes NodeStarted for that node, and observes Timeout near 7000ms before the 30-second provider-attempt budget.
    - Binary-owned production-builder cases prove no later node runs and no provider work continues after in-process stream teardown; the real HTTP disconnect and stalled-provider proof is named and executed by 05-11 rather than hidden in the generic library module.
    - `workflow_phase5_timeout_cancels_stalled_provider` is registered and proves, within a bounded drain, that timeout cancellation reaches the underlying provider before `NodeFailed(Timeout)` and prevents continued provider work or a retry.
    - `config_workflow_nested_env_overrides_match_contract` exercises the actual loader with all seven `LANCET_ENGINE__WORKFLOW__*` overrides and asserts typed parity after restoring the environment.
    - The live timeout regression body is present in workflow_phase5_production.rs; 05-18 registers it and 05-16 executes the exact binary test after the production BM25/test-fixture handoff is complete.
  </acceptance_criteria>
  <verify>
    <automated>$source = Get-Content 'engine/src/tests/workflow_phase5_production.rs' -Raw; if ($source -notmatch 'workflow_phase5_config_verify_generation_timeout') { throw 'live configuration timeout regression body is missing' }; $overlay = Get-Content 'config/config.verify.toml' -Raw; foreach ($value in @('generation_timeout_secs = 30','generation_node_timeout_ms = 7000')) { if ($overlay -notmatch [regex]::Escape($value)) { throw "live timeout contract value is missing: $value" } }; if ($source -notmatch '7000|generation_node_timeout_ms') { throw 'live test does not reference the configured node deadline' }; Write-Output 'live timeout implementation and test-body handoff validated'</automated>
    <automated>cargo check --bin engine --manifest-path engine/Cargo.toml --locked; $checkExit = $LASTEXITCODE; if ($checkExit -ne 0) { exit $checkExit }</automated>
    <automated>cargo test --bin engine --manifest-path engine/Cargo.toml --locked --no-run; $testCompileExit = $LASTEXITCODE; if ($testCompileExit -ne 0) { exit $testCompileExit }</automated>
    <automated>$source = Get-Content 'engine/src/tests/workflow_phase5_production.rs' -Raw; $library = Get-Content 'engine/src/tests/workflow_phase5.rs' -Raw; $configTests = Get-Content 'engine/src/tests.rs' -Raw; if ($source -notmatch 'workflow_phase5_config_verify_generation_timeout') { throw 'live configuration timeout regression body is missing' }; if ($library -notmatch 'workflow_phase5_timeout_cancels_stalled_provider') { throw 'stalled-provider timeout cancellation regression body is missing' }; if ($configTests -notmatch 'config_workflow_nested_env_overrides_match_contract') { throw 'nested workflow environment override regression body is missing' }; $overlay = Get-Content 'config/config.verify.toml' -Raw; foreach ($value in @('generation_timeout_secs = 30','generation_node_timeout_ms = 7000')) { if ($overlay -notmatch [regex]::Escape($value)) { throw "live timeout contract value is missing: $value" } }; $engineSection = [regex]::Match($overlay, '(?ms)^\[engine\]\s*(.*?)(?=^\[|\z)').Value; if ([string]::IsNullOrWhiteSpace($engineSection) -or $engineSection -notmatch '(?m)^lancedb_path\s*=') { throw 'shared verification overlay lost engine.lancedb_path' }; $runner = Get-Content 'engine/src/workflow/runner.rs' -Raw; $runNode = [regex]::Match($runner, '(?s)(?:pub\s+)?async\s+fn\s+run_node\b.*?(?=\n\s*(?:pub\s+)?async\s+fn\b|\z)').Value; $timeoutArm = [regex]::Match($runNode, '(?s)Err\(_\)\s*=>\s*\{\s*cancel\.cancel\(\).*?NodeError::timeout').Value; if ([string]::IsNullOrWhiteSpace($timeoutArm)) { throw 'run_node timeout arm does not cancel before constructing Timeout' } </automated>
    <automated>$names = @('workflow_phase5_timeout_cancels_stalled_provider','config_workflow_nested_env_overrides_match_contract'); $listed = cargo test --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; foreach ($name in $names) { $pattern = '(^|::)' + [regex]::Escape($name) + ': test$'; $hits = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -match $pattern }); if ($hits.Count -ne 1) { throw "exact focused registration missing or duplicated: $name" }; $output = & cargo test --manifest-path engine/Cargo.toml --locked $name -- --nocapture; $exit = $LASTEXITCODE; if ($exit -ne 0) { exit $exit } }</automated>
  </verify>
  <done>Regression coverage turns the verification runtime checks into automated evidence for settings, deadline arithmetic, nested environment overrides, and cancellation teardown through the stalled provider.</done>
  <must_haves>
    - AI-SPEC timeout and cancellation scenarios are covered without relying on wall-clock sleeps longer than the configured test values.
    - D-03 zero-evidence and D-04 pre-stream validation remain true while timeout and cancellation controls are applied.
  </must_haves>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Configuration file to Rust settings | Operator-controlled TOML can contain unknown or unsafe timeout values. |
| HTTP and gRPC stream lifetime to Rust task | Client disconnect is an asynchronous teardown signal that controls paid and resource-intensive work. |
| Rust workflow to external providers and stores | Embedding, graph, retrieval, and generation calls may stall or fail independently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09-01 | Tampering | `[engine.workflow]` parser | high | mitigate | Deny unknown workflow fields and reject non-positive values before the engine starts serving requests. |
| T-05-09-02 | Denial of service | Node deadlines | high | mitigate | Apply per-node and nested operation timeouts to the real adapters, including the 65-second generation outer budget. |
| T-05-09-03 | Denial of service | Client disconnect cancellation | high | mitigate | Tie stream drop and sender closure to CancellationToken cancellation and cancellation-first selects. |
| T-05-09-04 | Information disclosure | Provider error handling | medium | mitigate | Preserve typed safe NodeError categories and do not expose provider credentials or raw transport details in stream failures. |
| T-05-09-05 | Repudiation | Timeout versus cancellation classification | medium | mitigate | Add deterministic tests that distinguish Timeout from Cancelled and retain trace/session identity on terminal events. |
</threat_model>

<verification>
- The two task-level Cargo registration guards pass for `workflow_phase5_settings_applied_to_production` and `workflow_phase5_config_verify_generation_timeout`; each exact test name is listed once before its single-filter run.
- Unknown workflow settings fail loudly, all seven values apply to production execution, `config/config.verify.toml` sets the provider attempt to 30 seconds, and the live slow-provider harness produces the expected 7000ms generation-node timeout.
- Fast sub-second fake/paused-time tests remain separate semantic evidence and do not substitute for the wall-clock configuration check.
- Disconnect tests demonstrate prompt Rust teardown without later nodes or provider attempts.
</verification>

<success_criteria>
ORCH-03 is enforced for production I/O: settings are live, deadlines are bounded and distinct, exactly-one generation retry remains predictable, and native stream cancellation terminates work.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-09-SUMMARY.md` when done.
</output>
