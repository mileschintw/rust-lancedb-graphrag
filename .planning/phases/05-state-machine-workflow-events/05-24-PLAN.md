---
phase: 05-state-machine-workflow-events
plan: 24
type: execute
wave: 17
depends_on:
  - 05-16
  - 05-21
files_modified:
  - engine/src/retrieval/fusion.rs
  - engine/src/retrieval/tests.rs
  - engine/src/workflow/nodes/retrieve.rs
autonomous: true
gap_closure: true
requirements:
  - ORCH-01
  - ORCH-03
  - ORCH-05
must_haves:
  truths:
    - "RetrieveHybrid preserves the ordered, admitted reformulation variants, calls fuse_candidates exactly once for each variant, uses dense candidates only for variant zero, and evaluates BM25 for every variant per D-07 and D-08."
    - "For two or more variants, a second cross-variant RRF pass merges the per-variant fused outputs with a documented finite score formula and deterministic tie/order behavior; the one-variant path returns the established fuse_candidates scores and order unchanged."
    - "A two-variant regression asserts exact cross-variant scores and proves that per-variant source provenance survives the second pass."
  artifacts:
    - path: "engine/src/retrieval/fusion.rs"
      provides: "A bounded cross-variant RRF merge over already-fused per-variant outputs with explicit scoring and tie rules."
      contains: "fuse_cross_variant_candidates"
    - path: "engine/src/workflow/nodes/retrieve.rs"
      provides: "The RetrieveHybrid two-pass composition: one fuse_candidates call per ordered variant followed by the cross-variant merge."
      contains: "fuse_cross_variant_candidates"
    - path: "engine/src/retrieval/tests.rs"
      provides: "Exact two-variant score and deterministic tie/order regression coverage."
  key_links:
    - "WorkflowContext.variants -> RetrieveHybrid variant loop -> fuse_candidates(vector_i, bm25_i) -> per-variant fused lists"
    - "Per-variant fused lists -> fuse_cross_variant_candidates -> final candidate order, scores, and retained VariantProvenance"
    - "D-08 variant-zero embedding ownership -> dense candidates only for variant zero while D-07 still processes every admitted BM25 variant"
---

<objective>
Close the resolved cross-variant RRF contract that the executed 05-02 baseline could not be amended to specify: one existing fuse_candidates call per reformulation variant, followed by a second RRF merge over the per-variant fused outputs with exact scoring and ordering assertions.

Purpose: Make the research resolution executable for future multi-variant reformulation while preserving the locked one-variant behavior and the D-07/D-08 retrieval boundary.
Output: A two-pass fusion helper, an explicit RetrieveHybrid call site, and exact two-variant regression coverage.
</objective>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-preconditions.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-02-PLAN.md
@.planning/phases/05-state-machine-workflow-events/05-16-PLAN.md
@.planning/phases/05-state-machine-workflow-events/05-21-PLAN.md
@AGENTS.md
@rust-guidelines.md
@engine/src/retrieval/fusion.rs
@engine/src/workflow/nodes/retrieve.rs
@engine/src/retrieval/tests.rs

<scope_rationale>
This additive plan is deliberately separate from executed 05-02 and the already-reviewed typed-provenance cleanup in 05-21. It owns only the missing two-pass algorithm contract across the existing Rust fusion and RetrieveHybrid seams. The three-file inventory is one vertical retrieval slice: fusion defines the second pass, RetrieveHybrid wires it, and the retrieval test module proves exact scores and ordering. No protobuf, gateway, configuration, or real reformulation strategy work is added.
</scope_rationale>

<preservation_boundary>
Keep D-07 ordered multi-variant retrieval, D-08 variant-zero-only embedding, the eight-variant admission owned by 05-02/05-14, the existing source weights and rrf_k, finite-score rejection, candidate limits, and the typed provenance serialization owned by 05-21. Do not edit the executed 05-02 plan or summary. Do not flatten all variants into one dense/BM25 input before the per-variant calls.
</preservation_boundary>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Implement and trace the two-pass cross-variant RRF path</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-CONTEXT.md D-06 through D-08; 05-RESEARCH.md Pitfall 5 and Open Questions (RESOLVED) item 2; 05-02-PLAN.md frozen baseline contract; 05-16-PLAN.md retrieval ownership; 05-21-PLAN.md typed provenance ownership; engine/src/retrieval/fusion.rs; engine/src/workflow/nodes/retrieve.rs; engine/src/retrieval/tests.rs</read_first>
  <files>engine/src/retrieval/fusion.rs, engine/src/workflow/nodes/retrieve.rs, engine/src/retrieval/tests.rs</files>
  <action>Introduce fuse_cross_variant_candidates in engine/src/retrieval/fusion.rs as the second-pass merge over Vec&lt;Vec&lt;FusedCandidate&gt;&gt;. In RetrieveHybridNode, iterate the already-admitted ordered WorkflowContext.variants with a Rust for (variant_index, variant) in variants.iter().enumerate() loop; for each variant_index, call the existing fuse_candidates(vector_candidates_i, bm25_candidates_i, settings) exactly once, supplying dense candidates only for variant zero and that variant's BM25 candidates for every admitted variant, then pass the collected per-variant fused lists to fuse_cross_variant_candidates. Preserve each inner list's complete VariantProvenance entries and the existing source weights, candidate limits, finite-score guards, and rrf_k from RetrievalSettings. For one variant, make the second-pass helper return that list's established fused_score, fields, provenance, and order unchanged. For two or more variants, define and document the exact cross-variant score as sum(1.0 / (settings.rrf_k + rank as f64)) over each occurrence's one-based rank in the sorted per-variant fuse_candidates output; reject any non-finite contribution or total. Select retained candidate metadata from the highest inner fused_score, breaking ties by lower variant index, lower per-variant rank, then the existing candidate identity sort key. Sort final results by cross-variant score descending, best per-variant rank ascending, first variant index ascending, then the existing candidate identity sort key. Register cross_variant_rrf_two_variant_exact_scores in engine/src/retrieval/tests.rs as an end-to-end RetrieveHybrid-path tracer with two fake reformulation variants; it must assert the exact formula, one-variant identity, deterministic tie behavior, and retained vector/BM25 provenance rather than calling only the helper in isolation. This implements the resolved research contract per D-07 and preserves D-08 rather than choosing a flatten-then-fuse alternative.</action>
  <acceptance_criteria>
    - RetrieveHybrid has one explicit fuse_candidates invocation for each admitted ordered variant and one explicit second-pass fuse_cross_variant_candidates invocation after those per-variant lists exist.
    - The second-pass score is exactly the sum of 1.0 / (rrf_k + one_based_per_variant_rank) for each variant occurrence when at least two variants are present; all contributions and totals are finite-checked.
    - A single variant returns the existing fuse_candidates score/order identity, while two variants use the documented second-pass score and deterministic tie/order rules.
    - The exact-score test proves a two-variant result numerically and retains every inner vector/BM25 VariantProvenance contribution.
  </acceptance_criteria>
  <verify>
    <automated>$name = 'cross_variant_rrf_two_variant_exact_scores'; $listed = &amp; cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $pattern = '(^|::)' + [regex]::Escape($name) + ': test$'; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -match $pattern }); if ($registered.Count -ne 1) { throw "exact Cargo registration missing or duplicated for $name" }; $output = &amp; cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $name --nocapture; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
  </verify>
  <done>The production retrieval path executes the resolved two-pass algorithm, preserves one-variant behavior, and has an exact two-variant score/provenance tracer.</done>
</task>

<task type="auto">
  <name>Task 2: Add executable call-site and tie-order guards</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-RESEARCH.md Open Questions (RESOLVED) item 2; 05-24 Task 1 outputs; engine/src/retrieval/fusion.rs; engine/src/workflow/nodes/retrieve.rs; engine/src/retrieval/tests.rs</read_first>
  <files>engine/src/retrieval/fusion.rs, engine/src/workflow/nodes/retrieve.rs, engine/src/retrieval/tests.rs</files>
  <action>Add cross_variant_rrf_tie_order_is_deterministic to the retrieval test module. Build equal-score candidates whose occurrences span different variants and ranks, then assert the prescribed best-rank, first-variant, and candidate-identity tie sequence, along with stable serialized output across repeated runs. Add a source-scoped verification guard over the RetrieveHybrid variant loop that requires exactly one fuse_candidates( call in the loop body, a later fuse_cross_variant_candidates( call, a variant-zero-only dense input branch, and no whole-request flattening path. Keep the guard anchored to the RetrieveHybrid implementation region so unrelated helper calls cannot satisfy it. Keep the exact formula in the helper's documentation and test assertions, and retain the existing typed provenance enum boundary from 05-21.</action>
  <acceptance_criteria>
    - The tie-order regression proves score ties resolve by best per-variant rank, then first variant index, then the existing candidate identity key, and repeated serialization is identical.
    - The source-scoped guard proves the per-variant loop contains exactly one fuse_candidates call and that the second-pass helper is invoked after the loop.
    - The source-scoped guard proves dense retrieval remains variant-zero-only and the implementation has no flatten-then-fuse request path.
  </acceptance_criteria>
  <verify>
    <automated>$retrieve = Get-Content 'engine/src/workflow/nodes/retrieve.rs' -Raw; $region = [regex]::Match($retrieve, '(?s)impl\s+RetrieveHybridNode.*?(?=\n\s*impl\s+|\z)').Value; if ([string]::IsNullOrWhiteSpace($region)) { throw 'RetrieveHybridNode implementation region missing' }; $loopMatch = [regex]::Match($region, 'for\s*\(\s*variant_index\b.*?\)\s+in\s+'); if (-not $loopMatch.Success) { throw 'ordered RetrieveHybrid variant loop missing' }; $mergePos = $region.IndexOf('fuse_cross_variant_candidates(', $loopMatch.Index); if ($mergePos -lt 0) { throw 'cross-variant second-pass helper call missing' }; $loop = $region.Substring($loopMatch.Index, $mergePos - $loopMatch.Index); $fuseCount = [regex]::Matches($loop, '\bfuse_candidates\s*\(').Count; if ($fuseCount -ne 1) { throw "expected one fuse_candidates call in the per-variant loop, found $fuseCount" }; if ($region -notmatch '(?s)variant_index\s*==\s*0.*?vector|vector.*?variant_index\s*==\s*0') { throw 'variant-zero-only dense branch missing' }; if ($region -match '(?s)flat_map\s*\(.*?(?:vector|bm25)|flatten\s*\(') { throw 'whole-request flattening path remains in RetrieveHybrid' }; $name = 'cross_variant_rrf_tie_order_is_deterministic'; $listed = &amp; cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $pattern = '(^|::)' + [regex]::Escape($name) + ': test$'; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -match $pattern }); if ($registered.Count -ne 1) { throw "exact Cargo registration missing or duplicated for $name" }; $output = &amp; cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $name --nocapture; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
  </verify>
  <done>The two-pass contract is protected by a scoped call-site guard and deterministic tie/order regression, with no ambiguity left for a flatten-then-fuse implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reformulation variants -> RetrieveHybrid | Ordered variant strings and their retrieval outputs control bounded per-variant work. |
| Per-variant candidates -> cross-variant RRF | Candidate ranks and scores influence final ordering and can carry non-finite or adversarial values. |
| Fusion provenance -> serialized retrieval result | Inner vector/BM25 provenance crosses the Rust response boundary and must remain attributable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-24-01 | Denial of service | RetrieveHybrid variant loop | high | mitigate | Consume only the already-admitted maximum of eight variants, invoke bounded per-variant fusion once each, preserve candidate limits, and retain no unbounded flattened request accumulator. |
| T-05-24-02 | Tampering | cross-variant score accumulator | high | mitigate | Use the documented one-based-rank formula, reject non-finite contributions/totals, and apply deterministic tie/order rules proved by exact tests. |
| T-05-24-03 | Repudiation | VariantProvenance retention | medium | mitigate | Carry every inner vector/BM25 provenance contribution through the second pass and assert its exact retention in the two-variant regression. |
| T-05-24-SC | Tampering | npm/pip/cargo installs | high | accept | This plan adds no package-manager dependency or install task; no package legitimacy decision is introduced. |
</threat_model>

<verification>
The future executor runs the exact two-variant score tracer, the deterministic tie-order regression, and the RetrieveHybrid source-scoped call-site guard. The plan does not alter the executed 05-02 artifact or any gateway/protobuf contract.
</verification>

<success_criteria>
The pending Phase 5 plan set has an executable owner for the research-resolved two-pass D-07 fusion contract: every admitted variant receives one existing fusion call, the per-variant outputs receive a documented second RRF pass, exact scores and tie order are tested, and D-08 variant-zero embedding plus typed provenance remain intact.
</success_criteria>

<output>
Create .planning/phases/05-state-machine-workflow-events/05-24-SUMMARY.md when done.
</output>
