---
phase: 05-state-machine-workflow-events
plan: 15
type: execute
wave: 13
depends_on:
  - 05-13
  - 05-18
files_modified:
  - engine/src/prompt.rs
  - engine/src/workflow/ports.rs
  - engine/src/generation/mod.rs
  - engine/src/tests/workflow_phase5.rs
autonomous: true
gap_closure: true
requirements:
  - ORCH-02
  - ORCH-03
must_haves:
  truths:
    - "The public asynchronous prompt helpers document their inputs, output, graph_weight behavior, cancellation behavior, and every PromptAssemblyError outcome."
    - "graph_weight has an executable contract: zero excludes graph facts, while a positive value includes graph facts without changing the evidence-selection authority of prompt assembly."
    - "Synchronous prompt helpers are test-only crate-visible conveniences or are removed; no public production synchronous blocking API remains."
    - "Fake workflow ports are compiled only under cfg(test), while production adapters use the real service implementations from 05-08."
    - "All 25 workflow fake-port call sites have already been relocated out of the binary test module by 05-18, and the complete binary test target has passed its introducing --no-run gate before this cfg(test) change."
  artifacts:
    - path: "engine/src/prompt.rs"
      provides: "Documented async prompt API, explicit graph_weight semantics, and test-only sync helpers"
    - path: "engine/src/workflow/ports.rs"
      provides: "cfg(test)-gated fake workflow ports and source-level production compilation assertion"
  key_links:
    - "pack_evidence_and_graph_prompt cancellation -> PromptAssemblyError::Cancelled"
    - "graph_weight -> graph-fact inclusion semantics -> prompt regression test"
    - "cfg(test) fake ports -> deterministic Rust tests without production fake symbols"
---

<objective>
Close the prompt API documentation and production test-double hygiene findings without changing the locked prompt protocol.

Purpose: Review warnings WR-10/WR-11 and the verification warning about Fake* ports are executable quality gaps. This plan documents the public async contract, makes graph_weight auditable, and gates fakes out of production compilation.
Output: Focused prompt API tests and cfg(test)-only workflow fakes with source assertions.
</objective>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-preconditions.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-reversibility.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-PATTERNS.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEW.md
@.planning/phases/05-state-machine-workflow-events/05-VERIFICATION.md
@.planning/phases/05-state-machine-workflow-events/05-18-PLAN.md
@AGENTS.md
@engine/src/prompt.rs
@engine/src/workflow/ports.rs

## Artifacts this phase produces

- Restored public async prompt function summaries with graph_weight and complete error documentation.
- Test-only crate-visible synchronous helper coverage, if retained for existing tests.
- cfg(test)-gated FakeQueryReformulator, FakeQueryEmbeddingPort, FakeGraphQueryPort, FakeDenseRetrievalPort, FakeBm25RetrievalPort, and FakeReranker definitions with source assertions.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Restore the documented asynchronous prompt contract</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; 05-CONTEXT.md; 05-REVIEW.md; engine/src/prompt.rs; engine/src/workflow/nodes/assemble_prompt.rs; engine/src/tests/workflow_phase5.rs</read_first>
  <files>engine/src/prompt.rs, engine/src/tests/workflow_phase5.rs</files>
  <action>Restore rustdoc summaries for the public asynchronous `pack_evidence_prompt` and `pack_evidence_and_graph_prompt` functions, naming their evidence/graph inputs, output, cancellation behavior, and all `PromptAssemblyError` variants including EmptyEvidence, NoEvidenceFits, and Cancelled. Define and document graph_weight as follows: `0.0` excludes graph facts from the assembled prompt; any positive value includes graph facts, while evidence selection and ordering remain owned by retrieval rather than being silently re-ranked by prompt assembly. Preserve the existing cancellation checks and D-01 structured generation input contract. Gate `pack_evidence_prompt_sync` and `pack_evidence_and_graph_prompt_sync` as test-only crate-visible helpers (`#[cfg(test)] pub(crate)`) because their existing callers are in `prompt.rs:234,255` and are outside this task's file inventory; no public production synchronous blocking wrapper may remain. Add the named tests `workflow_phase5_prompt_api_surface` and `workflow_phase5_prompt_graph_weight_semantics` to `engine/src/tests/workflow_phase5.rs`, using the `05-PATTERNS.md` prompt and generation analogs.</action>
  <acceptance_criteria>
    - Both public async prompt helpers have complete summaries, parameter/return/error sections, and explicit graph_weight semantics; `engine/src/tests/workflow_phase5.rs` contains the two named prompt regressions.
    - Zero graph weight omits graph facts, positive graph weight includes them, and cancellation returns PromptAssemblyError::Cancelled.
    - Synchronous helpers are absent from the public API or are cfg(test) crate-visible only; focused tests exercise the retained behavior.
  </acceptance_criteria>
  <verify>
    <automated>$testFile = 'engine/src/tests/workflow_phase5.rs'; if (!(Test-Path -LiteralPath $testFile)) { throw 'workflow_phase5.rs test target is missing' }; $testSource = Get-Content -LiteralPath $testFile -Raw; foreach ($name in @('workflow_phase5_prompt_api_surface','workflow_phase5_prompt_graph_weight_semantics')) { if (!$testSource.Contains($name)) { throw "Prompt regression registration missing from $testFile`: $name" } }</automated>
    <automated>$test = 'workflow_phase5_prompt_api_surface'; $listed = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $pattern = '(^|::)' + [regex]::Escape($test) + ': test$'; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -match $pattern }); if ($registered.Count -ne 1) { throw "exact Cargo registration missing or duplicated for $test" }; $output = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $test --nocapture; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
    <automated>$test = 'workflow_phase5_prompt_graph_weight_semantics'; $listed = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $pattern = '(^|::)' + [regex]::Escape($test) + ': test$'; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -match $pattern }); if ($registered.Count -ne 1) { throw "exact Cargo registration missing or duplicated for $test" }; $output = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $test --nocapture; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
  </verify>
  <done>The async prompt API is documented and tested, graph_weight behavior is explicit, and public synchronous blocking helpers are removed or test-only.</done>
  <must_haves>
    - D-01 remains unchanged; no model output, citation, or metadata contract is broadened.
  </must_haves>
</task>

<task type="auto">
  <name>Task 2: Gate workflow fake ports out of production compilation</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; 05-REVIEW.md; 05-VERIFICATION.md; 05-18-PLAN.md; engine/src/lib.rs; engine/src/tests.rs; engine/src/workflow/ports.rs; engine/src/generation/mod.rs; engine/src/tests/workflow_phase5.rs</read_first>
  <files>engine/src/workflow/ports.rs, engine/src/generation/mod.rs, engine/src/tests/workflow_phase5.rs</files>
  <action>After 05-18 has moved the generic Phase 5 workflow matrix into library unit-test scope and passed `cargo test --bin engine --manifest-path engine/Cargo.toml --locked --no-run`, place every Fake* workflow port and its impl blocks under cfg(test) while preserving the test-only names and trait behavior used by deterministic workflow tests. Gate the existing FakeGenerator declaration and impl in engine/src/generation/mod.rs under cfg(test) as well; add a source assertion over the production module that fails if an ungated FakeGenerator symbol or production adapter reference remains. Keep NoOpQueryReformulator available wherever production construction requires the locked ORCH-05 pass-through, but do not gate real production port traits or adapters. Add workflow_phase5_fake_ports_test_only as a source assertion that locates each fake declaration and verifies it is inside a cfg(test) region; the assertion must cover FakeQueryReformulator, FakeQueryEmbeddingPort, FakeGraphQueryPort, FakeDenseRetrievalPort, FakeBm25RetrievalPort, FakeReranker, and FakeGenerator. Use the 05-PATTERNS.md existing FakeGenerator and port seams; production dependency construction remains the real-field path owned by 05-08. Re-run the complete binary `--no-run` gate after applying cfg(test) so no unresolved binary test import can be hidden; then compile the library test target, where engine::workflow::ports exposes the fakes to engine/src/tests/workflow_phase5.rs, while only workflow_phase5_production.rs remains registered in the binary target.</action>
  <acceptance_criteria>
    - All six named Fake* port definitions and impls plus FakeGenerator are cfg(test)-gated, while production traits, real adapters, and NoOpQueryReformulator remain available.
    - The 25 relocated binary-test fake call sites remain library-owned, and the binary test target passes `cargo test --bin engine --manifest-path engine/Cargo.toml --locked --no-run` after gating.
    - The source assertion fails if a fake declaration loses its cfg(test) boundary, if the production generation module exports FakeGenerator, or if a production adapter references a fake.
    - Existing deterministic tests retain their fake-port behavior under test compilation.
  </acceptance_criteria>
  <verify>
    <automated>$test = 'workflow_phase5_fake_ports_test_only'; $listed = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $pattern = '(^|::)' + [regex]::Escape($test) + ': test$'; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -match $pattern }); if ($registered.Count -ne 1) { throw "exact Cargo registration missing or duplicated for $test" }; $output = cargo test --lib --manifest-path engine/Cargo.toml --locked -- --exact $test --nocapture; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
  </verify>
  <done>Production compilation has no Fake* workflow port symbols, and focused source/test assertions prove the cfg(test) boundary.</done>
  <must_haves>
    - The test-only fake gate does not replace or weaken the real production adapter proof in 05-08.
  </must_haves>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Public prompt API to callers | Documentation and error semantics govern how cancellation and graph context are interpreted. |
| Test fixtures to production build | Fake ports can accidentally become runtime dependencies if compilation boundaries are absent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-15-01 | Tampering | Prompt assembly semantics | medium | mitigate | Document and test graph_weight and cancellation behavior against the existing prompt implementation. |
| T-05-15-02 | Denial of service | Synchronous prompt wrappers | medium | mitigate | Remove public blocking wrappers or gate them to tests so production async paths cannot block the runtime. |
| T-05-15-03 | Tampering | Fake workflow ports | high | mitigate | cfg(test)-gate all six fake definitions and assert their source boundaries. |
| T-05-15-04 | Information disclosure | Prompt errors | medium | mitigate | Preserve typed safe errors and do not add raw provider or prompt content to public docs or events. |
</threat_model>

<verification>
- The two prompt registration guards and fake-port registration guard each pass with one exact Cargo filter.
- Source assertions prove complete async docs, explicit graph_weight behavior, test-only synchronous helpers, and cfg(test) fake ports.
- No production dependency path is allowed to reference a fake port.
</verification>

<success_criteria>
ORCH-02 and ORCH-03 retain an auditable prompt contract and production-only adapter boundary, with WR-10/WR-11 and the fake-port warning closed by executable checks.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-15-SUMMARY.md` when done.
</output>
