---
phase: 05-state-machine-workflow-events
plan: 08
type: execute
wave: 7
depends_on:
  - 05-05
  - 05-07
files_modified:
  - engine/src/main.rs
  - engine/src/workflow/mod.rs
  - engine/src/workflow/ports.rs
  - engine/src/workflow/nodes/graph_context.rs
  - engine/src/workflow/nodes/retrieve.rs
  - engine/src/tests.rs
  - engine/src/tests/workflow_phase5_production.rs
autonomous: true
gap_closure: true
requirements:
  - ORCH-01
  - ORCH-02
  - ORCH-03
  - ORCH-04
  - ORCH-05
must_haves:
  truths:
    - "The production query_rag handler runs the real five-node workflow in the locked order: ReformulateQuery, ExtractGraphContext, RetrieveHybrid, AssemblePrompt, and GenerateAnswer."
    - "The production runner owns real embedder, graph, dense, BM25, reranker, and generator dependencies rather than an all-None dependency container or a sink-less inline remainder."
    - "Cross-variant retrieval, variant-zero embedding, graph degradation, and zero-evidence short-circuit behavior are reachable from a production query."
    - "A successful production query populates the accumulated WorkflowContext used by response construction and checkpoint emission, including retrieval results, evidence blocks, prompt, answer, citations, notices, and retrieval snapshot data."
    - "The QueryReformulator pass-through node remains the ORCH-05 entry point while real orchestration uses the same state-machine path."
  artifacts:
    - path: "engine/src/main.rs"
      provides: "Production workflow builder, request-scoped adapters, five-node registration, and query_rag execution through WorkflowRunner"
    - path: "engine/src/workflow/mod.rs"
      provides: "Dependency construction and context-to-response wiring without a production remainder bridge"
    - path: "engine/src/workflow/ports.rs"
      provides: "Typed GraphQueryPort and fake-port graph-fact contract"
    - path: "engine/src/workflow/nodes"
      provides: "Real graph, retrieval, prompt, and generation node behavior over WorkflowContext"
    - path: "engine/src/tests/workflow_phase5.rs"
      provides: "Production-path regression coverage for five-node reachability and context population"
    - path: "engine/src/tests/workflow_phase5_production.rs"
      provides: "Binary-target production-builder coverage kept separate from library-target fake-port tests"
  key_links:
    - "query_rag -> request-scoped WorkflowDependencies -> five registered nodes -> WorkflowContext -> streamed terminal response"
    - "embedder and graph adapters -> ExtractGraphContext -> graph_context and notices -> retrieval and full checkpoint context"
    - "GraphQueryPort typed GraphFactBlock vector -> WorkflowContext.graph_facts -> prompt packing and GenerationRequest"
    - "dense and BM25 adapters -> RetrieveHybrid -> evidence_blocks and final_candidates -> AssemblePrompt -> GenerateAnswer"
    - "correlation_id -> WorkflowContext.trace_id and event identity, preserving D-29"
---

<objective>
Close the production reachability gap: make a real query enter and complete through the Rust five-node state machine with real service adapters and accumulated context.

Purpose: The historical plans built the workflow library and test-only runner, but verification proved that production query_rag still registers one pass-through node and then executes the old inline path. This plan makes ORCH-01, ORCH-02, ORCH-03, ORCH-04, and ORCH-05 true on the production path without changing the locked protocol or standalone graph RPC.
Output: Production adapter wiring, five-node registration, removal or isolation of the inline remainder, and a Rust regression test that exercises the production builder.
</objective>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-preconditions.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-reversibility.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-PATTERNS.md
@.planning/phases/05-state-machine-workflow-events/05-VERIFICATION.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEWS.md
@.planning/phases/05-state-machine-workflow-events/05-AI-SPEC.md
@AGENTS.md
@rust-guidelines.md
@engine/src/main.rs
@engine/src/workflow/mod.rs
@engine/src/workflow/ports.rs
@engine/src/workflow/runner.rs
@engine/src/workflow/nodes/graph_context.rs
@engine/src/workflow/nodes/retrieve.rs
@engine/src/workflow/nodes/assemble_prompt.rs
@engine/src/workflow/nodes/generate.rs

## Artifacts this phase produces

- A production `WorkflowDependencies` builder backed by the existing engine embedder, graph/database, dense, BM25, reranker, and generator services.
- A production `query_rag` runner with all five public nodes registered in D-06 order and the pass-through QueryReformulator retained for ORCH-05.
- A production-path regression test proving node reachability, answer-chunk cardinality, zero-evidence behavior, graph degradation, and context population.
- A typed graph-fact carrier whose `GraphQueryPort` contract and `WorkflowContext` use one authoritative fact representation; the prompt/generation handoff and reachability expansion are owned by 05-22 after the target seam is ready.
</context>

<scope_rationale>
This plan owns the binary-side `engine/src/tests.rs` production-module registration, the production-handler assertion strengthening needed for the builder/context boundary, and the GraphQueryPort call-site migration needed to compile the initial production test bodies. It owns the production-query regression bodies `workflow_phase5_production_five_node`, `workflow_phase5_production_dependencies_are_real`, and `workflow_phase5_production_context_population`; 05-22 owns the typed prompt/generation handoff and the final reachability expansion, while later plans may rerun these exact tests after their owned adapter changes but must not reimplement or move them. 05-18 owns the later generic-library registration, removal of the duplicate generic binary registration, and the 18 BM25 fixture/construction migrations. The production builder/context slice therefore compiles its own module immediately while leaving the target-aware fake-port/BM25 handoff to 05-18.
</scope_rationale>

<atomicity_rationale>
The seven-file inventory is the smallest executable production builder/context boundary: `main.rs` owns handler and service-field wiring, `workflow/mod.rs` and `ports.rs` define dependency/context contracts, graph/retrieval nodes consume those contracts, and `tests.rs` plus the production test module compile and exercise the binary-owned seam. Prompt/generation node edits and the final reachability test are intentionally split into 05-22 after 05-18 establishes the library/binary target handoff; this avoids claiming a vacuous library gate and keeps each plan's file ownership reviewable.
</atomicity_rationale>

<tasks>

<task type="tracer">
  <name>Task 1: Wire one real production query through all five workflow nodes</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; engine/src/main.rs; engine/src/workflow/mod.rs; engine/src/workflow/ports.rs; engine/src/workflow/runner.rs; engine/src/workflow/nodes/graph_context.rs; engine/src/workflow/nodes/retrieve.rs; engine/src/workflow/nodes/assemble_prompt.rs; engine/src/workflow/nodes/generate.rs; 05-CONTEXT.md; 05-RESEARCH.md; 05-VERIFICATION.md</read_first>
  <files>engine/src/main.rs, engine/src/workflow/mod.rs, engine/src/tests.rs, engine/src/tests/workflow_phase5_production.rs</files>
  <action>Build a named request-scoped production dependency factory, `build_production_workflow`, in the Rust service and add the test bodies `workflow_phase5_production_five_node`, `workflow_phase5_production_dependencies_are_real`, and `workflow_phase5_production_context_population` in the binary-owned `workflow_phase5_production.rs` module. Register that production module in `engine/src/tests.rs` in this task so the new test bodies are compiled at the introducing wave; 05-18 later owns the generic-library registration, BM25 fixture migration, and keeps exactly one production registration. Keep generic fake-port workflow tests in `workflow_phase5.rs` for the library target. Populate every applicable WorkflowDependencies port from the existing service fields: the real embedder, database-backed graph adapter, dense LanceDB adapter built from `self.nodes`, BM25 index adapter, reranker, generator, and effective retrieval settings. The production factory must construct concrete adapters around those existing fields and place them in `Some(Arc<dyn ...>)` slots; it must reject or fail the production build before runner execution if any required slot is absent, rather than allowing an omitted adapter to surface only during a request. It must not call the empty constructor or substitute Fake* ports. Preserve the validated QueryRequest and filter needed by dense and lexical retrieval, and make each adapter propagate the workflow CancellationToken. Register ReformulateQuery, ExtractGraphContext, RetrieveHybrid, AssemblePrompt, and GenerateAnswer on the production runner in the D-06 order; retain the pass-through QueryReformulator required by ORCH-05. Make query_rag invoke the named production builder over the validated request with trace_id equal to correlation_id per D-29 and keep the existing unary-to-server-streaming contract from D-18. The tracer must use real production builder invocation and real event sink plumbing, not a standalone library runner with deterministic local ports. Keep the inline compatibility helper as a typed-failure seam for its existing test callers, without moving prompt/generation request construction into this task. Follow the 05-PATTERNS.md controller and adapter analogs for engine/src/main.rs, engine/src/workflow/node.rs, engine/src/workflow/nodes/graph_context.rs, and engine/src/workflow/nodes/retrieve.rs; document any justified seam deviation in the implementation. Do not alter the standalone QueryGraph RPC per D-10. Addresses review concerns HIGH-2, MEDIUM-1, MEDIUM-2, and the review finding that Option-wrapped production dependencies could fail only at request time.</action>
  <action>Keep request construction cheap: clone existing shared service and resource handles with `Arc::clone` inside the per-request builder, while store opening, index building, client construction, and other resource-heavy initialization remain outside it. Extend `workflow_phase5_production_dependencies_are_real` to invoke the builder repeatedly and assert shared-handle identity or an equivalent construction counter remains stable, proving that the builder creates only lightweight request-local wrappers.</action>
  <acceptance_criteria>
    - `query_rag` constructs all five nodes in the exact D-06 order and no longer constructs a production runner with an all-None WorkflowDependencies value.
    - The production adapter factory consumes the existing engine service fields and stores non-None concrete adapters for embedding, graph, dense, BM25, reranking, and generation; the dedicated dependency test fails if any slot is None or backed by a Fake* type.
    - Repeated production-builder construction clones shared Arc-backed handles without reopening stores, rebuilding indexes, or constructing resource-heavy clients; the focused dependency regression detects reinitialization or changed shared-handle identity.
    - The production closure invokes the state-machine runner with the request-scoped context and event sink; it does not call a sink-less remainder function.
    - A normal request reaches GenerateAnswer through the state machine, while a malformed request is still rejected before the stream opens per D-04.
  </acceptance_criteria>
  <verify>
    <automated>$main = Get-Content 'engine/src/main.rs' -Raw; $query = [regex]::Match($main, '(?s)async fn query_rag\b.*?(?=\n\s*(?:pub\s+)?(?:async\s+)?fn\b|\z)').Value; $builder = [regex]::Match($main, '(?s)fn build_production_workflow\b.*?(?=\n\s*(?:pub\s+)?(?:async\s+)?fn\b|\z)').Value; if ([string]::IsNullOrWhiteSpace($query) -or [string]::IsNullOrWhiteSpace($builder)) { throw 'production query_rag or builder region missing' }; if (-not $query.Contains('build_production_workflow')) { throw 'query_rag does not invoke the production workflow builder' }; if ($query -match 'execute_inline_query_rag_remainder') { throw 'legacy inline remainder remains reachable from query_rag' }; if (-not $builder.Contains('WorkflowDependencies')) { throw 'production builder does not construct WorkflowDependencies' }; foreach ($field in @('self.embedder','self.database','self.bm25_index','self.reranker','self.generator')) { if (-not $builder.Contains($field)) { throw "production builder does not consume $field" } }; foreach ($field in @('reformulator','embedding_port','graph_port','dense_port','bm25_port','reranker_port','generator')) { if (-not $builder.Contains($field)) { throw "production dependency field missing: $field" } }; foreach ($adapter in @('EmbeddingProvider','GraphQueryPort','DenseRetrievalPort','Bm25RetrievalPort','Reranker','Generator')) { if (-not $builder.Contains($adapter)) { throw "production adapter type missing: $adapter" } }; if (@([regex]::Matches($builder, 'Some\s*\(')).Count -lt 7) { throw 'production builder does not construct all seven non-None dependency slots' }; if ($builder -match 'Fake(?:Query|Graph|Dense|Bm25|Reranker|Generator)') { throw 'production builder references a test-only fake' }; $nodes = @('ReformulateQueryNode','ExtractGraphContextNode','RetrieveHybridNode','AssemblePromptNode','GenerateAnswerNode'); $positions = @(); foreach ($node in $nodes) { $pos = $builder.IndexOf($node); if ($pos -lt 0) { throw "production builder missing $node" }; $positions += $pos }; if (@([regex]::Matches($builder, 'add_node')).Count -ne 5) { throw 'production builder does not register exactly five nodes' }; for ($i=1; $i -lt $positions.Count; $i++) { if ($positions[$i] -le $positions[$i-1]) { throw 'production node registration order is not D-06 order' } }; $production = Get-Content 'engine/src/tests/workflow_phase5_production.rs' -Raw; foreach ($test in @('workflow_phase5_production_five_node','workflow_phase5_production_dependencies_are_real')) { if (-not $production.Contains($test)) { throw "production test body missing: $test" } }; $compile = cargo check --bin engine --manifest-path engine/Cargo.toml --locked; $compileExit = $LASTEXITCODE; if ($compileExit -ne 0) { exit $compileExit }</automated>
    <automated>$builder = [regex]::Match((Get-Content 'engine/src/main.rs' -Raw), '(?s)fn build_production_workflow\b.*?(?=\n\s*(?:pub\s+)?(?:async\s+)?fn\b|\z)').Value; if ($builder -notmatch 'Arc::clone') { throw 'production builder does not show cheap Arc handle cloning' }; $production = Get-Content 'engine/src/tests/workflow_phase5_production.rs' -Raw; if ($production -notmatch 'workflow_phase5_production_dependencies_are_real[\s\S]{0,5000}(Arc::ptr_eq|strong_count|construction)') { throw 'production dependency test lacks a no-reinitialization guard' }</automated>
  </verify>
  <done>The production happy path traverses the five registered nodes through real adapter dependencies, and repeated request construction is proven to reuse shared handles without resource reinitialization.</done>
  <must_haves>
    - D-06 order is observable from production node lifecycle events.
    - D-10 standalone QueryGraph remains untouched.
    - D-18 streaming and D-29 trace identity remain intact.
    - ORCH-01 and ORCH-05 are reachable from `query_rag`, not only from unit-test runners.
  </must_haves>
  <reversibility rating="costly">Production query routing changes the execution path for every RAG request; keep the adapter boundary and the existing service fields explicit so the change is reviewable and revertible as one plan commit.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Transfer retrieval and answer state into the real WorkflowContext</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; engine/src/workflow/ports.rs; engine/src/workflow/mod.rs; engine/src/workflow/nodes/graph_context.rs; engine/src/workflow/nodes/retrieve.rs; engine/src/main.rs; engine/src/tests/workflow_phase5_production.rs; 05-CONTEXT.md; 05-AI-SPEC.md; 05-REVIEWS.md</read_first>
  <files>engine/src/workflow/ports.rs, engine/src/workflow/mod.rs, engine/src/workflow/nodes/graph_context.rs, engine/src/workflow/nodes/retrieve.rs, engine/src/main.rs</files>
  <action>Move the production graph and retrieval data flow into WorkflowContext and the existing nodes. Change the current GraphQueryPort contract in `engine/src/workflow/ports.rs` from a string-typed graph result to `Result<Vec<GraphFactBlock>, NodeError>` (or the equivalent typed fact vector already used by the prompt layer), and update every production implementation and in-scope adapter call site needed for this wave so the conversion boundary is explicit; 05-18 later relocates the already-typed generic fake-dependent blocks into the library target without changing their semantics. Make `ExtractGraphContext` store that exact typed vector in `WorkflowContext.graph_facts`; it may derive a separate rendered graph-context string for legacy display fields, but the typed vector is authoritative in context. RetrieveHybrid must consume the variant-zero embedding prepared by the upstream graph-context prelude per D-08, query BM25 for all accepted variants and perform D-07 cross-variant RRF, preserve vector and lexical result identifiers, create evidence blocks, and populate final_candidates and retrieval snapshot provenance. ExtractGraphContext must run before hybrid retrieval per D-06; graph no-match or logical failure continues with chunk-only evidence and a visible notice/checkpoint per D-09, while graph timeout remains distinguishable from cancellation. Preserve D-03: zero evidence is a valid success that exits before AssemblePrompt and GenerateAnswer and emits no AnswerChunk. Keep `run_inline_prompt_generation_remainder` only as the existing inline compatibility seam for its test callers and make its no-generator branch return typed `LlmGenerationFailed`; 05-22 owns the prompt/generation request handoff, fabrication-text guards, caller assertions, and final production reachability assertions. Do not move prompt/generation node construction into this task. Record any justified port-shape deviation at the file/function boundary. Do not add workflow metadata or per-node tracing fields prohibited by D-30 and D-31. Pattern reference: follow the `engine/src/workflow/nodes/graph_context.rs` and `engine/src/workflow/nodes/retrieve.rs` sections in `05-PATTERNS.md` for the existing graph outcome match and dense/BM25 fusion call-site shape. Addresses review concern MEDIUM-4 by keeping the inline fallback seam explicit while leaving provider-facing assertions to its owning plan.</action>
  <acceptance_criteria>
    - Production retrieval leaves non-empty vector_results, bm25_results, final_candidates, and evidence_blocks whenever the fixtures return evidence.
    - Production graph context and graph degradation notices survive into the same accumulated context used for checkpoint serialization and the terminal response.
    - GraphQueryPort no longer exposes a bare string result: the production graph adapter and current test seam return typed GraphFactBlock values, and the vector reaches WorkflowContext without a string-only port boundary.
    - The graph node leaves the authoritative typed GraphFactBlock vector in WorkflowContext; provider-facing transfer is verified by 05-22.
    - Zero-evidence production queries complete successfully without entering prompt or generation and without emitting an AnswerChunk, while retrieval failures remain typed failures.
    - The no-generator inline helper branch returns typed LlmGenerationFailed as the compatibility seam; its provider-facing caller assertions are owned by 05-22.
  </acceptance_criteria>
  <verify>
    <automated>$ports = Get-Content 'engine/src/workflow/ports.rs' -Raw; $graph = Get-Content 'engine/src/workflow/nodes/graph_context.rs' -Raw; $retrieve = Get-Content 'engine/src/workflow/nodes/retrieve.rs' -Raw; $workflow = Get-Content 'engine/src/workflow/mod.rs' -Raw; if ($ports -notmatch 'GraphQueryPort[\s\S]{0,900}Vec\s*<\s*GraphFactBlock\s*>') { throw 'GraphQueryPort is not typed as GraphFactBlock values' }; if ($graph -notmatch 'graph_facts\s*=') { throw 'ExtractGraphContext does not populate WorkflowContext.graph_facts' }; foreach ($needle in @('variant-zero','final_candidates','evidence_blocks')) { if ($retrieve -notmatch [regex]::Escape($needle)) { throw "retrieval context guard missing $needle" } }; if ($workflow -notmatch 'LlmGenerationFailed') { throw 'inline compatibility helper does not return typed generation failure' }; $compile = cargo check --bin engine --manifest-path engine/Cargo.toml --locked; $compileExit = $LASTEXITCODE; if ($compileExit -ne 0) { exit $compileExit }</automated>
  </verify>
<done>The real graph and retrieval nodes own typed context, evidence, and degradation state, the inline compatibility seam returns typed failure, and 05-22 owns provider-facing prompt/generation assertions.</done>
  <must_haves>
    - D-03, D-06, D-07, D-08, and D-09 are enforced on the production path.
    - D-01 and D-02 remain distinct at the event boundary.
    - ORCH-02 and ORCH-04 receive the same accumulated context that ORCH-01 executes.
  </must_haves>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client request to Rust service | Query text, session, correlation, and filters are untrusted before validation. |
| Rust workflow to retrieval and model adapters | External database, graph, embedding, reranker, and provider calls can stall, fail, or return malformed data. |
| Rust workflow to Go stream | Workflow events and checkpoint snapshots cross the gRPC stream boundary and must retain identity and type separation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08-01 | Tampering | `query_rag` request context | high | mitigate | Validate session, correlation, query, and filter before creating the stream; carry the validated request into every adapter. |
| T-05-08-02 | Denial of service | Production retrieval and generation adapters | high | mitigate | Route all work through cancellable workflow nodes so the timeout and disconnect plans can bound every real I/O operation. |
| T-05-08-03 | Information disclosure | Workflow event and checkpoint boundary | medium | mitigate | Keep checkpoint JSON in checkpoint events only and keep provider errors typed and human-safe; Go SSE mapping is covered by 05-11. |
| T-05-08-04 | Spoofing | Session and trace identity | medium | mitigate | Set trace_id from correlation_id per D-29 and use the validated session identity for every event and snapshot. |
| T-05-08-05 | Repudiation | Production workflow history | medium | mitigate | Populate full context and preserve node identity so later checkpoint rows can explain the request path. |
</threat_model>

<verification>
- The production source/body guards and binary compile checks for the builder/context boundary pass in this wave; 05-22 owns the final typed prompt/generation handoff and reachability filters, and 05-20 Task 2 reruns the complete production filter set after later generation/timing changes.
- 05-18 owns the later target split and relocates the generic fake-dependent blocks without weakening these production-builder tests.
- The production query handler has one five-node state-machine path with real dependencies and no sink-less remainder path.
- Production regression coverage owned here proves D-03 zero-evidence behavior, D-06 ordering, D-07 and D-08 retrieval semantics, graph degradation, and typed context population; 05-22 is the authority for provider-request, fabrication-text, and terminal reachability assertions.
</verification>

<success_criteria>
ORCH-01, ORCH-02, ORCH-03, ORCH-04, and ORCH-05 have a real production builder/context path with the historical inline production path retired from reachability; 05-22 completes the typed prompt/generation request proof.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-08-SUMMARY.md` when done.
</output>
