---
phase: 05-state-machine-workflow-events
plan: 26
type: execute
wave: 1
depends_on: []
files_modified: ["engine/src/main.rs", "engine/src/tests.rs", "gateway/main_test.go"]
autonomous: true
requirements: [ORCH-01, ORCH-02]
gap_closure: true
gap_ids: [G-05-1]

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "TestRAGQueryCrossRuntime and TestRAGQueryClientDisconnectCancelsRustWorkflow pass regardless of what config/config.toml's [openrouter].generation_model is currently set to."
    - "engine/src/main.rs treats LANCET_OPENROUTER__GENERATION_MODEL as an explicit boundary-critical override, consistent with its sibling LANCET_OPENROUTER__* overrides."
    - "Both real-engine gateway tests pin their spawned engine subprocess's generation_model to the value matching their own httptest mock, independent of ambient config.toml."
  artifacts:
    - "engine/src/main.rs (load_settings gains an explicit LANCET_OPENROUTER__GENERATION_MODEL override)"
    - "engine/src/tests.rs (new focused test proving env overrides config.toml for this field)"
    - "gateway/main_test.go (assertCleanRAGChildEnv allowlist + both engineEnv() call sites pin the override)"
  key_links:
    - "LANCET_OPENROUTER__GENERATION_MODEL env var -> settings.openrouter.generation_model -> EffectiveRagSettings.generation_model -> OpenRouter capability preflight request (openrouter.rs's model-in-list check)"
    - "gateway ragChildEnv(...) -> spawned engine subprocess env -> assertCleanRAGChildEnv allowlist gate"
---

<objective>
Closes UAT gap G-05-1's Blocker B: gateway/main_test.go's TestRAGQueryCrossRuntime and TestRAGQueryClientDisconnectCancelsRustWorkflow spawn the real engine binary but never override generation_model for it, so the spawned engine silently reads config/config.toml's live value while the tests' own httptest /models and /chat/completions mocks hardcode "openai/gpt-4o-mini" in 5 places (main_test.go:2074, 2111, 2142, 2397, 3457). Any future config.toml generation_model change breaks both tests again — it already happened twice this session (gpt-4o-mini -> nvidia/nemotron-3.5-lightning:free -> dots-studio/dots-3-note-preview:free). Decouple the tests from ambient config.toml entirely by adding generation_model to engine/src/main.rs's existing explicit env-override boundary and pinning both tests' spawned engine env to the model string their own mocks already expect — this leaves the 5 existing hardcoded mock assertions/responses untouched.

Out of scope by design: config/config.example.toml:78 still says `generation_model = "openai/gpt-4o-mini"`, which remains a legitimate, working example default — no change needed there.

Purpose: Make TestRAGQueryCrossRuntime and TestRAGQueryClientDisconnectCancelsRustWorkflow immune to this exact class of break on any future config.toml edit, matching the pattern already used for the OpenRouter endpoint overrides.
Output: An explicit LANCET_OPENROUTER__GENERATION_MODEL override in engine/src/main.rs, a focused unit test proving env beats config.toml for this field, and both gateway integration tests pinning their spawned engine's generation_model via env instead of depending on ambient config.toml.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-state-machine-workflow-events/05-UAT.md
@.planning/debug/g-05-1-engine-startup-blockers.md
@engine/src/main.rs
@gateway/main_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit LANCET_OPENROUTER__GENERATION_MODEL override</name>
  <files>engine/src/main.rs, engine/src/tests.rs</files>
  <behavior>
    - Test: setting LANCET_OPENROUTER__GENERATION_MODEL to a sentinel value before calling load_settings() results in settings.openrouter.generation_model equal to that sentinel (save/restore the var so the test doesn't leak env state, following the ENV_MUTEX-guarded pattern already used by config_workflow_nested_env_overrides_match_contract).
  </behavior>
  <action>
    In `load_settings()` (engine/src/main.rs, ~lines 646-670), add one more explicit override block immediately after the existing `LANCET_OPENROUTER__CHAT_ENDPOINT` block: read `LANCET_OPENROUTER__GENERATION_MODEL`, and when non-empty (trimmed) set `settings.openrouter.generation_model` to it. This is convention parity with the other boundary-critical overrides at that call site (deployment/process-test override names kept explicit and independent of the generic config crate source's version-specific parsing details) — the generic `config::Environment::with_prefix("LANCET").separator("__")` source already resolves this field today, so this is not itself a correctness fix; it is making the override contract guaranteed rather than incidental.

    In engine/src/tests.rs, add a new focused test modeled on the existing `config_workflow_nested_env_overrides_match_contract` test's ENV_MUTEX-guarded save/restore pattern (~line 311-352): lock ENV_MUTEX, save the current value of LANCET_OPENROUTER__GENERATION_MODEL, set it to a distinct sentinel string, call load_settings(), restore the original env state, then assert settings.openrouter.generation_model equals the sentinel. This proves "env overrides config.toml for this field" — the contract the gateway integration tests in Task 2 depend on — it does not by itself distinguish the new explicit block from the generic env source (both satisfy this assertion); the explicit block's purpose is guaranteed-override parity with its siblings, not something this test alone proves.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked openrouter_generation_model_env_override -- --exact</automated>
  </verify>
  <done>load_settings() honors LANCET_OPENROUTER__GENERATION_MODEL explicitly; the new test proves it; `cargo build --manifest-path engine/Cargo.toml --locked` compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the spawned engine's generation_model in both real-engine gateway tests</name>
  <files>gateway/main_test.go</files>
  <action>
    In `assertCleanRAGChildEnv`'s `allowed` map (~lines 2779-2786), add `"LANCET_OPENROUTER__GENERATION_MODEL": true` alongside the existing `LANCET_OPENROUTER__*` entries.

    In `TestRAGQueryCrossRuntime`'s `engineEnv := ragChildEnv(...)` call (~lines 2206-2213), add `"LANCET_OPENROUTER__GENERATION_MODEL=openai/gpt-4o-mini"` to the extra-vars list, matching the model string its own /api/v1/models and /api/v1/chat/completions mock handlers already hardcode (lines 2074, 2111, 2142, 2397). Do not change those 5 existing hardcoded mock assertions/responses — pinning the override to the value they already expect makes the test correct again without touching them.

    In `TestRAGQueryClientDisconnectCancelsRustWorkflow`'s `engineEnv := ragChildEnv(...)` call (~lines 3510-3517), add the same `"LANCET_OPENROUTER__GENERATION_MODEL=openai/gpt-4o-mini"` entry, matching its own separate /api/v1/models mock (line 3457).

    Both tests' spawned engine subprocess now resolves generation_model from this explicit env override regardless of what config/config.toml currently says, closing the coupling that broke both tests this session.
  </action>
  <verify>
    <automated>cd gateway && go test ./... -run "TestRAGQueryCrossRuntime|TestRAGQueryClientDisconnectCancelsRustWorkflow" -count=1</automated>
  </verify>
  <done>Both tests pass; the spawned engine subprocess's generation_model no longer depends on config/config.toml's ambient value (spot-checked by temporarily editing it and confirming both tests are unaffected).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Go test harness -> spawned engine subprocess env | `ragChildEnv(...)` constructs the full environment handed to the real `engine.exe`/`seed_rag_fixture.exe` child process; this is the only channel besides config.toml through which the test controls engine behavior. |
| config/config.toml -> engine process (production boundary, unaffected by this plan) | Ambient file the engine reads by default; this plan's override exists specifically so the two tests stop depending on it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.26-01 | Tampering | `assertCleanRAGChildEnv` allowlist / `ragChildEnv(...)` | low | mitigate | The new `LANCET_OPENROUTER__GENERATION_MODEL` entry is added to the explicit allowlist (same treatment as its sibling endpoint overrides) so `assertCleanRAGChildEnv` still fails the test if any *other*, unexpected `LANCET_*`/`OPENROUTER_*` variable leaks into the spawned child — the allowlist stays a closed set. |
| T-05.26-02 | Tampering | engine/src/main.rs's new override block | low | accept | Mirrors the existing empty-string-guarded pattern used by the other `LANCET_OPENROUTER__*` overrides; only a deliberately non-empty value takes effect, at the same trust level as config.toml itself (both are operator/process-launcher controlled, not attacker-controlled network input). |

</threat_model>

<verification>
- `cargo build --manifest-path engine/Cargo.toml --locked` compiles clean.
- `cargo test --manifest-path engine/Cargo.toml --locked` full suite passes (no regression).
- `go build ./...` and `go vet ./...` from gateway/ are clean.
- `go test ./... -run "TestRAGQueryCrossRuntime|TestRAGQueryClientDisconnectCancelsRustWorkflow" -count=1` passes.
- Full `go test ./...` (gateway) still exits 0.
</verification>

<success_criteria>
Blocker B (test-fixture staleness coupling gateway's real-engine integration tests to ambient config.toml's generation_model) is closed: both tests pass independent of config.toml's current value, and engine/src/main.rs's explicit override list stays the single guaranteed source of truth for this field going forward.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-26-SUMMARY.md` when done
</output>
