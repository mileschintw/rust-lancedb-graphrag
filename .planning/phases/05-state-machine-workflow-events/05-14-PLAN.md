---
phase: 05-state-machine-workflow-events
plan: 14
type: execute
wave: 10
depends_on:
  - 05-09
  - 05-13
files_modified:
  - engine/src/workflow/node.rs
  - engine/src/workflow/runner.rs
  - engine/src/workflow/nodes/reformulate.rs
  - engine/src/workflow/nodes/graph_context.rs
  - engine/src/workflow/nodes/retrieve.rs
  - engine/src/workflow/nodes/assemble_prompt.rs
  - engine/src/workflow/nodes/generate.rs
  - engine/src/tests/workflow_phase5_production.rs
autonomous: true
gap_closure: true
requirements:
  - ORCH-01
  - ORCH-02
  - ORCH-03
must_haves:
  truths:
    - "Every workflow node exposes exactly one typed NodeKind: ReformulateQuery, ExtractGraphContext, RetrieveHybrid, AssemblePrompt, or GenerateAnswer."
    - "Runner timeout selection, lifecycle naming, checkpoint naming, and answer-chunk decisions dispatch exhaustively from NodeKind rather than string comparisons or unknown-name fallbacks."
    - "Nine reformulation variants are rejected before ReformulateQuery completes and before downstream graph, retrieval, prompt, or generation adapters run."
    - "Typed NodeError retryability from 05-13 is consumed by the runner without inventing a second retrying event or changing the locked five-node order."
  artifacts:
    - path: "engine/src/workflow/node.rs"
      provides: "Closed NodeKind enum and Node trait contract"
    - path: "engine/src/workflow/nodes"
      provides: "Exact NodeKind implementations for all five production nodes"
    - path: "engine/src/workflow/runner.rs"
      provides: "Exhaustive typed dispatch and early variant admission"
    - path: "engine/src/tests/workflow_phase5_production.rs"
      provides: "Typed dispatch and nine-variant admission regression coverage"
  key_links:
    - "Node::kind -> runner timeout/lifecycle/checkpoint dispatch"
    - "ReformulateQuery result -> early maximum-variant admission -> no downstream adapter call"
    - "05-13 NodeError retryability -> NodeFailed.retryable without a retrying event"
---

<objective>
Replace stringly workflow dispatch with an exhaustive typed NodeKind contract while keeping the five-node state-machine path explicit.

Purpose: The scope checker found typed dispatch mixed with event/checkpoint delivery across eleven files. This additive plan owns the typed node contract and dispatch only; 05-10 owns event delivery and snapshots after this plan completes.
Output: Typed NodeKind values on all five nodes, exhaustive runner matches, early variant admission, and focused dispatch tests.
</objective>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-preconditions.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-reversibility.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-PATTERNS.md
@.planning/phases/05-state-machine-workflow-events/05-VERIFICATION.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEW.md
@.planning/phases/05-state-machine-workflow-events/05-13-PLAN.md
@AGENTS.md
@engine/src/workflow/node.rs
@engine/src/workflow/runner.rs
@engine/src/workflow/nodes/reformulate.rs
@engine/src/workflow/nodes/graph_context.rs
@engine/src/workflow/nodes/retrieve.rs
@engine/src/workflow/nodes/assemble_prompt.rs
@engine/src/workflow/nodes/generate.rs
@engine/src/tests/workflow_phase5_production.rs

## Artifacts this phase produces

- A closed `NodeKind` enum with the five exact public workflow node values.
- Node implementations and runner dispatch that use typed matches for timeouts and lifecycle metadata.
- Early nine-variant admission and typed retryability forwarding.
- Focused Rust tests proving the complete dispatch map and admission boundary.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Establish the typed NodeKind tracer for reformulation</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; 05-CONTEXT.md; 05-13-PLAN.md; engine/src/workflow/node.rs; engine/src/workflow/runner.rs; engine/src/workflow/nodes/reformulate.rs; engine/src/tests/workflow_phase5_production.rs</read_first>
  <files>engine/src/workflow/node.rs, engine/src/workflow/runner.rs, engine/src/workflow/nodes/reformulate.rs, engine/src/tests/workflow_phase5_production.rs</files>
  <action>Define the closed `NodeKind` enum with exactly ReformulateQuery, ExtractGraphContext, RetrieveHybrid, AssemblePrompt, and GenerateAnswer, add the typed `Node::kind` contract, and implement ReformulateQueryNode::kind. Route one reformulation lifecycle through the runner's typed timeout and label helpers in the tracer test `workflow_phase5_nodekind_tracer`; retain the existing coarse public event names and the D-06 order. Move the maximum-variant check to the successful ReformulateQuery boundary so a result with nine variants becomes a typed failure before NodeCompleted and before downstream adapter construction. Preserve ORCH-05 pass-through semantics, D-03 zero-evidence ownership, D-06 ordering, D-20 lifecycle granularity, and D-22 typed failure categories. Use the `05-PATTERNS.md` Node/runner analogs and leave event ownership to 05-10. Because 05-18 owns `engine/src/tests.rs` registration, this task verifies the NodeKind tracer body and both Rust targets compile; its exact registered `--bin engine` filter is deferred to 05-20 Task 2 after 05-18.</action>
  <acceptance_criteria>
    - NodeKind contains exactly the five locked node values and ReformulateQueryNode returns the matching value.
    - The tracer uses typed dispatch for reformulation timeout and lifecycle labels and registers exactly once.
    - Nine variants fail before ReformulateQuery completion and no downstream port is invoked.
  </acceptance_criteria>
  <verify>
    <automated>$production = Get-Content 'engine/src/tests/workflow_phase5_production.rs' -Raw; if (-not $production.Contains('workflow_phase5_nodekind_tracer')) { throw 'NodeKind tracer test body missing' }; $lib = cargo check --lib --manifest-path engine/Cargo.toml --locked; $libExit = $LASTEXITCODE; if ($libExit -ne 0) { exit $libExit }; $bin = cargo check --bin engine --manifest-path engine/Cargo.toml --locked; $binExit = $LASTEXITCODE; if ($binExit -ne 0) { exit $binExit }</automated>
  </verify>
  <done>The typed node contract exists and one real reformulation path proves early admission and typed dispatch.</done>
  <must_haves>
    - D-03, D-06, D-20, D-22, and ORCH-05 remain intact.
  </must_haves>
</task>

<task type="auto">
  <name>Task 2: Add typed graph and retrieval dispatch</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; 05-CONTEXT.md; engine/src/workflow/node.rs; engine/src/workflow/runner.rs; engine/src/workflow/nodes/graph_context.rs; engine/src/workflow/nodes/retrieve.rs; engine/src/tests/workflow_phase5_production.rs</read_first>
  <files>engine/src/workflow/runner.rs, engine/src/workflow/nodes/graph_context.rs, engine/src/workflow/nodes/retrieve.rs, engine/src/tests/workflow_phase5_production.rs</files>
<action>Implement exact NodeKind values for ExtractGraphContextNode and RetrieveHybridNode and replace their runner dispatch branches with typed matches for timeout selection, lifecycle labels, and checkpoint labels. Keep graph augmentation before hybrid retrieval per D-06, variant-zero embedding per D-08, all-variant retrieval per D-07, and graph degradation per D-09. Forward the typed NodeError retryability state that 05-13 exposes without adding a retrying event. Add `workflow_phase5_nodekind_dispatch` coverage for the first three values, their labels, and the nine-variant pre-completion failure; no string-name comparison or unknown-node fallback may satisfy the match. Because 05-18 owns `engine/src/tests.rs` registration, this task verifies the NodeKind dispatch body and both Rust targets compile; its exact registered `--bin engine` filter is deferred to 05-20 Task 2 after 05-18. Pattern reference: use the `engine/src/workflow/runner.rs`, `engine/src/workflow/nodes/graph_context.rs`, and `engine/src/workflow/nodes/retrieve.rs` sections of `05-PATTERNS.md` for timeout races, graph outcome handling, and dense/BM25 dispatch.</action>
  <acceptance_criteria>
    - ExtractGraphContext and RetrieveHybrid return their exact NodeKind values.
    - Runner dispatch for the first three nodes is exhaustive over typed values and preserves D-06/D-07/D-08/D-09 behavior.
    - The focused test proves typed retryability is available for NodeFailed construction without a retrying event.
  </acceptance_criteria>
  <verify>
    <automated>$production = Get-Content 'engine/src/tests/workflow_phase5_production.rs' -Raw; if (-not $production.Contains('workflow_phase5_nodekind_dispatch')) { throw 'NodeKind dispatch test body missing' }; $lib = cargo check --lib --manifest-path engine/Cargo.toml --locked; $libExit = $LASTEXITCODE; if ($libExit -ne 0) { exit $libExit }; $bin = cargo check --bin engine --manifest-path engine/Cargo.toml --locked; $binExit = $LASTEXITCODE; if ($binExit -ne 0) { exit $binExit }</automated>
  </verify>
  <done>Graph and retrieval nodes participate in typed dispatch with locked ordering, timeout, degradation, and retryability semantics.</done>
  <must_haves>
    - D-06, D-07, D-08, D-09, D-15, and D-22 are covered by typed node behavior.
  </must_haves>
</task>

<task type="auto">
  <name>Task 3: Complete exhaustive prompt and generation dispatch</name>
  <read_first>AGENTS.md; rust-guidelines.md; 05-PATTERNS.md; 05-CONTEXT.md; 05-13-PLAN.md; engine/src/workflow/node.rs; engine/src/workflow/runner.rs; engine/src/workflow/nodes/assemble_prompt.rs; engine/src/workflow/nodes/generate.rs; engine/src/tests/workflow_phase5_production.rs</read_first>
  <files>engine/src/workflow/runner.rs, engine/src/workflow/nodes/assemble_prompt.rs, engine/src/workflow/nodes/generate.rs, engine/src/tests/workflow_phase5_production.rs</files>
<action>Implement exact NodeKind values for AssemblePromptNode and GenerateAnswerNode, then finish every runner match over all five NodeKind variants for timeout selection, lifecycle/checkpoint labels, answer-chunk eligibility, zero-evidence skip, and typed failure mapping. Keep D-01 full validated answer chunks, D-02 distinct FinalAnswer, D-11/D-12/D-13/D-14 generation policy from 05-13, and D-15 no retrying event. Add `workflow_phase5_nodekind_exhaustive` to assert all five exact mappings and source-level absence of stringly fallback dispatch. Do not add workflow metadata or per-node spans per D-30 and D-31; 05-10 consumes this typed contract for event delivery and snapshots. Because 05-18 owns `engine/src/tests.rs` registration, this task verifies the exhaustive-dispatch body and both Rust targets compile; its exact registered `--bin engine` filter is deferred to 05-20 Task 2 after 05-18. Pattern reference: use the `engine/src/workflow/runner.rs` and `engine/src/main.rs query_rag handler (controller, streaming)` sections of `05-PATTERNS.md` for exhaustive runner decisions and preserved terminal/zero-evidence boundaries.</action>
  <acceptance_criteria>
    - All five node files return the matching NodeKind values.
    - Every runner match that controls timeout, label, checkpoint, answer chunk, or failure is exhaustive over exactly five variants.
    - The focused test proves no stringly fallback can dispatch an unknown node and preserves D-01/D-02 terminal semantics.
  </acceptance_criteria>
  <verify>
    <automated>$production = Get-Content 'engine/src/tests/workflow_phase5_production.rs' -Raw; if (-not $production.Contains('workflow_phase5_nodekind_exhaustive')) { throw 'NodeKind exhaustive test body missing' }; $lib = cargo check --lib --manifest-path engine/Cargo.toml --locked; $libExit = $LASTEXITCODE; if ($libExit -ne 0) { exit $libExit }; $bin = cargo check --bin engine --manifest-path engine/Cargo.toml --locked; $binExit = $LASTEXITCODE; if ($binExit -ne 0) { exit $binExit }</automated>
  </verify>
  <done>The runner has one exhaustive typed dispatch contract for all five nodes and the event/snapshot plan can consume it without string comparisons.</done>
  <must_haves>
    - ORCH-01, ORCH-02, ORCH-03, D-01, D-02, D-11, D-12, D-13, D-14, D-15, D-30, and D-31 remain covered.
  </must_haves>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Node implementation to runner dispatch | A missing or fallback match can skip timeout, event, or failure semantics. |
| Reformulation output to downstream adapters | Excess variants can amplify work and must be rejected before fan-out. |
| Typed error to event contract | Retryability must describe the actual provider/node state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-14-01 | Tampering | NodeKind dispatch | high | mitigate | Use a closed enum and exhaustive typed matches for every runner decision. |
| T-05-14-02 | Denial of service | Reformulation fan-out | high | mitigate | Reject nine variants before downstream adapter calls and completion. |
| T-05-14-03 | Tampering | NodeFailed.retryable | medium | mitigate | Forward typed NodeError retryability from 05-13; do not use a caller literal. |
| T-05-14-04 | Repudiation | Node lifecycle history | medium | mitigate | Preserve exact five-node labels and D-06 order in typed dispatch tests. |
</threat_model>

<verification>
- The three focused Cargo registration guards pass with one exact filter per test.
- All five NodeKind values and node implementations are present, runner matches are exhaustive, and nine-variant admission is early.
- No typed-dispatch task modifies event serialization or gateway behavior owned by 05-10/05-11.
</verification>

<success_criteria>
ORCH-01, ORCH-02, and ORCH-03 have an exhaustive typed five-node dispatch contract with early variant admission and typed retryability forwarding.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-14-SUMMARY.md` when done.
</output>
