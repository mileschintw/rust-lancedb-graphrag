---
phase: 05-state-machine-workflow-events
plan: 11
type: execute
wave: 19
depends_on:
  - 05-10
  - 05-13
  - 05-16
  - 05-19
  - 05-20
  - 05-17
files_modified:
  - engine/src/bin/seed_rag_fixture.rs
  - gateway/main.go
  - gateway/checkpoint_sink.go
  - gateway/main_test.go
autonomous: true
gap_closure: true
requirements:
  - ORCH-02
  - ORCH-03
  - ORCH-04
must_haves:
  truths:
    - "A real engine plus gateway request to /rag/query produces the five coarse node lifecycles, one full AnswerChunk, FinalAnswer, and successful WorkflowCompleted as raw SSE, with correct trace and session identity."
    - "NodeFailed.retryable is forwarded to SSE and transport errors never fabricate a successful terminal response or silently erase a diagnostic."
    - "Checkpoint events stay out of SSE response data while their complete snapshots are durably dispatched by Go-owned PostgreSQL persistence."
    - "DispatchPending has explicit envelope ownership and bounded retry/drain behavior; accepted records are not lost or reordered during backpressure or dispatcher shutdown."
    - "A canceled request tears down the gRPC stream and Rust workflow promptly, while detached checkpoint work remains governed by its own dispatcher lifetime."
    - "The production LanceDB fixture contains graph entities, entity edges, and 2048-dimensional name vectors, and the real provider request observes the seeded graph marker."
  artifacts:
    - path: "gateway/main.go"
      provides: "SSE event mapping, retryable forwarding, pending-dispatch handling, and stream error observability"
    - path: "gateway/checkpoint_sink.go"
      provides: "Context-aware checkpoint persistence and lossless accepted-envelope dispatcher drain"
    - path: "gateway/main_test.go"
      provides: "Real cross-runtime SSE, protobuf wire-field, checkpoint persistence, pending ownership, cancellation, and backpressure regressions"
    - path: "engine/src/bin/seed_rag_fixture.rs"
      provides: "Graph-bearing production fixture rows matching entities, entity_edges, and name-vector query shapes"
  key_links:
    - "Rust WorkflowEvent stream -> gateway writeWorkflowEventSSE -> ordered client SSE frames"
    - "05-13 provider/preflight classification -> 05-10 typed NodeFailed.retryable -> gateway SSE retryable field"
    - "CheckpointEvent -> CheckpointEnvelope -> explicit dispatcher ownership -> PostgreSQL workflow_checkpoints row"
    - "HTTP request context -> gRPC QueryRAG context -> Rust stream cancellation"
    - "seed_rag_fixture entities/entity_edges/name_vector rows -> production graph augmentation -> typed graph facts -> provider request marker"
    - "trace_id and sequence ordinal -> SSE identity and durable checkpoint ordering"
    - "05-17 generated RetrievalSnapshot fields -> gateway/main_test.go reflection/marshal wire fixture"
---

<objective>
Close the Go-side delivery and verification gaps so real Rust workflow events reach the client and full snapshots survive checkpoint backpressure.

Purpose: The Rust transport was built, but the authoritative runtime checks still fail because production did not emit all events and the gateway discarded DispatchPending. This plan proves the complete engine-to-SSE path, forwards retryability, and makes checkpoint ownership explicit without moving PostgreSQL into Rust.
Output: Gateway SSE hardening, dispatcher pending/close/context fixes, and regression coverage against real cross-runtime behavior and isolated PostgreSQL schemas.
</objective>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-preconditions.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-reversibility.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-AI-SPEC.md
@.planning/phases/05-state-machine-workflow-events/05-VALIDATION.md
@.planning/phases/05-state-machine-workflow-events/05-VERIFICATION.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEWS.md
@.planning/phases/05-state-machine-workflow-events/05-13-PLAN.md
@.planning/phases/05-state-machine-workflow-events/05-19-PLAN.md
@.planning/phases/05-state-machine-workflow-events/05-20-PLAN.md
@AGENTS.md
@go-guidelines.md
@gateway/go.mod
@gateway/main.go
@gateway/checkpoint_sink.go
@gateway/main_test.go
@gateway/db/schema.hcl
@gateway/db/schema.sql
@gateway/db/query.sql

## Artifacts this phase produces

- A real engine-plus-gateway SSE regression proving the post-gap event sequence and response identity.
- A graph-bearing production fixture that seeds matching `entities`, `entity_edges`, and 2048-dimensional `name_vector` rows used by the real graph augmentation path.
- A dispatcher that distinguishes Accepted from Pending ownership, drains accepted and retained envelopes on close in submission order, and honors the supplied persistence context.
- Isolated-schema PostgreSQL tests for full context snapshots, ordering, cancellation behavior, and non-stalling checkpoint backpressure.
- A separate real HTTP client-disconnect regression proving stalled generation cancellation and absence of later workflow events.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove and harden the real engine-to-gateway SSE stream</name>
  <read_first>AGENTS.md; go-guidelines.md; rust-guidelines.md; 05-PATTERNS.md; 05-17-PLAN.md; gateway/go.mod; gateway/main.go; gateway/main_test.go; gateway/checkpoint_sink.go; engine/src/bin/seed_rag_fixture.rs; engine/src/db/mod.rs; engine/src/main.rs; 05-CONTEXT.md; 05-AI-SPEC.md; 05-VALIDATION.md; 05-VERIFICATION.md; 05-REVIEWS.md; 05-13-PLAN.md</read_first>
  <files>engine/src/bin/seed_rag_fixture.rs, gateway/main.go, gateway/main_test.go</files>
  <action>Use the existing cross-runtime engine and gateway harness in the named `TestRAGQueryCrossRuntime` test rather than only a fake stream to exercise POST /rag/query against the production five-node Rust path. Extend `engine/src/bin/seed_rag_fixture.rs` before running that harness: open `entities_table()` and `entity_edges_table()`, then seed exactly the deterministic graph contract `GRAPH_SEED_ENTITY_ID = "00000000-0000-4000-8000-000000000101"`, `GRAPH_NEIGHBOR_ENTITY_ID = "00000000-0000-4000-8000-000000000102"`, and `GRAPH_EDGE_ID = "00000000-0000-4000-8000-000000000103"`. Because `validate_schema` compares complete Arrow fields, construct the entity batch with exactly `entity_id`, `name`, `entity_type`, `name_vector`, `summary`, `summary_vector`, `unsummarized_refs`, `community_ids`, and `source_chunk_ids`, and the edge batch with exactly `edge_id`, `source_node_id`, `target_node_id`, `relation_type`, `weight`, `document_id`, `summary`, and `summary_vector`; keep the nullable summary, summary_vector, unsummarized_refs, and community_ids values valid nulls. Give the two entity rows names `GRAPH_FIXTURE_MARKER_SEED` and `GRAPH_FIXTURE_MARKER_NEIGHBOR`, relation type `GRAPH_FIXTURE_MARKER_RELATION`, non-null UUID entity IDs, entity types, the exact 2048-dimensional `name_vector` `[1.0, followed by 2047 zeros]` matching the mock embedding `[1.0, followed by 2047 zeros]`, and `source_chunk_ids` referencing existing `CHUNKS[0].id` and `CHUNKS[1].id`. Seed the edge with `edge_id = GRAPH_EDGE_ID`, `source_node_id = GRAPH_SEED_ENTITY_ID`, `target_node_id = GRAPH_NEIGHBOR_ENTITY_ID`, `relation_type = GRAPH_FIXTURE_MARKER_RELATION`, `weight = 1.0`, and `document_id = DOCUMENT_ID`. Before the seeder exits, read back the entity and edge rows and fail if the IDs, endpoints, chunk references, vector dimension/first coordinate, relation, weight, or document linkage differ. Verify the nearest query using the same mock vector yields zero distance and `dense_score(0.0) = 1.0`, which is explicitly above `graph.seed_match_min_score = 0.5`; arbitrary non-matching vectors or non-UUID endpoint values do not satisfy this fixture. Use the stable graph marker in the entity names and relation so nearest-entity lookup, neighborhood fetch, and graph-fact rendering are proven from production rows rather than a fake provider. Keep D-19 SSE-only behavior and the existing first-frame/header ordering, then assert raw SSE frames contain node_started and node_completed for ReformulateQuery, ExtractGraphContext, RetrieveHybrid, AssemblePrompt, and GenerateAnswer, exactly one full AnswerChunk, a distinct FinalAnswer, and a successful WorkflowCompleted with trace_id equal to correlation_id per D-29. Require the running mock provider to observe `GRAPH_FIXTURE_MARKER_SEED`, `GRAPH_FIXTURE_MARKER_NEIGHBOR`, and `GRAPH_FIXTURE_MARKER_RELATION` in the actual outbound generation request and recorded chat evidence as well as the five-node SSE sequence, so the test discriminates production graph-to-model wiring from a library-only or fake stream. Extend the gateway mapping to forward the Rust NodeFailed.retryable field, retain typed safe failure categories, keep CheckpointEvent data out of SSE DTOs, and guard nil terminal responses before dereferencing them. Preserve D-04 malformed-request status before stream open, D-05 in-band workflow failures with normal HTTP 200 after headers, D-16 request-context cancellation, D-18 streaming, and D-20 coarse public-node lifecycle. Keep the endpoint without resume or reconnect behavior per D-21. After headers are written, convert an injected gRPC Recv error into one terminal stream_error frame with machine-readable GRPC_RECV_ERROR and flush it before returning; never invent workflow_completed success. On every post-open receive-error branch, check r.Context().Err() immediately before writing or flushing any error frame; when the HTTP client has disconnected, record expected cancellation and return without a stream_error write or flush. Apply the same request-context guard before each normal post-open event write so a disconnect cannot trigger a broken-pipe diagnostic after cancellation. Add an EOF watchdog: if the stream closes after HTTP 200 without either workflow_completed or stream_error, emit and flush one terminal stream_error frame with STREAM_EOF_WITHOUT_TERMINAL. Add raw-SSE tests named TestRAGQueryPostOpenRecvFailureSSE and TestRAGQueryEOFWithoutTerminalSSE that assert status 200, text/event-stream, the exact terminal frame/code, and no success terminal. Add the named real-client cancellation test TestRAGQueryClientDisconnectCancelsRustWorkflow: wrap the gateway handler in httptest.NewServer, use an http.Client to POST /rag/query, read raw SSE until GenerateAnswer node_started, close the response body mid-stream while the provider endpoint is stalled, wait for the mock provider request context to observe cancellation, and use a test-only server/event recorder to assert no later node event, final answer, workflow-completed, or stream_error event is written after disconnect and no second paid provider call occurs. This test is the authoritative cross-runtime proof of HTTP client disconnect -> gateway request context -> gRPC cancellation -> Rust CancellationToken propagation and in-flight provider termination; it must not be replaced by httptest.NewRequest, an in-process Rust runner, or a fake stream. Add TestRetrievalSnapshotWireContract in gateway/main_test.go using generated protobuf reflection/marshal to assert RetrievalSnapshot fields 10 and 11, preserved fields 1 through 9, ordered values, and round-trip fidelity; this Go fixture consumes the generated contract from 05-17 and does not populate production retrieval state. Follow the 05-PATTERNS.md assignments explicitly: mirror grpcEngine.Ingest's stream/receive adapter shape, the queryRAG pre-stream decode boundary, and the parseSSEEvents/parseTerminalResponseDTO helpers in gateway/main_test.go; use the named TestRAGQueryCrossRuntime harness as the production-controller regression analog. The cross-runtime tests must build/launch the Rust engine, issue real HTTP client requests, parse raw SSE, and fail if they only observe a library or fake stream.</action>
  <acceptance_criteria>
    - The real cross-runtime test observes the exact five-node lifecycle, one AnswerChunk, FinalAnswer, and WorkflowCompleted in order from raw SSE.
    - NodeFailed SSE data includes the retryable value derived by Rust, and transport/session identity fields remain correct.
    - No checkpoint snapshot or internal checkpoint field is present in any SSE response DTO.
    - Malformed requests fail before any SSE headers, while in-band workflow failures remain HTTP 200 after headers and close normally after their terminal event.
    - Nil or missing terminal response data cannot panic the gateway, and post-open receive errors produce one terminal stream_error SSE frame with GRPC_RECV_ERROR rather than a fabricated success.
    - An EOF after HTTP 200 without workflow_completed produces one terminal stream_error SSE frame with STREAM_EOF_WITHOUT_TERMINAL, so an incomplete stream cannot look successful.
    - `seed_rag_fixture.rs` uses the exact UUIDs `00000000-0000-4000-8000-000000000101`, `00000000-0000-4000-8000-000000000102`, and `00000000-0000-4000-8000-000000000103`; matching edge endpoints; existing `CHUNKS[0].id`/`CHUNKS[1].id`; identical 2048-dimensional `[1.0, 0.0, ...]` vectors; `weight = 1.0`; and the graph marker, with read-back assertions proving the linkage and a computed seed score of at least 0.5.
    - `TestRAGQueryCrossRuntime` observes `GRAPH_FIXTURE_MARKER_SEED`, `GRAPH_FIXTURE_MARKER_NEIGHBOR`, and `GRAPH_FIXTURE_MARKER_RELATION` in the real provider request, proving the exact seeded graph path rather than only field-name presence.
    - `TestRAGQueryClientDisconnectCancelsRustWorkflow` uses `httptest.NewServer` and `http.Client`, closes the live response during a stalled GenerateAnswer, observes provider-context cancellation, records no later node/terminal/stream_error events, and proves no additional paid provider call occurs; the handler checks `r.Context().Err()` before any post-open error write or flush.
    - `TestRetrievalSnapshotWireContract` proves generated RetrievalSnapshot fields 10 and 11 and their ordered round-trip values in the Go runtime.
    - Every gateway integration fixture that claims, leases, dequeues, or batch-selects mutable rows creates and uses a unique per-test schema before any query; every before/after snapshot, count, or row-query error calls `t.Fatalf` and cannot be converted into an empty comparison.
  </acceptance_criteria>
  <verify>
    <automated>$build = cargo build --bin engine --manifest-path engine/Cargo.toml --locked; $buildExit = $LASTEXITCODE; if ($buildExit -ne 0) { exit $buildExit }</automated>
    <automated>$source = Get-Content 'gateway/main_test.go' -Raw; $seeder = Get-Content 'engine/src/bin/seed_rag_fixture.rs' -Raw; $body = [regex]::Match($source, '(?s)func\s+TestRAGQueryCrossRuntime\b.*?(?=\nfunc\s|\z)').Value; if ([string]::IsNullOrWhiteSpace($body)) { throw 'cross-runtime test function body missing' }; foreach ($needle in @('enginePath','LANCET_OPENROUTER__CHAT_ENDPOINT','exec.Command','/rag/query','GRAPH_FIXTURE_MARKER_SEED','parseSSEEvents')) { if (-not $body.Contains($needle)) { throw "function-scoped production harness guard missing $needle" } }; foreach ($needle in @('entities_table','entity_edges_table','entity_id','name','entity_type','name_vector','summary','summary_vector','unsummarized_refs','community_ids','source_chunk_ids','edge_id','source_node_id','target_node_id','relation_type','weight','document_id','GRAPH_FIXTURE_MARKER')) { if (-not $seeder.Contains($needle)) { throw "graph fixture seeder guard missing $needle" } }</automated>
    <automated>$seeder = Get-Content 'engine/src/bin/seed_rag_fixture.rs' -Raw; foreach ($expected in @('GRAPH_SEED_ENTITY_ID = "00000000-0000-4000-8000-000000000101"','GRAPH_NEIGHBOR_ENTITY_ID = "00000000-0000-4000-8000-000000000102"','GRAPH_EDGE_ID = "00000000-0000-4000-8000-000000000103"','GRAPH_FIXTURE_MARKER_SEED','GRAPH_FIXTURE_MARKER_NEIGHBOR','GRAPH_FIXTURE_MARKER_RELATION','CHUNKS[0].id','CHUNKS[1].id','FixedSizeListArray','2048','dense_score','0.5')) { if (-not $seeder.Contains($expected)) { throw "exact graph fixture contract missing $expected" } }; $source = Get-Content 'gateway/main_test.go' -Raw; $body = [regex]::Match($source, '(?s)func\s+TestRAGQueryCrossRuntime\b.*?(?=\nfunc\s|\z)').Value; foreach ($expected in @('vector[0] = 1','make([]float32, 2048)','GRAPH_FIXTURE_MARKER_SEED','GRAPH_FIXTURE_MARKER_NEIGHBOR','GRAPH_FIXTURE_MARKER_RELATION')) { if (-not $body.Contains($expected)) { throw "exact cross-runtime graph/provider assertion missing $expected" } }</automated>
    <automated>$test = 'TestRAGQueryCrossRuntime'; $source = Get-Content 'gateway/main_test.go' -Raw; foreach ($needle in @('func ' + $test, 'exec.Command', 'httptest.NewServer', 'http.Client', 'Close()', '"/rag/query"', 'parseSSEEvents', 'parseTerminalResponseDTO', 'node_started', 'node_completed', 'answer_chunk', 'final_answer', 'workflow_completed', 'ReformulateQuery', 'ExtractGraphContext', 'RetrieveHybrid', 'AssemblePrompt', 'GenerateAnswer', 'stream_error')) { if (-not $source.Contains($needle)) { throw "real cross-runtime SSE proof guard missing $needle" } }; $listed = go -C gateway test -list '^TestRAGQueryCrossRuntime$'; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -eq $test }); if ($registered.Count -ne 1) { throw "exact Go registration missing or duplicated for $test" } ; $output = go -C gateway test -run '^TestRAGQueryCrossRuntime$' -count=1; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
    <automated>$test = 'TestRAGQueryPostOpenRecvFailureSSE'; $source = Get-Content 'gateway/main_test.go' -Raw; foreach ($needle in @('func ' + $test, 'stream_error', 'GRPC_RECV_ERROR', 'text/event-stream', 'StatusOK')) { if (-not $source.Contains($needle)) { throw "post-open Recv SSE proof guard missing $needle" } }; $listed = go -C gateway test -list '^TestRAGQueryPostOpenRecvFailureSSE$'; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -eq $test }); if ($registered.Count -ne 1) { throw "exact Go registration missing or duplicated for $test" }; $output = go -C gateway test -run '^TestRAGQueryPostOpenRecvFailureSSE$' -count=1; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
    <automated>$test = 'TestRAGQueryEOFWithoutTerminalSSE'; $source = Get-Content 'gateway/main_test.go' -Raw; foreach ($needle in @('func ' + $test, 'stream_error', 'STREAM_EOF_WITHOUT_TERMINAL', 'text/event-stream', 'StatusOK')) { if (-not $source.Contains($needle)) { throw "EOF-without-terminal SSE proof guard missing $needle" } }; $listed = go -C gateway test -list '^TestRAGQueryEOFWithoutTerminalSSE$'; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -eq $test }); if ($registered.Count -ne 1) { throw "exact Go registration missing or duplicated for $test" }; $output = go -C gateway test -run '^TestRAGQueryEOFWithoutTerminalSSE$' -count=1; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
    <automated>$test = 'TestRAGQueryClientDisconnectCancelsRustWorkflow'; $source = Get-Content 'gateway/main_test.go' -Raw; foreach ($needle in @('func ' + $test, 'httptest.NewServer', 'http.Client', 'Close()', 'GenerateAnswer', 'Context().Done', 'node_started', 'workflow_completed')) { if (-not $source.Contains($needle)) { throw "real client-disconnect proof guard missing $needle" } }; $listed = go -C gateway test -list '^TestRAGQueryClientDisconnectCancelsRustWorkflow$'; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -eq $test }); if ($registered.Count -ne 1) { throw "exact Go registration missing or duplicated for $test" }; $output = go -C gateway test -run '^TestRAGQueryClientDisconnectCancelsRustWorkflow$' -count=1; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
    <automated>$query = [regex]::Match((Get-Content 'gateway/main.go' -Raw), '(?s)func \(a app\) queryRAG\b.*?(?=\nfunc )').Value; if ($query -notmatch 'r\.Context\(\)\.Err\(\)') { throw 'queryRAG has no explicit request-cancellation guard before post-open writes' }; $test = 'TestRetrievalSnapshotWireContract'; $source = Get-Content 'gateway/main_test.go' -Raw; foreach ($needle in @('func ' + $test, 'VariantCount', 'VariantIdentities', 'proto.Marshal', 'proto.Unmarshal')) { if (-not $source.Contains($needle)) { throw "RetrievalSnapshot Go wire-contract guard missing $needle" } }; $listed = go -C gateway test -list '^TestRetrievalSnapshotWireContract$'; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -eq $test }); if ($registered.Count -ne 1) { throw "exact Go registration missing or duplicated for $test" }; $output = go -C gateway test -run '^TestRetrievalSnapshotWireContract$' -count=1; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
    <automated>cargo build --bin seed_rag_fixture --manifest-path engine/Cargo.toml --locked; if ($LASTEXITCODE -ne 0) { throw 'graph-bearing production fixture seeder does not compile' }</automated>
  </verify>
  <done>The gateway demonstrably relays the production Rust workflow to a correct SSE client contract and exposes retryability and transport failures without leaking checkpoint data.</done>
  <must_haves>
    - D-01, D-02, D-04, D-05, D-16, D-18, D-19, D-20, D-21, D-22, and D-29 are asserted at the client boundary.
    - Checkpoint JSON is never serialized into the SSE response.
  </must_haves>
  <reversibility rating="costly">SSE field and error handling changes are client-visible; preserve existing event names and headers while adding only the required retryability and observability behavior.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Preserve checkpoint ownership across pending backpressure and shutdown</name>
  <precondition>For PostgreSQL integration assertions, TEST_DATABASE_URL must point to a reachable local test database; each test creates and uses its own schema before issuing workflow checkpoint queries.</precondition>
  <read_first>AGENTS.md; go-guidelines.md; 05-PATTERNS.md; gateway/go.mod; gateway/checkpoint_sink.go; gateway/main.go; gateway/main_test.go; gateway/db/schema.hcl; gateway/db/schema.sql; gateway/db/query.sql; 05-CONTEXT.md; 05-RESEARCH.md; 05-VERIFICATION.md; 05-REVIEWS.md</read_first>
  <files>gateway/main.go, gateway/checkpoint_sink.go, gateway/main_test.go</files>
  <action>Make CheckpointDispatcher ownership explicit: Submit may return Accepted only after the dispatcher owns the envelope, while DispatchPending remains an owned envelope that writeWorkflowEventSSE must transfer into a bounded pending queue or report as an explicit dispatch failure. Never ignore the DispatchResult. Ensure the dispatcher drains accepted primary, overflow, and pending queues in the original submission order after the input channel closes; specifically, nextEnvelope must drain the primary FIFO before any overflow FIFO so an overflow item cannot overtake an earlier primary item, and Close cannot finish while an accepted envelope remains undrained. Add or extend the dedicated `TestWorkflowCheckpointPendingDrainAndPersistence` test to record sink receive order by trace/sequence ordinal before any SQL ORDER BY, then assert the persisted rows retain that order, while exercising the pending path, dispatcher close, full snapshot persistence, and context cancellation. Make PostgresCheckpointSink.SaveCheckpoint honor its supplied context; the detached dispatcher uses its own lifecycle context so checkpoint persistence is not coupled to the request stream, while a directly canceled sink call is canceled. Keep D-23 and D-26 Go-owned PostgreSQL persistence, D-27 nonblocking checkpoint handoff, D-28 full accumulated context with lossless evidence/prompt/answer/retrieval fields and the fixed-size query-embedding digest defined by 05-10, D-24 indefinite retention without cleanup, and D-25 no fetch API. Follow the `05-PATTERNS.md` assignments explicitly: use the `gateway/db/schema.hcl` table conventions, `gateway/db/query.sql` single-row insert convention, `PostgresCheckpointSink.SaveCheckpoint` context boundary, and the isolated Go integration-test pattern. Update all mutable-row integration fixtures to use a unique per-test schema and make every before/after count or row query fatal on error per AGENTS.md.</action>
  <acceptance_criteria>
    - The sixth and later submissions return explicit Pending ownership, and the gateway either retains each returned envelope in the dispatcher pending queue or reports an explicit failure; no result is discarded.
    - Close drains every Accepted and retained Pending envelope exactly once, including the race where the primary channel closes while overflow is non-empty, and the recorder observes submission order with primary-before-overflow precedence.
    - Direct SaveCheckpoint calls honor cancellation, while detached dispatcher persistence uses a separate dispatcher lifetime and does not stall SSE writes.
    - Isolated PostgreSQL tests persist ordered trace and sequence rows with every D-28 context key for retrieval, graph, evidence, prompt, answer, citations, notices, and snapshot state, plus the fixed-size query-embedding digest, without loss or truncation.
    - Backpressure tests complete within the bounded latency budget, prove FinalAnswer delivery is not held behind database persistence, and report explicit ownership when a bounded pending queue cannot accept an envelope.
  </acceptance_criteria>
  <verify>
    <automated>if (-not $env:TEST_DATABASE_URL) { throw 'TEST_DATABASE_URL is required for the checkpoint persistence proof' }; $test = 'TestWorkflowCheckpointPendingDrainAndPersistence'; $listed = go -C gateway test -list '^TestWorkflowCheckpointPendingDrainAndPersistence$'; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -eq $test }); if ($registered.Count -ne 1) { throw "exact Go registration missing or duplicated for $test" }; $output = go -C gateway test -run '^TestWorkflowCheckpointPendingDrainAndPersistence$' -count=1; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
  </verify>
  <done>Gateway checkpoint dispatch has explicit pending ownership, lossless accepted-envelope shutdown, context-correct persistence, and isolated regression coverage for the complete snapshot contract.</done>
  <must_haves>
    - D-23, D-24, D-25, D-26, D-27, D-28, and D-29 remain visible in implementation and tests.
    - The phase does not add PostgreSQL access to Rust, a checkpoint fetch API, or a retention cleanup job.
    - Every global checkpoint claim or query in tests uses its per-test schema and treats query errors as fatal.
  </must_haves>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust gRPC stream to Go gateway | Untrusted or failed upstream frames cross into SSE mapping and must retain typed identity. |
| Go gateway to browser/client | SSE payloads are client-visible and must not include checkpoint snapshots or provider secrets. |
| Go dispatcher to PostgreSQL | Checkpoint envelopes cross into parameterized persistence under bounded asynchronous backpressure. |
| HTTP request lifetime to detached dispatcher lifetime | Client cancellation must stop query work without silently discarding already-owned checkpoint records. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-11-01 | Information disclosure | SSE response mapping | high | mitigate | Keep checkpoint payloads out of SSE DTOs, guard nil responses, and log transport errors without emitting raw provider details. |
| T-05-11-02 | Tampering | Checkpoint envelope identity | high | mitigate | Preserve trace, session, node, and one-source sequence ordinals through envelope creation and parameterized insert. |
| T-05-11-03 | Denial of service | Dispatcher backpressure | high | mitigate | Use bounded pending ownership, nonblocking handoff, explicit failure, and deterministic drain on Close. |
| T-05-11-04 | Spoofing | Request and gRPC correlation | medium | mitigate | Forward validated session and correlation identity and assert it in raw SSE and database tests. |
| T-05-11-05 | Information disclosure | Raw checkpoint query content | medium | accept | D-23 explicitly accepts local raw-content checkpoint storage for this phase; access remains behind the Go-owned database boundary and no client fetch API is added. |
| T-05-11-06 | Repudiation | Workflow checkpoint history | medium | mitigate | Persist full snapshots and ordered rows, use isolated schemas in tests, and fail query errors rather than treating missing evidence as success. |
</threat_model>

<verification>
- The Task 1 `go -C gateway` registration guard runs the real `TestRAGQueryCrossRuntime` proof with a fatal list/test exit check.
- The Task 1 raw-SSE registration guards run `TestRAGQueryPostOpenRecvFailureSSE` and `TestRAGQueryEOFWithoutTerminalSSE`; each proves HTTP 200 after headers, one terminal `stream_error`, its exact machine-readable code, and no fabricated success terminal.
- The Task 1 cancellation registration guard runs `TestRAGQueryClientDisconnectCancelsRustWorkflow` with a live `httptest.NewServer`/`http.Client` response close, provider-context cancellation, no later node/answer/workflow-completed/stream-error event, and no second provider call.
- The Task 1 wire registration guard runs `TestRetrievalSnapshotWireContract` and proves fields 10 and 11 plus fields 1 through 9 survive generated Go reflection and marshal round-trip.
- The Task 2 `go -C gateway` registration guard runs `TestWorkflowCheckpointPendingDrainAndPersistence` with `TEST_DATABASE_URL` required and fatal list/test exit checks.
- A real engine plus gateway run produces the required five-node SSE sequence and terminal events.
- Dispatcher backpressure and close tests prove accepted and retained pending envelopes are not silently lost.
- PostgreSQL rows contain full context snapshots with trace and sequence fidelity.
</verification>

<success_criteria>
ORCH-02, ORCH-03, and ORCH-04 are observable end-to-end: Rust events reach the client, disconnects terminate work, and Go-owned full snapshots persist reliably under bounded backpressure.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-11-SUMMARY.md` when done.
</output>
