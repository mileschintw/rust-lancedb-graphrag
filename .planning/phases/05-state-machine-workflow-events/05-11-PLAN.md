---
phase: 05-state-machine-workflow-events
plan: 11
type: execute
wave: 11
depends_on:
  - 05-10
  - 05-13
files_modified:
  - gateway/main.go
  - gateway/checkpoint_sink.go
  - gateway/main_test.go
autonomous: true
gap_closure: true
requirements:
  - ORCH-02
  - ORCH-03
  - ORCH-04
must_haves:
  truths:
    - "A real engine plus gateway request to /rag/query produces the five coarse node lifecycles, one full AnswerChunk, FinalAnswer, and successful WorkflowCompleted as raw SSE, with correct trace and session identity."
    - "NodeFailed.retryable is forwarded to SSE and transport errors never fabricate a successful terminal response or silently erase a diagnostic."
    - "Checkpoint events stay out of SSE response data while their complete snapshots are durably dispatched by Go-owned PostgreSQL persistence."
    - "DispatchPending has explicit envelope ownership and bounded retry/drain behavior; accepted records are not lost during backpressure or dispatcher shutdown."
    - "A canceled request tears down the gRPC stream and Rust workflow promptly, while detached checkpoint work remains governed by its own dispatcher lifetime."
  artifacts:
    - path: "gateway/main.go"
      provides: "SSE event mapping, retryable forwarding, pending-dispatch handling, and stream error observability"
    - path: "gateway/checkpoint_sink.go"
      provides: "Context-aware checkpoint persistence and lossless accepted-envelope dispatcher drain"
    - path: "gateway/main_test.go"
      provides: "Real cross-runtime SSE, checkpoint persistence, pending ownership, cancellation, and backpressure regressions"
  key_links:
    - "Rust WorkflowEvent stream -> gateway writeWorkflowEventSSE -> ordered client SSE frames"
    - "05-13 provider/preflight classification -> 05-10 typed NodeFailed.retryable -> gateway SSE retryable field"
    - "CheckpointEvent -> CheckpointEnvelope -> explicit dispatcher ownership -> PostgreSQL workflow_checkpoints row"
    - "HTTP request context -> gRPC QueryRAG context -> Rust stream cancellation"
    - "trace_id and sequence ordinal -> SSE identity and durable checkpoint ordering"
---

<objective>
Close the Go-side delivery and verification gaps so real Rust workflow events reach the client and full snapshots survive checkpoint backpressure.

Purpose: The Rust transport was built, but the authoritative runtime checks still fail because production did not emit all events and the gateway discarded DispatchPending. This plan proves the complete engine-to-SSE path, forwards retryability, and makes checkpoint ownership explicit without moving PostgreSQL into Rust.
Output: Gateway SSE hardening, dispatcher pending/close/context fixes, and regression coverage against real cross-runtime behavior and isolated PostgreSQL schemas.
</objective>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-preconditions.md
@D:/Repos/lancet/.codex/gsd-core/references/planner-reversibility.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-AI-SPEC.md
@.planning/phases/05-state-machine-workflow-events/05-VALIDATION.md
@.planning/phases/05-state-machine-workflow-events/05-VERIFICATION.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEW.md
@.planning/phases/05-state-machine-workflow-events/05-13-PLAN.md
@AGENTS.md
@go-guidelines.md
@gateway/go.mod
@gateway/main.go
@gateway/checkpoint_sink.go
@gateway/main_test.go
@gateway/db/schema.hcl
@gateway/db/schema.sql
@gateway/db/query.sql

## Artifacts this phase produces

- A real engine-plus-gateway SSE regression proving the post-gap event sequence and response identity.
- A dispatcher that distinguishes Accepted from Pending ownership, drains accepted and retained envelopes on close, and honors the supplied persistence context.
- Isolated-schema PostgreSQL tests for full context snapshots, ordering, cancellation behavior, and non-stalling checkpoint backpressure.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove and harden the real engine-to-gateway SSE stream</name>
  <read_first>AGENTS.md; go-guidelines.md; 05-PATTERNS.md; gateway/go.mod; gateway/main.go; gateway/main_test.go; gateway/checkpoint_sink.go; 05-CONTEXT.md; 05-AI-SPEC.md; 05-VALIDATION.md; 05-VERIFICATION.md; 05-REVIEW.md; 05-13-PLAN.md</read_first>
  <files>gateway/main.go, gateway/main_test.go</files>
  <action>Use the existing cross-runtime engine and gateway harness in the named `TestRAGQueryCrossRuntime` test rather than only a fake stream to exercise POST /rag/query against the production five-node Rust path. Keep D-19 SSE-only behavior and the existing first-frame/header ordering, then assert raw SSE frames contain node_started and node_completed for ReformulateQuery, ExtractGraphContext, RetrieveHybrid, AssemblePrompt, and GenerateAnswer, exactly one full AnswerChunk, a distinct FinalAnswer, and a successful WorkflowCompleted with trace_id equal to correlation_id per D-29. Extend the gateway mapping to forward the Rust NodeFailed.retryable field, retain typed safe failure categories, keep CheckpointEvent data out of SSE DTOs, and guard nil terminal responses before dereferencing them. Preserve D-04 malformed-request status before stream open, D-05 in-band workflow failures with normal HTTP 200 after headers, D-16 request-context cancellation, D-18 streaming, and D-20 coarse public-node lifecycle. Keep the endpoint without resume or reconnect behavior per D-21. Make post-open gRPC receive errors observable through structured logging and a failed handler outcome without inventing a successful terminal frame. Follow the `05-PATTERNS.md` assignments explicitly: mirror `grpcEngine.Ingest`'s stream/receive adapter shape, the `queryRAG` pre-stream decode boundary, and the `parseSSEEvents`/`parseTerminalResponseDTO` helpers in `gateway/main_test.go`; use the named `TestRAGQueryCrossRuntime` harness as the production-controller regression analog. The test must build/launch the Rust engine, issue the HTTP request, parse raw SSE, and fail if it only observes a library or fake stream.</action>
  <acceptance_criteria>
    - The real cross-runtime test observes the exact five-node lifecycle, one AnswerChunk, FinalAnswer, and WorkflowCompleted in order from raw SSE.
    - NodeFailed SSE data includes the retryable value derived by Rust, and transport/session identity fields remain correct.
    - No checkpoint snapshot or internal checkpoint field is present in any SSE response DTO.
    - Malformed requests fail before any SSE headers, while in-band workflow failures remain HTTP 200 after headers and close normally after their terminal event.
    - Nil or missing terminal response data cannot panic the gateway, and post-open receive errors are logged without a fabricated success.
  </acceptance_criteria>
  <verify>
    <automated>$test = 'TestRAGQueryCrossRuntime'; $source = Get-Content 'gateway/main_test.go' -Raw; foreach ($needle in @('func ' + $test, 'exec.Command', 'httptest.NewRequest', '"/rag/query"', 'parseSSEEvents', 'parseTerminalResponseDTO', 'node_started', 'node_completed', 'answer_chunk', 'final_answer', 'workflow_completed', 'ReformulateQuery', 'ExtractGraphContext', 'RetrieveHybrid', 'AssemblePrompt', 'GenerateAnswer')) { if (-not $source.Contains($needle)) { throw "real cross-runtime SSE proof guard missing $needle" } }; $listed = go -C gateway test -list '^TestRAGQueryCrossRuntime$'; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -eq $test }); if ($registered.Count -ne 1) { throw "exact Go registration missing or duplicated for $test" }; go -C gateway test -run '^TestRAGQueryCrossRuntime$' -count=1; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
  </verify>
  <done>The gateway demonstrably relays the production Rust workflow to a correct SSE client contract and exposes retryability and transport failures without leaking checkpoint data.</done>
  <must_haves>
    - D-01, D-02, D-04, D-05, D-16, D-18, D-19, D-20, D-21, D-22, and D-29 are asserted at the client boundary.
    - Checkpoint JSON is never serialized into the SSE response.
  </must_haves>
  <reversibility rating="costly">SSE field and error handling changes are client-visible; preserve existing event names and headers while adding only the required retryability and observability behavior.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Preserve checkpoint ownership across pending backpressure and shutdown</name>
  <precondition>For PostgreSQL integration assertions, TEST_DATABASE_URL must point to a reachable local test database; each test creates and uses its own schema before issuing workflow checkpoint queries.</precondition>
  <read_first>AGENTS.md; go-guidelines.md; 05-PATTERNS.md; gateway/go.mod; gateway/checkpoint_sink.go; gateway/main.go; gateway/main_test.go; gateway/db/schema.hcl; gateway/db/schema.sql; gateway/db/query.sql; 05-CONTEXT.md; 05-RESEARCH.md; 05-VERIFICATION.md; 05-REVIEW.md</read_first>
  <files>gateway/main.go, gateway/checkpoint_sink.go, gateway/main_test.go</files>
  <action>Make CheckpointDispatcher ownership explicit: Submit may return Accepted only after the dispatcher owns the envelope, while DispatchPending remains an owned envelope that writeWorkflowEventSSE must transfer into a bounded pending queue or report as an explicit dispatch failure. Never ignore the DispatchResult. Ensure the dispatcher drains primary, overflow, and pending queues in FIFO order after the input channel closes, and that Close cannot finish while an accepted envelope remains undrained. Add or extend the dedicated `TestWorkflowCheckpointPendingDrainAndPersistence` test to exercise the pending path, dispatcher close, full snapshot persistence, and context cancellation. Make PostgresCheckpointSink.SaveCheckpoint honor its supplied context; the detached dispatcher uses its own lifecycle context so checkpoint persistence is not coupled to the request stream, while a directly canceled sink call is canceled. Keep D-23 and D-26 Go-owned PostgreSQL persistence, D-27 nonblocking checkpoint handoff, D-28 full accumulated context, D-24 indefinite retention without cleanup, and D-25 no fetch API. Follow the `05-PATTERNS.md` assignments explicitly: use the `gateway/db/schema.hcl` table conventions, `gateway/db/query.sql` single-row insert convention, `PostgresCheckpointSink.SaveCheckpoint` context boundary, and the isolated Go integration-test pattern. Update all mutable-row integration fixtures to use a unique per-test schema and make every before/after count or row query fatal on error per AGENTS.md.</action>
  <acceptance_criteria>
    - The sixth and later submissions return explicit Pending ownership, and the gateway either retains each returned envelope in the dispatcher pending queue or reports an explicit failure; no result is discarded.
    - Close drains every Accepted and retained Pending envelope exactly once, including the race where the primary channel closes while overflow is non-empty.
    - Direct SaveCheckpoint calls honor cancellation, while detached dispatcher persistence uses a separate dispatcher lifetime and does not stall SSE writes.
    - Isolated PostgreSQL tests persist ordered trace and sequence rows with full context snapshot keys for retrieval, graph, evidence, prompt, answer, citations, notices, and snapshot state.
    - Backpressure tests complete within the bounded latency budget and prove FinalAnswer delivery is not held behind database persistence.
  </acceptance_criteria>
  <verify>
    <automated>if (-not $env:TEST_DATABASE_URL) { throw 'TEST_DATABASE_URL is required for the checkpoint persistence proof' }; $test = 'TestWorkflowCheckpointPendingDrainAndPersistence'; $listed = go -C gateway test -list '^TestWorkflowCheckpointPendingDrainAndPersistence$'; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; $registered = @($listed | ForEach-Object { $_.ToString().Trim() } | Where-Object { $_ -eq $test }); if ($registered.Count -ne 1) { throw "exact Go registration missing or duplicated for $test" }; go -C gateway test -run '^TestWorkflowCheckpointPendingDrainAndPersistence$' -count=1; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }</automated>
  </verify>
  <done>Gateway checkpoint dispatch has explicit pending ownership, lossless accepted-envelope shutdown, context-correct persistence, and isolated regression coverage for the complete snapshot contract.</done>
  <must_haves>
    - D-23, D-24, D-25, D-26, D-27, D-28, and D-29 remain visible in implementation and tests.
    - The phase does not add PostgreSQL access to Rust, a checkpoint fetch API, or a retention cleanup job.
    - Every global checkpoint claim or query in tests uses its per-test schema and treats query errors as fatal.
  </must_haves>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust gRPC stream to Go gateway | Untrusted or failed upstream frames cross into SSE mapping and must retain typed identity. |
| Go gateway to browser/client | SSE payloads are client-visible and must not include checkpoint snapshots or provider secrets. |
| Go dispatcher to PostgreSQL | Checkpoint envelopes cross into parameterized persistence under bounded asynchronous backpressure. |
| HTTP request lifetime to detached dispatcher lifetime | Client cancellation must stop query work without silently discarding already-owned checkpoint records. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-11-01 | Information disclosure | SSE response mapping | high | mitigate | Keep checkpoint payloads out of SSE DTOs, guard nil responses, and log transport errors without emitting raw provider details. |
| T-05-11-02 | Tampering | Checkpoint envelope identity | high | mitigate | Preserve trace, session, node, and one-source sequence ordinals through envelope creation and parameterized insert. |
| T-05-11-03 | Denial of service | Dispatcher backpressure | high | mitigate | Use bounded pending ownership, nonblocking handoff, explicit failure, and deterministic drain on Close. |
| T-05-11-04 | Spoofing | Request and gRPC correlation | medium | mitigate | Forward validated session and correlation identity and assert it in raw SSE and database tests. |
| T-05-11-05 | Information disclosure | Raw checkpoint query content | medium | accept | D-23 explicitly accepts local raw-content checkpoint storage for this phase; access remains behind the Go-owned database boundary and no client fetch API is added. |
| T-05-11-06 | Repudiation | Workflow checkpoint history | medium | mitigate | Persist full snapshots and ordered rows, use isolated schemas in tests, and fail query errors rather than treating missing evidence as success. |
</threat_model>

<verification>
- The Task 1 `go -C gateway` registration guard runs the real `TestRAGQueryCrossRuntime` proof with a fatal list/test exit check.
- The Task 2 `go -C gateway` registration guard runs `TestWorkflowCheckpointPendingDrainAndPersistence` with `TEST_DATABASE_URL` required and fatal list/test exit checks.
- A real engine plus gateway run produces the required five-node SSE sequence and terminal events.
- Dispatcher backpressure and close tests prove accepted and retained pending envelopes are not silently lost.
- PostgreSQL rows contain full context snapshots with trace and sequence fidelity.
</verification>

<success_criteria>
ORCH-02, ORCH-03, and ORCH-04 are observable end-to-end: Rust events reach the client, disconnects terminate work, and Go-owned full snapshots persist reliably under bounded backpressure.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-11-SUMMARY.md` when done.
</output>
