---
phase: 05-state-machine-workflow-events
plan: 06
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - buf.gen.yaml
  - config/config.toml
  - config/config.example.toml
  - config/config.verify.toml
  - gateway/proto/lancet/v1/lancet.pb.go
  - gateway/proto/lancet/v1/lancet_grpc.pb.go
  - engine/src/pb/lancet/v1/lancet.v1.rs
  - engine/src/pb/lancet/v1/lancet.v1.tonic.rs
  - engine/src/main.rs
  - engine/src/tests.rs
  - gateway/main.go
  - gateway/main_test.go
  - gateway/checkpoint_sink.go
autonomous: true
requirements: [ORCH-02, ORCH-03, ORCH-04]
must_haves:
  truths:
    - "The coordinated protobuf generation lands matching Rust and Go server-streaming APIs, and every Go QueryRAG caller consumes the stream before the full gateway suite is used as a gate."
    - "`/rag/query` is SSE-only, prefetches the first workflow frame before committing HTTP 200, flushes frames incrementally, and maps client disconnect to gRPC cancellation without inventing a JSON fallback."
    - "The global chi 60-second timeout cannot terminate `/rag/query`; the route has no shorter replacement deadline, while node-level budgets remain the source of workflow bounds."
    - "The timeout arithmetic is explicit: 5s + 10s + 15s + 2s + 65s worst case is 97s, and `generation_node_timeout_ms=65000` is distinct from the existing 30-second per-attempt provider timeout."
    - "The Go checkpoint dispatcher drains a primary capacity of one into owned overflow capacity of five, reports an owned pending envelope instead of dropping an accepted record, and drains on close; the six-record case is a direct handoff-unit assertion, not a service-path claim."
    - "Every workflow event and persisted checkpoint uses one trace_id; SSE headers expose session/correlation identity when known, while terminal failure remains present in the SSE payload."
    - "[FLAGGED ASSUMPTION — spec-less edge probe ORCH-02/unclassified] First-frame identity, SSE framing, flushing, terminal closure, and gRPC error separation are explicit acceptance obligations because the fallback supplied no resolved edge coverage."
    - "[FLAGGED ASSUMPTION — spec-less edge probe ORCH-03/concurrency] Concurrent HTTP streams and checkpoint dispatchers cannot share response state, trace identity, overflow records, or cancellation."
  artifacts:
    - path: "gateway/main.go"
      provides: "Generated server-streaming QueryRAG adapter, SSE-only route, first-frame prefetch, flush, identity, and cancellation behavior."
    - path: "gateway/checkpoint_sink.go"
      provides: "Bounded primary/overflow/pending dispatcher and compile-safe checkpoint envelope seam."
    - path: "gateway/main_test.go"
      provides: "Live incremental SSE, route-timeout topology, generated API migration, identity, cancellation, and cross-runtime tests."
    - path: "config/config.toml"
      provides: "Production defaults for all node timeouts and the separate generation node wall-clock budget."
    - path: "config/config.example.toml"
      provides: "Documented override names, units, and sub-second test configuration convention."
    - path: "config/config.verify.toml"
      provides: "Verification overlay containing deterministic workflow timeout overrides rather than inheriting an untestable 65-second-only path."
  key_links:
    - "buf generation -> matching Go/Rust streaming trait -> gateway client compile"
    - "first Recv -> identity headers -> WriteHeader/first SSE frame -> Flush"
    - "http.Request.Context cancellation -> gRPC context -> Rust cancellation token"
    - "workflow Checkpoint event -> dispatcher primary/overflow/pending -> 05-05 PostgreSQL sink"
    - "workflow timeout config -> Rust runner node budgets; OpenRouter 30-second attempt timeout remains separate"
  prohibitions:
    - statement: "The gateway must not buffer the whole workflow, expose a JSON fallback, or let `/rag/query` inherit the blanket 60-second timeout."
      status: unverified
      flagged: true
      rationale: "Spec-less prohibition fallback: no descriptor-bearing prohibition check was available; live incremental transport and route topology tests must verify this boundary."
    - statement: "The Go relay must not reinterpret Rust node semantics or expose provider secrets in SSE/checkpoint payloads."
      status: unverified
      flagged: true
      rationale: "Spec-less prohibition fallback: the relay boundary is retained from the research responsibility map without claiming a wired descriptor."
---

<objective>
Land the cross-runtime streaming contract and make the Go gateway a faithful, cancellable SSE relay with deterministic timeout configuration and a reliable checkpoint handoff seam. Coordinate code generation and migrate the complete existing Go QueryRAG test surface in one landing wave.

Purpose: Remove the generated-API drift window and prove that a real client can observe the Rust workflow incrementally without a gateway timeout or checkpoint side channel changing query behavior.
Output: Full protobuf outputs, Go SSE relay, deterministic timeout configuration, dispatcher seam, and migrated gateway tests including `TestRAGQueryCrossRuntime`.
</objective>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-PATTERNS.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEWS.md
@.planning/phases/05-state-machine-workflow-events/05-AI-SPEC.md
@.planning/phases/05-state-machine-workflow-events/05-01-SUMMARY.md
@AGENTS.md
@go-guidelines.md
@rust-guidelines.md

## Review dispositions carried into this plan

- 05-01 leaves the Go generated API in its unary state; this plan is the only coordinated codegen landing. A precondition checks that transition state, then full configured Rust+Go generation and all Go call-site migration happen before the gateway suite is claimed green.
- `/rag/query` is removed from the blanket chi `middleware.Timeout(60*time.Second)` group rather than given a shorter replacement. The explicit arithmetic is 5 + 10 + 15 + 2 + 65 = 97 seconds; the workflow's node budgets and D-16 client cancellation provide the bounds.
- `generation_node_timeout_ms=65000` is mandatory per the 2026-08-12 D-17 amendment. `openrouter.generation_timeout_secs=30` remains the per-attempt provider limit; the plan never collapses those keys.
- The first gRPC frame is prefetched so session/correlation headers can be committed before SSE status. Malformed validation remains a plain HTTP 4xx/tonic Status; d1 trailer metadata is only asserted for an actual pre-stream transport error that emits it. In-stream failures are typed SSE events and normal stream closure.
- The dispatcher uses primary capacity one, owned overflow capacity five, and an explicit `Pending` result carrying the envelope if direct admission exceeds the service contract. It never treats a bounded `try_send` loss as success, and its close path drains owned records. The sixth-record scenario is named as a direct handoff unit test because the fixed workflow has at most five checkpoint boundaries.
- A reviewer suggestion to add retry backoff/jitter is explicitly rejected: D-12 requires one immediate byte-identical retry with no backoff.

## Artifacts this phase produces

- `gateway/proto/lancet/v1/lancet.pb.go`, `gateway/proto/lancet/v1/lancet_grpc.pb.go`, and the two Rust generated files: matching full-template streaming outputs.
- `gateway/main.go`: stream consume/relay, first-frame prefetch, SSE encoding/flush, identity, cancellation, and route timeout topology.
- `gateway/main_test.go`: migrated unary callers, live server/incremental readers, cross-runtime migration, SSE/error/identity/flush tests, and route-timeout regression.
- `gateway/checkpoint_sink.go`: dispatcher, pending/overflow ownership, drain/close, and fake sink seam for 05-05.
- `config/config.toml`, `config/config.example.toml`, `config/config.verify.toml`: five node budgets, distinct 65-second generation node budget, and sub-second override keys.
- `engine/src/main.rs`, `engine/src/tests.rs`: regenerated service implementation and Rust-side stream call-site updates required by the full contract.
- `buf.gen.yaml`: restored full configured plugin/output set with a comment explaining the transition-state omission versus the final generation.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Coordinate full protobuf generation and trace one live SSE frame path</name>
  <files>buf.gen.yaml, gateway/proto/lancet/v1/lancet.pb.go, gateway/proto/lancet/v1/lancet_grpc.pb.go, engine/src/pb/lancet/v1/lancet.v1.rs, engine/src/pb/lancet/v1/lancet.v1.tonic.rs, engine/src/main.rs, gateway/main.go</files>
  <precondition>05-01 has completed the Rust-only transition and the existing Go generated files are still present; confirm this before running final full-template generation.</precondition>
  <read_first>05-01-SUMMARY.md; 05-CONTEXT.md D-04, D-05, D-16, D-18, D-19, D-21, D-29; 05-RESEARCH.md Go gateway, timeout, codegen, and SSE sections; 05-PATTERNS.md gateway main/test sections; proto/lancet/v1/lancet.proto; buf.gen.yaml; gateway/main.go; gateway/proto/lancet/v1/lancet_grpc.pb.go; AGENTS.md; go-guidelines.md</read_first>
  <action>Restore the complete configured protobuf generation template and regenerate Rust and Go together so the server-streaming `QueryRAG` method, `WorkflowEvent` oneof, and checkpoint fields have one landing point. Preserve existing generated files by omitting only the Go plugins during 05-01's transition; the final `buf.gen.yaml` must explicitly contain the normal Rust and Go outputs and must not rely on `clean:false` as a preservation mechanism. Update Rust service call sites to the generated stream API and update the Go client adapter to consume `Recv()` frames.</action>
  <action>Implement the thinnest faithful SSE path in `gateway/main.go`. Validate the HTTP request before opening gRPC; call `Recv()` once before committing headers; for a first event set `X-Lancet-Session-ID` and `X-Lancet-Correlation-ID` from the event identity and set `X-Lancet-Error-Kind` when the prefetched event is a failure. Then write the prefetched frame and subsequent frames as SSE with the required event/data encoding and `http.ResponseController`/flusher, flushing after each frame. Keep all Rust event semantics intact, omit transport-only checkpoint frames from SSE while forwarding them to the dispatcher, map a pre-first-frame gRPC error through the existing error path, and map post-open Recv errors to normal typed stream termination without a second HTTP status. Remove `/rag/query` from the blanket 60-second chi timeout group; retain existing read/header bounds and add no route timeout that is shorter than the 97-second worst-case arithmetic.</action>
  <verify>
    <automated>buf generate</automated>
    <automated>go test ./gateway -run '^TestRAGQuerySSEFirstFrame$' -count=1</automated>
    <automated>cargo check --manifest-path engine/Cargo.toml --locked</automated>
  </verify>
  <acceptance_criteria>Full generated Rust and Go APIs compile together; one live HTTP request receives a prefetched first workflow event followed by flushed SSE data; identity headers are set before HTTP 200; checkpoint frames do not leak to SSE; pre-open and post-open errors follow their separate contracts; `/rag/query` is outside the blanket 60-second timeout topology.</acceptance_criteria>
  <done>A real Go HTTP client can observe the first Rust workflow event through the final generated stream contract without API drift or an inherited 60-second deadline.</done>
  <reversibility rating="one-way">Regenerated protobuf files and the unary-to-streaming Go adapter are a shared runtime contract; keeping all generated outputs in one coordinated task limits the rollback surface.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Complete SSE migration, deterministic timeout config, and reliable checkpoint dispatch</name>
  <files>config/config.toml, config/config.example.toml, config/config.verify.toml, gateway/main.go, gateway/main_test.go, gateway/checkpoint_sink.go, engine/src/main.rs, engine/src/tests.rs</files>
  <read_first>Task 1 outputs; 05-REVIEWS.md findings on live SSE harnesses, route timeout isolation, checkpoint overflow/drain, identity headers, generated model coverage, and `TestRAGQueryCrossRuntime`; gateway/main_test.go; gateway fake engine/client helpers; config/config.toml; config/config.example.toml; config/config.verify.toml; go-guidelines.md</read_first>
  <action>Finish every existing Go QueryRAG caller and migrate `TestRAGQueryCrossRuntime` in the same task as the generated API. Replace unary JSON assertions with a stream-drain helper that collects SSE workflow frames and asserts terminal/cardinality semantics. Use `httptest.NewServer` with a response writer that supports flushing plus an incremental reader (and a controlled gRPC/net.Pipe fake where needed); do not use `httptest.ResponseRecorder` alone to claim flush or first-frame behavior. Cover first-frame prefetch, per-frame flush, in-stream Recv error after HTTP 200, malformed HTTP 4xx before stream creation, SSE-only content behavior, header identity, client disconnect cancellation, no checkpoint leakage, concurrent requests, and a deterministic route-topology check proving `/rag/query` is not wrapped by the 60-second middleware without waiting 60 seconds.</action>
  <action>Define and wire workflow timeout configuration under the existing engine settings convention: reformulate 5000ms, retrieve 10000ms, graph 15000ms, prompt 2000ms, and `generation_node_timeout_ms=65000`; preserve `openrouter.generation_timeout_secs=30` as the per-attempt provider timeout. Add matching environment/TOML overrides down to sub-second values in all three config files and assert the arithmetic `65000 >= 2*30000 + 5000` in config validation. Extend the existing `REQUIRED_EFFECTIVE_RAG_KEYS`, annotation map, section allowlist, and exact-set equality assertions in `engine/src/tests.rs` so every new workflow key is required, documented with units/ranges, and present in the example/verification overlays. Implement `CheckpointEnvelope` and `CheckpointDispatcher` with primary capacity one, owned FIFO overflow capacity five, explicit `Accepted`/`Pending` results, a drain loop, and a close-and-drain path. A pending result retains the complete envelope for a caller-controlled drain; no record is reported accepted after a drop. Add fake sink coverage for all five service checkpoints and a sixth direct-handoff pending outcome, and leave the PostgreSQL write adapter to 05-05.</action>
  <verify>
    <automated>go test ./gateway -run '^(TestRAGQuery|TestQueryRAG|TestCheckpointDispatcher)' -count=1</automated>
    <automated>go test ./gateway -run '^TestRAGQueryCrossRuntime$' -count=1</automated>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked query_rag_stream -- --nocapture</automated>
  </verify>
  <acceptance_criteria>All Go QueryRAG callers, including `TestRAGQueryCrossRuntime`, use the generated server stream; live tests prove incremental flush and error/cancellation behavior; route timeout isolation and 97-second arithmetic are mechanically checked; all timeout overlays accept sub-second test values; the dispatcher preserves or explicitly returns ownership of every accepted checkpoint and exposes no silent-loss path.</acceptance_criteria>
  <done>The cross-runtime stream, gateway SSE route, timeout configuration, and checkpoint handoff seam are complete and ready for Rust node expansion and PostgreSQL persistence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client -> Go SSE route | Untrusted request/headers and connection lifetime cross into gateway request handling. |
| Go gateway -> Rust gRPC stream | HTTP cancellation and generated protobuf frames cross the runtime boundary. |
| Rust checkpoint frame -> Go dispatcher | Full workflow context crosses into a bounded asynchronous persistence handoff. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06-01 | Denial of service | SSE route timeout/flush | high | mitigate | Remove only `/rag/query` from the blanket 60-second timeout, retain node-level bounds, and flush incrementally without whole-stream buffering. |
| T-05-06-02 | Spoofing | SSE identity headers | high | mitigate | Copy identity only from prefetched Rust event fields; retain trace_id consistency and do not accept client-supplied identity as authoritative. |
| T-05-06-03 | Information disclosure | SSE/checkpoint serialization | medium | mitigate | Forward typed safe event fields, keep checkpoint frames transport-only, and preserve D-22 safe error messages. |
| T-05-06-04 | Denial of service | checkpoint overflow | high | mitigate | Own FIFO overflow and pending envelopes, drain on close, and make acceptance results explicit instead of silently dropping full-channel records. |
| T-05-06-05 | Tampering | generated protobuf/API boundary | high | mitigate | Generate Rust and Go from the same proto in one task and run compile plus cross-runtime tests before completion. |

The spec-less prohibition fallback is flagged-unverified in `must_haves.prohibitions`; no descriptor-bearing prohibition check is claimed.
</threat_model>

<verification>
Run the generated-code compile checks, focused live SSE and dispatcher tests, the migrated `TestRAGQueryCrossRuntime`, and Rust stream tests. Confirm the full gateway test suite only after all unary call sites have migrated; do not use a pre-migration suite result as evidence.
</verification>

<success_criteria>
Rust and Go share one generated server-streaming contract; `/rag/query` delivers flushed SSE frames with correct pre-open/in-stream error semantics and cancellation; the route is not capped by the old 60-second middleware; timeout arithmetic and sub-second overrides are tested; and checkpoint frames reach a reliable bounded/pending handoff without leaking into client SSE.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-06-SUMMARY.md` when done.
</output>
