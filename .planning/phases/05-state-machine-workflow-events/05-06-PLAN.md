---
phase: 05-state-machine-workflow-events
plan: 06
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - buf.gen.yaml
  - config/config.toml
  - config/config.example.toml
  - gateway/proto/lancet/v1/lancet.pb.go
  - gateway/proto/lancet/v1/lancet_grpc.pb.go
  - engine/src/pb/lancet/v1/lancet.v1.rs
  - engine/src/pb/lancet/v1/lancet.v1.tonic.rs
  - engine/src/main.rs
  - engine/src/tests.rs
  - gateway/main.go
  - gateway/main_test.go
  - gateway/checkpoint_sink.go
autonomous: true
requirements: [ORCH-02, ORCH-03]

must_haves:
  truths:
    - "A generated server-streaming QueryRAG client is consumed by Go with first-frame prefetch, so pre-stream gRPC trailer errors remain HTTP 4xx and a valid request commits SSE 200 only after the first event (D-04, D-05, D-18, D-19)."
    - "The Go gateway forwards client-visible workflow events as flushed SSE frames, consumes standalone Checkpoint events for detached persistence, and keeps checkpoint JSON out of the public DTO (D-19, D-26, D-27)."
    - "Before PostgreSQL persistence is added, the gateway has a compile-safe CheckpointSink/CheckpointDispatcher seam and a recording fake: Dispatch never blocks SSE, Full/Closed handoff is retained in bounded owned overflow, and Drain/Pending makes delivery or pending state explicit (D-26, D-27)."
    - "The checked-in code-generation transition is executable: 05-01 leaves buf.gen.yaml Rust-only with clean=false and the Go generated client/gateway unary, and this plan restores the full four-plugin template and regenerates Go/Rust only inside one coordinated task before exposing a streaming gateway boundary."
    - "CheckpointDispatcher uses primary capacity exactly 1 and FIFO overflow capacity exactly 5, matching 05-01's fixed five checkpoint boundaries; Full/Closed records become FIFO/Pending/overflow state rather than silent loss."
    - "The in-memory CheckpointSink is explicitly a pre-PostgreSQL test/development seam; after 05-05, main selects the PostgreSQL sink while CheckpointDispatcher remains the coordinator for admission, drain, and pending state."
    - "newHTTPServer retains ReadTimeout=60s and ReadHeaderTimeout=10s as request-read bounds, has no independent WriteTimeout or IdleTimeout response cap, and /rag/query remains outside the blanket chi timeout."
    - "The /rag/query route is outside the blanket 60-second middleware, while Rust node budgets remain the configured execution bound and LLMGeneration uses D-17's exact 30-second per-node default; the single retry stays within that deadline and never creates a longer outer envelope (D-11, D-12, D-17)."
    - "All five workflow timeout settings are represented in both TOML files and are observable through explicit LANCET_ENGINE__WORKFLOW__... overrides, including sub-second test values."
    - "Transport regression tests keep trace_id equal to correlation_id and WorkflowCompleted.session_id independent, and they do not introduce SSE resume/reconnect behavior (D-21, D-29)."
    - "The existing Go RAG test surface, including TestRAGQueryCrossRuntime and every direct unary QueryRAG or successful /rag/query body assertion in gateway/main_test.go, consumes server-stream frames and validates FinalAnswer plus terminal WorkflowCompleted data before the unfiltered Go suite runs (D-18, D-19)."
  artifacts:
    - path: "gateway/main.go"
      provides: "Server-streaming gRPC adapter, first-frame-safe SSE forwarding, standalone checkpoint dispatch, and route timeout isolation"
    - path: "gateway/main_test.go"
      provides: "Deterministic streaming fake, recordingCheckpointSink fake, parseSSEWorkflowEvents and terminalWorkflowResponse helpers, migrated legacy /rag/query assertions including TestRAGQueryCrossRuntime, and transport boundary regression tests"
    - path: "gateway/checkpoint_sink.go"
      provides: "Compile-safe CheckpointEnvelope, CheckpointSink, bounded nonblocking dispatcher, owned overflow/drain contract, and pre-PostgreSQL sink seam"
    - path: "gateway/checkpoint_sink.go::checkpointPrimaryCapacity/checkpointOverflowCapacity"
      provides: "The exact dispatcher capacities 1 and 5 shared with the Rust CheckpointHandoff contract"
    - path: "gateway/checkpoint_sink.go::memoryCheckpointSink"
      provides: "An inspectable test/development-only in-memory sink that is not the post-05-05 production sink"
    - path: "buf.gen.yaml"
      provides: "The final full four-plugin Buf template restored only after the unary-to-streaming gateway transition is coordinated"
    - path: "engine/src/main.rs"
      provides: "WorkflowConfigSettings, explicit environment bindings, generation budget validation, and service stream wiring"
    - path: "engine/src/tests.rs"
      provides: "Exact workflow config contract and sub-second override tests"
    - path: "config/config.toml"
      provides: "Committed engine.workflow timeout defaults"
    - path: "config/config.example.toml"
      provides: "Credential-free documented workflow timeout defaults"
  key_links:
    - from: "gateway/main.go::queryRAG"
      to: "gateway/main.go::grpcEngine.QueryRAG"
      via: "stream.Recv() prefetches the first event before SSE headers, then client events are serialized and flushed while Checkpoint events are dispatched separately"
    - from: "engine/src/main.rs::WorkflowConfigSettings"
      to: "engine/src/workflow/runner.rs::WorkflowRunner"
      via: "validated per-node durations are passed into the runner after synchronous ReceiveQuery validation"
    - from: "gateway/main_test.go"
      to: "gateway/main.go::queryRAG"
      via: "server-streaming fake proves trailer errors, SSE framing, identity separation, and route timeout isolation"
    - from: "gateway/main.go::queryRAG"
      to: "gateway/checkpoint_sink.go::CheckpointDispatcher"
      via: "standalone Checkpoint events become envelopes and enter a nonblocking bounded queue; the dispatcher owns overflow and exposes Drain/Pending before 05-05 supplies the SQL sink"
  prohibitions:
    - requirement_id: ORCH-02
      category: disclosure
      statement: "MUST NOT place standalone Checkpoint JSON or context_snapshot_json in any public SSE data frame; checkpoint payloads remain transport-only for Go persistence (D-26, D-27)."
      verification: test
    - requirement_id: ORCH-02
      category: transport
      statement: "MUST NOT add Last-Event-ID handling, replay buffers, or reconnect/resume behavior to /rag/query (D-21)."
      verification: test
    - requirement_id: ORCH-03
      category: timeout
      statement: "MUST NOT replace the removed blanket gateway timeout with a fixed route deadline; Rust's validated per-node budgets remain the only execution bounds (D-17)."
      verification: test
    - requirement_id: ORCH-02
      category: boundary
      statement: "MUST NOT modify or route the standalone QueryGraph RPC through the workflow event/SSE adapter (D-10)."
      verification: test

---

<objective>
Complete the gateway/configuration slice that consumes the Rust streaming contract: one ReformulateQuery event reaches SSE, timeout settings are explicit and arithmetically safe, and deterministic transport tests protect the boundary.

Purpose: finish the cross-runtime entry path without mixing gateway regression work into the Rust contract/runner plan.
Output: streaming Go gateway, workflow timeout configuration and validation, and focused Rust/Go transport tests.
</objective>

<scope_justification>
05-06 intentionally keeps its 12-file surface consolidated: coordinated protobuf regeneration, the final Buf template, the Go server-streaming adapter, the compile-safe checkpoint sink/dispatcher seam, Rust timeout wiring, committed TOML, and their boundary tests must be built and verified as one Wave 2 contract before Wave 3 Rust nodes and Wave 6 PostgreSQL persistence. Splitting the generated contract from the adapter would leave an intermediate Go build without a defined Checkpoint dispatch target; the added seam file is the smallest way to make that dependency explicit. The surface remains below the 15-file hard cap.
</scope_justification>

<execution_sub_boundaries>
The 12-file surface is intentionally atomic, but execution is reviewable at these concrete checkpoints: (1) assert the three unary-state Buf/gateway predicates and stop on drift; (2) restore the four-plugin template, regenerate both runtimes, and require generated-contract/build agreement before changing the stream adapter; (3) implement the CheckpointDispatcher seam and first-frame SSE path, then run its exact transport tests; (4) wire the five TOML/env settings with `generation_node_timeout_ms=30000`, validate the locked D-17 deadline and retry-within-deadline arithmetic, and run the exact config tests; (5) rerun the transition/build guard, focused transport/config tests, and unfiltered Rust/Go/vet gates before completing the task. No checkpoint permits a full-template generation or plan boundary between codegen and the coordinated adapter change.
</execution_sub_boundaries>

<scope_exception>
Exact file count: 12 files in `files_modified`.
Coupling rationale: these files are coupled by the atomic generated-client transition and cannot be split without breaking the streaming cardinality or the generated-client transition: the final Buf template and both generated runtime contracts must change in the same boundary as the Go gRPC/SSE adapter, checkpoint-dispatch seam, Rust timeout wiring, committed TOML, and boundary tests; otherwise the Go gateway has a generated client it cannot implement or a handler with no matching transport contract.
Ordered execution sub-boundaries/checkpoints: (1) assert the unary Buf/gateway predicates and stop on drift; (2) restore the four-plugin template and regenerate both generated runtimes; (3) compile the generated contract with the Go adapter and Rust service before continuing; (4) implement the dispatcher, first-frame SSE path, HTTP timeout inspection, and Rust timeout/env wiring; (5) run exact transport/config tests followed by unfiltered Rust/Go/vet gates, with no standard codegen or plan boundary between the unary assertion and coordinated adapter.
Accepted review basis: this scope exception is accepted on the basis of the Phase 5 cross-AI review and the current operator's explicit instruction to continue revisions until all checks pass.
</scope_exception>

<codegen_transition_gate>
Before Task 1 runs a full `buf generate`, the executor must assert all three unary-state predicates: `buf.gen.yaml` has `clean: false` and only the Rust prost/tonic plugin entries; `gateway/proto/lancet/v1/lancet_grpc.pb.go` still exposes the unary QueryRAG client; and `gateway/main.go` still declares the unary `engine.QueryRAG` method. Any failed predicate halts the task with a drift diagnostic. Only after those assertions pass may Task 1 replace the template with the full four-plugin configuration, regenerate, update the Go adapter, and compile the streaming state in the same task. No plan boundary or ordinary full-template generation is permitted between the unary-state assertion and the coordinated Go adapter.
</codegen_transition_gate>

<execution_context>
@D:/Repos/lancet/.codex/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-state-machine-workflow-events/05-01-SUMMARY.md
@.planning/phases/05-state-machine-workflow-events/05-CONTEXT.md
@.planning/phases/05-state-machine-workflow-events/05-RESEARCH.md
@.planning/phases/05-state-machine-workflow-events/05-REVIEWS.md
@.planning/phases/05-state-machine-workflow-events/05-AI-SPEC.md
@.planning/phases/05-state-machine-workflow-events/05-VALIDATION.md
@.planning/phases/05-state-machine-workflow-events/05-SOURCE-AUDIT.md
@.planning/phases/05-state-machine-workflow-events/COVERAGE.md
@AGENTS.md
@go-guidelines.md
@rust-guidelines.md
</context>

## Artifacts this phase produces

  - `gateway/main.go`: generated server-streaming `QueryRAG` client adapter, first-frame prefetch, SSE forwarding, transport-only checkpoint handling, and `/rag/query` timeout isolation.
  - `gateway/main_test.go`: deterministic streaming fake, `parseSSEWorkflowEvents`/`terminalWorkflowResponse` helpers, migrated legacy `/rag/query` assertions including `TestRAGQueryCrossRuntime`, pre-stream trailer tests, SSE framing/flush tests, identity tests, and route timeout regression coverage.
  - `gateway/checkpoint_sink.go`: compile-safe `CheckpointEnvelope`, `CheckpointSink`, and `CheckpointDispatcher` seam with bounded nonblocking admission, owned overflow/drain, pending-state reporting, and recording fake support.
  - `gateway/proto/lancet/v1/lancet.pb.go` and `gateway/proto/lancet/v1/lancet_grpc.pb.go`: coordinated server-streaming Go client/server contract regeneration.
  - `engine/src/pb/lancet/v1/lancet.v1.rs` and `engine/src/pb/lancet/v1/lancet.v1.tonic.rs`: final full-template regeneration matching the Go contract.
- `engine/src/main.rs`: `[engine.workflow]` settings, explicit environment bindings, effective generation budget validation, and runner timeout wiring.
- `engine/src/tests.rs`: exact configuration contract coverage and sub-second environment override assertions.
- `config/config.toml` and `config/config.example.toml`: all five documented workflow timeout keys.

<tasks>

<task type="tracer">
  <name>Task 1: Trace one generated workflow event from gRPC to SSE</name>
  <action_extension>Trace D-18 through the generated artifacts and adapter: the coordinated four-plugin Buf output must expose a server-streaming Go `QueryRAG` client before the SSE handler consumes it.</action_extension>
  <action_extension>Use `05-AI-SPEC.md` and `05-RESEARCH.md` as the approved server-streaming/SSE design because this repository has no existing SSE/runner analog; retain `05-PATTERNS.md`'s `gateway/main.go` Ingest/trailer compatibility references. The adapter action must implement D-16 native context-close cancellation, D-20 coarse SSE cardinality without sub-step or retry-attempt frames, and D-11's exactly-one retry ownership in `GenerateAnswer` rather than a gateway loop; use `engine/src/generation/openrouter.rs` for the provider per-attempt timeout precedent.</action_extension>
  <read_first>proto/lancet/v1/lancet.proto, buf.gen.yaml, gateway/proto/lancet/v1/lancet.pb.go, gateway/proto/lancet/v1/lancet_grpc.pb.go, gateway/main.go, gateway/checkpoint_sink.go, engine/src/main.rs, config/config.toml, config/config.example.toml, 05-01-PLAN.md, 05-CONTEXT.md, 05-REVIEWS.md, AGENTS.md, go-guidelines.md, rust-guidelines.md</read_first>
  <files>buf.gen.yaml, gateway/proto/lancet/v1/lancet.pb.go, gateway/proto/lancet/v1/lancet_grpc.pb.go, engine/src/pb/lancet/v1/lancet.v1.rs, engine/src/pb/lancet/v1/lancet.v1.tonic.rs, gateway/main.go, gateway/checkpoint_sink.go, engine/src/main.rs, config/config.toml, config/config.example.toml</files>
  <behavior>
    - Valid request -> generated server stream -> first event prefetched -> SSE 200 headers -> flushed client event frames.
    - Invalid pre-stream request -> gRPC trailer identity -> HTTP 4xx response without SSE headers or body framing.
    - Standalone Checkpoint event -> nonblocking CheckpointDispatcher admission/owned overflow and no checkpoint JSON in the SSE frame; PostgreSQL is supplied by 05-05 through the interface.
  </behavior>
  <action>Before any full code generation, run an executable transition guard that reads the checked-in `buf.gen.yaml`, asserts `clean: false` and only the two Rust plugin entries, confirms `gateway/proto/lancet/v1/lancet_grpc.pb.go` still exposes the unary QueryRAG client, and confirms `gateway/main.go` still has the unary `engine.QueryRAG` signature. If that precondition is not true, halt with a drift diagnostic rather than generating an uncoordinated Go streaming client. Then, in this same task with no plan boundary between codegen and adapter changes, restore `buf.gen.yaml` to the full four-plugin template (`clean: true`, Go protobuf, Go gRPC, Rust prost, and Rust tonic with `no_client=true`) and run the repository's standard `buf generate` once so the Go and Rust generated service contracts agree. Update the Go `engine` interface and `grpcEngine.QueryRAG` implementation to consume the regenerated server-streaming client while preserving `trailerError` for pre-stream status metadata; if the coordinated adapter does not compile, do not leave the task marked complete. In `gateway/checkpoint_sink.go`, define the compile-safe `CheckpointEnvelope`, `CheckpointSink`, and `CheckpointDispatcher` interfaces/implementations before wiring the stream: set `checkpointPrimaryCapacity = 1` and `checkpointOverflowCapacity = 5` to match 05-01's fixed five checkpoint boundaries, make `Dispatch` use nonblocking admission, move Full/Closed records into a bounded owned FIFO overflow, and never invoke the sink inline; the worker drains FIFO order, `Drain(ctx)` retries under an explicit caller-owned deadline, and `Pending`/close status exposes exact envelopes still held when a sink or transport is closed. Provide `memoryCheckpointSink` as an inspectable pre-PostgreSQL test/development seam so accepted records remain visible, not discarded; 05-05 later changes production `main` to `postgresCheckpointSink` while leaving this dispatcher as the coordinator. Rewrite `queryRAG` to prefetch one event before committing `Content-Type: text/event-stream`, `Cache-Control: no-cache`, and status 200; serialize only client-visible WorkflowEvent payloads as `data: {json}\n\n`, flush each frame, and call `CheckpointDispatcher.Dispatch` for standalone Checkpoint events without awaiting it. Keep in-stream NodeFailed/WorkflowCompleted(failed) payloads in-band and close the SSE response normally after the stream ends (D-05); keep `/rag/query` out of the root `middleware.Timeout(60*time.Second)` group without adding a fixed replacement budget, and preserve no Last-Event-ID or reconnect behavior (D-19, D-21). In `newHTTPServer`, retain `ReadTimeout: 60*time.Second` and `ReadHeaderTimeout: 10*time.Second` as request-read protection, verify `WriteTimeout` and `IdleTimeout` remain unset/zero so no independent response timeout caps SSE, and do not replace the read bounds with a route deadline. In `engine/src/main.rs`, use the locked CONTEXT.md D-17 interpretation as authoritative over the alternative retry-budget interpretation in 05-AI-SPEC.md: add `WorkflowConfigSettings` under `[engine.workflow]` with `reformulate_timeout_ms=5000`, `retrieve_timeout_ms=10000`, `graph_timeout_ms=15000`, `prompt_timeout_ms=2000`, and `generation_node_timeout_ms=30000` per locked D-17; preserve `openrouter.generation_timeout_secs` as the existing provider per-attempt cap and pass validated durations into the runner. Add explicit environment bindings for all five workflow keys, use checked arithmetic to reject zero/overflow settings and to prevent a retry from starting after the exact 30-second node deadline, and let a retryable result returned before that deadline receive exactly one retry with the remaining budget. A deadline or cancellation wins before a second call when no budget remains; no outer retry envelope is introduced. Use Go 1.25 conventions from `go-guidelines.md` for new code and tests, including request-scoped contexts where applicable. Keep QueryGraph untouched (D-10) and the existing query_rag span as the only Rust workflow span (D-31).</action>
  <verify>
    <automated>$bufText = Get-Content -LiteralPath 'buf.gen.yaml' -Raw; if ($bufText -notmatch 'clean:\s*true' -or $bufText -notmatch 'buf.build/protocolbuffers/go' -or $bufText -notmatch 'buf.build/grpc/go' -or $bufText -notmatch 'buf.build/community/neoeinstein-prost' -or $bufText -notmatch 'buf.build/community/neoeinstein-tonic') { throw 'Final buf.gen.yaml is not the coordinated four-plugin template' }; $grpcText = Get-Content -LiteralPath 'gateway/proto/lancet/v1/lancet_grpc.pb.go' -Raw; if ($grpcText -notmatch 'Recv\(\) \(\*WorkflowEvent, error\)') { throw 'Generated Go QueryRAG client is not server-streaming' }; cargo check --manifest-path engine/Cargo.toml --locked; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; Push-Location gateway; try { go build ./...; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } finally { Pop-Location }</automated>
  </verify>
  <acceptance_criteria>
    - The Go adapter consumes the generated server stream and commits SSE headers only after a successful first frame; pre-stream trailer errors remain HTTP 4xx.
    - Checkpoint-only transport events are dispatched independently and never serialized into the public SSE DTO; client-visible failures remain in-band.
    - `/rag/query` is not under the blanket 60-second timeout, and no fixed gateway duration can undercut configurable Rust/provider budgets.
    - The Rust engine loads all five workflow keys, accepts sub-second overrides, validates the exact 30,000-millisecond LLMGeneration node deadline plus zero/overflow guards before listening, and proves a single retry can occur only within that deadline.
  </acceptance_criteria>
  <done>One generated ReformulateQuery stream event reaches a flushed SSE client frame, while the validated timeout contract and checkpoint transport seam are in place.</done>
</task>

<task type="auto">
  <name>Task 2: Make streaming transport and workflow configuration regressions executable</name>
  <action_extension>Drive the transport regression tests from the approved `05-AI-SPEC.md`/`05-RESEARCH.md` contract because no existing server-streaming analog exists. Assert D-16 context cancellation closes `Recv` and the Rust receiver, D-20 event cardinality excludes sub-step/retry-attempt frames, and D-11 retry arithmetic remains generation-only and bounded by the exact node deadline.</action_extension>
  <read_first>engine/src/main.rs, engine/src/tests.rs, config/config.toml, config/config.example.toml, buf.gen.yaml, gateway/main.go, gateway/main_test.go (engineFunc, all QueryRAG/queryRAG call sites, successful /rag/query body assertions, and TestRAGQueryCrossRuntime), gateway/checkpoint_sink.go, 05-01-PLAN.md, 05-AI-SPEC.md, 05-REVIEWS.md, AGENTS.md, go-guidelines.md, rust-guidelines.md</read_first>
  <files>engine/src/tests.rs, gateway/main.go, gateway/main_test.go, gateway/checkpoint_sink.go</files>
  <behavior>
    - Config contract -> exactly one workflow section and five keys in both committed TOML files, with explicit sub-second environment override coverage.
    - Deterministic server fake -> first-frame prefetch, malformed pre-stream status, in-band failure, SSE flushing, checkpoint DTO exclusion, trace/session identity separation, and dispatcher delivery under a blocked sink.
    - Route inspection -> `/rag/query` is registered outside the blanket 60-second middleware and no resume/reconnect state is present.
    - HTTP server inspection -> ReadTimeout and ReadHeaderTimeout remain request-read bounds, while WriteTimeout and IdleTimeout do not independently cap the SSE response.
    - A non-EOF gRPC Recv error after SSE headers and at least one complete frame -> the handler closes immediately with only the complete prior frames, without a synthetic error event or malformed/partial JSON frame.
    - Legacy gateway coverage -> engineFunc, every direct unary QueryRAG fake, every successful /rag/query body assertion, and TestRAGQueryCrossRuntime are migrated to server-stream consumption; pre-stream validation, trailer, and request-size assertions remain explicit pre-stream checks.
    - Cross-runtime preservation -> TestRAGQueryCrossRuntime parses complete SSE data frames, requires FinalAnswer and terminal WorkflowCompleted success, and retains the answer, citation, snapshot, mock-provider, and strict provider-contract assertions from the real Rust run.
    - Named exact tests -> Rust config tests `test_workflow_config_contract`, `test_workflow_config_subsecond_override`, and `test_workflow_generation_node_timeout_is_30_seconds`, plus Go transport tests `TestQueryRAGFirstFramePrefetch`, `TestQueryRAGSSEFramesFlush`, `TestQueryRAGCheckpointSuppressedFromSSE`, `TestQueryRAGRouteTimeoutIsolation`, `TestHTTPServerKeepsReadBoundsWithoutResponseTimeout`, `TestQueryRAGTraceAndSessionIdentity`, `TestQueryRAGLastEventIDDoesNotResume`, `TestCheckpointDispatcherBackpressure`, `TestQueryRAGMidstreamRecvErrorClosesCleanly`, `TestParseSSEWorkflowEvents`, and `TestRAGQueryCrossRuntime` are each registered and pass.
  </behavior>
  <transport_error_contract>After SSE headers are committed, a deterministic server-stream fake must return at least one complete event and then a non-EOF gRPC `Recv` transport error. `queryRAG` must take an immediate close branch that preserves only complete earlier frames, does not emit a synthetic error SSE event, and does not serialize or flush malformed/partial JSON. Register, execute, and require PASS for `TestQueryRAGMidstreamRecvErrorClosesCleanly`.</transport_error_contract>
  <action_extension>Implement the post-header Recv-error branch in `queryRAG` separately from pre-stream trailer handling: after at least one complete frame has flushed, any non-EOF `Recv` error returns/ closes immediately, bypasses the normal error-event serializer, and leaves no malformed or partial JSON. The deterministic fake and `TestQueryRAGMidstreamRecvErrorClosesCleanly` must inspect the complete frame count and exact absence of a synthetic error frame.</action_extension>
  <action>Address the remaining checker/review concern before extending the focused tests: audit `gateway/main_test.go` with the current `QueryRAGResponse`, `QueryRAG`, `queryRAG:`, `/rag/query`, and recorder-body occurrences, then migrate the complete unary blast radius rather than only adding new streaming cases. Change `engineFunc`'s QueryRAG field/method and every affected test—`TestRAGQueryNoResults`, `TestRAGQueryValidMapping`, `TestRAGQueryCallerSessionAndFilters`, `TestRAGQueryRejectsUnknownOrTrailingJSON`, `TestRAGQueryInvalidArgumentStatus`, `TestRAGQueryProviderErrorPreservesIdentity`, `TestRAGQueryEmbeddingTransportIdentity`, `TestRAGQueryEmbeddingInvalidPayloadIdentity`, `TestRAGQueryDenseRetrievalIdentity`, `TestRAGQueryRejectsOversizedBody`, `TestRAGQueryRejectsHugeFilterBody`, and `TestRAGQueryCrossRuntime`—to the stream-shaped engine contract. Define the exact test helpers `parseSSEWorkflowEvents` and `terminalWorkflowResponse`: parse only complete blank-line-delimited `data:` frames into generated `WorkflowEvent` values, require the terminal `WorkflowCompleted` payload, and expose the `FinalAnswer` payload for existing response assertions. Convert successful whole-body JSON assertions in the local RAG tests to those helpers; keep malformed-input, trailer-identity/provider-transport, and request-size checks as explicit pre-stream HTTP assertions. Migrate `TestRAGQueryCrossRuntime` so the real `grpcEngine` path consumes the generated server stream with `Recv` rather than a unary response, then parse SSE frames and require one `FinalAnswer` plus terminal `WorkflowCompleted(success)` before asserting the existing answer `DENSE_AND_LEXICAL_FIXTURE_MARKER [1]`, citation `[1]` and document identity, retrieval snapshot embedding model/candidate limit, chat/metadata/embedding call counts, model name, usage, strict schema, and dense-plus-lexical evidence contract. Add `TestParseSSEWorkflowEvents` for the helper contract and preserve the exact `TestRAGQueryCrossRuntime` name for the migrated integration scenario. Extend the exact config-contract arrays, section allowlist, adjacent annotation checks, and `Settings`/`EffectiveRagSettings` fixtures in `engine/src/tests.rs` for all five workflow keys; assert a sub-second `LANCET_ENGINE__WORKFLOW__...` override is deserialized, the generation-node default is exactly 30,000 milliseconds per locked D-17, and the existing provider timeout remains a separate per-attempt cap. Add `test_workflow_config_contract`, `test_workflow_config_subsecond_override`, and `test_workflow_generation_node_timeout_is_30_seconds`, including a fast retry-within-deadline case and rejection of a retry that would begin after the node deadline. In `gateway/main_test.go`, replace the remaining unary fakes with deterministic server-streaming fakes, add the `recordingCheckpointSink` fake implementing `CheckpointSink`, and add focused tests for first-frame prefetch, trailer-to-header mapping, in-band NodeFailed/WorkflowCompleted failure, SSE frame boundaries and flushes, standalone checkpoint suppression from SSE, route timeout isolation, trace_id matching correlation identity while session_id remains independent, and `CheckpointDispatcher` backpressure. Name those tests `TestQueryRAGFirstFramePrefetch`, `TestQueryRAGSSEFramesFlush`, `TestQueryRAGCheckpointSuppressedFromSSE`, `TestQueryRAGRouteTimeoutIsolation`, `TestHTTPServerKeepsReadBoundsWithoutResponseTimeout`, `TestQueryRAGTraceAndSessionIdentity`, `TestQueryRAGLastEventIDDoesNotResume`, and `TestCheckpointDispatcherBackpressure`. Configure `recordingCheckpointSink` to block its persistence method, prove `queryRAG` still flushes FinalAnswer, release the sink, call the explicit drain contract, and assert every envelope arrives in FIFO order; close the sink case and assert `Pending` exposes the exact retained envelopes. Add `TestHTTPServerKeepsReadBoundsWithoutResponseTimeout` around `newHTTPServer`: assert `ReadTimeout` is 60 seconds, `ReadHeaderTimeout` is 10 seconds, and `WriteTimeout`/`IdleTimeout` are zero or unset, so request-read protection remains while SSE has no independent response deadline. Exercise both fake-store and transport paths without provider latency, and keep all pre-stream versus post-stream assertions explicit. Use separate exact cargo/go test filters for each focused behavior, then run the complete unfiltered Rust and Go suites at this wave boundary; every native command must have its exit status checked.</action>
  <verify>
    <automated>$rustList = @(cargo test --manifest-path engine/Cargo.toml --locked --bin engine -- --list 2>&amp;1)
$rustListExit = $LASTEXITCODE
if ($rustListExit -ne 0) { exit $rustListExit }
$expectedRustTests = @('test_workflow_config_contract', 'test_workflow_config_subsecond_override', 'test_workflow_generation_node_timeout_is_30_seconds')
foreach ($leaf in $expectedRustTests) {
  $escapedLeaf = [regex]::Escape($leaf)
  $registered = @($rustList | ForEach-Object { $line = $_.ToString(); if ($line -match ("^\s*(?<full>[A-Za-z0-9_:]*" + $escapedLeaf + "):\s+test\s*$")) { $Matches['full'] } })
  if ($registered.Count -ne 1) { throw "Expected one exact Rust registration ending in $leaf, found $($registered.Count)" }
  $fullName = $registered[0]
  $run = @(cargo test --manifest-path engine/Cargo.toml --locked --bin engine -- --exact $fullName --nocapture 2>&amp;1)
  $runExit = $LASTEXITCODE
  if ($runExit -ne 0) { exit $runExit }
  if (@($run | Where-Object { $_.ToString() -match ("^test\s+" + [regex]::Escape($fullName) + "\s+\.\.\.\s+ok\s*$") }).Count -lt 1) { throw "Exact Rust test did not report PASS: $fullName" }
}
Push-Location gateway
try {
  $mainTestText = Get-Content -LiteralPath 'main_test.go' -Raw
  foreach ($definition in @('func parseSSEWorkflowEvents\(', 'func terminalWorkflowResponse\(', 'func TestParseSSEWorkflowEvents\(t \*testing.T\)', 'func TestRAGQueryCrossRuntime\(t \*testing.T\)')) {
    if ($mainTestText -notmatch $definition) { throw "Required streaming test/helper definition is missing: $definition" }
  }
  if ($mainTestText -match 'queryRAG\s+func\(ctx context\.Context, req \*pb\.QueryRAGRequest\) \(\*pb\.QueryRAGResponse, error\)') { throw 'gateway/main_test.go still contains the legacy unary QueryRAG fake signature' }
  if ($mainTestText -match 'json\.Unmarshal\(recorder\.Body\.Bytes\(\),\s*&(?:res|resp|response)\b') { throw 'gateway/main_test.go still unmarshals a legacy successful /rag/query body as JSON' }
  $expectedGoTests = @('TestQueryRAGFirstFramePrefetch', 'TestQueryRAGSSEFramesFlush', 'TestQueryRAGCheckpointSuppressedFromSSE', 'TestQueryRAGRouteTimeoutIsolation', 'TestHTTPServerKeepsReadBoundsWithoutResponseTimeout', 'TestQueryRAGTraceAndSessionIdentity', 'TestQueryRAGLastEventIDDoesNotResume', 'TestCheckpointDispatcherBackpressure', 'TestQueryRAGMidstreamRecvErrorClosesCleanly', 'TestParseSSEWorkflowEvents', 'TestRAGQueryCrossRuntime')
  foreach ($name in $expectedGoTests) {
    $escapedName = [regex]::Escape($name)
    $list = @(go test ./... -list ("^" + $escapedName + "$") 2>&amp;1)
    $listExit = $LASTEXITCODE
    if ($listExit -ne 0) { exit $listExit }
    $registered = @($list | Where-Object { $_.ToString() -match ("^" + $escapedName + "$") })
    if ($registered.Count -ne 1) { throw "Expected one exact Go registration for $name, found $($registered.Count)" }
    $run = @(go test -v -count=1 ./... -run ("^" + $escapedName + "$") 2>&amp;1)
    $runExit = $LASTEXITCODE
    if ($runExit -ne 0) { exit $runExit }
    if (@($run | Where-Object { $_.ToString() -match ("^--- PASS:\s+" + $escapedName + "(?:\s|$)") }).Count -lt 1) { throw "Exact Go test did not report PASS: $name" }
  }
  go test -count=1 ./...
  $fullGoExit = $LASTEXITCODE
  if ($fullGoExit -ne 0) { exit $fullGoExit }
  go vet ./...
  $vetExit = $LASTEXITCODE
  if ($vetExit -ne 0) { exit $vetExit }
} finally { Pop-Location }
cargo test --manifest-path engine/Cargo.toml --locked
$fullRustExit = $LASTEXITCODE
if ($fullRustExit -ne 0) { exit $fullRustExit }</automated>
  </verify>
  <acceptance_criteria>
    - Config tests identify every workflow key exactly once, prove a sub-second environment override, assert `generation_node_timeout_ms=30000` as the D-17 node default, and cover zero/overflow validation plus the exactly-one-retry-within-deadline boundary.
    - Go tests prove the pre-stream 4xx/trailer path, valid SSE framing after first-frame prefetch, in-band failure delivery, checkpoint payload suppression, timeout isolation, and independent trace/session IDs.
    - `gateway/main_test.go`'s `engineFunc` and every listed legacy RAG test use the streaming fake or an explicit pre-stream rejection path; successful `/rag/query` assertions use `parseSSEWorkflowEvents` and `terminalWorkflowResponse` rather than whole-body `QueryRAGResponse` decoding.
    - `TestParseSSEWorkflowEvents` and the migrated `TestRAGQueryCrossRuntime` are each registered exactly once, run with an exact `-run` filter, and report one `--- PASS`; the cross-runtime test requires both `FinalAnswer` and terminal `WorkflowCompleted(success)` while retaining its answer, citation, snapshot, and provider-contract checks.
    - `TestQueryRAGMidstreamRecvErrorClosesCleanly` proves a post-header non-EOF Recv failure closes the SSE response immediately after the last complete frame, with no synthetic error event and no malformed or partial JSON frame.
    - An SSE assertion proves no standalone Checkpoint payload or `context_snapshot_json` field is present in any public `data:` frame.
    - `TestHTTPServerKeepsReadBoundsWithoutResponseTimeout` proves the live `newHTTPServer` retains `ReadTimeout=60s` and `ReadHeaderTimeout=10s` while `WriteTimeout` and `IdleTimeout` do not cap the response independently.
    - The final full Buf template and generated stream are reached only after the unary-state transition guard; the guard is executable and the coordinated codegen/adapter change has no plan-boundary drift window.
    - The dispatcher tests assert primary capacity 1 and FIFO overflow capacity 5 from the 05-01/05-06 contract, and identify the in-memory sink as test/development-only before 05-05 selects PostgreSQL in production.
    - The Wave 2 checkpoint seam compiles without PostgreSQL: the recording fake receives every envelope after a blocked-sink drain, while a closed sink leaves exact pending envelopes observable and never claims delivery.
    - A transport regression sends a `Last-Event-ID` header and proves the handler neither resumes nor replays prior events.
    - Route inspection proves `/rag/query` has no fixed replacement gateway deadline; only the validated Rust node budgets govern workflow execution.
    - Existing QueryGraph contract tests and source checks remain green, and no QueryGraph implementation or protobuf file is added to this plan's modified surface.
    - The focused filters are separate literal substrings with checked exit codes, followed by unfiltered `go test ./...`, `go vet ./...`, and the unfiltered locked Rust suite.
  </acceptance_criteria>
  <done>Gateway transport and workflow configuration have deterministic boundary regression coverage, the complete existing Go RAG test surface including TestRAGQueryCrossRuntime is stream-migrated, and both runtime suites pass at the wave boundary.</done>
</task>

</tasks>

<threat_model>
ASVS Level: 1. Blocking threshold: high. High-severity threats block plan acceptance/execution.

## Trust Boundaries

| Boundary | Description |
|---|---|
| HTTP client → Go `/rag/query` | Untrusted request JSON and connection lifetime enter the SSE handler. |
| Go gateway → Rust QueryRAG | Generated server-streaming messages and trailer metadata cross the process boundary. |
| Rust configuration environment → engine startup | Environment overrides influence timeout and retry resource budgets. |
| Go event adapter → SSE client | Typed workflow payloads are serialized for an untrusted client without checkpoint JSON. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-23 | Tampering | QueryRAG stream/SSE adapter | high | mitigate | Prefetch the first event, preserve trailer-derived identity, serialize generated typed payloads, and keep checkpoint-only events out of the public DTO. |
| T-05-24 | Denial of Service | SSE route and timeout settings | high | mitigate | Exempt `/rag/query` from the stale global 60-second timeout, retain bounded Rust per-node budgets, and validate the two-attempt generation envelope before startup. |
| T-05-25 | Information Disclosure | SSE event serialization | medium | mitigate | Map only client-visible oneof payloads to SSE and dispatch standalone checkpoint JSON to persistence without writing it to the response. |
| T-05-26 | Spoofing | trace_id/session_id | medium | mitigate | Use server-minted correlation_id as trace_id and assert it remains independent from session_id in transport tests (D-29). |
| T-05-28 | Denial of Service | CheckpointDispatcher | high | mitigate | Keep Dispatch nonblocking, bound both primary and owned FIFO overflow, retry only through the explicit Drain contract, and expose retained envelopes when the sink closes so backpressure cannot stall SSE or become an unreported loss. |
</threat_model>

<verification>
- `buf`-generated Go clients, Rust engine code, and gateway code compile together.
- Focused configuration and streaming transport tests pass with every command exit checked.
- `gateway/main_test.go` explicitly proves that Checkpoint JSON stays out of SSE, a `Last-Event-ID` request cannot resume or replay, `/rag/query` has no fixed replacement timeout, and the standalone QueryGraph contract remains unchanged.
- Unfiltered locked Rust and Go suites plus `go vet ./...` pass at the wave boundary.
</verification>

<success_criteria>
The Go gateway reliably distinguishes pre-stream rejection from streamed workflow failure, forwards valid events as SSE without leaking checkpoint snapshots, and the Rust timeout configuration is explicit, overrideable, and safe for the locked generation retry envelope.
</success_criteria>

<output>
Create `.planning/phases/05-state-machine-workflow-events/05-06-SUMMARY.md` when done.
</output>
