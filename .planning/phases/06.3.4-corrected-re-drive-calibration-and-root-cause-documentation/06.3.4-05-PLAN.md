---
phase: 06.3.4
plan: 05
type: execute
wave: 4
depends_on: [06.3.4-04, 06.3.4-02]
files_modified:
  - eval/src/lancet_eval/score.py
  - eval/src/lancet_eval/cli.py
  - eval/src/lancet_eval/gate.py
  - eval/src/lancet_eval/report.py
  - eval/src/lancet_eval/templates/report.md.j2
  - eval/report.schema.json
  - eval/tests/test_calibration_worksheet_size.py
  - eval/runs/2026-XX-XX-multihop_rag/calibration_worksheet.jsonl
  - eval/runs/2026-XX-XX-multihop_rag/calibration_completed.jsonl
  - eval/runs/2026-XX-XX-multihop_rag/judge_cache.json
  - eval/runs/2026-XX-XX-multihop_rag/report.json
  - eval/runs/2026-XX-XX-multihop_rag/report.md
  - .planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-FINDINGS.md
autonomous: false
requirements: [OBS-05]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "The judged slice size is derived from the drive's observed judgeable-record count and the judging stage's spend cap by a rule written down before either number was read, and both the rule and the number it produced are recorded and printed in the report (D-48, D-47)."
    - "The calibration worksheet is emitted at the committed calibration size rather than at the harness's historical default, and the size is a stated parameter rather than a constant buried in the emitter (D-48)."
    - "The report prints the committed calibration size as its own RunMetadata field named `calibration_size_committed`, distinct from 06.3.4-02's `calibration_completed_n` which counts human dual-scores actually completed, so a short worksheet is visible as a shortfall rather than as a smaller target. Neither field is renamed into the other."
    - "The judged pass runs under a fail-closed spend cap carrying the judging figure from 06.3.3-BUDGETS.md as amended at 06.3.4-03's decision, passed explicitly on the judged invocation; the judge loop stops itself at the cap rather than relying on an operator interrupting it (D-47)."
    - "The per-judged-question cost input the cap-derived bound rests on is recorded — with the judge model id, the price source, the date read and the prompt token budget it was computed from — before the judged pass runs, so a bound that later proves optimistic is diagnosable rather than mysterious."
    - "A worksheet shorter than the committed calibration size, including one shortened by a transient judge error, is a recorded shortfall with its cause named — not a verification failure and not a reason to re-run the judged pass at additional cost."
    - "Human calibration is performed fresh by a human on the corrected run, at a task that genuinely halts execution; no agent writes, completes, infers or back-fills a human score, and the superseded run's sixteen scores are not carried forward (D-48)."
    - "Judge-versus-human agreement is computed on the paired integers using the two statistics 06.3.4-02 built, and judged numbers are published under an explicit uncalibrated label whenever either statistic falls below the committed target."
    - "The feed-back invocation runs at the same judged sample scope and seed as the judged pass, so the judged population is unchanged and no additional provider call is made; the judge cache's verdict count is recorded before and after and is unchanged."
    - "An agreement statistic exactly at the committed target satisfies it (specless edge: adjacency)."
    - "A derived judged slice of zero and an empty worksheet are each defined, recorded outcomes rather than errors or defaults (specless edge: empty)."
    - "The calibration attestation and the agreement result are written as the first section of 06.3.4-FINDINGS.md, which 06.3.4-07 then appends both dispositions beneath — one findings document, written in two plans."
    - "The 2026-09-03 run is untouched: no score, worksheet or cache entry from it is reused, and the two runs remain separately dated artefacts (D-45, specless edge: adjacency)."
  artifacts:
    - "eval/src/lancet_eval/score.py and cli.py — the calibration worksheet size as an explicit parameter and option, and a required --stage-cap on the judged score path"
    - "eval/src/lancet_eval/report.py, eval/src/lancet_eval/templates/report.md.j2 and eval/report.schema.json — the committed calibration size as the new RunMetadata field calibration_size_committed; all three move together because RunMetadata forbids extra fields"
    - "eval/src/lancet_eval/gate.py — the judged-slice derivation rule mirrored for machine use beside the other committed decision inputs"
    - "eval/tests/test_calibration_worksheet_size.py — the worksheet sizing and the derivation rule under test"
    - "eval/runs/<dated>-multihop_rag/calibration_worksheet.jsonl — emitted at the committed calibration size"
    - "eval/runs/<dated>-multihop_rag/calibration_completed.jsonl — the human's scores, written by the human"
    - "eval/runs/<dated>-multihop_rag/report.json and report.md — republished with the judged population, agreement and the calibration state"
    - ".planning/phases/06.3.4-.../06.3.4-FINDINGS.md — created here with its first section, the calibration attestation and the agreement result; the two dispositions are appended beneath it by 06.3.4-07"
  key_links:
    - "The committed calibration size and the agreement target are read from gate.py as landed by 06.3.4-01; nothing here restates them"
    - "The two agreement statistics and the uncalibrated label come from 06.3.4-02's agreement module and its wiring through the scorer"
    - "The judgeable-record count is read from the report 06.3.4-04 published, by dimension or detail key, or computed from its journal by the judge loop's own predicate"
    - "The judging-stage spend cap comes from 06.3.3-BUDGETS.md's dated entry as amended by any judging cap recorded at 06.3.4-03's decision"
    - "06.3.4-07 reads the graph presence and influence rates, the complement rate and the paired deltas from the report this plan republishes, and appends its dispositions beneath this plan's calibration section"
    - "score.py's existing calibration guards — the refusal of a calibration file fed back without judging, and at a sample scope narrower than the cached verdict count — are what force this plan's ordering and must keep working unchanged"
  prohibitions:
    - "No agent may write, complete, infer or back-fill a human score, and no score from the superseded run may be carried into the corrected run's calibration file"
    - "The human-scoring halt must be a decision checkpoint that genuinely blocks; it must not be a verification checkpoint, which this repository's end-of-phase setting would defer into a batch and thereby skip the halt entirely"
    - "The judged slice size must not be chosen after seeing which value produces a better-looking number; the rule is committed in this plan and the number it produces is recorded with the inputs it was computed from"
    - "The agreement target must not be changed after an observation exists; a change is a recorded amendment carrying its own rationale and date, never a silent edit"
    - "A judged number below the agreement target must never be published without its explicit uncalibrated label; withholding or labelling are the two permitted outcomes"
    - "The existing calibration guards in score.py must not be weakened or bypassed to make an invocation succeed; the ordering bends to the guards, not the other way round"
    - "No calibration caveat, agreement value or label may be written into DimensionResult.reason on a status-ok row, and DimensionResult._validate_consistency must not be relaxed to permit one; this plan reads the detail floats and the RunMetadata.notes sentence 06.3.4-02 established and adds no competing surface"
    - "The judged pass must not be invoked without an explicit spend cap; the judging figure is read from 06.3.3-BUDGETS.md as amended at 06.3.4-03's decision, and an absent or unreadable figure stops the stage rather than being supplied from memory"
    - "No command in this plan may resolve the run directory by taking the newest match of the date glob; the resolution excludes the superseded run and requires exactly one remaining candidate, and a zero-candidate or multi-candidate result halts rather than picking one"
---

<objective>
Turn the corrected drive's records into judged numbers that can be believed.

Purpose: the judged half of the benchmark has never actually run. In the superseded run 98 of 100
sampled questions were skipped for want of evidence, so the sixteen human scores judged answers that
barely existed and cannot carry forward. This plan produces a genuinely populated judged slice for
the first time, has a human score a fresh worksheet, and measures how far the judge and the human
actually agree — before anything published leans on the judge.

Two things about the ordering are not stylistic. The judged slice size is derived from what the
drive actually produced, so it is measured rather than chosen; and the harness's own calibration
guards refuse a calibration file fed back without judging, or at a sample scope narrower than the
verdicts already cached. Those guards are what stop a feed-back invocation silently discarding a
paid judged population. The ordering here bends to them and leaves them untouched.

Tasks 1-4 are one indivisible sequence: the worksheet size is committed and the paid judged pass is
run *before* the halt, the halt is the one step in this family no agent may perform, and the
feed-back consumes the human's scores immediately after it. There is no seam inside that unit. The
two dispositions the full drive's numbers make possible — whether graph yield cleared its floor, and
whether the graph makes results worse — sit entirely *after* it, and are therefore taken in
`06.3.4-07`, on fresh context rather than at the tail of this plan.

Output: the sized worksheet, the human's scores, the republished report carrying agreement and its
calibration state, and the first section of `06.3.4-FINDINGS.md`.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-CONTEXT.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-AI-SPEC.md
@.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-STAGED-GATE.md
@.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-04-SUMMARY.md
@CLAUDE.md
</context>

<judged_slice_derivation>
## The judged-slice derivation rule

This rule is committed **here, in this plan file**, before the drive it applies to has run. That
placement is the point: by the time this plan executes, the number it consumes already exists, so
the only way the rule can be independent of the number is for the rule to predate it in the
repository. Task 1 mirrors it into `gate.py` for machine use; the rule of record is this section.

**Inputs**

| Symbol | Meaning | Kind |
|---|---|---|
| `J` | Observed count of **judgeable** records in the primary arm: records the harness's usable-record predicate admits, whose gold question is present, **and whose structured citations are non-empty**. Read from the published report where 06.3.2 surfaces it as a dimension or detail key; otherwise computed directly from the journal by applying that same predicate, and recorded as computed rather than read. Either source is valid — the record states which was used. | measured |
| `C_j` | The judging-stage spend cap, from `06.3.3-BUDGETS.md`'s dated entry as amended by any judging cap recorded at 06.3.4-03's decision. | committed input |
| `p` | A conservative per-judged-question cost, recorded **before** the judged pass runs. It is not a guess: record it as an arithmetic derivation showing (a) the judge model id from the corpus config (`config.judge_model`), (b) that model's published input and output price per million tokens with the URL and the date read, (c) the prompt token budget — `judge.py:30-33`'s `DEFAULT_MAX_TOKENS = 400` for output, plus the evidence budget for input — and (d) the resulting per-question figure, rounded **up**. Record the arithmetic itself, not only the result. A `p` guessed high shrinks `N` needlessly; a `p` guessed low breaches the cap, and `judge_once` has no mid-loop spend check of its own — which is why the judged invocation also carries `--stage-cap` as the mechanical backstop. | committed input |
| `N_cap` | `floor(C_j / p)` | derived from committed inputs |
| `K` | The committed calibration size — the worksheet's row count — read from `gate.py`. | committed input |
| `V0` | Verdict count already in `judge_cache.json` before the judged pass. Expected zero, because every prior scoring pass in this phase ran with judging off. | measured |

**Rule**

    N = min(J, N_cap)          and N must be >= V0

The empty-citation condition on `J` is not decorative. The judge loop counts a record without
structured citations as skipped for want of evidence and never requests a verdict for it, so such a
record cannot join the judged population however large a slice is requested. Sizing against total
usable records instead would produce a slice the harness silently cannot fill — which is exactly the
shape of the failure this family exists to correct.

`N >= V0` is required because the harness refuses a calibration file fed back at a sample scope
narrower than the verdicts already cached; violating it would either raise or discard paid verdicts.

**Defined outcomes**

| Condition | Outcome |
|---|---|
| `N >= K` | Judged pass at `N`; worksheet emitted at `K` rows. |
| `0 < N < K` | Judged pass at `N`; worksheet emitted at `N` rows; recorded as short of the committed calibration size, with which bound caused it; agreement computed over fewer pairs and the judged numbers carry that statement. |
| `N == 0` | No judged pass, no worksheet, no agreement. Judged dimensions are published with the no-calibration label. A recorded outcome, not a failure. |
| `N_cap < J` | The **cap** bounded the slice, not the data. Recorded as such, with both figures. |
| `N_cap == J` | Both bounds coincide; the record states both rather than picking one. |
| `N == K` exactly | Satisfies the committed calibration size, which is stated as at-or-above. |
| Worksheet emits fewer than `K` rows even though `N >= K` | Possible when a transient judge error leaves a record without a cached verdict, since the emitter keeps only cache-verified rows (`score.py:564-566`). Recorded as a shortfall naming the error count as its cause. **This is not a verification failure**, the human checkpoint proceeds on the rows that exist, and the judged pass is not re-run to fill the gap at additional cost. |

Everything on the left of that table is a comparison against a committed input. `J`, `V0`, the
observed spend and the observed agreement are **measured** and are never asserted in advance.
</judged_slice_derivation>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Size the worksheet deliberately, and derive the judged slice from what the drive produced</name>
  <files>eval/src/lancet_eval/score.py, eval/src/lancet_eval/cli.py, eval/src/lancet_eval/gate.py, eval/src/lancet_eval/report.py, eval/src/lancet_eval/templates/report.md.j2, eval/report.schema.json, eval/tests/test_calibration_worksheet_size.py</files>
  <read_first>
    - `eval/src/lancet_eval/score.py:521-604` — the calibration worksheet block: the judge-disabled refusal (523-531), the header row (535-543), the misleading `# Select up to 20 representative items across query types` comment at 545, the cache-verified selection (547-566), the hardcoded `if len(selected_records) == 20: break` at 567-568 that terminates it, the emitted row shape (586-595) with `human_groundedness`/`human_faithfulness` written as `None`, and the emitted-count print at 602-603.
    - `eval/src/lancet_eval/score.py:358-389` — the judged-question selection, which draws a seeded sample from the primary arm's question identifiers rather than a prefix.
    - `eval/src/lancet_eval/score.py:390-446` — the judge loop, in particular the `if not rec.structured_citations: continue` skip at 399-401, which is the predicate the derivation rule's judgeable count is defined on.
    - `eval/src/lancet_eval/score.py:332-356` — the two calibration guards: the refusal of a calibration file fed back without judging (332-346), and the refusal at a sample scope narrower than the cached verdict count (347-356). Both must keep working unchanged.
    - `eval/src/lancet_eval/score.py:1027-1069` — how `RunMetadata` is assembled (1040-1057) and written. (Do NOT read `score.py:770-815` for this — that range is judged-dimension assembly, not metadata.)
    - `eval/src/lancet_eval/report.py:24-45` — `RunMetadata`, its `extra="forbid"` config, and the `calibration_completed_n` field 06.3.4-02 added.
    - `eval/report.schema.json` and `eval/tests/test_schema.py` — the published schema and its conformance test, which must move with any new metadata field.
    - `eval/src/lancet_eval/gate.py` as landed by 06.3.4-01 — the committed calibration size, the agreement target, the floors and the per-stage cap reader.
    - `eval/src/lancet_eval/cli.py:735-790` and `eval/src/lancet_eval/measure.py:500-520` — `--stage-cap`'s option shape and the mid-loop stop rule, for the judged-path equivalent this task adds to `score`.
    - `.planning/phases/06.3.2-.../06.3.2-05-PLAN.md` — that phase's worksheet work: it makes the judge-disabled combination refuse, and states explicitly that the row count stays as it is and belongs here.
    - `.planning/phases/06.3.4-.../06.3.4-02-PLAN.md` — the reserved field name `calibration_completed_n`, which this plan must not rename or reuse, and the `detail`-plus-`notes` surface where the agreement values and the calibration caveat land.
    - `eval/tests/test_score_judged.py` — the existing worksheet tests, which must keep passing with their arguments unedited.
  </read_first>
  <behavior>
    - Requesting a worksheet with an explicit size emits exactly that many rows when at least that many selectable records exist, and exactly as many as exist when fewer do.
    - Requesting a worksheet without an explicit size preserves the harness's existing row count exactly, so no existing behaviour or test changes.
    - The selection is deterministic: the same run directory and the same requested size produce the same rows in the same order across two invocations.
    - A requested size of zero emits a worksheet with a header and no scoring rows, and reports that it did.
    - The committed calibration size reaches run metadata as `calibration_size_committed`, distinct from 06.3.4-02's `calibration_completed_n`, and both survive into the rendered report and validate against `eval/report.schema.json`.
    - The derivation rule, given a judgeable count and a cap-derived bound, returns the smaller; it returns zero when the judgeable count is zero; and it reports which bound was binding, including when both coincide.
    - The derivation rule refuses a result below the cached verdict count, naming that count, rather than returning a value the harness's own guard would later reject.
    - Invoking `score` with judging enabled and no `--stage-cap` exits non-zero before any judge call is dispatched, with a message naming the judging cap's source.
    - Invoking `score --run <dir> --no-judge` with **no** `--stage-cap` — the literal shape 06.3.4-04 Task 3 uses — still succeeds and writes `report.json` exactly as it did before this change, because no provider call is made on that path.
    - The judge loop stops once accumulated judged spend reaches the cap, records that it stopped for that reason, and leaves the verdicts already cached intact and reusable.
  </behavior>
  <action>
    Two changes, both small, both in service of the same thing: making a number that was previously
    implicit into a number that is stated.

    First, the worksheet size. The emitter today terminates its selection at a fixed row count with
    no way to ask for another, which is why the committed calibration size cannot currently be
    honoured. Add the size as an explicit parameter on the scoring entry point and as an option on
    the score command, defaulting to the harness's existing count so nothing changes for a caller
    that does not ask. Under this back-edge rule a harness gap found by the drive lands in this
    seam; 06.3.2 examined this same block and recorded explicitly that the row count belongs here.

    While in that block, make the selector's own description state the rule it actually implements —
    a deterministic pass over the primary arm's records in journal order, keeping those with a
    cached verdict until the requested size is reached. It does not balance across question types,
    and a description implying otherwise would have a reader believe the worksheet is stratified
    when it is not. Do not add stratification: the committed size is small, the selection is already
    deterministic, and changing the selection basis now would change what the judged population
    means midway through the family.

    Second, carry the committed calibration size into run metadata as a field named exactly
    `calibration_size_committed`, beside — not in place of — `calibration_completed_n`, the field
    06.3.4-02 added for the count a human filled in. They answer different questions: one is how many
    rows the worksheet asked for, the other how many were actually scored. Collapsing them would make
    a half-finished worksheet indistinguishable from a smaller target. `RunMetadata` is
    `extra="forbid"` (`report.py:27`), so this field requires editing `report.py`, adding a row to
    the metadata table in `templates/report.md.j2`, and updating `eval/report.schema.json` — all
    three move together or `eval/tests/test_schema.py` fails. Neither field may be renamed into the
    other, and neither is a synonym for `sample_size_judged` (`score.py:1051`), which counts the
    judged slice `N` — a third, legitimately different population. The metadata table will therefore
    carry three adjacent counts, so give each a label in `report.md.j2` that says what it counts
    rather than reusing the field name verbatim: how many records were judged, how many worksheet
    rows were asked for, and how many a human filled in. Record the three labels in the summary.

    Third, the judged spend cap. `06.3.3-BUDGETS.md` requires every 06.3.4 paid driver to enforce
    its cap with no silent default, and the judged pass is a paid driver: `judge_once` has no
    mid-loop spend check, so the derivation's `N_cap` is a *pre*-estimate rather than an enforcement.
    Add `--stage-cap` to the `score` command, **required whenever judging is enabled** and not
    required under `--no-judge` (which makes no provider call), and add the matching stop rule to the
    judge loop at `score.py:390-446`, following `measure.py:500-520`'s shape: accumulate observed
    judged spend, and break the loop once it reaches the cap. Do not copy `measure`'s `= 5.0`
    default. A capped stop leaves the verdicts already cached intact — the cache is keyed per
    question — so the worksheet still emits over whatever was judged, and the shortfall is recorded
    with the cap named as its cause.

    **Implement "conditionally required" explicitly, not with Typer's `required`.** Typer cannot
    express a requirement contingent on another option's value: declaring the parameter required
    makes it mandatory for every invocation, including `--no-judge`. Declare it instead as
    `float | None = None` and raise inside the command body — before `score_run` is called and
    before any provider client is constructed — when judging is enabled and the value is `None`,
    with a message naming the judging cap's source in `06.3.3-BUDGETS.md`. This matters concretely
    for backward compatibility: **06.3.4-04 Task 3 is a wave-3 plan that already ran
    `lancet-eval score --run <dir> --no-judge` with no cap**, and 06.3.4-05 Task 4 re-scores that
    same run directory after this change lands. A blanket `required` would retroactively break that
    sibling's published invocation and any re-run of it. The behaviour block and acceptance criteria
    below pin exactly that command shape as a case that must keep succeeding.

    Then mirror the derivation rule from this plan's `judged_slice_derivation` section into
    `gate.py`, beside the other committed decision inputs, as a function over the judgeable count,
    the cap-derived bound and the cached verdict count, returning the slice size and which bound was
    binding. The rule of record is this plan file; the module is for machine use and its tests
    assert the same branches the table names.

    Finally, apply it. Obtain the judgeable-record count two ways, in this order of preference.
    Prefer the published report: if 06.3.2 surfaces the count as a dimension or detail key, read it
    and name the key. If it does not — and it may not, because the citation-emptiness condition is a
    per-record journal property rather than an obviously published figure — compute the count
    directly from the corrected run's journal by applying exactly the predicate the judge loop
    applies: primary arm, gold question present, structured citations non-empty. Record which of the
    two sources was used and, when computed, record the predicate and the command that computed it.

    Both paths are legitimate and the fallback is not a degradation: it produces the same number
    against the same predicate, from the record set the judge will actually read. What is not
    legitimate is a defaulted or assumed count, which would size a paid stage against a number
    nobody measured. If neither path can produce a count — the journal unreadable, or the predicate's
    fields absent from the records — stop and name what was missing.

    Then read the judging cap from `gate.py`'s cap reader and record the per-question cost input `p`
    as the full arithmetic derivation the `judged_slice_derivation` table specifies — judge model id,
    published input and output price with URL and date read, the token budget (`judge.py:30-33`), and
    the rounded-up per-question figure. Record it **before** the judged pass, not alongside its
    results. Read the cached verdict count; compute the slice size and record it together with every
    input and which bound was binding.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_calibration_worksheet_size.py eval/tests/test_score_judged.py eval/tests/test_schema.py eval/tests/test_report.py -x</automated>
    <fails_when>pytest exits non-zero, or its summary line reports one or more `failed`/`error` items instead of ending in `passed`. A schema-conformance failure naming `calibration_size_committed`, or a failure in an unmodified `test_score_judged.py` worksheet test, means the metadata field or the worksheet default changed something it should not have.</fails_when>
  </verify>
  <acceptance_criteria>
    - Every behaviour in the `<behavior>` block has a named test in `eval/tests/test_calibration_worksheet_size.py`, and both that module and `eval/tests/test_score_judged.py` pass under the verify command.
    - The four existing worksheet tests in `eval/tests/test_score_judged.py` pass with their arguments unedited, proving the default preserves existing behaviour.
    - A test asserts two successive emissions at the same requested size produce identical rows in identical order.
    - A test asserts the requested-size-zero case emits a header and no scoring rows.
    - A test asserts the derivation function returns the smaller bound, names which bound was binding, handles the coincident case, returns zero on a zero judgeable count, and refuses a result below the cached verdict count.
    - A test asserts `RunMetadata` declares both `calibration_size_committed` (this plan) and `calibration_completed_n` (06.3.4-02) as distinct fields carrying distinct values, and that neither was renamed into the other or into `sample_size_judged`; a synthetic report carrying both validates against `eval/report.schema.json`.
    - A test asserts `score` with judging enabled and no `--stage-cap` exits non-zero before any judge call is dispatched. A **separate** test invokes the exact command shape 06.3.4-04 Task 3 uses — `score --run <dir> --no-judge`, no cap argument — against a synthetic reconciled run and asserts it exits zero and writes `report.json`; this is the regression guard against the option being declared unconditionally required, which Typer's `required` would do. A third test asserts the judge loop breaks once accumulated spend reaches the cap and leaves already-cached verdicts intact.
    - A test asserts the conditional requirement is enforced in the command body rather than by Typer's `required` flag, by confirming the parameter's declared default is `None` and that the failure surfaces as the command's own message rather than a missing-option usage error.
    - The two existing calibration guards still raise on their two invalid combinations, proven by tests that were not modified.
    - The summary records the derived judged slice size, every input it was computed from with its source, which bound was binding, and the rule it was computed by — cited to this plan's `judged_slice_derivation` section rather than restated.
    - The summary records `p` as the full arithmetic derivation — judge model id, published price with URL and date read, token budget, rounded-up per-question figure — and records that it was written down before the judged pass ran.
    - The summary records the misleading worksheet-selector description at `score.py:545` as corrected to describe the deterministic journal-order pass the code actually performs, and states that stratification was deliberately **not** added because changing the selection basis mid-family would change what the judged population means.
    - The summary records which source produced the judgeable count — the published report by key, or the journal by predicate — and, on the journal path, the predicate and the command that computed it. Falling back to the journal is recorded as a normal outcome, not as a failure.
    - Only if neither source can produce a count does the summary record that no slice size was derived, naming what was missing; a defaulted count is never used.
    - `uv run --project eval ruff check --preview eval/src eval/tests` is clean and `uv lock --project eval --check` confirms no dependency was added.
  </acceptance_criteria>
  <done>
    The worksheet's size is something the caller states rather than something the emitter assumes,
    and the judged slice size is a recorded consequence of what the drive produced rather than a
    number anybody picked.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the judged pass as its own capped stage and emit the worksheet</name>
  <files>eval/runs/2026-XX-XX-multihop_rag/judge_cache.json, eval/runs/2026-XX-XX-multihop_rag/calibration_worksheet.jsonl, eval/runs/2026-XX-XX-multihop_rag/report.json, eval/runs/2026-XX-XX-multihop_rag/report.md</files>
  <precondition>The corrected run's journal is reconciled and its report published by 06.3.4-04; the judging-stage cap is recorded and unspent; the provider key is present; the derived judged slice size from task 1 is recorded with its inputs, including `p` as a written-out arithmetic derivation; and the run-directory resolution — exclude `2026-09-03-multihop_rag` from `eval/runs/????-??-??-multihop_rag`, require exactly one remaining candidate — returns the literal pinned in `06.3.4-DRIVE-PRECONDITIONS.md`, halting the task if it does not.</precondition>
  <reversibility rating="costly">Judged provider spend is unrecoverable, but it is bounded by the judging cap, and the verdict cache makes the feed-back invocation free. The slice size was derived rather than chosen, so the stage cannot quietly grow.</reversibility>
  <read_first>
    - The recorded derived slice size and its inputs from task 1's record.
    - `eval/src/lancet_eval/cli.py:307-368` — the score command's judging, sample and worksheet options.
    - `eval/src/lancet_eval/score.py:325-345` — how the judged subset is selected from the primary arm using the configured seed, so the same requested scope reproduces the same subset.
    - `eval/src/lancet_eval/judge.py:78-94,201-230` — the cache entry shape and the cache's atomic write, so a verdict count can be read before and after.
    - `.planning/phases/06.3.3-.../06.3.3-BUDGETS.md` and `.planning/phases/06.3.4-.../06.3.4-STAGED-GATE.md` — the judging cap and any amendment to it.
    - `.planning/phases/06.3.1-.../06.3.1-AI-SPEC.md` lines 960-971 — the calibration note: the worksheet size, the two statistics, the target and the standard-library constraint.
  </read_first>
  <action>
    Record the judge cache's verdict count and the provider's reported spend and remaining allowance
    immediately before the stage.

    Resolve the run directory by the phase's canonical recipe — exclude `2026-09-03-multihop_rag`
    from the date glob, require exactly one remaining candidate, halt on zero or two — and never by
    a newest-match, which returns the superseded run whenever the corrected one is absent.

    Run the judged pass at the derived slice size, with judging enabled, passing `--stage-cap` with
    the judging figure, and emitting the calibration worksheet at the committed calibration size in
    the same invocation. Emitting in the same invocation is what lets the emitter select only rows
    that already have a cached verdict — a worksheet row without one cannot contribute to agreement,
    because agreement is computed by matching the human's score against the cached verdict for that
    row's key. The cap is the mechanical backstop behind the derived `N_cap`: `judge_once` has no
    spend check of its own, so without it a `p` that proved optimistic would overrun the judging cap
    silently.

    Record the provider's reported spend and remaining allowance immediately after, compute the
    stage's observed spend and its observed cost per judged question, and state both against the
    judging cap and against the per-question cost input the derivation used. If the observed cost
    per question exceeds the input the cap bound was computed from, record that the bound was
    optimistic and by how much — it does not retroactively change the slice, and the slice must not
    be resized after the fact to make the numbers agree.

    Record what the judged pass actually produced: the count of records judged, the count skipped
    for want of evidence, the count of judge errors, and the verdict count in the cache afterwards.
    In the superseded run 98 of 100 sampled questions were skipped for want of evidence; a
    materially populated judged slice here is one of this family's central claims and is worth
    stating as an observation with its counts.

    Verify the worksheet before a human is asked to spend an hour on it: count its scoring rows,
    compare against the committed calibration size, and confirm every row carries a question, an
    answer, evidence and a cache key, with the two human score fields present and empty. If the row
    count falls short of the committed size, record the shortfall and which bound caused it —
    including the case the derivation table names explicitly, where `N >= K` but a transient judge
    error left a record without a cached verdict so the cache-verified emitter had fewer than `K`
    rows to select from. **A short worksheet is a recorded shortfall, not a verification failure**;
    the checkpoint proceeds on the rows that exist and the judged pass is not re-run to fill the gap
    at additional cost. If the count is zero, record that as the outcome and stop before the
    checkpoint — there is nothing for a human to score, and the judged dimensions will be published
    with the no-calibration state.

    Do not reuse anything from the superseded run: not a score, not a worksheet row, not a cache
    entry. Its cache, its worksheet and its scores all belong to a population being replaced, and
    every one of them is keyed to answers that no longer exist.
  </action>
  <verify>
    <automated>uv run --project eval python -c "import json,pathlib; c=[p for p in sorted(pathlib.Path('eval/runs').glob('????-??-??-multihop_rag')) if p.name!='2026-09-03-multihop_rag']; assert len(c)==1, f'expected exactly one corrected run directory, found {[p.name for p in c]}'; d=c[0]; ws=d/'calibration_worksheet.jsonl'; rows=[json.loads(l) for l in ws.read_text(encoding='utf-8').splitlines() if l.strip()]; hdr=[r for r in rows if r.get('type')=='header']; body=[r for r in rows if r.get('type')!='header']; assert len(hdr)==1, 'exactly one header row expected'; miss=[r.get('question_id') for r in body if not r.get('cache_key') or r.get('human_groundedness') is not None or r.get('human_faithfulness') is not None]; assert not miss, f'rows unfit for human scoring: {miss}'; cache=json.load(open(d/'judge_cache.json',encoding='utf-8')); v=sum(1 for e in cache.values() if isinstance(e,dict) and e.get('verdict')); print('RUN',d.name,'WORKSHEET_ROWS',len(body),'CACHED_VERDICTS',v)"</automated>
    <fails_when>The `WORKSHEET_ROWS` line is absent from stdout, or the command exits non-zero on `expected exactly one corrected run directory`, on `exactly one header row expected`, or on `rows unfit for human scoring` (a row missing its cache key, or with either human score field already filled). A `WORKSHEET_ROWS` count below the committed calibration size is **not** a failure — it is the recorded-shortfall branch, and the acceptance criteria require the causing bound to be named rather than the count to be raised.</fails_when>
  </verify>
  <acceptance_criteria>
    - The judge cache's verdict count, the provider spend and the remaining allowance are recorded immediately before and immediately after the stage.
    - The judged pass ran at the derived slice size with `--stage-cap` carrying the judging figure, and the summary records the exact invocation including the sample scope and the cap, so the feed-back invocation can reproduce it.
    - Observed stage spend and observed cost per judged question are recorded against the judging cap and against the per-question cost input `p` the derivation used, with a statement of whether `p` was optimistic and by how much; the slice is **not** resized after the fact to make the figures agree.
    - If the judge loop stopped at the cap rather than at the end of the slice, the summary records the stop, the verdicts cached at that point, and that the stop was taken by the loop's own rule.
    - The counts of records judged, skipped for want of evidence, and judge errors are recorded as observations, with the superseded run's skip count (98 of 100) named as the comparison point for why a populated slice matters.
    - The worksheet's scoring-row count is recorded and compared against the committed calibration size; a shortfall names the bound that caused it — including a judge-error shortfall where `N >= K` but fewer than `K` records carried a cached verdict. The summary states that a shortfall is a recorded outcome and that the judged pass was not re-run to fill it.
    - The summary records the run directory as resolved by the exclude-the-superseded-run recipe, and states that it can never return `eval/runs/2026-09-03-multihop_rag/`.
    - The verify command exits clean: exactly one header row, and every scoring row carries a cache key with both human score fields present and unfilled.
    - If the worksheet carries no scoring rows, the summary records that as the outcome, states that the checkpoint is not reached, and states that the judged dimensions will carry the no-calibration label.
    - Nothing from the superseded run appears in the corrected run's cache, worksheet or scores, and `git diff --quiet -- eval/runs/2026-09-03-multihop_rag/` exits clean.
  </acceptance_criteria>
  <done>
    A genuinely populated judged slice exists for the first time in this project, inside its own
    spend cap, and the worksheet a human is about to fill in has been checked to be fillable.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: Human calibration — the scores no agent may supply</name>
  <files>eval/runs/2026-XX-XX-multihop_rag/calibration_completed.jsonl</files>
  <precondition>The calibration worksheet exists with at least one scoring row, every row carries a cache key and both human score fields empty, and the judged verdicts those rows will be compared against are already cached.</precondition>
  <reversibility rating="reversible">The scores are data the human can revise before feed-back. What is not reversible is doing it wrong: a back-filled or copied score silently converts the agreement figure from a measurement into a tautology.</reversibility>
  <what-built>
    The corrected run has been driven across the whole committed sample, reconciled, published, and
    judged at a slice size derived from what it actually produced. The calibration worksheet has been
    emitted at the committed calibration size, and every row carries the question, the generated
    answer, the evidence the judge saw, and the cache key that identifies the judge's verdict for it.
    Both human score fields on every row are present and empty.
  </what-built>
  <how-to-verify>
    1. Open the calibration worksheet in the corrected run directory and read its scoring rows.
    2. Confirm the row count against the committed calibration size recorded in `gate.py`, and
       against the shortfall note in `06.3.4-05-SUMMARY.md` if there is one.
    3. Confirm each row carries a question, an answer and the evidence, and that no human score
       field is pre-filled.
    4. Score each row twice — groundedness and faithfulness, each on the rubric's integer scale —
       writing the completed rows to the completed-calibration file in the same run directory.
       Expect roughly forty to fifty minutes; the committed size was set against exactly that.
    5. Do not consult the judge's verdict for a row before scoring it, and do not carry over any
       score from the superseded run. Both would destroy the measurement this exists to take.
  </how-to-verify>
  <decision>
    Whether the calibration sitting is complete — every worksheet row carries both human scores —
    and execution may proceed to compute agreement; or whether it is partial or declined, in which
    case the judged numbers are published with the calibration state that actually holds.
  </decision>
  <context>
    This is a `checkpoint:decision` rather than a `checkpoint:human-verify` deliberately and
    structurally. This repository sets human verification to be batched at the end of a phase; a
    verification checkpoint here would be folded into that batch and never halt, and execution would
    run on past the one step in this entire family that no agent is permitted to perform. A decision
    checkpoint always halts. The same mistyping was caught and corrected in sibling 06.3.3.

    D-48 is explicit: calibration is performed fresh, by a human, on the corrected run. There is no
    agent back-fill. The superseded run's sixteen scores judged answers that are being replaced, and
    98 of 100 sampled questions there were skipped for want of evidence — those scores are not a
    starting point, a prior, or a fallback.

    Present, before asking: the worksheet path and its scoring-row count; the committed calibration
    size and where it is committed; the expected effort; the two dimensions to score and their
    scale; and the agreement target the resulting scores will be measured against, together with
    what happens below it — the judged numbers are published under an explicit uncalibrated label,
    not withheld and not quietly reported as though calibrated.

    A partial sitting is an acceptable answer and must be recorded as one. Fewer completed rows
    means a weaker agreement figure over fewer pairs, which the report will say. What is not
    acceptable is a full-looking file that a human did not fill in.
  </context>
  <options>
    <option id="complete">
      <name>Complete — every worksheet row scored on both dimensions</name>
      <pros>Agreement is computed over the full committed set of pairs, which is the basis the target was set against. The judged half of the benchmark becomes citable for the first time.</pros>
      <cons>One uninterrupted sitting of roughly forty to fifty minutes, on unfamiliar text, where rushing degrades exactly the measurement being taken.</cons>
    </option>
    <option id="partial">
      <name>Partial — some rows scored, the rest left empty and recorded as such</name>
      <pros>Agreement over fewer pairs is still a measurement, and an honest small sample beats a padded one. The completed count is printed in the report, so a reader sees what it rests on.</pros>
      <cons>A small pair count makes both statistics unstable, which makes falling below the target more likely and the uncalibrated label more likely to apply.</cons>
    </option>
    <option id="declined">
      <name>Declined — no human scoring this cycle</name>
      <pros>Honest. The judged dimensions are published with the no-calibration label, which is a state the report distinguishes from a failed calibration.</pros>
      <cons>The judged numbers cannot be cited as calibrated in Phase 6.4, and the judged provider spend already incurred buys less than it could have.</cons>
    </option>
  </options>
  <resume-signal>
    Reply with the chosen option and, for the complete or partial branches, confirm the completed
    calibration file has been written into the corrected run directory and how many rows carry both
    scores. The reply, the row count and the date are recorded in `06.3.4-FINDINGS.md` as the
    calibration attestation.
  </resume-signal>
  <acceptance_criteria>
    - Execution genuinely halted at this task and did not proceed to task 4 before a human reply was received; the summary records the halt.
    - The completed calibration file exists in the corrected run directory on the complete or partial branch, and every row it marks as scored carries both human scores as integers within the rubric's scale.
    - The count of rows carrying both scores is recorded, together with the committed calibration size, and any shortfall is stated as a shortfall.
    - The attestation states plainly that the scores are human-authored on the corrected run, that no agent wrote, completed or inferred any score, and that no score from the superseded run was carried forward.
    - On the declined branch, the summary records the decision and that the judged dimensions will carry the no-calibration label rather than an agreement figure.
    - The worksheet file itself is unchanged by the scoring; the human's scores land in the completed file.
  </acceptance_criteria>
  <done>
    A human has scored the corrected run's answers, freshly, and said so — or has said they did not,
    which the report will state just as plainly.
  </done>
</task>

<task type="auto">
  <name>Task 4: Feed the scores back, compute agreement, and label what it licenses</name>
  <files>eval/runs/2026-XX-XX-multihop_rag/report.json, eval/runs/2026-XX-XX-multihop_rag/report.md, .planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-FINDINGS.md</files>
  <read_first>
    - `eval/src/lancet_eval/score.py:332-356` — the two calibration guards, which dictate that the feed-back invocation runs with judging enabled and at a sample scope no narrower than the cached verdict count.
    - `eval/src/lancet_eval/score.py:448-519` — the calibration block: the header version check, the blank-score rejection at 487, the scale check, and the matching of a row to its cached verdict by cache key.
    - `eval/src/lancet_eval/agreement.py` and the wiring 06.3.4-02 landed — the two statistics, the committed target, the exclusion counting, and specifically **where the calibration state and caveat live**: the `calibration_*` floats in each judged dimension's `detail` (including the three-valued `calibration_state`) and the human-readable sentence in `RunMetadata.notes`. They are **not** in `DimensionResult.reason` — `dimensions.py:28-41` forbids a `reason` on a `status="ok"` row, and `report.md.j2:38-42` does not render one.
    - `eval/src/lancet_eval/gate.py` — the committed agreement target and calibration size.
    - Task 2's record of the exact judged invocation, its sample scope, its `--stage-cap` figure and the cache verdict count.
  </read_first>
  <action>
    Feed the human's scores back and republish, without spending anything further.

    Run the scoring pass again with judging enabled, at the same sample scope and seed as task 2's
    judged pass, supplying the completed calibration file, and passing the same `--stage-cap` figure
    (the cap is required whenever judging is enabled; this invocation should spend nothing, and the
    cap plus the unchanged verdict count together are what prove it). Judging enabled is not optional: the
    harness refuses a calibration file fed back with judging off once verdicts are cached, precisely
    because rewriting the report without them would silently discard a paid judged population. The
    sample scope must match too — the judged subset is a seeded selection rather than a prefix, so a
    different scope selects different questions, misses the cache, and pays for verdicts twice.

    Record the cache's verdict count before and after this invocation and confirm it is unchanged.
    That is the check that proves the feed-back was free and that the judged population it published
    is the one that was paid for.

    Read out and record: both agreement statistics, the count of pairs they were computed over, the
    count of rows excluded for missing one side, the completed human dual-score count, and the
    committed calibration size — each with the field or detail key it came from. Compare each
    statistic against the committed target under its stated boundary semantics, where a value
    exactly at the target satisfies it.

    Then confirm what the report says about it, checking the surfaces 06.3.4-02 actually built.
    When either statistic is below the target, both judged dimensions must carry
    `detail["calibration_state"] == 1.0` and `RunMetadata.notes` must carry the sentence naming the
    statistic and its value; when no calibration was performed at all, `calibration_state` must read
    `0.0` and `notes` must carry the different, no-calibration sentence. The two states must be
    distinguishable from the machine-readable report alone, without parsing prose. Read the rendered
    `report.md` and the machine-readable `report.json` and record that the state and the sentence
    appear in both, or that they correctly do not. A judged number below the target published without
    its state is the failure this whole sequence exists to prevent.

    Do **not** expect the caveat in `DimensionResult.reason` and do not add it there if it is
    missing: `_validate_consistency` (`dimensions.py:28-41`) raises `status 'ok' cannot have a
    reason`, and a scored judged dimension is always `ok`. If the caveat appears to be absent,
    the fault is in the `detail`/`notes` wiring, never a case for relaxing that validator.

    Publication here is again two commands — `score` writes `report.json`, and `report` writes
    `report.md` and `metadata.json` (`cli.py:423-432`). Run both, or `report.md` and `metadata.json`
    will still carry 06.3.4-04's judging-off content while `report.json` carries the judged one.

    Open `06.3.4-FINDINGS.md` and write its first section: the calibration attestation and the
    agreement result. Carry the human's attestation from the checkpoint verbatim in substance — that
    the scores are fresh, human-authored on the corrected run, with no agent back-fill and nothing
    carried over from the superseded run — together with the pair count, both statistics, the target,
    the outcome of each comparison, and the resulting calibration state of the judged dimensions.
  </action>
  <verify>
    <automated>uv run --project eval python -c "import json,pathlib; c=[p for p in sorted(pathlib.Path('eval/runs').glob('????-??-??-multihop_rag')) if p.name!='2026-09-03-multihop_rag']; assert len(c)==1, f'expected exactly one corrected run directory, found {[p.name for p in c]}'; d=c[0]; r=json.load(open(d/'report.json',encoding='utf-8')); m=r.get('metadata') or {}; assert m.get('partial') is False, m; assert m.get('calibration_size_committed') is not None, 'calibration_size_committed absent from run metadata'; assert m.get('calibration_completed_n') is not None, 'calibration_completed_n absent from run metadata'; assert (d/'report.md').is_file(); assert (d/'metadata.json').is_file(), 'metadata.json missing - the report command was not run'; jd=[x for x in (r.get('dimensions') or []) if x.get('name') in ('groundedness','faithfulness')]; assert jd, 'no judged dimensions in report'; assert all(x.get('reason') is None for x in jd if x.get('status')=='ok'), 'judged ok-dimension carries a reason'; assert all('calibration_state' in (x.get('detail') or {}) for x in jd), 'calibration_state absent from a judged dimension detail'; cache=json.load(open(d/'judge_cache.json',encoding='utf-8')); v=sum(1 for e in cache.values() if isinstance(e,dict) and e.get('verdict')); print('REPUBLISHED size_committed=',m.get('calibration_size_committed'),'completed_n=',m.get('calibration_completed_n'),'states=',[ (x.get('detail') or {}).get('calibration_state') for x in jd],'cached_verdicts=',v)"</automated>
    <fails_when>The `REPUBLISHED` line is absent from stdout, or the command exits non-zero on any of its assertions: `expected exactly one corrected run directory`; a metadata `partial` that is not `False`; `calibration_size_committed absent from run metadata` or `calibration_completed_n absent from run metadata` (one of the two reserved fields was renamed or collapsed); a missing `report.md` or `metadata.json missing - the report command was not run`; `no judged dimensions in report`; `judged ok-dimension carries a reason` (the caveat was routed into the field the schema forbids); or `calibration_state absent from a judged dimension detail` (the caveat has no machine-readable surface). Separately, a `cached_verdicts` count differing from the one task 2 recorded means the feed-back paid for verdicts again.</fails_when>
  </verify>
  <acceptance_criteria>
    - The feed-back invocation is recorded in full and matches task 2's judged invocation in sample scope and seed; the summary states why both had to match.
    - The judge cache's verdict count before and after the feed-back is recorded and unchanged, and the summary states this as the evidence that no further provider call was made.
    - Both agreement statistics (`detail["calibration_kappa"]`, `detail["calibration_spearman"]`), the pair count (`calibration_pairs_n`), the excluded-row count (`calibration_excluded_n`), `metadata.calibration_completed_n` and `metadata.calibration_size_committed` are recorded, each naming the field or detail key it came from.
    - Each statistic is compared against the committed target with the comparison outcome stated, and a value exactly at the target is recorded as satisfying it. A statistic that came back undefined is recorded by its `*_state` companion key rather than as a zero.
    - The summary records whether the calibration caveat appears in both the rendered and the machine-readable report — as `detail["calibration_state"]` in `report.json` and as the `RunMetadata.notes` sentence in `report.md` — and that its presence or absence matches the comparison outcome.
    - The summary records that no judged dimension with `status == "ok"` carries a `reason`, and that `_validate_consistency` was not relaxed to permit one.
    - Where no calibration was performed, the summary records that the judged dimensions carry `calibration_state == 0.0` and the no-calibration sentence rather than an agreement figure, and that this is distinguishable from the below-target state (`1.0`) without reading prose.
    - Both `score` and `report` were run, so `report.json`, `report.md` and `metadata.json` all reflect the judged population rather than 06.3.4-04's judging-off content.
    - `06.3.4-FINDINGS.md` exists and its calibration section carries the attestation, the pair count, both statistics, the target, both comparison outcomes and the resulting calibration state.
    - The verify command exits clean, showing the republished report carries both reserved metadata fields and a `calibration_state` on each judged dimension.
    - The two existing calibration guards were not weakened or bypassed; the summary records that the invocation was shaped to satisfy them.
  </acceptance_criteria>
  <done>
    The judged numbers now carry a measured statement of how far the judge and a human agreed, and
    a label that says what that licenses — including when the answer is that they did not agree.
  </done>
</task>

</tasks>

<out_of_scope>
| Item | Owner | Why not here |
|---|---|---|
| The full two-arm drive and the first publication of the report | 06.3.4-04 | This plan judges; it does not drive. |
| The graph-yield disposition and the paired-comparison disposition | 06.3.4-07 | They sit entirely after this plan's paid-judged / human-halt / feed-back unit and depend only on 06.3.4-04's report and this plan's own findings. Split out so the family's two headline dispositions are taken on fresh context; see the sizing-exception entry in this plan's ledger. |
| The two agreement statistics themselves and the uncalibrated label's implementation | 06.3.4-02 | Built before any human score existed, deliberately. This plan uses them. |
| The committed floors, the trigger, the calibration size and the agreement target | 06.3.4-01 | Committed before any observation. Read here, never restated. |
| Pairing the judged dimensions across arms | Excluded by D-39 | It would double judged spend for no answer this family needs. |
| Fixing the graph's traversal quality, fan-out, depth or reranking | Out of scope for the whole family (D-03) | The bar is that it runs and outright bugs are fixed, not that it performs well. A poor result is reported, not tuned away. |
| Re-seeding the eval corpus | Out of scope (D-05) | A store-population finding is reported as an ingest finding, not resolved by re-seeding here. |
| The root-cause document, the `docs/` note and the ROADMAP edit | 06.3.4-06 | They quote the dispositions 06.3.4-07 writes into this file, and therefore follow both plans. |
| Any change to `eval/runs/2026-09-03-multihop_rag/` | Nobody — prohibited by D-45 | Its sixteen scores and two cached verdicts are evidence, not a starting point. |
</out_of_scope>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This plan's contribution. The complete phase inventory is the `artifacts_this_phase_produces`
section of `06.3.4-01-PLAN.md`.

| Kind | Artifact |
|---|---|
| Modified Python modules | `eval/src/lancet_eval/score.py`, `cli.py` — the worksheet size as an explicit parameter and option, the corrected worksheet-selector description, and the judged-path spend-cap stop rule |
| Modified schema surface | `eval/src/lancet_eval/report.py` (new `RunMetadata.calibration_size_committed`), `eval/src/lancet_eval/templates/report.md.j2` and `eval/report.schema.json` — all three move together because `RunMetadata` forbids extra fields |
| Modified Python module | `eval/src/lancet_eval/gate.py` — the judged-slice derivation rule mirrored beside the other committed decision inputs |
| New tests | `eval/tests/test_calibration_worksheet_size.py` |
| New run artefacts | `eval/runs/<dated>-multihop_rag/calibration_worksheet.jsonl`, `calibration_completed.jsonl`, an extended `judge_cache.json` |
| Republished run artefacts | `eval/runs/<dated>-multihop_rag/report.json` (from `score`), plus `report.md` and `metadata.json` (from `report`) — with the judged population, both agreement statistics and the calibration state |
| New phase record | `06.3.4-FINDINGS.md` — created here with its first section, the calibration attestation and the agreement result. 06.3.4-07 appends the graph-yield and paired-comparison dispositions beneath it. |
| New CLI flags | Two options on the `score` command: the calibration worksheet's row count (defaulting to the existing behaviour), and `--stage-cap` (required whenever judging is enabled, no default). |
| Reserved metadata field names | `calibration_size_committed` (this plan). `calibration_completed_n` is reserved by 06.3.4-02 and must not be renamed or reused here. |
| New dependencies | None. |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human ratings → published judged numbers | The single step in this family no agent may perform; a fabricated score turns the agreement figure into a tautology. |
| derived slice size → paid judged stage | A number derived from a measurement authorises spend; a chosen number authorises whatever the chooser preferred. |
| eval harness → paid provider | The judged pass issues real judge calls against a metered external service. |
| judge cache → feed-back invocation | A mismatched sample scope silently misses the cache and pays for the same verdicts twice, or discards a paid population. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.4-23 | Spoofing | human calibration ratings | high | mitigate | The halt is a `checkpoint:decision` with a blocking human gate, which this repository's end-of-phase verification batching cannot defer; the attestation states the scores are human-authored with no back-fill; the worksheet is verified to carry empty score fields before the human sees it; no code path in this plan writes a human score. |
| T-06.3.4-24 | Tampering | judged slice size | high | mitigate | The derivation rule is committed in this plan file before the drive it consumes has run, mirrored into `gate.py` under test, and the applied result is recorded with every input and the binding bound. |
| T-06.3.4-25 | Denial of Service | judged provider spend | high | mitigate | `--stage-cap` is required on `score` whenever judging is enabled, with no default, and the judge loop (`score.py:390-446`) breaks once accumulated judged spend reaches it — `judge_once` has no spend check of its own, so the derived `N_cap` alone was a pre-estimate rather than an enforcement. `p` is recorded as written-out arithmetic before the pass, so an optimistic bound is diagnosable. Spend and allowance are read before and after; the feed-back invocation reuses the cache and its verdict count is asserted unchanged. Severity raised from medium after cross-AI review found `score` had no fail-closed cap. |
| T-06.3.4-38 | Tampering | run-directory resolution | high | mitigate | Every command in this plan resolves the run directory by excluding `2026-09-03-multihop_rag` from the date glob and requiring exactly one remaining candidate, halting on zero or two. A newest-match resolution here would judge and republish over the superseded run's artefacts. |
| T-06.3.4-39 | Tampering | calibration caveat surface | high | mitigate | The caveat is read from `detail["calibration_state"]` and `RunMetadata.notes`, the surfaces 06.3.4-02 built. This plan neither writes it into `DimensionResult.reason` nor relaxes `_validate_consistency` to permit it; a `must_haves` prohibition forbids both, and task 4's verify asserts every judged `status="ok"` dimension carries `reason is None` while still carrying a `calibration_state`. |
| T-06.3.4-26 | Tampering | judged population on feed-back | high | mitigate | The feed-back runs with judging enabled at the same sample scope and seed; the harness's two existing calibration guards are left intact and the invocation is shaped to satisfy them rather than the reverse. |
| T-06.3.4-28 | Information Disclosure | worksheet and findings contents | low | accept | Worksheet rows and findings carry public news-corpus text and model output; no credential or personal data is present, and the provider key is read from the environment and never written into a run artefact. |
| T-06.3.4-SC | Tampering | package-manager installs | high | mitigate | No install task exists. The stated evaluation contract forbids new Python dependencies; both agreement statistics are standard library, and `uv lock --project eval --check` proves the lock is unchanged. |
</threat_model>

<verification>
- `uv run --project eval pytest` passes; the summary records the test count before and after.
- `uv run --project eval ruff check --preview eval/src eval/tests` is clean.
- `uv lock --project eval --check` confirms no dependency was added.
- The calibration worksheet's scoring-row count is recorded against the committed calibration size, and every row carries a cache key with both human score fields present and unfilled before the checkpoint.
- Execution halted at the human-scoring checkpoint and resumed only on a human reply.
- The judge cache's verdict count is unchanged across the feed-back invocation.
- The judged pass and the feed-back invocation both carried `--stage-cap`; the judged stage's observed spend is recorded against it and against the recorded `p`.
- The republished report carries `metadata.calibration_size_committed`, `metadata.calibration_completed_n`, both agreement statistics in each judged dimension's `detail`, and `detail["calibration_state"]` — with no judged `status="ok"` dimension carrying a `reason`.
- `report.json`, `report.md` and `metadata.json` all reflect the judged population, because both `score` and `report` were run.
- `06.3.4-FINDINGS.md` exists and carries its calibration section, with each observed value naming its source key. The two disposition sections are 06.3.4-07's and are not expected here.
- `git diff --quiet -- eval/runs/2026-09-03-multihop_rag/` exits clean.
</verification>

<success_criteria>
- The judged slice size is a recorded consequence of the drive's observed judgeable-record count and the judging cap, computed by a rule that predates both.
- A human scored a freshly emitted worksheet at the committed calibration size, with no agent back-fill and nothing carried over from the superseded run, at a checkpoint that genuinely halted.
- Judge-versus-human agreement is measured on both required statistics and compared against the committed target, and judged numbers below it are published under an explicit uncalibrated label.
- `06.3.4-FINDINGS.md` exists carrying the calibration attestation and the agreement result, ready for 06.3.4-07 to append both dispositions beneath.
</success_criteria>

<output>
Create `.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-05-SUMMARY.md` when done.
</output>

## Landed sibling contracts (supersedes the original revise-before-execute table)

**Siblings 06.3.1, 06.3.2 and 06.3.3 have all executed and completed.** Read the landed values as
facts. Verified against the working tree during the cross-AI review round (`3bbf435`).

| Formerly-unavailable input | Landed value / location |
|---|---|
| The corrected run's records, its published report and the judgeable-record count the slice size is derived from | **06.3.4-04**. Still measured, nowhere assumed. The fallback path — computing `J` from the journal by the judge loop's own predicate — remains legitimate and is not a degradation. |
| The two agreement statistics, the exclusion counting, the completed-score count in metadata and the calibration caveat | **06.3.4-02**, on surfaces the schema admits: `detail["calibration_kappa"]`, `detail["calibration_spearman"]`, `detail["calibration_state"]`, `detail["calibration_pairs_n"]`, `detail["calibration_excluded_n"]`, `metadata.calibration_completed_n`, and the human-readable sentence in `RunMetadata.notes`. **Not** in `DimensionResult.reason` — `_validate_consistency` (`dimensions.py:28-41`) rejects a `reason` on a `status="ok"` row, which both reviewers reproduced empirically. |
| The committed calibration size, the agreement target, the yield floor, the complement trigger, their boundary semantics and the per-stage cap reader | **06.3.4-01**, in `gate.py`. Read from there; nothing here restates them. |
| The graph population counts and the zero-count reclassification verdict | **Answered (06.3.3).** `06.3.3-STORE-BASELINE.md`: `graph_disposition: populated`, `unpopulated: false` — the store-suspension branch **will not fire** and the yield floor applies. The branch stays in the disposition as a guard; the summary records that it did not fire and why. |
| The judging-stage spend cap and the remaining allowance | **Landed (06.3.3, D-47).** Judging cap **$5.00**, as amended by any judging cap recorded at 06.3.4-03's decision, under the clause requiring enforcement with no silent default. Remaining daily allowance at the go decision was ~$14.95. |
| The dimension and detail key names for graph presence, graph influence, the no-match complement, pairing coverage and the paired deltas | **Landed (06.3.2).** `graph_presence_rate` (score) with `detail["no_match_rate"]` (`dimensions.py:450-537`), `graph_influence_rate` (`dimensions.py:539-589`), `graph_ablation_delta` with `detail["pairing_coverage"]` (`dimensions.py:95-129`). An absent key still halts the disposition and is recorded as absent, never read as a zero. |
| The corrected worksheet candidate selector | **Landed.** `score.py:547-568` keeps only rows with a cached verdict and stops at a hardcoded `20`; the comment at 545 claiming selection "across query types" is inaccurate and this plan corrects it without changing the selection basis. Task 2 still asserts the emitted row count against the committed size rather than assuming it. |
| Whether the judge prompt version changed | **Check at execute time** against the cache header before the judged pass; a bump is a recorded cost, not a surprise. |
| Observed agreement between judge and human | **Still unavailable — by design.** The human, at task 3, for the first time on answers that actually exist. The superseded run's sixteen scores cannot carry forward (D-48). |
| Observed graph yield and no-match rate | **06.3.4-04**'s full drive. The broken run gave 494 graph timeouts against only 4 unavailable results, so the true rate has never been observed. |
| Whether the graph makes results worse | **This plan's** paired-comparison disposition (D-49), on 06.3.4-04's published delta. |
| Actual judged spend | **This plan.** The superseded run's `$0.0199` is not a usable estimate — that run was cheap because 966 of its queries generated nothing, and its judged pass therefore scored almost nothing. Verified against that run's own artefacts: its worksheet and its judge cache each hold sixteen entries, matching the sixteen human scores the decision record names — not the smaller figure `06.3.4-PATTERNS.md` states. Recompute rather than inherit any figure from that document. |
| Whether a sibling already parameterised the calibration worksheet's row count | **No.** `score.py:567` still carries the literal `20`. 06.3.2-05 examined the same block and recorded that the row count belongs here under D-48. |
| Whether a sibling already added a fail-closed cap to the judged path | **No.** `score` has no `--stage-cap` and `judge_once` has no spend check. Task 1 adds both. |

## Review Dispositions Ledger

### Round 1 — 3bbf435

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| Task 4's uncalibrated label has nowhere legal to live until 06.3.4-02 is corrected; republishing judged dimensions with a `reason` raises a `ValidationError` (cursor L185@3bbf435; antigravity L516-517@3bbf435; consensus #3 L587@3bbf435) | HIGH | Resolved upstream in 06.3.4-02 (detail floats plus `RunMetadata.notes`, `DimensionResult` untouched) and consumed here explicitly: Task 4's `read_first`, action and acceptance criteria now name `detail["calibration_state"]` and the `notes` sentence as the surfaces to check, and forbid adding the caveat to `reason` or relaxing `_validate_consistency` if it appears absent. Task 4's verify asserts every judged `status="ok"` dimension has `reason is None` **and** carries a `calibration_state`. New threat `T-06.3.4-39`; new `must_haves` prohibition. |
| `files_modified` omits `report.py` and the template; `RunMetadata` is `extra="forbid"` so the committed-size field cannot exist without them (cursor L186@3bbf435) | MEDIUM | Plan 05: `eval/src/lancet_eval/report.py`, `eval/src/lancet_eval/templates/report.md.j2` and `eval/report.schema.json` added to `files_modified`, to the artifacts table and to Task 1's `read_first`; Task 1's action states all three move together or `test_schema.py` fails; the verify command runs `test_schema.py` and `test_report.py`. |
| The metadata field name `calibration_n` is not reserved in 02 and must not collide (cursor L186, L193@3bbf435) | MEDIUM | Names fixed on both sides before either lands: this plan reserves `calibration_size_committed`, 06.3.4-02 reserves `calibration_completed_n`. Task 4's verify is re-pointed off `calibration_n` and now asserts **both** fields present and distinct, with a dedicated failure message per field. Recorded in both plans' artifacts tables and `must_haves`. |
| Judged spend cap is not fail-closed; `score` has no `--stage-cap` and an autonomous stretch of Task 2 will not interrupt (cursor L187@3bbf435; consensus #2 L586@3bbf435) | MEDIUM | Plan 05, Task 1 now adds `--stage-cap` to `score` — required whenever judging is enabled, not required under `--no-judge` — plus a stop rule on the judge loop at `score.py:390-446` following `measure.py:500-520`. Task 2 and Task 4 both pass it. Two new `<behavior>` items and acceptance criteria; `T-06.3.4-25` severity raised medium→high; new `must_haves` truth and prohibition. **Backward-compatibility guard added:** Typer cannot express a requirement contingent on another flag, so a naive `required=True` would make the cap mandatory on every invocation and retroactively break the `score --run <dir> --no-judge` command 06.3.4-04 Task 3 (wave 3) already published and which Task 4 re-runs. The option is therefore declared `float \| None = None` with the check raised in the command body, and a dedicated regression test pins 04's literal no-cap `--no-judge` invocation as still succeeding. |
| `N_cap = floor(C_j / p)` needs `p` before the judged pass, and `judge_once` has no mid-loop spend check (cursor L188@3bbf435) | MEDIUM | Plan 05: the `judged_slice_derivation` table's `p` row now requires a written-out arithmetic derivation — judge model id from `config.judge_model`, published input/output price with URL and date read, the token budget from `judge.py:30-33`, and the rounded-up figure — recorded **before** the pass. New acceptance criterion in Task 1 and a `must_haves` truth. The `--stage-cap` stop rule above is the mechanical backstop for a `p` that proves optimistic. |
| Worksheet may emit fewer than `K` rows if a transient judge error leaves a record uncached; the checkpoint must account for `K < 12` without failing verification (antigravity L518-519@3bbf435) | MEDIUM | Plan 05: a new row in the `judged_slice_derivation` defined-outcomes table names this exact case with `score.py:564-566` as its mechanism; Task 2's action and acceptance criteria state that a short worksheet is a recorded shortfall rather than a verification failure and that the judged pass is not re-run to fill it; Task 2's `<fails_when>` states explicitly that a low `WORKSHEET_ROWS` count is not a failure. New `must_haves` truth. |
| Worksheet comment vs selector: fix the "across query types" description and do not add stratification (cursor L189@3bbf435) | LOW | Already in the action; now also an acceptance criterion requiring the summary to record the correction and the deliberate decision not to stratify, with the reason (changing the selection basis mid-family would change what the judged population means). |
| Stale line citations throughout (`score.py:473-556`, `325-345`, `345-402`, `290-330`, `770-815`, `395-475`) | MEDIUM | Plan 05: Tasks 1 and 4 re-anchored against `3bbf435` — worksheet block `521-604` (with the hardcoded `20` at 567-568 and the misleading comment at 545), judged selection `358-389`, judge loop `390-446` (citation skip at 399-401), guards `332-356`, metadata `1027-1069`, calibration block `448-519`. `score.py:770-815` is flagged as judged-dimension assembly, not metadata. |
| Same resume glob inherited from 01/03/04 (consensus #1 L585@3bbf435) | HIGH | Plan 05: Task 2's `<precondition>` and every verify command use the exclude-the-superseded-run recipe; new threat `T-06.3.4-38` and a `must_haves` prohibition. |
| Revise-before-execute describes 06.3.1–06.3.3 as unexecuted (cursor L23@3bbf435; antigravity L385@3bbf435; consensus #5 L589@3bbf435) | MEDIUM | Replaced by the "Landed sibling contracts" table above, which also records that the store-suspension branch **will not fire** (store `populated`) so the yield floor applies. Same finding class handled in all six plans; see 06.3.4-01's ledger. |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| Confirm `test_calibration_worksheet_size.py` tests both `N < K` and `N = K` branches (antigravity L523@3bbf435) | **Already covered, no change needed.** Task 1's `<behavior>` requires "exactly that many rows when at least that many selectable records exist, and exactly as many as exist when fewer do", and an acceptance criterion requires a named test per behaviour. Recorded so the item is not mistaken for an omission. |
| Add stratification to the worksheet selector so it matches its description | **Rejected — not raised as a fix, and explicitly counter-indicated.** Cursor's L189 says to correct the description and *not* add stratification. Changing the selection basis midway through the family would change what the judged population means, and the committed size is small enough that stratification would not stabilise it. The comment is corrected; the code's behaviour is not. |
