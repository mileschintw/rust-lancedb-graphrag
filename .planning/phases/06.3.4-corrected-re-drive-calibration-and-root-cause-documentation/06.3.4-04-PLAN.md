---
phase: 06.3.4
plan: 04
type: execute
wave: 3
depends_on: [06.3.4-03]
files_modified:
  - eval/runs/2026-XX-XX-multihop_rag/journal.jsonl
  - eval/runs/2026-XX-XX-multihop_rag/report.json
  - eval/runs/2026-XX-XX-multihop_rag/report.md
  - eval/runs/2026-XX-XX-multihop_rag/metadata.json
autonomous: true
requirements: [OBS-05]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The full two-arm drive over the whole committed sample runs only after 06.3.4-03's dated decision entry records a decision to proceed and a full-drive spend cap, and the drive asserts that entry exists before issuing a query (D-44, D-47)."
    - "The full drive resumes into the same dated run directory the tracer and the staged drive opened, so the staged record set is a prefix of the full record set rather than a separate run that has to be reconciled."
    - "Provider spend is read immediately before and immediately after the stage and the observed stage spend is recorded against the cap the decision entry carries; a cap reached mid-drive stops the drive and is recorded, rather than being discovered afterwards (D-47)."
    - "The merged journal carries each work unit of the cross-product of the committed sample and the configured arms exactly once — no duplicate, no dropped unit — and the check is a computed comparison rather than an operator's assertion."
    - "Records driven during the staged stage keep the sampling flag they were written with, so a record remains attributable to the stage it came from after the two stages have merged into one journal (specless edge: ordering)."
    - "The journal carries at most one index generation across staged and full records, because the scorer refuses a journal that mixes generations and a mid-drive index rebuild would make the merged run unscorable."
    - "The journal's header moves from staged to publishable only when the completeness comparison passes; an incomplete journal is re-resumed for exactly the missing work units and publication waits, and the flag is never forced (specless edge: empty)."
    - "The published report is produced with judging switched off, so no judged spend is incurred in this plan and the judged stage stays separately capped (D-47)."
    - "Publication takes two commands, not one: `score --no-judge` writes `report.json` from the reconciled journal, and `report` is what writes `report.md` and `metadata.json`. A stage that runs only the scorer leaves two of the three published artefacts absent."
    - "The run directory this stage resumes into is resolved by excluding the superseded run from the date glob and requiring exactly one remaining candidate, matched against the literal pinned in 06.3.4-DRIVE-PRECONDITIONS.md; it is never resolved as the newest match, because that rule returns the superseded run whenever the corrected one is absent."
    - "The full drive runs under a fail-closed in-process spend cap carrying the figure 06.3.4-03's decision entry recorded, passed explicitly on the invocation; an autonomous thousand-work-unit drive stops itself at the cap rather than depending on anything watching it."
    - "The index-generation audit is taken on one field and stated once: the record's top-level index_generation, with records missing it counted separately as unattributable. The snapshot copy is corroboration, not a second denominator."
    - "Every number the published report carries is recorded as an observation with the dimension or detail key it came from; nothing in this plan asserts a value in advance except a threshold committed before the drive existed."
    - "A drive that lands zero usable pairs, or a report whose pairing join is empty, is published with that stated as the finding rather than suppressed or re-driven until it improves (specless edge: empty)."
    - "The 2026-09-03 run is untouched: the corrected records land in their own dated directory, neither run is merged into the other, and no file of the former is rewritten (D-45, specless edge: adjacency)."
  artifacts:
    - "eval/runs/<dated>-multihop_rag/journal.jsonl — extended to the full cross-product and reconciled to publishable"
    - "eval/runs/<dated>-multihop_rag/report.json and report.md — the published corrected report, scored with judging off"
    - "eval/runs/<dated>-multihop_rag/metadata.json — run provenance including the index generation read from the engine"
  key_links:
    - "The authorising decision, the full-drive cap and the spend projection come from 06.3.4-03's dated entry in 06.3.4-STAGED-GATE.md; this plan enforces them and does not restate them"
    - "The completeness comparison and the one-directional header reconciliation come from 06.3.4-01's work on journal.py and its reconcile command"
    - "The resolved dated run directory is the one recorded in 06.3.4-DRIVE-PRECONDITIONS.md; the resume resolver's date glob is what joins the three drives into one journal"
    - "The dimensions the published report carries, their denominators and their intervals are defined by 06.3.2; an absent dimension is recorded as absent, never defaulted"
    - "The judged slice this drive makes possible is sized by 06.3.4-05 from the usable-record count this drive produces; nothing here fixes that number"
  prohibitions:
    - "The full drive must not be invoked before 06.3.4-03's decision entry exists and records a decision to proceed with a full-drive cap"
    - "The full drive must not pass a question limit, because a limited invocation stamps its records with the sampling flag and destroys the attribution that distinguishes the staged stage from the full stage"
    - "The run directory must not be given a name outside the shape the resume resolver globs for; a decorated name silently forks a second run instead of resuming the first"
    - "No file already present in eval/runs/2026-09-03-multihop_rag/ may be rewritten, moved or deleted, and the two runs must never be merged into one directory or one report"
    - "The header's sampling flag must never be forced to publishable on an incomplete journal, and no record line may be rewritten, reordered or dropped to make a completeness comparison pass"
    - "A result that reflects badly on the system must not be withheld, re-driven or re-scored until it improves; publication is the default and any re-drive is a recorded act carrying its reason"
    - "No command in this plan may resolve the run directory by taking the newest match of the date glob; the resolution excludes the superseded run and requires exactly one remaining candidate, and a zero-candidate or multi-candidate result halts rather than picking one"
    - "The full drive must not be invoked without an explicit spend cap carrying the figure 06.3.4-03's decision entry recorded; an absent or unreadable cap is an unmet precondition that stops the stage, never a value the driver supplies for itself"
---

<objective>
Drive the whole committed sample across both arms into the run directory the staged stage already
opened, prove the merged journal is complete and undamaged, and publish the corrected report.

Purpose: this is the run Phase 6.4 documents for v1 milestone closure. Everything before it was
either machinery or a sample small enough to be thrown away. Three things therefore have to hold at
once: the spend must stay inside the cap a human set at 06.3.4-03 against numbers they could see;
the merged journal must be a clean superset of the staged prefix, because a duplicated or dropped
work unit corrupts every denominator downstream; and the report must actually render, which it
cannot do until the journal that began as a staged prefix is reconciled to publishable.

Publication happens here and deliberately not in 06.3.4-03. A staged journal cannot be rendered at
all, and staged numbers are gate inputs rather than results — publishing them would put a
provisional figure into the record under the same name the real one will carry.

Judging is not in this plan. It is its own capped stage owned by 06.3.4-05, so a judged-spend
overrun cannot ride in on the back of the deterministic drive.

Output: the completed journal, the reconciled header, and the published `report.json`, `report.md`
and `metadata.json` in the corrected run directory.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-CONTEXT.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-AI-SPEC.md
@.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-STAGED-GATE.md
@.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-DRIVE-PRECONDITIONS.md
@.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drive the whole committed sample across both arms, under the cap a human set</name>
  <files>eval/runs/2026-XX-XX-multihop_rag/journal.jsonl</files>
  <precondition>`06.3.4-STAGED-GATE.md` carries a dated decision entry choosing a go branch and naming a full-drive spend cap as a two-decimal USD figure this task can pass to `--stage-cap`; the engine and gateway are running against the isolated eval environment under the budgets 06.3.3 derived; the provider key is present; and nothing has ingested or rebuilt an index since the staged stage. Additionally, asserted before the first provider call and each halting the task: the run-directory resolution — exclude `2026-09-03-multihop_rag` from `eval/runs/????-??-??-multihop_rag`, require exactly one remaining candidate — returns the literal path pinned in `06.3.4-DRIVE-PRECONDITIONS.md`; that path is not `eval/runs/2026-09-03-multihop_rag/`; and this preflight's index generation equals the one `06.3.4-03-SUMMARY.md` recorded.</precondition>
  <reversibility rating="one-way">Provider spend across the full cross-product is unrecoverable, and the records become the run of record that Phase 6.4 documents for milestone closure. The authorising decision was taken deliberately at 06.3.4-03's closing checkpoint, which is why this task only asserts it rather than re-opening it.</reversibility>
  <read_first>
    - `.planning/phases/06.3.4-.../06.3.4-STAGED-GATE.md` — the dated decision entry: the option chosen, the full-drive cap, the projected spend, the remaining allowance at decision time, and any yield investigation carried forward.
    - `.planning/phases/06.3.4-.../06.3.4-DRIVE-PRECONDITIONS.md` — the resolved run-directory name, the sort-order verdict on the committed sample, and the standing constraint that nothing may ingest or rebuild an index mid-phase.
    - `.planning/phases/06.3.4-.../06.3.4-03-SUMMARY.md` — the staged stage's observed spend, its observed cost per query, the preflight transcript and the index generation the engine reported.
    - `eval/src/lancet_eval/cli.py:214-271` — `resolve_run_dir`'s date glob and newest-match rule (214-227), and the `run` command's options (230-271) including the required `--stage-cap` 06.3.4-01 added and the fact that `--limit` marks the run partial.
    - `eval/src/lancet_eval/run.py:155-240` — `drive`'s signature including `stage_spend_cap`, the work-unit cross-product (177-180), the resume key set, how `--limit` stamps `partial` on the header (173, 192) and on every record (139), the in-process cap stop rule, and the per-record append-and-flush discipline (`journal.py:126-131`) that makes an interrupted drive resumable.
    - `eval/src/lancet_eval/journal.py:45-131` — `RunRecord` (note `index_generation` at the record top level, 62), `journal_key` (70-72), `load_done` (75-103, whose `except` at 101-102 skips **every** unparseable line, not only a truncated trailing one), and `write_header`'s new-or-empty condition (113-124).
  </read_first>
  <action>
    Before issuing any query, assert the authorisation. Read the decision entry in
    `06.3.4-STAGED-GATE.md`, confirm it records a go branch, and record in the plan summary the
    option chosen, its date, the full-drive cap it carries and the remaining allowance it recorded.
    If the entry is absent, or records a hold, stop here and record that the drive was not
    authorised. Do not treat a passing staged gate as authorisation on its own; the gate produces
    numbers and the human produces the decision.

    Run the preflight and record its transcript, every canary row against its floor, and the index
    generation the engine reports. Compare that generation against the one the staged stage
    recorded. If they differ, something rebuilt an index between the two stages, the merged journal
    would be unscorable under the scorer's own mixed-generation guard, and the correct action is to
    stop and record the finding — not to drive over it.

    Resolve the run directory the way this phase resolves it everywhere — by exclusion, not by
    recency:

        RUN=$(uv run --project eval python -c "import pathlib; c=[p for p in sorted(pathlib.Path('eval/runs').glob('????-??-??-multihop_rag')) if p.name != '2026-09-03-multihop_rag']; assert len(c)==1, f'expected exactly one corrected run directory, found {[p.name for p in c]}'; print(c[0].as_posix())")

    Compare `$RUN` against the literal pinned in `06.3.4-DRIVE-PRECONDITIONS.md` and halt on any
    difference. Do **not** use `resolve_run_dir`'s newest-match and do **not** use a
    `ls -d ... | tail -1` subshell: both return `eval/runs/2026-09-03-multihop_rag/` whenever the
    corrected directory is missing, and this is the plan that would then extend the superseded
    journal to the full sample and reconcile its header to publishable — publishing the superseded
    run as the corrected one. The resolver also globs for a plain date-and-corpus name, so any
    decorated or suffixed directory name silently forks a second run instead of continuing the first.

    Read and record the provider's reported spend and remaining allowance immediately before the
    stage, by the same mechanism the staged stage used, so the two figures are comparable.

    Drive the full committed sample across both configured arms into that directory as a single
    resumed, unlimited invocation, under the cap the decision entry named:

        uv run --project eval lancet-eval run --corpus multihop_rag --out "$RUN/journal.jsonl" \
            --resume --stage-cap <full-drive cap from 06.3.4-STAGED-GATE.md's decision entry>

    Pass no question limit: the limit option stamps the sampling flag onto the header and onto every
    record it writes, and the staged records' flag is precisely what attributes a record to the
    stage it came from once the two stages share one journal. Resume is what makes this safe to run
    over an existing prefix — the resume key set skips work units already recorded, so no staged
    record is re-driven and no provider call is paid for twice.

    The cap is enforced **inside** `drive()` by the stop rule 06.3.4-01 added, checked before each
    dispatched work unit in both the sequential and the pooled branch. That mechanism is the control,
    not an operator's attention: this plan is `autonomous: true` and a thousand-work-unit drive at a
    graph-on p95 of 30.4 s runs for hours with nothing watching it. Records are appended and flushed
    per work unit (`journal.py:126-131`), so a capped stop loses nothing and resumes cleanly. If the
    cap stops the drive before the cross-product is complete, record how many work units were
    recorded and what was spent, and treat resuming as requiring a fresh recorded cap decision at
    06.3.4-03's checkpoint rather than a silent continuation under a raised figure.

    Read and record the provider's reported spend and remaining allowance immediately after the
    stage. Compute and record the stage's observed spend, its observed cost per query, and both
    against the cap and against the projection 06.3.4-03 made. State whether the projection held.
    Record the new work-unit count the drive reported, the resulting journal length, and the count of
    distinct work-unit keys, so a duplicated or dropped record is visible rather than inferred.
  </action>
  <verify>
    <automated>uv run --project eval python -c "import json,pathlib; c=[p for p in sorted(pathlib.Path('eval/runs').glob('????-??-??-multihop_rag')) if p.name!='2026-09-03-multihop_rag']; assert len(c)==1, f'expected exactly one corrected run directory, found {[p.name for p in c]}'; p=c[0]/'journal.jsonl'; rows=[json.loads(l) for l in p.read_text(encoding='utf-8').splitlines() if l.strip()]; recs=[r for r in rows if 'question_id' in r]; keys=[(r['corpus'],r['question_id'],r['graph_arm']) for r in recs]; raw=[r.get('index_generation') for r in recs]; gens={g for g in raw if g}; unattributable=sum(1 for g in raw if not g); snap_only=sum(1 for r in recs if not r.get('index_generation') and (r.get('snapshot') or {}).get('index_generation')); print('run',p.parent.name,'records',len(recs),'distinct_keys',len(set(keys)),'gens',sorted(gens),'unattributable',unattributable,'snapshot_only',snap_only); assert len(keys)==len(set(keys)), 'duplicate work-unit key'; assert len(gens)&lt;=1, f'mixed index generations {sorted(gens)}'; print('FULL_JOURNAL_OK')"</automated>
    <fails_when>The `FULL_JOURNAL_OK` line is absent from stdout, or the command exits non-zero on `expected exactly one corrected run directory` (zero candidates or a fork), `duplicate work-unit key`, or `mixed index generations`. A non-zero `unattributable` or `snapshot_only` count is **not** a failure — both are printed as findings to record, per the D-33 criterion below.</fails_when>
  </verify>
  <acceptance_criteria>
    - The summary records the authorising decision entry: its date, the option chosen, the full-drive cap and the remaining allowance it carried; and states that this was read before any query was issued.
    - The preflight transcript is recorded in full with every canary row against its floor, and the drive did not start on a failed canary.
    - The index generation the engine reported at this preflight is recorded verbatim and compared against the staged stage's; if they differ the summary records that the drive was stopped and why.
    - The resolved run directory is recorded as a literal path, matches the one pinned in `06.3.4-DRIVE-PRECONDITIONS.md`, and the summary records that it was obtained by the exclude-the-superseded-run recipe rather than by a newest match; the summary states explicitly that this resolution can never return `eval/runs/2026-09-03-multihop_rag/`.
    - The summary states that the drive was invoked with resume, with no question limit, and with `--stage-cap` carrying the figure from `06.3.4-STAGED-GATE.md`'s decision entry — quoting that figure and the entry's date — and gives the reason the limit was withheld in terms of record attribution.
    - The verify command exits clean: exactly one corrected run directory exists, no work-unit key appears twice, and the journal carries at most one distinct index generation.
    - **The index-generation audit is taken on the record's top-level `index_generation` field and stated once.** The count of records missing it (`unattributable`) is recorded in the summary; a non-zero count is a finding against the partial-snapshot provenance work owed by 06.3.1 (D-33), naming how many records of the run of record are unattributable — the same audit failure that produced 966 unattributable records in the superseded run. The verify command *additionally* prints `snapshot_only`, the count of records where the snapshot copy is populated but the top-level field is not; that count is recorded as corroboration, not as a second denominator, and the summary states which field the audit was taken on so this plan and 06.3.4-03 cannot report two different numbers for the same question.
    - Provider spend and remaining allowance are recorded immediately before and immediately after the stage; the observed stage spend and observed cost per query are computed from them and stated against both the cap and 06.3.4-03's projection, with a statement of whether the projection held.
    - If the cap stopped the drive mid-stage, the summary records the stop, the work units recorded at that point, the observed spend, and that resuming requires a fresh recorded cap decision at 06.3.4-03's checkpoint rather than a silently raised figure. The summary also states that the stop was taken by `drive()`'s own rule rather than by an operator noticing.
    - `git diff --quiet -- eval/runs/2026-09-03-multihop_rag/` exits clean.
  </acceptance_criteria>
  <done>
    The corrected system has been driven across the whole committed sample in both arms, into one
    dated directory, for a recorded cost inside a cap a human set against numbers they could see.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove the merged journal is complete and undamaged, then reconcile the header</name>
  <files>eval/runs/2026-XX-XX-multihop_rag/journal.jsonl</files>
  <read_first>
    - `eval/src/lancet_eval/journal.py` as landed by 06.3.4-01 — the completeness comparison, its cross-product construction, the one-directional header reconciliation and its atomic write.
    - `eval/src/lancet_eval/cli.py` as landed by 06.3.4-01 — the reconcile command, its arguments and what it prints when the journal is incomplete.
    - `eval/src/lancet_eval/score.py:117-186` — how a journal is loaded, the mixed-generation guard (173-178), and the document-map guard.
    - `eval/src/lancet_eval/score.py:1027-1069` — how the header's `partial` flag reaches `RunMetadata` (1040-1057) and the condition under which `report.json` is written (1065-1069). (Do NOT read `score.py:770-815` for this — that range is paired-ablation dimension assembly, not metadata.)
    - `.planning/phases/06.3.4-.../06.3.4-01-SUMMARY.md` — what the reconciliation was proven to do and what it deliberately does not do, and the literal corrected run directory path it pinned.
  </read_first>
  <action>
    Establish, before touching the header, that the journal is worth publishing.

    Run the completeness comparison through the reconcile command. It rebuilds the cross-product of
    the committed sample's question identifiers and the corpus's configured arms and compares it
    against the work-unit key set the journal actually carries. Record the verdict either way.

    If work units are missing, record how many and which — the command reports them — and re-resume
    the drive for exactly those units under the same recorded conditions, passing `--stage-cap` with
    the same figure from the decision entry (the cap is a required option; a re-resume is a paid
    invocation like any other and may not be run bare). Then re-run the comparison. Missing units are an expected branch, not an anomaly: a stage interrupted
    at the cap, or a transport failure on a handful of queries, lands here. What must not happen is
    the header being marked publishable while the comparison still fails. If the comparison cannot
    be made to pass, record the remaining gap as the finding and leave the journal staged; the
    report then does not render, and that is the correct outcome rather than a problem to route
    around.

    Once the comparison passes, run the reconciliation. It rewrites exactly one line — the header's
    sampling flag — through a temporary file and an atomic replace, and copies every record line
    through unchanged. Confirm afterwards that the record line count is unchanged from before, that
    the first line is the only line that differs, and that the staged stage's records still carry
    the sampling flag they were written with. That flag is the attribution mechanism; if it has been
    rewritten, the merged journal can no longer say which stage a record came from.

    Record the counts that make the merged journal auditable: total records, records per arm,
    records carrying the staged flag, records without it, and the count of distinct work-unit keys.
    These are observations. Do not compare them against any expected figure other than the
    cross-product the completeness comparison computed from the committed inputs.
  </action>
  <verify>
    <automated>RUN=$(uv run --project eval python -c "import pathlib; c=[p for p in sorted(pathlib.Path('eval/runs').glob('????-??-??-multihop_rag')) if p.name!='2026-09-03-multihop_rag']; assert len(c)==1, f'expected exactly one corrected run directory, found {[p.name for p in c]}'; print(c[0].as_posix())") &amp;&amp; uv run --project eval lancet-eval reconcile --run "$RUN" &amp;&amp; uv run --project eval python -c "import json,pathlib,sys; p=pathlib.Path(sys.argv[1])/'journal.jsonl'; lines=[l for l in p.read_text(encoding='utf-8').splitlines() if l.strip()]; h=json.loads(lines[0]); assert h.get('type')=='header', h; assert h.get('partial') is False, h; print('HEADER_PUBLISHABLE','records',len(lines)-1)" "$RUN"</automated>
    <fails_when>On the completeness-passed branch: the `HEADER_PUBLISHABLE` line is absent from stdout, or the command exits non-zero — from `expected exactly one corrected run directory`, from `reconcile` reporting missing work units, or from the header assertion still reading `partial: true`. On the **incomplete** branch a non-zero exit with `reconcile` naming the missing work-unit count is the gate working correctly, not a task failure; it is recorded as such per the acceptance criterion below and must never be resolved by forcing the flag.</fails_when>
  </verify>
  <acceptance_criteria>
    - The completeness verdict is recorded before the reconciliation was attempted, together with the cross-product size it was computed against and the source of both inputs.
    - If work units were missing, the summary records how many, that the drive was re-resumed for exactly those units, and the verdict of the re-run comparison; if a gap remained, the summary records it as the finding and states that the journal was left staged and the report was not rendered.
    - The record line count before and after the reconciliation is recorded and unchanged, and the summary states the check that established only the first line differs.
    - The count of records carrying the staged sampling flag is recorded and is unchanged by the reconciliation, and the summary states that this flag is what attributes a record to its stage.
    - The verify command exits clean and reports the header as publishable — **on the branch where the completeness comparison passed.** On the incomplete branch the command is expected to exit non-zero, because the reconciliation correctly refuses and the header correctly still reads as staged. That non-zero exit is the gate working, not the task failing; it is recorded as such together with the missing-key count, and it must never be resolved by forcing the flag, editing a record line, or bypassing the reconciliation.
    - Total records, records per arm, and distinct work-unit key count are recorded as observations, each compared only against the cross-product computed from the committed sample and the configured arms.
    - `git diff --quiet -- eval/runs/2026-09-03-multihop_rag/` exits clean.
  </acceptance_criteria>
  <done>
    The merged journal is provably the full cross-product, carries every record exactly once with
    its stage still attributable, and is marked publishable by a computed comparison rather than by
    anyone's assertion.
  </done>
</task>

<task type="auto">
  <name>Task 3: Score with judging off and publish the corrected report</name>
  <files>eval/runs/2026-XX-XX-multihop_rag/report.json, eval/runs/2026-XX-XX-multihop_rag/report.md, eval/runs/2026-XX-XX-multihop_rag/metadata.json</files>
  <read_first>
    - `eval/src/lancet_eval/cli.py:303-360` — the `score` command, its `--judge/--no-judge` default, and the fact that it calls `render_markdown` unconditionally after scoring.
    - `eval/src/lancet_eval/cli.py:363-440` — the **`report` command**, which is what writes `report.md` (`:423-427`) and `metadata.json` (`:431-432`), and its own partial guard at `:404-408`. `score_run` writes only `report.json` (`score.py:1065-1069`) — it does not write the other two, so a stage that runs only the scorer publishes one of the three artefacts this plan owes.
    - `eval/src/lancet_eval/report.py:145-200` — the two render guards (`render_markdown` 149-153, `render_json` 190-194), which now pass because the header has been reconciled.
    - `eval/src/lancet_eval/report.py:24-45` — `RunMetadata`'s fields, which are what `metadata.json` is asserted against.
    - `eval/src/lancet_eval/score.py:1027-1069` — how run metadata is assembled (1040-1057), including the index generation, the result hash and the `partial` flag. (Do NOT read `score.py:770-815` for this — that range is paired-ablation dimension assembly.)
    - `.planning/phases/06.3.2-.../06.3.2-02-PLAN.md` and `06.3.2-04-PLAN.md` — the dimension names, denominators, detail keys and intervals the published report is expected to carry, as those plans define them.
    - `.planning/phases/06.3.1-.../06.3.1-AI-SPEC.md` lines 922-996 — the pairing and coverage note including the below-one theoretical maximum, and the graph-yield note with its provisional status.
    - `.planning/phases/06.3-.../06.3-10-SUMMARY.md` — the recorded-run summary this plan's summary follows in shape, in particular its reviewer checklist, its spot-check and its methodological caveats.
  </read_first>
  <action>
    Score the reconciled journal with judging switched off and publish the report.

    Judging stays off here for two reasons and both matter: judged spend is a separately capped
    stage owned by 06.3.4-05, and the judged slice size is derived from the usable-record count this
    scoring pass is what produces. Scoring with judging on now would both spend outside its cap and
    fix the judged population before the number that sizes it exists.

    **Publication is two commands, in this order,** because the harness splits the work between
    them:

        RUN=$(uv run --project eval python -c "import pathlib; c=[p for p in sorted(pathlib.Path('eval/runs').glob('????-??-??-multihop_rag')) if p.name != '2026-09-03-multihop_rag']; assert len(c)==1, f'expected exactly one corrected run directory, found {[p.name for p in c]}'; print(c[0].as_posix())")
        uv run --project eval lancet-eval score  --run "$RUN" --no-judge
        uv run --project eval lancet-eval report --run "$RUN"

    `score_run` writes only `report.json` (`score.py:1065-1069`). `report.md` and `metadata.json`
    are written by the **`report` command** (`cli.py:423-432`). Running the scorer alone would leave
    two of the three artefacts this task owes absent, and every criterion below about
    `metadata.json` would be unsatisfiable. Both cross-AI reviewers flagged this; it is a harness
    fact, not a preference.

    Then confirm `metadata.json` carries the run's provenance — the index generation as the engine
    reported it, the commit hash, the models, the seed, the committed sample size, the result hash,
    the dependency lock hash and `partial: false` — by validating it field-by-field against
    `RunMetadata` (`report.py:24-45`) rather than eyeballing the file. The index generation is
    read from the engine and recorded verbatim; it is never hand-written and never copied from the
    superseded run, whose 966 unattributable records are part of why this family exists.

    Then read the published report and record, in the plan summary, what it actually says. Follow
    the shape `06.3-10-SUMMARY.md` established — the rendered report, the reviewer checklist, the
    reference-set spot-check — with the spot-check drawn from usable records. For each number
    record the dimension or detail key it came from. Include at least: the unusable-record count and
    rate with its interval; the per-path yields; the graph presence and influence rates; the pairing
    coverage with its pair count and the answerable-question count beside it; the paired ablation
    delta with its interval and its stratification by question type; and the paired latency and
    prompt-token deltas.

    State beside the coverage figure that its theoretical maximum is below one because null
    questions cannot enter the join, and give the answerable count, so the shortfall is not read as
    failure. If a dimension or detail key the report was expected to carry is absent, record it as
    absent and name it — do not substitute a zero, and do not describe an absent measurement as a
    measured zero.

    Publish what the run says, including if it says something unflattering. A negative ablation
    delta, a low graph presence rate, or a high unusable-record rate are all publishable outcomes;
    their disposition is 06.3.4-05's work and is not a reason to withhold, re-drive or re-score
    here. Note in the summary any number whose disposition is owed downstream, and where it will be
    taken.

    Record finally that the superseded run is untouched, and that the two runs remain two separately
    dated directories neither merged nor compared as though they were one measurement — the old
    ablation delta was computed by a different method on a nearly empty join and is not comparable.
  </action>
  <verify>
    <automated>RUN=$(uv run --project eval python -c "import pathlib; c=[p for p in sorted(pathlib.Path('eval/runs').glob('????-??-??-multihop_rag')) if p.name!='2026-09-03-multihop_rag']; assert len(c)==1, f'expected exactly one corrected run directory, found {[p.name for p in c]}'; print(c[0].as_posix())") &amp;&amp; uv run --project eval lancet-eval score --run "$RUN" --no-judge &amp;&amp; uv run --project eval lancet-eval report --run "$RUN" &amp;&amp; uv run --project eval python -c "import json,pathlib,sys; from lancet_eval.report import RunMetadata; d=pathlib.Path(sys.argv[1]); r=json.load(open(d/'report.json',encoding='utf-8')); assert (r.get('metadata') or {}).get('partial') is False, r.get('metadata'); assert (d/'report.md').is_file(), 'report.md missing'; assert (d/'metadata.json').is_file(), 'metadata.json missing — the report command was not run'; m=RunMetadata.model_validate(json.load(open(d/'metadata.json',encoding='utf-8'))); assert m.partial is False and m.index_generation, m; print('REPORT_PUBLISHED dimensions=',len(r.get('dimensions') or []),'index_generation=',m.index_generation)" "$RUN" &amp;&amp; git diff --quiet --exit-code -- eval/runs/2026-09-03-multihop_rag/ &amp;&amp; echo PUBLISH_OK</automated>
    <fails_when>The `PUBLISH_OK` line is absent from stdout, or the command exits non-zero — from `expected exactly one corrected run directory`, from `score`/`report` exiting non-zero (`Cannot render report for partial run` means the header was never reconciled), from `report.md missing`, from `metadata.json missing — the report command was not run`, from a `ValidationError` when `metadata.json` is validated against `RunMetadata`, from an empty `index_generation`, or from `git diff` reporting a modification under `eval/runs/2026-09-03-multihop_rag/`.</fails_when>
  </verify>
  <acceptance_criteria>
    - `report.json`, `report.md` **and** `metadata.json` all exist in the corrected run directory, and the summary records that `report.md` and `metadata.json` came from the `report` command rather than from the scorer, which writes only `report.json`.
    - `metadata.json` validates field-by-field against `RunMetadata` and carries `partial: false` and a non-empty `index_generation`.
    - The summary states that scoring ran with judging switched off, and gives both reasons — the separate judged cap and the derived judged-slice size.
    - `metadata.json` carries the index generation the engine reported, and the summary states it was read from the engine rather than written, and was not copied from the superseded run.
    - Every number recorded in the summary names the dimension or detail key it was read from; no number appears without one.
    - The pairing coverage is recorded with its pair count and the answerable-question count, and with the statement that its theoretical maximum is below one because null questions cannot enter the join.
    - Any dimension or detail key expected from 06.3.2 but absent from the published report is named as absent, and no absent measurement is recorded as a zero.
    - An unflattering result — a negative delta, a presence rate below the committed floor, or a high unusable rate — is recorded as published, with its disposition named as owed to 06.3.4-05 rather than acted on here.
    - If the pairing join produced no pairs at all, the summary records that as the finding and no coverage ratio is stated.
    - The summary records that the superseded run is untouched, that the two runs remain separately dated, and that the old ablation figure is not comparable with the new one.
    - `uv run --project eval pytest` still passes and `git diff --quiet -- eval/runs/2026-09-03-multihop_rag/` exits clean.
  </acceptance_criteria>
  <done>
    The corrected run has a rendered, machine-readable, provenance-carrying report, and what it says
    is written down as observed — including the parts nobody would have chosen.
  </done>
</task>

</tasks>

<out_of_scope>
| Item | Owner | Why not here |
|---|---|---|
| The judged pass, the calibration worksheet, human scoring and agreement | 06.3.4-05 | Judging is its own capped stage (D-47), and its slice size is derived from the usable-record count this plan produces. |
| The graph-yield disposition and the negative-delta investigation | 06.3.4-05 | This plan publishes the numbers; the dispositions are taken on them, once. |
| Deciding whether to spend on the full drive | 06.3.4-03 | The decision is a closing checkpoint on that plan; this plan asserts it and enforces its cap. |
| The staged gate evaluator, the completeness comparison and the reconciliation | 06.3.4-01 | Consumed here as landed. |
| Dimension definitions, denominators, pairing and intervals | 06.3.2 | Consumed by contract; an absent key is recorded as absent, never defaulted. |
| The root-cause document, the `docs/` note and the ROADMAP edit | 06.3.4-06 | The documentation closes the family after the numbers exist. |
| Any change to `eval/runs/2026-09-03-multihop_rag/` | Nobody — prohibited by D-45 | It is the primary evidence behind every forensic number this family rests on. |
</out_of_scope>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This plan's contribution. The complete phase inventory is the `artifacts_this_phase_produces`
section of `06.3.4-01-PLAN.md`.

| Kind | Artifact |
|---|---|
| Extended run artefact | `eval/runs/<dated>-multihop_rag/journal.jsonl` — completed to the full cross-product and reconciled to publishable |
| New run artefact | `eval/runs/<dated>-multihop_rag/report.json` — written by `score --no-judge` |
| New run artefact | `eval/runs/<dated>-multihop_rag/report.md` and `metadata.json` — written by the **`report`** command (`cli.py:423-432`), not by the scorer; run provenance with the index generation read from the engine |
| New phase record | None. The run record for this stage is `06.3.4-04-SUMMARY.md`, following `06.3-10-SUMMARY.md`'s shape. |
| New CLI flags | None added here. This plan **consumes** the required `--stage-cap` option 06.3.4-01 adds to `run`, passing the full-drive figure from `06.3.4-STAGED-GATE.md`'s decision entry. |
| New dependencies | None. |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorded decision → unrecoverable spend | The full drive is ten times the staged stage; its only authorisation is a file a human wrote. |
| eval harness → paid provider | The full cross-product of real queries against a metered external service. |
| staged journal → merged journal | Two stages write one file; a duplicated, dropped or re-flagged record corrupts every denominator downstream. |
| journal header → published report | The reconciliation is what makes the run publishable; forcing it would publish an incomplete run under a complete run's name. |
| engine-reported index generation → run provenance | A generation mismatch between stages silently makes the merged run unscorable. |
| corrected run directory → superseded run directory | Two dated directories side by side; conflating them destroys the forensic record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.4-17 | Elevation of Privilege | full-drive invocation | high | mitigate | Task 1 asserts the dated go entry and its cap before issuing a query and records what it read; a hold or an absent entry stops the task. |
| T-06.3.4-18 | Tampering | merged journal integrity | high | mitigate | The verify command proves no duplicate work-unit key and at most one index generation; the reconciliation is proven line-preserving by 06.3.4-01's tests and re-checked here on record counts and staged-flag counts. |
| T-06.3.4-19 | Spoofing | published completeness | high | mitigate | The header moves to publishable only through the computed completeness comparison; the plan's prohibitions forbid forcing the flag, and an unresolvable gap leaves the journal staged and the report unrendered. |
| T-06.3.4-20 | Tampering | run provenance | high | mitigate | The index generation is read from the engine at preflight, compared against the staged stage's, and asserted single-valued across the merged journal by the verify command. |
| T-06.3.4-21 | Denial of Service | provider allowance exhaustion | high | mitigate | The full drive is invoked with `--stage-cap` carrying the figure the decision entry recorded, and `drive()`'s in-process stop rule (06.3.4-01) halts the loop at that figure in both the sequential and the pooled branch. This is the control, because the plan is `autonomous: true` and a thousand work units at a graph-on p95 of 30.4 s runs for hours unattended. Spend and allowance are still read before and after as corroboration; records flush per work unit so a capped stop is safe and resumable; and resuming requires a fresh recorded decision rather than a raised figure. Severity raised from medium after cross-AI review found the enforcement was operator vigilance on an autonomous plan. |
| T-06.3.4-37 | Tampering | run-directory resolution | high | mitigate | Every command in this plan resolves the run directory by excluding `2026-09-03-multihop_rag` from the date glob and requiring exactly one remaining candidate, halting on zero or two, then comparing against the literal pinned in `06.3.4-DRIVE-PRECONDITIONS.md`. Newest-match and `ls -d ... \| tail -1` are prohibited. This plan is where a wrong resolution would do the most damage: it would extend the superseded journal to the full sample and then reconcile its header to publishable, publishing the superseded run as the corrected one. |
| T-06.3.4-22 | Repudiation | published results | medium | mitigate | Every recorded number names the dimension or detail key it came from; absent keys are recorded as absent rather than as zeros; unflattering results are published with their disposition named rather than withheld. |
| T-06.3.4-SC | Tampering | package-manager installs | high | mitigate | No install task exists. This plan runs existing commands; the stated evaluation contract forbids new Python dependencies and `uv lock --project eval --check` proves the lock is unchanged. |
</threat_model>

<verification>
- The dated go decision and its full-drive cap are recorded in the summary as read before the drive.
- The merged journal carries every work unit of the committed cross-product exactly once, with at most one index generation.
- The reconcile command reports the journal complete and the header reads as publishable.
- `report.json`, `report.md` **and** `metadata.json` all exist — the latter two produced by the `report` command, which is the only writer of them — and `metadata.json` validates against `RunMetadata` with `partial: false` and a non-empty `index_generation`.
- Every run-directory resolution in this plan excluded the superseded run and found exactly one remaining candidate, matching the pinned literal.
- The full drive was invoked with `--stage-cap` carrying the decision entry's figure; observed spend for the stage is recorded against that cap and against 06.3.4-03's projection.
- `git diff --quiet -- eval/runs/2026-09-03-multihop_rag/` exits clean.
- `uv run --project eval pytest` still passes and `uv lock --project eval --check` confirms no dependency was added.
</verification>

<success_criteria>
- The whole committed sample has been driven across both arms into one dated run directory, for a recorded cost inside a cap a human set before it was spent.
- The merged journal is provably complete, carries no duplicate or dropped work unit, and keeps each record attributable to the stage that produced it.
- The corrected report is published with judging off, carries its provenance, and states what the run actually measured — including the parts that reflect badly on the system.
</success_criteria>

<output>
Create `.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-04-SUMMARY.md` when done.
</output>

## Landed sibling contracts (supersedes the original revise-before-execute table)

**Siblings 06.3.1, 06.3.2 and 06.3.3 have all executed and completed.** Read the landed values as
facts. Verified against the working tree during the cross-AI review round (`3bbf435`).

| Formerly-unavailable input | Landed value / location |
|---|---|
| The authorising go decision, the full-drive spend cap and the spend projection this plan enforces | **06.3.4-03**'s closing `checkpoint:decision`, recorded in `06.3.4-STAGED-GATE.md`. Without it this plan does not start. The cap is now passed to `--stage-cap` rather than watched. |
| The completeness comparison, the one-directional header reconciliation, the reconcile command and the required `--stage-cap` on `run` | **06.3.4-01**, confirmed absent from the tree at `3bbf435` and therefore genuinely this phase's work. |
| The corrected harness: wire capture, per-path and graph dimensions, unified denominators, the paired ablation with its intervals and stratification, the preflight canary | **Landed (06.3.2).** Key names: `graph_ablation_delta` → `detail["pairing_coverage"]`, `graph_presence_rate` → `detail["no_match_rate"]`, `graph_influence_rate`. An absent key is still recorded as absent, never defaulted. |
| The six derived timeout budgets, and the decay verdict that decides whether a full drive is safe to start | **Landed / answered (06.3.3, D-18/D-19/D-21).** Budgets in force; `06.3.3-MEASUREMENT.md` records `decay_present: False`. The preflight canary is what proves the budgets are live at drive time. |
| The graph population counts | **Answered (06.3.3).** `graph_disposition: populated`, `unpopulated: false` — the yield floor **applies**. Consumed here only as context for what the published numbers mean; the disposition is 06.3.4-05's. |
| Failure signalling: the new notice code, partial-snapshot provenance on failed records, the graph-influence counter | **Landed (06.3.1).** `NOTICE_CODE_RETRIEVAL_FAILED = 19` at `proto/lancet/v1/lancet.proto:96`; `graph_influence_rate` exists as a dimension. The published report can carry these; an absent key is still recorded as absent rather than as zero. |
| Observed graph yield, `NoMatchFound` rate, pairing coverage, unusable-record rate and the ablation delta | **Still unknown.** Produced by this plan's full drive, for the first time at full scale. All measured; none asserted here. |
| Actual spend for the full stage | **This plan.** The superseded run's `$0.0199` is not a usable estimate — that run was cheap because 966 of its queries generated nothing. The only defensible basis is the staged stage's observed per-query cost, recorded in `06.3.4-03-SUMMARY.md`. The cap itself is `$5.00` per `06.3.3-BUDGETS.md`, as amended by 06.3.4-03's decision entry. |
| Whether the graph makes results worse | Published here as a number; **disposed in 06.3.4-05** (D-49) after a bounded investigation. Not suppressed and not re-driven here. |
| The judged slice size | **06.3.4-05**, derived from the judgeable-record count this plan's scoring pass makes possible. Nothing here fixes it. |
| Where the index generation lives on a journal record | **Measured over the superseded journal, and now unified into a single audit.** `index_generation` is carried at **both** the record top level (`RunRecord.index_generation`, `journal.py:62`) **and** inside `snapshot`. Of 1001 records, 34 populate both and 967 populate neither; there is no record where only one location is set. **This plan and 06.3.4-03 now both audit the top-level field** — the earlier split, where 03 read the top level and 04 read the snapshot path, would have produced two different unattributable counts for the same question. The snapshot copy is printed separately as `snapshot_only` and recorded as corroboration only. If a later change populates the snapshot copy where the top-level field stays empty, `snapshot_only` is what surfaces it. |
| The naming of the corrected run directory | It must stay the plain date-and-corpus shape the resume resolver globs for. `06.3.4-PATTERNS.md`'s summary skeleton shows a decorated variant with a trailing qualifier; that variant would fork a new run instead of resuming. Use the literal pinned in `06.3.4-DRIVE-PRECONDITIONS.md`, resolved by the exclude-the-superseded-run recipe. |

## Review Dispositions Ledger

### Round 1 — 3bbf435

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| Autonomous full drive has no mechanical cap; `drive()` reads spend nowhere and "watch the observed spend" is not something an autonomous executor can implement (cursor L147@3bbf435; antigravity L468-469@3bbf435; consensus #2 L586@3bbf435) | HIGH | Plan 04, Task 1: the drive invocation is written out with `--stage-cap` carrying the decision entry's figure, enforced by `drive()`'s in-process stop rule from 06.3.4-01; the action states explicitly that this is the control because the plan is `autonomous: true`. A capped stop is a defined outcome requiring a fresh recorded decision to resume, not a silently raised figure. Task 2's re-resume must also carry the cap. `T-06.3.4-21` severity raised medium→high; new `must_haves` truth and prohibition. |
| Same resume glob: `sorted(...)[-1]` publishes the superseded run as the corrected one if 01/03 wrote into it (cursor L148@3bbf435; consensus #1 L585@3bbf435) | HIGH | Plan 04: all three verify commands and the action now use the exclude-the-superseded-run recipe (glob, drop `2026-09-03-multihop_rag`, require exactly one candidate, halt on zero or two), compared against the pinned literal; a new `<precondition>` asserts it before the first provider call. New threat `T-06.3.4-37` names this plan as where a wrong resolution does the most damage. New `must_haves` truth and prohibition. |
| `metadata.json` is written by the `report` command, not by `score`; an executor running only the scorer produces neither `metadata.json` nor `report.md` (cursor L149@3bbf435) | MEDIUM | Plan 04, Task 3: the action now prescribes both commands in order and explains the split, citing `score.py:1065-1069` (scorer writes only `report.json`) against `cli.py:423-432` (report writes the other two). `read_first` gains `cli.py:363-440` and `report.py:24-45`. The verify command runs `report` and validates `metadata.json` against `RunMetadata`, with a dedicated `metadata.json missing — the report command was not run` assertion. Two new acceptance criteria and a `must_haves` truth. |
| Two different `index_generation` audits: 03 reads the top-level field, 04 reads the snapshot path, and the two will disagree (cursor L150@3bbf435; antigravity L493-494@3bbf435) | MEDIUM | Unified on `RunRecord.index_generation` (the top-level field), which is what 06.3.4-03 already used. Plan 04's verify now reads that field for the `unattributable` count and prints `snapshot_only` separately as corroboration; the acceptance criterion states which field the audit is taken on so the two plans cannot report different numbers for the same question. Recorded as a `must_haves` truth and in the landed-contracts table. |
| `score.py:770-815` in `read_first` is paired ablation, not metadata; same stale-citation class as 01/02 (cursor L151@3bbf435) | MEDIUM | Plan 04: Tasks 2 and 3 re-anchored to `score.py:1027-1069` with an explicit note about the wrong range; `cli.py:214-306` → `214-271`, `cli.py:307-368` → `303-360` plus the newly cited `363-440`; `report.py:126-180` → `145-200`; `journal.py:30-104` → `45-131`; `run.py:82-163` → `155-240`. |
| `load_done` tolerates more than a truncated trailing line (cursor L47@3bbf435, applies to this plan's `read_first` too) | LOW | Plan 04, Task 1 `read_first` corrected to state that `load_done`'s `except` at `journal.py:101-102` skips every unparseable line. |
| Revise-before-execute describes 06.3.1–06.3.3 as unexecuted (cursor L23@3bbf435; antigravity L385@3bbf435; consensus #5 L589@3bbf435) | MEDIUM | Replaced by the "Landed sibling contracts" table above. Same finding class handled in all six plans; see 06.3.4-01's ledger. |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| POSIX `ls -d \| tail` in Task 2's verify fails on a Windows-native repo (cursor L152@3bbf435; antigravity L491-492, L497@3bbf435) | **Fixed, but recorded here because the stated reason is rejected.** The subshells are gone because they resolve "newest" — the resume hazard — not because of shell portability. `automated` verify blocks run under Git Bash in this repo; phase 06.3.3's executor ran `test -s` / `grep -qiE` idioms to completion. The remaining POSIX constructs stay, per the verify-command-grounding contract. |
| Ensure `assert len(gens)<=1` does not fail when empty strings are present (antigravity L494@3bbf435) | **Already satisfied; no change needed.** The reviewer verified this themselves: the comprehension filters `{g for g in raw if g}`, so empty values cannot inflate the set. Recorded so the observation is not mistaken for an open item. |
