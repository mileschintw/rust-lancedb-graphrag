---
phase: 06.3.4
plan: 07
type: execute
wave: 5
depends_on: [06.3.4-05]
files_modified:
  - .planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-FINDINGS.md
autonomous: true
requirements: [OBS-05]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The graph-yield disposition is taken once, against the floor and the complement trigger committed before any of these numbers existed, and against the store-population suspension determination (D-32)."
    - "A missed yield floor triggers a bounded investigation owed before Phase 6.4 publishes results; it does not fail the run and it does not apply in production (D-32)."
    - "A paired comparison showing the graph makes results worse is reported after a bounded investigation that distinguishes a measurement artefact from a real finding; it is not suppressed and it is not re-driven or re-scored until it improves (D-49)."
    - "An observed graph presence rate exactly at the floor satisfies it, and an observed complement rate exactly at the trigger threshold does not fire the trigger (specless edge: adjacency)."
    - "An empty pairing join and a paired delta of exactly zero are each defined, recorded outcomes rather than errors or defaults (specless edge: empty)."
    - "Both dispositions are written beneath the calibration section 06.3.4-05 already wrote into 06.3.4-FINDINGS.md, so the phase's findings stay one document that Phase 6.4 reads once rather than two records it has to reconcile."
    - "Every observed value in both dispositions names the dimension or detail key it was read from; an absent key is recorded as absent and is never read as a zero."
    - "The 2026-09-03 run is untouched by this plan: no file of it is rewritten, moved or deleted, and the two runs remain separately dated artefacts (D-45, specless edge: adjacency)."
  artifacts:
    - ".planning/phases/06.3.4-.../06.3.4-FINDINGS.md — extended beneath 06.3.4-05's calibration section with the graph-yield disposition and the paired-comparison disposition including the negative-delta investigation, and closed with what is owed downstream"
  key_links:
    - "The committed yield floor, the complement trigger and their boundary semantics are read from gate.py as landed by 06.3.4-01; nothing here restates them"
    - "The graph presence and influence rates, the complement rate, the paired deltas with their intervals and stratification, and the pairing coverage are read from the report 06.3.4-04 published and 06.3.4-05 republished, each by dimension or detail key"
    - "The store-population suspension determination comes from 06.3.3-STORE-BASELINE.md's zero-count reclassification verdict"
    - "The calibration section this plan writes beneath is 06.3.4-05 task 4's; this plan appends and never rewrites it"
    - "Any yield investigation carried forward from 06.3.4-03's staged decision is discharged in this record rather than duplicated"
    - "06.3.4-06 quotes both dispositions from this file rather than re-deriving them, which is why it follows this plan"
  prohibitions:
    - "The yield floor and the complement trigger must not be changed after an observation exists; a change is a recorded amendment carrying its own rationale and date, never a silent edit"
    - "A negative paired delta must not be suppressed, re-driven or re-scored until it improves; the bounded investigation qualifies the finding, it does not decide whether to publish it"
    - "The calibration section 06.3.4-05 wrote into 06.3.4-FINDINGS.md must not be rewritten, reordered or removed; this plan appends beneath it"
    - "No command in this plan may resolve the run directory by taking the newest match of the date glob; the resolution excludes the superseded run and requires exactly one remaining candidate, and a zero-candidate or multi-candidate result halts rather than picking one"
    - "No file already present in eval/runs/2026-09-03-multihop_rag/ may be rewritten, moved or deleted, and its published figures must never be presented as comparable with the corrected run's"
    - "No number in this record may be re-derived, re-driven or re-scored to change it; the dispositions read what 06.3.4-04 and 06.3.4-05 published and dispose of it"
---

<objective>
Take the two dispositions the corrected drive's numbers finally make possible — whether the graph
returns anything on this corpus, and whether it helps — each once, against thresholds committed
before the numbers existed.

Purpose: these are the two questions this whole family exists to answer, and they are the two most
likely places for an inconvenient number to quietly become a re-run. They are given their own plan
so that they are taken on a fresh agent's best context rather than on the fumes left after a paid
judged pass, a blocking human halt and a republication — and so that the plan seam falls *after*
that unit rather than inside it.

Both are written so that an unflattering answer is a publishable outcome rather than a failure
state. 06.3.4-06 quotes this file; anything left implicit here becomes a gap in Phase 6.4.

Output: `06.3.4-FINDINGS.md` extended beneath its calibration section with the graph-yield
disposition, the paired-comparison disposition, and the closing record of what is owed downstream.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-CONTEXT.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-AI-SPEC.md
@.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-STAGED-GATE.md
@.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-FINDINGS.md
@.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-05-SUMMARY.md
@CLAUDE.md
</context>

<provenance>
## Where this plan's content came from

The two tasks below are the graph-yield disposition and the paired-comparison disposition that were
task 5 of `06.3.4-05-PLAN.md` through the cross-AI review round (reviewed at `3bbf435`, revised at
`08692e5`/`7933cf8`). They were moved here in a targeted scope fix, not rewritten: every
`read_first` entry, every clause of the action, and every acceptance criterion is carried across
and redistributed by disposition. The 1 → 2 task mapping is recorded in the ledger at the foot of
this file so the fidelity of the move can be checked mechanically rather than by eye.

This plan therefore carries reviewed content even though it does not itself appear in
`06.3.4-REVIEWS.md`'s `plans_reviewed` list — that list is a dated record of what existed at
`3bbf435` and is deliberately left as it was.
</provenance>

<tasks>

<task type="auto">
  <name>Task 1: The graph-yield disposition — does the graph return anything on this corpus</name>
  <files>.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-FINDINGS.md</files>
  <precondition>`06.3.4-FINDINGS.md` exists and already carries 06.3.4-05's calibration section, and the corrected run's `report.json` is published and readable — where the run directory is resolved by excluding `2026-09-03-multihop_rag` from `eval/runs/????-??-??-multihop_rag` and requiring exactly one remaining candidate, halting the task if that resolution returns zero or more than one.</precondition>
  <read_first>
    - `eval/src/lancet_eval/gate.py` — the committed yield floor, the complement trigger, their boundary semantics, and the suspension handling.
    - `.planning/phases/06.3.3-.../06.3.3-STORE-BASELINE.md` — the graph population counts and the zero-count reclassification verdict, which decides whether the yield comparison is made at all.
    - `.planning/phases/06.3.1-.../06.3.1-AI-SPEC.md` lines 940-960 — the D-32 note: the floor, the complement, the on-miss disposition, the suspension rule, the absence of an influence floor, and the explicitly provisional basis for the floor's value.
    - The published report from 06.3.4-04 and 06.3.4-05's republication — the graph presence and influence rates and the complement rate, read by dimension or detail key: `graph_presence_rate` with `detail["no_match_rate"]` (`dimensions.py:450-537`) and `graph_influence_rate` (`dimensions.py:539-589`).
    - `.planning/phases/06.3.4-.../06.3.4-STAGED-GATE.md` — any yield investigation carried forward from the staged stage's decision, which this task discharges rather than duplicates.
    - `06.3.4-FINDINGS.md` as 06.3.4-05 left it — the calibration section this task writes beneath and must not alter.
  </read_first>
  <action>
    Take the graph-yield disposition, once, on the full drive's published numbers, and write it into
    `06.3.4-FINDINGS.md` beneath the calibration section 06.3.4-05 wrote. Append; do not rewrite,
    reorder or remove anything already in the file.

    First read the store-population suspension determination. If the store inspection found no nodes,
    no edges and no entities, the yield comparison is not made at all: record that the finding belongs
    to ingestion rather than to query yield, and say so plainly rather than reporting a yield of zero
    as though it measured traversal. 06.3.3 recorded `graph_disposition: populated`, so this branch is
    expected not to fire — record that it did not fire and why, rather than dropping the check.

    Otherwise compare the observed graph presence rate on usable graph-on records against the
    committed floor, and the complement — the rate of no-match outcomes among graph-on records whose
    graph node completed — against its trigger, both under their committed boundary semantics:
    at-or-above satisfies the floor, and the trigger fires only strictly above its threshold. Record
    the graph influence rate beside them as a diagnostic and record that it has no floor at all.

    If the floor is missed or the complement fires, write the bounded investigation: what will be
    examined, in what order, and what would distinguish a measurement artefact from a real finding.
    Record explicitly that a miss does not fail the run and does not apply in production — it is
    owed before Phase 6.4 publishes results — and record that the floor's value is itself
    provisional, chosen before the true rate was measurable. If the staged stage already carried a
    yield investigation forward, discharge it here rather than opening a second one.

    Every value goes into the record with the dimension or detail key it was read from. A key that is
    absent from the published report halts the disposition and is recorded as absent; it is never
    read as a zero, because a missing key and a measured zero are different findings and this family
    exists because they were once confused.
  </action>
  <verify>
    <automated>D=.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation; test -s $D/06.3.4-FINDINGS.md &amp;&amp; grep -qi "calibration" $D/06.3.4-FINDINGS.md &amp;&amp; grep -qi "graph_presence_rate" $D/06.3.4-FINDINGS.md &amp;&amp; grep -qi "graph_influence_rate" $D/06.3.4-FINDINGS.md &amp;&amp; ! grep -qiE "^[^|]*\bTBD\b" $D/06.3.4-FINDINGS.md &amp;&amp; git diff --quiet --exit-code -- eval/runs/2026-09-03-multihop_rag/ &amp;&amp; echo YIELD_DISPOSITION_OK</automated>
    <fails_when>The `YIELD_DISPOSITION_OK` line is absent from stdout, or the command exits non-zero — which happens when `06.3.4-FINDINGS.md` is missing or zero-length, when 06.3.4-05's `calibration` section is no longer present (this task appends and must not remove it), when the record never names `graph_presence_rate` or `graph_influence_rate` as the keys its observations were read from, when any non-table line still carries an unfilled placeholder token, or when `git diff` reports a modification under `eval/runs/2026-09-03-multihop_rag/`.</fails_when>
  </verify>
  <acceptance_criteria>
    - `06.3.4-FINDINGS.md` gains a graph-yield section beneath the existing calibration section, and 06.3.4-05's calibration section is byte-for-byte unchanged by this task.
    - The suspension determination is recorded first in the yield section; where it applies, the record states the comparison was not made and reclassifies the finding as belonging to ingestion rather than query yield. Where it does not apply, the record states that it did not fire and cites `06.3.3-STORE-BASELINE.md`'s verdict as the reason.
    - Where the comparison is made, both the presence rate against the floor and the complement rate against its trigger are recorded with their boundary semantics stated, and a value exactly at a threshold is disposed according to those semantics rather than by judgement.
    - The influence rate is recorded with the statement that it has no floor.
    - On a floor miss or a fired trigger, the record carries a bounded investigation naming what will be examined, in what order, and what would distinguish an artefact from a real finding, together with the statements that the miss does not fail the run, does not apply in production, and is owed before Phase 6.4 publishes.
    - A yield investigation carried forward from the staged stage is discharged in this record rather than duplicated.
    - Each observed value names the dimension or detail key it came from, and an absent key is recorded as absent rather than read as a zero.
    - The summary records the run directory as resolved by the exclude-the-superseded-run recipe, and states that it can never return `eval/runs/2026-09-03-multihop_rag/`.
    - `git diff --quiet -- eval/runs/2026-09-03-multihop_rag/` exits clean.
  </acceptance_criteria>
  <done>
    The question this family could not answer before — does the graph return anything on this corpus
    — has one recorded disposition, taken against a floor committed before the number existed.
  </done>
</task>

<task type="auto">
  <name>Task 2: The paired-comparison disposition — whether the graph makes results worse</name>
  <files>.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-FINDINGS.md</files>
  <precondition>`06.3.4-FINDINGS.md` carries both the calibration section from 06.3.4-05 and the graph-yield disposition from task 1, and the corrected run's published report is readable at the run directory resolved by the exclude-the-superseded-run recipe.</precondition>
  <read_first>
    - `.planning/phases/06.3.1-.../06.3.1-CONTEXT.md` — D-32, D-49, and the ablation decisions D-37 through D-43 that define what the paired delta is and is not.
    - The published report from 06.3.4-04 and 06.3.4-05's republication — the paired deltas with their intervals and their stratification, the pairing coverage and the unusable-record rate, read by key: `graph_ablation_delta` with `detail["pairing_coverage"]` (`dimensions.py:95-129`).
    - `06.3.4-FINDINGS.md` as task 1 left it — the calibration and graph-yield sections this task writes beneath and must not alter.
    - `.planning/phases/06.3.4-.../06.3.4-STAGED-GATE.md` — the staged stage's recorded verdict, for any investigation it left owed that closes here.
  </read_first>
  <action>
    Take the paired-comparison disposition, once, on the full drive's published numbers, and write it
    into `06.3.4-FINDINGS.md` beneath the graph-yield section. Append; do not rewrite, reorder or
    remove anything already in the file.

    Read the paired delta, its interval, its pair count, its pairing coverage and its stratification
    by question type. Then state the disposition the numbers support, under these rules:

    - A delta that is negative — the graph making results worse — is a **publishable outcome**. It is
      reported. It is not suppressed, not re-driven and not re-scored until it looks better. The
      bounded investigation qualifies the finding; it does not decide whether the finding is
      published.
    - The bounded investigation, run when the delta is negative, asks whether this is another
      measurement artefact: does the pairing coverage support the claim; does the interval straddle
      zero; is the aggregate hiding a positive effect on the question types that require traversal;
      is the unusable-record rate materially different between the arms, so that the two arms are
      being scored over different populations; did the graph node complete on the graph-on arm; and
      do the latency and prompt-token deltas suggest the graph did work whose cost is visible while
      its benefit is not. Each of those is a check with a recorded answer, not a conclusion.
    - A delta of exactly zero is not negative, and is recorded as no measurable difference.
    - An interval that straddles zero is reported with the interval as the signal; there is no
      minimum pair count below which the delta is withheld.
    - A pairing join with no pairs at all is recorded as that outcome, with no delta and no interval
      computed, and with the empty join named as the reason.

    Close the file with what is owed downstream: which investigations remain open, what would close
    each, and where each conclusion will be recorded. Phase 6.4's evaluation narrative reads this
    file through 06.3.4-06; anything left implicit here becomes a gap there.
  </action>
  <verify>
    <automated>D=.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation; test -s $D/06.3.4-FINDINGS.md &amp;&amp; grep -qi "calibration" $D/06.3.4-FINDINGS.md &amp;&amp; grep -qi "graph_presence_rate" $D/06.3.4-FINDINGS.md &amp;&amp; grep -qi "graph_influence_rate" $D/06.3.4-FINDINGS.md &amp;&amp; grep -qi "graph_ablation_delta" $D/06.3.4-FINDINGS.md &amp;&amp; ! grep -qiE "^[^|]*\bTBD\b" $D/06.3.4-FINDINGS.md &amp;&amp; git diff --quiet --exit-code -- eval/runs/2026-09-03-multihop_rag/ &amp;&amp; echo FINDINGS_OK</automated>
    <fails_when>The `FINDINGS_OK` line is absent from stdout, or the command exits non-zero — which happens when `06.3.4-FINDINGS.md` is missing or zero-length, when it never names `calibration`, `graph_presence_rate`, `graph_influence_rate` or `graph_ablation_delta` as the keys its observations were read from, when any non-table line still carries an unfilled placeholder token, or when `git diff` reports a modification under `eval/runs/2026-09-03-multihop_rag/`.</fails_when>
  </verify>
  <acceptance_criteria>
    - `06.3.4-FINDINGS.md` carries three sections — the calibration attestation and agreement (06.3.4-05), the graph-yield disposition (task 1), and the paired-comparison disposition (this task) — and each observed value names the dimension or detail key it came from.
    - The paired delta, its interval, its pair count, its pairing coverage and its stratification are recorded, and the disposition states which of the defined outcomes applies.
    - Where the delta is negative, the record states that the result is published, carries each named check of the bounded investigation with a recorded answer, and states that the finding was neither suppressed nor re-driven; the summary records that no re-drive or re-score was performed to change it.
    - A delta of exactly zero, an interval straddling zero, and an empty pairing join are each disposed as the defined outcome named for them, with no delta computed in the empty case.
    - The record closes with the open investigations, what would close each, and where each conclusion will be recorded for Phase 6.4.
    - The calibration and graph-yield sections written before this task are unchanged by it.
    - `git diff --quiet -- eval/runs/2026-09-03-multihop_rag/` exits clean.
  </acceptance_criteria>
  <done>
    The second question this family could not answer before — does the graph help — has a recorded
    disposition taken against thresholds committed before the numbers existed, written so that an
    unflattering answer reads as a result.
  </done>
</task>

</tasks>

<out_of_scope>
| Item | Owner | Why not here |
|---|---|---|
| The judged pass, the calibration worksheet, the human halt and the agreement figure | 06.3.4-05 | This plan disposes of the drive's deterministic numbers; the calibration section of `06.3.4-FINDINGS.md` is already written when this plan starts and is appended beneath, never rewritten. |
| The full two-arm drive and the publication of the report these dispositions read | 06.3.4-04 | This plan reads published numbers; it does not drive, score or republish. |
| The committed yield floor, the complement trigger, their boundary semantics and the suspension handling | 06.3.4-01 | Committed before any observation. Read from `gate.py` here, never restated. |
| Pairing the judged dimensions across arms | Excluded by D-39 | It would double judged spend for no answer this family needs. |
| Fixing the graph's traversal quality, fan-out, depth or reranking | Out of scope for the whole family (D-03) | The bar is that it runs and outright bugs are fixed, not that it performs well. A poor result is reported, not tuned away. |
| Re-seeding the eval corpus | Out of scope (D-05) | A store-population finding is reported as an ingest finding, not resolved by re-seeding here. |
| The root-cause document, the `docs/` note and the ROADMAP edit | 06.3.4-06 | They quote this file's dispositions and therefore follow it. |
| Any change to `eval/runs/2026-09-03-multihop_rag/` | Nobody — prohibited by D-45 | Its sixteen scores and two cached verdicts are evidence, not a starting point. |
</out_of_scope>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This plan's contribution. The complete phase inventory is the `artifacts_this_phase_produces`
section of `06.3.4-01-PLAN.md`.

| Kind | Artifact |
|---|---|
| Extended phase record | `06.3.4-FINDINGS.md` — the graph-yield disposition and the paired-comparison disposition including the negative-delta investigation, appended beneath 06.3.4-05's calibration section, and closed with what is owed downstream |
| Modified source | None. This plan reads published numbers and writes one planning record. |
| New dependencies | None. |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| observed number → recorded disposition | The dispositions are where an inconvenient result could quietly become a re-run. |
| published report → quoted finding | A value read from the wrong run directory, or a missing key read as a zero, would put a number in the record that nothing measured. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.4-27 | Repudiation | the two dispositions | high | mitigate | Both are taken against thresholds committed before the numbers existed, with boundary semantics stated; a negative delta is published by rule and this plan's prohibitions forbid re-driving or re-scoring to change it. Placing the dispositions in their own plan means the seam falls after 06.3.4-05's paid judged pass and human halt, so the disposition is taken on fresh context rather than at the tail of a long autonomous stretch. |
| T-06.3.4-40 | Tampering | run-directory resolution on the read path | high | mitigate | Both tasks' preconditions resolve the run directory by excluding `2026-09-03-multihop_rag` from the date glob and requiring exactly one remaining candidate, halting on zero or two. A newest-match resolution here would quote the superseded run's figures as the corrected run's. Both verify commands additionally assert `git diff --quiet` over the superseded run. |
| T-06.3.4-41 | Tampering | the calibration section written by 06.3.4-05 | medium | mitigate | Both tasks append beneath the existing section and both verify commands re-assert that `calibration` is still present in the file, so a task that rewrote the file wholesale rather than appending fails its own gate. A `must_haves` prohibition forbids rewriting, reordering or removing it. |
| T-06.3.4-28 | Information Disclosure | findings contents | low | accept | The findings carry public news-corpus text, model output and aggregate figures; no credential or personal data is present, and the provider key is read from the environment and never written into a planning record. |
| T-06.3.4-SC | Tampering | package-manager installs | high | mitigate | No install task exists and this plan modifies no source. The stated evaluation contract forbids new Python dependencies; `uv lock --project eval --check` still confirms the lock is unchanged at phase verification. |
</threat_model>

<verification>
- `06.3.4-FINDINGS.md` carries all three sections — calibration, graph yield, paired comparison — with each observed value naming its source key.
- The calibration section 06.3.4-05 wrote is unchanged; both dispositions were appended beneath it.
- The store-population suspension branch is recorded as fired or not fired, with the record it was read from cited either way.
- Both dispositions state their boundary semantics, and any value landing exactly on a threshold is disposed by those semantics rather than by judgement.
- The record closes with the open investigations, what would close each, and where each conclusion will be recorded for Phase 6.4.
- `git diff --quiet -- eval/runs/2026-09-03-multihop_rag/` exits clean.
- `uv run --project eval pytest` still passes and `uv lock --project eval --check` confirms no dependency was added; this plan changes no source, so both are unchanged from 06.3.4-05's closing state.
</verification>

<success_criteria>
- The graph-yield question and the does-the-graph-help question each have one recorded disposition, taken against committed thresholds, with an unflattering answer written as a result rather than as a failure.
- Both dispositions live beneath 06.3.4-05's calibration section in one findings document, which 06.3.4-06 quotes rather than re-derives.
- Every number in both dispositions names the key it was read from, and no number was re-derived, re-driven or re-scored to change it.
</success_criteria>

<output>
Create `.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-07-SUMMARY.md` when done.
</output>

## Landed sibling contracts

**Siblings 06.3.1, 06.3.2 and 06.3.3 have all executed and completed.** Read the landed values as
facts. Verified against the working tree during the cross-AI review round (`3bbf435`).

| Formerly-unavailable input | Landed value / location |
|---|---|
| The committed yield floor, the complement trigger, their boundary semantics and the suspension handling | **06.3.4-01**, in `gate.py`. Read from there; nothing here restates them. |
| The graph population counts and the zero-count reclassification verdict | **Answered (06.3.3).** `06.3.3-STORE-BASELINE.md`: `graph_disposition: populated`, `unpopulated: false` — the store-suspension branch **will not fire** and the yield floor applies. The branch stays in the disposition as a guard; the summary records that it did not fire and why. |
| The dimension and detail key names for graph presence, graph influence, the no-match complement, pairing coverage and the paired deltas | **Landed (06.3.2).** `graph_presence_rate` (score) with `detail["no_match_rate"]` (`dimensions.py:450-537`), `graph_influence_rate` (`dimensions.py:539-589`), `graph_ablation_delta` with `detail["pairing_coverage"]` (`dimensions.py:95-129`). An absent key still halts the disposition and is recorded as absent, never read as a zero. |
| Observed graph yield and no-match rate | **06.3.4-04**'s full drive. The broken run gave 494 graph timeouts against only 4 unavailable results, so the true rate has never been observed. |
| Whether the graph makes results worse | **This plan's** paired-comparison disposition (D-49), on 06.3.4-04's published delta. |
| The calibration attestation and agreement section of `06.3.4-FINDINGS.md` | **06.3.4-05**, task 4. Present when this plan starts; appended beneath, never rewritten. |
| Any yield investigation carried forward from the staged stage | **06.3.4-03**'s recorded decision in `06.3.4-STAGED-GATE.md`. Discharged here rather than duplicated. |

## Revise-before-execute ledger

### 2026-09-08 — created by targeted scope fix on 06.3.4-05

This plan was created in revision iteration 1 of the plan-check loop, in response to a `scope_sanity`
BLOCKER against `06.3.4-05-PLAN.md` (5 declared tasks against a 2-3 target). Task 5 of that plan —
the two dispositions — was moved here whole.

**Why this seam and not another.** 06.3.4-05's tasks 1-4 are one indivisible unit: tasks 1-2 must
commit the worksheet size and run the paid judged pass *before* the halt, task 3 *is* the blocking
human halt, and task 4 must consume the human's scores immediately after it. Any boundary inside
1-4 puts a plan seam inside the paid-judged / human-halt / feed-back sequence. Task 5 sits entirely
after that unit and depends only on 06.3.4-04's published report, 06.3.4-05's own findings and
inputs committed by 06.3.4-01 and 06.3.3 — not on the judged-pass machinery. It is therefore the
only seam in that plan that costs nothing structurally, and it buys the most: the two headline
dispositions of the whole family are taken on a fresh agent's early context rather than at the tail
of a five-task plan that has already spent money and stopped for a human.

**Field mapping — 06.3.4-05 task 5 → this plan's two tasks.** Every field is carried; none is
dropped or shortened.

| Source field in 06.3.4-05 task 5 | Destination |
|---|---|
| `<name>` "The two dispositions — graph yield, and whether the graph makes results worse" | Split into task 1's and task 2's names, one disposition each |
| `read_first` — `gate.py`; `06.3.3-STORE-BASELINE.md`; `06.3.1-AI-SPEC.md` 940-960; `06.3.4-STAGED-GATE.md` | Task 1 `read_first` (the yield inputs), with `06.3.4-STAGED-GATE.md` also on task 2 for the investigation it may leave owed |
| `read_first` — `06.3.1-CONTEXT.md` (D-32, D-49, D-37..D-43) | Task 2 `read_first` |
| `read_first` — the published report line (presence, influence, complement, deltas, intervals, stratification, pairing coverage, unusable-record rate) | Split by disposition: presence / influence / complement to task 1, deltas / intervals / stratification / pairing coverage / unusable-record rate to task 2. Key names from 06.3.2 added inline in both. |
| `action` — suspension determination, floor and complement comparison, influence diagnostic, on-miss bounded investigation, discharge of the staged investigation | Task 1 `action`, verbatim in substance |
| `action` — the five paired-comparison rules and the closing "what is owed downstream" paragraph | Task 2 `action`, verbatim in substance |
| `verify` — the composite `FINDINGS_OK` gate | Task 2 `verify` unchanged; task 1 gets the same gate minus the `graph_ablation_delta` clause, as `YIELD_DISPOSITION_OK` |
| `acceptance_criteria` — the three-section criterion, suspension, boundary semantics, influence-has-no-floor, on-miss investigation, staged-investigation discharge, `git diff` clean | Task 1 takes suspension, boundary semantics, influence, on-miss investigation, staged discharge; task 2 takes the three-section criterion and the closing-record criterion; `git diff` clean on both |
| `acceptance_criteria` — paired delta with interval / pair count / coverage / stratification, negative-delta publication, zero delta, straddling interval, empty join | Task 2 `acceptance_criteria` |
| `done` | Split into two, one per disposition |
| `must_haves` truths carrying `(D-32)`, `(D-49)`, the adjacency truth and the empty-pairing-join clause | This plan's `must_haves.truths`, with the D-NN parentheticals preserved |
| Threat `T-06.3.4-27` and the "observed number → recorded disposition" trust boundary | This plan's `<threat_model>`, moved rather than duplicated |

**Newly added here, not carried:** the `<precondition>` on each task (06.3.4-05 task 5 had none
because its predecessor ran in the same plan; across a plan boundary the assumption needs
asserting), threats `T-06.3.4-40` and `T-06.3.4-41`, and the `must_haves` truth and prohibition
protecting 06.3.4-05's calibration section from being rewritten rather than appended to.
