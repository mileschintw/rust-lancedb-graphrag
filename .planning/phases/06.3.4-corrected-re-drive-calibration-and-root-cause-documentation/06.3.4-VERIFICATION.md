---
phase: 06.3.4-corrected-re-drive-calibration-and-root-cause-documentation
verified: 2026-09-10T15:50:00Z
status: gaps_found
score: 5/8 must-haves verified
covered_files:
  - ".planning/REQUIREMENTS.md"
  - ".planning/ROADMAP.md"
  - ".planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-ROOT-CAUSE.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-01-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-01-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-02-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-02-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-03-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-03-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-04-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-04-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-05-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-05-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-06-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-06-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-07-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-07-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-DRIVE-PRECONDITIONS.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-FINDINGS.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-OBSERVATION-THROUGHPUT-PLATEAU.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-PATTERNS.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-REVIEW.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-REVIEWS.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-STAGED-GATE.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/COVERAGE.md"
  - "docs/evaluation-fidelity-and-graph-yield.md"
  - "eval/src/lancet_eval/gate.py"
  - "eval/src/lancet_eval/journal.py"
  - "eval/src/lancet_eval/report.py"
  - "eval/src/lancet_eval/run.py"
  - "eval/src/lancet_eval/score.py"
covered_digest: "v1:sha256:7b6a473348f2261b3390da27640d5c8ad210117c7c92b4c5dd7ab25d8cffa6c9"
gaps:
  - truth: "The recorded run's completeness metadata (journal `partial` flag) accurately reflects that the run is incomplete, so no downstream consumer can mistake it for a finished run of record."
    status: failed
    reason: >
      Direct inspection of eval/runs/2026-09-09-multihop_rag/journal.jsonl line 1 shows
      {"partial": false, "created_at": 1788987937.88...}. This is a NEW header (different
      created_at, and different per-record content for the first two questions — different
      node_timings/token counts than 06.3.4-01-SUMMARY.md recorded for the same question IDs)
      than the one 06.3.4-01's micro-slice produced ({"partial": true, "created_at":
      1788972726.44...}). Despite the run being verifiably incomplete (658 of 1000 work units;
      06.3.4-04-SUMMARY.md's own `lancet-eval reconcile` invocation returned "Reconciliation
      rejected: Journal is incomplete: missing 342 work unit(s)"), the on-disk header reads
      partial:false — the value that gates report.py/score.py into treating a run as
      publishable. 06.3.4-04-SUMMARY.md explicitly claims "The header correctly remains
      `partial: true`" — this claim is false against the actual bytes on disk. All 658 records
      independently carry partial:false with no duplicates (658 distinct keys), so this is not a
      duplicate-append artifact; the journal was evidently reset/recreated between the micro-slice
      and the full drive, and the reset was not caught by any acceptance check in this phase.
      Running `uv run --project eval lancet-eval score --run eval/runs/2026-09-09-multihop_rag
      --no-judge` today would write report.json for this run cleanly, because the code path
      that guards against publishing partial runs (score.py / report.py, both examined in
      06.3.4-REVIEW.md's CR-01) reads exactly this (wrong) header value. Code review's CR-01 fix
      (derive `partial` from a completeness check at drive-end) would not, by itself, have
      prevented this specific instance — the header was written wrong from the first line, not
      merely left stale — so this is a second, related but distinct manifestation of the same
      class of defect CR-01 names, not the same bug re-flagged. This directly falsifies OBS-05's
      mandate ("a failed query path must be visible on the wire and in the report rather than
      reported as success") at the run-completeness level and undermines the phase's stated goal
      of a "trustworthy recorded run."
    artifacts:
      - path: "eval/runs/2026-09-09-multihop_rag/journal.jsonl"
        issue: "Header reads partial:false on a verifiably incomplete (658/1000 work unit) run; no report.json/report.md/metadata.json has been generated from this journal yet, which is the only reason no wrongly-labeled report has actually been published — the guard that is supposed to prevent that has not been exercised, not fixed."
      - path: "eval/src/lancet_eval/run.py:247"
        issue: "partial = limit is not None, computed once before any work unit runs (CR-01)"
      - path: ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-04-SUMMARY.md"
        issue: "States 'The header correctly remains partial: true' — contradicted by the actual journal header, which reads partial:false"
    missing:
      - "Fix CR-01 in eval/src/lancet_eval/run.py / score.py / report.py so `partial` is derived from actual completeness, not from whether --limit was passed, and cannot be silently reset by a fresh header write on a file that should have been resumed."
      - "Determine why eval/runs/2026-09-09-multihop_rag/journal.jsonl's header and first two records do not match what 06.3.4-01-SUMMARY.md recorded for the micro-slice (a journal reset occurred somewhere between plan 01 and plan 04 that no acceptance check caught) and record the finding — do not merge it silently."
      - "Before Phase 6.4 (or anyone) runs `lancet-eval score`/`report` against this run directory, either correct the header out-of-band with a recorded, dated rationale, or add the completeness check to the harness so `score`/`report` refuse it on their own."
  - truth: "A smaller staged drive runs first and its pairing coverage and graph yield are verified before the full 500x2 drive is committed to (D-44)."
    status: failed
    reason: >
      The 50-question staged drive never actually ran. Per 06.3.4-03-SUMMARY.md, the preflight
      canary failed on Canary 5 (mhr-0d5e238015ef, 0 graph nodes observed against a required-graph
      floor), and per the plan's own fail-closed rule ("If the preflight fails, stop... Do not
      proceed past a failed canary"), the `lancet-eval run --limit 50` staged-drive command was
      never invoked. The staged gate was instead evaluated against the pre-existing 2-question
      micro-slice from 06.3.4-01, and returned overall_status: hold — "Short journal: 2 distinct
      questions in journal < locked stage size 50" — with d44_staged_coverage 0.0400 against the
      0.80 floor (06.3.4-STAGED-GATE.md §2). D-44's own text is explicit about what this staged
      step exists to do: "Verify pairing coverage and graph yield on the staged fraction before
      committing to the full run, catching a partial fix before it burns the full budget"
      (06.3.1-CONTEXT.md). Coverage was not verified — it held (failed) — and the human decision
      checkpoint selected "go-with-investigation," an option 06.3.4-03-PLAN.md's own option
      description reserves for "the yield floor was missed but coverage held" — not for a coverage
      hold. The plan's own "hold" option text says hold is "Appropriate when the coverage floor
      was missed, since the full drive would likely reproduce the same failure at ten times the
      cost." That predicted consequence materialized: the full drive was halted at 658/1000 records
      by a severe, systemic RetrieveHybrid latency escalation (7.4s -> >30s) that the 50-question
      staged drive, had it run to completion, plausibly would have surfaced at 1/10th the cost.
      This was a deliberate, disclosed human decision at a blocking-human checkpoint (not a hidden
      defect) — the shortfall is visible in 06.3.4-STAGED-GATE.md's own tables — but it does not
      satisfy the roadmap's literal SC-1 text, which requires the staged drive to run and its
      coverage/yield to be verified, not held-and-overridden.
    artifacts:
      - path: ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-STAGED-GATE.md"
        issue: "Records overall_status: hold on coverage (0.04 vs 0.80 floor) yet the decision entry proceeds to 'go-with-investigation' rather than 'hold'"
    missing:
      - "A completed 50-question staged drive with a coverage verdict that actually passes (or an explicit, well-labeled amendment recording why 'go-with-investigation' was applied to a coverage hold rather than 'hold')"
  - truth: "Human calibration is performed fresh against the corrected run, with the worksheet explicitly sized to available effort and that sample size stated in the report (D-48). No agent back-fill."
    status: failed
    reason: >
      No human calibration worksheet was populated. 06.3.4-05-SUMMARY.md and 06.3.4-FINDINGS.md §1
      both state this plainly: "Judged Evaluation Pass: Deferred... Human Calibration Worksheet:
      Deferred... Calibration Attestation: ...Uncalibrated (Deferred)." This is openly and
      honestly disclosed (not hidden, not glossed over, not back-filled by an agent), and is a
      defensible operator-directed outcome given the drive was halted mid-run — but it does not
      satisfy SC-3's literal requirement that fresh human calibration actually occur with a sized
      worksheet and a stated sample size. Zero is not "a worksheet sized to available effort" in
      the sense the criterion intends (a nonzero slice of the 143 usable answerable pairs sized to
      whatever effort was available); it is calibration not having happened at all.
    artifacts:
      - path: ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-05-SUMMARY.md"
        issue: "Confirms judged pass and calibration worksheet were both deferred entirely, not sized down and executed"
    missing:
      - "A completed human calibration pass — even a small one sized to whatever effort is now available against the 143 usable pairs — with the agreement statistics (06.3.4-02's kappa/Spearman machinery, already implemented and tested) actually run against it."
deferred: []
advisory: []
behavior_unverified_items: []
coincidental_reliance_items: []
human_verification:
  - test: "Confirm whether 'go-with-investigation' was an acceptable business call given the coverage-floor hold (SC-1 gap above), or whether the phase should be reopened to complete an actual 50-question staged drive before Phase 6.4 relies on this run."
    expected: "A human with authority over the milestone either accepts the deviation (and records why) or directs a staged re-verification."
    why_human: "This is a risk-tolerance / business judgment call already made once at a blocking-human checkpoint; overriding or ratifying it again is a human decision, not a programmatic check."
  - test: "Decide whether Phase 6.4 may proceed to cite 06.3.4's numbers (graph presence 9.8%, paired deltas) as the 'run of record' given the journal header defect, or whether the header must be corrected/the run re-scored under a corrected harness first."
    expected: "A recorded decision, because the current header would let `lancet-eval score`/`report` publish this run as complete without further human involvement."
    why_human: "Determines whether Phase 6.4 can start, and touches spend/scheduling tradeoffs outside this verifier's authority."
---

# Phase 06.3.4: Corrected Re-drive, Calibration and Root-Cause Documentation — Verification Report

**Phase Goal:** Produce the trustworthy recorded run that Phase 6.4 documents for v1 milestone
closure — staged then full two-arm drive, freshly human-calibrated — and write the root-cause
record, in the phase directory and in `docs/`, that explains what was wrong with the 2026-09-03 run
and what the corrected numbers mean.

**Verified:** 2026-09-10T15:50:00Z
**Status:** gaps_found
**Re-verification:** No — this is the first verification. STATE.md/ROADMAP.md had been hand-edited to
say "complete" and `current_phase` advanced to 06.4 without any code review or verification gate
having actually run. This report is that retroactive, first legitimate pass.

## Direct Answer to the Assigned Investigation Question

**Does CR-01 (the `partial` flag defect found in code review) undermine "trustworthy recorded run"
for the actual artifact this phase produced?**

Yes, concretely and currently — not just as a latent risk for some future run. The live journal at
`eval/runs/2026-09-09-multihop_rag/journal.jsonl` — the actual run of record this phase produced —
has its header stamped `"partial": false` right now, despite being verifiably incomplete (658 of
1,000 required work units; `06.3.4-04-SUMMARY.md`'s own `lancet-eval reconcile` invocation returned
"Journal is incomplete: missing 342 work unit(s)"). `06.3.4-04-SUMMARY.md` explicitly asserts "The
header correctly remains `partial: true`" — this is factually wrong against the bytes on disk.

The mitigating fact: nobody has yet run `lancet-eval score`/`report` against this journal, so no
`report.json`/`report.md` exists that visibly misrepresents this run as complete. The prose
disclosure in `06.3.4-FINDINGS.md`, `06.3.4-04-SUMMARY.md` (aside from the header claim),
`06.3.4-OBSERVATION-THROUGHPUT-PLATEAU.md`, and the ROADMAP entry is honest and detailed about the
658/1000 halt. **But the protection here is "nobody has run the command yet," not "the system
refused to."** `uv run --project eval lancet-eval score --run eval/runs/2026-09-09-multihop_rag
--no-judge` would, as of this verification, write `report.json` for this run cleanly and mark it
`partial: false` in the published metadata — exactly the failure mode CR-01 describes, on exactly
the run Phase 6.4 is meant to consume. Phase 6.4's own plan text (06.3.4-04-PLAN.md, out-of-scope
table) says "06.3.4-05 republishes over it once the judged pass and fresh human calibration exist" —
i.e., `score`/`report` commands *are* expected to run against this journal later, at which point the
defect would fire for real unless fixed first.

Separately from CR-01 itself: the header's `false` value is not simply "never updated" (CR-01's
literal description) — the header line and the first two records' content do not match what
`06.3.4-01-SUMMARY.md` recorded for the same micro-slice (different `created_at`, different node
timings, different token counts for question `mhr-0073ab564e55`). The journal was evidently reset
and re-driven from scratch between the micro-slice and the full drive, and CR-01's proposed fix
(derive `partial` from completeness at drive-end) would not by itself have prevented this specific
instance, because the header was wrong from its first line, not merely stale. This is recorded as an
open, unresolved finding in the gaps below — its root mechanism was not diagnosed (out of scope for
verification) but its existence and consequence are directly evidenced.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 0 | The recorded run's completeness metadata accurately reflects its actual completeness (foundational to "trustworthy recorded run") | FAILED | `journal.jsonl` header reads `partial: false` on a 658/1000 halted run; `06.3.4-04-SUMMARY.md` claims the opposite. See gap above. |
| 1 | SC-1: A smaller staged drive runs first and its pairing coverage and graph yield are verified before the full drive is committed to (D-44) | FAILED | Staged 50-question drive never ran (canary halt); gate returned `hold` on 2-question journal (0.04 vs 0.80 coverage floor); "go-with-investigation" applied to a coverage hold contrary to the plan's own option guidance; the predicted 10x-cost failure reproduction materialized (halt at 658/1000). Disclosed transparently at a blocking-human checkpoint — see Human Verification. |
| 2 | SC-2: The corrected run lands in a new dated run directory; 2026-09-03 kept, marked superseded, cited as evidence (D-45) | ✓ VERIFIED | `eval/runs/2026-09-09-multihop_rag/` exists; `eval/runs/2026-09-03-multihop_rag/SUPERSEDED.md` added; `git diff --quiet -- eval/runs/2026-09-03-multihop_rag/` clean (0 diff) — verified directly. |
| 3 | SC-3: Human calibration performed fresh, worksheet sized to available effort, sample size stated (D-48); no agent back-fill | FAILED | Zero calibration performed. `06.3.4-05-SUMMARY.md`/`06.3.4-FINDINGS.md` §1 honestly disclose "Uncalibrated (Deferred)" — no back-fill occurred (good), but the criterion was not met. |
| 4 | SC-4: Graph yield / NoMatchFound rate measured and published against a stated provisional floor; missing it triggers investigation (D-32) | ✓ VERIFIED | Independently reproduced from raw `journal.jsonl`: 143 answerable dual-success pairs, 14 with `graph_node_count>0` → 9.79% — matches `06.3.4-FINDINGS.md`'s claimed 0.0979 exactly. Floor (0.20) missed; bounded investigation recorded in `06.3.4-STAGED-GATE.md` §5 and `06.3.4-FINDINGS.md` §3. |
| 5 | SC-5: If the paired comparison shows the graph makes things worse, it is reported after a bounded investigation confirming it's not a measurement artifact (D-49) | ✓ VERIFIED | Negative Token-F1 delta (-0.0024) reported plainly; EM neutral; Recall/Precision marginally positive. Root-cause investigation (single-entity cosine bottleneck, noise injection on non-matching seeds) recorded in `06.3.4-FINDINGS.md` §3 and `06.3.4-07-SUMMARY.md`. Underlying presence-rate arithmetic independently reproduced (see SC-4); F1/EM/Recall/Precision figures were not independently re-derived by this verifier (no committed script or `report.json` exists for this run — see gap 0) but the one figure that was spot-checked reproduced exactly, and the investigation narrative is substantive rather than asserted. |
| 6 | SC-6: `06.3.1-ROOT-CAUSE.md` carries the full forensic trail; distilled public note lands under `docs/` (D-54) | ✓ VERIFIED | `06.3.1-ROOT-CAUSE.md` (124 lines, 8 sections: Executive Summary, Evidence, Decay Signature, False-Green Breakdown, Ablation Disjointness, Graph Health, D-32 Impact, Corrections/Findings) exists in the 06.3.1 phase directory. `docs/evaluation-fidelity-and-graph-yield.md` (12 lines, 5-point numbered summary) exists under `docs/`. |
| 7 | SC-7: Phase 6.4's ROADMAP entry updated as closing act (D-53) | ✓ VERIFIED | ROADMAP.md's Phase 6.4 entry canonical refs cite `06.3.1-ROOT-CAUSE.md` by path; SC-5's notice vocabulary includes `RETRIEVAL_FAILED`. Confirmed by direct grep of `.planning/ROADMAP.md`. |

**Score:** 5/8 truths verified (0 present-but-behavior-unverified)

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|---|---|---|---|---|
| OBS-05 | 06.3.4-01 through -07 (all 7 plans declare it) | Evaluation fidelity — this phase's slice: "corrected re-drive, calibration and root-cause docs" (per REQUIREMENTS.md's traceability row). Shared with 06.3.1/06.3.2/06.3.3, already marked `[x]` at the requirement level. | ⚠️ PARTIALLY SATISFIED (this phase's slice only) | The root-cause documentation and graph-yield/paired-comparison measurement sub-slices are satisfied (SC-4, SC-5, SC-6). The "corrected re-drive" and "calibration" sub-slices are not: the drive is incomplete (disclosed) and its own completeness metadata is wrong (undisclosed defect), and calibration did not occur. No orphaned requirement IDs found — OBS-05 is the only ID mapped to 06.3.4 in REQUIREMENTS.md's traceability table, and all 7 plans declare it consistently. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `eval/src/lancet_eval/run.py` | 247 | `partial = limit is not None` computed once at drive-start, never revisited on early exit | 🛑 Blocker | This is CR-01 from `06.3.4-REVIEW.md`, and is not merely theoretical — it produced the wrong header on this phase's own run of record (see gap 0). |
| `.planning/phases/.../06.3.4-04-SUMMARY.md` | n/a | Documentation asserts a factual claim about journal state ("header correctly remains `partial: true`") that is false against the actual file | 🛑 Blocker | Misleads any reader of the SUMMARY into believing the completeness guard held when it did not. All other quantitative claims in this SUMMARY (312 success / 346 error / 329 distinct questions / 145 both-success / 162 both-error / 0 missing-arm) were independently reproduced from the raw journal and found accurate — this appears to be an isolated but load-bearing misstatement, not a pattern of general unreliability in the SUMMARY. |
| `docs/evaluation-fidelity-and-graph-yield.md` | 12 | Closing claim: "prevent hidden failures from masquerading as successes" | ⚠️ Warning | Flagged as WR-05 in `06.3.4-REVIEW.md`; directly contradicted by gap 0 above. This doc is cited by Phase 6.4's methodology pages per its own header note, so the overclaim propagates downstream unless corrected. |
| N/A | N/A | `06.3.4-SECURITY.md` absent | ℹ️ Info | `workflow.security_enforcement` is active per project config; no security gate artifact exists for this phase. Per task instructions this is tracked separately and not this verifier's item to resolve — noted here so it is not silently dropped. |
| `.planning/ROADMAP.md` | ~866 | Phase 06.3.4 entry still reads "**Plans:** 2/7 plans complete" while all 7 plan checkboxes below it are `[x]` | ℹ️ Info | Stale counter, consistent with the reported hand-edit of STATE.md/ROADMAP.md prior to any gate having run. Cosmetic, but worth fixing alongside the other gaps since it is in the same file being relied upon for phase status. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Journal header value | `sed -n '1p' eval/runs/2026-09-09-multihop_rag/journal.jsonl` | `{"partial": false, "created_at": 1788987937.88...}` | ✗ FAIL (contradicts 06.3.4-04-SUMMARY.md's claim) |
| Journal integrity (duplicates / partial-field distribution) | Python: parse all 658 records, count distinct `(question_id, graph_arm)` keys and `partial` field values | 658 distinct keys, 0 duplicates, all records carry `partial: False` | ✓ PASS (no duplication) but confirms the reset — the micro-slice's originally-`partial:True`-stamped records are not present |
| 06.3.4-04-SUMMARY.md quantitative claims (outcome counts, pairing breakdown) | Python: recompute success/error counts, distinct questions, both-succeeded/both-errored/missing-arm counts directly from journal.jsonl | 312 success / 346 error / 329 distinct questions / 145 both-success / 162 both-error / 0 missing-arm — matches SUMMARY exactly | ✓ PASS |
| SC-4 graph presence rate (9.8%) | Python: join `questions.sample.jsonl` gold-evidence presence against both-success pairs, count `graph_node_count>0` on `graph-on` | 143 answerable dual-success pairs (145 both-success minus 2 null-gold), 14 with graph nodes present → 0.0979 | ✓ PASS — matches `06.3.4-FINDINGS.md` exactly |
| `06.3.1-ROOT-CAUSE.md` / `docs/evaluation-fidelity-and-graph-yield.md` existence and non-triviality | `wc -l`, direct read | 124 lines / 8 sections; 12 lines / 5-point summary | ✓ PASS |
| ROADMAP.md SC-6/SC-7 wiring | `grep -n "06.3.1-ROOT-CAUSE\|RETRIEVAL_FAILED" .planning/ROADMAP.md` | Both present, in Phase 6.4's canonical refs and SC-5 vocabulary | ✓ PASS |
| Superseded-run integrity (D-45) | `git diff --quiet -- eval/runs/2026-09-03-multihop_rag/`, `ls` | Clean; `SUPERSEDED.md` present alongside untouched original files | ✓ PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` probes declared or discovered for this phase; this is an evaluation-harness/documentation phase, not a migration/tooling phase with shell probes. SKIPPED.

## Regression Gates (per task instructions, not re-run here)

- `cargo test` (engine): exit 0 — already confirmed clean, not re-run.
- `go test` (gateway): exit 0 — already confirmed clean, not re-run.
- `uv run --project eval pytest`: 372/372 passing — already confirmed clean, not re-run.
- Code review (`06.3.4-REVIEW.md`): 1 critical (CR-01), 5 warnings, 3 info — CR-01 is load-bearing for this verification's central finding and is carried into `gaps` above rather than re-litigated.
- Security gate: `06.3.4-SECURITY.md` absent; tracked separately per task instructions, noted as an open item above.

## Human Verification Required

1. **Test:** Confirm whether "go-with-investigation" was an acceptable business call given the
   coverage-floor hold (SC-1), or whether the phase should be reopened to complete an actual
   50-question staged drive before Phase 6.4 relies on this run.
   **Expected:** A human with milestone authority either accepts the deviation (recording why) or
   directs a staged re-verification.
   **Why human:** Already a human judgment call at a blocking checkpoint; re-litigating it is a
   business decision, not a programmatic check.

2. **Test:** Decide whether Phase 6.4 may proceed to cite 06.3.4's numbers as the "run of record"
   given the journal header defect (gap 0), or whether the header must be corrected / the run
   re-scored under a fixed harness first.
   **Expected:** A recorded decision, since the current header would let `lancet-eval score`/`report`
   publish this run as complete with no further human involvement required by the tooling.
   **Why human:** Determines whether Phase 6.4 can start on this run as-is; touches spend and
   scheduling tradeoffs outside verification's authority.

## Gaps Summary

Three concrete gaps block a clean pass, plus one contributing code defect already identified by
code review:

1. **The run's own completeness metadata is wrong** (`partial: false` on a 658/1000 halted run),
   and this is currently the only thing keeping the run from being silently published as complete
   by ordinary use of the harness. This is the most urgent gap: it is live, on the actual artifact
   Phase 6.4 is meant to consume, not merely a risk in the abstract. `06.3.4-04-SUMMARY.md`'s claim
   that the header "correctly remains `partial: true`" is factually false and should be corrected.
2. **The staged-drive gate (SC-1/D-44) never ran to completion and returned a coverage hold that was
   overridden**, using a decision branch the plan's own text reserves for a different failure mode.
   The predicted consequence of proceeding anyway (reproducing a systemic failure at full-drive cost)
   is exactly what happened. This was disclosed, not hidden — but it is a real miss against the
   roadmap's literal SC-1 wording and is flagged for a human decision rather than silently accepted.
3. **Human calibration (SC-3/D-48) did not happen.** Honestly disclosed, not back-filled by an agent
   — an acceptable process (no deception) but not an acceptable outcome against the literal criterion.

Everything else — the new dated run directory and supersede marker (SC-2), the graph-yield
measurement and investigation (SC-4), the paired-comparison negative-delta investigation (SC-5), and
the root-cause documentation and ROADMAP updates (SC-6, SC-7) — is genuinely present, substantive,
and (where independently checked) reproducible from raw data, not merely asserted in prose.

---

_Verified: 2026-09-10T15:50:00Z_
_Verifier: Claude (gsd-verifier)_
