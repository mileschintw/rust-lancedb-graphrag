---
phase: 06.3.4-corrected-re-drive-calibration-and-root-cause-documentation
verified: 2026-09-13T00:00:00Z
status: gaps_found
score: 6/8 must-haves verified
covered_files:
  - ".planning/REQUIREMENTS.md"
  - ".planning/ROADMAP.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-01-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-01-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-02-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-02-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-03-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-03-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-04-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-04-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-05-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-05-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-06-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-06-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-07-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-07-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-08-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-08-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-09-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-09-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-DRIVE-PRECONDITIONS.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-FINDINGS-PREFIX.sha256"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-FINDINGS.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-OBSERVATION-THROUGHPUT-PLATEAU.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-PATTERNS.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-REVIEW.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-REVIEWS.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-SECURITY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-STAGED-GATE.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/COVERAGE.md"
  - "docs/evaluation-fidelity-and-graph-yield.md"
  - "eval/runs/2026-09-09-multihop_rag/journal.jsonl"
  - "eval/src/lancet_eval/cli.py"
  - "eval/src/lancet_eval/dimensions.py"
  - "eval/src/lancet_eval/gate.py"
  - "eval/src/lancet_eval/journal.py"
  - "eval/src/lancet_eval/judge.py"
  - "eval/src/lancet_eval/measure.py"
  - "eval/src/lancet_eval/report.py"
  - "eval/src/lancet_eval/run.py"
  - "eval/src/lancet_eval/score.py"
  - "eval/src/lancet_eval/thresholds.py"
covered_digest: "v1:sha256:3016f79f2da97db35fdb64be55139e594e3e5777f248c52a2226808b1b619982"
re_verification:
  previous_status: gaps_found
  previous_score: 5/8
  gaps_closed:
    - "The recorded run's completeness metadata (journal `partial` flag) accurately reflects that the run is incomplete (Gap 0 / CR-01 / T-06.3.4-19)"
  gaps_remaining:
    - "SC-1: staged drive coverage/yield verification before full drive (D-44) — remains a disclosed, unremedied deviation"
    - "SC-3: fresh human calibration (D-48) — remains deferred, no back-fill"
  regressions: []
overrides_applied: 0
deferred: []
advisory: []
behavior_unverified_items: []
coincidental_reliance_items: []
gaps:
  - truth: "A smaller staged drive runs first and its pairing coverage and graph yield are verified before the full 500x2 drive is committed to (D-44)."
    status: failed
    reason: >
      Unchanged since the prior verification, and explicitly not remediated by the gap-closure
      round by design. `06.3.4-STAGED-GATE.md` §6's new "Amendment: SC-1 Interpretation &
      Disclosed Deviation (2026-09-12, Gap-Closure Plan 06.3.4-08)" independently confirms every
      element of the original gap: the 50-question staged drive never ran (preflight canary halt
      on Canary 5); the gate was evaluated against the pre-existing 2-question micro-slice,
      returning `overall_status: hold` at `d44_staged_coverage: 0.0400` against the `0.80` floor;
      and `go-with-investigation` was applied to a coverage hold, an option
      `06.3.4-03-PLAN.md`'s own text reserves for a yield-floor miss with coverage held, not for a
      coverage hold. The amendment's own item 4 states plainly: "this is a disclosed deviation,
      not an SC-1 pass... this amendment clarifies how the decision must be read and does not
      reverse it." An artifact that self-describes as not-a-pass cannot be read as satisfying the
      truth. Item 5 confirms "No re-drive is performed or scheduled by this gap-closure round" —
      `06.3.4-08-PLAN.md`'s own prohibitions structurally forbid one ("No re-drive of the
      50-question staged drive is performed or scheduled by this plan"). This is a knowingly
      carried-forward gap, not a newly discovered one, and not something 08/09 attempted to close
      (08's own source-coverage audit lists it as "dated amendment; no re-drive — operator-locked").
    artifacts:
      - path: ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-STAGED-GATE.md"
        issue: "§6 records `overall_status: hold` on coverage (0.04 vs 0.80 floor) with 'go-with-investigation' applied to it, then a dated amendment (2026-09-12) confirms this is a disclosed deviation, explicitly not an SC-1 pass, and states no re-drive is scheduled."
    missing:
      - "A completed 50-question staged drive with a coverage verdict that actually passes the D-44 floor, OR a human-accepted override recorded in this VERIFICATION.md's `overrides:` frontmatter (see suggestion below) that formally closes SC-1 as an accepted deviation rather than leaving it as an open gap every re-verification will re-flag."
  - truth: "Human calibration is performed fresh against the corrected run, with the worksheet explicitly sized to available effort and that sample size stated in the report (D-48). No agent back-fill."
    status: failed
    reason: >
      Unchanged since the prior verification. `06.3.4-05-SUMMARY.md` and `06.3.4-FINDINGS.md` §1
      still state plainly: judged evaluation pass and human calibration worksheet both "Deferred,"
      calibration attestation "Uncalibrated (Deferred)." Zero calibration rows exist anywhere in
      the phase directory or `eval/runs/2026-09-09-multihop_rag/`
      (`calibration_worksheet.jsonl`/`calibration_completed.jsonl` are absent from that run
      directory; only the superseded 2026-09-03 run has them). Both gap-closure plans' own
      prohibitions explicitly forbid touching this: `06.3.4-08-PLAN.md` — "No task in this plan
      starts a judged pass, spends provider money, or performs or simulates human calibration...
      no agent back-fill of a calibration worksheet is permitted (D-48)"; `06.3.4-09-PLAN.md` —
      "No task in this plan performs or simulates human calibration, or writes any row of a
      calibration worksheet; SC-3 stays deferred (D-48)." `06.3.4-09` did build the *precondition*
      controls this criterion's spend depends on (judge spend metering, `--stage-cap` on `score`,
      reachable judged-slice-size refusal, provenance fields) — but the calibration pass itself
      did not happen and is not scheduled by this round. Openly and honestly disclosed (not
      hidden, not back-filled), which is why this is not classified as a new defect, but the
      literal criterion — fresh human calibration performed, sample size stated — is still not met.
    artifacts:
      - path: ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-05-SUMMARY.md"
        issue: "Confirms judged pass and calibration worksheet remain deferred entirely."
    missing:
      - "An actual human calibration pass — even a small one sized to whatever effort is available against the answerable pairs — with 06.3.4-02's kappa/Spearman agreement machinery run against it. This is now unblocked at the control level (09 built the spend cap and slice-sizing machinery this needs) but is explicitly gated behind Phase 06.3.4.1's unpark criteria and a separately authorised spend decision (per Phase 6.4's ROADMAP entry and 06.3.4.1's own Out of Scope list)."
human_verification:
  - test: "Confirm whether the SC-1 'go-with-investigation' override should stand permanently as an accepted deviation from D-44 (formalized via an `overrides:` entry in this VERIFICATION.md), or whether the phase should still be reopened for an actual 50-question staged drive before any run built on this journal is cited as validated."
    expected: "A human with authority over the milestone either records an explicit override (reason/accepted_by/accepted_at) accepting the deviation, or directs a staged re-verification."
    why_human: "This is a risk-tolerance/business judgment call already made once at a blocking-human checkpoint (2026-09-09) and reaffirmed as 'disclosed, not reversed' by the 2026-09-12 amendment — but no one has yet converted that into a formal, closable verification override, and the gap-closure round's own prohibitions explicitly declined to touch it. It will keep re-appearing as an open gap on every re-verification until a human either overrides it or a real staged drive runs."
---

# Phase 06.3.4: Corrected Re-drive, Calibration and Root-Cause Documentation — Verification Report

**Phase Goal:** Produce the trustworthy recorded run that Phase 6.4 documents for v1 milestone
closure — staged then full two-arm drive, freshly human-calibrated — and write the root-cause
record, in the phase directory and in `docs/`, that explains what was wrong with the 2026-09-03 run
and what the corrected numbers mean.

**Verified:** 2026-09-13T00:00:00Z
**Status:** gaps_found
**Re-verification:** Yes — this is a re-verification after gap closure. The prior
`06.3.4-VERIFICATION.md` (2026-09-10T15:50:00Z, `status: gaps_found`, 5/8) is STALE: it predates
gap-closure plans `06.3.4-08` and `06.3.4-09`, both completed and committed 2026-09-12. This report
independently re-checks all 8 must-haves/success-criteria against the current codebase and the
current 9-plan phase (all 9 now have `SUMMARY.md`), rather than trusting either the stale report's
conclusions or the new plans' own self-reported claims.

## Direct Answer to the Assigned Investigation Question

**Is Gap 0 (the `partial`-flag defect, CR-01 / T-06.3.4-19) actually closed, and do SC-1/SC-3
remain open?**

**Gap 0 is closed — verified independently, not taken on the SUMMARY's word.** Three independent
checks converge:

1. **The live journal header now reads correctly.** `head -n 1
   eval/runs/2026-09-09-multihop_rag/journal.jsonl` returns
   `{"type": "header", "corpus": "multihop_rag", "partial": true, "created_at":
   1788987937.8855517}` — `partial: true` (staged/incomplete), matching the run's actual state
   (658/1000 work units). This is the same file the prior verification found reading
   `partial: false` on the identical incomplete run.
2. **The correction did not touch a single record byte.** Diffing `git show
   33e774b:eval/runs/2026-09-09-multihop_rag/journal.jsonl` (the pre-correction commit) against the
   current working file, skipping line 1, is byte-for-byte identical
   (`RECORDS_BYTE_IDENTICAL`, independently reproduced by this verifier, not read from the
   SUMMARY) — only the header line changed, exactly as `06.3.4-08-PLAN.md`'s prohibitions required.
3. **The fix is structural, not a one-off hand-edit.** `score.py`'s `score_run` now defaults
   `effective_partial = True` and only ever flips to `False` when the header *explicitly* says
   `partial: false` **and** an independent `completeness_comparison` against the records confirms
   it (score.py:1387-1399) — so a headerless journal, a journal missing the `partial` key, or a
   journal whose header says staged all fail closed as incomplete regardless of record content.
   `drive()`'s header write is fail-closed at start (`partial=True` unconditionally, run.py:271)
   and a single `finally: _safe_reconcile()` (run.py:381-382) wraps the *entire* work-dispatch
   `try` block (run.py:279-380), so all four exit routes — no-remaining-units early return
   (280-282), cap-already-reached early return (286-287), normal completion (379-380), and any
   exception propagating through the block — pass through the same reconciliation before control
   leaves the function. This was confirmed by direct code reading (not by trusting a single
   `'reconcile_header' in inspect.getsource(...)` grep, which cannot distinguish a single
   pre-loop call from a wrapping `finally`) and by running the two named end-to-end tests plan 08
   claims prove it — `test_drive_header_staged_mid_run_and_publishable_on_completion` and
   `test_drive_resume_no_remaining_units_reconciles_header` — both PASS in isolation.

   One caveat, stated for precision rather than blurred: `lancet-eval score --no-judge` cannot be
   run end-to-end against the actual `2026-09-09-multihop_rag` journal to watch the completeness
   refusal fire on this specific run, because an *earlier*, unrelated structural guard
   (`score.py:221`, the OI-03 index-drift check — an unmapped `document_id` from corpus/index
   drift) raises first. The fail-closed completeness path is therefore verified by direct code
   reading plus the passing synthetic `<behavior>`-block tests in `test_journal_reconcile.py`
   (all of which construct fixture journals under `tmp_path`), not by an end-to-end run against
   this journal. This does not weaken the finding — the header, the derivation logic, and the
   byte-identity are all independently confirmed against real bytes — but Phase 6.4/06.3.4.1
   would hit the OI-03 guard before ever reaching the completeness guard on this particular
   journal, which is exactly why 06.3.4.1 exists to fix index drift first.
4. `06.3.4-04-SUMMARY.md`'s false claim ("The header correctly remains `partial: true`") is now
   corrected in place with a dated note, and `06.3.4-FINDINGS.md` gained a new §5 documenting the
   journal-reset finding (structurally attributable to neither `drive()` nor the CLI's `run`
   command — an out-of-band reset between commits `eb893ba` and `33e774b`, external actor not
   reconstructable from the repo).
5. `docs/evaluation-fidelity-and-graph-yield.md` point 5's overclaim ("prevent hidden failures
   from masquerading as successes") is rewritten to a scoped, accurate claim naming the 658/1000
   incomplete state explicitly. All three secondary review findings tied to the same files
   (WR-01 unknown-arm silent default, WR-05 doc overclaim, IN-01 inline `import time`) are also
   independently confirmed fixed in the current source (`GRAPH_ARMS` membership check with
   `ValueError` at run.py:80-84, module-level `import time` at run.py:7, doc rewrite confirmed by
   direct read).

**SC-1 and SC-3 remain genuinely open** — not closed, not silently dropped, and not eligible to be
marked VERIFIED by this verifier without a human-recorded override (see Gaps and Human
Verification below). Both are honestly and repeatedly disclosed as unremedied in the same round
that closed Gap 0; the gap-closure plans explicitly declined to touch either (their own
`prohibitions` forbid a re-drive and forbid any calibration back-fill).

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 0 | The recorded run's completeness metadata accurately reflects its actual completeness (foundational to "trustworthy recorded run") | ✓ VERIFIED | Journal header now reads `partial: true` on the still-incomplete (658/1000) run; record lines byte-identical pre/post correction (independently diffed); `score_run`'s completeness derivation is fail-closed by construction, confirmed by code reading and passing tests. See Direct Answer above. |
| 1 | SC-1: A smaller staged drive runs first and its pairing coverage and graph yield are verified before the full drive is committed to (D-44) | ✗ FAILED | Unchanged. 50-question staged drive never ran; gate held at 0.04 vs 0.80 coverage; `go-with-investigation` applied to a coverage hold. `06.3.4-STAGED-GATE.md` §6's own 2026-09-12 amendment states explicitly "this is a disclosed deviation, not an SC-1 pass... does not reverse it." No re-drive scheduled. See Human Verification. |
| 2 | SC-2: The corrected run lands in a new dated run directory; 2026-09-03 kept, marked superseded, cited as evidence (D-45) | ✓ VERIFIED | `eval/runs/2026-09-09-multihop_rag/` exists (659-line journal); `eval/runs/2026-09-03-multihop_rag/SUPERSEDED.md` present; `git diff --quiet -- eval/runs/2026-09-03-multihop_rag/` clean — re-confirmed directly, unchanged since prior verification. |
| 3 | SC-3: Human calibration performed fresh, worksheet sized to available effort, sample size stated (D-48); no agent back-fill | ✗ FAILED | Unchanged. Zero calibration rows exist for the 2026-09-09 run; `06.3.4-05-SUMMARY.md`/`06.3.4-FINDINGS.md` §1 still disclose "Uncalibrated (Deferred)." 06.3.4-09 built the spend-cap/slice-sizing controls this pass would need, but did not perform it — its own prohibitions forbid doing so. |
| 4 | SC-4: Graph yield / NoMatchFound rate measured and published against a stated provisional floor; missing it triggers investigation (D-32) | ✓ VERIFIED (regression check) | `06.3.4-FINDINGS.md` still reports 9.79% presence rate (14/143 answerable dual-success pairs) against the 0.20 floor with the bounded investigation in `06.3.4-STAGED-GATE.md` §5 and `06.3.4-FINDINGS.md` §3. Nothing in 08/09 touches this computation; re-confirmed unchanged by direct read, not re-derived from raw data this round (already independently reproduced in the prior verification pass). |
| 5 | SC-5: If the paired comparison shows the graph makes things worse, it is reported after a bounded investigation confirming it's not a measurement artifact (D-49) | ✓ VERIFIED (regression check) | `06.3.4-FINDINGS.md` still reports Token-F1 delta -0.0024 with the root-cause narrative (single-entity cosine bottleneck, unfiltered 1-hop noise injection) unchanged. Not touched by 08/09; re-confirmed unchanged by direct read. |
| 6 | SC-6: `06.3.1-ROOT-CAUSE.md` carries the full forensic trail; distilled public note lands under `docs/` (D-54) | ✓ VERIFIED | `06.3.1-ROOT-CAUSE.md` exists in the 06.3.1 phase directory; `docs/evaluation-fidelity-and-graph-yield.md` exists under `docs/` (5-point summary, point 5 corrected by 08 as noted above). Unchanged in shape since prior verification. |
| 7 | SC-7: Phase 6.4's ROADMAP entry updated as closing act (D-53) | ✓ VERIFIED | `.planning/ROADMAP.md` Phase 6.4 entry cites `06.3.1-ROOT-CAUSE.md` by path and includes `RETRIEVAL_FAILED` in its notice vocabulary (line 946). Confirmed by direct grep. Phase 6.4 is now explicitly PARKED pending Phase 06.3.4.1's unpark gates, and its canonical-refs note states this run "is not a run of record and must not be cited as complete" — a stronger, more explicit statement of the same SC-7 intent than existed at the prior verification. |

**Score:** 6/8 truths verified (0 present-but-behavior-unverified)

### Deferred Items

None. SC-1 and SC-3 were checked against Phase 06.3.4.1's and Phase 6.4's goals/success criteria
per Step 9b and do not qualify as "deferred": Phase 06.3.4.1's scope is retrieval/index diagnosis
(OI-01/OI-02/OI-03), not a staged-drive re-run or human calibration, and both are explicitly listed
under **Out of Scope** in 06.3.4.1's own ROADMAP entry ("judged/human calibration spend... blocked
until T-24/T-25/T-43 exist," "Rewriting 06.3.4 as complete," "Agent back-fill of calibration
worksheets"). Phase 6.4 is parked and does not claim to address either. Kept as real gaps.

### Advisory (New Scope, Unevidenced)

None — this re-verification is scoped to the phase's own gap-closure round, and no new-scope,
unevidenced findings surfaced outside what is already tracked in `06.3.4-REVIEW.md` /
`06.3.4-SECURITY.md` (both noted stale below, not new findings).

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `eval/runs/2026-09-09-multihop_rag/journal.jsonl` | Header agrees with contents (incomplete at 658/1000) | ✓ VERIFIED | `partial: true`; record lines 2-659 byte-identical to pre-correction commit `33e774b`. |
| `eval/src/lancet_eval/journal.py::reconcile_header` | Bidirectional four-state reconcile, `(publishable, header_changed, message)` | ✓ VERIFIED | Docstring states all four states explicitly; matches plan 08's contract. |
| `eval/src/lancet_eval/run.py::drive` | Fail-closed initial header; reconciled on every exit path | ✓ VERIFIED | `partial=True` unconditional at write (run.py:271); single wrapping `finally: _safe_reconcile()` (run.py:381) covers all 4 exit routes. Confirmed via code read + 2 passing named end-to-end tests. |
| `eval/src/lancet_eval/score.py::score_run` | Fails closed on unverified completeness | ✓ VERIFIED | `effective_partial` defaults `True`; only cleared by an explicit publishable header **and** an independent `completeness_comparison` pass (score.py:1387-1399). |
| `eval/src/lancet_eval/report.py`, `cli.py` | Refusal wording no longer attributes to `--limit` | ✓ VERIFIED | `grep -rn "smoke knob"` returns nothing; both sites reference "completeness comparison" and `lancet-eval reconcile`. |
| `.planning/.../06.3.4-STAGED-GATE.md` §6 | Dated SC-1 deviation amendment, original entry preserved | ✓ VERIFIED | Amendment present and dated 2026-09-12; original Decision Checkpoint entry (date, decider, basis) unaltered above it. |
| `.planning/.../06.3.4-FINDINGS.md` §5 | Journal-reset finding recorded | ✓ VERIFIED | New dated §5 present, states structural impossibility of in-tree cause and that the external actor is not reconstructable. |
| `docs/evaluation-fidelity-and-graph-yield.md` | Point 5 scoped, accurate | ✓ VERIFIED | Rewritten; no longer claims to "prevent hidden failures... at the run level" in general terms; names the 658/1000 incomplete state explicitly. |
| `eval/src/lancet_eval/{judge,measure,gate,dimensions,thresholds}.py` | Judged spend metering, reachable slice-size refusal, provenance fields, unified floor | ✓ VERIFIED | `JudgeUsage`, `compute_judge_spend`, `stage_spend_cap` on `score_run`, `derive_judged_slice_size` wired into `score_run`, `judged_slice_committed`/`verdicts_obtained`/`judged_slice_state` on `RunMetadata` and in the regenerated `eval/report.schema.json`, `GRAPH_YIELD_INVESTIGATION_FLOOR` relocated to `thresholds.py` and re-exported — all present in code, not merely claimed. Exercises no real journal (SUMMARY's own claim); consistent with its own prohibitions. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `drive()`'s end-of-run reconciliation | `reconcile_header` | Single `finally: _safe_reconcile()` wrapping the whole dispatch block | ✓ WIRED | One completeness authority, confirmed by code read; not two divergent paths. |
| `score_run`'s `header_partial`/`effective_partial` | `RunMetadata.partial` | Direct assignment (score.py:1432) | ✓ WIRED | Both `report.py`'s `render_markdown`/`render_json` and the `report.json` write guard (`score.py:1446`) gate on this same value. |
| `derive_judged_slice_size` | `score_run`'s judged-subset selection | Call site feeding `judgeable_count`/`cached_verdict_count`/`cost_per_question`/`stage_spend_cap` | ✓ WIRED | Confirmed present in `score.py` per 09-SUMMARY.md's cited call site and independently grepped; no longer dead code imported only by a test. |
| Live journal header correction | `reconcile_header` (not a hand-edit) | `lancet-eval reconcile` CLI invocation | ✓ WIRED | Confirmed via `git show 33e774b:... | diff` — only line 1 changed, consistent with an atomic single-line rewrite, not a manual full-file edit. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `eval/runs/2026-09-09-multihop_rag/journal.jsonl` header | `partial` | `lancet-eval reconcile` invocation against `completeness_comparison` over the real 658 records | Yes — flipped by a real comparison, not a hand-typed literal | ✓ FLOWING |
| `RunMetadata.partial` (score path) | `effective_partial` | `completeness_comparison(journal_path, records_corpus)` gated behind an explicit `header_partial is False` check | Yes, when exercised — verified via passing synthetic-journal tests; not exercisable end-to-end against this specific run because an earlier, unrelated OI-03 guard (`score.py:221`) raises first | ✓ FLOWING (code path); ⚠️ not observed end-to-end on this journal (see Direct Answer caveat) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Journal header value (post gap-closure) | `head -n 1 eval/runs/2026-09-09-multihop_rag/journal.jsonl` | `{"type": "header", "corpus": "multihop_rag", "partial": true, "created_at": 1788987937.8855517}` | ✓ PASS |
| Record-line byte-identity across the header correction | `git show 33e774b:...journal.jsonl \| tail -n +2` diffed against `tail -n +2` of the working file | 0 differences | ✓ PASS — independently reproduced, not read from SUMMARY |
| `reconcile_header` docstring / four-state contract | Direct read of `journal.py:168-183` | States all 4 (completeness, header) states explicitly | ✓ PASS |
| `drive()` fail-closed initial header + universal exit-path reconciliation | Direct read of `run.py:230-382`; named tests `test_drive_header_staged_mid_run_and_publishable_on_completion`, `test_drive_resume_no_remaining_units_reconciles_header` | Both tests PASS in isolation (`2 passed`) | ✓ PASS |
| `score_run` fail-closed completeness derivation | Direct read of `score.py:1387-1399`; attempted live invocation against the real run | Logic confirmed fail-closed by inspection and by the passing `<behavior>`-block synthetic tests; live invocation raised an unrelated, earlier OI-03 structural guard (`Unmapped document_id`) before reaching the completeness branch | ✓ PASS (code + synthetic tests); not demonstrable end-to-end on this journal |
| `smoke knob` wording removed | `grep -rn "smoke knob" eval/src/lancet_eval/` | No matches | ✓ PASS |
| WR-01 unknown-arm handling | `eval/tests/test_run_arms.py::test_unknown_arm_raises_value_error` exists and named | Present, asserts `ValueError, match="Unknown arm 'unknown-arm'"` | ✓ PASS |
| No debt markers (TBD/FIXME/XXX) in phase-touched source files | `grep -n -E "TBD|FIXME|XXX"` across all 10 touched `eval/src/lancet_eval/*.py` files | No matches | ✓ PASS |
| Full eval test suite | `uv run --project eval pytest eval/tests -q` (run from repo root) | `406 passed` | ✓ PASS — matches 09-SUMMARY.md's claimed count exactly |
| Full workspace `pytest` from repo root (includes non-eval tests) | `uv run --project eval pytest -q` | `1 failed, 431 passed` — the 1 failure is `scripts/test_phase02_live_evidence.py::...test_wrong_model_is_rejected_in_optimized_isolated_subprocess`, asserting `sys.flags.optimize == 1` | ℹ️ Info, not a gap — see Anti-Patterns |
| SC-2 superseded-run integrity | `git diff --quiet -- eval/runs/2026-09-03-multihop_rag/`, `ls` | Clean; `SUPERSEDED.md` present | ✓ PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` probes declared or discovered for this phase. SKIPPED (same as prior
verification — evaluation-harness/documentation phase, not a migration/tooling phase).

## Regression Gates

- `uv run --project eval pytest eval/tests -q`: **406 passed** (run from repo root this round; the
  same command run from `eval/` as CWD produces a spurious failure in
  `test_wire_capture.py::test_historical_pre_change_journal_loads` because that test hard-codes a
  repo-root-relative path — a CWD-dependent test, not a real regression; noted below).
- `uv run --project eval pytest -q` (whole workspace, from repo root): 1 failed / 431 passed — the
  failure (`scripts/test_phase02_live_evidence.py`) is outside `eval/tests`, untouched by this
  phase's `files_modified`, and fails on an environment property (`sys.flags.optimize`) unrelated to
  06.3.4's changes. Not a carried-forward gap from the prior verification (which never listed it)
  and the file was not git-modified by this phase — per the re-verification evidence gate, this is
  📋 Advisory, not a blocker.
- Cargo/Go regression suites: not re-run this round (08/09 touch only `eval/`; prior verification
  already confirmed these clean and no Rust/Go file is in either plan's `files_modified`).
- Code review (`06.3.4-REVIEW.md`): **STALE.** Predates 08/09. Its CR-01/WR-01/WR-05/IN-01 findings
  are independently confirmed fixed in current source by this verification (see Direct Answer and
  Anti-Patterns). The artifact itself has not been re-run/updated to reflect this — same class of
  staleness as the prior `06.3.4-VERIFICATION.md` before this round.
- Security gate (`06.3.4-SECURITY.md`): **STALE and not this verifier's item to close**, but flagged
  because it materially affects phase-completion routing. Frontmatter still reads `status: open`,
  `threats_open: 4` (T-06.3.4-19/24/25/43), all `high`/`block_on: high` — **BLOCKING** by the
  project's own gate policy. This verifier independently confirmed all four underlying code defects
  now have landing implementations (T-19 via Gap 0 above; T-24/25/43 via the `judge.py`/`measure.py`/
  `gate.py`/`dimensions.py`/`report.py` changes cited in the Required Artifacts table), but
  `/gsd-secure-phase 06.3.4` has not been re-run to confirm `threats_open: 0` in the gate's own
  record. `STATE.md`'s own "Next" line already names this as required before the phase can advance.

## Human Verification Required

1. **Test:** Confirm whether the SC-1 "go-with-investigation" override should stand permanently as
   an accepted deviation from D-44 (formalized via an `overrides:` entry in this VERIFICATION.md),
   or whether the phase should still be reopened for an actual 50-question staged drive before any
   run built on this journal is cited as validated.
   **Expected:** A human with milestone authority either records an explicit override
   (reason/accepted_by/accepted_at) or directs a staged re-verification.
   **Why human:** Already a human judgment call at a blocking checkpoint (2026-09-09), reaffirmed as
   "disclosed, not reversed" by the 2026-09-12 amendment — but never converted into a closable
   verification override. It will keep re-appearing as an open gap on every future re-verification
   until a human either overrides it or a real staged drive runs.

   **This looks intentional and may be override-eligible.** To accept this deviation formally, add
   to this file's frontmatter:

   ```yaml
   overrides:
     - must_have: "A smaller staged drive runs first and its pairing coverage and graph yield are verified before the full 500x2 drive is committed to (D-44)."
       reason: "Preflight canary halted the 50-question staged drive before it could run; the operator made an explicit go-with-investigation decision at a blocking checkpoint on 2026-09-09, and the 2026-09-12 gap-closure amendment (06.3.4-STAGED-GATE.md §6) records this as a disclosed, non-reversed deviation rather than a hidden defect."
       accepted_by: "<name>"
       accepted_at: "<ISO timestamp>"
   ```

   (Note: the previous verification's second human-verification item — whether Phase 6.4 may cite
   this run's numbers as-is — is **no longer open**. `.planning/ROADMAP.md`'s Phase 6.4 entry now
   explicitly states the 06.3.4 run "is not a run of record and must not be cited as complete," and
   names Phase 06.3.4.1 as the gate before any citation. That question has been answered by the
   project's own subsequent planning, not left hanging.)

## Gaps Summary

Two concrete, previously-known gaps remain; one gap is newly closed:

1. **CLOSED this round: the run's completeness metadata is now honest.** The journal header reads
   `partial: true` on the still-incomplete run, the correction was applied through the fixed
   `reconcile_header` path with byte-identical records (independently verified), and the
   publication path (`score_run`/`report`/`cli`) now fails closed by construction rather than by
   the accident of nobody having run the command yet. `06.3.4-04-SUMMARY.md`'s false claim is
   corrected in place with a dated note, and the journal-reset finding is recorded rather than
   silently absorbed.

2. **SC-1 (D-44 staged gate) remains a disclosed, unremedied deviation.** The 50-question staged
   drive never ran; the gate held on coverage; the operator's checkpoint decision proceeded anyway.
   This is honestly and repeatedly disclosed across three artifacts (`06.3.4-STAGED-GATE.md` §6's
   original entry and its 2026-09-12 amendment, `06.3.4-08-PLAN.md`'s prohibitions, and this
   report) — it is not hidden — but it does not satisfy the roadmap's literal SC-1 wording, and no
   one has yet formally accepted it as an override. Flagged for human decision above.

3. **SC-3 (D-48 fresh human calibration) remains deferred.** Zero calibration rows exist for the
   corrected run. `06.3.4-09` built the spend-metering and slice-sizing controls a future judged
   pass will need, but performing the pass itself is explicitly out of scope for this round and for
   Phase 06.3.4.1, and Phase 6.4 (which would consume it) is parked.

Everything else — the new dated run directory and supersede marker (SC-2), the graph-yield
measurement and investigation (SC-4), the paired-comparison negative-delta investigation (SC-5),
and the root-cause documentation and ROADMAP updates (SC-6, SC-7) — remains genuinely present,
substantive, and unaffected by this gap-closure round; all re-confirmed unchanged.

Separately, two phase gate artifacts are now stale relative to the current code state and should be
re-run before the phase is considered fully closed: `06.3.4-REVIEW.md` (predates the fixes it
would otherwise re-flag) and `06.3.4-SECURITY.md` (`threats_open: 4`, blocking by the project's own
gate policy, despite the underlying code defects now being fixed). Neither is a new defect
introduced by this round — both are artifacts that have not yet been re-run to reflect a state this
verification independently confirms is fixed.

---

_Verified: 2026-09-13T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
