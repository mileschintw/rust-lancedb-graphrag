---
phase: 06.3.4-corrected-re-drive-calibration-and-root-cause-documentation
verified: 2026-09-13T22:00:00Z
status: gaps_found
score: 6/8 must-haves verified
covered_files:
  - ".planning/REQUIREMENTS.md"
  - ".planning/ROADMAP.md"
  - ".planning/STATE.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-01-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-01-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-02-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-02-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-03-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-03-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-04-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-04-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-05-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-05-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-06-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-06-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-07-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-07-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-08-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-08-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-09-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-09-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-10-PLAN.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-10-SUMMARY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-DRIVE-PRECONDITIONS.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-FINDINGS-PREFIX.sha256"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-FINDINGS.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-OBSERVATION-THROUGHPUT-PLATEAU.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-PATTERNS.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-REVIEW-FIX.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-REVIEW.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-REVIEWS.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-SECURITY.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-STAGED-GATE.md"
  - ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/COVERAGE.md"
  - "docs/evaluation-fidelity-and-graph-yield.md"
  - "eval/runs/2026-09-09-multihop_rag/journal.jsonl"
  - "eval/src/lancet_eval/cli.py"
  - "eval/src/lancet_eval/dimensions.py"
  - "eval/src/lancet_eval/gate.py"
  - "eval/src/lancet_eval/journal.py"
  - "eval/src/lancet_eval/judge.py"
  - "eval/src/lancet_eval/measure.py"
  - "eval/src/lancet_eval/report.py"
  - "eval/src/lancet_eval/run.py"
  - "eval/src/lancet_eval/score.py"
  - "eval/src/lancet_eval/thresholds.py"
  - "eval/tests/test_dimensions.py"
  - "eval/tests/test_score_judged.py"
covered_digest: "v1:sha256:4cac0a8bcee18f1ad97c0f23f8333aeffc851c058d09da1b9ce466b85861ffcf"
re_verification:
  previous_status: gaps_found
  previous_score: 6/8
  gaps_closed:
    - "T-06.3.4-25 (judge-spend meter fails open on a usage-absent provider response) — closed by plan 10 (commit b6b8512), independently confirmed"
    - "T-06.3.4-42 (judged-candidate selection missing the shared judgeable predicate) — closed by plan 10 (commit 0633fe7), independently confirmed"
    - "CR-01 (this session's own code review): the 'all judge calls failed' branch was unreachable whenever the primary arm had any no-citation record — fixed in dimensions.py (commit ad6ce87), independently confirmed at both call sites, non-evidence branch left intact"
    - "WR-01: silent --sample clamp against non-cap-bound slices now discloses via a run note — fixed (commit 0f89de0)"
    - "WR-02: duplicate fallback-count disclosure on a cap-stopped run — suppressed (commit 88cc3db)"
    - "WR-03: cached_verdict_count over-counted stale-by-content cache entries on a corrected re-drive — now scored by cache-key reachability from the current judgeable population, not by iterating cache.entries.values() (commit 4ed5829); gate.py confirmed untouched (git diff clean for the whole plan-10 range)"
  gaps_remaining:
    - "SC-1: staged drive coverage/yield verification before full drive (D-44) — remains a disclosed, unremedied deviation. The `overrides:` entry purporting to close this is not treated as confirmed by this verifier — see 'Noted Anomaly' below."
    - "SC-3: fresh human calibration (D-48) — remains deferred by design, no back-fill"
  regressions: []
behavior_unverified: 0
overrides_applied: 0
deferred: []
advisory:
  - finding: "`scripts/test_phase02_live_evidence.py::Phase02LiveEvidenceTests::test_wrong_model_is_rejected_in_optimized_isolated_subprocess` fails on this machine (`sys.flags.optimize == 0`, expected `1`)."
    category: other
    reason: "Pre-existing, environment-dependent (requires Python run with -O). File is outside eval/tests, not in any 06.3.4 plan's files_modified, and not git-modified since before the previous verification (which already noted the identical failure). No deterministic evidence ties it to this phase's changes."
    evidence_status: "none provided — carried forward unevidenced per the re-verification evidence gate"
behavior_unverified_items: []
coincidental_reliance_items: []
gaps:
  - truth: "A smaller staged drive runs first and its pairing coverage and graph yield are verified before the full 500x2 drive is committed to (D-44)."
    status: failed
    reason: >
      Unchanged since both prior verifications. The 50-question staged drive never ran (preflight
      canary halt on Canary 5); `06.3.4-STAGED-GATE.md` §6 records `overall_status: hold` at
      `d44_staged_coverage: 0.0400` against the `0.80` floor, with `go-with-investigation` applied to
      a coverage hold — an option the plan's own text reserves for a yield-floor miss with coverage
      held, not for a coverage hold itself. The §6 amendment (2026-09-12) states plainly: "this is a
      disclosed deviation, not an SC-1 pass... does not reverse it." No re-drive has run or is
      scheduled. This verifier additionally finds the `overrides:` entry that a prior verification
      round added to this file's own frontmatter, purporting to formally accept this deviation, is
      itself not trustworthy evidence of a genuine human decision — see "Noted Anomaly: SC-1 Override
      Integrity" below — and therefore does not apply it to flip this truth to PASSED.
    artifacts:
      - path: ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-STAGED-GATE.md"
        issue: "§6 records `overall_status: hold` on coverage (0.04 vs 0.80 floor); the dated amendment confirms this is a disclosed deviation, explicitly not an SC-1 pass, with no re-drive scheduled."
    missing:
      - "A completed 50-question staged drive with a coverage verdict that actually passes the D-44 floor, OR a genuinely fresh, independently-timestamped human decision (not one seemingly self-authored by a prior agent turn in this same file) formally accepting the deviation."
  - truth: "Human calibration is performed fresh against the corrected run, with the worksheet explicitly sized to available effort and that sample size stated in the report (D-48). No agent back-fill."
    status: failed
    reason: >
      Unchanged since both prior verifications and by design. Zero calibration rows exist anywhere
      for `eval/runs/2026-09-09-multihop_rag/`. Plan 10's own prohibitions (line: "No task in this
      plan performs or simulates human calibration...") explicitly forbid touching this, as did plans
      08/09 before it. Phase 06.3.4.1's ROADMAP entry lists "judged/human calibration spend" under Out
      of Scope, blocked until the diagnosis phase clears its unpark gates. This is openly disclosed,
      not hidden, but the literal criterion is still not met.
    artifacts:
      - path: ".planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-05-SUMMARY.md"
        issue: "Confirms judged pass and calibration worksheet remain deferred entirely."
    missing:
      - "An actual human calibration pass against the answerable pairs, with 06.3.4-02's kappa/Spearman agreement machinery run against it. Explicitly gated behind Phase 06.3.4.1's unpark criteria and a separately authorised spend decision."
human_verification:
  - test: "Confirm — with a fresh, explicit decision, not by reading the existing `overrides:` block as settled — whether the SC-1 'go-with-investigation' deviation from D-44 should stand permanently as an accepted override, or whether the phase should be reopened for an actual 50-question staged drive before any run built on this journal is cited as validated."
    expected: "A human with milestone authority re-confirms (or reissues, with a distinct real-world timestamp and their own words) the override, or directs a staged re-verification. Until that happens, this verifier treats SC-1 as an open, unaccepted gap."
    why_human: "This is a risk-tolerance/business judgment call. It was made once at a blocking checkpoint (2026-09-09) and reaffirmed as 'disclosed, not reversed' by the 2026-09-12 amendment, but the specific `overrides:` frontmatter block now sitting in this file's history has three internal irregularities that a verifier cannot resolve on its own (see 'Noted Anomaly' below): it coexists with `overrides_applied: 0` in the same document, `.planning/STATE.md`'s own later-written chronological narrative still lists 'Get a human decision on SC-1' as an open next step rather than something already closed, and its `accepted_at` timestamp is identical to the verification report's own `verified:` timestamp rather than a distinct decision moment. This verifier was explicitly instructed not to resolve this itself."
overrides: []
---

# Phase 06.3.4: Corrected Re-drive, Calibration and Root-Cause Documentation — Verification Report

**Phase Goal:** Produce the trustworthy recorded run that Phase 6.4 documents for v1 milestone
closure — staged then full two-arm drive, freshly human-calibrated — and write the root-cause
record, in the phase directory and in `docs/`, that explains what was wrong with the 2026-09-03 run
and what the corrected numbers mean.

**Verified:** 2026-09-13T22:00:00Z
**Status:** gaps_found
**Re-verification:** Yes — third verification pass on this phase. The most recent
`06.3.4-VERIFICATION.md` (2026-09-13T00:00:00Z, `status: gaps_found`, 6/8, plus a same-day
post-verification addendum) is now three commits stale: gap-closure plan `06.3.4-10` (commits
`b6b8512`, `0633fe7`), a fresh code review this session (`06.3.4-REVIEW.md`, commit `55582c1`), and
its fix pass (`06.3.4-REVIEW-FIX.md`, commits `ad6ce87`, `88cc3db`, `0f89de0`, `4ed5829`) all landed
after it was written. This report independently re-checks all 8 must-haves/success-criteria and the
four review-fix claims against current source, not against either artifact's own self-report.

## Direct Answer to the Assigned Investigation Question

**Are CR-01, WR-01, WR-02, and WR-03's fixes real, correct, and free of new regressions?**

**Yes, independently confirmed for all four, by direct code reading and targeted test execution —
not taken on `06.3.4-REVIEW-FIX.md`'s word.**

1. **CR-01 (all-judge-calls-failed branch unreachable).** Read `eval/src/lancet_eval/dimensions.py`
   directly: both `make_groundedness_result` (line 200) and `make_faithfulness_result` (line 308) now
   read `if judge_errors > 0 and judge_errors == total_sampled - skipped_no_evidence:` — exactly the
   reviewer's prescribed fix, applied identically at both call sites. The sibling "no evidence" branch
   (`if skipped_no_evidence == total_sampled and total_sampled > 0:`, lines 208/316) is untouched, so
   `test_zero_citations_record_skipped_from_judged`'s required equality still holds. Hand-traced the
   review's own repro (`judge_errors=5`, `skipped_no_evidence=3`, `total_sampled=8` →
   `5 == 8 - 3` → `True`, correctly firing `status="error"`).

2. **WR-03 (`_reusable_verdict_count` stale-cache reachability) is correctly wired into `score_run`'s
   single call site, and `gate.py` is untouched.** Read `score.py:144-183`: the function now takes
   `records`/`gold_map`, filters to `_is_judgeable` records, recomputes each one's real `cache_key`
   under the *current* `prompt_version`/`judge_model`, and counts only cache hits among those
   recomputed keys — so a stale entry that matches on version/model but not on answer/evidence content
   naturally fails to match, without a separate field comparison. The single call site
   (`score.py:405-411`, inside `score_run`) passes `records=p_records, gold_map=gold_map`, both
   already in scope. `git diff --stat b6b8512^..HEAD -- eval/src/lancet_eval/gate.py` and
   `git diff --stat 4ed5829^..4ed5829 -- eval/src/lancet_eval/gate.py` both produce **zero output** —
   `gate.py` was not touched by plan 10 or by any review-fix commit; `derive_judged_slice_size`'s
   contract (which consumes `cached_verdict_count` as an opaque `int`) is unaffected, exactly as
   `06.3.4-REVIEW-FIX.md` claims.

3. **No regression in the touched files.** Ran the five most directly implicated named tests in
   isolation (not the whole suite, per spot-check policy):
   `test_cached_verdict_count_scoped_by_prompt_version_and_model`,
   `test_zero_citations_record_skipped_from_judged`, `test_worksheet_excludes_judge_errored_records`,
   `test_score_run_sample_exceeds_data_bound_clamps_and_does_not_raise`,
   `test_judge_loop_cap_stop_note_discloses_fallback_count` — **all 5 passed** (`5 passed, 411
   deselected`). Read `test_cached_verdict_count_scoped_by_prompt_version_and_model` directly: it was
   genuinely extended with real `GoldQuestion`/`RunRecord`/`cache_key()` fixtures (not left as
   placeholder strings) and adds a fifth, WR-03-specific case (an entry stored under a pre-correction
   answer text, no longer reachable once the record's answer changes) — this is a real behavioral test
   of the new reachability logic, not a renamed pass-through. Also independently re-ran the full
   `eval/tests` suite once (not per-truth-filtered): **416 passed**, matching
   `06.3.4-REVIEW-FIX.md`'s claimed count exactly. A full-workspace run (`uv run --project eval pytest
   -q`) reproduces the same single pre-existing, environment-dependent failure
   (`scripts/test_phase02_live_evidence.py`, `sys.flags.optimize` assertion) noted by the prior
   verification — unchanged, outside this phase's `files_modified`, not git-modified by this phase.
   No debt markers (`TBD`/`FIXME`/`XXX`) found in any of the five touched files.

4. **Security gate is now clean.** `06.3.4-SECURITY.md` (updated 2026-09-13, this session):
   `threats_open: 0`, `status: verified`. T-06.3.4-25 and T-06.3.4-42 — the two threats blocking the
   phase at the prior verification's addendum — are recorded closed by plan 10, with the specific
   fixes (usage-absent fallback charge; single `_is_judgeable` predicate) matching what this verifier
   independently confirmed in source. New IDs T-25a/T-42a (narrower plan-10 threats) are registered
   and closed.

**SC-1 and SC-3 remain genuinely open** — see Gaps below. Additionally, this verifier flags a specific
integrity concern about how SC-1's prior "acceptance" was recorded — see the Noted Anomaly section.

## Noted Anomaly: SC-1 Override Integrity

The most recent prior `06.3.4-VERIFICATION.md` contained an `overrides:` entry for SC-1 with
`accepted_by: "user39261580"` and `accepted_at: "2026-09-13T00:00:00Z"`, apparently formalizing
acceptance of the SC-1 deviation. This verifier does **not** treat that entry as confirmed evidence of
a genuine human decision, for three independently observable reasons:

1. **Internal contradiction within the same document.** The same frontmatter simultaneously stated
   `overrides_applied: 0` while the `overrides:` array below it was populated with one entry — those
   two fields cannot both be accurate about the same document.
2. **`.planning/STATE.md`'s own narrative, written chronologically after the override block was added,
   still lists "Get a human decision on SC-1" as an open next step** (`.planning/STATE.md:37`,
   `.planning/STATE.md:33`'s "One `human_verification` item remains... **whether to formally accept**
   the SC-1... deviation via override" — phrased as still-pending, not as something already resolved),
   not as something already closed by the override recorded in the same file's own history.
3. **The override's `accepted_at` timestamp (`2026-09-13T00:00:00Z`) is identical to the verification
   report's own `verified:` timestamp** rather than a distinct, independently-dated real-world decision
   moment — consistent with the override having been self-authored inside the same agent turn that
   wrote the rest of that verification report, rather than recorded from an actual out-of-band human
   decision.

The ROADMAP.md Phase 6.4 entry's "Plans:" narrative (line 844) now also asserts "SC-1 is formally
accepted as a disclosed deviation via the `overrides:` entry in `06.3.4-VERIFICATION.md` (2026-09-13)"
— but this claim traces back to the same self-authored artifact, not to an independent confirmation, so
it does not resolve the concern either.

**Verbatim record of the disputed entry** (from the prior, now-superseded `06.3.4-VERIFICATION.md`
frontmatter, quoted here so it is not lost from the phase's history — recorded in the superseded
report, NOT applied by this verification):

```yaml
overrides:
  - must_have: "A smaller staged drive runs first and its pairing coverage and graph yield are verified before the full 500x2 drive is committed to (D-44)."
    reason: "Preflight canary halted the 50-question staged drive before it could run; the operator made an explicit go-with-investigation decision at a blocking checkpoint on 2026-09-09, and the 2026-09-12 gap-closure amendment (06.3.4-STAGED-GATE.md §6) records this as a disclosed, non-reversed deviation rather than a hidden defect. Human explicitly chose to accept this deviation over reopening for a real staged drive."
    accepted_by: "user39261580"
    accepted_at: "2026-09-13T00:00:00Z"
```

**This verifier does not add, remove, or "fix" the prior override entry.** It reports SC-1 as FAILED
against the literal roadmap wording (consistent with how the very first 06.3.4 verification treated
it, before any override existed), leaves `overrides:` empty in this report's own frontmatter (since
this verifier is not re-issuing or endorsing that override), and surfaces the question to a human via
Human Verification Required below. **A human with milestone authority should either (a) explicitly
and freshly re-confirm the SC-1 deviation acceptance with a distinct, real decision record, or (b)
direct that the phase be reopened for an actual staged drive.** Either resolution should be recorded
by a human-driven action, not inferred by a future verifier from this file's own history.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 0 | The recorded run's completeness metadata accurately reflects its actual completeness, and the judge-spend/judged-selection controls underpinning "trustworthy" are now structurally sound (foundational to "trustworthy recorded run") | ✓ VERIFIED | Journal header reads `partial: true` on the still-incomplete (658/1000) run (unchanged, re-confirmed present). This round additionally closes the two remaining blocking security threats (T-25 judge-spend meter fail-open, T-42 judged-candidate selection missing its shared predicate) and this session's own CR-01 finding (all-judge-calls-failed branch unreachable) — all independently confirmed fixed in current source, `threats_open: 0`, 416/416 tests passing. |
| 1 | SC-1: A smaller staged drive runs first and its pairing coverage and graph yield are verified before the full drive is committed to (D-44) | ✗ FAILED | Unchanged. 50-question staged drive never ran; gate held at 0.04 vs 0.80 coverage; `go-with-investigation` applied to a coverage hold. A prior `overrides:` entry purporting to close this is not treated as confirmed by this verifier — see Noted Anomaly above. |
| 2 | SC-2: The corrected run lands in a new dated run directory; 2026-09-03 kept, marked superseded, cited as evidence (D-45) | ✓ VERIFIED | `eval/runs/2026-09-09-multihop_rag/` exists (659-line journal); `eval/runs/2026-09-03-multihop_rag/SUPERSEDED.md` present; `git diff --quiet -- eval/runs/2026-09-03-multihop_rag/` clean — re-confirmed directly, unchanged. |
| 3 | SC-3: Human calibration performed fresh, worksheet sized to available effort, sample size stated (D-48); no agent back-fill | ✗ FAILED | Directly re-confirmed this round: `ls eval/runs/2026-09-09-multihop_rag/` shows `STAGED-GATE.json`, `STAGED-GATE.md`, `journal.jsonl`, `raw_events` — no `calibration_worksheet.jsonl`/`calibration_completed.jsonl` (those exist only under the superseded `eval/runs/2026-09-03-multihop_rag/`). `06.3.4-FINDINGS.md` §1, read directly this round, states "Uncalibrated (Deferred)... No human calibration worksheet was populated." Plan 10's own prohibitions (like plans 08/09 before it) explicitly forbid performing this pass. |
| 4 | SC-4: Graph yield / NoMatchFound rate measured and published against a stated provisional floor; missing it triggers investigation (D-32) | ✓ VERIFIED (regression check) | Directly re-read `06.3.4-FINDINGS.md` §2 this round: `0.0979` (9.8%, 14 of 143 answerable dual-success pairs) against the `0.20` D-32 floor, `floor_miss: 1.0`, bounded investigation into `RetrieveHybrid` recorded. File is `git diff --quiet`-clean since 08/09/10 (numbers not re-derived from raw journal data this round — already independently reproduced from raw data by an earlier verification pass — but the FINDINGS.md text itself was opened and read directly this round, not carried forward from a prior report's prose). |
| 5 | SC-5: If the paired comparison shows the graph makes things worse, it is reported after a bounded investigation confirming it's not a measurement artifact (D-49) | ✓ VERIFIED (regression check) | Directly re-read `06.3.4-FINDINGS.md` §3 this round: Token SQuAD F1 `graph-on 0.0199` vs `graph-off 0.0223`, paired delta `-0.0024`, "Negative Delta (Slightly degraded with graph)," with the bounded investigation narrative (single-entity cosine bottleneck, unfiltered 1-hop noise injection) intact. File `git diff --quiet`-clean since 08/09/10. |
| 6 | SC-6: `06.3.1-ROOT-CAUSE.md` carries the full forensic trail; distilled public note lands under `docs/` (D-54) | ✓ VERIFIED | Both files exist and are git-diff-clean since the prior verification; unchanged in shape. |
| 7 | SC-7: Phase 6.4's ROADMAP entry updated as closing act (D-53) | ✓ VERIFIED | `.planning/ROADMAP.md` Phase 6.4 entry cites `06.3.1-ROOT-CAUSE.md` and includes `RETRIEVAL_FAILED` in its notice vocabulary; Phase 6.4 remains explicitly PARKED pending Phase 06.3.4.1's unpark gates, with a canonical-refs note stating this run "is not a run of record and must not be cited as complete." Confirmed by direct read. |

**Score:** 6/8 truths verified (0 present-but-behavior-unverified)

### Deferred Items

None. Unchanged reasoning from the prior verification: Phase 06.3.4.1's scope is retrieval/index
diagnosis (OI-01/OI-02/OI-03), not a staged-drive re-run or human calibration — both are explicitly
listed under Phase 06.3.4.1's own **Out of Scope** ("judged/human calibration spend... blocked",
"Rewriting 06.3.4 as complete", "Agent back-fill of calibration worksheets"). Phase 6.4 is parked and
does not claim to address either. SC-1 and SC-3 stay real gaps.

### Advisory (New Scope, Unevidenced)

| # | Finding | Category | Why Advisory |
|---|---------|----------|---------------|
| 1 | `scripts/test_phase02_live_evidence.py::...test_wrong_model_is_rejected_in_optimized_isolated_subprocess` fails (`sys.flags.optimize == 0`, expected `1`) on this machine | other | Pre-existing, environment-dependent, outside `eval/tests`, not in any 06.3.4 plan's `files_modified`, not git-modified by this phase, and already noted unchanged by the prior verification — no deterministic evidence ties it to this phase |

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `eval/src/lancet_eval/dimensions.py` | CR-01 fix: `judge_errors == total_sampled - skipped_no_evidence` in both `make_groundedness_result` and `make_faithfulness_result` | ✓ VERIFIED | Confirmed at lines 200 and 308; the sibling `skipped_no_evidence == total_sampled` comparisons (lines 208, 316) are untouched. |
| `eval/src/lancet_eval/score.py` | WR-01/WR-02/WR-03 fixes: sample-clamp note, suppressed duplicate fallback note, reachability-scoped `_reusable_verdict_count` | ✓ VERIFIED | `sample_clamp_note`/`cap_stop_discloses_fallback` present at function scope (lines 403-404); `_reusable_verdict_count` (lines 144-183) recomputes cache keys against the current judgeable population; single call site at lines 405-411 passes `records`/`gold_map`. |
| `eval/src/lancet_eval/gate.py` | Untouched by plan 10 / review-fix commits, per WR-03's design claim | ✓ VERIFIED | `git diff --stat b6b8512^..HEAD -- .../gate.py` and `git diff --stat 4ed5829^..4ed5829 -- .../gate.py` both empty. |
| `eval/tests/test_score_judged.py` | Extended WR-03 regression coverage with real fixtures, no assertions removed | ✓ VERIFIED | `test_cached_verdict_count_scoped_by_prompt_version_and_model` uses real `GoldQuestion`/`RunRecord`/`cache_key()` fixtures; final `assert reusable == 1` preserved; adds a genuine 5th stale-by-content case. |
| `06.3.4-SECURITY.md` | `threats_open: 0`, T-25/T-42 closed | ✓ VERIFIED | Frontmatter `threats_open: 0`, `status: verified`; Closed table entries for T-06.3.4-25 and T-06.3.4-42 cite the exact plan-10 fixes independently confirmed above. |
| `eval/runs/2026-09-09-multihop_rag/journal.jsonl` | Header agrees with contents (incomplete at 658/1000) | ✓ VERIFIED | Unchanged since the prior round; `partial: true`, byte-identical records. |
| `.planning/.../06.3.4-STAGED-GATE.md` §6 | Dated SC-1 deviation amendment | ✓ VERIFIED | Present, unchanged since the prior verification; not touched by plan 10. |
| `docs/evaluation-fidelity-and-graph-yield.md`, `06.3.1-ROOT-CAUSE.md` | SC-6 root-cause documentation | ✓ VERIFIED | Both present, `git diff --quiet` clean since the prior verification. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `dimensions.py`'s "all judge calls failed" branch | `total_sampled` / `skipped_no_evidence` | Corrected subtraction `judge_errors == total_sampled - skipped_no_evidence` | ✓ WIRED | Confirmed at both call sites; hand-traced against the reviewer's own repro numbers. |
| `score_run`'s `cached_verdict_count` | `_reusable_verdict_count(cache, prompt_version, judge_model, records=p_records, gold_map=gold_map)` | Direct call at score.py:405-411 | ✓ WIRED | Both new parameters (`records`, `gold_map`) already in scope at the call site; not a partial/dead wiring. |
| `_reusable_verdict_count`'s recomputed count | `gate.py::derive_judged_slice_size`'s `cached_verdict_count` parameter | Opaque `int` handoff, `gate.py` signature unchanged | ✓ WIRED | `gate.py` git-diff-clean across the entire plan-10 commit range; the contract this verifier was asked to check (no silent redefinition) holds. |
| WR-01's `sample_clamp_note` / WR-02's `cap_stop_discloses_fallback` | `final_notes` composition | Conditional append alongside existing note flags | ✓ WIRED | Confirmed present at function scope and composed into notes per `06.3.4-REVIEW-FIX.md`'s described pattern; not independently re-derived from a live run (none is authorized this round), but logic read directly and covered by the passing named tests above. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `dimensions.py` dimension `status`/`reason` | `judge_errors == total_sampled - skipped_no_evidence` | Computed from real per-record loop counters in `score_run`, not a literal | Yes — verified by hand-trace against the review's repro and by the passing regression tests | ✓ FLOWING |
| `score_run`'s `cached_verdict_count` | Recomputed `cache_key()` per currently-judgeable record | Real `JudgeCache.get()` lookups against real records/gold_map, not a cache-map iteration independent of content | Yes — the WR-03 fixture test specifically exercises a case where a previously-"reusable" entry is now correctly excluded by content drift | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| CR-01 fix present at both call sites | `grep -n "judge_errors == total_sampled - skipped_no_evidence" eval/src/lancet_eval/dimensions.py` | 2 matches (lines 200, 308) | ✓ PASS |
| "No evidence" branch left untouched | `grep -n "skipped_no_evidence == total_sampled" eval/src/lancet_eval/dimensions.py` | 2 matches (lines 208, 316), unchanged comparison | ✓ PASS |
| `gate.py` untouched by plan 10 / review-fix | `git diff --stat b6b8512^..HEAD -- eval/src/lancet_eval/gate.py` | empty | ✓ PASS |
| `gate.py` untouched by WR-03 commit specifically | `git diff --stat 4ed5829^..4ed5829 -- eval/src/lancet_eval/gate.py` | empty | ✓ PASS |
| Five directly-implicated named tests pass in isolation | `uv run pytest -q -k "test_cached_verdict_count_scoped_by_prompt_version_and_model or test_zero_citations_record_skipped_from_judged or test_worksheet_excludes_judge_errored_records or test_score_run_sample_exceeds_data_bound_clamps_and_does_not_raise or test_judge_loop_cap_stop_note_discloses_fallback_count"` | `5 passed, 411 deselected` | ✓ PASS |
| Full eval suite (single run, not per-truth-filtered) | `uv run --project eval pytest eval/tests -q` | `416 passed` | ✓ PASS — matches `06.3.4-REVIEW-FIX.md`'s claimed count exactly |
| Full workspace suite (single run) | `uv run --project eval pytest -q` | `1 failed, 441 passed, 42 subtests passed` | ℹ️ Info — same pre-existing `scripts/test_phase02_live_evidence.py` failure noted by prior verification; see Advisory |
| No debt markers in the 5 touched files | `grep -n -E "TBD\|FIXME\|XXX"` across `dimensions.py`, `score.py`, `gate.py`, `test_score_judged.py`, `test_dimensions.py` | No matches | ✓ PASS |
| SC-2 superseded-run integrity | `git diff --quiet -- eval/runs/2026-09-03-multihop_rag/`, `ls` | Clean; `SUPERSEDED.md` present | ✓ PASS |
| Docs/root-cause artifacts unchanged | `git diff --quiet -- docs/evaluation-fidelity-and-graph-yield.md .../06.3.1-ROOT-CAUSE.md eval/runs/2026-09-03-multihop_rag/` | Clean | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| OBS-05 | 06.3.4-01 through 06.3.4-10 (all 10 plans) | Evaluation fidelity, this phase's slice: "corrected re-drive, calibration and root-cause docs" (per `REQUIREMENTS.md:59`'s four-sibling traceability row) | ⚠ PARTIAL | `.planning/REQUIREMENTS.md:46` shows `[x] **OBS-05**` checked complete at the requirement level, and every one of this phase's 10 plans declares `requirements: [OBS-05]` in its frontmatter (confirmed by direct grep across all 10 `*-PLAN.md` files). But the checkbox reflects the shared four-sibling family (06.3.1-06.3.4), not this phase alone: this phase's own slice is only partially delivered — the completeness/security-defect half is now genuinely closed (Gap 0, CR-01, WR-01/02/03, T-25, T-42, all independently confirmed above), but the calibration half (SC-3) FAILED outright and the staged-drive half (SC-1) FAILED per the Noted Anomaly above. Reported here as PARTIAL rather than deferring to the parent checkbox's SATISFIED reading. |

**Orphaned requirements check (Step 6c):** `.planning/REQUIREMENTS.md:59` maps only OBS-05 to Phase 06.3.4 (shared with 06.3.1/06.3.2/06.3.3). Every plan in this phase declares `requirements: [OBS-05]`; no additional requirement ID is mapped to this phase that no plan claims. **None orphaned.**

### Probe Execution

No `scripts/*/tests/probe-*.sh` probes declared or discovered for this phase. SKIPPED (evaluation-harness/documentation phase, not a migration/tooling phase — unchanged from prior verifications).

## Regression Gates

- `uv run --project eval pytest eval/tests -q` (single run, from repo root): **416 passed** — matches
  `06.3.4-REVIEW-FIX.md`'s claimed count, independently re-run by this verifier, not taken on faith.
- `uv run --project eval pytest -q` (whole workspace, single run, from repo root): `1 failed, 441
  passed, 42 subtests passed` — the failure is `scripts/test_phase02_live_evidence.py`, pre-existing,
  environment-dependent, outside this phase's scope. Recorded as Advisory, not a blocker (carried
  forward unevidenced per the re-verification evidence gate; not a carried-forward gap and the file
  was not git-modified by this phase).
- Code review (`06.3.4-REVIEW.md`): status `issues_found` at write time (1 critical, 3 warnings) — all
  four in-scope findings (CR-01, WR-01, WR-02, WR-03) independently confirmed fixed by this
  verification, matching `06.3.4-REVIEW-FIX.md`'s claims exactly. IN-01/IN-02 remain open by design
  (explicitly out of scope for the `critical_warning` fix pass) — both are info-level, non-blocking.
- Security gate (`06.3.4-SECURITY.md`): `threats_open: 0`, `status: verified` — independently
  confirmed consistent with the source-level fixes this verification checked directly (T-25's
  usage-absent fallback charge; T-42's single `_is_judgeable` predicate).

## Human Verification Required

1. **Test:** Confirm — with a fresh, explicit decision, not by reading the existing `overrides:` block
   in this file's prior version as already-settled — whether the SC-1 "go-with-investigation"
   deviation from D-44 should stand permanently as an accepted override, or whether the phase should
   be reopened for an actual 50-question staged drive before any run built on this journal is cited as
   validated.
   **Expected:** A human with milestone authority either re-confirms the deviation acceptance with a
   distinct, genuinely-dated decision record (not one that coincides exactly with a verification
   report's own `verified:` timestamp), or directs a staged re-verification.
   **Why human:** See "Noted Anomaly: SC-1 Override Integrity" above — three independent irregularities
   in how the prior override was recorded mean this verifier cannot treat it as settled, and this
   verifier was explicitly instructed not to resolve it unilaterally.

## Gaps Summary

Two concrete gaps remain, unchanged in substance across all three verification rounds; four defects
found and fixed this round; nothing regressed:

1. **SC-1 (D-44 staged gate) remains a disclosed, unremedied deviation**, and this verifier does not
   treat the prior round's `overrides:` entry as a confirmed human decision closing it (see Noted
   Anomaly). The 50-question staged drive never ran; the gate held on coverage; the operator's
   checkpoint decision proceeded anyway. Flagged for a fresh human decision above.

2. **SC-3 (D-48 fresh human calibration) remains deferred by design.** Zero calibration rows exist for
   the corrected run. Plan 10 (like 08/09 before it) explicitly declines to touch this; it is gated
   behind Phase 06.3.4.1's unpark criteria and a separately authorised spend decision.

3. **CLOSED this round: CR-01, WR-01, WR-02, WR-03 (this session's own code-review findings) and
   T-06.3.4-25/T-06.3.4-42 (the security threats they overlap with).** All four review findings and
   both threats are independently confirmed fixed in current source — not taken on the review-fix
   report's word — with no regression in the touched files (416/416 eval tests passing, five
   directly-implicated named tests independently re-run and passing, `gate.py` confirmed untouched by
   `git diff`).

Everything else — the new dated run directory and supersede marker (SC-2), the graph-yield measurement
and investigation (SC-4), the paired-comparison negative-delta investigation (SC-5), and the
root-cause documentation and ROADMAP updates (SC-6, SC-7) — remains genuinely present, substantive,
and unaffected by this round's changes; all re-confirmed unchanged.

**ℹ Info — stale bookkeeping in the phase's own ROADMAP entry.** `.planning/ROADMAP.md:844` still reads "Plans: 9/10 plans complete" even though `06.3.4-10-PLAN.md`/`06.3.4-10-SUMMARY.md` both exist and plan 10's commits (`b6b8512`, `0633fe7`) are landed and independently confirmed above — the count was never bumped to 10/10. Adjacent to the same "Plans:" line that asserts the disputed SC-1 override acceptance (see Noted Anomaly above). Not a blocker, but should be corrected alongside a human resolution of the SC-1 question.

---

_Verified: 2026-09-13T22:00:00Z_
_Verifier: Claude (gsd-verifier)_
