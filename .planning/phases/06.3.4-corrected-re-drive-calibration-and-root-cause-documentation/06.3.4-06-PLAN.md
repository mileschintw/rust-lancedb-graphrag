---
phase: 06.3.4
plan: 06
type: execute
wave: 6
depends_on: [06.3.4-07]
files_modified:
  - .planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-ROOT-CAUSE.md
  - docs/evaluation-fidelity-and-graph-yield.md
  - eval/runs/2026-09-03-multihop_rag/SUPERSEDED.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [OBS-05]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The full forensic trail exists as 06.3.1-ROOT-CAUSE.md inside the 06.3.1 phase directory, which is where this phase's own success criterion places it (D-54)."
    - "Every forensic number in that document cites the record set it was computed from, and the numbers from the superseded run are computed from its journal rather than read out of its misleading report."
    - "A distilled public-facing note exists under docs/, written for a reader who does not know the evaluation harness, and states in its own text that it is a note Phase 6.4's evaluation pages cite rather than one of those pages (D-54)."
    - "The 2026-09-03 run carries a marker recording that it is superseded, retained as primary evidence, and which of its published numbers are non-comparable — added as a new file, with no existing file in that directory rewritten, moved or deleted (D-45)."
    - "Phase 6.4's ROADMAP entry gains the root-cause document in its canonical references, as this family's closing act (D-53)."
    - "Phase 6.4's fifth success criterion gains the new notice code, spelled as the name actually present in the proto file at the free tag, converted to the vocabulary form by the same prefix-trimming the gateway applies — not by a name assumed at planning time (D-53)."
    - "The ROADMAP edit is scoped: exactly the canonical-reference line and the notice-vocabulary line change, and no other line of the file is touched (specless edge: adjacency)."
    - "An absent notice-code value at the free tag is a recorded unmet input that halts the vocabulary edit, not a licence to write a plausible name (specless edge: empty)."
    - "Every wire notice code present in the proto enum but absent from Phase 6.4's fifth success criterion after the one authorised addition is enumerated by name in this plan's summary as a future decision, so the criterion's remaining shortfall against the wire is visible rather than silently carried into 6.4 (D-53)."
    - "Where a sibling phase has already recorded a verdict this document explains — the decay disposition, the store population — the document quotes that record by path rather than re-deriving or hypothesising the answer."
    - "The two runs remain two separately dated artefacts: the superseded one is cited as evidence throughout the forensic record and is never merged into, overwritten by, or numerically compared with the corrected one (D-45)."
    - "The distilled note does not claim, preempt or duplicate the three docs/ pages Phase 6.4's own first success criterion owns."
  artifacts:
    - ".planning/phases/06.3.1-.../06.3.1-ROOT-CAUSE.md — the full forensic trail, placed in 06.3.1's directory as this phase's success criterion requires"
    - "docs/evaluation-fidelity-and-graph-yield.md — the distilled public-facing note; creates docs/"
    - "eval/runs/2026-09-03-multihop_rag/SUPERSEDED.md — an additive marker; no existing file in that directory is touched"
    - ".planning/ROADMAP.md — Phase 6.4's canonical references and its notice-vocabulary success criterion"
  key_links:
    - "The forensic evidence is eval/runs/2026-09-03-multihop_rag/journal.jsonl, sampled and aggregated rather than read whole, and the findings already distilled from it in 06.3.1-CONTEXT.md"
    - "The corrected numbers, the calibration attestation and both dispositions come from 06.3.4-FINDINGS.md and the corrected run's published report"
    - "The notice-code name is read from proto/lancet/v1/lancet.proto at the free tag, and its vocabulary spelling is derived by the same prefix trim gateway/main.go applies when converting a typed code to a wire string"
    - "Phase 6.4's dependency repointing and its canonical-ref rewording were already executed at split time; only the root-cause reference and the notice-vocabulary addition remain (D-53)"
    - "Placing the root-cause document in the 06.3.1 directory is mandated by this phase's own ROADMAP success criterion and is not a back-edge into a sibling's scope (D-46)"
  prohibitions:
    - "No existing file under eval/runs/2026-09-03-multihop_rag/ may be rewritten, moved or deleted; the marker is a new file added beside them"
    - "The notice-code name must not be hard-coded, guessed, or taken from an illustrative example in a decision record; it is read from the proto file verbatim and halts the edit if absent"
    - "The ROADMAP edit must be a scoped replacement of the two affected lines; the file must never be rewritten wholesale, and no phase entry other than 6.4's may change"
    - "The distilled note must not present itself as, or preempt, the evaluation-methodology, design-narrative or observability pages Phase 6.4 owns"
    - "The superseded run's published figures must not be presented as comparable with the corrected run's; where an old figure is cited it is cited as evidence of the defect, never as a measurement of the system"
    - "No forensic number may be carried forward from a summary or a report without being recomputed from, or attributed to, the record set that produced it"
---

<objective>
Write down what went wrong, why the corrected numbers can be believed, and close Phase 6.4's entry
so the family ends where it said it would.

Purpose: the 2026-09-03 run reported a thousand successes over a system in which 966 queries
produced nothing. That is the failure this family exists to correct, and a corrected run without a
record of what it corrects is just a second set of numbers. Three artefacts carry that record: the
full forensic trail beside the phase that fixed the engine, a short public-facing note a reader
outside this project can follow, and a marker on the old run saying plainly that it is retained as
evidence rather than as a result.

Then the closing act. Phase 6.4 depends on this family and documents its output; two edits to its
ROADMAP entry were deliberately deferred to this moment. One adds the forensic record to what 6.4
must read. The other adds the new notice code to the vocabulary 6.4 documents as part of the API
contract — and the name for it is read out of the proto file rather than assumed, because the
decision record's version of that name was illustrative and was never locked.

Output: `06.3.1-ROOT-CAUSE.md`, the distilled note under `docs/`, the supersede marker, and the two
scoped ROADMAP edits.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-CONTEXT.md
@.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-FINDINGS.md
@.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-STAGED-GATE.md
@.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-PATTERNS.md
@CLAUDE.md
</context>

<placement_note>
`06.3.1-ROOT-CAUSE.md` is written **into the 06.3.1 phase directory**, not into this one. That is
mandated by this phase's own ROADMAP success criterion and by D-54, and it is the reference Phase
6.4's canonical refs gain. It is **not** a back-edge into a sibling's scope under D-46: no plan,
summary or code belonging to 06.3.1 is modified, and nothing about 06.3.1's own work is re-opened.
A document placement required by this phase's success criterion is this phase's own deliverable, and
it must not be relocated into this phase's directory to satisfy a rule it does not violate.
</placement_note>

<tasks>

<task type="auto">
  <name>Task 1: The full forensic trail — 06.3.1-ROOT-CAUSE.md</name>
  <files>.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-ROOT-CAUSE.md</files>
  <read_first>
    - `.planning/phases/06.3.1-.../06.3.1-CONTEXT.md` — the whole decision record, and in particular its existing-code-insights section, which already distils the forensic findings from the broken journal and names which decision each one produced.
    - `eval/runs/2026-09-03-multihop_rag/journal.jsonl` — **sampled and aggregated, never read whole.** Use command-line aggregation to recompute the counts the document states: records by outcome, by arm, by notice code, by node failure, empty-answer count, records carrying an empty index generation, and the position of each failure in the drive order.
    - `eval/runs/2026-09-03-multihop_rag/report.md` — the misleading scored output, read for what it claimed rather than for numbers to reuse.
    - `.planning/phases/06.3.4-.../06.3.4-FINDINGS.md` — the corrected run's calibration attestation, the graph-yield disposition and the paired-comparison disposition.
    - `.planning/phases/06.3.4-.../06.3.4-STAGED-GATE.md` and `06.3.4-04-SUMMARY.md` — the staged gate's observations and the full drive's published numbers and spend.
    - `.planning/phases/06.3.4-.../06.3.4-PATTERNS.md` — the proposed section structure for this document and its stated tone, which has no analog elsewhere in the repository.
    - `.planning/phases/06.3-.../06.3-VERIFICATION.md` and `06.3-REVIEW.md` — the carried-forward defect and the review findings that close in this family. (`06.3-REVIEW.md` names five IDs — CR-01, WR-02, WR-03, WR-04, IN-02 — despite the ROADMAP's "four" phrasing; use the file, not the phrasing.)
    - `.planning/phases/06.3.3-.../06.3.3-MEASUREMENT.md` — the **landed decay verdict** (`decay_present: False`), the restart-boundary discriminator supporting `process_state`, the censoring census and the per-node dispositions. Quote this by path in the decay section; do not hypothesise a cause the sibling already tested.
    - `.planning/phases/06.3.3-.../06.3.3-STORE-BASELINE.md` — the **landed store population** (`graph_disposition: populated`, `unpopulated: false`). Quote by path in the graph-health and yield-floor sections rather than re-deriving.
    - `.planning/phases/06.3.3-.../06.3.3-BUDGETS.md` — the derived budgets and the per-stage caps, for the "what changed as a result" section.
  </read_first>
  <action>
    Write `06.3.1-ROOT-CAUSE.md` as the document a reader consults when they want to know exactly
    what was wrong with the 2026-09-03 run and why the corrected run is different. Technical and
    forensic in tone; no promotion, no reassurance.

    Cover, in this order: a short executive summary that answers the two questions directly — why
    the great majority of records carried empty answers, and why the ablation delta was unreliable;
    the evidence from the journal as a counts table with the node-failure breakdown; the decay
    signature, relating failure to position in the drive order; the false-green breakdown, naming
    each dimension that read healthy over a broken system and what it would have read under the
    corrected definitions; the ablation disjointness analysis, with the pair count and coverage the
    old computation actually rested on; the graph-health breakdown separating timeouts from
    unavailability from designed no-match outcomes; the consequence for the yield floor, namely that
    the true no-match rate was unknowable while traversal was timing out; and finally the
    corrections this family made and what the corrected run therefore establishes.

    Every number in the document is computed from the record set that produced it, and the document
    says which one. Numbers from the broken run come from its journal, aggregated by command, not
    from its report — the report is the thing being explained. Numbers from the corrected run come
    from its published report and from `06.3.4-FINDINGS.md`, cited by dimension or detail key.
    Where a number was carried into a decision record earlier in this family, recompute it and note
    if it differs; a forensic document that propagates an unchecked figure is doing the same thing
    the broken report did.

    Keep the two runs distinct throughout. The old figures are cited as evidence of a defect, never
    as measurements of the system, and never placed beside a corrected figure as though the two were
    a before-and-after of the same quantity — the ablation delta in particular was computed by a
    different method over a nearly empty join and is not comparable.

    Close with what remains open: any investigation `06.3.4-FINDINGS.md` left owed, what would close
    it, and where its conclusion will be recorded. Link to the distilled note task 2 writes.
  </action>
  <verify>
    <automated>R=.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-ROOT-CAUSE.md; test -s $R &amp;&amp; grep -q "06.3.3-MEASUREMENT.md" $R &amp;&amp; grep -q "06.3.3-STORE-BASELINE.md" $R &amp;&amp; ! grep -qiE "^[^|]*\bTBD\b" $R &amp;&amp; S=$(git status --porcelain -- eval/runs/2026-09-03-multihop_rag/) &amp;&amp; test -z "$(printf '%s' "$S" | grep -v 'SUPERSEDED')" &amp;&amp; echo ROOT_CAUSE_WRITTEN</automated>
    <fails_when>The `ROOT_CAUSE_WRITTEN` line is absent from stdout, or the command exits non-zero — which happens when `06.3.1-ROOT-CAUSE.md` is missing or zero-length, when it never cites `06.3.3-MEASUREMENT.md` or `06.3.3-STORE-BASELINE.md` by path (meaning the decay and population sections hypothesised rather than quoted the landed verdicts), when any non-table line still carries an unfilled placeholder token, when `git status` itself exits non-zero, or when its porcelain output names any change under `eval/runs/2026-09-03-multihop_rag/` other than the new `SUPERSEDED.md`. `git status` is captured into a variable before filtering, so a failing `git` surfaces as a failure rather than being swallowed into an empty-and-therefore-clean-looking pipeline.</fails_when>
  </verify>
  <acceptance_criteria>
    - `06.3.1-ROOT-CAUSE.md` exists in the 06.3.1 phase directory and carries all eight sections named in the action, in that order.
    - The executive summary answers both questions — the empty answers and the unreliable delta — in plain prose before any table appears.
    - Every number in the document names the record set it was computed from; no number from the broken run is taken from that run's report rather than its journal.
    - At least one number carried forward from an earlier decision record is recomputed, and the document states whether it matched.
    - The counts table breaks records down by outcome, by arm, by notice code and by node failure, and states the count of records carrying an empty index generation.
    - The decay section relates failure to position in the drive order and then **quotes 06.3.3's landed verdict by path** — `06.3.3-MEASUREMENT.md`'s `decay_present: False` and its restart-boundary discriminator — rather than asserting or re-deriving a cause. The sibling has executed; the hypothesis has an answer and the document states it.
    - The `process_state` explanation is stated as the **supported** hypothesis, in the same epistemic register `06.3.3-MEASUREMENT.md` uses, and is never upgraded to a proven cause. The document says what the restart-boundary discriminator supports and what it does not settle; a root-cause record that overstates its own certainty is the failure mode this whole family exists to correct, and it would be a particularly poor one to commit in the document that explains it.
    - Both spellings of every notice code the document names are given: the proto form (`NOTICE_CODE_RETRIEVAL_FAILED`) and the wire form clients actually observe after `gateway/main.go:543-554` trims the prefix (`RETRIEVAL_FAILED`). The existing ROADMAP criterion already uses the trimmed form, so a document naming only the proto form would read as describing a different vocabulary than the one 6.4 documents.
    - The closing section quotes `06.3.4-FINDINGS.md`'s dispositions **by section heading**, reproducing each disposition's own wording for the headline outcome rather than paraphrasing it. A negative paired delta in particular is carried across in the terms 06.3.4-07 recorded it, since a summary is exactly where an unflattering result would soften into a milder sentence without anyone deciding to soften it.
    - The graph-health and yield-floor sections quote `06.3.3-STORE-BASELINE.md`'s `graph_disposition: populated` / `unpopulated: false` by path, so the reader can see that the yield floor applies rather than being suspended as an ingest finding.
    - The false-green section names each dimension that read healthy over a broken system and states what the corrected definition changes about it.
    - The graph-health section separates timeouts, unavailability and designed no-match outcomes, and states explicitly that the true no-match rate was unknowable while traversal was timing out.
    - The old ablation figure is present only as evidence of the defect, with an explicit statement that it is not comparable with the corrected paired figure.
    - The closing section names every investigation left open by `06.3.4-FINDINGS.md`, what would close each, and where its conclusion will be recorded.
    - No existing file under `eval/runs/2026-09-03-multihop_rag/` is modified, and the journal was aggregated rather than read whole; the summary records the aggregation commands used.
  </acceptance_criteria>
  <done>
    A reader can reconstruct, from one document and the journal it cites, exactly how a thousand
    reported successes were produced by a system that mostly did not work.
  </done>
</task>

<task type="auto">
  <name>Task 2: The distilled public note, and the marker that says the old run is evidence</name>
  <files>docs/evaluation-fidelity-and-graph-yield.md, eval/runs/2026-09-03-multihop_rag/SUPERSEDED.md</files>
  <read_first>
    - `.planning/phases/06.3.1-.../06.3.1-ROOT-CAUSE.md` as written by task 1 — the source this note distils.
    - `.planning/phases/06.3-.../06.3-10-SUMMARY.md` lines 110-119 — the methodological-caveats section, which is the house style this note follows: a numbered list, one plain sentence per item.
    - `.planning/ROADMAP.md` Phase 6.4, success criteria 1 and 4 — the three `docs/` pages and the honest-limitations section 6.4 owns, which this note must serve without preempting.
    - `.planning/phases/06.3-.../06.3-VERIFICATION.md` — the precedent for a phase naming a defect it carries forward, which the supersede marker follows in shape.
    - `.gitignore:61-63` — which run directories are tracked, so the marker lands in a tracked path.
  </read_first>
  <action>
    Two short documents, both for readers who will not read the forensic trail.

    First, `docs/evaluation-fidelity-and-graph-yield.md`. This creates the `docs/` directory. Write
    it in the house style the earlier run summary established: a numbered list, one plain sentence
    per item, no evaluation-harness jargon left unexplained. Cover five things and stop: what broke;
    why it broke; why the numbers nevertheless looked healthy; what changed as a result; and why the
    corrected run is believed — naming the staged validation, the fresh human calibration and the
    measurement pass that separated a budget problem from a resource-exhaustion problem.

    State in the note's own opening that it is a short note the evaluation-methodology and results
    page cites, not that page itself. Phase 6.4's first success criterion owns three `docs/` pages
    including that one; this note exists so their narrative has something to point at, and it must
    not claim their scope. Leave out the forensic tables and the investigation log — they are one
    link away in the root-cause document, which this note references by path.

    Carry the corrected run's headline calibration state and both dispositions in one sentence each,
    including an unflattering one if that is what `06.3.4-FINDINGS.md` recorded. A distilled note
    that quietly drops the inconvenient half is the same failure in a smaller font.

    Second, `SUPERSEDED.md` in the 2026-09-03 run directory. A new file beside the existing seven —
    nothing there is rewritten, moved or deleted; that run is the primary evidence behind every
    forensic number in this family and behind the corrected run's justification. The marker states
    that the run is superseded and by which dated run; that it is retained deliberately as evidence
    under D-45; which of its published figures are non-comparable and why; and where the full
    explanation lives. It is a signpost for someone who opens the directory expecting results, so
    keep it to a handful of lines.
  </action>
  <verify>
    <automated>test -s docs/evaluation-fidelity-and-graph-yield.md &amp;&amp; test -s eval/runs/2026-09-03-multihop_rag/SUPERSEDED.md &amp;&amp; grep -q "06.3.1-ROOT-CAUSE.md" docs/evaluation-fidelity-and-graph-yield.md &amp;&amp; grep -q "06.3.1-ROOT-CAUSE.md" eval/runs/2026-09-03-multihop_rag/SUPERSEDED.md &amp;&amp; ! grep -qiE "^[^|]*\bTBD\b" docs/evaluation-fidelity-and-graph-yield.md &amp;&amp; S=$(git status --porcelain -- eval/runs/2026-09-03-multihop_rag/) &amp;&amp; test -z "$(printf '%s' "$S" | grep -v 'SUPERSEDED')" &amp;&amp; T=$(git ls-files eval/runs/2026-09-03-multihop_rag/) &amp;&amp; test "$(printf '%s' "$T" | grep -v 'SUPERSEDED' | grep -c .)" -ge 7 &amp;&amp; echo DOCS_AND_MARKER_OK</automated>
    <fails_when>The `DOCS_AND_MARKER_OK` line is absent from stdout, or the command exits non-zero — which happens when either document is missing or zero-length, when either fails to reference `06.3.1-ROOT-CAUSE.md` by path, when the note still carries an unfilled placeholder token on a non-table line, when `git status` or `git ls-files` itself exits non-zero, when the porcelain output names any change under `eval/runs/2026-09-03-multihop_rag/` other than the new `SUPERSEDED.md`, or when fewer than seven pre-existing tracked files remain tracked in that directory. Both `git` invocations are captured into variables before filtering, so a failing `git` cannot read as a clean empty result.</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/evaluation-fidelity-and-graph-yield.md` exists, creating `docs/`, and follows the numbered-list house style with one plain sentence per item.
    - The note's opening states that it is a note Phase 6.4's evaluation pages cite rather than one of those pages, and names the three pages that criterion owns so the boundary is explicit.
    - The note covers exactly the five topics named in the action and carries no forensic table or investigation log; it references the root-cause document by path for those.
    - The note states the corrected run's calibration state and both dispositions in one sentence each, including an unflattering disposition where `06.3.4-FINDINGS.md` recorded one.
    - `SUPERSEDED.md` exists in the 2026-09-03 run directory, states the retention rationale, names its non-comparable figures, and points at the root-cause document.
    - `SUPERSEDED.md` names the superseding run as the **literal directory path** pinned in `06.3.4-DRIVE-PRECONDITIONS.md` — not "the corrected run" and not a newest-match description — so a reader landing in the superseded directory is carried to exactly one place. The summary records the literal path written and states that it matches the pinned value.
    - The verify command exits clean: nothing under the superseded run directory is modified, and its seven pre-existing tracked files are all still tracked.
    - The summary records that the marker was added as a new file and that no pre-existing file in that directory was rewritten, moved or deleted.
  </acceptance_criteria>
  <done>
    Someone who opens the old run directory learns immediately that it is evidence rather than a
    result, and someone reading the project's documentation gets the short version of what happened
    without having to read the long one.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close Phase 6.4's entry — the root-cause reference and the notice vocabulary</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` Phase 6.4's entry — its canonical-reference line, which already names this family and the corrected run after the split-time edit, and its fifth success criterion, which enumerates the notice vocabulary by name.
    - `.planning/phases/06.3.1-.../06.3.1-CONTEXT.md` D-53 — which parts were executed at split time and which two remain for this moment; and D-28, whose enum name is illustrative there and explicitly not locked.
    - `proto/lancet/v1/lancet.proto` — the notice-code enum, its reserved tags and the free tag the new code occupies.
    - `gateway/main.go` around the notice-code conversion — how a typed code becomes the wire string an API client sees, which is the spelling the documented vocabulary must match.
  </read_first>
  <action>
    Two scoped replacements in Phase 6.4's entry, and nothing else in the file. Use a scoped edit,
    never a whole-file rewrite: the roadmap holds every other phase in the project and a wholesale
    write would destroy entries outside the edit window.

    First, the canonical references. Add the root-cause document by its path, described as the
    forensic record of the 2026-09-03 collapse and the rationale for the correction, so 6.4's
    planner reads it before writing the evaluation narrative. Name the distilled `docs/` note in the
    same sentence: D-54 places that note squarely in 6.4's design-narrative and honest-limitations
    path, and a reference 6.4 never sees does not serve the purpose it was written for. Record this
    as a deliberate reading of D-53 rather than a silent widening. Keep the addition inside the
    existing line — do not rewrap the surrounding prose.

    Second, the notice vocabulary in the fifth success criterion. Read the enum value that occupies
    the free tag in `proto/lancet/v1/lancet.proto` and take its name **verbatim**. Derive the
    vocabulary spelling by trimming the shared prefix, exactly as the gateway does when it converts
    a typed code into the wire string a client observes — so what the criterion documents is what an
    API client actually sees. Add that spelling to the existing enumeration, leaving every other
    name and the enumeration's order unchanged.

    If no enum value occupies that tag, the sibling that adds it has not landed. Stop, record the
    unmet input and its producer, and leave the criterion untouched. Do not write the name the
    shared decision record uses as an example — that name was illustrative and was never locked, and
    a documented API vocabulary carrying a spelling the wire does not emit is precisely the class of
    defect this family exists to remove.

    Change nothing else. The other success criteria are untouched, no other phase entry is touched,
    and any further omission observed in that vocabulary — a code present on the wire but absent
    from the list — is recorded as a note in the plan summary for a future decision, not silently
    added here. D-53 authorises one addition.

    That enumeration is **required, not optional**, and it is not a formality: the criterion's list
    is already short of the wire before this plan's single addition. Compare the full `NoticeCode`
    enum in `proto/lancet/v1/lancet.proto` against the names the criterion carries, applying the
    gateway's prefix trim to each, and record every name present in the enum but absent from the
    criterion. As read at planning time the criterion names `RETRIEVAL_DEGRADED` where the wire
    carries the two distinct codes `RETRIEVAL_DEGRADED_DENSE` (tag 11) and `RETRIEVAL_DEGRADED_BM25`
    (tag 16); and it omits `MODEL_NOTICE` (4), `MODEL_WARNING` (5), `BASIS_RECONCILED` (15) and
    `GRAPH_ABLATION` (18), while covering tags 20–22 only by the loose phrase "index-staleness
    codes". Re-derive the list at execute time from the proto rather than copying this paragraph —
    it is an illustration of the shortfall's shape, not a substitute for reading the file — and
    record what is actually observed. Documenting a vocabulary that does not match `extractNoticeCodes`
    is the same class of defect this family exists to remove, so 6.4 must inherit the gap in writing
    rather than by omission.
  </action>
  <verify>
    <automated>uv run --project eval python -c "import re,subprocess,difflib; src=open('proto/lancet/v1/lancet.proto',encoding='utf-8').read(); m=re.search(r'(NOTICE_CODE_[A-Z0-9_]+)\s*=\s*19\s*;',src); assert m, 'no NoticeCode value at the free tag: the sibling adding it has not landed; halt and record the unmet input'; name=m.group(1)[len('NOTICE_CODE_'):]; sect=lambda ls: (lambda i: ls[i:next((k for k in range(i+1,len(ls)) if ls[k].startswith('### Phase ')),len(ls))])(next(k for k,l in enumerate(ls) if l.startswith('### Phase 6.4:'))); cur=sect(open('.planning/ROADMAP.md',encoding='utf-8').read().splitlines()); prev=sect(subprocess.run(['git','show','HEAD:.planning/ROADMAP.md'],capture_output=True,text=True,encoding='utf-8').stdout.splitlines()); sc=[l for l in cur if 'notice-code vocabulary' in l]; assert len(sc)==1, f'expected one notice-vocabulary line in the 6.4 entry, found {len(sc)}'; assert name in sc[0], f'{name} absent from the notice vocabulary line'; d=[l for l in difflib.unified_diff(prev,cur,n=0) if l[:1] in '+-' and not l.startswith(('+++','---'))]; adds=[l for l in d if l.startswith('+')]; dels=[l for l in d if l.startswith('-')]; assert len(adds)==2 and len(dels)==2, f'6.4 entry edit not scoped to two lines: +{len(adds)} -{len(dels)}'; assert '06.3.1-ROOT-CAUSE.md' in ' '.join(adds), 'root-cause document missing from the canonical references'; print('ROADMAP_CLOSED', name)"</automated>
    <fails_when>The `ROADMAP_CLOSED` line is absent from stdout, or the command exits non-zero on any of its assertions: `no NoticeCode value at the free tag` (the producing sibling has not landed — halt and record the unmet input rather than writing a plausible name); `expected one notice-vocabulary line in the 6.4 entry` (the criterion moved or was duplicated, so re-locate the target lines before editing); `<name> absent from the notice vocabulary line`; `6.4 entry edit not scoped to two lines` with its `+N -N` counts (the edit touched more of the entry than the two authorised lines); or `root-cause document missing from the canonical references`.</fails_when>
  </verify>
  <acceptance_criteria>
    - Phase 6.4's canonical-reference line names `06.3.1-ROOT-CAUSE.md` by path with a description of what it contains, and names the distilled `docs/` note; the summary records the reading of D-53 that justifies including the note.
    - The notice-code name added to the fifth success criterion is byte-identical to the proto enum value at the free tag with the shared prefix trimmed, and the summary records the proto line it was read from.
    - The summary states that the name was read from the proto file rather than taken from the decision record's illustrative example.
    - If the proto carries no value at that tag, the criterion is left untouched, the verify command halts on its first assertion, and the summary records the unmet input and its producing sibling.
    - Every other name in the vocabulary enumeration, and its order, is unchanged.
    - The verify command proves the edit is scoped to exactly two replaced lines **of Phase 6.4's entry**; no other success criterion within that entry changed. The diff is deliberately sliced to the `### Phase 6.4:` section of both the working tree and `HEAD` rather than taken over the whole file: execute-phase flips plan checkboxes elsewhere in `ROADMAP.md`, and a whole-file diff would fail spuriously on any such flip that is staged-but-uncommitted when this task runs.
    - The summary carries a table enumerating **every** `NoticeCode` value present in `proto/lancet/v1/lancet.proto` but absent from the fifth success criterion after the one authorised addition, each with its tag number and its prefix-trimmed wire spelling, marked as a note for a future decision. The table is derived at execute time from the proto file, not copied from this plan's illustration, and none of the enumerated names is added to the criterion here — D-53 authorises exactly one addition.
    - The summary states the criterion's `RETRIEVAL_DEGRADED` entry against the wire's two distinct codes, if that discrepancy still holds at execute time, so 6.4 inherits it in writing rather than by omission.
    - The edit was applied as a scoped replacement; the summary records that the file was not rewritten wholesale.
  </acceptance_criteria>
  <done>
    Phase 6.4 now points at the record of what went wrong, and the vocabulary it will document
    matches the codes the wire actually emits.
  </done>
</task>

</tasks>

<out_of_scope>
| Item | Owner | Why not here |
|---|---|---|
| The three `docs/` pages — design narrative, observability walkthrough, evaluation methodology and results | Phase 6.4 (SC 1) | This plan writes one short note those pages cite; writing them here would preempt the phase that owns them. |
| The honest-limitations section and the four diagrams | Phase 6.4 (SC 3, SC 4) | Same reason; the note supplies material, not the section. |
| Adding the new notice code to the proto, the engine or the gateway | 06.3.1 (D-28, D-52) | This plan reads the name that phase lands and documents it; it does not create it. |
| Promoting the debt backlog and the v1 requirements reconciliation | Phase 6.4 (SC 6, SC 7) | Milestone closure is that phase's work. |
| The agreement figure and the calibration attestation | 06.3.4-05 | Quoted here from `06.3.4-FINDINGS.md`; not re-derived. |
| The graph-yield disposition and the paired-comparison disposition | 06.3.4-07 | Quoted here from `06.3.4-FINDINGS.md`, appended beneath 06.3.4-05's section; not re-derived. Split from 06.3.4-05 into its own plan during plan review (see 06.3.4-07's revise-before-execute ledger). |
| Deleting, rewriting or reorganising the 2026-09-03 run | Nobody — prohibited by D-45 | It is the primary evidence behind every forensic number this family rests on. |
| Repointing Phase 6.4's dependency and rewording its canonical-ref note | Already executed at split time (D-53) | Only the root-cause reference and the notice-vocabulary addition remained. |
</out_of_scope>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This plan's contribution. The complete phase inventory is the `artifacts_this_phase_produces`
section of `06.3.4-01-PLAN.md`.

| Kind | Artifact |
|---|---|
| New forensic document | `.planning/phases/06.3.1-.../06.3.1-ROOT-CAUSE.md` — the full forensic trail, placed in 06.3.1's directory as this phase's own success criterion requires |
| New public document | `docs/evaluation-fidelity-and-graph-yield.md` — the distilled note; creates `docs/` |
| New run marker | `eval/runs/2026-09-03-multihop_rag/SUPERSEDED.md` — additive only; no existing file in that directory is touched |
| Modified roadmap entry | `.planning/ROADMAP.md` — Phase 6.4's canonical references and its notice-vocabulary success criterion, as two scoped line replacements |
| New CLI flags | None. |
| New dependencies | None. |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| forensic evidence → published explanation | The document is the project's account of its own failure; a propagated or unchecked number repeats the defect it describes. |
| superseded run directory → additive marker | A marker is written into the directory holding the primary evidence; any non-additive write destroys it. |
| proto enum → documented API vocabulary | A documented vocabulary that does not match the wire misleads every client that reads it. |
| roadmap file → every other phase entry | A wholesale write to the roadmap silently destroys entries outside the edit window. |
| corrected run → superseded run | Presenting the two as a before-and-after of the same quantity would fabricate a comparison neither run supports. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.4-29 | Tampering | superseded run directory | high | mitigate | The marker is a new file; both document tasks assert via `git status` that no pre-existing file in that directory is modified, and that its seven tracked files remain tracked. |
| T-06.3.4-30 | Spoofing | documented notice vocabulary | high | mitigate | The name is read from the proto file at the free tag and its wire spelling derived by the same prefix trim the gateway applies; an absent tag halts the edit rather than admitting an assumed name. |
| T-06.3.4-31 | Tampering | the roadmap file | high | mitigate | Scoped replacement only, never a whole-file write; the verify command asserts exactly two lines changed and that one of them carries the root-cause reference. |
| T-06.3.4-32 | Repudiation | forensic numbers | medium | mitigate | Every number names the record set it was computed from; broken-run figures are aggregated from the journal rather than reused from its report, and at least one carried-forward figure is recomputed and checked. |
| T-06.3.4-33 | Information Disclosure | published documents | low | accept | Both documents carry public news-corpus text, model output and this project's own measurements; no credential or personal data appears, and no run artefact carries the provider key. |
| T-06.3.4-SC | Tampering | package-manager installs | high | mitigate | No install task exists. This plan writes documentation and edits one markdown file; `uv lock --project eval --check` proves the lock is unchanged. |
</threat_model>

<verification>
- `06.3.1-ROOT-CAUSE.md` exists in the 06.3.1 phase directory with all eight sections, every number names the record set it came from, and the decay and graph-health sections cite `06.3.3-MEASUREMENT.md` and `06.3.3-STORE-BASELINE.md` by path rather than hypothesising.
- The summary enumerates every wire notice code absent from Phase 6.4's fifth success criterion after the one authorised addition, derived from the proto at execute time.
- `docs/evaluation-fidelity-and-graph-yield.md` exists and states in its opening that it is a note Phase 6.4's pages cite rather than one of them.
- `eval/runs/2026-09-03-multihop_rag/SUPERSEDED.md` exists; no pre-existing file in that directory is modified and all seven remain tracked.
- The ROADMAP edit changes exactly two lines, both inside Phase 6.4's entry.
- The notice-code spelling added to the fifth success criterion matches the proto enum value at the free tag with the shared prefix trimmed.
- `uv lock --project eval --check` confirms no dependency was added.
</verification>

<success_criteria>
- The full forensic trail exists where this phase's success criterion places it, and explains both the empty answers and the unreliable delta from the evidence rather than from the misleading report.
- A short public-facing note exists that a reader outside this project can follow, without preempting the documentation Phase 6.4 owns.
- The 2026-09-03 run is unmistakably marked as retained evidence rather than as results, with nothing in it altered.
- Phase 6.4's entry points at the forensic record and documents a notice vocabulary that matches what the wire emits.
</success_criteria>

<output>
Create `.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-06-SUMMARY.md` when done.
</output>

## Landed sibling contracts (supersedes the original revise-before-execute table)

**Siblings 06.3.1, 06.3.2 and 06.3.3 have all executed and completed.** Read the landed values as
facts — this plan's whole job is to write down what happened, so hypothesising a sibling state that
has already been replaced by a committed record is its central failure mode. Verified against the
working tree during the cross-AI review round (`3bbf435`).

| Formerly-unavailable input | Landed value / location |
|---|---|
| The exact name of the new notice-code enum value | **Landed (06.3.1, D-28).** `proto/lancet/v1/lancet.proto:96` carries `NOTICE_CODE_RETRIEVAL_FAILED = 19`. The halt-if-absent branch in task 3 therefore will not fire; it stays as a guard. Task 3 still reads the name from the file verbatim rather than from this row — a documented vocabulary must match the file, not a planning note. |
| The wire spelling clients observe for that code | **Landed.** `gateway/main.go:543-554` converts by `strings.TrimPrefix(..., "NOTICE_CODE_")`, so the observed spelling is `RETRIEVAL_FAILED`, pinned by `TestRetrievalFailedNoticeRendersAsString`. |
| The rest of the wire vocabulary, for the omissions this plan must enumerate | **Landed.** Tags 1-5, 10-16, 18-22 are all occupied; tag 17 is reserved. The criterion's list is already short of that set before this plan's single addition — see task 3's action for the shape of the shortfall, and re-derive it from the proto at execute time. |
| The corrected run's published numbers, the calibration attestation and both dispositions | **06.3.4-04**, **06.3.4-05** and **06.3.4-07**, in the corrected run's report and in `06.3.4-FINDINGS.md`. Quoted here, never re-derived. |
| Which investigations remain open at the family's close | **06.3.4-07**'s dispositions (split out of 06.3.4-05 during plan review). If the yield floor was missed, the investigation owed before Phase 6.4 publishes is named in both documents this plan writes. |
| Whether the collapse was mis-sized budgets or resource exhaustion — the decay section's conclusion | **Answered (06.3.3, D-18/D-19/D-21).** `06.3.3-MEASUREMENT.md` records `decay_present: False`, with the restart-boundary discriminator (delta -11.3 s, 95% CI [-12.3, -10.5]) supporting the `process_state` hypothesis. The decay section **quotes that verdict by path**; it no longer records a hypothesis and names the pass that would test it. |
| The store population behind the graph-health and yield-floor sections | **Answered (06.3.3).** `06.3.3-STORE-BASELINE.md`: `graph_disposition: populated`, `unpopulated: false`, canary entities present and connected. Quote by path. |
| Which corrected dimension definitions the false-green section contrasts against | **Landed (06.3.2).** The redefinitions — self-contradiction checks, the unified denominator convention, the paired ablation with pairing coverage — are in `eval/src/lancet_eval/dimensions.py` and `score.py`, and are what make the old figures explicable. |
| Whether `docs/` already exists and carries a naming convention | It did not exist at `3bbf435`; this plan creates it. If Phase 6.4 or another phase has since created it, follow the convention in place and record the name actually used. |
| Phase 6.4's own three `docs/` pages | **Phase 6.4** (SC 1), not yet written. The note written here must not preempt them; if 6.4 has since written its evaluation-methodology page, fold this note's material in by reference rather than duplicating it, and record that choice. |
| Trustworthy figures for the superseded run | Nobody — recompute from `eval/runs/2026-09-03-multihop_rag/journal.jsonl` and its artefacts. `06.3.4-PATTERNS.md` is a useful structural guide but carries at least one figure that does not survive checking: that run's worksheet and judge cache each hold **sixteen** entries, matching the sixteen human scores the decision record names, not the smaller figure that document states. Treat every number in it as a hypothesis — which is what task 1's recomputation criterion exists for. |
| Whether the roadmap's Phase 6.4 entry still has the shape this plan edits | The split-time edits to `Depends on:` and the canonical-ref note are in place (`ROADMAP.md:885` already names 06.3.4 and D-53; SC-5 is `ROADMAP.md:892`). If the entry has since changed, re-locate the two target lines before editing and record the lines actually replaced. |

## Review Dispositions Ledger

### Round 1 — 3bbf435

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| Revise-before-execute still lists 06.3.1/06.3.2/06.3.3 as unexecuted; an executor following the "absent → hypothesize" rows would under-cite live artefacts, especially the decay verdict and the notice enum (cursor L219@3bbf435; antigravity L385@3bbf435; consensus #5 L589@3bbf435) | MEDIUM | Plan 06: table replaced by "Landed sibling contracts" carrying tag 19 = `RETRIEVAL_FAILED`, `decay_present: False` with the restart-boundary discriminator figures, `graph_disposition: populated`, and the 06.3.2 dimension surface. This plan is the one where the finding bites hardest, since its whole output is a written record. |
| Quote `06.3.3-MEASUREMENT.md` and `06.3.3-STORE-BASELINE.md` by path in the decay and population sections rather than re-deriving them (cursor L228@3bbf435) | MEDIUM | Plan 06, Task 1: both files added to `read_first` with the specific verdicts to quote; the decay acceptance criterion rewritten from "names which sibling's measurement pass tested that hypothesis" to requiring the landed verdict be quoted by path; a new criterion covers the store-population quote; the verify command now greps for both filenames so a hypothesising draft fails the gate. New `must_haves` truth. |
| ROADMAP SC-5's vocabulary is already incomplete relative to the wire; the summary *must* record the omissions or 6.4 will document a list that does not match `extractNoticeCodes` (cursor L220, L227@3bbf435) | MEDIUM | Plan 06, Task 3: the enumeration is promoted from "recorded as a note" to a **required** summary table with tag numbers and prefix-trimmed spellings, derived from the proto at execute time. The action names the shape of the shortfall observed at planning time — `RETRIEVAL_DEGRADED` against the wire's `_DENSE`/`_BM25` split, plus `MODEL_NOTICE`, `MODEL_WARNING`, `BASIS_RECONCILED`, `GRAPH_ABLATION`, and tags 20–22 covered only as "index-staleness codes" — while requiring re-derivation rather than copying. Two new acceptance criteria; a new `must_haves` truth. D-53's one-addition limit is restated so the enumeration cannot become a silent widening. |
| Tag 19 is no longer free, so the halt-if-absent branch will not run (cursor L207@3bbf435; antigravity L538@3bbf435) | LOW | Plan 06: recorded in the landed-contracts table. The branch is deliberately retained as a guard and task 3 still reads the name from the proto verbatim rather than from any planning note — the reviewers agreed this is the right rule even though the tag is now occupied. |
| `06.3-REVIEW.md` names five finding IDs despite the ROADMAP's "four" phrasing | LOW | Plan 06, Task 1 `read_first` now states this and directs the executor to the file rather than the phrasing. |
| `PATTERNS.md` carries an unreliable worksheet count | LOW | Already in the table; retained and restated with the verified figure (sixteen entries in both the worksheet and the judge cache). |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| POSIX `test` / `grep` in Task 2 and Task 3 verifies fail in native pwsh; replace with Python one-liners (cursor L221@3bbf435; antigravity L542-543, L548@3bbf435; consensus #4 L588@3bbf435) | **Rejected on evidence.** `automated` verify blocks run under Git Bash in this repo. Phase 06.3.3's executor demonstrably ran `D=.planning/...; test -s $D/....md && grep -qiE "isolated" ... && echo store-baseline-complete` to completion — this exact idiom, on this exact machine. Per the verify-command-grounding contract the proven idiom is inherited rather than re-derived. All three verifies are *strengthened* along 06.3.3's lines (`test -s` rather than `test -f`, plus content and placeholder greps) but stay POSIX. |
| Task 3's section slice could over-capture past `## Backlog` (antigravity L544-545@3bbf435) | **No change needed — the reviewer verified it holds.** They confirmed by running Python that the slice from `### Phase 6.4:` to the next `### Phase ` line captures exactly one `notice-code vocabulary` line. The existing assertion `len(sc)==1` is itself the guard if the file's shape ever changes, and its failure message now appears in the task's `<fails_when>`. |

### Round 2 — 2969a4c

Both reviewers rated this plan LOW. Neither cross-plan finding touches it; its four items are
editorial precision in a plan whose entire output is a written record.

#### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| **Task 1's root cause must cite `process_state` as the *supported* hypothesis, not proven; `06.3.3-MEASUREMENT.md` frames it that way and the doc should not upgrade it (Fable, LOW).** | LOW | New acceptance criterion requiring the `process_state` explanation to be stated in the same epistemic register the sibling uses, saying what the restart-boundary discriminator supports **and what it does not settle**, and never as a proven cause. Reasoned in the criterion itself: a root-cause record that overstates its own certainty is the failure mode this family exists to correct, and the document explaining that failure is the worst place to repeat it. |
| **Notice codes are exposed by the gateway with the `NOTICE_CODE_` prefix trimmed; the doc should state both the wire form and the proto form, since the existing ROADMAP line already uses the trimmed form (Fable, LOW).** | LOW | New acceptance criterion requiring both spellings for every notice code the document names — proto `NOTICE_CODE_RETRIEVAL_FAILED` and wire `RETRIEVAL_FAILED` — citing `gateway/main.go:543-554` as where the trim happens. Without it, a document naming only the proto form would read as describing a different vocabulary than the one 6.4's criterion documents. |
| **Add a one-line forward reference from `SUPERSEDED.md` to the corrected run directory's literal path pinned in DRIVE-PRECONDITIONS (Fable, suggestion).** | LOW | Adopted and tightened into its own acceptance criterion: `SUPERSEDED.md` must name the superseding run as the **literal directory path** from `06.3.4-DRIVE-PRECONDITIONS.md`, never "the corrected run" and never a newest-match description, with the summary recording the path written and confirming it matches the pinned value. This is the same anti-newest-match discipline plans 03, 04 and 07 apply to resolution, applied to prose. |
| **Quote 07's dispositions by section heading so 06 cannot "summarize" a negative delta into a softer sentence (Grok).** | LOW | Adopted here rather than in 07, since this is the plan that does the quoting. New acceptance criterion: the closing section quotes `06.3.4-FINDINGS.md` **by section heading** and reproduces each disposition's own wording for the headline outcome rather than paraphrasing — named explicitly for the negative-delta case, because a summary is exactly where an unflattering result softens without anyone deciding to soften it. 06.3.4-07's new append-only prefix-hash ledger gives this quoting a byte range whose integrity is provable. |

#### Review Feedback Deferred / Rejected
| Concern | Reason |
|---------|--------|
| **ROADMAP "exactly two lines" is brittle: a scoped replace that misses `GRAPH_ABLATION` or the DENSE/BM25 split still passes `grep` if `RETRIEVAL_FAILED` is inserted. Leave the summary table of still-undocumented wire codes as an acceptance criterion, not only a summary note (Grok, LOW).** | **Already satisfied in round 1; no further change.** Grok's own diagnosis — that the enumeration rather than the ROADMAP edit is the real check — is exactly the round-1 resolution: the enumeration was promoted from a summary note to a **required summary table** with tag numbers and prefix-trimmed spellings, re-derived from the proto at execute time, backed by two acceptance criteria and a `must_haves` truth. The two-line assertion is deliberately retained as a *scoping* guard (it prevents a whole-file rewrite of ROADMAP.md, threat `T-06.3.4-31`), not as a completeness guard, and it was never asked to carry completeness. Recorded so the finding is visibly closed rather than skipped. |
| **CC-1 / POSIX verify recipes will not run in native PowerShell; Task 3's verify uses `uv` (Fable LOW, Grok LOW).** | **Rejected on primary evidence, measured this round.** Round 1 rejected the general POSIX claim on 06.3.3 precedent; CC-1 is the narrower and different claim that `uv` is absent from bash's PATH, so it was tested rather than assumed: `command -v uv` returns `/c/Users/user3/.local/bin/uv` (exit 0), `command -v python` resolves, `~/.local/bin` is on bash's PATH, and the full composite `RUN=$(uv run --project eval python -c ...) && test -s ... && grep -q ...` idiom executed end to end and printed its sentinel. Fable's own note concedes Task 3's verify "runs fine from PowerShell... the only issue is if run under bash" — and under bash is precisely where it was measured to work. No verify changed. |
