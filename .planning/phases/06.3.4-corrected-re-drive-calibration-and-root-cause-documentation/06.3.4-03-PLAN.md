---
phase: 06.3.4
plan: 03
type: execute
wave: 2
depends_on: [06.3.4-01]
files_modified:
  - .planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-STAGED-GATE.md
  - eval/runs/2026-XX-XX-multihop_rag/journal.jsonl
  - eval/runs/2026-XX-XX-multihop_rag/STAGED-GATE.json
autonomous: false
requirements: [OBS-05]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A smaller staged drive runs before the full two-arm drive, and its pairing coverage and graph yield are computed and compared against the committed floors before the full drive is committed to (D-44)."
    - "The go/no-go decision, both observed values, the suspension determination and the projected full-drive spend against its cap are all recorded before any unlimited drive is invoked."
    - "The staged drive is the sorted prefix of the committed sample and is driven into the same dated run directory the full drive will resume into, so the staged record set is a true prefix rather than a separate run (specless edge: ordering)."
    - "The index generation is read from what the engine reported at drive time and recorded verbatim; it is never written by hand and never copied from the superseded run."
    - "Observed spend for the staged stage is recorded against the cap the human set at 06.3.3's closing decision, and the full drive's projected spend is derived from the staged stage's observed per-query cost rather than from the superseded run's figure."
    - "A staged drive that produced no usable pairs at all yields an explicit verdict naming the empty result, and the full drive is not started on it (specless edge: empty)."
    - "An observed pairing coverage exactly at the floor satisfies it, an observed graph presence rate exactly at the floor satisfies it, and an observed complement rate exactly at the trigger threshold does not fire the trigger (specless edge: adjacency)."
    - "When the store inspection found no nodes, edges or entities, the graph-yield comparison is suspended entirely and the finding is recorded as belonging to ingestion rather than to query yield."
    - "Missing the yield floor triggers a bounded investigation before Phase 6.4 publishes results; it does not fail the run and it does not apply in production (D-32)."
    - "The 2026-09-03 run is untouched by this plan; the staged records land in a separate dated directory and neither run is merged into the other (D-45, specless edge: adjacency)."
  artifacts:
    - "eval/runs/<dated>-multihop_rag/journal.jsonl — extended with the staged prefix's records"
    - "eval/runs/<dated>-multihop_rag/STAGED-GATE.json and its rendered companion — the machine-readable verdict"
    - ".planning/phases/06.3.4-.../06.3.4-STAGED-GATE.md — observed values, comparisons, spend, projection, and the dated go/no-go"
  key_links:
    - "The floors, the trigger and the boundary semantics come from gate.py as landed by 06.3.4-01; nothing here restates them"
    - "The suspension determination comes from 06.3.3-STORE-BASELINE.md's zero-count reclassification verdict"
    - "The per-stage caps come from the dated go/no-go entry in 06.3.3-BUDGETS.md"
    - "The observed coverage and yield come from dimension names and detail keys 06.3.2 defines; an absent key halts the gate rather than defaulting"
    - "The dated run directory is the one 06.3.4-01's micro-slice opened; the resume resolver's date glob is what joins the three drives into one journal"
  prohibitions:
    - "The unlimited drive must not be invoked before the go/no-go entry exists and records a decision to proceed"
    - "No floor, trigger or cap may be changed after a staged number has been observed; a change is a recorded amendment with its own rationale and date, never a silent edit"
    - "A staged drive that missed a floor must not be re-driven until the number improves; the miss is recorded and investigated, and re-driving after a fix is itself a recorded act with its reason"
    - "No file already present in eval/runs/2026-09-03-multihop_rag/ may be rewritten, moved or deleted"
    - "The index generation must not be written by hand into any artefact; it is read from the engine and recorded verbatim"
---

<objective>
Drive the staged prefix, compute the two numbers the staged gate exists to check, compare them
against the floors committed before any of them existed, record the verdict together with what the
staged stage actually cost — and close the plan on the decision to spend, so that decision sits on
a plan boundary rather than in the middle of one.

Purpose: the 2026-09-03 collapse was not visible until the run was over. D-44 exists so that a
partial fix is caught on a hundred queries instead of a thousand. The staged stage is also the only
place a defensible estimate of the full drive's cost can come from: the superseded run's `$0.0199`
is not a comparison point, because that run was cheap precisely because 966 of its queries generated
nothing at all.

Everything this plan asserts in advance is a **threshold**. Everything it produces is a **measured
value** and is recorded, never predicted.

Output: the staged records in the corrected run directory, the machine-readable verdict beside them,
and `06.3.4-STAGED-GATE.md` carrying the observations, the comparisons, the spend and the dated
decision.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-CONTEXT.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-AI-SPEC.md
@.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-01-SUMMARY.md
@.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-DRIVE-PRECONDITIONS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drive the staged prefix into the corrected run directory</name>
  <files>eval/runs/2026-XX-XX-multihop_rag/journal.jsonl</files>
  <precondition>The engine and gateway are running against the isolated eval environment under the budgets 06.3.3 wrote; the provider key is present; `06.3.3-BUDGETS.md` carries a decision to proceed with its three per-stage caps; and `06.3.4-DRIVE-PRECONDITIONS.md` records every drive precondition as met.</precondition>
  <reversibility rating="costly">Provider spend for a hundred queries is not recoverable, and the records land in the run of record. It is bounded by the staged cap and is the smallest stage that can answer the question the gate asks, which is why the staged stage exists at all.</reversibility>
  <read_first>
    - `.planning/phases/06.3.4-.../06.3.4-DRIVE-PRECONDITIONS.md` — every precondition and its observation, in particular the sort-order verdict, the resolved run-directory name, and the standing constraint that nothing may ingest or rebuild an index between the two drives.
    - `.planning/phases/06.3.3-.../06.3.3-BUDGETS.md` — the dated decision entry, its three per-stage caps, and the remaining allowance recorded at that time.
    - `.planning/phases/06.3.3-.../06.3.3-MEASUREMENT.md` — the decay verdict and the per-node dispositions, because a drive started against an unresolved defect disposition measures the defect rather than the system.
    - `eval/src/lancet_eval/cli.py:214-306` — the run command's options and the resume resolver, as landed.
    - `eval/src/lancet_eval/preflight.py` — the preflight checks, including the query-path canary checks 06.3.2 added.
    - `eval/src/lancet_eval/run.py:82-163` — the work-unit cross-product, the resume key set, and how the sampling knob stamps records.
  </read_first>
  <action>
    Run the preflight first and record its full transcript, including every canary row and its
    floors. The canary is the only place a missing graph hit may refuse to start a drive, and it is
    also what proves the budgets 06.3.3 derived are actually in force, because it reads them live.
    If the preflight fails, stop and record which check failed and what it observed. Do not proceed
    past a failed canary.

    Read the index generation the engine reports at preflight and record it verbatim in the plan
    summary. Do not transcribe it from the superseded run's metadata and do not write it by hand:
    966 records in that run carried an empty index generation and were unattributable to any index
    at all, which is part of why this family exists.

    Read and record the provider's reported spend and remaining allowance immediately before the
    drive, by the same mechanism 06.3.3's measurement pass used, so the two figures are comparable.
    If that mechanism is unavailable, record which mechanism was used instead.

    Drive the staged prefix — the sorted first fifty questions of the committed sample, across both
    configured arms — into the corrected run directory, resuming so that the micro-slice's records
    are not re-driven. Confirm before starting that the resolver picks the directory recorded in the
    preconditions ledger and not a new one beside it.

    Read and record the provider's reported spend immediately after the drive. Compute the staged
    stage's observed spend and its observed cost per query, and record both against the staged cap.

    Record the record count the drive reported, the resulting journal length, and the count of
    distinct work-unit keys, so a duplicated or dropped record is visible rather than inferred.
  </action>
  <verify>
    <automated>uv run --project eval python -c "import json,sys,pathlib; p=sorted(pathlib.Path('eval/runs').glob('????-??-??-multihop_rag'))[-1]/'journal.jsonl'; rows=[json.loads(l) for l in p.read_text(encoding='utf-8').splitlines() if l.strip()]; recs=[r for r in rows if 'question_id' in r]; keys=[(r['corpus'],r['question_id'],r['graph_arm']) for r in recs]; gens={r.get('index_generation','') for r in recs if r.get('index_generation')}; print('records',len(recs),'distinct_keys',len(set(keys)),'gens',sorted(gens)); assert len(keys)==len(set(keys)), 'duplicate work-unit key'; assert len(gens)<=1, f'mixed index generations {sorted(gens)}'; print('STAGED_JOURNAL_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The preflight transcript is recorded in full in the plan summary, showing every canary row and the floor it was checked against, and the drive did not start on a failed canary.
    - The index generation is recorded verbatim from what the engine reported at preflight, and the summary states that it was read from the engine rather than written.
    - The resolved run directory name is recorded and matches the name in the preconditions ledger; the summary states the check that confirmed the resolver picked it rather than creating a new one.
    - The staged drive covered the sorted prefix of the committed sample across both configured arms; the summary records the question count, the arm list and the resulting record count as observed.
    - The verify command exits clean: no work-unit key appears twice, and the journal carries at most one distinct index generation.
    - Provider spend and remaining allowance are recorded immediately before and immediately after the stage, the observed stage spend and observed cost per query are computed from them, and both are stated against the staged cap from 06.3.3's decision entry.
    - The summary states that the superseded run's cost figure is not a comparison point and why.
    - `git diff --quiet -- eval/runs/2026-09-03-multihop_rag/` exits clean.
  </acceptance_criteria>
  <done>
    A hundred queries of the corrected system are recorded in the corrected run directory, with the
    index they ran against, what they cost, and no duplicated or dropped work unit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Evaluate the gate and record what was observed, what it was compared against, and what it implies for the full drive</name>
  <files>.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-STAGED-GATE.md, eval/runs/2026-XX-XX-multihop_rag/STAGED-GATE.json</files>
  <read_first>
    - `eval/src/lancet_eval/gate.py` as landed by 06.3.4-01 — the committed floors, the trigger, the boundary semantics, the suspension handling and the verdict shape.
    - `.planning/phases/06.3.3-.../06.3.3-STORE-BASELINE.md` — the graph population counts and the zero-count reclassification verdict, which decides whether the yield comparison is made at all.
    - `.planning/phases/06.3.1-.../06.3.1-AI-SPEC.md` lines 922-996 — the pairing note, its statement that the theoretical maximum coverage is below one because null questions cannot enter the join, and the yield-floor note with its basis and its provisional status.
    - `.planning/phases/06.3.4-.../06.3.4-DRIVE-PRECONDITIONS.md` — the recorded question-type composition of the staged prefix and the count among it carrying no gold evidence.
    - The staged drive's recorded spend, cost per query and cap from task 1's summary.
  </read_first>
  <action>
    Run the staged-gate command over the corrected run directory. It scores the journal offline
    with judging switched off and evaluates the verdict; it must not be replaced with the scoring
    command, which refuses a journal the sampling knob marked partial.

    Then write `06.3.4-STAGED-GATE.md`. It carries four things and nothing that pretends to be a
    fifth.

    First, what was observed: the pairing coverage, the count of pairs behind it, the number of
    answerable questions in the staged prefix, the graph presence rate on usable graph-on records
    with its interval, the graph influence rate beside it, and the complement rate among graph-on
    records whose graph node completed. Each is stated with the dimension or detail key it was read
    from. State beside the coverage figure that the theoretical maximum is below one because null
    questions cannot enter the join, and give the answerable count, so a reader does not read the
    shortfall as failure.

    Second, what each was compared against: the floor or trigger, where it is committed, and the
    outcome of the comparison under the stated boundary semantics. Record the suspension
    determination and, when it applies, record that the yield comparison was not made and that the
    finding belongs to ingestion rather than to query yield. Record that the influence rate has no
    floor and is reported as a diagnostic only.

    Third, what it cost and what the full drive would cost: the staged stage's observed spend, its
    observed cost per query, the staged cap, and a projection of the full drive's spend derived from
    that observed per-query cost and the full drive's work-unit count, stated against the full-drive
    cap from 06.3.3's decision entry, together with the remaining allowance read at the time of
    writing. State plainly that the superseded run's figure is not a basis for this projection and
    why.

    Fourth, the disposition. If a floor was missed, record the bounded investigation this triggers —
    what will be looked at, in what order, and what would distinguish a measurement artefact from a
    real finding — and record explicitly that a miss does not fail the run and does not apply in
    production. If the staged drive produced no usable pairs at all, record that as the finding and
    state that no coverage ratio was computed.

    Leave the decision entry itself empty with a placeholder heading. Task 3 fills it. Writing a
    decision before it has been taken is how a checkpoint becomes ceremonial.
  </action>
  <verify>
    <automated>uv run --project eval lancet-eval staged-gate --run "$(ls -d eval/runs/????-??-??-multihop_rag | tail -1)" &amp;&amp; test -f .planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-STAGED-GATE.md &amp;&amp; echo GATE_RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - The machine-readable verdict file exists in the run directory and the gate command exited without halting on an absent input; if it did halt, the summary names the dimension or detail key that was absent and no verdict was fabricated.
    - Every observed value in `06.3.4-STAGED-GATE.md` names the dimension or detail key it was read from, and no observed value appears without one.
    - Every comparison names its threshold, where that threshold is committed, and the outcome under the stated boundary semantics.
    - The coverage figure is accompanied by the pair count, the answerable-question count, and the statement that the theoretical maximum is below one because null questions cannot enter the join.
    - The suspension determination is recorded, and when it applies the record states that the yield comparison was not made and reclassifies the finding as belonging to ingestion.
    - The record states that the influence rate has no floor.
    - The staged stage's observed spend, its observed cost per query, the staged cap, the projected full-drive spend derived from that per-query cost, the full-drive cap and the remaining allowance are all present, and the record states why the superseded run's figure is not a basis for the projection.
    - If a floor was missed, the record carries a bounded investigation plan naming what will be examined, in what order, and what would distinguish a measurement artefact from a real finding, and states that the miss does not fail the run and does not apply in production.
    - If no usable pairs were produced, the record names that as the finding and carries no computed coverage ratio.
    - The decision section exists with its heading and is explicitly empty pending task 3.
  </acceptance_criteria>
  <done>
    Both gate numbers, everything they were compared against, what the stage cost and what the full
    drive would cost are written down in one place, before anyone is asked whether to spend it.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: Go/no-go for the full two-arm drive — the decision that closes this plan</name>
  <files>.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-STAGED-GATE.md</files>
  <precondition>`06.3.4-STAGED-GATE.md` is complete except for its decision section, so the decision is taken against a finished basis rather than a partial one.</precondition>
  <reversibility rating="one-way">The full drive's provider spend is unrecoverable once used, and the records it writes become the run of record that Phase 6.4 documents for milestone closure. This checkpoint is the authorising gate for that spend and is deliberately placed at the plan boundary.</reversibility>
  <what-built>
    The staged prefix has been driven into the corrected run directory under the budgets 06.3.3
    derived, behind a passing preflight canary. It has been scored offline and evaluated against the
    floors committed in `gate.py` before any of these numbers existed. `06.3.4-STAGED-GATE.md`
    carries the observed pairing coverage and its pair count, the observed graph presence, influence
    and complement rates, the suspension determination, the staged stage's observed spend and cost
    per query, and a projection of the full drive's spend derived from that observed cost.
  </what-built>
  <how-to-verify>
    1. Read `06.3.4-STAGED-GATE.md` end to end — the observations, the comparisons, the spend and
       the projection, and the investigation plan if a floor was missed.
    2. Check that each observed value names the dimension or detail key it came from, and each
       threshold names where it is committed.
    3. Read the preflight transcript in `06.3.4-03-SUMMARY.md` and confirm every canary row passed
       against its floor.
    4. Check the remaining daily allowance on the provider account now, and compare it against the
       projected full-drive spend and the full-drive cap recorded in `06.3.3-BUDGETS.md`.
    5. Answer the decision below.
  </how-to-verify>
  <decision>
    Whether to commit to the full two-arm drive over the whole committed sample, and under what
    spend cap; or to hold while a floor miss is investigated.
  </decision>
  <context>
    This is a `checkpoint:decision` rather than a `checkpoint:human-verify` because it gates work not
    yet done rather than verifying work already done, and is therefore not deferred into an
    end-of-phase batch by `workflow.human_verify_mode = end-of-phase`. It closes the plan
    deliberately, so a hold stops at a clean boundary instead of leaving a plan half-executed.

    Present, before asking: both observed gate numbers with their pair and record counts; each
    threshold and where it is committed; the suspension determination and which branch the yield
    check therefore followed; the staged stage's observed spend and cost per query; the projected
    full-drive spend derived from it; the full-drive cap; and the remaining allowance read now.

    State plainly that the superseded run's figure of `$0.0199` is not a comparison point. That run
    cost almost nothing because 966 of its queries produced no answer at all. A drive in which
    retrieval and generation actually run will cost materially more, and putting the old figure
    beside the new projection invites exactly the wrong inference.

    If a floor was missed, say so directly and present the investigation plan. A missed yield floor
    does not fail the run and does not apply in production — it triggers a bounded investigation
    before Phase 6.4 publishes results. A missed coverage floor is different in kind: it is an
    environment-integrity signal that the full drive would likely reproduce at ten times the cost.

    Both branches close the plan. A recorded hold, with the conditions that would change the answer,
    is a completed plan; an unrecorded pause is not.
  </context>
  <options>
    <option id="go">
      <name>Go — commit to the full two-arm drive under the stated cap</name>
      <pros>The staged stage exercised the corrected system on real queries and the numbers are in view. The corrected run is what Phase 6.4's evaluation documentation depends on for milestone closure; delay costs the milestone directly.</pros>
      <cons>Commits unrecoverable spend at ten times the staged stage's size. Anything the staged prefix's fifty questions did not exercise is still unknown, and the prefix is a sorted slice rather than a random one.</cons>
    </option>
    <option id="hold">
      <name>Hold — investigate the miss first, with the conditions that would change the answer recorded</name>
      <pros>Appropriate when the coverage floor was missed, since the full drive would likely reproduce the same failure at ten times the cost; or when the projected spend exceeds the cap or the remaining allowance.</pros>
      <cons>Everything downstream waits — the judged pass, the human calibration, the root-cause record and Phase 6.4. The conditions must be written down or the hold becomes indefinite.</cons>
    </option>
    <option id="go-with-investigation">
      <name>Go, carrying a recorded yield investigation into the results</name>
      <pros>The correct branch when the yield floor was missed but coverage held: the floor is explicitly provisional, a miss does not fail the run, and the investigation is owed before Phase 6.4 publishes rather than before the drive starts.</pros>
      <cons>Commits the spend before the investigation concludes; if the investigation later finds the yield miss was a measurement artefact, the full drive measured it too.</cons>
    </option>
  </options>
  <resume-signal>
    Reply with the chosen option and, on either go branch, the spend cap for the full drive as a
    figure the next plan can enforce. On a hold, reply with the reason and the conditions that would
    change the answer. Either reply is recorded in `06.3.4-STAGED-GATE.md` with its date and the
    observed basis it rests on.
  </resume-signal>
  <acceptance_criteria>
    - `06.3.4-STAGED-GATE.md` carries a dated decision entry naming who decided and which option was chosen.
    - The entry states the observed basis: both gate numbers, the suspension determination, the staged stage's observed spend and cost per query, the projected full-drive spend, and the remaining allowance read at decision time.
    - The entry states explicitly that the superseded run's cost figure is not a usable comparison, and why.
    - On either go branch, the entry carries a full-drive spend cap as a figure the next plan can enforce.
    - On the branch carrying a yield investigation, the entry names the investigation as owed before Phase 6.4 publishes and points at where its conclusion will be recorded.
    - On a hold, the entry states the reason and the conditions that would change the answer.
    - No unlimited drive is invoked anywhere in this phase before this entry exists and records a decision to proceed.
  </acceptance_criteria>
  <done>
    The commitment to the full drive is taken deliberately, on a plan boundary, against numbers
    observed on the corrected system rather than hoped for, and is recorded with the cap the next
    plan runs under.
  </done>
</task>

</tasks>

<out_of_scope>
| Item | Owner | Why not here |
|---|---|---|
| The full two-arm drive over the whole committed sample | 06.3.4-04 | Authorised by this plan's closing decision, deliberately placed on the boundary. |
| Judging, the calibration worksheet and human scoring | 06.3.4-05 | The staged gate is evaluated with judging switched off; judged spend is a separate capped stage. |
| Publishing a report | 06.3.4-04 | A staged journal cannot be rendered, and the staged numbers are gate inputs, not published results. |
| The final graph-yield disposition and the negative-delta investigation | 06.3.4-05 | Those are taken on the full drive's numbers; the staged stage only decides whether to produce them. |
| Changing a floor, a trigger or a cap | Nobody — prohibited once a number has been observed | A change after observation is a recorded amendment with its own rationale and date. |
| Any change to `eval/runs/2026-09-03-multihop_rag/` | Nobody — prohibited by D-45 | It is the primary evidence behind every forensic number this family rests on. |
</out_of_scope>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This plan's contribution. The complete phase inventory is the `artifacts_this_phase_produces`
section of `06.3.4-01-PLAN.md`.

| Kind | Artifact |
|---|---|
| Extended run artefact | `eval/runs/<dated>-multihop_rag/journal.jsonl` — the staged prefix's records appended to the corrected run |
| New run artefact | `eval/runs/<dated>-multihop_rag/STAGED-GATE.json` and its rendered companion — the machine-readable verdict with its inputs and comparisons |
| New phase record | `06.3.4-STAGED-GATE.md` — observations, comparisons, suspension determination, spend and projection, and the dated go/no-go |
| New CLI flags | None. |
| New dependencies | None. |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staged observation → authorisation of unrecoverable spend | A wrong or fabricated gate number authorises a drive ten times its size. |
| eval harness → paid provider | A hundred real queries against a metered external service. |
| engine-reported index generation → run provenance | A hand-written generation makes the whole record set unattributable, which is the audit failure that produced this family. |
| corrected run directory → superseded run directory | Two dated directories side by side; conflating them destroys the forensic record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.4-11 | Spoofing | gate observations | high | mitigate | Every observed value in the record names the dimension or detail key it was read from; the evaluator halts on an absent key rather than defaulting; the machine-readable verdict is written beside the human-readable one so the two can be compared. |
| T-06.3.4-12 | Elevation of Privilege | unlimited drive invocation | high | mitigate | The `checkpoint:decision` closes the plan and its acceptance criteria state that no unlimited drive runs anywhere in the phase before the decision entry exists; 06.3.4-04's drive task carries the matching precondition. |
| T-06.3.4-13 | Tampering | run provenance | high | mitigate | The index generation is read from the engine at preflight and recorded verbatim; the verify command asserts the journal carries at most one distinct generation, which is also the scorer's own guard. |
| T-06.3.4-14 | Tampering | superseded run directory | high | mitigate | `git diff --quiet` over that directory is an acceptance criterion of task 1; the standing constraint is recorded in the preconditions ledger. |
| T-06.3.4-15 | Denial of Service | provider allowance exhaustion | medium | mitigate | Spend and remaining allowance are read immediately before and after the stage; the projection for the full drive is derived from observed per-query cost and compared against both the cap and the allowance at the checkpoint. |
| T-06.3.4-16 | Repudiation | the spend decision | medium | mitigate | The decision is recorded with its date, its chooser, the option taken and the observed basis, in a committed file rather than in a transcript. |
| T-06.3.4-SC | Tampering | package-manager installs | high | mitigate | No install task exists. This plan runs existing commands; the evaluation contract forbids new Python dependencies and `uv lock --project eval --check` proves the lock is unchanged. |
</threat_model>

<verification>
- The preflight transcript shows every canary row passing against its floor, and is recorded in the plan summary.
- The staged journal carries no duplicated work-unit key and at most one distinct index generation.
- The machine-readable verdict exists in the run directory and its comparison inputs match the committed constants.
- `06.3.4-STAGED-GATE.md` carries observations, comparisons, spend, projection and a dated decision.
- `git diff --quiet -- eval/runs/2026-09-03-multihop_rag/` exits clean.
- `uv run --project eval pytest` still passes.
</verification>

<success_criteria>
- The staged drive's pairing coverage and graph yield were computed and compared against the committed floors, and the verdict plus both observed values plus the suspension determination were recorded before any unlimited drive was invoked.
- The full drive's projected spend rests on the staged stage's observed per-query cost, not on the superseded run's figure.
- The commitment to the full drive is a recorded decision with a date, a chooser, an option and a cap.
</success_criteria>

<output>
Create `.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-03-SUMMARY.md` when done.
</output>

## Revise-before-execute

Read this before executing. Siblings 06.3.1, 06.3.2 and 06.3.3 are **planned and committed but not
executed**; none of their code or output exists yet. Their absence is a sequencing fact, not a defect
in this plan.

| Assumed-unavailable input | Produced by |
|---|---|
| The corrected harness: paired ablation, scored graph and per-path dimensions, unified denominators, the preflight canary | **06.3.2** (not executed). The gate reads its dimension names and detail keys; an absent key halts the evaluation rather than defaulting. |
| Pairing coverage actually achievable on this corpus | **06.3.2**'s pairing implementation plus **this plan's** staged drive. It is a measured value and is nowhere asserted here. |
| The six derived timeout budgets in `config/config.toml` | **06.3.3** (not executed). Task 1's precondition is that the engine runs under them, and the canary is what proves it. |
| Whether the collapse was mis-sized budgets or resource exhaustion — and therefore whether a full drive is safe to start | **06.3.3**'s decay verdict (D-18, D-19, D-21) in `06.3.3-MEASUREMENT.md`, read at task 1. |
| Graph population counts — which decide whether the yield floor applies or is suspended as an ingest finding | **06.3.3**'s store inspection (D-05) in `06.3.3-STORE-BASELINE.md`. |
| Whether graph traversal is inherently too slow, and the latency cost to record for Phase 6.4 | **06.3.3** (D-04). |
| Failure signalling: the new notice code at tag 19, partial-snapshot provenance, the graph-influence counter | **06.3.1** (not executed). The graph influence rate this gate reports depends on the counter; if it has not landed, that is recorded as an absent input rather than reported as a zero rate. |
| Observed graph yield and `NoMatchFound` rate — currently unknown, because the broken run produced 494 graph timeouts against only 4 unavailable results | **This plan's** staged drive, for the first time. |
| The three per-stage spend caps, and the remaining allowance | **06.3.3**'s dated decision entry in `06.3.3-BUDGETS.md` (D-47, shared with 06.3.3). |
| Whether the graph makes results worse | **06.3.4-05**, on the full drive's paired comparison (D-49). Not answered here. |
| The staged-gate evaluator, the committed floors and the resume-safe run directory | **06.3.4-01** (this phase, wave 1). |
| Whether the store's condition changed between 06.3.3's measurement pass and this drive | **06.3.3**'s pass consumes the condition it measures — it appends checkpoint rows and advances store versions, and re-seeding is out of scope. This drive therefore starts from a post-pass store. That is why the preconditions ledger records the store state and why nothing may ingest between the two drives. |

