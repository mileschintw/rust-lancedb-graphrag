---
phase: 06.3.4
plan: 09
type: execute
wave: 8
depends_on: [06.3.4-08]
gap_closure: true
files_modified:
  - eval/src/lancet_eval/judge.py
  - eval/src/lancet_eval/measure.py
  - eval/src/lancet_eval/score.py
  - eval/src/lancet_eval/cli.py
  - eval/src/lancet_eval/gate.py
  - eval/src/lancet_eval/dimensions.py
  - eval/src/lancet_eval/report.py
  - eval/src/lancet_eval/thresholds.py
  - eval/report.schema.json
  - eval/tests/test_judge.py
  - eval/tests/test_gate.py
  - eval/tests/test_score_judged.py
  - eval/tests/test_dimensions.py
autonomous: true
requirements: [OBS-05]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A judged pass cannot start without an explicit per-stage spend cap, and the judge loop stops dispatching once accumulated judged spend reaches it — the same fail-closed shape `drive()` already enforces on the unjudged path (D-47, T-06.3.4-25)."
    - "Judged spend is measurable before it is capped: `judge_once` surfaces the usage the provider already returns and currently discards, so there is something real to meter against the cap rather than an estimate standing in for a measurement."
    - "Judge spend is priced with its own committed constants naming the judge model and the date the price was recorded, because the judge runs a different model at different prices than the generator `compute_spend` prices; reusing the generation constants would under- or over-state the cap by an unstated factor."
    - "`derive_judged_slice_size`'s documented refusal — that a derived slice may never fall below the count of verdicts already cached — is reachable in the one case it exists for: a previously-judged population that a narrower re-run would silently shrink, including when the judgeable count has itself collapsed to zero (T-06.3.4-24, IN-03)."
    - "`derive_judged_slice_size` has a production caller. It is the mechanism that sizes the judged subset in `score_run`, not a function imported by a test and called by nothing (T-06.3.4-24)."
    - "An explicitly requested `--sample` larger than the cap-derived slice size is refused by name rather than silently honoured, because a cap that a flag can exceed is not a cap (D-47)."
    - "A published report states how large the judged slice it committed to was, how many verdicts it actually obtained, and which constraint bound the slice — so a reduced-n judged publication is visible in the artefact rather than reconstructable only from logs (T-06.3.4-43)."
    - "The three judged-slice provenance fields exist in both the Pydantic model and the committed JSON schema, because `eval/report.schema.json` is asserted byte-identical against `emit_schema()` and a model change without a schema regeneration breaks report validation."
    - "The graph-yield investigation floor has one definition in the repository. `dimensions.py` reads it rather than restating `0.20` as a literal, and the relocation does not create an import cycle (IN-02)."
    - "Discovery of a decision input by filesystem glob is deterministic and refuses genuine ambiguity rather than taking whichever path the filesystem happened to return first (WR-03)."
    - "A judged slice sized at exactly the cap-derived bound is committed rather than refused, and a judged slice whose judgeable population is zero with no cached verdicts returns zero with its reason named rather than raising (specless edge: adjacency, empty)."
    - "No task in this plan spends provider money. Every judged path is exercised against a mocked HTTP transport; the controls are built here and exercised against real spend only under a later, separately authorised decision."
  artifacts:
    - "eval/src/lancet_eval/judge.py — `judge_once` returning provider usage alongside the verdict; a `JudgeUsage` model carrying the token counts the response already contains"
    - "eval/src/lancet_eval/measure.py — committed judge price constants naming the model and the date, `compute_judge_spend`, and the per-question judge cost estimator the slice derivation needs"
    - "eval/src/lancet_eval/score.py — `stage_spend_cap` on `score_run`, the metered judge loop with its spend break, and `derive_judged_slice_size` wired as the judged-subset sizing mechanism"
    - "eval/src/lancet_eval/cli.py — `--stage-cap` on the `score` command, required when judging is enabled"
    - "eval/src/lancet_eval/gate.py — reordered `derive_judged_slice_size` guards; deterministic, ambiguity-refusing file discovery in `_find_budgets_file` and `_find_store_baseline_file`"
    - "eval/src/lancet_eval/thresholds.py — the single definition of the graph-yield investigation floor, imported by both `gate.py` and `dimensions.py`"
    - "eval/src/lancet_eval/dimensions.py — the judged-slice state constants and the three provenance values in the groundedness and faithfulness `detail` dicts"
    - "eval/src/lancet_eval/report.py — `judged_slice_committed`, `verdicts_obtained` and `judged_slice_state` on `RunMetadata`"
    - "eval/report.schema.json — regenerated from `emit_schema()` so the byte-identity assertion holds"
    - "eval/tests/test_judge.py, eval/tests/test_gate.py, eval/tests/test_score_judged.py, eval/tests/test_dimensions.py — the cap break, the reachable refusal, the wired sizing, and the provenance fields"
  key_links:
    - "T-06.3.4-43's three fields are populated by T-06.3.4-24's wiring — the fields were absent because the sizing function had no caller to produce them, so wiring the caller and adding the fields are one causal chain, not two coincidental fixes"
    - "`derive_judged_slice_size` needs a `cost_per_question` that does not exist until judge metering lands, so metering precedes the sizing wiring inside this plan; the brief's 'T-24 first' ordering is T-24 before T-43, not T-24 before T-25"
    - "`compute_judge_spend` takes plain token counts rather than a judge type, so `judge.py` never imports `measure.py` and `measure.py` never imports `judge.py`"
    - "`DimensionResult.detail` is typed `dict[str, float]`, so the judged-slice state travels there as a float code following the `CALIBRATION_STATE_*` precedent in `agreement.py`, while `RunMetadata.judged_slice_state` carries the human-readable name from the one mapping that defines both"
    - "`thresholds.py` imports only the standard library, which is why it can hold the graph-yield floor for both `gate.py` and `dimensions.py`; a direct `dimensions.py` → `gate.py` import cycles through `gate.py` → `report.py` → `dimensions.py`"
    - "`gate.py` continues to re-export the graph-yield floor under its existing name so `test_gate.py` and `06.3.4-STAGED-GATE.md`'s `gate.py:GRAPH_YIELD_INVESTIGATION_FLOOR` citation stay valid"
    - "The `score` command's `--stage-cap` mirrors the `run` command's existing required cap, so the two paid entry points enforce D-47 the same way"
  prohibitions:
    - "No task's `<action>` or `<verify>` may invoke `lancet-eval score` or `lancet-eval report` against `eval/runs/2026-09-09-multihop_rag/`; every judged-path command in this plan runs against a synthetic fixture journal under a pytest `tmp_path` with a mocked HTTP transport"
    - "No task in this plan makes a real provider call, starts a judged pass against real data, or spends provider money; the judge is exercised through `pytest_httpx` mocks only"
    - "No task in this plan performs or simulates human calibration, or writes any row of a calibration worksheet; SC-3 stays deferred (D-48)"
    - "The judge price constants must not be invented silently: each carries a comment naming the judge model it prices and the date the price was recorded, and the summary states the source"
    - "Generation and embedding pricing constants must not be reused to price judge calls; the judge runs a different model at different prices and a shared constant would misstate the cap"
    - "No `--sample` value may exceed the cap-derived slice size silently; exceeding it raises and names both numbers"
    - "`eval/report.schema.json` must not be hand-edited; it is regenerated from `emit_schema()` so the byte-identity assertion in `eval/tests/test_schema.py` remains a real check"
    - "The graph-yield floor's value must not change in this plan; IN-02 is a relocation of one definition, not a re-derivation of a committed threshold, and changing a floor after an observation exists requires a dated amendment (D-32)"
    - "No existing test may be deleted, skipped, or have an assertion removed to reach a green suite"
    - "No file already present in `eval/runs/2026-09-03-multihop_rag/` or `eval/runs/2026-09-09-multihop_rag/` may be rewritten, moved or deleted (D-45)"
    - "Nothing in this plan touches OI-01, OI-02 or OI-03, or Phase 6.4's ROADMAP entry"
---

<objective>
Build the three controls that must exist before any future judged or calibration pass touches
metered provider spend, and make a reduced-n judged publication visible in the artefact.

Purpose: `06.3.4-SECURITY.md` surfaced T-06.3.4-24, T-06.3.4-25 and T-06.3.4-43 — three high,
`mitigate`-disposition threats that neither code review nor verification caught, because both were
scoped to the executed drive rather than to the deferred judged pass. `06.3.4-05-PLAN.md` itself
records at line 805 that its Task 1 was supposed to add both a `score` stage cap and spend metering
in the judge loop; the code shows neither landed. They are not exploited today only because judging
was deferred, not because a control exists. Phase 06.3.4.1's Out of Scope section names these three
as the gate that blocks Phase 6.4's judged and calibration spend.

Output: a judged path that cannot start uncapped, a slice-sizing function with a reachable
invariant and a real caller, and three provenance fields carried through the Pydantic model, the
committed JSON schema and both judged dimensions.
</objective>

<gate_overrides>
## Gate overrides recorded visibly — do not read these absences as oversights

The Nyquist Dimension 8 override and the deliberate specless-probe skip that apply to this
gap-closure round are recorded once, in full, in `06.3.4-08-PLAN.md`'s `<gate_overrides>` section.
They apply identically here: there is no `06.3.4-RESEARCH.md` and therefore no
`06.3.4-VALIDATION.md`, and
`SPECLESS_FALLBACK_DISABLED = "OBS-05 edge/prohibition surface already authored across
06.3.4-01..07 at original planning time; single producer per section — not re-run for this
gap-closure round."` This plan's `must_haves` are derived directly from T-06.3.4-24, T-06.3.4-25,
T-06.3.4-43, WR-03 and IN-02. The adjacency and empty edges that are load-bearing here — a slice
sized exactly at the cap-derived bound, and a zero judgeable population with no cached verdicts —
are stated as truths above.

`must_haves` use the plain YAML string-list shape every plan in this phase uses, not the object
form the orchestrator's brief suggested; see `06.3.4-08-PLAN.md` for the reasoning.

**`git diff --quiet -- eval/runs/` is intentionally about the uncommitted working tree.** It runs
inside each task's `<verify>`, before that task's commit, asserting that no run directory was
touched. A pinned revision range would defeat the purpose. `verify.plan-structure`'s R6 warning is
acknowledged and declined on that basis.

**Comment-text discipline allowlist.** One literal a negative gate greps for also appears verbatim
in an `<action>` body, unavoidably, because the action's job is to name the exact construct being
replaced. That gate is scoped to `eval/src/lancet_eval/gate.py`, never to this plan.

<!-- planner-discipline-allow: matches[0] -->
</gate_overrides>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-CONTEXT.md
@.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-SECURITY.md
@.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-REVIEW.md
@.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-PATTERNS.md
@.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-08-PLAN.md
@CLAUDE.md

**Do not open `06.3.4-REVIEWS.md`.** This is a `gap_closure` run, not a `reviews` run.

**Project guideline files.** Every file this plan touches is Python or JSON. Read neither
`rust-guidelines.md` nor `go-guidelines.md`.

**`06.3.4-05-PLAN.md` is referenced by line, not read whole.** It is a 906-line file; the only
passage this plan needs is around line 805, Task 1, where it records that a `score` stage cap and
judge-loop spend metering were supposed to be built. Read that passage; do not load the file.

**Scope of the PATTERNS.md reference.** Loaded for the `06.3.4-NN-SUMMARY.md` pattern assignment
governing `06.3.4-09-SUMMARY.md`. Read it for shapes, never for figures.
</context>

<source_coverage_audit>
This plan's slice of the round's coverage audit. The full table — every VERIFICATION gap, REVIEW
finding, SECURITY threat and CONTEXT decision mapped to a plan and task, including the deferrals
and the SC-3 non-closure — is in `06.3.4-08-PLAN.md`'s `<source_coverage_audit>` section. This plan
owns: SECURITY T-06.3.4-24, T-06.3.4-25 and T-06.3.4-43; REVIEW IN-03, WR-03 and IN-02; and the
D-47 per-stage-cap and D-48 judged-slice-sizing decisions' control surfaces. Nothing in this plan
is unplanned and nothing it owns is deferred.
</source_coverage_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: T-06.3.4-25 — meter judged spend, then cap it</name>
  <files>eval/src/lancet_eval/judge.py, eval/src/lancet_eval/measure.py, eval/src/lancet_eval/score.py, eval/src/lancet_eval/cli.py, eval/tests/test_judge.py, eval/tests/test_score_judged.py</files>
  <precondition>`06.3.4-08` has landed: `uv run --project eval pytest -q` returns exit 0 and `uv run --project eval python -c "import inspect, lancet_eval.score as S; assert 'completeness_comparison' in inspect.getsource(S.score_run)"` succeeds. Task 1 edits `score.py` in a different region than 08 did, and 08's completeness logic must be settled first.</precondition>
  <read_first>
    - `eval/src/lancet_eval/judge.py` lines 186-196 — `_post_judge`, which returns the provider's **whole** `resp.json()`.
    - `eval/src/lancet_eval/judge.py` lines 261-350 — `judge_once`, returning `tuple[JudgeVerdict | None, str | None]`. Note line 322 reads only `choices` from `resp_json`; the OpenRouter `usage` block is present in that same dict and is discarded. That is the hook.
    - `eval/src/lancet_eval/measure.py` lines 45-114 — `GENERATION_INPUT_PRICE_PER_1M`, `GENERATION_OUTPUT_PRICE_PER_1M`, `EMBEDDING_PRICE_PER_1M`, `ESTIMATED_EMBEDDING_TOKENS_PER_QUERY`, and `compute_spend`. This module imports only the standard library and `httpx` — no `lancet_eval` module — which is why the judge pricing belongs here without creating a cycle.
    - `eval/src/lancet_eval/score.py` lines 130-139 — `score_run`'s signature, which the new cap parameter joins.
    - `eval/src/lancet_eval/score.py` lines 403-459 — the judge loop, which today has no spend accumulator and no break condition. The single production `judge_once` call is at line 433.
    - `eval/src/lancet_eval/cli.py` lines 416-473 — the `score` command, which carries no cap parameter of any kind. **Do not confuse this with the `--stage-cap` already on the `measure` command around cli.py:859** — that is Phase 06.3.3's latency-measurement path, a different command entirely.
    - `eval/src/lancet_eval/cli.py` lines 230-326 — the `run` command's existing required `--stage-cap`, the shape this task mirrors.
    - `eval/corpora/multihop_rag.toml` `[models]` — `judge_model = "meta-llama/llama-3.3-70b-instruct"`, `judge_max_tokens = 400`. These are the model and completion ceiling the price constants and the per-question estimator must describe.
    - `eval/tests/test_judge.py` lines 110-215 — the four direct `judge_once` call sites (lines 119, 157, 179, 205) whose tuple arity changes, and the `pytest_httpx` mocking pattern to follow.
  </read_first>
  <behavior>
    - `judge_once` on a successful mocked response returns the verdict, no error, and a usage object
      carrying the prompt and completion token counts from the mocked response body.
    - `judge_once` on a response whose body omits `usage` returns the verdict with a usage of `None`
      rather than raising.
    - `judge_once` on a transport failure returns no verdict, an error string, and no usage.
    - `compute_judge_spend` on known token counts returns the arithmetic product of those counts and
      the committed judge price constants, and does not equal the value `compute_spend` would give
      for the same counts.
    - `score_run` with judging enabled and no `stage_spend_cap` raises a `ScoreError` naming the
      missing cap.
    - `score_run` with judging disabled and no cap does not raise — the unjudged path is unaffected.
    - The judge loop stops dispatching once accumulated judged spend reaches the cap: with a cap set
      below the cost of the second judged question, exactly one judge HTTP call is made and the
      remaining candidates are left unjudged.
    - A cap-stopped judged pass is distinguishable from a completed one in `score_run`'s own return
      value, not only in a log line.
  </behavior>
  <action>
    Build the meter first, then the cap that reads it. **Write the `<behavior>` tests against
    `pytest_httpx` mocks and `tmp_path` journals and watch them fail before changing source. No
    command in this task makes a real provider call or reads any directory under `eval/runs/`.**

    **1. Surface the usage the provider already returns.** `_post_judge` (judge.py:186-196) hands
    `judge_once` the whole response body; `judge_once` reads `choices` at line 322 and throws the
    rest away, including OpenRouter's `usage` block. Add a small `JudgeUsage` pydantic model in
    `judge.py` carrying `prompt_tokens: int` and `completion_tokens: int`, parsed defensively from
    `resp_json.get("usage")` — a missing or malformed block yields `None`, never an exception, since
    a judge verdict must not be lost because a provider omitted an accounting field.

    Change `judge_once`'s return to `tuple[JudgeVerdict | None, str | None, JudgeUsage | None]`.
    This is a breaking signature change with five call sites: `score.py:433` and the four in
    `eval/tests/test_judge.py` (lines 119, 157, 179, 205). Update all five. On the re-ask path,
    accumulate usage across attempts rather than reporting only the last attempt's, because a
    re-asked judge call costs what both attempts cost.

    **2. Price judge calls with their own committed constants.** Add to `measure.py`, beside the
    existing generation and embedding constants: `JUDGE_INPUT_PRICE_PER_1M` and
    `JUDGE_OUTPUT_PRICE_PER_1M`, each with a comment naming the judge model they price
    (`meta-llama/llama-3.3-70b-instruct`, from the corpus `[models]` block) and the date the price
    was recorded. **Do not reuse the generation constants** — the judge runs a different model at
    different prices, and a shared constant would misstate the cap by an unstated factor. Record the
    price source in the summary.

    Add `compute_judge_spend(prompt_tokens: int, completion_tokens: int) -> float` taking plain
    integers, so `judge.py` never imports `measure.py` and `measure.py` never imports `judge.py`.

    Add `estimate_judge_cost_per_question(...)` returning the expected worst-case cost of one judged
    question from the configured `judge_max_tokens` completion ceiling and a committed prompt-token
    estimate constant, following the `ESTIMATED_EMBEDDING_TOKENS_PER_QUERY` precedent already in this
    module. Task 2 needs this value to derive a slice size before any judging has happened, which is
    why it lands here rather than there.

    **3. Add the cap to `score_run` and to the `score` CLI command.** Give `score_run` a
    `stage_spend_cap: float | None = None` keyword parameter. When judging is enabled and the cap is
    `None`, raise `ScoreError` naming the missing cap — fail closed, mirroring the `run` command's
    required cap (D-47). When judging is disabled the cap is irrelevant and its absence must not
    raise, so the whole deterministic path stays usable exactly as it is today.

    Add `--stage-cap` to the `score` command (cli.py:416-473) and forward it. Its help text must
    state that it is required when `--judge` is passed and that it bounds accumulated judged spend.
    Model the option on the `run` command's existing cap, not on the `measure` command's unrelated
    one.

    **4. Meter and break the judge loop** (score.py:403-459). Accumulate judged spend from each
    `judge_once` usage via `compute_judge_spend`. Before dispatching each next judged question,
    compare accumulated spend against the cap and stop dispatching when it is reached — check
    **before** the call, as `drive()` does at run.py:341-345, so the cap bounds what is spent rather
    than merely reporting what was spent. Cached verdicts cost nothing and must not consume cap
    headroom.

    Record the outcome on `score_run`'s own return path, not only in a log line: carry the
    accumulated judged spend and a cap-stopped flag through to the values Task 3 will publish as
    `verdicts_obtained` and `judged_slice_state`. Until Task 3 lands, hold them in locals with the
    names Task 3 will consume so the seam between the two tasks is a rename, not a redesign.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_judge.py eval/tests/test_score_judged.py -q &amp;&amp; uv run --project eval python -c "
import inspect, lancet_eval.judge as J, lancet_eval.measure as M, lancet_eval.score as S, lancet_eval.cli as C
assert 'JudgeUsage' in str(inspect.signature(J.judge_once).return_annotation), 'judge_once still returns a 2-tuple with no usage'
assert hasattr(J, 'JudgeUsage'), 'JudgeUsage model absent'
assert hasattr(M, 'JUDGE_INPUT_PRICE_PER_1M') and hasattr(M, 'JUDGE_OUTPUT_PRICE_PER_1M'), 'judge price constants absent'
assert M.JUDGE_INPUT_PRICE_PER_1M != M.GENERATION_INPUT_PRICE_PER_1M or M.JUDGE_OUTPUT_PRICE_PER_1M != M.GENERATION_OUTPUT_PRICE_PER_1M, 'judge pricing aliases generation pricing'
assert hasattr(M, 'compute_judge_spend'), 'compute_judge_spend absent'
assert 'stage_spend_cap' in inspect.signature(S.score_run).parameters, 'score_run has no stage_spend_cap'
src = inspect.getsource(S.score_run); assert 'compute_judge_spend' in src, 'judge loop is not metered'
assert 'stage-cap' in inspect.getsource(C.score_benchmark), 'score CLI command has no --stage-cap'
print('METER_AND_CAP_OK')
" &amp;&amp; ! grep -rn "compute_spend(" eval/src/lancet_eval/judge.py &amp;&amp; git diff --quiet --exit-code -- eval/runs/ &amp;&amp; echo TASK1_OK</automated>
    <fails_when>The `TASK1_OK` line is absent from stdout, or the command exits non-zero. Specifically: any `<behavior>` test fails — in particular the cap-break test, which asserts exactly one judge HTTP call is made when the cap sits below the cost of the second question, and which fails if the cap is checked after dispatch rather than before; `judge_once still returns a 2-tuple with no usage` or `JudgeUsage model absent` (there is nothing to meter); `judge price constants absent`; `judge pricing aliases generation pricing` (the judge is being priced at the generator's rate, which misstates the cap by an unstated factor); `compute_judge_spend absent`; `score_run has no stage_spend_cap`; `judge loop is not metered`; `score CLI command has no --stage-cap`; `grep` finding a `compute_spend(` call inside `judge.py`, which would mean the module boundary that keeps `judge.py` and `measure.py` acyclic was crossed; or `git diff` reporting any modification under `eval/runs/` — no task in this plan touches a run directory.</fails_when>
  </verify>
  <acceptance_criteria>
    - Every case in the `<behavior>` block has a named test driven through `pytest_httpx`, and the
      summary records that the tests were written and observed failing before source changed.
    - `judge_once` returns `(verdict, error, usage)`. All five call sites are updated: `score.py:433`
      and `eval/tests/test_judge.py` lines 119, 157, 179 and 205. A missing or malformed provider
      `usage` block yields `None` rather than raising, proven by a test.
    - Re-asked judge calls accumulate usage across attempts; a test asserts the reported token counts
      are the sum, not the last attempt's.
    - `measure.py` carries `JUDGE_INPUT_PRICE_PER_1M` and `JUDGE_OUTPUT_PRICE_PER_1M`, each with a
      comment naming `meta-llama/llama-3.3-70b-instruct` and the date the price was recorded. The
      summary states the price source. At least one of the two differs from its generation
      counterpart, and no generation or embedding constant is reused to price a judge call.
    - `compute_judge_spend` takes plain integer token counts. `judge.py` imports nothing from
      `measure.py` and `measure.py` imports nothing from `judge.py`.
    - `estimate_judge_cost_per_question` exists, derives from the configured `judge_max_tokens` and a
      committed prompt-token estimate constant, and is documented as the input Task 2's slice
      derivation consumes.
    - `score_run` accepts `stage_spend_cap`. With judging enabled and no cap it raises `ScoreError`
      naming the missing cap; with judging disabled and no cap it does not raise.
    - The `score` CLI command carries `--stage-cap`, forwards it, and its help text states that it is
      required with `--judge` and that it bounds accumulated judged spend.
    - The judge loop compares accumulated spend against the cap **before** dispatching each next
      question. Cached verdicts consume no cap headroom, proven by a test in which a fully-cached
      population judges to completion under a cap smaller than one live call would cost.
    - A cap-stopped judged pass is distinguishable from a completed one in `score_run`'s return
      value. The locals holding accumulated spend and the cap-stopped flag carry the names Task 3
      will publish them under.
    - `uv run --project eval pytest` reports no failures and no errors. No test was deleted, skipped
      or de-asserted.
    - `git diff --quiet -- eval/runs/` exits clean.
  </acceptance_criteria>
  <done>
    Judged spend is a measured quantity bounded by an explicit cap that must be supplied before a
    judged pass can start — the same fail-closed shape the unjudged drive has enforced since
    06.3.4-01.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: T-06.3.4-24 / IN-03 — make the slice-size refusal reachable and give it a caller</name>
  <files>eval/src/lancet_eval/gate.py, eval/src/lancet_eval/score.py, eval/tests/test_gate.py, eval/tests/test_score_judged.py</files>
  <precondition>Task 1 has landed: `uv run --project eval python -c "import lancet_eval.measure as M; assert hasattr(M,'estimate_judge_cost_per_question')"` succeeds. `derive_judged_slice_size` takes a `cost_per_question` that does not exist until Task 1's estimator lands, so the sizing wiring cannot precede the metering.</precondition>
  <read_first>
    - `eval/src/lancet_eval/gate.py` lines 386-420 — `derive_judged_slice_size` in full. The
      documented refusal ("Refuses a result below cached_verdict_count") is guarded at line 405 by
      `if n_cap < cached_verdict_count or judgeable_count < cached_verdict_count: raise ValueError`,
      but it is preceded at line 397 by `if judgeable_count <= 0: return 0,
      "judgeable_count_is_zero"`. When the judgeable population has collapsed to zero while cached
      verdicts exist, the shortcut fires first and the refusal never runs.
    - `eval/src/lancet_eval/gate.py` lines 51-66 and 137-152 — `_find_budgets_file` and
      `_find_store_baseline_file`, both taking `matches[0]` from an unsorted `Path.glob`.
    - `eval/src/lancet_eval/score.py` lines 345-369 — the existing `calibration_file` guard, whose
      fail-closed style this task mirrors: it refuses a `--sample` narrower than the cached verdict
      count and explains both numbers in the message.
    - `eval/src/lancet_eval/score.py` lines 386-401 — the judged-subset selection. When `sample` is
      given it seeds a deterministic sample; otherwise it takes every primary-arm question. That
      `else` branch is the unbounded path a cap must bound.
    - `eval/src/lancet_eval/score.py` lines 340-343 — `judged_sample_count`, `cache`, `judged_qids`,
      the locals this wiring populates.
    - `eval/tests/test_gate.py` line 19 — `derive_judged_slice_size` is imported here and called by
      nothing. That import is the whole of its current reachability.
  </read_first>
  <behavior>
    - `derive_judged_slice_size(judgeable_count=0, stage_spend_cap=5.0, cost_per_question=0.01,
      cached_verdict_count=3)` raises `ValueError` naming the cached verdict count. This is the case
      the unreachable guard exists for and today returns `(0, "judgeable_count_is_zero")`.
    - `derive_judged_slice_size(judgeable_count=0, ..., cached_verdict_count=0)` still returns
      `(0, "judgeable_count_is_zero")` — a genuinely empty population with nothing cached is not an
      error.
    - A cap-derived bound exactly equal to the judgeable count returns that count with the
      coincident binding, not a refusal.
    - `score_run` with judging enabled sizes its judged subset through `derive_judged_slice_size`,
      and the resulting size is bounded by the cap when the cap is the binding constraint.
    - `score_run` with an explicit `--sample` larger than the cap-derived slice size raises,
      naming both the requested sample and the derived bound.
    - `score_run` with an explicit `--sample` at or below the derived bound honours the sample.
    - `_find_budgets_file` and `_find_store_baseline_file` return the same path across repeated
      calls given the same tree, and raise naming every candidate when more than one matches.
  </behavior>
  <action>
    **Write the `<behavior>` tests first and watch them fail.** The gate tests run offline; the
    `score_run` tests use `tmp_path` journals and `pytest_httpx` mocks. Nothing here reads
    `eval/runs/`.

    **1. Reorder `derive_judged_slice_size`'s guards so the documented invariant is reachable**
    (gate.py:386-420). Compute `n_cap` first — keeping the existing `cost_per_question <= 0` branch
    that falls back to the judgeable count. Then evaluate the `cached_verdict_count` refusal. Only
    then take the `judgeable_count <= 0` shortcut. With that order, a zero judgeable population
    against a non-zero cached verdict count raises as documented, while a zero population with
    nothing cached still returns its named reason rather than an error. Update the docstring to
    state the evaluation order explicitly, since the order is the fix.

    **2. Wire it into `score_run`'s judged-subset selection** (score.py:386-401) so it is the
    mechanism that sizes the judged subset rather than a function nothing calls. Compute its four
    inputs from what `score_run` already has:

    - `judgeable_count` — the number of distinct primary-arm question ids that have gold data, i.e.
      the population the loop could actually judge.
    - `cached_verdict_count` — the count already computed at score.py:346-348 for the
      `calibration_file` guard; reuse that expression rather than recomputing it differently.
    - `cost_per_question` — Task 1's `estimate_judge_cost_per_question`.
    - `stage_spend_cap` — Task 1's new parameter.

    Use the returned slice size as the judged subset size when `--sample` was not given, replacing
    the unbounded `else` branch at score.py:400-401. When `--sample` **was** given, treat the derived
    size as a ceiling: a requested sample at or below it is honoured; a requested sample above it
    raises `ScoreError` naming both numbers, because a cap a flag can exceed is not a cap. Mirror the
    message style of the existing `calibration_file` guard at score.py:345-369 — state what was
    requested, what the bound is, and what to do about it.

    Keep the existing deterministic seeded selection (`sample_questions(..., seed=config.sample_seed)`)
    as the mechanism that chooses *which* questions enter the slice; this task changes *how many*,
    not *which*. Carry the returned binding string ("cap", "data", "coincident") into the local Task 3
    will publish as the judged-slice state.

    **3. WR-03 — make decision-input discovery deterministic and ambiguity-refusing.** In both
    `_find_budgets_file` (gate.py:51-66) and `_find_store_baseline_file` (gate.py:137-152), sort the
    glob matches and raise a `FileNotFoundError`-or-equivalent naming **every** candidate when more
    than one distinct path matches, instead of taking `matches[0]` from an unsorted result. A
    committed decision input selected by whichever path the filesystem returned first is not a
    committed decision input. Exactly one `06.3.3` phase directory exists today, so this changes no
    current behaviour — it closes the case where a second one appears.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_gate.py eval/tests/test_score_judged.py -q &amp;&amp; uv run --project eval python -c "
import inspect, pytest, lancet_eval.gate as G, lancet_eval.score as S
try:
    G.derive_judged_slice_size(judgeable_count=0, stage_spend_cap=5.0, cost_per_question=0.01, cached_verdict_count=3)
    raise SystemExit('IN-03 guard still unreachable: zero judgeable with cached verdicts did not raise')
except ValueError:
    pass
assert G.derive_judged_slice_size(judgeable_count=0, stage_spend_cap=5.0, cost_per_question=0.01, cached_verdict_count=0) == (0, 'judgeable_count_is_zero'), 'empty-population reason lost'
src = inspect.getsource(S.score_run)
assert 'derive_judged_slice_size' in src, 'derive_judged_slice_size still has no production caller'
gsrc = inspect.getsource(G)
assert 'matches[0]' not in gsrc, 'WR-03 unsorted glob index still present'
print('SLICE_SIZING_OK')
" &amp;&amp; git diff --quiet --exit-code -- eval/runs/ &amp;&amp; echo TASK2_OK</automated>
    <fails_when>The `TASK2_OK` line is absent from stdout, or the command exits non-zero. Specifically: `IN-03 guard still unreachable: zero judgeable with cached verdicts did not raise` — the guards were not reordered and the documented refusal is still dead in the one case it exists for; `empty-population reason lost` — the reorder went too far and a genuinely empty population with nothing cached now raises instead of returning its named reason; `derive_judged_slice_size still has no production caller` — T-06.3.4-24's core finding is unchanged and T-06.3.4-43 would remain unpopulatable; `WR-03 unsorted glob index still present`; any `<behavior>` test failing, in particular the `--sample`-exceeds-the-bound refusal, whose absence means a flag can still exceed the cap; or `git diff` reporting a modification under `eval/runs/`.</fails_when>
  </verify>
  <acceptance_criteria>
    - Every case in the `<behavior>` block has a named test, written and observed failing before
      source changed.
    - `derive_judged_slice_size` evaluates the `cached_verdict_count` refusal before the
      `judgeable_count <= 0` shortcut, and its docstring states the evaluation order and why the
      order is load-bearing.
    - A zero judgeable population with a non-zero cached verdict count raises `ValueError` naming the
      cached count. A zero judgeable population with nothing cached still returns
      `(0, "judgeable_count_is_zero")`.
    - A cap-derived bound exactly equal to the judgeable count returns that count with the coincident
      binding rather than being refused.
    - `derive_judged_slice_size` is called from `score_run`'s judged-subset selection. The summary
      names the call site by line and states which of the four inputs came from which existing
      expression, including that `cached_verdict_count` reuses the expression already at
      score.py:346-348 rather than a second, divergent computation.
    - The unbounded `else` branch that took every primary-arm question is replaced by the derived
      size.
    - A `--sample` above the derived bound raises `ScoreError` naming both the requested sample and
      the bound; a `--sample` at or below it is honoured. The message follows the style of the
      existing `calibration_file` guard.
    - Which questions enter the slice is still chosen by the existing seeded deterministic selection;
      only the count changed. The summary states this explicitly.
    - The returned binding string is carried into the local Task 3 publishes as the judged-slice
      state.
    - No `matches[0]` remains in `gate.py`. Both finder functions sort deterministically and raise
      naming every candidate on genuine ambiguity, proven by a test that plants two matching files.
    - `uv run --project eval pytest` reports no failures and no errors. No test was deleted, skipped
      or de-asserted.
    - `git diff --quiet -- eval/runs/` exits clean.
  </acceptance_criteria>
  <done>
    The judged slice is sized by the function that documents the invariant, that invariant fires in
    the case it was written for, and no flag can quietly exceed the cap that bounds it.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: T-06.3.4-43 — publish the judged slice's provenance, and give the yield floor one definition</name>
  <files>eval/src/lancet_eval/report.py, eval/src/lancet_eval/dimensions.py, eval/src/lancet_eval/score.py, eval/src/lancet_eval/thresholds.py, eval/src/lancet_eval/gate.py, eval/report.schema.json, eval/tests/test_score_judged.py, eval/tests/test_dimensions.py</files>
  <precondition>Task 2 has landed: `uv run --project eval python -c "import inspect, lancet_eval.score as S; assert 'derive_judged_slice_size' in inspect.getsource(S.score_run)"` succeeds. The three fields this task publishes are produced by Task 2's wiring; adding them before a caller exists would publish defaults.</precondition>
  <read_first>
    - `eval/src/lancet_eval/report.py` lines 24-46 — `RunMetadata`'s field list, ending `partial: bool = False` and `notes: str = ""`, with `model_config = ConfigDict(extra="forbid")`.
    - `eval/src/lancet_eval/report.py` lines 48-66 — `_validate_non_blank_fields`, the model validator listing the required non-blank string fields.
    - `eval/src/lancet_eval/dimensions.py` lines 16-45 — `DimensionResult`, whose `detail` is typed
      `dict[str, float]`. A string state cannot travel in `detail`; this is why the state is encoded
      as a float code there.
    - `eval/src/lancet_eval/agreement.py` lines 23-25 — `CALIBRATION_STATE_NONE = 0.0`,
      `CALIBRATION_STATE_BELOW_TARGET = 1.0`, `CALIBRATION_STATE_SATISFIED = 2.0`. This is the
      established precedent for encoding an enum state into a float-typed `detail` dict; follow it.
    - `eval/src/lancet_eval/dimensions.py` lines 149-230 and 232-310 — `make_groundedness_result`
      and `make_faithfulness_result`, both keyword-only with defaulted optional parameters. The new
      provenance parameters join that pattern.
    - `eval/src/lancet_eval/dimensions.py` lines 549-556 — `floor_val = 0.20` at line 553, the
      duplicated literal.
    - `eval/src/lancet_eval/gate.py` line 21 (`GRAPH_YIELD_INVESTIGATION_FLOOR: float = 0.20`) and
      its three uses at lines 261, 287 and 296. `eval/tests/test_gate.py` line 15 imports the name
      from `gate`, and `06.3.4-STAGED-GATE.md` §2 cites it as `gate.py:GRAPH_YIELD_INVESTIGATION_FLOOR`.
    - `eval/src/lancet_eval/thresholds.py` lines 1-12 — imports only `dataclasses` and `typing`. It
      imports no `lancet_eval` module, which is what makes it a safe shared home.
    - `eval/src/lancet_eval/report.py` line 13 — `from lancet_eval.dimensions import DimensionResult`.
      Together with `gate.py:13` (`from lancet_eval.report import CorpusReport`) this is the cycle a
      direct `dimensions.py` → `gate.py` import would close: dimensions → gate → report → dimensions.
    - `eval/tests/test_schema.py` lines 16-24 — `test_schema_file_byte_identical`, which asserts the
      committed `eval/report.schema.json` is byte-identical to `emit_schema()`.
    - `eval/src/lancet_eval/score.py` lines 1003-1020 — where `make_groundedness_result` is called.
    - `eval/src/lancet_eval/score.py` lines 1289-1308 — the `RunMetadata(...)` construction.
  </read_first>
  <behavior>
    - A judged `score_run` publishes `judged_slice_committed` equal to the size
      `derive_judged_slice_size` returned, and `verdicts_obtained` equal to the number of verdicts
      actually collected, including cached ones.
    - A judged pass stopped by the spend cap publishes a `judged_slice_state` naming the cap stop,
      and `verdicts_obtained` strictly below `judged_slice_committed`.
    - A judged pass that completed its whole committed slice publishes the completed state, with
      `verdicts_obtained` equal to `judged_slice_committed` minus any judge errors and
      evidence-skips, and the difference explained by the existing `judge_errors` and
      `skipped_no_evidence` detail keys.
    - An unjudged `score_run` publishes the not-judged state with both counts zero.
    - Both `answer_groundedness` and `answer_faithfulness` carry `judged_slice_committed`,
      `verdicts_obtained` and the numeric judged-slice state code in their `detail`.
    - `emit_schema()` output equals the committed `eval/report.schema.json` byte for byte after
      regeneration, and a `CorpusReport` carrying the three new fields validates against it.
    - `dimensions.graph_presence`'s floor and `gate.GRAPH_YIELD_INVESTIGATION_FLOOR` are the same
      object, and importing `lancet_eval.dimensions` standalone does not raise.
  </behavior>
  <action>
    **Write the `<behavior>` tests first and watch them fail.** All judged paths use `tmp_path`
    journals and mocked transports; nothing reads `eval/runs/`.

    **1. Define the judged-slice state once, in the place the float-typed dict forces.**
    `DimensionResult.detail` is `dict[str, float]`, so a string state cannot travel there. Follow the
    `CALIBRATION_STATE_*` precedent in `agreement.py`: add to `dimensions.py` a set of float
    constants — a not-judged state, a committed-slice-completed state, a cap-bound state (the slice
    size was bounded by the cap rather than by the data) and a cap-stopped state (the loop broke on
    accumulated spend) — together with a single `dict[float, str]` mapping from code to
    human-readable name. That mapping is the one source of truth; do not maintain a parallel string
    enum anywhere else.

    **2. Add the three fields to `RunMetadata`** (report.py:24-46): `judged_slice_committed: int = 0`,
    `verdicts_obtained: int = 0`, and `judged_slice_state: str` defaulting to the not-judged name
    from that mapping. Add `judged_slice_state` to `_validate_non_blank_fields`'s required list, and
    add a validator rejecting any value outside the mapping's names — a free-text state field is a
    field that can be made to say anything.

    **3. Regenerate the committed schema — do not hand-edit it.**
    `eval/tests/test_schema.py:16` asserts `eval/report.schema.json` is byte-identical to
    `emit_schema()`, so a model change without regeneration breaks report validation. Regenerate with:

    `uv run --project eval python -c "from pathlib import Path; from lancet_eval.report import emit_schema; f = open(Path('eval/report.schema.json'), 'w', encoding='utf-8', newline='\n'); f.write(emit_schema()); f.close()"`

    The explicit `newline="\n"` matters on Windows: the default would translate line endings and
    produce a file that differs from `emit_schema()`'s output on disk.

    **4. Populate the three fields from Task 2's wiring** at the `RunMetadata(...)` construction
    (score.py:1289-1308), reading the locals Task 1 and Task 2 established: the committed slice size
    and its binding string from the `derive_judged_slice_size` call, the verdict count from the judge
    loop (counting cached verdicts, which are verdicts the run relies on even though it did not pay
    for them again), and the cap-stopped flag. Map the binding and the cap-stopped flag onto one
    state code: cap-stopped wins over cap-bound, which wins over completed.

    **5. Carry the same three values into both judged dimensions.** Add matching keyword-only
    parameters to `make_groundedness_result` and `make_faithfulness_result` (dimensions.py:149 and
    232), defaulted so existing callers and tests keep working, and emit them into the returned
    `DimensionResult.detail` — the two counts as floats and the state as its float code. Pass them
    from score.py's call site around line 1015. This is what T-06.3.4-43's claimed mitigation
    asserted already existed; the claim was false and this is what makes it true.

    **6. IN-02 — one definition of the graph-yield investigation floor.** A direct
    `from lancet_eval.gate import GRAPH_YIELD_INVESTIGATION_FLOOR` inside `dimensions.py` would
    close the cycle dimensions → gate → report → dimensions, confirmed against the imports at
    `gate.py:13` and `report.py:13`. Take the relocation the review offered as its fallback: move
    the constant into `thresholds.py`, which imports only the standard library. Have `gate.py`
    import it under its existing name so `gate.GRAPH_YIELD_INVESTIGATION_FLOOR` keeps resolving for
    `test_gate.py:15` and for `06.3.4-STAGED-GATE.md`'s citation, and have `dimensions.py:553` read
    it instead of the `0.20` literal. **The value does not change** — this is a relocation, not a
    re-derivation, and changing a committed floor after observations exist would require a dated
    amendment under D-32.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_schema.py eval/tests/test_dimensions.py eval/tests/test_score_judged.py eval/tests/test_gate.py -q &amp;&amp; uv run --project eval pytest -q &amp;&amp; uv run --project eval python -c "
import lancet_eval.dimensions as D, lancet_eval.gate as G, lancet_eval.thresholds as T
from lancet_eval.report import RunMetadata
for f in ('judged_slice_committed','verdicts_obtained','judged_slice_state'):
    assert f in RunMetadata.model_fields, f'RunMetadata missing {f}'
assert hasattr(T,'GRAPH_YIELD_INVESTIGATION_FLOOR'), 'floor not relocated to thresholds'
assert G.GRAPH_YIELD_INVESTIGATION_FLOOR is T.GRAPH_YIELD_INVESTIGATION_FLOOR, 'gate no longer re-exports the shared floor'
assert G.GRAPH_YIELD_INVESTIGATION_FLOOR == 0.20, 'the committed graph-yield floor value changed'
import inspect; assert 'floor_val = 0.20' not in inspect.getsource(D), 'IN-02 duplicated literal still present in dimensions.py'
import json; sch = json.load(open('eval/report.schema.json', encoding='utf-8'))
props = sch['\$defs']['RunMetadata']['properties'] if '\$defs' in sch else sch['properties']
for f in ('judged_slice_committed','verdicts_obtained','judged_slice_state'):
    assert f in props, f'committed schema missing {f}'
print('PROVENANCE_OK')
" &amp;&amp; git diff --quiet --exit-code -- eval/runs/ &amp;&amp; echo TASK3_OK</automated>
    <fails_when>The `TASK3_OK` line is absent from stdout, or the command exits non-zero. Specifically: `test_schema_file_byte_identical` failing, which means `eval/report.schema.json` was hand-edited or not regenerated — on Windows this also fires when the regeneration omitted `newline="\n"`; `RunMetadata missing <field>`; `committed schema missing <field>` (the model and the committed schema drifted apart); `floor not relocated to thresholds` or `gate no longer re-exports the shared floor` (either the relocation did not happen or it broke `test_gate.py`'s import and `06.3.4-STAGED-GATE.md`'s citation); `the committed graph-yield floor value changed` — IN-02 is a relocation and a changed floor would be an undated amendment to a committed threshold; `IN-02 duplicated literal still present in dimensions.py`; any `<behavior>` test failing, in particular the cap-stopped case where `verdicts_obtained` must be strictly below `judged_slice_committed`; an `ImportError` from importing `lancet_eval.dimensions`, which would mean the relocation introduced the cycle it exists to avoid; or `git diff` reporting a modification under `eval/runs/`.</fails_when>
  </verify>
  <acceptance_criteria>
    - Every case in the `<behavior>` block has a named test, written and observed failing before
      source changed.
    - `RunMetadata` carries `judged_slice_committed: int`, `verdicts_obtained: int` and
      `judged_slice_state: str`. `judged_slice_state` is validated against the single code-to-name
      mapping and is in `_validate_non_blank_fields`'s required list.
    - The judged-slice state is defined once as float constants plus one `dict[float, str]` mapping
      in `dimensions.py`, following the `CALIBRATION_STATE_*` precedent. No parallel string enum
      exists anywhere else. The summary states why `detail`'s `dict[str, float]` typing forced this.
    - `eval/report.schema.json` was regenerated with the exact command in the action, not hand-edited,
      and `test_schema_file_byte_identical` passes. The summary records the command used.
    - Both `answer_groundedness` and `answer_faithfulness` carry all three values in `detail`, passed
      from score.py's call site. The parameters are defaulted so no existing caller broke.
    - A cap-stopped judged pass publishes `verdicts_obtained` strictly below
      `judged_slice_committed` and the cap-stopped state; an unjudged run publishes the not-judged
      state with both counts zero.
    - `GRAPH_YIELD_INVESTIGATION_FLOOR` is defined once, in `thresholds.py`, re-exported by
      `gate.py` under the same name, and imported by `dimensions.py` in place of the literal. Its
      value is unchanged at `0.20`. `import lancet_eval.dimensions` raises no `ImportError`.
    - `uv run --project eval pytest` reports no failures and no errors. No test was deleted, skipped
      or de-asserted. The summary records the final count and states that it changed only by tests
      added, naming each.
    - `uv lock --project eval --check` is clean; no dependency was added.
    - `git diff --quiet -- eval/runs/` exits clean.
  </acceptance_criteria>
  <done>
    A reduced-n judged publication announces itself in the artefact — how large a slice was
    committed to, how many verdicts came back, and which constraint bound it — and the graph-yield
    floor has one definition instead of two that can silently diverge.
  </done>
</task>

</tasks>

<out_of_scope>
| Item | Owner | Why not here |
|---|---|---|
| The journal completeness defect, the live journal's header correction, and the four record corrections | 06.3.4-08 | Same files (`score.py`, `cli.py`, `report.py`) in different regions; 09 is sequenced after 08 so the two never contend for a verification ordering. |
| Running an actual judged pass or calibration pass against real provider spend | Nobody in this phase | These are the controls that must exist **before** such a pass. Exercising them is a later, separately authorised spend decision (D-47). |
| Human calibration (SC-3, D-48) | Deferred, unchanged | No back-fill, no agent-simulated scoring. |
| OI-01, OI-02, OI-03 | Phase 06.3.4.1 | Retargeted 2026-09-12. That phase's Out of Scope names T-24/T-25/T-43 as the gate this plan closes. |
| Phase 6.4's docs, quickstart, v1 closure | Phase 6.4 (parked) | Blocked behind 06.3.4.1's unpark gates. |
| WR-02 (`config.eval.toml` `[engine.workflow]` duplication), WR-04 (`read_store_suspension` regex parsing) | Deferred | Operator-locked. |
| Changing the graph-yield floor's value, the coverage floor, the complement trigger or the agreement target | Nobody | Committed before observations existed. IN-02 relocates one definition; it does not re-derive a threshold (D-32). |
| Any change to `eval/runs/` | Nobody — prohibited by D-45 | Both the superseded and the corrected run directories are evidence. |
</out_of_scope>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This plan's contribution to the gap-closure round. Plan 08's contribution is listed in its own file.

| Kind | Artifact |
|---|---|
| New model | `JudgeUsage` in `judge.py` — `prompt_tokens: int`, `completion_tokens: int`, parsed defensively from the provider `usage` block. |
| Changed signature | `judge_once(...) -> tuple[JudgeVerdict \| None, str \| None, JudgeUsage \| None]` — was a 2-tuple. Five call sites: `score.py:433`, `eval/tests/test_judge.py` 119, 157, 179, 205. |
| New constants | `measure.JUDGE_INPUT_PRICE_PER_1M`, `measure.JUDGE_OUTPUT_PRICE_PER_1M`, and a committed judge prompt-token estimate — each commented with the judge model priced and the date recorded. |
| New functions | `measure.compute_judge_spend(prompt_tokens, completion_tokens)`, `measure.estimate_judge_cost_per_question(...)`. |
| Changed signature | `score_run(..., stage_spend_cap: float \| None = None)` — required when judging is enabled. |
| New CLI parameter | `--stage-cap` on the `score` command. Distinct from the `measure` command's unrelated existing `--stage-cap`. |
| Changed behaviour | The judge loop meters accumulated spend and stops dispatching at the cap, checked before each call. Cached verdicts consume no headroom. |
| Changed behaviour | `derive_judged_slice_size` evaluates the cached-verdict refusal before the zero-population shortcut, and is called from `score_run`'s judged-subset selection. |
| New refusal | `score_run` raises when `--sample` exceeds the cap-derived slice size, naming both numbers. |
| Changed behaviour | `_find_budgets_file` and `_find_store_baseline_file` sort deterministically and raise on genuine ambiguity naming every candidate. |
| New fields | `RunMetadata.judged_slice_committed: int`, `RunMetadata.verdicts_obtained: int`, `RunMetadata.judged_slice_state: str` (validated against the single code-to-name mapping). |
| New constants | The judged-slice state float codes and the `dict[float, str]` code-to-name mapping in `dimensions.py`, following the `CALIBRATION_STATE_*` precedent. |
| New parameters | `make_groundedness_result` and `make_faithfulness_result` gain the three provenance values, emitted into `DimensionResult.detail`. |
| Regenerated artefact | `eval/report.schema.json` — regenerated from `emit_schema()` with `newline="\n"`, never hand-edited. |
| Relocated constant | `GRAPH_YIELD_INVESTIGATION_FLOOR` moves to `thresholds.py`; `gate.py` re-exports it under the same name; `dimensions.py` imports it in place of the `0.20` literal. Value unchanged. |
| New tests | The `<behavior>` cases in all three tasks. Exact names recorded in the summary. |
| New dependencies | None. `uv lock --project eval --check` must stay clean. |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval harness → paid provider | The judge loop makes metered calls against an external service with no in-process bound today. |
| committed decision input → derived slice size | A slice size chosen without the cached-verdict invariant can silently shrink a previously-judged population. |
| judged slice → published report | A reduced-n judged publication that does not say it is reduced reads as a full one. |
| filesystem glob → committed threshold | A decision input selected by whichever path the filesystem returned first is not committed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.4-25 | Denial of Service | judged provider spend | high | mitigate | **Closing evidence:** `score_run` gains a required-when-judging `stage_spend_cap` and raises `ScoreError` naming the missing cap when judging is enabled without one; the `score` CLI command gains `--stage-cap`. `judge_once` returns the provider usage it previously discarded, priced by committed judge-specific constants (`compute_judge_spend`), and the judge loop compares accumulated spend against the cap **before** dispatching each next question, as `drive()` does at run.py:341-345. A test asserts exactly one judge HTTP call is made when the cap sits below the cost of the second question, and a second asserts cached verdicts consume no cap headroom. |
| T-06.3.4-24 | Tampering | judged slice size | high | mitigate | **Closing evidence:** `derive_judged_slice_size`'s guards are reordered so the documented cached-verdict refusal is evaluated before the zero-population shortcut; a test exercises `judgeable_count=0, cached_verdict_count>0` and asserts it raises, and a companion test asserts a genuinely empty population with nothing cached still returns its named reason. The function is called from `score_run`'s judged-subset selection (the caller it previously lacked), replacing the unbounded `else` branch at score.py:400-401, with a `--sample`-exceeds-the-bound refusal naming both numbers. |
| T-06.3.4-43 | Repudiation | reduced-n judged publication | high | mitigate | **Closing evidence:** `judged_slice_committed`, `verdicts_obtained` and `judged_slice_state` exist on `RunMetadata`, in the regenerated `eval/report.schema.json` (asserted byte-identical against `emit_schema()`), and in both `answer_groundedness` and `answer_faithfulness` `detail` dicts. They are populated from T-06.3.4-24's wiring, which is why this threat could not be closed independently of that one. A cap-stopped judged pass publishes `verdicts_obtained` strictly below `judged_slice_committed` plus the cap-stopped state, proven by test. |
| T-06.3.4-47 | Spoofing | committed decision-input discovery | medium | mitigate | `_find_budgets_file` and `_find_store_baseline_file` no longer take `matches[0]` from an unsorted glob; they sort deterministically and raise naming every candidate on genuine ambiguity, proven by a test that plants two matching files (WR-03). |
| T-06.3.4-48 | Tampering | committed threshold values under a refactor | medium | mitigate | IN-02 relocates `GRAPH_YIELD_INVESTIGATION_FLOOR` without changing it. The verify asserts the value is still `0.20` and that `gate.GRAPH_YIELD_INVESTIGATION_FLOOR` and `thresholds.GRAPH_YIELD_INVESTIGATION_FLOOR` are the same object, so a relocation that silently re-derived the floor fails mechanically. A `must_haves` prohibition states that changing a committed floor requires a dated amendment (D-32). |
| T-06.3.4-49 | Tampering | committed report schema | medium | mitigate | `eval/report.schema.json` is regenerated from `emit_schema()` rather than hand-edited, and `test_schema_file_byte_identical` is the standing check. A `must_haves` prohibition forbids hand-editing it, which would turn a real byte-identity check into a rubber stamp. |
| T-06.3.4-46 | Denial of Service | provider spend during gap closure | low | accept | No task in this plan makes a real provider call; every judged path is exercised through `pytest_httpx` mocks. A `must_haves` prohibition forbids starting a judged pass. |
| T-06.3.4-10 | Information Disclosure | judged artefacts | low | accept | See `06.3.4-SECURITY.md`'s Accepted Risks Log R-01. Judged artefacts carry public corpus text and model output; the provider key is read from the environment at call time and never persisted. The new usage fields carry token counts, not content. |
| T-06.3.4-SC | Tampering | package-manager installs | high | mitigate | No install task exists and no new dependency is introduced — every change uses the standard library, `pydantic` and `httpx`, all already locked. `uv lock --project eval --check` must confirm the lock is unchanged. No `## Package Legitimacy Audit` is required because no package is added. |
</threat_model>

<verification>
- `uv run --project eval pytest` reports no failures and no errors, with no test deleted, skipped or de-asserted to reach that state.
- `uv lock --project eval --check` is clean; no dependency was added.
- `test_schema_file_byte_identical` passes against the regenerated `eval/report.schema.json`.
- `score_run` with judging enabled and no `stage_spend_cap` raises; with judging disabled and no cap it does not.
- `derive_judged_slice_size(judgeable_count=0, cached_verdict_count=3, ...)` raises; with `cached_verdict_count=0` it returns `(0, "judgeable_count_is_zero")`.
- `derive_judged_slice_size` has a production caller in `score_run`.
- `gate.GRAPH_YIELD_INVESTIGATION_FLOOR is thresholds.GRAPH_YIELD_INVESTIGATION_FLOOR` and equals `0.20`; `import lancet_eval.dimensions` raises no `ImportError`.
- `matches[0]` appears nowhere in `gate.py`.
- No real provider call was made at any point; the summary records that every judged path ran against a mocked transport.
- `git diff --quiet -- eval/runs/` exits clean.
- The Rust and Go suites are untouched by this plan and are not re-run as part of it; the phase-level regression gate covers them.
</verification>

<success_criteria>
- A judged pass cannot start without an explicit spend cap, and the judge loop stops dispatching when it reaches it.
- The judged slice is sized by the function that documents the invariant, and that invariant fires in the case it exists for.
- A reduced-n judged publication is visible in the published artefact rather than reconstructable only from logs.
- `/gsd-secure-phase 06.3.4` can close T-06.3.4-24, T-06.3.4-25 and T-06.3.4-43 on the evidence this plan produces without re-deriving it, so `threats_open` reaches 0 together with 08's closure of T-06.3.4-19.
</success_criteria>

<output>
Create `.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-09-SUMMARY.md` when done.
</output>

## Revise-before-execute ledger

### 2026-09-12 — created by `/gsd-plan-phase 06.3.4 --gaps`

Created in gap-closure mode from `06.3.4-SECURITY.md` (`threats_open: 4`) and `06.3.4-REVIEW.md`
(IN-03, WR-03, IN-02). Every file:line anchor was re-verified against the working tree during
planning; anchors are anchors, not guarantees, and an executor finding drift should trust the code.

**Findings made during planning that the source artefacts did not carry**, recorded here so they
are not re-derived:

1. **Metering must precede the slice wiring.** `derive_judged_slice_size` takes a
   `cost_per_question` that does not exist anywhere in the harness until judge metering lands. The
   brief's "keep T-24 first" ordering is T-24 before T-43 — the field-population chain — not T-24
   before T-25. Task order here is metering, then sizing, then provenance.
2. **`DimensionResult.detail` is typed `dict[str, float]`** (`dimensions.py:24`), so
   `judged_slice_state` cannot travel there as a string. The repository already solved this once:
   `agreement.py:23-25` encodes calibration states as float constants. Task 3 follows that
   precedent with one code-to-name mapping rather than two parallel enums.
3. **`eval/report.schema.json` is asserted byte-identical** to `emit_schema()` by
   `eval/tests/test_schema.py:16`. No CLI command emits it, so the regeneration command is written
   out in full in Task 3 — including `newline="\n"`, without which the default Windows line-ending
   translation produces a file that differs from the generator's output.
4. **The IN-02 cycle is confirmed, not suspected.** `report.py:13` imports `DimensionResult` from
   `dimensions.py` and `gate.py:13` imports `CorpusReport` from `report.py`, so a direct
   `dimensions.py` → `gate.py` import closes dimensions → gate → report → dimensions. The review's
   own fallback — relocate the constant — is therefore the only option, and `thresholds.py` is
   chosen because it imports nothing from `lancet_eval`.
5. **WR-03's raise is safe.** Only one `06.3.3` phase directory exists, and both corpora declare
   `arms = ["graph-on", "graph-off"]` and nothing else, so neither the ambiguity raise nor 08's
   unknown-arm raise changes any current behaviour.
6. **`judge_once` has five call sites, not one.** `score.py:433` plus `eval/tests/test_judge.py`
   lines 119, 157, 179 and 205. All five are named in Task 1 so the breaking signature change does
   not surprise the executor mid-task.

**Verify-command grounding.** The mechanically-supplied `prior_verify_commands` were Rust and
latency commands from sibling phase 06.3.3 and were not used. The binding precedent is this phase's
own suite: `uv run --project eval pytest eval/tests/test_X.py -q`, matching
`06.3.4-01-PLAN.md` through `06.3.4-08-PLAN.md`. Every path named in a verify command was confirmed
to exist in the working tree during planning.
