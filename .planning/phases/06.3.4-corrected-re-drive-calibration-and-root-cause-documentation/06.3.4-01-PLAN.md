---
phase: 06.3.4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - eval/src/lancet_eval/gate.py
  - eval/src/lancet_eval/journal.py
  - eval/src/lancet_eval/cli.py
  - eval/tests/test_gate.py
  - eval/tests/test_journal_reconcile.py
  - .planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-DRIVE-PRECONDITIONS.md
  - eval/runs/2026-XX-XX-multihop_rag/journal.jsonl
  - eval/runs/2026-XX-XX-multihop_rag/STAGED-GATE.json
autonomous: true
requirements: [OBS-05]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The staged drive's decision inputs — both floors, the complement trigger, the suspension rule, the calibration size and the judged-slice derivation rule — are committed to the repository before any staged number exists, so the go/no-go is a function of measured data and a pre-committed threshold rather than a choice made after seeing the data (D-32, D-44, D-48)."
    - "The gate reads the staged numbers from the scored report object directly, because the scoring command refuses to render a journal the sampling knob marked as partial, and a staged drive always produces such a journal."
    - "A journal that began as a staged prefix can be published once the full committed sample has actually been driven, and the transition is conditioned on a computed completeness comparison rather than on an operator's assertion."
    - "The completeness comparison is made between the journal's recorded work-unit key set and the full cross-product of the committed sample and the configured arms; the comparison result is recorded whether it passes or fails."
    - "The header's sampling flag moves in one direction only. A complete journal may be marked publishable; a publishable journal is never marked back to staged, and no record line is altered by the transition."
    - "Records driven during the staged stage keep the sampling flag they were written with, because that flag is what attributes a record to the stage it came from after the two stages have merged into one journal (specless edge: ordering)."
    - "The staged prefix, the full drive and the tracer's micro-slice all address one dated run directory and reach it through the same resume path, so the staged record set is a true prefix of the full record set rather than a separate run that has to be reconciled."
    - "The corrected run's directory name matches the shape the resume resolver searches for, so a second invocation continues the existing run instead of silently starting a new one beside it."
    - "The committed sample file's sort invariant is verified against the file itself before any drive relies on it, because the staged prefix is defined as the sorted prefix and that property belongs to the file rather than to the flag that reads it."
    - "The 2026-09-03 run and the corrected run remain two separately dated directories; neither is merged into the other and no file of the former is rewritten (D-45, specless edge: adjacency)."
    - "A gate evaluated on a journal with no usable pairs produces an explicit verdict naming the empty input as the reason, rather than a division that cannot be taken or a default that reads as success (specless edge: empty)."
  artifacts:
    - "eval/src/lancet_eval/gate.py — the committed decision inputs and the staged-gate evaluator"
    - "eval/src/lancet_eval/journal.py — the completeness comparison and the one-directional header reconciliation"
    - "eval/src/lancet_eval/cli.py — the staged-gate command and the reconcile command"
    - "eval/tests/test_gate.py, eval/tests/test_journal_reconcile.py — golden vectors for both"
    - ".planning/phases/06.3.4-.../06.3.4-DRIVE-PRECONDITIONS.md — the verified drive preconditions ledger"
    - "eval/runs/<dated>-multihop_rag/ — the corrected run directory, opened by the tracer's micro-slice"
  key_links:
    - "gate.py reads the scored report object returned by score_run, not the score CLI, which raises on a partial journal at report.py's render guard"
    - "The per-stage spend caps are read from 06.3.3-BUDGETS.md's recorded go/no-go entry, which is where the human set them; they are not restated as independent values here"
    - "The suspension determination for the graph-yield floor is read from 06.3.3-STORE-BASELINE.md's zero-count reclassification verdict"
    - "The dated run directory this plan opens is the same directory 06.3.4-03 and 06.3.4-04 resume into; the resume resolver's date-glob is what joins them"
    - "The staged pairing-coverage and graph-presence numbers the gate reads come from dimension names and detail keys 06.3.2 defines; a missing key is a halt, not a default"
  prohibitions:
    - "No floor, trigger, calibration size or derivation rule may be changed after a staged or full number has been observed; a change is a recorded amendment carrying its own rationale and its own date, never a silent edit"
    - "No file already present in eval/runs/2026-09-03-multihop_rag/ may be rewritten, moved or deleted; that run is the primary evidence behind every forensic number this family rests on"
    - "The header reconciliation must never rewrite, reorder or drop a record line, and must never move the sampling flag from publishable back to staged"
    - "A gate verdict must never be produced by reading a number the report did not carry; an absent dimension or detail key halts the evaluation and is reported as absent"
---

<objective>
Prove the whole corrected-drive chain on one thin slice before any of it carries real weight:
committed decision inputs, a live micro-drive into the corrected run directory, an offline score,
a gate evaluated against the committed floors, and a recorded verdict. Then land the two pieces of
machinery the chain cannot run without — the staged-gate evaluator that reads a journal the
scoring command refuses to render, and the header reconciliation that lets a journal which began
as a staged prefix be published once the full sample has actually been driven.

Purpose: the staged-then-full design in D-44 collides with a latch in the existing harness.
`Journal.write_header` (`eval/src/lancet_eval/journal.py:86-97`) writes the header only when the
file is new or empty, so a drive that begins with the sampling knob stamps the journal as partial
permanently. `score_run` carries that flag into report metadata and declines to write `report.json`
for it (`eval/src/lancet_eval/score.py:801,811`); `render_markdown` and `render_json` raise on it
(`eval/src/lancet_eval/report.py:130-133,174-177`); and the `score` command calls `render_markdown`
unconditionally (`eval/src/lancet_eval/cli.py:405-413`). Verified empirically against a synthetic
partial journal: `score_run` completes and returns a full report, `report.json` is not written, and
the `score` command exits with `Cannot render report for partial run`. Without this plan the staged
drive cannot be scored at all and the full drive that resumes into it can never be published.

Under D-46's back-edge rule a harness defect discovered *by* the drive lands in this seam, not back
in 06.3.2 — which is planned, not executed, and whose plans were checked and carry no work on the
header flag or on `write_header`.

Output: `gate.py`, the reconciliation on `journal.py`, two CLI commands, two test modules, the
committed drive-preconditions ledger, and the corrected run directory opened by a real micro-slice.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-CONTEXT.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-AI-SPEC.md
@.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-PATTERNS.md
@CLAUDE.md
</context>

<decision_inputs>
These are the values this phase treats as **inputs to a comparison**, not outputs of a measurement.
They are locked by `06.3.1-AI-SPEC.md` §5 and must be committed before the staged drive runs.

| Input | Value | Source |
|---|---|---|
| Staged pairing-coverage floor | `0.80` | AI-SPEC §5 "Staged drive integrity" |
| Graph-yield investigation floor | `graph_presence_rate >= 0.20` on usable graph-on records | AI-SPEC §5 D-32 note |
| Complement trigger | `NoMatchFound` rate `> 0.80` among graph-on whose graph node completed | AI-SPEC §5 D-32 note |
| Floor suspension | Suspended entirely when the store inspection found node, edge and entity counts of zero | AI-SPEC §5 D-32 note; read from `06.3.3-STORE-BASELINE.md` |
| Influence floor | None. `graph_influence_rate` has no floor at all | AI-SPEC §5 D-32 note |
| Calibration size | `12` dual scores, `24` ratings | AI-SPEC §5 D-48 note |
| Agreement target | Quadratic weighted kappa **and** Spearman rho, each `>= 0.70` | AI-SPEC §5 D-48 note |
| Staged size | `50` questions across both configured arms | AI-SPEC §5 "Staged drive integrity" |
| Per-stage spend caps | Read from the dated go/no-go entry in `06.3.3-BUDGETS.md` | D-47, set by the human at 06.3.3's closing checkpoint |

**Boundary semantics are part of the lock.** The yield floor is satisfied at exactly `0.20` because
it is stated as at-or-above. The complement trigger does not fire at exactly `0.80` because it is
stated as strictly above. The staged coverage floor is satisfied at exactly `0.80`.

**A miss is not a run failure.** Missing the yield floor triggers a bounded investigation before the
results are published in Phase 6.4. It does not fail the run, and it does not apply in production
(D-32). The only place a missing graph hit may refuse to start a drive is the preflight canary, and
only on its two designated rows (D-31).

Everything else this phase touches — the observed coverage, the observed yield, the observed
`NoMatchFound` rate, the observed agreement, the observed spend, the derived judged-slice size —
is a **measured value** and is never asserted in advance.
</decision_inputs>

<tasks>

<task type="tracer">
  <name>Task 1: One question, all the way — decision inputs, a live micro-slice, a score, a gate, a recorded verdict</name>
  <files>eval/src/lancet_eval/gate.py, eval/src/lancet_eval/cli.py, eval/tests/test_gate.py</files>
  <precondition>The engine and gateway are running against the isolated eval environment with the budgets 06.3.3 wrote, the provider key is present, and `06.3.3-BUDGETS.md` carries a go verdict with its per-stage caps — this task issues real queries against a paid provider.</precondition>
  <reversibility rating="reversible">New module, new command, and a handful of queries whose cost is immaterial against the staged cap. Nothing existing changes shape.</reversibility>
  <read_first>
    - `eval/src/lancet_eval/score.py:100-186` — how a journal is loaded, the mixed-generation guard at 144-155, the document-map guard, and where `sampled_questions` comes from.
    - `eval/src/lancet_eval/score.py:770-815` — how `index_generation`, `result_hash` and the sampling flag reach report metadata, and the condition under which `report.json` is written.
    - `eval/src/lancet_eval/report.py:126-180` — the two render guards this command must route around by never calling them.
    - `eval/src/lancet_eval/cli.py:214-306` — `resolve_run_dir`'s date glob and newest-match rule, and the `run` command's options.
    - `eval/src/lancet_eval/cli.py:307-368` — the `score` command, and specifically that it renders unconditionally after scoring.
    - `.planning/phases/06.3.1-.../06.3.1-AI-SPEC.md` lines 922-996 — the normative pairing join and coverage note, and the graph-yield floor note with its suspension and complement rules.
    - `.planning/phases/06.3.3-.../06.3.3-BUDGETS.md` — the dated go/no-go entry and its three per-stage caps.
    - `.planning/phases/06.3.3-.../06.3.3-STORE-BASELINE.md` — the graph population counts and the zero-count reclassification verdict.
    - `.planning/phases/06.3.2-.../06.3.2-02-PLAN.md` and `06.3.2-04-PLAN.md` — the names of the graph-health dimensions and the pairing detail keys this gate reads, as those plans define them.
  </read_first>
  <action>
    Create `eval/src/lancet_eval/gate.py` holding, in this order, the three layers the staged gate
    needs.

    First, the committed decision inputs from the table in this plan's `decision_inputs` section, as
    module-level constants with the boundary semantics encoded in the comparison operators rather
    than described beside them: at-or-above for both floors, strictly-above for the complement
    trigger. Include the calibration size and the agreement target so a later plan reads them from
    one place, and a function that reads the three per-stage caps from `06.3.3-BUDGETS.md` rather
    than restating them, so a cap the human set cannot drift from the cap this phase enforces.

    Second, an evaluator that takes a scored report object and the store-baseline suspension
    determination and returns a structured verdict. It reads the staged pairing coverage and the
    staged graph presence rate from the dimension names and detail keys 06.3.2 defines. If a name or
    key it needs is absent from the report, it must stop and say which one was absent — it must not
    substitute a default, because a defaulted number here would authorise real spend. When the
    pairing join produced no pairs at all it returns a verdict whose reason names the empty input,
    never a ratio it cannot compute. When the suspension determination says the store carried no
    nodes, edges or entities, the yield comparison is not made at all and the verdict records the
    finding as belonging to ingestion rather than to query yield.

    Third, a renderer that writes the verdict to the run directory as both a machine-readable and a
    human-readable file, carrying: every input value it compared against and where that value came
    from, every observed value it read and which dimension it read it from, the outcome of each
    comparison, the suspension determination, and the overall verdict.

    Add a `staged-gate` command to `eval/src/lancet_eval/cli.py` that takes a run directory, calls
    `score_run` with judging switched off and reads the returned report object directly, then
    evaluates and renders. It must not call the `score` command or either render function, since
    both refuse a journal the sampling knob marked partial and a staged journal is always such a
    journal. Exit non-zero when the evaluator halts on an absent input.

    Then drive the thinnest real slice through the whole chain: confirm the run directory the
    resume resolver will pick, drive a two-question slice through both configured arms into it, and
    run `staged-gate` on the result. Record in the plan summary the resolved directory name, the
    per-node timings and workflow metadata that arrived on the wire, the index generation the
    engine reported, and the verdict the gate produced.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_gate.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `eval/tests/test_gate.py` proves, on synthetic report objects, that: a coverage value exactly at the coverage floor satisfies it; a presence value exactly at the yield floor satisfies it; a complement value exactly at the trigger threshold does not fire the trigger and a value above it does; a report with zero pairs yields a verdict whose reason names the empty input and carries no computed ratio; a suspension determination of zero counts causes the yield comparison to be skipped and reclassified; and an absent dimension or detail key raises rather than defaulting.
    - The named test module passes under `uv run --project eval pytest eval/tests/test_gate.py -x`.
    - The three per-stage caps in `gate.py` are obtained by reading `06.3.3-BUDGETS.md` at run time, and a test proves that a cap absent from that record is reported as absent rather than defaulted.
    - The `staged-gate` command completes on a journal whose header carries the partial sampling flag, which the `score` command refuses; the summary records both outcomes side by side as evidence that the routing was necessary.
    - A live micro-slice of two questions across both configured arms is driven into the corrected run directory and the resolved directory name is recorded; that name matches the shape `resolve_run_dir` globs for, and the summary states the check that confirmed it.
    - The journal records produced by the micro-slice carry non-empty per-node timings and a workflow-metadata object with the graph presence and graph influence fields present as keys; the summary records which keys arrived. Their **values** are measured data and are recorded, not asserted.
    - The index generation the engine reported at drive time is recorded verbatim in the summary from what the engine returned, and the summary states that it was read rather than written.
    - A gate verdict file is present in the run directory and its recorded comparison inputs match the committed constants, checked by comparison against `gate.py` rather than by restating them.
    - No file under `eval/runs/2026-09-03-multihop_rag/` differs from its committed state; the summary records the check that established this.
  </acceptance_criteria>
  <done>
    The full chain — committed threshold, real query, wire diagnostics, offline score, gate
    comparison, recorded verdict — has run end to end on real data, and the corrected run directory
    exists with its first records in it.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Let a journal that began as a staged prefix be published once the full sample has actually been driven</name>
  <files>eval/src/lancet_eval/journal.py, eval/src/lancet_eval/cli.py, eval/tests/test_journal_reconcile.py</files>
  <read_first>
    - `eval/src/lancet_eval/journal.py:30-104` — the record model and its sampling field, `journal_key`, `load_done`'s tolerance of a truncated trailing line, `write_header`'s new-or-empty condition, and `append`'s flush discipline.
    - `eval/src/lancet_eval/run.py:82-163` — how the sampling knob sets the flag on both the header and every record, how the resume key set is built, and the work-unit cross-product of questions and configured arms.
    - `eval/src/lancet_eval/score.py:109-142` — where the header's sampling flag is read out of the journal.
    - `eval/src/lancet_eval/corpus.py` — `load_sample_questions` and `load_corpus_config`, for the committed sample and the configured arm list the completeness comparison is taken against.
    - `.gitignore:130` — the temporary-journal path already ignored, which is the established atomic-write idiom in this tree.
  </read_first>
  <behavior>
    - A journal whose recorded work-unit key set equals the full cross-product of the committed sample and the configured arms is reported complete; the comparison returns the missing keys when it is not.
    - Reconciling a complete journal rewrites only the header line's sampling flag; every record line is byte-identical afterwards and the line count is unchanged.
    - Reconciling an incomplete journal makes no change and reports which keys are missing.
    - Reconciling an already-publishable journal is a no-op and reports it as such.
    - The transition runs in one direction only: there is no path that marks a publishable journal back as staged.
    - A journal with a truncated trailing line is treated by the completeness comparison exactly as `load_done` treats it, so the two never disagree about what has been driven.
    - The write is atomic: an interrupted reconciliation leaves the original journal intact.
  </behavior>
  <action>
    Add a completeness comparison and a header reconciliation to `eval/src/lancet_eval/journal.py`.

    The completeness comparison rebuilds the full cross-product of the committed sample's question
    identifiers and the corpus's configured arms, compares it against the key set `load_done`
    returns for the journal, and returns both the verdict and the missing keys. It takes the
    committed sample as its input rather than a count, so the predicate is over a locked input and
    never over a number someone typed.

    The reconciliation reads the header line, and only when the completeness comparison passes,
    rewrites that one line with its sampling flag cleared. Write through a temporary file beside the
    journal and replace atomically, following the idiom `.gitignore` already anticipates. Every
    record line is copied through unchanged. There is no parameter that sets the flag the other way.

    Expose it as a `reconcile` command on the CLI taking a run directory, printing the verdict and,
    when incomplete, the count and a sample of the missing keys.

    Leave the per-record sampling flags exactly as they were written. A record driven during the
    staged stage carries that flag, and after the two stages have merged into one journal that flag
    is what attributes a record to the stage it came from. Do not rewrite them; state in the module
    docstring what they mean after a resume.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_journal_reconcile.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Every behaviour in the `<behavior>` block has a named test in `eval/tests/test_journal_reconcile.py` and the module passes under `uv run --project eval pytest eval/tests/test_journal_reconcile.py -x`.
    - A test compares the record lines of a journal before and after a successful reconciliation and asserts they are byte-identical, and that only the first line differs.
    - A test asserts that no public entry point exists that can set the header's sampling flag back to the staged value, by inspecting the reconciliation's signature and its accepted arguments.
    - A test drives the completeness comparison against a journal missing exactly one work-unit key and asserts that key is named in the result.
    - A test asserts an interrupted write leaves the original journal readable and unchanged.
    - `score_run` on a reconciled journal writes `report.json` into the run directory, proven by a test that reconciles a synthetic complete staged journal and then scores it.
    - The existing eval suite still passes: `uv run --project eval pytest` reports no new failures against the count recorded in the summary.
    - If a sibling phase has already landed equivalent behaviour, the tests are still added and the summary records that no production change was needed and which sibling landed it.
  </acceptance_criteria>
  <done>
    A journal that began as a staged prefix can be scored, published and reported once the full
    committed sample has actually been driven, and the transition rests on a computed comparison
    rather than on anyone's word.
  </done>
</task>

<task type="auto">
  <name>Task 3: The drive-preconditions ledger — verify the invariants the drive rests on, before it rests on them</name>
  <files>.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-DRIVE-PRECONDITIONS.md</files>
  <read_first>
    - `eval/src/lancet_eval/corpus.py:200-225` — `sample_corpus` and the sort key it returns results under, which is the property the staged prefix depends on.
    - `eval/corpora/multihop_rag/questions.sample.jsonl` — sampled, not read whole: its length, whether its identifier column is already in sorted order, and the question-type distribution of its first fifty rows.
    - `eval/src/lancet_eval/cli.py:214-232` — `resolve_run_dir`'s glob and its newest-match rule.
    - `eval/src/lancet_eval/score.py:144-155` — the guard that refuses a journal carrying more than one index generation.
    - `.planning/phases/06.3.2-.../06.3.2-03-PLAN.md` — the preflight canary set and its floors, and the committed canary file it produces.
    - `.planning/phases/06.3.3-.../06.3.3-BUDGETS.md` and `06.3.3-STORE-BASELINE.md` — the caps, the derived budgets, the store population and the reclassification verdict.
    - `.gitignore:61-63` — which run directories are tracked and which are ignored.
  </read_first>
  <action>
    Write `06.3.4-DRIVE-PRECONDITIONS.md` as the ledger of things this phase's drives assume and
    that nothing in the plan ordering guarantees. Each entry states what was checked, how it was
    checked, what was observed, and what happens if it is later found untrue.

    Cover at least: that the committed sample file's identifier column is already in sorted order
    and that the sampling knob therefore yields the sorted prefix, checked against the file rather
    than argued from the flag's documentation; the question-type composition of the first fifty
    rows and the count among them that carry no gold evidence, which is what puts a ceiling on
    achievable pairing coverage below one; that the corrected run directory's name matches the
    shape the resume resolver globs for and sorts after the superseded run's name, so a resumed
    drive continues rather than forks; that the committed canary file 06.3.2 produces is present
    with its designated graph rows; that the derived budgets 06.3.3 wrote are what the engine is
    running under; that the store population and the reclassification verdict are recorded and
    which of the two the yield floor therefore follows; that the three per-stage caps are present in
    06.3.3's go/no-go entry; and that nothing may ingest or rebuild an index between the staged and
    the full drive, because the scorer refuses a journal carrying more than one index generation and
    a mid-drive rebuild would make the merged journal unscorable.

    Record the observed values for every check. Where a check is against a locked constant, name the
    constant and where it is committed. Where a check produced a number, record the number and the
    date it was taken, and state plainly that it is an observation rather than a target.

    Also record, as a standing constraint for the whole phase, that the superseded run directory is
    read-only evidence: it is cited, never edited, and the corrected run is a separate dated
    directory rather than a continuation of it.
  </action>
  <verify>
    <automated>test -f .planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-DRIVE-PRECONDITIONS.md &amp;&amp; git diff --quiet --exit-code -- eval/runs/2026-09-03-multihop_rag/ &amp;&amp; echo PRECONDITIONS_OK</automated>
  </verify>
  <acceptance_criteria>
    - The ledger carries one entry per check named in the action, and every entry states the method, the observation and the consequence of the assumption later proving untrue.
    - The sort-order check is recorded as having been run against the committed sample file itself, with the row count and the verdict, and not inferred from the sampling flag's documentation.
    - The first-fifty composition is recorded as counts by question type together with the count carrying no gold evidence, and the ledger states that these counts put a ceiling below one on achievable pairing coverage rather than restating the coverage floor.
    - The run-directory check records the resolved name, states that it matches the resolver's glob shape, and states that it sorts after the superseded run's name.
    - The single-index-generation constraint is recorded as a standing constraint for the whole phase, naming the scorer guard that enforces it and the consequence of violating it mid-drive.
    - The three per-stage caps are recorded by reference to 06.3.3's go/no-go entry, with the date that entry carries, and are not restated as free-standing figures with no provenance.
    - The store population and the reclassification verdict are recorded, and the ledger states which of the two branches the yield floor follows as a result.
    - A check that could not be run because its producing sibling has not executed is recorded as unmet with its producer named, rather than omitted or assumed.
    - `git diff --quiet -- eval/runs/2026-09-03-multihop_rag/` exits clean, and the ledger records that the superseded run is read-only evidence for the whole phase.
  </acceptance_criteria>
  <done>
    Every assumption the staged and full drives rest on has been checked against the artefact that
    would falsify it, and the result is written down where a later reader can tell an observation
    from a target.
  </done>
</task>

</tasks>

<out_of_scope>
| Item | Owner | Why not here |
|---|---|---|
| The staged 50-question drive and its gate evaluation | 06.3.4-03 | This plan builds and proves the evaluator; running the real staged drive and taking its verdict is the next plan, so the money decision lands on a plan boundary. |
| The full two-arm drive | 06.3.4-04 | Authorised by 06.3.4-03's closing decision, not by this plan. |
| Quadratic weighted kappa and Spearman rho | 06.3.4-02 | This plan commits the agreement target as a constant; computing agreement is that plan's work. |
| Wire capture, per-path dimensions, the pairing join and the coverage number itself | 06.3.2 | This plan consumes those dimension names and detail keys by contract. |
| The preflight canary set and its floors | 06.3.2 (D-10, D-11, D-12) | This plan verifies the committed canary file is present; it does not author it. |
| Deriving timeout budgets, the decay verdict, the store inspection | 06.3.3 | Consumed here as recorded verdicts in that phase's committed records. |
| Any change to `eval/runs/2026-09-03-multihop_rag/` | Nobody — prohibited by D-45 | It is the primary evidence behind every forensic number this family rests on. |
</out_of_scope>

<artifacts_this_phase_produces>
## Artifacts this phase produces

The complete inventory for phase 06.3.4. Later plans reference this section rather than repeating it.

| Kind | Artifact | Plan |
|---|---|---|
| New Python module | `eval/src/lancet_eval/gate.py` — committed decision inputs, the staged-gate evaluator, the verdict renderer | 01 |
| Modified Python module | `eval/src/lancet_eval/journal.py` — completeness comparison and one-directional header reconciliation | 01 |
| Modified CLI | `eval/src/lancet_eval/cli.py` — `staged-gate` and `reconcile` commands | 01 |
| New Python module | `eval/src/lancet_eval/agreement.py` — quadratic weighted kappa and Spearman rho on stdlib | 02 |
| Modified Python modules | `eval/src/lancet_eval/score.py`, `eval/src/lancet_eval/dimensions.py` — agreement metrics, the calibration count, and the uncalibrated label | 02 |
| New tests | `eval/tests/test_gate.py`, `eval/tests/test_journal_reconcile.py`, `eval/tests/test_agreement.py` | 01, 02 |
| New run directory | `eval/runs/<dated>-multihop_rag/` — the corrected run: `journal.jsonl`, `report.json`, `report.md`, `metadata.json`, `judge_cache.json`, `calibration_worksheet.jsonl`, `calibration_completed.jsonl`, the staged-gate verdict files | 01, 03, 04, 05 |
| New phase record | `06.3.4-DRIVE-PRECONDITIONS.md` — verified drive preconditions ledger | 01 |
| New phase record | `06.3.4-STAGED-GATE.md` — observed staged numbers, the comparisons, the spend projection and the recorded go/no-go | 03 |
| New phase record | `06.3.4-FINDINGS.md` — the graph-yield disposition, the calibration attestation and agreement, and the paired-comparison disposition including the negative-delta investigation | 05 |
| New forensic document | `.planning/phases/06.3.1-.../06.3.1-ROOT-CAUSE.md` — the full forensic trail, placed in 06.3.1's directory as this phase's own success criterion requires | 06 |
| New run marker | `eval/runs/2026-09-03-multihop_rag/SUPERSEDED.md` — additive only; no existing file in that directory is touched | 06 |
| New public document | `docs/<name>.md` — the distilled public-facing note; creates `docs/` | 06 |
| Modified roadmap entry | `.planning/ROADMAP.md` — Phase 6.4's canonical refs and its notice-vocabulary success criterion | 06 |
| New CLI flags | `staged-gate` and `reconcile` commands only. No new flag on `run`, `score` or `report`. | 01 |
| New dependencies | None. The stated evaluation contract forbids new Python dependencies; kappa, Spearman and the bootstrap are stdlib. | — |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed decision input → authorised spend | A threshold constant is what decides whether a paid drive starts; a wrong or mutable constant authorises spend nobody bounded. |
| scored report → gate verdict | The gate reads numbers produced by code from an unexecuted sibling; an absent or renamed key silently defaulting would fabricate a verdict. |
| journal file → published report | The reconciliation rewrites a line of the run of record; a bad write corrupts the benchmark artefact itself. |
| superseded run directory → forensic record | Every number in this family's decision record came from a directory this phase must never modify. |
| eval harness → paid provider | The micro-slice issues real queries against a metered external service. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.4-01 | Tampering | `Journal` header reconciliation | high | mitigate | Temporary-file write plus atomic replace; a test asserts record lines are byte-identical and the line count unchanged; no entry point exists that moves the flag toward the staged value. |
| T-06.3.4-02 | Tampering | `eval/runs/2026-09-03-multihop_rag/` | high | mitigate | The task's automated check runs `git diff --quiet` over that directory; the standing constraint is written into the preconditions ledger for every later plan. |
| T-06.3.4-03 | Spoofing | gate verdict inputs | high | mitigate | The evaluator halts and names the missing dimension or detail key rather than defaulting; a test proves the halt. |
| T-06.3.4-04 | Repudiation | committed decision inputs | medium | mitigate | Floors, triggers and sizes are module constants committed before any observation exists; the verdict record cites each input and where it is committed, so a later change is visible in history. |
| T-06.3.4-05 | Denial of Service | paid provider spend | medium | mitigate | The micro-slice is two questions across both arms and runs under the staged-stage cap 06.3.3's go/no-go entry set; the task carries a precondition that the entry exists. |
| T-06.3.4-06 | Information Disclosure | run artefacts committed to git | low | accept | The corpus is public news text under its stated licence and the run directory carries no credential; the provider key is read from the environment and never written into a run artefact. |
| T-06.3.4-SC | Tampering | package-manager installs | high | mitigate | No install task exists in this plan or this phase. The evaluation contract forbids new Python dependencies; kappa, Spearman, the bootstrap and the Wilson interval are stdlib. `uv sync --locked` installs only what `uv.lock` already pins, and `uv lock --check` proves the lock is unchanged. |
</threat_model>

<verification>
- `uv run --project eval pytest` passes; the summary records the test count before and after.
- `uv run --project eval ruff check --preview eval/src eval/tests` is clean.
- `uv lock --project eval --check` confirms no dependency was added.
- `git diff --quiet -- eval/runs/2026-09-03-multihop_rag/` exits clean.
- The corrected run directory exists, its name matches the resume resolver's glob shape, and it contains the micro-slice's journal and a gate verdict file.
- `06.3.4-DRIVE-PRECONDITIONS.md` exists and every check it names carries an observation or an explicit unmet marker with its producer.
</verification>

<success_criteria>
- The staged gate can be evaluated on a journal the scoring command refuses to render, proven by running both and recording the two outcomes.
- A complete journal that began as a staged prefix can be scored into a written `report.json`, proven by a test.
- Every decision input this phase compares against is committed before any observation exists, and the verdict record cites each one.
- The chain from committed threshold through a live query to a recorded verdict has run once, on real data, end to end.
</success_criteria>

<output>
Create `.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-01-SUMMARY.md` when done.
</output>

## Revise-before-execute

Read this before executing. Every row names an input this plan assumed unavailable at planning time
and the sibling or task that produces it. Siblings 06.3.1, 06.3.2 and 06.3.3 are **planned and
committed but not executed**; none of their code or output exists yet. Their absence is a sequencing
fact, not a defect in this plan. If a row's producer has landed and its contract differs from what is
assumed here, adjust this plan before executing it.

| Assumed-unavailable input | Produced by |
|---|---|
| The corrected harness: paired ablation, scored graph and per-path dimensions, unified denominators, the preflight canary | **06.3.2** (not executed). This plan reads its dimension names and detail keys by contract only. |
| The exact dimension and detail key names the gate reads — the graph presence rate, the pairing coverage, the `NoMatchFound` complement | **06.3.2** plans 02 and 04. If a name differs from what is assumed, the evaluator must halt on it rather than default, which is what its acceptance criteria require. |
| Pairing coverage actually achievable on this corpus | **06.3.2**'s pairing implementation plus this phase's staged drive (06.3.4-03). |
| The six derived timeout budgets in `config/config.toml` | **06.3.3** (not executed). The micro-slice's precondition is that the engine runs under them. |
| Whether the collapse was mis-sized budgets or resource exhaustion — and therefore whether a drive is safe to start at all | **06.3.3**'s decay verdict (D-18, D-19, D-21), recorded in `06.3.3-MEASUREMENT.md`. |
| Graph population counts — which decide whether the yield floor applies or is suspended as an ingest finding | **06.3.3**'s store inspection (D-05), recorded in `06.3.3-STORE-BASELINE.md`. |
| Whether graph traversal is inherently too slow, and the latency cost to record for Phase 6.4 | **06.3.3** (D-04). |
| Failure signalling: the new notice code at tag 19, partial-snapshot provenance, the graph-influence counter | **06.3.1** (not executed). The micro-slice's wire-field check reads these keys; their absence is a finding to record, not a plan defect. |
| Observed graph yield and `NoMatchFound` rate — currently unknown, because the broken run produced 494 graph timeouts against only 4 unavailable results | This phase's own corrected drive (06.3.4-03, 06.3.4-04). |
| The three per-stage spend caps, and the actual spend | **06.3.3**'s dated go/no-go entry in `06.3.3-BUDGETS.md` (D-47, shared with 06.3.3). The 2026-09-03 figure of `$0.0199` is not a usable estimate — that run was cheap precisely because 966 of its queries generated nothing. |
| Whether the graph makes results worse | This phase's corrected paired comparison (D-49), disposed in 06.3.4-05. |
| Whether a sibling already landed the header reconciliation | 06.3.2's plans were checked at planning time and carry no work on the header flag or on `write_header`. Task 2 is written verify-then-implement: if a sibling has landed it, add the tests anyway and record that no production change was needed. |
| The exact name of the new notice-code enum value | **06.3.1**. It is illustrative in the shared decision record, not locked. Nothing in this plan may hard-code it; 06.3.4-06 reads it from the proto file verbatim. |
