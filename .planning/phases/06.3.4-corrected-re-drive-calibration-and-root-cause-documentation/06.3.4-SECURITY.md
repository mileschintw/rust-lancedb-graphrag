---
phase: "06.3.4"
slug: "corrected-re-drive-calibration-and-root-cause-documentation"
status: verified
# threats_open = count of OPEN threats at or above workflow.security_block_on severity (the blocking gate)
threats_open: 0
asvs_level: 1
created: "2026-09-10"
updated: "2026-09-13"
---

# Phase 06.3.4 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.
>
> Retroactive audit (register was authored at plan time — all plans carry `<threat_model>`
> blocks). First `/gsd-secure-phase` run for 06.3.4 was 2026-09-10 after the phase had been
> closed without the three gates running.
>
> **2026-09-13 (plans 08/09):** T-19/24/43 closed; T-44–49 registered; T-25 kept open and
> T-42 reopened — the named fallback and shared judgeable predicate were still absent.
>
> **2026-09-13 (plan 10, this run):** L1 re-audit after `06.3.4-10` landed the two missing
> mitigations. `asvs_level: 1` + `register_authored_at_plan_time: true` + `threats_open: 0`
> short-circuits the auditor (grep-depth is sufficient). No `## Threat Flags` sections exist
> in any SUMMARY. Two previously omitted plan-01 IDs (T-34; T-35 staged-coverage, distinct
> from plan-02's T-35) and two plan-10 IDs (T-25a, T-42a) are added to the written register.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| committed decision input → authorised spend | A threshold constant decides whether a paid drive starts | spend authorization |
| scored report → gate verdict | Gate reads numbers from code in an unexecuted sibling plan | gate telemetry |
| journal file → published report | Reconciliation rewrites the run-of-record line | run completeness metadata |
| superseded run directory → forensic record | Every forensic number in the shared decision record came from this directory | historical run evidence |
| eval harness → paid provider | Real queries against a metered external service (drive + judge calls) | provider spend |
| provider accounting field → in-process spend meter | The provider decides whether to report `usage`; a meter that trusts its presence is controlled by the counterparty | observed vs estimated spend |
| human calibration file → published judged scores | External file decides whether judged numbers are published as calibrated | calibration ratings |
| engine-reported index generation → run provenance | Hand-written generation makes the record set unattributable | run provenance |
| forensic evidence → published explanation | The root-cause doc is the project's own account of its failure | public documentation |
| roadmap file → every other phase entry | A wholesale write would silently destroy entries outside the edit window | planning bookkeeping |
| judgeable population → derived slice size | A slice sized against a larger population than the loop will judge publishes a commitment it cannot deliver | judged N |
| judge cache → cap-derived bound | Cache entries that no lookup can reuse inflate the bound with slots that are actually fresh paid calls | reusable verdict count |
| judged slice → published report | Spend that was estimated rather than observed must not read as measured | fallback disclosure |

---

## Threat Register

62 threats declared across the 10 plans' `<threat_model>` blocks (some IDs reused across sibling
plans for distinct threats — noted by plan of origin below). 62 closed, 0 open at or above
`block_on: high`.

### Open — blocking

*None.*

### Closed

| Threat ID (plan of origin) | Category | Severity | Disposition | Evidence |
|-----------|----------|----------|-------------|----------|
| T-06.3.4-01 (01) | Tampering | high | mitigate | Atomic temp-file+replace in `journal.py:reconcile_header`; byte-identical/no-forward-flip tests pass. |
| T-06.3.4-02 (01) | Tampering | high | mitigate | `git diff --quiet` over `eval/runs/2026-09-03-multihop_rag/` — clean. |
| T-06.3.4-03 (01) | Spoofing | high | mitigate | `gate.py` raises naming the missing key before any threshold comparison. |
| T-06.3.4-04 (01) | Repudiation | medium | mitigate | Committed module constants, cited in every verdict. |
| T-06.3.4-05 (01) | DoS | high | mitigate | Bounded-window dispatch in `drive()`, spend checked before each submit. |
| T-06.3.4-06 (01) | Info Disclosure | low | accept | See Accepted Risks Log. |
| T-06.3.4-34 (01) | Tampering | high | mitigate | `06.3.4-DRIVE-PRECONDITIONS.md` §3 records `resolve_run_dir(..., resume=True)` hitting the superseded directory before the micro-slice, the `--no-resume --limit 2` first invocation opening `2026-09-09-multihop_rag`, and the post-slice pin; CLI exposes `--resume/--no-resume` (`cli.py:259`). Previously omitted from the written register. |
| T-06.3.4-35 (01, staged coverage gate) | Spoofing | high | mitigate | `gate.py` recomputes D-44 staged coverage against the locked stage size and publishes it separately from journal-relative `harness_pairing_coverage` (`test_gate.py` asserts the two differ). Previously omitted from the written register (ID reused by plan 02). |
| T-06.3.4-SC (01) | Tampering | high | mitigate | `uv lock --check` clean. |
| T-06.3.4-07 (02) | Spoofing | high | mitigate | No code path writes the calibration file; blank-score rejection intact. |
| T-06.3.4-08 (02) | Repudiation | medium | mitigate | Scorer tested against committed `AGREEMENT_TARGET`, not a literal. |
| T-06.3.4-09 (02) | Tampering | high | mitigate | `_validate_consistency` forbids `status="ok"` + non-null `reason`. |
| T-06.3.4-35 (02, calibration validator relaxation) | Tampering | high | mitigate | Same validator guard, enforced at the pydantic model level. |
| T-06.3.4-10 (02) | Info Disclosure | low | accept | See Accepted Risks Log. |
| T-06.3.4-SC (02) | Tampering | high | mitigate | `uv lock --check` clean. |
| T-06.3.4-11 (03) | Spoofing | high | mitigate | Same `gate.py` raise mechanism as T-03. |
| T-06.3.4-12 (03) | EoP | high | mitigate | Dated human decision checkpoint recorded before full drive ran. |
| T-06.3.4-13 (03) | Tampering | high | mitigate | Mixed-generation guard, tested; journal independently confirmed single generation. |
| T-06.3.4-14 (03) | Tampering | high | mitigate | Clean git-diff over superseded run directory. |
| T-06.3.4-15 (03) | DoS | high | mitigate | `--stage-cap` from `06.3.3-BUDGETS.md`; `drive()`'s in-process stop rule enforces it. |
| T-06.3.4-36 (03) | Tampering | high | mitigate | Pinned literal run-directory path used directly; superseded directory never touched. |
| T-06.3.4-16 (03) | Repudiation | medium | mitigate | Spend decision recorded with date/chooser/basis. |
| T-06.3.4-SC (03) | Tampering | high | mitigate | `uv lock --check` clean. |
| T-06.3.4-17 (04) | EoP | high | mitigate | Full-drive invocation carried the authorized `--stage-cap`, issued after the dated decision. |
| T-06.3.4-18 (04) | Tampering | high | mitigate | Mixed-generation guard + dedup; journal independently reproduced: 658 distinct keys, 0 duplicates, single generation. |
| T-06.3.4-20 (04) | Tampering | high | mitigate | Same as T-13/T-18. |
| T-06.3.4-21 (04) | DoS | high | mitigate | `--workers 1` bounded overshoot; observed spend $0.1194 vs $5.00 cap. |
| T-06.3.4-37 (04) | Tampering | high | mitigate | Canonical exclusion of superseded date; never newest-match. |
| T-06.3.4-22 (04) | Repudiation | medium | mitigate | `06.3.4-04-SUMMARY.md` quantitative claims independently reproduced exactly. |
| T-06.3.4-SC (04) | Tampering | high | mitigate | `uv lock --check` clean. |
| T-06.3.4-23 (05) | Spoofing | high | mitigate | No code path writes a human score; worksheet honestly disclosed as deferred, not back-filled. |
| T-06.3.4-38 (05) | Tampering | high | mitigate | Same pinning pattern as T-36/T-37. |
| T-06.3.4-39 (05) | Tampering | high | mitigate | Same `_validate_consistency` guard as T-09; calibration caveat carried outside `reason`. |
| T-06.3.4-26 (05) | Tampering | high | mitigate | Deterministic seeded-sampling mechanism intact (no feed-back run occurred — judging deferred). |
| T-06.3.4-SC (05) | Tampering | high | mitigate | `uv lock --check` clean. |
| T-06.3.4-29 (06) | Tampering | high | mitigate | `SUPERSEDED.md` additive; clean git diff on the directory. |
| T-06.3.4-30 (06) | Spoofing | high | mitigate | Notice name read from `proto/lancet/v1/lancet.proto` at the free tag. |
| T-06.3.4-31 (06) | Tampering | medium | mitigate | Scoped 2-line ROADMAP update confirmed via `git show`. |
| T-06.3.4-32 (06) | Repudiation | medium | mitigate | Forensic numbers cite their source record sets, independently reproduced. |
| T-06.3.4-33 (06) | Info Disclosure | low | accept | See Accepted Risks Log. |
| T-06.3.4-SC (06) | Tampering | high | mitigate | No install task; `uv lock --check` clean. |
| T-06.3.4-27 (07) | Repudiation | high | mitigate | Dispositions taken against pre-committed floors; negative delta published plainly. |
| T-06.3.4-40 (07) | Tampering | high | mitigate | Same pinning pattern; read-path git-diff-clean confirmed. |
| T-06.3.4-41 (07) | Tampering | medium | mitigate | Calibration section un-rewritten; 07's additions appended beneath it. |
| T-06.3.4-28 (07) | Info Disclosure | low | accept | See Accepted Risks Log. |
| T-06.3.4-SC (07) | Tampering | high | mitigate | No source modified; `uv lock --check` clean. |
| T-06.3.4-19 (08) | Spoofing | high | mitigate | Closed 2026-09-13. `run.py:246-271,279-382` writes fail-closed `partial=True` unconditionally, revised only via `_safe_reconcile()` in a `try/finally` covering every `drive()` exit path; `journal.py:168-239` `reconcile_header` is bidirectional with atomic temp-file+`os.replace`; `score.py` independently re-verifies an explicit `partial: false` header via `completeness_comparison` before trusting it. Live artifact: `eval/runs/2026-09-09-multihop_rag/journal.jsonl` line 1 reads `"partial": true`. |
| T-06.3.4-24 (09) | Tampering | high | mitigate | Closed 2026-09-13. `gate.py:398-442` reorders the cached-verdict-refusal guard (line 424) ahead of the zero-shortcut (line 429); `score.py` now calls it as `score_run`'s real production caller. |
| T-06.3.4-43 (09) | Repudiation | high | mitigate | Closed 2026-09-13. `report.py` — `RunMetadata.judged_slice_committed`/`verdicts_obtained`/`judged_slice_state` present and validated; `dimensions.py` both judged and `no_judge` branches populate all three fields. |
| T-06.3.4-44 (08) | Tampering (test integrity) | medium | mitigate | No test deleted; the two invalidated tests were split/rewritten in `test_journal_reconcile.py`, verified by name. Full suite green. |
| T-06.3.4-45 (08) | Repudiation (planning records) | medium | mitigate | `06.3.4-04-SUMMARY.md:46` carries a dated correction, not a silent rewrite; `06.3.4-STAGED-GATE.md` retains the original `Selected Option:` plus an appended amendment; `docs/evaluation-fidelity-and-graph-yield.md` point 5 rewritten. |
| T-06.3.4-46 (09/10) | DoS (gap-closure spend) | low | accept | See Accepted Risks Log R-02. |
| T-06.3.4-47 (09) | Spoofing (decision-input discovery) | medium | mitigate | No `matches[0]` remains anywhere in `gate.py`; ambiguous matches raise `FileNotFoundError` naming all candidates. |
| T-06.3.4-48 (09) | Tampering (threshold relocation) | medium | mitigate | `thresholds.py:114` defines `GRAPH_YIELD_INVESTIGATION_FLOOR = 0.20`; re-exported from `gate.py`; imported by `dimensions.py`; value unchanged. |
| T-06.3.4-49 (09/10) | Tampering (report schema) | medium | mitigate | Plan 09 regenerated schema from `emit_schema()`. Plan 10 forbids further regeneration: fallback count travels in `RunMetadata.notes` and `DimensionResult.detail`, not as a Pydantic field; `eval/report.schema.json` unmodified. |
| T-06.3.4-25 (05/10) | Denial of Service | high | mitigate | Closed 2026-09-13 by plan 10. `score.py:527-533`: when `judge_once` returns `usage is None`, charge `cost_per_question` and increment `usage_absent_fallback_count`; observed zero-token `usage` charges zero and increments nothing. Cap check at `score.py:512-514` therefore advances on every dispatched call. Count reaches both judged dimensions' `detail` (`dimensions.py`) and `RunMetadata.notes` (`score.py:1441+`). Regression: `test_judge_loop_breaks_at_spend_cap_with_usage_absent_fallback` (mixed fixture stops early); `test_judge_loop_usage_absent_distinct_from_observed_zero`; `test_make_groundedness_and_faithfulness_results_carry_usage_absent_fallback_count`. |
| T-06.3.4-42 (05/10) | Spoofing | high | mitigate | Closed 2026-09-13 by plan 10. Module-level `_is_judgeable` (`score.py:136-141`) — gold present AND non-empty `structured_citations` — is the single predicate for candidate selection (`score.py:441`), the judge loop (`score.py:488`), and calibration-worksheet selection (`score.py:892`). `derive_judged_slice_size` receives that filtered `judgeable_count`. Tests: `test_judgeable_predicate_parity_and_selection_sizing`, `test_worksheet_selection_parity_with_judgeable_predicate`. |
| T-06.3.4-25a (10) | Denial of Service | high | mitigate | `--sample` refusal is scoped to `judged_slice_binding == "cap"` (`score.py:462-469`); data-bound / coincident / zero-judgeable bindings clamp. `test_score_run_sample_exceeds_cap_derived_bound_raises` still passes; clamped-sample case covered in `test_score_judged.py`. |
| T-06.3.4-42a (10) | Tampering | medium | mitigate | `_reusable_verdict_count` (`score.py:144-154`) counts only cache entries whose `prompt_version` and `judge_model` match configured values; `score.py:374-378` feeds that count into slice derivation and calibration guards. Test: `test_score_judged.py` stale-`prompt_version` cache scoping. |
| T-06.3.4-SC (10) | Tampering | high | mitigate | No install task; `uv lock --project eval --check` clean per `06.3.4-10-SUMMARY.md`. |

*Status: open · closed*
*Severity: critical > high > medium > low — only open threats at or above `block_on: high` count toward `threats_open`*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| R-01 | T-06.3.4-06, T-06.3.4-10, T-06.3.4-28 (05), T-06.3.4-28 (07), T-06.3.4-33 | All five are Information Disclosure / low severity: run artefacts, worksheets, and findings/root-cause documents carry only public MultiHop-RAG news-corpus text, model output, and this project's own aggregate measurements. No credential, API key, or personal data is written into any run artefact or planning document — the provider key is read from the environment at call time and never persisted. Confirmed by direct inspection during this audit and the phase's own code review. | orchestrator (retroactive `/gsd-secure-phase` audit) | 2026-09-10 |
| R-02 | T-06.3.4-46 (09/10) | Denial of Service / low: gap-closure plans 08/09/10 forbid live provider calls. Judged paths are exercised through `pytest_httpx` mocks against `tmp_path` journals; `06.3.4-10-SUMMARY.md` records `eval/runs/` untouched and lock check clean. | orchestrator (`/gsd-secure-phase` after plan 10) | 2026-09-13 |

*Accepted risks do not resurface in future audit runs.*

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-09-10 | 52 | 48 | 4 | gsd-security-auditor (retroactive-STRIDE = false; register was plan-authored) |
| 2026-09-13 | 58 | 56 | 2 | gsd-security-auditor + orchestrator override (T-19/24/43 confirmed closed by gap-closure plans 08/09; T-44-49 self-registered and closed by the same plans; T-25 kept open and T-42 reopened against the auditor's own cited evidence) |
| 2026-09-13 | 62 | 62 | 0 | orchestrator L1 (`asvs_level: 1` short-circuit). Plan 10 closed T-25 and T-42; registered T-25a and T-42a; added previously omitted T-34 and T-35 (01 staged coverage). |

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-09-13

---

## Next

1. `/gsd-validate-phase 06.3.4` — validate test coverage.
2. `/gsd-verify-work 06.3.4` — run UAT.

**Not a security item (see `06.3.4-VERIFICATION.md`):** SC-1 (D-44 staged-drive coverage floor)
remains a disclosed-but-unaccepted deviation and SC-3 (D-48 fresh human calibration) remains
deferred. Those still need a human decision; they are not threat-register entries.
