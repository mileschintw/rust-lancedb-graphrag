# Phase 06.3.4 Plan 01: Summary

## Overview
Plan 06.3.4-01 implemented the foundational execution infrastructure for Phase 06.3.4:
1. **Committed Decision Inputs & Staged Gate Evaluator (`eval/src/lancet_eval/gate.py`)**: Defined committed thresholds (`STAGED_PAIRING_COVERAGE_FLOOR = 0.80`, `GRAPH_YIELD_INVESTIGATION_FLOOR = 0.20`, `COMPLEMENT_TRIGGER = 0.80`, `STAGED_SIZE = 50`, `CALIBRATION_SIZE = 12`, `AGREEMENT_TARGET = 0.70`). Implemented D-44 coverage recomputation (`n_pairs / locked_stage_size`), short-journal fail-closed rule, achievable ceiling arithmetic, and structured JSON/Markdown verdict renderers.
2. **Fail-Closed Stage Cap Enforcement & Cumulative Accumulator (`eval/src/lancet_eval/run.py` & `cli.py`)**: Transformed the submit-all dispatch comprehension in `drive()` into a bounded-window dispatch holding at most `effective_workers` in flight, checking `compute_spend(records) >= stage_spend_cap` before submitting each next unit. Seeded cumulative spend accumulator from `load_records()` when `resume=True`. Added required `--stage-cap` CLI option.
3. **Journal Completeness Comparison & Atomic Reconciliation (`eval/src/lancet_eval/journal.py`)**: Added `completeness_comparison` delegating directly to `load_done()` over the committed sample cross-product, and `reconcile_header()` performing atomic header sampling flag replacement via a temporary file without altering record bytes.
4. **Drive Preconditions Ledger (`06.3.4-DRIVE-PRECONDITIONS.md`)**: Verified sample question ID sorting, first-50 question-type composition (19 comparison, 13 temporal, 13 inference, 5 null), null-gold drops (5, setting achievable ceiling at 0.90), resolver resolution identity, per-stage caps parser, single index generation invariant, store baseline disposition, and read-only evidence constraint (D-45).
5. **Live Tracer Micro-Slice (`eval/runs/2026-09-09-multihop_rag/`)**: Drove 2 questions (4 work units across `graph-on` and `graph-off`) through live Engine and Gateway into a new dated run directory with `--no-resume --limit 2 --stage-cap 2.0`.

---

## Live Tracer Micro-Slice Evidence

### 1. Resolver Readings Before and After
- **Before Micro-Slice**: `resolve_run_dir("multihop_rag", resume=True)` evaluated to:
  `D:\Repos\lancet\eval\runs\2026-09-03-multihop_rag`
  (The superseded evidence directory).
- **Resolver Safeguard Assertion**: Verified that `--no-resume` opened `D:\Repos\lancet\eval\runs\2026-09-09-multihop_rag` which differed from the superseded run path.
- **After Micro-Slice**: `resolve_run_dir("multihop_rag", resume=True)` evaluated to:
  `D:\Repos\lancet\eval\runs\2026-09-09-multihop_rag`
  (Now points to the corrected run directory for all future `--resume` calls).

### 2. Invocation Command & Purpose of Flags
- **Command**:
  ```bash
  uv run --project eval lancet-eval run --corpus multihop_rag --no-resume --limit 2 --stage-cap 2.0
  ```
- **Flag Protections**:
  - `--no-resume`: Prevented the resolver from resuming into and mutating the superseded `2026-09-03-multihop_rag` evidence directory.
  - `--limit 2`: Ensured `partial = True` was written to the journal header (`write_header` is one-shot; running without limit on a fresh file would have latched the header as publishable permanently).
  - `--stage-cap 2.0`: Enforced the budget cap allocated in `06.3.3-BUDGETS.md` with no silent default.

### 3. Header Latch State & Observed Wire Diagnostics
- **Journal Header**:
  ```json
  {"type": "header", "corpus": "multihop_rag", "partial": true, "created_at": 1788972726.4429429}
  ```
- **Reported Index Generation**: `lance-701` (read live from engine snapshot events).
- **Per-Node Timings & Workflow Wire Metadata**:
  - Unit 1 (`mhr-0073ab564e55`, `graph-on`):
    - Timings: `ReformulateQuery` 0.0ms, `ExtractGraphContext` 8113.0ms, `RetrieveHybrid` 2889.0ms, `AssemblePrompt` 526.0ms, `GenerateAnswer` 15009.0ms. Total: 26541.0ms.
    - Wire Meta: `graph_node_count: 0`, `graph_edge_count: 0`, `graph_prompt_fact_count: 0`, `vector_count: 32`, `bm25_count: 32`, `prompt_tokens: 1956`, `completion_tokens: 213`, `degraded_mode: true`.
  - Unit 2 (`mhr-0073ab564e55`, `graph-off`):
    - Timings: `ReformulateQuery` 0.0ms, `ExtractGraphContext` 222.0ms, `RetrieveHybrid` 3129.0ms, `AssemblePrompt` 402.0ms, `GenerateAnswer` 4215.0ms. Total: 7973.0ms.
    - Wire Meta: `graph_node_count: 0`, `graph_edge_count: 0`, `graph_prompt_fact_count: 0`, `vector_count: 32`, `bm25_count: 32`, `prompt_tokens: 1956`, `completion_tokens: 164`, `degraded_mode: true`.
  - Unit 3 (`mhr-0085f76defbe`, `graph-on`):
    - Timings: `ReformulateQuery` 0.0ms, `ExtractGraphContext` 8724.0ms, `RetrieveHybrid` 2619.0ms, `AssemblePrompt` 380.0ms, `GenerateAnswer` 7948.0ms. Total: 19674.0ms.
    - Wire Meta: `graph_node_count: 3`, `graph_edge_count: 2`, `graph_prompt_fact_count: 2`, `vector_count: 32`, `bm25_count: 32`, `prompt_tokens: 2149`, `completion_tokens: 189`, `degraded_mode: false` (GRAPH HIT).
  - Unit 4 (`mhr-0085f76defbe`, `graph-off`):
    - Timings: `ReformulateQuery` 0.0ms, `ExtractGraphContext` 239.0ms, `RetrieveHybrid` 3211.0ms, `AssemblePrompt` 323.0ms, `GenerateAnswer` 5053.0ms. Total: 8832.0ms.
    - Wire Meta: `graph_node_count: 0`, `graph_edge_count: 0`, `graph_prompt_fact_count: 0`, `vector_count: 32`, `bm25_count: 32`, `prompt_tokens: 1774`, `completion_tokens: 369`, `degraded_mode: true`.
- **Observed Spend**: `$0.0014` against `$2.00` staged cap.

### 4. Gate Verdict
- **Command**: `uv run --project eval lancet-eval staged-gate --run eval/runs/2026-09-09-multihop_rag`
- **Output Files**: `STAGED-GATE.json`, `STAGED-GATE.md`
- **Verdict Details**:
  - `overall_status`: `hold`
  - `reason`: `Short journal: 2 distinct questions in journal < locked stage size 50`
  - `d44_staged_coverage`: `0.0400` (2 pairs / 50 locked size)
  - `harness_pairing_coverage`: `1.0000` (2 pairs / 2 journal questions; diagnostic)
  - `distinct_questions_in_journal`: `2`
  - `d44_coverage_ceiling`: `1.0000`
  - `headroom_answerable_drops`: `10`
  - `presence_rate`: `0.5000` (1 of 2 graph-on questions observed graph nodes > 0; >= 0.20 floor)
  - `yield_outcome`: `pass`
  - `no_match_rate`: `0.0000`
  - `complement_trigger_fired`: `False`

### 5. Historical Evidence Integrity (D-45)
- **Assertion**: `git status --porcelain eval/runs/2026-09-03-multihop_rag/` and `git diff --quiet --exit-code -- eval/runs/2026-09-03-multihop_rag/` exited with returncode `0` and empty diff.
- The superseded run directory was untouched and remains read-only evidence.

---

## Verification Results
- `eval/tests/test_gate.py`: 11 tests passing.
- `eval/tests/test_stage_cap.py`: 7 tests passing.
- `eval/tests/test_journal_reconcile.py`: 10 tests passing.
- Full unit test suite passing (36/36 tests).
- Preconditions script: `PRECONDITIONS_OK`.
