---
phase: 06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme
plan: 04
type: execute
wave: 2
depends_on: ["06.3.1-01", "06.3.1-02", "06.3.1-03"]
files_modified:
  - engine/src/workflow/nodes/retrieve.rs
  - engine/src/workflow/nodes/assemble_prompt.rs
  - engine/src/workflow/mod.rs
  - engine/src/workflow/events.rs
  - engine/src/workflow/runner.rs
  - engine/src/tests/workflow_phase5.rs
  - gateway/internal/sse/sse.go
  - gateway/internal/sse/sse_test.go
  - gateway/main_test.go
  - scripts/engine-test-targets.sh
  - scripts/gateway-test-targets.sh
autonomous: true
requirements: [OBS-05]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A retrieval node that dies before finishing still emits a snapshot carrying the provenance known before retrieval ran — index generation, embedding model, both weights, rrf_k, candidate and final limits, and the active filter — with an empty chunk list (D-33, SC-2, empty-input edge)."
    - "The provenance on a failed record is populated, not blank: index_generation is a non-empty string on a timed-out retrieval, where 966 records of the 2026-09-03 run carried an empty one and were unattributable to any index generation."
    - "A failed retrieval that emits a partial snapshot with an empty chunk list, and a completed retrieval that found nothing, are distinct observable outcomes that never collapse into each other: the first carries the retrieval-failure notice with success false, the second carries the zero-evidence notice with success true (empty-input edge, adjacency edge)."
    - "Graph influence — graph-derived facts that actually reached the assembled prompt — is reported separately from graph presence, so a query with graph nodes found but zero facts packed is distinguishable from a query where the graph found nothing (D-06, SC-4)."
    - "A Go gateway client receives both the partial snapshot and the graph-influence count on the workflow_completed frame without the frame failing to serialise or violating the existing final_response/notices precedence (D-52)."
    - "The engine's success flag already reports false for a failed retrieval node; this plan pins that behaviour with a regression test rather than changing it, so production semantics are unchanged (D-29, D-30, SC-3)."
  artifacts:
    - engine/src/workflow/nodes/retrieve.rs
    - engine/src/workflow/nodes/assemble_prompt.rs
    - engine/src/workflow/mod.rs
    - engine/src/workflow/events.rs
    - engine/src/workflow/runner.rs
    - engine/src/tests/workflow_phase5.rs
    - gateway/internal/sse/sse.go
    - gateway/internal/sse/sse_test.go
    - gateway/main_test.go
    - scripts/engine-test-targets.sh
    - scripts/gateway-test-targets.sh
  key_links:
    - "retrieve.rs seeds ctx.snapshot before the first await -> the mutation survives the node future being dropped on timeout -> emit_terminal_once reads ctx.snapshot -> WorkflowCompletedEvent.partial_snapshot -> gateway SSE partial_snapshot key."
    - "pack_evidence_and_graph_prompt -> PackedEvidence.graph_facts.len() -> ctx.graph_prompt_fact_count -> WorkflowMetadata.graph_prompt_fact_count -> gateway metadata map."
    - "Every test added by plans 01-04 -> scripts/engine-test-targets.sh and scripts/gateway-test-targets.sh invariants, updated once, here."
  prohibitions:
    - "The success predicate must be the node-failure condition; it must not be implemented as a conjunction of the two per-path retrieval degradation notices, which are emitted at the end of the node body and never fire on a node timeout."
    - "Production success semantics must not be changed, and no eval-only behaviour switch may be introduced; the divergence D-29 anticipated does not exist in the engine."
    - "Nothing under eval/ may be modified; harness capture and journal persistence are owned by sibling 06.3.2."
    - "No timeout budget VALUE may be changed; the six values are derived in sibling 06.3.3."
    - "Test-count invariants must be raised to the newly measured values, never lowered to make a red gate pass."
    - "Graph traversal, fan-out, depth, candidate limits and prompt packing weights must not be retuned."
---

<objective>
Make a failed query attributable, and make graph contribution measurable, then close the phase's
test-count invariants.

Three things land here, all building on the proven wire path from plan 01:

1. **Provenance on failure (D-33, SC-2).** Seed the retrieval snapshot with everything known
   *before* retrieval runs, so a node killed mid-flight still reports which index generation,
   embedding model and retrieval parameters produced the failure. 966 of 1000 records in the
   recorded run carried a blank index generation and cannot be attributed to anything — an audit
   failure for a benchmark.
2. **Graph influence at prompt assembly (D-06, SC-4).** Count graph-derived facts that actually
   reached the assembled prompt, reported separately from graph presence. Without both, a
   near-zero ablation delta cannot distinguish "the graph found nothing" from "the graph found
   things the model ignored" — which is the measurement sibling 06.3.2 depends on.
3. **The phase's test-count invariants**, updated once, at the end, for all four plans.

Output: a partial snapshot on every failed terminal, a graph-influence counter on
`WorkflowMetadata`, both surfaced through the gateway SSE contract, and green invariant gates.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-CONTEXT.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-PATTERNS.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-01-SUMMARY.md
@rust-guidelines.md
@go-guidelines.md
</context>

<critical_findings>
1. **Why seeding the snapshot early works.** `engine/src/workflow/runner.rs:386` wraps
   `node.run(ctx, cancel)` in `tokio::time::timeout`. On expiry the node future is **dropped**,
   but `ctx` is owned by `run_workflow` and only mutably borrowed by that future — so every
   mutation applied to `ctx` **before** the drop persists and is visible to
   `emit_terminal_once`. Assigning `ctx.snapshot` at the top of the node body is therefore the
   correct and only reliable mechanism for D-33. Anything written after an await point inside
   the node is unreachable on the 904 records this exists to fix.
2. **Today's snapshot is built at the very end.** `engine/src/workflow/nodes/retrieve.rs:287-300`
   constructs `ctx.snapshot` after dense retrieval, per-variant BM25, cross-variant fusion and
   reranking have all completed — which is why a timeout leaves it `None`.
3. **The success flag is already correct; do not "fix" it.** Verified during planning:
   `run_workflow` (`runner.rs:512`) sets `overall_err` on any node `Err` and breaks, and
   `emit_terminal_once` (`runner.rs:665-676`) emits `success = false` with `final_response: None`.
   `gateway/internal/sse/sse.go:91` emits a top-level `notices` list **only** when
   `final_response` is nil, and all 904 `RetrieveHybrid`-timeout records in
   `eval/runs/2026-09-03-multihop_rag/journal.jsonl` carry one — proving they took the failure
   branch. The existing test `workflow_phase5_retrieve_timeout_ten_seconds`
   (`engine/src/tests/workflow_phase5.rs:956-1024`) already asserts `!completed_event.success`.
   **The false green came entirely from the harness** (`eval/src/lancet_eval/run.py:58` hardcodes
   `outcome="success"` for every non-exception result) — 06.3.2's D-27/D-01 work. See
   `<out_of_scope_deferrals>`. This plan pins the engine half and adds no eval-mode switch.
4. **`events::workflow_completed` has exactly two call sites**, both in
   `runner.rs::emit_terminal_once` (lines 654 and 666). Extending its signature is a two-line
   ripple, not a refactor.
5. **The gateway `workflow_completed` payload has a strict precedence invariant.**
   `gateway/internal/sse/sse_test.go:138-203` (`TestWriteWorkflowEventNoticePrecedence`) pins
   that `final_response` and a top-level `notices` list are mutually exclusive. The new
   `partial_snapshot` key must be set **unconditionally on the payload map**, outside that
   either/or, so provenance is not coupled to the failure branch in the DTO shape.
6. **The harness will not choke on the additive keys.** `eval/src/lancet_eval/client.py:90`
   declares `model_config = ConfigDict(extra="ignore")` on `WorkflowCompleted`, so unknown keys
   are dropped rather than raising. 06.3.2 declares them on `RunRecord`; this plan only has to
   put them on the wire.
7. **Graph weight `0.0` hard-excludes facts before packing** (`engine/src/prompt.rs:324-327`), so
   influence must read `0` on that path even when presence counters are non-zero. That is the
   test case that proves influence and presence are distinct.
8. **Test-count invariant gates exist and are strict.** `scripts/engine-test-targets.sh` asserts
   `TOTAL == 488`, `LIB_COUNT == 448`, `BIN_INSPECT_COUNT == 18`, `INTEG_CONFIG_COUNT == 22`.
   `scripts/gateway-test-targets.sh` asserts `EXPECTED_TOTAL == 112` with a per-package
   breakdown (`gateway 78`, `gateway/db 7`, `gateway/internal/config 4`,
   `gateway/internal/sse 11`, `gateway/internal/telemetry 12`). Plans 01-03 were instructed not
   to touch them; this plan updates both, once, to the newly measured values.
</critical_findings>

<artifacts_this_phase_produces>
Newly created symbols across all four plans of phase 06.3.1. A source-grounding or drift pass
must treat every entry below as new-in-this-phase.

**Proto (`proto/lancet/v1/lancet.proto`)** — plan 01
- `NOTICE_CODE_RETRIEVAL_FAILED = 19`
- `WorkflowCompletedEvent.partial_snapshot` (tag 8, `RetrievalSnapshot`) — **populated by this plan**
- `WorkflowMetadata.graph_prompt_fact_count` (tag 11, `uint32`) — **populated by this plan**

**Generated Rust / Go** — plan 01
- `NoticeCode::RetrievalFailed`, `WorkflowCompletedEvent::partial_snapshot`, `WorkflowMetadata::graph_prompt_fact_count`
- `pb.NoticeCode_NOTICE_CODE_RETRIEVAL_FAILED`, `GetPartialSnapshot()`, `GetGraphPromptFactCount()`

**Engine Rust**
- `WorkflowContext.graph_prompt_fact_count: u32` (`engine/src/workflow/mod.rs`) — **this plan**
- New `partial_snapshot` parameter on `events::workflow_completed` (`engine/src/workflow/events.rs`) — **this plan**
- `is_table_not_found(&lancedb::Error) -> bool` (`engine/src/db/mod.rs`) — plan 02
- `GraphSpanRetainingSampler`, `RETAINED_GRAPH_SPAN_NAMES` (`engine/src/telemetry/mod.rs`) — plan 03

**Gateway SSE payload keys (`gateway/internal/sse/sse.go`)** — **this plan**
- `partial_snapshot` on the `workflow_completed` payload
- `graph_prompt_fact_count` inside the `workflow_completed` `metadata` map

**Config documentation (no new keys)** — plan 03
- Budget-nesting comment block in `config/config.toml` and `config/config.example.toml`

**New tests**
- `workflow_phase5_retrieve_failure_emits_retrieval_failed_notice`, `workflow_phase5_zero_evidence_does_not_emit_retrieval_failed_notice`, `derive_degraded_mode_includes_retrieval_failed_regardless_of_position`, `TestRetrievalFailedNoticeRendersAsString` (plan 01)
- `get_or_create_table_fails_closed_on_non_missing_table_error`, `get_or_create_table_creates_only_on_missing_table_for_all_six_accessors` (plan 02)
- `graph_spans_retained_when_global_sampler_is_off` (plan 03)
- `workflow_phase5_retrieve_timeout_emits_partial_snapshot_with_provenance`, `workflow_phase5_graph_facts_reaching_prompt_are_counted`, `TestWorkflowCompletedCarriesPartialSnapshotAndGraphInfluence` (**this plan**)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit retrieval provenance even when the retrieval node never finishes</name>
  <read_first>
    - engine/src/workflow/nodes/retrieve.rs (whole file, 333 lines; especially the field set on `RetrieveHybridNode` at 16-25, `with_snapshot_metadata` at 46-54, the entry guard at 71-87, and the terminal snapshot construction at 261-309)
    - engine/src/workflow/runner.rs (lines 379-395 the timeout wrapper; lines 574-678 `emit_terminal_once`, especially the metadata construction at 620-631 and the two `events::workflow_completed` calls at 654 and 666)
    - engine/src/workflow/events.rs (lines 355-375 the `workflow_completed` builder)
    - engine/src/tests/workflow_phase5.rs (lines 956-1024 `workflow_phase5_retrieve_timeout_ten_seconds` — the paused-clock analog and `FakeDenseRetrievalPort::stall()`)
    - proto/lancet/v1/lancet.proto (the `RetrievalSnapshot` message and the `WorkflowCompletedEvent.partial_snapshot` field added by plan 01)
    - .planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-CONTEXT.md (D-33, D-29, D-30, D-52)
    - rust-guidelines.md (repo root)
  </read_first>
  <files>engine/src/workflow/nodes/retrieve.rs, engine/src/workflow/events.rs, engine/src/workflow/runner.rs, engine/src/tests/workflow_phase5.rs</files>
  <behavior>
    - Test `workflow_phase5_retrieve_timeout_emits_partial_snapshot_with_provenance`: a
      `RetrieveHybridNode` built via `with_snapshot_metadata` with a known index generation and
      embedding model, whose dense port stalls past the budget, produces a terminal event whose
      `partial_snapshot` is present, whose `index_generation` equals the configured value and is
      not the empty string, whose `embedding_model` equals the configured value, whose weights,
      `rrf_k`, `candidate_limit` and `final_limit` match the retrieval settings, and whose
      retrieved-chunk list is empty. The same event has `success` false and carries the
      retrieval-failure notice from plan 01.
    - Test (extend the existing zero-evidence test from plan 01, or add an assertion to it): a
      retrieval that *completes* with zero results produces `success` true, the zero-evidence
      notice, and its snapshot inside `final_response` — the failure and no-evidence shapes never
      collapse into one another.
    - Existing test `workflow_phase5_retrieve_timeout_ten_seconds` still passes unchanged,
      pinning `success == false` on a retrieval timeout (the D-29/D-30 engine guarantee).
  </behavior>
  <action>
    In `engine/src/workflow/nodes/retrieve.rs`, seed the snapshot **before any await point** in
    `execute` — after the cancellation guard and the variant-defaulting block, before the dense
    retrieval call. Assign `ctx.snapshot` a `RetrievalSnapshot` populated with everything known
    at that moment: `index_generation`, `embedding_model`, both weights, `rrf_k`,
    `candidate_limit`, `final_limit`, the active filter, the variant count and variant
    identities. Leave the retrieved-chunk list empty and the result hash empty, because neither
    is known yet. Add a comment stating (a) that this seed is what survives the node future
    being dropped on a budget expiry, (b) that it is overwritten on the success path, and (c)
    that an **empty result hash on a partial snapshot means retrieval never completed** — which
    is deliberately distinguishable from a hash computed over zero chunks, so sibling 06.3.2
    can tell "never ran to completion" from "completed and matched nothing" when it journals
    this field.

    **There will be two `ctx.snapshot` assignments in `execute` after this change, and that is
    correct.** Leave the existing terminal assignment at the end of the function exactly as it
    is — it overwrites the seed with the complete snapshot including the retrieved chunks and
    the result hash. Collapsing the two into one would drop the chunk list and the result hash
    from every successful query. Do not restructure the node, do not move the zero-evidence
    check, and do not emit any notice from this file.

    In `engine/src/workflow/events.rs`, add a `partial_snapshot: Option<RetrievalSnapshot>`
    parameter to `workflow_completed` and set the corresponding field on the constructed event.

    In `engine/src/workflow/runner.rs::emit_terminal_once`, pass `None` at the success call site
    (the snapshot already travels inside `final_response`) and `ctx.snapshot.clone()` at the
    failure call site. Document at the failure site that this is the only channel by which a
    failed terminal carries provenance, since `final_response` is absent there by contract.

    Add the new test to `engine/src/tests/workflow_phase5.rs`, modelled on the existing
    paused-clock retrieval-timeout test. Build the node with `with_snapshot_metadata` using
    distinctive non-empty values so a blank field fails loudly.

    **Do not** change the `success` flag logic, and **do not** add an eval-only behaviour switch
    or configuration key. See `<critical_findings>` item 3: the engine already satisfies D-29's
    predicate and D-30 requires production semantics to stay put. **Do not** implement the
    predicate as a conjunction of the two per-path retrieval degradation notices — they are
    emitted at the end of the node body and would evaluate false on exactly the records that
    must be caught.

    Do not edit `scripts/engine-test-targets.sh` in this task; Task 3 owns it.
  </action>
  <acceptance_criteria>
    - `cargo test --manifest-path engine/Cargo.toml workflow_phase5_retrieve_timeout_emits_partial_snapshot_with_provenance` passes.
    - That test asserts `index_generation` is non-empty AND equals the value passed to `with_snapshot_metadata`, and asserts the retrieved-chunk list length is `0`.
    - `cargo test --manifest-path engine/Cargo.toml workflow_phase5_retrieve_timeout_ten_seconds` still passes unchanged, and still asserts the terminal `success` is false.
    - `cargo test --manifest-path engine/Cargo.toml workflow_phase5_zero_evidence_does_not_emit_retrieval_failed_notice` still passes and additionally asserts the zero-evidence terminal reports `success` true.
    - Both snapshot assignments exist: `grep -c 'ctx.snapshot = Some' engine/src/workflow/nodes/retrieve.rs` is `2` — the seed and the terminal overwrite.
    - The seed precedes the first await: the line number from `grep -n 'ctx.snapshot = Some' engine/src/workflow/nodes/retrieve.rs | head -1` is strictly lower than the line number from `grep -n 'retrieve_dense' engine/src/workflow/nodes/retrieve.rs | head -1`.
    - The terminal assignment still populates the result hash — the guard against collapsing the two into one: `grep -c 'result_hash: result_hasher' engine/src/workflow/nodes/retrieve.rs` is `1`.
    - `grep -c 'partial_snapshot' engine/src/workflow/events.rs` is `>= 2` (parameter and field assignment).
    - `grep -c 'ctx.snapshot.clone()' engine/src/workflow/runner.rs` is `1`.
    - `grep -c 'RetrievalDegradedDense' engine/src/workflow/runner.rs` is `0` — the success predicate was not reimplemented as a notice conjunction.
    - `cargo clippy --manifest-path engine/Cargo.toml --all-targets -- -D warnings` reports no new warnings.
    - `git diff --stat -- eval/ config/` is empty.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml partial_snapshot_with_provenance</automated>
  </verify>
  <done>A retrieval node killed by its budget produces a terminal event carrying a populated, non-blank provenance snapshot with an empty chunk list, alongside `success=false` and the retrieval-failure notice; a completed-but-empty retrieval remains a distinct success-shaped outcome.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Count graph facts that actually reached the assembled prompt</name>
  <read_first>
    - engine/src/workflow/nodes/assemble_prompt.rs (whole file, 110 lines; the empty-evidence early return at 70-80 and the `pack_evidence_and_graph_prompt` match at 82-107)
    - engine/src/prompt.rs (lines 165-176 `PackedEvidence`; lines 320-335 the graph-weight semantics doc — `0.0` hard-excludes before packing)
    - engine/src/workflow/mod.rs (lines 76-147 the `WorkflowContext` struct and its constructor — where the new field is declared and defaulted)
    - engine/src/workflow/runner.rs (lines 620-643 the `WorkflowMetadata` construction and the span-record block that follows it)
    - engine/src/workflow/nodes/graph_context.rs (lines 125-160 — where `graph_node_count`/`graph_edge_count` are populated; the *presence* counters the new field must remain distinct from)
    - engine/src/tests/workflow_phase5.rs (an existing test that drives a full workflow with a graph port, to model the new test on)
    - .planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-CONTEXT.md (D-06, D-03, D-09)
    - rust-guidelines.md (repo root)
  </read_first>
  <files>engine/src/workflow/mod.rs, engine/src/workflow/nodes/assemble_prompt.rs, engine/src/workflow/runner.rs, engine/src/tests/workflow_phase5.rs</files>
  <behavior>
    - Test `workflow_phase5_graph_facts_reaching_prompt_are_counted`, case A: a workflow whose
      graph port returns facts and whose prompt assembly runs with a positive graph weight
      produces terminal metadata whose graph-influence count is greater than zero and equals the
      number of graph facts left on the context after packing.
    - Case B: the same workflow with graph weight `0.0` produces terminal metadata whose
      graph-influence count is `0` while the graph presence counters remain greater than zero —
      proving influence and presence are independent measurements.
    - Case C: a workflow that never reaches prompt assembly (retrieval failed) produces terminal
      metadata whose graph-influence count is `0`.
  </behavior>
  <action>
    In `engine/src/workflow/mod.rs`, add `pub graph_prompt_fact_count: u32` to `WorkflowContext`,
    initialised to `0` in `WorkflowContext::new`. Place it adjacent to `graph_node_count` and
    `graph_edge_count` and document that it measures graph **influence** — facts that survived
    into the assembled prompt — as opposed to those two, which measure graph **presence**
    (D-06). State that a non-zero presence with a zero influence is a meaningful, expected
    reading, not an inconsistency.

    In `engine/src/workflow/nodes/assemble_prompt.rs`, in the `Ok(packed)` arm, set
    `ctx.graph_prompt_fact_count = packed.graph_facts.len() as u32;` alongside the existing
    context assignments. Do not set it in the empty-evidence early-return path — that path packs
    a model-only prompt with no graph facts and must leave the count at zero.

    In `engine/src/workflow/runner.rs::emit_terminal_once`, add
    `graph_prompt_fact_count: ctx.graph_prompt_fact_count` to the `WorkflowMetadata`
    construction, and add a matching `current_span.record("lancet.workflow.graph_prompt_fact_count", ...)`
    line in the block immediately below, following the existing naming convention exactly.

    Add the three-case test to `engine/src/tests/workflow_phase5.rs`. Use
    `AssemblePromptNode::with_settings` to control the graph weight for case B.

    Do **not** retune traversal, fan-out, depth, candidate limits or prompt packing weights —
    the graph scope for this family is make-it-run plus outright bug fixes only. Do not edit
    `scripts/engine-test-targets.sh`; Task 3 owns it.
  </action>
  <acceptance_criteria>
    - `cargo test --manifest-path engine/Cargo.toml workflow_phase5_graph_facts_reaching_prompt_are_counted` passes with all three cases asserted.
    - Case B explicitly asserts the graph presence counters are greater than zero **in the same terminal event** where the influence count is zero.
    - `grep -c 'graph_prompt_fact_count' engine/src/workflow/mod.rs` is `>= 2` (declaration and initialisation).
    - `grep -c 'graph_prompt_fact_count' engine/src/workflow/nodes/assemble_prompt.rs` is `1`.
    - `grep -c 'graph_prompt_fact_count' engine/src/workflow/runner.rs` is `>= 2` (metadata field and span record).
    - `grep -c 'lancet.workflow.graph_prompt_fact_count' engine/src/workflow/runner.rs` is `1`.
    - `grep -c 'ctx.graph_prompt_fact_count = ' engine/src/workflow/nodes/assemble_prompt.rs` is `1` — assigned once, in the successful-packing arm only, so the model-only early return leaves it at its zero initialisation.
    - `cargo clippy --manifest-path engine/Cargo.toml --all-targets -- -D warnings` reports no new warnings.
    - `git diff --stat -- engine/src/prompt.rs engine/src/graph/` is empty (no traversal or packing retune).
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml graph_facts_reaching_prompt</automated>
  </verify>
  <done>Terminal workflow metadata reports how many graph-derived facts reached the assembled prompt, independently of how many graph nodes and edges were found.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Surface both new fields through the gateway and close the phase test invariants</name>
  <read_first>
    - gateway/internal/sse/sse.go (lines 83-135 — the `workflow_completed` payload construction, the `final_response`/`notices` precedence branch, and the `metadata` map with its nil fallback)
    - gateway/internal/sse/dto.go (the `ToQueryRAGResponseDTO` and snapshot DTO shapes to reuse for `partial_snapshot`)
    - gateway/internal/sse/sse_test.go (lines 100-210 `TestWriteWorkflowEventNoticePrecedence`; lines 380-425 `TestSSEWorkflowCompletedMetadataForwarding` — the two tests this change must keep green and the pattern for the new one)
    - gateway/main_test.go (the notice-code test added by plan 01, to mirror for an end-to-end assertion)
    - scripts/engine-test-targets.sh (whole file — the seven named assertions and the invariant-history comment convention)
    - scripts/gateway-test-targets.sh (whole file — `EXPECTED_TOTAL`, `EXPECTED_PACKAGES`, and the invariant-history comment convention)
    - go-guidelines.md (repo root; gateway/go.mod pins go 1.25.0)
  </read_first>
  <files>gateway/internal/sse/sse.go, gateway/internal/sse/sse_test.go, gateway/main_test.go, scripts/engine-test-targets.sh, scripts/gateway-test-targets.sh</files>
  <behavior>
    - Test `TestWorkflowCompletedCarriesPartialSnapshotAndGraphInfluence`: a terminal event with
      `Success: false`, no `final_response`, a populated `partial_snapshot` and a metadata
      message carrying a non-zero graph-influence count serialises to a `workflow_completed`
      frame whose `partial_snapshot` object carries the index generation, and whose `metadata`
      map carries the graph-influence count.
    - Same test, second case: a terminal event with `Success: true` and a `final_response`
      serialises with the `partial_snapshot` key present and null, `metadata` still carrying the
      influence count, and **no** top-level `notices` key — the existing precedence invariant is
      untouched.
    - `TestWriteWorkflowEventNoticePrecedence` and `TestSSEWorkflowCompletedMetadataForwarding`
      both still pass unmodified.
  </behavior>
  <action>
    In `gateway/internal/sse/sse.go`, in the `WorkflowEvent_WorkflowCompleted` case:

    (a) Add `graph_prompt_fact_count` to the `metadata` map, in **both** the populated branch
    (from the new proto getter) and the nil-metadata fallback branch (as `uint32(0)`), keeping
    the key ordering and style of the surrounding entries.

    (b) Set a `partial_snapshot` key on `wcPayload` **unconditionally**, before the
    `final_response`/`notices` branch — not inside either arm. Populate it from the new proto
    getter using the same snapshot DTO conversion the response DTO already uses, or nil when the
    event carries no partial snapshot. Keeping the key outside the branch means provenance is not
    structurally coupled to the failure path and the existing precedence invariant is
    untouched.

    Add the new test to `gateway/internal/sse/sse_test.go` following the existing
    `splitFrame`/`writeEvent` helpers. Then add a matching end-to-end assertion in
    `gateway/main_test.go` if the neighbouring notice test from plan 01 does not already cover
    the HTTP path — mirror its structure rather than inventing a new harness.

    Do not change `gateway/main.go`.

    **Then close the phase invariants.** Run `sh scripts/engine-test-targets.sh` and
    `sh scripts/gateway-test-targets.sh`, read the actual counts they print, and update the
    expected constants in both scripts to those measured values — engine `TOTAL`, `LIB_BIN_SUM`
    and `LIB_COUNT`; gateway `EXPECTED_TOTAL` and the affected `EXPECTED_PACKAGES` rows. Append a
    line to each script's invariant-history comment naming phase 06.3.1 and summarising which
    plans added tests. The counts must go **up**: plans 01-04 add tests and remove none. Lowering
    a value to make a red gate pass is never correct, and a count that did not rise means a test
    silently stopped compiling into the run — investigate rather than adjust.
  </action>
  <acceptance_criteria>
    - `cd gateway && go test ./internal/sse/... -run TestWorkflowCompletedCarriesPartialSnapshotAndGraphInfluence -count=1` passes.
    - `cd gateway && go test ./internal/sse/... -run 'TestWriteWorkflowEventNoticePrecedence|TestSSEWorkflowCompletedMetadataForwarding' -count=1` passes, with both test bodies unmodified: `git diff -- gateway/internal/sse/sse_test.go` shows only added lines.
    - `grep -c 'graph_prompt_fact_count' gateway/internal/sse/sse.go` is `2` (populated branch and nil fallback).
    - `grep -c 'partial_snapshot' gateway/internal/sse/sse.go` is `1`, and it appears before the line containing `GetFinalResponse() != nil`: `grep -n 'partial_snapshot' gateway/internal/sse/sse.go` reports a lower line number than `grep -n 'GetFinalResponse() != nil' gateway/internal/sse/sse.go`.
    - `git diff --stat -- gateway/main.go` is empty.
    - `sh scripts/engine-test-targets.sh` exits `0` and prints "All 7 Rust test target invariants verified successfully."
    - `sh scripts/gateway-test-targets.sh` exits `0`.
    - The engine `TOTAL` constant is strictly greater than `488` and `LIB_COUNT` strictly greater than `448`: `grep -oE '\-ne 488' scripts/engine-test-targets.sh | wc -l` is `0` and `grep -oE '\-ne 448' scripts/engine-test-targets.sh | wc -l` is `0`.
    - The gateway `EXPECTED_TOTAL` is strictly greater than `112`: `grep -c 'EXPECTED_TOTAL=112' scripts/gateway-test-targets.sh` is `0`.
    - Both scripts contain a new invariant-history comment line mentioning `06.3.1`.
    - `cd gateway && go vet ./...` is clean and `cd gateway && go test ./... -count=1` passes in full.
    - `cargo test --manifest-path engine/Cargo.toml` passes in full.
  </acceptance_criteria>
  <verify>
    <automated>sh scripts/engine-test-targets.sh &amp;&amp; sh scripts/gateway-test-targets.sh</automated>
  </verify>
  <done>Both new fields are visible to a Go gateway client on the `workflow_completed` frame without disturbing the `final_response`/`notices` precedence invariant, and both test-count invariant gates are green at their newly measured, higher values.</done>
</task>

</tasks>

<threat_model>
ASVS level 1; block on **high**.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go gateway HTTP/SSE -> API client | Retrieval provenance and graph-influence counts now reach unauthenticated local clients on the failure terminal. |
| Rust engine gRPC -> Go gateway | Two new proto fields must be populated and rendered consistently across both generated trees. |
| Test-invariant scripts -> CI signal | The gates are the repo's guard against tests silently vanishing from the run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.1-15 | Information disclosure | `partial_snapshot` exposed to API clients on a failure terminal | medium | mitigate | The seeded snapshot carries only retrieval configuration and provenance — index generation, embedding model name, weights, `rrf_k`, limits, the caller's own filter, and the caller's own query variants. It carries no chunk text, no document content, and no store path. The chunk list is empty by construction on the failure path. Do not add corpus content, absolute paths, or driver error text to the seeded snapshot. |
| T-06.3.1-16 | Information disclosure | Variant identities echoed back on failure | low | accept | `variant_identities` are derived from the caller's own query and are already returned inside `final_response.snapshot` on the success path; echoing them on the failure path discloses nothing the caller did not send. Deployment is local-only by design. |
| T-06.3.1-17 | Tampering | New DTO key placed inside the `final_response`/`notices` branch | medium | mitigate | Acceptance criteria pin that `partial_snapshot` is set before the branch, and `TestWriteWorkflowEventNoticePrecedence` must pass with its body unmodified, so the precedence invariant cannot be relaxed to accommodate the new key. |
| T-06.3.1-18 | Repudiation | Test-count invariants weakened to clear a red gate | high | mitigate | Acceptance criteria assert the old constants (`488`, `448`, `112`) are **absent** from the updated scripts and that both scripts exit 0, so the only passing state is one where the counts rose. An invariant-history line naming this phase records why. |
| T-06.3.1-19 | Spoofing | Influence count mistaken for presence, or vice versa | medium | mitigate | Distinct proto field, distinct span attribute, distinct DTO key, and a test case asserting non-zero presence alongside zero influence in the same terminal event. Field documentation states the distinction at the declaration site. |
| T-06.3.1-20 | Denial of service | Snapshot seeding on every query | low | accept | The seed is a single struct construction of small scalar fields plus two clones already performed on the success path; it runs once per query before any I/O. No measurable cost. |
| T-06.3.1-SC | Tampering | npm/pip/cargo/go supply chain | high | mitigate | **No package-manager installs in this plan.** No crate added to `engine/Cargo.toml`, no module added to `gateway/go.mod`, no Python dependency (nothing under `eval/` is touched). Acceptance gate: `git diff --stat -- engine/Cargo.toml engine/Cargo.lock gateway/go.mod gateway/go.sum buf.gen.yaml` is empty at plan close. |
</threat_model>

<verification>
1. `cargo test --manifest-path engine/Cargo.toml` passes in full.
2. `cd gateway && go test ./... -count=1` passes in full.
3. `sh scripts/engine-test-targets.sh` and `sh scripts/gateway-test-targets.sh` both exit 0.
4. `cargo clippy --manifest-path engine/Cargo.toml --all-targets -- -D warnings` is clean; `go vet ./...` is clean.
5. `git diff --stat -- eval/ config/ gateway/main.go engine/src/prompt.rs engine/src/graph/` is empty.
6. `git diff --stat -- engine/Cargo.toml engine/Cargo.lock gateway/go.mod gateway/go.sum` is empty.
</verification>

<success_criteria>
- ROADMAP Success Criterion 2 is met: a failed retrieval emits a partial snapshot with populated
  provenance and an empty chunk list.
- ROADMAP Success Criterion 3 is met: the engine reports `success=false` when the retrieval node
  failed and produced no evidence, pinned by regression test, implemented as the node-failure
  condition and not as a notice conjunction, with production semantics unchanged.
- ROADMAP Success Criterion 4 is met: a graph-influence counter at `AssemblePrompt` records
  whether graph-derived context reached the prompt, distinct from graph presence.
- ROADMAP Success Criterion 7 is completed: the SSE DTO carries the partial snapshot without
  choking, with a gateway test.
- Both test-count invariant gates are green at raised values covering all four plans.
</success_criteria>

<out_of_scope_deferrals>
- **The harness half of the false green (D-27, D-01) -> owned by sibling 06.3.2.** Planning-time
  forensics established that the engine already emits `success=false` for all 966 failed records
  and that `eval/src/lancet_eval/run.py:58` hardcodes `outcome="success"` for every non-exception
  result, discarding it. Persisting `NodeCompletedEvent.duration_ms`, `WorkflowMetadata`
  (including the two fields this plan adds), and declaring them on `RunRecord` is 06.3.2's work.
  **Nothing under `eval/` is modified here.**
- **Scored report dimensions, paired ablation, denominator convention,
  `wire_contract_conformance` redefinition, preflight canary, and the four open
  `06.3-REVIEW.md` findings -> sibling 06.3.2.**
- **The six timeout budget VALUES, the latency measurement pass, graph population inspection, and
  the decay discrimination -> sibling 06.3.3.** Any engine or config defect the measurement pass
  exposes lands in 06.3.3 or 06.3.4 and does not reopen phase 06.3.1 (D-46 back-edge rule).
- **The corrected re-drive, human calibration, root-cause documentation and the Phase 6.4 ROADMAP
  update -> sibling 06.3.4.**
- **Explicit non-change, recorded so it is not read as a defect:** the six
  `ExtractGraphContext` "Query embedding timed out" failures in the recorded run currently fail
  the whole query. That is *not* a violation of D-29's "graph failure of any kind stays a
  non-fatal degradation" clause — the failing operation is the query-embedding prelude
  (`engine/src/workflow/nodes/graph_context.rs:73-93`), whose output feeds dense retrieval, not
  the graph traversal at `:113-121` (which already degrades non-fatally and returns `Ok`).
  Making an embedding failure non-fatal would silently produce a retrieval run with no query
  vector. Left unchanged deliberately.
</out_of_scope_deferrals>

<output>
Create `.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-04-SUMMARY.md` when done.
</output>
