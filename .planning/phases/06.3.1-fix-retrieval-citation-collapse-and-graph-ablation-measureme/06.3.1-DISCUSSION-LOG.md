# Phase 06.3.1: Fix retrieval citation collapse and graph ablation measurement fidelity - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `06.3.1-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-09-04
**Phase:** 06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measurement-fidelity
**Areas discussed:** Graph-RAG Diagnostics (user-added, highest priority), Timeout fix shape (incl. `get_or_create_table` and the decay signature), Failure visibility, Ablation pairing, Re-drive scope + gate, AI-SPEC handling, Wire-contract blast radius, Phase 6.4 contract updates, Fix sequencing and plan decomposition

**Framing note:** the discussion opened by establishing, from the recorded journal, that the ROADMAP's premise was wrong — the near-zero citation rate is a perfect mirror of retrieval success (verified as set identity), and the cause is `RetrieveHybrid` node timeouts, not citation prompting or the generation-model swap. Every area below was shaped by that evidence.

---

## Graph-RAG Diagnostics

*User-added area, contributed as free text alongside the four offered: "How to gather enough data to know what's wrong with and how to fix/improve graph-rag function which is the key component of this project." Discussed first.*

| Option | Description | Selected |
|--------|-------------|----------|
| Capture the wire data | Persist `NodeCompletedEvent.duration_ms` + `WorkflowCompletedEvent.metadata`; zero engine change | ✓ |
| Persist OTel traces per query | Richer, span-level, but needs a collector and traces are ephemeral | |
| Both — wire always, traces sampled | Complete but more setup | |

**User's choice:** Capture the wire data.
**Notes:** Grounded on the discovery that `client.py:218` discards `node_completed` in a bare `pass` arm, and `graph_node_count` appears zero times in the 1000-record journal.

| Option | Description | Selected |
|--------|-------------|----------|
| Diagnostic sidecar only | Keep `report.md`'s 12-dimension contract frozen for 6.4 | *(first answer, reversed)* |
| First-class scored dimension | Graph metrics into `report.md`; widens 6.4's contract | ✓ |
| Sidecar now, promote if earned | Evidence-gated | |

**User's choice:** First-class scored dimension.
**Notes:** **This is a reversal.** The user initially selected "Diagnostic sidecar only", then interrupted and asked to return to the question. On re-presentation with the fuller tradeoff — including that `graph_ablation_delta` already sits in `report.md` and is the dimension currently lying — they changed to the scored dimension. Recorded because the reversal is load-bearing for what Phase 6.4 documents.

| Option | Description | Selected |
|--------|-------------|----------|
| Make it run + fix outright bugs | No tuning, no redesign | ✓ |
| Diagnose only — fix nothing in the graph | | |
| Run + bugs + tuning if evidence supports | Risks becoming a graph-quality phase | |

**User's choice:** Make it run + fix outright bugs.

| Option | Description | Selected |
|--------|-------------|----------|
| Raise the budget, accept slow graph queries | | ✓ |
| Document as a measured limitation and move on | | |
| Escalate — stop and re-decide | | |

**User's choice:** Raise the budget, accept slow graph queries.

| Option | Description | Selected |
|--------|-------------|----------|
| Verify graph population first | Inspect the surviving eval store before touching timeouts | ✓ |
| Verify, and re-seed if empty or sparse | | |
| No — query-time only | | |

**User's choice:** Verify population first; re-seeding explicitly excluded.
**Notes:** Prompted by an acknowledged limit in the earlier reasoning — `get_or_create_table` was exonerated on evidence about the *documents* table, which said nothing about whether the *graph* tables were populated.

| Option | Description | Selected |
|--------|-------------|----------|
| Presence and influence, reported separately | Presence free on the wire; influence needs an `AssemblePrompt` counter | ✓ |
| Presence only | Cheaper; leaves a zero delta ambiguous | |
| Influence only | Loses traversal-vs-prompt-drop distinction | |

**User's choice:** Presence and influence, separately.
**Notes:** Grounded on `lancet.proto:92` — graph retrieval is not fused into `retrieved_chunks`, so the two are genuinely different measurements.

| Option | Description | Selected |
|--------|-------------|----------|
| Structured fields only | Git-friendly; a new question still needs a re-drive | |
| Full raw SSE stream | Any future question answerable; large committed artifacts | |
| Raw on failures + sampled successes | Bounded; failures are where forensics goes | ✓ |

**User's choice:** Raw stream on failures and a sampled subset.

| Option | Description | Selected |
|--------|-------------|----------|
| Extend the existing preflight | Probe the query path so preflight fails loud | ✓ |
| Mid-run abort threshold | Catches degradation that starts healthy | |
| Both | This run needed both | |
| No — dimensions are guard enough | Detector, not guard | |

**User's choice:** Extend the existing preflight.

| Option | Description | Selected |
|--------|-------------|----------|
| Total duration is enough to start | Avoids buying telemetry before it is needed | |
| Wire-level graph sub-timings | One instrument, engine change | |
| Turn OTel retention on for the graph node | Uses Phase 6's existing Cypher instrumentation | ✓ |

**User's choice:** OTel retention for the graph node specifically.
**Notes:** Recorded in CONTEXT.md as a refinement of the wire-data decision, not a reversal — wire capture stays primary for all queries.

| Option | Description | Selected |
|--------|-------------|----------|
| Symmetric coverage across all three paths | | ✓ |
| Graph deep, other paths presence only | | |
| Graph only | | |

**User's choice:** Symmetric across vector, BM25 and graph.
**Notes:** Asked after correcting a possible over-focus on the graph — the graph-off arm collapsed too (482/500 zero chunks), so the vector/BM25 path is broken independently.

| Option | Description | Selected |
|--------|-------------|----------|
| Committed canary set with fixed expectations | Deterministic, diffable | ✓ |
| N randomly sampled questions per run | Broader but non-reproducible | |
| Reuse the single-question probe | Thin — this run's first 11 all passed | |

**User's choice:** Committed canary set.

| Option | Description | Selected |
|--------|-------------|----------|
| Phase-dir root-cause doc, cited by 6.4 | | |
| A permanent doc under `docs/` | | |
| Both — full trail in-phase, distilled note in `docs/` | | ✓ |
| No separate doc | | |

**User's choice:** Both.

---

## Timeout fix shape (incl. `get_or_create_table` and the decay signature)

| Option | Description | Selected |
|--------|-------------|----------|
| Fix the production values | `config.eval.toml` overrides only `lancedb_path` | ✓ |
| Eval overlay only | Would benchmark a config nobody runs | |
| Production plus eval headroom | Two sets of numbers to explain | |

**User's choice:** Fix the production values.

| Option | Description | Selected |
|--------|-------------|----------|
| From measured p95/p99 with a stated multiplier | | ✓ |
| Generous round numbers now, tighten later | | |
| Replace per-node timeouts with one query deadline | Interacts with D-13 | |

**User's choice:** Measured p95/p99.

| Option | Description | Selected |
|--------|-------------|----------|
| A cheap high-budget measurement pass first | Converges with the ROADMAP's staged pre-check | ✓ |
| Add a measurement mode to the harness | New surface, a bypass switch | |
| Derive from the 34 successes plus probes | Survivorship-biased | |

**User's choice:** High-budget measurement pass.
**Notes:** Asked because the current budgets truncate the very measurement needed.

| Option | Description | Selected |
|--------|-------------|----------|
| Investigate slowness as a bug first, then raise | | ✓ |
| Raise first, investigate later | | |
| Raise only — slowness is its own phase | | |

**User's choice:** Investigate first.

| Option | Description | Selected |
|--------|-------------|----------|
| All six budgets re-derived together | | ✓ |
| Only the four that failed | | |
| Only retrieve and graph_operation | | |

**User's choice:** All six.

| Option | Description | Selected |
|--------|-------------|----------|
| Startup validation for coherent nesting | Structural mechanism | |
| Document the relationships, no enforcement | | ✓ |
| Just fix the values | | |

**User's choice:** Document, do not enforce.
**Notes:** Raised after finding `graph_operation_timeout_ms=4000` nested inside `graph_node_timeout_ms=15000`, so the outer budget can never fire.

| Option | Description | Selected |
|--------|-------------|----------|
| The preflight canary is the guard | | ✓ |
| Canary plus a config-value floor test | | |
| Canary plus a node-timeout behavior test | | |

**User's choice:** Canary only.

| Option | Description | Selected |
|--------|-------------|----------|
| ROADMAP shape — fail closed except `TableNotFound` | | ✓ |
| Fail closed, and validate schema after create | Closes the drift gap too | |
| Stop creating tables on the accessor path entirely | Architecturally cleanest, bigger change | |

**User's choice:** ROADMAP shape.
**Notes:** Two ROADMAP corrections surfaced here — the function backs six accessors, not five, and the create path skips `validate_schema` (that gap is left open, recorded as deferred).

| Option | Description | Selected |
|--------|-------------|----------|
| Decay is primary, budgets secondary | | |
| Test both, let the measurement pass decide | | ✓ |
| Fix budgets, re-drive, see if decay recurs | Burns a paid drive to learn it | |

**User's choice:** Test both.
**Notes:** Asked after computing the success-density decay across journal positions (10/11 → 13 → 4 → 3 → 0), which undercuts a uniformly-too-tight budget as the sole explanation.

| Option | Description | Selected |
|--------|-------------|----------|
| Retrieval-only first, then small full-query confirm | Needs a harness mode that does not exist | |
| Full queries throughout | Real load, real cost | |
| Full queries against a cheap generation model | Preserves load at a fraction of spend | ✓ |

**User's choice:** Full queries, cheap model.

| Option | Description | Selected |
|--------|-------------|----------|
| Fix decay here — it is the blocking defect | | |
| Fix here, checkpoint if it grows | | |
| Diagnose here, fix in a dedicated phase | | ✓ *(later superseded)* |

**User's choice (free text):** "Diagnose here, fix in another sub phase before 6.4, just like this phase was derived from 6.3."
**Notes:** **Superseded later in the same discussion** — see Fix sequencing below, where the user folded the decay fix back in and chose a four-way split of the whole phase instead.

| Option | Description | Selected |
|--------|-------------|----------|
| Re-drive in 06.3.2 after the decay fix | | |
| Conditionally in 06.3.1, moved only if decay confirmed | | ✓ *(later superseded)* |
| In 06.3.1 regardless, re-drive again if needed | | |

**User's choice:** Conditional. **Superseded** by the four-way split.

| Option | Description | Selected |
|--------|-------------|----------|
| Upward latency trend with query ordinal | Measures the phenomenon directly | ✓ |
| Any timeout at raised budgets | Can miss early-stage decay | |
| Both | Risks triggering on noise | |

**User's choice:** Upward latency trend.

| Option | Description | Selected |
|--------|-------------|----------|
| Restart the engine mid-pass | Splits process-state from store-state | ✓ |
| Restart plus a checkpoint-truncation segment | Isolates the D-58 hypothesis | |
| No — keep the pass simple | | |

**User's choice:** Mid-run restart, two segments.

| Option | Description | Selected |
|--------|-------------|----------|
| As-is — reproduce the original conditions | Free; best chance of reproducing | ✓ |
| Clear checkpoints, keep the store | Destroys evidence before measuring it | |
| Fully clean — re-seed | ~3h and paid embeddings | |

**User's choice:** As-is.

---

## Failure visibility

| Option | Description | Selected |
|--------|-------------|----------|
| Both — engine semantics and harness check | Defence in depth | ✓ |
| Engine only | Single source of truth | |
| Harness only | Leaves real API users misled | |

**User's choice:** Both.

| Option | Description | Selected |
|--------|-------------|----------|
| Add the notice; keep success semantics | | |
| Add the notice AND make total failure not-success | | ✓ *(with a graph exemption)* |
| Add the notice, rely on `degraded_mode` | | |

**User's choice (free text):** "Add the notice AND make total failure not-success unless the non-graph lag on (for test case only, cause we still can not confirm if graph can really get any data in production's messy/fresh data)."
**Notes:** Reflected back and split into two confirming questions rather than interpreted. Grounded on the finding that `derive_degraded_mode` flagged all 494 graph-on records (which the harness discarded), but nothing at all fired for the graph-off arm because no notice exists for a dead `RetrieveHybrid`.

| Option | Description | Selected |
|--------|-------------|----------|
| Production wire contract | One contract, one behavior | |
| Eval/test only | Shipped API untouched near v1 | ✓ |
| Production behind a flag | | |

**User's choice:** Eval/test only.

**User's counter-question:** *"Is it somehow confirm graph can definitely return something in production? like the vector-rag?"*
**Answer given:** No, and requiring it would be wrong. `GraphAugmentationOutcome::NoMatchFound` (`service.rs:487`) is a first-class designed outcome. Vector/BM25 are ranking operations — over a non-empty index there is always a nearest neighbour, so zero results means broken. Graph traversal is conditional on entity resolution, so zero results does not imply broken. Noted as unverified: whether `NoMatchFound` is threshold-driven (recorded as a deferred research item).

| Option | Description | Selected |
|--------|-------------|----------|
| Only non-graph total failure is not-success | | |
| Same, plus graph dimensions never gate a run | | |
| Same, plus a canary-only conditional guarantee | | ✓ |

**User's choice (free text addition):** Canary-only conditional guarantee, plus: *"btw, the benchmark data we use should be able to run multi-hop so we should probably no see non-graph result as normal?"*
**Notes:** Accepted as a correct and important refinement — the production exemption and the MultiHop-RAG expectation are different scopes. Noted that the true `NoMatchFound` rate is currently unknown (494 timeouts vs only 4 `GRAPH_UNAVAILABLE`).

| Option | Description | Selected |
|--------|-------------|----------|
| Reported finding, no threshold yet | Honest about the missing baseline | |
| Reported finding plus a provisional floor | | ✓ |
| Hard gate on the re-drive | Risks blocking v1 on graph quality | |

**User's choice:** Reported finding plus a provisional floor.

| Option | Description | Selected |
|--------|-------------|----------|
| Exclude infrastructure failures from all denominators | | ✓ |
| Score as failure | Conflates environment with model quality | |
| Report both views | Doubles report surface | |

**User's choice:** Exclude and report separately.
**Notes:** Raised alongside the existing inconsistency — retrieval dimensions already excluded snapshot-less records (caveat 6) while answer dimensions counted them as zeros.

| Option | Description | Selected |
|--------|-------------|----------|
| Redefine to include self-contradiction checks | Would have read 0.034, not 1.000 | ✓ |
| Leave it — new dimensions cover health | | |
| Keep the metric, rename and re-caption | | |

**User's choice:** Redefine.

| Option | Description | Selected |
|--------|-------------|----------|
| CR-01 calibration verification bypass | | ✓ |
| WR-03 `primary_arm` always graph-on | | ✓ |
| WR-02 duplicate journal records | | ✓ |
| WR-04 + IN-02 silent config/cache failures | | ✓ |

**User's choice:** All four (multi-select).

| Option | Description | Selected |
|--------|-------------|----------|
| New notice code at the free tag (19) | Keeps "found nothing" distinct from "died trying" | ✓ |
| Reuse `NO_EVIDENCE` | Merges a designed outcome with a failure | |
| Emit both `RETRIEVAL_DEGRADED_DENSE`/`_BM25` | Not quite "the node never ran" | |

**User's choice:** New notice code.
**Notes:** Grounded on `retrieve.rs` — the snapshot (:287) and `NO_EVIDENCE` (:305) are both at the end of `run`, so a timeout reaches neither.

| Option | Description | Selected |
|--------|-------------|----------|
| Emit a partial snapshot on failure | Failed records currently carry `index_generation: ""` | ✓ |
| Leave it null | | |
| Partial, explicitly marked incomplete | | |

**User's choice:** Partial snapshot on failure.

---

## Ablation pairing

| Option | Description | Selected |
|--------|-------------|----------|
| Drop the pair, report coverage explicitly | | ✓ |
| Count the missing arm as zero | Conflates infrastructure with capability | |
| Drop, plus an unpaired-cause breakdown | | |

**User's choice:** Drop and report coverage.
**Notes:** Presented with the measured overlap — the two arms shared only 8 questions, so the full 500×2 drive supports 8 pairs (1.6% coverage).

| Option | Description | Selected |
|--------|-------------|----------|
| Refuse below a pair floor, CI above it | Would have reported "skipped: 8 pairs" | |
| Always report, with a confidence interval | | ✓ |
| Delta and coverage, no statistics | | |

**User's choice:** Always report with a CI.

| Option | Description | Selected |
|--------|-------------|----------|
| Recall plus deterministic answer metrics | No extra API spend | ✓ |
| Recall only, as the ROADMAP specifies | | |
| Recall, deterministic, and judged dimensions | Doubles judged spend | |

**User's choice:** Recall plus deterministic answer metrics.

| Option | Description | Selected |
|--------|-------------|----------|
| Stratify by question type, plus aggregate | Tests the graph's premise where it should show | ✓ |
| Aggregate only | | |
| Stratify above a sample floor only | | |

**User's choice:** Stratify and aggregate.

| Option | Description | Selected |
|--------|-------------|----------|
| Enforce a maximum positional gap | | |
| Document it, do not enforce | | ✓ |
| Report the distribution only | | |

**User's choice:** Document.
**Notes:** Measured first — all 500 questions had their arms a median of 1 position apart (max 9), so D-47's interleaving already works and paired comparison is robust to the decay.

| Option | Description | Selected |
|--------|-------------|----------|
| Paired latency delta next to quality delta | | |
| Latency plus token cost | | ✓ |
| Quality delta only | | |

**User's choice:** Latency and token cost.

| Option | Description | Selected |
|--------|-------------|----------|
| Redefine in place, mark the old run non-comparable | | ✓ |
| Add a new paired dimension, retire the old | | |
| Redefine in place, no annotation | | |

**User's choice:** Redefine in place with a non-comparability marker.

| Option | Description | Selected |
|--------|-------------|----------|
| Report a negative delta plainly | | |
| Report it, and trigger a graph phase | | |
| Report it, but investigate before publishing | | ✓ |

**User's choice:** Investigate before publishing.

---

## Re-drive scope + gate

| Option | Description | Selected |
|--------|-------------|----------|
| Same 500×2, gated on pairing coverage | | |
| Smaller staged drive first, then full 500×2 | | ✓ |
| Larger sample | | |

**User's choice:** Staged, then full.

| Option | Description | Selected |
|--------|-------------|----------|
| Keep it, marked superseded and cited as evidence | | ✓ |
| Keep it, marked superseded, no citation | | |
| Remove it once the corrected run lands | Destroys the forensic record | |

**User's choice:** Keep, mark superseded, cite as evidence.

| Option | Description | Selected |
|--------|-------------|----------|
| Fresh full calibration | | |
| Fresh, sized to available effort | | ✓ |
| Defer calibration to a follow-up | Reopens the gap that kept 6.3 open | |

**User's choice:** Fresh, explicitly sized.
**Notes:** Flagged that the corrected run demands *more* human scoring than the broken one — 98 of 100 sampled questions were `skipped_no_evidence` before, so the worksheet will be full for the first time.

| Option | Description | Selected |
|--------|-------------|----------|
| A stated phase budget with a checkpoint before the full drive | | |
| Per-stage caps, no overall ceiling | | ✓ |
| Rely on the existing daily limit | | |

**User's choice:** Per-stage caps.

| Option | Description | Selected |
|--------|-------------|----------|
| Requirement-less internal-quality phase | | |
| Cite OBS-02, with the shared-ID hazard noted | | |
| Add a new requirement for evaluation fidelity | | ✓ |

**User's choice:** New requirement.
**Notes:** Presented with the shared-ID hazard — OBS-02 is already `[x]` and mapped to Phase 06.3, and this repo has previously had a parent phase's checkbox wrongly un-checked for an ID split across phases (`RAG-03`).

| Option | Description | Selected |
|--------|-------------|----------|
| Everything but the drive — complete without a run | | |
| Same, plus a small validated run | | |
| Do not complete 06.3.1 until 06.3.2 lands | | ✓ *(later superseded)* |

**User's choice:** Hold open. **Superseded** by the four-way split.

---

## AI-SPEC / eval-plan handling

| Option | Description | Selected |
|--------|-------------|----------|
| Amend 06.3's AI-SPEC in place | | |
| New AI-SPEC for the family via `/gsd-ai-integration-phase` | | ✓ |
| No AI-SPEC — plan directly from CONTEXT.md | | |

**User's choice:** New AI-SPEC for the family.

---

## Wire-contract blast radius

| Option | Description | Selected |
|--------|-------------|----------|
| Regenerate both protos, add gateway coverage | | ✓ |
| Regenerate both protos, no new tests | | |
| Engine only — gateway follows in 6.4 | | |

**User's choice:** Regenerate both, add coverage.
**Notes:** Grounded on `gateway/main.go:554` converting notice codes generically, so no gateway logic change is needed — but the vendored `gateway/proto/lancet/v1/lancet.pb.go` must be regenerated or the new code surfaces to clients as the bare number `19`.

---

## Phase 6.4 contract updates

| Option | Description | Selected |
|--------|-------------|----------|
| This family updates 6.4's entry at closure | | ✓ |
| Depends-on and canonical refs only | | |
| Leave it all to 6.4 | | |

**User's choice:** Update 6.4's entry at closure.

---

## Fix sequencing and plan decomposition

**User's counter-question:** *"Is gsd really allow this 'complete later phase then back to this phase'? If not, then how?"*
**Answer given:** GSD does not enforce sequential completion — `phase complete` takes a phase number with no predecessor check, `phase insert --after` is first-class, and `Depends on:` is advisory metadata consumed by `analyze-dependencies` and `plan-phase` rather than a gate. But the repo's own precedent points the other way: Phase 6.3 closed while explicitly carrying this defect forward to 06.3.1.

| Option | Description | Selected |
|--------|-------------|----------|
| Follow the 6.3 precedent — complete 06.3.1, 06.3.2 owns the re-drive | | |
| Keep 06.3.1 open until 06.3.2 lands | | |
| No 06.3.2 — fold the decay fix into 06.3.1 | | |

**User's choice (free text):** "Put this 06.3.2 with 06.3.1 now then later do the split for the entire huge 06.3.1 since it's large enough to split already."
**Notes:** This **supersedes three earlier decisions** — the conditional 06.3.2 insertion, the conditional re-drive location, and holding 06.3.1 open. The decay fix returns to scope, and the split becomes a deliberate decomposition of the whole phase rather than a carve-out.

| Option | Description | Selected |
|--------|-------------|----------|
| Roadmap split into sibling phases before planning | | ✓ |
| Keep one phase; let plan-phase decompose into waves | | |
| Decide after planning reveals the real size | | |

**User's choice (free text addition):** "Roadmap split into sibling phases before planning (with the same approach as phase 6 that share the same discussion and context)."
**Notes:** Answered with the shared-context operational hazards this repo has already hit — the `context_path` auto-load failure below a 500K context window, and the phase-dir-scoped decision-coverage glob that silently skips. Both written into CONTEXT.md as planner warnings.

| Option | Description | Selected |
|--------|-------------|----------|
| Three siblings: engine+config / harness / measurement+drive | | |
| Two siblings: all fixes / measurement and drive | | |
| Four siblings — split the drive out from measurement | | ✓ |

**User's choice:** Four siblings, so the go/no-go for paid spend sits on a phase boundary.

---

## Claude's Discretion

No area was answered "You decide" — every decision is the user's. Left to downstream judgment: budget multiplier values, canary set composition and size, measurement-pass sample size, the provisional graph-yield floor value, the confidence-interval method, and the new requirement's ID and wording.

---

## Deferred Ideas

- Graph-RAG quality work — traversal tuning, reranking, redesign.
- Mid-run abort threshold on rolling failure rate.
- Re-seeding the eval corpus with a verified graph build.
- Replacing per-node timeouts with a single end-to-end query deadline.
- Startup validation enforcing coherent timeout nesting.
- Post-create `validate_schema` in `get_or_create_table`; removing lazy creation from the accessor path. The schema-drift gap in the create path remains open.
- A first-class harness "measurement mode" flag.
- Runtime enforcement of arm adjacency.
- Pairing the judged dimensions across arms.
- Extending the stricter `success = false` semantics to production after v1.
- Whether `NoMatchFound` is threshold-driven rather than purely structural — unverified during this discussion.
