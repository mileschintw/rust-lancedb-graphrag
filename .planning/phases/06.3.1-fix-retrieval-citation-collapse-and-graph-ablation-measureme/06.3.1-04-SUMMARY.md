# Phase 06.3.1 Plan 04 Summary

## Metadata
- **Phase:** 06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measurement
- **Plan:** 06.3.1-04
- **Date:** 2026-09-05
- **Status:** Complete

## Objective
Emit retrieval provenance on terminal failure events (`partial_snapshot`), measure graph influence reaching the assembled prompt (`graph_prompt_fact_count`) independently of graph presence counters, expose both fields across the gateway SSE interface, and close all phase test-count invariants at newly elevated targets.

## Deliverables & Key Changes
1. **Retrieval Snapshot Early Seeding & Terminal Emission (D-33, D-29, D-30):**
   - In `engine/src/workflow/nodes/retrieve.rs`, seeded `ctx.snapshot` before the first await point with initial retrieval provenance (index generation, embedding model, weights, rrf_k, limits, active filter, variants), leaving `result_hash` and `retrieved_chunks` empty.
   - On the happy path, step 4 overwrites `ctx.snapshot` with retrieved chunks and populated `result_hash`.
   - In `engine/src/workflow/events.rs`, replaced plan 01 inert stub `partial_snapshot: None` with a function parameter on `events::workflow_completed`.
   - In `engine/src/workflow/runner.rs::emit_terminal_once`, passed `ctx.snapshot.clone()` on failure, ensuring terminal failures carry provenance attribution.
2. **Graph Influence Measurement & Span Field Declaration (D-06, D-03, D-09):**
   - In `engine/src/workflow/mod.rs`, added `pub graph_prompt_fact_count: u32` to `WorkflowContext`.
   - In `engine/src/workflow/nodes/assemble_prompt.rs`, set `ctx.graph_prompt_fact_count = packed.graph_facts.len() as u32;` when prompt packing succeeds.
   - In `engine/src/workflow/runner.rs`, forwarded `ctx.graph_prompt_fact_count` into `WorkflowMetadata` and recorded `"lancet.workflow.graph_prompt_fact_count"` on the active span.
   - Declared `"lancet.workflow.graph_prompt_fact_count" = tracing::field::Empty` on the `query_rag` span at both declaration sites (`engine/src/service.rs` and `engine/src/tests/telemetry_query.rs`), preventing silent no-op discards in `tracing`.
3. **Gateway SSE Exposure & DTO Extraction:**
   - In `gateway/internal/sse/dto.go`, extracted `ToRetrievalSnapshotDTO` without adding variant fields (preserving the exact 10-key contract).
   - In `gateway/internal/sse/sse.go`, unconditionally attached `partial_snapshot` before the `final_response`/`notices` branch, and forwarded `graph_prompt_fact_count` in `metadata`.
   - Updated `gateway/main_test.go` payload key set expectations for `workflow_completed` and metadata.
4. **Test Coverage & Phase Invariants Closure:**
   - Added `workflow_phase5_retrieve_timeout_emits_partial_snapshot_with_provenance` asserting all 7 audit discriminators.
   - Added `workflow_phase5_graph_facts_reaching_prompt_are_counted` covering Cases A, B (graph_weight 0.0), and C (retrieval failure).
   - Added Case D span attribute assertion in `telemetry_query.rs`.
   - Added `TestWorkflowCompletedCarriesPartialSnapshotAndGraphInfluence` in `gateway/internal/sse/sse_test.go`.
   - Updated `scripts/engine-test-targets.sh` (TOTAL 498, lib 458) and `scripts/gateway-test-targets.sh` (EXPECTED_TOTAL 114, gateway 79, internal/sse 12). Both invariant gates pass cleanly.

## Verification
- `cargo test --manifest-path engine/Cargo.toml partial_snapshot_with_provenance`: PASS
- `cargo test --manifest-path engine/Cargo.toml graph_facts_reaching_prompt`: PASS
- `cargo test --manifest-path engine/Cargo.toml test_workflow_span_carries_workflow_metadata_attributes`: PASS
- `cargo clippy --manifest-path engine/Cargo.toml --lib --bins -- -D warnings`: PASS
- `cd gateway && go test ./... -count=1`: PASS
- `cd gateway && go vet ./...`: PASS
- `bash scripts/engine-test-targets.sh`: PASS (498 total)
- `bash scripts/gateway-test-targets.sh`: PASS (114 total)
