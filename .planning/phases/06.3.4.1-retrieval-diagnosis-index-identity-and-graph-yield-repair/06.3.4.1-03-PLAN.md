---
phase: 06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/src/bin/retrieval_soak.rs
  - engine/src/workflow/nodes/retrieve.rs
  - engine/src/service.rs
  - eval/src/lancet_eval/flatness.py
  - eval/tests/test_flatness.py
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-OI02-PROFILE.md
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/soak/
autonomous: false
requirements: [OBS-05]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The OI-02 accumulator is named from measurement before any fix is written: a read-only, API-free soak bin reproduces RetrieveHybrid growth on at least one arm and stays flat on another, and the profile names which accumulator class explains the difference (D-64)."
    - "The soak mirrors the production process: `#[tokio::main]` multi-thread runtime and `engine::telemetry::init(&settings.engine.telemetry)` exactly as `main.rs`. It includes one whole-workflow arm that runs the production node sequence with only the embedding and generation provider ports replaced, and it records whether the OTel collector was reachable and the build profile (release)."
    - "Engine-level profiling finds the cause (D-65). The flatness proof itself is not claimed here; it runs later on the harness → gateway → engine path in paid drive 1 (06.3.4.1-12)."
    - "Per-iteration evidence includes RetrieveHybrid sub-stage durations (open_table, checkout, dense KNN, BM25, fusion), tokio `num_alive_tasks` and `global_queue_depth`, plus externally sampled working set, private bytes, handle count and thread count."
    - "The flatness adapter feeds soak iterations and, later, drive journals to `decay.analyze_decay(records, COMMITTED_THRESHOLDS, restart_ordinal=None, node_name=\"RetrieveHybrid\")` with gap-free ordinals in line order. A censored set reads as unavailable, never as flat; `COMMITTED_THRESHOLDS` and `decay.py` are not modified (D-64)."
    - "A restart workaround is not an outcome of this plan: the checkpoint offers a named fix, extended profiling, or (only on allocator evidence) mimalloc, and never a restart or a raised budget (D-64)."
  artifacts:
    - path: "engine/src/bin/retrieval_soak.rs"
      provides: "OI-02 P0 profiler, 5 arms, JSONL per iteration"
    - path: "eval/src/lancet_eval/flatness.py"
      provides: "soak and journal adapters to analyze_decay, FlatnessResult"
      exports: ["FlatnessResult", "records_from_soak_jsonl", "records_from_run_journal", "flatness_verdict", "slice_medians"]
    - path: ".planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-OI02-PROFILE.md"
      provides: "per-arm verdicts, discriminator readings, named accumulator class"
  key_links:
    - from: "eval/src/lancet_eval/flatness.py"
      to: "eval/src/lancet_eval/decay.py"
      via: "analyze_decay with COMMITTED_THRESHOLDS, unmodified"
      pattern: "analyze_decay"
    - from: "engine/src/bin/retrieval_soak.rs"
      to: "engine/src/service.rs"
      via: "ProductionDenseRetrievalPort / ProductionBm25RetrievalPort / attempt_graph_augmentation, exactly as build_production_workflow builds them"
      pattern: "ProductionDenseRetrievalPort"
  prohibitions:
    - "The soak must not call any provider API; query vectors come from stored `embedding` and `name_vector` rows"
    - "The soak must not write to the eval store; it opens it only through DatabaseManager::open_and_validate"
    - "decay.py and COMMITTED_THRESHOLDS must not be edited to make a verdict pass"
---

<objective>
Find the in-process resource that makes RetrieveHybrid (and AssemblePrompt, and the embedding call)
slow down linearly within one engine process (OI-02, layer 3), before touching any fix (D-64). The
evidence so far rules out store growth and points at process state. 06.3.3's segment 2 stayed flat for
160 records, so a flat process is reproducible, and the profiler must reproduce both conditions.

Output: a read-only soak bin, P1 per-query sub-stage tracing in RetrieveHybrid, a flatness adapter
reused by later gates, the soak evidence and a profile document. A blocking decision then confirms the
named cause and the fix direction.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-CONTEXT.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RESEARCH.md
@.planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-MEASUREMENT.md
@.planning/phases/06.3.4-corrected-re-drive-calibration-and-root-cause-documentation/06.3.4-OBSERVATION-THROUGHPUT-PLATEAU.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: retrieval_soak bin and P1 RetrieveHybrid sub-stage tracing</name>
  <files>engine/src/bin/retrieval_soak.rs, engine/src/workflow/nodes/retrieve.rs, engine/src/service.rs</files>
  <read_first>
    - `rust-guidelines.md` (M-LOG-STRUCTURED, M-LOG-NOT-PRINT with the inspect-bin JSON-stdout exception, M-YIELD-POINTS)
    - `engine/src/main.rs:20-160` (runtime, `engine::telemetry::init`, snapshot build, reranker, generator construction)
    - `engine/src/service.rs:70-163` (`build_production_workflow`), `:227-345` (`GraphAugmentationOutcome`, `attempt_graph_augmentation`), `:531-649` (`ProductionDenseRetrievalPort`, `ProductionBm25RetrievalPort`)
    - `engine/src/workflow/nodes/retrieve.rs:66-334`, `engine/src/workflow/ports.rs` (port traits for embedding/generation), `engine/src/workflow/runner.rs:380-400` (node timeout)
    - `engine/src/bin/inspect_lancedb.rs:779-823` (bin skeleton), RESEARCH §C (arms, discriminators, counters) and §Code Examples "Stable tokio counters"
  </read_first>
  <action>
    Create `engine/src/bin/retrieval_soak.rs` under `#[tokio::main]` (the same runtime flavour as `main.rs`). Call `engine::telemetry::init(&settings.engine.telemetry)` exactly as `main.rs` does, loading settings the same way with `LANCET_ENV=eval`. Open the store via `DatabaseManager::open_and_validate`, build the startup snapshot as `main.rs` does, and add no `#[cfg(test)]` module (the engine test-count gate is unchanged by this bin).

    Flags:
    - `--arm <R|RP|RG|RGT|W>`
    - `--iterations <n>` (default 300)
    - `--graph-timeout-ms <n>` (RGT only, default 200)
    - `--lancedb-path`

    Query vectors come from the stored `nodes.embedding` of a fixed, seeded set of 300 chunk rows (and `entities.name_vector` for graph arms). Record the seed.

    Arms:
    - **R**: `RetrieveHybridNode::execute` with the production dense and BM25 ports built exactly as `build_production_workflow` builds them.
    - **RP**: R plus `prompt::pack_evidence_and_graph_prompt` over the retrieved evidence.
    - **RG**: RP plus `service::attempt_graph_augmentation` with no timeout (the 06.3.3 segment-2 condition).
    - **RGT**: RP plus graph augmentation wrapped in `tokio::time::timeout(graph_timeout)`, dropping the future on expiry (the segment-1 / 06.3.4 condition).
    - **W**: the full production node sequence ReformulateQuery → ExtractGraphContext → RetrieveHybrid → AssemblePrompt → GenerateAnswer through the production runner, with production retrieval, BM25 and graph ports. Only the embedding port (returns the stored vector) and the generation port (returns one fixed valid JSON `ModelOutput` with an `[1]` citation) are replaced by deterministic in-bin types implementing the `workflow/ports.rs` traits.

    If the production port structs' fields are not reachable from a bin, add `pub fn new(...)` constructors in `service.rs` without changing behaviour.

    Print one JSON line per iteration: `arm, ordinal, retrieve_ms, open_table_ms, checkout_ms, dense_ms, bm25_ms, fusion_ms, pack_ms, graph_ms, graph_timed_out, alive_tasks, global_queue_depth, rss_hint`. Print a header line first: `build_profile` (from `cfg!(debug_assertions)`), `binary_path`, `binary_mtime`, `collector_endpoint`, `collector_reachable` (a 500 ms TCP connect to the configured OTLP endpoint), `seed`, `pid`.

    In `retrieve.rs`, add P1 production instrumentation: one `tracing::info!` per query with fields `open_table_ms, checkout_ms, dense_ms, bm25_ms, fusion_ms, alive_tasks = Handle::current().metrics().num_alive_tasks(), global_queue = ...global_queue_depth()` and message `retrieve_hybrid_substages`. It must not change the node's result or timing contract, and it must not log query text or chunk content.
  </action>
  <verify>
    <automated>cargo build --manifest-path engine/Cargo.toml --release --bin retrieval_soak && cargo run --manifest-path engine/Cargo.toml --release --quiet --bin retrieval_soak -- --arm R --iterations 5 --lancedb-path ./data/lancedb-eval | tail -n 5 | uv run --project eval python -c "import sys, json; rows = [json.loads(l) for l in sys.stdin if l.strip()]; assert len(rows) == 5 and all('retrieve_ms' in r and 'alive_tasks' in r for r in rows), rows"</automated>
    <fails_when>non-zero exit; AssertionError printed; fewer than 5 iteration lines</fails_when>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked && sh scripts/engine-test-targets.sh</automated>
    <fails_when>non-zero exit, or stderr contains "FAIL:"</fails_when>
  </verify>
  <acceptance_criteria>
    - The soak header line reports `build_profile: "release"` and a boolean `collector_reachable`.
    - All five arms start and emit iteration lines on a 5-iteration smoke run.
    - `scripts/engine-test-targets.sh` passes with unchanged counts (the soak bin has no tests; the P1 change adds none).
    - The engine lib test suite passes unchanged.
  </acceptance_criteria>
  <done>A production-faithful, read-only, API-free soak can reproduce both the flat and the growing process conditions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flatness adapter, soak runs and the OI-02 profile</name>
  <files>eval/src/lancet_eval/flatness.py, eval/tests/test_flatness.py, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-OI02-PROFILE.md, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/soak/</files>
  <read_first>
    - `eval/src/lancet_eval/decay.py:104-139` (`validate_and_sort_records`: gap-free ordinals, segment), `:142-306` (both prongs, censoring on `error_kind == 1`), `:426-531` (`analyze_decay`)
    - `eval/src/lancet_eval/thresholds.py:90-114` (`COMMITTED_THRESHOLDS`)
    - `eval/src/lancet_eval/measure.py:62-68` (`MeasurementRecord` shape), `eval/src/lancet_eval/journal.py:45-73` (`RunRecord`, `node_timings`, `node_failures`, `error_type`)
    - RESEARCH §C "Profiling approach" and Pitfalls 6-7
  </read_first>
  <behavior>
    - `records_from_soak_jsonl` assigns ordinals 1..n in line order with `segment="segment-1"` and a `RetrieveHybrid` NodeTiming per line; a line with `graph_timed_out` does not censor RetrieveHybrid.
    - `records_from_run_journal` assigns ordinals in journal line order (the header is skipped), and maps a RetrieveHybrid node failure with `error_kind == 1` to a censored observation. A record whose `error_type` is a harness deadline or read timeout also counts as a censored RetrieveHybrid observation (conservative: it makes the verdict unavailable rather than hiding).
    - `flatness_verdict` returns `passed=True` only when both prongs are available and not fired; a censored set gives `passed=False` with `reason="unavailable"`; an empty input gives `passed=False` with `reason="n=0"`.
    - `slice_medians(records, size=50)` returns per-50 medians in ordinal order.
  </behavior>
  <action>
    Create `eval/src/lancet_eval/flatness.py`:
    - `FlatnessResult` (frozen dataclass): `passed`, `reason`, `trend_available`, `window_available`, `decay_present`, `slope_ms_per_query`, `window_delta_ms`, `censored_count`, `unusable_dropped_count`, `slice_medians_ms`, `n`.
    - `records_from_soak_jsonl(path, arm)`.
    - `records_from_run_journal(journal_path)`.
    - `flatness_verdict(records)`, calling `decay.analyze_decay(records, COMMITTED_THRESHOLDS, restart_ordinal=None, node_name="RetrieveHybrid")`.
    - `slice_medians`.

    Build the adapter record objects as lightweight models carrying exactly the attributes `validate_and_sort_records` reads. Do not edit `decay.py` or `thresholds.py`. Write tests first.

    Then run the soak (engine stopped; release build), 300 iterations per arm, in this order, each in a fresh process: R, RP, RG, RGT, W. Save each arm's stdout to `soak/<arm>.jsonl`. Alongside each run, sample the soak process every 5 s from PowerShell with `Get-Process -Id <pid>` selecting `WorkingSet64`, `PrivateMemorySize64`, `HandleCount` and the thread count into `soak/<arm>-proc.csv`.

    Compute each arm's verdict with `flatness_verdict` and 50-iteration slice medians, and correlate `alive_tasks`, `global_queue_depth`, working set and threads against ordinal (Theil-Sen slope via the same helper).

    Write `06.3.4.1-OI02-PROFILE.md`:
    - a per-arm table: verdict, slope, window delta, slice medians, counter slopes, collector reachable, build profile;
    - which of RESEARCH §C's ranked classes (1 abandoned CPU work outliving cancelled futures, 2 allocation/allocator degradation, 3 lance session cache growth, 4 per-query open_table/checkout state, 5 checkpoint/gateway backpressure), or a telemetry/exporter backlog visible only in arm W, the evidence supports;
    - for each class, the discriminator reading that supports or rules it out.

    If no arm grows, say so explicitly and list what the soak does not reproduce (for example the gateway or harness side). Do not guess a cause.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_flatness.py eval/tests/test_decay.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the output</fails_when>
    <automated>for a in R RP RG RGT W; do test "$(grep -c '"ordinal"' .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/soak/$a.jsonl)" -ge 300 || exit 1; done && grep -q "Named accumulator" .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-OI02-PROFILE.md</automated>
    <fails_when>non-zero exit; any arm file with fewer than 300 iteration lines; the profile lacks a "Named accumulator" section</fails_when>
  </verify>
  <acceptance_criteria>
    - Five soak JSONL files with at least 300 iterations each, and five process CSVs, exist under `soak/`.
    - `06.3.4.1-OI02-PROFILE.md` has a per-arm verdict table and a "Named accumulator" section that either names one class with its discriminator evidence or states `inconclusive` with what was not reproduced.
    - `git diff --quiet --exit-code -- eval/src/lancet_eval/decay.py eval/src/lancet_eval/thresholds.py` exits 0.
  </acceptance_criteria>
  <done>The growth is reproduced (or its absence documented) and attributed to a class by measurement.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Confirm the OI-02 root cause and the fix direction (D-64)</name>
  <decision>Which OI-02 fix does 06.3.4.1-07 implement, given the profile?</decision>
  <context>
    D-64 forbids a restart workaround and requires a root-cause fix whose flatness is then proved over
    the whole of paid drive 1. Present the per-arm table and the "Named accumulator" section of
    `06.3.4.1-OI02-PROFILE.md`. Remind the user that drive 1 still runs the old graph seeding
    (RESEARCH Pitfall 11), so the fix must hold with it. mimalloc is a new crate that breaks the AI-SPEC
    §2 no-new-dependency lock (package audit verdict OK, RESEARCH Package Legitimacy Audit); it is only
    on the table if the profile shows allocator/working-set growth that removing allocations does not
    explain.
  </context>
  <options>
    <option id="fix-named-class">
      <name>Implement the fix for the named class (06.3.4.1-07's branch for that class)</name>
      <pros>Direct D-64 compliance; 07's soak re-run proves the fix on the arm that reproduced growth</pros>
      <cons>Only valid if the profile is conclusive</cons>
    </option>
    <option id="extend-profiling">
      <name>Inconclusive: extend profiling before any fix</name>
      <pros>Avoids fixing the wrong thing (RESEARCH A4)</pros>
      <cons>More free soak time; the user names which extra arm or counter to add (for example gateway-in-the-loop, or longer iterations)</cons>
    </option>
    <option id="mimalloc">
      <name>Allocator pressure confirmed and allocation removal insufficient: adopt mimalloc (M-MIMALLOC-APPS)</name>
      <pros>Project Rust guideline recommends it for applications</pros>
      <cons>New dependency against the AI-SPEC §2 lock; needs this explicit approval and a Cargo.lock change</cons>
    </option>
  </options>
  <resume-signal>Reply with fix-named-class (and the class number), extend-profiling (with the extra arm or counter), or mimalloc.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| soak bin → eval store | read-only access to the runtime store |
| engine logs | new per-query tracing fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.4.1-03-01 | Tampering | retrieval_soak store access | medium | mitigate | Opens only via `DatabaseManager::open_and_validate`, issues only queries; no `add`/`delete`/`optimize` call exists in the bin |
| T-06.3.4.1-03-02 | Information disclosure | P1 tracing in retrieve.rs | low | mitigate | Logs durations and counters only; no query text, chunk content or API key (M-LOG-STRUCTURED) |
| T-06.3.4.1-03-03 | Denial of service (financial) | soak | low | mitigate | No provider call: embedding and generation ports are in-process deterministic stand-ins |
| T-06.3.4.1-03-04 | Repudiation | flatness verdict | high | mitigate | Adapter is a thin shim onto unmodified `analyze_decay`; censored sets read unavailable; `git diff` guard on decay.py/thresholds.py |
</threat_model>

<verification>
- `cargo test --manifest-path engine/Cargo.toml --locked`, `sh scripts/engine-test-targets.sh`, `uv run --project eval pytest -q` exit 0.
</verification>

<success_criteria>
The accumulator class is named by measurement (or honestly inconclusive), and the user has chosen the
fix direction before any engine fix is written.
</success_criteria>

## Artifacts this phase produces

- Bin `retrieval_soak` (flags `--arm R|RP|RG|RGT|W`, `--iterations`, `--graph-timeout-ms`, `--lancedb-path`), in-bin deterministic embedding/generation ports.
- `tracing::info!` event `retrieve_hybrid_substages` in `retrieve.rs` (fields listed in Task 1).
- Possibly `pub fn new` constructors on `ProductionDenseRetrievalPort` / `ProductionBm25RetrievalPort` in `service.rs`.
- Module `lancet_eval.flatness`: `FlatnessResult`, `records_from_soak_jsonl`, `records_from_run_journal`, `flatness_verdict`, `slice_medians`.
- Files: `06.3.4.1-OI02-PROFILE.md`, `soak/{R,RP,RG,RGT,W}.jsonl`, `soak/*-proc.csv`.

<output>
Create `.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-03-SUMMARY.md` when done.
</output>
