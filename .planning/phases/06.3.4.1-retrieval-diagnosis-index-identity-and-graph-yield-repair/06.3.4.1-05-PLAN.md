---
phase: 06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair
plan: 05
type: execute
wave: 2
depends_on: [06.3.4.1-01, 06.3.4.1-03]
files_modified:
  - eval/src/lancet_eval/score.py
  - eval/src/lancet_eval/dimensions.py
  - eval/src/lancet_eval/report.py
  - eval/report.schema.json
  - eval/tests/test_schema.py
  - eval/tests/test_score_deterministic.py
  - eval/src/lancet_eval/thresholds.py
  - eval/src/lancet_eval/unpark_gates.py
  - eval/tests/test_unpark_gates.py
  - eval/tests/test_gate.py
autonomous: true
requirements: [OBS-05]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Scored reports carry `final_answer_em`, `final_answer_containment`, `answer_usable`, `final_answer_missing_rate` and `null_abstention_correctness` next to the unchanged `answer_exact_match`/`answer_f1`; SC-3 is judged on `answer_usable` (D-70)."
    - "`null_abstention_correctness` is computed on null_query records only, and null records never enter `answer_usable`'s denominator (D-72)."
    - "`final_answer_missing` records stay in every answer denominator (a model output, 06.3.1 D-34); the D-34 infrastructure exclusions are unchanged."
    - "The D-69 companion tripwire (0.159), the null-query rejection baseline (29/42) and the SC-2 reading (timeout must not be the plurality error class; a tie counts as dominant) are committed as literals in `thresholds.py` before paid drive 1 (D-69, D-73 pattern)."
    - "The SC-3 evaluator reads `VECTOR_BASELINE_USABLE_FLOOR` from `thresholds.py` and returns MISS with reason `floor not committed` when the constant is absent; it never supplies a default (D-73)."
    - "Gate readings are computed, not judged: `python -m lancet_eval.unpark_gates` prints PASS or MISS for SC-1, SC-2 (error mode plus D-64 flatness via `flatness_verdict`) and SC-3 against the committed literals, and the D-84 checkpoint presents that output (D-84)."
    - "The SC-1 evaluator checks that the run's header `partial` equals `not completeness_comparison(...)` and that `report.json` was written by the unmodified fail-closed path; it adds no flag to `score_run` or `report` (D-87a)."
    - "An empty population returns MISS with reason `n=0`, never PASS (specless edge: empty)."
  artifacts:
    - path: "eval/src/lancet_eval/unpark_gates.py"
      provides: "SC-1/SC-2/SC-3 evaluators and D-69 companion rate"
      exports: ["GateReading", "evaluate_sc1", "evaluate_sc2", "evaluate_sc3", "citation_rejection_rate", "main"]
    - path: "eval/src/lancet_eval/thresholds.py"
      provides: "CITATION_REJECTION_TRIPWIRE, CITATION_REJECTION_NULL_BASELINE, SC2_TIMEOUT_DOMINANCE_RULE, FINAL_ANSWER_MISSING_REVIEW_RATE"
      contains: "CITATION_REJECTION_TRIPWIRE"
    - path: "eval/report.schema.json"
      provides: "schema carrying the five new dimensions"
  key_links:
    - from: "eval/src/lancet_eval/score.py"
      to: "eval/src/lancet_eval/metrics.py"
      via: "per-arm lists built with _build_mean_dim beside answer_exact_match"
      pattern: "final_answer_em"
    - from: "eval/src/lancet_eval/unpark_gates.py"
      to: "eval/src/lancet_eval/flatness.py"
      via: "evaluate_sc2 calls records_from_run_journal + flatness_verdict"
      pattern: "flatness_verdict"
  prohibitions:
    - "No new flag, keyword or bypass may be added to score_run or report; the SC-1 evaluator only reads their outputs"
    - "The evaluator must not default a missing threshold to any number"
---

<objective>
Put the new answer metrics into the fail-closed scored report, commit the drive-1 companion literals,
and make every SC-1..SC-3 gate reading a computed PASS/MISS rather than a judgement.

Purpose: drive 1 gates SC-2 and SC-3 (D-85), and D-84 halts before graph work on an SC-3 miss. The
checkpoint that decides that must present a computed reading against literals committed before the
data existed (D-73).

Output: five new dimensions in score/report/schema, the committed literals, and `unpark_gates.py`.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-CONTEXT.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RESEARCH.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-AI-SPEC.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Five answer dimensions in score, report and schema</name>
  <files>eval/src/lancet_eval/score.py, eval/src/lancet_eval/dimensions.py, eval/src/lancet_eval/report.py, eval/report.schema.json, eval/tests/test_schema.py, eval/tests/test_score_deterministic.py</files>
  <read_first>
    - `eval/src/lancet_eval/score.py:244-274` (guards: do not touch), `:292-375` (per-arm list collection), `:1096-1119` (`_build_mean_dim` for `answer_exact_match`/`answer_f1`), `:1457-1545` (fail-closed path: do not touch)
    - `eval/src/lancet_eval/dimensions.py:64+` (`REGISTERED_DIMENSIONS`), `eval/src/lancet_eval/report.py:215` (`emit_schema`)
    - `eval/src/lancet_eval/usability.py` (D-34 predicate), `eval/src/lancet_eval/metrics.py` (06.3.4.1-01 functions)
    - `eval/tests/test_schema.py`, `eval/tests/test_score_deterministic.py`
  </read_first>
  <behavior>
    - A fixture journal with graph-off answers `...\nAnswer: Yes` (gold Yes), `...\nAnswer: No` (gold Yes) and a missing line (gold Yes) gives `answer_usable` 1/3 and `final_answer_missing_rate` 1/3 for that arm.
    - A null question with `Answer: Insufficient information.` gives `null_abstention_correctness` 1.0 and is absent from `answer_usable`'s n.
    - `answer_exact_match` and `answer_f1` values on the existing deterministic fixtures are unchanged.
    - The regenerated `report.schema.json` validates a report containing the five dimensions, and `test_schema.py` pins the schema.
  </behavior>
  <action>
    In `score.py`'s per-arm collection loop, add parallel lists:
    - `final_answer_em` (`metrics.final_answer_em`);
    - `final_answer_containment` (`gold_contained` on the full answer);
    - `answer_usable` (`metrics.answer_usable`, as 1.0/0.0);
    - `final_answer_missing` (1.0 when `extract_final_answer` is None);
    - `null_abstention_correctness` (`metrics.null_abstention_correct`, null questions only).

    The first four are computed for non-null questions only. Build each with `_build_mean_dim` right after `answer_f1`. Use the same D-34 usable-record predicate as the existing answer dimensions, and keep `final_answer_missing` records inside the denominators. Register the five names in `REGISTERED_DIMENSIONS` with one-line descriptions citing D-70/D-72. Regenerate `eval/report.schema.json` via `emit_schema` and update `test_schema.py`.

    Do not edit the guard block (244-274) or the fail-closed block (1457-1545).
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_score_calibration_agreement.py eval/tests/test_score_judged.py eval/tests/test_schema.py eval/tests/test_report.py eval/tests/test_dimensions.py eval/tests/test_score_deterministic.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - The five dimension names appear in `REGISTERED_DIMENSIONS` and in `eval/report.schema.json`.
    - `git diff` of `score.py` touches neither the `score_run` guard block nor the `effective_partial` block (reviewed by line range in the summary).
    - The full `uv run --project eval pytest -q` passes.
  </acceptance_criteria>
  <done>The fail-closed report carries the D-70/D-72 metrics next to the unchanged legacy ones.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drive-1 literals and computed SC-1/SC-2/SC-3 gate readings</name>
  <files>eval/src/lancet_eval/thresholds.py, eval/src/lancet_eval/unpark_gates.py, eval/tests/test_unpark_gates.py, eval/tests/test_gate.py</files>
  <read_first>
    - `eval/src/lancet_eval/thresholds.py:90-114` (the committed-constant pattern with provenance comment)
    - `eval/src/lancet_eval/diagnostic.py` (`build_rows`, `classify_record`), `eval/src/lancet_eval/flatness.py` (06.3.4.1-03)
    - `eval/src/lancet_eval/journal.py:144-239` (`reconcile_header`, `completeness_comparison`), `eval/src/lancet_eval/stats.py` (`wilson_ci`), `eval/src/lancet_eval/usability.py`
    - AI-SPEC §5 dimensions #1-#8 (rubrics and pass rules) and the SC-3 floor note
  </read_first>
  <behavior>
    - `evaluate_sc2`: errors {timeout: 5, citation_basis_mixed: 5} gives MISS (a tie counts as dominant); {timeout: 3, transport: 5} gives PASS for the error-mode clause; a censored flatness set gives MISS with reason `flatness unavailable`; the overall SC-2 reading is PASS only when both clauses pass.
    - `citation_rejection_rate`: 1 `citation_marker_mismatch` gives MISS; total 20/100 (> 0.159) gives MISS; null rejections 30/40 (> 29/42) gives MISS; otherwise PASS. Null and non-null are reported separately.
    - `evaluate_sc3`: with `VECTOR_BASELINE_USABLE_FLOOR` absent (monkeypatched away) gives MISS `floor not committed`; with floor 0.47 and 40/80 usable gives PASS; n=0 gives MISS `n=0`. Output carries a Wilson CI, strata by `question_type` and binary-vs-entity gold each with its constant-Yes baseline, and the count of GenerateAnswer failures D-34 excluded.
    - `evaluate_sc1`: header `partial: false` with complete records and an existing `report.json` gives PASS; header `partial: false` with a missing unit gives MISS; no `report.json` gives MISS.
  </behavior>
  <action>
    Append to `thresholds.py`, each with a provenance comment `# 06.3.4.1 D-69/D-73: committed before paid drive 1 (AI-SPEC §5 #2/#4/#6).`:
    - `CITATION_REJECTION_TRIPWIRE: float = 0.159`
    - `CITATION_REJECTION_NULL_BASELINE: tuple[int, int] = (29, 42)`
    - `SC2_TIMEOUT_DOMINANCE_RULE: str = "plurality_tie_is_dominant"`
    - `FINAL_ANSWER_MISSING_REVIEW_RATE: float = 0.10`

    Do not add `VECTOR_BASELINE_USABLE_FLOOR` here (06.3.4.1-10 commits it after G is fixed). Do not touch `COMMITTED_THRESHOLDS` or `GRAPH_YIELD_INVESTIGATION_FLOOR`.

    Create `eval/src/lancet_eval/unpark_gates.py` with:
    - `GateReading` (frozen dataclass: `gate`, `status` "PASS"/"MISS", `reason`, `value`, `ci`, `n`, `detail`);
    - `evaluate_sc1(run_dir)`;
    - `evaluate_sc2(journal_path, engine_pid_before, engine_pid_after)`: a PID change is MISS `engine restarted`; uses `diagnostic.classify_record` and `flatness.flatness_verdict(flatness.records_from_run_journal(...))`, and prints the both-arm-error question count and class pairs;
    - `citation_rejection_rate(journal_path, questions)`: denominator = records with a GenerateAnswer node timing or failure;
    - `evaluate_sc3(rows, populations_path)`: G read from the `g_question_ids` list in the committed selection file; the floor read with `getattr(thresholds, "VECTOR_BASELINE_USABLE_FLOOR", None)`;
    - `main(argv)` for `python -m lancet_eval.unpark_gates --stage drive1 --run <dir> --gold-chunks <file> --populations <diag_selection.json> --engine-pid-before N --engine-pid-after N --out <md>`, which writes a markdown gate table plus JSON and exits 0 whatever the verdict (the verdict is data, not an error).

    Write the tests first. Add constant-existence and type tests for the four literals to `test_gate.py`.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_unpark_gates.py eval/tests/test_gate.py eval/tests/test_stage_cap.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the output</fails_when>
    <automated>uv run --project eval pytest -q && uv run --project eval ruff check --preview eval/src eval/tests</automated>
    <fails_when>non-zero exit; a "failed" count above 0; any ruff error line</fails_when>
  </verify>
  <acceptance_criteria>
    - `thresholds.py` has the four new literals with provenance comments and no `VECTOR_BASELINE_USABLE_FLOOR`.
    - `test_unpark_gates.py` covers every behaviour bullet (at least 12 tests).
    - `git diff --quiet --exit-code -- eval/src/lancet_eval/decay.py` exits 0 (the committed decay criterion is unchanged).
  </acceptance_criteria>
  <done>Every drive-1 gate reading is a computed PASS/MISS against literals committed before the drive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| journal → scorer/evaluators | model output and wire data become published numbers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.4.1-05-01 | Repudiation | fail-closed report | high | mitigate | No edit to `score_run` guards or `effective_partial`; SC-1 evaluator only reads header, `completeness_comparison` and `report.json` |
| T-06.3.4.1-05-02 | Tampering | threshold literals | high | mitigate | Literals committed before drive 1 with provenance; SC-3 floor absent gives MISS, never a default; tests pin values |
| T-06.3.4.1-05-03 | Repudiation | timeout relabelling | medium | mitigate | `classify_record` maps harness deadlines to `timeout`; tie at plurality counts as dominant; tests pin both |
</threat_model>

<verification>
- `uv run --project eval pytest -q && uv run --project eval ruff check --preview eval/src eval/tests` exit 0.
</verification>

<success_criteria>
Reports carry the new answer dimensions; drive-1 gates print computed PASS/MISS against committed
literals.
</success_criteria>

## Artifacts this phase produces

- Dimensions: `final_answer_em`, `final_answer_containment`, `answer_usable`, `final_answer_missing_rate`, `null_abstention_correctness` (score.py, dimensions.py, report.schema.json).
- `thresholds.py`: `CITATION_REJECTION_TRIPWIRE`, `CITATION_REJECTION_NULL_BASELINE`, `SC2_TIMEOUT_DOMINANCE_RULE`, `FINAL_ANSWER_MISSING_REVIEW_RATE`.
- Module `lancet_eval.unpark_gates`: `GateReading`, `evaluate_sc1`, `evaluate_sc2`, `citation_rejection_rate`, `evaluate_sc3`, `main` (`python -m lancet_eval.unpark_gates --stage drive1 ...`).

<output>
Create `.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-05-SUMMARY.md` when done.
</output>
