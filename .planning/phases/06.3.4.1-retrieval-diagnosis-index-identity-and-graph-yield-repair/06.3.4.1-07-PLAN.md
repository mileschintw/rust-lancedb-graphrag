---
phase: 06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair
plan: 07
type: execute
wave: 4
depends_on: [06.3.4.1-03, 06.3.4.1-06]
files_modified:
  - engine/src/workflow/nodes/graph_context.rs
  - engine/src/workflow/runner.rs
  - engine/src/service.rs
  - engine/src/db/mod.rs
  - engine/src/telemetry/mod.rs
  - engine/src/tests.rs
  - engine/src/workflow/ports.rs
  - engine/src/prompt.rs
  - engine/src/main.rs
  - engine/Cargo.toml
  - engine/Cargo.lock
  - scripts/engine-test-targets.sh
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-OI02-PROFILE.md
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/soak/post-fix/
autonomous: true
requirements: [OBS-05]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The OI-02 fix removes the accumulating resource named in `06.3.4.1-OI02-PROFILE.md` and chosen at 06.3.4.1-03's checkpoint; it is not a restart, a raised budget or a retry change (D-64)."
    - "The fix holds on the pre-OI-01 graph path, because paid drive 1 still runs the old seeding (RESEARCH Pitfall 11)."
    - "The regression proof re-runs the soak arm that reproduced growth, plus arm W, on the fixed build: both read `passed=True` under `flatness_verdict` with both prongs available, and the slice-median table is recorded next to the pre-fix one (D-64 engine-level evidence; the harness-path proof is drive 1, D-65)."
    - "The regression proof excludes changes the profile did not implicate: the tiktoken singleton lands separately in 06.3.4.1-08 unless the profile names allocation class 2, so the proof shows the named cause was the one fixed (D-64)."
    - "Every Rust test added updates `scripts/engine-test-targets.sh` in the same commit, and no value is lowered."
  artifacts:
    - path: ".planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-OI02-PROFILE.md"
      provides: "Fix section: class, change, regression test, post-fix soak verdicts"
    - path: ".planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/soak/post-fix/"
      provides: "post-fix soak JSONL for the reproducing arm and arm W"
  key_links:
    - from: "06.3.4.1-OI02-PROFILE.md Named accumulator"
      to: "the fix branch implemented"
      via: "06.3.4.1-03 Task 3 decision"
      pattern: "Named accumulator"
  prohibitions:
    - "No engine restart, budget increase, retry change or LANCET_ENGINE__WORKFLOW__* override may be offered as the OI-02 fix"
    - "decay.py and COMMITTED_THRESHOLDS must not change"
---

<objective>
Fix the OI-02 accumulator at its root (D-64), in the engine, for the class the profile named and the
user confirmed at 06.3.4.1-03's checkpoint. Then prove on the same soak arms that growth is gone.

Purpose: layer 3. Drive 1 can only show flatness over ~200 records (D-64 as amended) if the
accumulation is gone at the source. The harness-path flatness proof is drive 1 itself (D-65); this plan
supplies the engine-level evidence first so a paid drive is not spent on an unfixed engine.

Output: the fix with a regression test, and post-fix soak evidence appended to the profile.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-OI02-PROFILE.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-03-SUMMARY.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RESEARCH.md
@.planning/phases/06.1-index-rebuild-and-swap-bu-deterministic-proofs-cr-04-cr-05-d/06.1-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Root-cause fix for the named accumulator class, with a regression test</name>
  <precondition>06.3.4.1-03 Task 3 resolved as fix-named-class (or mimalloc), and `06.3.4.1-OI02-PROFILE.md` "Named accumulator" names the class.</precondition>
  <files>engine/src/workflow/nodes/graph_context.rs, engine/src/workflow/runner.rs, engine/src/service.rs, engine/src/db/mod.rs, engine/src/telemetry/mod.rs, engine/src/tests.rs, scripts/engine-test-targets.sh (plus engine/src/workflow/ports.rs and engine/src/prompt.rs on the class 2/4 branch, and engine/Cargo.toml, engine/Cargo.lock, engine/src/main.rs on the mimalloc branch; each branch touches a subset)</files>
  <read_first>
    - `rust-guidelines.md` (M-YIELD-POINTS, M-DOCUMENTED-MAGIC; M-MIMALLOC-APPS only if that branch was chosen)
    - `06.3.4.1-OI02-PROFILE.md` (Named accumulator and discriminator readings) and the 06.3.4.1-03 checkpoint resolution in `06.3.4.1-03-SUMMARY.md`
    - `engine/src/workflow/nodes/graph_context.rs:94-190`, `engine/src/workflow/runner.rs:370-400`, `engine/src/service.rs:241-345,531-649`, `engine/src/db/mod.rs:35-62,111-177`, `engine/src/telemetry/mod.rs:355+`
    - `06.1-VERIFICATION.md` (CR-01: `Table::clone()` shares one pinned_version; two `checkout()`s on a shared handle race). Any table-handle caching must never call `checkout` on a shared handle.
    - RESEARCH §C ranked candidates and "Constraint the planner must carry"
  </read_first>
  <behavior>
    - The regression test reproduces the named mechanism in-process and fails before the fix: N timed-out operations leave at most a bounded number of in-flight operations (class 1), or no per-query table open occurs after snapshot construction (class 2/4), or the session cache stays under its bound (class 3), or the span/exporter queue stays bounded (telemetry class).
    - Existing workflow, graph-degradation and GRAPH_TIMEOUT notice tests are unchanged and green.
  </behavior>
  <action>
    Implement only the branch for the confirmed class:
    - **Class 1, abandoned CPU work outliving cancelled futures.** Run graph augmentation in a `tokio::spawn`ed task holding an owned permit from a per-service `tokio::sync::Semaphore` sized 1 (doc-commented with the profile's evidence). The permit is released only when the underlying operation actually completes, not when the `timeout` fires. A timed-out query still emits `GRAPH_TIMEOUT` exactly as today, and the next query's graph operation waits on the permit inside its own timeout instead of stacking CPU work. Apply the same owned-permit rule to the node-timeout path in `runner.rs` if the profile shows RetrieveHybrid work also outliving its timeout.
    - **Class 2, allocation/allocator degradation.** Remove the implicated per-query large allocations. Open and check out the `nodes` table once per `CorpusSnapshot` into a read-only handle held by the snapshot, never re-checked-out (the 06.1 CR-01 constraint). Replace the full-question `name_vector` flat scan only if the profile shows it, since OI-01 replaces it anyway. Include the tiktoken singleton in this branch only.
    - **Class 3, lance session cache growth.** Construct the `DatabaseManager` connection with an explicit `lancedb::Session` whose metadata and index cache capacities are bounded, with the bound and its derivation in a doc comment.
    - **Class 4, per-query `open_table` + `checkout` state.** As in class 2's table-handle change.
    - **Telemetry/exporter backlog (arm W only).** Bound the span exporter queue and batch settings in `telemetry::init`, with a drop policy recorded in a doc comment.
    - **mimalloc branch.** Add `mimalloc` to `engine/Cargo.toml` with `#[global_allocator]` in `main.rs` and the bins, then record the Cargo.lock change and the audit verdict. Only if chosen at the checkpoint.

    Write the regression test first in `engine/src/tests.rs` (or the module's existing test file) and see it fail, then implement. Update `scripts/engine-test-targets.sh` counts in the same commit. Record in the profile's new "Fix" section the class, the files changed and why each change targets the named mechanism.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked && sh scripts/engine-test-targets.sh</automated>
    <fails_when>non-zero exit, or stderr contains "FAIL:"</fails_when>
  </verify>
  <acceptance_criteria>
    - The profile has a "Fix" section naming the class, the diff summary and the regression test name.
    - The regression test fails on the pre-fix commit (recorded in the summary with the failing output line) and passes after.
    - No config budget value changes in this plan: `git diff --quiet --exit-code -- config/` exits 0.
  </acceptance_criteria>
  <done>The named accumulator is removed at its source, with a test that pins the mechanism.</done>
</task>

<task type="auto">
  <name>Task 2: Post-fix soak regression proof on the reproducing arm and arm W</name>
  <files>.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/soak/post-fix/, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-OI02-PROFILE.md</files>
  <read_first>
    - `06.3.4.1-OI02-PROFILE.md` (which arm grew), `eval/src/lancet_eval/flatness.py`
  </read_first>
  <action>
    Build the release engine and bins. With the engine stopped, re-run `retrieval_soak` with `--iterations 300` for the arm(s) that grew pre-fix and for arm W, each in a fresh process, saving to `soak/post-fix/<arm>.jsonl` with the same 5 s `Get-Process` CSV sampling. Compute `flatness_verdict` and 50-iteration slice medians with `lancet_eval.flatness`.

    Append to the profile a before/after table per arm: verdict, slope, window delta, slice medians, `alive_tasks`/queue slope and working-set slope. If any re-run arm still reads `passed=False`, stop: record it as an unfixed OI-02, do not proceed to paid stages, and leave the next step to the user through `/gsd-plan-phase 06.3.4.1 --gaps`. Budget pass A (06.3.4.1-11) derives budgets only from a non-decaying engine (D-66).
  </action>
  <verify>
    <automated>for f in .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/soak/post-fix/*.jsonl; do uv run --project eval python -c "import sys; from pathlib import Path; from lancet_eval.flatness import records_from_soak_jsonl, flatness_verdict; p = Path(sys.argv[1]); r = flatness_verdict(records_from_soak_jsonl(p, p.stem)); print(p.stem, r); sys.exit(0 if r.passed else 1)" "$f" || exit 1; done</automated>
    <fails_when>non-zero exit: any post-fix arm has passed=False (decay present, or a prong unavailable), or no post-fix JSONL exists</fails_when>
  </verify>
  <acceptance_criteria>
    - `soak/post-fix/` holds at least two arm files (the reproducing arm and W) with at least 300 iterations each.
    - Every post-fix arm reads `passed=True`; the profile's before/after table shows the pre-fix growing slope and the post-fix flat one side by side.
  </acceptance_criteria>
  <done>The engine no longer accumulates under the conditions that reproduced OI-02 offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| query path concurrency | per-query work shares process resources |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.4.1-07-01 | Denial of service | abandoned work / unbounded caches | high | mitigate | Class-specific bound (owned permit, bounded session cache, bounded exporter queue, per-snapshot handle) pinned by a failing-first regression test and a flat post-fix soak |
| T-06.3.4.1-07-02 | Tampering | snapshot isolation | high | mitigate | Any cached table handle is created once per `CorpusSnapshot` at its pinned version and never re-checked-out (06.1 CR-01); rebuild creates a new snapshot |
| T-06.3.4.1-07-03 | Tampering | supply chain (mimalloc branch only) | medium | mitigate | Only on explicit checkpoint approval; audit verdict OK recorded in RESEARCH; Cargo.lock change committed and reviewed |
</threat_model>

<verification>
- `cargo test --manifest-path engine/Cargo.toml --locked && (cd gateway && go test ./...)` and `sh scripts/engine-test-targets.sh` exit 0.
- Every `soak/post-fix/*.jsonl` reads `passed=True`.
</verification>

<success_criteria>
The accumulator is fixed at the root, proven flat offline on the arms that reproduced it, with no
budget or restart workaround.
</success_criteria>

## Artifacts this phase produces

- Engine change set for the confirmed class (one of: owned-permit semaphore around graph augmentation / node work; per-snapshot read-only `nodes` handle; bounded `lancedb::Session`; bounded span exporter queue; `mimalloc` global allocator).
- Regression test in `engine/src/tests.rs` (name recorded in the profile).
- Files: `soak/post-fix/<arm>.jsonl`, `soak/post-fix/<arm>-proc.csv`; "Fix" and before/after sections in `06.3.4.1-OI02-PROFILE.md`.

<output>
Create `.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-07-SUMMARY.md` when done.
</output>
