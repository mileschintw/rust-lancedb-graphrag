# 06.3.4.1-04 Task 3: Reconcile Dry-Run Record

**Date:** 2026-09-24
**Store:** `./data/lancedb-eval` (engine stopped throughout), PostgreSQL `lancet_eval` schema (via `docker compose exec db psql`)
**Mode:** dry-run / read-only only. **Nothing was deleted.** This record is the pre-approval evidence 06.3.4.1-06's `checkpoint:decision` will confirm before `reconcile_eval_store --apply` runs (D-58: the reconcile is one-way).

## 1. LanceDB extras (index identity, OI-03)

The reconcile's `extras` computation (union of `document_id` across `documents`, `nodes`, `edges`, `entity_edges`, minus the 346 `document_map.json` `entries` keys) found **exactly 4** extra documents — the same four RESEARCH A1 identified by inference, with **zero difference**:

| Extra `document_id` | PostgreSQL filename (source of title) | Mapped article it duplicates | Duplicate status |
|---|---|---|---|
| `0370301a-0ad7-4267-a337-ba2dda34595f` | `ChatGPT__Everything_you_need_to_know_about_the_AI-powered_ch.txt` | `cc30a4d5-bb47-4331-bd0e-546f962942ea` — "ChatGPT: Everything you need to know about the AI-powered chatbot" | This is the `document_map.json` **alias** (`aliases: {"0370301a...": "cc30a4d5..."}`, D-60) — a historical lookup entry, not an `entries` key, so it is an extra by construction |
| `f9992c53-a97e-412c-b2c9-978dc892c1fe` | `Know_the_price-matching_policies_for_Best_Buy__Target__Walma.txt` | `07217281-cb1f-4278-8b24-ec505d2cc2db` — "Know the price-matching policies for Best Buy, Target, Walmart, and others" | Retry duplicate |
| `fa85cb0e-13f9-4909-908c-0dc39d0d40de` | `Moneyline_Betting_-_What_is_the_Moneyline_and_How_Does_it_Wo.txt` | `ce65feec-5cef-4a2e-9751-2af724aa9b0e` — "Moneyline Betting - What is the Moneyline and How Does it Work?" | Retry duplicate |
| `3aadce3b-564d-41b8-b968-d785e6ff09f8` | `The_people_who_ruined_the_internet.txt` | `8732e3f4-d147-4616-b7b2-a456a9444aed` — "The people who ruined the internet" | Retry duplicate |

**Set equals RESEARCH A1's four exactly** (`0370301a…`, `f9992c53…`, `fa85cb0e…`, `3aadce3b…`) — no difference. RESEARCH A1 identified these by inference from journal leaks ("MEDIUM confidence... Confirm with a store listing before deleting"); this dry-run's `extras` computation, over a live full read of all four document-linked LanceDB tables, IS that store listing, and confirms the set with zero symmetric difference.

Titles above come from the PostgreSQL `filename` column (the gateway's original upload filename, `lancet_eval.documents.filename`), not from a raw_content dump — `inspect_lancedb` has no mode that emits `raw_content` or chunk title text (no existing bin flag for it; RESEARCH's suggested route). The PostgreSQL filename for each extra is byte-for-byte the mapped article's title (spaces→underscores, truncated to Postgres's stored length), which is independent confirmation of the "duplicate" relationship beyond RESEARCH's original inference. Noted as a resolved deviation, not a gap: no code change to `reconcile_eval_store.rs`'s report shape was needed for this task's own acceptance criteria (extras list + counts), and this task's `files_modified` does not include that file.

Cross-check: `reconcile/document_ids_pre.json` (the `--document-ids` probe, independent of the reconcile bin) shows `documents` and `nodes` both containing 350 distinct document_id values (350 = 346 mapped + 4 extras), `edges` also 350, `entity_edges` only 347 (346 mapped + 1 of the 4 extras — the other 3 extras' entity extraction apparently produced entity_edges rows attributed to a document_id that didn't survive as a distinct set member, or those 3 extras' extraction runs produced zero entity_edges; not investigated further, out of this task's scope since it doesn't change the extras/allow computation).

## 2. PostgreSQL extras (D-62, Open Question 5)

`lancet_eval.documents` has **367** rows total; the document map has 346 entries; **21** rows are extras — **matches the expected 21 exactly**, no difference from RESEARCH's stated count.

`uv run --project eval lancet-eval identity pg-delete-extras --corpus multihop_rag` (dry-run, no `--apply`) independently confirms the same 21 IDs. Filenames/status/created_at recorded via `SELECT id, filename, status, created_at FROM lancet_eval.documents ORDER BY id` through `run_psql`, filtered in Python against the dry-run's own 21-ID list (not re-derived by a second `NOT IN (...)` query) — see `reconcile/pg_extras.json`.

| `id` | `filename` | `status` | `created_at` |
|---|---|---|---|
| `02038bd6-acdd-4895-b983-f7cc45ababdd` | `The_restaurant_nearest_Google.txt` | failed | 2026-09-02 10:10:56.722756 |
| `0370301a-0ad7-4267-a337-ba2dda34595f` | `ChatGPT__Everything_you_need_to_know_about_the_AI-powered_ch.txt` | **queued** | 2026-08-31 20:23:59.409318 |
| `12399f25-2f92-4843-9d48-df7dca3871a2` | `These_jellyfish_can_learn_without_brains.txt` | failed | 2026-09-02 11:39:28.127166 |
| `1c5c0745-5331-472c-99a6-9b05b57bd97c` | `Tim_Sweeney_on_Epic_s_victory_royale_over_Google.txt` | failed | 2026-09-02 13:37:34.297789 |
| `1cb7048c-522a-43c5-8535-49d6b853f320` | `Top_25_MLB_offseason_trade_candidates.txt` | failed | 2026-09-02 14:56:44.508675 |
| `213f38a2-239f-4be7-ab28-626296d8bc09` | `Fantasy_Football_WR_PPR_Rankings_Week_13__Who_to_start__best.txt` | failed | 2026-09-01 01:26:01.607681 |
| `32d2c38d-ee13-4f93-a963-6cb6e46835aa` | `Thirty-seven_phases_of_agony_define_Ireland_s_greatest_heart.txt` | failed | 2026-09-02 12:09:48.228063 |
| `3aadce3b-564d-41b8-b968-d785e6ff09f8` | `The_people_who_ruined_the_internet.txt` | **queued** | 2026-09-02 08:49:54.381041 |
| `53b6be50-e36b-4139-ad53-0f9f6b562da4` | `The_obsessive_tormenter_who_made_professors__lives_miserable.txt` | failed | 2026-09-02 08:15:25.749082 |
| `56b160f2-55c0-4084-adb1-a8570aee0160` | `Today_the_FTX_jury_suffered_through_a_code_review.txt` | failed | 2026-09-02 14:16:46.006188 |
| `61a7872e-1f1a-47fa-a972-4906afbc502e` | `Top_25_MLB_offseason_trade_candidates.txt` | failed | 2026-09-02 15:01:08.587659 |
| `7ff91dc7-25aa-4d99-ae48-1a24f9d5cae9` | `There_s_something_going_on_with_AI_startups_in_France.txt` | failed | 2026-09-02 11:12:13.600876 |
| `8d192b83-3727-494b-a767-cc0412ae84fc` | `Top_25_MLB_offseason_trade_candidates.txt` | failed | 2026-09-02 14:50:54.376641 |
| `9423e8e2-8db9-4e82-8841-6688c29e0b7e` | `Suicide_prevention_program_at_Nebraska_children_s_hospital_e.txt` | failed | 2026-09-02 01:28:24.799003 |
| `b1541663-27bb-4a53-a10e-1ff15e7bfb12` | `The_best_video_games_of_the_year_so_far.txt` | failed | 2026-09-02 06:40:12.140806 |
| `be6ee61c-b36f-4179-9cd2-ab07d7f331dd` | `Thursday_Night_Football_FanDuel_Picks__NFL_DFS_lineup_advice.txt` | failed | 2026-09-02 13:07:27.993931 |
| `cd88cd4f-268c-4ae5-96b2-793147271d64` | `The_people_who_ruined_the_internet.txt` | failed | 2026-09-02 09:21:24.959119 |
| `da3f31e4-097b-4deb-85f7-060fb2d61fc1` | `This_horror_flick_boasts_the_best_opening_13_minutes_of_a_fi.txt` | failed | 2026-09-02 12:37:18.878034 |
| `ea999b7e-aff3-4f52-a52f-f6cf2546261f` | `Their_kids_died_after_buying_drugs_on_Snapchat._Now_the_pare.txt` | failed | 2026-09-02 10:42:51.393557 |
| `f9992c53-a97e-412c-b2c9-978dc892c1fe` | `Know_the_price-matching_policies_for_Best_Buy__Target__Walma.txt` | **queued** | 2026-09-01 11:44:08.172379 |
| `fa85cb0e-13f9-4909-908c-0dc39d0d40de` | `Moneyline_Betting_-_What_is_the_Moneyline_and_How_Does_it_Wo.txt` | **queued** | 2026-09-01 16:32:11.04637 |

**Finding:** 17/21 are `status: failed` (a retried POST that never completed ingestion — these rows never reached LanceDB at all). 4/21 are `status: queued`, and these are **exactly** the 4 LanceDB extras from §1 — the retry that eventually succeeded, fully ingested into LanceDB, but whose PostgreSQL row was never advanced past `queued` to `completed`. This is consistent with RESEARCH's hypothesis ("Likely cause is `seed_corpus`'s per-article retry: each attempt POSTs a new gateway document") and adds a new observation: `Top_25_MLB_offseason_trade_candidates.txt` was retried at least 3 times total (`1cb7048c…`, `61a7872e…`, `8d192b83…`, all `failed`), and `The_people_who_ruined_the_internet.txt` was retried at least twice (`cd88cd4f…` failed, `3aadce3b…` queued/succeeded).

## 3. Per-table before / to_delete / after (LanceDB)

From `reconcile/dryrun.json` (`reconcile_eval_store --document-map eval/corpora/multihop_rag/document_map.json --lancedb-path ./data/lancedb-eval`, dry-run):

| Table | before | to_delete | after |
|---|---|---|---|
| `documents` | 350 | 4 | 346 |
| `nodes` | 19,210 | 634 | 18,576 |
| `edges` | 18,860 | 630 | 18,230 |
| `staged_documents_v2` | 0 | 0 | 0 |
| `entity_edges` | 46,092 | 1,040 | 45,052 |

`entity_edges.to_delete` (1,040) is the sum of the document_id-cascade deletions (step 2, rows whose OWN `document_id` is one of the 4 extras) plus the orphan-incident deletions (step 5, rows on a mapped document that touch an entity being fully orphaned) — see §4, `orphan_incident_entity_edges = 0`, meaning **all 1,040** came from step 2 alone; no mapped-document entity_edges row touches an orphaned entity in this store.

## 4. Entity rewrite / orphan / untouched / isolated_untouched

| Metric | Count |
|---|---|
| `entities.total` | 27,971 |
| `entities.rewrite` (mixed mapped+extra chunks; provenance pruned to mapped only) | 669 |
| `entities.orphan` (all `source_chunk_ids` were extra; entity deleted) | 116 |
| `entities.untouched` (no chunk touched an extra) | 27,186 |
| `entities.isolated_untouched` (untouched AND degree-0 after prune) | 2,408 |
| `orphan_incident_entity_edges` (entity_edges deleted via orphan pruning, step 5, distinct from step 2's document_id cascade) | 0 |

`669 + 116 + 27,186 = 27,971` — accounts for every entity exactly once.

**Exactness note (not blocking):** the reconcile classifies "all chunks extra" (empty surviving intersection) as orphan regardless of why the intersection is empty. `entities.isolated_untouched = 2,408` is close to `06.3.3-STORE-BASELINE.md`'s pre-existing baseline of 2,447 isolated (degree-0) entities — the difference (39) is degree-0 entities that were *not* untouched (i.e., were themselves rewritten or orphaned by this reconcile), not a miscount. No entity in this store has originally-empty `source_chunk_ids` (extraction always attaches provenance), so `orphan = 116` reflects only genuine "every source chunk was in a deleted document" cases, not a pre-existing-empty-provenance artifact.

## 5. LanceDB table versions (before == after — dry-run wrote nothing)

| Table | version |
|---|---|
| `communities` | 1 |
| `documents` | 701 |
| `edges` | 701 |
| `entities` | 6,266 |
| `entity_edges` | 697 |
| `nodes` | 701 |
| `staged_documents_v2` | 765 |

`versions_before == versions_after` for all seven tables, machine-asserted by the plan's own verify command (`assert d['versions_before'] == d['versions_after']`) — confirmed passing. This is the D-62 "seven-table" baseline (not the six the pre-existing accessors covered — `communities_table()` was added to `DatabaseManager` in Task 2 specifically so this record could include it).

## 6. `staged_documents_v2` rows

**0** — matches `06.3.3-STORE-BASELINE.md`'s baseline (0 rows) and `identity check`'s `staged_rows: 0`. No staged-job replay hazard exists in the current store (RESEARCH §D "Staged-job replay hazard").

## 7. D-62 "before" numbers (from `reconcile/document_ids_pre.json`, the independent `--document-ids` probe)

| Source | documents | nodes (distinct doc ids) | edges (distinct doc ids) | entity_edges (distinct doc ids) | staged_documents_v2 rows |
|---|---|---|---|---|---|
| `inspect_lancedb --document-ids` | 350 | 350 | 350 | 347 | 0 |

PostgreSQL `lancet_eval.documents`: **367** rows total (`SELECT count(*)`), of which **346** are `status = 'completed'` (matches the 346-entry document map exactly) and **21** are not (the extras in §2).

These are the committed "before" numbers this reconcile's `--apply` (06.3.4.1-06) will be measured against.

## 8. Rollback layers (for 06.3.4.1-06, not created by this task)

This task deletes nothing and creates no backups — it only records what 06.3.4.1-06 must create immediately before running `--apply`:

1. **Directory copy (primary rollback):** stop the engine, then copy `data/lancedb-eval/` to `data/lancedb-eval.pre-06.3.4.1-reconcile/` (~720 MB; outside git, matching the existing `data/lancedb.pre-05-25.bak` precedent). A full filesystem-level restore if the LanceDB version layer below is insufficient or was itself damaged.
2. **LanceDB version layer (secondary rollback):** the seven table versions recorded in §5 above (`documents=701, nodes=701, edges=701, entity_edges=697, entities=6266, staged_documents_v2=765, communities=1`) are the exact versions to `table.checkout(v)` + `table.restore()` back to, per table, if `--apply`'s cascade needs to be undone without a full directory restore. `reconcile_eval_store` never calls `optimize`, compaction or `cleanup_old_versions` (grep-verified in Task 2, `reconcile_eval_store.rs` has no call sites outside its own doc comment), so this layer stays available after `--apply` runs.
3. **PostgreSQL backup:** `docker compose exec -T db pg_dump -U postgres -d lancet -n lancet_eval > <backup path outside git>` before `identity pg-delete-extras --apply` — required by `delete_pg_extras`'s own apply-mode guard (`pg_dump_path` must point at an existing non-empty file).

## 9. Commands run (all read-only; nothing deleted)

```bash
# Engine stopped, docker compose db service up (health=healthy) throughout.
cargo run --manifest-path engine/Cargo.toml --release --quiet --bin inspect_lancedb -- \
  --document-ids --lancedb-path ./data/lancedb-eval \
  > reconcile/document_ids_pre.json

cargo run --manifest-path engine/Cargo.toml --release --quiet --bin reconcile_eval_store -- \
  --document-map eval/corpora/multihop_rag/document_map.json --lancedb-path ./data/lancedb-eval \
  > reconcile/dryrun.json   # no --apply: dry-run is the default

uv run --project eval lancet-eval identity check --corpus multihop_rag
# exit 1, expected: 4 LanceDB extras + 21 PostgreSQL extras + the alias's presence
# all fail the identity gate by design (D-61 preflight gate, closed by 06.3.4.1-06)

uv run --project eval lancet-eval identity pg-delete-extras --corpus multihop_rag
# dry-run (no --apply): confirms the same 21 extra ids independently

# filename/status/created_at for the 21 PostgreSQL extras, via run_psql,
# filtered against the dry-run's own 21-id list (not a second NOT IN query)
uv run --project eval python <ad-hoc script using lancet_eval.seed.run_psql> \
  > reconcile/pg_extras.json
```

## 10. Delete nothing (confirmed)

- `dryrun.json`'s `versions_before == versions_after` for all seven tables (§5).
- `identity pg-delete-extras` was run **without** `--apply`.
- No `docker compose exec ... DELETE` was ever issued.
- `git status` on `data/` is not applicable (gitignored); the store's PostgreSQL row counts (367 total, 21 extra) and LanceDB extras (4) match RESEARCH's predictions exactly, which is independent evidence nothing changed between RESEARCH's investigation and this task's live read.
