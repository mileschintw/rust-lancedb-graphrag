---
phase: 06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair
plan: 09
type: execute
wave: 5
depends_on: [06.3.4.1-06, 06.3.4.1-07]
files_modified:
  - engine/src/bin/diag_probe.rs
  - eval/src/lancet_eval/diagnostic.py
  - eval/tests/test_diagnostic.py
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-GOLD-COVERAGE.md
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/diagnostic/post-reconcile/gold_coverage.jsonl
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/diagnostic/post-reconcile/gold_chunks.jsonl
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/diagnostic/post-reconcile/rechunk_check.jsonl
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/diagnostic/post-reconcile/vector_top4.jsonl
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-VECTOR-DIAG.md
autonomous: false
requirements: [OBS-05]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Columns (a), (b) and (c) are computed on the reconciled store by direct probes before any paid drive (D-85): (b) by the post-reconcile `--gold-chunks` run, and (c) by a dense-only top-4 probe that uses the production dense port at the pinned nodes version."
    - "Column (c) is yes when at least one gold evidence item's gold chunk_id is among the question's dense top-4 chunk_ids; the probe records the `index_generation` it read, which must equal `document_map.json`'s."
    - "The gold-doc versus `document_map.json` coverage table is regenerable by `python -m lancet_eval.diagnostic coverage`, never hand-edited (D-61)."
    - "The D-59 disposition is decided before any re-ingest: mapped documents with an `absent` gold fact are re-ingest candidates, and facts split across chunks are shown, with a re-chunk check of the stored document body, whether re-ingestion can close them; if it cannot, the decision is put to the user rather than redesigning the chunker (D-59)."
    - "Whether the diagnostic sample includes 10 null_query questions is decided at the checkpoint before the sample is committed, so D-72's abstention metric does not end up with n = 0 by default (D-72, RESEARCH Open Question 2)."
    - "The question-embedding probe is a paid stage authorised at a blocking checkpoint with an explicit `--stage-cap`, and the bin stops dispatch before exceeding it (D-86)."
    - "The (b)-yes population G and its intersection with (c)-yes, V, are computed and counted here; G is frozen only in 06.3.4.1-10, after any D-59 re-ingest (D-63)."
  artifacts:
    - path: "engine/src/bin/diag_probe.rs"
      provides: "`--vector-top-k` dense-only probe with embedding cache and stage cap; `--rechunk-check`"
    - path: "eval/src/lancet_eval/diagnostic.py"
      provides: "(c) join, compute_populations, coverage subcommand"
      contains: "def compute_populations"
    - path: ".planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-VECTOR-DIAG.md"
      provides: "(a)/(b)/(c) counts, |G|, |V|, D-59 candidates, checkpoint outcome"
  key_links:
    - from: "engine/src/bin/diag_probe.rs"
      to: "engine/src/service.rs ProductionDenseRetrievalPort"
      via: "dense top-4 at the startup nodes_version"
      pattern: "ProductionDenseRetrievalPort"
    - from: "eval/src/lancet_eval/diagnostic.py"
      to: "diagnostic/post-reconcile/vector_top4.jsonl"
      via: "c_gold_in_vector_top4 join on chunk_ids"
      pattern: "vector_top4"
  prohibitions:
    - "The (c) probe must not run before its cap is authorised at Task 2"
    - "Doc-level presence must not be used to define G (D-63)"
---

<objective>
Layer 4, first half: answer "is the gold chunk in the index, and does plain vector retrieval find it?"
on the reconciled store, without touching graph architecture (ROADMAP constraint 4). Then decide D-59
and the null-question question with the numbers in hand.

Purpose: G (the SC-3/SC-4/SC-5 denominator, D-63) and V (SC-5's restriction, D-82) are defined by these
columns and must be fixed before the drives that are scored on them.

Output: `diag_probe` (vector top-k and re-chunk check), the (c) join and coverage table, a decision
record, and `06.3.4.1-VECTOR-DIAG.md`.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-CONTEXT.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RESEARCH.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-STORE-BASELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: diag_probe bin, (c) join, coverage table, post-reconcile (b) run</name>
  <files>engine/src/bin/diag_probe.rs, eval/src/lancet_eval/diagnostic.py, eval/tests/test_diagnostic.py, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-GOLD-COVERAGE.md, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/diagnostic/post-reconcile/</files>
  <read_first>
    - `rust-guidelines.md`
    - `engine/src/main.rs:20-160` (settings, embedding client and model construction), `engine/src/service.rs:531-600` (`ProductionDenseRetrievalPort`), `engine/src/retrieval/dense.rs:56-101,162-164`, `engine/src/chunker` module (production chunker and its config)
    - `engine/src/bin/retrieval_soak.rs` (06.3.4.1-03 bin skeleton, no test module)
    - `eval/src/lancet_eval/diagnostic.py`, `eval/src/lancet_eval/measure.py:92-120` (`compute_spend` price constants: embeddings $0.12/M)
    - RESEARCH §E (the (b) rule, re-ingest analysis) and §L (spend basis)
    - `06.3.4.1-OI02-PROFILE.md` `## Fix` (06.3.4.1-07, revised): probes read the LIVE store `./data/lancedb-eval` only, never a `data/lancedb-replay*` copy. If 07's fix wrote the eval store, `index_generation` changed; cite the current value from `document_map.json`, not `lance-702`. Check whether the fix changed `ProductionDenseRetrievalPort::new`.
  </read_first>
  <action>
    Create `engine/src/bin/diag_probe.rs` (no `#[cfg(test)]` module, so the engine test-count gate is unchanged).

    Mode `--vector-top-k <k> --questions <jsonl> --stage-cap <usd> [--cache <path>] [--estimate-only]`:
    - Load settings with `LANCET_ENV=eval` and open the store with `open_and_validate`.
    - Build the startup snapshot's `nodes_version` and `ProductionDenseRetrievalPort` as `main.rs` does.
    - Embed questions in batches of 32 with the same embedding client and model the engine uses. Cache vectors in a JSONL file keyed by `(question_id, embedding_model)`; the default cache path is `data/probe-cache/question_embeddings.jsonl` (gitignored), so re-runs are free.
    - Meter spend as provider-reported (or, if absent, estimated) embedding tokens × $0.12/M, and stop dispatch before a batch would exceed `--stage-cap`. Print the running total.
    - Run a dense-only top-k at the pinned version for each question.
    - Print one JSON line per question: `question_id`, `chunk_ids` in rank order, `scores`, `index_generation`, `embedding_model`. The final line is a summary `{spend_usd, questions, cached, stopped_by_cap}`.
    - `--estimate-only` prints the projected cost and makes no call.

    Mode `--rechunk-check <doc_ids.json>`: for each listed document, run the production chunker over `documents.raw_content` with the configured chunk size, compare the resulting chunk texts to the stored `nodes.content` ordered by `chunk_index`, and print `{document_id, identical: bool, stored_chunks, rechunked_chunks}`. It is read-only and makes no API call.

    In `diagnostic.py`:
    - `build_rows` gains `vector_top4_path` (CLI option `--vector-top4` on the `table` subcommand) and sets `c_gold_in_vector_top4` (true when any item's gold `chunk_ids` intersect the question's top-4).
    - Add `compute_populations(rows) -> dict` returning `g_question_ids` (non-null, b yes), `v_question_ids` (G and c yes), counts and per-type counts.
    - Add subcommand `coverage --corpus --gold-chunks --out-dir`, which writes `gold_coverage.jsonl` (per question, per evidence title: mapped document_id, document present in LanceDB, the (b) tri-state) and `06.3.4.1-GOLD-COVERAGE.md`.
    - Add tests for the (c) join and `compute_populations` on fixtures.

    Then, free and read-only:
    1. Re-run `inspect_lancedb --gold-chunks` on the reconciled store → `diagnostic/post-reconcile/gold_chunks.jsonl`.
    2. Generate the coverage table.
    3. List the documents with any `absent` item and the documents with `split_across_chunks` items, and run `--rechunk-check` on both → `rechunk_check.jsonl`.
    4. Run `diag_probe --vector-top-k 4 --estimate-only` for the 500-question sample and record the projection.
  </action>
  <verify>
    <automated>cargo build --manifest-path engine/Cargo.toml --release --bin diag_probe && sh scripts/engine-test-targets.sh</automated>
    <fails_when>non-zero exit, or stderr contains "FAIL:" (the new bin must not change counted targets)</fails_when>
    <automated>uv run --project eval pytest eval/tests/test_diagnostic.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the output</fails_when>
    <automated>test -s .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/diagnostic/post-reconcile/gold_chunks.jsonl && test -s .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/diagnostic/post-reconcile/gold_coverage.jsonl && test -f .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-GOLD-COVERAGE.md</automated>
    <fails_when>non-zero exit: a post-reconcile artifact is missing or empty</fails_when>
  </verify>
  <acceptance_criteria>
    - The post-reconcile (b) tri-state counts and the lists of absent and split documents are recorded in the summary, with `rechunk_check.jsonl` showing `identical` per document.
    - The `--estimate-only` projection for 500 questions is recorded (expected under $0.005 per RESEARCH §L).
  </acceptance_criteria>
  <done>(a) and (b) exist on the reconciled store, the coverage table is regenerable, and the (c) probe is built and costed but not yet run.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Authorise the (c) probe cap, decide D-59 and null inclusion</name>
  <decision>Three bound decisions: (1) the (c) embedding-probe `--stage-cap`; (2) the D-59 disposition; (3) whether the diagnostic sample includes 10 null_query questions.</decision>
  <context>
    Present from Task 1:
    - the post-reconcile (b) counts;
    - the absent-fact documents (re-ingest candidates, N);
    - the split-fact documents with their `rechunk_check` result (identical means re-ingest reproduces the same split and cannot close it; RESEARCH §E);
    - the (c) cost projection.

    Proposed caps, which the user authorises (RESEARCH §L, D-86):
    - (c) probe: $0.05.
    - D-59 re-ingest, if any: max($0.10, $0.05 × N). This is its own capped stage, executed in 06.3.4.1-10. Extraction runs ~55 LLM calls per document, estimated ~$0.01–0.02/doc [ASSUMED A3].

    Null questions: D-72 excludes them from G and SC-3, but excluding them from the drive leaves the abstention metric at n = 0 in the run of record; RESEARCH A10 recommends 90 non-null + 10 null. D-86 has no overall ceiling (06.3.1 D-47).
  </context>
  <options>
    <option id="probe-cap-0.05">
      <name>(1) Authorise the (c) probe at $0.05</name>
      <pros>Covers the 500-question projection many times over; cached vectors make re-runs free</pros>
      <cons>Tiny provider spend</cons>
    </option>
    <option id="d59-none">
      <name>(2a) D-59: no re-ingest (N = 0, or only split facts that re-chunking reproduces)</name>
      <pros>No paid extraction; split-fact questions simply fall outside G by D-63's definition</pros>
      <cons>G is smaller by the split-fact questions</cons>
    </option>
    <option id="d59-reingest">
      <name>(2b) D-59: re-ingest the N absent-fact documents under cap max($0.10, $0.05 × N)</name>
      <pros>Restores gold chunks the store is actually missing; DATA-03 is exercised through the normal ingest path</pros>
      <cons>Paid extraction; changes index_generation again; runs in 06.3.4.1-10 before G is frozen</cons>
    </option>
    <option id="d59-stop-ask">
      <name>(2c) D-59: re-ingest cannot close the gap (split facts the user wants in G); stop and ask about the chunker</name>
      <pros>Honours D-59's "if that happens, stop and ask"</pros>
      <cons>A chunker change is outside this phase's decisions; it would need a new decision</cons>
    </option>
    <option id="nulls-include-10">
      <name>(3a) Include 10 null_query questions (90 non-null + 10 null = 100)</name>
      <pros>D-72 abstention metric has n > 0 in the run of record</pros>
      <cons>Ten of the 200 drive records do not feed SC-3</cons>
    </option>
    <option id="nulls-exclude">
      <name>(3b) Exclude null_query questions (100 non-null)</name>
      <pros>All drive records feed G</pros>
      <cons>D-72 metric reports n = 0 in the run of record</cons>
    </option>
  </options>
  <resume-signal>Reply with one choice for each of (1), (2) and (3), for example "probe-cap-0.05, d59-none, nulls-include-10", and the cap figures if they differ.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run the (c) probe under the authorised cap and record G and V candidates</name>
  <precondition>Task 2 recorded an authorised (c) probe cap; OPENROUTER_API_KEY is set; `uv run --project eval lancet-eval identity check --corpus multihop_rag` passes.</precondition>
  <files>.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/diagnostic/post-reconcile/vector_top4.jsonl, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-VECTOR-DIAG.md</files>
  <read_first>
    - Task 2's recorded decisions (in the plan summary), `06.3.4.1-GOLD-COVERAGE.md`
  </read_first>
  <action>
    Run `cargo run --manifest-path engine/Cargo.toml --release --quiet --bin diag_probe -- --vector-top-k 4 --questions eval/corpora/multihop_rag/questions.sample.jsonl --stage-cap <authorised>` → `diagnostic/post-reconcile/vector_top4.jsonl`. Assert its `index_generation` equals `document_map.json`'s and that `stopped_by_cap` is false. Regenerate the table with (a), (b) and (c) into `diagnostic/post-reconcile/`, and compute populations.

    Write `06.3.4.1-VECTOR-DIAG.md`:
    - the three Task 2 decisions verbatim with the date;
    - (a)/(b)/(c) yes counts overall and by `question_type`;
    - |G| and |V| (candidates, frozen in 10);
    - the vector-only Recall@4 on G compared with the 06.3.4 journal's 0.3613 hybrid Recall@4, labelled non-comparable (different index generation and retrieval mode);
    - the D-59 candidate list and disposition;
    - the observed spend against the cap.
  </action>
  <verify>
    <automated>uv run --project eval python -c "import json; rows = [json.loads(l) for l in open('.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/diagnostic/post-reconcile/vector_top4.jsonl')]; s = rows[-1]; m = json.load(open('eval/corpora/multihop_rag/document_map.json')); assert s.get('stopped_by_cap') is False, s; assert all(r['index_generation'] == m['index_generation'] for r in rows[:-1]); assert len(rows) - 1 == 500"</automated>
    <fails_when>AssertionError printed: stopped by cap, a generation mismatch, or not exactly 500 question lines</fails_when>
  </verify>
  <acceptance_criteria>
    - `06.3.4.1-VECTOR-DIAG.md` states |G|, |V|, the three decisions and the spend, which is at or below the authorised cap.
    - No file under `eval/runs/` changed.
  </acceptance_criteria>
  <done>The vector-only diagnosis on the reconciled store is recorded, and every decision needed to freeze G and the sample is made.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe bin → embedding provider | paid API calls with the project key |
| probe bin → eval store | read-only access |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.4.1-09-01 | Denial of service (financial) | `diag_probe --vector-top-k` | medium | mitigate | Required `--stage-cap` checked before each batch; authorised at a blocking checkpoint; embedding cache makes re-runs free |
| T-06.3.4.1-09-02 | Information disclosure | API key | medium | mitigate | Key read from env by the existing client; never printed; cache holds vectors and question IDs only, under gitignored `data/` |
| T-06.3.4.1-09-03 | Tampering | eval store | low | mitigate | `open_and_validate` plus query-only calls; the re-chunk check reads `raw_content` and never writes |
</threat_model>

<verification>
- `uv run --project eval pytest -q` and `sh scripts/engine-test-targets.sh` exit 0.
</verification>

<success_criteria>
Columns (a)-(c) exist on the reconciled store, and G and V candidates, D-59 and null inclusion are
decided with numbers.
</success_criteria>

## Artifacts this phase produces

- Bin `diag_probe`: modes `--vector-top-k K --questions --stage-cap [--cache] [--estimate-only]` and `--rechunk-check <ids.json>`; embedding cache under `data/probe-cache/` (untracked).
- `lancet_eval.diagnostic`: `build_rows(..., vector_top4_path)`, `compute_populations`, subcommand `coverage`.
- Files: `06.3.4.1-GOLD-COVERAGE.md`, `06.3.4.1-VECTOR-DIAG.md`, `diagnostic/post-reconcile/{gold_chunks,gold_coverage,rechunk_check,vector_top4}.jsonl`.

<output>
Create `.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-09-SUMMARY.md` when done.
</output>
