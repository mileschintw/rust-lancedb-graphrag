---
phase: 06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair
plan: 10
type: execute
wave: 6
depends_on: [06.3.4.1-09, 06.3.4.1-05, 06.3.4.1-02]
files_modified:
  - eval/src/lancet_eval/seed.py
  - eval/tests/test_seed.py
  - eval/corpora/multihop_rag/document_map.json
  - eval/scripts/select_diag_sample.py
  - eval/tests/test_select_diag_sample.py
  - eval/corpora/multihop_rag_diag.toml
  - eval/corpora/multihop_rag/questions.diag.jsonl
  - eval/corpora/multihop_rag/diag_selection.json
  - .gitignore
  - eval/src/lancet_eval/thresholds.py
  - eval/tests/test_gate.py
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-STORE-BASELINE.md
autonomous: true
requirements: [OBS-05, DATA-03]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-59 runs only as 06.3.4.1-09's checkpoint decided: re-ingest of the listed absent-fact documents through the normal gateway ingest path under the authorised cap, or a recorded no-op. No chunker or ingest redesign happens here (D-59; D-56 honoured: targeted, never a full re-seed)."
    - "A re-ingested document moves to its new `document_id` in `document_map.json`, the old ID is added under `aliases`, stale provenance is re-pruned with the reconcile bin, the engine restarts with `staged_documents_v2` at 0 rows, and `index_generation` is re-read by `record_index_generation()` (D-59, D-62)."
    - "DATA-03's REQUIREMENTS.md checkbox changes only if the re-ingest actually ran and its post-ingest (b) re-probe shows the re-ingested gold facts `in_chunk`. When D-59 is a no-op, this plan's `update_requirements` step passes only OBS-05, and DATA-03 stays unchecked (D-57, RESEARCH §N)."
    - "G (non-null, b yes for every evidence item) and V (G and c yes) are frozen after any re-ingest and committed in `diag_selection.json` before paid drive 1 (D-63)."
    - "The diagnostic sample is drawn reproducibly with `corpus.sample_questions` per `question_type` stratum at seed 42 (proportional 36 comparison / 32 inference / 22 temporal from G, plus 10 null_query questions if 06.3.4.1-09 chose to include them), and the method, seed, quotas, drawn IDs, |G| and |V| are recorded (D-68, D-72)."
    - "A sibling corpus `multihop_rag_diag` points `[questions] file` at `questions.diag.jsonl` and `[documents] map_corpus` at `multihop_rag`, so `completeness_comparison` measures against the diagnostic questions and a finished drive can legitimately reach `partial: false` without `--limit` and without any change to the fail-closed path (D-68, D-87a)."
    - "`VECTOR_BASELINE_USABLE_FLOOR` is committed in `thresholds.py` as a literal equal to max(0.40, B_G + 0.10), rounded up to three decimals, where B_G is the share of the drawn G questions whose normalised gold equals the modal yes/no label; it lands in a commit older than drive 1's first record (D-73)."
    - "Drive run directories for this corpus are tracked by git: `.gitignore` gains `!eval/runs/*-multihop_rag_diag/` next to the existing `!eval/runs/*-multihop_rag/` exception (D-87 run of record must be committable)."
    - "The re-ingest helper is idempotent: it refuses a document whose gold facts are no longer `absent` in the latest (b) probe, and it refuses an ID that is not a current map entry, so a second run is a no-op (specless edge: idempotency, DATA-03)."
    - statement: "Re-ingest runs with the gateway and engine under this plan's control only, and the provenance prune runs after it completes with `staged_documents_v2` at 0 rows (specless edges: concurrency, DATA-03 and DATA-04)."
      verification: backstop
  artifacts:
    - path: "eval/corpora/multihop_rag/diag_selection.json"
      provides: "frozen G, V, drawn sample, method, seed, quotas"
      contains: "g_question_ids"
    - path: "eval/corpora/multihop_rag_diag.toml"
      provides: "diagnostic corpus sharing the one document map"
      contains: "map_corpus"
    - path: "eval/src/lancet_eval/thresholds.py"
      provides: "VECTOR_BASELINE_USABLE_FLOOR literal with provenance"
      contains: "VECTOR_BASELINE_USABLE_FLOOR"
  key_links:
    - from: "eval/corpora/multihop_rag_diag.toml"
      to: "eval/corpora/multihop_rag/document_map.json"
      via: "[documents] map_corpus = \"multihop_rag\" through seed.get_document_map_path"
      pattern: "map_corpus"
    - from: "eval/src/lancet_eval/unpark_gates.py evaluate_sc3"
      to: "eval/src/lancet_eval/thresholds.py"
      via: "getattr VECTOR_BASELINE_USABLE_FLOOR"
      pattern: "VECTOR_BASELINE_USABLE_FLOOR"
  prohibitions:
    - "The sample must not be a --limit prefix and the diagnostic corpus must not carry a copied document_map.json"
    - "The floor must not be computed or changed after any drive-1 record exists"
    - "DATA-03 must not be marked complete when the re-ingest did not run"
---

<objective>
Close layer 4's setup: run D-59 only if it was authorised, then freeze G and V, commit the reproducible
~100-question diagnostic sample and its sibling corpus config, and commit the SC-3 floor before any
drive-1 record exists.

Purpose: SC-3, SC-4 and SC-5 headlines are all computed on G (D-63, D-82). The same question IDs are
driven in drive 1 and drive 2, so graph-off invariance can be checked per question. The SC-3 floor must
predate the data it tests (D-73).

Output: an optional re-ingest, `diag_selection.json`, `questions.diag.jsonl`, `multihop_rag_diag.toml`,
the `.gitignore` exception, and the floor literal.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-VECTOR-DIAG.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-09-SUMMARY.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RESEARCH.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-AI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-59 targeted re-ingest as authorised, or a recorded no-op</name>
  <precondition>06.3.4.1-09 Task 2's D-59 choice is recorded in `06.3.4.1-VECTOR-DIAG.md`. For d59-reingest only: Docker `db` up, OPENROUTER_API_KEY set, and the authorised cap figure recorded.</precondition>
  <files>eval/src/lancet_eval/seed.py, eval/tests/test_seed.py, eval/corpora/multihop_rag/document_map.json, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-STORE-BASELINE.md</files>
  <read_first>
    - `06.3.4.1-VECTOR-DIAG.md` (the D-59 decision and candidate list)
    - `eval/src/lancet_eval/seed.py:263-435` (`seed_corpus` upload at 331-345, poll at 354-397, title skip at 316-318), `record_index_generation`
    - `eval/src/lancet_eval/measure.py:136-180` (`check_provider_allowance`)
    - `engine/src/bin/reconcile_eval_store.rs` (`--target-document-ids`, snapshot guard)
    - RESEARCH §E "Targeted re-ingest (D-59) through the normal path"
  </read_first>
  <behavior>
    - `reingest_documents` refuses an ID that is not a current `entries` key, and a document whose latest (b) probe shows no `absent` fact; both refusals happen before any network call.
    - On success (mocked gateway), the map entry moves to the new ID, `aliases[old] = new`, and the save is atomic.
    - It stops between documents when observed provider spend reaches the cap and returns the documents not processed.
  </behavior>
  <action>
    **Branch d59-none** (the expected outcome per RESEARCH A2): make no code or data change. Append to `06.3.4.1-STORE-BASELINE.md` a "D-59 disposition" section quoting the decision, the absent/split counts and the re-chunk check. In this plan's `update_requirements` step, pass only `OBS-05` to `requirements.mark-complete`; DATA-03 was not exercised and must stay unchecked. The Behavior tests are still written, against the helper added in the d59-reingest branch, only if that branch runs; otherwise record "not triggered".

    **Branch d59-reingest:** add `seed.reingest_documents(settings, corpus_name, document_ids: list[str], *, gold_chunks_path: Path, stage_cap_usd: float) -> dict`. Factor the multipart upload and the poll-to-completed loop out of `seed_corpus` into `_upload_document` and `_await_completed` without changing `seed_corpus`'s behaviour. For each ID:
    1. Stop the engine and gateway, copy the store to a fresh snapshot directory, then run `reconcile_eval_store --target-document-ids` (apply) to cascade-delete that document.
    2. Start the engine and gateway, upload the article body through the gateway, and poll to `completed`.
    3. Update the map (new ID, alias old to new) and read the provider allowance.

    After all documents:
    - stop the engine and gateway, and run the reconcile dry-run then apply to prune provenance the exact-match resolver re-appended;
    - assert `staged_documents_v2 == 0`, start the engine, run `record_index_generation`, and run `identity check`;
    - re-run `inspect_lancedb --gold-chunks` and confirm the re-ingested facts are `in_chunk`.

    Append counts, spend and the new generation to the baseline. Only in this branch, when the re-probe confirms `in_chunk`, pass `OBS-05 DATA-03` to `requirements.mark-complete`. Write the Behavior tests first.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_seed.py -x && uv run --project eval lancet-eval identity check --corpus multihop_rag</automated>
    <fails_when>non-zero exit from either command, or "passed": false in the identity report</fails_when>
  </verify>
  <acceptance_criteria>
    - `06.3.4.1-STORE-BASELINE.md` has a "D-59 disposition" section naming the branch taken.
    - Branch d59-reingest only: `document_map.json` `aliases` gains one entry per re-ingested document, spend is at or below the cap, and every re-ingested fact is `in_chunk`.
    - Branch d59-none: `git diff --quiet --exit-code -- eval/corpora/multihop_rag/document_map.json eval/src/lancet_eval/seed.py` exits 0, and `grep -n "DATA-03" .planning/REQUIREMENTS.md` still shows `- [ ] **DATA-03**`.
  </acceptance_criteria>
  <done>D-59 either restored the missing gold chunks through the normal path under its own cap, or was recorded as not needed, with DATA-03's checkbox reflecting which.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze G and V, draw and commit the diagnostic sample and corpus</name>
  <files>eval/scripts/select_diag_sample.py, eval/tests/test_select_diag_sample.py, eval/corpora/multihop_rag_diag.toml, eval/corpora/multihop_rag/questions.diag.jsonl, eval/corpora/multihop_rag/diag_selection.json, .gitignore</files>
  <read_first>
    - `eval/src/lancet_eval/corpus.py:195-218` (`load_sample_questions`, `sample_questions(all, n, seed)`: sort by id, `random.Random(seed).sample`, re-sort)
    - `eval/corpora/multihop_rag.toml`, `.gitignore:60-66`
    - `eval/src/lancet_eval/diagnostic.py` (`compute_populations`), `diagnostic/post-reconcile/` (or re-probe output after re-ingest)
    - `eval/src/lancet_eval/journal.py:144-239` (`completeness_comparison`)
    - RESEARCH §L "Sample" and "Diagnostic corpus config"
  </read_first>
  <behavior>
    - The selection is deterministic: two runs produce byte-identical `questions.diag.jsonl` and `diag_selection.json`.
    - Every drawn non-null question is in G; quotas are 36/32/22 when each stratum of G has at least that many members, otherwise the stratum is taken whole and the shortfall recorded; 10 null questions are drawn only when `--include-nulls 10` is passed.
    - `completeness_comparison` over a synthetic `multihop_rag_diag` journal holding all (question, arm) units returns complete; one missing unit returns incomplete.
    - `load_document_map("multihop_rag_diag")` returns the `multihop_rag` map.
  </behavior>
  <action>
    Create `eval/scripts/select_diag_sample.py` with `main(argv)` and options `--gold-chunks`, `--vector-top4`, `--include-nulls {0,10}` (the value decided at 06.3.4.1-09) and `--seed 42`.

    It builds rows via `diagnostic.build_rows` (no journal) and computes G and V with `compute_populations`. It draws per stratum with `corpus.sample_questions(<raw question dicts of that stratum in G>, quota, seed)`, with quotas from proportional allocation of 90 over G's comparison/inference/temporal counts (36/32/22 on the full sample; recompute with the largest-remainder method on G's actual counts and record the result). Nulls are drawn with the same function from the 53 null questions.

    It writes:
    - `eval/corpora/multihop_rag/questions.diag.jsonl`: the drawn questions' original lines from `questions.sample.jsonl`, verbatim, sorted by `question_id`.
    - `eval/corpora/multihop_rag/diag_selection.json`: `method`, `seed`, `quotas`, `strata_counts_in_G`, `drawn_question_ids`, `g_question_ids` (all of G), `v_question_ids`, `g_size`, `v_size`, `null_included`, `source_index_generation` (from the map), `date`.

    If fewer than 60 drawn non-null questions are in G, exit non-zero with a message instead of widening silently; that halts the plan for a user decision.

    Create `eval/corpora/multihop_rag_diag.toml` as a copy of `multihop_rag.toml` with `[questions] file = "multihop_rag/questions.diag.jsonl"`, `sample_size` equal to the drawn count, and `[documents] map_corpus = "multihop_rag"`.

    Add `!eval/runs/*-multihop_rag_diag/` to `.gitignore` directly under the existing `!eval/runs/*-multihop_rag/` line.

    Tests go in `eval/tests/test_select_diag_sample.py`.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_select_diag_sample.py eval/tests/test_corpus.py eval/tests/test_journal_reconcile.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the output</fails_when>
    <automated>git check-ignore -q eval/runs/2026-10-01-drive1-multihop_rag_diag/journal.jsonl; test $? -eq 1</automated>
    <fails_when>exit status other than 0 from the test: git still ignores a path under a *-multihop_rag_diag run directory</fails_when>
  </verify>
  <acceptance_criteria>
    - `diag_selection.json` records `g_size`, `v_size`, quotas, seed 42, `null_included` matching 06.3.4.1-09's decision, and `source_index_generation` equal to `document_map.json`'s.
    - `questions.diag.jsonl` line count equals `len(drawn_question_ids)`, and every line exists verbatim in `questions.sample.jsonl`.
    - Re-running the script leaves `git diff --quiet --exit-code -- eval/corpora/multihop_rag/questions.diag.jsonl eval/corpora/multihop_rag/diag_selection.json` at exit 0.
  </acceptance_criteria>
  <done>G, V and the diagnostic sample are frozen, reproducible and committed, and a finished drive over them can be complete by construction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Commit the SC-3 floor literal before drive 1 (D-73)</name>
  <files>eval/src/lancet_eval/thresholds.py, eval/tests/test_gate.py</files>
  <read_first>
    - `eval/src/lancet_eval/thresholds.py:90-114` and the 06.3.4.1-05 literals
    - AI-SPEC §5 "Note: SC-3 floor (D-73)" (derivation, halt-risk statement); RESEARCH §H
    - `eval/corpora/multihop_rag/diag_selection.json`, `questions.diag.jsonl`
  </read_first>
  <behavior>
    - `VECTOR_BASELINE_USABLE_FLOOR` exists, is a float in [0.40, 1.0], and equals `ceil(max(0.40, B_G + 0.10) * 1000) / 1000` recomputed in the test from `questions.diag.jsonl` (the drawn non-null questions, all in G) with `squad_normalize` on the gold answers.
  </behavior>
  <action>
    Compute B_G = (count of drawn non-null questions whose `squad_normalize(gold_answer)` equals the modal label among {"yes", "no"}) / (count of drawn non-null questions). Compute the floor = max(0.40, B_G + 0.10) rounded up to three decimals.

    Append to `thresholds.py` the literal with the comment `# 06.3.4.1 D-73: max(0.40, B_G + 0.10); B_G = <k>/<n> (modal label '<yes|no>') over the drive-1 G sample in questions.diag.jsonl; evaluated <YYYY-MM-DD>; committed before paid drive 1. A miss triggers investigation (D-84), not suppression.` followed by `VECTOR_BASELINE_USABLE_FLOOR: float = <value>`.

    Also record B over the full G (from `diag_selection.json`) and the AI-SPEC halt-risk statement (P(miss) at the committed floor for a true rate of 0.537 with n equal to the G sample size) in the plan summary. Write the test first.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_gate.py eval/tests/test_unpark_gates.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "VECTOR_BASELINE_USABLE_FLOOR: float" eval/src/lancet_eval/thresholds.py` shows exactly one literal with the provenance comment on the preceding line.
    - The commit adding it predates any `eval/runs/*-drive1-multihop_rag_diag/` path in git history (checked in 06.3.4.1-12).
  </acceptance_criteria>
  <done>The SC-3 floor is a committed literal derived from gold labels only, before any drive-1 data exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → gateway ingest (re-ingest branch) | article bodies uploaded; paid extraction triggered |
| selection script → committed corpus files | derived populations become gate denominators |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.4.1-10-01 | Denial of service (financial) | `reingest_documents` | medium | mitigate | Authorised cap from 06.3.4.1-09; allowance read between documents; stops before exceeding |
| T-06.3.4.1-10-02 | Tampering | store during re-ingest | high | mitigate | Snapshot before each targeted delete; eval-path and schema isolation guards in the bin and harness; re-prune plus identity check after |
| T-06.3.4.1-10-03 | Repudiation | G/sample/floor | high | mitigate | Deterministic script (byte-identical re-run), committed selection with method and seed, floor literal committed before drive 1 |
| T-06.3.4.1-10-04 | Repudiation | requirement status | medium | mitigate | DATA-03 marked only when re-ingest ran and re-probe confirmed; branch recorded in the baseline |
</threat_model>

<verification>
- `uv run --project eval pytest -q && uv run --project eval ruff check --preview eval/src eval/tests eval/scripts` exit 0.
- `uv run --project eval lancet-eval identity check --corpus multihop_rag_diag` exits 0 (the diag corpus resolves the one map and the gate passes).
</verification>

<success_criteria>
The store is final for drive 1, G/V and the sample are frozen and committed, and the SC-3 floor predates
drive 1.
</success_criteria>

## Artifacts this phase produces

- `lancet_eval.seed`: `reingest_documents`, `_upload_document`, `_await_completed` (re-ingest branch only).
- Script `eval/scripts/select_diag_sample.py` (`--gold-chunks`, `--vector-top4`, `--include-nulls`, `--seed`).
- Corpus `multihop_rag_diag` (`eval/corpora/multihop_rag_diag.toml`), `questions.diag.jsonl`, `diag_selection.json` (fields in Task 2).
- `.gitignore` exception `!eval/runs/*-multihop_rag_diag/`.
- `thresholds.VECTOR_BASELINE_USABLE_FLOOR`.

<output>
Create `.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-10-SUMMARY.md` when done.
</output>
