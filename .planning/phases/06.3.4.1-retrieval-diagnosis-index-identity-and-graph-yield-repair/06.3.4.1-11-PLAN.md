---
phase: 06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair
plan: 11
type: execute
wave: 7
depends_on: [06.3.4.1-10, 06.3.4.1-08]
files_modified:
  - eval/runs/
  - config/config.toml
  - config/config.example.toml
  - engine/src/config.rs
  - scripts/engine-test-targets.sh
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-BUDGETS.md
autonomous: false
requirements: [OBS-05]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Budget pass A is a paid stage run only after a blocking checkpoint authorises its `--stage-cap`; `measure`'s $5.00 default is never relied on (D-86; RESEARCH Pitfall 8)."
    - "All six budgets are re-derived on the fixed engine with 06.3.3's committed rule (`derive_budget`: p95 × 1.5 via `DecisionThresholds`, `check_nesting_invariants` with ≥ 500 ms slack raising the outer budget, `check_harness_ceilings`; generation from `derive_provider_contract_budget` = 65000) — 06.3.3's own values are not simply restored (D-66)."
    - "Derivation proceeds only if pass A's own RetrieveHybrid decay verdict is available and not present, so no budget is derived from a leaking engine (D-66, D-64)."
    - "`config/config.toml`, `config/config.example.toml` and the `engine/src/config.rs` serde defaults are written together in one commit, and a Rust unit test asserts the three agree; the stale 06.3.3 nesting comment block is rewritten to the new values (D-66)."
    - "`06.3.4.1-BUDGETS.md` carries the derivation trail in the 06.3.3-BUDGETS shape, records the `33e774b` overwrite of production and eval configs as a finding, and records every D-86 stage cap as it is authorised; `06.3.3-BUDGETS.md` is not edited (D-66, D-86)."
    - "Raised-ceiling `LANCET_ENGINE__WORKFLOW__*` overrides used for the measurement are recorded, and are unset before any drive; the engine is restarted on the new committed budgets and preflight passes (D-66; RESEARCH §K)."
  artifacts:
    - path: ".planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-BUDGETS.md"
      provides: "pass-A derivation trail, 33e774b finding, authorised stage caps"
    - path: "engine/src/config.rs"
      provides: "re-derived default_*_timeout_ms values and the three-way agreement test"
      contains: "fn default_retrieve_timeout_ms"
    - path: "eval/runs/"
      provides: "eval/runs/<date>-passA-measure-multihop_rag/ measurement journal and measurement.json"
  key_links:
    - from: "eval/runs/<date>-passA-measure-multihop_rag/"
      to: "config/config.toml"
      via: "measure.derive_budgets_from_records → written values"
      pattern: "retrieve_timeout_ms"
  prohibitions:
    - "No budget may be written from a pass whose decay verdict is present or unavailable"
    - "config/config.eval.toml must stay lancedb_path-only; budgets go only into config.toml, config.example.toml and config.rs"
    - "No LANCET_ENGINE__WORKFLOW__* variable may remain set when drive 1 starts"
---

<objective>
Re-derive the six workflow budgets on the fixed engine (D-66) from a capped, authorised measurement pass,
and write production config and code defaults together with a derivation trail.

Purpose: the `33e774b` overwrite left config and code defaults disagreeing, and 06.3.3's values were
measured on the leaking engine. Drive 1's flatness proof needs zero RetrieveHybrid timeouts over ~200
records (AI-SPEC §5 note), so its budgets must come from the fixed engine by the committed rule.

Output: the pass-A measurement run, new budgets in the config trio with an agreement test, and
`06.3.4.1-BUDGETS.md`.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-BUDGETS.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RESEARCH.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-OI02-PROFILE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Authorise budget pass A's stage cap and measurement settings (D-86)</name>
  <decision>Authorise pass A's `--stage-cap` and its raised-ceiling measurement settings.</decision>
  <context>
    Pass A drives about 160 questions × 2 arms of `multihop_rag` through harness → gateway → engine on the
    fixed engine with the D-71 prompt, as one segment with no restart. 06.3.3 observed $0.083 for 320
    records; the all-at-max estimate is ~$0.31 (RESEARCH §L).

    Proposed cap: $0.50. A cap-stop leaves the pass partial and derivation must then wait. Raised
    ceilings are set only for this pass, through `LANCET_ENGINE__WORKFLOW__*` overrides, so latency is
    not censored: 120000 ms for retrieve, graph_operation, graph_node and prompt, 30000 ms for
    query_embedding, and generation_node unchanged at 65000. They are recorded and unset afterwards.
    There is no overall phase ceiling (06.3.1 D-47).
  </context>
  <options>
    <option id="authorise-0.50">
      <name>Authorise $0.50 with the raised ceilings above</name>
      <pros>At least 1.6× the all-at-max estimate; uncensored latencies for derivation</pros>
      <cons>Paid spend</cons>
    </option>
    <option id="authorise-other">
      <name>Authorise a different cap or ceiling set (state the figures)</name>
      <pros>User control of spend</pros>
      <cons>A lower cap risks a partial pass and a re-run</cons>
    </option>
  </options>
  <resume-signal>Reply authorise-0.50, or authorise-other with the cap and ceilings.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run budget pass A and derive the six budgets</name>
  <precondition>Task 1 recorded an authorised cap; Docker `db` up; OPENROUTER_API_KEY set; `uv run --project eval lancet-eval identity check --corpus multihop_rag` passes; the engine binary is a release build containing 06.3.4.1-07 and -08.</precondition>
  <files>eval/runs/, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-BUDGETS.md</files>
  <read_first>
    - `eval/src/lancet_eval/measure.py:319-440` (`derive_budgets_from_records`), `:440+` (`run_measurement_pass`), `eval/src/lancet_eval/latency.py:352-614`
    - `eval/src/lancet_eval/cli.py:837-916` (`measure` options: `--stage-cap` default 5.0 must be overridden)
    - `06.3.3-BUDGETS.md` (trail shape), `git show 33e774b -- config/` (the overwrite)
  </read_first>
  <action>
    Start the release engine with `LANCET_ENV=eval` and the authorised `LANCET_ENGINE__WORKFLOW__*` raised ceilings, then start the gateway. Record the engine PID, start time, binary path, mtime and profile.

    Run `uv run --project eval lancet-eval measure --corpus multihop_rag --sample-size 160 --segment-boundary 100000 --workers 1 --stage-cap <authorised> --out eval/runs/<UTC-date>-passA-measure-multihop_rag`. Record the PID afterwards; a different PID invalidates the pass.

    Compute `decay.analyze_decay(records, COMMITTED_THRESHOLDS, restart_ordinal=None, node_name="RetrieveHybrid")` on the measurement records. Continue only if both prongs are available and decay is not present. Otherwise stop, record the OI-02 regression on the harness path, and route to `/gsd-plan-phase 06.3.4.1 --gaps` (D-64 forbids working around it).

    Run `derive_budgets_from_records` for the six budgets with `check_nesting_invariants(required_slack_ms=500)` and `check_harness_ceilings` against the harness SSE read timeout and question deadline. Create `06.3.4.1-BUDGETS.md` in the 06.3.3 shape:
    - per-node p95 with CI, multiplier, allowance, proposed value, and any nesting raise;
    - the harness-ceiling report;
    - the pass's spend against the cap;
    - the raised-ceiling env values;
    - a "Finding: 33e774b overwrite" section quoting the value changes in `config.toml`, `config.example.toml` and `config.eval.toml`, and that config and code defaults then disagreed;
    - a "Stage caps (D-86)" table with pass A's authorised figure (later plans append).
  </action>
  <verify>
    <automated>RUN=$(ls -d eval/runs/*-passA-measure-multihop_rag | tail -n 1) && uv run --project eval python -c "import sys, json, pathlib; p = pathlib.Path(sys.argv[1]); m = json.loads((p / 'measurement.json').read_text()); print(sorted(m.keys()))" "$RUN" && grep -q "Finding: 33e774b overwrite" .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-BUDGETS.md</automated>
    <fails_when>non-zero exit: no passA run directory, measurement.json missing or unparseable, or BUDGETS.md lacks the 33e774b finding</fails_when>
  </verify>
  <acceptance_criteria>
    - `06.3.4.1-BUDGETS.md` shows a decay verdict with both prongs available and not present, six derived values, and the nesting and ceiling checks.
    - Observed spend is at or below the authorised cap; the engine PID was unchanged across the pass.
  </acceptance_criteria>
  <done>Six budgets are derived by the committed rule from a non-decaying engine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write the budgets to the config trio with an agreement test; restart on them</name>
  <files>config/config.toml, config/config.example.toml, engine/src/config.rs, scripts/engine-test-targets.sh, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-BUDGETS.md</files>
  <read_first>
    - `rust-guidelines.md`
    - `engine/src/config.rs:125-146` (`default_*_timeout_ms`), `:321-344` (startup nesting validation)
    - `config/config.toml:32-60`, `config/config.example.toml:57-69`
    - `engine/tests/config_startup.rs` (existing startup tests)
  </read_first>
  <behavior>
    - A unit test parses `config/config.toml` and `config/config.example.toml` (via `include_str!`) and asserts their seven `[engine.workflow]` timeout values equal the corresponding `default_*_timeout_ms()` values.
    - The startup validation (`graph_node >= query_embedding + graph_operation`; `generation_node >= 2 × generation_timeout_secs × 1000`) passes with the new values.
  </behavior>
  <action>
    Write the six derived budgets (plus `generation_node_timeout_ms = 65000` and the unchanged `reformulate_timeout_ms`) into `config/config.toml`'s `[engine.workflow]`, `config/config.example.toml` and `engine/src/config.rs`'s `default_*_timeout_ms` functions in one commit. Rewrite the comment blocks above them (`config.toml:32-50` and the example's equivalent) to state the new nesting arithmetic and cite `06.3.4.1-BUDGETS.md`. Add the agreement unit test first, see it fail against the current mismatch, then write the values. Update `scripts/engine-test-targets.sh` counts.

    Then unset every `LANCET_ENGINE__WORKFLOW__*` variable (record `Get-ChildItem Env:LANCET_ENGINE__WORKFLOW__*` returning nothing), rebuild and restart the release engine and the gateway, and run `uv run --project eval lancet-eval preflight --corpus multihop_rag_diag`. It must pass: identity gate, generation and canaries with node durations under the new budgets. The preflight's handful of probe queries ride on this stage's authorisation. Append the preflight transcript summary to `06.3.4.1-BUDGETS.md`.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked config && sh scripts/engine-test-targets.sh</automated>
    <fails_when>non-zero exit, "0 passed" for the config filter, or stderr contains "FAIL:"</fails_when>
    <automated>uv run --project eval pytest eval/tests/test_preflight.py -k effective -x</automated>
    <fails_when>non-zero exit, or "no tests ran" (deselected everything)</fails_when>
  </verify>
  <acceptance_criteria>
    - The three sources agree (unit test green); `config/config.eval.toml` is unchanged from 06.3.4.1-02 (`git diff --quiet --exit-code -- config/config.eval.toml`).
    - `06.3.4.1-BUDGETS.md` records a passing `preflight --corpus multihop_rag_diag` on the new budgets with no env overrides set.
  </acceptance_criteria>
  <done>Production config, example config and code defaults carry the same derived budgets, and the engine runs on them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env overrides → effective config | process env silently overrides config files |
| harness → provider | paid measurement queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.4.1-11-01 | Denial of service (financial) | measure pass | medium | mitigate | Blocking checkpoint authorises `--stage-cap`, passed explicitly (never the $5 default) |
| T-06.3.4.1-11-02 | Tampering | effective budgets | high | mitigate | Env overrides recorded, then unset and verified absent before restart; agreement unit test pins file = code defaults |
| T-06.3.4.1-11-03 | Repudiation | derivation provenance | medium | mitigate | Trail with per-node p95, rule, nesting raise; decay verdict must be available and not present before any value is written |
</threat_model>

<verification>
- `cargo test --manifest-path engine/Cargo.toml --locked && (cd gateway && go test ./...)`, `sh scripts/engine-test-targets.sh`, `uv run --project eval pytest -q` exit 0.
</verification>

<success_criteria>
Six budgets derived on the fixed engine by the committed rule, written consistently to three sources,
running live with preflight passing.
</success_criteria>

## Artifacts this phase produces

- Run directory `eval/runs/<date>-passA-measure-multihop_rag/` (journal, measurement.json).
- New values in `config/config.toml`, `config/config.example.toml`, `engine/src/config.rs` `default_*_timeout_ms`; agreement unit test in `config.rs`.
- `06.3.4.1-BUDGETS.md` (derivation trail, 33e774b finding, stage-cap table).

<output>
Create `.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-11-SUMMARY.md` when done.
</output>
