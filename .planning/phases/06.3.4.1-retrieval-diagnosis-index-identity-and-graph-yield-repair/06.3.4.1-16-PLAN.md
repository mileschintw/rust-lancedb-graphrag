---
phase: 06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair
plan: 16
type: execute
wave: 12
depends_on: [06.3.4.1-15]
files_modified:
  - proto/lancet/v1/lancet.proto
  - engine/src/pb/lancet/v1/lancet.v1.rs
  - gateway/proto/lancet/v1/lancet.pb.go
  - engine/src/workflow/runner.rs
  - engine/src/workflow/nodes/retrieve.rs
  - engine/src/service.rs
  - engine/src/tests.rs
  - scripts/engine-test-targets.sh
  - gateway/internal/sse/sse.go
  - gateway/internal/sse/dto.go
  - gateway/internal/sse/sse_test.go
  - eval/src/lancet_eval/client.py
  - eval/src/lancet_eval/journal.py
  - eval/src/lancet_eval/run.py
  - eval/src/lancet_eval/measure.py
  - eval/tests/test_wire_capture.py
  - eval/tests/fixtures/sse/graph_seed_fields.txt
autonomous: true
requirements: [OBS-05, DATA-05]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every drive record carries the seeding diagnostics end to end (engine → gateway SSE → harness journal): `graph_seed_count`, `graph_path_found`, `graph_boosted_chunk_count`, `graph_degree_capped_count` and `graph_seed_document_ids` on `WorkflowMetadata`, so column (d) is computable per record from the seeds' source documents (D-79)."
    - "Each retrieved chunk carries `graph_boosted` on `StructuredCitation` (the message `RetrievalSnapshot.retrieved_chunks` uses), so D-81's retrieval-composition measure reads graph provenance from the final retrieved set, not only from citations (D-81)."
    - "The proto change is additive only: `WorkflowMetadata` tags 12–16 and `StructuredCitation` tag 10, the next free tags; nothing is renumbered or removed (the `bb9d2086` precedent), and both generated trees are regenerated with `buf generate`."
    - "The gateway maps every new field explicitly in both the populated and the nil-metadata default maps, and older journals without the fields still load because every harness field is `int | None = None` / `bool | None = None` / `list[str] | None = None` on the `extra=\"forbid\"` models (OBS-05 provenance)."
  artifacts:
    - path: "proto/lancet/v1/lancet.proto"
      provides: "WorkflowMetadata 12-16, StructuredCitation.graph_boosted = 10"
      contains: "graph_boosted = 10"
    - path: "gateway/internal/sse/sse.go"
      provides: "explicit SSE mapping of the new fields"
      contains: "graph_seed_count"
    - path: "eval/src/lancet_eval/journal.py"
      provides: "WorkflowWireMeta new optional fields"
      contains: "graph_path_found"
  key_links:
    - from: "engine/src/workflow/runner.rs"
      to: "gateway/internal/sse/sse.go"
      via: "WorkflowMetadata fields 12-16 in WorkflowCompletedEvent"
      pattern: "GetGraphSeedCount"
    - from: "eval/src/lancet_eval/client.py"
      to: "eval/src/lancet_eval/journal.py"
      via: "run.py/measure.py mapping into WorkflowWireMeta and retrieved chunks"
      pattern: "graph_seed_document_ids"
  prohibitions:
    - "No existing proto field may be renumbered, renamed or removed"
    - "Question text and entity names must not be added to the wire; only counts, booleans and document IDs"
---

<objective>
Carry the new graph diagnostics from the engine to the journal so SC-4 and SC-5 can be computed per
record: seed count, path found, degree-capped count and seed document IDs per query, plus a
graph-boosted flag per retrieved chunk (D-79, D-81).

Purpose: the per-question table's column (d) and D-81's composition measure must come from the run of
record itself, not from an offline estimate.

Output: additive proto fields, regenerated bindings, engine population, gateway mapping and harness
capture, each with tests.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-PATTERNS.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-15-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additive proto fields, regenerated bindings, engine population</name>
  <precondition>Network access for `buf generate` (buf.gen.yaml uses remote plugins; RESEARCH Pitfall 13); `buf --version` reports 1.72.0.</precondition>
  <files>proto/lancet/v1/lancet.proto, engine/src/pb/lancet/v1/lancet.v1.rs, gateway/proto/lancet/v1/lancet.pb.go, engine/src/workflow/runner.rs, engine/src/workflow/nodes/retrieve.rs, engine/src/service.rs, engine/src/tests.rs, scripts/engine-test-targets.sh</files>
  <read_first>
    - `rust-guidelines.md`
    - `proto/lancet/v1/lancet.proto:126-152,237-252`, `buf.gen.yaml`
    - The `graph_prompt_fact_count` trace in PATTERNS (runner.rs:638,649; service.rs:922 span declaration; assemble_prompt.rs:96)
    - `engine/src/workflow/nodes/retrieve.rs` snapshot/`StructuredCitation` construction; `engine/src/workflow/mod.rs` ctx fields from 06.3.4.1-14/-15
  </read_first>
  <behavior>
    - A completed graph-on workflow with 2 seeds, a path, 1 boosted chunk and 0 capped emits `WorkflowMetadata{graph_seed_count: 2, graph_path_found: true, graph_boosted_chunk_count: 1, graph_degree_capped_count: 0, graph_seed_document_ids: [..]}` and exactly one retrieved chunk with `graph_boosted = true`.
    - A graph-off workflow emits zeros, false, an empty list and no boosted chunk.
    - A prost encode/decode round trip preserves all new fields.
  </behavior>
  <action>
    In `lancet.proto`, append to `WorkflowMetadata`, each with a one-line comment citing 06.3.4.1 D-79/D-81:
    - `uint32 graph_seed_count = 12;`
    - `bool graph_path_found = 13;`
    - `uint32 graph_boosted_chunk_count = 14;`
    - `uint32 graph_degree_capped_count = 15;`
    - `repeated string graph_seed_document_ids = 16;`

    Append `bool graph_boosted = 10;` to `StructuredCitation`.

    Run `buf generate` from the repo root, regenerating `engine/src/pb/**` and `gateway/proto/**`, and commit the generated files unchanged. In `runner.rs`, populate the five fields from the ctx fields next to `graph_prompt_fact_count` and record `lancet.workflow.graph_seed_count` and `lancet.workflow.graph_path_found` on the span, declaring them next to `service.rs:922`. In `retrieve.rs`, set `graph_boosted` on each retrieved-chunk `StructuredCitation` from the provenance 06.3.4.1-15 attached. Fix every exhaustive struct literal the new fields break (the 05-23 precedent). Write the tests first and update the gate counts.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked && sh scripts/engine-test-targets.sh</automated>
    <fails_when>non-zero exit, or stderr contains "FAIL:"</fails_when>
    <automated>grep -q "GraphSeedCount" gateway/proto/lancet/v1/lancet.pb.go && grep -q "graph_boosted" engine/src/pb/lancet/v1/lancet.v1.rs</automated>
    <fails_when>non-zero exit: a generated tree was not regenerated</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff` of `lancet.proto` shows only added lines.
    - Both generated trees contain the new fields; the engine round-trip test passes.
  </acceptance_criteria>
  <done>The engine emits the seeding diagnostics and per-chunk graph provenance on the wire.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gateway SSE maps the new fields explicitly</name>
  <files>gateway/internal/sse/sse.go, gateway/internal/sse/dto.go, gateway/internal/sse/sse_test.go</files>
  <read_first>
    - `go-guidelines.md`, with the version detected from `gateway/go.mod` (`go 1.25.0`); follow it up to 1.25
    - `gateway/internal/sse/sse.go:109-139` (populated map at ~119 and nil-meta default map at ~133), `gateway/internal/sse/dto.go` (retrieved-chunk DTO), `gateway/internal/sse/sse_test.go:602-706` (`GraphPromptFactCount: 7` assertions)
  </read_first>
  <behavior>
    - A `WorkflowCompletedEvent` with the new metadata values produces SSE JSON carrying `graph_seed_count`, `graph_path_found`, `graph_boosted_chunk_count`, `graph_degree_capped_count` and `graph_seed_document_ids` with those values, on both the success and failure payloads.
    - A nil metadata produces the zero defaults for all five keys.
    - A retrieved chunk with `GraphBoosted = true` serialises `"graph_boosted": true` in the snapshot's `retrieved_chunks`.
  </behavior>
  <action>
    Add the five keys to both maps in `sse.go`, using the generated getters `GetGraphSeedCount`, `GetGraphPathFound`, `GetGraphBoostedChunkCount`, `GetGraphDegreeCappedCount` and `GetGraphSeedDocumentIds`; in the default map use `uint32(0)`, `false` and `[]string{}`. Add `GraphBoosted bool` with json tag `graph_boosted` to the retrieved-chunk DTO in `dto.go` and map it from the proto. Extend `sse_test.go` in the style of the `GraphPromptFactCount` assertions. Write the tests first.
  </action>
  <verify>
    <automated>cd gateway && go test ./internal/sse/... && go vet ./internal/sse/...</automated>
    <fails_when>non-zero exit, "FAIL" in the output, or any vet diagnostic line</fails_when>
    <automated>cd gateway && go test ./...</automated>
    <fails_when>non-zero exit, or "FAIL" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - New assertions exist for all five metadata keys (success and failure payloads), the nil default, and `graph_boosted`.
  </acceptance_criteria>
  <done>The gateway forwards every new field explicitly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Harness captures the new fields into the journal</name>
  <files>eval/src/lancet_eval/client.py, eval/src/lancet_eval/journal.py, eval/src/lancet_eval/run.py, eval/src/lancet_eval/measure.py, eval/tests/test_wire_capture.py, eval/tests/fixtures/sse/graph_seed_fields.txt</files>
  <read_first>
    - `eval/src/lancet_eval/client.py:100-130` (`WorkflowMetadata` model, `extra="ignore"`, `graph_prompt_fact_count` at 113) and the retrieved-chunk model
    - `eval/src/lancet_eval/journal.py:20-73` (`WorkflowWireMeta` `extra="forbid"`, retrieved chunk model), `eval/src/lancet_eval/run.py:123-134`, `eval/src/lancet_eval/measure.py:264`
    - `eval/tests/test_wire_capture.py`, `eval/tests/fixtures/sse/wire_diagnostics.txt` (fixture format)
  </read_first>
  <behavior>
    - Parsing `graph_seed_fields.txt` yields a journal record whose `workflow_meta` carries the five values and whose retrieved chunk carries `graph_boosted: true`.
    - Loading the committed `eval/runs/2026-09-09-multihop_rag/journal.jsonl` still succeeds (fields default to None).
  </behavior>
  <action>
    Add `graph_seed_count: int | None = None`, `graph_path_found: bool | None = None`, `graph_boosted_chunk_count: int | None = None`, `graph_degree_capped_count: int | None = None` and `graph_seed_document_ids: list[str] | None = None` to `client.WorkflowMetadata` and `journal.WorkflowWireMeta`. Add `graph_boosted: bool | None = None` to the retrieved-chunk models in both. Map them in `run.py` and `measure.py` beside `graph_prompt_fact_count`. Create the SSE fixture from `wire_diagnostics.txt` with the new keys, and write the tests first.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_wire_capture.py eval/tests/test_client_contract.py eval/tests/test_journal.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the output</fails_when>
    <automated>uv run --project eval pytest -q</automated>
    <fails_when>non-zero exit, or a "failed" count above 0</fails_when>
  </verify>
  <acceptance_criteria>
    - The legacy-journal load test passes; the new-field capture test passes.
  </acceptance_criteria>
  <done>Every drive record journals the seeding diagnostics and per-chunk graph provenance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine → gateway → API clients | new public wire fields |
| wire → journal | recorded provenance |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.4.1-16-01 | Information disclosure | new wire fields | low | mitigate | Counts, booleans and document UUIDs only; no question text or entity names |
| T-06.3.4.1-16-02 | Tampering | wire compatibility | medium | mitigate | Additive tags only; old journals load (test); both generated trees regenerated |
| T-06.3.4.1-16-03 | Repudiation | silent field loss in gateway | medium | mitigate | Explicit mapping in both SSE maps with Go tests per key |
</threat_model>

<verification>
- `cargo test --manifest-path engine/Cargo.toml --locked && (cd gateway && go test ./...)`, `sh scripts/engine-test-targets.sh`, `uv run --project eval pytest -q` exit 0.
</verification>

<success_criteria>
Seed count, path found, boosted-chunk count, capped count, seed documents and per-chunk graph
provenance reach the journal for every record.
</success_criteria>

## Artifacts this phase produces

- Proto: `WorkflowMetadata.graph_seed_count=12`, `graph_path_found=13`, `graph_boosted_chunk_count=14`, `graph_degree_capped_count=15`, `graph_seed_document_ids=16`; `StructuredCitation.graph_boosted=10`.
- Span fields `lancet.workflow.graph_seed_count`, `lancet.workflow.graph_path_found`.
- Gateway SSE keys (same names) and DTO `GraphBoosted`.
- Harness fields on `client.WorkflowMetadata`, `journal.WorkflowWireMeta` and the retrieved-chunk models; fixture `eval/tests/fixtures/sse/graph_seed_fields.txt`.

<output>
Create `.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-16-SUMMARY.md` when done.
</output>
