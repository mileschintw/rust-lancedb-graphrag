# Phase 06.3.4.1: Retrieval diagnosis, index identity, and graph-yield repair - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-22
**Phase:** 06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair
**Areas discussed:** Index identity repair, RetrieveHybrid fix bar, Answer usability metric, Graph seed strategy, Presence/SC-4 denominator, Gate-miss policy, Run-of-record status, Drive sequencing & spend
**Context taken in:** Phases 06.3.3 and 06.3.4 (user-requested), plus the shared `06.3.1-CONTEXT.md`.

**Pre-discussion evidence presented:** graph-off answers are a median of 105 words and 106/150 contain the gold string (EM 0 is a format artifact); `RetrieveHybrid` median rose linearly from 5.0s to 29.0s; `33e774b` overwrote 06.3.3's derived budgets; four doc counts (346 / 346+1 alias / 350 / 367); layer 1 is already closed.

---

## Index identity repair

| Question | Options | Selected |
|---|---|---|
| How should the index hold only the mapped set? | Surgical reconcile / Full re-seed / Query-time filter | Surgical reconcile |
| Gold chunks missing for mapped docs? | Re-ingest those docs only / Record and exclude / Fix chunker-ingest root cause | Re-ingest those docs only |
| How does identity stay locked? | Preflight hard gate / Recorded check only / Preflight warn | Preflight hard gate |
| How is the post-reconcile index distinguished? | New generation + baseline record / Just the coverage table / You decide | New generation + baseline record |
| Map alias (0370301a → cc30a4d5)? | Collapse to canonical / Keep both / You decide | Collapse to canonical |
| "Gold-in-index subset" definition? | All gold chunks present / Any gold chunk present / Doc-level only | All gold chunks present |

**Notes:** A title-level check during the discussion found all 447 non-null questions have every gold doc in the subset, so doc-level coverage can't discriminate.

---

## RetrieveHybrid fix bar

| Question | Options | Selected |
|---|---|---|
| What counts as fixed for OI-02? | Root-cause fix + flatness proof / Restart workaround / Workaround now, fix later | Root-cause fix + flatness proof |
| The `33e774b` budget overwrite? | Re-derive after the fix / Revert to 06.3.3 values / Keep 30s/40s/60s | Re-derive after the fix (config.eval.toml restored to lancedb_path-only now) |
| Diagnostic sample size? | ~100×2 / ~50×2 / Full 447 | ~100×2 |
| "Answer citation constraints" failures (~15%)? | Diagnose, bounded / Out of scope / Fix fully | Diagnose, bounded |
| Retry policy? | No retries / 1 retry for transient errors only / Keep --retries 2 | No retries, record everything |
| Where the flatness proof runs? | Same path as eval harness / Engine-only soak / Both | Same path as eval harness |

---

## Answer usability metric

| Question | Options | Selected |
|---|---|---|
| How to measure SC-3 / column (e)? | Both short-answer + lenient metric / Lenient only / Short-answer only | Both |
| Where the short-answer instruction lives? | Production prompt, additive / Eval-only variant / Request-level proto option | Production prompt, additive |
| null_query questions? | Separate abstention metric / Exclude / Include in SC-3 | Separate abstention metric |
| SC-3 pass bar? | Pre-committed floor in thresholds.py / User sets now / Report-only | Pre-committed floor |
| Separating the format gain from the retrieval gain? | Lenient metric on 06.3.4 data first / A/B third arm / Don't separate | Lenient metric on old data first |
| Missing final-answer line? | Score as miss + separate count / Fall back to lenient / Exclude | Score as miss, count separately |

---

## Graph seed strategy

| Question | Options | Selected |
|---|---|---|
| What replaces whole-question cosine limit(1)? | Mention extraction + per-mention match / LLM query-time extraction / Whole-question top-k | Mention extraction + per-mention match |
| What the graph contributes? | Seed-to-seed paths + chunk boost / Path facts in prompt only / Filtered neighbourhoods | Seed-to-seed paths + chunk boost |
| Ship mode? | Production default, old path removed / Config flag / Eval-only until proven | Production default, old path removed |
| Traversal cost bound? | Degree cap + result budget / Hop=1 only / You decide | Degree cap + result budget |
| Column (d) definition? | Seed sourced from a gold doc / Any seed / Path touches gold chunk | Seed sourced from a gold doc |
| D-31 canaries? | Re-validate + add path canary / Keep as-is / You decide | Re-validate + add path canary |

---

## Presence/SC-4 denominator

| Question | Options | Selected |
|---|---|---|
| Meaning of presence? | Keep D-06 + add composition / Redefine as either / Composition only | Keep D-06 + add composition |
| Denominator? | Headline on gold-in-index, also report all / All answerable pairs / Gold-in-index only | Headline on gold-in-index, also report all |

---

## Gate-miss policy

| Question | Options | Selected |
|---|---|---|
| SC-4/SC-5 still miss after the fixes? | One bounded iteration, then disclose / Iterate until met / Dated floor amendment | One bounded iteration, then disclose |
| SC-3 misses: does OI-01 still start? | Hard gate, stop and ask / Proceed in parallel / Proceed regardless | Hard gate: stop and ask |

---

## Run-of-record status

| Question | Options | Selected |
|---|---|---|
| What run does 6.4 cite? | Final ~100×2 drive / Separate full 500×2 / Diagnostic only, 6.4 decides | Final ~100×2 drive is the run of record |
| Recording non-comparability? | SUPERSEDED marker + docs addendum / Phase artifacts only / You decide | SUPERSEDED marker + non-comparability note |

---

## Drive sequencing & spend

| Question | Options | Selected |
|---|---|---|
| Paid drive sequencing? | Free first, then 2 paid drives / One per layer / Single final drive | Free first, then 2 paid drives |
| Setting spend caps? | checkpoint:decision per paid drive / Pre-authorise now / One phase cap | checkpoint:decision per paid drive |

---

## Claude's Discretion

Mention-extraction heuristic and thresholds; degree-cap and fact-count values (with the derivation recorded); chunk-boost mechanics; final-answer line format; sample selection; proposed SC-3 floor and stage-cap figures (the user confirms both); OI-02 profiling approach.

## Deferred Ideas

Full re-seed; query-time LLM entity extraction; a separate 500×2 run of record; judged and human calibration; cleanup of the leftover worktree.
