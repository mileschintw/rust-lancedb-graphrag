# 06.3.4.1-03: OI-02 RetrieveHybrid Profile

Read-only, API-free `retrieval_soak` (release build) drove 300 iterations per arm against the
live `./data/lancedb-eval` store (`LANCET_ENV=eval`, engine and gateway both stopped for the
entire run). Query vectors/text: a SplitMix64-seeded (`seed=5494759071201636403`, recorded on
every header line) sample of 300 `nodes` rows for R/RP and 300 `entities` rows for RG/RGT/W (an
entity's own `name_vector` is used as the query embedding for the graph-touching arms, which
guarantees `attempt_graph_augmentation`'s seed match trivially succeeds — see
`retrieval_soak.rs`'s `Arm::uses_entity_seed` doc comment). All five headers report
`build_profile: "release"` and `collector_reachable: false` (no local OTel collector running,
as expected). Process sampling: PowerShell `Get-Process` every 1s (not the plan's stated 5s —
see Methodology Note below) into `soak/<arm>-proc.csv`.

## Per-arm verdict table

| Arm | n | Flatness verdict | Slope prong | p-value | Slope (ms/query) | Window prong | Window delta (ms) | Slice medians (ms, 50-iter) |
|---|---|---|---|---|---|---|---|---|
| R | 300 | **decay_present** (fails) | fired | 0.0020 | 0.0203 | not fired | 32.9 | 98.2, 97.3, 98.9, 97.2, 96.8, 110.4 |
| RP | 300 | **decay_present** (fails) | fired | 0.0130 | 0.0114 | not fired | 7.6 | 84.1, 83.6, 84.0, 86.6, 83.2, 87.3 |
| RG | 300 | **flat** (passes) | not fired | 0.0668 | 0.0083 | not fired | 5.3 | 74.3, 73.4, 73.6, 76.0, 75.1, 75.0 |
| RGT | 300 | **flat** (passes) | not fired | 0.5596 | -0.0027 | not fired | 2.5 | 74.3, 75.6, 74.2, 74.9, 73.6, 74.4 |
| W | 300 | **flat** (passes) | not fired | 0.0554 | 0.0096 | not fired | 6.0 | 74.5, 74.4, 74.6, 76.2, 75.4, 77.2 |

`censored_count=0` and `unusable_dropped_count=0` on every arm — no RetrieveHybrid failures,
no missing timings. `COMMITTED_THRESHOLDS` and `decay.py` were not edited to reach any of this
(`git diff --quiet -- eval/src/lancet_eval/decay.py eval/src/lancet_eval/thresholds.py` passes).

**R and RP formally fail the machine flatness verdict, but not for a practically meaningful
reason.** Both fire only the Mann-Kendall slope prong (window prong never fires on any arm —
every window delta is 2.5-32.9ms against the 500ms materiality threshold). At n=300,
Mann-Kendall has enough statistical power to flag a slope of 0.02ms/query (R) or 0.011ms/query
(RP) as "significant" (p=0.002, p=0.013) even though the total drift implied over the whole run
is 6.1ms (R) and 3.4ms (RP) — smaller than the noise band visible in the slice medians
themselves (which swing by ~2-13ms between consecutive 50-iteration blocks with no consistent
direction). This is large-n statistical sensitivity, not decay: applying the same instrument
production-side (paid drive 1, 06.3.4.1-12) at n≈500 records per arm should be read the same
way — a "fired" slope prong with a sub-millisecond-per-query slope is not what OI-02 is looking
for.

RGT's graph augmentation timed out on **300/300 iterations** (`graph_ms` observed 200.3-215.1ms
against the 200ms timeout — this store's graph seed lookup + neighborhood fetch + two Cypher
executions consistently take just over 200ms), so RGT is not a weak reproduction of the
segment-1 abandoned-future condition — every iteration genuinely abandons a real in-flight
`attempt_graph_augmentation` future exactly as `tokio::time::timeout` drops it in production.

## Named accumulator

**Inconclusive at this soak's scale.** No arm reproduces RetrieveHybrid latency growth of any
practical magnitude — R and RP's statistically-significant slopes amount to single-digit
milliseconds of total drift over 300 queries, RG/RGT/W show no significant slope at all, and no
arm's window prong fires. This does not match 06.3.4's observed growth (RetrieveHybrid median
5.0s -> 29.3s over seven 50-record slices) or 06.3.3 segment 1's growth (2.97s -> 10.83s over
four 40-record slices) in either direction or magnitude.

### What was reproduced

- **A real, deterministic abandoned-future condition** (RGT, 300/300 timeouts) with zero
  measurable effect on `alive_tasks`/`global_queue_depth` (both flat at 3 tasks / 0 queue depth
  across every sample, every arm) and no measurable effect on subsequent `RetrieveHybrid`
  iterations' `retrieve_ms`.
- **Per-query `open_table`/`checkout` state**: flat and tiny throughout (`open_table_ms`
  ~1.0-2.1ms, `checkout_ms` ~0.12-0.23ms, all five arms, ordinal 1 through 300 — no drift).
- **Process-level counters** (working set, private bytes, handle count, thread count): all four
  rise sharply in the first 2-3 process samples (~1-3s of wall time — process/runtime/thread-pool
  warm-up: `target/release/retrieval_soak.exe`'s own startup, tokio worker + Lance
  `spawn_cpu`/blocking-pool thread spin-up, first LanceDB dataset opens), then **plateau** for
  the remainder of the run. RGT (guaranteed abandonment on every iteration) plateaus at 46
  threads / ~233 handles / ~187MB working set after its first ~3 samples and stays there for the
  following ~105 one-second samples. RG, R, RP and W show the same shape.

### What was not reproduced, and why that is a real discriminator, not a soak defect

- **The ~15-20x baseline gap between this soak (release, ~55-110ms median `retrieve_ms`) and
  production (06.3.3 segment 2's flat baseline: ~1.6-1.7s; 06.3.4's starting point: ~5.0s) is
  substantially a build-profile confound, not evidence the soak under-reproduces the store's real
  cost.** `06.3.3-BUDGETS.md`'s own "Engine startup proof" section records the only documented
  live-binary launch command for that phase: `cargo run --manifest-path engine/Cargo.toml
  --locked --bin engine` — no `--release` flag, i.e. a **debug** build. A supplementary
  exploratory run of this same soak bin in debug mode (`cargo run` without `--release`, arm R,
  50 iterations, not part of the committed `soak/` artifact set —
  `header.build_profile: "debug"`) measured `retrieve_ms` at **377-529ms** (median 439ms) against
  the release build's 76-136ms range for the same arm — roughly a 4-5x gap from the optimization
  level alone, with **no growth** over those 50 debug-mode iterations either (first-5/last-5
  medians ~445ms/~450ms). That still leaves an unexplained ~3-4x gap to production's observed
  1.6-5s baseline (real network/gRPC/HTTP round-trip overhead absent from this in-process soak,
  a possibly warmer-vs-colder store state, and production's per-request node construction are
  candidate remaining contributors, not measured here).
- **Sustained, unbounded resource growth over hundreds of queries at soak speed.** Every
  process-level counter plateaus within a few seconds of warm-up and does not continue climbing
  through the remaining ~15-140 seconds of wall time each arm's 300 iterations took (RG/RGT/W
  ran 130-140 one-second samples after warm-up with flat readings). If class 2/3 (allocator or
  Lance session-cache growth) is the true accumulator, either it requires materially more
  iterations/wall-time than 300 release-speed queries provide to become visible, or it requires
  concurrent/production request patterns this single-threaded sequential soak does not generate.
- **The harness/gateway path.** This soak never leaves the engine process — no gRPC, no SSE
  streaming, no gateway checkpoint dispatch. A slope that depends on that path (candidate 5,
  lowest-ranked in RESEARCH, and any HTTP/connection-pool-side accumulation) is invisible here by
  construction.

### Discriminator readings against RESEARCH's ranked candidates

| # | Candidate | Discriminator | Reading |
|---|---|---|---|
| 1 | Abandoned CPU work outliving a timed-out/cancelled future | `alive_tasks`/`global_queue_depth` vs ordinal; thread count vs time | **Not supported at this scale.** Flat on the main tokio runtime despite 300/300 real abandonments in RGT. Thread count (which would show Lance's `spawn_cpu` blocking-pool growth, per RESEARCH's own note that `alive_tasks`/`global_queue_depth` cannot see that pool) rises during warm-up then plateaus — no continued growth through ~105-140 samples post-warm-up. |
| 2 | Allocator/memory degradation from large per-query allocations | Working set / private bytes vs ordinal (external sampler) | **Inconclusive, not ruled out.** All arms show a real working-set rise (~10MB -> ~150-200MB) but it happens in the first few seconds (warm-up shape) and plateaus; no arm shows continued growth proportional to query count. |
| 3 | Lance session cache growth | Soak loop with default `Session` (this soak never opens a second explicit session) | **Inconclusive, not exercised as a contrast.** The soak always uses the default session (matching production); no small-vs-default-session comparison was run this pass. |
| 4 | Per-query `open_table` + `checkout` state | Sub-stage timing of `open_table`, `checkout` | **Not supported.** Both are flat and sub-2ms across all 1500 combined iterations. |
| 5 | PostgreSQL/gateway backpressure | N/A to this soak | Sanity-only per RESEARCH; not exercised (no gateway/Postgres in this soak). |

## Header/build metadata (per official release-mode run)

| Arm | build_profile | collector_reachable | binary_mtime | pid |
|---|---|---|---|---|
| R | release | false | 1790229541 (unix s) | varies per launch |
| RP | release | false | 1790229541 | varies |
| RG | release | false | 1790229541 | varies |
| RGT | release | false | 1790229541 | varies |
| W | release | false | 1790229541 | varies |

All five share the same `retrieval_soak.exe` build (`binary_mtime_unix_secs: 1790229541`), built
once before the five sequential runs.

## Methodology note (deviation from the plan's literal "every 5s")

The plan's Task 2 action says to sample the soak process "every 5 s". At soak speed
(55-140ms/iteration, 300 iterations complete in 15-140s depending on arm) a 5s cadence would
have produced only 3-28 samples per arm — too few to see the warm-up/plateau shape this profile
relies on. Samples were taken every 1s instead (still satisfying "sample ... externally"; the
5s figure in the plan was written against production's much slower per-query cost, where 5s
easily captures many queries per sample). 42-140 samples were collected per arm. Recorded here
as a deviation, not silently changed.

## Conclusion

The accumulator class OI-02 is investigating is **not named by this pass** — no arm reproduces
practically meaningful RetrieveHybrid growth, and process-level counters plateau rather than
climb. The pass does, however, rule out per-query `open_table`/`checkout` state (class 4) and
finds no evidence of unbounded task/queue accumulation on the main tokio runtime even under
guaranteed real abandonment (weakening, not confirming, class 1 as tested — with the explicit
caveat that Lance's own blocking pool is invisible to those two counters and only partially
visible via thread count, which itself plateaus). It surfaces a concrete, evidenced alternative
explanation for a large share of the previously-unexplained baseline gap between 06.3.3/06.3.4's
production numbers and this soak's release-mode numbers: those drives' only documented engine
launch command omits `--release`. That is a build-profile confound in the *comparison*, not
proof that growth itself doesn't happen in production — a debug-mode soak run showed a higher
but still flat baseline over 50 iterations, not growth.

This profile does not attempt to decide between the checkpoint's three options; it supplies the
evidence for that decision (Task 3).

## Extended profiling (2026-09-24)

The checkpoint's first presentation was inconclusive. The user chose **extend-profiling**
(not fix-named-class or mimalloc) and authorized two additions before Task 3 is re-presented,
with no engine fix written in this plan: (1) a debug-build soak at the same full n=300 scale as
the release runs, on the theory that debug's build-profile confound (documented above) might
also be masking a real accumulator that needs more per-query wall time to surface; (2) a
gateway-in-the-loop arm to measure the remaining ~3-4x gap to production, built only if it stays
a thin driver script against the existing harness with no paid provider call — otherwise
documented as a finding, not improvised. `CARGO_BUILD_JOBS=4` was used throughout (the
machine's full parallelism was previously shown to exhaust resources); nothing else ran on the
machine during either addition, and no soak, engine or gateway process was left running at the
end (`tasklist` confirmed clean after each run and again at the end of this session).

### 1. Debug-build soak at full scale

Built once (`cargo build --manifest-path engine/Cargo.toml --locked --bin retrieval_soak`, no
`--release`, 22.5s) and run directly from `engine/target/debug/retrieval_soak.exe` (not via
`cargo run`, so the sampled PID is the soak process itself, not a `cargo` wrapper). All four
arms — **R, RG, RGT and W** (RP omitted; the user's floor was "R and W at minimum, RG/RGT if
time allows without a large cost", and RG/RGT fit inside a fifteen-minute wall-clock budget on
top of R and W) — ran at the full 300 iterations, same seed (`5494759071201636403`), same store
(`./data/lancedb-eval`, `LANCET_ENV=eval`), same `--graph-timeout-ms 200` default, engine and
gateway stopped, one fresh process per arm, saved to `soak/debug/<arm>.jsonl` +
`soak/debug/<arm>-proc.csv`. Process sampling stayed at 1s cadence (same deviation from the
plan's literal "every 5s" already recorded above, for the same reason: debug iterations still
complete in well under 5s each). All four headers report `build_profile: "debug"` and the same
`binary_mtime_unix_secs: 1790230694` — one debug binary build, four sequential runs, matching
the release methodology's "one binary, four/five sequential runs" pattern.

**Wall-clock cost, for the record:** R ≈ 150s, RGT ≈ 210s, RG ≈ 380s, W ≈ 430s (roughly
4-6x each arm's release wall-clock time). This is itself a partial test of the original
profile's own caveat that "class 2/3 ... requires materially more iterations/wall-time than 300
release-speed queries provide to become visible" — the debug run gives each of the 300
iterations several times longer to accumulate state, and no arm still shows growth (below).

| Arm | n | Flatness verdict | Slope prong | p-value | Slope (ms/query) | Total drift over n=300 (ms) | Window prong | Window delta (ms) | Slice medians (ms, 50-iter) |
|---|---|---|---|---|---|---|---|---|---|
| R | 300 | **flat** (passes) | not fired | 0.0000 | -0.5450 | -163.5 | not fired | -154.0 | 578.5, 581.2, 580.8, 591.7, 459.4, 457.3 |
| RG | 300 | **flat** (passes) | not fired | 0.7388 | -0.0051 | -1.5 | not fired | 22.9 | 288.0, 291.7, 279.9, 290.2, 291.8, 284.5 |
| RGT | 300 | **flat** (passes) | not fired | 0.0961 | 0.0293 | 8.8 | not fired | 24.6 | 290.0, 290.1, 285.6, 291.5, 292.3, 294.3 |
| W | 300 | **decay_present** (fails) | fired | 0.0001 | 0.0638 | 19.1 | not fired | 24.8 | 285.6, 285.2, 281.8, 297.8, 287.6, 304.3 |

`censored_count=0` and `unusable_dropped_count=0` on all four. `git diff --quiet --exit-code --
eval/src/lancet_eval/decay.py eval/src/lancet_eval/thresholds.py` still passes — neither file
was touched to reach this table.

**R's significant *negative* slope (getting faster, not slower) is the trend prong's directional
filter working as designed** — the Mann-Kendall/Theil-Sen trend prong only fires on a growing
(decaying) slope, and a p≈0 downward slope correctly reads `prong_slope_fired: False`. The
slice medians show why: a step down from ~580ms to ~458ms around slice 4/5 (iteration ~200),
consistent with a one-time OS-level effect (page cache, disk read-ahead, or Windows working-set
trim) rather than a query-count-proportional trend — the same warm-up-shaped, not
growth-shaped, pattern the release pass found in process counters, here visible in latency
itself for the first time. **W repeats the exact release-pass pattern of R/RP:** the slope
prong fires at p=0.0001, but the total drift it represents is 19.1ms over 300 queries —
smaller than the noise band between adjacent 50-iteration slice medians (281.8 to 304.3ms, a
22.5ms swing with no consistent direction). This is the same large-n statistical-sensitivity
reading as the release pass's R/RP arms, not evidence of decay.

**Process counters (working set, handle count, thread count) plateau in debug exactly as they
did in release**, just at higher absolute values (debug's allocator and larger binary bring a
higher working-set floor). Excluding the final ~2 samples of each run (process teardown —
working set/threads/handles drop sharply as the process exits, a sampling artifact of the 1.5s
grace window after the soak PID disappears, not an in-run signal) and comparing the first-half
vs second-half median of the remaining "stable" window:

| Arm | Stable samples | Working-set median (MB) | First-half median (MB) | Second-half median (MB) | Delta (MB) |
|---|---|---|---|---|---|
| R | 168 | 216.4 | 214.7 | 217.2 | +2.5 |
| RG | 389 | 261.3 | 258.5 | 265.7 | +7.3 |
| RGT | 229 | 240.4 | 242.6 | 238.8 | -3.9 |
| W | 393 | 258.3 | 259.9 | 256.6 | -3.3 |

No arm shows a one-directional, sustained climb — RG's +7.3MB is the largest, well inside the
run's own min-max range (200.6-314.2MB) driven by ordinary allocator fluctuation, not a trend.
Debug gave every arm 4-6x more wall-clock time for a slow accumulator to become visible and
none did.

**Baseline comparison, now at matched n=300 on both sides (superseding the earlier 50-iteration
exploratory debug run, which this run confirms was not a small-sample fluke):**

| Arm | Release median (retrieve_ms unless noted) | Debug median (retrieve_ms unless noted) | Ratio |
|---|---|---|---|
| R | 98.2 ms | 545.5 ms | 5.6x |
| RG | 74.3 ms | 288.0 ms | 3.9x |
| RGT | 74.3 ms | 291.0 ms | 3.9x |
| W (retrieve_ms) | 74.5 ms | 289.6 ms | 3.9x |
| W (full_workflow_ms, debug-only — no release equivalent recorded) | — | 1287.1 ms | — |

(R's retrieve_ms is not directly comparable across arms within a build profile — R/RP draw
seeded rows from `nodes`, RG/RGT/W from `entities`; different query content drives different
dense/BM25 cost. The ratio column compares the *same* arm's release vs debug run, which holds
query content fixed.) The 3.9-5.6x ratio is consistent with the single-arm 50-iteration
exploratory run's ~4-5x estimate — that earlier number was not a fluke of small n.

### 2. Free evidence from the existing production journal (zero additional cost)

Before attempting a gateway-in-the-loop build, the committed 06.3.4 run journal
(`eval/runs/2026-09-09-multihop_rag/journal.jsonl`, 658 records — the same run
`06.3.4-OBSERVATION-THROUGHPUT-PLATEAU.md`'s 5.0s→29.3s RetrieveHybrid figure comes from) was
re-read for a question this profile can answer at no soak cost: for each record, does
`RunRecord.duration_ms` (the harness's own measured wall time, `journal.py`) minus the sum of
`node_timings[].duration_ms` (the engine's own per-node measured time, which already includes
`RetrieveHybrid`, `ExtractGraphContext`, `AssemblePrompt`, `GenerateAnswer`, `ReformulateQuery`)
grow over the run? If it does, that gap — everything the harness clock sees that no engine node
claims — is exactly the SSE/gRPC/harness-transport overhead candidate 5 predicts. If it stays
flat while `RetrieveHybrid`'s own node timing grows, the growth is already fully inside the
engine's own instrumentation before any transport is added, which rules candidate 5 out for
*this* growth episode specifically (not for the general baseline gap — see the caveat below).

50-record slice medians, in journal order:

| Slice | n | RetrieveHybrid node-time median (ms) | Wall-minus-nodes gap median (ms) |
|---|---|---|---|
| 0 | 50 | 4910.5 | 5.0 |
| 1 | 50 | 10441.0 | 4.0 |
| 2 | 50 | 13947.0 | 4.5 |
| 3 | 50 | 16785.5 | 4.0 |
| 4 | 50 | 21254.0 | 5.0 |
| 5 | 50 | 24626.0 | 5.0 |
| 6 | 50 | 28960.0 | 5.0 |
| 7 | 50 | 29266.5 | 30157.0 |
| 8 | 50 | 30814.0 | 30295.5 |
| 9-13 | 208 | unavailable (RetrieveHybrid censored) | ~30300-30580 |

Through slices 0-6 (RetrieveHybrid still completing and reported), the wall-minus-nodes gap
sits at a flat 4-5ms regardless of how large `RetrieveHybrid` itself has grown (4.9s to 29.0s) —
**the harness/gateway/transport overhead this metric measures is negligible and does not track
the growth.** From slice 7 onward the gap jumps to ~30s, but this is not transport growth
either: `RetrieveHybrid` becomes `None` (censored/timed out) in those slices, so the "gap" stops
measuring transport and starts measuring the timeout budget consumed by a node that no longer
reports a completed timing — a different phenomenon, already covered by the release pass's
censoring handling (Pitfall 6), not a new discriminator reading.

**Scoped conclusion:** this rules out candidate 5 (gateway/harness backpressure) as an
explanation for *this specific, already-observed* RetrieveHybrid growth episode — the growth is
fully present in the engine's own self-measured node timing before transport is added, so a
transport-side accumulator cannot be what produced it. It does **not** by itself explain the
remaining debug-soak-to-production baseline gap (below) — `node_timings` for `ExtractGraphContext`
already includes real embedding-provider network time in production (absent from this soak's
`FixedEmbeddingPort`), and that is inside "node time", not inside the "gap" this metric isolates.

### 3. Gateway-in-the-loop arm: documented, not built

This was not attempted. Confirmed by reading the actual provider-client contract
(`engine/src/config.rs`, `engine/src/generation/openrouter.rs`,
`engine/src/generation/tests.rs`) rather than assuming: the engine validates the configured
generation model against `model_metadata_endpoint` (`/models`) at startup
(`config.rs:333` `validate_against_provider`, called from `main.rs:70` and `main.rs:127`,
*before* the workflow ever runs), requiring the response to advertise `response_format`/
`json_schema` in `supported_parameters` — startup fails closed otherwise. The chat completion
call itself (`generation/openrouter.rs:611-623`) sends `response_format: {type: "json_schema",
json_schema: {strict: true, ...}}` and requires a schema-conformant reply carrying `finish_reason`
and `usage`, matching the extensive fixture set already required just to unit-test this client
(`generation/tests.rs`, 2000+ lines of hand-built OpenRouter-shaped JSON fixtures). Reproducing
that contract correctly for a live process, not a test fixture, is what "more than a thin driver
script" concretely means here. A correct build would need:

- A stub HTTP server implementing three endpoints, not one: `/models` (model metadata with
  `supported_parameters` including `response_format`/`json_schema` for the configured model
  id — required at engine startup, before any query runs), `/embeddings` (returning a
  2048-dimension vector, matching `voyage-4-large`'s configured `dimensions`), and
  `/chat/completions` (returning a strict-schema-valid `ModelOutput` JSON body with
  `finish_reason` and `usage` fields the client parses).
- Overriding all three `LANCET_OPENROUTER__*_ENDPOINT` env vars (already confirmed
  override-able — `config.rs:827-849`) to point at the stub, plus a dummy API key, so that any
  endpoint the stub misses fails closed with 401 rather than silently falling through to the
  real OpenRouter API and spending real money.
- A precondition check that `staged_documents_v2` has 0 rows before starting this second engine
  instance — a non-empty table replays ingestion at startup (`main.rs:114-121`, RESEARCH
  Pitfall 14), which would call the (stubbed, but still real-request-shaped) extraction path and
  is out of scope for a latency-only measurement.
- A running PostgreSQL instance, because the gateway writes checkpoint rows to the eval schema
  on every query — running this arm would move the D-22 checkpoint baseline the same way a real
  drive does, which needs disclosing, not treating as free.
- A thin SSE client driver loop hitting the gateway's real ingress N times and recording
  round-trip latency — this piece alone would be the "thin driver script" the checkpoint
  authorized; the four items above are what make the whole arm not thin.

None of this was built. The zero-cost production-journal analysis in §2 is the substitute this
pass offers for "measure the remaining gap": it positively rules out harness/transport overhead
as a source of the *already-observed* growth, but it cannot speak to whether a *fresh*
gateway-in-the-loop run — with concurrent request patterns, a second live engine process, and
real provider network calls — would surface an accumulator this soak's single-threaded,
provider-free design cannot reproduce. That specific question (RESEARCH candidate 5, and any
new HTTP/connection-pool-side accumulation) remains open, exactly as the original pass said.

### Updated Named accumulator conclusion (2026-09-24)

**Still not named by measurement — now on two independent build profiles instead of one.**
Debug, run at the same n=300 scale and given 4-6x more wall-clock time per arm than release,
reproduces the same shape the release pass found: R/W show a statistically-significant slope
too small to matter in practice (debug W: 19.1ms total drift over 300 queries; release R/RP:
6.1ms/3.4ms), RG/RGT are flat with no significant slope, and process counters plateau rather
than climb on both profiles. This weakens the original profile's own hedge that class 2/3 might
need more wall-time than 300 release-speed queries provide — debug's 300 queries take several
times longer in wall-clock and still show no growth, though "more time" and "more queries" are
not identical variables, and this still does not test concurrent/production request patterns.

**What newly moved:** the release-vs-production baseline gap is now partially attributed rather
than wholly unexplained. Build profile alone (release → debug, matched n=300, same arms, same
seed) accounts for 3.9-5.6x. The production-journal analysis in §2 positively rules out
harness/gateway transport overhead as a contributor to the specific growth episode already on
record (flat 4-5ms gap while RetrieveHybrid's own node time grows 4.9s→29.0s). What remains
unattributed — real provider network latency for embedding/generation (not exercised by this
soak's deterministic stand-ins) and production's per-request node construction and concurrent
load pattern (not exercised by this soak's single-threaded sequential design) — is unchanged
from the original pass, not newly ruled in or out.

**No arm on either build profile shows the sustained, query-count-proportional growth OI-02 is
investigating.** No discriminator moved from "inconclusive" to "supports" or "rules out" for
candidates 2 (allocator) or 3 (Lance session cache) specifically — process counters plateau on
both profiles, which is suggestive but not conclusive against slow accumulation, since neither
soak run generates the concurrent/production request pattern RESEARCH itself flags as a
condition those candidates might require. Candidate 1 (abandoned CPU work) stays weakened, not
confirmed, for the same reasons the original pass gave. Candidate 5 (gateway/harness
backpressure) is now more strongly excluded than before, specifically for the already-observed
growth episode, via the zero-cost journal analysis in §2 — not via a new gateway-in-the-loop
run, which was not built (§3).

This section does not select a checkpoint option. Task 3 is re-presented below with the
combined release + debug evidence and the gateway-in-the-loop finding.

## Long-duration soak (2026-09-24)

The checkpoint's second presentation (release + debug, matched n=300, plus the zero-cost
journal analysis) was still inconclusive. The user chose **extend-profiling: long-duration
soak** again (not fix-named-class or mimalloc), explicitly ruling out a restart workaround, and
authorized one addition before Task 3 is re-presented a third time, with no engine fix written:
a **debug-build arm-W soak paced to approximate production's wall-clock cadence, run for
approximately 2.5 hours** — the one dimension the release and debug passes above, at 15-140s and
150-430s respectively, could not test. `CARGO_BUILD_JOBS=4` was used for every build. Nothing
else ran on the machine during the soak itself: the `lancet-collector` OTel-collector-contrib
Docker container (part of this repo's standing observability stack, unrelated to this session)
was found reachable on `127.0.0.1:4317` before this pass started — a departure from every prior
pass's `collector_reachable: false` — and was deliberately stopped for the run's duration to
preserve methodology parity with the release and debug passes above, then restarted afterward.
No soak, engine, gateway, or sampler process was left running at the end (`Get-Process`/
`Get-NetTCPConnection` confirmed clean after the run).

### 1. Pacing derivation

The idle gap inserted between iterations approximates the real per-question time production
spends on everything *other* than `RetrieveHybrid` itself — reformulation, graph context
extraction (a real embedding-provider network call), prompt assembly, and answer generation (a
real generation-provider network call) — plus harness/transport overhead. The committed
06.3.4 run journal (`eval/runs/2026-09-09-multihop_rag/journal.jsonl`, 658 records) has no
separate LLM-judge timing — judging runs offline via `lancet-eval score`, never during the
drive itself — so this derivation captures generation and embedding-provider time, not judge
time; the checkpoint's "generation/judge time" framing is read here as "everything besides
`RetrieveHybrid`" for lack of a judge component to measure in a drive journal.

For each record with a completed `RetrieveHybrid` node timing and no top-level `error_type`
(356 of 658; the other 302 have no `RetrieveHybrid` entry at all — the same censoring past
slice 6 documented in "Free evidence from the existing production journal" above; 0 records
carried a non-null `error_type`), the gap is `duration_ms - RetrieveHybrid_node_duration_ms`
(the harness's own measured wall time for the record, minus just the `RetrieveHybrid` node's
own measured time — deliberately not `duration_ms` minus the *sum* of all five node timings,
which is the different, much smaller ~4-5ms "transport-only" gap computed earlier in this
document for a different purpose).

| Statistic | Value |
|---|---|
| Usable records (completed RetrieveHybrid, no error_type) | 356 / 658 |
| Gap median | 18653.5 ms |
| Gap IQR (Q1 / Q3) | 7793.5 ms / 45874.0 ms |
| Gap first-20-usable-records median | 11611.5 ms |
| Gap last-20-usable-records median | 33701.0 ms |

The gap itself grows across the drive (confounded with `RetrieveHybrid`'s own growth, since
`ExtractGraphContext`/`GenerateAnswer` cost was not independently measured for trend in this
pass) — the wide IQR reflects that, not measurement noise. The run's overall median (not the
first- or last-slice value) was used as a deliberate middle-of-the-road choice, disclosed rather
than picked to make a target number come out nicer.

The soak bin's own arm-W iteration already includes some non-`RetrieveHybrid` work (`AssemblePrompt`,
`ExtractGraphContext`, and `GenerateAnswer` — all near-instant here since they call the in-bin
`FixedEmbeddingPort`/`FixedGenerator`, never a real provider). To avoid double-counting that
already-included overhead, it was measured from the prior round's debug arm-W pass
(`full_workflow_ms` median 1287.1ms minus `retrieve_ms` median 289.6ms = 997.5ms) and subtracted:

```
pace_ms = gap_median - already_included_overhead
        = 18653.5 - 997.5
        = 17656 ms
```

### 2. Run parameters and sample coverage

```
LANCET_ENV=eval ./engine/target/debug/retrieval_soak.exe \
  --arm W --iterations 1000 --pace-ms 17656 --duration-secs 9000 \
  --lancedb-path ./data/lancedb-eval
```

`--iterations 1000` requests 1000 distinct seeded `entities` rows up front (the entities table
has 27,971 rows total — `inspect_lancedb --graph-population` confirmed this before the run) —
**the sample size was raised, not cycled**, per the checkpoint's instruction to document which.
`--duration-secs 9000` (2.5h) overrode `--iterations` as the loop's stop condition. The run
completed 474 iterations against a 1000-row sample (`cycles: 1`, `cycled: false` in the
run-completion stderr summary) — the sample comfortably covered the run with margin to spare;
no row was queried twice. Total wall-clock elapsed: 8995.3s (2h 29m 55s), essentially exactly
the 9000s target (the loop's own deadline check has second-level granularity against the last
iteration's own duration, hence the ~5s undershoot). Binary: `engine/target/debug/retrieval_soak.exe`,
built from commit `df10c98b` (the session-cache-logging commit below), `build_profile: "debug"`,
`collector_reachable: false`.

This pass also added a discriminator the original profile flagged as "not exercised as a
contrast": `lance::session::Session::size_bytes()` and `approx_num_items()`, read through the
already-open `nodes` table's dataset handle (`Table::dataset()` -> `DatasetConsistencyWrapper::get()`
-> `Dataset::session()`, all public `lancedb` 0.31.0 / `lance` 1.0.4 API — no new plumbing) and
logged on every iteration line, across all five arms (committed as `feat(06.3.4.1-03): log lance
session cache size per soak iteration`, `df10c98b`). `size_bytes()` performs a full `deep_size_of`
walk over the session's caches — its own doc comment: "not trivial to compute" — but this is
cheap relative to the soak's ~18s-per-iteration pace and is the documented public API for exactly
this purpose.

### 3. Flatness verdict (unmodified `flatness_verdict`/`analyze_decay`)

`git diff --quiet --exit-code -- eval/src/lancet_eval/decay.py eval/src/lancet_eval/thresholds.py`
exits 0 — neither file was touched to reach this verdict.

| n | Verdict | Slope prong | p-value | Slope (ms/query) | Total implied drift over n (ms) | Window prong | Window delta (ms) |
|---|---|---|---|---|---|---|---|
| 474 | **decay_present** (fails) | fired | 3.11e-08 | 0.05621 | 26.64 | not fired | -35.72 |

`censored_count=0`, `unusable_dropped_count=0` — every one of the 474 iterations produced a
usable `RetrieveHybrid` timing (this soak arm never times out or errors; it is not RGT).

**This is the same large-n statistical-sensitivity pattern seen in every prior pass, now at an
even larger n.** The trend prong's p-value (3.11e-08) is far smaller than the release pass's
R/RP arms (p=0.002/0.013) or the debug pass's W arm (p=0.0001) — expected, since Mann-Kendall's
power grows with n and this run has the largest n of any arm tested across all three passes
(474 vs 300). But the *practical* magnitude shrank, not grew: total implied drift is 26.64ms
over the whole 474-iteration, ~2.5-hour run — smaller than the debug pass's W-arm drift (19.1ms
over 300 iterations, a comparable per-iteration rate) and two orders of magnitude below the
500ms window-prong materiality threshold (which correctly did not fire: window delta is -35.7ms,
i.e. the run's tail was very slightly *faster* than its head, not slower). Retrieval or
full-pipeline latency did not trend upward at any timescale this run exercised.

### 4. 15-minute wall-clock slices

Ten full 15-minute slices span the 474 iterations exactly (48+47+47+48+47+48+47+47+47+48=474);
`start_min` is minutes since the first iteration. `retrieve_ms`/`full_workflow_ms`/`alive_tasks`
come from the soak JSONL; `session_size_bytes`/`session_approx_num_items` are the new per-iteration
Lance session-cache reads.

| Slice | Start (min) | n | retrieve_ms median | full_workflow_ms median | alive_tasks | session_size_bytes median (MB) | session_approx_num_items median |
|---|---|---|---|---|---|---|---|
| 0 | 0 | 48 | 303.5 | 1304.6 | 3 | 17.37 | 7718 |
| 1 | 15 | 47 | 296.4 | 1328.8 | 3 | 17.45 | 7868 |
| 2 | 30 | 47 | 271.8 | 1230.2 | 3 | 17.50 | 7977 |
| 3 | 45 | 48 | 279.3 | 1215.1 | 3 | 17.53 | 8052 |
| 4 | 60 | 47 | 282.8 | 1233.1 | 3 | 17.56 | 8118 |
| 5 | 75 | 48 | 293.7 | 1324.8 | 3 | 17.58 | 8178 |
| 6 | 90 | 47 | 304.4 | 1322.6 | 3 | 17.61 | 8235 |
| 7 | 105 | 47 | 312.0 | 1342.6 | 3 | 17.63 | 8341 |
| 8 | 120 | 47 | 304.0 | 1339.0 | 3 | 17.64 | 8370 |
| 9 | 135 | 48 | 310.6 | 1343.7 | 3 | 17.65 | 8401.5 |

Whole-run stats (all 474 iterations): `retrieve_ms` median 296.3ms (mean 305.6, stdev 46.5,
range 232.2-669.9ms); `full_workflow_ms` median 1317.3ms (mean 1337.7, stdev 160.0, range
1168.2-2663.1ms). First-5 vs last-5 raw `retrieve_ms`: `[310.3, 322.0, 307.0, 297.4, 312.4]` vs
`[298.1, 331.6, 317.6, 293.0, 296.7]` — indistinguishable. `alive_tasks` is flat at 3 across
every single one of the 474 iterations, exactly as in the release and debug passes; the isolated
high/low values in the whole-run range above are single-iteration outliers, not a slice-level
trend (no 15-minute-slice median leaves the ~270-315ms / ~1215-1344ms bands).

Process-level counters, PowerShell `Get-Process` sampled every 5s (1801 samples total, matching
the plan's literal "every 5 s" cadence — appropriate at this soak's much slower per-iteration
pace, unlike the release/debug passes' 1s deviation):

| Slice | Start (min) | n | working_set median (MB) | private_bytes median (MB) | handle_count median | thread_count median |
|---|---|---|---|---|---|---|
| 0 | 0 | 180 | 270.2 | 195.0 | 189 | 33.5 |
| 1 | 15 | 180 | 273.0 | 199.8 | 190 | 34 |
| 2 | 30 | 179 | 259.1 | 201.6 | 190 | 33 |
| 3 | 45 | 180 | 260.4 | 203.9 | 190 | 34 |
| 4 | 60 | 179 | 258.9 | 203.6 | 190 | 33 |
| 5 | 75 | 180 | 257.1 | 201.9 | 190 | 33 |
| 6 | 90 | 179 | 258.6 | 203.0 | 190 | 33 |
| 7 | 105 | 180 | 257.9 | 202.2 | 190 | 33 |
| 8 | 120 | 179 | 259.2 | 203.9 | 189 | 33 |
| 9 | 135 | 180 | 259.2 | 203.4 | 189 | 33 |

A stable-window comparison (excluding the first 3 minutes of warm-up and the last 3 samples of
process teardown, 1762 of 1801 samples remaining, first-half vs second-half median): working set
delta -1.7MB, private bytes delta +1.5MB, handles delta 0, threads delta 0 — flat within noise,
confirming the plateau the release and debug passes found, now confirmed to hold for the full
2.5-hour duration rather than just their 15-430s windows. One refinement over the prior passes'
characterization: `private_bytes` shows a genuine gradual ramp from ~195MB (slice 0 median) to
~204MB (slice 3 median onward, then flat) — over tens of minutes here, not the "first 2-3 seconds"
the faster release/debug passes described. This is not a contradiction; it is the same warm-up
phenomenon stretched across more wall-clock time because this run's iterations are ~60x slower
(paced) than the unpaced passes', so an equivalent iteration-count warm-up window naturally spans
more real minutes. It still plateaus and does not continue climbing through the remaining ~105
minutes.

### 5. Session cache: a genuine, but bounded and uncorrelated, growth signal

`session_size_bytes` and `session_approx_num_items` are the one counter in any of the three
passes (release, debug, long-duration) that does **not** plateau within the run: both climb
monotonically across all ten 15-minute slices, with no slice showing a decrease.

```
session_size_bytes:        first=17,326,621  last=17,653,310  delta=+326,689 bytes (+1.9%)
session_approx_num_items:  first=7,620       last=8,421       delta=+801 items (+10.5%)
```

But the growth **decelerates** sharply rather than continuing linearly — the per-slice delta in
`session_size_bytes` falls from 74,646 bytes (slice 0->1) to 7,964 bytes (slice 8->9), a ~9.4x
reduction; `session_approx_num_items`'s per-slice delta falls from 150 to ~31 (one slice, 6->7,
breaks the smooth decay with a 106-item jump — plausibly one previously-uncached index/metadata
object touched, not a new regime). This is the signature of a cache converging toward a working
set ceiling as it absorbs more of the 474 distinct entities' index/metadata footprint, not
unbounded growth — though the still-positive final-slice delta means it had not fully flattened
by minute 150.

**This is the class-3 (Lance session cache growth) discriminator the original profile flagged
as "inconclusive, not exercised as a contrast" — now exercised, and it genuinely moves: the
session cache does grow, continuously, for longer than any process-level OS counter does.**
But — and this is the load-bearing caveat — **this growth does not correlate with retrieval
latency in this soak.** `retrieve_ms` and `full_workflow_ms` stayed flat across the exact same
ten slices the session cache was visibly still growing in (section 4's table). If session-cache
size were the accumulator driving `RetrieveHybrid`'s production slowdown, this soak's own
`RetrieveHybrid`/`AssemblePrompt`/`GenerateAnswer` costs should have tracked the cache's growth
at least partially over 2.5 hours of continuous, real (if decelerating) cache expansion; they
did not. Two readings are both consistent with this: (a) session-cache lookups stay effectively
O(1)/O(log n) regardless of cache size at the range tested here (up to ~8,400 approx items,
~17.65MB), so cache growth alone does not explain the accumulator; or (b) production's cache
would grow to a materially larger size than this soak's single-arm, 474-distinct-row run
reaches — a full paid drive touches far more distinct documents/entities/chunks across many more
queries and (in production) concurrent request patterns this soak's single-threaded sequential
design never generates — at which point a size-dependent cost (eviction churn, lock contention,
or a different internal data structure's complexity class) could still kick in beyond what was
tested here. This pass cannot distinguish (a) from (b); it can only report that at this soak's
scale, real and continuing cache growth did not visibly slow retrieval.

### 6. Telemetry-exporter class and collector state

As in every prior pass, this run has `collector_reachable: false` (the `lancet-collector`
container was stopped for the run's duration, see above) — the telemetry-exporter-queue
accumulator (part of RESEARCH's lowest-ranked candidate 5) remains untested under a genuinely
reachable, actively-exporting collector in every pass to date, release, debug, and long-duration
alike. This is a known, disclosed gap, not new to this round.

### Updated Named accumulator conclusion (2026-09-24, long-duration soak)

**Still not named by measurement — now tested across three independent dimensions: build
profile (release vs. debug), iteration count/wall-time at soak speed (n=300 unpaced, both
profiles), and wall-clock duration with production-matched pacing (n=474, 2.5 hours, debug).**
None of the three passes reproduces `RetrieveHybrid` or full-pipeline latency growth of any
practical magnitude on any tested arm. The long-duration pass specifically closes the gap the
first two passes' own hedge left open ("class 2/3 might need more wall-time than 300
release-speed queries provide to become visible") — this run gave the accumulator not just more
iterations, but 2.5 real hours with production-matched ~17.7-second idle gaps between queries,
and `retrieve_ms`/`full_workflow_ms` still did not trend upward (total implied drift 26.6ms over
474 iterations — noise, not decay, exactly as `flatness_verdict`'s own window prong and the
first-5-vs-last-5 raw comparison confirm).

**What newly moved:** class 3 (Lance session cache growth) gained its first positive evidence
across all three passes — a real, continuous, if decelerating, growth in `session_size_bytes`/
`session_approx_num_items` sustained for the full 2.5-hour run, unlike every process-level OS
counter (working set, private bytes, handles, threads), which plateau within the first
tens-of-minutes across all three passes and then hold flat. But this growth is evidenced to be
**uncorrelated with this soak's own retrieval latency** (section 5) — a genuine finding, not the
absence of one, and one the original profile's discriminator table could not make since it never
had this counter to read. Classes 1 (abandoned CPU work) and 5 (gateway/harness backpressure,
for the already-observed growth episode specifically) remain weakened/excluded as in the prior
passes — nothing in this pass changes either. The telemetry-exporter sub-class of candidate 5
remains untested under a live collector, unchanged from before (section 6).

**No arm, on any build profile, iteration count, or wall-clock duration tested across three full
passes, reproduces the sustained, query-count- or wall-time-proportional growth OI-02 is
investigating.** The single largest unattributed gap — real provider network latency for
embedding/generation (never exercised by this soak's deterministic `FixedEmbeddingPort`/
`FixedGenerator` stand-ins) and production's concurrent, multi-request load pattern (never
exercised by this soak's single-threaded sequential design, on any of the three passes) —
is unchanged from every prior pass. This pass did not attempt either: adding concurrency or a
real provider call would each violate a `must_haves.prohibitions` line from this plan's Task 1
(no provider API call) or introduce a variable ("--workers" — like concurrent load) explicitly
out of this plan's read-only, single-arm-at-a-time soak design.

This section does not select a checkpoint option. Task 3 is re-presented below with the combined
release, debug, and long-duration evidence.
