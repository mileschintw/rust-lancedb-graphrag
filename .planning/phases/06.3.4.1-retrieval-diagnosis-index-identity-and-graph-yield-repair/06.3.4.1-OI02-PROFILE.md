# 06.3.4.1-03: OI-02 RetrieveHybrid Profile

Read-only, API-free `retrieval_soak` (release build) drove 300 iterations per arm against the
live `./data/lancedb-eval` store (`LANCET_ENV=eval`, engine and gateway both stopped for the
entire run). Query vectors/text: a SplitMix64-seeded (`seed=5494759071201636403`, recorded on
every header line) sample of 300 `nodes` rows for R/RP and 300 `entities` rows for RG/RGT/W (an
entity's own `name_vector` is used as the query embedding for the graph-touching arms, which
guarantees `attempt_graph_augmentation`'s seed match trivially succeeds — see
`retrieval_soak.rs`'s `Arm::uses_entity_seed` doc comment). All five headers report
`build_profile: "release"` and `collector_reachable: false` (no local OTel collector running,
as expected). Process sampling: PowerShell `Get-Process` every 1s (not the plan's stated 5s —
see Methodology Note below) into `soak/<arm>-proc.csv`.

## Per-arm verdict table

| Arm | n | Flatness verdict | Slope prong | p-value | Slope (ms/query) | Window prong | Window delta (ms) | Slice medians (ms, 50-iter) |
|---|---|---|---|---|---|---|---|---|
| R | 300 | **decay_present** (fails) | fired | 0.0020 | 0.0203 | not fired | 32.9 | 98.2, 97.3, 98.9, 97.2, 96.8, 110.4 |
| RP | 300 | **decay_present** (fails) | fired | 0.0130 | 0.0114 | not fired | 7.6 | 84.1, 83.6, 84.0, 86.6, 83.2, 87.3 |
| RG | 300 | **flat** (passes) | not fired | 0.0668 | 0.0083 | not fired | 5.3 | 74.3, 73.4, 73.6, 76.0, 75.1, 75.0 |
| RGT | 300 | **flat** (passes) | not fired | 0.5596 | -0.0027 | not fired | 2.5 | 74.3, 75.6, 74.2, 74.9, 73.6, 74.4 |
| W | 300 | **flat** (passes) | not fired | 0.0554 | 0.0096 | not fired | 6.0 | 74.5, 74.4, 74.6, 76.2, 75.4, 77.2 |

`censored_count=0` and `unusable_dropped_count=0` on every arm — no RetrieveHybrid failures,
no missing timings. `COMMITTED_THRESHOLDS` and `decay.py` were not edited to reach any of this
(`git diff --quiet -- eval/src/lancet_eval/decay.py eval/src/lancet_eval/thresholds.py` passes).

**R and RP formally fail the machine flatness verdict, but not for a practically meaningful
reason.** Both fire only the Mann-Kendall slope prong (window prong never fires on any arm —
every window delta is 2.5-32.9ms against the 500ms materiality threshold). At n=300,
Mann-Kendall has enough statistical power to flag a slope of 0.02ms/query (R) or 0.011ms/query
(RP) as "significant" (p=0.002, p=0.013) even though the total drift implied over the whole run
is 6.1ms (R) and 3.4ms (RP) — smaller than the noise band visible in the slice medians
themselves (which swing by ~2-13ms between consecutive 50-iteration blocks with no consistent
direction). This is large-n statistical sensitivity, not decay: applying the same instrument
production-side (paid drive 1, 06.3.4.1-12) at n≈500 records per arm should be read the same
way — a "fired" slope prong with a sub-millisecond-per-query slope is not what OI-02 is looking
for.

RGT's graph augmentation timed out on **300/300 iterations** (`graph_ms` observed 200.3-215.1ms
against the 200ms timeout — this store's graph seed lookup + neighborhood fetch + two Cypher
executions consistently take just over 200ms), so RGT is not a weak reproduction of the
segment-1 abandoned-future condition — every iteration genuinely abandons a real in-flight
`attempt_graph_augmentation` future exactly as `tokio::time::timeout` drops it in production.

## Named accumulator

**Inconclusive at this soak's scale.** No arm reproduces RetrieveHybrid latency growth of any
practical magnitude — R and RP's statistically-significant slopes amount to single-digit
milliseconds of total drift over 300 queries, RG/RGT/W show no significant slope at all, and no
arm's window prong fires. This does not match 06.3.4's observed growth (RetrieveHybrid median
5.0s -> 29.3s over seven 50-record slices) or 06.3.3 segment 1's growth (2.97s -> 10.83s over
four 40-record slices) in either direction or magnitude.

### What was reproduced

- **A real, deterministic abandoned-future condition** (RGT, 300/300 timeouts) with zero
  measurable effect on `alive_tasks`/`global_queue_depth` (both flat at 3 tasks / 0 queue depth
  across every sample, every arm) and no measurable effect on subsequent `RetrieveHybrid`
  iterations' `retrieve_ms`.
- **Per-query `open_table`/`checkout` state**: flat and tiny throughout (`open_table_ms`
  ~1.0-2.1ms, `checkout_ms` ~0.12-0.23ms, all five arms, ordinal 1 through 300 — no drift).
- **Process-level counters** (working set, private bytes, handle count, thread count): all four
  rise sharply in the first 2-3 process samples (~1-3s of wall time — process/runtime/thread-pool
  warm-up: `target/release/retrieval_soak.exe`'s own startup, tokio worker + Lance
  `spawn_cpu`/blocking-pool thread spin-up, first LanceDB dataset opens), then **plateau** for
  the remainder of the run. RGT (guaranteed abandonment on every iteration) plateaus at 46
  threads / ~233 handles / ~187MB working set after its first ~3 samples and stays there for the
  following ~105 one-second samples. RG, R, RP and W show the same shape.

### What was not reproduced, and why that is a real discriminator, not a soak defect

- **The ~15-20x baseline gap between this soak (release, ~55-110ms median `retrieve_ms`) and
  production (06.3.3 segment 2's flat baseline: ~1.6-1.7s; 06.3.4's starting point: ~5.0s) is
  substantially a build-profile confound, not evidence the soak under-reproduces the store's real
  cost.** `06.3.3-BUDGETS.md`'s own "Engine startup proof" section records the only documented
  live-binary launch command for that phase: `cargo run --manifest-path engine/Cargo.toml
  --locked --bin engine` — no `--release` flag, i.e. a **debug** build. A supplementary
  exploratory run of this same soak bin in debug mode (`cargo run` without `--release`, arm R,
  50 iterations, not part of the committed `soak/` artifact set —
  `header.build_profile: "debug"`) measured `retrieve_ms` at **377-529ms** (median 439ms) against
  the release build's 76-136ms range for the same arm — roughly a 4-5x gap from the optimization
  level alone, with **no growth** over those 50 debug-mode iterations either (first-5/last-5
  medians ~445ms/~450ms). That still leaves an unexplained ~3-4x gap to production's observed
  1.6-5s baseline (real network/gRPC/HTTP round-trip overhead absent from this in-process soak,
  a possibly warmer-vs-colder store state, and production's per-request node construction are
  candidate remaining contributors, not measured here).
- **Sustained, unbounded resource growth over hundreds of queries at soak speed.** Every
  process-level counter plateaus within a few seconds of warm-up and does not continue climbing
  through the remaining ~15-140 seconds of wall time each arm's 300 iterations took (RG/RGT/W
  ran 130-140 one-second samples after warm-up with flat readings). If class 2/3 (allocator or
  Lance session-cache growth) is the true accumulator, either it requires materially more
  iterations/wall-time than 300 release-speed queries provide to become visible, or it requires
  concurrent/production request patterns this single-threaded sequential soak does not generate.
- **The harness/gateway path.** This soak never leaves the engine process — no gRPC, no SSE
  streaming, no gateway checkpoint dispatch. A slope that depends on that path (candidate 5,
  lowest-ranked in RESEARCH, and any HTTP/connection-pool-side accumulation) is invisible here by
  construction.

### Discriminator readings against RESEARCH's ranked candidates

| # | Candidate | Discriminator | Reading |
|---|---|---|---|
| 1 | Abandoned CPU work outliving a timed-out/cancelled future | `alive_tasks`/`global_queue_depth` vs ordinal; thread count vs time | **Not supported at this scale.** Flat on the main tokio runtime despite 300/300 real abandonments in RGT. Thread count (which would show Lance's `spawn_cpu` blocking-pool growth, per RESEARCH's own note that `alive_tasks`/`global_queue_depth` cannot see that pool) rises during warm-up then plateaus — no continued growth through ~105-140 samples post-warm-up. |
| 2 | Allocator/memory degradation from large per-query allocations | Working set / private bytes vs ordinal (external sampler) | **Inconclusive, not ruled out.** All arms show a real working-set rise (~10MB -> ~150-200MB) but it happens in the first few seconds (warm-up shape) and plateaus; no arm shows continued growth proportional to query count. |
| 3 | Lance session cache growth | Soak loop with default `Session` (this soak never opens a second explicit session) | **Inconclusive, not exercised as a contrast.** The soak always uses the default session (matching production); no small-vs-default-session comparison was run this pass. |
| 4 | Per-query `open_table` + `checkout` state | Sub-stage timing of `open_table`, `checkout` | **Not supported.** Both are flat and sub-2ms across all 1500 combined iterations. |
| 5 | PostgreSQL/gateway backpressure | N/A to this soak | Sanity-only per RESEARCH; not exercised (no gateway/Postgres in this soak). |

## Header/build metadata (per official release-mode run)

| Arm | build_profile | collector_reachable | binary_mtime | pid |
|---|---|---|---|---|
| R | release | false | 1790229541 (unix s) | varies per launch |
| RP | release | false | 1790229541 | varies |
| RG | release | false | 1790229541 | varies |
| RGT | release | false | 1790229541 | varies |
| W | release | false | 1790229541 | varies |

All five share the same `retrieval_soak.exe` build (`binary_mtime_unix_secs: 1790229541`), built
once before the five sequential runs.

## Methodology note (deviation from the plan's literal "every 5s")

The plan's Task 2 action says to sample the soak process "every 5 s". At soak speed
(55-140ms/iteration, 300 iterations complete in 15-140s depending on arm) a 5s cadence would
have produced only 3-28 samples per arm — too few to see the warm-up/plateau shape this profile
relies on. Samples were taken every 1s instead (still satisfying "sample ... externally"; the
5s figure in the plan was written against production's much slower per-query cost, where 5s
easily captures many queries per sample). 42-140 samples were collected per arm. Recorded here
as a deviation, not silently changed.

## Conclusion

The accumulator class OI-02 is investigating is **not named by this pass** — no arm reproduces
practically meaningful RetrieveHybrid growth, and process-level counters plateau rather than
climb. The pass does, however, rule out per-query `open_table`/`checkout` state (class 4) and
finds no evidence of unbounded task/queue accumulation on the main tokio runtime even under
guaranteed real abandonment (weakening, not confirming, class 1 as tested — with the explicit
caveat that Lance's own blocking pool is invisible to those two counters and only partially
visible via thread count, which itself plateaus). It surfaces a concrete, evidenced alternative
explanation for a large share of the previously-unexplained baseline gap between 06.3.3/06.3.4's
production numbers and this soak's release-mode numbers: those drives' only documented engine
launch command omits `--release`. That is a build-profile confound in the *comparison*, not
proof that growth itself doesn't happen in production — a debug-mode soak run showed a higher
but still flat baseline over 50 iterations, not growth.

This profile does not attempt to decide between the checkpoint's three options; it supplies the
evidence for that decision (Task 3).

## Extended profiling (2026-09-24)

The checkpoint's first presentation was inconclusive. The user chose **extend-profiling**
(not fix-named-class or mimalloc) and authorized two additions before Task 3 is re-presented,
with no engine fix written in this plan: (1) a debug-build soak at the same full n=300 scale as
the release runs, on the theory that debug's build-profile confound (documented above) might
also be masking a real accumulator that needs more per-query wall time to surface; (2) a
gateway-in-the-loop arm to measure the remaining ~3-4x gap to production, built only if it stays
a thin driver script against the existing harness with no paid provider call — otherwise
documented as a finding, not improvised. `CARGO_BUILD_JOBS=4` was used throughout (the
machine's full parallelism was previously shown to exhaust resources); nothing else ran on the
machine during either addition, and no soak, engine or gateway process was left running at the
end (`tasklist` confirmed clean after each run and again at the end of this session).

### 1. Debug-build soak at full scale

Built once (`cargo build --manifest-path engine/Cargo.toml --locked --bin retrieval_soak`, no
`--release`, 22.5s) and run directly from `engine/target/debug/retrieval_soak.exe` (not via
`cargo run`, so the sampled PID is the soak process itself, not a `cargo` wrapper). All four
arms — **R, RG, RGT and W** (RP omitted; the user's floor was "R and W at minimum, RG/RGT if
time allows without a large cost", and RG/RGT fit inside a fifteen-minute wall-clock budget on
top of R and W) — ran at the full 300 iterations, same seed (`5494759071201636403`), same store
(`./data/lancedb-eval`, `LANCET_ENV=eval`), same `--graph-timeout-ms 200` default, engine and
gateway stopped, one fresh process per arm, saved to `soak/debug/<arm>.jsonl` +
`soak/debug/<arm>-proc.csv`. Process sampling stayed at 1s cadence (same deviation from the
plan's literal "every 5s" already recorded above, for the same reason: debug iterations still
complete in well under 5s each). All four headers report `build_profile: "debug"` and the same
`binary_mtime_unix_secs: 1790230694` — one debug binary build, four sequential runs, matching
the release methodology's "one binary, four/five sequential runs" pattern.

**Wall-clock cost, for the record:** R ≈ 150s, RGT ≈ 210s, RG ≈ 380s, W ≈ 430s (roughly
4-6x each arm's release wall-clock time). This is itself a partial test of the original
profile's own caveat that "class 2/3 ... requires materially more iterations/wall-time than 300
release-speed queries provide to become visible" — the debug run gives each of the 300
iterations several times longer to accumulate state, and no arm still shows growth (below).

| Arm | n | Flatness verdict | Slope prong | p-value | Slope (ms/query) | Total drift over n=300 (ms) | Window prong | Window delta (ms) | Slice medians (ms, 50-iter) |
|---|---|---|---|---|---|---|---|---|---|
| R | 300 | **flat** (passes) | not fired | 0.0000 | -0.5450 | -163.5 | not fired | -154.0 | 578.5, 581.2, 580.8, 591.7, 459.4, 457.3 |
| RG | 300 | **flat** (passes) | not fired | 0.7388 | -0.0051 | -1.5 | not fired | 22.9 | 288.0, 291.7, 279.9, 290.2, 291.8, 284.5 |
| RGT | 300 | **flat** (passes) | not fired | 0.0961 | 0.0293 | 8.8 | not fired | 24.6 | 290.0, 290.1, 285.6, 291.5, 292.3, 294.3 |
| W | 300 | **decay_present** (fails) | fired | 0.0001 | 0.0638 | 19.1 | not fired | 24.8 | 285.6, 285.2, 281.8, 297.8, 287.6, 304.3 |

`censored_count=0` and `unusable_dropped_count=0` on all four. `git diff --quiet --exit-code --
eval/src/lancet_eval/decay.py eval/src/lancet_eval/thresholds.py` still passes — neither file
was touched to reach this table.

**R's significant *negative* slope (getting faster, not slower) is the trend prong's directional
filter working as designed** — the Mann-Kendall/Theil-Sen trend prong only fires on a growing
(decaying) slope, and a p≈0 downward slope correctly reads `prong_slope_fired: False`. The
slice medians show why: a step down from ~580ms to ~458ms around slice 4/5 (iteration ~200),
consistent with a one-time OS-level effect (page cache, disk read-ahead, or Windows working-set
trim) rather than a query-count-proportional trend — the same warm-up-shaped, not
growth-shaped, pattern the release pass found in process counters, here visible in latency
itself for the first time. **W repeats the exact release-pass pattern of R/RP:** the slope
prong fires at p=0.0001, but the total drift it represents is 19.1ms over 300 queries —
smaller than the noise band between adjacent 50-iteration slice medians (281.8 to 304.3ms, a
22.5ms swing with no consistent direction). This is the same large-n statistical-sensitivity
reading as the release pass's R/RP arms, not evidence of decay.

**Process counters (working set, handle count, thread count) plateau in debug exactly as they
did in release**, just at higher absolute values (debug's allocator and larger binary bring a
higher working-set floor). Excluding the final ~2 samples of each run (process teardown —
working set/threads/handles drop sharply as the process exits, a sampling artifact of the 1.5s
grace window after the soak PID disappears, not an in-run signal) and comparing the first-half
vs second-half median of the remaining "stable" window:

| Arm | Stable samples | Working-set median (MB) | First-half median (MB) | Second-half median (MB) | Delta (MB) |
|---|---|---|---|---|---|
| R | 168 | 216.4 | 214.7 | 217.2 | +2.5 |
| RG | 389 | 261.3 | 258.5 | 265.7 | +7.3 |
| RGT | 229 | 240.4 | 242.6 | 238.8 | -3.9 |
| W | 393 | 258.3 | 259.9 | 256.6 | -3.3 |

No arm shows a one-directional, sustained climb — RG's +7.3MB is the largest, well inside the
run's own min-max range (200.6-314.2MB) driven by ordinary allocator fluctuation, not a trend.
Debug gave every arm 4-6x more wall-clock time for a slow accumulator to become visible and
none did.

**Baseline comparison, now at matched n=300 on both sides (superseding the earlier 50-iteration
exploratory debug run, which this run confirms was not a small-sample fluke):**

| Arm | Release median (retrieve_ms unless noted) | Debug median (retrieve_ms unless noted) | Ratio |
|---|---|---|---|
| R | 98.2 ms | 545.5 ms | 5.6x |
| RG | 74.3 ms | 288.0 ms | 3.9x |
| RGT | 74.3 ms | 291.0 ms | 3.9x |
| W (retrieve_ms) | 74.5 ms | 289.6 ms | 3.9x |
| W (full_workflow_ms, debug-only — no release equivalent recorded) | — | 1287.1 ms | — |

(R's retrieve_ms is not directly comparable across arms within a build profile — R/RP draw
seeded rows from `nodes`, RG/RGT/W from `entities`; different query content drives different
dense/BM25 cost. The ratio column compares the *same* arm's release vs debug run, which holds
query content fixed.) The 3.9-5.6x ratio is consistent with the single-arm 50-iteration
exploratory run's ~4-5x estimate — that earlier number was not a fluke of small n.

### 2. Free evidence from the existing production journal (zero additional cost)

Before attempting a gateway-in-the-loop build, the committed 06.3.4 run journal
(`eval/runs/2026-09-09-multihop_rag/journal.jsonl`, 658 records — the same run
`06.3.4-OBSERVATION-THROUGHPUT-PLATEAU.md`'s 5.0s→29.3s RetrieveHybrid figure comes from) was
re-read for a question this profile can answer at no soak cost: for each record, does
`RunRecord.duration_ms` (the harness's own measured wall time, `journal.py`) minus the sum of
`node_timings[].duration_ms` (the engine's own per-node measured time, which already includes
`RetrieveHybrid`, `ExtractGraphContext`, `AssemblePrompt`, `GenerateAnswer`, `ReformulateQuery`)
grow over the run? If it does, that gap — everything the harness clock sees that no engine node
claims — is exactly the SSE/gRPC/harness-transport overhead candidate 5 predicts. If it stays
flat while `RetrieveHybrid`'s own node timing grows, the growth is already fully inside the
engine's own instrumentation before any transport is added, which rules candidate 5 out for
*this* growth episode specifically (not for the general baseline gap — see the caveat below).

50-record slice medians, in journal order:

| Slice | n | RetrieveHybrid node-time median (ms) | Wall-minus-nodes gap median (ms) |
|---|---|---|---|
| 0 | 50 | 4910.5 | 5.0 |
| 1 | 50 | 10441.0 | 4.0 |
| 2 | 50 | 13947.0 | 4.5 |
| 3 | 50 | 16785.5 | 4.0 |
| 4 | 50 | 21254.0 | 5.0 |
| 5 | 50 | 24626.0 | 5.0 |
| 6 | 50 | 28960.0 | 5.0 |
| 7 | 50 | 29266.5 | 30157.0 |
| 8 | 50 | 30814.0 | 30295.5 |
| 9-13 | 208 | unavailable (RetrieveHybrid censored) | ~30300-30580 |

Through slices 0-6 (RetrieveHybrid still completing and reported), the wall-minus-nodes gap
sits at a flat 4-5ms regardless of how large `RetrieveHybrid` itself has grown (4.9s to 29.0s) —
**the harness/gateway/transport overhead this metric measures is negligible and does not track
the growth.** From slice 7 onward the gap jumps to ~30s, but this is not transport growth
either: `RetrieveHybrid` becomes `None` (censored/timed out) in those slices, so the "gap" stops
measuring transport and starts measuring the timeout budget consumed by a node that no longer
reports a completed timing — a different phenomenon, already covered by the release pass's
censoring handling (Pitfall 6), not a new discriminator reading.

**Scoped conclusion:** this rules out candidate 5 (gateway/harness backpressure) as an
explanation for *this specific, already-observed* RetrieveHybrid growth episode — the growth is
fully present in the engine's own self-measured node timing before transport is added, so a
transport-side accumulator cannot be what produced it. It does **not** by itself explain the
remaining debug-soak-to-production baseline gap (below) — `node_timings` for `ExtractGraphContext`
already includes real embedding-provider network time in production (absent from this soak's
`FixedEmbeddingPort`), and that is inside "node time", not inside the "gap" this metric isolates.

### 3. Gateway-in-the-loop arm: documented, not built

This was not attempted. Confirmed by reading the actual provider-client contract
(`engine/src/config.rs`, `engine/src/generation/openrouter.rs`,
`engine/src/generation/tests.rs`) rather than assuming: the engine validates the configured
generation model against `model_metadata_endpoint` (`/models`) at startup
(`config.rs:333` `validate_against_provider`, called from `main.rs:70` and `main.rs:127`,
*before* the workflow ever runs), requiring the response to advertise `response_format`/
`json_schema` in `supported_parameters` — startup fails closed otherwise. The chat completion
call itself (`generation/openrouter.rs:611-623`) sends `response_format: {type: "json_schema",
json_schema: {strict: true, ...}}` and requires a schema-conformant reply carrying `finish_reason`
and `usage`, matching the extensive fixture set already required just to unit-test this client
(`generation/tests.rs`, 2000+ lines of hand-built OpenRouter-shaped JSON fixtures). Reproducing
that contract correctly for a live process, not a test fixture, is what "more than a thin driver
script" concretely means here. A correct build would need:

- A stub HTTP server implementing three endpoints, not one: `/models` (model metadata with
  `supported_parameters` including `response_format`/`json_schema` for the configured model
  id — required at engine startup, before any query runs), `/embeddings` (returning a
  2048-dimension vector, matching `voyage-4-large`'s configured `dimensions`), and
  `/chat/completions` (returning a strict-schema-valid `ModelOutput` JSON body with
  `finish_reason` and `usage` fields the client parses).
- Overriding all three `LANCET_OPENROUTER__*_ENDPOINT` env vars (already confirmed
  override-able — `config.rs:827-849`) to point at the stub, plus a dummy API key, so that any
  endpoint the stub misses fails closed with 401 rather than silently falling through to the
  real OpenRouter API and spending real money.
- A precondition check that `staged_documents_v2` has 0 rows before starting this second engine
  instance — a non-empty table replays ingestion at startup (`main.rs:114-121`, RESEARCH
  Pitfall 14), which would call the (stubbed, but still real-request-shaped) extraction path and
  is out of scope for a latency-only measurement.
- A running PostgreSQL instance, because the gateway writes checkpoint rows to the eval schema
  on every query — running this arm would move the D-22 checkpoint baseline the same way a real
  drive does, which needs disclosing, not treating as free.
- A thin SSE client driver loop hitting the gateway's real ingress N times and recording
  round-trip latency — this piece alone would be the "thin driver script" the checkpoint
  authorized; the four items above are what make the whole arm not thin.

None of this was built. The zero-cost production-journal analysis in §2 is the substitute this
pass offers for "measure the remaining gap": it positively rules out harness/transport overhead
as a source of the *already-observed* growth, but it cannot speak to whether a *fresh*
gateway-in-the-loop run — with concurrent request patterns, a second live engine process, and
real provider network calls — would surface an accumulator this soak's single-threaded,
provider-free design cannot reproduce. That specific question (RESEARCH candidate 5, and any
new HTTP/connection-pool-side accumulation) remains open, exactly as the original pass said.

### Updated Named accumulator conclusion (2026-09-24)

**Still not named by measurement — now on two independent build profiles instead of one.**
Debug, run at the same n=300 scale and given 4-6x more wall-clock time per arm than release,
reproduces the same shape the release pass found: R/W show a statistically-significant slope
too small to matter in practice (debug W: 19.1ms total drift over 300 queries; release R/RP:
6.1ms/3.4ms), RG/RGT are flat with no significant slope, and process counters plateau rather
than climb on both profiles. This weakens the original profile's own hedge that class 2/3 might
need more wall-time than 300 release-speed queries provide — debug's 300 queries take several
times longer in wall-clock and still show no growth, though "more time" and "more queries" are
not identical variables, and this still does not test concurrent/production request patterns.

**What newly moved:** the release-vs-production baseline gap is now partially attributed rather
than wholly unexplained. Build profile alone (release → debug, matched n=300, same arms, same
seed) accounts for 3.9-5.6x. The production-journal analysis in §2 positively rules out
harness/gateway transport overhead as a contributor to the specific growth episode already on
record (flat 4-5ms gap while RetrieveHybrid's own node time grows 4.9s→29.0s). What remains
unattributed — real provider network latency for embedding/generation (not exercised by this
soak's deterministic stand-ins) and production's per-request node construction and concurrent
load pattern (not exercised by this soak's single-threaded sequential design) — is unchanged
from the original pass, not newly ruled in or out.

**No arm on either build profile shows the sustained, query-count-proportional growth OI-02 is
investigating.** No discriminator moved from "inconclusive" to "supports" or "rules out" for
candidates 2 (allocator) or 3 (Lance session cache) specifically — process counters plateau on
both profiles, which is suggestive but not conclusive against slow accumulation, since neither
soak run generates the concurrent/production request pattern RESEARCH itself flags as a
condition those candidates might require. Candidate 1 (abandoned CPU work) stays weakened, not
confirmed, for the same reasons the original pass gave. Candidate 5 (gateway/harness
backpressure) is now more strongly excluded than before, specifically for the already-observed
growth episode, via the zero-cost journal analysis in §2 — not via a new gateway-in-the-loop
run, which was not built (§3).

This section does not select a checkpoint option. Task 3 is re-presented below with the
combined release + debug evidence and the gateway-in-the-loop finding.

## Long-duration soak (2026-09-24)

The checkpoint's second presentation (release + debug, matched n=300, plus the zero-cost
journal analysis) was still inconclusive. The user chose **extend-profiling: long-duration
soak** again (not fix-named-class or mimalloc), explicitly ruling out a restart workaround, and
authorized one addition before Task 3 is re-presented a third time, with no engine fix written:
a **debug-build arm-W soak paced to approximate production's wall-clock cadence, run for
approximately 2.5 hours** — the one dimension the release and debug passes above, at 15-140s and
150-430s respectively, could not test. `CARGO_BUILD_JOBS=4` was used for every build. Nothing
else ran on the machine during the soak itself: the `lancet-collector` OTel-collector-contrib
Docker container (part of this repo's standing observability stack, unrelated to this session)
was found reachable on `127.0.0.1:4317` before this pass started — a departure from every prior
pass's `collector_reachable: false` — and was deliberately stopped for the run's duration to
preserve methodology parity with the release and debug passes above, then restarted afterward.
No soak, engine, gateway, or sampler process was left running at the end (`Get-Process`/
`Get-NetTCPConnection` confirmed clean after the run).

### 1. Pacing derivation

The idle gap inserted between iterations approximates the real per-question time production
spends on everything *other* than `RetrieveHybrid` itself — reformulation, graph context
extraction (a real embedding-provider network call), prompt assembly, and answer generation (a
real generation-provider network call) — plus harness/transport overhead. The committed
06.3.4 run journal (`eval/runs/2026-09-09-multihop_rag/journal.jsonl`, 658 records) has no
separate LLM-judge timing — judging runs offline via `lancet-eval score`, never during the
drive itself — so this derivation captures generation and embedding-provider time, not judge
time; the checkpoint's "generation/judge time" framing is read here as "everything besides
`RetrieveHybrid`" for lack of a judge component to measure in a drive journal.

For each record with a completed `RetrieveHybrid` node timing and no top-level `error_type`
(356 of 658; the other 302 have no `RetrieveHybrid` entry at all — the same censoring past
slice 6 documented in "Free evidence from the existing production journal" above; 0 records
carried a non-null `error_type`), the gap is `duration_ms - RetrieveHybrid_node_duration_ms`
(the harness's own measured wall time for the record, minus just the `RetrieveHybrid` node's
own measured time — deliberately not `duration_ms` minus the *sum* of all five node timings,
which is the different, much smaller ~4-5ms "transport-only" gap computed earlier in this
document for a different purpose).

| Statistic | Value |
|---|---|
| Usable records (completed RetrieveHybrid, no error_type) | 356 / 658 |
| Gap median | 18653.5 ms |
| Gap IQR (Q1 / Q3) | 7793.5 ms / 45874.0 ms |
| Gap first-20-usable-records median | 11611.5 ms |
| Gap last-20-usable-records median | 33701.0 ms |

The gap itself grows across the drive (confounded with `RetrieveHybrid`'s own growth, since
`ExtractGraphContext`/`GenerateAnswer` cost was not independently measured for trend in this
pass) — the wide IQR reflects that, not measurement noise. The run's overall median (not the
first- or last-slice value) was used as a deliberate middle-of-the-road choice, disclosed rather
than picked to make a target number come out nicer.

The soak bin's own arm-W iteration already includes some non-`RetrieveHybrid` work (`AssemblePrompt`,
`ExtractGraphContext`, and `GenerateAnswer` — all near-instant here since they call the in-bin
`FixedEmbeddingPort`/`FixedGenerator`, never a real provider). To avoid double-counting that
already-included overhead, it was measured from the prior round's debug arm-W pass
(`full_workflow_ms` median 1287.1ms minus `retrieve_ms` median 289.6ms = 997.5ms) and subtracted:

```
pace_ms = gap_median - already_included_overhead
        = 18653.5 - 997.5
        = 17656 ms
```

### 2. Run parameters and sample coverage

```
LANCET_ENV=eval ./engine/target/debug/retrieval_soak.exe \
  --arm W --iterations 1000 --pace-ms 17656 --duration-secs 9000 \
  --lancedb-path ./data/lancedb-eval
```

`--iterations 1000` requests 1000 distinct seeded `entities` rows up front (the entities table
has 27,971 rows total — `inspect_lancedb --graph-population` confirmed this before the run) —
**the sample size was raised, not cycled**, per the checkpoint's instruction to document which.
`--duration-secs 9000` (2.5h) overrode `--iterations` as the loop's stop condition. The run
completed 474 iterations against a 1000-row sample (`cycles: 1`, `cycled: false` in the
run-completion stderr summary) — the sample comfortably covered the run with margin to spare;
no row was queried twice. Total wall-clock elapsed: 8995.3s (2h 29m 55s), essentially exactly
the 9000s target (the loop's own deadline check has second-level granularity against the last
iteration's own duration, hence the ~5s undershoot). Binary: `engine/target/debug/retrieval_soak.exe`,
built from commit `df10c98b` (the session-cache-logging commit below), `build_profile: "debug"`,
`collector_reachable: false`.

This pass also added a discriminator the original profile flagged as "not exercised as a
contrast": `lance::session::Session::size_bytes()` and `approx_num_items()`, read through the
already-open `nodes` table's dataset handle (`Table::dataset()` -> `DatasetConsistencyWrapper::get()`
-> `Dataset::session()`, all public `lancedb` 0.31.0 / `lance` 1.0.4 API — no new plumbing) and
logged on every iteration line, across all five arms (committed as `feat(06.3.4.1-03): log lance
session cache size per soak iteration`, `df10c98b`). `size_bytes()` performs a full `deep_size_of`
walk over the session's caches — its own doc comment: "not trivial to compute" — but this is
cheap relative to the soak's ~18s-per-iteration pace and is the documented public API for exactly
this purpose.

### 3. Flatness verdict (unmodified `flatness_verdict`/`analyze_decay`)

`git diff --quiet --exit-code -- eval/src/lancet_eval/decay.py eval/src/lancet_eval/thresholds.py`
exits 0 — neither file was touched to reach this verdict.

| n | Verdict | Slope prong | p-value | Slope (ms/query) | Total implied drift over n (ms) | Window prong | Window delta (ms) |
|---|---|---|---|---|---|---|---|
| 474 | **decay_present** (fails) | fired | 3.11e-08 | 0.05621 | 26.64 | not fired | -35.72 |

`censored_count=0`, `unusable_dropped_count=0` — every one of the 474 iterations produced a
usable `RetrieveHybrid` timing (this soak arm never times out or errors; it is not RGT).

**This is the same large-n statistical-sensitivity pattern seen in every prior pass, now at an
even larger n.** The trend prong's p-value (3.11e-08) is far smaller than the release pass's
R/RP arms (p=0.002/0.013) or the debug pass's W arm (p=0.0001) — expected, since Mann-Kendall's
power grows with n and this run has the largest n of any arm tested across all three passes
(474 vs 300). But the *practical* magnitude shrank, not grew: total implied drift is 26.64ms
over the whole 474-iteration, ~2.5-hour run — smaller than the debug pass's W-arm drift (19.1ms
over 300 iterations, a comparable per-iteration rate) and two orders of magnitude below the
500ms window-prong materiality threshold (which correctly did not fire: window delta is -35.7ms,
i.e. the run's tail was very slightly *faster* than its head, not slower). Retrieval or
full-pipeline latency did not trend upward at any timescale this run exercised.

### 4. 15-minute wall-clock slices

Ten full 15-minute slices span the 474 iterations exactly (48+47+47+48+47+48+47+47+47+48=474);
`start_min` is minutes since the first iteration. `retrieve_ms`/`full_workflow_ms`/`alive_tasks`
come from the soak JSONL; `session_size_bytes`/`session_approx_num_items` are the new per-iteration
Lance session-cache reads.

| Slice | Start (min) | n | retrieve_ms median | full_workflow_ms median | alive_tasks | session_size_bytes median (MB) | session_approx_num_items median |
|---|---|---|---|---|---|---|---|
| 0 | 0 | 48 | 303.5 | 1304.6 | 3 | 17.37 | 7718 |
| 1 | 15 | 47 | 296.4 | 1328.8 | 3 | 17.45 | 7868 |
| 2 | 30 | 47 | 271.8 | 1230.2 | 3 | 17.50 | 7977 |
| 3 | 45 | 48 | 279.3 | 1215.1 | 3 | 17.53 | 8052 |
| 4 | 60 | 47 | 282.8 | 1233.1 | 3 | 17.56 | 8118 |
| 5 | 75 | 48 | 293.7 | 1324.8 | 3 | 17.58 | 8178 |
| 6 | 90 | 47 | 304.4 | 1322.6 | 3 | 17.61 | 8235 |
| 7 | 105 | 47 | 312.0 | 1342.6 | 3 | 17.63 | 8341 |
| 8 | 120 | 47 | 304.0 | 1339.0 | 3 | 17.64 | 8370 |
| 9 | 135 | 48 | 310.6 | 1343.7 | 3 | 17.65 | 8401.5 |

Whole-run stats (all 474 iterations): `retrieve_ms` median 296.3ms (mean 305.6, stdev 46.5,
range 232.2-669.9ms); `full_workflow_ms` median 1317.3ms (mean 1337.7, stdev 160.0, range
1168.2-2663.1ms). First-5 vs last-5 raw `retrieve_ms`: `[310.3, 322.0, 307.0, 297.4, 312.4]` vs
`[298.1, 331.6, 317.6, 293.0, 296.7]` — indistinguishable. `alive_tasks` is flat at 3 across
every single one of the 474 iterations, exactly as in the release and debug passes; the isolated
high/low values in the whole-run range above are single-iteration outliers, not a slice-level
trend (no 15-minute-slice median leaves the ~270-315ms / ~1215-1344ms bands).

Process-level counters, PowerShell `Get-Process` sampled every 5s (1801 samples total, matching
the plan's literal "every 5 s" cadence — appropriate at this soak's much slower per-iteration
pace, unlike the release/debug passes' 1s deviation):

| Slice | Start (min) | n | working_set median (MB) | private_bytes median (MB) | handle_count median | thread_count median |
|---|---|---|---|---|---|---|
| 0 | 0 | 180 | 270.2 | 195.0 | 189 | 33.5 |
| 1 | 15 | 180 | 273.0 | 199.8 | 190 | 34 |
| 2 | 30 | 179 | 259.1 | 201.6 | 190 | 33 |
| 3 | 45 | 180 | 260.4 | 203.9 | 190 | 34 |
| 4 | 60 | 179 | 258.9 | 203.6 | 190 | 33 |
| 5 | 75 | 180 | 257.1 | 201.9 | 190 | 33 |
| 6 | 90 | 179 | 258.6 | 203.0 | 190 | 33 |
| 7 | 105 | 180 | 257.9 | 202.2 | 190 | 33 |
| 8 | 120 | 179 | 259.2 | 203.9 | 189 | 33 |
| 9 | 135 | 180 | 259.2 | 203.4 | 189 | 33 |

A stable-window comparison (excluding the first 3 minutes of warm-up and the last 3 samples of
process teardown, 1762 of 1801 samples remaining, first-half vs second-half median): working set
delta -1.7MB, private bytes delta +1.5MB, handles delta 0, threads delta 0 — flat within noise,
confirming the plateau the release and debug passes found, now confirmed to hold for the full
2.5-hour duration rather than just their 15-430s windows. One refinement over the prior passes'
characterization: `private_bytes` shows a genuine gradual ramp from ~195MB (slice 0 median) to
~204MB (slice 3 median onward, then flat) — over tens of minutes here, not the "first 2-3 seconds"
the faster release/debug passes described. This is not a contradiction; it is the same warm-up
phenomenon stretched across more wall-clock time because this run's iterations are ~60x slower
(paced) than the unpaced passes', so an equivalent iteration-count warm-up window naturally spans
more real minutes. It still plateaus and does not continue climbing through the remaining ~105
minutes.

### 5. Session cache: a genuine, but bounded and uncorrelated, growth signal

`session_size_bytes` and `session_approx_num_items` are the one counter in any of the three
passes (release, debug, long-duration) that does **not** plateau within the run: both climb
monotonically across all ten 15-minute slices, with no slice showing a decrease.

```
session_size_bytes:        first=17,326,621  last=17,653,310  delta=+326,689 bytes (+1.9%)
session_approx_num_items:  first=7,620       last=8,421       delta=+801 items (+10.5%)
```

But the growth **decelerates** sharply rather than continuing linearly — the per-slice delta in
`session_size_bytes` falls from 74,646 bytes (slice 0->1) to 7,964 bytes (slice 8->9), a ~9.4x
reduction; `session_approx_num_items`'s per-slice delta falls from 150 to ~31 (one slice, 6->7,
breaks the smooth decay with a 106-item jump — plausibly one previously-uncached index/metadata
object touched, not a new regime). This is the signature of a cache converging toward a working
set ceiling as it absorbs more of the 474 distinct entities' index/metadata footprint, not
unbounded growth — though the still-positive final-slice delta means it had not fully flattened
by minute 150.

**This is the class-3 (Lance session cache growth) discriminator the original profile flagged
as "inconclusive, not exercised as a contrast" — now exercised, and it genuinely moves: the
session cache does grow, continuously, for longer than any process-level OS counter does.**
But — and this is the load-bearing caveat — **this growth does not correlate with retrieval
latency in this soak.** `retrieve_ms` and `full_workflow_ms` stayed flat across the exact same
ten slices the session cache was visibly still growing in (section 4's table). If session-cache
size were the accumulator driving `RetrieveHybrid`'s production slowdown, this soak's own
`RetrieveHybrid`/`AssemblePrompt`/`GenerateAnswer` costs should have tracked the cache's growth
at least partially over 2.5 hours of continuous, real (if decelerating) cache expansion; they
did not. Two readings are both consistent with this: (a) session-cache lookups stay effectively
O(1)/O(log n) regardless of cache size at the range tested here (up to ~8,400 approx items,
~17.65MB), so cache growth alone does not explain the accumulator; or (b) production's cache
would grow to a materially larger size than this soak's single-arm, 474-distinct-row run
reaches — a full paid drive touches far more distinct documents/entities/chunks across many more
queries and (in production) concurrent request patterns this soak's single-threaded sequential
design never generates — at which point a size-dependent cost (eviction churn, lock contention,
or a different internal data structure's complexity class) could still kick in beyond what was
tested here. This pass cannot distinguish (a) from (b); it can only report that at this soak's
scale, real and continuing cache growth did not visibly slow retrieval.

### 6. Telemetry-exporter class and collector state

As in every prior pass, this run has `collector_reachable: false` (the `lancet-collector`
container was stopped for the run's duration, see above) — the telemetry-exporter-queue
accumulator (part of RESEARCH's lowest-ranked candidate 5) remains untested under a genuinely
reachable, actively-exporting collector in every pass to date, release, debug, and long-duration
alike. This is a known, disclosed gap, not new to this round.

### Updated Named accumulator conclusion (2026-09-24, long-duration soak)

**Still not named by measurement — now tested across three independent dimensions: build
profile (release vs. debug), iteration count/wall-time at soak speed (n=300 unpaced, both
profiles), and wall-clock duration with production-matched pacing (n=474, 2.5 hours, debug).**
None of the three passes reproduces `RetrieveHybrid` or full-pipeline latency growth of any
practical magnitude on any tested arm. The long-duration pass specifically closes the gap the
first two passes' own hedge left open ("class 2/3 might need more wall-time than 300
release-speed queries provide to become visible") — this run gave the accumulator not just more
iterations, but 2.5 real hours with production-matched ~17.7-second idle gaps between queries,
and `retrieve_ms`/`full_workflow_ms` still did not trend upward (total implied drift 26.6ms over
474 iterations — noise, not decay, exactly as `flatness_verdict`'s own window prong and the
first-5-vs-last-5 raw comparison confirm).

**What newly moved:** class 3 (Lance session cache growth) gained its first positive evidence
across all three passes — a real, continuous, if decelerating, growth in `session_size_bytes`/
`session_approx_num_items` sustained for the full 2.5-hour run, unlike every process-level OS
counter (working set, private bytes, handles, threads), which plateau within the first
tens-of-minutes across all three passes and then hold flat. But this growth is evidenced to be
**uncorrelated with this soak's own retrieval latency** (section 5) — a genuine finding, not the
absence of one, and one the original profile's discriminator table could not make since it never
had this counter to read. Classes 1 (abandoned CPU work) and 5 (gateway/harness backpressure,
for the already-observed growth episode specifically) remain weakened/excluded as in the prior
passes — nothing in this pass changes either. The telemetry-exporter sub-class of candidate 5
remains untested under a live collector, unchanged from before (section 6).

**No arm, on any build profile, iteration count, or wall-clock duration tested across three full
passes, reproduces the sustained, query-count- or wall-time-proportional growth OI-02 is
investigating.** The single largest unattributed gap — real provider network latency for
embedding/generation (never exercised by this soak's deterministic `FixedEmbeddingPort`/
`FixedGenerator` stand-ins) and production's concurrent, multi-request load pattern (never
exercised by this soak's single-threaded sequential design, on any of the three passes) —
is unchanged from every prior pass. This pass did not attempt either: adding concurrency or a
real provider call would each violate a `must_haves.prohibitions` line from this plan's Task 1
(no provider API call) or introduce a variable ("--workers" — like concurrent load) explicitly
out of this plan's read-only, single-arm-at-a-time soak design.

This section does not select a checkpoint option. Task 3 is re-presented below with the combined
release, debug, and long-duration evidence.

## Checkpoint resolution (2026-09-24)

After three soak rounds — release (n=300), debug (n=300), and a 2.5-hour production-paced debug
run (n=474) — none reproduced production's observed `RetrieveHybrid` growth (06.3.4: median
4.9s to 29.0s). The user's decision: **diagnose-in-drive-1**. Neither `fix-named-class` nor
`mimalloc` is available — both require a named accumulator class, and none was named. The named
accumulator is:

**None (not reproduced at engine level).** Three independent dimensions (build profile,
iteration count/wall-time at soak speed, and 2.5 real hours of production-paced wall-clock time)
all failed to surface sustained, query-count- or wall-time-proportional latency growth in
`RetrieveHybrid` or the full pipeline. The soak's own instrumentation (sub-stage timings, tokio
task/queue counters, process-level OS counters, and — new in the long-duration pass — Lance
session-cache size) is production-faithful (release build, `engine::telemetry::init`, production
ports, production node sequence for arm W) but single-threaded and provider-free by the plan's
own prohibitions, and that is the ceiling of what this soak design can rule in or out.

**Facts recorded for the drive-1 diagnosis (from the orchestrator, supplementing this document's
own soak evidence):**

- **Production ran `--workers 1`.** The 06.3.4 drive that produced the 4.9s->29.0s growth was
  single-worker, sequential, matching this soak's own concurrency (or lack of it). Concurrency
  therefore cannot explain the growth — it was never present as a variable in the episode this
  profile is trying to explain, so the soak's inability to test concurrent request patterns
  (repeatedly flagged above as an untested dimension) is not the missing piece for *this specific
  episode*, even though it remains an open question for production load in general.
- **Query embedding and generation both happen outside the `RetrieveHybrid` span.** Provider
  network latency (real embedding/generation calls, absent from this soak's `FixedEmbeddingPort`/
  `FixedGenerator` stand-ins) cannot explain growth measured *inside* the `RetrieveHybrid` span
  itself, because that span does not include those calls. This narrows, but does not close, the
  soak-to-production gap this document already partially attributed to build profile (3.9-5.6x)
  and ruled out for transport overhead (`docs/... Free evidence from the existing production
  journal`, above).
- **The eval store was not written to during the 06.3.4 drive.** The store's newest `_versions`
  entries date to 2026-09-02; the drive that produced the growth ran 2026-09-09/10. Store
  mutation during the drive (index rebuilds, ingestion, version churn) is therefore not a
  candidate explanation for growth observed on 2026-09-09/10 — the store was static throughout.
- **Class 3 (Lance session cache growth) is recorded as a watch item, not ruled out and not
  named.** The long-duration soak (section "Session cache: a genuine, but bounded and
  uncorrelated, growth signal", above) found the one counter across all three passes that does
  not plateau — `session_size_bytes`/`session_approx_num_items` grow continuously (if
  decelerating) for the full 2.5-hour run — but this growth does not correlate with this soak's
  own retrieval latency, which stayed flat over the same window. Two readings remain open and
  undistinguished: (a) session-cache lookups stay effectively O(1)/O(log n) at the scale tested
  here (~8,400 items, ~17.65MB), so cache growth alone doesn't explain the accumulator; or (b)
  production's cache would need to grow materially larger than this soak's single-arm,
  474-distinct-row run reaches before a size-dependent cost kicks in. Drive 1 should watch
  `session_size_bytes`/`session_approx_num_items` (now logged per-iteration by the P1
  instrumentation from Task 1 and the soak bin) alongside `RetrieveHybrid` sub-stage timings, in
  case a full paid drive's much larger distinct-entity footprint crosses whatever threshold this
  soak's scale did not reach.

**Consequences for downstream plans:**

- **Plan 06.3.4.1-07's Task 1 precondition (fix-named-class or mimalloc, with a named class) is
  NOT met.** This plan named neither. 07 must be skipped or replanned before it runs. The
  orchestrator will take that to the user directly; this plan does not edit `07-PLAN.md`.
- **The D-64 root-cause identification moves to paid drive 1**, via the `retrieve_hybrid_substages`
  P1 tracing added in Task 1 (`open_table_ms`, `checkout_ms`, `dense_ms`, `bm25_ms`, `fusion_ms`,
  `alive_tasks`, `global_queue_depth`, plus the session-cache counters logged by the soak/P1
  instrumentation) and the flatness adapter (`eval/src/lancet_eval/flatness.py`,
  `records_from_run_journal`) built in Task 2, both already reusable against a live drive journal
  with no further engine change.
- **RESEARCH Pitfall 11 applies:** drive 1 runs the old graph seeding, not any seeding fix this
  phase's later plans may introduce. Any cause identified in drive 1 must be verified to hold on
  that old-seeding path, and re-verified once seeding changes land — a fix found under one graph
  seeding regime is not assumed to transfer to the other without a fresh check.
- **D-64 still forbids a restart workaround.** No restart, and no raised timeout budget, is an
  acceptable substitute for a named root cause, in this plan or in drive 1's diagnosis.

This section selects **diagnose-in-drive-1**. No engine code was changed to reach this decision.

## Full-pipeline diagnosis (07)

Plan 07 replaces the class-based fix that plan 03's "diagnose-in-drive-1" resolution deferred:
the OI-02 decay cause is unknown and may be anywhere in the full pipeline (harness, gateway,
gRPC transport, engine process state, provider network), not just inside the engine's own
`RetrieveHybrid` span, so this task establishes zero-cost forensics before any replay or fix is
attempted. All evidence below is read-only: no store write, no engine code change, no paid
provider call.

### Journal forensics (07)

Computed via `eval/src/lancet_eval/oi02.py`'s `forensics` subcommand (`journal_timeline` +
`slice_table`, tested in `eval/tests/test_oi02.py`, 22/22 passing) against
`eval/runs/2026-09-09-multihop_rag/journal.jsonl` (658 records) with
`eval/runs/2026-09-06-measure-multihop_rag/journal.jsonl` (320 records, 06.3.3's two-segment
measurement pass) as contrast. Full output: `forensics/timeline.json`,
`forensics/slices_ordinal.json`, `forensics/slices_wallclock.json`,
`forensics/contrast_timeline.json`, `forensics/contrast_slices_ordinal.json`,
`forensics/hidden_gaps.json`, `forensics/idle_recovery.json`.

**Correction (post-hoc, before Task 2 started):** an earlier revision of this section computed
`slices_ordinal.json` over BOTH arms mixed together and mislabeled the combined
`ExtractGraphContext` column as "graph-off subset". Recomputed per-arm below — the correction
changes the ExtractGraphContext finding materially (see below) and removes an incorrect causal
claim about growth "continuing after RetrieveHybrid is censored". `RetrieveHybrid` growth and
node-order findings are unaffected by this correction.

**RetrieveHybrid growth tracks query ordinal, not per-question content, on both arms.**
50-record slice medians (ordinal order), split by `graph_arm`:

| Slice | graph-on n | RH (on) | AP (on) | EGC (on) | GA (on) | graph-off n | RH (off) | AP (off) | EGC (off) | GA (off) |
|---|---|---|---|---|---|---|---|---|---|---|
| 0 | 50 | 7337.0 | 504.5 | 24153.5 | 6893.0 | 50 | 7200.0 | 539.0 | 343.5 | 5492.5 |
| 1 | 50 | 15249.0 | 796.5 | 40505.5 | 5389.0 | 50 | 15163.0 | 753.0 | 446.0 | 4815.5 |
| 2 | 50 | 23622.0 | 988.0 | 40744.5 | 5074.5 | 50 | 24025.0 | 1008.0 | 574.0 | 5133.0 |
| 3 | 50 | 28765.5 | 993.5 | 41027.0 | 8749.5 | 50 | 29325.0 | 1042.0 | 826.0 | 8625.0 |
| 4 | 50 | 30814.0 | 252.0 | 41319.5 | 4337.0 | — | unavailable | unavailable | 704.5 | unavailable |
| 5 | — | unavailable | unavailable | 41572.5 | unavailable | — | unavailable | unavailable | 766.0 | unavailable |
| 6 | 29 | unavailable | unavailable | 44865.0 | unavailable | 29 | unavailable | unavailable | 844.0 | unavailable |

(RH/AP/EGC/GA = RetrieveHybrid/AssemblePrompt/ExtractGraphContext/GenerateAnswer. Node
execution order per this plan's own read_first is ReformulateQuery -> ExtractGraphContext ->
RetrieveHybrid -> AssemblePrompt -> GenerateAnswer — **ExtractGraphContext runs BEFORE
RetrieveHybrid on every record**, so EGC values in slices where RH is already censored are not
"the mechanism moving to a different node after RH stops" — they are simply the same growth
pattern observed at an earlier point in each of those *later* records' own execution, on
records whose RH observation happens to be unavailable. The two are correlated by ordinal, not
causally sequenced the way the earlier revision implied.)

**Corrected reading: `ExtractGraphContext` on the graph-on arm is enormous and growing
(24,153ms at slice 0 to 44,865ms at slice 6, ~1.9x) — far larger than anything RetrieveHybrid
itself reaches, and structurally distinct from graph-off's ExtractGraphContext (343.5ms to
844ms, ~2.5x, embedding-only, no traversal).** graph-on's ExtractGraphContext includes real
graph-Cypher traversal cost on top of the same embedding call graph-off pays, so its much
larger absolute scale is expected; what is new is that BOTH arms' ExtractGraphContext grow, at
different absolute magnitudes but a broadly similar *shape* (steep early growth, flattening
by slice 3-4) to RetrieveHybrid's own growth on both arms. graph-on's EGC medians
(40,505-44,865ms across slices 1-6) sit close to, but do not clearly exceed, the OLD
(06.3.3-derived) `graph_node_timeout_ms` ceiling of 45,674ms — see the config-ambiguity
discussion below; this is suggestive, not conclusive, since real bounded-traversal (`Google`
at 542-543 edges, `MAX_TOTAL_EDGES=500`) cost could independently plateau near that range with
no timeout involved at all.

**`AssemblePrompt` — a pure in-process, no-network, string-formatting node — grows on both
arms too** (graph-on: 504.5ms -> 993.5ms, ~2.0x through slice 3, then drops to 252.0 at slice
4; graph-off: 539.0ms -> 1042.0ms, ~1.9x through slice 3). A pure-CPU node growing roughly in
step with the network-bound nodes on the SAME ordinal range is still evidence against "it's
just provider network latency getting slower" as a sufficient explanation on its own — some
part of the growth correlates with query count/wall-time across dissimilar node types, which
argues for a process-wide contributor. This reading survives the correction above (it was
never based on the mislabeled EGC column) but is now cross-checked per-arm rather than on a
mixed-arm slice table. The slice-4 AssemblePrompt drop to 252.0ms on graph-on (n=50, so not a
small-sample artifact) is unexplained by this pass and flagged as an open question, not
smoothed over.

**`GenerateAnswer` does not show the same clean monotonic growth** on either arm (graph-on:
6893-8749.5ms with no consistent direction; graph-off: 5492.5-8625ms, similarly non-monotonic)
— plausibly because real generation length/complexity varies per-question and dominates any
smaller systemic drift at that node's own much larger absolute scale.

**Hidden-gap / idle-recovery instrument is not discriminating at production's cadence.**
`hidden_gap_ms` (harness-observed gap between one record's `workflow_meta.completed_at_ms`
and the next record's `started_at_ms`) exceeds the plan's default 2000ms flag threshold on
647 of 658 records — because a single production question already takes several seconds to
several tens of seconds end-to-end at this drive's own pace, so nearly every inter-record gap
trivially exceeds a 2-second threshold calibrated for a much faster harness. This instrument,
as specified, does not isolate genuine retry/idle anomalies from ordinary per-question
sequencing at production's speed; `idle_recovery`'s largest flagged gaps (200s+) sit inside
the already-censored tail (ordinals 597-657, `before_median_ms`/`after_median_ms` both `None`
because `RetrieveHybrid` is unavailable on both sides) and add no new signal beyond what the
slice table above already shows. Disclosed as a limitation, not silently dropped.

**Contrast (06.3.3 two-segment measurement pass, 320 records):** slices 0-2 (before the
documented mid-run restart) grow 3341 -> 9946ms; slices 3-6 (after the restart) flatten at
1558-1801ms — reproducing `06.3.3-MEASUREMENT.md`'s own restart-discriminator finding exactly
from this pass's independent tooling, which cross-validates `journal_timeline`/`slice_table`
against a journal whose ground truth is already published.

**Real query variants vs the soak's stand-ins:** this pass did not additionally re-derive
per-record `reformulation_used`/prompt-token-length variance beyond what `journal_timeline`
already surfaces per-row (`forensics/timeline.json`); the production journal's real questions
and real retrieved-chunk content (never exercised by the soak's `FixedEmbeddingPort`/
`FixedGenerator` stand-ins) remain the single largest category of untested input this pass
still cannot speak to without the full-stack replay in Task 2.

**Fresh-process finding, corrected: N=2, not N=1.** The engine's `completed` query counter's
first post-reset export already reads 1 (OTel Sum instruments are lazily created on first
`add()`, so the first exported point already reflects the first real completion, not a
zero baseline) and reads 2 at the sample immediately before record 1 started (21:05:39.635Z,
0.7s before record 1's 21:05:40.364Z start) — so 2 queries completed on the freshly-restarted
engine before record 1 itself started (plausibly a gateway health-check Ping plus one warm-up
QueryRAG). `fresh_process: true`, `prior_query_count: 2`.

### Launch and config diff (07)

**`drive_era_commit`: `33e774bd71034b54ec14f8079e3cfac83aee56f1`** — the earliest commit
containing every flag the recorded invocation needed (`-S --retries` search over
`eval/src/lancet_eval/cli.py`'s history), matching the planner's own pre-recorded finding
exactly. Committed 2026-09-10T15:31:21-07:00 (2026-09-10T22:31:21Z), roughly 50 minutes AFTER
the drive's own window ended (21:41:24Z) — the drive ran on the working tree as it stood at
`ff3188fb` (`git log -1 --format=%H --before=2026-09-09T21:05:40Z main`), with the `--retries`
CLI code present but uncommitted, then `33e774bd` committed both that code and the drive's own
output (journal, raw_events, config changes) together after the fact.
**`proto_changed_since_drive_era: false`** (`git diff --quiet 33e774bd HEAD -- proto/
engine/src/pb gateway/proto` exits 0) — the wire contract has not moved since the drive.

**`config_toml_at_drive_time`: UNKNOWN — the two candidate readings contradict each other and
neither is fully settled by this pass's evidence.** `33e774bd`'s diff to `config/config.toml`
replaced six 06.3.3-derived production timeout budgets with wider values, and (per the
`drive_era_commit` finding above) that commit landed AFTER the drive — but that alone does not
prove which values were actually live in the uncommitted working tree DURING the drive itself
(the same working tree that also carried the uncommitted `--retries` CLI change).

| Budget | Narrow candidate (`ff3188fb`, committed) | Wide candidate (`33e774bd`, committed) |
|---|---|---|
| `query_embedding_timeout_ms` | 645 | 15000 |
| `retrieve_timeout_ms` | 16647 | 30000 |
| `graph_operation_timeout_ms` | 38595 | 40000 |
| `graph_node_timeout_ms` | 45674 | 60000 |
| `prompt_timeout_ms` | 1070 | 10000 |
| `generation_node_timeout_ms` | 65000 (unchanged either way) | 65000 |

Evidence for each reading, from `engine/src/workflow/runner.rs::run_node` (confirmed this pass
by reading the code, not assumed): each node's timeout IS a hard `tokio::time::timeout` wrapping
just that node's own `node.run()` call, independent of any other node's duration — so a
completed `RetrieveHybrid` observation strictly could not exceed whichever `retrieve_timeout_ms`
was actually live.

- **Against narrow (16647) for `retrieve_timeout_ms`:** graph-on slice 2-4 RetrieveHybrid
  medians are 23,622-30,814ms — 1.4-1.9x OVER 16,647ms. A completed observation cannot exceed
  its own hard per-node timeout, so if these are genuine completions (not censored/timed-out
  observations misread as complete — `journal_timeline` only reports a node duration when
  `node_timings` actually carries that node, which `run.py`'s retry loop only journals from a
  `success`-outcome attempt), `retrieve_timeout_ms` cannot have been 16647 for these records.
- **For narrow (45674) for `graph_node_timeout_ms`:** graph-on's `ExtractGraphContext` medians
  (40,505-44,865ms across slices 1-6) sit just under 45,674ms with an increasing trend toward
  it, never exceeding it — a plausible signature of an actively-enforced ~45,674ms ceiling
  (values clustering under, and creeping toward, a hard cutoff), though real bounded-traversal
  cost plateauing coincidentally in that range cannot be ruled out with this evidence alone.
- **Loki has no effective-settings/startup-timeout log line to settle this directly** — queried
  `{service_name="lancet-engine"} |~ "(?i)timeout|effective|workflow settings"` over the
  21:04:00-21:04:30Z startup window: zero matches (`forensics/loki_log_volume.json` records the
  query used for the adjacent log-volume check; the timeout/effective-settings query itself
  returned no rows and is not separately persisted since it produced nothing to persist).

**Resolution for Task 2:** left as `unknown`, per this task's own explicit allowance to record
"unknown, with sources tried" rather than force an unsupported conclusion. Task 2's replay
copies whichever config `config/config.toml`/`config.eval.toml` present at HEAD (currently the
WIDE values, since `33e774bd` is already committed and nothing later narrowed them back) unless
a specific arm's header records an explicit `-EngineConfigDir` override — this is disclosed as
a fidelity gap against an unresolved historical fact, not silently assumed to match either
candidate. If Task 4's bisection needs to discriminate this specific factor, config value is
available as one bisection arm.

**Production-vs-06.3.3 / production-vs-soak table:**

| Factor | Production (06.3.4 drive) | 06.3.3 segment 2 (flat baseline) | Plan 03 soak |
|---|---|---|---|
| Build profile | **debug** (established, PSReadLine history) | debug (`06.3.3-BUDGETS.md`) | release AND debug both tested |
| Launch command | `cargo run --manifest-path engine/Cargo.toml --bin engine` (no `--release`) | same documented command | `retrieval_soak.exe`, built directly (release or debug), never `cargo run` |
| stdout/stderr sink | console (no redirection found) | unknown, not investigated this pass | console |
| Collector reachable | **true** (established, zero export gaps, continuous Loki logs, no WARN/ERROR, no memory_limiter refusals) | unknown, not investigated this pass | **false** (deliberately stopped, every soak round) |
| `retrieve_timeout_ms` live at run time | **unknown** (16647 or 30000 — contradictory evidence, see above) | 16647 (06.3.3 predates the derivation itself for segment 1; segment 2 post-dates D-16 derivation) | N/A (soak bypasses the workflow's timeout envelope entirely) |
| Path | per-request gRPC service (`service.rs::query_rag`), `tokio::spawn`, gateway trace-context propagated | same | in-process arm loop, no gRPC, no gateway, no trace propagation |
| Ingestion/debounce background tasks | present (`DatabaseManager::initialize`, `spawn_rebuild_debounce_task`, per `main.rs`) | present | absent (soak binary has no ingestion worker) |
| Query text/vectors | real corpus questions, real embeddings | real corpus questions, real embeddings | seeded `nodes`/`entities` rows, `FixedEmbeddingPort`/`FixedGenerator` |
| `workers`/`retries` | `workers=1`, `retries=2` (established, `06.3.4-04-SUMMARY.md`) | workers/retries not recorded in this pass's contrast source | N/A (soak has no retry loop) |
| Engine log volume per 15-min window | declines over the drive (233K at window 2 -> ~20K by window 15), tracking declining query throughput, not per-query growth (`forensics/loki_log_volume.json`) | not investigated this pass | N/A (soak has no OTLP-bound logging by design) |
| WARN/ERROR-level engine log lines | **zero**, entire drive window (queried) | not investigated this pass | N/A |

**No level filter on any tracing layer, and the fmt layer's stdout->stderr move
(`784b8972`) predate the drive** — both already-known facts from the plan's own read_first
list, re-confirmed present at `ff3188fb` by inspection; not re-derived from scratch this pass
since the planner had already established them.

### Ranked hypotheses (07)

Ranked by evidence strength. "Soak coverage" states whether plan 03's soak (release + debug +
2.5h production-paced, all single-threaded, in-process, provider-free) could structurally have
seen this factor at all.

**1. Process-wide resource degradation across the full gRPC/tokio service path — NOT a
soak-testable path, and the strongest-supported hypothesis.**
- **Supports:** `AssemblePrompt` (pure CPU, no network, no LanceDB) grows ~2x on BOTH arms
  in the same ordinal range RetrieveHybrid and ExtractGraphContext also grow (corrected
  per-arm reading — see Journal forensics above). A pure-CPU node's duration correlating with
  ordinal/wall-time, on both arms independently, is evidence against "it's only provider
  network latency" as a sufficient explanation — some part of the growth correlates across
  dissimilar node types, which argues for a process-wide contributor. This is the one new
  piece of evidence this pass adds that no soak round could have produced.
- **Contradicts:** `GenerateAnswer` shows no clean monotonic trend on either arm — if the
  mechanism were purely process-wide and strictly proportional, the largest-magnitude node
  should show it most clearly; instead its variance looks dominated by per-question answer
  length, which may simply be masking a smaller proportional drift at its own larger scale.
  The graph-on AssemblePrompt slice-4 drop to 252ms (n=50) is also unexplained and not
  obviously consistent with a smooth process-wide accumulator.
- **Soak coverage:** structurally impossible. Plan 03's soak never leaves the engine process
  (no gRPC, no `tokio::spawn` per-request task, no gateway, no trace-context propagation, no
  concurrent background ingestion/debounce tasks). Every soak round explicitly ruled out
  process-level OS counters (working set, handles, threads) as an explanation, but never tested
  the per-request service path itself.
- **Discriminating replay/bisection:** Task 2's full-stack replay running the real `engine`
  binary behind the real gateway is the first test this hypothesis has ever received. If the
  replay's `AssemblePrompt` also grows without any real provider call in the loop (stub-served),
  that isolates the mechanism to the engine/gateway/transport path, independent of provider
  network variance.

**2. Debug-build-specific degradation, compounding rather than replacing hypothesis 1.**
- **Supports:** production's own launch is now established as `debug`, not `release`
  (PSReadLine history, zero `--release` invocations found). The already-published
  release-vs-debug soak comparison (`06.3.4.1-03`) shows a consistent 3.9-5.6x baseline
  multiplier from build profile alone — a real, measured effect, though previously only
  connected to the *static* baseline gap, never to *growth*. Debug builds carry unoptimized
  allocation/drop code paths that could make any per-request allocation-heavy pattern (Rust
  string/Vec churn in prompt assembly, protobuf (de)serialization, tracing span construction)
  cost measurably more per call as heap fragmentation or allocator bookkeeping grows with
  request count — a mechanism release optimization could mask or eliminate.
- **Contradicts:** the debug-build soak (both the n=300 and the 2.5h n=474 production-paced
  passes) found NO growth at soak scale, on the same debug binary type, including for
  process-level allocator-adjacent counters (working set, private bytes) — those plateaued.
- **Soak coverage:** partial. The soak tested debug-build allocation cost in isolation, but
  never combined with the concurrent gRPC/tokio service-task pattern hypothesis 1 covers — the
  soak's debug pass and hypothesis 1's untested path have never been tested together.
- **Discriminating replay/bisection:** Task 2's replay defaults to the established debug
  launch; Task 4's bisection ladder tests `release` explicitly as one factor, which
  simultaneously discriminates both hypotheses 1 and 2 if growth disappears under release.

**3. Lance session cache growth (class 3, carried forward from 06.3.4.1-03) — watch item,
not newly strengthened or weakened by this pass.**
- **Supports:** the long-duration soak already found real, continuous (if decelerating)
  `session_size_bytes`/`session_approx_num_items` growth, uncorrelated with that soak's own
  flat retrieval latency at the scale tested (~8,400 items, ~17.65MB). Production's much larger
  distinct-entity footprint across 658 records (vs the soak's 474-1000-row single-arm sample)
  could cross a size threshold the soak never reached.
- **Contradicts:** nothing new this pass; the soak's own finding (growth without latency
  correlation, at its tested scale) stands unchanged.
- **Soak coverage:** already exercised once (06.3.4.1-03's long-duration pass); not re-tested
  this pass.
- **Discriminating replay/bisection:** the `request_process_state` instrumentation
  (`session_approx_num_items` on every request, `session_size_bytes` every 50th) that
  06.3.4.1-03 already added stays live through Task 2's replay and Task 4's bisection —
  free to observe on every arm without new engine work.

**4. Telemetry/logging-exporter path (collector-reachable, no-level-filter, console-sink
combination) — new this pass, structurally the cleanest surviving difference from the soak.**
- **Supports:** production ran with the collector reachable throughout (zero export gaps) and
  no tracing-layer level filter (all three OTLP exporters plus the fmt layer active on every
  span/event, including the enormous h2/tonic TRACE-level transport frame logging directly
  observed in Loki for this session). Every soak round to date ran with the collector
  deliberately stopped (`collector_reachable: false`), so this exact combination — an actively
  exporting collector plus unfiltered TRACE-level spans under real per-request gRPC/tokio load
  — has never been tested.
- **Contradicts:** the log-VOLUME-over-time check this pass performed (`sum(count_over_time(...))`
  per 15-minute window) shows volume DECLINING over the drive (233K -> ~20K), tracking declining
  query throughput as RetrieveHybrid grows, not growing per-query — so simple "logs pile up and
  slow things down" is not directly supported by the volume trend alone. Zero WARN/ERROR-level
  lines were found anywhere in the window, ruling out an error-driven log storm specifically.
- **Soak coverage:** structurally impossible for the collector-reachable state specifically
  (every soak round stopped the collector by design); the no-level-filter condition itself was
  present in every soak round's own binary but never combined with a reachable collector under
  real request load.
- **Discriminating replay/bisection:** this is exactly Task 2/Route A's first three default
  arms (`-Observability off`, the stderr sink factor, and engine console-only telemetry via
  `-EngineConfigDir`) — already planned, not a new task this finding adds, but this pass
  provides the evidence for why those three arms are ordered first.

**5. Abandoned CPU work outliving a timed-out/cancelled future (class 1) — still weakened,
not confirmed, unchanged from 06.3.4.1-03.**
- **Supports:** IF the narrow `retrieve_timeout_ms` (16647) candidate turns out to be the one
  live during the drive, many individual RetrieveHybrid calls in the later slices genuinely
  did time out in production — a real-world instance of exactly the abandoned-future condition
  RGT's soak arm simulated (300/300 guaranteed timeouts). This support is now conditional on
  the config-ambiguity above being resolved toward the narrow reading, not unconditional as the
  earlier revision of this section stated.
- **Contradicts:** RGT's own soak arm (guaranteed real abandonment on every iteration) showed
  zero measurable effect on `alive_tasks`/`global_queue_depth` and no effect on subsequent
  `RetrieveHybrid` timings, across 300 release iterations, 300 debug iterations, and — via the
  W-arm long-duration pass's own flat `alive_tasks=3` throughout — no accumulation signal
  visible on the counters this soak could read. Also: the graph-on `RetrieveHybrid` medians
  themselves (23,622-30,814ms, slices 2-4) exceed the narrow 16,647ms candidate, which is only
  possible if those are genuinely non-timed-out completions — direct evidence against the
  narrow candidate specifically for RetrieveHybrid, independent of whichever candidate turns
  out to be correct for `graph_node_timeout_ms`.
- **Soak coverage:** directly tested (RGT arm), all three soak passes.
- **Discriminating replay/bisection:** Task 2's replay carries the same
  `alive_tasks`/`global_queue_depth`/session-cache instrumentation into a real multi-hundred-
  record production-paced run; if those counters stay flat there too while RetrieveHybrid
  grows, this hypothesis is further weakened under real conditions, not just soak conditions.

**6. Gateway/harness/transport backpressure (class 5) — ruled out for the already-observed
growth episode, unchanged from 06.3.4.1-03.**
- **Supports:** none newly found this pass.
- **Contradicts:** 06.3.4.1-03's zero-cost wall-minus-nodes analysis already showed a flat
  4-5ms transport gap through slices 0-6 while `RetrieveHybrid`'s own node-measured time grew
  4.9s->29.0s — the growth is fully inside the engine's own instrumentation before any
  transport is added. This pass's `journal_timeline` re-derivation (via `hidden_gap_ms`, a
  different but related metric — harness-to-harness inter-record gap rather than
  wall-minus-sum-of-nodes) did not surface a new discriminating signal (see the "not
  discriminating at production's cadence" finding above) but does not contradict the prior
  finding either.
- **Soak coverage:** structurally impossible (no gateway/harness in the soak); already
  substituted for by the zero-cost journal analysis, not a gateway-in-the-loop build (documented
  as not built, 06.3.4.1-03 §3).
- **Discriminating replay/bisection:** Task 2's real gateway-in-the-loop replay is the first
  actual test of this hypothesis under live conditions (not just journal post-hoc analysis);
  expected outcome, given the prior finding, is that growth reproduces with the gateway in the
  loop and gateway-side counters (`otlp_4317_conns`, RPC-level timings) stay flat.

**Net ranking for Task 2/4:** hypothesis 1 (process-wide, full-path degradation) is the lead
candidate, specifically because it is the one class of explanation the soak was structurally
incapable of testing and for which this pass found genuinely new corroborating evidence
(`AssemblePrompt` growth on both arms, correctly attributed after the per-arm correction).
Hypothesis 2 (debug build) is not a competing alternative but a probable amplifier, testable as
one bisection factor. Hypothesis 4 (telemetry/logging path) is new this pass and maps directly
onto Route A's already-planned first three arms. Hypothesis 5 (abandoned CPU work) is now
explicitly conditional on the unresolved config ambiguity rather than unconditionally
supported. Hypotheses 3 and 6 stay as previously characterized — none newly strengthened, none
newly ruled out beyond what 06.3.4.1-03 already established.

### Full-stack replay (07)

**Arm:** `replay/pre-fix/primary/` — `full-stack`, HEAD commit `f76264a0`, debug build, launched via
`cargo run --manifest-path engine/Cargo.toml --locked --bin engine` (matches `forensics.json`'s
established `engine_launch_command`), console stderr sink (matches production's established
`stdout_stderr_sink`), observability on with `telemetry: otlp` for both engine and gateway
(matches production's established `collector_reachable: true`), store source reconciled
(`data/lancedb-replay`), workers 1, retries 2 (fixed), 175 questions / 350 records, stub delays
343ms embed / 6970ms chat (rounded down from `forensics.json`'s `stub_delay_defaults` 343.5/6970.5
— disclosed, not tuned after seeing data). Ran 2026-09-25T07:48:08Z–08:45:35Z (57m 26s wall
clock), harness exit 0, observed notional spend $0.1070 against the $1.00 stage cap.

**M1 (baseline, store-matched), not met.**
```
primary_slice0_ms:   1271.0 ms   (replay's first-50 RetrieveHybrid median)
denominator (soak_w_ms_reconciled): 270.06685 ms   (retrieval_soak --arm W --iterations 50
                                                     against data/lancedb-replay, debug build,
                                                     collector_reachable:false)
ratio:                4.7062
target (m1_prod_ratio.debug): 16.9538   (needs 0.5x-2x band: 8.4769-33.9076)
status: NOT MET
```
The soak denominator was measured twice. The first pass (373.6628 ms) ran with
`collector_reachable: true`, because `lancet-collector` was already running as a persistent
dev-stack container unrelated to this replay. Plan 03's own arm-W debug reference (289.6 ms, the
`m1_prod_ratio` denominator) was measured with `collector_reachable: false`
(`soak/debug/W.jsonl` header, confirmed by inspection). The collector was stopped for a second
pass (`docker stop lancet-collector`, ~50s downtime, restarted immediately after) to match that
condition; `soak-baseline.json` records both readings, with the corrected one live. The
correction moves the ratio from 3.40 to 4.71 — still well outside the 8.48-33.9 band either way,
so the "not met" verdict does not depend on which soak reading is used.

**M2 (growth), not grows.**
```
slice0_ms:        1271.0 ms
later (slice-3):  1296.5 ms   (ratio 1.02x — "grows" needs >=1.5x, "not grows" needs <1.2x)
window_delta_ms:   237 ms     ("grows" needs >=500ms with positive slope; "not grows" needs <500ms)
class: NOT GROWS
committed flatness_verdict (full run, n=350, unmodified): passed=false, reason=decay_present,
  slope_ms_per_query=0.909, window_delta_ms=237  (the hypersensitive committed trend prong; per
  plan 03's own finding this fires on single-digit-ms-per-window drift at this n and is not
  usable to classify arms on its own — reported here unmodified as the plan requires)
committed flatness_verdict (first 200, unmodified): passed=true, reason=flat,
  slope_ms_per_query=0.389, window_delta_ms=243
```
`order_match_production: true`. `outcome_counts: {"success": 350}` — no errors, no timeouts, no
retries consumed.

**Fidelity table (production vs. replay, 50-record slices by ordinal; production is the 06.3.4
journal's ordinal slices):**

| Slice | RetrieveHybrid (prod / replay) | ExtractGraphContext (prod / replay) | AssemblePrompt (prod / replay) | GenerateAnswer (prod / replay) |
|---|---|---|---|---|
| 0 | 4,910.5 / 1,271.0 | 2,564.0 / 586.5 | 453.0 / 260.0 | 6,970.5 / 7,245.0 |
| 1 | 10,441.0 / 1,433.5 | 4,711.5 / 633.0 | 574.0 / 266.0 | 5,426.0 / 7,257.5 |
| 2 | 13,947.0 / 1,250.0 | 5,925.0 / 591.5 | 725.5 / 263.5 | 3,427.0 / 7,249.5 |
| 3 | 16,785.5 / 1,296.5 | 6,629.5 / 576.0 | 786.0 / 263.5 | 6,275.0 / 7,255.5 |
| 4 | 21,254.0 / 1,350.5 | 8,519.0 / 739.0 | 957.5 / 287.5 | 4,847.0 / 7,267.5 |
| 5 | 24,626.0 / 1,537.0 | 10,879.5 / 674.0 | 1,019.5 / 293.0 | 5,419.0 / 7,272.0 |
| 6 | 28,960.0 / 1,554.0 | 12,876.0 / 619.5 | 998.0 / 297.0 | 8,898.5 / 7,268.5 |
| growth (slice6/slice0) | 5.90x / 1.22x | 5.02x / 1.06x | 2.20x / 1.14x | 1.28x / 1.00x |

Production grows on every node — including `AssemblePrompt`, pure CPU with no network or
LanceDB call, and `GenerateAnswer`, whose replay figure is pinned flat by design (a fixed
6970ms stub chat delay). The replay stays essentially flat on every node. Whatever mechanism
produced production's cross-node growth did not activate under this arm's conditions, even
though this arm reproduces two of the four combinations the ranked hypotheses called out as
never-before-tested: the real per-request gRPC/tokio service path with `tokio::spawn` (hypothesis
1) AND the collector-reachable, no-level-filter, console-sink combination under real load
(hypothesis 4). Neither, alone or together, was sufficient to reproduce growth here — evidence
against both hypotheses in their simple form, not for them.

`graph_notice_counts: {"GRAPH_ABLATION": 175, "GRAPH_UNAVAILABLE": 175}` — every one of the 350
records carries a graph notice; graph traversal effectively never found a usable seed.
`stub-stats.json` explains why: `{"models":1,"embeddings":350,"chat_completions":350,
"embedding_fallback_count":350}` — **100% of embedding calls used the hash fallback vector**,
none used the intended per-question top-ranked-chunk vector from the 06.3.4 journal.
`data/replay/stub-vectors.jsonl` was never built: Task 2's action text specifies
`retrieval_soak --export-embeddings` as a distinct step to produce that file before the primary
run, and `header.json` confirms `"stub_vector_source": "hash-fallback-only"`. This is a real,
disclosed fidelity gap, not a fix: it means `ExtractGraphContext`'s replay figures above reflect
graph traversal against random seeds, not production's real ones, which is the single most
likely reason `ExtractGraphContext`'s 5.02x production growth (the second-largest of any node)
did not reproduce at all. This is presented as evidence for Task 3, not corrected unilaterally —
building the vector map and re-running is exactly what the checkpoint's `rerun-primary` option
exists for.

**Process-counter and session-cache trends** (from `proc.csv`, 555 five-second samples, and
Loki `request_process_state` events — the engine's console stderr sink means these events were
never captured to a local file per this arm's config, so they were queried from Loki instead of
`engine-events.jsonl`, which does not exist for this arm; 255 of 350 requests' events were found
in Loki, request_ordinal 1-349, a partial-capture gap attributed to OTLP batching intervals, not
investigated further this pass):
- `working_set`: 179 MB → 263 MB over the run (modest, not runaway); `handle_count`: 183 → 205;
  `thread_count`: stable in the 38-50 range.
- `egress_443`: 0 throughout (matches `header.json`'s `egress_443_max: 0`). `otlp_4317_engine`
  stayed at 3 connections; `otlp_4317_gateway` varied 2-3.
- `session_approx_num_items`: 7,584 (ordinal 1) → 7,585 (ordinal 349) — flat. No ingestion ran
  during this replay (retrieval-only), so this is expected, not evidence against hypothesis 3.
- `alive_tasks`: 14-18 throughout, no trend. `global_queue_depth`: 0 for every sampled request —
  no backpressure accumulation.

None of the process-level counters this instrumentation can see show a trend that would explain
production's growth. Combined with the fidelity table, this arm's evidence points toward either
(a) a mechanism tied to real provider content/timing that the stub cannot produce, or (b) a
mechanism present in the drive-era code/environment but no longer present, or differently
triggered, at HEAD — which is exactly what Route B's first arm (drive-era whole stack) is
designed to distinguish.

**Live store and schema untouched, proof:** `live-state.before.json` and `live-state.after.json`
(the live `data/lancedb-eval` table versions and `lancet_eval` row counts, read-only) are
byte-identical. `copy-state.before.json`/`copy-state.after.json` (the store copy's table
versions and the scratch DB's pending reconciliation-intent count) are also identical.
`uv run --project eval lancet-eval identity check --corpus multihop_rag` passed after the run.
`header.json` records `paid: false`, `egress_443_max: 0`, and
`spend_guard: "dummy key; egress sampling is supporting evidence only"`. No real provider call
was possible: `OPENROUTER_API_KEY` was a dummy value, and the three endpoint env vars pointed
only at the loopback stub.

### Full-stack replay: `primary-r2` (07, supersedes `primary` per the Task 3 `rerun-primary` resolution)

**Why this arm exists.** Task 3's checkpoint was resolved `rerun-primary`: the reading above is
retained as the record of the first (invalid) attempt, but per the resolution text, **every
later reference to the primary run now resolves to `primary-r2`** — this subsection is the
active M1/M2 reference for Task 4 routing.

**The fix, built before this run** (full detail: `07-HANDOFF.md`). `data/replay/stub-vectors.jsonl`
was never built for the first primary; `retrieval_soak --export-embeddings` existed in source
(Task 2) but the caller-side pipeline to invoke it — matching each replay question to production's
own top-ranked retrieved chunk, exporting that chunk's stored vector from the reconciled store
copy, and keying the stub's vector map by the exact embedded text — did not exist yet. Built as
two new `oi02.py` subcommands (`extract-chunk-ids`, `build-stub-vectors`; RED `12e965d8`, GREEN
`0d339ae3`), keyed by the exact question text the engine embeds (`ctx.variants[0]` ==
`ctx.original_query` unmodified, confirmed by reading `graph_context.rs` and `workflow/mod.rs` —
no query reformulation ever fired across all 350 first-primary records, so no risk of a derived
embed input missing the map):

```
replay_questions:          175
matched_to_prod_top_chunk: 174   (1 question had no production top chunk recorded)
unique_chunk_ids:          139
exported_from_store_copy:  137   (2 chunk ids absent from the reconciled copy -- D-88 store drift)
stub_vectors_matched:      172   (98.3%; 3 questions fall back to the stub's deterministic hash
                                   vector: 1 no_prod_chunk + 2 no_embedding)
```

**Smoke validation before launch** (`-Label smoke -Questions 9`, reusing the store copy; results
reverted afterward — diagnostic only, not a plan-required artifact): `stub-stats.json` showed
`embedding_fallback_count: 0/18` (every smoke question was among the 172 matched). **18/18
records' top-ranked retrieved chunk exactly matched production's top-ranked chunk** for that same
question — the strongest available proof the vector map does what it claims. **6/9 graph-on
records got real graph traversal** (empty notices, `ExtractGraphContext` ~1200-2600ms) versus
3/9 `GRAPH_UNAVAILABLE` (timeout/no-seed variance, not a vector defect) — a categorical change
from the first primary's 0/350 real-seed rate. Cleared to launch.

**Arm:** `replay/pre-fix/primary-r2/` — `full-stack`, HEAD commit `5e0914a9`. Every parameter
matches `primary/header.json` verbatim except `stub_vector_source`
(`hash-fallback-only` → `data/replay/stub-vectors.jsonl`): debug build, `cargo run
--manifest-path engine/Cargo.toml --locked --bin engine`, console stderr sink, observability on
(`telemetry: otlp` both processes), store source reconciled (`data/lancedb-replay`, reused —
not recopied), workers 1, retries 2 fixed, 175 questions / 350 records, stub delays 343ms
embed / 6970ms chat. Ran 2026-09-25T19:22:19Z–20:21:53Z (59m 34s wall clock) — comparable to
`primary`'s 57m 26s, not the multi-hour production-scale duration a reproduced growth would
have produced. Harness exit 0, 350/350 success, no errors/timeouts/retries consumed.

**M1 (baseline, store-matched), not met.**
```
primary_slice0_ms: 1441.5 ms   (replay's first-50 RetrieveHybrid median)
denominator (soak_w_ms_reconciled): 270.06685 ms
ratio:              5.3376
target (m1_prod_ratio.debug): 16.9538   (0.5x-2x band: 8.4769-33.9076)
status: NOT MET
```
Slightly higher than the first primary's 1271.0ms/4.71 ratio, consistent with slice 0 now doing
real (occasionally successful) graph traversal instead of uniform ablation — still well outside
the band either way. Tracked as `### Starting gap (07)` below, per the plan's routing: M1 and M2
are read independently and neither blocks the other.

**M2 (growth), not grows.**
```
slice0_ms:        1441.5 ms
later (slice-6):  1747.5 ms   (ratio 1.21x -- just over the "not grows" <1.2x line but nowhere
                                near the "grows" >=1.5x line; the classifier's own later_slice
                                field uses slice-3, 1405.5ms, ratio 0.975x)
window_delta_ms:   265 ms     ("grows" needs >=500ms with positive slope; "not grows" needs <500ms)
class: NOT GROWS
committed flatness_verdict (full run, n=350, unmodified): passed=false, reason=decay_present,
  slope_ms_per_query=0.631, window_delta_ms=265  (hypersensitive trend prong, same class of
  single-digit-ms-per-window drift plan 03 found unusable for arm classification -- reported
  unmodified as the plan requires)
committed flatness_verdict (first 200, unmodified): passed=true, reason=flat,
  slope_ms_per_query=-0.471, window_delta_ms=-377
```
`order_match_production: true`. Real graph seeding did not reproduce growth: RetrieveHybrid's
per-slice medians (1441.5 / 1483.5 / 1311.0 / 1405.5 / 1304.5 / 1569.0 / 1747.5 ms) stay flat
within normal run-to-run noise, nothing resembling production's 4,910→28,960ms climb.

**Fidelity table (production vs. `primary-r2`, 50-record slices by ordinal):**

| Slice | RetrieveHybrid (prod / replay) | ExtractGraphContext (prod / replay) | AssemblePrompt (prod / replay) | GenerateAnswer (prod / replay) |
|---|---|---|---|---|
| 0 | 4,910.5 / 1,441.5 | 2,564.0 / 645.5 | 453.0 / 313.0 | 6,970.5 / 7,295.0 |
| 1 | 10,441.0 / 1,483.5 | 4,711.5 / 602.0 | 574.0 / 289.5 | 5,426.0 / 7,282.5 |
| 2 | 13,947.0 / 1,311.0 | 5,925.0 / 710.0 | 725.5 / 290.5 | 3,427.0 / 7,281.0 |
| 3 | 16,785.5 / 1,405.5 | 6,629.5 / 858.5 | 786.0 / 283.5 | 6,275.0 / 7,274.5 |
| 4 | 21,254.0 / 1,304.5 | 8,519.0 / 771.5 | 957.5 / 281.0 | 4,847.0 / 7,272.0 |
| 5 | 24,626.0 / 1,569.0 | 10,879.5 / 747.0 | 1,019.5 / 307.0 | 5,419.0 / 7,292.0 |
| 6 | 28,960.0 / 1,747.5 | 12,876.0 / 672.0 | 998.0 / 341.5 | 8,898.5 / 7,317.5 |
| growth (slice6/slice0) | 5.90x / 1.21x | 5.02x / 1.04x | 2.20x / 1.09x | 1.28x / 1.00x |

The result is essentially unchanged from the first (invalid) primary's fidelity table: production
still grows on every node, including the pure-CPU `AssemblePrompt` node, and the replay stays flat
on every node — now with real graph seeding ruled out as the explanation for
`ExtractGraphContext`'s non-growth specifically. **This closes the ranked hypothesis that random
(non-seeded) graph traversal was suppressing `ExtractGraphContext`'s reproduction of growth**: with
real seeds, most graph-on records now succeed (below), and `ExtractGraphContext`'s replay figures
are structurally different in kind (real traversal work, not embedding-fallback noise) — yet the
growth still does not appear on this or any other node.

`graph_notice_counts: {"GRAPH_ABLATION": 175, "GRAPH_DEGRADED": 1, "GRAPH_UNAVAILABLE": 65}` — the
175 `GRAPH_ABLATION` records are exactly the graph-off arm (expected, unrelated to seeding).
Of the 175 graph-on records, 65 hit `GRAPH_UNAVAILABLE` (timeout/no-seed) and 1 `GRAPH_DEGRADED`,
leaving **109/175 (62.3%) with real, successful, unflagged graph traversal** — a categorical
change from the first primary's 0/175. `stub-stats.json`: `{"models":1,"embeddings":350,
"chat_completions":350,"embedding_fallback_count":6}` — 6/350 embedding calls (3 questions × 2
arms) used the hash fallback, matching the disclosed 172/175-matched fallback count exactly.

**Process-counter trends** (`proc.csv`, 497 five-second samples; `engine-events.jsonl` was not
produced for this arm — `stderr_sink: console` matches `primary`'s and production's established
sink, so per-request `request_process_state` events go to Loki, not a local file, same
documented limitation as the first primary's write-up, not a new gap):
- `working_set`: 170 MB → 227 MB over the run (max 297 MB) — modest, not runaway.
- `handle_count`: 187 → 198 (max 235); `thread_count`: 41 → 42 (max 67, transient).
- `egress_443`: 0 throughout every sample (matches `header.json`'s `egress_443_max: 0`, confirms
  no real provider call was possible). `otlp_4317_engine`: constant 3 connections throughout.

No process-level counter shows a trend that would explain production's growth, mirroring the
first primary's finding.

**Live store and schema untouched, proof:** `live-state.before.json`/`live-state.after.json` are
byte-identical; `copy-state.before.json`/`copy-state.after.json` are byte-identical.
`uv run --project eval lancet-eval identity check --corpus multihop_rag` passed after the run.
`header.json` records `paid: false`, `egress_443_max: 0`,
`spend_guard: "dummy key; egress sampling is supporting evidence only"`.

**Routing conclusion (Task 3/4 table).** M2 is **not grows** (whatever M1 reads) — this routes to
**Route B: start with the drive-era whole stack** (worktree at `drive_era_commit`
`33e774bd71034b54ec14f8079e3cfac83aee56f1` from `forensics.json`, run against a pre-reconcile
store copy). M1 is **not met**, tracked separately below as the starting gap; per the plan's
routing rule this never overrides Route B's growth-driven choice. The HANDOVER's hint — production
grew on every node including pure-CPU `AssemblePrompt` (2.2x) — still stands and is weighed first
among Route B's own step 4 free-diagnosis factors if the drive-era arm itself does not grow
either.

### Starting gap (07)

M1 is not met on either the first primary (ratio 4.71) or `primary-r2` (ratio 5.34) — both well
below the 8.48-33.91 band `m1_prod_ratio.debug` requires. Real graph seeding moved the ratio
slightly (4.71→5.34) but not into the band, so **seeding realism is not the starting-gap
mechanism** either. Not yet tested for free: build profile (`release` is a Route A default-order
factor, not yet run), collector reachability at soak-baseline measurement time (already
controlled for per the first primary's write-up), and the drive-era code/launch combination —
which Route B's first arm tests for both the growth and the starting gap simultaneously. No
arm has yet isolated a starting-gap-specific factor; this section will be updated once Route B's
arms produce evidence either way.
