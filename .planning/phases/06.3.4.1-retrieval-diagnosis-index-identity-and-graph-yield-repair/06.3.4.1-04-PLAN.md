---
phase: 06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair
plan: 04
type: execute
wave: 2
depends_on: [06.3.4.1-01, 06.3.4.1-02]
files_modified:
  - engine/src/bin/inspect_lancedb.rs
  - engine/src/inspect_lancedb_tests.rs
  - engine/src/bin/reconcile_eval_store.rs
  - engine/src/reconcile_eval_store_tests.rs
  - scripts/engine-test-targets.sh
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RECONCILE-DRYRUN.md
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/reconcile/dryrun.json
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/reconcile/pg_extras.json
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/reconcile/document_ids_pre.json
autonomous: true
requirements: [OBS-05, DATA-04]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The reconcile is surgical, not a full re-seed and not a query-time filter: `reconcile_eval_store` deletes every document not in `document_map.json` `entries` (aliases are not allowed) from `edges`, `nodes`, `documents`, `staged_documents_v2` and `entity_edges`, in that dependent-first order (D-58; D-56 honoured: no full re-seed)."
    - "Entity provenance is pruned by the surviving-chunk allow-list, not by extra-document prefix. An entity with no surviving chunk is an orphan and is deleted with its incident `entity_edges`. An entity with some surviving chunks is rewritten with every other column preserved (`name_vector`, `summary`, `summary_vector`, `unsummarized_refs`, `community_ids`). Pre-existing degree-0 entities are untouched (D-58)."
    - "The alias document `0370301a…` is treated as an extra, so its rows are deleted while the alias entry stays in `document_map.json` as a historical lookup (D-60)."
    - "`--dry-run` is the default. `--apply` refuses unless `--snapshot-dir` exists and contains a copy of `nodes.lance`, and the bin refuses any LanceDB path other than the configured eval path. It never calls optimize, compaction or cleanup_old_versions, so LanceDB version restore stays available (D-58 reversibility)."
    - "Pre-reconcile counts, deleted document IDs, per-table versions and the 21 unidentified PostgreSQL rows are recorded from a live read-only census before anything is deleted (D-62 before-record; RESEARCH A1 checked against a store listing)."
    - "The prune is idempotent: running the reconcile twice leaves the second run with zero extras, zero rewrites and zero orphans (specless edge: idempotency, DATA-04)."
    - "A targeted mode `--target-document-ids <file>` deletes only the listed mapped IDs through the same cascade, for D-59 re-ingest (06.3.4.1-10); it uses the same snapshot and isolation guards."
  artifacts:
    - path: "engine/src/bin/reconcile_eval_store.rs"
      provides: "dry-run-by-default cascade reconcile with apply guards"
    - path: "engine/src/reconcile_eval_store_tests.rs"
      provides: "synthetic temp-store tests for the cascade"
    - path: "engine/src/bin/inspect_lancedb.rs"
      provides: "`--document-ids` mode (JSON contract shared with identity.py)"
      contains: "DocumentIds"
    - path: ".planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RECONCILE-DRYRUN.md"
      provides: "exact extras, per-table row counts, entity rewrite/orphan counts, PG extras, versions"
  key_links:
    - from: "eval/src/lancet_eval/identity.py"
      to: "engine/src/bin/inspect_lancedb.rs"
      via: "`--document-ids` JSON {documents,nodes,edges,entity_edges,staged_documents_v2_rows}"
      pattern: "staged_documents_v2_rows"
    - from: "engine/src/bin/reconcile_eval_store.rs"
      to: "engine/src/ingest.rs"
      via: "dependent-first delete order and entity delete-then-add pattern (1255-1345), with columns carried through instead of nulls"
      pattern: "entities_schema"
  prohibitions:
    - "The reconcile bin must not call optimize, compaction or cleanup_old_versions on any table"
    - "The entity rewrite must not write null for summary, summary_vector, unsummarized_refs or community_ids when the source row had a value"
    - "No row may be deleted by this plan: every live run in Task 3 is dry-run or read-only"
---

<objective>
Build the tooling for layer 2 (index identity, OI-03) and take a read-only census of what it would
delete: a `--document-ids` listing mode, the cascade reconcile bin with a dry-run default and apply
guards, and a committed dry-run record.

Purpose: D-58 is one-way. The user confirms the exact dry-run numbers at 06.3.4.1-06's checkpoint
before anything is deleted, so the dry run has to be exact and committed. RESEARCH identifies the four
LanceDB extras only by inference (A1) and the 21 PostgreSQL extras not at all; this census makes both
concrete.

Output: two Rust modes/bins with tests, and `06.3.4.1-RECONCILE-DRYRUN.md` plus JSON ID lists.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-CONTEXT.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RESEARCH.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-PATTERNS.md
@.planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-STORE-BASELINE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: inspect_lancedb --document-ids listing mode</name>
  <files>engine/src/bin/inspect_lancedb.rs, engine/src/inspect_lancedb_tests.rs, scripts/engine-test-targets.sh</files>
  <read_first>
    - `rust-guidelines.md`
    - `engine/src/bin/inspect_lancedb.rs` (as extended by 06.3.4.1-01: `InspectMode`, `USAGE`, `parse_args`, `mode_count`, `main`; `inspect_graph_population` 392-531 for the six accessor calls)
    - `eval/src/lancet_eval/identity.py` (06.3.4.1-02: the JSON contract consumer)
    - `engine/src/db/mod.rs:154-177,299-325` (accessors, `staged_documents_table`)
  </read_first>
  <behavior>
    - On a synthetic store with documents {A, B}, nodes for {A, B, C}, entity_edges for {A}, edges for {B}, and 2 staged rows, the output equals `{"documents":["A","B"],"nodes":["A","B","C"],"edges":["B"],"entity_edges":["A"],"staged_documents_v2_rows":2}` (sorted, de-duplicated).
    - An empty store yields empty lists and 0.
  </behavior>
  <action>
    Add `InspectMode::DocumentIds` with flag `--document-ids` (added to `USAGE`, `parse_args`, `mode_count`). Select only the `document_id` column from `documents`, `nodes`, `edges` and `entity_edges` (`Select::columns(&["document_id"])`, full scan, read-only). Collect each into a `BTreeSet`, count `staged_documents_v2` rows with `count_rows(None)`, and print exactly one JSON object with the keys of the contract in 06.3.4.1-02: `documents, nodes, edges, entity_edges, staged_documents_v2_rows`. Add the two behaviour tests and update `BIN_INSPECT_COUNT` / `TOTAL` in `scripts/engine-test-targets.sh` to the measured values in the same commit.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked --bin inspect_lancedb document_ids</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>sh scripts/engine-test-targets.sh</automated>
    <fails_when>non-zero exit, or stderr contains "FAIL:"</fails_when>
  </verify>
  <acceptance_criteria>
    - `cargo run --manifest-path engine/Cargo.toml --release --quiet --bin inspect_lancedb -- --document-ids --lancedb-path ./data/lancedb-eval` prints one JSON object whose `documents` list has 350 IDs (06.3.3-STORE-BASELINE) or records the actual count in the summary.
    - The key set equals `{"documents","nodes","edges","entity_edges","staged_documents_v2_rows"}` exactly.
  </acceptance_criteria>
  <done>identity.py's LanceDB side has a real producer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reconcile_eval_store bin, cascade with allow-list prune and apply guards</name>
  <files>engine/src/bin/reconcile_eval_store.rs, engine/src/reconcile_eval_store_tests.rs, scripts/engine-test-targets.sh</files>
  <read_first>
    - `rust-guidelines.md` (M-DOCUMENTED-MAGIC for the 500-ID chunk size; error style `Result<_, String>`)
    - `engine/src/bin/inspect_lancedb.rs` (bin skeleton, `#[path]` test module, UUID validation 750-754)
    - `engine/src/ingest.rs:625-638` (delete order), `:1255-1345` (entity delete-then-add with `ListBuilder`; note the nulls written at ~1311-1314, which this bin must NOT copy)
    - `engine/src/db/mod.rs:206-325` (all seven table schemas, `entities_schema()`)
    - `engine/src/graph/mod.rs:332-334` (`escape_sql_literal`), `engine/src/config.rs` (how `LANCET_ENV=eval` resolves `lancedb_path`)
    - `engine/src/inspect_lancedb_tests.rs` (synthetic store fixtures)
    - RESEARCH §D cascade steps 1-7 and "Snapshot and record before deleting"
  </read_first>
  <behavior>
    - Extras = distinct document_id over documents ∪ nodes ∪ edges ∪ entity_edges minus the map entries; alias IDs count as extras.
    - After apply: extra-doc rows are gone from edges, nodes, documents, staged_documents_v2 and entity_edges; mapped rows are untouched.
    - An entity whose `source_chunk_ids` are all extra chunks is deleted, and every entity_edges row touching it is deleted.
    - An entity with chunks in both a mapped and an extra doc survives with only the mapped chunk IDs, and its `name_vector`, `summary`, `summary_vector`, `unsummarized_refs` and `community_ids` are byte-equal to before.
    - A degree-0 entity with surviving chunks is untouched; stale provenance pointing at a chunk ID that no longer exists anywhere is pruned even when its document is mapped (allow-list, not prefix).
    - A second apply is a no-op: extras 0, rewrites 0, orphans 0.
    - `--apply` without an existing `--snapshot-dir` containing `nodes.lance` is refused before any write; a `--lancedb-path` that is not the configured eval path is refused; a non-UUIDv4 map key is refused.
    - `--target-document-ids` deletes exactly the listed mapped IDs through the same cascade and prune.
  </behavior>
  <action>
    Create `engine/src/bin/reconcile_eval_store.rs` following the `inspect_lancedb` skeleton (`enum Mode { DryRun, Apply }`, `Config`, `pub const USAGE`, a `while let` argument parser, `#[cfg(test)] #[path = "../reconcile_eval_store_tests.rs"] mod tests;`).

    Flags:
    - `--document-map <path>` (required)
    - `--lancedb-path <path>` (required)
    - `--apply`
    - `--snapshot-dir <path>`
    - `--target-document-ids <file>` (a JSON array of UUIDs)

    Isolation: load settings with `LANCET_ENV=eval` semantics and refuse unless the canonicalised `--lancedb-path` equals the canonicalised configured eval `lancedb_path` and differs from the base (dev) `lancedb_path`.

    Algorithm (RESEARCH §D):
    1. Compute extras, or take the target list.
    2. Delete `document_id IN (...)` in chunks of 500 UUID-validated, `escape_sql_literal`-escaped IDs, in order `edges`, `nodes`, `documents`, `staged_documents_v2`, `entity_edges`.
    3. Read surviving `nodes.chunk_id` into a `HashSet`.
    4. Read all nine entity columns. For each entity, `kept = source_chunk_ids ∩ surviving`. Empty means orphan; changed means rewrite.
    5. Rewrite by deleting the changed entity IDs (chunks of 500) and adding one `RecordBatch` built with `db::entities_schema()`, carrying every original column array through and replacing only `source_chunk_ids`.
    6. Delete the orphans, then the `entity_edges` whose `source_node_id` or `target_node_id` is an orphan.
    7. Assert `staged_documents_v2` `count_rows == 0` after apply.

    Record `version()` of all seven tables before and after. Print one JSON report:
    - `mode`, `allow_count`, `extras`
    - `per_table` {`<table>`: {`before`, `to_delete`, `after`}}
    - `entities` {`total`, `rewrite`, `orphan`, `untouched`, `isolated_untouched`}
    - `orphan_incident_entity_edges`, `versions_before`, `versions_after`

    In dry-run mode, compute everything with read-only queries and write nothing. Never call `optimize`, compaction or `cleanup_old_versions`. Tests use synthetic temp stores (one test per behaviour bullet). Add a `BIN_RECONCILE_COUNT` awk counter, `:-0` default, echo line, `TOTAL` term and assertion to `scripts/engine-test-targets.sh` (the per-bin pattern at lines 33 and 76-79), with measured values.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked --bin reconcile_eval_store</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>sh scripts/engine-test-targets.sh</automated>
    <fails_when>non-zero exit, or stderr contains "FAIL:", or no "reconcile_eval_store (bin):" line is echoed</fails_when>
  </verify>
  <acceptance_criteria>
    - At least 10 tests in `reconcile_eval_store_tests.rs`, all passing, including column preservation and the second-run no-op.
    - `grep -nE "optimize|cleanup_old_versions|compact" engine/src/bin/reconcile_eval_store.rs` returns no call sites (doc-comment mentions only, if any).
    - The engine test-count gate asserts the new bin's count.
  </acceptance_criteria>
  <done>The cascade reconcile exists, is dry-run by default, preserves every entity column, and cannot run apply without a snapshot or against the dev store.</done>
</task>

<task type="auto">
  <name>Task 3: Live read-only census, committed dry-run record</name>
  <precondition>Docker Desktop is running and `docker compose ps db` shows the `db` service up; `data/lancedb-eval` exists; no engine process is running (`tasklist` shows no `engine.exe`).</precondition>
  <files>.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RECONCILE-DRYRUN.md, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/reconcile/dryrun.json, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/reconcile/pg_extras.json, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/reconcile/document_ids_pre.json</files>
  <read_first>
    - `06.3.3-STORE-BASELINE.md` (count and version table layout; 350 docs; 2,447 isolated entities)
    - RESEARCH §D "The four document counts, resolved" and Open Question 5
    - `eval/src/lancet_eval/identity.py` CLI (`identity check`, `identity pg-delete-extras` dry-run)
  </read_first>
  <action>
    With the engine stopped, run, all read-only:
    1. `inspect_lancedb --document-ids` → `reconcile/document_ids_pre.json`.
    2. `reconcile_eval_store --document-map eval/corpora/multihop_rag/document_map.json --lancedb-path ./data/lancedb-eval` (dry-run) → `reconcile/dryrun.json`.
    3. `uv run --project eval lancet-eval identity check --corpus multihop_rag`. It is expected to fail now; save its report.
    4. `uv run --project eval lancet-eval identity pg-delete-extras --corpus multihop_rag` (dry-run). Add each extra row's `filename, status, created_at` via `SELECT id, filename, status, created_at FROM lancet_eval.documents WHERE id NOT IN (...)` through `run_psql` → `reconcile/pg_extras.json`.

    Write `06.3.4.1-RECONCILE-DRYRUN.md`:
    - the LanceDB extras with, for each, its title (from `documents.raw_content` first line or the chunk content) and the mapped article it duplicates;
    - whether the set equals RESEARCH A1's four (`0370301a…`, `f9992c53…`, `fa85cb0e…`, `3aadce3b…`), with any difference stated;
    - per-table before / to_delete / after;
    - entity rewrite, orphan and untouched counts, and orphan-incident entity_edges;
    - all seven table versions;
    - the PostgreSQL extras with filename/status/created_at and their count against the expected 21;
    - `staged_documents_v2` rows;
    - the rollback layers 06.3.4.1-06 will create: directory copy to `data/lancedb-eval.pre-06.3.4.1-reconcile/` (~720 MB, outside git), recorded table versions for `checkout(v)` + `restore()`, and `pg_dump -n lancet_eval` to a path outside git.

    Delete nothing.
  </action>
  <verify>
    <automated>uv run --project eval python -c "import json; d = json.load(open('.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/reconcile/dryrun.json')); assert d['mode'] == 'dry_run' and isinstance(d['extras'], list) and 'entities' in d and 'versions_before' in d, d.keys(); assert d['versions_before'] == d['versions_after'], 'dry run changed a table version'"</automated>
    <fails_when>AssertionError printed (wrong mode, missing keys, or a version changed during the dry run), or non-zero exit</fails_when>
    <automated>test -s .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/reconcile/pg_extras.json && grep -q "Rollback layers" .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RECONCILE-DRYRUN.md</automated>
    <fails_when>non-zero exit: pg_extras.json empty or missing, or the record has no "Rollback layers" section</fails_when>
  </verify>
  <acceptance_criteria>
    - `dryrun.json` `versions_before == versions_after` (nothing written).
    - The record lists every extra LanceDB document_id and every extra PostgreSQL id with counts, or states the actual counts where they differ from 4 and 21.
    - `document_ids_pre.json` `documents` count and the PostgreSQL row count are recorded as the D-62 "before" numbers.
  </acceptance_criteria>
  <done>The exact one-way change is known, written down and committed before anyone approves it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document_map.json → reconcile predicates | map keys become delete predicates |
| operator → `--apply` | a one-way store mutation is requested |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.4.1-04-01 | Tampering | reconcile target selection | high | mitigate | Refuse any path other than the configured eval `lancedb_path`; refuse the dev path; dry-run default |
| T-06.3.4.1-04-02 | Tampering | predicate construction | high | mitigate | UUIDv4-validate every ID, `escape_sql_literal`, chunks of 500; entity IDs come from the store, not from input text |
| T-06.3.4.1-04-03 | Denial of service | irreversible data loss | high | mitigate | `--apply` requires an existing `--snapshot-dir` containing `nodes.lance`; no optimize/cleanup so LanceDB version restore remains; the census in Task 3 writes nothing |
| T-06.3.4.1-04-04 | Tampering | entity rewrite | medium | mitigate | Carry all original column arrays through; tests assert byte-equality of `name_vector`, `summary_vector`, `community_ids`, `unsummarized_refs`, `summary` |
</threat_model>

<verification>
- `cargo test --manifest-path engine/Cargo.toml --locked` and `sh scripts/engine-test-targets.sh` exit 0.
- `reconcile/dryrun.json` shows unchanged versions.
</verification>

<success_criteria>
The reconcile tooling is tested on synthetic stores and a committed dry run states exactly what will be
deleted and how to roll it back.
</success_criteria>

## Artifacts this phase produces

- Rust: `InspectMode::DocumentIds`, flag `--document-ids`; bin `reconcile_eval_store` with flags `--document-map`, `--lancedb-path`, `--apply`, `--snapshot-dir`, `--target-document-ids`; JSON report fields listed in Task 2.
- Gate script: `BIN_RECONCILE_COUNT` counter and assertion.
- Files: `06.3.4.1-RECONCILE-DRYRUN.md`, `reconcile/{dryrun.json,pg_extras.json,document_ids_pre.json}`.

<output>
Create `.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-04-SUMMARY.md` when done.
</output>
