---
phase: 06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair
plan: 18
type: execute
wave: 14
depends_on: [06.3.4.1-17]
files_modified:
  - eval/runs/
  - config/config.toml
  - config/config.example.toml
  - engine/src/config.rs
  - scripts/engine-test-targets.sh
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-BUDGETS.md
autonomous: false
requirements: [OBS-05, DATA-05]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Budget pass B runs only after a blocking checkpoint authorises its `--stage-cap`, and re-derives `graph_operation_timeout_ms` on the new seeding with the committed rule, re-running the nesting and harness-ceiling checks, after OI-01 has landed (D-77, D-66, D-86)."
    - "Pass B's RetrieveHybrid decay verdict must be available and not present (the new graph path must not reintroduce OI-02), and its ExtractGraphContext slope and `GRAPH_TIMEOUT` rate are reported against `max_tolerated_graph_timeout_rate` = 0.10 (D-64, D-77)."
    - "The re-derived graph budget (and any nesting-raised outer budget) is written to config.rs, config.toml and config.example.toml together, with the agreement test green and the pass-B trail appended to `06.3.4.1-BUDGETS.md` (D-66)."
    - "All eight canaries, including the new seed-to-seed path canary and the two existing `require_graph_node` canaries, are re-validated live under the new seeding by `preflight --corpus multihop_rag_diag` on the committed budgets with no env overrides; a failing canary stops the plan (D-80, 06.3.1 D-31)."
  artifacts:
    - path: ".planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-BUDGETS.md"
      provides: "pass-B derivation, GRAPH_TIMEOUT rate, canary re-validation transcript, authorised caps"
    - path: "eval/runs/"
      provides: "eval/runs/<date>-passB-measure-multihop_rag/"
  key_links:
    - from: "eval/runs/<date>-passB-measure-multihop_rag/"
      to: "config/config.toml graph_operation_timeout_ms"
      via: "derive_budget p95 x 1.5 + check_nesting_invariants"
      pattern: "graph_operation_timeout_ms"
  prohibitions:
    - "No budget may be derived from a pass whose RetrieveHybrid decay verdict is present or unavailable"
    - "No canary floor may be relaxed to make preflight pass"
---

<objective>
Re-derive the graph operation budget on the new seeding (D-77), then prove the canaries hold live on
the engine that drive 2 will use (D-80).

Purpose: drive 2 is the run of record. Its graph budget must come from the new graph path's measured
latency, and the canaries are the hard floors that stop a broken graph path before paid spend.

Output: the pass-B measurement run, the updated budget(s) in the config trio, and a canary
re-validation transcript.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-BUDGETS.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-SEED-PROBE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Authorise budget pass B and the canary probes (D-86)</name>
  <decision>Authorise pass B's `--stage-cap` and raised ceilings, and the live canary probes.</decision>
  <context>
    Pass B uses the same shape as pass A (about 160 questions × 2 arms of `multihop_rag`, one segment,
    raised ceilings through env overrides for this pass only), now with mention seeding, paths and the
    chunk boost. The estimate is ~$0.12 at the mean and $0.31 all-at-max; mention embeddings add under
    $0.001 (AI-SPEC §5 stage caps). The proposed cap is $0.50. The preflight canary probes afterwards
    (8 invocations, under $0.01) ride on this authorisation. There is no overall phase ceiling
    (06.3.1 D-47).
  </context>
  <options>
    <option id="authorise-0.50">
      <name>Authorise $0.50 with the pass-A ceiling set</name>
      <pros>Consistent with pass A; uncensored graph latency</pros>
      <cons>Paid spend</cons>
    </option>
    <option id="authorise-other">
      <name>Authorise a different cap or ceilings (state them)</name>
      <pros>User control</pros>
      <cons>A low cap risks a partial pass</cons>
    </option>
  </options>
  <resume-signal>Reply authorise-0.50, or authorise-other with figures.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run pass B and re-derive the graph budget</name>
  <precondition>Task 1 authorised a cap; the release engine contains 06.3.4.1-14/-15/-16; Docker `db` up; OPENROUTER_API_KEY set; `identity check --corpus multihop_rag` passes.</precondition>
  <files>eval/runs/, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-BUDGETS.md</files>
  <read_first>
    - `06.3.4.1-BUDGETS.md` (pass-A section and ceiling set), `eval/src/lancet_eval/measure.py:319-440`, `eval/src/lancet_eval/latency.py:352-614`, `eval/src/lancet_eval/thresholds.py` (`max_tolerated_graph_timeout_rate` in `COMMITTED_THRESHOLDS`)
  </read_first>
  <action>
    As in 06.3.4.1-11 Task 2: start the engine with the authorised raised ceilings, record PID, start time, binary path, mtime and profile, then run `uv run --project eval lancet-eval measure --corpus multihop_rag --sample-size 160 --segment-boundary 100000 --workers 1 --stage-cap <authorised> --out eval/runs/<UTC-date>-passB-measure-multihop_rag`. Record the PID afterwards.

    Require the RetrieveHybrid `analyze_decay` verdict to be available and not present; otherwise stop and route to `--gaps` (OI-02 regression on the new graph path). Report the ExtractGraphContext slope, the graph-on `GRAPH_TIMEOUT` rate against 0.10, and graph-on seed-count / path-found / boosted-chunk distributions from the new wire fields.

    Derive `graph_operation_timeout_ms` with `derive_budget` (p95 × 1.5) and re-run `check_nesting_invariants(required_slack_ms=500)` over all six budgets, keeping pass A's other five values unless a nesting violation raises an outer budget. Re-run `check_harness_ceilings`. Append a "Pass B (D-77)" section to `06.3.4.1-BUDGETS.md` and pass B's authorised cap to the stage-cap table.
  </action>
  <verify>
    <automated>grep -q "Pass B (D-77)" .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-BUDGETS.md && ls -d eval/runs/*-passB-measure-multihop_rag</automated>
    <fails_when>non-zero exit: no pass-B section in BUDGETS.md or no passB run directory</fails_when>
  </verify>
  <acceptance_criteria>
    - The pass-B section records a RetrieveHybrid decay verdict with both prongs available and not present, the derived `graph_operation_timeout_ms`, the nesting report and the `GRAPH_TIMEOUT` rate.
    - Spend is at or below the authorised cap, with an unchanged engine PID across the pass.
  </acceptance_criteria>
  <done>The graph budget is re-derived from the new graph path's measured latency on a non-decaying engine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write the graph budget, restart, re-validate all canaries live</name>
  <files>config/config.toml, config/config.example.toml, engine/src/config.rs, scripts/engine-test-targets.sh, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-BUDGETS.md</files>
  <read_first>
    - `engine/src/config.rs` (`default_graph_operation_timeout_ms`, `default_graph_node_timeout_ms`, the agreement test), `config/config.toml` `[engine.workflow]`
    - `eval/src/lancet_eval/preflight.py` `run_preflight_checks`
  </read_first>
  <behavior>
    - The agreement test stays green with the new graph (and any raised outer) budget in all three sources; startup nesting validation passes.
  </behavior>
  <action>
    Write the pass-B `graph_operation_timeout_ms` (and `graph_node_timeout_ms` only if nesting raised it) to `engine/src/config.rs` defaults, `config/config.toml` and `config/config.example.toml` in one commit, updating the nesting comment to cite the pass-B section. Update the gate counts if tests changed.

    Unset every `LANCET_ENGINE__WORKFLOW__*` variable (recorded as empty), rebuild and restart the release engine and the gateway, and run `uv run --project eval lancet-eval preflight --corpus multihop_rag_diag`. Every check must pass, including all 8 canaries: the path canary's `require_seed_path` and both existing `require_graph_node` canaries under the new seeding. Record the per-canary transcript (chunks, graph nodes, path found, prompt facts, node durations against budgets) in `06.3.4.1-BUDGETS.md`.

    If a canary fails, stop and record which floor failed. Do not relax a floor or swap a canary; that goes to the user through `/gsd-plan-phase 06.3.4.1 --gaps`.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked config && sh scripts/engine-test-targets.sh</automated>
    <fails_when>non-zero exit, "0 passed" for the config filter, or stderr contains "FAIL:"</fails_when>
    <automated>uv run --project eval lancet-eval preflight --corpus multihop_rag_diag</automated>
    <fails_when>non-zero exit, or any check line reporting failure</fails_when>
  </verify>
  <acceptance_criteria>
    - `06.3.4.1-BUDGETS.md` records a passing live preflight with all 8 canaries and no env overrides set.
    - `git diff --quiet --exit-code -- config/config.eval.toml` exits 0.
  </acceptance_criteria>
  <done>Drive 2's engine runs on budgets derived for the new graph path, and every canary holds live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → provider | paid measurement and canary probes |
| env overrides → effective config | silent overrides |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.4.1-18-01 | Denial of service (financial) | pass B | medium | mitigate | Blocking checkpoint authorises `--stage-cap`; passed explicitly |
| T-06.3.4.1-18-02 | Denial of service | graph op latency | medium | mitigate | Budget re-derived from measured p95 on the new path; nesting checked; GRAPH_TIMEOUT rate reported against 0.10 |
| T-06.3.4.1-18-03 | Tampering | effective budgets | high | mitigate | Env overrides unset and verified before restart; agreement test green |
</threat_model>

<verification>
- `cargo test --manifest-path engine/Cargo.toml --locked && (cd gateway && go test ./...)` exit 0; live preflight passes.
</verification>

<success_criteria>
The graph budget reflects the new graph path, and all canaries pass live on the drive-2 engine.
</success_criteria>

## Artifacts this phase produces

- Run directory `eval/runs/<date>-passB-measure-multihop_rag/`.
- Updated `graph_operation_timeout_ms` (and possibly `graph_node_timeout_ms`) in the config trio.
- "Pass B (D-77)" and canary re-validation sections in `06.3.4.1-BUDGETS.md`.

<output>
Create `.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-18-SUMMARY.md` when done.
</output>
