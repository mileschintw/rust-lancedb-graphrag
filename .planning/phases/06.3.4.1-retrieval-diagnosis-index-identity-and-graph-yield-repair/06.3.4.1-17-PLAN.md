---
phase: 06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair
plan: 17
type: execute
wave: 13
depends_on: [06.3.4.1-16, 06.3.4.1-12]
files_modified:
  - eval/src/lancet_eval/thresholds.py
  - eval/src/lancet_eval/unpark_gates.py
  - eval/tests/test_unpark_gates.py
  - eval/tests/test_gate.py
  - eval/src/lancet_eval/diagnostic.py
  - eval/tests/test_diagnostic.py
  - eval/src/lancet_eval/preflight.py
  - eval/corpora/multihop_rag/canary.jsonl
  - eval/tests/test_preflight_canary.py
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-SEED-PROBE.md
autonomous: true
requirements: [OBS-05, DATA-05]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SC-4 presence keeps the D-06 definition (`graph_node_count > 0 or graph_edge_count > 0` on graph-on), stays comparable to 06.3.4's 14/143, and is headlined over pairs(G) among dual-success pairs; PASS needs the headline at or above `GRAPH_YIELD_INVESTIGATION_FLOOR = 0.20` (not amended) and a Wilson lower bound above the committed 0.098 (D-81, D-82, 06.3.1 D-32)."
    - "D-81's retrieval-composition measure is the share of pairs where graph-on's final retrieved set holds at least one `graph_boosted` chunk that graph-off's set lacks, headlined on pairs(V) against the committed `GRAPH_COMPOSITION_CHANGE_FLOOR = 0.10` (D-81)."
    - "SC-5 PASS is committed as: n_pairs(V) ≥ 1 and either composition change ≥ the floor, or a paired Δ in retrieval coverage@4 or `answer_usable` whose bootstrap CI excludes 0 with n_pairs(V) ≥ 2; direction is not part of the rule, and a negative Δ is reported per 06.3.1 D-49 (D-82)."
    - "Every SC-4/SC-5 number is also reported over all answerable pairs (A) and all usable graph-on records, so the G and V restrictions stay visible (D-82); headline pairing uses `pairing.form_pairs` with 06.3.1 D-34 exclusions and D-37 pairing, and D-38 paired bootstrap CIs, stratified by `question_type` (D-40)."
    - "The pre-drive-2 literals (0.098, 0.10, the SC-5 rule) are committed in `thresholds.py` before drive 2's first record, so a miss triggers the D-83 iteration rule rather than a moved threshold (D-83)."
    - "The canary set is extended with at least one question whose gold answer needs a seed-to-seed path, checked by a new `require_seed_path` floor (`graph_path_found` and `graph_node_count ≥ 1` and `graph_prompt_fact_count ≥ 1` and retrieved chunks ≥ 1), and the manifest's hard-coded counts move from 7 rows / 6 IDs to 8 / 7; canaries stay hard preflight gates (D-80; DATA-05 compile-into-prompt check)."
    - "Column (d) for drive records is computed from the wire `graph_seed_document_ids` against the gold evidence documents (D-79)."
  artifacts:
    - path: "eval/src/lancet_eval/unpark_gates.py"
      provides: "evaluate_sc4, evaluate_sc5, graph_off_invariance, --stage drive2"
      contains: "def evaluate_sc5"
    - path: "eval/src/lancet_eval/thresholds.py"
      provides: "GRAPH_PRESENCE_WILSON_LOWER_FLOOR, GRAPH_COMPOSITION_CHANGE_FLOOR, SC5_VISIBILITY_RULE"
      contains: "GRAPH_COMPOSITION_CHANGE_FLOOR"
    - path: "eval/corpora/multihop_rag/canary.jsonl"
      provides: "8 rows / 7 IDs including a require_seed_path canary"
      contains: "require_seed_path"
  key_links:
    - from: "eval/src/lancet_eval/unpark_gates.py evaluate_sc5"
      to: "eval/src/lancet_eval/pairing.py"
      via: "form_pairs + compute_paired_delta over pairs(V), pairs(G), pairs(A)"
      pattern: "compute_paired_delta"
    - from: "eval/src/lancet_eval/preflight.py check_canary_floors"
      to: "journal WorkflowWireMeta.graph_path_found"
      via: "require_seed_path floor"
      pattern: "require_seed_path"
  prohibitions:
    - "GRAPH_YIELD_INVESTIGATION_FLOOR (0.20) must not be edited"
    - "The canary row must be copied verbatim from questions.sample.jsonl, not written by hand"
---

<objective>
Commit the drive-2 gate rules and make SC-4 and SC-5 computed readings, and extend the canaries with a
seed-to-seed path canary. All of this lands before any drive-2 record exists.

Purpose: drive 2 is the run of record 6.4 cites (D-87). Its gates must read against literals committed
before the data (the D-73 pattern), with the restricted and unrestricted populations both visible (D-82).

Output: three literals, `evaluate_sc4`, `evaluate_sc5` and graph-off invariance in `unpark_gates.py`,
column (d) from the wire, and the path canary with its floor.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-AI-SPEC.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-SEED-PROBE.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-16-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drive-2 literals and computed SC-4/SC-5 readings, column (d) from the wire</name>
  <files>eval/src/lancet_eval/thresholds.py, eval/src/lancet_eval/unpark_gates.py, eval/tests/test_unpark_gates.py, eval/tests/test_gate.py, eval/src/lancet_eval/diagnostic.py, eval/tests/test_diagnostic.py</files>
  <read_first>
    - AI-SPEC §5 dimensions #9-#12 (rubrics and pass rules), §6 "Graph-off invariance"
    - `eval/src/lancet_eval/pairing.py:74-200` (`form_pairs`, `compute_paired_delta`), `eval/src/lancet_eval/stats.py` (`wilson_ci`), `eval/src/lancet_eval/dimensions.py:690-707` (existing presence/influence counting, `None` = not reported)
    - `eval/src/lancet_eval/unpark_gates.py` (06.3.4.1-05), `eval/src/lancet_eval/diagnostic.py`, `eval/src/lancet_eval/thresholds.py:90-114`
  </read_first>
  <behavior>
    - `evaluate_sc4`: 10/40 pairs(G) with presence gives headline 0.25 and a Wilson lower bound of about 0.14, so PASS; 5/40 gives MISS; 1/5 (0.20, lower bound about 0.036) gives MISS on the Wilson clause; n=0 gives MISS `n=0`. Output also carries pairs(A), all-usable-graph-on, influence rate and no-match rate.
    - `evaluate_sc5`: composition 5/40 on pairs(V) (0.125 ≥ 0.10) gives PASS; composition 2/40 with a coverage Δ CI excluding 0 and n_pairs ≥ 2 gives PASS; composition 2/40 with a CI that includes 0 gives MISS; n_pairs(V) = 1 with no composition change gives MISS (no interval at n = 1); a negative Δ whose CI excludes 0 gives PASS, flagged `negative`.
    - A chunk counts as a composition change only if it is `graph_boosted` in graph-on and absent from graph-off's final set; a mere set difference without the flag does not count (the reformulation confound).
    - `graph_off_invariance(drive1_journal, drive2_journal)` reports per-question `answer_usable` agreement and final-retrieved-set equality for graph-off records of the same question IDs.
    - `d_graph_seed_hit` from a record whose `graph_seed_document_ids` includes a gold document is True; with `None` (an older journal) it stays None.
  </behavior>
  <action>
    Append to `thresholds.py`, each with the comment `# 06.3.4.1 D-81/D-82/D-83: committed before paid drive 2 (AI-SPEC §5 #9/#11/#12).`:
    - `GRAPH_PRESENCE_WILSON_LOWER_FLOOR: float = 0.098`
    - `GRAPH_COMPOSITION_CHANGE_FLOOR: float = 0.10`
    - `SC5_VISIBILITY_RULE: str = "composition_floor_or_paired_ci_excludes_zero_n_ge_2"`

    Do not touch `GRAPH_YIELD_INVESTIGATION_FLOOR`.

    In `unpark_gates.py`, add `evaluate_sc4(journal_path, populations_path)`, `evaluate_sc5(journal_path, populations_path)`, `graph_off_invariance(drive1_journal, drive2_journal)` and `--stage drive2` handling in `main` (options `--drive1-run`, `--run`, `--populations`, `--gold-chunks`, `--out`). SC-5's paired deltas use `compute_paired_delta` (paired bootstrap B = 10000, seed 42) for retrieval coverage@4, `answer_usable` and `final_answer_em`, plus cost deltas (latency, prompt tokens), each with `n_pairs`, `pairing_coverage` and `question_type` strata. Print drive 2's re-reports of SC-2/SC-3 and the D-69 rate as disclosures, not gates.

    In `diagnostic.py`, fill `d_graph_seed_hit`, `seed_count` and `path_found` from the graph-on record's wire fields when present, preferring them over the offline seed-probe file. Write the tests first.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_unpark_gates.py eval/tests/test_gate.py eval/tests/test_diagnostic.py eval/tests/test_pairing.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - Three new literals exist with provenance comments; `GRAPH_YIELD_INVESTIGATION_FLOOR` is unchanged (`git diff` shows no edit to that line).
    - At least 12 new tests cover the Behavior bullets.
  </acceptance_criteria>
  <done>SC-4 and SC-5 are computed readings against literals committed before drive 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seed-to-seed path canary and its preflight floor (D-80)</name>
  <files>eval/src/lancet_eval/preflight.py, eval/corpora/multihop_rag/canary.jsonl, eval/tests/test_preflight_canary.py, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-SEED-PROBE.md</files>
  <read_first>
    - `eval/src/lancet_eval/preflight.py:293-400` (`check_canary_manifest`: exact 7 rows / 6 IDs at 345-365, verbatim text check at 367-386), `:497-652` (`check_canary_floors`, `require_graph_node` handling)
    - `eval/corpora/multihop_rag/canary.jsonl`, `diagnostic/seed_probe.jsonl`
    - RESEARCH §I "Choosing the path canary"
  </read_first>
  <behavior>
    - The manifest check accepts exactly 8 rows / 7 distinct IDs and rejects 7/6 and 9/8.
    - A canary row with `require_seed_path: true` passes when the probe outcome has `graph_path_found` true, `graph_node_count ≥ 1`, `graph_prompt_fact_count ≥ 1` and at least 1 retrieved chunk, and fails naming the first unmet condition otherwise.
    - The existing six rows' behaviour is unchanged.
  </behavior>
  <action>
    Choose the path canary from `diagnostic/seed_probe.jsonl`: a non-null question in the diagnostic sample with at least 2 content (non-hub) seeds, `path_found` true, a stable top path, and gold evidence whose document contains a path entity's source chunk. Confirm the path's edges with `inspect_lancedb --entity-name <name>` and a one-hop neighbourhood listing, and record the choice with the path in a "D-80 path canary" section of `06.3.4.1-SEED-PROBE.md` (the 06.3.3-STORE-BASELINE §5 pattern).

    Append one row to `canary.jsonl` with the question copied verbatim from `questions.sample.jsonl`: `graph_arm: "graph-on"`, `min_retrieved_chunks: 1`, `require_graph_node: true`, `require_seed_path: true`, other flags false.

    In `preflight.py`, change the manifest constants to 8 rows / 7 IDs, as named module constants with a comment citing D-80, and implement `require_seed_path` next to `require_graph_node` in `check_canary_floors`, reading `graph_path_found` and `graph_prompt_fact_count` from the probe outcome's workflow metadata. Write the tests first. Live re-validation of all canaries, including the two existing `require_graph_node` ones, happens in 06.3.4.1-18 on the new engine.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_preflight_canary.py eval/tests/test_preflight.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the output</fails_when>
    <automated>uv run --project eval python -c "import json; rows = [json.loads(l) for l in open('eval/corpora/multihop_rag/canary.jsonl', encoding='utf-8') if l.strip()]; assert len(rows) == 8 and len({r['question_id'] for r in rows}) == 7 and sum(1 for r in rows if r.get('require_seed_path')) >= 1, len(rows)"</automated>
    <fails_when>AssertionError printed: the canary file is not 8 rows / 7 IDs with at least one require_seed_path row</fails_when>
  </verify>
  <acceptance_criteria>
    - `06.3.4.1-SEED-PROBE.md` has a "D-80 path canary" section naming the question, its seeds and the confirmed path.
    - The full `uv run --project eval pytest -q` passes.
  </acceptance_criteria>
  <done>A path canary exists as a hard preflight floor with its manifest counts updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| journal → published gates | computed readings decide the 6.4 unpark |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.4.1-17-01 | Repudiation | SC-4/SC-5 thresholds | high | mitigate | Literals committed before drive 2 with provenance; 0.20 floor untouched; readings computed |
| T-06.3.4.1-17-02 | Repudiation | composition confound | medium | mitigate | Only `graph_boosted` chunks count as composition change; test pins that a plain set difference does not |
| T-06.3.4.1-17-03 | Tampering | canary manifest | medium | mitigate | Verbatim text check against `questions.sample.jsonl`; count constants pinned by tests |
</threat_model>

<verification>
- `uv run --project eval pytest -q && uv run --project eval ruff check --preview eval/src eval/tests` exit 0.
</verification>

<success_criteria>
Drive-2 gates are committed and computable, and a path canary guards the new graph path at preflight.
</success_criteria>

## Artifacts this phase produces

- `thresholds.py`: `GRAPH_PRESENCE_WILSON_LOWER_FLOOR`, `GRAPH_COMPOSITION_CHANGE_FLOOR`, `SC5_VISIBILITY_RULE`.
- `lancet_eval.unpark_gates`: `evaluate_sc4`, `evaluate_sc5`, `graph_off_invariance`, `main --stage drive2`.
- `lancet_eval.diagnostic`: (d)/seed/path from wire fields.
- `preflight.py`: canary manifest constants (8/7), `require_seed_path` floor; `canary.jsonl` new row.

<output>
Create `.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-17-SUMMARY.md` when done.
</output>
