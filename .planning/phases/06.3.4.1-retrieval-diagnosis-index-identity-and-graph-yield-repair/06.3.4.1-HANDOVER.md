# 06.3.4.1 Orchestrator Handover (2026-09-25)

**Status:** PAUSED on user instruction, because the usage limit is close. No executor is running, and no engine, gateway, stub or replay process is running. Tree is clean at the commit that added this file.

**Resume by:** telling the orchestrator to continue plan 07 from this file, or running `/gsd-execute-phase 06.3.4.1`. Read this file first either way.

**User scope instruction (2026-09-24):** stop phase execution after plan 07 completes. Plans 08-20 continue later, in another session.

## Where the phase stands

| Plan | Status |
|---|---|
| 01-06 | Complete (each has a SUMMARY) |
| 07 | In progress, Tasks 1-2 done, **Task 3 checkpoint RESOLVED (below), not yet executed** |
| 08-20 | Not started. 08, 09, 11 and 12 were aligned to 07's `## Fix` section in commit `cc176875` |

## Plan 07 Task 3: user decision (2026-09-25)

**Decision:** `rerun-primary`.

1. First build the missing per-question vector map, `data/replay/stub-vectors.jsonl`, via `retrieval_soak --export-embeddings`, as Task 2's action specifies.
2. Then re-run the primary replay as `primary-r2`. The plan allows this once. `primary-r2` replaces `primary` as the reference everywhere.

**Why:** the first primary replay (commits `a1f9562d`, `d0b08105`) was not a valid test of the graph path.
- The stub served hash-fallback vectors for 350/350 embedding calls.
- Every record got GRAPH_ABLATION or GRAPH_UNAVAILABLE, so ExtractGraphContext never ran on real seeds.
- In production that node grew 5.02x, the second-largest growth after RetrieveHybrid.

**Readings from the first primary** (profile `### Full-stack replay (07)`):
- **M1 not met:** ratio 4.71 against a target of 16.95; the pass band is 8.48-33.91.
- **M2 does not grow:** RetrieveHybrid went 1,271 to 1,296 ms (1.02x). Production went 5.9x.
- **Growth is on every production node**, including pure-CPU AssemblePrompt (2.2x). That hints at a machine-wide or process-wide cause, so weigh it when ordering arms.

**After `primary-r2`**, route per the plan's Task 3/4 routing table:
- **M2 grows:** Route A, bisecting on HEAD.
- **Still does not grow:** Route B. Start with the drive-era whole stack: a worktree at `drive_era_commit` from `forensics.json`, run against a pre-reconcile store copy.

The user chose "rerun" and did not ask to be consulted again between `primary-r2` and routing. The plan's own blocking checkpoints still apply: Task 5 (fix decision) is always a stop.

## Paid-run pre-authorization (user, 2026-09-25)

The user approved a paid run "if required". Record this at the plan's paid-replay decision point (Task 5, `paid-replay` option, D-86). Use the plan's proposed stage cap, **$0.25**, via `--stage-cap`. Any spend above $0.25, or a second paid run, needs a fresh user confirmation. Use paid only if the free replays cannot reproduce the growth.

## Executor dispatch notes (orchestrator)

- Run sequentially on `main`. Right before each `Agent()` call, set the sentinel with the FULL plan id; it goes stale while you wait for the user:
  `node .claude/gsd-core/bin/gsd-tools.cjs query dispatch-isolation --raw --phase "06.3.4.1" --plan "06.3.4.1-07" --force-isolation none`
- Dispatch settings: `subagent_type: gsd-executor`, `model: sonnet`, run in background. The prompt must carry the `[gsd:dispatch phase="06.3.4.1" plan="06.3.4.1-07"]` tag and the `<project_root_pin>` block with `PINNED_ROOT='D:/Repos/lancet'`. Copy the template from the last plan 07 continuation prompt; it is in the session transcript.
- The executor must read `07-HANDOFF.md` (executor-level gotchas: roughly 10 Windows/PowerShell fixes, the store copy and scratch database to reuse, pre-building binaries) and this file first.
- Executor contexts fill up. Tell each executor to write a HANDOFF at a clean commit if its context gets heavy. Poll long replays in the background.
- Docker: the `db` service must be up and healthy. The observability stack (collector, prometheus, loki, grafana, jaeger) is used by replays; `docker compose ps` to check.
- Never write the live store `./data/lancedb-eval` or the live `lancet_eval` schema; `identity check` must still pass. Keep `data/oi02-evidence/` (the gitignored Prometheus export of the 06.3.4 drive). Rollback copies from plan 06 are `data/lancedb-eval.pre-06.3.4.1-reconcile/` plus a pg_dump.
- `CARGO_BUILD_JOBS=4`.

## After plan 07 completes

1. Spot-check the SUMMARY and a clean tree.
2. Confirm STATE.md points at plan 08.
3. Stop. Do not start wave 5. Phase-end gates (code review with an explicit REVIEW path and narrowed file scope, regression, verifier) run only after plan 20, in a later session. Skip the parent-artifact close-out, because double-decimal phase numbers break it.
