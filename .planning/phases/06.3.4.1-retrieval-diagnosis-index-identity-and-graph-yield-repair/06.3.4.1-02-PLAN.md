---
phase: 06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - eval/src/lancet_eval/identity.py
  - eval/src/lancet_eval/seed.py
  - eval/src/lancet_eval/cli.py
  - eval/tests/test_identity.py
  - eval/src/lancet_eval/preflight.py
  - eval/src/lancet_eval/run.py
  - eval/src/lancet_eval/measure.py
  - eval/tests/conftest.py
  - eval/tests/test_preflight.py
  - eval/tests/test_run_arms.py
  - eval/src/lancet_eval/corpus.py
  - config/config.eval.toml
  - eval/tests/test_seed.py
  - eval/tests/test_corpus.py
autonomous: true
requirements: [OBS-05]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A preflight identity gate compares the document-ID sets of PostgreSQL `lancet_eval.documents`, LanceDB (`documents` IDs, distinct `nodes.document_id`, `entity_edges.document_id` as a subset) and `document_map.json` entries, and fails on any symmetric difference, printing all three differences (D-61)."
    - "The gate is structural: `drive()` and `run_measurement_pass()` call it before dispatching any query and abort on mismatch. There is no flag, keyword argument or environment variable that skips it; tests substitute only the I/O layer (D-61)."
    - "The map alias `0370301a…` must be absent from both stores for the gate to pass, and `document_map.json` keeps it only under `aliases` (D-60)."
    - "The gate also requires `staged_documents_v2` to hold 0 rows, so an engine start can never replay a staged ingest mid-drive (RESEARCH §D staged-job replay hazard)."
    - "`index_generation` is written to `document_map.json` only by `record_index_generation()`, which reads `snapshot.index_generation` from a live `/rag/query` probe and raises if none is observed; it is never hand-written (D-62)."
    - "`config/config.eval.toml` overrides only `[engine] lancedb_path`. The effective eval workflow budgets equal `config/config.toml`'s, so the restore changes no effective value (D-66, first half)."
    - "A corpus config may name a different corpus's document map through `[documents] map_corpus`, so exactly one `document_map.json` exists; `score_run`'s mixed-generation, unmapped-document and effective-partial guards are unchanged (D-68 enabler, D-87a)."
    - "Deleting PostgreSQL extras is refused unless the target schema passes `assert_schema_isolated` and a non-empty pg_dump file path is supplied; it runs as one transaction over UUID-validated literals (D-58 PostgreSQL side)."
  artifacts:
    - path: "eval/src/lancet_eval/identity.py"
      provides: "LanceDB/PostgreSQL/map identity comparison, gate, PG extras delete"
      exports: ["IdentityReport", "IdentityGateError", "compute_identity", "require_index_identity", "list_pg_documents", "list_lancedb_document_ids", "delete_pg_extras"]
    - path: "eval/src/lancet_eval/preflight.py"
      provides: "check_index_identity wired right after store isolation"
      contains: "def check_index_identity"
    - path: "eval/src/lancet_eval/seed.py"
      provides: "run_psql helper, record_index_generation, map indirection"
      contains: "def record_index_generation"
    - path: "config/config.eval.toml"
      provides: "lancedb_path-only overlay"
  key_links:
    - from: "eval/src/lancet_eval/run.py"
      to: "eval/src/lancet_eval/identity.py"
      via: "require_index_identity called at the top of drive() before any dispatch"
      pattern: "require_index_identity"
    - from: "eval/src/lancet_eval/identity.py"
      to: "engine/src/bin/inspect_lancedb.rs"
      via: "subprocess `--document-ids` JSON contract (implemented by 06.3.4.1-04)"
      pattern: "--document-ids"
    - from: "eval/src/lancet_eval/seed.py"
      to: "eval/src/lancet_eval/corpus.py"
      via: "get_document_map_path reads CorpusConfig.map_corpus"
      pattern: "map_corpus"
  prohibitions:
    - "No code path may skip the identity gate: no CLI option, no keyword argument on drive()/run_measurement_pass(), no environment variable"
    - "index_generation must never be typed into document_map.json by hand or by any function other than record_index_generation"
    - "The diagnostic corpus must never get a copied document_map.json"
---

<objective>
Make "refuses to drive on an identity mismatch" (D-61) a property of the harness entry points, not a
checklist item. Add the one function allowed to write `index_generation` (D-62). Restore the eval
config overlay (D-66). Add the map indirection the diagnostic corpus needs (D-68).

Purpose: layer 2 of the mandatory order. After 06.3.4.1-06 applies the reconcile, this gate is what
proves identity holds before every paid stage. Until then it correctly refuses every paid stage,
which is why no paid stage in this plan set is ordered before 06.3.4.1-06.

Output: `identity.py`, the preflight check and the drive/measure gate, `record_index_generation`, the
`map_corpus` indirection, and a `lancedb_path`-only `config.eval.toml`.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-CONTEXT.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RESEARCH.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-PATTERNS.md
</context>

**`--document-ids` JSON contract (shared with 06.3.4.1-04, which implements the Rust side):** one JSON
object on stdout: `{"documents": [..], "nodes": [..], "edges": [..], "entity_edges": [..],
"staged_documents_v2_rows": <int>}`, where each list is the sorted, de-duplicated `document_id` set
of that table.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: identity.py computes the three-way comparison and guards the PostgreSQL delete</name>
  <files>eval/src/lancet_eval/identity.py, eval/src/lancet_eval/seed.py, eval/src/lancet_eval/cli.py, eval/tests/test_identity.py</files>
  <read_first>
    - `eval/src/lancet_eval/seed.py:28-155` (`_quote_pg_identifier`, `assert_schema_isolated`, the `psql` to `docker compose exec -T db psql` fallback inside `reset_eval_schema`), `:170-252` (`DocumentMap`, `load_document_map`, `save_document_map_atomic`)
    - `eval/src/lancet_eval/preflight.py:34-70` (`check_store_isolation` return shape)
    - `eval/src/lancet_eval/cli.py:108-155` (command pattern, exit code 1 on failure)
    - `gateway/db/schema.eval.hcl` (the `lancet_eval.documents` columns `id`, `status`)
    - RESEARCH §J (gate inputs), §D (the 21 unidentified PostgreSQL rows, cascade to `document_reconciliation_intents`)
  </read_first>
  <behavior>
    - Equal sets and 0 staged rows: `IdentityReport.passed` is True.
    - An extra ID in LanceDB `documents`, a map ID missing from `nodes`, or an extra PostgreSQL ID each fail, with the specific symmetric difference named in `detail`.
    - `entity_edges` IDs outside the map fail; `edges` IDs outside the map fail.
    - The alias ID present in either store fails with reason `alias_present` (D-60).
    - `staged_documents_v2_rows > 0` fails with reason `staged_rows_present`.
    - A PostgreSQL row whose status is not `completed` fails.
    - `delete_pg_extras(apply=True)` without an existing non-empty `pg_dump_path` raises; with a non-`lancet_eval` schema raises (via `assert_schema_isolated`); a non-UUID ID raises before any SQL is built; dry-run returns the extra IDs and executes no DELETE.
  </behavior>
  <action>
    Factor the psql fallback out of `reset_eval_schema` into `seed.run_psql(settings, sql: str, *, timeout_s: float = 30.0) -> str`. Keep the fallback order and error wording, keep `reset_eval_schema` behaviour identical, and pass SQL on stdin (`-v ON_ERROR_STOP=1`) so no literal lands on a command line.

    Create `eval/src/lancet_eval/identity.py` with:
    - `IdentityGateError(RuntimeError)`.
    - `IdentityReport` (pydantic, `extra="forbid"`): `map_ids`, `lance_documents`, `lance_nodes`, `lance_edges`, `lance_entity_edges`, `pg_status: dict[str, str]`, `staged_rows`, `alias_ids`, `failures: list[str]`, `passed`.
    - `list_lancedb_document_ids(lancedb_path) -> dict`: runs `cargo run --manifest-path engine/Cargo.toml --release --quiet --bin inspect_lancedb -- --document-ids --lancedb-path <path>` from `repo_root()`, `timeout=1800`, and parses the JSON contract above. It raises `IdentityGateError` on a non-zero exit or unparseable output. The first run compiles the binary, which is why the timeout is long; `cargo run` also rebuilds the stale `inspect_lancedb.exe` RESEARCH flags.
    - `list_pg_documents(settings) -> dict[str, str]` via `run_psql` with `SELECT id, status FROM lancet_eval.documents ORDER BY id` (unaligned, tuples-only output).
    - `compute_identity(settings, corpus_name) -> IdentityReport`, using `load_document_map(corpus_name)` so the diag corpus resolves to the one map.
    - `require_index_identity(settings, corpus_name) -> IdentityReport`, which raises `IdentityGateError` with all failures joined.
    - `delete_pg_extras(settings, allow_ids, *, apply: bool, pg_dump_path: Path | None) -> list[str]` with the behaviour above. In apply mode it runs `BEGIN; DELETE FROM lancet_eval.documents WHERE id NOT IN (<quoted uuid literals>); COMMIT;` and returns the deleted IDs, re-listed afterwards to confirm the set.

    Add CLI commands `lancet-eval identity check --corpus <c>` (exit 1 on failure; prints the report JSON) and `lancet-eval identity pg-delete-extras --corpus <c> [--apply --pg-dump <path>]` (dry-run by default). Tests in `eval/tests/test_identity.py` monkeypatch `subprocess.run` and `run_psql`; no live store or database.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_identity.py eval/tests/test_seed.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `test_identity.py` has one test per behaviour bullet (at least 9), all passing; existing `test_seed.py` still passes (the `reset_eval_schema` refactor is behaviour-preserving).
    - `uv run --project eval lancet-eval identity --help` lists `check` and `pg-delete-extras`.
    - `grep -n "run_psql" eval/src/lancet_eval/seed.py eval/src/lancet_eval/identity.py` shows the helper defined once in seed.py and used by identity.py.
  </acceptance_criteria>
  <done>The three-way identity comparison and a snapshot-guarded PostgreSQL delete exist and are unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the gate into preflight, drive() and run_measurement_pass() with no bypass</name>
  <files>eval/src/lancet_eval/preflight.py, eval/src/lancet_eval/run.py, eval/src/lancet_eval/measure.py, eval/tests/conftest.py, eval/tests/test_preflight.py, eval/tests/test_run_arms.py</files>
  <read_first>
    - `eval/src/lancet_eval/preflight.py:655-706` (`run_preflight_checks`, step order), `:220-229` (mismatch message template)
    - `eval/src/lancet_eval/run.py:230-383` (`drive` signature and start), `eval/src/lancet_eval/measure.py:440+` (`run_measurement_pass` start)
    - `eval/tests/conftest.py`, `eval/tests/test_run_arms.py`, `eval/tests/test_measure.py` (how fixtures call `drive`/`run_measurement_pass`)
  </read_first>
  <behavior>
    - `run_preflight_checks` output has `index_identity` as the second check, right after `store_isolation` and before any gateway check.
    - `drive()` raises `IdentityGateError` before creating or opening the journal when the report fails; the journal path does not exist afterwards.
    - `run_measurement_pass()` raises before its first query on a failing report.
    - With a passing report both proceed exactly as before (existing tests stay green).
  </behavior>
  <action>
    Add `check_index_identity(settings, corpus_name) -> PreflightCheckResult` to `preflight.py`. It wraps `identity.compute_identity`, never raises (any exception becomes `passed=False` with the exception text), and puts the three symmetric differences, the alias check and the staged count in `detail`. Insert it into `run_preflight_checks` immediately after the `# 1. Store isolation` step.

    At the top of `drive()`, before the journal header is created or read and before any executor, call `identity.require_index_identity(settings, corpus)`. Do the same at the top of `run_measurement_pass()`. Import the function at module level (`from lancet_eval.identity import require_index_identity`) so there is exactly one call site per entry point.

    Do not add any parameter, CLI option or environment variable that disables it. In `eval/tests/conftest.py`, add an autouse fixture that monkeypatches `lancet_eval.identity.list_lancedb_document_ids` and `lancet_eval.identity.list_pg_documents` to return sets equal to the fixture corpus's map, so the real comparison logic still runs in every existing test. Tests that need a failing gate override those two I/O functions. Add the behaviour tests to `test_preflight.py` and `test_run_arms.py`.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_preflight.py eval/tests/test_run_arms.py eval/tests/test_measure.py eval/tests/test_stage_cap.py eval/tests/test_journal_reconcile.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the output</fails_when>
    <automated>uv run --project eval pytest -q</automated>
    <fails_when>non-zero exit, or a "failed" count above 0 in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "require_index_identity(" eval/src/lancet_eval/run.py eval/src/lancet_eval/measure.py` reports exactly 1 call in each file.
    - `drive` and `run_measurement_pass` signatures are unchanged except for no new parameters (`git diff` of their `def` lines is empty).
    - The new failing-gate test asserts the journal file does not exist after the raise.
  </acceptance_criteria>
  <done>No paid drive or measurement pass can start on a mismatched index, and there is no way to ask it to.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: record_index_generation, map_corpus indirection, lancedb_path-only eval overlay</name>
  <files>eval/src/lancet_eval/seed.py, eval/src/lancet_eval/corpus.py, config/config.eval.toml, eval/tests/test_seed.py, eval/tests/test_corpus.py</files>
  <read_first>
    - `eval/src/lancet_eval/seed.py:209-252` (`get_document_map_path`, `load_document_map`, `save_document_map_atomic`), `:405-435` (the inline `/rag/query` probe and its `except Exception: pass`)
    - `eval/src/lancet_eval/corpus.py:103-150` (`CorpusConfig.__init__`)
    - `config/config.eval.toml` (current 14 lines) and `config/config.toml:32-60`
    - `eval/src/lancet_eval/preflight.py:187-229,402-467` (`check_corpus_generation`, `read_effective_workflow_config`)
    - `eval/src/lancet_eval/score.py:244-274` (the guards that must stay unchanged)
  </read_first>
  <behavior>
    - `record_index_generation` with a mocked SSE body containing `snapshot.index_generation = "lance-802"` writes that value atomically; with no generation in the body it raises `SeedError` and leaves the file byte-identical.
    - `CorpusConfig("multihop_rag").map_corpus == "multihop_rag"`; a temp TOML with `[documents] map_corpus = "multihop_rag"` resolves `get_document_map_path` to `eval/corpora/multihop_rag/document_map.json`.
    - A corpus with no TOML falls back to its own name.
    - `score_run` still raises on an unmapped document_id when the corpus uses the indirection (the guard is not relaxed).
    - With `LANCET_ENV=eval`, `read_effective_workflow_config` returns the same seven workflow values as for the base config.
  </behavior>
  <action>
    - Add `CorpusConfig.map_corpus = str(self.documents.get("map_corpus", corpus_name))`.
    - Change `get_document_map_path(corpus_name)` to use `load_corpus_config(corpus_name).map_corpus`, falling back to `corpus_name` on `CorpusError`. `load_document_map` stays the single loader.
    - Add `record_index_generation(settings, corpus_name) -> str`, factored from the inline probe at `seed.py:405-435`. It POSTs the existing probe question to `/rag/query`, parses the `data:` lines, reads `snapshot.index_generation`, sets `doc_map.index_generation` and calls `save_document_map_atomic`. It raises `SeedError("No index_generation observed ...")` instead of swallowing parse errors. Replace the inline block with a call to it.
    - Rewrite `config/config.eval.toml` to exactly its `[engine]` table with `lancedb_path = "./data/lancedb-eval"` plus a one-line comment citing 06.3.1 D-13 and 06.3.4.1 D-66. Delete the whole `[engine.workflow]` block that `33e774b` added (the overwrite itself is recorded as a finding in 06.3.4.1-11).
    - Add a test that the file parses to `{"engine": {"lancedb_path": ...}}` only.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_seed.py eval/tests/test_corpus.py eval/tests/test_preflight.py eval/tests/test_score_deterministic.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the output</fails_when>
    <automated>uv run --project eval python -c "import tomllib; d = tomllib.load(open('config/config.eval.toml', 'rb')); assert d == {'engine': {'lancedb_path': './data/lancedb-eval'}}, d"</automated>
    <fails_when>AssertionError printed, or non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `config/config.eval.toml` has no `[engine.workflow]` table.
    - `record_index_generation` is the only writer of `index_generation`: `grep -n "index_generation =" eval/src/lancet_eval/*.py` shows assignments only inside `record_index_generation` (and model field declarations).
    - Full `uv run --project eval pytest -q` passes.
  </acceptance_criteria>
  <done>The generation is probe-sourced only, the diag corpus can share the one map, and the eval overlay is back to its documented shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → PostgreSQL (via psql/docker) | SQL built from map IDs crosses into the database |
| harness → cargo subprocess | store path passed as an argument |
| engine `/rag/query` → document_map.json | a probe response becomes committed provenance |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.4.1-02-01 | Tampering | `delete_pg_extras` | high | mitigate | `assert_schema_isolated` first; UUID-validate every ID before quoting; SQL on stdin with `ON_ERROR_STOP`; one transaction; refuse without an existing non-empty pg_dump path |
| T-06.3.4.1-02-02 | Elevation of privilege | identity gate bypass | high | mitigate | Single call at the top of `drive()`/`run_measurement_pass()`; no parameter, option or env var; tests patch only the two I/O functions |
| T-06.3.4.1-02-03 | Repudiation | `index_generation` provenance | medium | mitigate | Only `record_index_generation` writes it, from a live probe; missing value raises and leaves the file unchanged |
| T-06.3.4.1-02-04 | Information disclosure | psql error output | low | accept | Output contains document IDs and statuses only; DSN is passed via env as today, no credential echoed |
</threat_model>

<verification>
- `uv run --project eval pytest -q && uv run --project eval ruff check --preview eval/src eval/tests` exit 0.
- `git diff --quiet --exit-code -- eval/runs/2026-09-03-multihop_rag/ eval/runs/2026-09-09-multihop_rag/` exits 0.
</verification>

<success_criteria>
The identity gate exists at preflight and at both paid entry points with no bypass; the generation has
one probe-sourced writer; the eval overlay is `lancedb_path`-only.
</success_criteria>

## Artifacts this phase produces

- Module `lancet_eval.identity`: `IdentityGateError`, `IdentityReport`, `list_lancedb_document_ids`, `list_pg_documents`, `compute_identity`, `require_index_identity`, `delete_pg_extras`.
- `lancet_eval.seed`: `run_psql`, `record_index_generation`; `get_document_map_path` now honours `map_corpus`.
- `lancet_eval.corpus.CorpusConfig.map_corpus`; TOML key `[documents] map_corpus`.
- `lancet_eval.preflight.check_index_identity` (check name `index_identity`).
- CLI: `lancet-eval identity check`, `lancet-eval identity pg-delete-extras [--apply --pg-dump PATH]`.
- Test fixture: autouse identity I/O patch in `eval/tests/conftest.py`.

<output>
Create `.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-02-SUMMARY.md` when done.
</output>
