---
phase: 06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair
plan: 14
type: execute
wave: 10
depends_on: [06.3.4.1-13]
files_modified:
  - engine/src/graph/index.rs
  - engine/src/workflow/ports.rs
  - engine/src/main.rs
  - engine/src/ingest.rs
  - engine/src/config.rs
  - config/config.toml
  - config/config.example.toml
  - engine/src/service.rs
  - engine/src/workflow/nodes/graph_context.rs
  - engine/src/workflow/mod.rs
  - engine/src/tests.rs
  - scripts/engine-test-targets.sh
autonomous: true
requirements: [DATA-05]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Production graph augmentation uses mention seeding and seed-to-seed paths (06.3.4.1-13's library) in place of `nearest_to(full-question embedding).column(\"name_vector\").limit(1)` + `fetch_neighborhood(hop 1)` + `narrow_via_cypher`; the old seeding path is deleted, and there is no config flag or eval-only mode (D-75, D-78)."
    - "Only path facts reach the prompt as graph facts, never unfiltered 1-hop neighbourhoods, and the D-06 counters still work: `graph_node_count`/`graph_edge_count` reflect path entities/relations and `graph_prompt_fact_count` counts the facts that reached the prompt (D-76; DATA-05 compile-into-prompt still holds)."
    - "`CorpusSnapshot` carries an immutable `Arc<GraphIndex>` built at startup and rebuilt at the same trigger as BM25, swapped atomically with it; a failed index build degrades the graph like a failed BM25 rebuild rather than serving a stale mix (RESEARCH Pattern 2)."
    - "Mention extraction reads the original question text and runs after the graph-off early return, so the ablation arm is unaffected (D-75, graph-off invariance)."
    - "New graph settings `max_seeds = 6`, `mention_vector_top_k = 3`, `degree_cap`, `max_path_facts`, `max_graph_chunk_candidates = 8` (the two derived values as decided at 06.3.4.1-13) are written to `engine/src/config.rs` defaults, `config/config.toml` `[engine.graph]` and `config/config.example.toml` together, each with its derivation in a doc comment, and the agreement test covers them (D-77, D-66 write-together rule)."
    - "`GraphAugmentationOutcome` keeps its variants (`Succeeded { facts }`, `NoMatchFound`, `AttemptedAndFailed { reason }`); every new failure maps to `AttemptedAndFailed`, and a timeout still emits `GRAPH_TIMEOUT` as a non-fatal degradation (06.3.1 D-29)."
    - "The context records `graph_seed_count`, `graph_path_found`, `graph_degree_capped_count`, `graph_seed_document_ids` and `graph_chunk_candidates` on the success branch only, for the wire fields (06.3.4.1-16) and the chunk boost (06.3.4.1-15) (D-79)."
    - "The boost semantics decided at 06.3.4.1-13 (paths only, or paths plus seed chunks when no path) determine `graph_chunk_candidates` (D-76)."
  artifacts:
    - path: "engine/src/service.rs"
      provides: "attempt_graph_augmentation rewritten over GraphIndex + seeding + paths; old path removed"
      contains: "find_seed_paths"
    - path: "engine/src/workflow/ports.rs"
      provides: "CorpusSnapshot.graph_index; GraphPort query takes the question text"
      contains: "graph_index"
    - path: "engine/src/config.rs"
      provides: "GraphSettings max_seeds, mention_vector_top_k, degree_cap, max_path_facts, max_graph_chunk_candidates"
      contains: "max_path_facts"
  key_links:
    - from: "engine/src/workflow/nodes/graph_context.rs"
      to: "engine/src/service.rs attempt_graph_augmentation"
      via: "graph_port.query_graph(question text, query embedding, cancel) after the ablation early return"
      pattern: "query_graph"
    - from: "engine/src/ingest.rs rebuild trigger"
      to: "engine/src/workflow/ports.rs CorpusSnapshot"
      via: "GraphIndex::build alongside the BM25 rebuild, atomic swap"
      pattern: "GraphIndex::build"
  prohibitions:
    - "No flag, setting or code path may re-enable the old full-question limit(1) seeding"
    - "The QueryGraph RPC (Cypher-backed) must not change behaviour"
---

<assumption_delta_decision>
- Primary noun: the seed **set** (multiple mention-derived seed entities per query).
- Decision: `promote`. The multi-seed representation replaces the single nearest-entity seed as the
  only production path, and the single-seed path is deleted (D-78). Nothing is added alongside.
- Suggested invariant test (accepted as part of Task 3): every graph-on query goes through
  `match_seeds` and `find_seed_paths`, and no production call site remains for the old single-seed
  nearest-entity lookup.
</assumption_delta_decision>

<objective>
Cut production graph augmentation over to 06.3.4.1-13's mention seeding and path search as the only
seeding path (D-75, D-78), with the per-generation `GraphIndex` in the corpus snapshot and the new
settings in the config trio.

Purpose: SC-4 (presence above 0.20) and SC-5 (visible paired effect) are measured on the production
default, not an eval mode (D-78; OBS-05 same system). The old seeding was the OI-01 source of 9.8%
presence and unfiltered 1-hop noise.

Output: snapshot, config and service/node changes with tests; the old seeding code removed.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-SEED-PROBE.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-13-SUMMARY.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-PATTERNS.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-BUDGETS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CorpusSnapshot carries an immutable GraphIndex, built at startup and at rebuild</name>
  <files>engine/src/graph/index.rs, engine/src/workflow/ports.rs, engine/src/main.rs, engine/src/ingest.rs, engine/src/tests.rs, scripts/engine-test-targets.sh</files>
  <read_first>
    - `rust-guidelines.md`
    - `engine/src/workflow/ports.rs:19-38` (`CorpusSnapshot`, `new`) and every `CorpusSnapshot::new` call site (`grep -rn "CorpusSnapshot::new" engine/src`)
    - `engine/src/main.rs:30-50,103-112` (startup snapshot, debounce task), `engine/src/ingest.rs:1590-1710` (rebuild-and-swap and degraded handling)
    - `06.1-VERIFICATION.md` (snapshot isolation constraints)
  </read_first>
  <behavior>
    - `CorpusSnapshot::new` takes an `Arc<GraphIndex>`; startup builds it from the store.
    - After an ingest-triggered rebuild, the new snapshot's `graph_index` reflects the new entities, and a query holding the old `Arc<CorpusSnapshot>` keeps the old index (no mutation in place).
    - A `GraphIndex::build` failure during rebuild marks the snapshot `rebuild_degraded = true` and emits the existing `IndexRebuildFailed` notice path, the same as a BM25 failure.
  </behavior>
  <action>
    Add `pub graph_index: Arc<GraphIndex>` to `CorpusSnapshot` and the parameter to `CorpusSnapshot::new`. In `main.rs`, build it with `GraphIndex::build(&database).await?` alongside BM25 before the first snapshot. In the rebuild path in `ingest.rs`, build it next to the BM25 rebuild and swap both inside the same new snapshot; on failure, follow the existing degraded branch.

    Update every `CorpusSnapshot::new` call site and test fake (tests pass an empty `GraphIndex`, via a `GraphIndex::empty()` constructor added for tests and cold starts). Write the tests first and update the gate counts.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked && sh scripts/engine-test-targets.sh</automated>
    <fails_when>non-zero exit, or stderr contains "FAIL:"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rn "CorpusSnapshot::new(" engine/src` shows every call passing a graph index argument, and the build compiles.
    - The rebuild-degradation test for a GraphIndex failure passes.
  </acceptance_criteria>
  <done>The snapshot carries a per-generation graph index with the same isolation and degradation semantics as BM25.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Graph settings in config.rs, config.toml and config.example.toml together</name>
  <files>engine/src/config.rs, config/config.toml, config/config.example.toml, scripts/engine-test-targets.sh</files>
  <read_first>
    - `engine/src/config.rs:94-123` (`default_seed_match_min_score`, `GraphConfigSettings`, `Default`, `GraphSettings`), the agreement test added by 06.3.4.1-11
    - `06.3.4.1-SEED-PROBE.md` (the derived and confirmed `degree_cap`, `max_path_facts`)
  </read_first>
  <behavior>
    - `GraphSettings` exposes `max_seeds = 6`, `mention_vector_top_k = 3`, `degree_cap = <confirmed>`, `max_path_facts = <confirmed>` and `max_graph_chunk_candidates = 8`, with `seed_match_min_score = 0.5` and `max_hop_cap` unchanged.
    - The agreement test asserts the `[engine.graph]` values in `config.toml` and `config.example.toml` equal the `default_*` functions.
    - Config validation rejects `max_seeds = 0`, `degree_cap = 0` and `max_path_facts = 0`.
  </behavior>
  <action>
    For each new key, add a `default_*` function whose doc comment carries its derivation:
    - `degree_cap`: the p99 of the post-reconcile degree distribution, from `06.3.4.1-STORE-BASELINE.md` §4.
    - `max_path_facts`: min(16, floor(0.10 × (evidence_token_budget − answer_budget) / p95 fact tokens)), from `06.3.4.1-SEED-PROBE.md`.
    - `max_seeds`: from RESEARCH §F.1 and the probe's seed-count distribution.
    - `mention_vector_top_k`: RESEARCH §F.1.
    - `max_graph_chunk_candidates`: equals `final_limit`.

    Also add, for each key, a `#[serde(default = ...)]` field on `GraphConfigSettings`, a line in the `Default` impl, and a field on `GraphSettings` with its mapping. Add an `[engine.graph]` table to `config/config.toml` and `config/config.example.toml` with the same values and comments citing D-77. Extend the agreement test and add validation (non-zero) with tests, written first. Update the gate counts.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked config && sh scripts/engine-test-targets.sh</automated>
    <fails_when>non-zero exit, "0 passed" for the config filter, or stderr contains "FAIL:"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "\[engine.graph\]" config/config.toml config/config.example.toml` matches in both files.
    - `git diff --quiet --exit-code -- config/config.eval.toml` exits 0.
  </acceptance_criteria>
  <done>The new graph settings exist once, with derivations, consistent across the three sources.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cut over attempt_graph_augmentation and the graph node; remove the old seeding</name>
  <reversibility rating="costly">Removes the old seeding code path and changes production graph behaviour; undo means restoring from git and re-measuring (D-78).</reversibility>
  <files>engine/src/service.rs, engine/src/workflow/nodes/graph_context.rs, engine/src/workflow/mod.rs, engine/src/workflow/ports.rs, engine/src/tests.rs, scripts/engine-test-targets.sh</files>
  <read_first>
    - `rust-guidelines.md`
    - `engine/src/service.rs:216-345` (`validate_document_id`, `GraphAugmentationOutcome`, `attempt_graph_augmentation`), `:469-520` (`query_graph`, the `graph_traversal` span), `:83-93` (graph port construction)
    - `engine/src/workflow/nodes/graph_context.rs:72-190` (embedding, ablation early return at ~95-105, timeout, presence counters at ~127-148, degradation at ~160-182)
    - `engine/src/workflow/mod.rs:100-150` (ctx fields, `graph_prompt_fact_count` precedent), `engine/src/workflow/ports.rs` (graph port trait + fakes at ~308-441)
    - 06.3.4.1-13 decision (boost semantics) in its summary
  </read_first>
  <behavior>
    - A graph-on query whose question names two entities joined by a 2-hop path yields `Succeeded { facts }` with the path fact rendered, `graph_path_found = true`, `graph_seed_count = 2`, and `graph_chunk_candidates` from the path entities.
    - A graph-on query with seeds but no path yields `graph_path_found = false`, and candidates per the 06.3.4.1-13 decision.
    - A question with no mention and no vector match yields `NoMatchFound`.
    - A graph-off query returns before any mention extraction or index access and leaves every new ctx field at its default.
    - A graph operation exceeding `graph_operation_timeout` still yields `GRAPH_TIMEOUT` and the query continues.
    - Invariant (assumption-delta): no production code references the old single-nearest-entity lookup.
  </behavior>
  <action>
    Change the graph port method to `query_graph(&self, question: &str, query_embedding: &[f32], cancel)`. Keep the embedding parameter so RetrieveHybrid's existing use of the ExtractGraphContext embedding is untouched. Update the trait, the production implementation and the test fakes.

    Rewrite `attempt_graph_augmentation(database, snapshot_graph_index, question, embedder, settings)`:
    1. `extract_mentions(question)`;
    2. `match_seeds` with `LanceMentionVectorSearch` over the production embedding client;
    3. with no seed, `NoMatchFound`;
    4. `find_seed_paths`;
    5. `Succeeded { facts }`.

    Return a struct alongside the outcome carrying `seed_count`, `path_found`, `degree_capped_count`, `seed_document_ids` and `chunk_candidates`. Apply the boost rule from 06.3.4.1-13. Set `graph_node_count`/`graph_edge_count` from path entities/relations. Every error maps to `AttemptedAndFailed { reason }`, with no panic.

    Delete the old seeding code in `service.rs`: the single-result nearest-entity query over `name_vector` (currently `service.rs:256-273`), its `fetch_neighborhood(.., 1, true)` expansion and its `narrow_via_cypher` narrowing. `fetch_neighborhood`, `narrow_via_cypher` and the QueryGraph RPC stay for their other callers.

    In `graph_context.rs`, keep the `disable_graph_context` early return first. Pass the original request question text (not the reformulated variants) to the port. On success, set the new ctx fields `graph_seed_count: u32`, `graph_path_found: bool`, `graph_degree_capped_count: u32`, `graph_seed_document_ids: Vec<String>` and `graph_chunk_candidates: Vec<String>`, declared in `workflow/mod.rs` with defaults following `graph_prompt_fact_count`. Record `lancet.graph.seed_count` and `lancet.graph.path_found` on the existing `graph_traversal` span.

    Write the tests first and update the gate counts.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked && sh scripts/engine-test-targets.sh && (cd gateway && go test ./...)</automated>
    <fails_when>non-zero exit, or stderr contains "FAIL:"</fails_when>
    <automated>test "$(grep -c 'limit(1)' engine/src/service.rs)" -eq 0</automated>
    <fails_when>non-zero exit: service.rs still contains a limit(1) query (the old single-seed lookup)</fails_when>
  </verify>
  <acceptance_criteria>
    - All six Behavior cases have passing tests, including the graph-off early-return test and the invariant test.
    - `graph_context.rs` early return precedes any use of the question text (checked in the summary by line numbers).
    - QueryGraph RPC tests are unchanged and green.
  </acceptance_criteria>
  <done>Production graph-on queries seed from question mentions and inject only seed-to-seed path facts; the old seeding path no longer exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API client question → graph lookups | untrusted text drives production seeding |
| embedding provider | vector fallback calls per graph-on query |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.4.1-14-01 | Tampering | predicate injection via question text | high | mitigate | Mentions only hit in-memory maps and vector search; predicates carry UUID-validated escaped IDs only |
| T-06.3.4.1-14-02 | Denial of service | graph operation cost | high | mitigate | Seed, degree and fact caps from config (validated non-zero); `graph_operation_timeout` unchanged until 06.3.4.1-18 re-derives it; timeout remains non-fatal |
| T-06.3.4.1-14-03 | Tampering | snapshot isolation | high | mitigate | `GraphIndex` immutable in `Arc`, swapped with BM25 inside a new snapshot; tests pin old-snapshot stability |
| T-06.3.4.1-14-04 | Information disclosure | span attributes | low | mitigate | Only counts and a boolean are recorded; no question text or entity names in spans |
</threat_model>

<verification>
- `cargo test --manifest-path engine/Cargo.toml --locked && (cd gateway && go test ./...)` and `sh scripts/engine-test-targets.sh` exit 0.
</verification>

<success_criteria>
Mention seeding with path facts is the only production seeding path, with its settings consistent across
the config trio and graph-off untouched.
</success_criteria>

## Artifacts this phase produces

- `CorpusSnapshot.graph_index: Arc<GraphIndex>`; `GraphIndex::empty()`.
- Graph port method `query_graph(question, query_embedding, cancel)`; rewritten `attempt_graph_augmentation` plus its metadata struct.
- `WorkflowContext` fields `graph_seed_count`, `graph_path_found`, `graph_degree_capped_count`, `graph_seed_document_ids`, `graph_chunk_candidates`; span fields `lancet.graph.seed_count`, `lancet.graph.path_found`.
- Config keys `[engine.graph] max_seeds, mention_vector_top_k, degree_cap, max_path_facts, max_graph_chunk_candidates` and their `default_*` functions.

<output>
Create `.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-14-SUMMARY.md` when done.
</output>
