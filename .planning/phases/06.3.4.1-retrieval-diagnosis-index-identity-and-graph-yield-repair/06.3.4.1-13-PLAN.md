---
phase: 06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair
plan: 13
type: execute
wave: 9
depends_on: [06.3.4.1-12]
files_modified:
  - engine/src/graph/index.rs
  - engine/src/graph/seeding.rs
  - engine/src/graph/paths.rs
  - engine/src/graph/mod.rs
  - engine/src/graph/tests.rs
  - scripts/engine-test-targets.sh
  - engine/src/bin/diag_probe.rs
  - eval/src/lancet_eval/diagnostic.py
  - eval/tests/test_diagnostic.py
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-SEED-PROBE.md
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/diagnostic/seed_probe.jsonl
autonomous: false
requirements: [OBS-05, DATA-05]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-55 is honoured: graph retrieval redesign (seeding, path search, chunk boost) is this phase's work, and this plan starts it only after 06.3.4.1-12's D-84 decision recorded pass-proceed."
    - "Seeding extracts candidate mentions from the original question text with no query-time LLM call: maximal capitalised spans (inner `of|and|&|de|the|-|.|'` allowed), leading stop-words and possessives stripped, plus the sub-span without a leading `The` (D-75)."
    - "Mentions match in the order case-folded exact name → normalised name → `name_vector` top-k for still-unmatched mentions only, with all unmatched mentions embedded in one batched call and searched in one multi-vector KNN; a vector match needs `score ≥ seed_match_min_score`; multiple seeds are allowed up to `max_seeds` (D-75)."
    - "The name index, entity degree and entity→source_chunk_ids maps are immutable per-generation structures (`GraphIndex`), built once from one scan and never mutated per query, so they are not an OI-02-style accumulator (RESEARCH Pattern 2)."
    - "Paths connect seeds: a direct seed–seed edge is a 1-hop path and a shared neighbour X of two seeds is a 2-hop path A–X–B, found by Rust set intersection over one `entity_edges` scan of the seeds; Cypher is not used; only path facts are produced, never unfiltered 1-hop neighbourhoods (D-76)."
    - "Traversal cost is bounded: an intermediate X with degree above `DEGREE_CAP` is excluded, and path facts are ranked by Σ weight × 1/ln(2 + degree) and truncated at `MAX_PATH_FACTS`. `DEGREE_CAP` is the p99 of the post-reconcile degree distribution and `MAX_PATH_FACTS` = min(16, floor(0.10 × (evidence_token_budget − answer_budget) / p95 tokens per rendered fact)); both derivations are recorded in doc comments and in the seed-probe record (D-77)."
    - "Column (d) is yes when at least one seed entity's `source_chunk_ids` resolve to a gold evidence document (chunk_id prefix = document_id); seed count and path found are recorded per question (D-79)."
    - "An offline seed/path probe over the diagnostic sample runs before any production cutover, under the mention-embedding cap authorised at 06.3.4.1-12, and reports seed count, (d), path found by question type, candidate chunks and degree-cap exclusions (RESEARCH §F.7, A9)."
    - "Whether seed source chunks are boosted when no seed-to-seed path exists is decided at a blocking checkpoint with the probe's numbers, before any fusion code is written (RESEARCH Open Question 1; D-76)."
    - "The mention extractor contains no corpus-specific list (no hard-coded publisher names); publisher hubs are demoted only by degree cap and specificity weight."
  artifacts:
    - path: "engine/src/graph/index.rs"
      provides: "GraphIndex { name_casefold, name_normalized, degree, entity_chunks, entity_names } built from one scan"
      contains: "pub struct GraphIndex"
    - path: "engine/src/graph/seeding.rs"
      provides: "extract_mentions, match_seeds, Seed, MatchKind, MentionVectorSearch trait"
      contains: "pub fn extract_mentions"
    - path: "engine/src/graph/paths.rs"
      provides: "find_seed_paths, PathFact, SeedPathResult, DEGREE_CAP/MAX_PATH_FACTS derivation docs"
      contains: "pub async fn find_seed_paths"
    - path: ".planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-SEED-PROBE.md"
      provides: "offline probe results, D-77 cap derivation, Q1 decision"
  key_links:
    - from: "engine/src/bin/diag_probe.rs --graph-seeds"
      to: "engine/src/graph/seeding.rs + paths.rs"
      via: "the same library functions production will call in 06.3.4.1-14"
      pattern: "find_seed_paths"
    - from: "eval/src/lancet_eval/diagnostic.py"
      to: "diagnostic/seed_probe.jsonl"
      via: "d_graph_seed_hit, seed_count, path_found columns"
      pattern: "seed_probe"
  prohibitions:
    - "Mention strings must never be interpolated into a LanceDB predicate or a Cypher query; only UUID-validated entity IDs enter predicates"
    - "No production call site changes in this plan; attempt_graph_augmentation is replaced only in 06.3.4.1-14"
---

<objective>
Build OI-01's seeding and path search as library code, prove it end to end on the real reconciled
graph with an offline probe (store → mention seeds → seed-to-seed paths → candidate chunks → diagnostic
columns d / seeds / path), derive the D-77 caps from data, and decide the one open design question
before any production cutover.

Purpose: layer 5 starts here, after SC-3 passed (D-84). RESEARCH flags that seed-to-seed paths may be
rare on this corpus (Pitfall 12, A9). Measuring that offline for under $0.01 comes before a paid drive
is spent on it.

Output: `graph/index.rs`, `graph/seeding.rs`, `graph/paths.rs` with tests; `diag_probe --graph-seeds`;
diagnostic (d)/seed/path columns; `06.3.4.1-SEED-PROBE.md`; the boost decision.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-CONTEXT.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RESEARCH.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-PATTERNS.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-STORE-BASELINE.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-DRIVE1.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GraphIndex, mention seeding and seed-to-seed paths as tested library code</name>
  <precondition>06.3.4.1-12 Task 3 recorded pass-proceed (or a user decision explicitly allowing OI-01).</precondition>
  <files>engine/src/graph/index.rs, engine/src/graph/seeding.rs, engine/src/graph/paths.rs, engine/src/graph/mod.rs, engine/src/graph/tests.rs, scripts/engine-test-targets.sh</files>
  <read_first>
    - `rust-guidelines.md` (M-DOCUMENTED-MAGIC for every cap and heuristic constant; M-YIELD-POINTS for scans)
    - `engine/src/graph/mod.rs:328-380` (`fetch_neighborhood`: UUID validation, `escape_sql_literal`, bidirectional IN predicate), `engine/src/graph/context_strategy.rs` (`GraphFact`)
    - `engine/src/service.rs:241-313` (current vector seed + `dense_score` threshold semantics kept), `:439-442` (`get_embeddings(&[...])` batching)
    - `engine/src/bin/inspect_lancedb.rs:621-661` (case-folded name scan), `engine/src/db/mod.rs:250-275` (entities, entity_edges schemas)
    - `lancedb-0.31.0` `query.rs:1115` (`add_query_vector`) via RESEARCH §F table
    - RESEARCH §F.1-§F.3, §F.6; PATTERNS "graph/seeding.rs", "graph/paths.rs", "No Analog Found"
  </read_first>
  <behavior>
    - `extract_mentions("Does the article from CBSSports.com credit Jonathan Smith with ...")` includes `CBSSports.com` and `Jonathan Smith` and excludes `Does`; `"What did The Verge's review say"` yields `The Verge` and `Verge`; possessive `'s` / `’s` stripped; a question with no capitalised span yields an empty list.
    - `match_seeds`: an exact case-folded name match wins over a normalised match, which wins over a vector match; a vector candidate below `seed_match_min_score` is rejected; seeds are capped at `max_seeds` and ordered deterministically (match kind, then specificity, then entity_id); the fake `MentionVectorSearch` is called once with all unmatched mentions.
    - `find_seed_paths` on a synthetic graph: seeds A, B with edge A–B gives one 1-hop path; A–X–B gives a 2-hop path through X; the same with degree(X) > cap gives no path and `degree_capped_count = 1`; more paths than `max_path_facts` are truncated after ranking; a single seed yields `path_found = false`; candidate chunk IDs are the ranked union of path entities' `source_chunk_ids`, capped at `max_graph_chunk_candidates`.
    - `GraphIndex::build` over a temp store: a self-loop counts twice in degree; name maps collapse case and punctuation.
  </behavior>
  <action>
    Create `engine/src/graph/index.rs` with `pub struct GraphIndex { name_casefold: HashMap<String, Vec<String>>, name_normalized: HashMap<String, Vec<String>>, degree: HashMap<String, u32>, entity_chunks: HashMap<String, Vec<String>>, entity_names: HashMap<String, String> }`. Add `pub async fn build(db: &DatabaseManager) -> Result<GraphIndex, String>`, which does one `entities` scan (`entity_id, name, source_chunk_ids`) and one `entity_edges` scan (`source_node_id, target_node_id`). Add accessors plus `seed_document_ids(entity_id)`, which derives document IDs from the `{document_id}:{index}` chunk_id prefix.

    Create `engine/src/graph/seeding.rs`:
    - `pub fn extract_mentions(question: &str) -> Vec<String>` (the rules in Behavior, with the stop-word list and connector set as doc-commented constants);
    - `pub enum MatchKind { Exact, Normalized, Vector }` and `pub struct Seed { entity_id, name, match_kind, score, degree }`;
    - `#[async_trait] pub trait MentionVectorSearch { async fn search(&self, mentions: &[String], top_k: usize) -> Result<Vec<Vec<(String, f64)>>, String>; }`;
    - `pub async fn match_seeds(index: &GraphIndex, mentions: &[String], search: &dyn MentionVectorSearch, settings: &SeedSettings) -> Result<Vec<Seed>, String>`;
    - a production `LanceMentionVectorSearch` that embeds with one batched call and runs one multi-vector `nearest_to` + `add_query_vector` over `name_vector` with `limit(top_k)`, scoring with `retrieval::dense::dense_score`.

    Create `engine/src/graph/paths.rs` with `pub struct PathFact { entities: Vec<String>, relations: Vec<String>, score: f64, rendered: String }` and `pub struct SeedPathResult { facts: Vec<GraphFact>, paths: Vec<PathFact>, path_found: bool, candidate_chunk_ids: Vec<String>, degree_capped_count: u32 }`. Add `pub async fn find_seed_paths(db: &DatabaseManager, index: &GraphIndex, seeds: &[Seed], settings: &PathSettings) -> Result<SeedPathResult, String>`: UUID-validate the seed IDs, run one `entity_edges` scan with `source_node_id IN (...) OR target_node_id IN (...)`, then do Rust `HashSet` intersections over seed pairs (at most 15 pairs at 6 seeds).

    `SeedSettings { max_seeds, mention_vector_top_k, seed_match_min_score }` and `PathSettings { degree_cap, max_path_facts, max_graph_chunk_candidates }` are plain structs here; 06.3.4.1-14 maps config onto them.

    Register the modules in `graph/mod.rs`. Write the behaviour tests in `graph/tests.rs` first. Update the lib count and TOTAL in `scripts/engine-test-targets.sh` to the measured values.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked graph::</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>sh scripts/engine-test-targets.sh</automated>
    <fails_when>non-zero exit, or stderr contains "FAIL:"</fails_when>
  </verify>
  <acceptance_criteria>
    - At least 14 new graph tests pass, one or more per Behavior bullet.
    - `grep -n "narrow_via_cypher\|CypherQuery" engine/src/graph/paths.rs engine/src/graph/seeding.rs` returns nothing.
    - No existing call site changed: `git diff --quiet --exit-code -- engine/src/service.rs engine/src/workflow/` exits 0.
  </acceptance_criteria>
  <done>Mention seeding and bounded seed-to-seed path search exist as tested, deterministic library code.</done>
</task>

<task type="auto">
  <name>Task 2: Offline seed/path probe on the real graph, D-77 cap derivation, diagnostic columns</name>
  <precondition>06.3.4.1-12 Task 3 authorised the mention-embedding probe cap; OPENROUTER_API_KEY set; `identity check --corpus multihop_rag_diag` passes.</precondition>
  <files>engine/src/bin/diag_probe.rs, eval/src/lancet_eval/diagnostic.py, eval/tests/test_diagnostic.py, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-SEED-PROBE.md, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/diagnostic/seed_probe.jsonl</files>
  <read_first>
    - `engine/src/bin/diag_probe.rs` (06.3.4.1-09 modes and spend metering)
    - `reconcile/graph_population_post.json` and `06.3.4.1-STORE-BASELINE.md` §4 (p99 degree)
    - `engine/src/generation/mod.rs:73-85` (evidence and answer token budgets), `engine/src/prompt.rs` (fact rendering and cl100k counting)
    - `eval/src/lancet_eval/diagnostic.py`
  </read_first>
  <action>
    Set `DEGREE_CAP` to the p99 degree from `graph_population_post.json`, rounded up. Add a `--graph-seeds --questions <jsonl> --stage-cap <usd> --degree-cap <n>` mode to `diag_probe`: build `GraphIndex`, extract mentions from each question's original text, run `match_seeds` with the production vector search (embedding cache and spend metering as in the vector mode), and run `find_seed_paths` with `max_path_facts` unbounded for the probe only, so every found path is reported.

    Print per question: `question_id`, `mentions`, `seeds[{entity_id, name, match_kind, score, degree, source_document_ids}]`, `seed_count`, `path_found`, `paths[{rendered, score, tokens}]` (tokens = cl100k count of the rendered fact), `candidate_chunk_ids`, `degree_capped_count`. The final line is a summary with spend.

    Run it over `eval/corpora/multihop_rag/questions.diag.jsonl` → `diagnostic/seed_probe.jsonl`. Compute `MAX_PATH_FACTS = min(16, floor(0.10 × (8192 − 2048) / p95(tokens)))`.

    In `diagnostic.py`, `build_rows` gains `seed_probe_path` (CLI `--seed-probe`): `d_graph_seed_hit` (any seed `source_document_ids` intersecting the question's gold document IDs from the map), `seed_count` and `path_found`. Add tests.

    Write `06.3.4.1-SEED-PROBE.md`:
    - seed-count distribution; exact / normalised / vector match shares;
    - (d) rate on G and on all non-null;
    - `path_found` rate by `question_type`;
    - hub seeds (degree > p95) and how often publisher names seeded;
    - `degree_capped_count` totals;
    - candidate-chunk count distribution and the share of questions whose candidates include a chunk outside the (c)-probe's dense top-4 (a composition-change proxy, labelled as such);
    - the D-77 derivation with the numbers used (p99, p95 fact tokens, the resulting two caps);
    - spend against the authorised cap.
  </action>
  <verify>
    <automated>cargo build --manifest-path engine/Cargo.toml --release --bin diag_probe && uv run --project eval pytest eval/tests/test_diagnostic.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the output</fails_when>
    <automated>uv run --project eval python -c "import json; rows = [json.loads(l) for l in open('.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/diagnostic/seed_probe.jsonl')]; q = [r for r in rows if 'question_id' in r]; assert q and all('seed_count' in r and 'path_found' in r for r in q); assert rows[-1].get('stopped_by_cap') is False, rows[-1]"</automated>
    <fails_when>AssertionError printed: empty probe output, missing columns, or the probe stopped on its cap</fails_when>
  </verify>
  <acceptance_criteria>
    - `06.3.4.1-SEED-PROBE.md` states `DEGREE_CAP` and `MAX_PATH_FACTS` with their input numbers, the path-found rate by type, and the (d) rate on G.
    - Probe spend is at or below the authorised cap.
  </acceptance_criteria>
  <done>The new seeding and path search are measured on the real graph for the exact diagnostic questions, and the caps are derived from data.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Seed-chunk boost when no seed-to-seed path exists (Open Question 1)</name>
  <decision>When a question has seeds but no seed-to-seed path, should the chunk boost use the seeds' own source chunks?</decision>
  <context>
    D-76 says to boost the source chunks of path entities. When no path exists, the literal reading
    boosts nothing, and SC-4/SC-5 can only move on questions with paths (RESEARCH Pitfall 12). Present
    from `06.3.4.1-SEED-PROBE.md`: path-found rate overall and by type, (d) rate on G, seed hub rates,
    and the composition-change proxy under each option (paths only, versus paths plus length-0 seed
    paths). Also confirm the derived `DEGREE_CAP` and `MAX_PATH_FACTS`.

    Whichever option is chosen, it ships as the production default with no flag (D-78) and is reported
    per D-49 if the result is negative. Choosing by which option makes SC-4 pass is the tuning D-83
    forbids; the choice is about what the graph should contribute.
  </context>
  <options>
    <option id="paths-only">
      <name>Paths only (D-76 literal): no path means no graph chunk candidates</name>
      <pros>Strictest reading; injects only bridge evidence</pros>
      <cons>If paths are rare, graph-on barely changes retrieval composition</cons>
    </option>
    <option id="paths-plus-seed-chunks">
      <name>Paths first, and when no path exists use the top seeds' source chunks (a length-0 path), capped at max_graph_chunk_candidates</name>
      <pros>The graph contributes entity-anchored chunks on questions without a bridge</pros>
      <cons>An extension of D-76's wording that needs this explicit approval; hub seeds are still demoted by specificity</cons>
    </option>
  </options>
  <resume-signal>Reply paths-only or paths-plus-seed-chunks, and confirm (or amend) the two caps.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user question text → graph lookups | untrusted text drives entity matching |
| probe → embedding provider | paid calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.4.1-13-01 | Tampering | predicate / Cypher injection via mentions | high | mitigate | Mentions are matched in in-memory hash maps and by vector only; predicates carry UUID-validated, `escape_sql_literal`-escaped entity IDs; Cypher is not used |
| T-06.3.4.1-13-02 | Denial of service | hub expansion | high | mitigate | Seeds capped at `max_seeds`; one edge scan; intermediates above `DEGREE_CAP` excluded; facts truncated at `MAX_PATH_FACTS` |
| T-06.3.4.1-13-03 | Denial of service | per-query memory | medium | mitigate | `GraphIndex` built once per generation and immutable; no per-query cache growth |
| T-06.3.4.1-13-04 | Denial of service (financial) | mention probe | low | mitigate | Cap authorised at 06.3.4.1-12; cached embeddings; metered per batch |
</threat_model>

<verification>
- `cargo test --manifest-path engine/Cargo.toml --locked`, `sh scripts/engine-test-targets.sh`, `uv run --project eval pytest -q` exit 0.
</verification>

<success_criteria>
The OI-01 library is tested, measured on the real graph for the diagnostic sample, its caps are
derived, and the boost semantics are decided before production cutover.
</success_criteria>

## Artifacts this phase produces

- Rust: `graph::index::GraphIndex` (`build`, `seed_document_ids`), `graph::seeding::{extract_mentions, match_seeds, MatchKind, Seed, SeedSettings, MentionVectorSearch, LanceMentionVectorSearch}`, `graph::paths::{find_seed_paths, PathFact, SeedPathResult, PathSettings}`; doc-commented derivations of `DEGREE_CAP` and `MAX_PATH_FACTS`.
- `diag_probe --graph-seeds --questions --stage-cap --degree-cap`.
- `lancet_eval.diagnostic.build_rows(..., seed_probe_path)` (CLI `--seed-probe`) filling `d_graph_seed_hit`, `seed_count`, `path_found`.
- Files: `06.3.4.1-SEED-PROBE.md`, `diagnostic/seed_probe.jsonl`.

<output>
Create `.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-13-SUMMARY.md` when done.
</output>
