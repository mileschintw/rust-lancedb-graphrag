---
phase: 06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/src/bin/inspect_lancedb.rs
  - engine/src/inspect_lancedb_tests.rs
  - scripts/engine-test-targets.sh
  - eval/src/lancet_eval/diagnostic.py
  - eval/tests/test_diagnostic.py
  - eval/tests/fixtures/diagnostic/gold_chunks.jsonl
  - eval/tests/fixtures/diagnostic/journal_slice.jsonl
  - eval/src/lancet_eval/metrics.py
  - eval/tests/test_metrics.py
  - eval/scripts/retro_lenient.py
  - eval/tests/test_retro_lenient.py
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RETRO-LENIENT.md
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/diagnostic/pre-reconcile/gold_chunks.jsonl
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/diagnostic/pre-reconcile/table.jsonl
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/diagnostic/pre-reconcile/table.md
autonomous: true
requirements: [OBS-05]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The per-question diagnostic table exists end to end on real data: `inspect_lancedb --gold-chunks` reads the live eval store read-only, `python -m lancet_eval.diagnostic table` joins it with `document_map.json` and a journal, and writes one JSONL row and one markdown yes/no row per sampled question. It is regenerated by command, never hand-edited."
    - "Column (b) is computed on stored `nodes.content` (the full chunk text, not the 512-character excerpt) with `normalize_ws` containment, per evidence item, as a tri-state `in_chunk` / `split_across_chunks` / `absent`. Question-level (b) is yes only when every evidence item is `in_chunk` (D-63). Doc-level presence is never used as a denominator."
    - "Column (e) `answer_usable` is judged on the extracted final-answer line only, and the last `Answer:` line wins. A missing or empty line sets `final_answer_missing` and scores a miss that stays in the denominator (D-70, D-74)."
    - "`gold_contained` is whole-token containment of the SQuAD-normalised gold token sequence, so `no` never matches inside `not` or `know` (D-70). Existing `em_f1` and `abstention_rate` are unchanged."
    - "Null-query questions are scored only by `null_abstention_correct` on the extracted line. They never enter G or SC-3's number (D-72)."
    - "D-69's three citation-constraint messages are classified per record as distinct error classes (`citation_basis_mixed`, `citation_basis_retrieval`, `model_only_unsupported`), next to `citation_marker_mismatch`, `timeout`, `transport` and `other`. A harness deadline or read timeout is classed `timeout`, never `transport` (D-69)."
    - "The D-74 retro reproduces 91/147 whole-token containment on the 06.3.4 journal's graph-off non-null successes through a read-only script labelled `partial-run diagnostic, n=147`. It never goes through the fail-closed scorer or report generator, writes no `report.json`, and adds no CLI flag (D-74; 06.3.1 D-43 non-comparability)."
    - "D-57 is honoured by this plan set: no task un-checks OBS-05, DATA-04 or DATA-05, and no plan instructs `requirements.revert-phase` on them."
    - "Free work runs before any paid stage (D-85): nothing in this plan calls a provider."
    - "One diagnostic row per sampled question_id: a question's graph-on and graph-off records sit side by side in one row and are never merged into one value. A second record for the same (question_id, arm) raises `DiagnosticError` instead of being silently chosen (specless edge: adjacency, OBS-05; delivered here)."
    - "Empty inputs are explicit, never silent: a missing or empty `Answer:` line is a `final_answer_missing` miss inside the denominator; a sampled question with no journal record gets arm outcome `not_run`; gate evaluators return MISS with reason `n=0`, never PASS, on an empty population (specless edge: empty, OBS-05; delivered here and in 06.3.4.1-05)."
    - "Ordering is specified and stable: table rows sort by question_id, flatness ordinals follow journal line order, and when two `Answer:` lines exist the last wins (specless edge: ordering, OBS-05; delivered here and in 06.3.4.1-05)."
    - "Store-changing tools are idempotent: re-running the reconcile dry-run after `--apply` reports zero extras, zero entity rewrites and zero orphans, and the D-59 re-ingest helper refuses a document whose gold facts are no longer `absent` in the latest (b) probe or whose ID is no longer a map entry, so a second run is a no-op (specless edge: idempotency, DATA-03; delivered by 06.3.4.1-04, -06 and -10)."
    - statement: "Reconcile `--apply` and the D-59 re-ingest run only with the engine and gateway stopped. An interrupted apply is recovered from the snapshot directory or a table-version restore after a fresh dry-run, never by re-running `--apply` over a half-pruned store (specless edge: concurrency, DATA-03; delivered by 06.3.4.1-06 and -10)."
      verification: backstop
    - "The provenance prune is idempotent: a second prune over an already-pruned store rewrites zero entities, and every entity that keeps at least one surviving source chunk survives with every other column preserved (specless edge: idempotency, DATA-04; delivered by 06.3.4.1-04)."
    - statement: "No entity extraction runs concurrently with the prune: the prune runs after any D-59 re-ingest has completed and with `staged_documents_v2` at 0 rows, so the exact-match resolver cannot append provenance mid-prune (specless edge: concurrency, DATA-04; delivered by 06.3.4.1-06 and -10)."
      verification: backstop
  artifacts:
    - path: "engine/src/bin/inspect_lancedb.rs"
      provides: "read-only `--gold-chunks` mode emitting per-evidence-item (b) JSONL"
      contains: "GoldChunks"
    - path: "eval/src/lancet_eval/diagnostic.py"
      provides: "DiagnosticRow/ArmResult models, table builder, JSONL + markdown writer, `python -m` entry"
      exports: ["DiagnosticRow", "ArmResult", "DiagnosticError", "build_rows", "classify_record", "write_table"]
    - path: "eval/src/lancet_eval/metrics.py"
      provides: "extract_final_answer, gold_contained, final_answer_em, answer_usable, null_abstention_correct"
      contains: "def extract_final_answer"
    - path: "eval/scripts/retro_lenient.py"
      provides: "D-74 read-only retro"
    - path: ".planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RETRO-LENIENT.md"
      provides: "partial-run diagnostic, n=147 attribution baseline"
  key_links:
    - from: "eval/src/lancet_eval/diagnostic.py"
      to: "engine/src/bin/inspect_lancedb.rs"
      via: "gold-chunk JSONL produced by `--gold-chunks`, read by `build_rows`"
      pattern: "gold_chunks"
    - from: "eval/src/lancet_eval/diagnostic.py"
      to: "eval/src/lancet_eval/metrics.py"
      via: "column (e) computed by answer_usable on the extracted line"
      pattern: "answer_usable"
    - from: "eval/scripts/retro_lenient.py"
      to: "eval/src/lancet_eval/journal.py"
      via: "load_records over the committed 2026-09-09 journal, read-only"
      pattern: "load_records"
  prohibitions:
    - requirement_id: OBS-05
      category: transparency
      status: resolved
      verification: test
      flagged: true
      flagged_unverified: true
      statement: "MUST NOT publish, label or cite a halted or partial drive (including the D-74 retro) as a complete scored run. Partial-run output is always labelled with its n, and only the unmodified fail-closed score/report path may write report.json."
    - requirement_id: OBS-05
      category: transparency
      status: resolved
      verification: judgment
      flagged: true
      flagged_unverified: true
      statement: "MUST NOT set, move or relax a gate threshold (SC-3 floor, D-69 tripwire, SC-2 reading, SC-4 Wilson clause, composition floor) after the data it tests exists, and MUST NOT suppress, re-run or tune toward a gate miss; a miss is disclosed."
    - requirement_id: OBS-05
      category: transparency
      status: resolved
      verification: judgment
      flagged: true
      flagged_unverified: true
      statement: "MUST NOT present pre-06.3.4.1 EM, Token-F1 or graph-presence numbers as comparable with runs made after the index, prompt or seeding changed."
    - requirement_id: OBS-05
      category: transparency
      status: resolved
      verification: test
      flagged: true
      flagged_unverified: true
      statement: "MUST NOT relabel a timeout as another error class, or retry it away, so that SC-2 passes."
    - requirement_id: DATA-03
      category: safety
      status: resolved
      verification: test
      flagged: true
      flagged_unverified: true
      statement: "MUST NOT delete from or re-ingest into anything other than the isolated eval LanceDB store and the lancet_eval PostgreSQL schema, and MUST NOT make a one-way store change before a restorable snapshot is recorded."
    - requirement_id: DATA-04
      category: safety
      status: resolved
      verification: test
      flagged: true
      flagged_unverified: true
      statement: "MUST NOT delete an entity or entity edge that still has a surviving provenance chunk in a mapped document, and MUST NOT trigger paid re-extraction outside an authorised D-59 stage."
    - requirement_id: DATA-05
      category: transparency
      status: resolved
      verification: judgment
      flagged: true
      flagged_unverified: true
      statement: "MUST NOT make the graph path eval-only, toggleable, or special-cased to the evaluation corpus (for example hard-coded publisher names); production and eval run the same seeding and the same prompt."
    - requirement_id: DATA-05
      category: transparency
      status: resolved
      verification: judgment
      flagged: true
      flagged_unverified: true
      statement: "MUST NOT hide, switch off, or re-run a negative graph-on result until it improves; it is reported (06.3.1 D-49)."
---

<objective>
Lead the phase with one thin, production-quality path through every layer the diagnosis touches:
the live eval store, a read-only Rust probe, the harness, and the required per-question yes/no table
(ROADMAP "Required diagnostic artifact"). Then fill in the answer-usability column and the free D-74
attribution baseline.

ROADMAP goal (verbatim, not reframed as a user story, see Flagged Assumptions): "Separate seeding/index
drift vs vector/hybrid retrieval vs graph architecture; restore a working vector baseline; then make
graph-on produce a measurable paired effect."

Purpose: every later gate (SC-2..SC-5), the D-83/D-84 iteration rules and the SC-3 population G are
read off this table. Building it first, on real data, proves the columns can be computed before any
money is spent (D-85). Columns (c) and (d) and the SC evaluators are added by later plans without
changing the table's shape.

Output: `--gold-chunks` probe mode, `diagnostic.py`, five new metric functions, the D-74 retro script
and its result, and a pre-reconcile table over the 06.3.4 journal labelled as a partial-run diagnostic.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-CONTEXT.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RESEARCH.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-PATTERNS.md
@.planning/phases/06.3.1-fix-retrieval-citation-collapse-and-graph-ablation-measureme/06.3.1-CONTEXT.md
@CLAUDE.md
</context>

## Flagged Assumptions (spec-less probe and research)

- **DATA-05 edge probe: `unclassified`, left unresolved.** No edge predicate is authored for it. The
  DATA-05 surface ("compile graph context into the prompt") is checked through the path canary in
  06.3.4.1-17/-18 (`graph_prompt_fact_count >= 1` on a `require_seed_path` canary). The probe category
  itself stays unresolved and is recorded here rather than dropped.
- Edge-probe no-silent-drop count: 8 surfaced = 5 explicit truths + 2 backstop truths + 1 flagged
  assumption (above).
- Prohibition recall: 8 kept, all authored descriptor-less above. Canon referral: predicate or Cypher
  injection through question mention strings is canon injection, so it is covered by the threat models
  and `/gsd-secure-phase` and is not minted here.
- **MVP user-story framing.** The ROADMAP Goal line is not in "As a / I want to / so that" form. This
  plan does not invent one. If story framing is wanted, run `/gsd mvp-phase 06.3.4.1` first.
- RESEARCH A2 (the (b)=no rate is a few percent at most) is untested until Task 1's live run. The
  pre-reconcile table records it; the binding G is fixed in 06.3.4.1-10 after the reconcile.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer, one diagnostic row from store to table (gold-chunk probe to diagnostic.py)</name>
  <precondition>`data/lancedb-eval/nodes.lance` exists (the runtime eval store) and no engine or ingest process is writing to it (`tasklist` shows no `engine.exe`).</precondition>
  <files>engine/src/bin/inspect_lancedb.rs, engine/src/inspect_lancedb_tests.rs, scripts/engine-test-targets.sh, eval/src/lancet_eval/diagnostic.py, eval/tests/test_diagnostic.py</files>
  <read_first>
    - `rust-guidelines.md` (CLAUDE.md requires it before any Rust edit; relevant: M-LOG-STRUCTURED, M-DOCUMENTED-MAGIC)
    - `engine/src/bin/inspect_lancedb.rs` — `string_column` (101-108), `query_columns` (140-159), `inspect_entity_name` (621-661), `InspectMode`/`USAGE`/`parse_args`/`mode_count` (663-777), `main` (779-823)
    - `engine/src/inspect_lancedb_tests.rs` — synthetic temp-store fixtures (`NodeFixture`, `valid_nodes`)
    - `scripts/engine-test-targets.sh` — `BIN_INSPECT_COUNT` assertion (31) and the header rule forbidding lowered counts
    - `eval/src/lancet_eval/usability.py` (module shape), `eval/src/lancet_eval/journal.py:20-141` (`RunRecord`, `WorkflowWireMeta` extra="forbid", `load_records`), `eval/src/lancet_eval/seed.py:170-230` (`DocumentMap`, `load_document_map`), `eval/src/lancet_eval/corpus.py:24-44,195-218` (`GoldQuestion`, `is_null`, `load_sample_questions`)
    - `eval/src/lancet_eval/metrics.py:36-72` (`normalize_ws`, `squad_normalize`, `fact_matches_excerpt`)
    - RESEARCH §E (the (b) rule and tri-state) and §M (table inputs and error classes); AI-SPEC §4b `ArmResult`/`DiagnosticRow` shape (a proposal, not code)
  </read_first>
  <action>
    Rust (follow `rust-guidelines.md`): add `InspectMode::GoldChunks { questions: PathBuf, map: PathBuf }` to `inspect_lancedb.rs`, with flags `--gold-chunks <questions.jsonl>` and `--map <document_map.json>`. Add both to `USAGE`, `parse_args` (value-required errors) and `mode_count`. The mode opens the store only through `DatabaseManager::open_and_validate` (read-only). It loads `document_map.json` entries (`document_id -> title`; aliases are ignored), reads every `evidence_list` item (`title`, `fact`) of every question line, and maps title to document_id. It then selects `document_id, chunk_id, chunk_index, content` from `nodes` with one `document_id IN (...)` predicate per 500 IDs. Every ID is UUID-validated (the `Uuid::parse_str` + `get_version_num() == 4` pattern) and escaped with `escape_sql_literal`.

    For each evidence item, compute `state`:
    - `in_chunk` when `normalize_ws(fact)` is a substring of `normalize_ws(content)` for some chunk of that document. `normalize_ws` is collapse-whitespace plus lowercase, the same rule as `eval/src/lancet_eval/metrics.py` `normalize_ws`; write a Rust twin and pin it with a test on the same inputs.
    - else `split_across_chunks` when the fact is contained in the concatenation of chunks with adjacent `chunk_index` i and i+1.
    - else `absent`.
    - `unmapped_title` when the title has no map entry.

    Print one JSON object per evidence item to stdout with keys `question_id`, `evidence_index`, `title`, `document_id` (string or null), `state`, `chunk_ids` (sorted chunk_ids containing the fact; empty unless `in_chunk`). Null questions (no evidence) print nothing. Sort by (question_id, evidence_index). Add tests in `inspect_lancedb_tests.rs` on a synthetic temp store covering `in_chunk`, `split_across_chunks`, `absent`, `unmapped_title` and the whitespace/case rule. Update `BIN_INSPECT_COUNT` (and `TOTAL`) in `scripts/engine-test-targets.sh` to the newly measured values in the same commit; never lower a value.

    Python: create `eval/src/lancet_eval/diagnostic.py` (module docstring; `from __future__ import annotations`), with:
    - `DiagnosticError(Exception)`.
    - `ArmResult` (pydantic, `ConfigDict(extra="forbid")`): `arm`, `outcome` (`success` | `error` | `not_run`), `error_class` (str | None), `answer_usable` (bool | None), `final_answer_missing` (bool | None), `final_answer` (str | None).
    - `DiagnosticRow` (extra="forbid"): `question_id`, `question_type`, `is_null`, `a_gold_doc_in_map` (bool | None; None for null questions), `b_items` (list of per-item states), `b_gold_chunk_in_lancedb` (bool | None), `c_gold_in_vector_top4` (bool | None, not yet measured), `d_graph_seed_hit` (bool | None), `seed_count` (int | None), `path_found` (bool | None), `e_answer_usable` (bool | None, the graph-off arm's), `arms: dict[str, ArmResult]`, `in_gold_in_index_subset` (bool).
    - `build_rows(corpus_name, journal_path | None, gold_chunks_path) -> list[DiagnosticRow]`: one row per question from `load_sample_questions(corpus_name)`. (a) is true iff every evidence title is in the map (`load_document_map`). (b) is true iff every item is `in_chunk` (D-63). `in_gold_in_index_subset = (not is_null) and b is True`. Journal records attach per arm via `load_records`. A second record for the same (question_id, arm) raises `DiagnosticError`. Questions without a record get `outcome="not_run"`. Rows sort by question_id.
    - `write_table(rows, out_dir)`: writes `table.jsonl` (one `model_dump_json` per line) and `table.md` (header row plus one yes/no/— row per question, columns `question_id | type | a | b | c | d | e (graph-off) | graph-on outcome | graph-off outcome | seeds | path | final_answer_missing (graph-off)`), preceded by a one-line provenance header naming the journal path, the gold-chunk file and the record count.
    - `main(argv)` behind `if __name__ == "__main__"`, with subcommand `table` and options `--corpus`, `--journal`, `--gold-chunks`, `--out-dir`, `--label`. The label is printed as the first markdown line; when the journal header says `partial: true` the label must contain `partial-run diagnostic` and n.

    In this task `answer_usable` and `error_class` are left None; Task 2 computes them. Test the builder in `eval/tests/test_diagnostic.py` with fixtures `eval/tests/fixtures/diagnostic/gold_chunks.jsonl` and `journal_slice.jsonl` (hand-built, 4 questions: one all-in_chunk, one with a split item, one null, one with no journal record, plus a duplicate-record case that must raise).
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked --bin inspect_lancedb gold_chunks</automated>
    <fails_when>non-zero exit, or the summary line reports "0 passed" (the filter matched no test)</fails_when>
    <automated>sh scripts/engine-test-targets.sh</automated>
    <fails_when>non-zero exit, or stderr contains "FAIL:"</fails_when>
    <automated>uv run --project eval pytest eval/tests/test_diagnostic.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the output</fails_when>
    <automated>OUT=.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/diagnostic/pre-reconcile && mkdir -p "$OUT" && cargo run --manifest-path engine/Cargo.toml --release --quiet --bin inspect_lancedb -- --gold-chunks eval/corpora/multihop_rag/questions.sample.jsonl --map eval/corpora/multihop_rag/document_map.json --lancedb-path ./data/lancedb-eval > "$OUT/gold_chunks.jsonl" && uv run --project eval python -m lancet_eval.diagnostic table --corpus multihop_rag --journal eval/runs/2026-09-09-multihop_rag/journal.jsonl --gold-chunks "$OUT/gold_chunks.jsonl" --out-dir "$OUT" --label "pre-reconcile" && EXPECTED=$(uv run --project eval python -c "import json; print(sum(1 for l in open('eval/corpora/multihop_rag/questions.sample.jsonl', encoding='utf-8') if l.strip() for e in json.loads(l).get('evidence_list', []) if e.get('fact')))") && test "$(wc -l < "$OUT/table.jsonl")" -eq 500 && test "$(wc -l < "$OUT/gold_chunks.jsonl")" -eq "$EXPECTED"</automated>
    <fails_when>non-zero exit from any step; table.jsonl line count is not 500; gold_chunks.jsonl line count differs from the number of evidence items with a non-empty fact (RESEARCH §E expects 1185)</fails_when>
  </verify>
  <acceptance_criteria>
    - `inspect_lancedb --gold-chunks` against `./data/lancedb-eval` emits one JSON line per evidence item with a non-empty fact (1185 expected per RESEARCH §E; evidence items with an empty fact are skipped), each with keys `question_id, evidence_index, title, document_id, state, chunk_ids`.
    - `diagnostic/pre-reconcile/table.jsonl` has 500 lines; `table.md` first line contains `partial-run diagnostic` (the 2026-09-09 journal header is `partial: true`).
    - Column (a) is yes for all 447 non-null rows (CONTEXT domain evidence); if not, the summary records the mismatch and the task stops rather than editing data.
    - The pre-reconcile (b) tri-state counts (`in_chunk` / `split_across_chunks` / `absent` / `unmapped_title`) are recorded in the plan summary.
    - `scripts/engine-test-targets.sh` exits 0 with the raised counts; `git diff --quiet --exit-code -- eval/runs/2026-09-09-multihop_rag/ eval/runs/2026-09-03-multihop_rag/` exits 0.
  </acceptance_criteria>
  <done>One real command pair turns the live store plus a journal into the per-question table; (a) and (b) are real; the shape carries every ROADMAP column.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Final-answer extraction, lenient metrics and error classes fill column (e)</name>
  <files>eval/src/lancet_eval/metrics.py, eval/tests/test_metrics.py, eval/src/lancet_eval/diagnostic.py, eval/tests/test_diagnostic.py</files>
  <read_first>
    - `eval/src/lancet_eval/metrics.py` — `normalize_ws` (38-40), `squad_normalize` (43-59), `em_f1` (385-406), `abstention_outcome`/`abstention_rate` (441-480), `MetricOutcome`
    - `eval/tests/test_metrics.py` (existing style)
    - `eval/src/lancet_eval/diagnostic.py` (from Task 1)
    - `eval/src/lancet_eval/journal.py:45-73` (`node_failures`, `error_type`), `eval/src/lancet_eval/run.py:198-206` (transport `error_type`), `eval/src/lancet_eval/client.py:163,253-255` (deadline/read-timeout raise sites)
    - RESEARCH §A "Extraction rule" + golden cases, §B classification table, §G metric list; RESEARCH §Code Examples "Final-answer extraction"; AI-SPEC row 33 is WRONG and must not be copied
  </read_first>
  <behavior>
    - `extract_final_answer("...\nAnswer: Yes")` is `yes`; `**Answer:** Yes` and `**Answer**: **Yes**` are both `yes` (emphasis characters are stripped, never the text between them).
    - `Answer: Yes [1]` is `yes` (markers are stripped); with two Answer lines the last wins; no line gives None; `Answer:` followed by nothing gives None.
    - `gold_contained("no", "I do not know")` is False; `gold_contained("no", "The answer is no.")` is True.
    - The (e) loophole: final line `No` with `yes` in the prose makes `answer_usable` False for gold `Yes`.
    - `null_abstention_correct` is 1.0 when the line normalises to `insufficient information`, 0.0 otherwise, and raises `ValueError` on a non-null question.
    - `classify_record` maps the three D-69 messages, the marker-mismatch message, a node `error_kind == 1`, a harness deadline or read-timeout `error_type`, and any other `error_type` to `citation_basis_mixed` / `citation_basis_retrieval` / `model_only_unsupported` / `citation_marker_mismatch` / `timeout` / `timeout` / `transport`, and anything else to `other`.
  </behavior>
  <action>
    In `metrics.py`, add next to `squad_normalize` and leave `em_f1` and `abstention_rate` byte-identical (D-70):
    - `_ANSWER_LINE = re.compile(r"(?im)^[ \t>*_-]*answer[ \t*_]*:[ \t]*(?P<a>.+?)[ \t]*$")` and `_MARKER = re.compile(r"\[\s*\d+\s*\]")`.
    - `extract_final_answer(answer: str | None) -> str | None`: take the last match, remove markers, remove `*` characters, replace `_` with a space, apply `squad_normalize`; empty gives None.
    - `gold_contained(gold: str, text: str) -> bool`: whole-token containment of the normalised gold token sequence (the RESEARCH §Code Examples form).
    - `final_answer_em(question: GoldQuestion, answer: str) -> MetricOutcome`: 1.0 iff the extracted line equals `squad_normalize(gold_answer)`; missing gives 0.0 with `detail={"final_answer_missing": True}`.
    - `answer_usable(question: GoldQuestion, answer: str) -> bool`: `gold_contained(gold, extracted_line)` on the extracted line only; missing gives False.
    - `null_abstention_correct(question: GoldQuestion, answer: str) -> MetricOutcome`, with the `abstention_outcome` non-null guard copied (raises `ValueError`).

    Every new function has a one-line docstring naming D-70 or D-72. In `diagnostic.py`, add `classify_record(record) -> str | None` implementing the behaviour table (match the D-69 messages by their stable prefixes: "answer basis 'mixed' requires at least one cited evidence ID", "answer basis 'retrieval' requires at least one cited evidence ID", "ModelOnly answer basis is not supported"). Fill `ArmResult.error_class`, `answer_usable`, `final_answer_missing` and `final_answer` for successful records, and `DiagnosticRow.e_answer_usable` from the graph-off arm. For null questions (e) stays None and the arm carries `null_abstention_correct` in `final_answer` context only (D-72: nulls never feed (e)). Write the tests first (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_metrics.py eval/tests/test_diagnostic.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the output</fails_when>
    <automated>uv run --project eval ruff check --preview eval/src/lancet_eval/metrics.py eval/src/lancet_eval/diagnostic.py</automated>
    <fails_when>non-zero exit, or any "error" line in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `test_metrics.py` contains a test for each behaviour bullet (at least 10 new test functions), all passing.
    - `git diff` of `metrics.py` shows no change inside the bodies of `em_f1`, `abstention_outcome` or `abstention_rate`.
    - Regenerating the pre-reconcile table (the Task 1 live command) yields `final_answer_missing: true` on every graph-off success row (the 06.3.4 prompt predates D-71; AI-SPEC row 38: 150/150).
    - The D-69 class counts in the regenerated table equal 25 mixed / 8 retrieval / 10 model_only (RESEARCH §B).
  </acceptance_criteria>
  <done>Column (e), `final_answer_missing` and the error class are computed per arm and pinned by golden tests.</done>
</task>

<task type="auto">
  <name>Task 3: D-74 retro, read-only attribution baseline on the 06.3.4 journal</name>
  <files>eval/scripts/retro_lenient.py, eval/tests/test_retro_lenient.py, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RETRO-LENIENT.md</files>
  <read_first>
    - `eval/scripts/drive_measurement_pass.py:1-35` (docstring/imports/logging header only)
    - RESEARCH §G "D-74 retro" and §Code Examples "D-74 retro"; AI-SPEC §5 note on the SC-3 floor (proxy numbers)
    - `eval/src/lancet_eval/stats.py` (`wilson_ci`)
  </read_first>
  <action>
    Create `eval/scripts/retro_lenient.py`. Its imports are limited to `lancet_eval.journal.load_records`, `lancet_eval.corpus.load_sample_questions`, `lancet_eval.metrics` and `lancet_eval.stats`. It never imports the scoring or report modules, calls no subprocess, and opens no file for writing except the markdown path given by `--out`.

    For the graph-off, non-null, successful records of `eval/runs/2026-09-09-multihop_rag/journal.jsonl`, compute and print:
    - character-substring containment;
    - whole-token `gold_contained` on the full answer, overall and by `question_type`;
    - the count of `Answer:` lines present;
    - the stricter proxy that drops hits whose gold (`no`) already appears in the question text;
    - Wilson 95% CIs.

    Also compute the D-69 baseline: rejections over records that reached GenerateAnswer, split null vs non-null, per class and arm, with the caveat that it was recorded under `--retries 2` and is a lower bound (RESEARCH §A item 4).

    The first line of the output is exactly `partial-run diagnostic (06.3.4 journal, graph-off successes), n=147`, with n computed, not typed. Mark the 06.3.4 and 2026-09-03 EM/F1 as non-comparable (06.3.1 D-43 pattern). Write `06.3.4.1-RETRO-LENIENT.md` from the script output (`--out`).

    `eval/tests/test_retro_lenient.py`:
    - runs the script's `main()` against the committed journal and asserts 91 hits out of n=147;
    - asserts the label line;
    - asserts that no `report.json` appears anywhere under a tmp copy of the run directory;
    - asserts, by `ast` inspection of the script source, that none of its imports names the score or report modules.
  </action>
  <verify>
    <automated>uv run --project eval pytest eval/tests/test_retro_lenient.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the output</fails_when>
    <automated>uv run --project eval python eval/scripts/retro_lenient.py --out .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RETRO-LENIENT.md && grep -q "n=147" .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RETRO-LENIENT.md && git diff --quiet --exit-code -- eval/runs/</automated>
    <fails_when>non-zero exit; "n=147" absent from the markdown; any tracked file under eval/runs/ changed</fails_when>
  </verify>
  <acceptance_criteria>
    - `06.3.4.1-RETRO-LENIENT.md` states whole-token 91/147 (inference 50/51, comparison 32/68, temporal 9/28), substring 106, `Answer:` lines 0/150, D-69 43/355 (null 29/42, non-null 14/313) with the `--retries 2` lower-bound caveat.
    - No `report.json` or `report.md` exists or changes under `eval/runs/2026-09-09-multihop_rag/`.
    - `uv run --project eval pytest -q` passes in full (no regression).
  </acceptance_criteria>
  <done>The format-vs-fix attribution baseline exists, is reproducible, and cannot be mistaken for a scored run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| question/map files → Rust probe | corpus JSON and titles become LanceDB predicate inputs |
| journal → harness | model-generated answer text is parsed by regex |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.4.1-01-01 | Tampering | `inspect_lancedb --gold-chunks` predicate build | medium | mitigate | Only map `document_id`s enter the `IN (...)` list, each UUIDv4-validated and `escape_sql_literal`-escaped; titles and facts are never interpolated into predicates |
| T-06.3.4.1-01-02 | Tampering | eval store | medium | mitigate | Mode opens via `DatabaseManager::open_and_validate` and issues only `query()`; a test asserts table versions are unchanged after the mode runs |
| T-06.3.4.1-01-03 | Repudiation | D-74 retro | high | mitigate | Script imports no scoring/report module (AST test), writes no report.json (test), and labels output `partial-run diagnostic, n=147`; the fail-closed path is untouched |
| T-06.3.4.1-01-04 | Denial of service | `_ANSWER_LINE` regex on model text | low | accept | Linear-time pattern over answers already capped at `MAX_ANSWER_CHARS = 16384` |
</threat_model>

<verification>
- `cargo test --manifest-path engine/Cargo.toml --locked --bin inspect_lancedb` and `sh scripts/engine-test-targets.sh` exit 0.
- `uv run --project eval pytest -q && uv run --project eval ruff check --preview eval/src eval/tests` exit 0.
- `git diff --quiet --exit-code -- eval/runs/2026-09-03-multihop_rag/ eval/runs/2026-09-09-multihop_rag/` exits 0.
</verification>

<success_criteria>
The required table can be regenerated by two commands on real data; columns (a), (b) and (e) are
computed by tested code; the D-74 retro reproduces 91/147 read-only.
</success_criteria>

## Artifacts this phase produces

- Rust: `InspectMode::GoldChunks`, CLI flags `--gold-chunks`, `--map`; a `normalize_ws` twin in the bin.
- Python module `lancet_eval.diagnostic`: `DiagnosticError`, `ArmResult`, `DiagnosticRow` (fields listed in Task 1), `build_rows`, `classify_record`, `write_table`, `main` (`python -m lancet_eval.diagnostic table`).
- Python `lancet_eval.metrics`: `_ANSWER_LINE`, `_MARKER`, `extract_final_answer`, `gold_contained`, `final_answer_em`, `answer_usable`, `null_abstention_correct`.
- Script `eval/scripts/retro_lenient.py` (`main`, `--out`).
- Files: `06.3.4.1-RETRO-LENIENT.md`, `diagnostic/pre-reconcile/{gold_chunks.jsonl,table.jsonl,table.md}`, test fixtures under `eval/tests/fixtures/diagnostic/`.

<output>
Create `.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-01-SUMMARY.md` when done.
</output>
