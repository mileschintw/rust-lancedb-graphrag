---
phase: "06.3.4.1"
slug: "retrieval-diagnosis-index-identity-and-graph-yield-repair"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: true
wave_0_complete: false
created: "2026-09-22"
---

# Phase 06.3.4.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from `06.3.4.1-RESEARCH.md` § Validation Architecture; filled 2026-09-23 from the committed
> plans 01–20 (54 tasks: 44 implementation tasks, each with `<automated>` + `<fails_when>`, and 10
> blocking `checkpoint:decision` tasks).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (eval) · cargo test (engine) · go test (gateway) |
| **Config file** | eval/pyproject.toml · engine/Cargo.toml · gateway/go.mod |
| **Quick run command** | `uv run --project eval pytest eval/tests/<touched_module>.py -x` / `cargo test --manifest-path engine/Cargo.toml --locked <filter>` |
| **Full suite command** | `uv run --project eval pytest && uv run --project eval ruff check --preview eval/src eval/tests && cargo test --manifest-path engine/Cargo.toml --locked && (cd gateway && go test ./...)` |
| **Estimated runtime** | ~300 seconds (full suite; per-module quick runs ~10–60s) |

---

## Sampling Rate

- **After every task commit:** Run that task's own `<automated>` command(s) from the Per-Task Verification Map (per-module form: `uv run --project eval pytest eval/tests/<touched_module>.py -x` / `cargo test --manifest-path engine/Cargo.toml --locked <filter>`, plus `sh scripts/engine-test-targets.sh` after any Rust test change)
- **After every plan wave:** Run `uv run --project eval pytest && uv run --project eval ruff check --preview eval/src eval/tests && cargo test --manifest-path engine/Cargo.toml --locked && (cd gateway && go test ./...)`
- **Before `/gsd-verify-work`:** Full suite must be green, plus `sh scripts/engine-test-targets.sh` with the raised counts
- **Paid stages:** each paid drive or pass is one invocation with `--workers 1 --retries 0 --no-resume` under the `--stage-cap` authorised at its checkpoint; the engine PID and start time are captured before and after; the journal is scored only through `score`/`report`
- **Max feedback latency:** 300 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 06.3.4.1-01-01 | 01 | 1 | OBS-05 (D-63, D-85) | T-06.3.4.1-01-01, T-06.3.4.1-01-02 | `--gold-chunks` opens the eval store read-only (a test asserts table versions unchanged); only UUIDv4-validated, `escape_sql_literal`-escaped map `document_id`s enter the `IN (...)` predicate, never titles or facts; (b) is a per-evidence-item tri-state on full `nodes.content`, never doc-level; a duplicate (question_id, arm) record raises `DiagnosticError` | tracer: unit (Rust + pytest) + integration (live eval store, read-only) | `cargo test --manifest-path engine/Cargo.toml --locked --bin inspect_lancedb gold_chunks`<br>`sh scripts/engine-test-targets.sh`<br>`uv run --project eval pytest eval/tests/test_diagnostic.py -x`<br>`OUT=.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/diagnostic/pre-reconcile && mkdir -p "$OUT" && cargo run --manifest-path engine/Cargo.toml --release --quiet --bin inspect_lancedb -- --gold-chunks eval/corpora/multihop_rag/questions.sample.jsonl --map eval/corpora/multihop_rag/document_map.json --lancedb-path ./data/lancedb-eval > "$OUT/gold_chunks.jsonl" && uv run --project eval python -m lancet_eval.diagnostic table --corpus multihop_rag --journal eval/runs/2026-09-09-multihop_rag/journal.jsonl --gold-chunks "$OUT/gold_chunks.jsonl" --out-dir "$OUT" --label "pre-reconcile" && EXPECTED=$(uv run --project eval python -c "import json; print(sum(1 for l in open('eval/corpora/multihop_rag/questions.sample.jsonl', encoding='utf-8') if l.strip() for e in json.loads(l).get('evidence_list', []) if e.get('fact')))") && test "$(wc -l < "$OUT/table.jsonl")" -eq 500 && test "$(wc -l < "$OUT/gold_chunks.jsonl")" -eq "$EXPECTED"` | ❌ W0 `eval/tests/test_diagnostic.py` + `fixtures/diagnostic/*` (created in this task); ✅ `engine/src/inspect_lancedb_tests.rs` | ⬜ pending |
| 06.3.4.1-01-02 | 01 | 1 | OBS-05 (D-69, D-70, D-72) | T-06.3.4.1-01-04 | Last `Answer:` line wins; a missing or empty line is a `final_answer_missing` miss kept in the denominator; whole-token `gold_contained` (`no` never matches `not`/`know`); nulls scored only by `null_abstention_correct`; harness deadline/read-timeout classed `timeout`, never `transport`; `em_f1`/`abstention_rate` byte-identical; linear regex over capped answers | unit (TDD) | `uv run --project eval pytest eval/tests/test_metrics.py eval/tests/test_diagnostic.py -x`<br>`uv run --project eval ruff check --preview eval/src/lancet_eval/metrics.py eval/src/lancet_eval/diagnostic.py` | ✅ `test_metrics.py` (new tests); `test_diagnostic.py` from 01-01 | ⬜ pending |
| 06.3.4.1-01-03 | 01 | 1 | OBS-05 (D-74) | T-06.3.4.1-01-03 | The retro is read-only: it imports no score/report module (AST test), writes no `report.json`, labels its output `partial-run diagnostic ... n=147`; tracked `eval/runs/` unchanged | unit + integration (committed journal) | `uv run --project eval pytest eval/tests/test_retro_lenient.py -x`<br>`uv run --project eval python eval/scripts/retro_lenient.py --out .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RETRO-LENIENT.md && grep -q "n=147" .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RETRO-LENIENT.md && git diff --quiet --exit-code -- eval/runs/` | ❌ W0 `eval/tests/test_retro_lenient.py` (created in this task) | ⬜ pending |
| 06.3.4.1-02-01 | 02 | 1 | OBS-05 (D-60, D-61) | T-06.3.4.1-02-01, T-06.3.4.1-02-04 | `delete_pg_extras` asserts `lancet_eval` schema isolation first, UUID-validates every ID before quoting, runs one transaction, and refuses apply without an existing non-empty pg_dump; dry-run executes no DELETE; psql output carries IDs and statuses only | unit (TDD, subprocess mocked) | `uv run --project eval pytest eval/tests/test_identity.py eval/tests/test_seed.py -x` | ❌ W0 `eval/tests/test_identity.py` (created in this task); ✅ `test_seed.py` | ⬜ pending |
| 06.3.4.1-02-02 | 02 | 1 | OBS-05 (D-61) | T-06.3.4.1-02-02 | The identity gate is structural: exactly one `require_index_identity` call at the top of `drive()` and of `run_measurement_pass()`, before any dispatch; no flag, keyword argument or env var skips it; tests patch only the I/O layer | unit (TDD) + full pytest | `uv run --project eval pytest eval/tests/test_preflight.py eval/tests/test_run_arms.py eval/tests/test_measure.py eval/tests/test_stage_cap.py eval/tests/test_journal_reconcile.py -x`<br>`uv run --project eval pytest -q` | ✅ (new tests in existing files; `identity.py` from 02-01) | ⬜ pending |
| 06.3.4.1-02-03 | 02 | 1 | OBS-05 (D-62, D-66, D-68) | T-06.3.4.1-02-03 | `index_generation` is written only by `record_index_generation` from a live probe; a missing value raises and leaves the map unchanged; `config.eval.toml` holds `lancedb_path` only (asserted) | unit (TDD) + config assertion | `uv run --project eval pytest eval/tests/test_seed.py eval/tests/test_corpus.py eval/tests/test_preflight.py eval/tests/test_score_deterministic.py -x`<br>`uv run --project eval python -c "import tomllib; d = tomllib.load(open('config/config.eval.toml', 'rb')); assert d == {'engine': {'lancedb_path': './data/lancedb-eval'}}, d"` | ✅ | ⬜ pending |
| 06.3.4.1-03-01 | 03 | 1 | OBS-05 (D-64, D-65) | T-06.3.4.1-03-01, T-06.3.4.1-03-02, T-06.3.4.1-03-03 | `retrieval_soak` opens the store via `open_and_validate` and only queries (no add/delete/optimize); embedding and generation ports are in-process stand-ins, so no provider call; P1 tracing logs durations and counters only (no query text, chunk content or key) | integration (release bin, 5-iteration smoke on the eval store) + full cargo test + count gate | `cargo build --manifest-path engine/Cargo.toml --release --bin retrieval_soak && cargo run --manifest-path engine/Cargo.toml --release --quiet --bin retrieval_soak -- --arm R --iterations 5 --lancedb-path ./data/lancedb-eval \| tail -n 5 \| uv run --project eval python -c "import sys, json; rows = [json.loads(l) for l in sys.stdin if l.strip()]; assert len(rows) == 5 and all('retrieve_ms' in r and 'alive_tasks' in r for r in rows), rows"`<br>`cargo test --manifest-path engine/Cargo.toml --locked && sh scripts/engine-test-targets.sh` | ❌ W0 `engine/src/bin/retrieval_soak.rs` (created in this task); ✅ engine suite | ⬜ pending |
| 06.3.4.1-03-02 | 03 | 1 | OBS-05 (D-64, D-65) | T-06.3.4.1-03-04 | The flatness adapter is a thin shim onto the unmodified `analyze_decay`; a censored set reads `unavailable`, never `not present`; at least 300 ordinals per soak arm; the profile names the accumulator class | unit (TDD) + record | `uv run --project eval pytest eval/tests/test_flatness.py eval/tests/test_decay.py -x`<br>`for a in R RP RG RGT W; do test "$(grep -c '"ordinal"' .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/soak/$a.jsonl)" -ge 300 \|\| exit 1; done && grep -q "Named accumulator" .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-OI02-PROFILE.md` | ❌ W0 `eval/tests/test_flatness.py` (created in this task); ✅ `test_decay.py` | ⬜ pending |
| 06.3.4.1-03-03 | 03 | 1 | OBS-05 (D-64) | — | Human picks the OI-02 fix direction (named class, extended profiling, or mimalloc on allocator evidence only); a restart workaround or raised budget is not an option | manual (checkpoint:decision) | manual — blocking `checkpoint:decision` (no `<automated>`; the chosen option is recorded in the plan SUMMARY) | n/a | ⬜ pending |
| 06.3.4.1-04-01 | 04 | 2 | OBS-05 (D-58, D-61) | — | Read-only full scan of the `document_id` column of documents/nodes/edges/entity_edges plus the `staged_documents_v2` row count; exact five-key JSON contract consumed by `identity.py`; an empty store yields empty lists and 0 | unit (TDD, synthetic temp store) | `cargo test --manifest-path engine/Cargo.toml --locked --bin inspect_lancedb document_ids`<br>`sh scripts/engine-test-targets.sh` | ✅ `inspect_lancedb_tests.rs` (new tests) | ⬜ pending |
| 06.3.4.1-04-02 | 04 | 2 | DATA-04 (D-58, D-60) | T-06.3.4.1-04-01, T-06.3.4.1-04-02, T-06.3.4.1-04-03, T-06.3.4.1-04-04 | Dry-run by default; refuses any path but the configured eval `lancedb_path` (and the dev path); `--apply` requires an existing `--snapshot-dir` containing `nodes.lance`, with no optimize/cleanup; UUIDv4-validated escaped predicates in 500-ID chunks; the allow-list prune keeps every entity with a surviving source chunk and preserves every other column byte-for-byte | unit (TDD, synthetic temp store) | `cargo test --manifest-path engine/Cargo.toml --locked --bin reconcile_eval_store`<br>`sh scripts/engine-test-targets.sh` | ❌ W0 `engine/src/bin/reconcile_eval_store.rs` + `engine/src/reconcile_eval_store_tests.rs` (created in this task) | ⬜ pending |
| 06.3.4.1-04-03 | 04 | 2 | OBS-05, DATA-04 (D-58, D-62) | T-06.3.4.1-04-03 | The live census is read-only (dry-run table versions before == after, asserted); PG extras listed with filename, status and created_at; the record names the rollback layers before any approval | record (live store, read-only) | `uv run --project eval python -c "import json; d = json.load(open('.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/reconcile/dryrun.json')); assert d['mode'] == 'dry_run' and isinstance(d['extras'], list) and 'entities' in d and 'versions_before' in d, d.keys(); assert d['versions_before'] == d['versions_after'], 'dry run changed a table version'"`<br>`test -s .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/reconcile/pg_extras.json && grep -q "Rollback layers" .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RECONCILE-DRYRUN.md` | n/a (records produced by this task) | ⬜ pending |
| 06.3.4.1-05-01 | 05 | 2 | OBS-05 (D-70, D-72) | T-06.3.4.1-05-01 | `score_run` guards and `effective_partial` untouched; the five answer dimensions are added to score, report and schema beside the unchanged legacy metrics; null records never enter `answer_usable`'s denominator | unit | `uv run --project eval pytest eval/tests/test_score_calibration_agreement.py eval/tests/test_score_judged.py eval/tests/test_schema.py eval/tests/test_report.py eval/tests/test_dimensions.py eval/tests/test_score_deterministic.py -x` | ✅ (new tests in existing files) | ⬜ pending |
| 06.3.4.1-05-02 | 05 | 2 | OBS-05 (D-69, D-73, D-84) | T-06.3.4.1-05-02, T-06.3.4.1-05-03 | Drive-1 literals are committed with provenance before any drive-1 record; an absent SC-3 floor reads MISS, never a default; an empty population reads MISS `n=0`; harness deadlines are classed `timeout` and a plurality tie counts as dominant, all pinned by tests | unit (TDD) + full pytest + ruff | `uv run --project eval pytest eval/tests/test_unpark_gates.py eval/tests/test_gate.py eval/tests/test_stage_cap.py -x`<br>`uv run --project eval pytest -q && uv run --project eval ruff check --preview eval/src eval/tests` | ❌ W0 `eval/tests/test_unpark_gates.py` (created in this task); ✅ `test_gate.py`, `test_stage_cap.py` | ⬜ pending |
| 06.3.4.1-06-01 | 06 | 3 | OBS-05, DATA-04 (D-58, D-60) | T-06.3.4.1-06-01 | Human confirms the committed dry run (LanceDB extras, PG extra rows, per-table deletes, entity rewrites, seven table versions) and the rollback layers before the one-way apply, and authorises the single generation-probe query | manual (checkpoint:decision) | manual — blocking `checkpoint:decision` (no `<automated>`; the chosen option is recorded in the plan SUMMARY) | n/a | ⬜ pending |
| 06.3.4.1-06-02 | 06 | 3 | OBS-05, DATA-04 (D-58, D-61, D-62) | T-06.3.4.1-06-01, T-06.3.4.1-06-02, T-06.3.4.1-06-03, T-06.3.4.1-06-04 | Snapshot and pg_dump recorded before apply; apply output compared to the dry run and halts on difference; `staged_documents_v2_rows == 0` before engine start; backups stay under gitignored `data/`; the identity gate passes on a new probe-recorded generation | integration (live store + PostgreSQL, one-way) | `uv run --project eval lancet-eval identity check --corpus multihop_rag`<br>`uv run --project eval python -c "import json; m = json.load(open('eval/corpora/multihop_rag/document_map.json')); assert m['index_generation'] != 'lance-701' and m['index_generation'].startswith('lance-'), m['index_generation']; assert '0370301a-0ad7-4267-a337-ba2dda34595f' in m['aliases'] and len(m['entries']) == 346"` | n/a (live check; identity CLI from 02-01) | ⬜ pending |
| 06.3.4.1-06-03 | 06 | 3 | OBS-05, DATA-04 (D-62, D-77) | — | The post-reconcile baseline records `index_generation`, graph population and the degree distribution (p99) that D-77 derives from | record | `grep -q "p99" .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-STORE-BASELINE.md && grep -q "index_generation" .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-STORE-BASELINE.md && uv run --project eval python -c "import json; d = json.load(open('.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/reconcile/graph_population_post.json')); assert d, 'empty graph population'"` | n/a (record produced by this task) | ⬜ pending |
| 06.3.4.1-07-01 | 07 | 4 | OBS-05 (D-64, D-65) | T-06.3.4.1-07-01, T-06.3.4.1-07-02, T-06.3.4.1-07-03 | The named accumulator is bounded at its source and pinned by a failing-first regression test; any cached handle is per-`CorpusSnapshot` at its pinned version; mimalloc only on explicit checkpoint approval | unit (TDD) + count gate | `cargo test --manifest-path engine/Cargo.toml --locked && sh scripts/engine-test-targets.sh` | ✅ engine suite (new tests in `engine/src/tests.rs`) | ⬜ pending |
| 06.3.4.1-07-02 | 07 | 4 | OBS-05 (D-64, D-65) | T-06.3.4.1-07-01 | The post-fix soak on the reproducing arm and arm W reads flat by the unmodified decay verdict (every file must pass) | record (offline soak, no provider) | `for f in .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/soak/post-fix/*.jsonl; do uv run --project eval python -c "import sys; from pathlib import Path; from lancet_eval.flatness import records_from_soak_jsonl, flatness_verdict; p = Path(sys.argv[1]); r = flatness_verdict(records_from_soak_jsonl(p, p.stem)); print(p.stem, r); sys.exit(0 if r.passed else 1)" "$f" \|\| exit 1; done` | n/a (soak output produced by this task; `flatness.py` from 03-02) | ⬜ pending |
| 06.3.4.1-08-01 | 08 | 5 | OBS-05 (D-69, D-71) | T-06.3.4.1-08-01, T-06.3.4.1-08-02, T-06.3.4.1-08-03 | The D-71 instruction is additive: every existing guard sentence asserted verbatim, `detect_suspicious_text` untouched; the validator is unchanged, an uncited `Answer:` line is neutral and an unresolvable marker is dropped, never accepted | unit (TDD, Rust) | `cargo test --manifest-path engine/Cargo.toml --locked prompt::`<br>`cargo test --manifest-path engine/Cargo.toml --locked generation::`<br>`sh scripts/engine-test-targets.sh` | ✅ `generation/tests.rs`, `prompt.rs` tests (new cases) | ⬜ pending |
| 06.3.4.1-08-02 | 08 | 5 | OBS-05 | — | Prompt packing uses the cl100k singleton with unchanged token counts (tested) | unit (TDD, Rust) | `cargo test --manifest-path engine/Cargo.toml --locked prompt:: && sh scripts/engine-test-targets.sh` | ✅ | ⬜ pending |
| 06.3.4.1-09-01 | 09 | 5 | OBS-05 (D-63, D-82) | T-06.3.4.1-09-01, T-06.3.4.1-09-02, T-06.3.4.1-09-03 | `diag_probe` is query-only on the eval store; `--stage-cap` is required and checked before each batch; the API key is read from env and never printed; the cache stays under gitignored `data/`; the (c) probe is built and costed but not run before authorisation | build + unit + record | `cargo build --manifest-path engine/Cargo.toml --release --bin diag_probe && sh scripts/engine-test-targets.sh`<br>`uv run --project eval pytest eval/tests/test_diagnostic.py -x`<br>`test -s .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/diagnostic/post-reconcile/gold_chunks.jsonl && test -s .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/diagnostic/post-reconcile/gold_coverage.jsonl && test -f .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-GOLD-COVERAGE.md` | ❌ W0 `engine/src/bin/diag_probe.rs` (created in this task); ✅ `test_diagnostic.py` (from 01-01) | ⬜ pending |
| 06.3.4.1-09-02 | 09 | 5 | OBS-05 (D-59, D-72, D-86) | T-06.3.4.1-09-01 | Human authorises the (c) probe cap, the D-59 disposition and its cap, and null inclusion before any spend or sample commit | manual (checkpoint:decision) | manual — blocking `checkpoint:decision` (no `<automated>`; the chosen option is recorded in the plan SUMMARY) | n/a | ⬜ pending |
| 06.3.4.1-09-03 | 09 | 5 | OBS-05 (D-59, D-63, D-82) | T-06.3.4.1-09-01 | The (c) probe runs under the authorised cap and completes (`stopped_by_cap` false) over all 500 questions at the map's current `index_generation` | record (paid probe, capped) | `uv run --project eval python -c "import json; rows = [json.loads(l) for l in open('.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/diagnostic/post-reconcile/vector_top4.jsonl')]; s = rows[-1]; m = json.load(open('eval/corpora/multihop_rag/document_map.json')); assert s.get('stopped_by_cap') is False, s; assert all(r['index_generation'] == m['index_generation'] for r in rows[:-1]); assert len(rows) - 1 == 500"` | n/a (record produced by this task) | ⬜ pending |
| 06.3.4.1-10-01 | 10 | 6 | DATA-03 (D-59) | T-06.3.4.1-10-01, T-06.3.4.1-10-02, T-06.3.4.1-10-04 | Targeted re-ingest runs only as authorised, under its own cap (allowance read between documents), with a snapshot before each delete and eval-path/schema isolation guards; refuses a document whose facts are no longer `absent`; identity re-checked after; DATA-03 marked only on a confirmed re-probe | unit + live identity check | `uv run --project eval pytest eval/tests/test_seed.py -x && uv run --project eval lancet-eval identity check --corpus multihop_rag` | ✅ `test_seed.py` (new tests) | ⬜ pending |
| 06.3.4.1-10-02 | 10 | 6 | OBS-05 (D-68, D-72, D-82) | T-06.3.4.1-10-03 | G, V and the diagnostic sample come from a deterministic script (seed 42, byte-identical re-run), committed with method, quotas and IDs; the drive-1 journal path is not gitignored | unit (TDD) | `uv run --project eval pytest eval/tests/test_select_diag_sample.py eval/tests/test_corpus.py eval/tests/test_journal_reconcile.py -x`<br>`git check-ignore -q eval/runs/2026-10-01-drive1-multihop_rag_diag/journal.jsonl; test $? -eq 1` | ❌ W0 `eval/tests/test_select_diag_sample.py` (created in this task); ✅ `test_corpus.py`, `test_journal_reconcile.py` | ⬜ pending |
| 06.3.4.1-10-03 | 10 | 6 | OBS-05 (D-73, D-84) | T-06.3.4.1-10-03 | The SC-3 floor is a literal derived from gold labels only, committed before any drive-1 record exists and pinned by tests | unit (TDD) | `uv run --project eval pytest eval/tests/test_gate.py eval/tests/test_unpark_gates.py -x` | ✅ `test_gate.py`; `test_unpark_gates.py` from 05-02 | ⬜ pending |
| 06.3.4.1-11-01 | 11 | 7 | OBS-05 (D-66, D-86) | T-06.3.4.1-11-01 | Human authorises pass A's `--stage-cap` and the raised-ceiling settings (never the $5 default) | manual (checkpoint:decision) | manual — blocking `checkpoint:decision` (no `<automated>`; the chosen option is recorded in the plan SUMMARY) | n/a | ⬜ pending |
| 06.3.4.1-11-02 | 11 | 7 | OBS-05 (D-64, D-66, D-86) | T-06.3.4.1-11-01, T-06.3.4.1-11-03 | Pass A runs under the authorised cap; budgets are derived by the committed rule with a per-node p95 trail, and only when the decay verdict is available and not present | record (paid pass, capped) | `RUN=$(ls -d eval/runs/*-passA-measure-multihop_rag \| tail -n 1) && uv run --project eval python -c "import sys, json, pathlib; p = pathlib.Path(sys.argv[1]); m = json.loads((p / 'measurement.json').read_text()); print(sorted(m.keys()))" "$RUN" && grep -q "Finding: 33e774b overwrite" .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-BUDGETS.md` | n/a (record produced by this task) | ⬜ pending |
| 06.3.4.1-11-03 | 11 | 7 | OBS-05 (D-66) | T-06.3.4.1-11-02 | Derived budgets are identical across `config.toml`, `config.example.toml` and `config.rs` defaults (agreement test); env overrides are unset and verified absent before restart | unit (TDD, Rust + pytest) | `cargo test --manifest-path engine/Cargo.toml --locked config && sh scripts/engine-test-targets.sh`<br>`uv run --project eval pytest eval/tests/test_preflight.py -k effective -x` | ✅ | ⬜ pending |
| 06.3.4.1-12-01 | 12 | 8 | OBS-05 (D-69, D-73, D-86) | T-06.3.4.1-12-01, T-06.3.4.1-12-04 | Human authorises drive 1's cap and confirms the committed SC-2 reading, D-69 tripwire and SC-3 floor before any record exists | manual (checkpoint:decision) | manual — blocking `checkpoint:decision` (no `<automated>`; the chosen option is recorded in the plan SUMMARY) | n/a | ⬜ pending |
| 06.3.4.1-12-02 | 12 | 8 | OBS-05 (D-64, D-69, D-87a) | T-06.3.4.1-12-01, T-06.3.4.1-12-02, T-06.3.4.1-12-03, T-06.3.4.1-12-04 | Drive 1 is scored only through the unmodified fail-closed score/report path; a halted drive stays `partial: true`; SC-2/SC-3 are computed by `unpark_gates` against pre-committed literals; earlier run directories unchanged | record (paid drive, capped) | `RUN=$(ls -d eval/runs/*-drive1-multihop_rag_diag \| tail -n 1) && test -s "$RUN/journal.jsonl" && test -s "$RUN/gates-drive1.md" && test -s "$RUN/diagnostic/table.md" && grep -qE "SC-2.*(PASS\|MISS)" "$RUN/gates-drive1.md" && grep -qE "SC-3.*(PASS\|MISS)" "$RUN/gates-drive1.md"`<br>`git diff --quiet --exit-code -- eval/runs/2026-09-03-multihop_rag/ eval/runs/2026-09-09-multihop_rag/` | n/a (run produced by this task) | ⬜ pending |
| 06.3.4.1-12-03 | 12 | 8 | OBS-05 (D-83, D-84) | T-06.3.4.1-12-04 | Human reads the computed drive-1 gates; SC-3 is a hard gate before OI-01; a miss allows one table-justified iteration, never a restart, budget raise or retries | manual (checkpoint:decision) | manual — blocking `checkpoint:decision` (no `<automated>`; the chosen option is recorded in the plan SUMMARY) | n/a | ⬜ pending |
| 06.3.4.1-13-01 | 13 | 9 | DATA-05 (D-75, D-76, D-77) | T-06.3.4.1-13-01, T-06.3.4.1-13-02, T-06.3.4.1-13-03 | Mentions are matched only in in-memory maps and by vector (no Cypher, no question text in predicates; UUID-validated escaped entity IDs); seeds capped, intermediates above `DEGREE_CAP` excluded, facts truncated at `MAX_PATH_FACTS`; `GraphIndex` immutable per generation | unit (TDD, Rust) | `cargo test --manifest-path engine/Cargo.toml --locked graph::`<br>`sh scripts/engine-test-targets.sh` | ❌ W0 `engine/src/graph/{index,seeding,paths}.rs` (created in this task); ✅ `engine/src/graph/tests.rs` | ⬜ pending |
| 06.3.4.1-13-02 | 13 | 9 | OBS-05, DATA-05 (D-77, D-79) | T-06.3.4.1-13-04 | The offline seed/path probe runs under the cap authorised at 12-03 and completes without a cap stop; caps are derived from measured data | build + unit + record | `cargo build --manifest-path engine/Cargo.toml --release --bin diag_probe && uv run --project eval pytest eval/tests/test_diagnostic.py -x`<br>`uv run --project eval python -c "import json; rows = [json.loads(l) for l in open('.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/diagnostic/seed_probe.jsonl')]; q = [r for r in rows if 'question_id' in r]; assert q and all('seed_count' in r and 'path_found' in r for r in q); assert rows[-1].get('stopped_by_cap') is False, rows[-1]"` | ✅ `test_diagnostic.py` (01-01), `diag_probe.rs` (09-01) | ⬜ pending |
| 06.3.4.1-13-03 | 13 | 9 | DATA-05 (D-76, D-78) | — | Human decides the seed-chunk boost with the probe's numbers before fusion code is written; the chosen option ships as the production default with no flag | manual (checkpoint:decision) | manual — blocking `checkpoint:decision` (no `<automated>`; the chosen option is recorded in the plan SUMMARY) | n/a | ⬜ pending |
| 06.3.4.1-14-01 | 14 | 10 | DATA-05 (D-75, D-78) | T-06.3.4.1-14-03 | `CorpusSnapshot` carries an immutable `Arc<GraphIndex>` built at startup and at rebuild and swapped with BM25 inside a new snapshot; old-snapshot stability pinned by tests | unit (full cargo test) + count gate | `cargo test --manifest-path engine/Cargo.toml --locked && sh scripts/engine-test-targets.sh` | ✅ | ⬜ pending |
| 06.3.4.1-14-02 | 14 | 10 | DATA-05 (D-66, D-77) | T-06.3.4.1-14-02 | Seed, degree and fact caps are validated non-zero and identical across `config.rs`, `config.toml` and `config.example.toml` | unit (TDD, Rust) | `cargo test --manifest-path engine/Cargo.toml --locked config && sh scripts/engine-test-targets.sh` | ✅ | ⬜ pending |
| 06.3.4.1-14-03 | 14 | 10 | DATA-05 (D-75, D-76, D-78) | T-06.3.4.1-14-01, T-06.3.4.1-14-02, T-06.3.4.1-14-04 | Graph-on queries seed only from question mentions through in-memory maps and vector search; the old seeding (`limit(1)`) is removed (count asserted 0); the graph timeout stays non-fatal; spans carry counts and a boolean only | unit (TDD) + gateway `go test` + grep | `cargo test --manifest-path engine/Cargo.toml --locked && sh scripts/engine-test-targets.sh && (cd gateway && go test ./...)`<br>`test "$(grep -c 'limit(1)' engine/src/service.rs)" -eq 0` | ✅ | ⬜ pending |
| 06.3.4.1-15-01 | 15 | 11 | DATA-05 (D-76, D-78) | T-06.3.4.1-15-03 | The graph list enters RRF once with its own weight; empty-list fusion is byte-identical to before (graph-off invariance) | unit (TDD, Rust) | `cargo test --manifest-path engine/Cargo.toml --locked retrieval:: && cargo test --manifest-path engine/Cargo.toml --locked config && sh scripts/engine-test-targets.sh` | ✅ `retrieval/tests.rs` (new tests) | ⬜ pending |
| 06.3.4.1-15-02 | 15 | 11 | DATA-05 (D-76, D-81) | T-06.3.4.1-15-01, T-06.3.4.1-15-02, T-06.3.4.1-15-04 | Graph candidate chunk IDs are validated as `<uuidv4>:<int>` and escaped before the IN list, fetched at the snapshot's pinned `nodes_version`, capped at `max_graph_chunk_candidates`; every boosted chunk is flagged | unit (TDD, Rust) | `cargo test --manifest-path engine/Cargo.toml --locked && sh scripts/engine-test-targets.sh` | ✅ | ⬜ pending |
| 06.3.4.1-16-01 | 16 | 12 | OBS-05, DATA-05 (D-79, D-81) | T-06.3.4.1-16-01, T-06.3.4.1-16-02 | The proto change is additive (next free tags, nothing renumbered) and both generated trees are regenerated; the wire carries counts, booleans and document UUIDs only | unit (Rust) + grep on generated bindings | `cargo test --manifest-path engine/Cargo.toml --locked && sh scripts/engine-test-targets.sh`<br>`grep -q "GraphSeedCount" gateway/proto/lancet/v1/lancet.pb.go && grep -q "graph_boosted" engine/src/pb/lancet/v1/lancet.v1.rs` | ✅ | ⬜ pending |
| 06.3.4.1-16-02 | 16 | 12 | OBS-05 (D-79, D-81) | T-06.3.4.1-16-03 | The gateway SSE maps every new field explicitly in both the populated and the nil-metadata maps, with a Go test per key | unit (TDD, Go) + vet | `cd gateway && go test ./internal/sse/... && go vet ./internal/sse/...`<br>`cd gateway && go test ./...` | ✅ `gateway/internal/sse/sse_test.go` (new tests) | ⬜ pending |
| 06.3.4.1-16-03 | 16 | 12 | OBS-05 (D-79, D-81) | T-06.3.4.1-16-02 | The harness journals every new field; older journals without the fields still load (`extra="forbid"` models with `None` defaults) | unit (TDD) | `uv run --project eval pytest eval/tests/test_wire_capture.py eval/tests/test_client_contract.py eval/tests/test_journal.py -x`<br>`uv run --project eval pytest -q` | ✅ `test_wire_capture.py`; ❌ W0 `eval/tests/fixtures/sse/graph_seed_fields.txt` (created in this task) | ⬜ pending |
| 06.3.4.1-17-01 | 17 | 13 | OBS-05, DATA-05 (D-81, D-82, D-83) | T-06.3.4.1-17-01, T-06.3.4.1-17-02 | Drive-2 literals are committed with provenance before drive 2; the 0.20 floor is untouched; only `graph_boosted` chunks count as a composition change (a plain set difference does not); SC-4/SC-5 are computed | unit (TDD) | `uv run --project eval pytest eval/tests/test_unpark_gates.py eval/tests/test_gate.py eval/tests/test_diagnostic.py eval/tests/test_pairing.py -x` | ✅ `test_gate.py`, `test_pairing.py`; `test_unpark_gates.py` (05-02), `test_diagnostic.py` (01-01) | ⬜ pending |
| 06.3.4.1-17-02 | 17 | 13 | DATA-05 (D-80) | T-06.3.4.1-17-03 | The path canary is a hard preflight floor (`require_seed_path`); canary text is verified verbatim against `questions.sample.jsonl`; manifest counts pinned (8 rows, 7 question IDs) | unit (TDD) + manifest check | `uv run --project eval pytest eval/tests/test_preflight_canary.py eval/tests/test_preflight.py -x`<br>`uv run --project eval python -c "import json; rows = [json.loads(l) for l in open('eval/corpora/multihop_rag/canary.jsonl', encoding='utf-8') if l.strip()]; assert len(rows) == 8 and len({r['question_id'] for r in rows}) == 7 and sum(1 for r in rows if r.get('require_seed_path')) >= 1, len(rows)"` | ✅ `test_preflight_canary.py`, `canary.jsonl` | ⬜ pending |
| 06.3.4.1-18-01 | 18 | 14 | OBS-05 (D-86) | T-06.3.4.1-18-01 | Human authorises pass B's cap, raised ceilings and the canary probes | manual (checkpoint:decision) | manual — blocking `checkpoint:decision` (no `<automated>`; the chosen option is recorded in the plan SUMMARY) | n/a | ⬜ pending |
| 06.3.4.1-18-02 | 18 | 14 | OBS-05, DATA-05 (D-64, D-77) | T-06.3.4.1-18-01, T-06.3.4.1-18-02 | The graph budget is re-derived from measured p95 on the new path under the authorised cap; nesting checked; the `GRAPH_TIMEOUT` rate is reported | record (paid pass, capped) | `grep -q "Pass B (D-77)" .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-BUDGETS.md && ls -d eval/runs/*-passB-measure-multihop_rag` | n/a (record produced by this task) | ⬜ pending |
| 06.3.4.1-18-03 | 18 | 14 | OBS-05, DATA-05 (D-66, D-80) | T-06.3.4.1-18-03 | The graph budget is identical across the config trio (agreement test); env overrides are unset before restart; all 8 canaries pass live and no floor is relaxed | unit (Rust) + live preflight | `cargo test --manifest-path engine/Cargo.toml --locked config && sh scripts/engine-test-targets.sh`<br>`uv run --project eval lancet-eval preflight --corpus multihop_rag_diag` | ✅ | ⬜ pending |
| 06.3.4.1-19-01 | 19 | 15 | OBS-05 (D-86) | T-06.3.4.1-19-01 | Human authorises drive 2's cap (the run of record) | manual (checkpoint:decision) | manual — blocking `checkpoint:decision` (no `<automated>`; the chosen option is recorded in the plan SUMMARY) | n/a | ⬜ pending |
| 06.3.4.1-19-02 | 19 | 15 | OBS-05, DATA-05 (D-82, D-87, D-87a) | T-06.3.4.1-19-01, T-06.3.4.1-19-02, T-06.3.4.1-19-03, T-06.3.4.1-19-04 | The run of record is scored fail-closed through the unmodified path; a halted run stays partial and is not a run of record; SC-1/SC-4/SC-5 are computed against pre-committed literals; earlier runs unchanged | record (paid drive, capped) | `RUN=$(ls -d eval/runs/*-drive2-multihop_rag_diag \| tail -n 1) && test -s "$RUN/journal.jsonl" && test -s "$RUN/gates-drive2.md" && test -s "$RUN/diagnostic/table.md" && grep -qE "SC-1.*(PASS\|MISS)" "$RUN/gates-drive2.md" && grep -qE "SC-4.*(PASS\|MISS)" "$RUN/gates-drive2.md" && grep -qE "SC-5.*(PASS\|MISS)" "$RUN/gates-drive2.md"`<br>`git diff --quiet --exit-code -- eval/runs/2026-09-03-multihop_rag/ eval/runs/2026-09-09-multihop_rag/` | n/a (run produced by this task) | ⬜ pending |
| 06.3.4.1-19-03 | 19 | 15 | OBS-05, DATA-05 (D-83, D-88) | T-06.3.4.1-19-03 | Human reads the computed drive-2 gates; at most one table-justified iteration; the 0.20 floor is not amended without an explicit dated authorisation; a negative result is reported, not toggled off | manual (checkpoint:decision) | manual — blocking `checkpoint:decision` (no `<automated>`; the chosen option is recorded in the plan SUMMARY) | n/a | ⬜ pending |
| 06.3.4.1-20-01 | 20 | 16 | OBS-05 (D-71, D-88) | T-06.3.4.1-20-01, T-06.3.4.1-20-02 | The SUPERSEDED notice and dated addendum state non-comparability; only a new file is added under the 06.3.4 run (`git diff --quiet` guard on existing files) | record (grep + git diff) | `test -f eval/runs/2026-09-09-multihop_rag/SUPERSEDED.md && grep -q "drive2-multihop_rag_diag" eval/runs/2026-09-09-multihop_rag/SUPERSEDED.md && grep -q "Addendum .*06.3.4.1" docs/evaluation-fidelity-and-graph-yield.md && git diff --quiet --exit-code -- eval/runs/2026-09-09-multihop_rag/journal.jsonl eval/runs/2026-09-09-multihop_rag/STAGED-GATE.md eval/runs/2026-09-03-multihop_rag/` | n/a (records produced by this task) | ⬜ pending |
| 06.3.4.1-20-02 | 20 | 16 | OBS-05 (D-57) | T-06.3.4.1-20-03 | Traceability is updated without changing any requirement checkbox (OBS-05, DATA-04, DATA-05 stay checked) | record (grep) | `grep -q "06.3.4.1-RUN-OF-RECORD.md" .planning/ROADMAP.md && grep -n "OBS-05 \|" .planning/REQUIREMENTS.md \| grep -q "06.3.4.1" && grep -q "\- \[x\] \*\*OBS-05\*\*" .planning/REQUIREMENTS.md && grep -q "\- \[x\] \*\*DATA-04\*\*" .planning/REQUIREMENTS.md && grep -q "\- \[x\] \*\*DATA-05\*\*" .planning/REQUIREMENTS.md` | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Task IDs are `06.3.4.1-<plan>-<task>`, numbered by task order inside each committed plan. The Requirement
column names only IDs from that plan's `requirements` list (DATA-03 appears only in plan 10); decision
IDs follow the phase rule (bare D-NN >= 55 is 06.3.4.1-CONTEXT, <= 54 is 06.3.1-CONTEXT). Where a task
has several `<automated>` commands they are all listed, separated by line breaks; `\|` in a cell is a
literal pipe in the command. Every listed command has a `<fails_when>` sibling in its plan.*

*Rows marked `record`, `integration` or `live` depend on the eval environment rather than on unit
fixtures: Docker Desktop (compose `db`) for PostgreSQL steps, the release engine and gateway started with
`LANCET_ENV=eval` for probes and drives, and network access for `buf generate` (16-01) and for any paid
stage. They check the artifact the task produced, so they are one-shot per task, not part of the per-commit
sampling loop.*

---

## Wave 0 Requirements

This phase has no separate Wave 0 plan. Each new test file, fixture and test-bearing bin below is
created test-first inside the task that first runs it (the 01-01 tracer or a `tdd="true"` task), and
no task's `<automated>` is `MISSING`. Every later consumer runs in a later wave than the creating task,
or later in the same plan.

- [ ] `eval/tests/test_diagnostic.py`, with fixtures `eval/tests/fixtures/diagnostic/gold_chunks.jsonl` and `eval/tests/fixtures/diagnostic/journal_slice.jsonl`: the required table builder, the (b) tri-state, the duplicate-record raise, and error classes including harness timeouts. Created in 01-01 (wave 1); extended in 01-02, 09-01 (wave 5), 13-02 (wave 9) and 17-01 (wave 13).
- [ ] `eval/tests/test_retro_lenient.py`: the D-74 retro (91/147), its label, no `report.json`, and the AST import guard. Created in 01-03 (wave 1).
- [ ] `eval/tests/test_identity.py`: the three-way identity comparison and the `delete_pg_extras` guards. Created in 02-01 (wave 1); the identity module is consumed by 02-02 in the same plan.
- [ ] `engine/src/bin/retrieval_soak.rs`: the read-only, API-free soak bin, smoke-run by its own verify. Created in 03-01 (wave 1).
- [ ] `eval/tests/test_flatness.py`: the soak-to-decay adapter, where a censored set reads unavailable. Created in 03-02 (wave 1). `flatness.py` is consumed by 07-02 (wave 4).
- [ ] `engine/src/bin/reconcile_eval_store.rs` and `engine/src/reconcile_eval_store_tests.rs`: the cascade reconcile on a synthetic temp store. Created in 04-02 (wave 2); used live by 04-03, 06-02 (wave 3) and 10-01 (wave 6).
- [ ] `eval/tests/test_unpark_gates.py`: the SC-1/SC-2/SC-3 evaluators and the committed literals. Created in 05-02 (wave 2); extended in 10-03 (wave 6) and 17-01 (wave 13).
- [ ] `engine/src/bin/diag_probe.rs`: the (c) vector probe. Created in 09-01 (wave 5); extended with the seed/path probe in 13-02 (wave 9).
- [ ] `eval/tests/test_select_diag_sample.py`: the deterministic G/V and diagnostic-sample draw. Created in 10-02 (wave 6).
- [ ] `engine/src/graph/index.rs`, `engine/src/graph/seeding.rs`, `engine/src/graph/paths.rs`: mention seeding and bounded seed-to-seed paths, tested in the existing `engine/src/graph/tests.rs`. Created in 13-01 (wave 9); consumed by 14-01 to 14-03 (wave 10).
- [ ] `eval/tests/fixtures/sse/graph_seed_fields.txt`: an SSE fixture carrying the new wire fields. Created in 16-03 (wave 12).
- [ ] `scripts/engine-test-targets.sh`: expected per-target counts and `TOTAL`, raised in the same commit as every Rust test addition and never lowered. Every task whose `<files>` lists it runs it in its `<automated>`.
- Framework install: none. pytest (`eval/pyproject.toml`), `cargo test` and `go test` are already configured, and every other test file named in the map already exists; those tasks add new cases to it.

`wave_0_complete` stays `false` until execution has created the files above.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| OI-02 fix direction (task 03-03) | OBS-05 (D-64) | Judging which accumulator class the profile evidence names is an engineering call, and mimalloc would add a new crate against the AI-SPEC §2 lock | Present the per-arm soak table and the "Named accumulator" section of `06.3.4.1-OI02-PROFILE.md`. The user replies `fix-named-class` (with the class), `extend-profiling` (with the arm or counter) or `mimalloc`. Record the choice in the 03 SUMMARY. A restart or budget raise is not an option. |
| One-way reconcile apply (task 06-01) | OBS-05, DATA-04 (D-58, D-60) | Deleting store and PostgreSQL rows is one-way without the snapshot layers, so the exact change needs human confirmation | Present `06.3.4.1-RECONCILE-DRYRUN.md`: LanceDB extras against RESEARCH A1's four, the PostgreSQL extra rows with filename, status and created_at, per-table deletes, entity rewrites, orphans and the seven table versions. The user replies `apply-as-recorded` or `hold-investigate`. Nothing is applied before that reply. |
| (c) probe cap, D-59 disposition, null inclusion (task 09-02) | OBS-05 (D-59, D-72, D-86) | Spend authorisation and sample scope are user decisions (D-86; RESEARCH Open Question 2) | Present the post-reconcile (b) counts, the absent-fact and split-fact documents with `rechunk_check`, and the (c) cost projection. The user authorises the probe cap, the D-59 re-ingest disposition and its cap, and include or exclude 10 null questions. 10-01 and 10-02 read the recorded choice. |
| Budget pass A cap (task 11-01) | OBS-05 (D-66, D-86) | Paid spend | The user authorises `--stage-cap` (proposed $0.50) and the raised-ceiling env set for this pass only. The settings are recorded, then unset before 11-03's restart. |
| Drive 1 cap and committed readings (task 12-01) | OBS-05 (D-69, D-73, D-86) | Paid spend. The committed literals can only be amended before any drive-1 record exists | The user authorises the cap (proposed $0.50) and confirms `SC2_TIMEOUT_DOMINANCE_RULE`, `CITATION_REJECTION_TRIPWIRE`, `CITATION_REJECTION_NULL_BASELINE` and `VECTOR_BASELINE_USABLE_FLOOR`, or amends them in a new commit before the drive. |
| D-84 gate before OI-01 (task 12-03) | OBS-05 (D-83, D-84) | The proceed, iterate or halt decision on a computed MISS is a user decision | Present `gates-drive1.md` and the smart-sampling rows of `diagnostic/table.md`. The user replies `pass-proceed` (which also authorises the $0.05 mention-probe cap), `iterate-once` (via `/gsd-plan-phase 06.3.4.1 --gaps`, citing rows) or `halt-disclose`. |
| Seed-chunk boost without a path (task 13-03) | DATA-05 (D-76, D-78) | This extends D-76's wording and needs explicit approval; choosing by which option passes SC-4 would be tuning | Present `06.3.4.1-SEED-PROBE.md`: path-found rate, the (d) rate on G, hub rates, and the composition proxy under each option. The user replies `paths-only` or `paths-plus-seed-chunks` and confirms `DEGREE_CAP` and `MAX_PATH_FACTS`. The choice ships as the default with no flag. |
| Budget pass B cap and canary probes (task 18-01) | OBS-05 (D-86) | Paid spend | The user authorises `--stage-cap` (proposed $0.50), the pass-A ceiling set and the 8 live canary probes. |
| Drive 2 cap, the run of record (task 19-01) | OBS-05 (D-86) | Paid spend | The user authorises `--stage-cap` (proposed $0.75, at least 2.5 times the worst case). |
| D-83 gate on the run of record (task 19-03) | OBS-05, DATA-05 (D-83, D-88) | Closing, iterating once or disclosing a miss is a user decision. The 0.20 floor changes only by an explicit dated amendment | Present `gates-drive2.md` and the smart-sampling rows. The user replies `all-pass-close`, `iterate-once` (via `--gaps`, citing rows) or `disclose-close`. A negative paired Δ is reported, not toggled off. |
| Engine and gateway stopped during reconcile `--apply` and the D-59 re-ingest; an interrupted apply is recovered from the snapshot, never by re-running `--apply` (backstop truth, plan 01) | DATA-03 | Process state on the operator's machine is outside what the harness can assert | Before 06-02's apply and 10-01's re-ingest, confirm with `tasklist` that no `engine.exe` or gateway process is running, and record it in the SUMMARY. On interruption, run a fresh dry run and restore from the snapshot directory or a table-version restore. |
| No entity extraction runs concurrently with the provenance prune (backstop truth, plan 01) | DATA-04 | Ordering across two stages and a live queue | Before any prune, confirm that the D-59 re-ingest has completed and `staged_documents_v2_rows == 0` (the `--document-ids` output), and record it in the 06 and 10 SUMMARYs. |
| Gate thresholds are never set, moved or relaxed after their data exists, and no miss is suppressed, re-run or tuned toward (judgment prohibition, plan 01) | OBS-05 | Intent is a judgement. Commit ordering is the checkable half | Confirm that every commit touching `eval/src/lancet_eval/thresholds.py` precedes the first journal record of the drive it governs (`git log` dates against the journal header), and that every MISS appears in `06.3.4.1-DRIVE1.md` or `06.3.4.1-RUN-OF-RECORD.md`. |
| Pre-06.3.4.1 EM, Token-F1 and graph-presence numbers are not presented as comparable (judgment prohibition, plan 01) | OBS-05 | Reading published prose for comparability claims | Review the 20-01 addendum in `docs/evaluation-fidelity-and-graph-yield.md` and `eval/runs/2026-09-09-multihop_rag/SUPERSEDED.md` for explicit non-comparability statements. |
| The graph path is not eval-only, toggleable or special-cased to the eval corpus (judgment prohibition, plan 01) | DATA-05 | Absence of a corpus-specific special case is a code-review judgement | Review the 13 to 16 diffs for corpus-specific constants (for example publisher names), eval-only flags or env switches. Production and eval run the same seeding and the same prompt. |
| A negative graph-on result is not hidden, switched off or re-run until it improves (judgment prohibition, plan 01; 06.3.1 D-49) | DATA-05 | Disclosure is a judgement | Confirm that `06.3.4.1-RUN-OF-RECORD.md` reports the paired Δ with its sign, and that any D-83 iteration cites table rows rather than the number. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies. All 44 implementation tasks carry at least one `<automated>`, each with a `<fails_when>`. The 10 blocking `checkpoint:decision` tasks are covered under Manual-Only.
- [x] Sampling continuity: no 3 consecutive tasks without automated verify. Every implementation task in every wave is automated; checkpoints are not implementation tasks.
- [x] Wave 0 covers all MISSING references. No `<automated>` is `MISSING`, and each new test file is created in the task that first runs it, before any later consumer.
- [x] No watch-mode flags. No `--watch` or equivalent appears in any plan.
- [x] Feedback latency < 300s. Per-commit sampling uses the per-module pytest, cargo filter and grep/assertion commands. The release builds in 01-01, 03-01, 09-01 and 13-02 are incremental after the first build. The `record`/`live` rows check an artifact once per task. The full suite (~300 s) is the per-wave gate.
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-09-23 (plan-phase revision 1, filled from the committed plans 01–20). `status` stays `draft` and `wave_0_complete` stays `false` until execution creates the Wave 0 files and `/gsd-validate-phase` reconciles the statuses.
