---
phase: 06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair
plan: 12
type: execute
wave: 8
depends_on: [06.3.4.1-11, 06.3.4.1-05]
files_modified:
  - eval/runs/
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-DRIVE1.md
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-BUDGETS.md
autonomous: false
requirements: [OBS-05]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Paid drive 1 runs after the index, OI-02 and prompt fixes, only after a blocking checkpoint authorises its `--stage-cap`, and gates SC-2 and SC-3 (D-85, D-86)."
    - "Drive 1 is one invocation `lancet-eval run --corpus multihop_rag_diag --out <pinned path> --workers 1 --retries 0 --no-resume --stage-cap <authorised>` into a new dated directory `eval/runs/<UTC-date>-drive1-multihop_rag_diag/` whose journal is asserted absent before the start; every timeout therefore appears once in SC-2's count (D-67, D-68)."
    - "The identity gate, the canaries and the index generation are checked by `preflight --corpus multihop_rag_diag` immediately before the drive, and no `LANCET_ENGINE__WORKFLOW__*` variable is set (D-61, D-66)."
    - "RetrieveHybrid flatness is judged over the whole drive-1 journal (about 200 records) on the harness → gateway → engine path with `flatness_verdict`; PASS needs both prongs available and not fired and an unchanged engine PID, so any RetrieveHybrid or harness timeout makes it unavailable (D-64 as amended, D-65)."
    - "D-69's classes are reported per question in the diagnostic table and as a rate split by null and non-null next to SC-2, against the committed 0.159 tripwire; the `--retries 0` lower-bound caveat on the 06.3.4 baseline is disclosed (D-69)."
    - "SC-3 is read on `answer_usable` over graph-off usable records in G against the committed `VECTOR_BASELINE_USABLE_FLOOR`, with a Wilson CI, strata, constant-Yes baselines and the D-34-excluded GenerateAnswer failure count; legacy EM/F1 are reported unchanged and labelled as near zero by construction (D-70, D-73)."
    - "Format-versus-fix attribution compares drive 1's full-answer containment on G and on all answerable questions with the retro's 91/147, which stays labelled partial-run and non-comparable (D-74)."
    - "A drive stopped by its cap or halted stays `partial: true` and is reported as a halted diagnostic, never as complete; a finished drive is scored and reported only through the unmodified fail-closed `score`/`report` path (D-87a)."
    - "SC-3 is a hard gate before OI-01: on a miss, the phase halts at a blocking decision before any graph work; at most one table-justified iteration is allowed (planned from cited table rows via `--gaps`), then a second miss halts with the failure disclosed (D-84, D-83 rule)."
  artifacts:
    - path: "eval/runs/"
      provides: "eval/runs/<date>-drive1-multihop_rag_diag/{journal.jsonl,report.json,report.md,metadata.json,diagnostic/,gates-drive1.md}"
    - path: ".planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-DRIVE1.md"
      provides: "SC-2/SC-3 readings, flatness, D-69 rates, attribution, spend, D-84 outcome"
  key_links:
    - from: "eval/runs/<date>-drive1-multihop_rag_diag/journal.jsonl"
      to: "eval/src/lancet_eval/unpark_gates.py"
      via: "python -m lancet_eval.unpark_gates --stage drive1"
      pattern: "gates-drive1"
  prohibitions:
    - "Drive 1 must not use --resume, --retries above 0, --limit, more than one worker, or an existing journal path"
    - "No engine restart, budget change or env override between the two PID reads"
    - "No graph-architecture work may start from this plan; OI-01 begins only after a PASS or a recorded user decision at Task 3"
---

<objective>
Run paid drive 1 on the fixed index, engine and prompt, and read SC-2 (timeout not dominant, flatness)
and SC-3 (the vector baseline answers some questions) as computed gates. Then stop at the D-84 decision
before any graph work.

Purpose: this is the layer-4 answer to "is vector enough?" and the harness-path flatness proof D-64 and
D-65 require. Graph repair (OI-01) is not allowed to start on a failing vector baseline (D-84).

Output: the drive-1 run directory scored through the fail-closed path, the diagnostic table, gate
readings, `06.3.4.1-DRIVE1.md`, and the recorded D-84 decision.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-CONTEXT.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-AI-SPEC.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-BUDGETS.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RETRO-LENIENT.md
@eval/corpora/multihop_rag/diag_selection.json
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Authorise paid drive 1 and confirm the committed readings (D-86)</name>
  <decision>Authorise drive 1's `--stage-cap` and confirm the SC-2 reading and D-69 tripwire committed in thresholds.py.</decision>
  <context>
    Drive 1 covers the diagnostic sample from `diag_selection.json` × 2 arms, about 200 records. The
    estimate is $0.073 at the mean and $0.19 all-at-max (RESEARCH §L). The proposed cap is $0.50,
    ≥ 2.5× the worst case, because a cap-stop voids the no-resume flatness proof.

    Committed literals to confirm, which can only be changed now, before any record exists:
    - `SC2_TIMEOUT_DOMINANCE_RULE = plurality_tie_is_dominant`;
    - `CITATION_REJECTION_TRIPWIRE = 0.159`;
    - `CITATION_REJECTION_NULL_BASELINE = 29/42`;
    - `VECTOR_BASELINE_USABLE_FLOOR`, with its value and the P(miss) statement from 06.3.4.1-10's summary.

    Disclosures:
    - flatness needs zero RetrieveHybrid and harness timeouts over the drive (AI-SPEC §5);
    - the 06.3.4 D-69 baseline (43/355) was recorded under `--retries 2` and is a lower bound, so the single-attempt rate may exceed 0.159 without any prompt effect (RESEARCH §A item 4);
    - drive 1's graph-on arm still runs the pre-OI-01 seeding.
  </context>
  <options>
    <option id="authorise-0.50">
      <name>Authorise $0.50 and confirm the committed literals</name>
      <pros>Covers the worst case 2.5×; literals already committed before data</pros>
      <cons>Paid spend</cons>
    </option>
    <option id="amend-then-authorise">
      <name>Amend a literal first (a new commit before the drive), then authorise</name>
      <pros>User control of the reading before data exists</pros>
      <cons>Needs a thresholds.py commit and its test before the drive</cons>
    </option>
  </options>
  <resume-signal>Reply authorise-0.50 (or a cap figure), or amend-then-authorise with the literal and value.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Drive 1, fail-closed scoring, diagnostic table and computed SC-2/SC-3 readings</name>
  <precondition>Task 1 recorded an authorised cap; the engine (release, 06.3.4.1-11 budgets) and gateway are running with `LANCET_ENV=eval`; `Get-ChildItem Env:LANCET_ENGINE__WORKFLOW__*` returns nothing; Docker `db` up; OPENROUTER_API_KEY set; the commit adding `VECTOR_BASELINE_USABLE_FLOOR` exists in `git log`; the engine and gateway were launched per the launch requirements in `06.3.4.1-OI02-PROFILE.md` `## Fix` (build profile, stdout/stderr sink, collector state); no 07 replay env var (`LANCET_OPENROUTER__*_ENDPOINT`, `LANCET_CONFIG_DIR`) is set at process, user or machine scope. Record whether the engine was freshly started at record 1 from its first `request_process_state` event (added by 06.3.4.1-07).</precondition>
  <files>eval/runs/, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-DRIVE1.md, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-BUDGETS.md</files>
  <read_first>
    - `eval/src/lancet_eval/cli.py:230-327` (`run` options; `--out` bypasses `resolve_run_dir`), `:378-500` (`reconcile`, `score`, `report`)
    - `eval/src/lancet_eval/unpark_gates.py`, `eval/src/lancet_eval/diagnostic.py`, `eval/src/lancet_eval/flatness.py`
    - `06.3.4-OBSERVATION-THROUGHPUT-PLATEAU.md` (the 06.3.4 50-record slice table to compare against)
    - `06.3.4.1-OI02-PROFILE.md` `## Fix` (06.3.4.1-07): launch requirements and the named OI-02 mechanism this drive must stay flat against
  </read_first>
  <action>
    Set `RUN=eval/runs/<UTC-date>-drive1-multihop_rag_diag` and assert `$RUN/journal.jsonl` does not exist, halting if it does. Record the engine PID and start time (`Get-Process -Name engine`). Run `uv run --project eval lancet-eval preflight --corpus multihop_rag_diag`, which must pass. Then run exactly:

    `uv run --project eval lancet-eval run --corpus multihop_rag_diag --out "$RUN/journal.jsonl" --workers 1 --retries 0 --no-resume --stage-cap <authorised>`

    Record the engine PID and start time again. Run `lancet-eval reconcile --run "$RUN"`. If the journal is complete, run `lancet-eval score --run "$RUN" --no-judge` and `lancet-eval report --run "$RUN"`. If it is incomplete (cap stop or halt), do not score or report; record the halt with the header left `partial: true` (D-87a).

    Build the per-question table:

    `uv run --project eval python -m lancet_eval.diagnostic table --corpus multihop_rag_diag --journal "$RUN/journal.jsonl" --gold-chunks .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/diagnostic/post-reconcile/gold_chunks.jsonl --vector-top4 .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/diagnostic/post-reconcile/vector_top4.jsonl --out-dir "$RUN/diagnostic" --label drive1`

    Use the re-probed gold-chunk file instead if 06.3.4.1-10 re-ingested. Compute the gates:

    `uv run --project eval python -m lancet_eval.unpark_gates --stage drive1 --run "$RUN" --gold-chunks <same> --populations eval/corpora/multihop_rag/diag_selection.json --engine-pid-before <pid> --engine-pid-after <pid> --out "$RUN/gates-drive1.md"`

    Write `06.3.4.1-DRIVE1.md` with:
    - the command lines, PIDs, binary path, mtime and profile;
    - SC-2 (error classes per arm, both-arm-error pairs, and the flatness verdict with slope, window delta and 50-record slice medians beside 06.3.4's 5.0 → 29.0 s table);
    - D-69 rates split null/non-null with Wilson CIs;
    - SC-3 with strata and constant-Yes baselines, D-34 exclusions and `final_answer_missing` rate against 0.10;
    - legacy EM/F1/Recall@4/Precision@4, labelled;
    - attribution against the retro's 91/147;
    - spend against the cap;
    - the header `partial` value.

    Append the authorised drive-1 cap to the BUDGETS stage-cap table.
  </action>
  <verify>
    <automated>RUN=$(ls -d eval/runs/*-drive1-multihop_rag_diag | tail -n 1) && test -s "$RUN/journal.jsonl" && test -s "$RUN/gates-drive1.md" && test -s "$RUN/diagnostic/table.md" && grep -qE "SC-2.*(PASS|MISS)" "$RUN/gates-drive1.md" && grep -qE "SC-3.*(PASS|MISS)" "$RUN/gates-drive1.md"</automated>
    <fails_when>non-zero exit: the drive-1 journal, gate readings or diagnostic table is missing, or the gate file lacks a PASS/MISS reading for SC-2 or SC-3</fails_when>
    <automated>git diff --quiet --exit-code -- eval/runs/2026-09-03-multihop_rag/ eval/runs/2026-09-09-multihop_rag/</automated>
    <fails_when>non-zero exit: a committed earlier run was modified</fails_when>
  </verify>
  <acceptance_criteria>
    - `06.3.4.1-DRIVE1.md` states SC-2 and SC-3 as PASS or MISS with values, n and CIs, the flatness verdict with both prong availabilities, and the engine PIDs before and after (equal).
    - If the drive completed, `report.json` exists and was produced by `score`; if not, no `report.json` exists and the header reads `partial: true`.
    - Spend is at or below the authorised cap.
  </acceptance_criteria>
  <done>SC-2 and SC-3 are read by computation on a fail-closed, fully evidenced drive.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: D-84 gate before OI-01</name>
  <decision>Proceed to graph repair, iterate once on the vector baseline, or halt, based on the computed drive-1 readings.</decision>
  <context>
    Present `gates-drive1.md` and the rows of `diagnostic/table.md` that drive any MISS (the AI-SPEC §7
    smart-sampling rows: (c) yes but (e) no, `final_answer_missing`, D-69 rejections, timeouts by
    ordinal). D-84: SC-3 is a hard gate before OI-01. An SC-3 miss allows one table-justified iteration
    under the D-83 rule, planned from cited rows via `/gsd-plan-phase 06.3.4.1 --gaps` and carrying its
    own D-86 cap checkpoint; a second miss halts with the failure disclosed while 6.4 stays parked. An
    SC-2 miss (timeout dominant or flatness unavailable or present) gets the same one-iteration shape on
    OI-02, and is never a restart, a budget raise or retries. The PASS option also authorises the offline
    mention-embedding probe of 06.3.4.1-13 (one batched embedding call per question, under $0.01 total)
    at a cap of $0.05.
  </context>
  <options>
    <option id="pass-proceed">
      <name>SC-2 and SC-3 PASS: proceed to OI-01 and authorise the mention-probe cap $0.05</name>
      <pros>Layer order satisfied; graph work may start</pros>
      <cons>None beyond spend</cons>
    </option>
    <option id="iterate-once">
      <name>MISS on the first reading: one table-justified iteration via --gaps (cite the rows)</name>
      <pros>D-83/D-84-compliant single fix round</pros>
      <cons>Another paid drive with its own cap; OI-01 plans stay blocked until a PASS</cons>
    </option>
    <option id="halt-disclose">
      <name>MISS after the iteration (or the user chooses not to iterate): halt before OI-01, disclose; 6.4 stays parked</name>
      <pros>Honest outcome; no graph work on a failing baseline</pros>
      <cons>SC-4/SC-5 are not attempted in this phase</cons>
    </option>
  </options>
  <resume-signal>Reply pass-proceed, iterate-once (listing the table rows that justify the fix), or halt-disclose.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → gateway → engine → provider | paid drive with model output flowing back into published numbers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.4.1-12-01 | Denial of service (financial) | drive 1 | medium | mitigate | In-process `--stage-cap` authorised at Task 1; `--workers 1` bounds overshoot to one query |
| T-06.3.4.1-12-02 | Repudiation | completeness | high | mitigate | Unmodified reconcile/score/report path; halted drives stay `partial: true` and are not scored; SC-1 evaluator reads outcomes only |
| T-06.3.4.1-12-03 | Tampering | run directory | medium | mitigate | Pinned new path asserted absent; no resume; earlier runs guarded by `git diff --quiet` |
| T-06.3.4.1-12-04 | Repudiation | gate readings | high | mitigate | PASS/MISS computed by `unpark_gates` against literals committed before the drive; D-84 decision recorded with cited rows |
</threat_model>

<verification>
- The drive-1 gate file and diagnostic table exist; the earlier committed runs are unchanged.
</verification>

<success_criteria>
Drive 1 is evidenced end to end, SC-2 and SC-3 are computed, and the D-84 decision is recorded before
any graph work.
</success_criteria>

## Artifacts this phase produces

- Run directory `eval/runs/<date>-drive1-multihop_rag_diag/` with `journal.jsonl`, `report.json`/`report.md`/`metadata.json` (if complete), `diagnostic/table.{jsonl,md}`, `gates-drive1.md`.
- `06.3.4.1-DRIVE1.md`; drive-1 cap appended to `06.3.4.1-BUDGETS.md`.

<output>
Create `.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-12-SUMMARY.md` when done.
</output>
