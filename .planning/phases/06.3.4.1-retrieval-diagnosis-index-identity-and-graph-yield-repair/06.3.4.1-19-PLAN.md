---
phase: 06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair
plan: 19
type: execute
wave: 15
depends_on: [06.3.4.1-18]
files_modified:
  - eval/runs/
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RUN-OF-RECORD.md
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-BUDGETS.md
autonomous: false
requirements: [OBS-05, DATA-05]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Paid drive 2 runs after OI-01, only after a blocking checkpoint authorises its `--stage-cap`, over the same question IDs as drive 1, and gates SC-4 and SC-5 (D-85, D-86)."
    - "Drive 2 is the run of record 6.4 cites: a new dated directory `eval/runs/<UTC-date>-drive2-multihop_rag_diag/` asserted absent before the start, one invocation with `--workers 1 --retries 0 --no-resume`, scored and reported through the fail-closed `score`/`report` path, with its sample size stated; no separate 500×2 run is made in this phase (D-87, D-67)."
    - "SC-1 is evidenced here: the run of record's journal `partial` flag equals `not completeness_comparison(...)`, and `score`/`report` accept it through the unmodified fail-closed path; a halted drive stays `partial: true` and is never reported as complete (D-87a)."
    - "Earlier drives and passes are kept as evidence and marked diagnostic with a `DIAGNOSTIC.md` marker in the drive-1 directory pointing at the run of record (D-87)."
    - "SC-4 and SC-5 headlines are on G / V among dual-success pairs, with the all-answerable numbers beside them; graph-off invariance against drive 1 is reported per question, and a systematic graph-off shift is investigated before drive 2 is cited (D-82)."
    - "Gate-miss policy: if SC-4 or SC-5 misses, one bounded iteration justified by cited per-question table rows is allowed through `/gsd-plan-phase 06.3.4.1 --gaps` with its own authorised cap; if it still misses, the phase closes with the failure disclosed, 6.4 stays parked pending a user decision, and the 0.20 floor is amended only by an explicit dated user authorisation (D-83, 06.3.1 D-49)."
    - "Drive 2 re-reports SC-2, SC-3 and the D-69 rate as disclosures; any regression is disclosed, not re-gated (D-85)."
  artifacts:
    - path: "eval/runs/"
      provides: "eval/runs/<date>-drive2-multihop_rag_diag/{journal.jsonl,report.json,report.md,metadata.json,diagnostic/,gates-drive2.md} and the drive-1 DIAGNOSTIC.md marker"
    - path: ".planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RUN-OF-RECORD.md"
      provides: "SC-1..SC-5 readings, sample size, provenance, D-83 outcome"
  key_links:
    - from: "eval/runs/<date>-drive2-multihop_rag_diag/journal.jsonl"
      to: "eval/src/lancet_eval/unpark_gates.py --stage drive2"
      via: "evaluate_sc1/sc4/sc5 + graph_off_invariance(drive1, drive2)"
      pattern: "gates-drive2"
  prohibitions:
    - "Drive 2 must not use --resume, --retries above 0, --limit, more than one worker, or an existing journal path"
    - "A miss must not be answered by re-running drive 2 unchanged, by toggling the graph path off, or by moving a threshold"
---

<objective>
Run paid drive 2, the run of record, on the repaired graph path, and read SC-1, SC-4 and SC-5 as
computed gates, with SC-2/SC-3 re-reported. Then record the D-83 decision.

Purpose: these are the Phase 6.4 unpark gates. 6.4 documents whatever this run shows, positive or
negative (06.3.1 D-49).

Output: the drive-2 run directory scored fail-closed, the diagnostic table, gate readings,
`06.3.4.1-RUN-OF-RECORD.md`, the drive-1 diagnostic marker and the D-83 decision.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-CONTEXT.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-DRIVE1.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-BUDGETS.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-SEED-PROBE.md
@eval/corpora/multihop_rag/diag_selection.json
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Authorise paid drive 2, the run of record (D-86)</name>
  <decision>Authorise drive 2's `--stage-cap`.</decision>
  <context>
    Drive 2 covers the same question IDs as drive 1 × 2 arms (about 200 records). The estimate is ~$0.08
    at the mean and ~$0.25 worst case, since graph-on prompts can reach the 8,192-token evidence ceiling
    (RESEARCH §L, AI-SPEC §5). The proposed cap is $0.75, ≥ 2.5× the worst case, because a cap-stop
    leaves the run of record partial.

    Committed before this drive: the SC-4 Wilson clause 0.098, `GRAPH_COMPOSITION_CHANGE_FLOOR` 0.10,
    the SC-5 rule, and the unamended 0.20 floor. There is no overall phase ceiling (06.3.1 D-47).
  </context>
  <options>
    <option id="authorise-0.75">
      <name>Authorise $0.75</name>
      <pros>Covers the worst case 3×</pros>
      <cons>Paid spend</cons>
    </option>
    <option id="authorise-other">
      <name>Authorise a different figure</name>
      <pros>User control</pros>
      <cons>Below ~$0.30 risks a partial run of record</cons>
    </option>
  </options>
  <resume-signal>Reply authorise-0.75, or authorise-other with the figure.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Drive 2, fail-closed scoring, diagnostic table and computed SC-1/SC-4/SC-5 readings</name>
  <precondition>Task 1 authorised a cap; the engine (release, 06.3.4.1-18 budgets) and gateway are running with `LANCET_ENV=eval`; `Get-ChildItem Env:LANCET_ENGINE__WORKFLOW__*` returns nothing; Docker `db` up; OPENROUTER_API_KEY set; the commit adding the three 06.3.4.1-17 literals exists in `git log`.</precondition>
  <files>eval/runs/, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RUN-OF-RECORD.md, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-BUDGETS.md</files>
  <read_first>
    - `06.3.4.1-12-PLAN.md` Task 2 (the same command sequence), `eval/src/lancet_eval/unpark_gates.py` (`--stage drive2`)
    - `06.3.4.1-DRIVE1.md` (the drive-1 run path for invariance)
  </read_first>
  <action>
    Set `RUN=eval/runs/<UTC-date>-drive2-multihop_rag_diag` and assert `$RUN/journal.jsonl` does not exist. Record the engine PID and start time. Run `uv run --project eval lancet-eval preflight --corpus multihop_rag_diag`, which must pass with all 8 canaries. Then run exactly `uv run --project eval lancet-eval run --corpus multihop_rag_diag --out "$RUN/journal.jsonl" --workers 1 --retries 0 --no-resume --stage-cap <authorised>`. Record the PID again. Run `lancet-eval reconcile --run "$RUN"`, and if complete, `lancet-eval score --run "$RUN" --no-judge` and `lancet-eval report --run "$RUN"`. If incomplete, stop there with the header `partial: true`; the run is then not a run of record.

    Build the table with `python -m lancet_eval.diagnostic table --corpus multihop_rag_diag --journal "$RUN/journal.jsonl" --gold-chunks <post-reconcile or re-probed file> --vector-top4 <file> --seed-probe .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/diagnostic/seed_probe.jsonl --out-dir "$RUN/diagnostic" --label drive2-run-of-record`. Then run `python -m lancet_eval.unpark_gates --stage drive2 --drive1-run <drive-1 dir> --run "$RUN" --populations eval/corpora/multihop_rag/diag_selection.json --gold-chunks <same> --out "$RUN/gates-drive2.md"`.

    Write `<drive-1 dir>/DIAGNOSTIC.md`: "Diagnostic drive (06.3.4.1 D-87); the run of record is `<RUN>`; do not cite as the phase's result". It is a new file, and no existing drive-1 file is modified.

    Write `06.3.4.1-RUN-OF-RECORD.md`:
    - the command lines, PIDs, binary provenance, `index_generation` and sample size (questions and records);
    - SC-1 evidence (header `partial` against `completeness_comparison`, and the `report.json` path);
    - SC-4 (pairs(G) headline, Wilson CI and lower-bound clause, pairs(A), all usable graph-on, influence and no-match, compared with 06.3.4's 14/143 labelled non-comparable);
    - SC-5 (composition on V/G/A, paired Δs with bootstrap CIs and strata, cost deltas, direction flag);
    - (d) seed-hit rate, seed-count and path-found distributions, `GRAPH_TIMEOUT` rate;
    - graph-off invariance against drive 1;
    - SC-2/SC-3/D-69 re-reports;
    - spend against the cap.

    Append the drive-2 cap to the BUDGETS stage-cap table.
  </action>
  <verify>
    <automated>RUN=$(ls -d eval/runs/*-drive2-multihop_rag_diag | tail -n 1) && test -s "$RUN/journal.jsonl" && test -s "$RUN/gates-drive2.md" && test -s "$RUN/diagnostic/table.md" && grep -qE "SC-1.*(PASS|MISS)" "$RUN/gates-drive2.md" && grep -qE "SC-4.*(PASS|MISS)" "$RUN/gates-drive2.md" && grep -qE "SC-5.*(PASS|MISS)" "$RUN/gates-drive2.md"</automated>
    <fails_when>non-zero exit: the drive-2 journal, gates file or table is missing, or a PASS/MISS reading for SC-1, SC-4 or SC-5 is absent</fails_when>
    <automated>git diff --quiet --exit-code -- eval/runs/2026-09-03-multihop_rag/ eval/runs/2026-09-09-multihop_rag/</automated>
    <fails_when>non-zero exit: an earlier committed run was modified</fails_when>
  </verify>
  <acceptance_criteria>
    - `06.3.4.1-RUN-OF-RECORD.md` states the sample size and every SC reading with n and CI.
    - If complete: header `partial: false`, `report.json` present from `score`. If not: `partial: true`, no report.
    - The drive-1 directory gains only `DIAGNOSTIC.md`.
  </acceptance_criteria>
  <done>The run of record exists, scored fail-closed, with SC-1, SC-4 and SC-5 read by computation.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: D-83 gate reading on the run of record</name>
  <decision>Close with the gates as read, iterate once on a table-justified basis, or close with the failure disclosed.</decision>
  <context>
    Present `gates-drive2.md` and the AI-SPEC §7 smart-sampling rows of `diagnostic/table.md`: (d) yes
    with no composition change; graph-on (e) flipped yes → no against graph-off; timeouts by ordinal;
    `final_answer_missing`.

    D-83: after the planned fixes, one more fix round is allowed. It must be justified by the
    per-question table, not by tuning toward the number (06.3.1 D-49). It is planned via
    `/gsd-plan-phase 06.3.4.1 --gaps` citing rows, and it carries its own D-86 cap checkpoint (the D-83
    drive's proposed cap is $0.75). A second miss closes the phase with the failure disclosed; 6.4 stays
    parked pending a user decision. The 0.20 floor is not amended unless the user explicitly authorises
    a dated amendment. A negative paired Δ that is visible is reported, not toggled off (D-78).
  </context>
  <options>
    <option id="all-pass-close">
      <name>SC-1, SC-4 and SC-5 PASS: close; proceed to D-88 (06.3.4.1-20) and the 6.4 unpark decision</name>
      <pros>Unpark gates met on the run of record</pros>
      <cons>None</cons>
    </option>
    <option id="iterate-once">
      <name>SC-4 or SC-5 MISS, first time: one table-justified iteration via --gaps (list the rows)</name>
      <pros>D-83-compliant single round</pros>
      <cons>Another paid drive; this drive becomes diagnostic if the iteration produces a new run of record</cons>
    </option>
    <option id="disclose-close">
      <name>MISS after the iteration (or no iteration chosen): close with the failure disclosed; 6.4 stays parked pending a user decision</name>
      <pros>Honest result; D-88 still records non-comparability</pros>
      <cons>6.4 stays blocked</cons>
    </option>
    <option id="dated-floor-amendment">
      <name>User-authorised dated amendment of the 0.20 floor (explicit wording and date required)</name>
      <pros>Only path D-83 allows for changing the floor</pros>
      <cons>Must be recorded with rationale; never inferred</cons>
    </option>
  </options>
  <resume-signal>Reply all-pass-close, iterate-once (with rows), disclose-close, or dated-floor-amendment (with wording and date).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → gateway → engine → provider | paid run of record feeding published unpark gates |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.4.1-19-01 | Denial of service (financial) | drive 2 | medium | mitigate | Authorised in-process `--stage-cap`; `--workers 1` overshoot bound |
| T-06.3.4.1-19-02 | Repudiation | run of record completeness | high | mitigate | Unmodified reconcile/score/report path; halted run stays partial and is not a run of record; SC-1 evaluator checks header against measured completeness |
| T-06.3.4.1-19-03 | Repudiation | gate readings | high | mitigate | Computed against literals committed before drive 2; D-83 decision recorded with cited rows |
| T-06.3.4.1-19-04 | Tampering | earlier runs | medium | mitigate | New pinned directory asserted absent; drive-1 directory gains only a new marker file; `git diff --quiet` guard on 2026-09-03/09 runs |
</threat_model>

<verification>
- The drive-2 gate file exists with SC-1/SC-4/SC-5 readings; earlier committed runs are unchanged.
</verification>

<success_criteria>
The run of record is scored fail-closed with every unpark gate read by computation, and the D-83
decision is recorded.
</success_criteria>

## Artifacts this phase produces

- Run directory `eval/runs/<date>-drive2-multihop_rag_diag/` (journal, report.json, report.md, metadata.json, `diagnostic/`, `gates-drive2.md`).
- `eval/runs/<date>-drive1-multihop_rag_diag/DIAGNOSTIC.md`.
- `06.3.4.1-RUN-OF-RECORD.md`; drive-2 cap row in `06.3.4.1-BUDGETS.md`.

<output>
Create `.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-19-SUMMARY.md` when done.
</output>
