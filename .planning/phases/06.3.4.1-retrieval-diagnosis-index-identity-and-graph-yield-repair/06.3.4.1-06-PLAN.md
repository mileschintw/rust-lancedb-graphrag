---
phase: 06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair
plan: 06
type: execute
wave: 3
depends_on: [06.3.4.1-04]
files_modified:
  - eval/corpora/multihop_rag/document_map.json
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-STORE-BASELINE.md
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/reconcile/apply.json
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/reconcile/pg_deleted.json
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/reconcile/document_ids_post.json
  - .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/reconcile/graph_population_post.json
autonomous: false
requirements: [OBS-05, DATA-04]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The surgical reconcile runs only after the user confirms the committed dry-run numbers at a blocking decision; the checkpoint confirms the numbers and rollback layers, it does not re-ask whether to reconcile, because D-58 is already the user's decision (D-58)."
    - "Before any delete, three rollback layers exist: a directory copy `data/lancedb-eval.pre-06.3.4.1-reconcile/`, recorded per-table versions (LanceDB `checkout(v)` + `restore()`), and a `pg_dump -n lancet_eval` file outside git (D-58 reversibility)."
    - "After apply, LanceDB `documents` = distinct `nodes.document_id` = the 346 `document_map.json` entries, PostgreSQL `lancet_eval.documents` = the same 346 IDs all `completed`, `staged_documents_v2` = 0 rows, and the alias `0370301a…` is in neither store (D-58, D-60, D-61)."
    - "The engine is restarted after the deletes and `index_generation` is refreshed in `document_map.json` only by `record_index_generation()` from a live probe; the new value differs from `lance-701` (D-62)."
    - "`06.3.4.1-STORE-BASELINE.md` records pre/post counts, deleted document IDs (LanceDB and PostgreSQL), all seven table versions, the engine-read `index_generation`, and the post-reconcile entity-degree distribution (min, median, p95, p99, max, isolated count, highest-degree ID) that D-77 derives its caps from (D-62)."
    - "Re-running the dry-run after apply reports zero extras, zero rewrites and zero orphans (specless edge: idempotency, DATA-03/DATA-04)."
    - "Apply runs with the engine and gateway stopped (no listener on 127.0.0.1:50051 or :8080), and an interrupted apply is recovered from the snapshot layers after a fresh dry-run, never by blindly re-running apply (specless edge: concurrency)."
    - "D-56 is honoured: only surgical deletes happen here; no full re-seed and no re-extraction."
  artifacts:
    - path: ".planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-STORE-BASELINE.md"
      provides: "post-reconcile store baseline in the 06.3.3-STORE-BASELINE shape, incl. degree distribution"
    - path: "eval/corpora/multihop_rag/document_map.json"
      provides: "index_generation refreshed from the engine probe; alias kept under aliases"
  key_links:
    - from: "eval/corpora/multihop_rag/document_map.json"
      to: "engine /rag/query snapshot"
      via: "record_index_generation()"
      pattern: "index_generation"
    - from: "06.3.4.1-STORE-BASELINE.md degree distribution"
      to: "06.3.4.1-13 D-77 cap derivation"
      via: "p99 degree read from graph_population_post.json"
      pattern: "p99"
  prohibitions:
    - "--apply must not run before the directory copy, the version record and the pg_dump all exist"
    - "index_generation must not be typed by hand; only record_index_generation writes it"
    - "The engine must not be started while staged_documents_v2 holds any row"
---

<objective>
Walk through the D-58 one-way door that 06.3.4.1-04 measured: snapshot, delete the extra and alias
documents from LanceDB and PostgreSQL with the allow-list provenance prune, restart the engine, read
the new generation from it, and write the post-reconcile baseline that D-77 consumes.

Purpose: layer 2 of the mandatory order. The run of record cannot be scored while any unmapped
document can leak into retrieval (RESEARCH Pitfall 1; `score.py:255-274` refuses it), and every later
paid stage is refused by the 06.3.4.1-02 gate until this lands.

Output: a reconciled eval store and schema, a refreshed generation, and `06.3.4.1-STORE-BASELINE.md`.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-CONTEXT.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-RECONCILE-DRYRUN.md
@.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-04-SUMMARY.md
@.planning/phases/06.3.3-retrieval-latency-measurement-pass-and-timeout-budget-deriva/06.3.3-STORE-BASELINE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the reconcile's exact dry-run output and rollback layers (D-58)</name>
  <decision>Confirm that the committed dry run is the change to apply, and authorise the single generation-probe query it needs.</decision>
  <context>
    D-58 (surgical reconcile) and D-60 (alias collapses to canonical) are already the user's decisions;
    this checkpoint does not re-ask them. It confirms the numbers before the one-way step, from
    `06.3.4.1-RECONCILE-DRYRUN.md`:
    - the exact LanceDB extra document IDs, and whether they match RESEARCH A1's four;
    - the exact PostgreSQL extra rows (expected 21) with filename, status and created_at;
    - per-table rows to delete, entities to rewrite, orphans and orphan-incident entity_edges;
    - all seven table versions.

    Undo cost: a restore from the directory copy (~720 MB) or from LanceDB version restore, plus a
    pg_restore, or else re-ingestion. After apply, `record_index_generation()` sends one `/rag/query`
    probe, about $0.0005 of generation; it is authorised here as part of this stage (AI-SPEC §5 stage
    caps: probes ride with the stage they precede).
  </context>
  <options>
    <option id="apply-as-recorded">
      <name>Apply exactly the recorded dry run</name>
      <pros>Restores identity (OI-03); unblocks the identity gate and every later paid stage</pros>
      <cons>One-way without the snapshot layers; deletes 21 PostgreSQL rows whose provenance is otherwise unknown</cons>
    </option>
    <option id="hold-investigate">
      <name>Hold: the dry run differs from expectation in a way that needs a look first</name>
      <pros>Nothing is deleted while an unexplained ID (for example an unexpected LanceDB extra or a PostgreSQL row that is not a seed-retry duplicate) is examined</pros>
      <cons>All paid stages stay blocked by the identity gate</cons>
    </option>
  </options>
  <resume-signal>Reply apply-as-recorded, or hold-investigate with the IDs to examine.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Snapshot, apply, restart and read the new generation</name>
  <precondition>Task 1 resolved as apply-as-recorded; Docker `db` service up (`docker compose ps db`); no process listening on 127.0.0.1:50051 or 127.0.0.1:8080; OPENROUTER_API_KEY present in the environment for the single generation probe.</precondition>
  <reversibility rating="one-way">Deletes rows from the eval LanceDB store and the lancet_eval schema; undo needs the snapshot directory, LanceDB version restore plus pg_restore, or re-ingestion (D-58).</reversibility>
  <files>eval/corpora/multihop_rag/document_map.json, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/reconcile/apply.json, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/reconcile/pg_deleted.json, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/reconcile/document_ids_post.json</files>
  <read_first>
    - `06.3.4.1-RECONCILE-DRYRUN.md` (the "Rollback layers" section)
    - `engine/src/bin/reconcile_eval_store.rs` USAGE, `eval/src/lancet_eval/identity.py` CLI, `eval/src/lancet_eval/seed.py` `record_index_generation`
    - `engine/src/main.rs:30-50,103-121` (startup pins `nodes_version` + BM25; staged-job replay)
  </read_first>
  <action>
    In order, stopping at the first failure:
    1. Confirm nothing listens on 127.0.0.1:50051 or :8080 (`netstat -ano`); record the check.
    2. Copy `data/lancedb-eval` to `data/lancedb-eval.pre-06.3.4.1-reconcile/` (`robocopy /E` or `cp -r`) and compare file counts and total bytes.
    3. Run `docker compose exec -T db pg_dump -U postgres -d lancet -n lancet_eval` into `data/backups/lancet_eval.pre-06.3.4.1-reconcile.sql` (create `data/backups/`; `data/` is gitignored) and assert it is non-empty.
    4. Run `cargo run --manifest-path engine/Cargo.toml --release --quiet --bin reconcile_eval_store -- --document-map eval/corpora/multihop_rag/document_map.json --lancedb-path ./data/lancedb-eval --apply --snapshot-dir data/lancedb-eval.pre-06.3.4.1-reconcile` → `reconcile/apply.json`. Compare `extras`, `per_table.to_delete` and entity counts with `reconcile/dryrun.json` and stop on any difference.
    5. Run `uv run --project eval lancet-eval identity pg-delete-extras --corpus multihop_rag --apply --pg-dump data/backups/lancet_eval.pre-06.3.4.1-reconcile.sql` → `reconcile/pg_deleted.json`.
    6. Re-run the reconcile dry-run and assert extras, rewrites and orphans are all 0.
    7. Run `inspect_lancedb --document-ids` → `reconcile/document_ids_post.json` and assert `staged_documents_v2_rows == 0`.
    8. Build the release engine, start it with `LANCET_ENV=eval` and start the gateway. Record the engine binary path, mtime, build profile, PID and start time.
    9. Run `record_index_generation(settings, "multihop_rag")` through a one-line `uv run --project eval python -c` call. It updates `document_map.json` `index_generation` from the probe.
    10. Run `uv run --project eval lancet-eval identity check --corpus multihop_rag`, which must pass.

    If any step after step 4 fails, stop and record the state. Do not re-run `--apply` without a fresh dry run (specless concurrency edge). Commit only the JSON records and `document_map.json`, never the backups.
  </action>
  <verify>
    <automated>uv run --project eval lancet-eval identity check --corpus multihop_rag</automated>
    <fails_when>non-zero exit, or the printed report has "passed": false</fails_when>
    <automated>uv run --project eval python -c "import json; m = json.load(open('eval/corpora/multihop_rag/document_map.json')); assert m['index_generation'] != 'lance-701' and m['index_generation'].startswith('lance-'), m['index_generation']; assert '0370301a-0ad7-4267-a337-ba2dda34595f' in m['aliases'] and len(m['entries']) == 346"</automated>
    <fails_when>AssertionError printed: generation unchanged or malformed, alias entry lost, or entry count not 346</fails_when>
  </verify>
  <acceptance_criteria>
    - `reconcile/apply.json` extras and deletion counts equal `reconcile/dryrun.json`'s.
    - The second dry run reports 0 extras, 0 rewrites, 0 orphans.
    - `document_ids_post.json`: `documents` has 346 IDs equal to the map entries and `staged_documents_v2_rows` is 0.
    - `git status --porcelain data/` prints nothing (backups stay untracked).
  </acceptance_criteria>
  <done>Identity holds across map, LanceDB and PostgreSQL, and the engine serves a new, probe-recorded generation.</done>
</task>

<task type="auto">
  <name>Task 3: Post-reconcile store baseline (D-62) with the degree distribution D-77 needs</name>
  <files>.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-STORE-BASELINE.md, .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/reconcile/graph_population_post.json</files>
  <read_first>
    - `06.3.3-STORE-BASELINE.md` (section layout, degree method: linear-interpolation p95, multi-edges counted, self-loop counts twice)
    - `engine/src/bin/inspect_lancedb.rs:392-531` (`--graph-population`), `:621-675` (`--entity-name`)
    - `reconcile/document_ids_pre.json`, `reconcile/dryrun.json`, `reconcile/apply.json`, `reconcile/pg_deleted.json`
  </read_first>
  <action>
    Stop the engine, then run `cargo run --manifest-path engine/Cargo.toml --release --quiet --bin inspect_lancedb -- --graph-population --lancedb-path ./data/lancedb-eval` → `reconcile/graph_population_post.json`. Compute p99 of entity degree with the same linear-interpolation rule as the tool's p95 (add it in the markdown from the sorted degree list if the tool does not emit it). Re-run the 06.3.3 §5 canary entity checks with `--entity-name` for the two `require_graph_node` canaries' gold entities (SBF, Google).

    Write `06.3.4.1-STORE-BASELINE.md` in the 06.3.3 shape:
    - §1 identity: pre/post counts per store, the deleted LanceDB IDs (with duplicated-article titles) and PostgreSQL IDs, the alias status.
    - §2 per-table row counts and all seven table versions, before and after.
    - §3 the `index_generation` read from the engine probe (quoted from `document_map.json`, not retyped).
    - §4 degree distribution: min, median, p95, p99, max, isolated count, highest-degree entity ID and name, and the full sorted-degree histogram in 10 buckets.
    - §5 canary entity presence.
    - §6 the rollback layers with their paths and sizes.
    - The record that the 06.3.3/06.3.4 journals' `lance-701` numbers belong to the pre-reconcile store (the D-88 non-comparability input).

    Restart the engine afterwards and confirm `identity check` still passes.
  </action>
  <verify>
    <automated>grep -q "p99" .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-STORE-BASELINE.md && grep -q "index_generation" .planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-STORE-BASELINE.md && uv run --project eval python -c "import json; d = json.load(open('.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/reconcile/graph_population_post.json')); assert d, 'empty graph population'"</automated>
    <fails_when>non-zero exit: the baseline lacks a p99 or index_generation entry, or graph_population_post.json is empty or unparseable</fails_when>
  </verify>
  <acceptance_criteria>
    - The baseline's §3 generation string equals `document_map.json`'s `index_generation`.
    - §4 states p99 and max degree with the entity IDs; §1 lists every deleted ID from both stores.
  </acceptance_criteria>
  <done>The post-reconcile store is described in the shape later plans and 6.4 cite, including the degree histogram for D-77.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator approval → one-way delete | human authorisation gates an irreversible store mutation |
| engine probe → committed map | runtime value becomes committed provenance |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3.4.1-06-01 | Denial of service | reconcile apply | high | mitigate | Blocking checkpoint on the recorded dry run; three rollback layers before apply; apply output compared to the dry run and halts on difference |
| T-06.3.4.1-06-02 | Tampering | wrong store/schema | high | mitigate | Bin refuses non-eval path; `delete_pg_extras` asserts schema isolation and requires the pg_dump path |
| T-06.3.4.1-06-03 | Information disclosure | backups | medium | mitigate | Backups under gitignored `data/`; `git status --porcelain data/` must be empty; only ID lists and counts are committed |
| T-06.3.4.1-06-04 | Tampering | staged replay at restart | high | mitigate | Assert `staged_documents_v2_rows == 0` before the engine start; identity gate re-checks it |
</threat_model>

<verification>
- `uv run --project eval lancet-eval identity check --corpus multihop_rag` exits 0.
- `git diff --quiet --exit-code -- eval/runs/2026-09-03-multihop_rag/ eval/runs/2026-09-09-multihop_rag/` exits 0.
</verification>

<success_criteria>
Map, LanceDB and PostgreSQL agree on 346 documents, the engine serves a new probe-recorded generation,
and the post-reconcile baseline is committed.
</success_criteria>

## Artifacts this phase produces

- Store state: reconciled `data/lancedb-eval` and `lancet_eval` schema (runtime, not tracked); backups `data/lancedb-eval.pre-06.3.4.1-reconcile/`, `data/backups/lancet_eval.pre-06.3.4.1-reconcile.sql` (untracked).
- Files: `06.3.4.1-STORE-BASELINE.md`, `reconcile/{apply.json,pg_deleted.json,document_ids_post.json,graph_population_post.json}`; `document_map.json` `index_generation` updated.

<output>
Create `.planning/phases/06.3.4.1-retrieval-diagnosis-index-identity-and-graph-yield-repair/06.3.4.1-06-SUMMARY.md` when done.
</output>
