# Phase 999.3: Query Reformulation Strategies - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-18
**Phase:** 999.3-query-reformulation-strategies
**Areas discussed:** Reformulation Strategy, State Port, Latency Optimization, Merging & Deduplication

---

## Strategy & State Ports

| Option | Description | Selected |
|--------|-------------|----------|
| Dual-Strategy | (Recommended) Allow both HyDE (for conceptual questions) and Multi-Query Expansion (for keyword/search questions) based on a runtime flag or model configuration | ✓ |
| Strictly Multi-Query Expansion | Highly concurrent, great for showcasing Rust async/tokio tasks | |
| Strictly HyDE | Simpler concurrent model, but provides strong semantic recall improvements | |
| You decide | Agent will choose the best fit | |

**User's choice:** Dual-Strategy (D-01), Dedicated `QueryReformulator` async trait (D-02), Parallel execution optimization (D-03), RRF candidate merging (D-04).
**Notes:** Decided to write an async `QueryReformulator` trait in Rust that will return a list of query variants. In v1, this will execute as a pass-through returning the original query.

---

## Agent Discretion

- **Discretion-01:** The agent has discretion over the default RRF constant parameter ($k \approx 60$) and how the deduplication merges chunk source citations.

## Deferred Ideas
- None.
