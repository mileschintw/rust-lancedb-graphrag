---
phase: 04
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/Cargo.toml
  - engine/Cargo.lock
  - engine/src/lib.rs
  - engine/src/graph/mod.rs
  - engine/src/graph/bridge.rs
  - engine/src/graph/tests.rs
  - .planning/phases/04-knowledge-graph-extraction-query/04-AI-SPEC.md
  - .planning/phases/04-knowledge-graph-extraction-query/04-COVERAGE.md
autonomous: true
requirements: [DATA-04, DATA-05, RAG-05]

assumption_delta:
  outcome: no-change
  rationale: "Detector hits ('additional', 'fallback') sit inside ROADMAP.md's verbatim '(Deferred — Phase 04.1 Success Criteria, carried forward unchanged)' block, not Phase 4's live spike-scoped Success Criteria (items 1-4 above that block). Independently re-read both blocks: no singular->plural / required->optional / derived->chosen identity shift exists anywhere in Phase 4's actual current scope — the hits are forward-reference artifacts, not a live transition."

must_haves:
  truths:
    - "A developer reading 04-AI-SPEC.md Section 2 sees `Status: CONFIRMED` (not CONDITIONAL), citing 04-RESEARCH.md as the empirical evidence — satisfies ROADMAP Success Criterion 3."
    - "04-AI-SPEC.md's repo-URL correction note cites the lance-graph repository as github.com/lancedb/lance-graph, independently re-confirmed via crates.io's registry API per 04-RESEARCH.md — correcting 04-CONTEXT.md's canonical_refs error without editing that gated prior-phase artifact."
    - "Running `cargo test --manifest-path engine/Cargo.toml --features graph-spike graph::` produces 5 passing tests reproducing the bridge round-trip (both directions), a fixed single-hop Cypher query with relationship-property projection, multi-hop traversal, open-vocabulary relation_type WHERE filtering, and hop_cap clamp enforcement — the patterns 04-RESEARCH.md proved in a now-deleted scratch crate, now checked into this repository and reproducible on demand. Satisfies ROADMAP Success Criteria 1 and 2."
    - "Running `cargo build --manifest-path engine/Cargo.toml` and `cargo test --manifest-path engine/Cargo.toml --locked` (both with default features, no `--features graph-spike`) succeed and compile zero code under engine/src/graph/ — the PoC is feature-gated, not wired into the production build; the cfg-gate (not file placement) is what satisfies the phase framing's 'not wired into production lib.rs' constraint."
    - "Phase 04.1 can cite this phase's checked-in engine/src/graph/{mod.rs,bridge.rs,tests.rs} and the updated 04-AI-SPEC.md as a known, reproducible starting point rather than an open compatibility risk — satisfies ROADMAP Success Criterion 4."
  artifacts:
    - path: "engine/Cargo.toml"
      provides: "Optional, feature-gated lance-graph + IPC-bridge dependency additions"
      contains: "graph-spike = [\"dep:lance-graph\""
    - path: "engine/Cargo.lock"
      provides: "Regenerated lockfile resolving the real combined lancedb ~0.31 + lance-graph 0.5.4 manifest for the first time in this repository"
      contains: "name = \"lance-graph\""
    - path: "engine/src/lib.rs"
      provides: "Feature-gated module declaration proving the PoC is invisible to the default build"
      contains: "#[cfg(feature = \"graph-spike\")]"
    - path: "engine/src/graph/mod.rs"
      provides: "Checked-in traversal PoC: typed errors, hop-cap clamp, three Cypher query shapes"
      exports: ["GraphSpikeError", "GraphSpikeErrorKind", "clamp_hop_cap", "MAX_HOP_CAP", "traverse_fixed_hop", "traverse_multi_hop", "traverse_filtered_by_relation_type"]
    - path: "engine/src/graph/bridge.rs"
      provides: "Proven arrow ~58.3 <-> arrow ^56.2 IPC bridge, both directions"
      exports: ["bridge_batch", "bridge_batch_back"]
    - path: "engine/src/graph/tests.rs"
      provides: "Five fixture-backed tests reproducing 04-RESEARCH.md's four proven scenarios plus the hop-cap clamp guard"
      contains: "bridge_round_trip_preserves_schema_and_values"
    - path: ".planning/phases/04-knowledge-graph-extraction-query/04-AI-SPEC.md"
      provides: "Framework Decision status locked to CONFIRMED, citing empirical evidence; repo-URL correction carried forward with independent re-confirmation"
      contains: "CONFIRMED"
    - path: ".planning/phases/04-knowledge-graph-extraction-query/04-COVERAGE.md"
      provides: "Reasoned API-coverage opt-out declaration"
      contains: "No external API integration"
  key_links:
    - from: "engine/Cargo.toml"
      to: "engine/src/lib.rs"
      via: "The graph-spike feature flag gates the `pub mod graph;` declaration — this pairing is the entire 'not wired into production' boundary; breaking either half silently unwires or unconditionally wires the PoC"
      pattern: "graph-spike"
    - from: "engine/src/graph/mod.rs"
      to: "engine/src/graph/bridge.rs"
      via: "Every traverse_* function calls bridge_batch before and bridge_batch_back after CypherQuery::execute()"
      pattern: "bridge_batch"
    - from: "engine/src/graph/mod.rs::clamp_hop_cap"
      to: "engine/src/graph/mod.rs::traverse_multi_hop"
      via: "hop_cap is clamped before it reaches the format!-built Cypher string (V5 input-validation mitigation, 04-RESEARCH.md Security Domain)"
      pattern: "clamp_hop_cap"
    - from: ".planning/phases/04-knowledge-graph-extraction-query/04-AI-SPEC.md"
      to: ".planning/phases/04-knowledge-graph-extraction-query/04-RESEARCH.md"
      via: "Section 2's Status: CONFIRMED line cites RESEARCH.md's empirical evidence rather than asserting an unsupported claim"
      pattern: "04-RESEARCH.md"
  flagged_assumptions:
    - requirement_id: DATA-04
      category: idempotency
      statement: "DATA-04's idempotency behavior (what happens if entity/relationship extraction runs twice on the same chunk) is deferred to Phase 04.1 — not applicable to this spike's scope. This phase's own checked-in code (the graph-spike PoC) has no persistence step to be idempotent about; 04-RESEARCH.md's lance-graph/lancedb compatibility finding is the prerequisite Phase 04.1's idempotency design will build on."
    - requirement_id: DATA-04
      category: concurrency
      statement: "DATA-04's concurrency behavior (guarantees if extraction is interrupted or run in parallel) is deferred to Phase 04.1 — not applicable to this spike's scope. No concurrent-write path exists in this phase's PoC; the compatibility finding this phase closes is the prerequisite for 04.1's concurrency design."
    - requirement_id: DATA-05
      category: unclassified
      statement: "DATA-05's full query-graph-context-into-RAG-prompt behavior (seeding, traversal against real entities/edges tables, prompt interleaving) is deferred to Phase 04.1. This spike only empirically confirms the lance-graph/lancedb IPC-bridge integration pattern DATA-05's implementation will depend on, and checks in a reproducible PoC of that pattern."
    - requirement_id: RAG-05
      category: unclassified
      statement: "RAG-05's ContextAssemblyStrategy trait/enum definition (PrecomputedSemantics/SourceChunks) is deferred to Phase 04.1 — not applicable to this spike's scope. No such trait is defined by this phase."
  prohibitions:
    - statement: "MUST NOT silently merge two entity mentions into one node when they refer to different real-world things that merely share a case-normalized name (D-05's exact-match resolver over-conflating distinct entities) without any operator-visible signal — deferred to Phase 04.1's actual extraction/resolution implementation; not applicable to this phase's PoC, which persists nothing."
      status: unresolved
    - statement: "MUST NOT persist sensitive or PII-bearing extracted entity/relation content into the globally-merged, undeletable v1 entities graph without redaction or scoping — deferred to Phase 04.1's actual extraction/persistence implementation; not applicable to this phase's PoC, which touches only synthetic fixture data and no real lancedb tables."
      status: unresolved
    - statement: "MUST NOT surface graph-derived entities/relationships (or a silently-failed graph augmentation) into a compiled RAG answer in a way indistinguishable in trustworthiness from citation-grounded chunk evidence — deferred to Phase 04.1's actual prompt-integration implementation; not applicable to this phase, which has no RAG-answer prompt assembly code."
      status: unresolved
---

<objective>
Close the CONDITIONAL status on 04-AI-SPEC.md's Framework Decision (Section 2) by making the Phase 04 spike's empirical finding durable, reviewable, and reproducible in this repository — per D-17/D-19's already-locked choice of `lance-graph` over hand-rolled DataFusion traversal, and per the ROADMAP's spike-scoped Success Criteria (NOT the deferred Phase 04.1 full-feature criteria).

04-RESEARCH.md already answered the spike's core unknown empirically, using a throwaway crate that was built, compiled, executed, and then deleted — nothing from it exists in this repository yet. This plan (a) checks in a minimal, feature-gated, non-production proof-of-concept reproducing the four patterns that throwaway crate proved (bridge round-trip, fixed single-hop, multi-hop, open-vocabulary relation_type filter), so the pattern is reviewable and re-runnable via `cargo test`, not just described in prose, and (b) updates 04-AI-SPEC.md's Status field from CONDITIONAL to CONFIRMED with a citation to that evidence, and carries forward the lance-graph repo-URL correction.

This plan does NOT implement DATA-04's extraction pipeline, DATA-05's real graph-query-into-RAG-prompt path, or RAG-05's ContextAssemblyStrategy trait — those are Phase 04.1's job per ROADMAP's "Deferred target" line and are explicitly out of scope here (see `flagged_assumptions` above).

Purpose: Turn "we know enough to plan Phase 04.1" from a claim into a checkable fact — a developer can run `cargo test --features graph-spike` and see the integration pattern work, and read 04-AI-SPEC.md and see the risk closed.
Output: A feature-gated `engine/src/graph/` PoC (not wired into the default build), an updated 04-AI-SPEC.md, and a `04-COVERAGE.md` API-coverage declaration.
</objective>

## Artifacts This Phase Produces

This phase creates or modifies exactly these symbols/files — no others:

- **`engine/Cargo.toml`** — 4 new optional dependency lines (`lance-graph`, `arrow-ipc`, `arrow-lg`, `arrow-ipc-lg`) + a new `[features]` section defining `graph-spike`.
- **`engine/Cargo.lock`** — regenerated by Cargo as a side effect of the above (expected to newly contain lance-graph's ~350+ transitive crates, none of which compile under default features).
- **`engine/src/lib.rs`** — 1 new feature-gated line: `#[cfg(feature = "graph-spike")] pub mod graph;`.
- **`engine/src/graph/mod.rs`** (new file) — `GraphSpikeErrorKind`, `GraphSpikeError`, `MAX_HOP_CAP`, `clamp_hop_cap`, `traverse_fixed_hop`, `traverse_multi_hop`, `traverse_filtered_by_relation_type`.
- **`engine/src/graph/bridge.rs`** (new file) — `bridge_batch`, `bridge_batch_back`.
- **`engine/src/graph/tests.rs`** (new file) — 2 fixture builders, 5 `#[test]` functions.
- **`.planning/phases/04-knowledge-graph-extraction-query/04-AI-SPEC.md`** — Section 2 Status field, Section 2 repo-URL note, Section 3 introductory callout updated (no new sections added).
- **`.planning/phases/04-knowledge-graph-extraction-query/04-COVERAGE.md`** (new file) — one-line opt-out declaration.

No production ingestion, query, gRPC, or prompt-assembly code is created or modified. No `entities`/`edges` LanceDB tables are created. `engine/src/main.rs` and `proto/lancet/v1/lancet.proto` are untouched.

<assumption_delta_decision>
outcome: no-change
rationale: Independently re-read ROADMAP.md's Phase 4 section. The "additional"/"fallback" detector hits are both inside the `*(Deferred — Phase 04.1 Success Criteria, carried forward unchanged:)*` block (items 3 and 4 of that deferred list), which is the ORIGINAL Phase 4 success criteria preserved verbatim for forward reference by the future 04.1 phase — not part of Phase 4's actual current spike-scoped Success Criteria (the four numbered items directly under "**Success Criteria:**", none of which contain either word). No singular->plural, required->optional, or derived->chosen identity transition exists in this phase's live scope; the checkpoint does not fire.
</assumption_delta_decision>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-knowledge-graph-extraction-query/04-CONTEXT.md
@.planning/phases/04-knowledge-graph-extraction-query/04-RESEARCH.md
@.planning/phases/04-knowledge-graph-extraction-query/04-PATTERNS.md
@.planning/phases/04-knowledge-graph-extraction-query/04-AI-SPEC.md
@rust-guidelines.md
@engine/Cargo.toml
@engine/src/lib.rs
@engine/src/retrieval/mod.rs

<interfaces>
From engine/src/retrieval/mod.rs: the project's typed-error convention — a `{X}ErrorKind` enum (`Debug, Clone, PartialEq, Eq`) paired with a `{X}Error { pub kind, message: String }` struct exposing `new(kind, message: impl Into<String>)`, `message()`, `Display`, and `std::error::Error`. `engine/src/graph/mod.rs`'s `GraphSpikeError`/`GraphSpikeErrorKind` must mirror this exactly (per rust-guidelines M-ERRORS-CANONICAL-STRUCTS) rather than reusing 04-RESEARCH.md's scratch-crate `Result<T, String>` shape, which was written outside this codebase's conventions.

From 04-RESEARCH.md `## Code Examples`: the exact, empirically-proven `bridge_batch` implementation (IPC encode with `arrow_ipc::writer::StreamWriter`, decode with `arrow_ipc_lg::reader::StreamReader`), the `GraphConfigBuilder`/`CypherQuery` traversal shape, and all four proven Cypher query strings and their exact expected row counts. Do not invent new assertions beyond what RESEARCH.md's fixtures proved — reuse its fixture shapes verbatim (2 entities/1 edge for the multi-hop case; 3 entities/2 edges for the single-hop-property and relation_type-filter cases).

From 04-AI-SPEC.md Section 3: the `bridge_batch_back` snippet and the `RelationshipMapping` construction — both explicitly marked "illustrative, NOT compiled/verified" there. This plan is what makes them compiled, tested facts.

Handoff produced by this plan: `engine::graph::{traverse_fixed_hop, traverse_multi_hop, traverse_filtered_by_relation_type, clamp_hop_cap, MAX_HOP_CAP}` and `engine::graph::bridge::{bridge_batch, bridge_batch_back}`, all gated behind the `graph-spike` Cargo feature — Phase 04.1 either builds on these directly or supersedes them once real `entities`/`edges` LanceDB tables exist.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Resolve the real combined manifest and prove the IPC bridge + fixed single-hop Cypher query end-to-end</name>
  <files>engine/Cargo.toml, engine/Cargo.lock, engine/src/lib.rs, engine/src/graph/mod.rs, engine/src/graph/bridge.rs, engine/src/graph/tests.rs</files>
  <read_first>
    - rust-guidelines.md
    - .planning/phases/04-knowledge-graph-extraction-query/04-RESEARCH.md
    - .planning/phases/04-knowledge-graph-extraction-query/04-PATTERNS.md
    - .planning/phases/04-knowledge-graph-extraction-query/04-CONTEXT.md
    - engine/Cargo.toml
    - engine/src/lib.rs
    - engine/src/retrieval/mod.rs
    - engine/src/retrieval/tests.rs
  </read_first>
  <action>
    Step 0 (resolution gate — run BEFORE creating any file under engine/src/graph/): edit engine/Cargo.toml only, then run `cargo check --manifest-path engine/Cargo.toml --features graph-spike` and confirm it resolves and compiles. This is the one genuine unknown 04-RESEARCH.md's scratch crate never actually exercised — that crate had no `lancedb` dependency at all, and the AI-SPEC's earlier `cargo add lance-graph` + `cargo tree --duplicates` check was done without the arrow renames and was fully reverted. The manifest you are about to lock — `lancedb ~0.31` + `lance-graph 0.5.4` + the renamed `arrow-lg`/`arrow-ipc-lg` crates, all in one Cargo.lock — has never been resolved together before. If Step 0 fails, STOP and report the resolver conflict; do not proceed to Step 1.

    Cargo.toml edit: append to `[dependencies]` (after the existing `unicode-segmentation` line, do not reorder existing lines): `lance-graph = { version = "0.5.4", default-features = false, optional = true }`, `arrow-ipc = { version = "~58.3", optional = true }`, `arrow-lg = { package = "arrow", version = "^56.2", optional = true }`, `arrow-ipc-lg = { package = "arrow-ipc", version = "^56.2", optional = true }`. Then append a new `[features]` section: `graph-spike = ["dep:lance-graph", "dep:arrow-ipc", "dep:arrow-lg", "dep:arrow-ipc-lg"]`. This is D-17/D-19's already-locked `lance-graph` choice, added as an optional dependency with zero production call sites — reversibility is rated on this task below.

    Step 1: in engine/src/lib.rs, insert `#[cfg(feature = "graph-spike")]` followed by `pub mod graph;` between the existing `pub mod generation;` and `pub mod prompt;` lines (alphabetical position, matching 04-PATTERNS.md's module-registration convention). This cfg-gate — not file placement — is what makes the PoC invisible to the default build; state this explicitly in the module doc-comment you write in Step 2.

    Step 2: create engine/src/graph/bridge.rs with a `//!` module doc-comment (first sentence <= 15 words per M-MODULE-DOCS/M-FIRST-DOC-SENTENCE) describing it as the arrow-version IPC bridge. Define `pub(crate) fn bridge_batch(batch: &arrow_array::RecordBatch) -> Result<arrow_lg::record_batch::RecordBatch, super::GraphSpikeError>` — forward direction (engine arrow ~58.3 into lance-graph's arrow ^56.2), via `arrow_ipc::writer::StreamWriter` then `arrow_ipc_lg::reader::StreamReader`, following 04-RESEARCH.md's `## Code Examples` "The arrow-version IPC bridge (proven working)" snippet exactly in logic, but replacing every `.map_err(|e| format!(...))` with `.map_err(|e| GraphSpikeError::new(GraphSpikeErrorKind::Bridge, format!(...)))`. Also define `pub(crate) fn bridge_batch_back(batch: &arrow_lg::record_batch::RecordBatch) -> Result<arrow_array::RecordBatch, super::GraphSpikeError>` — the inverse direction (arrow ^56.2 back to arrow ~58.3), via `arrow_ipc_lg::writer::StreamWriter` then `arrow_ipc::reader::StreamReader`, matching 04-AI-SPEC.md Section 3's `bridge_batch_back` illustrative snippet. `bridge_batch_back` was never compiled or run anywhere before this task — it exists only as an "illustrative, NOT compiled/verified" snippet in 04-AI-SPEC.md Section 3 — so getting this function right, and proving it in Step 4's round-trip test, is this task's single highest-value output.

    Step 3: create engine/src/graph/mod.rs with a `//!` module doc-comment describing this as the checked-in Phase 04 spike PoC, gated behind `graph-spike`, not part of the default build (describe the end state per M-NO-META-DESIGN-DOCUMENTATION — not a narrative of how the spike was discovered). Add `pub mod bridge;`. Define `#[derive(Debug, Clone, PartialEq, Eq)] pub enum GraphSpikeErrorKind { Bridge, GraphConfig, CypherParse, CypherExecute, InvalidHopCap }` and `#[derive(Debug, Clone, PartialEq, Eq)] pub struct GraphSpikeError { pub kind: GraphSpikeErrorKind, message: String }`, mirroring `engine::retrieval::RetrievalError`/`RetrievalErrorKind` exactly: `GraphSpikeError::new(kind, message: impl Into<String>)`, `pub fn message(&self) -> &str`, `impl Display`, `impl std::error::Error`. Define `pub const MAX_HOP_CAP: u32 = 3;` (resolving D-23's Claude's-Discretion "small bounded number like 3" default) and `pub fn clamp_hop_cap(requested: u32) -> Result<u32, GraphSpikeError>` returning `Err(GraphSpikeError::new(GraphSpikeErrorKind::InvalidHopCap, ...))` for `requested == 0` or `requested > MAX_HOP_CAP`, `Ok(requested)` otherwise. Define `pub async fn traverse_fixed_hop(entities: &arrow_array::RecordBatch, edges: &arrow_array::RecordBatch, seed_id: &str) -> Result<arrow_array::RecordBatch, GraphSpikeError>`: bridge both batches via `bridge::bridge_batch`, build a `GraphConfigBuilder` with `.with_node_label("Entity", "entity_id")` and a `RelationshipMapping { relationship_type: "RELATED".into(), source_id_field: "source_node_id".into(), target_id_field: "target_node_id".into(), type_field: Some("relation_type".into()), property_fields: vec!["relation_type".into()], filter_conditions: None }` via `.with_relationship_mapping(...)`, run the fixed single-hop Cypher `MATCH (seed:Entity {entity_id: $seed_id})-[r:RELATED]-(neighbor:Entity) RETURN seed.entity_id, r.relation_type, neighbor.entity_id, neighbor.name` (no `*` quantifier — RESEARCH.md's Pitfall 6 confirms fixed-hop patterns can project `r.<property>` directly, unlike variable-length ones) via `CypherQuery::new(...)?.with_config(config).with_parameter("seed_id", seed_id).execute(datasets, None::<ExecutionStrategy>)`, then bridge the result back via `bridge::bridge_batch_back`. Map every fallible step to the matching `GraphSpikeErrorKind` (`GraphConfig`, `CypherParse`, `CypherExecute`).

    Step 4: create engine/src/graph/tests.rs and register it with `#[cfg(test)] mod tests;` at the bottom of mod.rs (mirroring engine/src/retrieval/mod.rs's `tests` module registration). Add a `three_entity_two_edge_fixture() -> (arrow_array::RecordBatch, arrow_array::RecordBatch)` fixture builder producing entities (`entity_id`/`name`/`entity_type` Utf8 columns: Alice/abc-123, Bob/def-456, Acme/ghi-789) and edges (`source_node_id`/`target_node_id`/`relation_type` Utf8 columns: abc-123→def-456 "knows", abc-123→ghi-789 "founded_by") — RESEARCH.md's exact proven fixture. Write `#[test] fn bridge_round_trip_preserves_schema_and_values()`: take the fixture's entities `RecordBatch`, call `bridge::bridge_batch` then immediately `bridge::bridge_batch_back` on the result, and assert the final batch's schema (field names and count) and every cell value are identical to the original — this single test is what proves `bridge_batch_back`, the previously-unverified half. Write `#[test] fn fixed_single_hop_projects_relationship_properties()`: call `traverse_fixed_hop` with seed `"abc-123"` against the fixture, `await` it via a `#[tokio::test]` attribute (matching the existing async-test convention), and assert exactly 2 rows are returned with the set of `r.relation_type` values equal to `{"knows", "founded_by"}` (RESEARCH.md's exact proven result: "rows=2, cols=4, schema includes r.relation_type").
  </action>
  <verify>
    <automated>cargo check --manifest-path engine/Cargo.toml --features graph-spike; $cargoExit = $LASTEXITCODE; if ($cargoExit -ne 0) { exit $cargoExit }; cargo build --manifest-path engine/Cargo.toml; $buildExit = $LASTEXITCODE; if ($buildExit -ne 0) { exit $buildExit }; $testList = cargo test --manifest-path engine/Cargo.toml --features graph-spike -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; foreach ($name in @('bridge_round_trip_preserves_schema_and_values','fixed_single_hop_projects_relationship_properties')) { if ($testList -notmatch ('(?m)' + [regex]::Escape($name))) { throw "Expected graph-spike test is not registered: $name" } }; $testOutput = cargo test --manifest-path engine/Cargo.toml --features graph-spike graph::; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }; if ($testOutput -notmatch '(?m)running\s+[1-9]\d*\s+tests?') { throw 'graph-spike test filter matched zero tests' }; $libContent = Get-Content -Raw 'engine/src/lib.rs'; if ($libContent -notmatch 'cfg\(feature = "graph-spike"\)') { throw 'lib.rs is missing the feature-gated graph module declaration' }</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check --manifest-path engine/Cargo.toml --features graph-spike` exits 0 — proving the combined `lancedb ~0.31` + `lance-graph 0.5.4` + `arrow-lg ^56.2` manifest resolves and compiles for the first time in this repository, confirmed BEFORE engine/src/graph/mod.rs or bridge.rs are created (Step 0 ordering).
    - `cargo build --manifest-path engine/Cargo.toml` (default features, no `--features` flag) exits 0, proving the default build is unaffected by the new optional dependencies.
    - `cargo test --manifest-path engine/Cargo.toml --features graph-spike graph::` shows both `bridge_round_trip_preserves_schema_and_values` and `fixed_single_hop_projects_relationship_properties` passing.
    - `engine/src/lib.rs` contains `#[cfg(feature = "graph-spike")]` immediately preceding `pub mod graph;`.
    - `engine/Cargo.lock` is regenerated and includes a `lance-graph` package entry.
  </acceptance_criteria>
  <reversibility rating="reversible">graph-spike is an optional, Cargo-feature-gated dependency with zero production call sites (no unconditional lib.rs wiring, no consumer outside the PoC's own tests) — removal is a Cargo.toml edit plus deleting engine/src/graph/, with nothing else to migrate. This is distinct from D-17's own "costly" production-scope rating in 04-CONTEXT.md (which concerns swapping traversal engines once real call sites depend on one) — that rating is carried forward separately on Task 3, which finalizes D-17's status for Phase 04.1 to inherit as settled.</reversibility>
  <done>The real combined Cargo manifest resolves and compiles, and one full IPC-bridge-plus-Cypher path is proven end to end by a passing, checked-in test — not just described in a deleted scratch crate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the PoC to multi-hop traversal, relation_type filtering, and hop_cap clamp enforcement</name>
  <files>engine/src/graph/mod.rs, engine/src/graph/tests.rs</files>
  <read_first>
    - .planning/phases/04-knowledge-graph-extraction-query/04-RESEARCH.md
    - engine/src/graph/mod.rs
    - engine/src/graph/bridge.rs
    - engine/src/graph/tests.rs
  </read_first>
  <behavior>
    - `traverse_multi_hop` with hop_cap=3 against RESEARCH.md's proven 2-entity/1-edge fixture (Alice/abc-123 --knows--> Bob/def-456), seeded on "abc-123": returns exactly 1 row with `neighbor.entity_id == "def-456"` (RESEARCH.md's literal proven result — no new unproven assertion on multi-edge path-reuse semantics).
    - `traverse_filtered_by_relation_type` against the 3-entity/2-edge fixture, seeded on "abc-123" with `relation_type = "founded_by"`: returns exactly 1 row with `neighbor.entity_id == "ghi-789"` (RESEARCH.md's literal proven result), excluding the "knows"-typed edge to Bob.
    - `clamp_hop_cap(0)` and `clamp_hop_cap(MAX_HOP_CAP + 1)` both return `Err` with `kind == GraphSpikeErrorKind::InvalidHopCap`; `clamp_hop_cap(MAX_HOP_CAP)` returns `Ok(MAX_HOP_CAP)`.
  </behavior>
  <action>
    Add `pub async fn traverse_multi_hop(entities: &arrow_array::RecordBatch, edges: &arrow_array::RecordBatch, seed_id: &str, hop_cap: u32) -> Result<arrow_array::RecordBatch, GraphSpikeError>` to engine/src/graph/mod.rs: call `clamp_hop_cap(hop_cap)?` FIRST, before any Cypher string is built (this is the V5 input-validation guard from 04-RESEARCH.md's Security Domain — hop_cap must never reach the `format!` call unclamped). Then bridge both batches, build the same `GraphConfigBuilder` node-label config as `traverse_fixed_hop`, and run `format!("MATCH (seed:Entity {{entity_id: $seed_id}})-[r:RELATED*1..{hop_cap}]-(neighbor:Entity) RETURN seed.entity_id, neighbor.entity_id, neighbor.name")` — per RESEARCH.md's Pitfall 6, `r` must be omitted from this `RETURN` clause because variable-length quantifiers cannot project the relationship-pattern variable.

    Add `pub async fn traverse_filtered_by_relation_type(entities: &arrow_array::RecordBatch, edges: &arrow_array::RecordBatch, seed_id: &str, relation_type: &str) -> Result<arrow_array::RecordBatch, GraphSpikeError>`: same bridging/config setup, Cypher string `"MATCH (seed:Entity {entity_id: $seed_id})-[r:RELATED]-(neighbor:Entity) WHERE r.relation_type = $relation_type RETURN neighbor.entity_id, neighbor.name"`, with BOTH `seed_id` and `relation_type` passed as `.with_parameter(...)` values (not string-interpolated) since Cypher parameterizes ordinary value predicates safely, unlike the hop-count bound.

    Add a `two_entity_one_edge_fixture() -> (arrow_array::RecordBatch, arrow_array::RecordBatch)` builder to engine/src/graph/tests.rs mirroring RESEARCH.md's exact multi-hop fixture (Alice/abc-123, Bob/def-456; one "knows" edge abc-123→def-456). Add the three `<behavior>` tests above: `multi_hop_traversal_finds_one_hop_neighbor`, `relation_type_filter_excludes_non_matching_edge`, `clamp_hop_cap_rejects_zero_and_over_max` (this last one is synchronous, no `#[tokio::test]` needed).
  </action>
  <verify>
    <automated>$testList = cargo test --manifest-path engine/Cargo.toml --features graph-spike -- --list; $listExit = $LASTEXITCODE; if ($listExit -ne 0) { exit $listExit }; foreach ($name in @('bridge_round_trip_preserves_schema_and_values','fixed_single_hop_projects_relationship_properties','multi_hop_traversal_finds_one_hop_neighbor','relation_type_filter_excludes_non_matching_edge','clamp_hop_cap_rejects_zero_and_over_max')) { if ($testList -notmatch ('(?m)' + [regex]::Escape($name))) { throw "Expected graph-spike test is not registered: $name" } }; $testOutput = cargo test --manifest-path engine/Cargo.toml --features graph-spike graph::; $testExit = $LASTEXITCODE; if ($testExit -ne 0) { exit $testExit }; if ($testOutput -notmatch '(?m)running\s+[1-9]\d*\s+tests?') { throw 'graph-spike test filter matched zero tests' }; $defaultOutput = cargo test --manifest-path engine/Cargo.toml --locked; $defaultExit = $LASTEXITCODE; if ($defaultExit -ne 0) { exit $defaultExit }; if ($defaultOutput -match 'graph::') { throw 'Default-feature (--locked) test run must not include any graph-spike tests' }</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --manifest-path engine/Cargo.toml --features graph-spike graph::` reports exactly 5 tests passing (the 2 from Task 1 plus these 3).
    - `cargo test --manifest-path engine/Cargo.toml --locked` (default features) passes and its output contains no `graph::`-prefixed test names — proving the default CI gate (AI-SPEC Section 5, VALIDATION.md's full-suite command) is unaffected by this phase's additions.
    - `clamp_hop_cap(0)` and `clamp_hop_cap(4)` both return `Err` with `kind == GraphSpikeErrorKind::InvalidHopCap`; `clamp_hop_cap(3)` returns `Ok(3)`.
  </acceptance_criteria>
  <done>All four of 04-RESEARCH.md's proven Cypher/bridge scenarios plus the hop-cap clamp guard are checked into this repository as passing tests, and the existing default-feature test suite remains green and unaware of any of it.</done>
</task>

<task type="auto">
  <name>Task 3: Lock 04-AI-SPEC.md's Framework Decision to CONFIRMED and record the API-coverage opt-out</name>
  <files>.planning/phases/04-knowledge-graph-extraction-query/04-AI-SPEC.md, .planning/phases/04-knowledge-graph-extraction-query/04-COVERAGE.md</files>
  <read_first>
    - .planning/phases/04-knowledge-graph-extraction-query/04-AI-SPEC.md
    - .planning/phases/04-knowledge-graph-extraction-query/04-RESEARCH.md
    - .planning/phases/04-knowledge-graph-extraction-query/04-CONTEXT.md
  </read_first>
  <action>
    In 04-AI-SPEC.md Section 2 "Framework Decision," replace the `**Status:**` paragraph (currently describing the pending spike and its two open questions) with a paragraph stating the status is CONFIRMED, empirically verified via the Phase 04 spike, and pointing readers to 04-RESEARCH.md's `## Summary` and `## Code Examples` sections as the evidence — name specifically that the IPC bridge compiled and round-tripped real data, that `CypherQuery::execute()` takes a plain `HashMap<String, RecordBatch>` rather than a path/URI or typed `lance::Dataset` handle, and that both of the section's original open questions are therefore resolved (the second question is moot because no `lance::Dataset` handle is ever touched on this path). Keep the existing "Critical Finding" and numbered-questions prose below it as an unmodified historical record of what was being verified.

    In the same section's "**Correction to upstream artifact:**" paragraph, add one sentence noting that 04-RESEARCH.md's Package Legitimacy Audit independently re-confirmed the corrected URL via crates.io's registry API (`repository` field), strengthening the existing correction with a second, independent citation. Do not edit 04-CONTEXT.md itself — its canonical_refs error remains uncorrected in that file by design, consistent with this section's existing note that CONTEXT.md is a prior phase's gated artifact outside this workflow's write scope.

    In Section 3 "Framework Quick Reference," rewrite the introductory callout paragraph (currently instructing the reader to treat everything past that note as unverified pending the spike) to state plainly that the Phase 04 spike ran, the pattern below is empirically confirmed, and the checked-in reference implementation lives at engine/src/graph/{mod.rs,bridge.rs,tests.rs} (this phase's Task 1/2 output) rather than only in this document's illustrative code blocks. In the "Entry Point Pattern" code block's inline comments, update the `bridge_batch_back` comment and the `RelationshipMapping.type_field` comment (both currently flagging themselves as unverified) to instead note they are now confirmed working, matching Pitfall 4's "resolved, not just clarified" framing already present in 04-RESEARCH.md.

    Create .planning/phases/04-knowledge-graph-extraction-query/04-COVERAGE.md containing exactly one line of prose: a statement that no external API integration exists in this phase because lance-graph is a local, in-process Rust crate dependency rather than a hosted API or service — this is the API Coverage Decision Checkpoint's reasoned opt-out declaration for this phase.
  </action>
  <verify>
    <automated>$aiSpec = Get-Content -Raw '.planning/phases/04-knowledge-graph-extraction-query/04-AI-SPEC.md'; if ($aiSpec -notmatch 'Status:\*\*\s*CONFIRMED') { throw 'AI-SPEC Section 2 Status field does not read CONFIRMED' }; if ($aiSpec -notmatch '04-RESEARCH\.md') { throw 'AI-SPEC Status update does not cite 04-RESEARCH.md' }; if (-not (Test-Path '.planning/phases/04-knowledge-graph-extraction-query/04-COVERAGE.md')) { throw '04-COVERAGE.md is missing' }; $coverage = Get-Content -Raw '.planning/phases/04-knowledge-graph-extraction-query/04-COVERAGE.md'; if ($coverage -notmatch 'No external API integration') { throw '04-COVERAGE.md is missing the opt-out declaration' }</automated>
  </verify>
  <acceptance_criteria>
    - 04-AI-SPEC.md Section 2's Status field reads `CONFIRMED` and the surrounding paragraph cites `04-RESEARCH.md` by name.
    - 04-AI-SPEC.md Section 2's repo-URL correction paragraph cites `crates.io`'s registry API as an independent re-confirmation source.
    - 04-CONTEXT.md is NOT modified by this task (verify via `git diff --name-only` excluding it).
    - `.planning/phases/04-knowledge-graph-extraction-query/04-COVERAGE.md` exists and contains the string `No external API integration`.
  </acceptance_criteria>
  <reversibility rating="costly">Carried forward from D-17 in 04-CONTEXT.md ("swapping traversal engines later means rewriting every Cypher-pattern call site and re-verifying node/edge dataset compatibility"). This task is what finalizes D-17/D-19's status from CONDITIONAL to confirmed for Phase 04.1 to inherit as a settled architectural choice — flagged per the reversibility taxonomy, not gated with a checkpoint, since the human choice to select lance-graph over hand-rolled DataFusion was already made in 04-CONTEXT.md's D-17/D-19 (the door was already walked through; this task records the confirmation, it does not re-ask the question).</reversibility>
  <done>04-AI-SPEC.md's Framework Decision is locked to CONFIRMED with a citation to empirical evidence, the repo-URL correction is independently re-confirmed, and the API-coverage checkpoint is resolved with a written opt-out declaration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| hop_cap (internal Rust `u32`) to Cypher string | `traverse_multi_hop` builds a Cypher `*1..{hop_cap}` bound via `format!` — Cypher cannot parameterize this bound, so the value must be range-checked before interpolation. No network/gRPC caller reaches this in Phase 04 (no RPC wiring exists yet); this boundary is what Phase 04.1's `QueryGraph` RPC will inherit unchanged. |
| Cargo registry to build output | The four new optional dependencies (`lance-graph`, `arrow-ipc`, `arrow-lg`, `arrow-ipc-lg`) enter `engine/Cargo.lock`; only `graph-spike`-feature builds compile them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-01-01 | Tampering | `format!`-built Cypher string in `traverse_multi_hop` (hop_cap interpolation) | medium | mitigate | `clamp_hop_cap` rejects `0` and values above `MAX_HOP_CAP` (3) before the value ever reaches `format!`; `clamp_hop_cap_rejects_zero_and_over_max` (Task 2) proves the boundary. Carries forward 04-RESEARCH.md's Security Domain (V5 Input Validation) and 04-AI-SPEC.md Section 6's hop-cap guardrail — this PoC is the first place either is actually implemented in Rust rather than only documented. |
| T-04-01-02 | Information Disclosure | `bridge_batch`/`bridge_batch_back` error paths | low | accept | The PoC operates only on synthetic, non-sensitive fixture data — no real `lancedb` table, no gRPC/HTTP surface exists in this phase. `GraphSpikeError` messages carry only Arrow IPC codec error text, never row values. Acceptable for a non-production, feature-gated PoC; revisit when Phase 04.1 wires this against real `entities`/`edges` tables that may carry extracted, possibly-sensitive content. |
| T-04-01-SC | Tampering | Cargo dependency install (`lance-graph`, `arrow-ipc`, `arrow-lg`, `arrow-ipc-lg`) | high | mitigate | 04-RESEARCH.md's Package Legitimacy Audit already ran: `lance-graph` verdict `[OK]` (crates.io, ~3,659 downloads/week, repository independently confirmed as `github.com/lancedb/lance-graph`); `arrow-lg`/`arrow-ipc-lg` are renamed references to the already-vetted upstream `apache/arrow-rs` project, not new packages. No `[ASSUMED]`/`[SUS]` verdicts among this phase's additions, so no blocking `checkpoint:human-verify` is required. |
| T-04-01-03 | Denial of Service (build/CI cost, not runtime) | Full transitive dependency footprint (~350+ crates, cloud/geospatial deps per 04-RESEARCH.md Pitfall 5) | low | accept | Build-time/CI-budget concern, not a runtime attack surface — the tree is feature-gated and does not compile under the default `cargo build`/`cargo test --locked` this phase's own verification relies on (Task 1/2 acceptance criteria). Phase 04.1 should budget CI time for the `graph-spike`-enabled build per Pitfall 5. |

**Canon-referral breadcrumbs (not minted as prohibitions, per prohibition-probe protocol):** indirect prompt injection via chunk text altering extraction output is canon (OWASP LLM injection) and is N/A to this phase (no extraction code exists yet) — already covered by 04-AI-SPEC.md Section 5's injection-resistance eval dimension for Phase 04.1. GDPR/right-to-erasure for durably-persisted extracted entities is canon/compliance and is already flagged by 04-AI-SPEC.md's Regulatory Context as out-of-scope future deployment-hardening work, not re-litigated here.
</threat_model>

<verification>
- Confirm `cargo check --manifest-path engine/Cargo.toml --features graph-spike` and `cargo test --manifest-path engine/Cargo.toml --features graph-spike graph::` both succeed with 5 passing tests, proving the real combined manifest resolves and 04-RESEARCH.md's four proven patterns plus the hop-cap clamp are reproducible in this repository.
- Confirm `cargo build --manifest-path engine/Cargo.toml` and `cargo test --manifest-path engine/Cargo.toml --locked` (both default features, no `--features graph-spike`) succeed and produce zero `graph::`-prefixed output — proving the PoC is feature-gated, not production-wired.
- Confirm 04-AI-SPEC.md Section 2's Status field reads CONFIRMED with a 04-RESEARCH.md citation, and 04-COVERAGE.md exists with the opt-out declaration.
- Confirm 04-CONTEXT.md is untouched (`git diff --name-only` for this plan's commit excludes it).
</verification>

<success_criteria>
`lance-graph` 0.5.4's Cypher traversal API surface is empirically confirmed and checked into this repository as reproducible, passing tests (not just inferred from docs or described in a deleted scratch crate). 04-AI-SPEC.md's Critical Finding version-conflict question is resolved with a documented, reproducible integration pattern, and its Framework Decision status reads CONFIRMED rather than CONDITIONAL, citing that evidence. Phase 04.1 can be planned with the lance-graph/lancedb integration pattern as a known quantity.
</success_criteria>

<output>
Create `.planning/phases/04-knowledge-graph-extraction-query/04-01-SUMMARY.md` when done.
</output>
