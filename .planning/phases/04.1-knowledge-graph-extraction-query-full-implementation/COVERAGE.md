No external API integration: this phase's only "API"-shaped surfaces are the phase's own new/redesigned internal gRPC RPC (`QueryGraph`, D-20, gRPC-only per D-25 — not a third-party service) and the existing OpenRouter `Generator`/`ExtractionGenerator` HTTP seam, whose provider integration (auth, endpoint, retry policy at the RAG-answer layer) was already decided in Phase 3 (D-08) and is only reused here via a sibling trait (D-08) for entity/relation extraction, not re-integrated against a new external service. `lance-graph` (the other new dependency this phase promotes to the default build) is an embedded, in-process Rust crate performing local Cypher-over-Arrow pattern matching — not a network-facing external API/SDK/webhook surface — so it has no capability surface to enumerate in a coverage matrix. The detector's single signal (`api-coverage.cjs`) matched the word "API" inside a `04.1-04-PLAN.md` prohibition statement describing this phase's own internal `QueryGraph` response contract ("MUST NOT build the QueryGraph API response from traverse_fixed_hop's Cypher-projected result...") — a false-positive trigger on the phrase "QueryGraph API response," not a reference to any external API integration.
