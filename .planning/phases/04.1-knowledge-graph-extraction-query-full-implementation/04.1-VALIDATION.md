---
phase: "04.1"
slug: knowledge-graph-extraction-query-full-implementation
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: true
created: 2026-08-07
---

# Phase 04.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
>
> **Provenance:** Carried over from `04-VALIDATION.md` (Phase 04, the `lance-graph`/`lancedb`
> compatibility spike) at the user's direction — Phase 04's research and validation artifacts
> were written explicitly as forward-looking context for this phase (see `04.1-RESEARCH.md`'s
> provenance note). This copy has been revised below where Phase 04's completion changed the
> facts on the ground (the spike's proof-of-concept was checked in behind the `graph-spike`
> Cargo feature, not deleted as originally planned) and extended to cover Waves 2-5, which did
> not exist when `04-VALIDATION.md` was authored.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo test` (built-in Rust test harness), matching the existing `engine/src/tests.rs` convention (Phase 2 precedent) |
| **Config file** | none — Cargo's built-in test runner needs no separate config |
| **Quick run command** | `cargo test --manifest-path engine/Cargo.toml --features graph-spike graph::` (the `graph` module exists now, gated behind the `graph-spike` feature per `engine/Cargo.toml`; Wave 1 of this phase promotes it out of that gate — update this command once that lands) |
| **Full suite command** | `cargo test --manifest-path engine/Cargo.toml --features graph-spike --locked` |
| **Estimated runtime** | ~30 seconds (consistent with the existing engine suite; verified below) |

---

## Sampling Rate

- **After every task commit:** Run `cargo test --manifest-path engine/Cargo.toml --features graph-spike graph::`
- **After every plan wave:** Run `cargo test --manifest-path engine/Cargo.toml --features graph-spike --locked && cd gateway && go test ./...` (matches Phase 03 precedent, AI-SPEC Section 5 CI/CD Integration)
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

> Phase 04 was a SPIDR **Spike** whose acceptance bar was "we know enough to plan Phase 04.1," not
> shipped extraction/query functionality. Its `04-01-PLAN.md`/`04-01-SUMMARY.md` went further than
> the spike's own validation doc anticipated: the proven patterns were checked in as real, passing
> tests under `engine/src/graph/{mod,bridge,tests}.rs`, gated behind the `graph-spike` Cargo feature
> rather than deleted. **Verified 2026-08-07:** `cargo test --manifest-path engine/Cargo.toml
> --features graph-spike graph:: --locked` → **5 passed, 0 failed.** The rows below replace the old
> "forward ref"/`❌ Wave 0 (04.1)` placeholders with the actual checked-in test names, and add rows
> for Waves 2-5 (Plans 02-05), which did not exist as concrete plans when Phase 04's validation doc
> was written. Waves 2-5 rows are sourced from each plan's own `<verify>`/acceptance_criteria — this
> table summarizes, it does not replace, the authoritative per-task detail in each `PLAN.md`.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 04.1-01 (graph-spike promotion) | 01 | 1 | DATA-05 | — | arrow ~58.3 ↔ arrow ^56.2 IPC bridge round-trips a `RecordBatch` losslessly | unit | `cargo test --manifest-path engine/Cargo.toml --features graph-spike graph::tests::bridge_round_trip_preserves_schema_and_values` | ✅ `engine/src/graph/bridge.rs` | ✅ green |
| 04.1-01 (graph-spike promotion) | 01 | 1 | DATA-05 | — | Fixed single-hop Cypher query returns correct neighbor + relationship properties for a known fixture graph | unit (tokio) | `cargo test --manifest-path engine/Cargo.toml --features graph-spike graph::tests::fixed_single_hop_projects_relationship_properties` | ✅ `engine/src/graph/tests.rs` | ✅ green |
| 04.1-01 (graph-spike promotion) | 01 | 1 | DATA-05 | — | Variable-length (`*1..hop_cap`) Cypher query returns correct multi-hop neighbors, using node-only `RETURN` (relationship variable cannot be projected under a variable-length quantifier) | unit (tokio) | `cargo test --manifest-path engine/Cargo.toml --features graph-spike graph::tests::multi_hop_traversal_finds_one_hop_neighbor` | ✅ `engine/src/graph/tests.rs` | ✅ green |
| 04.1-01 (graph-spike promotion) | 01 | 1 | DATA-05 | — | Open-vocabulary `relation_type` filtering via `WHERE` on the generic wrapper label returns only matching-type edges | unit (tokio) | `cargo test --manifest-path engine/Cargo.toml --features graph-spike graph::tests::relation_type_filter_excludes_non_matching_edge` | ✅ `engine/src/graph/tests.rs` | ✅ green |
| 04.1-01 (graph-spike promotion) | 01 | 1 | — | V5 Input Validation | `hop_cap` is range-checked/clamped before interpolation into the Cypher string (D-23 guardrail) | unit | `cargo test --manifest-path engine/Cargo.toml --features graph-spike graph::tests::clamp_hop_cap_rejects_zero_and_over_max` | ✅ `engine/src/graph/tests.rs` | ✅ green |
| 04.1-01 (schema-restructure) | 01 | 1 | DATA-04 | — | `entities`/`entity_edges` schema-restructure decision is locked and every existing fixture/test migrated to it | unit | see `04.1-01-PLAN.md` `<verify>` blocks | ⬜ pending (this replan) | ⬜ pending |
| 04.1-02 (tracer) | 02 | 2 | DATA-04, DATA-05, RAG-05 | — | extract→persist→traverse→render→reach-the-provider proven end-to-end, incl. provider-payload-capture fix, seed-inclusive `fetch_neighborhood`, reserve-one-citable-chunk packing | integration | see `04.1-02-PLAN.md` `<verify>` blocks | ⬜ pending | ⬜ pending |
| 04.1-03 (concurrent extraction) | 03 | 3 | DATA-04 | — | bounded concurrent extraction (D-09), real min-content-length skip (D-07), idempotent re-ingestion by construction, documented stale-entity semantics, WR-01 bridge multi-batch fix | unit + integration | see `04.1-03-PLAN.md` `<verify>` blocks | ⬜ pending | ⬜ pending |
| 04.1-04 (QueryGraph redesign) | 04 | 4 | DATA-05 | V5 Input Validation | standalone `QueryGraph` RPC redesigned as induced-neighborhood query, hop-bounded, correct relation-type filtering at any hop depth, validated input | unit + integration | see `04.1-04-PLAN.md` `<verify>` blocks | ⬜ pending | ⬜ pending |
| 04.1-05 (score-interleaving) | 05 | 5 | RAG-05 | — | graph facts score-interleaved with chunk evidence under validated, configurable `graph_weight`; `graph_augmentation` outcome tagging observable end-to-end | unit + integration | see `04.1-05-PLAN.md` `<verify>` blocks | ⬜ pending | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `engine/src/graph/mod.rs`, `engine/src/graph/bridge.rs` — exist (checked in during Phase 04, behind the `graph-spike` feature).
- [x] `engine/src/graph/tests.rs` — exists; covers the five behaviors in the table above, all green as of 2026-08-07.
- [x] `engine/Cargo.toml` additions (`lance-graph`, `arrow-ipc`, `arrow-lg`, `arrow-ipc-lg`) — present as optional deps under the `graph-spike` feature (line 28, 34).

*Wave 0 test infrastructure is stood up and green. What remains for this phase is promoting the code out of the `graph-spike` feature gate (Plan 01) and building Waves 2-5 on top of it — not standing up the harness itself.*

---

## Manual-Only Verifications

*None identified from Phase 04's carryover. Revisit per-plan if `04.1-0{2,3,4,5}-PLAN.md` introduce a behavior that cannot be mechanically checked (e.g. subjective graph-context relevance judgments) during the reviews-incorporation replan.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (see Wave 0 Requirements above — all now present)
- [ ] No watch-mode flags
- [x] Feedback latency < 30s (Wave 1 graph:: suite measured at 0.05s; full suite ~30s per existing engine precedent)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending — Waves 2-5 have not yet executed; sign-off happens at `/gsd-validate-phase` or execute-time, not at replan time.
