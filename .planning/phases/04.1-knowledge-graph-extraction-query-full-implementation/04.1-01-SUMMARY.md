---
phase: 04.1-knowledge-graph-extraction-query-full-implementation
plan: 01
subsystem: database
tags: [rust, lancedb, lance-graph, schema, graph]

requires:
  - phase: 04-knowledge-graph-extraction-query
    provides: lance-graph compatibility PoC and confirmed version conflict resolution
provides:
  - Unconditional lance-graph dependency integration (removed graph-spike feature flag)
  - Dedicated entities and entity_edges table schemas and database accessors
  - Restructured nodes schema (removed dormant placeholder columns)
  - Updated LanceDB table initialization, validation, and fixture constructors
affects: [04.1-knowledge-graph-extraction-query-full-implementation]

tech-stack:
  added: []
  patterns: [table-membership edge isolation, fail-fast schema validation]

key-files:
  created: []
  modified:
    - engine/Cargo.toml
    - engine/src/lib.rs
    - engine/src/graph/mod.rs
    - engine/src/db/mod.rs
    - engine/src/db/tests.rs
    - engine/src/main.rs
    - engine/src/bin/seed_rag_fixture.rs
    - engine/src/inspect_lancedb_tests.rs
    - engine/src/retrieval/tests.rs
    - engine/tests/config_startup.rs
    - engine/src/tests.rs

key-decisions:
  - "Promoted lance-graph to unconditional dependency in default engine build."
  - "Restructured LanceDB schema: added dedicated entities and entity_edges tables, removed 4 dormant placeholder columns (community_ids, summary, summary_vector, unsummarized_refs) from nodes schema."
  - "Enforced entity-to-entity relationship isolation structurally via dedicated entity_edges table rather than ID-space conventions on edges table."

patterns-established:
  - "Structural Edge Separation: entity relationships live in entity_edges while chunk-structural edges remain in edges."

requirements-completed: [DATA-04, DATA-05]

coverage:
  - id: D1
    description: "lance-graph dependency promoted to default build and graph module compiled unconditionally"
    requirement: DATA-04
    verification:
      - kind: unit
        ref: "engine/src/graph/mod.rs#graph::tests"
        status: pass
    human_judgment: false
  - id: D2
    description: "entities and entity_edges tables added to default schema, 4 dormant columns removed from nodes, and all fixtures migrated"
    requirement: DATA-05
    verification:
      - kind: unit
        ref: "engine/src/db/tests.rs#initializes_and_validates_all_table_schemas"
        status: pass
      - kind: unit
        ref: "engine/src/db/tests.rs#entities_and_entity_edges_schema_shapes_are_valid"
        status: pass
      - kind: integration
        ref: "cargo test --manifest-path engine/Cargo.toml --locked"
        status: pass
    human_judgment: false

duration: 25min
completed: 2026-08-07
status: complete
---

# Phase 04.1 Plan 01: Graph Module Promotion & Schema Restructure Summary

**Promoted lance-graph to default build and restructured LanceDB schema with dedicated entities and entity_edges tables while removing dormant placeholder columns from nodes**

## Performance

- **Duration:** 25 min
- **Started:** 2026-08-07T12:46:48Z
- **Completed:** 2026-08-07T13:05:00Z
- **Tasks:** 3
- **Files modified:** 11

## Accomplishments

- Removed `graph-spike` feature gate from `engine/Cargo.toml` and `engine/src/lib.rs`, compiling `lance-graph` unconditionally into the default build.
- Added `entities` and `entity_edges` table schemas and `DatabaseManager` accessors (`entities_table()`, `entity_edges_table()`) in `engine/src/db/mod.rs`.
- Removed 4 dormant placeholder columns (`community_ids`, `summary`, `summary_vector`, `unsummarized_refs`) from `nodes_schema()`.
- Migrated all fixture construction call sites across `main.rs`, `seed_rag_fixture.rs`, `inspect_lancedb_tests.rs`, `retrieval/tests.rs`, `config_startup.rs`, and `src/tests.rs`.
- Passed full test suite (96 unit/integration tests in engine, 18 in inspect_lancedb, 9 in config_startup).

## Task Commits

1. **Task 1: Promote graph module out of graph-spike feature gate** - `feat(04.1-01): promote graph module out of graph-spike feature gate`
2. **Task 2: Confirm schema restructure checkpoint** - `docs(04.1-01): confirm restructure-now decision for graph schema`
3. **Task 3: Build entities/entity_edges schemas and migrate all fixtures** - `feat(04.1-01): add entities and entity_edges schemas, restructure nodes schema, and update fixtures`

## Files Created/Modified

- `engine/Cargo.toml` - Made lance-graph dependencies unconditional and removed `[features] graph-spike` block
- `engine/src/lib.rs` - Promoted `pub mod graph;` to unconditional module
- `engine/src/graph/mod.rs` - Made `bridge` module crate-private (`pub(crate)`)
- `engine/src/db/mod.rs` - Added `entities_schema()`, `entity_edges_schema()`, `entities_table()`, `entity_edges_table()`, removed 4 dormant columns from `nodes_schema()`, updated `table_schemas()` to 7 tables
- `engine/src/db/tests.rs` - Updated table list assertion to 7 tables and added schema shape verification tests
- `engine/src/main.rs` - Updated `replace_document_with_faults` node batch construction
- `engine/src/bin/seed_rag_fixture.rs` - Updated fixture node batch construction
- `engine/src/inspect_lancedb_tests.rs` - Updated both node batch construction sites
- `engine/src/retrieval/tests.rs` - Updated node batch construction
- `engine/tests/config_startup.rs` - Updated node batch construction
- `engine/src/tests.rs` - Updated `canonical_state` and `persisted_edge_summary_is_arrow_null` tests for summary location on `edges`

## Decisions Made

- Confirmed `restructure-now` decision for graph schema: added dedicated `entities` and `entity_edges` tables, and removed four dormant placeholder columns from `nodes`.
- Isolated entity-to-entity relationships in `entity_edges` to enforce table-membership separation from chunk-structural `edges`.

## Deviations from Plan

None - executed tasks as specified in `04.1-01-PLAN.md`.

## Issues Encountered

- `engine/src/tests.rs` had assertion checks (`summary_null_count` and `persisted_node_summary_is_arrow_null`) expecting `summary` on `nodes`. Updated those tests to query the `edges` table, where `summary` is stored.

## User Setup Required

None - local LanceDB store will automatically initialize with the new 7-table schema on next startup. Note that existing local dev LanceDB directories must be removed if schema drift errors occur.

## Next Phase Readiness

- Ready for Plan `04.1-02-PLAN.md` (tracer implementation: entity/relationship extraction, persistence into `entities`/`entity_edges`, and `lance-graph` traversal into prompt assembly).

---
*Phase: 04.1-knowledge-graph-extraction-query-full-implementation*
*Completed: 2026-08-07*
