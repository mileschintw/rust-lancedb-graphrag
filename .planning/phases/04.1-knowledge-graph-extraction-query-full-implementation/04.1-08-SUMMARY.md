---
phase: 04.1-knowledge-graph-extraction-query-full-implementation
plan: 08
subsystem: prompt
tags: [prompt, tie-breaker, extraction, logging, tracing]
requires: ["04.1-07"]
provides:
  - Evidence-favoring exact tie-breaker in pack_evidence_and_graph_prompt
  - Field-level numeric confidence tracing debug event on relation validation failure
affects: [engine/src/prompt.rs, engine/src/graph/extraction.rs, engine/src/tests.rs, engine/src/graph/tests.rs]
tech-stack:
  added: []
  patterns: [tie-break-comparator, tracing-event-capture-layer]
key-files:
  created: []
  modified:
    - engine/src/prompt.rs
    - engine/src/graph/extraction.rs
    - engine/src/tests.rs
    - engine/src/graph/tests.rs
key-decisions:
  - "pack_evidence_and_graph_prompt's tie-break comparator corrected to is_graph_a.cmp(&is_graph_b), ensuring evidence candidates win exact-score ties over graph facts as documented"
  - "validate_extraction_output emits a tracing::debug! event with numeric confidence f64 before returning Err on out-of-range confidence values"
requirements-completed: [DATA-04, RAG-05]
coverage:
  - deliverable: "Evidence-favoring exact tie-breaker"
    human_judgment: false
    verification:
      - kind: test
        ref: "engine/src/tests.rs#pack_evidence_and_graph_prompt_breaks_exact_ties_in_evidence_favor"
        status: pass
  - deliverable: "Field-level confidence tracing event"
    human_judgment: false
    verification:
      - kind: test
        ref: "engine/src/graph/tests.rs#validate_extraction_output_logs_confidence_field_on_out_of_range_failure"
        status: pass
duration: 5 min
completed: 2026-08-08T12:59:15Z
---

# Phase 04.1 Plan 08: Tie-Breaker & Extraction Validation Logging Fixes Summary

Fixes `pack_evidence_and_graph_prompt` exact-score tie-breaker to favor evidence over graph facts and adds numeric confidence field logging to `validate_extraction_output` out-of-range validation errors.

## Accomplishments

1. **Prompt Packing Tie-Breaker Fix:**
   - Corrected `pack_evidence_and_graph_prompt`'s comparator in `engine/src/prompt.rs` from `is_graph_b.cmp(&is_graph_a)` to `is_graph_a.cmp(&is_graph_b)`.
   - Added `pack_evidence_and_graph_prompt_breaks_exact_ties_in_evidence_favor` proving an exact-score tie under tight token budget admits the evidence block and excludes the graph fact.

2. **Extraction Validation Logging:**
   - Updated `validate_extraction_output` in `engine/src/graph/extraction.rs` to emit `tracing::debug!(confidence = rel.confidence as f64, ...)` on out-of-range confidence failure.
   - Added `ExtractionValidationDebugCaptureLayer` and `validate_extraction_output_logs_confidence_field_on_out_of_range_failure` test proving the numeric value is captured in event field logs without exposing raw text.

## Deviations from Plan

None - plan executed as written.

## Self-Check: PASSED
