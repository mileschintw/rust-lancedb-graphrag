---
phase: 04.1-knowledge-graph-extraction-query-full-implementation
plan: 04
type: execute
wave: 4
depends_on: ["04.1-03"]
files_modified:
  - proto/lancet/v1/lancet.proto
  - engine/src/pb/lancet/v1/lancet.v1.rs
  - engine/src/graph/mod.rs
  - engine/src/main.rs
  - engine/src/graph/tests.rs
  - engine/src/tests.rs
  - config/config.example.toml
  - gateway/proto/lancet/v1/lancet.pb.go
  - gateway/proto/lancet/v1/lancet_grpc.pb.go
autonomous: true
requirements: [DATA-05]

must_haves:
  truths:
    - "QueryGraph accepts a structured request (seed entity name or id, hop_depth, optional relation_type filter) — never a raw Cypher query string (D-20)"
    - "An out-of-range hop_depth (0, or above the configured ceiling) is rejected with a structured validation error before any Cypher execution reaches the format!-interpolated query string (D-23)"
    - "QueryGraphResponse.nodes and .edges are built from fetch_neighborhood's own accumulated output AS NARROWED BY graph::narrow_via_cypher (Plan 02's shared helper, reused unchanged here) — for EVERY hop_depth, not only hop_depth==1 — this is a THIRD option beyond D-20's (a) node-only / (b) correlate-back-to-edges_batch, chosen because REVIEWS.md proved (b) is mathematically impossible to reconstruct for path length > 1: a variable-hop Cypher result's final (seed, neighbor) pair does not identify which edge(s) along a multi-hop path connect them, so there is nothing to correlate back to. `narrow_via_cypher` sidesteps this entirely: it never tries to recover a relation PROPERTY from Cypher (edges/relation_type/weight always come from `fetch_neighborhood`'s own directed `edges_batch` columns, at every hop_depth) — it only asks Cypher which entity_ids are confirmed reachable (a pure membership question `traverse_multi_hop`'s node-only, `RETURN r`-free projection answers cleanly at ANY hop count, per 04-RESEARCH.md Pitfall 6), and uses that confirmed set to filter which of `fetch_neighborhood`'s own rows survive into the response. This is genuinely response-affecting Cypher usage at every hop depth (04.1-REVIEWS.md Codex HIGH), not merely a hop_depth==1-only validation log line"
    - "Relation-type filtering is correct at every hop_depth, including hop_depth > 1, AND nodes/edges stay mutually consistent under filtering — filtering is applied in Rust directly against the Cypher-narrowed edges_batch (a small, bounded, in-memory RecordBatch, already filtered once by narrow_via_cypher before this second, semantic filter runs); when relation_type_filter is set, QueryGraphResponse.edges is the filtered edge subset and QueryGraphResponse.nodes is exactly the set of entities that appear as source_node_id or target_node_id in one of those filtered edges (a strict subset of the full entities_batch) — never the full unfiltered neighborhood alongside filtered edges, which would leave edges pointing at nodes the response never lists or list nodes no returned edge touches (REVIEWS.md HIGH: 'relation-filter semantics are contradictory')"
    - "Every ID interpolated into a QueryGraph-triggered LanceDB predicate is validated (seed_entity_id as UUID, checked in the handler before any traversal call — not left to surface as an internal fetch_neighborhood error) or escaped (seed_entity_name lookup, via the same pub(crate) escape_sql_literal Plan 02's fetch_neighborhood uses) before use — reusing fetch_neighborhood's Plan 02 escaping/validation, not a parallel unescaped path, and a malformed seed_entity_id maps to Status::invalid_argument, never Status::internal"
    - "seed_entity_name and relation_type_filter are each rejected with Status::invalid_argument if they exceed a fixed byte-length ceiling (REVIEWS.md MEDIUM: 'QueryGraph adds unbounded caller-controlled strings' — the existing QueryRAG path enforces a byte limit on its query text, retrieval/mod.rs:359-365; QueryGraph's two caller-supplied strings previously had no equivalent bound) — checked in the handler BEFORE the case-folded name-lookup scan or the in-memory relation_type filter runs, so an oversized string is rejected before it does any work, not merely before it reaches a predicate"
    - "The Go gateway adds no HTTP wrapper for QueryGraph (D-25) — QueryGraph remains gRPC-only/internal"
  artifacts:
    - path: proto/lancet/v1/lancet.proto
      provides: "structured QueryGraphRequest (seed_entity_id/seed_entity_name, hop_depth, relation_type_filter) and QueryGraphResponse (nodes, edges)"
      contains: "message QueryGraphRequest"
    - path: engine/src/graph/mod.rs
      provides: "clamp_hop_cap_with_ceiling — a configurable-ceiling variant of the existing clamp_hop_cap, added alongside it (not replacing it). This plan does not add narrow_via_cypher/cypher_confirmed_neighbor_ids/constrain_to_cypher_matched (Plan 02 already adds them, pub(crate)) — it only reuses them, at every hop_depth, as the response-affecting Cypher-constrain step; also adds MAX_SEED_ENTITY_NAME_BYTES/MAX_RELATION_TYPE_FILTER_BYTES constants."
      contains: "clamp_hop_cap_with_ceiling"
  key_links:
    - from: "engine/src/main.rs (query_graph handler)"
      to: "engine/src/graph/mod.rs (fetch_neighborhood, clamp_hop_cap_with_ceiling)"
      via: "structured request validated (including the new length ceilings), hop_depth clamped, then the SAME fetch_neighborhood pipeline Plan 02 built for the RAG-path reused for the standalone RPC, uniformly for every hop_depth including 1"
      pattern: "fetch_neighborhood"
    - from: "engine/src/main.rs (query_graph handler, EVERY hop_depth, immediately after fetch_neighborhood returns)"
      to: "engine/src/graph/mod.rs (narrow_via_cypher) -> engine/src/graph/mod.rs (cypher_confirmed_neighbor_ids -> traverse_multi_hop) -> engine/src/graph/mod.rs (constrain_to_cypher_matched)"
      via: "the SAME shared, response-affecting Plan 02 helper trio, reused here unchanged rather than duplicated: QueryGraphResponse.nodes/.edges are built from narrow_via_cypher's returned (constrained_entities, constrained_edges) pair, hop_cap = the validated/clamped request hop_depth (not fixed at 1, unlike Plan 02's RAG-path) — Cypher's confirmed-reachable set genuinely determines which fetch_neighborhood-derived rows survive, at every hop_depth, before the relation_type_filter (if any) runs"
      pattern: "narrow_via_cypher"
  prohibitions:
    - statement: "MUST NOT attempt to recover per-edge relation_type/weight for a variable-length (hop_depth > 1) Cypher traversal result by correlating the final (seed, neighbor) node-id pair back against the edge batch — REVIEWS.md proved this is mathematically impossible for path length > 1 (the final pair does not identify intermediate-hop edges)"
      status: resolved
      resolution: "QueryGraphResponse for EVERY hop_depth (including 1) is served from fetch_neighborhood's own accumulated entities_batch/edges_batch, as narrowed by narrow_via_cypher's MEMBERSHIP filter — no Cypher call is ever asked to RECOVER a relation property for any case, sidestepping Pitfall 6 entirely rather than working around it. `narrow_via_cypher` only asks Cypher which entity_ids are reachable (a question traverse_multi_hop's node-only projection answers at any hop count); edges/relation_type/weight always come from fetch_neighborhood's own directed edges_batch columns. `traverse_fixed_hop` and `traverse_filtered_by_relation_type` (Phase 4 spike primitives) remain available, correct, and tested, but are intentionally NOT called by QueryGraph in this plan — `traverse_multi_hop` is called instead, via the shared `narrow_via_cypher` helper, at every hop_depth including 1; this is a deliberate design decision, not an oversight, noted here so a future reviewer does not flag `traverse_fixed_hop`/`traverse_filtered_by_relation_type` as orphaned."
    - statement: "MUST NOT build the QueryGraph API response by sourcing a field VALUE (entity_type, relation_type, weight, orientation) from lance-graph's Cypher result, at any hop_depth including 1 — REVIEWS.md HIGH proved traverse_fixed_hop's hop-1 projection (seed.entity_id, r.relation_type, r.weight, neighbor.entity_id, neighbor.name) lacks entity_type and a complete seed row, and an undirected Cypher match cannot preserve which node was the edge's original source vs. target"
      status: resolved
      resolution: "QueryGraph never special-cases hop_depth==1 for RESPONSE CONSTRUCTION — nodes/edges are served from fetch_neighborhood's own output, as narrowed by narrow_via_cypher, uniformly for every hop_depth including 1 (see the prohibition above). Corrected this revision, per cross-AI review (Codex HIGH — 'Cypher is present in production but not load-bearing'): every hop_depth's call DOES invoke traverse_multi_hop via the shared narrow_via_cypher helper (the same one Plan 02's attempt_graph_augmentation uses), and its result now genuinely constrains which rows survive into the response — Cypher determines MEMBERSHIP. It never determines a field's VALUE: the response's seed row, entity_type, relation_type/weight, and edge orientation still come exclusively from fetch_neighborhood's own directed columns, never from Cypher's undirected match projection. Both plans independently converged on the identical discriminating rule: Cypher may run in production and affect the response, but a response field's VALUE may never be sourced from its undirected, orientation-losing projection — only membership may be."
    - statement: "MUST NOT return QueryGraphResponse.nodes as the full unfiltered entities_batch while QueryGraphResponse.edges is the relation_type-filtered edge subset — REVIEWS.md HIGH (this round): for a two-hop path where the first edge does not match the filter but the second does, this leaves nodes the response's own edges never reference, or an edge whose connection to the seed the response cannot explain"
      status: resolved
      resolution: "When relation_type_filter is set, nodes is redefined as exactly the set of entities appearing as source_node_id or target_node_id in a MATCHING edge (a strict subset of entities_batch) — nodes and edges are always mutually consistent under filtering. When no filter is set, nodes remains the full seed-inclusive entities_batch. Two edge-case decisions follow directly and are locked, not left to executor discretion: (1) a filter matching zero edges returns Ok with empty nodes/edges, never Status::not_found — 'no matching relationships' is a valid, successful answer, not an error; (2) the seed entity is included in nodes under a filter ONLY if it is itself an endpoint of a matching edge — the unfiltered case's 'nodes always includes the seed' guarantee does not carry over to the filtered case, since filtering can legitimately produce a subgraph the seed is not part of."
    - statement: "MUST NOT accept an unbounded-length seed_entity_name or relation_type_filter from a gRPC caller, and MUST NOT let the seed_entity_name lookup scan cost grow silently unbounded — REVIEWS.md MEDIUM: 'QueryGraph adds unbounded caller-controlled strings' (no length limit, existing QueryRAG has one; and the name lookup scans the whole entities table)"
      status: resolved
      resolution: "seed_entity_name and relation_type_filter are each length-checked (Status::invalid_argument on overflow) against fixed constants (MAX_SEED_ENTITY_NAME_BYTES=512, MAX_RELATION_TYPE_FILTER_BYTES=128 — matching the extraction JSON-Schema's own entity-name/relation_type maxLength bounds from AI-SPEC §4b.1, Claude's-discretion choice per CONTEXT.md, reusing existing bound values rather than inventing new ones) BEFORE the name-lookup scan or the in-memory filter runs. The name-lookup full-table scan cost itself is explicitly ACCEPTED, not eliminated: entities.name has no secondary index in this schema (Plan 01), and a case-folded exact-match lookup cannot be pushed down as a LanceDB predicate the way a raw substring/prefix match could — bounding the query STRING'S length does not bound the TABLE'S size. This is judged acceptable for this phase given the project's local-first, single-caller (Go gateway only, per D-25) threat posture (matching T-04.1-12's existing accepted-risk framing for QueryGraph's call rate) — an indexed or paginated lookup is future hardening if `entities` grows large enough to matter, not this phase's scope."
---

<objective>
Replace the literal `{"status":"scaffolding"}` `QueryGraph` stub with the structured, hop-bounded, gRPC-only RPC D-20 through D-25 specify, reusing Plan 02's `fetch_neighborhood` pipeline (and, this revision, Plan 02's response-affecting `narrow_via_cypher` Cypher-constrain step) directly for the response — rather than trying to reconstruct per-edge relation properties from a Cypher variable-hop result, which REVIEWS.md proved cannot work for path length > 1. Also close two length-unbounded input gaps (`seed_entity_name`, `relation_type_filter`) REVIEWS.md flagged this round.

Purpose: DATA-05 covers both the always-on RAG-path augmentation (Plan 02/05) and this standalone, caller-driven graph query surface — a developer inspecting or debugging the knowledge graph needs a way to query it directly, independent of a full `/rag/query` call.
Output: a real `QueryGraph` RPC with a typed contract, hop-cap enforcement, correct relation-type filtering at any hop depth, response-affecting Cypher membership narrowing at every hop depth, and validated (length-bounded, UUID/escaped) input.

**D-19 gate note (this replan):** same as Plan 02 — D-19 is satisfied AS WRITTEN by reusing `narrow_via_cypher`; no CONTEXT.md revision is needed or made.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-CONTEXT.md
@.planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-REVIEWS.md
@.planning/phases/04-knowledge-graph-extraction-query/04-RESEARCH.md
@rust-guidelines.md
@proto/lancet/v1/lancet.proto
@engine/src/graph/mod.rs
@engine/src/tests.rs
@config/config.example.toml
</context>

## Artifacts This Phase Produces

- `proto/lancet/v1/lancet.proto` — `QueryGraphRequest { seed_entity_id, seed_entity_name, hop_depth, relation_type_filter }`, `QueryGraphNode { entity_id, name, entity_type }`, `QueryGraphEdge { source_entity_id, target_entity_id, relation_type, weight }`, `QueryGraphResponse { nodes, edges }`.
- `engine/src/graph/mod.rs` — `pub fn clamp_hop_cap_with_ceiling(requested: u32, configured_max: u32) -> Result<u32, GraphSpikeError>`; `pub const MAX_SEED_ENTITY_NAME_BYTES: usize`, `pub const MAX_RELATION_TYPE_FILTER_BYTES: usize`.
- `engine/src/main.rs` — real `query_graph()` handler, reusing Plan 02's `narrow_via_cypher` at every hop_depth (response-affecting Cypher membership constraint, no field VALUE ever sourced from Cypher).
- `config/config.example.toml`, `engine/src/tests.rs` — `engine.graph.seed_match_min_score`/`engine.graph.max_hop_cap` documented as required config keys (Plan 02 introduced the settings struct; this plan is the first to actually consume `max_hop_cap`, so this is where the required-keys list, example config, and the `config_example_matches_effective_rag_contract` section matcher gain the `engine.graph` entries).

<tasks>

<task type="auto">
  <name>Task 1: Redesign the QueryGraph proto contract</name>
  <read_first>proto/lancet/v1/lancet.proto, buf.gen.yaml, engine/src/main.rs (lines 42-53, how generated pb types are included and imported)</read_first>
  <files>proto/lancet/v1/lancet.proto</files>
  <action>
    Replace the current stub `message QueryGraphRequest { string query = 1; }` / `message QueryGraphResponse { string result_json = 1; }` (lines 113-119) with a structured contract per D-20:
    `message QueryGraphRequest { string seed_entity_id = 1; string seed_entity_name = 2; uint32 hop_depth = 3; string relation_type_filter = 4; }` (exactly one of `seed_entity_id`/`seed_entity_name` must be set — validated server-side in Task 2, matching this project's existing convention of proto-level structural typing plus handler-level semantic validation).
    Add `message QueryGraphNode { string entity_id = 1; string name = 2; string entity_type = 3; }` and `message QueryGraphEdge { string source_entity_id = 1; string target_entity_id = 2; string relation_type = 3; float weight = 4; }`.
    Replace `QueryGraphResponse` with `message QueryGraphResponse { repeated QueryGraphNode nodes = 1; repeated QueryGraphEdge edges = 2; }`.
    Run `buf generate` from the repository root — this regenerates both `engine/src/pb/lancet/v1/lancet.v1.rs` and `gateway/proto/lancet/v1/{lancet.pb.go,lancet_grpc.pb.go}`. Per D-25, do NOT add any new HTTP handler/route in the Go gateway for the regenerated Go types. Confirm via `git diff --stat` that no `gateway/**/handler*.go` or router file changed, only the generated `*.pb.go` files.
  </action>
  <verify>
    <automated>grep -q "relation_type_filter" engine/src/pb/lancet/v1/lancet.v1.rs</automated>
  </verify>
  <done>The proto's `QueryGraphRequest`/`QueryGraphResponse` are redefined per D-20 and `buf generate` has regenerated `engine/src/pb/lancet/v1/lancet.v1.rs` to carry the new field names (a whole-crate `cargo build` is expected to fail transiently at this point, since `main.rs`'s stub handler doesn't yet match the new fields; that failure is resolved by Task 2 and is not what this task's verify gates on).</done>
  <acceptance_criteria>
    - `grep -n "message QueryGraphRequest" -A 5 proto/lancet/v1/lancet.proto` shows `seed_entity_id`, `seed_entity_name`, `hop_depth`, `relation_type_filter`.
    - `grep -q "relation_type_filter" engine/src/pb/lancet/v1/lancet.v1.rs` confirms `buf generate` regenerated the Rust pb types against the new proto shape.
    - `git diff --name-only -- gateway/` touches only generated `*.pb.go` files, no handler/router source.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the QueryGraph handler as a Cypher-constrained induced-neighborhood query, with bounded input</name>
  <read_first>rust-guidelines.md, engine/src/main.rs (query_rag handler for structured-validation conventions and d1_status-style error mapping; the existing query_graph stub at ~1424-1431), engine/src/graph/mod.rs (clamp_hop_cap, fetch_neighborhood, narrow_via_cypher/cypher_confirmed_neighbor_ids/constrain_to_cypher_matched, traverse_multi_hop — all from Plan 02), engine/src/tests.rs (REQUIRED_EFFECTIVE_RAG_KEYS list, lines ~15-40), config/config.example.toml, .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-REVIEWS.md (this round's Cypher-response-affecting HIGH finding and the seed_entity_name/relation_type_filter length-limit MEDIUM finding, plus the still-relevant prior-round "Pitfall-6 correlation cannot recover multi-hop path relations" and "relation filtering ignores requested hop depth" HIGH findings)</read_first>
  <files>engine/src/graph/mod.rs, engine/src/main.rs, engine/src/graph/tests.rs, engine/src/tests.rs, config/config.example.toml</files>
  <behavior>
    - Test 1 (unit, graph::tests): `clamp_hop_cap_with_ceiling` rejects a `configured_max` above the compile-time `MAX_HOP_CAP` by silently capping it to `MAX_HOP_CAP` (defense in depth against a misconfigured TOML value), and otherwise behaves like `clamp_hop_cap` bounded to whichever ceiling is lower.
    - Test 2 (integration, engine/src/tests.rs or graph::tests against a real temp DB): a `QueryGraph` call with `hop_depth = 0` or `hop_depth` above the configured ceiling returns `Status::invalid_argument` without any Cypher execution or `fetch_neighborhood` call occurring.
    - Test 3 (the redesign's core proof — this revision, ALSO the primary proof that the response-affecting Cypher-constrain step does not break a real multi-hop, multi-edge response): a `QueryGraph` call with `hop_depth = 2` against a fixture `seed → neighbor1 → neighbor2` (two distinct edges, distinct `relation_type`s) returns `QueryGraphEdge` entries for BOTH edges with correct `relation_type`/`weight` — proving the induced-neighborhood design recovers what REVIEWS.md showed the correlate-back design mathematically cannot for a 2-hop path. This fixture's `(entities_batch, edges_batch)` is now also passed through `narrow_via_cypher` (this revision) before the response is built; since Cypher's `traverse_multi_hop` pattern-matches the SAME bridged data BFS produced, it confirms both `neighbor1` and `neighbor2` reachable and the response is unchanged — proving the Cypher-constrain step is correctness-preserving on real, non-adversarial multi-hop data, not merely a theoretical no-op.
    - Test 4: a `QueryGraph` call with `hop_depth = 2` AND a `relation_type_filter` set, against a fixture where only ONE of the two hops' edges matches the filter, returns exactly that one matching edge in `.edges` and exactly its two endpoint entities in `.nodes` — nothing from the non-matching first hop, and the seed itself is absent from `.nodes` in this fixture (the seed is the source of the non-matching first edge only, not an endpoint of the matching second edge) — proving nodes/edges stay mutually consistent under filtering, not a full-neighborhood response and not silently truncated to hop 1 (REVIEWS.md HIGH, this round: "relation-filter semantics are contradictory"). The Cypher-constrain step (this revision) runs BEFORE this relation_type filter and does not affect this test's outcome, since Cypher confirms both edges' endpoints structurally reachable regardless of relation_type (Cypher narrows by MEMBERSHIP only, never by edge-type semantics) — the relation_type_filter is the only step that removes the non-matching edge.
    - Test 4b (new, REVIEWS.md HIGH-driven edge case, this round): a `QueryGraph` call with a `relation_type_filter` that matches ZERO edges in the fetched neighborhood returns `Ok` with empty `.nodes` and empty `.edges` — never `Status::not_found` (a filter matching nothing is a valid, successful "no matching relationships" answer, not an error).
    - Test 5: a seed that is only ever the TARGET of an edge (never the source) still returns that edge in the response (D-24 bidirectionality, reused from Plan 02's `fetch_neighborhood`).
    - Test 6 (scoped to the UNFILTERED case only — REVIEWS.md HIGH, this round, made this scoping explicit since Test 4/4b now define the filtered case differently): with no `relation_type_filter` set, `QueryGraphResponse.nodes` includes the seed entity itself (per Plan 02's seed-inclusion fix to `fetch_neighborhood`) plus every entity reachable within `hop_depth` hops — this is the explicit response-semantics contract for the unfiltered case (REVIEWS.md MEDIUM: "QueryGraph response semantics are underspecified").
    - Test 7 (new, REVIEWS.md MEDIUM this round — invalid seed error mapping): a `QueryGraph` call with a malformed (non-UUID) `seed_entity_id` returns `Status::invalid_argument`, not `Status::internal` — validated in the handler before any call into `fetch_neighborhood`.
    - Test 8 (`query_graph_rejects_oversized_seed_entity_name`, new, REVIEWS.md MEDIUM this round — "QueryGraph adds unbounded caller-controlled strings"): a `QueryGraph` call whose `seed_entity_name` is longer than `graph::MAX_SEED_ENTITY_NAME_BYTES` returns `Status::invalid_argument` — constructed as a real entity's name PADDED past the byte ceiling (not an arbitrary oversized string), against a fixture where that exact entity exists in `entities_table()`. This proves the length check runs BEFORE the case-folded lookup, not merely coincides with a `not_found`: if the check were missing or ran after the scan, the padded name would fail to case-fold-match the real (shorter) stored name and return `Status::not_found`, not `Status::invalid_argument` — so observing `invalid_argument` specifically distinguishes "rejected for length" from "rejected because it didn't match."
    - Test 9 (`query_graph_rejects_oversized_relation_type_filter`, new, same REVIEWS.md MEDIUM finding): a `QueryGraph` call with `relation_type_filter` longer than `graph::MAX_RELATION_TYPE_FILTER_BYTES` returns `Status::invalid_argument`, checked before `fetch_neighborhood`/`narrow_via_cypher` run at all (an oversized filter is rejected regardless of whether the seed itself is valid).
  </behavior>
  <action>
    In `engine/src/graph/mod.rs`, add `pub fn clamp_hop_cap_with_ceiling(requested: u32, configured_max: u32) -> Result<u32, GraphSpikeError>` — a NEW function alongside the existing `clamp_hop_cap` (do not modify `clamp_hop_cap`'s signature or its existing test). Internally: clamp `configured_max` to `MAX_HOP_CAP.min(configured_max)` first, then apply the same `0`/`> effective_max` rejection logic `clamp_hop_cap` already uses.

    `GraphConfigSettings.max_hop_cap` and `.seed_match_min_score` already exist (Plan 02 added both fields, with `impl Default`, for forward-compatibility — Plan 02's RAG-path only ever reads `seed_match_min_score`, always uses a hardcoded 1-hop, and never reads `max_hop_cap`) — this is the first plan to actually READ `max_hop_cap`. Add `engine.graph.seed_match_min_score` and `engine.graph.max_hop_cap` to `config/config.example.toml` (a new `[engine.graph]` section, following the existing `[engine.retrieval]` section's `# unit=...; range=...` comment convention) and to `REQUIRED_EFFECTIVE_RAG_KEYS`/`REQUIRED_EFFECTIVE_RAG_ANNOTATIONS` in `engine/src/tests.rs` (lines ~15-90). **Also update the section matcher itself (REVIEWS.md MEDIUM, this round: "adding engine.graph keys to the required-key arrays without updating this section matcher will not validate the new section"):** `config_example_matches_effective_rag_contract`'s `if !matches!(section, "engine.retrieval" | "engine.retrieval.bm25" | "openrouter") { continue; }` (`engine/src/tests.rs` ~line 167-172) currently skips any section not in that list — add `"engine.graph"` to the `matches!` arms, otherwise the new `[engine.graph]` section's lines are never inspected/annotated-checked at all, and the two new keys in `REQUIRED_EFFECTIVE_RAG_KEYS` would simply never appear in `observed`, failing the `observed_keys == required_keys` assertion for the wrong reason (a config-example omission bug looking identical to a real regression).

    **Rewrite `query_graph()` (main.rs, replacing the current stub) — induced-neighborhood design, not Cypher correlate-back, and no hop_depth==1 exception:** extract the structured request; validate exactly one of `seed_entity_id`/`seed_entity_name` is non-empty (return `Status::invalid_argument` otherwise).

    **Length-limit validation (REVIEWS.md MEDIUM, this round — "QueryGraph adds unbounded caller-controlled strings," a two-part finding: no length limit at all, AND the name lookup scans the whole entity table):** in `engine/src/graph/mod.rs`, add `pub const MAX_SEED_ENTITY_NAME_BYTES: usize = 512;` and `pub const MAX_RELATION_TYPE_FILTER_BYTES: usize = 128;` — matching the extraction JSON-Schema's own `name`/`relation_type` `maxLength` bounds already declared in `04.1-AI-SPEC.md` §4b.1 (Claude's discretion, reusing an existing bound rather than inventing a new arbitrary one). In `query_graph()`, immediately after the exactly-one-of-seed_entity_id/seed_entity_name check and BEFORE any table scan: if `seed_entity_name` is set and `seed_entity_name.len() > graph::MAX_SEED_ENTITY_NAME_BYTES`, return `Status::invalid_argument`; if `relation_type_filter` is set (non-empty) and `relation_type_filter.len() > graph::MAX_RELATION_TYPE_FILTER_BYTES`, return `Status::invalid_argument`. This bounds the STRING'S length, not the entities TABLE'S size — the case-folded name-lookup below still performs a full-table scan by design (see the "Every ID interpolated..." must-have's resolution above for why: no secondary index exists on `entities.name`, and a case-folded exact match cannot be pushed down as a LanceDB predicate). Bounding input length closes the "unbounded string" half of the finding; the table-scan-cost half is an accepted, documented trade-off for this phase's local-first, single-caller threat posture, not eliminated.

    **Seed resolution and validation (REVIEWS.md MEDIUM, this round — "invalid QueryGraph seed IDs may map to internal instead of invalid_argument" and "the planned name lookup cannot directly reuse the proposed helper"):** if `seed_entity_id` was given directly, validate it with `Uuid::parse_str` IN THE HANDLER, before any call into `fetch_neighborhood` — return `Status::invalid_argument` immediately on a malformed UUID (do not let a malformed seed reach `fetch_neighborhood`'s own internal `Bridge`-kind validation error, which this handler would otherwise have no way to distinguish from a genuine internal fault). If `seed_entity_name` was given instead (already length-checked above): read `self.database.entities_table()`'s `entity_id`/`name` columns in full into memory (a small, bounded in-memory scan — the same "project only what's needed" discipline `fetch_neighborhood` already applies), case-fold every `name` via `unicode_casefold::UnicodeCaseFold::case_fold()` (the same pattern Plan 02's extraction-merge step and `retrieval/bm25.rs`'s `fold()` helper already use for D-05), case-fold the caller's `seed_entity_name` the same way, and collect all rows whose folded name matches. Zero matches → `Status::not_found`. Exactly one match → use its `entity_id`. Two or more matches (a defensive case that should not occur in practice, since `ExactMatchResolver` already merges entities by case-folded name at write time per D-05 — a duplicate here would itself indicate the merge invariant drifted) → deterministically pick the lexicographically smallest `entity_id` and `tracing::warn!` the unexpected duplicate (operator-visible, not request-failing). This name-lookup predicate is built using `graph::escape_sql_literal` (Plan 02 already declares this `pub(crate)`, specifically so this handler can reuse it rather than duplicating the escaping logic a third time alongside `retrieval/dense.rs`'s private copy).

    Clamp `hop_depth` via `clamp_hop_cap_with_ceiling(request.hop_depth, self.effective_settings.graph.max_hop_cap)`, mapping an `Err` to `Status::invalid_argument` BEFORE any traversal call. Call `graph::fetch_neighborhood(&self.database, &seed_entity_id, hop_cap, true)` (D-24: always bidirectional; `seed_entity_id` is now guaranteed well-formed by the handler-level check above, whether it came from the caller directly or was resolved from a name lookup — `fetch_neighborhood`'s own internal UUID check per Plan 02 still runs too, as defense in depth, but should never actually fire from this call site).

    **Cypher-confirmed neighborhood constraint — every hop_depth, this revision (per cross-AI review, Codex HIGH — see Plan 02's identical section for the full design rationale, not re-derived here):** immediately after `fetch_neighborhood` returns `(entities_batch, edges_batch)` and BEFORE building the response, call `let (entities_batch, edges_batch) = graph::narrow_via_cypher(&entities_batch, &edges_batch, &seed_entity_id, hop_cap).await;` (shadowing the original bindings — `hop_cap` here is the ALREADY-validated/clamped request hop_depth, not a fixed `1` like Plan 02's RAG-path, since `traverse_multi_hop`'s node-only projection works at any hop count per 04-RESEARCH.md Pitfall 6). Every subsequent read of `entities_batch`/`edges_batch` in this handler — both the unfiltered and `relation_type_filter`-ed response-building steps below — reads the Cypher-constrained pair, not `fetch_neighborhood`'s raw output.

    **Build the response from the CYPHER-CONSTRAINED `(entities_batch, edges_batch)` pair — for EVERY hop_depth, including 1 (REVIEWS.md HIGH, this round: "Plan 04's hop-1 exception cannot satisfy its own response contract" — no hop_depth==1 exception exists; that prior draft's exception, which built the response from `traverse_fixed_hop`'s Cypher-projected result instead, is REMOVED and stays removed — that projection lacks `entity_type`/a complete seed row, and an undirected Cypher match cannot preserve original edge orientation, so it could never satisfy this response's own field contract):** without a `relation_type_filter`: `QueryGraphResponse.nodes` = every row in the (Cypher-constrained) `entities_batch` (already seed-inclusive, per Plan 02's `fetch_neighborhood` fix and `narrow_via_cypher`'s own seed-union rule — needs no extra "add the seed" step); `QueryGraphResponse.edges` = every row in the (Cypher-constrained) `edges_batch`. **With a `relation_type_filter` set (REVIEWS.md HIGH, this round: "relation-filter semantics are contradictory" — resolved by redefining node selection, not just edge selection):** first filter the (Cypher-constrained) `edges_batch` in Rust by exact-match on `relation_type` (a simple in-memory `RecordBatch` column filter over an already-small, already-hop-bounded, already-Cypher-narrowed batch — no LanceDB predicate, no injection surface, since this is pure in-memory string comparison) to get `QueryGraphResponse.edges`; THEN set `QueryGraphResponse.nodes` = exactly the entities from the (Cypher-constrained) `entities_batch` whose `entity_id` appears as `source_node_id` OR `target_node_id` in one of those FILTERED edges — a strict subset, never the full unfiltered set. A filter matching zero edges yields `Ok` with both `nodes` and `edges` empty (never `Status::not_found` — "no matches" is a valid answer). The seed is included in `nodes` under a filter only if it happens to be an endpoint of a surviving edge — the unfiltered case's "always include the seed" guarantee does not extend to the filtered case. This design is intentionally a THIRD option beyond D-20's (a)/(b): `fetch_neighborhood` + `narrow_via_cypher` together ARE a genuine, real, hop-capped bidirectional BFS pre-narrow independently CONFIRMED by a real Cypher pattern-match against the same data (D-19's hybrid mechanism) — using that combined, already-Cypher-narrowed output directly (uniformly, for every hop_depth) is the authoritative answer for RESPONSE FIELDS. `traverse_multi_hop` (via `narrow_via_cypher`) IS now called from this production path, at every hop_depth including 1 — but strictly for MEMBERSHIP, never as the source of a field VALUE; `traverse_fixed_hop` and `traverse_filtered_by_relation_type` remain correct and tested (Plan 01's five checked-in spike tests) but are deliberately NOT called from any production path in this handler, matching Plan 02's identical choice and rationale (see the prohibition above).

    Map `graph::GraphSpikeError` to `Status`: kind `InvalidHopCap` → `invalid_argument`, everything else → `internal` (the handler-level seed-UUID pre-check above means a malformed seed never reaches this generic mapping as an `internal` error — it is already rejected as `invalid_argument` before `fetch_neighborhood` is ever called). A `narrow_via_cypher` failure never reaches this mapping at all — per Plan 02's design, `narrow_via_cypher` is infallible (it falls back to the unconstrained pair and logs a warning internally on any Cypher error), so this handler's error mapping only ever sees `fetch_neighborhood`'s own `Err`s, mirroring how `query_rag` maps its own error kinds.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked clamp_hop_cap_with_ceiling && cargo test --manifest-path engine/Cargo.toml --locked query_graph_recovers_multi_hop_edge_relation_properties && cargo test --manifest-path engine/Cargo.toml --locked query_graph_relation_filter_correct_at_hop_depth_two && cargo test --manifest-path engine/Cargo.toml --locked query_graph_relation_filter_returns_empty_on_no_match && cargo test --manifest-path engine/Cargo.toml --locked query_graph_rejects_out_of_range_hop_depth && cargo test --manifest-path engine/Cargo.toml --locked query_graph_bidirectional_seed_as_target && cargo test --manifest-path engine/Cargo.toml --locked query_graph_nodes_include_seed_when_unfiltered && cargo test --manifest-path engine/Cargo.toml --locked query_graph_rejects_malformed_seed_id && cargo test --manifest-path engine/Cargo.toml --locked query_graph_rejects_oversized_seed_entity_name && cargo test --manifest-path engine/Cargo.toml --locked query_graph_rejects_oversized_relation_type_filter</automated>
  </verify>
  <done>QueryGraph is a real, structured, hop-bounded RPC serving the induced neighborhood from `fetch_neighborhood`'s output AS CONSTRAINED BY a real, response-affecting Cypher membership check (`narrow_via_cypher`) for every hop_depth including 1 — Cypher never sources a field VALUE, only membership; relation-type filtering is correct at every hop depth with nodes/edges kept mutually consistent; variable-hop responses carry correct per-edge relation_type/weight without attempting an impossible correlate-back; a malformed seed_entity_id is rejected as invalid_argument, never internal; oversized seed_entity_name/relation_type_filter strings are rejected before any table scan.</done>
  <acceptance_criteria>
    - `grep -n "clamp_hop_cap_with_ceiling" engine/src/graph/mod.rs` matches a `pub fn` declaration distinct from the existing `clamp_hop_cap`.
    - `grep -n "r#\"\{\"status\":\"scaffolding\"\}\"#" engine/src/main.rs` returns no matches (the literal stub response is gone).
    - `grep -c "traverse_fixed_hop(\|traverse_filtered_by_relation_type(" engine/src/main.rs` returns `0` — `query_graph()` never calls either directly (and reuses no separate validation-only helper for them); Cypher is reached exclusively through `graph::narrow_via_cypher`, the same shared helper Plan 02's `attempt_graph_augmentation` uses.
    - `grep -n "narrow_via_cypher" engine/src/main.rs` matches inside `query_graph()`, positioned immediately after `fetch_neighborhood` returns and BEFORE the response-building steps (both the unfiltered and `relation_type_filter`-ed branches) — confirms the Cypher-constrain call runs at every hop_depth, before the values it affects are read.
    - `grep -v '^\s*//' engine/src/graph/mod.rs | grep -c "traverse_multi_hop("` returns at least `2` (the function's own definition plus at least one real call site inside `cypher_confirmed_neighbor_ids`, shared with Plan 02) — confirms `traverse_multi_hop` has production callers from BOTH plans.
    - `grep -n "Uuid::parse_str" engine/src/main.rs` shows a call inside `query_graph()` distinct from `query_rag`'s existing `session_id` check, positioned before any `fetch_neighborhood` call.
    - `grep -n "MAX_SEED_ENTITY_NAME_BYTES\|MAX_RELATION_TYPE_FILTER_BYTES" engine/src/graph/mod.rs engine/src/main.rs` shows both constants declared in `graph/mod.rs` and both referenced inside `query_graph()`'s validation, positioned before the name-lookup scan / relation_type filter run.
    - The nine named tests above pass under `cargo test --manifest-path engine/Cargo.toml --locked`.
    - `cargo test --manifest-path engine/Cargo.toml --locked clamp_hop_cap_rejects_zero_and_over_max` still passes unchanged, confirming the pre-existing `clamp_hop_cap` function and test were not modified.
    - `grep -n "engine.graph.max_hop_cap\|engine.graph.seed_match_min_score" engine/src/tests.rs config/config.example.toml` shows both new keys documented in both files.
    - `grep -n "\"engine.graph\"" engine/src/tests.rs` matches inside `config_example_matches_effective_rag_contract`'s section `matches!` list (not only inside the `REQUIRED_EFFECTIVE_RAG_KEYS`/`_ANNOTATIONS` arrays).
    - `grep -n "fn clamp_hop_cap_with_ceiling" engine/src/graph/tests.rs` matches — the named test function exists in source, not merely matched by an empty `cargo test` filter (plan-checker WARNING, iteration 2: chained `cargo test <name>` filters pass vacuously if the name doesn't exist).
    - `grep -n "fn query_graph_recovers_multi_hop_edge_relation_properties" engine/src/tests.rs engine/src/graph/tests.rs` matches.
    - `grep -n "fn query_graph_relation_filter_correct_at_hop_depth_two" engine/src/tests.rs engine/src/graph/tests.rs` matches.
    - `grep -n "fn query_graph_relation_filter_returns_empty_on_no_match" engine/src/tests.rs engine/src/graph/tests.rs` matches.
    - `grep -n "fn query_graph_rejects_out_of_range_hop_depth" engine/src/tests.rs engine/src/graph/tests.rs` matches.
    - `grep -n "fn query_graph_bidirectional_seed_as_target" engine/src/tests.rs engine/src/graph/tests.rs` matches.
    - `grep -n "fn query_graph_nodes_include_seed_when_unfiltered" engine/src/tests.rs engine/src/graph/tests.rs` matches.
    - `grep -n "fn query_graph_rejects_malformed_seed_id" engine/src/tests.rs engine/src/graph/tests.rs` matches.
    - `grep -n "fn query_graph_rejects_oversized_seed_entity_name" engine/src/tests.rs engine/src/graph/tests.rs` matches.
    - `grep -n "fn query_graph_rejects_oversized_relation_type_filter" engine/src/tests.rs engine/src/graph/tests.rs` matches.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gRPC caller → QueryGraph seed/hop_depth/relation_type_filter | The first genuinely caller-controlled hop_depth in this phase (Plan 02's RAG-path hardcodes 1-hop) — this is where an external gRPC caller's input reaches `fetch_neighborhood`'s already-escaping/validating predicate builder |
| gRPC caller → seed_entity_name lookup | A caller-supplied entity name string is used in a case-folded exact-match predicate against `entities.name`, escaped via `fetch_neighborhood`'s shared `escape_sql_literal` — not free-text search, bounded to a single-row lookup |
| gRPC caller → relation_type_filter | Applied as an in-memory Rust string-equality filter over an already-fetched, hop-bounded `RecordBatch` — never interpolated into any predicate string, so it carries no injection surface at all |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-11 | Tampering | `engine/src/graph/mod.rs` caller-controlled `hop_depth` reaching `fetch_neighborhood`'s Cypher-adjacent predicate builder | high | mitigate | `clamp_hop_cap_with_ceiling` rejects out-of-range values before any traversal call — this is the first plan where hop_depth is genuinely externally controlled (Plan 02's RAG-path used a hardcoded `1`) |
| T-04.1-12 | Denial of Service | Unbounded `QueryGraph` call rate against `entities`/`entity_edges` reads | low | accept | No new rate-limiting introduced this phase — consistent with the project's existing local-first, single-caller (Go gateway only, per D-25) threat posture |
| T-04.1-16 | Information Disclosure | Malformed `seed_entity_id` previously surfaced as `Status::internal`, a category that (per this codebase's convention) can carry more diagnostic detail than caller-input-error categories | medium | mitigate | REVIEWS.md MEDIUM, this round: the handler now validates `seed_entity_id` via `Uuid::parse_str` before any `fetch_neighborhood` call, mapping a malformed value to `Status::invalid_argument` directly — it never reaches the generic `GraphSpikeError` → `internal` fallback mapping at all |
| T-04.1-19 | Denial of Service / V5 Input Validation | `seed_entity_name`/`relation_type_filter` previously had no length ceiling, unlike `QueryRAG`'s existing byte-limited query text | medium | mitigate | REVIEWS.md MEDIUM, this round: `MAX_SEED_ENTITY_NAME_BYTES`/`MAX_RELATION_TYPE_FILTER_BYTES` reject oversized input before any table scan or in-memory filter runs. The name-lookup's full-table-scan COST (independent of input length, since `entities.name` has no secondary index) remains an accepted trade-off for this phase's local-first, single-caller posture — noted explicitly, not silently left unaddressed |

**Canon-referral breadcrumb:** the `hop_depth`-into-predicate risk is OWASP-injection canon, already covered as T-04.1-11/the reused `fetch_neighborhood` escaping mechanism — not re-minted as a separate prohibition.
</threat_model>

<verification>
- `cargo build --manifest-path engine/Cargo.toml` and `cargo test --manifest-path engine/Cargo.toml --locked` both succeed.
- `git diff --name-only -- gateway/` shows only regenerated `*.pb.go` files — no new Go HTTP route (D-25).
- A manual `grpcurl`/gRPC client call (if available) against a locally running engine confirms `QueryGraph` returns structured `nodes`/`edges`, not `result_json`.
</verification>

<success_criteria>
`QueryGraph` is a real, structured, gRPC-only RPC bounded by a configurable hop-cap ceiling, serving the induced neighborhood from Plan 02's `fetch_neighborhood` pipeline AS CONSTRAINED BY a real, response-affecting Cypher membership check (`graph::narrow_via_cypher`, this revision, per cross-AI review Codex HIGH) for every hop_depth — Cypher genuinely narrows the response when it disagrees with BFS, but never sources a field VALUE — correctly recovering relation properties, keeping nodes/edges mutually consistent under relation-type filtering at every hop depth, rejecting malformed seed input as `invalid_argument`, rejecting oversized `seed_entity_name`/`relation_type_filter` input before any table scan, and honoring D-17/D-19/DATA-05's Cypher-pattern-matching requirement as a genuine, response-affecting production mechanism rather than only Plan 01's spike tests.
</success_criteria>

<output>
Create `.planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-04-SUMMARY.md` when done.
</output>
