---
phase: 04.1-knowledge-graph-extraction-query-full-implementation
plan: 03
type: execute
wave: 3
depends_on: ["04.1-02"]
files_modified:
  - proto/lancet/v1/lancet.proto
  - engine/src/pb/lancet/v1/lancet.v1.rs
  - engine/src/graph/mod.rs
  - engine/src/main.rs
  - engine/src/graph/tests.rs
  - gateway/proto/lancet/v1/lancet.pb.go
  - gateway/proto/lancet/v1/lancet_grpc.pb.go
autonomous: true
requirements: [DATA-05]

must_haves:
  truths:
    - "QueryGraph accepts a structured request (seed entity name or id, hop_depth, optional relation_type filter) — never a raw Cypher query string, even though lance-graph could parse one (D-20)"
    - "An out-of-range hop_depth (0, or above the configured ceiling) is rejected with a structured validation error before any Cypher execution reaches the format!-interpolated query string (D-23)"
    - "For hop_depth > 1 (variable-length pattern), QueryGraphResponse still carries relation_type/weight per returned edge, recovered by correlating the Cypher result's matched entity-id pairs back against the already-in-scope edges_batch fetch_neighborhood already read (D-20 option (b)) — not by a second query"
    - "The Go gateway adds no HTTP wrapper for QueryGraph (D-25) — QueryGraph remains gRPC-only/internal"
  artifacts:
    - path: proto/lancet/v1/lancet.proto
      provides: "structured QueryGraphRequest (seed_entity_id/seed_entity_name, hop_depth, relation_type_filter) and QueryGraphResponse (nodes, edges)"
      contains: "message QueryGraphRequest"
    - path: engine/src/graph/mod.rs
      provides: "clamp_hop_cap_with_ceiling — a configurable-ceiling variant of the existing clamp_hop_cap, added alongside it (not replacing it)"
      contains: "clamp_hop_cap_with_ceiling"
  key_links:
    - from: "engine/src/main.rs (query_graph handler)"
      to: "engine/src/graph/mod.rs (fetch_neighborhood, traverse_multi_hop, clamp_hop_cap_with_ceiling)"
      via: "structured request validated, hop_depth clamped, then the same fetch_neighborhood/traverse pipeline Plan 01 built for the RAG-path reused for the standalone RPC"
      pattern: "clamp_hop_cap_with_ceiling"
---

<objective>
Replace the literal `{"status":"scaffolding"}` `QueryGraph` stub with the structured, hop-bounded, gRPC-only RPC D-20 through D-25 specify, reusing Plan 01's `fetch_neighborhood`/`traverse_*` pipeline rather than re-deriving traversal logic.

Purpose: DATA-05 covers both the always-on RAG-path augmentation (Plan 01/04) and this standalone, caller-driven graph query surface — a developer inspecting or debugging the knowledge graph needs a way to query it directly, independent of a full `/rag/query` call.
Output: a real `QueryGraph` RPC with a typed contract, hop-cap enforcement, and Pitfall-6-aware relation-property recovery for variable-hop results.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-CONTEXT.md
@.planning/phases/04-knowledge-graph-extraction-query/04-RESEARCH.md
@rust-guidelines.md
@proto/lancet/v1/lancet.proto
@engine/src/graph/mod.rs
</context>

## Artifacts This Phase Produces

- `proto/lancet/v1/lancet.proto` — `QueryGraphRequest { seed_entity_id, seed_entity_name, hop_depth, relation_type_filter }`, `QueryGraphNode { entity_id, name, entity_type }`, `QueryGraphEdge { source_entity_id, target_entity_id, relation_type, weight }`, `QueryGraphResponse { nodes, edges }`.
- `engine/src/graph/mod.rs` — `pub fn clamp_hop_cap_with_ceiling(requested: u32, configured_max: u32) -> Result<u32, GraphSpikeError>`.
- `engine/src/main.rs` — real `query_graph()` handler; `GraphConfigSettings { max_hop_cap: u32 }` TOML settings block (default 3) following the `RetrievalConfigSettings` pattern.

<tasks>

<task type="auto">
  <name>Task 1: Redesign the QueryGraph proto contract</name>
  <read_first>proto/lancet/v1/lancet.proto, buf.gen.yaml, engine/src/main.rs (lines 42-53, how generated pb types are included and imported)</read_first>
  <files>proto/lancet/v1/lancet.proto</files>
  <action>
    Replace the current stub `message QueryGraphRequest { string query = 1; }` / `message QueryGraphResponse { string result_json = 1; }` (lines 113-119) with a structured contract per D-20:
    `message QueryGraphRequest { string seed_entity_id = 1; string seed_entity_name = 2; uint32 hop_depth = 3; string relation_type_filter = 4; }` (exactly one of `seed_entity_id`/`seed_entity_name` must be set — validated server-side in Task 2, not at the proto level, matching this project's existing convention of proto-level structural typing plus handler-level semantic validation seen in `query_rag`'s `session_id` handling).
    Add `message QueryGraphNode { string entity_id = 1; string name = 2; string entity_type = 3; }` and `message QueryGraphEdge { string source_entity_id = 1; string target_entity_id = 2; string relation_type = 3; float weight = 4; }`.
    Replace `QueryGraphResponse` with `message QueryGraphResponse { repeated QueryGraphNode nodes = 1; repeated QueryGraphEdge edges = 2; }`.
    Run `buf generate` from the repository root (per `buf.gen.yaml`, this regenerates both `engine/src/pb/lancet/v1/lancet.v1.rs` and `gateway/proto/lancet/v1/{lancet.pb.go,lancet_grpc.pb.go}`) — the Go-side regeneration is expected and harmless; per D-25, do NOT add any new HTTP handler/route in the Go gateway for the regenerated Go types. Confirm via `git diff --stat` that no `gateway/**/handler*.go` or router file changed, only the generated `*.pb.go` files.
  </action>
  <verify>
    <automated>grep -q "relation_type_filter" engine/src/pb/lancet/v1/lancet.v1.rs</automated>
  </verify>
  <done>The proto's `QueryGraphRequest`/`QueryGraphResponse` are redefined per D-20 and `buf generate` has regenerated `engine/src/pb/lancet/v1/lancet.v1.rs` to carry the new field names (confirmed directly against the generated file, not via a full crate build — a whole-crate `cargo build` is expected to fail transiently at this point, since `main.rs`'s stub handler doesn't yet match the new fields; that failure is resolved by Task 2 and is not what this task's verify gates on).</done>
  <acceptance_criteria>
    - `grep -n "message QueryGraphRequest" -A 5 proto/lancet/v1/lancet.proto` shows `seed_entity_id`, `seed_entity_name`, `hop_depth`, `relation_type_filter`.
    - `grep -q "relation_type_filter" engine/src/pb/lancet/v1/lancet.v1.rs` confirms `buf generate` regenerated the Rust pb types against the new proto shape (this field is unique to the new contract, unlike `seed_entity_id` which could theoretically collide with unrelated content).
    - `git diff --name-only -- gateway/` touches only generated `*.pb.go` files, no handler/router source.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the QueryGraph handler — hop-cap ceiling, bidirectional traversal, Pitfall-6 relation-property recovery</name>
  <read_first>rust-guidelines.md, engine/src/main.rs (query_rag handler for structured-validation conventions and d1_status-style error mapping; the existing query_graph stub at ~1424-1431), engine/src/graph/mod.rs (clamp_hop_cap, traverse_multi_hop, fetch_neighborhood from Plan 01), .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-CONTEXT.md (D-20's Pitfall-6 discussion)</read_first>
  <files>engine/src/graph/mod.rs, engine/src/main.rs, engine/src/graph/tests.rs</files>
  <behavior>
    - Test 1 (unit, graph::tests): `clamp_hop_cap_with_ceiling` rejects a `configured_max` above the compile-time `MAX_HOP_CAP` by silently capping it to `MAX_HOP_CAP` (defense in depth against a misconfigured TOML value), and otherwise behaves like `clamp_hop_cap` bounded to whichever ceiling is lower.
    - Test 2 (integration, engine/src/tests.rs or graph::tests against a real temp DB): a `QueryGraph` call with `hop_depth = 0` or `hop_depth` above the configured ceiling returns `Status::invalid_argument` without any Cypher execution occurring (assert via a traversal call-count spy or by confirming no traversal-related log/error surfaces — whichever is more directly assertable given existing test doubles).
    - Test 3: a `QueryGraph` call with `hop_depth = 2` against a 3-entity fixture (seed → neighbor1 → neighbor2, matching the pattern of `three_entity_two_edge_fixture`/`two_entity_one_edge_fixture` in `graph::tests`) returns `QueryGraphEdge` entries with `relation_type`/`weight` populated for every returned edge — proving the correlate-back-to-edges_batch recovery works for the variable-hop case Pitfall 6 otherwise loses relationship properties on.
    - Test 4: a seed that is only ever the TARGET of an edge (never the source) still returns that edge in the response (D-24 bidirectionality, reused from Plan 01's `fetch_neighborhood`).
  </behavior>
  <action>
    In `engine/src/graph/mod.rs`, add `pub fn clamp_hop_cap_with_ceiling(requested: u32, configured_max: u32) -> Result<u32, GraphSpikeError>` — a NEW function alongside the existing `clamp_hop_cap` (do not modify `clamp_hop_cap`'s signature; it stays exactly as Phase 4 shipped it and its existing test stays untouched). Internally: clamp `configured_max` to `MAX_HOP_CAP.min(configured_max)` first (never let a misconfigured TOML value raise the ceiling above the compile-time constant), then apply the same `0`/`> effective_max` rejection logic `clamp_hop_cap` already uses.

    Add a `GraphConfigSettings { max_hop_cap: u32 }` struct to `main.rs`'s config block following `RetrievalConfigSettings`'s exact shape (`#[derive(Debug, Clone, Deserialize)]`, `#[serde(default = "default_max_hop_cap")]` where `default_max_hop_cap() -> u32 { 3 }`), nested under `EngineSettings` alongside the existing `retrieval` field, and threaded into `EffectiveRagSettings` the same way `retrieval` already is.

    Rewrite `query_graph()` (main.rs, replacing the current stub): extract the structured request; validate exactly one of `seed_entity_id`/`seed_entity_name` is non-empty (return `Status::invalid_argument` otherwise); if `seed_entity_name` was given, resolve it to an `entity_id` via a case-folded exact lookup against `self.database.entities_table()`'s `name` column (reuse the same `unicode_casefold` pattern Plan 01's entity-merge step already established) — return `Status::not_found` if no match. Clamp `hop_depth` via `clamp_hop_cap_with_ceiling(request.hop_depth, self.effective_settings.graph.max_hop_cap)`, mapping an `Err` to `Status::invalid_argument` BEFORE any traversal call, mirroring `query_rag`'s `RetrievalErrorKind` → `Status::invalid_argument` mapping pattern. Call `graph::fetch_neighborhood(&self.database, &seed_entity_id, hop_cap, true)` (D-24: always bidirectional) — this same function Plan 01 built. If `hop_cap == 1` and no `relation_type_filter` is set, call `traverse_fixed_hop` (relationship properties project directly, no Pitfall-6 issue). Otherwise call `traverse_multi_hop` (or `traverse_filtered_by_relation_type` when a filter is set) and implement D-20's option (b): after the Cypher result returns matched `seed.entity_id`/`neighbor.entity_id` pairs (no `r.relation_type`/`r.weight` for variable-length patterns per Pitfall 6), correlate each pair back against the `edges_batch` `fetch_neighborhood` already fetched into scope (before bridging) to recover `relation_type`/`weight` for the response's `QueryGraphEdge` entries — a node-id pair is not necessarily 1:1 with an edge row (two entities can be connected by more than one distinct relation, e.g. both `founded_by` and `acquired`), so emit ONE `QueryGraphEdge` per matching `edges_batch` row for that pair, not one per pair; do not issue a second lancedb or Cypher query to recover this data. Map `graph::GraphSpikeError` to `Status` (kind `InvalidHopCap` → `invalid_argument`, everything else → `internal`), mirroring how `query_rag` maps its own error kinds.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked clamp_hop_cap_with_ceiling query_graph_recovers_relation_properties_for_variable_hop query_graph_rejects_out_of_range_hop_depth query_graph_bidirectional_seed_as_target</automated>
  </verify>
  <done>QueryGraph is a real, structured, hop-bounded RPC reusing Plan 01's traversal pipeline; variable-hop responses still carry relation_type/weight per edge.</done>
  <acceptance_criteria>
    - `grep -n "clamp_hop_cap_with_ceiling" engine/src/graph/mod.rs` matches a `pub fn` declaration distinct from the existing `clamp_hop_cap`.
    - `grep -n "r#\"\{\"status\":\"scaffolding\"\}\"#" engine/src/main.rs` returns no matches (the literal stub response is gone).
    - The four named tests above pass under `cargo test --manifest-path engine/Cargo.toml --locked`.
    - `cargo test --manifest-path engine/Cargo.toml --locked clamp_hop_cap_rejects_zero_and_over_max` still passes unchanged, confirming the pre-existing `clamp_hop_cap` function and test were not modified.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gRPC caller → QueryGraph seed/hop_depth/relation_type_filter | The first genuinely caller-controlled hop_depth in this phase (Plan 01's RAG-path hardcodes 1-hop) — this is where an external gRPC caller's input reaches the `format!`-interpolated Cypher string |
| gRPC caller → seed_entity_name lookup | A caller-supplied entity name string is used in a case-folded exact-match predicate against `entities.name` — not free-text search, bounded to a single-row lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-09 | Tampering | `engine/src/graph/mod.rs` caller-controlled `hop_depth` into Cypher `format!` string | high | mitigate | `clamp_hop_cap_with_ceiling` rejects out-of-range values before any Cypher execution — this is the first plan where hop_depth is genuinely externally controlled (Plan 01's RAG-path used a hardcoded `1`), so it is rated high rather than Plan 01's medium |
| T-04.1-10 | Denial of Service | Unbounded `QueryGraph` call rate against `entities`/`edges` full-table-adjacent reads | low | accept | No new rate-limiting introduced this phase — consistent with the project's existing local-first, single-caller (Go gateway only, per D-25) threat posture; matches Phase 3's precedent of not rate-limiting internal gRPC calls |

**Canon-referral breadcrumb:** the `hop_depth`-into-Cypher-string risk is OWASP-injection canon, already covered as T-04.1-09/the reused `clamp_hop_cap` mechanism — not re-minted as a separate prohibition.
</threat_model>

<verification>
- `cargo build --manifest-path engine/Cargo.toml` and `cargo test --manifest-path engine/Cargo.toml --locked` both succeed.
- `git diff --name-only -- gateway/` shows only regenerated `*.pb.go` files — no new Go HTTP route (D-25).
- A manual `grpcurl`/gRPC client call (if available) against a locally running engine confirms `QueryGraph` returns structured `nodes`/`edges`, not `result_json`.
</verification>

<success_criteria>
`QueryGraph` is a real, structured, gRPC-only RPC bounded by a configurable hop-cap ceiling, reusing Plan 01's traversal pipeline, correctly recovering relation properties for variable-hop responses per D-20's option (b).
</success_criteria>

<output>
Create `.planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-03-SUMMARY.md` when done.
</output>