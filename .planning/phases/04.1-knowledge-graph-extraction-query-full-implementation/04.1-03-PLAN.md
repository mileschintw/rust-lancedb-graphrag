---
phase: 04.1-knowledge-graph-extraction-query-full-implementation
plan: 03
type: execute
wave: 3
depends_on: ["04.1-02"]
files_modified:
  - engine/src/main.rs
  - engine/src/graph/extraction.rs
  - engine/src/graph/bridge.rs
  - engine/src/graph/tests.rs
  - engine/src/tests.rs
autonomous: true
requirements: [DATA-04]

must_haves:
  truths:
    - "Extraction calls for chunks within one document run concurrently, bounded to 5 in flight (D-09), and a single chunk's extraction failure does not abort the document's other chunks or the document's completion (D-06) — collected via per-item Result handling, never try_collect's fail-fast short-circuit"
    - "The concurrency bound is proven by observed in-flight call count, not merely by the presence of buffer_unordered(5) in source text — a FakeExtractionGenerator with an artificial per-call delay records a maximum observed concurrency that is both >1 (real overlap occurred) and <=5 (the bound held)"
    - "Chunks whose content length falls below MIN_CHUNK_CONTENT_LENGTH skip the extraction call entirely (D-07) — this plan wires the placeholder constant Plan 02 declared into an actual skip check"
    - "Calling extract_and_persist_entities twice in direct sequence for the same document is idempotent by construction: its own delete-by-document_id step against entity_edges_table() (established in Plan 02) runs on every call, independent of replace_document_with_faults's unrelated edges_table() sweep — no duplicate entity_edges rows survive a second call, and no duplicate entity rows are created for names that already resolved"
    - "PROBE MAPPING (spec-less edge-coverage fallback, DATA-04 idempotency/concurrency categories): the idempotency category is covered by the truth immediately above plus the forced-failure-preserves-prior-graph truth below; the concurrency category is covered by this plan's first two truths (bounded, proven concurrency) plus Plan 02's serialized-single-worker invariant (no cross-document interleaving against the shared entities table) — these are not a separate, disconnected obligation from the REVIEWS.md findings this plan already resolves"
    - "REVIEWS.md HIGH ('re-ingestion can destroy previously valid graph edges' — Plan 02's Phase A/Phase B restructure is the design fix; this plan supplies the missing proof): forcing a real infrastructure failure during extract_and_persist_entities's Phase B (mutation) — after Phase B has already deleted this document's prior entity_edges rows but before the new rows are fully written — causes the function to return Err(..), and entity_edges_table()'s rows for that document_id after the failed call are BYTE-IDENTICAL (same edge_ids, same count) to what they were before the call, proving restore_version's rollback genuinely restores the pre-delete state rather than merely surfacing an error while leaving the table empty or partially written. This closes the exact gap Codex cited: 'Plan 03 only tests successful idempotent re-ingestion' — this plan now also tests the FAILED case"
    - "extract_and_persist_entities returns a typed ExtractionPersistSummary (not bare Result<(), String>) reporting prior_entity_edges_count (read immediately before Phase B's delete) and written_entity_edges_count (the final count for this document_id after Phase B completes); the caller in spawn_worker_with_boundary compares the two and logs a tracing::warn! coverage-regression signal whenever written_entity_edges_count < prior_entity_edges_count — surfacing D-06's per-chunk-failure-driven graph-completeness reduction as an observable signal on re-ingestion, rather than a fully silent Ok(()) that looks identical to a lossless re-ingestion"
    - "Extraction calls are retried per AI-SPEC §4b.1's bounded retry contract (up to 2 retries, 3 attempts total) on deserialize failure or GenerationErrorKind::SchemaValidation/ProviderError, and each ExtractionOutput's relation confidences are validated to fall within [0.0, 1.0] (finite) as a schema-validation-equivalent gate BEFORE persistence — never trusting OpenRouter's structured-output strict mode alone, per AI-SPEC's own 'never trust strict alone' guidance. Retry/failure logs carry only chunk_id, document_id, attempt number, and error kind/reason — NEVER the raw extracted text (a deliberate, documented deviation from AI-SPEC §4b.1's literal 'log the truncated raw model output' instruction, chosen to preserve this phase's own T-04.1-07/GraphSpikeError::Display no-row-values-in-logs discipline)"
    - "An entity whose source document is later replaced with content that no longer mentions it becomes inert but not corrupting: source_chunk_ids is NOT retroactively pruned in v1 (a documented, intentional limitation, matching the already-locked Test 5 behavior below — not silent ambiguity), the entity row itself is NOT deleted, and it remains a normal seed-match candidate. REVIEWS.md MEDIUM sharpening, this revision ('stale provenance becomes ambiguous, not merely stale' — chunk IDs are deterministic as {document_id}:{index}, so a stale source_chunk_ids entry does not merely point at content that no longer exists: after replacement, that SAME id now addresses the replacement document's chunk at that index, which may hold entirely unrelated text): a stale source_chunk_ids entry is documented and tested as POTENTIALLY MISLEADING provenance, not merely inert/dangling — accepted per D-33's broader 'accept and document, don't build speculative hardening' posture; chunk-generation-aware provenance tracking is 999.6's non-destructive-linking scope, not this phase's, to be revisited only if a future phase adds it"
    - "bridge_batch/bridge_batch_back never silently discard a second or later IPC-decoded RecordBatch (WR-01) — a real multi-batch fixture, constructed via a shared decode-all-batches helper both directions reuse, is either fully concatenated or produces an explicit error, never a truncated result"
  artifacts:
    - path: engine/src/graph/extraction.rs
      provides: "MIN_CHUNK_CONTENT_LENGTH wired into a real skip check; the concurrent per-document extraction runner; a chunk_id-keyed FakeExtractionGenerator variant with an optional artificial per-call delay and observed max-concurrency tracking, for deterministic concurrency tests; extract_with_retry (bounded 2-retry/3-attempt wrapper) and validate_extraction_output (confidence [0.0,1.0] gate)"
      contains: "MIN_CHUNK_CONTENT_LENGTH"
    - path: engine/src/graph/bridge.rs
      provides: "multi-batch-safe bridge_batch/bridge_batch_back (WR-01 fix) via a shared decode-all-batches helper"
      contains: "decode_all_batches"
    - path: engine/src/main.rs
      provides: "ExtractionPersistSummary { prior_entity_edges_count, written_entity_edges_count } — extract_and_persist_entities's upgraded return type (was Result<(), String>, now Result<ExtractionPersistSummary, String>), read by spawn_worker_with_boundary's call site to log a coverage-regression warning"
      contains: "struct ExtractionPersistSummary"
  key_links:
    - from: "engine/src/main.rs (extract_and_persist_entities)"
      to: "engine/src/graph/extraction.rs"
      via: "buffer_unordered(5)-bounded concurrent extraction calls (each wrapped in extract_with_retry), gathered and sorted back into chunk order before the sequential merge/persist step Plan 02 already established"
      pattern: "buffer_unordered"
    - from: "engine/src/main.rs (spawn_worker_with_boundary, extract_and_persist_entities call site established in Plan 02)"
      to: "engine/src/main.rs (ExtractionPersistSummary comparison)"
      via: "on Ok(summary), compare written_entity_edges_count against prior_entity_edges_count and tracing::warn! on regression; on Err, restore_version has already run inside extract_and_persist_entities (Plan 02), so the prior graph is preserved before this call site's own warn/error logging runs"
      pattern: "ExtractionPersistSummary"
  prohibitions:
    - statement: "MUST NOT present LLM-fabricated (hallucinated) entity/relationship claims — not actually supported by the source chunk text — as if they were reliably extracted, with no signal distinguishing extraction confidence from ground truth"
      status: unresolved
      verification: null
---

<objective>
Harden the extraction pipeline Plan 02 proved end-to-end: make chunk extraction genuinely concurrent (D-09) with a real content-length floor (D-07), and close 04-REVIEW.md's must-fix WR-01 bridge multi-batch bug now that the bridge runs against real, potentially multi-batch `entities`/`entity_edges` data instead of Phase 4's single-batch synthetic fixtures. Prove idempotent re-ingestion and define stale-entity semantics explicitly. Add AI-SPEC §4b.1's bounded extraction retry + confidence validation, and prove — via a real forced failure, not only a successful-path test — that re-ingestion's Phase-B mutation rollback (Plan 02) genuinely preserves the prior graph rather than risking silent data loss (REVIEWS.md HIGH).

Purpose: Plan 02's tracer intentionally used sequential single-chunk extraction to keep the architectural proof small; this plan delivers the actual D-09 concurrency contract and the correctness properties (idempotent re-ingestion, safe multi-batch IPC, defined stale-entity behavior, retried/validated extraction, and proven re-ingestion failure-safety) that make the pipeline production-grade rather than a scaled-up prototype.
Output: concurrent, bounded extraction; idempotent entity/entity_edges persistence; a WR-01-safe bridge; documented stale-entity semantics; retried/confidence-validated extraction; a proven-not-just-designed re-ingestion rollback guarantee with observable coverage-regression signaling.

**Spec-less prohibition-probe (this replan, since this phase has no plain SPEC.md with a `## Prohibitions` section):** ran the two-stage adversarial recall (Stage 1: ~10 raw candidates per DATA-04/DATA-05/RAG-05; Stage 2: drop routine engineering, keep values/safety/ethics) against this phase's three requirements. DATA-05 and RAG-05 yielded zero survivors after precision filtering — every Stage-1 candidate for those two either duplicated an already-`resolved` prohibition elsewhere in this phase's plans (entity conflation, orientation reversal, Cypher injection, trustworthiness-indistinguishability — all already closed) or was out of scope for a local-first, single-user tool with no auth boundaries (per PROJECT.md); this is the correct "clean requirement" precision outcome, not a coverage gap. DATA-04 yielded exactly one genuine, bespoke survivor — LLM extraction hallucination (fabricated relationships presented with no confidence/ground-truth distinction) — authored above in `must_haves.prohibitions` as `status: unresolved` (flagged, descriptor-less, per the spec-less fallback's explicit divergence rule: no wired mechanical check exists or is being built for hallucination detection in this phase's scope, so it is NOT marked `resolved`) — a real, open risk for future review, not a silent drop.

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-CONTEXT.md
@.planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-REVIEWS.md
@.planning/phases/04-knowledge-graph-extraction-query/04-REVIEW.md
@rust-guidelines.md
@engine/src/client/mod.rs
@engine/src/graph/bridge.rs
@engine/src/generation/mod.rs
</context>

## Artifacts This Phase Produces

- `engine/src/graph/extraction.rs` — real `MIN_CHUNK_CONTENT_LENGTH` skip check, a concurrent per-document extraction runner (function signature unchanged from Plan 02's caller expectations — only its internal body changes from sequential to `buffer_unordered(5)`), `FakeExtractionGenerator::with_keyed_responses(HashMap<String, Result<ExtractionOutput, GenerationError>>)`, `extract_with_retry(...)` (bounded 2-retry/3-attempt wrapper), `validate_extraction_output(...)` (confidence `[0.0,1.0]` gate).
- `engine/src/graph/bridge.rs` — `bridge_batch`/`bridge_batch_back` now handle 2+ IPC-decoded batches without data loss, via a shared `decode_all_batches` helper.
- `engine/src/main.rs` — `ExtractionPersistSummary { prior_entity_edges_count, written_entity_edges_count }`; `extract_and_persist_entities`'s return type upgraded from `Result<(), String>` (Plan 02) to `Result<ExtractionPersistSummary, String>`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Concurrent bounded extraction (D-09), real min-content-length skip (D-07), and idempotent re-ingestion</name>
  <read_first>rust-guidelines.md, engine/src/main.rs (extract_and_persist_entities from Plan 02, and the existing get_embeddings buffer_unordered(5) pattern in engine/src/client/mod.rs lines ~206-226), engine/src/graph/extraction.rs, engine/src/generation/mod.rs (FakeGenerator's call_count + queued-response shape, for the keyed variant below)</read_first>
  <files>engine/src/main.rs, engine/src/graph/extraction.rs, engine/src/tests.rs</files>
  <behavior>
    - Test 1: given a document with 6+ chunks and a `FakeExtractionGenerator::with_keyed_responses` configured to fail specifically for one chunk's `chunk_id` (keyed by ID, not queue position — REVIEWS.md MEDIUM: "the queued fake generator cannot reliably fail a specific chunk under concurrency" since `buffer_unordered(5)`'s completion order is not chunk order), `extract_and_persist_entities` still persists entities for every succeeding chunk, the failing chunk contributes zero entities, and the function itself returns `Ok(())` (D-06's per-chunk non-fatal contract, now proven under real concurrency).
    - Test 2: a chunk whose `content.trim().len()` is below `MIN_CHUNK_CONTENT_LENGTH` never reaches `extraction_generator.extract(...)` at all — assert via a `FakeExtractionGenerator` that records `call_count` and confirming it is less than the total chunk count for a document containing at least one trivially short chunk.
    - Test 3 (idempotency, corrected per REVIEWS.md — see action below): calling `extract_and_persist_entities` twice in direct sequence against the same job/database (no intervening `replace_document_with_faults` call needed, since Plan 02 made the delete-by-`document_id` step self-contained) results in exactly the same number of entity rows in `entities_table()` after the second call as after the first, exactly the same number of rows in `entity_edges_table()` for that `document_id` (proving the self-contained delete actually runs, not just entity-row dedup), and each entity's `source_chunk_ids` contains each of that document's contributing chunk ids exactly once.
    - Test 4: ingesting document A (mentioning entity "Acme") then document B (also mentioning "Acme", same case-normalized spelling) results in exactly one "Acme" entity row whose `source_chunk_ids` includes chunk ids from both documents — confirms cross-document, corpus-global merge (D-05) still works correctly once concurrency is introduced.
    - Test 5 (stale-entity semantics, REVIEWS.md MEDIUM): replacing document A (which introduced entity "Acme" with no other source) with new content that no longer mentions "Acme" — via `replace_document_with_faults` followed by `extract_and_persist_entities` — leaves the "Acme" entity row PRESENT in `entities_table()` with its original `source_chunk_ids` UNCHANGED (not pruned, not deleted) — asserting this is v1's documented behavior, not an oversight.
    - Test 5b (`stale_source_chunk_ids_can_reference_unrelated_replacement_content`, new, REVIEWS.md MEDIUM this round — sharpens Test 5's "inert" framing into the more precise "potentially misleading" one, without changing Test 5's asserted behavior): reuse Test 5's exact fixture and steps (document A introduces "Acme" at chunk index `i`, then is replaced with Acme-free content), but additionally replace document A's content such that the chunk at the SAME index `i` now contains clearly different, unrelated text (e.g. `"Unrelated replacement paragraph about widgets."`). After the second `extract_and_persist_entities` call, assert TWO things: (a) the "Acme" entity's `source_chunk_ids` still contains the chunk id `{document_id}:{i}` unchanged (Test 5's existing assertion, confirming no pruning), AND (b) directly querying `nodes_table()` for that exact chunk id returns a row whose `content` field is the NEW unrelated text, not anything resembling "Acme" — demonstrating concretely that the stale id now addresses different, unrelated content rather than merely "old" or "missing" content. This is a demonstration test, not a behavior-change test: it proves the documented risk is real and observable, it does not assert any new pruning/versioning behavior.
    - Test 6 (concurrency bound proof, REVIEWS.md MEDIUM this round: "Plan 03 does not prove the concurrency bound... only greps for buffer_unordered(5)"): ingest a document with 12 chunks (all above `MIN_CHUNK_CONTENT_LENGTH`), extracted via a `FakeExtractionGenerator::with_keyed_responses(...).with_delay(Duration::from_millis(20))` configured with 12 distinct successful keyed responses. After `extract_and_persist_entities` returns, assert `fake.max_observed_concurrency() <= 5` (the bound genuinely held) AND `fake.max_observed_concurrency() >= 2` (real overlap actually occurred during the run — a bare `<= 5` assertion alone would trivially pass even under accidental full serialization, which is exactly the unproven gap REVIEWS.md flagged).
  </behavior>
  <action>
    In `engine/src/graph/extraction.rs`, change `MIN_CHUNK_CONTENT_LENGTH` from a declared-but-unused placeholder (Plan 02) into an actually-enforced threshold: in `extract_and_persist_entities` (main.rs), before calling `extraction_generator.extract(...)` for a chunk, skip it (contribute zero entities/relations, no LLM call) if `chunk.content.trim().len() < graph::extraction::MIN_CHUNK_CONTENT_LENGTH`. Keep the value at `40` with the existing doc-comment rationale.

    Replace the per-chunk extraction loop's sequential `.await`s with a `futures::stream::iter(...).buffer_unordered(5)` pipeline matching `client::mod.rs`'s `get_embeddings` concurrency shape — critically, collect into a `Vec<(usize, Result<ExtractionOutput, GenerationError>)>` via `.collect::<Vec<_>>().await` (NOT `try_collect`, since D-06 requires every chunk's result to survive individually — `get_embeddings`'s `try_collect` is the WRONG pattern to copy here). Sort the collected results back into chunk order (mirroring `get_embeddings`'s `indexed.sort_unstable_by_key`) before the existing sequential merge/persist step runs.

    Add `FakeExtractionGenerator::with_keyed_responses(responses: std::collections::HashMap<String, Result<ExtractionOutput, GenerationError>>) -> Self` as an alternate constructor alongside Plan 02's existing queue-based `FakeExtractionGenerator::new`/`with_responses` — `extract()` on this variant looks up `request.chunk_id` in the map rather than popping a FIFO queue, making per-chunk-by-identity failure injection deterministic regardless of `buffer_unordered`'s completion order. Use this variant for Test 1.

    Add two fields to `FakeExtractionGenerator` unconditionally (used by every constructor, cheap when idle): `current_in_flight: Arc<AtomicUsize>` and `max_in_flight: Arc<AtomicUsize>`. In `extract()`, at entry: `let now = self.current_in_flight.fetch_add(1, Ordering::SeqCst) + 1; self.max_in_flight.fetch_max(now, Ordering::SeqCst);`; if a configured `delay: Duration` is set (default `Duration::ZERO`, no-op for every existing test), `tokio::time::sleep(delay).await` before producing the response; on exit, `self.current_in_flight.fetch_sub(1, Ordering::SeqCst);`. Add a builder method `pub fn with_delay(mut self, delay: Duration) -> Self` and an accessor `pub fn max_observed_concurrency(&self) -> usize { self.max_in_flight.load(Ordering::SeqCst) }`. Use `with_keyed_responses(...).with_delay(Duration::from_millis(20))` for Test 6.

    For Test 3's re-ingestion idempotency: call `extract_and_persist_entities` twice in direct sequence against the same `job`/`database` (this is now valid and correct per Plan 02's self-contained delete-by-`document_id` step against `entity_edges_table()` — no separate call to `replace_document_with_faults` is needed to make this idempotent, since `extract_and_persist_entities` owns its own cleanup independent of that function's unrelated `edges_table()` sweep). Assert both `entities_table().count_rows(...)` (or equivalent) AND `entity_edges_table().count_rows(...)` stay constant across the second call — asserting only entity-row counts would miss a scenario where entity dedup works but edges silently double.

    For Test 5: call `replace_document_with_faults` then `extract_and_persist_entities` for document A's first content, then call `replace_document_with_faults` then `extract_and_persist_entities` again for document A's second (Acme-free) content — assert the "Acme" entity row is still present with its original `source_chunk_ids`, and add a doc comment on `extract_and_persist_entities` stating this limitation explicitly, sharpened per REVIEWS.md MEDIUM this round: "v1 does not prune or delete entities when their source document changes; an entity can outlive its last supporting chunk. Because chunk IDs are deterministic ({document_id}:{index}, engine/src/main.rs) and reused positionally across a document's replacement, a stale source_chunk_ids entry does not merely point at content that no longer exists — after replacement, that SAME id now addresses the replacement document's chunk at that index, which may hold entirely unrelated text. A stale entry is therefore potentially MISLEADING provenance, not merely inert. This is accepted, documented behavior (matching D-33's broader accept-and-document posture) — not silently ambiguous — chunk-generation-aware provenance tracking belongs to 999.6's non-destructive entity-resolution scope, not this phase; revisit only if that phase (or another) adds it." Test 5b (below) demonstrates this concretely.

    For Test 5b: reuse Test 5's fixture, add one more `replace_document_with_faults` call replacing document A's chunk at the SAME index the "Acme" mention occupied with clearly unrelated text, then query `nodes_table()` directly for that chunk id and assert its `content` no longer resembles "Acme" while the entity's `source_chunk_ids` still lists that id (Test 5's assertion, re-confirmed) — this is a read-only demonstration, no new persistence logic.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked extraction_runs_concurrently_and_skips_short_chunks && cargo test --manifest-path engine/Cargo.toml --locked extraction_reingestion_is_idempotent_by_construction && cargo test --manifest-path engine/Cargo.toml --locked cross_document_entity_merge_still_works_under_concurrency && cargo test --manifest-path engine/Cargo.toml --locked stale_entity_survives_document_replacement && cargo test --manifest-path engine/Cargo.toml --locked stale_source_chunk_ids_can_reference_unrelated_replacement_content && cargo test --manifest-path engine/Cargo.toml --locked extraction_concurrency_bound_is_observed_not_assumed</automated>
  </verify>
  <done>Extraction runs concurrently bounded to 5 in-flight calls, chunks below the min-content-length threshold never trigger an LLM call, re-ingesting a document twice does not duplicate entity or entity_edges rows, and stale-entity behavior (including its potentially-misleading, not merely inert, provenance risk) is explicit and tested.</done>
  <acceptance_criteria>
    - `grep -n "buffer_unordered(5)" engine/src/main.rs` matches inside `extract_and_persist_entities`.
    - `grep -n "trim().len() < graph::extraction::MIN_CHUNK_CONTENT_LENGTH" engine/src/main.rs` matches (or an equivalent literal skip check).
    - `grep -n "with_keyed_responses" engine/src/graph/extraction.rs` matches a `pub fn` on `FakeExtractionGenerator`.
    - `grep -n "max_observed_concurrency" engine/src/graph/extraction.rs` matches a `pub fn` accessor, and `extraction_concurrency_bound_is_observed_not_assumed` asserts both a `<= 5` upper bound and a `>= 2` real-overlap lower bound (not just the upper bound alone).
    - `grep -n "fn stale_source_chunk_ids_can_reference_unrelated_replacement_content" engine/src/tests.rs` matches.
    - The seven named tests above (or clear equivalents) pass under `cargo test --manifest-path engine/Cargo.toml --locked`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix WR-01 — bridge_batch/bridge_batch_back silently drop multi-batch IPC streams</name>
  <read_first>rust-guidelines.md, engine/src/graph/bridge.rs, .planning/phases/04-knowledge-graph-extraction-query/04-REVIEW.md (WR-01 finding), .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-REVIEWS.md (Plan 02 "bridge test cannot produce two batches through the current public API" finding)</read_first>
  <files>engine/src/graph/bridge.rs, engine/src/graph/tests.rs</files>
  <behavior>
    - Test 1: a hand-built raw IPC byte stream containing TWO separate `RecordBatch`es of the same schema (constructed directly via `arrow_ipc::writer::StreamWriter` with two `.write(&batch)` calls before `.finish()` — built in the TEST itself, not through `bridge_batch`'s own single-input encode side, since that side by design only ever writes one batch per call) round-trips through a shared decode helper with ALL rows from BOTH batches present in the result — not just the first batch's rows.
  </behavior>
  <action>
    Refactor `bridge_batch` and `bridge_batch_back` (engine/src/graph/bridge.rs) to factor their read side into a shared, directly-testable helper — e.g. `fn decode_all_batches<R: std::io::Read>(reader: arrow_ipc_reader::StreamReader<R>) -> Result<RecordBatch, GraphSpikeError>` (one instantiation per arrow tree: `arrow_ipc`'s `StreamReader` for `bridge_batch_back`'s target type, `arrow_ipc_lg`'s for `bridge_batch`'s target type) that reads ALL batches the `StreamReader` yields (loop until `None`, collecting `Result`s) and concatenates them via `arrow::compute::concat_batches` (or each arrow tree's native equivalent) before returning — replacing the current `.next().transpose()...ok_or_else(...)` pattern, which reads only the first batch and silently drops the rest. Because this helper takes the `StreamReader` directly (not a caller-supplied single `RecordBatch`), the test can construct a genuine 2-batch byte stream independently (two `.write()` calls to a `StreamWriter` before `.finish()`) and feed it straight into the extracted decode helper — this is what makes Test 1 constructible at all, closing REVIEWS.md's "no way to produce two batches through the current public API" finding.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked bridge_preserves_all_rows_across_multiple_ipc_batches</automated>
  </verify>
  <done>A fixture proving a real 2-batch IPC stream no longer loses the second batch's rows passes, via a shared, directly-testable decode-all-batches helper.</done>
  <acceptance_criteria>
    - The new multi-batch test in `engine/src/graph/tests.rs` passes.
    - `grep -n "\.next()\.transpose()" engine/src/graph/bridge.rs` no longer appears as the terminal/only read of the `StreamReader` in either function.
    - `grep -n "fn decode_all_batches" engine/src/graph/bridge.rs` matches, and both `bridge_batch` and `bridge_batch_back` call it.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extraction retry + confidence validation (AI-SPEC §4b.1), and re-ingestion failure-safety proof (REVIEWS.md HIGH)</name>
  <read_first>rust-guidelines.md, engine/src/main.rs (extract_and_persist_entities's Phase A/Phase B structure, established Plan 02; restore_version, main.rs ~1500-1509), engine/src/graph/extraction.rs (ExtractionOutput/ExtractedRelation, FakeExtractionGenerator), engine/src/generation/mod.rs (GenerationErrorKind::SchemaValidation/ProviderError), engine/src/tests.rs (FailingEmbedder/PayloadEmbedder test doubles, ~line 3306-3328 — reuse these for the forced-failure test rather than inventing a new embedding test double), .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-AI-SPEC.md (§4b.1 "Retry logic" paragraph)</read_first>
  <files>engine/src/graph/extraction.rs, engine/src/main.rs, engine/src/tests.rs</files>
  <behavior>
    - Test 7 (`extraction_retries_then_succeeds_within_attempt_budget`, new): a `FakeExtractionGenerator` variant configured to return a deserialize-equivalent `GenerationError { kind: SchemaValidation, .. }` on its FIRST call for a given `chunk_id` and a valid `ExtractionOutput` on its SECOND call results in `extract_and_persist_entities` persisting the entities/relations from the SECOND (successful) attempt, and the fake generator's per-chunk call count for that `chunk_id` is exactly 2 (not 1, not 3) — proving the retry loop stops as soon as an attempt succeeds.
    - Test 8 (`extraction_retry_exhaustion_yields_zero_entities_not_function_failure`, new): a `FakeExtractionGenerator` configured to fail EVERY call (3 of 3 attempts) for a given `chunk_id` results in that chunk contributing zero entities/relations (D-06), the fake generator's per-chunk call count for that `chunk_id` is exactly 3, and `extract_and_persist_entities` as a whole still returns `Ok(summary)` — retry exhaustion for one chunk is D-06's per-chunk non-fatal case, categorically different from a Phase B infrastructure failure (Test 10 below).
    - Test 9 (`extraction_output_with_out_of_range_confidence_is_rejected_before_persistence`, new): a `FakeExtractionGenerator` returning a syntactically-valid `ExtractionOutput` whose one `ExtractedRelation.confidence` is `1.5` (out of `[0.0, 1.0]`) on its first two calls, then a valid in-range output on its third, results in the THIRD attempt's output being persisted (confirms `validate_extraction_output` treats an out-of-range confidence as a retry-triggering failure, "never trust strict alone" per AI-SPEC §4b.1) — and a variant of this same test where EVERY attempt returns an out-of-range confidence exhausts retries and contributes zero entities for that chunk, exactly like Test 8.
    - Test 10 (`extract_and_persist_entities_preserves_prior_graph_on_forced_persistence_failure`, new — closes REVIEWS.md HIGH "Plan 03 only tests successful idempotent re-ingestion"): call `extract_and_persist_entities` a first time with a working embedder (`PayloadEmbedder`, `engine/src/tests.rs` ~line 3318) and a `FakeExtractionGenerator` producing one new entity + relation, so `entity_edges_table()` gains at least one row for `job.document_id`; record that row's `edge_id`(s). Call `extract_and_persist_entities` a SECOND time (simulating re-ingestion) with a `FakeExtractionGenerator` returning a DIFFERENT new entity/relation pair (forcing at least one genuinely-new-name embedding call in Phase B) but with `embedder` swapped to `FailingEmbedder` (`engine/src/tests.rs` ~line 3306, already returns `Err` unconditionally). Assert the second call returns `Err(..)`, and assert `entity_edges_table()`'s rows for `job.document_id` after the failed second call are BYTE-IDENTICAL (same `edge_id`s, same count) to what they were after the first call — proving `restore_version`'s Phase-B rollback (Plan 02) genuinely restores the pre-delete state, not merely surfacing an error while leaving the table empty or partially written.
    - Test 11 (`extraction_persist_summary_reports_coverage_regression`, new): ingest a document with a `FakeExtractionGenerator` producing entities/relations for ALL of its chunks (a working first pass, `entity_edges_table()` ends up with N rows for `job.document_id`). Re-ingest the SAME document (a fresh `job`/chunk set with the same `document_id`) with a `FakeExtractionGenerator::with_keyed_responses` configured to fail (exhaust all 3 retries, Test 8's mechanism) for exactly one chunk that previously contributed an entity/relation. Assert the second call's returned `ExtractionPersistSummary` has `written_entity_edges_count < prior_entity_edges_count`, and that this specific scenario is distinguishable at the type level from Test 10's infrastructure-failure case (this one returns `Ok(summary)` with a visible regression; Test 10 returns `Err(..)` with prior data preserved) — these are two different, correctly-distinguished outcomes for two different causes.
  </behavior>
  <action>
    **Retry wrapper (engine/src/graph/extraction.rs):** add `pub(crate) async fn extract_with_retry(generator: &dyn ExtractionGenerator, request: ExtractionRequest) -> Result<ExtractionOutput, GenerationError>` implementing AI-SPEC §4b.1's bounded retry contract: up to 3 total attempts (the initial call plus 2 retries). Each attempt: call `generator.extract(request.clone()).await`; on `Ok(output)`, call `validate_extraction_output(&output)` (below) — if valid, return `Ok(output)` immediately (no further attempts); if EITHER the `extract` call returned `Err` OR `validate_extraction_output` rejected the output, and attempts remain, `tracing::warn!` with `chunk_id = request.chunk_id`, `document_id = request.document_id`, `attempt` (1-indexed), and `reason` (the `GenerationErrorKind` debug string, or a fixed `"confidence_out_of_range"`/similar literal for a validation rejection) — **deliberately never the raw extracted text or model output**, a documented deviation from AI-SPEC §4b.1's literal "log the truncated raw model output" instruction, chosen to preserve this phase's own T-04.1-07/`GraphSpikeError::Display` no-row-values-in-logs discipline (flag this deviation in a doc comment on `extract_with_retry` referencing both sources) — then retry; once attempts are exhausted, log the SAME warning one final time and return the last `Err` (constructing one via `GenerationError::new(GenerationErrorKind::SchemaValidation, "extraction output failed validation after retries")` if the final rejection was a validation failure rather than a provider `Err`).

    Add `pub(crate) fn validate_extraction_output(output: &ExtractionOutput) -> Result<(), String>`: for every `ExtractedRelation` in `output.relations`, reject (return `Err`) if `confidence` is non-finite or outside `[0.0, 1.0]`. This runs in addition to (never instead of) `serde_json::from_str::<ExtractionOutput>`'s own `deny_unknown_fields` structural validation and OpenRouter's `strict: true` JSON-Schema mode — "never trust strict alone" per AI-SPEC §4b.1.

    **Wire `extract_with_retry` into the concurrent pipeline (engine/src/main.rs):** in `extract_and_persist_entities`'s Phase A (established Plan 02), change each per-chunk future inside the `buffer_unordered(5)` stream from a direct `extraction_generator.extract(...)` call to `graph::extraction::extract_with_retry(extraction_generator, request).await` — the surrounding `buffer_unordered(5)`/`collect::<Vec<_>>()`/sort-back-into-order structure (this plan's Task 1) is unchanged; only the per-item future's body changes.

    **`ExtractionPersistSummary` and re-ingestion failure-safety proof (engine/src/main.rs — REVIEWS.md HIGH, "re-ingestion can destroy previously valid graph edges"):** add `pub(crate) struct ExtractionPersistSummary { pub prior_entity_edges_count: usize, pub written_entity_edges_count: usize }` and change `extract_and_persist_entities`'s return type from `Result<(), String>` (Plan 02) to `Result<ExtractionPersistSummary, String>`. Immediately before Phase B's delete-by-`document_id` step, count existing `entity_edges_table()` rows where `document_id = job.document_id` (a filtered scan/count against the table as it stands pre-delete) and store it as `prior_entity_edges_count`; after Phase B's writes complete, count the FINAL `entity_edges_table()` rows for `job.document_id` as `written_entity_edges_count`. Update the ONE call site (`spawn_worker_with_boundary`, established Plan 02): on `Ok(summary)`, if `summary.written_entity_edges_count < summary.prior_entity_edges_count`, `tracing::warn!` the two counts (a coverage-regression signal, never row content) before proceeding to mark the document `completed`; on `Err`, keep Plan 02's existing "log at `warn` and proceed to mark `completed`" behavior for the ordinary case, but this plan sharpens it: since Phase B's `restore_version` has ALREADY run inside `extract_and_persist_entities` by the time `Err` reaches this call site (Plan 02's design), the prior graph is preserved — log this specific case at `tracing::error!` instead of `warn!` (it indicates a genuine Phase-B infrastructure fault worth operator attention, even though D-06 still means the document proceeds to `completed` regardless — extraction remains additive, not a completion precondition).
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked extraction_retries_then_succeeds_within_attempt_budget && cargo test --manifest-path engine/Cargo.toml --locked extraction_retry_exhaustion_yields_zero_entities_not_function_failure && cargo test --manifest-path engine/Cargo.toml --locked extraction_output_with_out_of_range_confidence_is_rejected_before_persistence && cargo test --manifest-path engine/Cargo.toml --locked extract_and_persist_entities_preserves_prior_graph_on_forced_persistence_failure && cargo test --manifest-path engine/Cargo.toml --locked extraction_persist_summary_reports_coverage_regression</automated>
  </verify>
  <done>Extraction calls retry up to 3 total attempts on provider/schema/confidence-range failure before contributing zero entities for a chunk (D-06 still holds); a forced Phase-B infrastructure failure during re-ingestion is proven, not merely designed, to preserve the prior graph via restore_version; a D-06-driven coverage regression on re-ingestion is now observable via a typed summary rather than a silent Ok(()).</done>
  <acceptance_criteria>
    - `grep -n "fn extract_with_retry" engine/src/graph/extraction.rs` matches a `pub(crate) async fn`.
    - `grep -n "fn validate_extraction_output" engine/src/graph/extraction.rs` matches a `pub(crate) fn`.
    - `grep -n "extract_with_retry" engine/src/main.rs` matches inside `extract_and_persist_entities`'s Phase A concurrent pipeline.
    - `grep -n "struct ExtractionPersistSummary" engine/src/main.rs` matches, and `grep -n "Result<ExtractionPersistSummary, String>" engine/src/main.rs` matches `extract_and_persist_entities`'s signature.
    - `grep -n "written_entity_edges_count\|prior_entity_edges_count" engine/src/main.rs` shows both fields read at the `spawn_worker_with_boundary` call site (Plan 02's call site, updated by this task).
    - The five named tests above pass under `cargo test --manifest-path engine/Cargo.toml --locked`, and the full suite (`cargo test --manifest-path engine/Cargo.toml --locked`) remains green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Concurrent extraction results → shared in-memory known-entities accumulator | `buffer_unordered(5)`'s results are gathered into a `Vec` and re-sorted before the merge/persist step runs sequentially — no concurrent write access to the accumulator itself |
| Multi-batch IPC stream → bridged RecordBatch | A larger real `entities`/`entity_edges` read may legitimately split across multiple Arrow IPC stream messages depending on writer-side batching |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-09 | Information Disclosure / Integrity | `engine/src/graph/bridge.rs` silent multi-batch truncation (WR-01) | high | mitigate | Task 2's fix — a real graph read silently losing rows beyond the first IPC batch is a correctness bug on live data, per 04-CONTEXT.md's explicit carry-forward framing |
| T-04.1-10 | Denial of Service | Unbounded extraction concurrency | low | mitigate | `buffer_unordered(5)` bounds in-flight OpenRouter calls per document exactly as `client::mod.rs`'s existing `MAX_CONCURRENCY` convention already does for embeddings |
| T-04.1-17 | Repudiation / Integrity | Stale `source_chunk_ids` after document replacement (deterministic, positionally-reused chunk IDs) | medium | accept | REVIEWS.md MEDIUM, this round: a stale entry can address genuinely unrelated replacement content, not merely missing content (Test 5b demonstrates this concretely) — accepted per D-33's "accept and document, don't build speculative hardening" posture; chunk-generation-aware provenance tracking is 999.6's scope, not this phase's |
| T-04.1-18 | Information Disclosure | Extraction retry/validation-failure logging (`extract_with_retry`) | low | mitigate | Logs carry only `chunk_id`/`document_id`/attempt number/error kind or a fixed reason literal, never the raw extracted text or model output — a deliberate, documented deviation from AI-SPEC §4b.1's literal "log the truncated raw model output" instruction, chosen to preserve T-04.1-07/`GraphSpikeError::Display`'s no-row-values-in-logs discipline |

No canon-referral items this plan (no new external input surface beyond what Plan 02 already threat-modeled).
</threat_model>

<verification>
- `cargo test --manifest-path engine/Cargo.toml --locked` passes in full, including the thirteen new/updated tests this plan adds across its three tasks.
- Re-reading `extract_and_persist_entities`'s extraction-gathering step confirms `collect::<Vec<_>>()` is used (not `try_collect`), matching D-06's per-chunk non-fatal requirement.
- A forced Phase-B infrastructure failure during re-ingestion (Test 10) leaves `entity_edges_table()`'s rows for the affected `document_id` byte-identical to their pre-attempt state — not merely "an error was returned."
</verification>

<success_criteria>
Extraction is genuinely concurrent and bounded (D-09), trivially short chunks are skipped (D-07), re-ingestion is idempotent by construction (not by test-ordering luck), stale-entity behavior is explicit, tested, and correctly framed as potentially-misleading rather than merely-inert, the bridge no longer silently drops rows from a multi-batch IPC read (WR-01 closed), extraction calls retry with confidence validation per AI-SPEC §4b.1, and a forced Phase-B failure during re-ingestion is proven — not merely designed — to preserve the prior graph, with any D-06-driven coverage regression on a successful re-ingestion now observable via a typed summary.
</success_criteria>

<output>
Create `.planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-03-SUMMARY.md` when done.
</output>
