---
phase: 04.1-knowledge-graph-extraction-query-full-implementation
plan: 08
type: execute
wave: 8
depends_on: ["04.1-07"]
files_modified:
  - engine/src/prompt.rs
  - engine/src/tests.rs
  - engine/src/graph/extraction.rs
  - engine/src/graph/tests.rs
autonomous: true
gap_closure: true
requirements: [DATA-04, RAG-05]

must_haves:
  truths:
    - "pack_evidence_and_graph_prompt's tie-breaker between an Evidence candidate and a Graph candidate at an exact normalized-and-weighted score resolves in evidence's favor, matching the function's own doc comment ('ties are resolved... in evidence's favor') rather than silently favoring Graph (REVIEWS.md LOW, engine/src/prompt.rs:363-369) (test: pack_evidence_and_graph_prompt_breaks_exact_ties_in_evidence_favor)"
    - "validate_extraction_output emits a tracing::debug! event carrying the numeric out-of-range confidence value when a relation confidence fails validation, aiding diagnosis of schema-validation-failure root causes without ever logging raw extracted entity/relation text (REVIEWS.md LOW, engine/src/graph/extraction.rs) (test: validate_extraction_output_logs_confidence_field_on_out_of_range_failure)"
  artifacts:
    - path: engine/src/prompt.rs
      provides: "pack_evidence_and_graph_prompt's scored.sort_by tie-breaker comparator corrected from is_graph_b.cmp(&is_graph_a) to is_graph_a.cmp(&is_graph_b), so an exact-score tie places the Evidence candidate before the Graph candidate in a descending sort, matching the surrounding doc comment"
      contains: "is_graph_a.cmp(&is_graph_b)"
    - path: engine/src/tests.rs
      provides: "one new test proving evidence wins an exact-score tie against a graph fact under a token budget tight enough that only one of the two can be admitted"
      contains: "pack_evidence_and_graph_prompt_breaks_exact_ties_in_evidence_favor"
    - path: engine/src/graph/extraction.rs
      provides: "validate_extraction_output emits tracing::debug!(confidence = rel.confidence as f64, ...) immediately before returning Err on an out-of-range confidence"
      contains: "tracing::debug!"
    - path: engine/src/graph/tests.rs
      provides: "a small test-only tracing::field::Visit + tracing_subscriber::layer::Layer pair capturing debug-level 'confidence' event fields, plus one test proving the new debug! event fires with the correct numeric value"
      contains: "validate_extraction_output_logs_confidence_field_on_out_of_range_failure"
  key_links:
    - from: "engine/src/prompt.rs (pack_evidence_and_graph_prompt's scored.sort_by comparator)"
      to: "engine/src/prompt.rs (PackedEvidence.evidence / PackedEvidence.graph_facts)"
      via: "the corrected tie-break comparator determines which candidate is admitted first when a shared token budget can only fit one of two exactly-tied candidates — this plan's test proves the admitted candidate is now Evidence, not Graph, on an exact tie"
      pattern: "is_graph_a.cmp(&is_graph_b)"
    - from: "engine/src/graph/extraction.rs (validate_extraction_output's out-of-range branch)"
      to: "tracing subscriber (debug-level 'confidence' field)"
      via: "a new tracing::debug! call fires with the numeric confidence value before the function returns Err, captured in this plan's test via a dedicated tracing_subscriber::layer::Layer implementing on_event"
      pattern: "tracing::debug!"
  prohibitions: []
---

<objective>
Close the two remaining LOW-severity REVIEWS.md (Antigravity, 2026-08-08) findings that target already-executed Plans 05 and 03, whose PLAN.md text is historical record and cannot itself discharge a code-level finding: (1) `pack_evidence_and_graph_prompt`'s exact-score tie-breaker in `engine/src/prompt.rs` is inverted from its own doc comment, causing a `Graph` candidate to win an exact-score tie against an `Evidence` candidate rather than the documented "evidence wins ties" behavior; (2) `validate_extraction_output` (`engine/src/graph/extraction.rs`) logs only a generic categorical reason on a confidence-out-of-range failure, with no numeric detail to aid diagnosis. Both fixes are small, isolated, and independently verified.

**Empirically confirmed before this plan was written (not merely inferred from a code read):** the tie-break bug was reproduced and fixed in a throwaway local build — at `max_prompt_tokens=350`/`answer_token_budget=16` with one reserved evidence block, one additional evidence block, and one graph fact all reaching an exact 1.0-vs-1.0 normalized tie, the CURRENT (buggy) comparator admits the graph fact and excludes the second evidence block (`packed.evidence.len()==1`, `packed.graph_facts.len()==1`); after the one-line fix, the outcome flips to the documented behavior (`packed.evidence.len()==2`, `packed.graph_facts.len()==0`). The full pre-existing test suite (`cargo test --manifest-path engine/Cargo.toml --locked`) was also re-run against the fix and showed zero regressions, including `graph_fact_competes_for_shared_budget_beyond_reserved_slot` (`engine/src/tests.rs`) — that test's own large, deliberately-oversized "second" evidence block does not fit regardless of tie-break direction, so it is unaffected by this fix and needs no change.

**Prohibition-probe disposition (`must_haves.prohibitions: []`):** both tasks are small, isolated correctness/observability fixes against already-decided, already-shipped behavior — no new user-facing surface, no new optionality, no product/safety/ethics dimension. Stage 1/Stage 2 yield zero bespoke prohibitions, per the probe's own "clean-utility" worked example (matching the disposition Plan 06 and Plan 07 already recorded for their own review-driven fix tasks).

Purpose: two LOW-severity, single-reviewer findings against already-shipped code (Plans 03 and 05) need a real code change, which their own (executed) PLAN.md text cannot deliver — this plan is the closest still-open plan to carry both fixes.
Output: `pack_evidence_and_graph_prompt`'s tie-breaker corrected and regression-tested; `validate_extraction_output`'s out-of-range failure now logs the numeric confidence value, proven via a dedicated tracing capture test.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-REVIEWS.md
@rust-guidelines.md
@engine/src/prompt.rs
@engine/src/graph/extraction.rs
</context>

## Artifacts This Phase Produces

- `engine/src/prompt.rs` — `pack_evidence_and_graph_prompt`'s `scored.sort_by` tie-break comparator corrected to `is_graph_a.cmp(&is_graph_b)`.
- `engine/src/tests.rs` — new test `pack_evidence_and_graph_prompt_breaks_exact_ties_in_evidence_favor`.
- `engine/src/graph/extraction.rs` — `validate_extraction_output` gains a `tracing::debug!` call on its out-of-range branch.
- `engine/src/graph/tests.rs` — new test-only `ExtractionValidationDebugCaptureLayer`/`ExtractionValidationDebugVisitor` pair and new test `validate_extraction_output_logs_confidence_field_on_out_of_range_failure`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix the exact-score tie-breaker in pack_evidence_and_graph_prompt (REVIEWS.md LOW)</name>
  <read_first>engine/src/prompt.rs (pack_evidence_and_graph_prompt, ~line 255-436; the scored.sort_by comparator and its doc comment, ~line 356-369), engine/src/tests.rs (candidate_with_score ~line 5540; graph_facts_interleave_by_normalized_score ~line 5567 and graph_fact_competes_for_shared_budget_beyond_reserved_slot ~line 5606 as the existing fixture-construction pattern to adapt), .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-REVIEWS.md, .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-05-PLAN.md</read_first>
  <files>engine/src/prompt.rs, engine/src/tests.rs</files>
  <behavior>
    - Test: `pack_evidence_and_graph_prompt_breaks_exact_ties_in_evidence_favor` — with exactly one remaining (non-reserved) evidence candidate and exactly one graph fact (both normalize to 1.0 under the existing single-element min-max fallback, producing an exact score tie at `graph_weight=1.0`), and a token budget tight enough that only one of the two can be admitted alongside the reserved block, the Evidence candidate is admitted and the Graph candidate is excluded.
  </behavior>
  <action>
    In `engine/src/prompt.rs`'s `pack_evidence_and_graph_prompt`, locate the `scored.sort_by(|(score_a, candidate_a), (score_b, candidate_b)| { score_b.total_cmp(score_a).then_with(|| { let is_graph_a = ...; let is_graph_b = ...; is_graph_b.cmp(&is_graph_a) }) })` block (~line 363-369). Change the tie-break line from `is_graph_b.cmp(&is_graph_a)` to `is_graph_a.cmp(&is_graph_b)`. Do not change anything else in this function — the surrounding doc comment (~line 356-362, "ties are resolved explicitly below so graph facts are never silently deprioritized... ties broken in evidence's favor") already documents the INTENDED behavior this one-line change now actually implements; leave that comment as-is since it already describes the corrected behavior accurately.

    In `engine/src/tests.rs`, add `pack_evidence_and_graph_prompt_breaks_exact_ties_in_evidence_favor` near the other `pack_evidence_and_graph_prompt` tests (~line 5560-5850). Build `reserved = candidate_with_score("reserved", "Reserved top evidence block content for the question asked today.", 0.99)` and `tied_evidence = candidate_with_score("tied", "Second evidence block content also relevant to the question today.", 0.5)`. Build `evidence = assemble_evidence_blocks(&[reserved, tied_evidence])` and `facts = vec![GraphFactBlock { fact: GraphFact::new("Alice", "knows", "Bob", None, 0.5) }]` (import `crate::graph::context_strategy::GraphFact` and `crate::prompt::{assemble_evidence_blocks, pack_evidence_and_graph_prompt, GraphFactBlock}`, matching this file's existing per-test `use` style for these tests). Call `pack_evidence_and_graph_prompt("Question?", &evidence, &facts, 1.0, 350, 16).expect("pack succeeds")`. With exactly one remaining evidence candidate and one graph fact, both normalize to `1.0` regardless of their raw scores (the existing single-element min-max fallback in this function), and at `graph_weight=1.0` this produces an EXACT tie — this is deliberate, not incidental, and is the precise scenario the doc comment's "ties... in evidence's favor" describes. Assert `packed.evidence.len() == 2` (reserved + the tied evidence block, both admitted) and `packed.graph_facts.len() == 0` (the tied graph fact excluded) — on the CURRENT unfixed comparator this assertion fails with `evidence.len() == 1, graph_facts.len() == 1` (empirically confirmed before this plan was written); after the fix it passes as asserted.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked pack_evidence_and_graph_prompt_breaks_exact_ties_in_evidence_favor && cargo test --manifest-path engine/Cargo.toml --locked graph_fact_competes_for_shared_budget_beyond_reserved_slot && cargo test --manifest-path engine/Cargo.toml --locked graph_facts_interleave_by_normalized_score</automated>
  </verify>
  <done>The tie-break comparator is corrected; the new test proves evidence now wins an exact-score tie against a graph fact under a squeezed shared budget; the two pre-existing score-interleaving tests named above still pass unchanged (empirically confirmed: neither depends on tie-break direction).</done>
  <acceptance_criteria>
    - `grep -n "is_graph_a.cmp(&is_graph_b)" engine/src/prompt.rs` matches inside `pack_evidence_and_graph_prompt`'s `sort_by` closure.
    - `grep -n "is_graph_b.cmp(&is_graph_a)" engine/src/prompt.rs` returns NO match (the old, inverted comparator is gone, not duplicated alongside the fix).
    - `grep -n "fn pack_evidence_and_graph_prompt_breaks_exact_ties_in_evidence_favor" engine/src/tests.rs` matches.
    - The new test asserts both `packed.evidence.len() == 2` AND `packed.graph_facts.len() == 0` — not merely one of the two counts, so a partial/incorrect fix cannot pass by asserting only the easier half.
    - All three named tests in `<verify>` pass under `cargo test --manifest-path engine/Cargo.toml --locked`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add field-level tracing::debug! to validate_extraction_output on confidence-out-of-range failure (REVIEWS.md LOW)</name>
  <read_first>engine/src/graph/extraction.rs (validate_extraction_output, ~line 64-74; extract_with_retry's existing tracing::warn! pattern, ~line 94-101, for the no-raw-text logging discipline this task must preserve), engine/src/tests.rs (GraphAugmentationCaptureLayer/GraphAugmentationVisitor, ~line 6144-6183, as the closest existing tracing-capture pattern in this codebase — adapt its on_record shape to on_event, do not reuse it directly since it captures span field records, not event fields), .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-REVIEWS.md</read_first>
  <files>engine/src/graph/extraction.rs, engine/src/graph/tests.rs</files>
  <behavior>
    - Test: `validate_extraction_output_logs_confidence_field_on_out_of_range_failure` — calling `validate_extraction_output` with a relation confidence of `1.5` (out of the `[0.0, 1.0]` range) returns `Err` as before, AND emits a `tracing::debug!` event carrying a `confidence` field whose captured value equals `1.5_f64`.
  </behavior>
  <action>
    In `engine/src/graph/extraction.rs`'s `validate_extraction_output`, immediately BEFORE the existing `return Err(format!("relation confidence {} is out of range [0.0, 1.0]", rel.confidence))` line, add `tracing::debug!(confidence = rel.confidence as f64, "extraction output field-level validation failure: confidence out of range");`. Cast `rel.confidence` (an `f32`) to `f64` in the field expression — `tracing`'s field-value dispatch has a primitive `Value` implementation for `f64` but not `f32`, so casting avoids the field silently falling through to `record_debug` instead of `record_f64`. This carries only the numeric confidence value and a fixed diagnostic string — never raw extracted entity/relation text — preserving the same no-row-values-in-logs discipline `extract_with_retry`'s existing `tracing::warn!` calls already follow (T-04.1-07/`GraphSpikeError::Display` convention).

    In `engine/src/graph/tests.rs`, add a small test-only capture pair modeled on `engine/src/tests.rs`'s `GraphAugmentationCaptureLayer`/`GraphAugmentationVisitor` but adapted for EVENT capture rather than span-field capture: a struct `ExtractionValidationDebugCaptureLayer { captured: std::sync::Arc<std::sync::Mutex<Vec<f64>>> }`, and a visitor `ExtractionValidationDebugVisitor<'a> { captured: &'a std::sync::Arc<std::sync::Mutex<Vec<f64>>> }` implementing `tracing::field::Visit` with a `record_f64(&mut self, field: &tracing::field::Field, value: f64)` method that pushes `value` into `captured` when `field.name() == "confidence"`, and a no-op `record_debug` override (required by the `Visit` trait's lack of a default for it in this codebase's tracing version — mirror `GraphAugmentationVisitor`'s own explicit `record_debug` override, but body it as a no-op here since this test only cares about the `f64` field). Implement `tracing_subscriber::layer::Layer<S> for ExtractionValidationDebugCaptureLayer where S: tracing::Subscriber` with an `fn on_event(&self, event: &tracing::Event<'_>, _ctx: tracing_subscriber::layer::Context<'_, S>)` method that constructs the visitor and calls `event.record(&mut visitor)` — this differs from `GraphAugmentationCaptureLayer`'s `on_record` (which observes span field UPDATES via `tracing::span::Record`) because `tracing::debug!` produces a standalone EVENT, not a span field record; `on_event` is the correct hook for capturing event fields.

    Add `validate_extraction_output_logs_confidence_field_on_out_of_range_failure`: build `captured: Arc<Mutex<Vec<f64>>> = Arc::new(Mutex::new(Vec::new()))`, construct the layer, and register it via `tracing_subscriber::registry().with(tracing_subscriber::filter::LevelFilter::DEBUG).with(layer)` then `let _guard = tracing::subscriber::set_default(subscriber);` (the `LevelFilter::DEBUG` layer is required because this crate's ambient log level in tests may otherwise filter out `debug!`-level events before they reach the capture layer). Build an `ExtractionOutput { entities: vec![], relations: vec![ExtractedRelation { source: "A".into(), target: "B".into(), relation_type: "knows".into(), confidence: 1.5 }] }` (import `super::extraction::{ExtractionOutput, ExtractedRelation}`, or call fully-qualified as `super::extraction::ExtractionOutput`/`super::extraction::ExtractedRelation` matching this file's existing preference for calling `graph::` internals via `super::` rather than adding new names to the top-of-file `use super::{...}` list). Call `super::extraction::validate_extraction_output(&output)`, assert it is `Err`. Assert `captured.lock().unwrap().as_slice() == [1.5_f64]` — the debug event fired exactly once with the correct numeric value.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked validate_extraction_output_logs_confidence_field_on_out_of_range_failure</automated>
  </verify>
  <done>validate_extraction_output emits a tracing::debug! event carrying the numeric out-of-range confidence value on validation failure; the new capture test proves the event fires with the correct value; the function's existing Err return behavior and no-raw-text logging discipline are both unchanged.</done>
  <acceptance_criteria>
    - `grep -n "tracing::debug!" engine/src/graph/extraction.rs` matches inside `validate_extraction_output`'s body, positioned before its `return Err(...)` line.
    - `grep -n "confidence = rel.confidence as f64" engine/src/graph/extraction.rs` matches (confirms the `f32`-to-`f64` cast, required for `tracing`'s `record_f64` dispatch).
    - `grep -n "fn validate_extraction_output_logs_confidence_field_on_out_of_range_failure" engine/src/graph/tests.rs` matches.
    - `grep -n "fn on_event" engine/src/graph/tests.rs` matches inside the new `Layer` impl (confirms event capture, not span-field capture via `on_record`).
    - The new test asserts the captured `Vec<f64>` equals exactly `[1.5_f64]` — not merely that it is non-empty — so a field-name mismatch (e.g. capturing the wrong field) cannot pass by coincidence.
    - The test passes under `cargo test --manifest-path engine/Cargo.toml --locked`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shared evidence/graph-fact token budget → packed prompt composition | The boundary Task 1's fix crosses: which of two exactly-tied candidates is admitted into the LLM prompt when budget cannot fit both |
| `validate_extraction_output`'s failure path → tracing/log output | The boundary Task 2's fix crosses: diagnostic detail added to a failure log must stay numeric-only, never raw extracted entity/relation text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-27 | Repudiation | `engine/src/prompt.rs` `pack_evidence_and_graph_prompt` tie-break comparator (pre-fix inversion) | low | mitigate | REVIEWS.md LOW fix: correct `is_graph_b.cmp(&is_graph_a)` to `is_graph_a.cmp(&is_graph_b)` so an exact-score tie silently favoring the non-citable `Graph` candidate over citable `Evidence` (contradicting the documented intent) can no longer occur; narrow blast radius (exact-score ties only), regression-tested |
| T-04.1-28 | Information Disclosure | `engine/src/graph/extraction.rs` `validate_extraction_output` new `tracing::debug!` call | low | mitigate | The new event carries only the numeric `confidence` value and a fixed diagnostic string — never raw extracted entity/relation text — preserving the existing `GraphSpikeError::Display`/T-04.1-07 no-row-values-in-logs discipline; regression-tested via a dedicated capture test asserting the exact captured value |

**Canon-referral breadcrumb:** neither threat introduces a new trust boundary or new caller-controlled input — both are narrow, isolated correctness/observability fixes against already-shipped, already-accepted code paths; not re-litigated here.
</threat_model>

<verification>
- `cargo build --manifest-path engine/Cargo.toml` succeeds.
- `cargo test --manifest-path engine/Cargo.toml --locked` runs the full suite once at the end of this plan's work and shows 0 failed (confirms no regression against Plan 06/07's fixes or any pre-existing test, including the two named pre-existing `pack_evidence_and_graph_prompt` tests this plan's fix touches).
- Both named tests in this plan's tasks are present in source and pass individually, per each task's own `<acceptance_criteria>`.
</verification>

<success_criteria>
`pack_evidence_and_graph_prompt`'s exact-score tie-breaker now matches its own documented intent (evidence wins ties), proven by a new regression test and confirmed not to regress either pre-existing score-interleaving test. `validate_extraction_output` now logs the numeric out-of-range confidence value on failure, proven by a new tracing-capture test, without introducing any raw-text logging. Both of REVIEWS.md's remaining LOW-severity findings against already-executed Plans 03/05 are closed with real code changes landed in this (previously unexecuted) plan.
</success_criteria>

<output>
Create `.planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-08-SUMMARY.md` when done.
</output>
