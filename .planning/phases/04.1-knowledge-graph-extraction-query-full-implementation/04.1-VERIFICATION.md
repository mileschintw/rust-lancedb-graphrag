---
phase: 04.1-knowledge-graph-extraction-query-full-implementation
verified: 2026-08-09T00:00:00Z
status: passed
score: 4/4 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 3/4
  gaps_closed:
    - "Cypher-narrowing-on-disagreement proof (constrain_to_cypher_matched / cypher_confirmed_neighbor_ids) — closed by Plan 06, tests run individually and pass: constrain_to_cypher_matched_narrows_response_when_cypher_confirms_fewer_neighbors, constrain_to_cypher_matched_preserves_full_response_when_cypher_confirms_all_neighbors, cypher_confirmed_neighbor_ids_reflects_real_traverse_multi_hop_execution"
    - "MAX_TOTAL_EDGES single-hop-overflow rejection proof — closed by Plan 06, test run and passes: fetch_neighborhood_rejects_oversized_edge_batch (note: this closed only the single-hop-overflow case; the distinct multi-hop pre-dedup over-counting defect discovered by the 2026-08-08 review pass is what this round's Plan 09 closes)"
    - "Old CR-01 (dedup_edges_by_identity 4-tuple collision risk) — closed by Plan 06, re-keyed on edge_id, test run and passes: fetch_neighborhood_returns_both_edges_when_two_documents_share_identical_fact"
    - "GraphFact orientation preservation on RAG-path seed-as-target — closed by Plan 07, test run and passes: graph_fact_preserves_stored_edge_orientation_when_seed_is_target"
    - "Real ingestion-worker-queue path proof for extraction/persistence/traversal — closed by Plan 07, test run and passes: worker_queue_extracted_graph_facts_reach_provider_request_body"
    - "WR-01 (old numbering, final-hop MAX_FRONTIER_SIZE not checked) — closed by Plan 06, test run and passes: fetch_neighborhood_rejects_oversized_final_hop_frontier"
    - "Plan 06's own regression guard (query_graph_bidirectional_seed_as_target, protecting the deleted empty-confirmed-set fallback) — independently run, passes"
    - "REVIEWS.md two LOW findings (prompt tie-breaker, extraction confidence logging) — closed by Plan 08, tests run and pass: pack_evidence_and_graph_prompt_breaks_exact_ties_in_evidence_favor, validate_extraction_output_logs_confidence_field_on_out_of_range_failure"
    - "Fresh CR-01 (2026-08-08 review pass): fetch_neighborhood's MAX_TOTAL_EDGES bound counted raw per-hop row sums before dedup, causing false-positive rejection of genuinely in-bounds bidirectional hop_cap>=2 query_graph requests — closed by Plan 09, independently re-verified by direct code read (seen_edge_ids: HashSet<String> now accumulates distinct edge_id values, checked via seen_edge_ids.len() > MAX_TOTAL_EDGES) and by running the new regression test individually: fetch_neighborhood_accepts_in_bounds_neighborhood_despite_raw_recount_exceeding_max_total_edges (asserts Ok, exactly 499 rows, exactly 499 distinct edge_id values — all three present, not a partial proof), plus both pre-existing bound-rejection tests re-run to confirm no regression: fetch_neighborhood_rejects_oversized_edge_batch, fetch_neighborhood_rejects_oversized_final_hop_frontier, plus Plan 06's own downstream regression guard: query_graph_bidirectional_seed_as_target"
  gaps_remaining: []
  regressions: []
deferred: []
---

# Phase 04.1: Knowledge Graph Extraction & Query — Full Implementation Verification Report

**Phase Goal:** As a Lancet engineer, I want to extract entities and relationships from chunks during ingestion, persist them as a knowledge graph in LanceDB, and traverse them at query time via the now-confirmed lance-graph integration, so that RAG queries are grounded with graph context alongside the existing hybrid-retrieval evidence.

**Verified:** 2026-08-09
**Status:** passed
**Re-verification:** Yes — after gap-closure Plan 09 (previous verification 2026-08-08, status: gaps_found, score 3/4)

## Goal Achievement

### Observable Truths (Roadmap Success Criteria)

| # | Truth (ROADMAP SC) | Status | Evidence |
|---|---|---|---|
| 1 | Rust engine extracts entities and relationships from chunks during ingestion | ✓ VERIFIED | Unaffected by Plan 09 (`git show --stat 9dc9ad2` confirms the commit touches only `engine/src/graph/mod.rs`, `engine/src/graph/tests.rs`, `engine/src/tests.rs`, and this phase's own SUMMARY — no extraction-path files). Carried forward from the 2026-08-08 round unchanged. |
| 2 | Graph data is stored in LanceDB tables | ✓ VERIFIED | Unaffected by Plan 09. Carried forward unchanged. |
| 3 | Queries successfully traverse graph context to compile additional prompts for the LLM | ✓ VERIFIED | The sole remaining prior gap — `fetch_neighborhood`'s `MAX_TOTAL_EDGES` bound wrongly rejecting genuinely in-bounds bidirectional `hop_cap>=2` traversals — is CLOSED. Independently confirmed by direct code read of `engine/src/graph/mod.rs`: `total_edge_count: usize` is fully removed (`grep -n "total_edge_count"` returns no match); a `seen_edge_ids: HashSet<String>` now accumulates each hop's `edge_id` column values (fail-closed `Err(Bridge)` on missing/wrong-type column, mirroring `dedup_edges_by_identity`'s existing pattern) and the bound check is `if seen_edge_ids.len() > MAX_TOTAL_EDGES`. `MAX_TOTAL_EDGES` production constant is unchanged at 500. Read the new regression test's full body (`engine/src/tests.rs:6604-6690`) and confirmed its arithmetic: `hop_cap=3`, fixture is seed→hub1, hub1→hub2, then 497 hub2→leaf edges all sharing one `leaf_id` (raw per-hop sum reaches 501, distinct count stays 499); ran it individually — PASS, and its three load-bearing assertions (returns `Ok`, `edges_batch.num_rows() == 499`, distinct `edge_id` set size `== 499`) are all present in source, not merely claimed. Also ran both pre-existing bound-rejection tests (`fetch_neighborhood_rejects_oversized_edge_batch`, `fetch_neighborhood_rejects_oversized_final_hop_frontier`) — both still PASS, no regression. Also ran Plan 06's own downstream regression guard on the same modified function's response path (`query_graph_bidirectional_seed_as_target`) — PASS. |
| 4 | Define ContextAssemblyStrategy trait/enum and implement SourceChunks fallback strategy (Port for 999.5) | ✓ VERIFIED | Unaffected by Plan 09. Carried forward unchanged. |

**Score:** 4/4 roadmap success criteria VERIFIED. SC #3's last open gap (fresh CR-01, fetch_neighborhood MAX_TOTAL_EDGES pre-dedup over-counting) is now closed and independently re-proven, not merely accepted from SUMMARY.md/REVIEW.md claims.

### Truth-Verification Disposition (Step 5b)

Plan 09's two `must_haves.truths` are both declared `verification: backstop` (non-inferable from presence alone). Both resolve VERIFIED here because explicit evidence was gathered directly, not assumed:

- **"fetch_neighborhood tracks MAX_TOTAL_EDGES against distinct edge_id values"** — VERIFIED via direct source read of the replaced counting block (`seen_edge_ids: HashSet<String>`, fail-closed column read, `seen_edge_ids.len() > MAX_TOTAL_EDGES` check) plus the doc-comment update ("enforced against distinct edge_id values").
- **"a regression test proves this"** — this is the behavior-dependent half (a cross-hop accumulation invariant that symbol presence alone cannot prove). It is promoted from PRESENT_BEHAVIOR_UNVERIFIED to VERIFIED specifically because the single named test (`fetch_neighborhood_accepts_in_bounds_neighborhood_despite_raw_recount_exceeding_max_total_edges`) was run individually by this verifier (not inherited from SUMMARY.md) and its source body was read to confirm the fixture arithmetic and all three assertions are genuinely present, per Step 7b.

### Re-verification: Gap Closure Detail

All items from the 2026-08-08 verification round (score 3/4, one remaining `gaps:` entry) are closed. This round's closure test was run individually (`cargo test --manifest-path engine/Cargo.toml --locked --bin engine "tests::<name>" -- --exact` / `--lib "graph::tests::<name>" -- --exact`), not inferred from SUMMARY.md claims:

| Prior gap | Closure plan | Test | Run result |
|---|---|---|---|
| Fresh CR-01: `fetch_neighborhood`'s `MAX_TOTAL_EDGES` bound counted raw per-hop rows before dedup, over-rejecting valid in-bounds `hop_cap>=2` bidirectional requests | 09 | `fetch_neighborhood_accepts_in_bounds_neighborhood_despite_raw_recount_exceeding_max_total_edges` | ✓ PASS |
| Regression check: single-hop overflow still correctly rejected | 09 | `fetch_neighborhood_rejects_oversized_edge_batch` | ✓ PASS |
| Regression check: final-hop frontier overflow still correctly rejected | 09 | `fetch_neighborhood_rejects_oversized_final_hop_frontier` | ✓ PASS |
| Regression check: downstream response path through the modified function | — (Plan 06's own guard) | `query_graph_bidirectional_seed_as_target` | ✓ PASS |
| WR-04 (misleadingly-named test, renamed): `narrow_via_cypher_fails_open_on_invalid_hop_cap` | 09 | `narrow_via_cypher_fails_open_on_invalid_hop_cap` | ✓ PASS |

**Regressions:** None found. No previously-passing behavior broke. `git status` confirms a clean working tree at HEAD (`9dc9ad2`) — what was tested is what is committed.

### Deferred Items

None.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `engine/src/graph/mod.rs` | `fetch_neighborhood`'s `MAX_TOTAL_EDGES` check counts distinct `edge_id` values via `seen_edge_ids: HashSet<String>` | ✓ VERIFIED | Confirmed by direct read: `total_edge_count` fully removed (no match), `seen_edge_ids` present (4 occurrences: declaration, insert, length check, doc comment reference), fail-closed `"missing edge_id column"` present twice (once in the new counting loop, once in pre-existing `dedup_edges_by_identity`) |
| `engine/src/tests.rs` | New Plan 09 regression test | ✓ VERIFIED | `fetch_neighborhood_accepts_in_bounds_neighborhood_despite_raw_recount_exceeding_max_total_edges` present, full body read, all three assertions (`Ok`, 499 rows, 499 distinct edge_ids) confirmed present and passing |
| `engine/src/graph/tests.rs` | WR-04 rename | ✓ VERIFIED | `narrow_via_cypher_fails_open_on_invalid_hop_cap` present with accurate doc comment (mentions `CypherExecute`/`CypherParse` residual gap explicitly); old name `narrow_via_cypher_fails_open_on_genuine_cypher_error` no longer present (confirmed no match) |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `fetch_neighborhood`'s per-hop `entity_edges` `.select([..., "edge_id", ...])` | `seen_edge_ids` running `HashSet<String>` | each hop's `edge_id` column read into the accumulator before the bound check runs | ✓ WIRED | Confirmed at `engine/src/graph/mod.rs` — read directly, column read loop immediately precedes the `seen_edge_ids.len() > MAX_TOTAL_EDGES` check |
| `query_graph` RPC | `fetch_neighborhood(hop_cap=effective_settings.graph.max_hop_cap)` | explicit RPC, honors configured hop cap (default 3) | ✓ WIRED, DEFECT CLOSED | This is the call site previously exposed to the over-rejection defect at `hop_depth >= 2` — the defect is now fixed inside the shared function itself |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| In-bounds bidirectional multi-hop neighborhood no longer wrongly rejected | `cargo test --manifest-path engine/Cargo.toml --locked --bin engine "tests::fetch_neighborhood_accepts_in_bounds_neighborhood_despite_raw_recount_exceeding_max_total_edges" -- --exact` | 1 passed | ✓ PASS |
| Single-hop oversized edge batch still correctly rejected (no regression) | `cargo test --manifest-path engine/Cargo.toml --locked --bin engine "tests::fetch_neighborhood_rejects_oversized_edge_batch" -- --exact` | 1 passed | ✓ PASS |
| Final-hop frontier bound still enforced (no regression) | `cargo test --manifest-path engine/Cargo.toml --locked --bin engine "tests::fetch_neighborhood_rejects_oversized_final_hop_frontier" -- --exact` | 1 passed | ✓ PASS |
| Downstream response path through the modified function still correct | `cargo test --manifest-path engine/Cargo.toml --locked --bin engine "tests::query_graph_bidirectional_seed_as_target" -- --exact` | 1 passed | ✓ PASS |
| WR-04 rename passes with accurate scope | `cargo test --manifest-path engine/Cargo.toml --locked --lib "graph::tests::narrow_via_cypher_fails_open_on_invalid_hop_cap" -- --exact` | 1 passed | ✓ PASS |

Full workspace regression suite (`cargo test --manifest-path engine/Cargo.toml --locked` + `cd gateway && go test ./...`) was run by the orchestrator immediately before this dispatch and reported green (194+ tests, 0 failures). This verifier did not re-run the full suite (per spot-check constraints) but independently re-ran the five named tests above, plus confirmed via `git show --stat 9dc9ad2` and `git status` that the tested commit is exactly what is on disk and the tree is clean.

### Anti-Patterns Found

Anti-pattern table below uses the 2026-08-09 04.1-REVIEW.md's refreshed numbering (note: this renumbers relative to the 2026-08-08 verification round's table — old WR-01/WR-02/WR-03 are now WR-02/WR-03/WR-04, and a new WR-01 was surfaced by this pass).

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `engine/src/graph/mod.rs` | 377-402 (contrasted with the new counting loop at 356-364) | WR-01 (new, 2026-08-09 review): frontier-computation loop fails *open* (silently skips a batch) on a missing/malformed `source_node_id`/`target_node_id` column, immediately adjacent to this plan's own counting loop which correctly fails *closed* on the same failure class for `edge_id` | ⚠️ Warning | Reachable only via schema drift or a future `select()` projection change, not ordinary caller-controlled input (`hop_depth`, seed, `relation_type_filter`) — the same 6-column `.select(...)` projection that includes both `edge_id` and `source_node_id`/`target_node_id` means a missing column would also break the terminal `concat_batches` call. Same reachability class as WR-02/WR-03 below (both previously classified WARNING, not blocking). Does not open a gap against SC #3 — no caller-reachable path exercises it. |
| `engine/src/main.rs` | 1146, 1156 | WR-02 (was WR-01 in the 2026-08-08 round): `attempt_graph_augmentation` hardcodes `hop_cap=1`, ignoring `settings.graph.max_hop_cap` | ⚠️ Warning | Explicitly deferred by Plan 09 (config-plumbing, not counting-correctness); incidentally shields this call site from ever needing this round's fix, since the double-count condition requires `hop_cap>=2` |
| `engine/src/main.rs` | 2702-2712 | WR-03 (was WR-02): entity update is a non-atomic delete-then-add with a real, sequentially-awaited per-entity visibility gap | ⚠️ Warning | Data-visibility race under concurrent readers, not a correctness-of-persisted-data issue |
| `engine/src/main.rs` | 1864-1865, 1911-1912 | WR-04 (was WR-03): `query_graph`'s post-narrowing Arrow filters silently fall back to unfiltered data on failure with no `tracing::warn!` | ⚠️ Warning | Silent-failure observability gap |
| `engine/src/main.rs`, `engine/src/db/mod.rs`, `engine/src/bin/seed_rag_fixture.rs` | various | IN-01 through IN-05 (2026-08-09 review, carried forward unchanged): unreachable silent-default constructor, duplicate SQL-escaping helper, unused `communities` table accessor, duplicated `RetrievalSnapshot` construction, non-iterating `while let` in a dev fixture tool | ℹ️ Info | No functional impact; noted for completeness |

No `TBD`/`FIXME`/`XXX` debt markers found in any file modified by Plan 09 (`engine/src/graph/mod.rs`, `engine/src/tests.rs`, `engine/src/graph/tests.rs`), confirmed by direct grep.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| DATA-04 | Plans 01-03, 07 | Extract entities/relationships during ingestion, persist as graph nodes/edges in LanceDB | ✓ SATISFIED | Unaffected by Plan 09; carried forward |
| DATA-05 | Plans 02, 04, 05, 06, 07, 09 | Query graph context with lance-graph/Cypher-style pattern matching, compile into RAG prompt context | ✓ SATISFIED | Previously PARTIALLY SATISFIED pending the fresh CR-01 bound-checking defect; now fully satisfied — the standalone `query_graph` RPC no longer wrongly rejects genuinely in-bounds `hop_depth >= 2` bidirectional traversal requests |
| RAG-05 | Plan 02, 08 | Define `ContextAssemblyStrategy` enum/trait, `PrecomputedSemantics`/`SourceChunks`, default `SourceChunks` | ✓ SATISFIED | Unaffected by Plan 09; carried forward |

No orphaned requirement IDs: all three (DATA-04, DATA-05, RAG-05) declared across this phase's plan frontmatters (including Plan 09's `requirements: [DATA-05]`) are accounted for above.

### Human Verification Required

None. The one previously-open loose thread — genuine `CypherParse`/`CypherExecute` fail-open coverage (WR-04's original name overclaimed this) — is not an unproven SC-supporting behavior requiring human judgment. It is an accepted, explicitly documented test-coverage gap on an error branch: the renamed test's own doc comment states plainly that this residual case remains untested because forcing a real `lance-graph` 0.5.4 execution-time failure would require unverifiable internals, and records it as an accepted gap rather than silently implying coverage. This is Info-level, not a human-verification item, and does not gate `passed` status.

### Gaps Summary

None. Plan 09 closed the sole remaining gap from the 2026-08-08 verification round. Independently re-verified by this pass (not inherited from 04.1-09-SUMMARY.md or 04.1-REVIEW.md):

1. Direct code read of `engine/src/graph/mod.rs` confirms `total_edge_count` fully removed and replaced by `seen_edge_ids: HashSet<String>`, checked via `seen_edge_ids.len() > MAX_TOTAL_EDGES` after a fail-closed per-hop `edge_id` column read.
2. Direct read of the new regression test's full body confirms the fixture arithmetic (hop_cap=3, 499 distinct edges via 497-way convergence on one leaf entity, raw sum 501 vs. distinct 499) and all three load-bearing assertions are genuinely present in source.
3. Five named tests run individually by this verifier (not the orchestrator's pre-dispatch full-suite claim): the new regression test, both pre-existing bound-rejection tests, Plan 06's own downstream regression guard on the modified function's response path, and the WR-04 rename — all PASS, no regressions.
4. `git show --stat 9dc9ad2` confirms the change surface matches 04.1-REVIEW.md's claim (3 source files + this plan's SUMMARY), and `git status` confirms a clean tree — what was tested is what is committed.
5. The one fresh finding surfaced by the 2026-08-09 review (new WR-01, frontier loop fail-open inconsistency) was independently assessed for materiality against SC #3: it requires schema drift on `source_node_id`/`target_node_id` specifically while `edge_id` (read from the same batch, same `.select(...)` projection) remains readable — a scenario not reachable through any caller-controlled input, and the same reachability class as the pre-existing WR-02/WR-03 findings already classified WARNING rather than blocking in the prior round. It does not open a new gap against SC #3.

All four ROADMAP success criteria are now VERIFIED. Phase goal achieved.

---

_Verified: 2026-08-09_
_Verifier: Claude (gsd-verifier)_
