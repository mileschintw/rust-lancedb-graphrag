---
phase: 04.1-knowledge-graph-extraction-query-full-implementation
verified: 2026-08-07T00:00:00Z
status: gaps_found
score: 2/4 must-haves verified
behavior_unverified: 2
overrides_applied: 0
gaps:
  - truth: "Plan 02: 'lance-graph's Cypher engine now determines MEMBERSHIP in the RAG-path's returned GraphFacts ... proven by a direct unit test on constrain_to_cypher_matched fed a deliberately-reduced confirmed-set' (Test 3d/3e), and 'cypher_confirmed_neighbor_ids_reflects_real_traverse_multi_hop_execution' (Test 3f) proving a real lance-graph CypherQuery::execute() call correctly extracts the confirmed-reachable set"
    status: failed
    reason: "constrain_to_cypher_matched and cypher_confirmed_neighbor_ids are pub(crate) functions with ZERO call sites anywhere in the crate outside their own definitions and narrow_via_cypher's internal composition (confirmed via grep across engine/src/). No test ever calls either function directly, and no test constructs a scenario where Cypher's confirmed-reachable set is a strict subset of the BFS-discovered set to prove the response is genuinely narrowed on disagreement. The only test exercising narrow_via_cypher (`cypher_narrowing_fail_open`, engine/src/graph/tests.rs:315) proves fail-open on a nonexistent seed; `query_graph_recovers_multi_hop_edge_relation_properties` (engine/src/tests.rs:5325) proves narrow_via_cypher is a no-op when Cypher AGREES with BFS on real, non-adversarial data — its own doc comment says exactly this ('narrow_via_cypher step is correctness-preserving on real, non-adversarial data'). Since real BFS and real Cypher traverse the identical persisted graph, natural disagreement essentially never occurs — the deliberately-reduced-confirmed-set unit test was the ONLY way this plan's own must_haves specified to prove Cypher is genuinely response-affecting (the central fix this replan revision exists to deliver, per REVIEWS.md Codex HIGH: 'Cypher is present in production but not load-bearing'). That specific, required proof is absent from the codebase."
    artifacts:
      - path: engine/src/graph/mod.rs
        issue: "constrain_to_cypher_matched (line 557) and cypher_confirmed_neighbor_ids (line 523) are pub(crate) with no test call sites"
    missing:
      - "A unit test that calls graph::constrain_to_cypher_matched directly with a cypher_neighbor_ids set that is a strict subset of the BFS-discovered neighbor set, asserting the returned (entities, edges) batches exclude the unconfirmed neighbor and its edge"
      - "A unit test (or integration test) that calls graph::cypher_confirmed_neighbor_ids directly against a real fixture and asserts the returned HashSet matches the true neighbor set via a genuine traverse_multi_hop execution, independent of narrow_via_cypher's fallback wrapper"
  - truth: "Plan 02 Test 3b: 'fetch_neighborhood against a fixture where the seed has more entity_edges rows in a single hop than MAX_TOTAL_EDGES returns Err(GraphSpikeError), not a silently-truncated Ok'"
    status: failed
    reason: "MAX_TOTAL_EDGES (engine/src/graph/mod.rs:294) is enforced in fetch_neighborhood's hop loop (line 367), but no test anywhere in the crate exercises the over-limit path — grep for MAX_TOTAL_EDGES shows only the constant declaration and its two production call sites (declaration + check), zero references from engine/src/graph/tests.rs or engine/src/tests.rs. The cap-enforcement code is present and plausible by inspection but its correctness (does it actually return Err, does it fire at the right threshold, does frontier growth still get bounded on the final hop per code-review finding WR-01 below) is unproven."
    artifacts:
      - path: engine/src/graph/mod.rs
        issue: "MAX_TOTAL_EDGES / MAX_FRONTIER_SIZE cap-exceeded paths have no test coverage"
    missing:
      - "A fixture-driven test constructing more than MAX_TOTAL_EDGES entity_edges rows for one seed and asserting fetch_neighborhood returns Err(GraphSpikeError{kind: Bridge, ..})"
  - truth: "Plan 04.1-REVIEW.md CR-01: dedup_edges_by_identity's (source_node_id, target_node_id, relation_type, weight) 4-tuple identity key is claimed (Plan 04 SUMMARY key-decision) not to risk collapsing genuinely distinct edges from separate documents"
    status: failed
    reason: "The code review (04.1-REVIEW.md, CRITICAL, filed the same day as the phase's final commit; HEAD is that review commit with no follow-up fix) demonstrates this claim is false: dedup_edges_by_identity (engine/src/graph/mod.rs:485-521) keys on a 4-tuple that omits edge_id, the table's only genuinely unique identifier, which is never selected into the batch (the .select(...) calls at lines 353-358 deliberately exclude it). Two distinct edges (different edge_id, different document_id) with a coincidentally-identical 4-tuple -- plausible under this codebase's own temperature=0.0 deterministic extraction config -- are silently collapsed into one, dropping a real edge from every query response that traverses it. This is a known-wrong identity key with a named, small fix already proposed by the review, not merely an untested code path."
    artifacts:
      - path: engine/src/graph/mod.rs
        issue: "dedup_edges_by_identity (lines 485-521) keys on a 4-tuple that omits edge_id; the two .select(...) calls (lines 353-358, 429-433) never fetch edge_id into the batch at all"
    missing:
      - "Select edge_id alongside the existing columns in fetch_neighborhood's edges .select(...) call"
      - "Add edge_id to the local edges_schema built inside fetch_neighborhood"
      - "Change dedup_edges_by_identity to key its seen set on edge_id alone (or edge_id plus the existing tuple, for defense in depth) instead of the current 4-tuple"
deferred: []
behavior_unverified_items:
  - truth: "Plan 02 Test 3c: attempt_graph_augmentation's GraphFact rendering preserves the TRUE stored edge orientation when the seed is the edge's target_node_id (not source), never rendering a reversed triple"
    test: "Seed a fixture where the seed entity is entity_edges.target_node_id for its only edge, call attempt_graph_augmentation directly (or query_rag end-to-end), and assert the resulting GraphFact's entity_a_name/entity_b_name preserve the true stored direction, not the reverse"
    expected: "GraphFact renders '{true_source_name} —{relation}→ {seed_name}', never '{seed_name} —{relation}→ {true_source_name}'"
    why_human: "No test named graph_fact_preserves_stored_edge_orientation (or equivalent) exists against attempt_graph_augmentation specifically. The equivalent QueryGraph-path test (query_graph_bidirectional_seed_as_target, engine/src/tests.rs:5460) proves the edge is still returned when the seed is the target, but does not assert QueryGraphEdge.source_entity_id/target_entity_id ordering is the true stored direction rather than reversed. The orientation-preserving code itself (main.rs's attempt_graph_augmentation reads source_node_id/target_node_id straight off edges_batch) was confirmed present by direct code read, but its seed-as-target correctness is not exercised by any assertion that would fail if the orientation were silently reversed."
  - truth: "Plan 02: entity extraction, persistence, and lance-graph traversal proven through the REAL ingestion worker path (spawn_worker_with_boundary), not a direct call to extract_and_persist_entities that bypasses the gRPC/queue path"
    test: "Ingest a document through the real gRPC IndexDocument path (or spawn_worker/spawn_worker_with_boundary directly), let the worker call extract_and_persist_entities as part of its normal completion flow, then query_rag end-to-end and capture the outbound HTTP body"
    expected: "The queued/worker-driven path — not a direct unit-level call to extract_and_persist_entities — is what a real client uses; both should behave identically, but no test currently proves it for the graph path specifically"
    why_human: "graph_weight_reaches_actual_provider_request_body (engine/src/tests.rs:6043) and the underlying extraction/persistence tests all call extract_and_persist_entities directly (and process_job/process_job_with_boundary for chunk persistence), not through spawn_worker/spawn_worker_with_boundary's queue-driven flow. The production wiring of extraction_generator into spawn_worker in main() (confirmed present, main.rs:3057-3074) is real, but no test exercises the full worker-queue path with a real FakeExtractionGenerator end-to-end the way the schema/prompt path is exercised. Worker-level ingestion tests (worker_indexes_jobs_and_records_real_chunk_count etc.) do not assert on entities_table()/entity_edges_table() contents."
---

# Phase 04.1: Knowledge Graph Extraction & Query — Full Implementation Verification Report

**Phase Goal:** As a Lancet engineer, I want to extract entities and relationships from chunks during ingestion, persist them as a knowledge graph in LanceDB, and traverse them at query time via the now-confirmed lance-graph integration, so that RAG queries are grounded with graph context alongside the existing hybrid-retrieval evidence.

**Verified:** 2026-08-07
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Roadmap Success Criteria)

| # | Truth (ROADMAP SC) | Status | Evidence |
|---|---|---|---|
| 1 | Rust engine extracts entities and relationships from chunks during ingestion | ✓ VERIFIED | `engine/src/graph/extraction.rs` implements `ExtractionGenerator`/`OpenRouterExtractionGenerator`/`FakeExtractionGenerator`; `extract_and_persist_entities` (main.rs:2410) is a real two-phase (read-then-mutate) implementation with `buffer_unordered(5)` concurrency (Plan 03), `MIN_CHUNK_CONTENT_LENGTH` skip, retry/confidence validation. Production wiring confirmed: `main()` constructs a real `OpenRouterExtractionGenerator` from live settings and threads it into `spawn_worker` (main.rs:3057-3074) before any job is processed. `cargo test` run: 152 passed / 0 failed / 1 ignored in the main `engine` test binary; extraction-specific tests (`extraction_reingestion_is_idempotent_by_construction`, `extraction_concurrency_bound_is_observed_not_assumed`, cross-doc merge, stale-entity semantics, Phase B rollback byte-identical proof at tests.rs:4909) all present and passing. |
| 2 | Graph data is stored in LanceDB tables | ✓ VERIFIED | `entities_schema()`/`entity_edges_schema()`/`entities_table()`/`entity_edges_table()` confirmed in `engine/src/db/mod.rs` (lines 144-296); `table_schemas()` returns a 7-tuple including `entities`/`entity_edges`; `nodes_schema()` reduced to 19 fields (4 dormant columns removed). `db::tests::initializes_and_validates_all_table_schemas` passes. Entity-relation edges are structurally isolated in a dedicated `entity_edges` table, never `edges_table()` (grep-confirmed: `fetch_neighborhood`/`extract_and_persist_entities` reference only `entity_edges_table`). |
| 3 | Queries successfully traverse graph context to compile additional prompts for the LLM | ✗ FAILED | Traversal, Cypher-membership-narrowing, and prompt-compilation code paths all exist and are wired into production (`attempt_graph_augmentation` runs on every `/rag/query` before the `final_candidates` check, confirmed at main.rs:1426-1448; `query_graph` RPC is a real, tested handler at main.rs:1726 replacing the former stub; `pack_evidence_and_graph_prompt` is the actual function `openrouter.rs`'s `execute_one_call` calls). HOWEVER: (a) the load-bearing claim that lance-graph Cypher execution is genuinely response-affecting — the central fix this phase's replan revision exists to deliver — has no direct test proving `constrain_to_cypher_matched`/`cypher_confirmed_neighbor_ids` narrow a response when Cypher disagrees with BFS (see Gaps); (b) a real, unresolved CRITICAL code-review finding (CR-01) shows the edge-deduplication step can silently drop genuinely distinct edges across documents, undermining traversal correctness for any corpus with repeated facts; (c) `MAX_TOTAL_EDGES`'s reject-don't-truncate behavior is untested. |
| 4 | Define ContextAssemblyStrategy trait/enum and implement SourceChunks fallback strategy (Port for 999.5) | ✓ VERIFIED | `engine/src/graph/context_strategy.rs`: `pub enum ContextAssemblyStrategy { PrecomputedSemantics, SourceChunks }`, `impl Default` returning `SourceChunks`, `GraphFact` with private string fields and the sole escaping `GraphFact::new(...)` constructor, `Serialize`-only derive (no `Deserialize`) confirmed. Real production call site confirmed: `engine/src/prompt.rs:394-395` calls `ContextAssemblyStrategy::SourceChunks.assemble(fact)` inside `pack_evidence_and_graph_prompt`, which is the function `openrouter.rs`'s `execute_one_call` actually sends to OpenRouter — not test-only wiring. |

**Score:** 2/4 roadmap success criteria VERIFIED (SC #1, #2, #4); SC #3 is FAILED -- the core traversal/prompt-compilation mechanism is present and wired (which is why this is not a total failure of the phase), but two of its load-bearing correctness claims are either unproven (Cypher-narrowing-on-disagreement, MAX_TOTAL_EDGES rejection) or demonstrated false and unfixed (CR-01 edge-dedup defect). Three gap entries recorded below.

### Deferred Items

None — no gaps here map cleanly to a later milestone phase; all findings are within this phase's own declared scope (Plans 02/04's own must_haves explicitly promise the missing tests).

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `engine/src/db/mod.rs` | `entities_schema()`, `entity_edges_schema()`, accessors, 7-tuple `table_schemas()` | ✓ VERIFIED | Confirmed present, wired, tested |
| `engine/src/graph/extraction.rs` | `ExtractionGenerator` trait, `OpenRouterExtractionGenerator`, `FakeExtractionGenerator` | ✓ VERIFIED | Present, wired into production `main()`, tested |
| `engine/src/graph/context_strategy.rs` | `ContextAssemblyStrategy`, escaping `GraphFact` | ✓ VERIFIED | Present, wired into `prompt.rs`'s real packing path |
| `engine/src/graph/mod.rs` | `fetch_neighborhood`, `narrow_via_cypher`/`cypher_confirmed_neighbor_ids`/`constrain_to_cypher_matched`, `escape_sql_literal`, `clamp_hop_cap_with_ceiling` | ⚠️ PARTIAL | All functions exist and compile; `fetch_neighborhood`/`narrow_via_cypher` are wired and exercised; `constrain_to_cypher_matched`/`cypher_confirmed_neighbor_ids` are wired internally but have zero direct test coverage (see Gaps) |
| `engine/src/prompt.rs` | `GraphFactBlock`, `pack_evidence_and_graph_prompt`, score-interleaving (Plan 05) | ✓ VERIFIED | Confirmed real call site in `openrouter.rs`; score-interleaving, `graph_weight==0.0` hard-exclude, and empty-slice guards all have passing named tests |
| `proto/lancet/v1/lancet.proto` / `engine/src/main.rs` `query_graph` | Structured `QueryGraphRequest`/`QueryGraphResponse`, real handler | ✓ VERIFIED | Stub `{"status":"scaffolding"}` replaced; 10+ named `query_graph_*` tests pass; no Go gateway HTTP wrapper exists (confirmed via grep of `gateway/`, D-25 honored) |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `spawn_worker_with_boundary` | `extract_and_persist_entities` | called after successful chunk persistence, before marking document completed | ✓ WIRED | Confirmed in main.rs; production `extraction_generator` threaded from `main()` |
| `query_rag` (post-embedding) | `attempt_graph_augmentation` → `fetch_neighborhood` → `narrow_via_cypher` | runs before `final_candidates.is_empty()` branch, on every query | ✓ WIRED | Confirmed at main.rs:1426-1448; span-recorded outcome tag proven observable end-to-end for all three outcomes (`graph_augmentation_succeeded/no_match_found/attempted_and_failed_is_observable_end_to_end`, all passing, including a genuine forced-fault deleted-directory test) |
| `GenerationRequest.graph_facts`/`graph_weight` | `openrouter.rs` `execute_one_call` → `pack_evidence_and_graph_prompt` | same function both main.rs's pre-check and the real send path call | ✓ WIRED | Plan 05 discovered and fixed a real bug where `execute_one_call` had been calling the graph-blind `pack_evidence_prompt`; now fixed and proven via raw-HTTP-capture test `graph_weight_reaches_actual_provider_request_body` (passing) |
| `attempt_graph_augmentation` (after `fetch_neighborhood`) | `narrow_via_cypher` → `cypher_confirmed_neighbor_ids`/`constrain_to_cypher_matched` | restored, response-affecting production Cypher call site | ⚠️ WIRED BUT UNPROVEN | Call site confirmed present and positioned correctly; the claim that this link's result is genuinely response-affecting (differs from a no-op) has no direct test — see Gaps |

### Behavioral Spot-Checks / Test Run

Full workspace test suite run once (`cargo test --manifest-path engine/Cargo.toml --locked`), output captured to scratchpad log, ~4 min build + run:

| Suite | Result |
|---|---|
| `engine` lib/unit/integration tests | 152 passed; 0 failed; 1 ignored |
| `inspect_lancedb` bin tests | 18 passed; 0 failed |
| `seed_rag_fixture` bin tests | 0 tests |
| `config_startup` integration tests | 9 passed; 0 failed |
| Doc-tests | 0 tests |

Named-test cross-check against plan `<verify>` blocks (grepped from saved log):

| Named test (from plan) | Found in codebase | Status |
|---|---|---|
| `graph_weight_reaches_actual_provider_request_body` | yes, passing | ✓ PASS |
| `graph_augmentation_succeeded_is_observable_end_to_end` | yes, passing | ✓ PASS |
| `graph_augmentation_no_match_found_is_observable_end_to_end` | yes, passing | ✓ PASS |
| `graph_augmentation_attempted_and_failed_is_observable_end_to_end` | yes, passing (real forced fault: deleted `entities.lance` dir) | ✓ PASS |
| `query_rag_valid_zero_match` | yes, passing | ✓ PASS |
| `extraction_reingestion_is_idempotent_by_construction` | yes, passing | ✓ PASS |
| `extraction_concurrency_bound_is_observed_not_assumed` | yes, passing | ✓ PASS |
| `query_graph_recovers_multi_hop_edge_relation_properties` | yes, passing | ✓ PASS |
| `ingestion_extracts_persists_and_traverses_graph_context_end_to_end` | NOT FOUND under this name | renamed/absorbed — equivalent coverage exists via `attempt_graph_augmentation_scoring_and_neighborhood` (thin, NoMatchFound-only) + `graph_weight_reaches_actual_provider_request_body` (full E2E but bypasses the worker queue) |
| `graph_fact_preserves_stored_edge_orientation` | NOT FOUND under this name or equivalent for the RAG path specifically | see behavior_unverified_items |
| `fetch_neighborhood_includes_seed_row` | NOT FOUND under this name | equivalent coverage exists via `query_graph_nodes_include_seed_when_unfiltered` (real temp-dir DB, proves seed inclusion) |
| `fetch_neighborhood_rejects_oversized_edge_batch` | NOT FOUND, no equivalent | genuine gap — see Gaps |
| `constrain_to_cypher_matched_preserves_full_response_when_cypher_confirms_all_neighbors` | NOT FOUND, no equivalent | genuine gap — see Gaps |
| `constrain_to_cypher_matched_narrows_response_when_cypher_confirms_fewer_neighbors` | NOT FOUND, no equivalent | genuine gap — see Gaps |
| `cypher_confirmed_neighbor_ids_reflects_real_traverse_multi_hop_execution` | NOT FOUND, no equivalent | genuine gap — see Gaps |
| `narrow_via_cypher_falls_back_to_unconstrained_on_cypher_error` | NOT FOUND under this name | equivalent coverage via `cypher_narrowing_fail_open` (nonexistent-seed case) |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `engine/src/graph/mod.rs` | 353-358, 477-521 | Unresolved CRITICAL code-review finding (CR-01): `dedup_edges_by_identity` keys on `(source_node_id, target_node_id, relation_type, weight)`, omitting `edge_id` (the table's actual unique identifier, never selected into the batch). Two distinct edges from different documents with a coincidentally-identical 4-tuple (plausible under `temperature=0.0` deterministic extraction) are silently collapsed into one. Filed in `04.1-REVIEW.md` (HEAD commit `a1950db`), no fix commit exists after it. | 🛑 Blocker-class (data correctness) | Undermines SC #3's "successfully traverse graph context" for any corpus containing repeated facts across documents |
| `engine/src/graph/mod.rs` | 322-413 | WR-01 (code review, unresolved): `MAX_FRONTIER_SIZE` is checked only at the top of each loop iteration — the final hop's `next_frontier`, folded into `visited` at line 407, is never checked before being used to build the terminal `entity_id IN (...)` predicate. A high-degree final hop can produce an unbounded predicate string. | ⚠️ Warning | Defense-in-depth gap, not exploitable beyond a large-but-valid seed |
| `engine/src/main.rs` | 1751-1757 (per review) | WR-03: `seed_entity_id` UUID validation in `QueryGraph` does not enforce v4/RFC4122 the way `validate_document_id`/`session_id` do elsewhere in the same file | ℹ️ Info | Inconsistent-but-not-exploitable validation contract |
| `engine/src/main.rs` | 1758-1828 (per review) | WR-04: asymmetric not-found behavior between name-based (`Status::not_found`) and ID-based (`Ok` with empty response) seed resolution in `QueryGraph`, undocumented/untested as intentional | ℹ️ Info | API-consistency gap |
| `.planning/REQUIREMENTS.md` | 15, 23, 24 | DATA-04/DATA-05/RAG-05 are checked `[x]` but still carry stale "SPIKE ONLY ... deferred to Phase 04.1" annotations, now inaccurate since this phase delivered the full implementation. Plan 05's own summary notes `gsd-tools query requirements.mark-complete DATA-05` returned `table_unmatched` (no traceability-table row exists to update). | ℹ️ Info (documentation) | No functional impact; flagged as expected phase-transition follow-up per Plan 05's own summary |

No `TBD`/`FIXME`/`XXX` debt markers found in phase-modified files.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| DATA-04 | Plans 01-03 | Extract entities/relationships during ingestion, persist as graph nodes/edges in LanceDB | ✓ SATISFIED (with the CR-01 correctness caveat above) | `entities`/`entity_edges` tables, real `OpenRouterExtractionGenerator` wired into production, concurrent bounded extraction, idempotent re-ingestion, forced-failure rollback all tested and passing |
| DATA-05 | Plans 02, 04, 05 | Query graph context with lance-graph/Cypher-style pattern matching, compile into RAG prompt context | ⚠️ PARTIALLY SATISFIED | Both the always-on RAG-path augmentation and the standalone `QueryGraph` RPC exist, are wired, and mostly pass their named tests — but the specific "Cypher genuinely narrows the response on disagreement" claim (the central fix this phase's replan revision exists to deliver) is untested; CR-01's dedup defect also bears directly on this requirement |
| RAG-05 | Plan 02 | Define `ContextAssemblyStrategy` enum/trait, `PrecomputedSemantics`/`SourceChunks`, default `SourceChunks` | ✓ SATISFIED | Confirmed defined, tested, and wired into the real production prompt-packing path (not merely test-only) |

**Requirements traceability note:** REQUIREMENTS.md's own Traceability table (lines 47-53) lists only RAG-02/03/04 — it does not list DATA-04/DATA-05/RAG-05, so no requirement IDs are orphaned against this phase; all three IDs declared in this phase's plan frontmatter are accounted for above. The `[x]` checkboxes plus stale "SPIKE ONLY" annotations are a documentation gap (Info-level), not a coverage gap — Plan 05's own summary already flags this as expected phase-transition follow-up.

### Judgment-Tier Prohibitions (non-authoritative LLM-judge pass)

Plans 01/02/04/05 record `status: resolved` prohibitions with a `resolution:` narrative but no `verification:` field (judgment-tier, not test-tier). Per verification-overrides policy, these get a non-authoritative pass here and are flagged for human review, not silently accepted:

- Plan 01: entity-to-entity relationships MUST NOT rely on ID-space convention alone — resolved via the dedicated `entity_edges` table (confirmed structurally true by code read: `fetch_neighborhood`/`extract_and_persist_entities` reference only `entity_edges_table()`). **Non-authoritative: PASS.**
- Plan 02: hallucination/PII/trustworthiness-indistinguishability/escaping prohibitions — all have concrete code-level mitigations confirmed present (`GraphFact::new`'s escaping, `Serialize`-only derives, non-citable `<GRAPH_FACT>` delimiter). **Non-authoritative: PASS**, with the caveat that "prompt escaping is enforced by construction" was independently confirmed by direct code read, not merely narrative.
- Plan 03: LLM-fabricated (hallucinated) entity/relationship claims — explicitly recorded `status: unresolved`, `verification: null`. This is an intentionally left-open risk, not a silently-dropped one. **Flagged, not resolved — human review recommended.**
- Plan 04: Cypher field-VALUE-sourcing and relation-filter mutual-consistency prohibitions — confirmed by code read that response field values (`entity_type`, `relation_type`, `weight`, orientation) come exclusively from `fetch_neighborhood`'s own `edges_batch` columns, never from a Cypher projection. **Non-authoritative: PASS.**

**unverified-prohibition — human review recommended** applies to Plan 03's hallucination-detection prohibition (explicitly unresolved by design) and, by extension, to CR-01/the missing Cypher-narrowing tests above, which sit adjacent to these prohibitions' intent.

### Human Verification Required

### 1. Cypher-narrowing-on-disagreement proof (constrain_to_cypher_matched / cypher_confirmed_neighbor_ids)

**Test:** Feed `graph::constrain_to_cypher_matched` a `cypher_neighbor_ids` set that is a strict subset of a real BFS-discovered neighbor set (a hand-built fixture, no DB needed — it's a pure function), and separately call `graph::cypher_confirmed_neighbor_ids` against a real temp-dir fixture to confirm it extracts the true neighbor set via `traverse_multi_hop`.
**Expected:** The filtered response excludes the unconfirmed neighbor/edge; the extracted ID set matches ground truth.
**Why human:** These are the specific, plan-mandated proofs (Test 3d/3e/3f) that Cypher's execution is genuinely response-affecting rather than a no-op dead code path — the central claim this phase's entire replan revision exists to establish, per REVIEWS.md Codex HIGH. No test in the codebase currently exercises this; a human decision is needed on whether to require these tests before shipping or accept the risk that this code path is unverified.

### 2. MAX_TOTAL_EDGES / MAX_FRONTIER_SIZE cap-exceeded rejection

**Test:** Construct a fixture with more than 500 `entity_edges` rows for one seed's single hop and call `fetch_neighborhood`.
**Expected:** Returns `Err(GraphSpikeError{kind: Bridge, ..})`, not a silently truncated `Ok`.
**Why human:** No test exists; the code path is unexercised. Low likelihood of being hit in the current local-first, single-user usage pattern, but the plan explicitly required this test as an acceptance criterion.

### 3. CR-01 edge-dedup correctness fix

**Test:** Ingest two documents that independently extract an identical (source name, target name, relation_type, confidence) tuple, then query the shared entity's neighborhood.
**Expected:** Both distinct edges (different `edge_id`, different `document_id`) should be returned.
**Why human:** This is a data-correctness defect identified by this phase's own code review and left unfixed at HEAD. A human decision is needed: fix now (the review's suggested fix — key `dedup_edges_by_identity` on `edge_id`, currently not selected into the batch — is small and well-scoped), or explicitly accept the risk for v1 given the project's local-first/single-user posture.

### 4. GraphFact orientation preservation on the RAG-path (attempt_graph_augmentation), specifically seed-as-target

**Test:** Seed a fixture where the seed is `entity_edges.target_node_id`, run `attempt_graph_augmentation`/`query_rag`, inspect the resulting `GraphFact`'s rendered triple.
**Expected:** True stored direction preserved, never reversed.
**Why human:** Orientation-preserving code is present and read-confirmed; no test specifically targets this scenario for the RAG-path (only the parallel `QueryGraph`-path test, `query_graph_bidirectional_seed_as_target`, exercises a seed-as-target fixture, and it does not assert directional ID ordering).

### Gaps Summary

The phase's architecture is real and substantially delivered: entities/entity_edges are live LanceDB tables with structural (not convention-based) separation from chunk-structural edges; extraction runs concurrently and idempotently through a real `OpenRouterExtractionGenerator` wired into production `main()`; `ContextAssemblyStrategy` is a genuinely production-wired, escaping-safe, dual-variant type satisfying RAG-05; graph augmentation runs on every `/rag/query` call with a real, tested, forced-fault-proven fail-open contract; the `QueryGraph` RPC is a real gRPC-only, hop-bounded, relation-filterable handler; and `graph_weight`/score-interleaving genuinely reach the real outbound OpenRouter request body.

Two categories of gap keep this from a clean `passed`:

1. **The specific proof that lance-graph's Cypher execution is genuinely response-affecting (not a load-bearing no-op) is missing.** This was the central architectural claim this phase's multiple replan revisions were built to establish (closing a Codex HIGH finding from a prior review round), and the plan's own must_haves explicitly required a direct unit test on `constrain_to_cypher_matched` fed a deliberately-narrowed confirmed-set to prove it. That test does not exist; the only tests exercising `narrow_via_cypher` prove the fail-open path and the agrees-with-BFS path, neither of which can distinguish "Cypher genuinely narrows on disagreement" from "Cypher's result is silently discarded." `MAX_TOTAL_EDGES`'s reject-path is similarly unproven.

2. **A real, unresolved CRITICAL code-review finding (CR-01) exists at HEAD with no fix commit** — the edge-deduplication identity key can silently drop genuinely distinct edges from different documents, directly undermining graph traversal correctness for any corpus with repeated facts. This was discovered by the phase's own code-review pass (dated the same day as the phase's last commit) and never remediated.

Neither gap prevents the phase's core mechanism from functioning for the common case (152/152 relevant tests pass), but both are load-bearing correctness claims this phase's own plans and reviews flagged as necessary, and both remain open. Recommend either: (a) a small follow-up plan closing CR-01 (small, well-scoped fix per the review) and adding the three missing Cypher-narrowing tests, or (b) an explicit, documented risk-acceptance override for v1 given the project's local-first/single-user/low-corpus-scale posture — a human decision, not a default pass.

---

_Verified: 2026-08-07_
_Verifier: Claude (gsd-verifier)_
