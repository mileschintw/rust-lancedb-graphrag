---
phase: 04.1-knowledge-graph-extraction-query-full-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/Cargo.toml
  - engine/src/lib.rs
  - engine/src/graph/mod.rs
  - engine/src/db/mod.rs
  - engine/src/db/tests.rs
  - engine/src/main.rs
  - engine/src/bin/seed_rag_fixture.rs
  - engine/src/inspect_lancedb_tests.rs
  - engine/src/retrieval/tests.rs
  - engine/tests/config_startup.rs
autonomous: false
requirements: [DATA-04, DATA-05]

must_haves:
  truths:
    - "engine/Cargo.toml's lance-graph/arrow-ipc/arrow-lg/arrow-ipc-lg dependencies compile unconditionally (no graph-spike feature flag) and the 5 pre-existing graph::tests pass without --features graph-spike"
    - "entities and entity_edges are live LanceDB tables in the default schema (7-tuple table_schemas()); nodes no longer carries community_ids/summary/summary_vector/unsummarized_refs (those columns now live on entities per D-11)"
    - "entity_edges is a table structurally distinct from the pre-existing edges table — edges continues to hold only chunk-structural rows (next_chunk/same_section) written by replace_document_with_faults, unchanged; entity-to-entity relationships (D-13) can only ever be written to entity_edges, so D-13's 'edges reference entity IDs only' is enforced by table membership, not by ID-space convention"
    - "every pre-existing fixture/test that constructs a nodes RecordBatch (db/tests.rs, seed_rag_fixture.rs, inspect_lancedb_tests.rs x2, retrieval/tests.rs, tests/config_startup.rs) builds against the restructured 19-field nodes_schema with zero references to the four removed columns, and the full pre-04.1 test suite remains green"
  artifacts:
    - path: engine/src/db/mod.rs
      provides: "entities_schema()/entities_table(), entity_edges_schema()/entity_edges_table(), nodes_schema() with the four dormant columns removed, 7-tuple table_schemas()"
      contains: "pub fn entities_schema"
    - path: engine/src/db/tests.rs
      provides: "updated 7-table-name assertion (communities, documents, edges, entities, entity_edges, nodes, staged_documents_v2)"
      contains: "entity_edges"
  key_links:
    - from: "engine/src/main.rs (replace_document_with_faults)"
      to: "engine/src/db/mod.rs (edges_table)"
      via: "structural next_chunk/same_section edges continue to be written to the original edges table only — this function is NOT modified to know about entity_edges, which is Plan 02's own concern"
      pattern: "edges_table"
  prohibitions:
    - statement: "MUST NOT persist entity-to-entity relationships in the same physical table as chunk-structural (next_chunk/same_section) edges, relying only on ID-space disjointness to keep traversal safe"
      status: resolved
      resolution: "REVIEWS.md HIGH finding (04.1-REVIEWS.md, Plan 01 concerns) — D-13 is enforced structurally via a dedicated entity_edges table (this plan), not by convention. fetch_neighborhood (Plan 02) queries entity_edges exclusively and never reads the original edges table."
---

## Phase Goal

**As a** Lancet engineer, **I want to** extract entities and relationships from chunks during ingestion, persist them as a knowledge graph in LanceDB, and traverse them at query time via the now-confirmed `lance-graph` integration, **so that** RAG queries are grounded with graph context alongside the existing hybrid-retrieval evidence.

This plan lays the schema foundation the rest of the phase builds on: it promotes the Phase 4 spike out of its feature gate, locks the one-way schema-restructure decision with the human, and migrates every existing fixture/test that constructs a `nodes` batch so the restructured schema doesn't silently break the pre-04.1 suite. It intentionally does zero extraction/traversal/prompt work — that is Plan 02's tracer, which depends on this plan.

**Restructuring note (why this plan exists as its own wave):** the original phase plan combined this schema work with the end-to-end tracer in one plan. Cross-AI review (`04.1-REVIEWS.md`) found the schema-restructure's fixture/test fallout (`db/tests.rs`, `seed_rag_fixture.rs`, `inspect_lancedb_tests.rs`, `retrieval/tests.rs`) was real, in-scope work that wasn't in the original plan's `files_modified` — net-new work that pushed the combined plan well past the 2-3 task budget. Splitting it into its own wave-1 plan keeps both plans focused and auditable; the tracer (Plan 02) now starts from an already-migrated, green schema instead of doing schema and tracer work in the same task.

<objective>
Promote the Phase 4 `graph-spike` proof-of-concept into the default build; add a dedicated `entities` table and a dedicated `entity_edges` table (both new; the latter is a REVIEWS.md-driven correction — see Task 2's `<context>`); remove the four dormant placeholder columns from `nodes`; and migrate every existing fixture/test construction site so the full pre-04.1 suite passes against the restructured schema.

Purpose: catch schema-drift and fixture-fallout problems in one focused wave, before Plan 02's tracer has to debug both "does my new extraction code work" and "did I break Phase 02/03's existing tests" at the same time.
Output: `entities`/`entity_edges` tables live in the default schema; `nodes` restructured; zero fixture/test regressions.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-CONTEXT.md
@.planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-REVIEWS.md
@rust-guidelines.md
@engine/src/graph/mod.rs
@engine/src/graph/bridge.rs
@engine/src/db/mod.rs
@engine/src/db/tests.rs
</context>

## Artifacts This Phase Produces

- `engine/src/db/mod.rs` — `entities_schema()`, `pub async fn entities_table(&self)`, `entity_edges_schema()`, `pub async fn entity_edges_table(&self)`.
- `engine/src/db/tests.rs` — updated table-name assertion.

<tasks>

<task type="auto">
  <name>Task 1: Promote the graph module out of the graph-spike feature gate</name>
  <read_first>rust-guidelines.md, engine/Cargo.toml, engine/src/lib.rs, engine/src/graph/mod.rs, engine/src/graph/bridge.rs, .planning/phases/04-knowledge-graph-extraction-query/04-01-SUMMARY.md (Issues Encountered — the ~3-4min cold build note)</read_first>
  <files>engine/Cargo.toml, engine/src/lib.rs</files>
  <action>
    In engine/Cargo.toml: remove `optional = true` from the `lance-graph`, `arrow-ipc`, `arrow-lg`, `arrow-ipc-lg` dependency lines (they become plain unconditional dependencies) and delete the `[features] graph-spike = [...]` array entirely — D-17/Locked-Implications require these compile into every default build now that DATA-05 needs live traversal in the `/rag/query` path.
    In engine/src/lib.rs: remove the `#[cfg(feature = "graph-spike")]` attribute above `pub mod graph;` so the module is unconditional.
    While in engine/src/graph/mod.rs, opportunistically address 04-REVIEW.md's non-blocking IN-01/IN-03 items if touching this file makes it a low-cost fix (dedupe the repeated `GraphConfigBuilder`/dataset-map boilerplate across traverse_fixed_hop/traverse_multi_hop/traverse_filtered_by_relation_type into a shared private helper, and drop the `pub mod bridge;` visibility to `pub(crate) mod bridge;` since nothing outside the crate needs it) — do NOT change any traversal query/config behavior while doing this, only structure.
    The combined transitive dependency tree (~350+ crates including lance-graph's own tree) takes several minutes to build cold on first `cargo build`/`cargo check` after this change (04-01-SUMMARY.md's own "Issues Encountered" note: the Bash tool's default 120s timeout was insufficient). Run the verify command with `run_in_background: true` and poll, or pass an explicit longer timeout, rather than letting a 120s default kill the build mid-compile and misreport failure.
  </action>
  <verify>
    <automated>cargo build --manifest-path engine/Cargo.toml && cargo test --manifest-path engine/Cargo.toml --locked graph::</automated>
  </verify>
  <done>Default `cargo build`/`cargo test --locked` (no --features flag) compile the full lance-graph tree and run the 5 pre-existing graph:: tests, all passing.</done>
  <acceptance_criteria>
    - `grep -c 'graph-spike' engine/Cargo.toml` returns 0.
    - `grep -c 'cfg(feature = "graph-spike")' engine/src/lib.rs` returns 0.
    - `cargo test --manifest-path engine/Cargo.toml --locked graph::` reports the 5 named tests passing, 0 failed.
  </acceptance_criteria>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Confirm the schema restructure — entities table, entity_edges table, and nodes column removal (one-way door)</name>
  <read_first>.planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-CONTEXT.md (Graph Node/Edge Schema Resolution section, D-10 through D-16), .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-REVIEWS.md (Plan 01 "entity-only edge contract conflicts with existing storage behavior" finding)</read_first>
  <reversibility rating="one-way">D-10 (new dedicated entities table), this plan's new entity_edges table, and D-15 (drop community_ids/summary/summary_vector/unsummarized_refs from the already-shipped nodes table) are all drift-inducing schema changes against a live LanceDB store with no auto-migration (D-14/D-16). Undoing this after real data is ingested requires deleting the local LanceDB directory and re-ingesting the whole corpus — there is no rollback path once documents exist under the new schema. There is no schema-version field or migration registry in this codebase (confirmed: `validate_schema()` does exact field-equality comparison, nothing else) — "upgrading past this phase" concretely means a manual `rm -rf` of the local LanceDB directory followed by re-ingestion, not an automated version bump.</reversibility>
  <decision>Restructure the graph schema now: add a new `entities` table (D-10), add a new `entity_edges` table for entity-to-entity relationships (correction below), and remove the four dormant, never-populated columns (community_ids, summary, summary_vector, unsummarized_refs) from the existing `nodes` table (D-15). Document this as a manual store-reset boundary (delete the local LanceDB directory, re-ingest) — there is no in-engine schema-version/migration mechanism to "bump," consistent with D-22's existing "manual user intervention" clause.</decision>
  <context>
    These four `nodes` columns have been present but always-null since Phase 02 (a Port-999.1/999.4 placeholder). This phase's `entities` table is where that data conceptually belongs going forward (D-11).

    **Correction to CONTEXT.md, found by cross-AI review (`04.1-REVIEWS.md`, HIGH severity):** CONTEXT.md's D-13 ("edges.source_node_id/target_node_id reference entity IDs only") was written assuming entity-relation edges would live in the *existing* `edges` table. That table already holds chunk-structural rows (`next_chunk`/`same_section`, written by `replace_document_with_faults`, `engine/src/main.rs`) since Phase 02 — a fact CONTEXT.md's authors didn't have in view. Mixing entity-relation edges into that same table would make D-13 true only by ID-space convention (entity UUIDs never colliding with `<document_id>:<index>` chunk-ID strings), which a reviewer correctly flagged as unenforced-by-structure. This plan instead adds a **new, dedicated `entity_edges` table** (identical column shape to `edges_schema()` — `edge_id`, `source_node_id`, `target_node_id`, `relation_type`, `weight`, `document_id`, `summary`, `summary_vector` — just a distinct table) so D-13 is enforced by table membership: nothing in the codebase can traverse chunk-structural edges as if they were entity relationships, because they physically live in different tables. This also makes Plan 02's re-ingestion idempotency self-contained (its own delete-by-`document_id` step, not dependent on `replace_document_with_faults`'s unrelated sweep of the original `edges` table) — closing a second REVIEWS.md finding as a side effect. `edges_schema()` itself is UNCHANGED by this correction (D-13's original spirit — entity-only relationships — is honored via the new table, not by mutating the existing one), so this correction touches zero of Phase 02/03's existing `edges`-table fixtures.

    This is a local-first, single-developer project (no other consumer depends on the current `nodes`/`edges` schema in production) — the "delete and re-ingest" cost is a `rm -rf` plus a re-run of the existing ingestion path, not a live-migration project.
  </context>
  <options>
    <option id="restructure-now">
      <name>entities + entity_edges tables, drop four dormant nodes columns (recommended)</name>
      <pros>Clean single-owner schema per D-11 (entities own community/summary data going forward); D-13 structurally enforced via table separation rather than ID-space convention; no permanent special-casing of D-14's drift check; matches the locked CONTEXT.md decision plus the REVIEWS.md-driven entity_edges correction.</pros>
      <cons>Any existing local LanceDB store (including this project's own dev data) must be deleted and re-ingested before the engine will start — D-14's fail-fast check will refuse to boot against the old schema.</cons>
    </option>
    <option id="keep-dormant-columns">
      <name>Leave the four nodes columns in place, add entities/entity_edges alongside</name>
      <pros>No forced re-ingestion of existing local data; zero risk to already-shipped nodes rows.</pros>
      <cons>Contradicts the locked D-15 decision from /gsd-discuss-phase; leaves two tables both nominally "owning" community/summary semantics, which 999.1/999.4 would have to disambiguate later; D-14's drift check would need a permanent carve-out for four fields nobody writes.</cons>
    </option>
  </options>
  <resume-signal>Type "restructure-now" to proceed with the locked D-15 plan plus the entity_edges correction, or "keep-dormant-columns" to override it (note: overriding a locked CONTEXT.md decision should only be done with a clear reason, since D-11/D-15/D-16 were already decided during /gsd-discuss-phase).</resume-signal>
  <acceptance_criteria>
    - A resume signal ("restructure-now" or "keep-dormant-columns") is recorded before Task 3 begins.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build entities_schema()/entity_edges_schema(), restructure nodes_schema(), migrate every fixture</name>
  <read_first>rust-guidelines.md, engine/src/db/mod.rs, engine/src/db/tests.rs, engine/src/main.rs (replace_document_with_faults, lines ~1552-1750), engine/src/bin/seed_rag_fixture.rs, engine/src/inspect_lancedb_tests.rs, engine/src/retrieval/tests.rs, engine/tests/config_startup.rs</read_first>
  <precondition>Task 2's checkpoint resolved to "restructure-now" (this task performs that schema change).</precondition>
  <reversibility rating="one-way">Implements the D-10/D-15/D-16 schema restructure plus the entity_edges correction, both confirmed in Task 2.</reversibility>
  <files>engine/src/db/mod.rs, engine/src/db/tests.rs, engine/src/main.rs, engine/src/bin/seed_rag_fixture.rs, engine/src/inspect_lancedb_tests.rs, engine/src/retrieval/tests.rs, engine/tests/config_startup.rs</files>
  <behavior>
    - Test 1 (engine/src/db/tests.rs, replacing `initializes_and_validates_all_table_schemas`'s assertion): a freshly initialized `DatabaseManager` exposes exactly 7 tables — `communities`, `documents`, `edges`, `entities`, `entity_edges`, `nodes`, `staged_documents_v2` (sorted).
    - Test 2 (new, engine/src/db/tests.rs): `entities_schema()` has a non-nullable `entity_id`/`name`/`entity_type`/`name_vector`/`source_chunk_ids` and nullable `summary`/`summary_vector`/`unsummarized_refs`/`community_ids`. `entity_edges_schema()` has the identical field shape to `edges_schema()` (edge_id, source_node_id, target_node_id, relation_type, weight, document_id non-nullable; summary/summary_vector nullable) but is a distinct table.
    - Test 3 (existing, `edge_summary_placeholders_are_nullable_but_identifiers_are_required` in db/tests.rs): unchanged, still asserts against `edges_schema()` (unaffected by this plan's changes — confirms the original `edges` table's shape is untouched).
  </behavior>
  <action>
    **Schema (engine/src/db/mod.rs):** Add `entities_schema()` returning a `SchemaRef` with fields in this exact order: `entity_id` (Utf8, false), `name` (Utf8, false), `entity_type` (Utf8, false), `name_vector` (the existing `vector()` helper, false), `summary` (Utf8, true), `summary_vector` (`vector()`, true), `unsummarized_refs` (`list(DataType::Utf8)`, true), `community_ids` (`list(DataType::Int32)`, true), `source_chunk_ids` (`list(DataType::Utf8)`, false — D-12, always has at least one entry). Add `entity_edges_schema()` mirroring `edges_schema()` field-for-field exactly (same names, same types, same nullability: `edge_id`, `source_node_id`, `target_node_id`, `relation_type`, `weight`, `document_id`, `summary`, `summary_vector`) — a byte-for-byte-identical `Field` list to `edges_schema()`'s existing definition, just returned from a new function and registered as a new table name (Task 2's correction: this is a distinct table, not a mutation of `edges_schema()`). Add `pub async fn entities_table(&self) -> Result<Table, String>` and `pub async fn entity_edges_table(&self) -> Result<Table, String>` mirroring `nodes_table()`/`edges_table()` exactly (same `open_table`/error-message pattern). Extend `table_schemas()` from a 5-tuple to a 7-tuple array, adding `("entities", entities_schema())` and `("entity_edges", entity_edges_schema())`. Do NOT change `edges_schema()` itself — Task 2's correction adds a sibling table, not a mutation. Reuse `validate_schema()` verbatim for both new tables (D-14) — no special-casing.

    Remove `community_ids`, `summary`, `summary_vector`, `unsummarized_refs` `Field`s from `nodes_schema()` (D-15) — `nodes_schema()` goes from 23 fields to 19 fields (down to and including `content_type`, dropping the trailing four).

    **db/tests.rs:** Update `initializes_and_validates_all_table_schemas`'s `names` assertion from the 5-element array to the 7-element sorted array `["communities", "documents", "edges", "entities", "entity_edges", "nodes", "staged_documents_v2"]`. Add the two new schema-shape tests described in Behavior above (Test 2), placed near the existing `edge_summary_placeholders_are_nullable_but_identifiers_are_required` test.

    **main.rs (`replace_document_with_faults`):** the `RecordBatch::try_new(node_schema.clone(), vec![...])` call (~line 1656) currently ends with four `nullable("community_ids")?, nullable("summary")?, nullable("summary_vector")?, nullable("unsummarized_refs")?,` entries — delete those four trailing entries. The batch is built against a dynamically-fetched `node_schema`, so a stale column count is a runtime `RecordBatch::try_new` error, not a compile error; the existing node-persistence tests (in `engine/src/tests.rs`) will fail loudly if this is missed, since they run against the real restructured schema.

    **engine/src/bin/seed_rag_fixture.rs:** its `node_batch` construction (~line 110) ends with the same four `nullable(...)` entries (lines ~151-154) — delete them.

    **engine/src/inspect_lancedb_tests.rs:** it has TWO separate `node_batch`/RecordBatch constructions against `node_schema`, each ending with the same four `nullable(...)` entries (one around line 148-151, a second around line 536-539) — delete both. Do NOT touch the `edge_nullable("summary_vector")` call near line 191 — that constructs an `edges`-table batch, unaffected by this plan.

    **engine/src/retrieval/tests.rs:** its node-batch construction (~line 171) ends with the same four `nullable(...)` entries — delete them. Grep the file for `unsummarized_refs` to confirm there is exactly one such site before editing.

    **engine/tests/config_startup.rs:** its node-batch construction (~line 171-174, inside the real-binary-spawning startup integration test) ends with the same four `nullable(...)` entries — delete them.

    After all seven files are edited, confirm no reference to the four removed field names remains inside any `nodes_schema()`-shaped `RecordBatch::try_new` call across the crate: `grep -rn "unsummarized_refs\|community_ids\|summary_vector" engine/src engine/tests` should show matches ONLY inside `entities_schema()`'s own definition (these field names legitimately reappear there per D-11), `edges_schema()`'s pre-existing, unchanged `summary_vector` field, and `entity_edges_schema()`'s `summary_vector` field (a legitimate third match site — `entity_edges_schema()` is a byte-for-byte mirror of `edges_schema()`, which also carries `summary_vector`) — and nowhere else.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked db:: && cargo test --manifest-path engine/Cargo.toml --locked</automated>
  </verify>
  <done>All 7 tables (including entities and entity_edges) validate on startup; nodes_schema() no longer carries the four dormant columns; every fixture/test that constructs a nodes or edges RecordBatch builds and passes against the restructured schema; the full pre-04.1 suite is green.</done>
  <acceptance_criteria>
    - `cargo test --manifest-path engine/Cargo.toml --locked` exits 0 (full existing suite plus the new db:: tests).
    - `grep -n "pub fn entities_schema\|pub fn entity_edges_schema" engine/src/db/mod.rs` matches both.
    - `grep -n "\"entities\", entities_schema()\|\"entity_edges\", entity_edges_schema()" engine/src/db/mod.rs` matches both inside `table_schemas()`.
    - `grep -rn "unsummarized_refs\|community_ids\|summary_vector" engine/src engine/tests` shows matches only inside `entities_schema()`'s own field list, `edges_schema()`'s pre-existing, unchanged `summary_vector` field, and `entity_edges_schema()`'s `summary_vector` field (its mirror of `edges_schema()`) — zero matches inside any `nodes`-shaped batch construction.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LanceDB schema drift on startup | `entities`/`entity_edges` join the existing fail-fast/no-auto-migrate contract (D-14) — a mismatched local store refuses to boot rather than silently drifting |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-01 | Denial of Service (build cost, not runtime) | `lance-graph` promoted to the default build (~350+ transitive crates, several-minute cold build) | low | accept | Locked Implications: budget CI/build time; the dependency tree is not a runtime attack surface |
| T-04.1-02 | Tampering | Schema restructure leaves a pre-04.1 local LanceDB store in a state that fails `validate_schema()` | low | accept | Intentional fail-fast per D-14/D-16 — a stale store must be deleted and re-ingested, documented in Task 2's checkpoint, not a silent-drift risk |

No canon-referral items this plan (no new external input surface — schema-only work).
</threat_model>

<verification>
- `cargo build --manifest-path engine/Cargo.toml` and `cargo test --manifest-path engine/Cargo.toml --locked` both succeed with zero `--features` flags, confirming `graph` is fully in the default build and the schema restructure is fixture-complete.
- `git diff --name-only` for this plan's commits touches only the files listed in frontmatter `files_modified`.
</verification>

<success_criteria>
`entities` and `entity_edges` tables exist in the default schema with fail-fast drift validation; `nodes` no longer carries the four dormant placeholder columns; every pre-04.1 fixture/test that constructs a `nodes`-shaped batch has been migrated and the full suite is green. Plan 02's tracer can now build extraction/traversal logic on top of a stable, already-verified schema.
</success_criteria>

<output>
Create `.planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-01-SUMMARY.md` when done.
</output>