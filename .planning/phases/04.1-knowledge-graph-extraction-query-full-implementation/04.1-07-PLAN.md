---
phase: 04.1-knowledge-graph-extraction-query-full-implementation
plan: 07
type: execute
wave: 7
depends_on: ["04.1-06"]
files_modified:
  - engine/src/tests.rs
autonomous: true
gap_closure: true
requirements: [DATA-04, DATA-05, RAG-05]

must_haves:
  truths:
    - "Entity extraction, persistence, and lance-graph traversal are proven through the REAL ingestion worker path (spawn_worker) — not a direct call to extract_and_persist_entities that bypasses the queue — and that queue-driven persistence reaches the real outbound OpenRouter chat request body via query_rag, the same proof already established for the direct-call path (test: worker_queue_extracted_graph_facts_reach_provider_request_body)"
    - "attempt_graph_augmentation's GraphFact rendering preserves the TRUE stored edge orientation when the seed entity is the edge's target_node_id (not source) — the resulting fact never renders a reversed triple (test: graph_fact_preserves_stored_edge_orientation_when_seed_is_target)"
    - "DATA-04's idempotent-re-ingestion and concurrency-bound claims remain proven and passing from Plan 03 (extraction_reingestion_is_idempotent_by_construction, extraction_concurrency_bound_is_observed_not_assumed) — this plan adds no new work against those two probe rows, cited here only for completeness of the phase's gap-closure accounting"
    - "With this plan's two tests landed, all 5 items recorded in 04.1-VERIFICATION.md's frontmatter (gaps: CR-01/Cypher-narrowing/MAX_TOTAL_EDGES from Plan 06, behavior_unverified_items: orientation/worker-queue from this plan) have direct, passing, named test coverage"
  artifacts:
    - path: engine/src/tests.rs
      provides: "worker_queue_extracted_graph_facts_reach_provider_request_body and graph_fact_preserves_stored_edge_orientation_when_seed_is_target, plus a small private KeyedEmbedder test double enabling deterministic seed selection under attempt_graph_augmentation's vector nearest_to"
      contains: "graph_fact_preserves_stored_edge_orientation_when_seed_is_target"
  key_links:
    - from: "engine/src/main.rs (spawn_worker's queue loop)"
      to: "engine/src/main.rs (process_job_with_boundary -> extract_and_persist_entities) -> engine/src/tests.rs (capture_chat_request_body -> query_rag -> real outbound HTTP body)"
      via: "a document ingested through the real mpsc-channel-driven worker (not a direct extract_and_persist_entities call) is proven, end to end, to reach entities_table()/entity_edges_table() and then the real outbound OpenRouter chat request body as a <GRAPH_FACT> block"
      pattern: "spawn_worker"
    - from: "engine/src/main.rs (attempt_graph_augmentation's edges_batch column reads)"
      to: "engine/src/graph/context_strategy.rs (GraphFact::new(entity_a_name, relation_type, entity_b_name, ...))"
      via: "source_node_id/target_node_id are read directly off fetch_neighborhood's own directed edges_batch and passed into GraphFact::new in that same order, regardless of which endpoint the RAG-path's vector search happened to select as the seed — this plan's orientation test is the first to assert that ordering is preserved specifically when the seed is the target"
      pattern: "GraphFact::new"
  prohibitions: []
---

<objective>
Close the two `behavior_unverified_items:` from 04.1-VERIFICATION.md (verified 2026-08-07): both are missing-test gaps against already-shipped, already-correct code (confirmed present by direct code read at verification time, per VERIFICATION.md's own framing) — not known code defects. This plan adds no production code changes, only two new characterization tests through the real production entry points.

**Sequencing note:** Task 1 (the worker-queue path proof) runs first — it is the more foundational claim (proving the actual production entry point, `spawn_worker`, behaves identically to the direct-call path every other extraction test uses) — ahead of Task 2 (a narrower, specific orientation-correctness detail on `attempt_graph_augmentation`).

**Depends on Plan 06:** this plan and Plan 06 both modify `engine/src/tests.rs`, forcing sequential waves regardless; this plan's tests also exercise `fetch_neighborhood`/`dedup_edges_by_identity` internally (via `extract_and_persist_entities` → `attempt_graph_augmentation`/`spawn_worker`), so running after Plan 06's CR-01/WR-01 fixes land is also the logically correct order, even though neither of this plan's assertions targets those fixes directly.

**Prohibition-probe disposition (`must_haves.prohibitions: []`):** both tasks are pure test-writing against already-decided, already-shipped behavior — no new user-facing surface, no new optionality, no product/safety/ethics dimension. Stage 1/Stage 2 yield zero bespoke prohibitions, per the probe's own "clean-utility" worked example.

Purpose: the phase's own Plan 02 must_haves explicitly promised both proofs (Test 3c for orientation, the real-worker-path proof for extraction); neither test exists in the codebase, per 04.1-VERIFICATION.md.
Output: two new tests proving the queue-driven worker path reaches the real outbound provider request with graph data, and that `attempt_graph_augmentation` never renders a reversed relationship triple when the seed is an edge's target.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-VERIFICATION.md
@rust-guidelines.md
@engine/src/main.rs
</context>

## Artifacts This Phase Produces

- `engine/src/tests.rs` — `worker_queue_extracted_graph_facts_reach_provider_request_body`, `graph_fact_preserves_stored_edge_orientation_when_seed_is_target`, and a private `KeyedEmbedder` test double (implements `EmbeddingProvider`, maps specific entity names to specific fixed vectors so `attempt_graph_augmentation`'s vector-nearest-neighbor seed selection is deterministic between two otherwise-identical fixture entities).

<tasks>

<task type="auto">
  <name>Task 1: Prove extraction and graph augmentation reach the provider through the real worker queue, not a direct call</name>
  <read_first>engine/src/main.rs (spawn_worker/spawn_worker_with_boundary, ~line 2862-2977; process_job_with_boundary, ~line 2356-2396), .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-03-PLAN.md, .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-03-SUMMARY.md, .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-02-PLAN.md, .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-02-SUMMARY.md, engine/src/tests.rs (worker_indexes_jobs_and_records_real_chunk_count ~line 1063 for the spawn_worker test-harness pattern; capture_chat_request_body ~line 5905 and graph_weight_reaches_actual_provider_request_body ~line 6043 for the mock-HTTP-server/body-capture pattern this task reuses unmodified), .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-VERIFICATION.md</read_first>
  <files>engine/src/tests.rs</files>
  <action>
    Add `worker_queue_extracted_graph_facts_reach_provider_request_body`. Initialize a fresh `DatabaseManager`.

    First, stage and directly `process_job` ONE filler document reusing the exact text `graph_weight_reaches_actual_provider_request_body`'s `doc_a` already uses — `"# Reserved Chunk\n\nKeystone retrieval architecture explanation for the primary reserved evidence block."`, with `&FakeEmbedder` — via `stage_document`/`read_staged_jobs`/`process_job`, matching that existing test's exact pattern. This filler document is required because `capture_chat_request_body`'s hardcoded query text (`"keystone retrieval architecture explanation"`) needs a real BM25-matching chunk candidate; without it `query_rag` risks short-circuiting before the mock server's chat-completions `accept()` ever fires, hanging or panicking on `.expect("chat request was captured")`. This filler document is ingested via a DIRECT `process_job` call, not the worker queue — only the graph document below needs to prove the queue path.

    Second, spawn a REAL worker via `spawn_worker` (mirroring `worker_indexes_jobs_and_records_real_chunk_count`'s harness: `Arc::new(DashMap::new())` statuses, `mpsc::channel(QUEUE_CAPACITY)`, `Arc::new(FakeEmbedder)`), passing a NON-EMPTY `graph::extraction::FakeExtractionGenerator` (unlike `test_extraction_generator()`'s empty default) that returns entities Alice/Bob (`entity_type: "person"`) and one relation `source: "Alice", target: "Bob", relation_type: "knows", confidence: 0.9`. **Shutdown-channel hazard (bind the sender, do not discard it):** `let (shutdown_tx, shutdown_rx) = watch::channel(false);` — bind the sender to a real, non-`_`-prefixed-discard local (e.g. `shutdown_tx`, or `_shutdown_tx` per this file's existing naming convention, but NOT a bare `_`) and keep it alive in scope until after `worker.await` returns. `spawn_worker_with_boundary`'s loop is a `tokio::select! { biased; ... }` that polls `shutdown.changed()` first; if the sender is dropped, every subsequent `changed()` call returns `Err` immediately and forever, `changed.is_ok()` is false, and the loop `continue`s without ever polling `receiver.recv()` — the worker spins forever and `worker.await` never returns, hanging the test. Send exactly one `IngestionJob` through the channel with text `"# Graph Fixture\n\nAlice knows Bob in this fixture scenario every single day."`, drop the sender, `worker.await.unwrap()`. This routes through the worker's real completion flow — `process_job_with_boundary` first (chunk/embed/persist), then `extract_and_persist_entities` as the worker's own next step (main.rs's `spawn_worker_with_boundary` loop) — never a direct call to `extract_and_persist_entities` from this test.

    Assert `statuses.get(&document_id)`'s `.status == "completed"`. Assert `database.entities_table().await.unwrap().count_rows(None).await.unwrap() == 2` and `database.entity_edges_table().await.unwrap().count_rows(None).await.unwrap() == 1` — proving persistence happened via the queue-driven worker path specifically (no other test asserts on these two tables' contents after a `spawn_worker` run).

    Finally, call `capture_chat_request_body(&database, 1.0).await` on this SAME `database` (the helper already exists at ~line 5905, unmodified) and assert the returned body `.contains("<GRAPH_FACT")` — proving the graph fact persisted through the worker queue reaches the real outbound OpenRouter request body end to end, the same proof `graph_weight_reaches_actual_provider_request_body` already established for the direct-call path, now established for the queue-driven path specifically.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked worker_queue_extracted_graph_facts_reach_provider_request_body</automated>
  </verify>
  <done>A document ingested purely through spawn_worker's real queue (never a direct extract_and_persist_entities call) is proven to populate entities_table/entity_edges_table and to reach the real outbound provider chat request body as a GRAPH_FACT block.</done>
  <acceptance_criteria>
    - `grep -n "fn worker_queue_extracted_graph_facts_reach_provider_request_body" engine/src/tests.rs` matches.
    - The test's action calls `spawn_worker` (not a direct `extract_and_persist_entities` call) to ingest the graph-fact document, and separately asserts on `entities_table().count_rows` and `entity_edges_table().count_rows`.
    - The test asserts the `capture_chat_request_body` result contains `<GRAPH_FACT`.
    - The `watch::channel(false)` sender this test binds for `spawn_worker`'s shutdown receiver is kept in a named local variable (not discarded via a bare `_`) and stays in scope at least until after `worker.await` — a discarded sender would make the test hang rather than complete.
    - The test passes under `cargo test --manifest-path engine/Cargo.toml --locked` and completes (does not hang) within that command's normal run time.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Prove GraphFact orientation is preserved on the RAG path when the seed is the edge's target</name>
  <read_first>engine/src/main.rs (attempt_graph_augmentation, ~line 1056-1236), engine/src/graph/context_strategy.rs (GraphFact::new and its entity_a_name/entity_b_name accessors), .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-02-PLAN.md, .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-02-SUMMARY.md, engine/src/tests.rs (attempt_graph_augmentation_scoring_and_neighborhood ~line 4207 and seed_single_edge_graph ~line 5096 as the fixture-construction pattern to adapt, not reuse verbatim — seed_single_edge_graph hardcodes &FakeEmbedder, which this test cannot use), .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-VERIFICATION.md</read_first>
  <files>engine/src/tests.rs</files>
  <action>
    Add a small private struct implementing `EmbeddingProvider` (e.g. `KeyedEmbedder(std::collections::HashMap<String, Vec<f32>>)`), whose `get_embeddings` returns, for each input text, the mapped vector if present or a fixed filler vector (e.g. `vec![0.1_f32; 2048]`) otherwise. This is required because the shared `FakeEmbedder` returns an identical constant vector for every text, making it impossible to deterministically control which of two fixture entities `attempt_graph_augmentation`'s vector `nearest_to` seed-match selects.

    Add `graph_fact_preserves_stored_edge_orientation_when_seed_is_target`. Initialize a fresh `DatabaseManager`. Build a `KeyedEmbedder` mapping `"Carol"` to one fixed 2048-dim vector (e.g. all `0.9`) and `"Dave"` to a distinct fixed 2048-dim vector (e.g. all `-0.9`). Ingest a fixture via `extract_and_persist_entities` with a `graph::extraction::FakeExtractionGenerator` returning entities Carol/Dave (`entity_type: "person"`) and one relation `source: "Carol", target: "Dave", relation_type: "mentors", confidence: 0.75`, and the `KeyedEmbedder` as the embedder — so Carol is the edge's `source_node_id` and Dave is its `target_node_id`.

    Call `attempt_graph_augmentation(&database, &dave_vector, &settings)` DIRECTLY, passing the EXACT SAME vector assigned to `"Dave"` as the query embedding (distance 0 to Dave's stored `name_vector`, deterministically selecting Dave as the seed regardless of distance metric, since Carol's vector is materially different) and `GraphSettings { seed_match_min_score: 0.0, max_hop_cap: 3 }`. Match on the returned `GraphAugmentationOutcome`; assert it is `Succeeded` with exactly one fact in `facts`. Assert `facts[0].entity_a_name() == "Carol"` (the TRUE stored source) and `facts[0].entity_b_name() == "Dave"` (the seed, which is the edge's target) — proving the rendered triple is never reversed to `"Dave —mentors→ Carol"` even though Dave (not Carol) was the vector-search seed match.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked graph_fact_preserves_stored_edge_orientation_when_seed_is_target</automated>
  </verify>
  <done>attempt_graph_augmentation's GraphFact rendering is proven, by a fixture where the seed is deterministically the edge's target, to preserve true stored source/target orientation rather than rendering a reversed triple.</done>
  <acceptance_criteria>
    - `grep -n "fn graph_fact_preserves_stored_edge_orientation_when_seed_is_target" engine/src/tests.rs` matches.
    - The test asserts both `entity_a_name() == "Carol"` and `entity_b_name() == "Dave"` specifically (not merely that one fact exists), so a silently-reversed orientation would fail the assertion, not merely go unchecked.
    - The seed vector passed to `attempt_graph_augmentation` is the same vector value the fixture assigned to `"Dave"`, not `"Carol"` — confirming Dave (the target) is the one deterministically selected as the seed.
    - The test passes under `cargo test --manifest-path engine/Cargo.toml --locked`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker-queue ingestion (spawn_worker) vs. direct-call test coverage | Test-only boundary — closing a coverage gap between how every other extraction test exercises the code and how production actually invokes it; no new production surface |
| RAG-path GraphFact orientation → LLM prompt context | A silently-reversed relationship triple would present false information to the LLM as if it were the true stored fact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-24 | Spoofing | `engine/src/main.rs` `attempt_graph_augmentation` GraphFact orientation, seed-as-target case (previously untested) | low | mitigate | `graph_fact_preserves_stored_edge_orientation_when_seed_is_target` closes a previously-unproven correctness claim that could otherwise let a reversed relationship reach the LLM prompt as if it were the true stored direction |
| T-04.1-25 | Repudiation | Worker-queue path (`spawn_worker`) vs. direct `extract_and_persist_entities` call — previously unproven behavioral equivalence | low | mitigate | `worker_queue_extracted_graph_facts_reach_provider_request_body` closes the coverage gap; no production code changes in this plan, so this is a test-only closure, not a defect fix |

**Canon-referral breadcrumb:** neither threat introduces new caller-controlled input or a new trust boundary — both are coverage gaps against already-shipped, already-accepted production code paths (D-33's local-first/single-user framing still governs the underlying data); not re-litigated here.
</threat_model>

<verification>
- `cargo test --manifest-path engine/Cargo.toml --locked` (full suite) shows 0 failed — confirms no regression against Plan 06's fixes or any pre-existing test.
- Both named tests in this plan's tasks are present in source and pass individually, per each task's own `<acceptance_criteria>`.
- With this plan complete, every one of 04.1-VERIFICATION.md's 5 recorded items (3 `gaps:` closed by Plan 06, 2 `behavior_unverified_items:` closed by this plan) has direct, named, passing test coverage.
</verification>

<success_criteria>
The queue-driven worker path (`spawn_worker`) is proven, not merely assumed, to behave identically to the direct-call path already covered elsewhere — extraction, persistence, and the real outbound provider request all verified through the real production entry point. `attempt_graph_augmentation`'s GraphFact rendering is proven to preserve true stored edge orientation when the seed is the edge's target. Combined with Plan 06, all 5 items from 04.1-VERIFICATION.md's `gaps:`/`behavior_unverified_items:` are closed with passing, named tests.
</success_criteria>

<output>
Create `.planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-07-SUMMARY.md` when done.
</output>
