# Phase 04.1: Knowledge Graph Extraction & Query (Full Implementation) - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-06
**Phase:** 04.1-Knowledge Graph Extraction & Query (Full Implementation)
**Areas discussed:** Extracted content handling (PII/redaction policy)

---

## Approach for this discussion

The user's explicit instruction for this discussion was: bring over all decisions and AI-integration results already established in Phase 4 (the `lance-graph` compatibility spike) that are still sufficient, and only ask for input or revision where Phase 4's own results (verification, security, review, UAT) surfaced a genuine need. This session therefore skipped the standard multi-area gray-area menu and instead:

1. Read Phase 4's full artifact set (`04-CONTEXT.md`, `04-AI-SPEC.md`, `04-RESEARCH.md`, `04-REVIEW.md`, `04-SECURITY.md`, `04-VERIFICATION.md`, `04-UAT.md`, `04-01-SUMMARY.md`) end to end.
2. Carried forward all 32 implementation decisions (D-01–D-32) from `04-CONTEXT.md` into `04.1-CONTEXT.md`, now in-scope rather than deferred.
3. Layered in refinements the spike actually discovered (D-19's hybrid pre-narrow/Cypher mechanism, D-20's Pitfall-6 constraint on variable-hop relationship projection) as documentation, not new questions — these are empirical facts from the spike, not judgment calls.
4. Identified exactly one genuinely open item: Phase 4's verification report (`04-VERIFICATION.md`) recorded three judgment-tier prohibitions that passed Phase 4's UAT only because Phase 4 touched no real data. Two of the three (entity over-conflation, graph-fact trustworthiness) are already answered by existing decisions (D-12's `source_chunk_ids`, D-27's separate non-citable section) — no new question needed. The third (PII/sensitive content persistence) was genuinely undecided and became this session's one question.
5. Filled in `ROADMAP.md` §Phase 04.1's stub (`Goal: [Urgent work - to be planned]`, `Requirements: TBD`) directly from Phase 4's already-approved deferred-target block, since that was a mechanical carry-forward, not a new decision.

## Extracted Content Handling (PII / redaction policy)

| Option | Description | Selected |
|--------|-------------|----------|
| Accept & document (recommended) | Persist raw extracted entity/relation text as-is; extend the existing AR-04-01 accepted-risk rationale into this phase's threat register; keep row values out of error/log messages (matching the already-shipped `GraphSpikeError` pattern). Matches PROJECT.md's local-first single-user posture and scope-discipline constraint. | ✓ |
| Add basic redaction | Run extracted entity/relation text through a lightweight regex-based PII scrubber before persistence. | |
| You decide | Leave the call to the researcher/planner. | |

**User's choice:** Accept & document (recommended).
**Notes:** Recorded as D-33 in `04.1-CONTEXT.md`, with the carry-forward action of extending `AR-04-01` (from `04-SECURITY.md`) into this phase's own threat register, now scoped against real `entities`/`edges` tables instead of Phase 4's synthetic fixtures — this closes Phase 4's `T-04-01-02` "revisit when Phase 04.1 wires this against real tables" note.

---

## Claude's Discretion

Carried forward from `04-CONTEXT.md` (still open) plus new items surfaced by this discussion's reading of `04-RESEARCH.md`/`04-REVIEW.md`:

- Exact minimum content-length threshold for skipping extraction (D-07).
- Exact structured-output JSON schema for the per-chunk extraction call (D-08/D-02) — `04-AI-SPEC.md` §4b.1 provides a concrete starting shape.
- `QueryGraph`'s variable-hop relationship-property handling: node-only results vs. correlate-back-to-`edges_batch` (D-20's Pitfall-6 workaround) — recommendation is correlate-back, final call left to planner/implementer.
- Entity/edge ID generation scheme (expected UUID convention).
- Internal module/file layout for extraction, traversal, and `ContextAssemblyStrategy`.
- Exact score-normalization method for D-29/D-30's graph/chunk-evidence interleaving.
- WR-01's fix approach: explicit single-batch assertion (error on 2+ batches) vs. batch concatenation — both satisfy "don't silently drop data."

## Deferred Ideas

None — discussion stayed within phase scope. Non-destructive `SAME_AS`/`SYNONYM_OF` entity-resolution linking (999.6), community summaries (999.1), and LLM-assisted node/edge summarization (999.4) were confirmed as correctly out of scope by reading their existing CONTEXT.md files — not newly deferred by this discussion.
