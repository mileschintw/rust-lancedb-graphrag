---
phase: 04.1-knowledge-graph-extraction-query-full-implementation
plan: 09
subsystem: database
tags: [rust, lancedb, graph, arrow, bfs]

requires:
  - phase: 04.1-08
    provides: low-severity review findings dispositions
provides:
  - fetch_neighborhood MAX_TOTAL_EDGES distinct edge_id bound counting
  - multi-hop neighborhood regression test for distinct edge counting
  - narrow_via_cypher_fails_open_on_invalid_hop_cap test re-scoping and doc clarification
affects: [engine, graph]

tech-stack:
  added: []
  patterns: [distinct edge HashSet bound counting across BFS hops]

key-files:
  created: []
  modified:
    - engine/src/graph/mod.rs
    - engine/src/tests.rs
    - engine/src/graph/tests.rs

key-decisions:
  - "Track fetch_neighborhood MAX_TOTAL_EDGES bound against distinct edge_id values accumulated in seen_edge_ids HashSet across hops rather than raw per-hop row sums"

patterns-established:
  - "HashSet<String> distinct accumulator for multi-hop Arrow record batch row deduplication"

requirements-completed: [DATA-05]

coverage:
  - id: D1
    description: "fetch_neighborhood MAX_TOTAL_EDGES bound counts distinct edge_id values accumulated across hops"
    requirement: DATA-05
    verification:
      - kind: unit
        ref: "engine/src/tests.rs#fetch_neighborhood_accepts_in_bounds_neighborhood_despite_raw_recount_exceeding_max_total_edges"
        status: pass
      - kind: unit
        ref: "engine/src/tests.rs#fetch_neighborhood_rejects_oversized_edge_batch"
        status: pass
      - kind: unit
        ref: "engine/src/tests.rs#fetch_neighborhood_rejects_oversized_final_hop_frontier"
        status: pass
    human_judgment: false
  - id: D2
    description: "narrow_via_cypher_fails_open_on_invalid_hop_cap test re-scoped to document InvalidHopCap coverage"
    requirement: DATA-05
    verification:
      - kind: unit
        ref: "engine/src/graph/tests.rs#narrow_via_cypher_fails_open_on_invalid_hop_cap"
        status: pass
    human_judgment: false

duration: 10min
completed: 2026-08-09
status: complete
---

# Phase 04.1 Plan 09 Summary

**Distinct edge_id bound counting in fetch_neighborhood MAX_TOTAL_EDGES check, preventing false-positive multi-hop rejections**

## Performance

- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Fixed `fetch_neighborhood` (`engine/src/graph/mod.rs`) to count distinct `edge_id` values in `seen_edge_ids: HashSet<String>` across BFS hops instead of summing raw per-hop row counts.
- Added fail-closed `missing edge_id column` error handling for missing/unreadable `edge_id` columns during bound checking.
- Added characterization test `fetch_neighborhood_accepts_in_bounds_neighborhood_despite_raw_recount_exceeding_max_total_edges` in `engine/src/tests.rs` proving 499 distinct edges across 3 hops return `Ok` with exactly 499 distinct edges.
- Renamed and re-scoped `narrow_via_cypher_fails_open_on_invalid_hop_cap` in `engine/src/graph/tests.rs` with doc comments detailing its `InvalidHopCap` coverage and noting `CypherExecute`/`CypherParse` as an accepted residual gap.

## Task Commits

1. **Task 1: Fix CR-01 (fresh) — fetch_neighborhood's MAX_TOTAL_EDGES bound counts distinct edge_id values** - inline execution
2. **Task 2: Fix WR-04 — rename narrow_via_cypher_fails_open_on_genuine_cypher_error to match its actual coverage** - inline execution

## Files Created/Modified
- `engine/src/graph/mod.rs` - Replaced `total_edge_count` with `seen_edge_ids` HashSet in `fetch_neighborhood`
- `engine/src/tests.rs` - Added `fetch_neighborhood_accepts_in_bounds_neighborhood_despite_raw_recount_exceeding_max_total_edges`
- `engine/src/graph/tests.rs` - Renamed test function and added doc comment for WR-04

## Decisions Made
- Replaced raw row count accumulator in `fetch_neighborhood`'s `MAX_TOTAL_EDGES` check with a running `HashSet<String>` of `edge_id`s, ensuring bidirectional re-matches across BFS hops do not cause false-positive bound rejections.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Next Phase Readiness
- All gap closure plans for phase 04.1 are completed.

---
*Phase: 04.1-knowledge-graph-extraction-query-full-implementation*
*Completed: 2026-08-09*
