---
phase: 04.1-knowledge-graph-extraction-query-full-implementation
plan: 06
type: execute
wave: 6
depends_on: []
files_modified:
  - engine/src/graph/mod.rs
  - engine/src/graph/tests.rs
  - engine/src/tests.rs
autonomous: true
gap_closure: true
requirements: [DATA-04, DATA-05, RAG-05]

must_haves:
  truths:
    - "cypher_confirmed_neighbor_ids (engine/src/graph/mod.rs) is proven by a direct call, independent of narrow_via_cypher's fallback wrapper, to return the TRUE reachable-neighbor set via a genuine lance-graph CypherQuery::execute() run — not merely inferred from narrow_via_cypher's fail-open/agrees-with-BFS tests (test: cypher_confirmed_neighbor_ids_reflects_real_traverse_multi_hop_execution)"
    - "constrain_to_cypher_matched (engine/src/graph/mod.rs) genuinely narrows the returned (entities, edges) batches — excluding an entity/edge the Cypher-confirmed set does not contain — when fed a cypher_neighbor_ids set that is a strict subset of the BFS-discovered neighbor set, proving Cypher is response-affecting on disagreement, not silently discarded (test: constrain_to_cypher_matched_narrows_response_when_cypher_confirms_fewer_neighbors)"
    - "constrain_to_cypher_matched returns the full, unmodified (entities, edges) batches when the Cypher-confirmed set covers every BFS-discovered neighbor (test: constrain_to_cypher_matched_preserves_full_response_when_cypher_confirms_all_neighbors)"
    - "narrow_via_cypher (engine/src/graph/mod.rs) treats cypher_confirmed_neighbor_ids's Ok(empty_set) result as a genuine, successful zero-neighbor confirmation — narrowing the returned (entities, edges) to the seed alone (or to nothing, if the seed itself is absent from the input batches) — rather than falling back to the full unconstrained BFS neighborhood; only a genuine Err from cypher_confirmed_neighbor_ids or from constrain_to_cypher_matched still triggers the unconstrained fallback (REVIEWS.md MEDIUM, closing the total-disagreement case Gap 1's own must_haves above did not cover) (tests: narrow_via_cypher_narrows_to_empty_when_seed_absent_from_graph, narrow_via_cypher_narrows_to_seed_only_when_zero_neighbors_confirmed, narrow_via_cypher_fails_open_on_genuine_cypher_error)"
    - "fetch_neighborhood (engine/src/graph/mod.rs) returns Err(GraphSpikeError{kind: Bridge, ..}) — never a silently-truncated Ok — when a single hop's accumulated entity_edges rows exceed MAX_TOTAL_EDGES (test: fetch_neighborhood_rejects_oversized_edge_batch)"
    - "fetch_neighborhood's MAX_FRONTIER_SIZE bound is enforced against every hop's freshly-computed next_frontier, including the FINAL hop — not only checked at the top of a subsequent iteration that may not exist — closing WR-01 (04.1-REVIEW.md): a high-degree final hop can no longer produce an unbounded entity_id IN (...) predicate (test: fetch_neighborhood_rejects_oversized_final_hop_frontier)"
    - "dedup_edges_by_identity (engine/src/graph/mod.rs) keys on edge_id — the entity_edges table's own Uuid::new_v4()-generated unique identifier, now selected into fetch_neighborhood's edges batch — instead of the (source_node_id, target_node_id, relation_type, weight) 4-tuple Plan 04 chose, which two genuinely distinct edges from separate documents can coincidentally share under this project's temperature=0.0 deterministic extraction default (CR-01, 04.1-REVIEW.md CRITICAL). Two edges persisted from two different documents, with an identical (source name, target name, relation_type, confidence) 4-tuple, both survive a shared-entity neighborhood fetch — neither is silently collapsed into the other (test: fetch_neighborhood_returns_both_edges_when_two_documents_share_identical_fact)"
    - "DATA-04's idempotent-re-ingestion and concurrency-bound claims are already proven and passing (extraction_reingestion_is_idempotent_by_construction, extraction_concurrency_bound_is_observed_not_assumed, engine/src/tests.rs) — cited here as existing acceptance evidence per the spec-less-probe-fallback D-06 auto-resolution rule; this plan adds no new work against those two probe rows"
    - "DATA-05/RAG-05's graph-query-traversal-correctness claim is satisfied by this plan's Gap-1/Gap-2/CR-01/WR-01 truths above — the standalone unclassified probe rows for these two requirement IDs fold into these same entries rather than standing as separate bare, unresolved items"
  artifacts:
    - path: engine/src/graph/mod.rs
      provides: "fetch_neighborhood's local edges_schema and its single entity_edges_table() .select(...) projection carry a leading edge_id Utf8 field (the separate entities_table() .select(...) and its local entities_schema are unchanged); dedup_edges_by_identity keys its seen set on edge_id alone; the MAX_FRONTIER_SIZE check runs once per hop against that hop's freshly-computed next_frontier, not only at the top of the next iteration; narrow_via_cypher's Ok(confirmed_ids) arm no longer special-cases an empty confirmed set — it always calls constrain_to_cypher_matched, which correctly narrows to the seed alone (or to nothing) on total disagreement"
      contains: "edge_id"
    - path: engine/src/graph/tests.rs
      provides: "three new pure-fixture unit tests proving Cypher narrows the response on disagreement and that cypher_confirmed_neighbor_ids reflects a genuine lance-graph execution against the post-fix 5-column edges shape; the pre-existing cypher_narrowing_fail_open test renamed to narrow_via_cypher_narrows_to_empty_when_seed_absent_from_graph with corrected assertions; two new tests proving the seed-only-narrowing and genuine-error-fail-open cases"
      contains: "cypher_confirmed_neighbor_ids_reflects_real_traverse_multi_hop_execution"
    - path: engine/src/tests.rs
      provides: "three new DB-backed regression tests: CR-01's cross-document dedup proof, MAX_TOTAL_EDGES rejection, and the WR-01 final-hop frontier-cap proof"
      contains: "fetch_neighborhood_returns_both_edges_when_two_documents_share_identical_fact"
  key_links:
    - from: "engine/src/graph/mod.rs (fetch_neighborhood's entity_edges .select(...))"
      to: "engine/src/graph/mod.rs (dedup_edges_by_identity)"
      via: "edge_id is now selected into the batch fetch_neighborhood hands to dedup_edges_by_identity, which keys its seen set on that column instead of a 4-tuple that can coincide across genuinely distinct rows"
      pattern: "dedup_edges_by_identity"
    - from: "engine/src/graph/mod.rs (cypher_confirmed_neighbor_ids -> traverse_multi_hop -> bridge::bridge_batch)"
      to: "lance-graph's CypherQuery::execute()"
      via: "the post-CR-01-fix 5-column edges RecordBatch (edge_id, source_node_id, target_node_id, relation_type, weight) is bridged across the arrow-version IPC boundary and pattern-matched by a real Cypher query — Test cypher_confirmed_neighbor_ids_reflects_real_traverse_multi_hop_execution uses a fixture built to this exact post-fix shape specifically to prove the bridge/Cypher path tolerates the new edge_id column rather than silently failing into narrow_via_cypher's fail-open fallback"
      pattern: "traverse_multi_hop"
  prohibitions: []
---

<objective>
Close three `gaps:` items and fold two `behavior_unverified_items:` dispositions (04.1-VERIFICATION.md, verified 2026-08-07, status: gaps_found) that are all scoped to `engine/src/graph/mod.rs`'s traversal/dedup internals: (1) prove `constrain_to_cypher_matched`/`cypher_confirmed_neighbor_ids` are genuinely response-affecting, not a dead no-op path — the central architectural claim this phase's replan revisions exist to establish (REVIEWS.md Codex HIGH: "Cypher is present in production but not load-bearing"); (2) fix and regression-test CR-01, the unresolved CRITICAL code-review finding where `dedup_edges_by_identity`'s 4-tuple identity key can silently drop a genuinely distinct edge; (3) fix and regression-test WR-01, the unresolved code-review WARNING where `MAX_FRONTIER_SIZE` is never checked against the final hop's frontier, and prove the pre-existing `MAX_TOTAL_EDGES` rejection actually rejects rather than silently truncating.

**WR-01 decision (recorded per this plan's quality gate — fold in, not defer):** WR-01 sits inside the exact function (`fetch_neighborhood`) this plan is already modifying for CR-01 and already regression-testing for `MAX_TOTAL_EDGES`; the fix is a one-line relocation of an existing bound check. It is folded into Task 3 below, with its own dedicated regression test, rather than deferred.

**Sequencing note:** Task 1 (Gap 1 — Cypher-narrowing proof) runs first, ahead of the CR-01/WR-01 fixes, because it is the highest-value proof (per this plan's tracer-first sequencing: prove the central claim first) and has no dependency on either fix — `constrain_to_cypher_matched` is a pure filter function untouched by either change, and Task 1's `cypher_confirmed_neighbor_ids` test builds its OWN post-fix-shaped fixture by hand rather than calling `fetch_neighborhood`, so it does not need Task 2 to have run first.

**Prohibition-probe disposition (this round, `must_haves.prohibitions: []`):** Stage 1 recall over this round's actual work (a dedup-identity-key correctness fix, a bound-check relocation, and characterization tests for already-shipped, already-decided traversal code) surfaces only routine-engineering candidates ("must not mutate its input batches", "must not silently drop a row") — Stage 2 precision drops all of them per the probe's own worked "clean-utility" example: a correctness bug fix with no new user-facing surface, no new optionality, and no product/safety/ethics dimension carries zero bespoke prohibitions. CR-01 itself IS the routine-correctness item Stage 2 says to drop from this list — it is handled as a fix task, not minted as a separate prohibition. Plan 03's pre-existing `unresolved`/`judgment`-tier hallucination-detection prohibition (04.1-VERIFICATION.md's Judgment-Tier Prohibitions section) remains carried forward from the original phase, unresolved by design — this round does not re-litigate it, since it is not one of the 5 items `04.1-VERIFICATION.md`'s `gaps:`/`behavior_unverified_items:` scoped to this gap-closure round.

**REVIEWS.md addendum (this round, folded into Task 1 — addresses review concern: "narrow_via_cypher empty-result fallback"):** `narrow_via_cypher`'s `Ok(confirmed_ids)` arm currently short-circuits to the full unconstrained BFS neighborhood whenever Cypher confirms zero reachable neighbors (`engine/src/graph/mod.rs:623-627`), treating a genuine, successful "zero neighbors" result identically to an execution failure — silently defeating Cypher's narrowing authority on total disagreement (REVIEWS.md MEDIUM, Antigravity round). This is a DIFFERENT case from Gap 1's own must_haves above (which prove Cypher narrows on a PARTIAL-subset disagreement) — the reviewer's Consensus Summary confirms this specific all-neighbors-eliminated case is "not covered by Plans 06/07's gap-closure scope" as originally written. Task 1 below fixes and regression-tests this alongside its own Gap-1 proof work, since both concern the same function's disagreement-handling semantics and the fix is a small deletion, not a new subsystem.

## Review Findings Disposition (04.1-REVIEWS.md, Antigravity round, 2026-08-08)

This round's cross-AI review raised 7 findings. Four target already-executed Plans 01/03/04/05, whose PLAN.md text is historical record and cannot itself discharge a code-level finding — each is resolved either by a real code change in an unexecuted plan, or by an explicit, already-satisfied disposition:

- **MEDIUM — `narrow_via_cypher` empty-confirmed-set fallback** (targets Plan 02, executed; `engine/src/graph/mod.rs:623-627`): real code defect. Fixed and regression-tested in this plan's Task 1 above (folded into the same Gap-1 proof this plan already exists to deliver) — see the addendum paragraph above.
- **MEDIUM — whole-table scan on name-based seed lookup** (targets Plan 04, executed; `engine/src/main.rs:1773-1804`): already an accepted, explicitly-documented v1 trade-off — confirmed by direct code read (the call site's own comment cites `T-04.1-19` and explains `entities.name` has no secondary index). Plan 04's `04.1-04-PLAN.md` threat register already carries `T-04.1-19` with disposition `mitigate` (the length-ceiling half) and an explicit accepted-cost note (the table-scan half). No further action this round.
- **LOW — tie-breaker inverted from doc comment** (targets Plan 05, executed; `engine/src/prompt.rs:363-369`): real code defect, empirically confirmed (`is_graph_b.cmp(&is_graph_a)` puts `Graph` before `Evidence` on an exact-score tie, contradicting the doc comment). Fixed and regression-tested in `04.1-08-PLAN.md` (new plan, this round) rather than here, to keep this plan scoped to `graph/mod.rs` traversal internals.
- **LOW — Plan 01 Task 1 largely a no-op against current main**: confirmed by direct read — `engine/Cargo.toml:29-32` (`lance-graph`/`arrow-ipc` etc.) and `engine/src/lib.rs:6` (`pub mod graph;`) are already unconditional; no `optional = true` remains anywhere in the manifest. Plan 01 already executed and the codebase is already in the target end state regardless of how Task 1's own verification text was worded — no code change is needed and none is made here. Plan 01's historical text is left untouched (editing already-executed plan text changes no shipped behavior); recorded here for traceability only, per this round's execution contract.
- **LOW — `dedup_edges_by_identity` should reject a missing `edge_id` column with a clear error, not panic** (targets this plan, unexecuted): already the directed behavior in Task 2 below — the `edge_id` column read follows this function's existing `ok_or_else`-mapped-to-`GraphSpikeError` pattern, identical to how `source_node_id`/`target_node_id`/etc. are already read. Task 2's action and acceptance criteria now say so explicitly.
- **LOW — test teardowns should clean up temp LanceDB directories in `spawn_worker` test runs** (targets Plan 07, unexecuted): addressed directly in `04.1-07-PLAN.md`'s own tasks.
- **LOW — add `tracing::debug!` field-level validation failure detail in `validate_extraction_output`** (targets Plan 03, executed; `engine/src/graph/extraction.rs`): real code addition, not a Plan 03 text issue. Added in `04.1-08-PLAN.md` (new plan, this round) rather than here.

Purpose: DATA-05's central claim — that graph queries genuinely traverse and are constrained by real Cypher pattern-matching, not merely a validated-but-dead-code integration — has no direct proof in the codebase, a real CRITICAL data-correctness defect (CR-01) ships unfixed, and a real MEDIUM disagreement-handling defect in `narrow_via_cypher` ships unfixed. All three must close before this phase can be considered done.
Output: six new/updated characterization tests proving Cypher narrows the response on disagreement (including the total-disagreement/zero-confirmed-neighbors case) and fails open only on genuine execution errors; the CR-01 dedup-identity fix plus its cross-document regression test; the WR-01 frontier-cap relocation plus its regression test; the pre-existing `MAX_TOTAL_EDGES` rejection's first-ever test.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-VERIFICATION.md
@.planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-REVIEW.md
@rust-guidelines.md
@engine/src/graph/mod.rs
@engine/src/db/mod.rs
</context>

## Artifacts This Phase Produces

- `engine/src/graph/tests.rs` — new tests `constrain_to_cypher_matched_narrows_response_when_cypher_confirms_fewer_neighbors`, `constrain_to_cypher_matched_preserves_full_response_when_cypher_confirms_all_neighbors`, `cypher_confirmed_neighbor_ids_reflects_real_traverse_multi_hop_execution`, `narrow_via_cypher_narrows_to_seed_only_when_zero_neighbors_confirmed`, `narrow_via_cypher_fails_open_on_genuine_cypher_error`; `cypher_narrowing_fail_open` renamed to `narrow_via_cypher_narrows_to_empty_when_seed_absent_from_graph` with corrected assertions.
- `engine/src/graph/mod.rs` — `dedup_edges_by_identity` re-keyed on `edge_id: HashSet<String>`; `fetch_neighborhood`'s single `entity_edges_table()` `.select(...)` call and its local `edges_schema` gain a leading `edge_id` `Utf8` non-null field (the separate `entities_table()` `.select(...)` and local `entities_schema` are untouched — there is only ONE edges select in this function, not two); the `MAX_FRONTIER_SIZE` bound check relocates from the top of each loop iteration to immediately after that iteration's `next_frontier` is computed; `narrow_via_cypher`'s empty-confirmed-set early-return is deleted (REVIEWS.md MEDIUM fix).
- `engine/src/tests.rs` — new tests `fetch_neighborhood_returns_both_edges_when_two_documents_share_identical_fact`, `fetch_neighborhood_rejects_oversized_edge_batch`, `fetch_neighborhood_rejects_oversized_final_hop_frontier`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove Cypher genuinely narrows the response on disagreement (Gap 1), and fix the zero-confirmed-neighbors fallback (REVIEWS.md MEDIUM)</name>
  <read_first>engine/src/graph/mod.rs, engine/src/graph/tests.rs, .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-02-PLAN.md, .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-02-SUMMARY.md, .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-VERIFICATION.md, .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-REVIEWS.md</read_first>
  <files>engine/src/graph/mod.rs, engine/src/graph/tests.rs</files>
  <behavior>
    - narrow_via_cypher's Ok(confirmed_ids) arm, when confirmed_ids is empty (a genuine successful Cypher execution confirming zero reachable neighbors), narrows the returned batches to the seed alone via constrain_to_cypher_matched — it must NOT return the unconstrained (entities.clone(), edges.clone()) pair.
    - narrow_via_cypher's Err(...) arm (a genuine cypher_confirmed_neighbor_ids execution failure, e.g. an invalid hop_cap) is unaffected by this fix and still returns the unconstrained (entities.clone(), edges.clone()) pair unchanged.
  </behavior>
  <action>
    In `engine/src/graph/tests.rs`, add three tests exercising `constrain_to_cypher_matched` (currently `pub(crate) fn`, line ~557) and `cypher_confirmed_neighbor_ids` (currently `pub(crate) async fn`, line ~523) directly via `super::`, matching this file's existing style for `super::narrow_via_cypher` in `cypher_narrowing_fail_open` — do not add either name to the top-of-file `use super::{...}` import list; call them fully qualified.

    Test `constrain_to_cypher_matched_narrows_response_when_cypher_confirms_fewer_neighbors`: build the fixture via the existing private helper `three_entity_two_edge_fixture()` (Alice `abc-123` --knows--> Bob `def-456`, Alice --founded_by--> Acme `ghi-789`). Build `cypher_neighbor_ids: HashSet<String>` containing only `"def-456"` — simulating Cypher confirming Bob but NOT Acme, a strict subset of what BFS discovered. Call `super::constrain_to_cypher_matched(&entities, &edges, "abc-123", &cypher_neighbor_ids)`, expect `Ok`. Assert the returned entities batch has exactly 2 rows (Alice + Bob, Acme excluded) and the returned edges batch has exactly 1 row whose `relation_type` column value is `"knows"` (the founded_by edge to the unconfirmed Acme is excluded) — assert on the surviving relation_type value itself, not merely the row count, so the test cannot pass by coincidence if the wrong edge survived.

    Test `constrain_to_cypher_matched_preserves_full_response_when_cypher_confirms_all_neighbors`: same fixture, `cypher_neighbor_ids` containing both `"def-456"` and `"ghi-789"`. Call the same function, assert the returned entities batch has 3 rows and the returned edges batch has 2 rows — the full, unnarrowed response is preserved when Cypher agrees with BFS on everything.

    Test `cypher_confirmed_neighbor_ids_reflects_real_traverse_multi_hop_execution`: build a NEW fixture inline in this test (do not reuse `three_entity_two_edge_fixture()`, which has only 3 edge columns) shaped to match `fetch_neighborhood`'s POST-FIX output exactly: an entities `RecordBatch` with columns `entity_id`/`name`/`entity_type` (3 rows: Alice `abc-123`, Bob `def-456`, Acme `ghi-789`, same values as the existing fixture), and an edges `RecordBatch` with FIVE columns in this exact order — `edge_id` (Utf8, e.g. `"edge-1"`/`"edge-2"`), `source_node_id` (Utf8), `target_node_id` (Utf8), `relation_type` (Utf8), `weight` (Float32, e.g. `0.9`/`0.8`) — two rows: Alice--knows-->Bob and Alice--founded_by-->Acme. This 5-column shape matters: it is the exact shape `fetch_neighborhood` will produce after Task 2 of this plan adds `edge_id`, and this test is the canary that lance-graph's bridge/Cypher path tolerates that extra column rather than erroring internally (which `narrow_via_cypher`'s wrapper would otherwise silently swallow into its fail-open fallback, masking a real regression). Call `super::cypher_confirmed_neighbor_ids(&entities, &edges, "abc-123", 1).await`, expect `Ok`. Assert the returned `HashSet<String>` equals exactly `{"def-456", "ghi-789"}` — both real neighbors reachable via a genuine `traverse_multi_hop` execution, proven independently of `narrow_via_cypher`'s wrapper.

    **REVIEWS.md MEDIUM fix (folded into this task — addresses review concern: "narrow_via_cypher empty-result fallback"):** `narrow_via_cypher` (`engine/src/graph/mod.rs`, ~line 616-641) currently special-cases `cypher_confirmed_neighbor_ids`'s `Ok(confirmed_ids)` arm: `if confirmed_ids.is_empty() { tracing::warn!(...); return (entities.clone(), edges.clone()); }`, executed BEFORE `constrain_to_cypher_matched` ever runs. This conflates two distinct outcomes — a genuine, successful Cypher execution that confirms zero reachable neighbors (which SHOULD narrow the response, since `constrain_to_cypher_matched`'s own `allowed_ids.insert(seed_id.to_string())` step already correctly narrows to `{seed}` when its `cypher_neighbor_ids` argument is empty) versus an actual execution failure (the entirely separate `Err(err) => { ...; (entities.clone(), edges.clone()) }` arm below, which legitimately still falls back to unconstrained). Delete the `if confirmed_ids.is_empty() { ... }` block in its entirety so the `Ok(confirmed_ids)` arm ALWAYS proceeds directly to `match constrain_to_cypher_matched(entities, edges, seed_id, &confirmed_ids) { ... }`, whether or not `confirmed_ids` is empty. Leave the outer `Err(err) => { tracing::warn!(...); (entities.clone(), edges.clone()) }` arm and `constrain_to_cypher_matched`'s own inner `Err(err) => { tracing::warn!(...); (entities.clone(), edges.clone()) }` fallback arm completely unchanged — both remain legitimate fail-open paths for genuine infrastructure/execution errors, not touched by this fix.

    Update the existing `cypher_narrowing_fail_open` test (`engine/src/graph/tests.rs`, ~line 314-322): rename it to `narrow_via_cypher_narrows_to_empty_when_seed_absent_from_graph` and change its two assertions from `assert_eq!(out_entities.num_rows(), entities.num_rows())` / `assert_eq!(out_edges.num_rows(), edges.num_rows())` to `assert_eq!(out_entities.num_rows(), 0)` / `assert_eq!(out_edges.num_rows(), 0)`. Under the fix, a nonexistent seed (`"nonexistent-seed"`, absent from `three_entity_two_edge_fixture()`'s entities) still produces a genuine, successful `Ok(HashSet::new())` from `cypher_confirmed_neighbor_ids` (the Cypher `MATCH` pattern simply matches zero rows for an entity_id that does not exist — this is a successful empty match, not an execution error), so `constrain_to_cypher_matched` now correctly narrows to zero entities (no row in the fixture has `entity_id == "nonexistent-seed"`) and zero edges. Replace the doc comment above the test (currently "Invalid seed_id or empty neighborhood should fail open to original batches" / "Fail open invariant") to instead explain this corrected semantic: a nonexistent seed narrows to nothing, it does not fail open.

    Add `narrow_via_cypher_narrows_to_seed_only_when_zero_neighbors_confirmed`: build a fresh, LOCAL 2-entity/0-edge fixture inline in this test (do not reuse `three_entity_two_edge_fixture()` — its entities are NOT mutually isolated under the undirected `-[r]-` traversal pattern, so it cannot represent a genuinely isolated seed). Build an entities `RecordBatch` with the standard `entity_id`/`name`/`entity_type` `Utf8` columns and 2 rows (`"iso-1"`/`"Lonely"`/`"person"`, `"iso-2"`/`"Other"`/`"person"`), and an EMPTY edges `RecordBatch` (0 rows) using the same `source_node_id`/`target_node_id`/`relation_type` `Utf8` schema as `three_entity_two_edge_fixture`, built via empty `StringArray::from(Vec::<&str>::new())` columns for each. Call `super::narrow_via_cypher(&entities, &edges, "iso-1", 1).await`. Assert `out_entities.num_rows() == 1` (only the seed itself survives — a real, connectionless entity narrows to itself alone, never to the unrelated `"iso-2"` row) and `out_edges.num_rows() == 0`.

    Add `narrow_via_cypher_fails_open_on_genuine_cypher_error`: reuse `three_entity_two_edge_fixture()`, call `super::narrow_via_cypher(&entities, &edges, "abc-123", 0).await` — `hop_cap = 0` forces `clamp_hop_cap` inside `traverse_multi_hop` (called from `cypher_confirmed_neighbor_ids`) to return `Err(GraphSpikeErrorKind::InvalidHopCap)`, a genuine execution-path error distinct from a successful empty match. Assert `out_entities.num_rows() == entities.num_rows()` (3) and `out_edges.num_rows() == edges.num_rows()` (2) — the true fail-open path, triggered only by a real `Err` and never by a successful empty `Ok`, remains intact after this fix.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked constrain_to_cypher_matched_narrows_response_when_cypher_confirms_fewer_neighbors && cargo test --manifest-path engine/Cargo.toml --locked constrain_to_cypher_matched_preserves_full_response_when_cypher_confirms_all_neighbors && cargo test --manifest-path engine/Cargo.toml --locked cypher_confirmed_neighbor_ids_reflects_real_traverse_multi_hop_execution && cargo test --manifest-path engine/Cargo.toml --locked narrow_via_cypher_narrows_to_empty_when_seed_absent_from_graph && cargo test --manifest-path engine/Cargo.toml --locked narrow_via_cypher_narrows_to_seed_only_when_zero_neighbors_confirmed && cargo test --manifest-path engine/Cargo.toml --locked narrow_via_cypher_fails_open_on_genuine_cypher_error</automated>
  </verify>
  <done>All six named tests exist in engine/src/graph/tests.rs and pass. The first three directly prove constrain_to_cypher_matched narrows on disagreement and preserves on full agreement, and that cypher_confirmed_neighbor_ids reflects a real lance-graph execution against the post-CR-01-fix 5-column edges shape. The remaining three prove narrow_via_cypher's corrected disagreement semantics: a nonexistent seed narrows to nothing, an isolated real seed narrows to itself alone, and a genuine Cypher execution error (not a successful empty match) still fails open to the unconstrained input. Contingency: if cypher_confirmed_neighbor_ids_reflects_real_traverse_multi_hop_execution goes red on the 5-column fixture, that is signal the bridge/Cypher path rejects the added edge_id column — do NOT loosen or delete the test to make it pass. Instead keep edge_id selected only for dedup_edges_by_identity's own use and project it out of the batch narrow_via_cypher/traverse_multi_hop actually bridge into lance-graph, then re-run this test unchanged.</done>
  <acceptance_criteria>
    - `grep -n "fn constrain_to_cypher_matched_narrows_response_when_cypher_confirms_fewer_neighbors" engine/src/graph/tests.rs` matches.
    - `grep -n "fn constrain_to_cypher_matched_preserves_full_response_when_cypher_confirms_all_neighbors" engine/src/graph/tests.rs` matches.
    - `grep -n "fn cypher_confirmed_neighbor_ids_reflects_real_traverse_multi_hop_execution" engine/src/graph/tests.rs` matches.
    - The third test's edges fixture literally declares a field named `edge_id` ahead of `source_node_id` in its `Schema::new(vec![...])` call — confirms the fixture matches the post-fix 5-column shape, not the pre-existing 3/4-column fixtures.
    - `grep -n "fn narrow_via_cypher_narrows_to_empty_when_seed_absent_from_graph" engine/src/graph/tests.rs` matches; `grep -n "fn cypher_narrowing_fail_open" engine/src/graph/tests.rs` returns NO match (the old name was renamed, not duplicated).
    - `grep -n "fn narrow_via_cypher_narrows_to_seed_only_when_zero_neighbors_confirmed" engine/src/graph/tests.rs` matches.
    - `grep -n "fn narrow_via_cypher_fails_open_on_genuine_cypher_error" engine/src/graph/tests.rs` matches.
    - `grep -n "confirmed_ids.is_empty()" engine/src/graph/mod.rs` returns NO match inside `narrow_via_cypher`'s body (the early-return special case was deleted, not merely relocated).
    - All six tests pass under `cargo test --manifest-path engine/Cargo.toml --locked`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix CR-01 — dedup_edges_by_identity keys on edge_id, not a collidable 4-tuple</name>
  <read_first>engine/src/graph/mod.rs, engine/src/db/mod.rs (entity_edges_schema, edge_id field), engine/src/main.rs (extract_and_persist_entities weight/edge_id assignment, ~line 2640-2666), .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-04-PLAN.md, .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-04-SUMMARY.md, .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-REVIEW.md, .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-VERIFICATION.md</read_first>
  <files>engine/src/graph/mod.rs, engine/src/tests.rs</files>
  <behavior>
    - Test: `fetch_neighborhood_returns_both_edges_when_two_documents_share_identical_fact` — two separate documents, each independently extracting the identical (source name, target name, relation_type, confidence) fact, must both survive a shared-entity neighborhood fetch as two distinct edges (distinct `edge_id`), never collapsed to one.
  </behavior>
  <action>
    In `engine/src/graph/mod.rs`'s `fetch_neighborhood` (currently ~line 302-475): there is exactly ONE `.select(lancedb::query::Select::columns(&[...]))` call reading `["source_node_id", "target_node_id", "relation_type", "weight"]` — the `entity_edges_table()` query (~line 353-358). Add `"edge_id"` as the FIRST element of THAT columns array only. Add a matching `arrow_schema::Field::new("edge_id", arrow_schema::DataType::Utf8, false)` as the FIRST field of the local `edges_schema` built at ~line 447-452, ahead of `source_node_id`. Do NOT touch the SEPARATE `entities_table()` `.select(...)` call (`["entity_id", "name", "entity_type"]`, ~line 429-433) or its local `entities_schema` (~line 441-445) — those are unrelated to this fix and must stay exactly as they are; adding `edge_id` there would be a wrong-target edit with no schema field to receive it.

    In `dedup_edges_by_identity` (~line 485-521): replace the doc comment's rationale (currently justifying the 4-tuple) with one explaining that identity is now `edge_id` — the `entity_edges` table's own `Uuid::new_v4()`-generated unique identifier (set once per edge at creation time in `extract_and_persist_entities`, `engine/src/main.rs` ~line 2660), now selected into this function's input batch — which correctly distinguishes a genuinely distinct edge from the same edge merely re-observed across a later bidirectional BFS hop, unlike a value-tuple two unrelated edges can coincidentally share. Read the `edge_id` column as a `StringArray` (mirroring the existing `source_node_id`/`target_node_id`/etc. column-read pattern already in this function: `.column_by_name("edge_id").and_then(|c| c.as_any().downcast_ref::<arrow_array::StringArray>()).ok_or_else(|| GraphSpikeError::new(GraphSpikeErrorKind::Bridge, "missing edge_id column"))?`) — REVIEWS.md LOW noted this exact pattern is what prevents a panic on a legacy/test `RecordBatch` lacking `edge_id`: since `.and_then(...).ok_or_else(...)?` returns a typed `Err` rather than unwrapping, a missing or wrong-type `edge_id` column returns `Err(GraphSpikeError{kind: Bridge, ..})` from `dedup_edges_by_identity`, never a panic — this is already guaranteed by using the same pattern as the other four columns, not a separate code path to add. Remove the reads of `source_node_id`/`target_node_id`/`relation_type`/`weight` for identity purposes entirely — they are no longer needed once `edge_id` is the sole key. Change the `seen` set's type from the 4-element tuple to `HashSet<String>`, and each row's key from the 4-tuple construction to `edge_id_col.value(i).to_string()`.

    In `engine/src/tests.rs`, add `fetch_neighborhood_returns_both_edges_when_two_documents_share_identical_fact`: initialize a fresh `DatabaseManager`. Call `super::extract_and_persist_entities` twice with two separate `IngestionJob`s (two distinct `Uuid::new_v4()` document IDs, two distinct short paragraph texts each stating "Alice knows Bob" in different wording so both pass `MIN_CHUNK_CONTENT_LENGTH`), each backed by a `graph::extraction::FakeExtractionGenerator::new(Ok(...))` returning the IDENTICAL `ExtractionOutput` — entities Alice/Bob (`entity_type: "person"`), one relation `source: "Alice", target: "Bob", relation_type: "knows", confidence: 0.9` — and `&FakeEmbedder`. Before checking the fix, confirm the fixture itself is genuinely two documents: query `entity_edges_table()` with `.only_if("document_id = '{doc_a}'")` and separately `"document_id = '{doc_b}'"`, assert each returns exactly 1 row. Then query `entities_table()` with `.only_if("name = 'Alice'")`, collect the `entity_id` column's row 0 value as `alice_id`. Call `graph::fetch_neighborhood(&database, &alice_id, 1, true).await`, expect `Ok`. Assert the returned edges batch has exactly 2 rows, and that reading its `edge_id` column into a `HashSet<&str>` yields 2 distinct values — proving both documents' genuinely distinct edges survive, neither silently collapsed into the other.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked fetch_neighborhood_returns_both_edges_when_two_documents_share_identical_fact && cargo test --manifest-path engine/Cargo.toml --locked query_graph_recovers_multi_hop_edge_relation_properties && cargo test --manifest-path engine/Cargo.toml --locked query_graph_bidirectional_seed_as_target</automated>
  </verify>
  <done>dedup_edges_by_identity keys on edge_id alone; two distinct edges from two documents sharing an identical fact/confidence tuple both survive a shared-entity fetch_neighborhood call; pre-existing multi-hop/bidirectional query_graph tests still pass unchanged.</done>
  <acceptance_criteria>
    - `grep -n "fn fetch_neighborhood_returns_both_edges_when_two_documents_share_identical_fact" engine/src/tests.rs` matches.
    - `grep -n "HashSet<String>" engine/src/graph/mod.rs` includes a match inside `dedup_edges_by_identity`'s body (the `seen` set's new type).
    - `grep -n "\"edge_id\"" engine/src/graph/mod.rs` shows at least two matches: one inside `fetch_neighborhood`'s `.select(...)` columns array, one inside its local `edges_schema` field list.
    - The new test asserts both a row count of 2 AND at least 2 distinct `edge_id` values on the returned edges batch — not merely a row count (which the pre-fix 4-tuple-keyed code could also produce by coincidence if the two edges' confidences happened to differ).
    - `grep -n "ok_or_else" engine/src/graph/mod.rs` shows a match for the `edge_id` column read inside `dedup_edges_by_identity`'s body — confirming a missing/wrong-type `edge_id` column returns a typed `GraphSpikeError`, never a panic (REVIEWS.md LOW, addressed by reusing this function's own existing column-read pattern for the four other columns).
    - `cargo test --manifest-path engine/Cargo.toml --locked` for the three named tests above all pass.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix WR-01 — bound the final hop's frontier too; prove MAX_TOTAL_EDGES actually rejects</name>
  <read_first>engine/src/graph/mod.rs (as modified by Task 2), .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-02-PLAN.md, .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-02-SUMMARY.md, .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-REVIEW.md, .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-VERIFICATION.md</read_first>
  <files>engine/src/graph/mod.rs, engine/src/tests.rs</files>
  <behavior>
    - Test: `fetch_neighborhood_rejects_oversized_edge_batch` — more than `MAX_TOTAL_EDGES` rows for one seed's single hop returns `Err`, not a silently-truncated `Ok`.
    - Test: `fetch_neighborhood_rejects_oversized_final_hop_frontier` — a frontier that only becomes oversized on the LAST hop (no subsequent iteration exists to catch it under the old top-of-loop check) still returns `Err`.
  </behavior>
  <action>
    **WR-01 fix, in `fetch_neighborhood`'s hop loop (`engine/src/graph/mod.rs`, `for _hop in 1..=hop_cap { ... }`, ~line 322-413):** delete the `if frontier.len() > MAX_FRONTIER_SIZE { return Err(...) }` block currently at the TOP of the loop body (before the predicate/query). This deletion is safe: the initial `frontier` is `{seed_entity_id}` (size 1, trivially under the bound), and every subsequent `frontier` value was already validated as the PRIOR iteration's `next_frontier` once the check below is added — the induction covers every hop including the first. In its place, immediately after `next_frontier` is fully built (right after the `for batch in &edge_batches { ... }` loop that populates it, BEFORE `accumulated_edge_batches.extend(edge_batches)` and `visited.extend(next_frontier.clone())`), add the equivalent check against `next_frontier.len()` instead of `frontier.len()`, using the same `MAX_FRONTIER_SIZE`-exceeded error message shape (substituting `next_frontier.len()` for the count). This relocation means EVERY hop's freshly-computed frontier is bound-checked before it is folded into `visited` or reused as the next hop's query input — including the final hop, whose `next_frontier` was previously folded into `visited` (and therefore into the terminal `entity_id IN (...)` predicate) with no bound check at all, since there was no subsequent iteration's top-of-loop check to catch it.

    In `engine/src/tests.rs`, add `fetch_neighborhood_rejects_oversized_edge_batch`: initialize a fresh `DatabaseManager`, generate a `seed_id = Uuid::new_v4().to_string()` (no entity row needed — the over-limit check fires before any entities query). Build a `RecordBatch` matching `db::entity_edges_schema()` (`edge_id`, `source_node_id`, `target_node_id`, `relation_type`, `weight`, `document_id`, nullable `summary`, nullable `summary_vector` via `arrow_array::new_null_array`) with `graph::MAX_TOTAL_EDGES + 1` rows, every row's `source_node_id` set to `seed_id`, each row's `target_node_id`/`edge_id` a fresh `Uuid::new_v4()`, `relation_type: "related_to"`, `weight: 0.5_f32`, a shared `document_id`. `entity_edges_table().add(batch).execute().await`. Call `graph::fetch_neighborhood(&database, &seed_id, 1, true).await`, expect `Err`, assert `err.kind == graph::GraphSpikeErrorKind::Bridge` and `err.message().contains("MAX_TOTAL_EDGES")`.

    Add `fetch_neighborhood_rejects_oversized_final_hop_frontier`: same DB, `seed_id`/`hub_id = Uuid::new_v4().to_string()` each. Insert ONE row (hop 1): `source_node_id: seed_id, target_node_id: hub_id`, plus `edge_id`/`relation_type: "parent"`/`weight: 0.5_f32`/`document_id`. Insert `graph::MAX_FRONTIER_SIZE + 1` rows (hop 2): each with `source_node_id: hub_id`, a distinct fresh `Uuid::new_v4()` `target_node_id` and `edge_id`, `relation_type: "child"`, `weight: 0.5_f32`, the same `document_id` — total 202 rows, comfortably under `MAX_TOTAL_EDGES` so that check cannot mask this one. Call `graph::fetch_neighborhood(&database, &seed_id, 2, true).await` (`hop_cap = 2`, the LAST hop is where the 201-entry `next_frontier` is computed — there is no hop 3 for the old top-of-loop check to have caught it). Expect `Err`, assert `err.kind == graph::GraphSpikeErrorKind::Bridge` and `err.message().contains("MAX_FRONTIER_SIZE")`.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked fetch_neighborhood_rejects_oversized_edge_batch && cargo test --manifest-path engine/Cargo.toml --locked fetch_neighborhood_rejects_oversized_final_hop_frontier</automated>
  </verify>
  <done>Both new tests exist and pass: fetch_neighborhood rejects an oversized single-hop edge batch, and separately rejects an oversized frontier computed only on the final hop — proving WR-01's relocated check covers the case the old top-of-loop check structurally could not.</done>
  <acceptance_criteria>
    - `grep -n "fn fetch_neighborhood_rejects_oversized_edge_batch" engine/src/tests.rs` matches.
    - `grep -n "fn fetch_neighborhood_rejects_oversized_final_hop_frontier" engine/src/tests.rs` matches.
    - `grep -n "next_frontier.len() > MAX_FRONTIER_SIZE" engine/src/graph/mod.rs` matches inside `fetch_neighborhood`'s loop body, positioned after the `for batch in &edge_batches` block and before `accumulated_edge_batches.extend(edge_batches)`.
    - Reading `fetch_neighborhood`'s current source confirms exactly one runtime size comparison against the `MAX_FRONTIER_SIZE` constant exists in the whole function, positioned where the criterion above places it (operating on the freshly-computed hop result, not the pre-query variable) — the check was relocated, not duplicated.
    - Both named tests pass under `cargo test --manifest-path engine/Cargo.toml --locked`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ingested document content → extracted entity/edge persistence → query response / prompt context | Unchanged from the original phase's accepted D-33 risk framing (local-first, single-user, no redaction) — this round fixes a correctness defect within that already-accepted boundary, it does not change the boundary itself |
| entity_edges bidirectional BFS re-query (fetch_neighborhood) → dedup_edges_by_identity → caller response | The specific boundary CR-01 crosses: two structurally distinct persisted rows (different edge_id, different document_id) must not be collapsed into one at this dedup step |
| Caller-controlled hop_cap → per-hop frontier size → terminal SQL predicate length | The boundary WR-01 crosses: an entity's fan-out at the FINAL hop was previously able to reach the terminal `entity_id IN (...)` predicate unbounded |
| `narrow_via_cypher`'s Cypher-vs-BFS disagreement resolution → returned neighborhood → RAG prompt / QueryGraph response | The boundary the REVIEWS.md MEDIUM finding crosses: a genuine, successful "zero confirmed neighbors" Cypher result must narrow the response, not silently re-admit the full unconstrained BFS neighborhood as if Cypher had failed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-21 | Tampering | `engine/src/graph/mod.rs` `dedup_edges_by_identity` (pre-fix 4-tuple identity) | medium | mitigate | CR-01 fix: key on `edge_id`, the table's own corpus-unique identifier, instead of a value-tuple two distinct rows can coincidentally share under this project's `temperature: 0.0` deterministic default extraction config — closes the path where a genuinely distinct edge from a second document is silently suppressed from every query response that traverses the shared entity |
| T-04.1-22 | Denial of Service | `engine/src/graph/mod.rs` `fetch_neighborhood` final-hop frontier (WR-01) | low | mitigate | Relocate the `MAX_FRONTIER_SIZE` check to run against every hop's freshly-computed `next_frontier`, including the final hop, closing the one path where a high-degree final-hop entity could produce an unbounded `entity_id IN (...)` predicate string |
| T-04.1-23 | Denial of Service | `engine/src/graph/mod.rs` `fetch_neighborhood` `MAX_TOTAL_EDGES` enforcement (previously unproven by any test) | low | mitigate | Add `fetch_neighborhood_rejects_oversized_edge_batch`, proving the existing cap-exceeded rejection genuinely returns `Err` rather than an unproven, potentially-truncated `Ok` |
| T-04.1-26 | Tampering | `engine/src/graph/mod.rs` `narrow_via_cypher` (pre-fix empty-confirmed-set fallback) | medium | mitigate | REVIEWS.md MEDIUM fix: delete the `confirmed_ids.is_empty()` early-return so a genuine, successful zero-neighbor Cypher result narrows to the seed alone via `constrain_to_cypher_matched`, instead of being treated identically to an execution failure and silently re-admitting the full unconstrained BFS neighborhood — closes the total-disagreement case Gap 1's original test coverage did not reach |

**Canon-referral breadcrumb:** the underlying SQL-literal-escaping / hop-cap injection surface these functions touch remains covered by this phase's already-accepted T-04.1-01/T-04.1-11 mitigations (`escape_sql_literal`, `clamp_hop_cap`) — not re-litigated or re-minted here; this round's threats are scoped strictly to the dedup-identity and frontier-bound defects it fixes.
</threat_model>

<verification>
- `cargo build --manifest-path engine/Cargo.toml` succeeds.
- `cargo test --manifest-path engine/Cargo.toml --locked` runs the full suite once at the end of this plan's work and shows 0 failed (confirms no regression across the full pre-existing suite plus this plan's 8 net-new tests and one renamed test with corrected assertions).
- Every test named in this plan's three tasks is present in source (`grep -n "fn <name>"`) and passes individually, per each task's own `<acceptance_criteria>`.
</verification>

<success_criteria>
`constrain_to_cypher_matched`/`cypher_confirmed_neighbor_ids` are proven, by direct test, to be genuinely response-affecting rather than a validated-but-dead-code path — closing the central architectural claim this phase's replan revisions exist to establish. `narrow_via_cypher`'s empty-confirmed-set fallback bug (REVIEWS.md MEDIUM) is fixed and regression-tested, including the corrected genuine-fail-open-vs-successful-empty-match distinction. CR-01's dedup-identity defect is fixed (keyed on `edge_id`) and regression-tested against the exact cross-document scenario the code review demonstrated was broken. WR-01's final-hop frontier gap is fixed and regression-tested. `MAX_TOTAL_EDGES`'s rejection path has its first test. No pre-existing test regresses.
</success_criteria>

<output>
Create `.planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-06-SUMMARY.md` when done.
</output>
