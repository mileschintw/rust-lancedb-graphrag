---
phase: 04.1-knowledge-graph-extraction-query-full-implementation
plan: 05
type: execute
wave: 5
depends_on: ["04.1-04"]
files_modified:
  - engine/src/main.rs
  - engine/src/prompt.rs
  - engine/src/retrieval/mod.rs
  - engine/src/generation/mod.rs
  - engine/src/generation/openrouter.rs
  - engine/src/tests.rs
  - config/config.example.toml
autonomous: true
requirements: [DATA-05]

must_haves:
  truths:
    - "Graph facts and chunk evidence share a single evidence token budget (D-29) and are interleaved by normalized, graph_weight-scaled relevance score across both sources — upgrading Plan 02's append-order pack_evidence_and_graph_prompt in place (same function name, same call sites in main.rs and openrouter.rs — no rename, though the signature gains a graph_weight parameter), never packed chunk-evidence-first except for Plan 02's already-established reserve-one-citable-chunk rule, which this plan preserves"
    - "graph_weight is a configurable multiplier (TOML + env override, default 1.0) applied to normalized graph-match scores before merging with normalized RRF-fused chunk scores, validated with the same finite/nonnegative/bounded discipline already applied to vector_weight/bm25_weight (D-30)"
    - "The configured graph_weight genuinely reaches the actual outbound OpenRouter chat request body, proven by a real raw-HTTP-capture test — not only by an intermediate PackedEvidence assertion — because graph_weight is threaded through GenerationRequest (a new field, read by openrouter.rs's execute_one_call) rather than main.rs's pre-check and openrouter.rs's actual send call independently deriving it from two different places that could silently diverge (REVIEWS.md HIGH: 'graph_weight has no complete path to the actual provider request')"
    - "The graph_augmentation outcome tag (succeeded/no_match_found/attempted_and_failed, already built in Plan 02's attempt_graph_augmentation) is proven observable end-to-end through the FULL /rag/query handler for all three outcomes, not only through Plan 02's isolated-function-level tests — including a real tracing-capture assertion, not just behavioral inference"
  artifacts:
    - path: engine/src/prompt.rs
      provides: "score-interleaved packing of EvidenceBlock and GraphFactBlock into the shared token budget (upgrading Plan 02's append-order pack_evidence_and_graph_prompt), now taking graph_weight as an explicit parameter"
      contains: "graph_weight"
    - path: engine/src/retrieval/mod.rs
      provides: "graph_weight field + validation on RetrievalSettings, mirroring vector_weight/bm25_weight"
      contains: "graph_weight"
    - path: engine/src/generation/mod.rs
      provides: "GenerationRequest.graph_weight: f64 field (defaulted in ::new(), no external literal constructors exist per Plan 02's own grep-confirmed claim), the single source both main.rs's pre-check and openrouter.rs's actual send call read from"
      contains: "pub graph_weight"
  key_links:
    - from: "engine/src/main.rs (query_rag)"
      to: "engine/src/prompt.rs (pack_evidence_and_graph_prompt, now score-interleaved)"
      via: "graph facts (from Plan 02's already-early attempt_graph_augmentation call) and chunk candidates are normalized, weighted, and merged into one ranked sequence before the token-budget loop runs"
      pattern: "graph_weight"
---

<objective>
Upgrade Plan 02's append-only graph-context integration (graph facts placed after chunk evidence, unconditionally) into the score-interleaved, configurably-weighted integration D-29/D-30 specify — the RAG-path's production-grade shape, not the tracer's proof-of-concept shape. Prove the `graph_augmentation` outcome tag Plan 02 already built is genuinely observable through the full query handler for all three outcomes, not just in isolation.

Purpose: an always-on feature that runs on every `/rag/query` call must not silently degrade answer quality (unranked graph facts crowding out higher-value chunk evidence) or silently degrade observability (a failing traversal looking identical to "no relevant graph context existed").
Output: score-interleaved evidence packing with a configurable, validated `graph_weight`; end-to-end proof (behavioral + tracing-capture) that every outcome is observable.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-CONTEXT.md
@.planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-AI-SPEC.md
@.planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-REVIEWS.md
@rust-guidelines.md
@engine/src/prompt.rs
@engine/src/retrieval/mod.rs
</context>

## Artifacts This Phase Produces

- `engine/src/prompt.rs` — `pack_evidence_and_graph_prompt` upgraded in place from append-order to normalized-score interleaving; gains an explicit `graph_weight: f64` parameter (no longer implicit/absent).
- `engine/src/retrieval/mod.rs` — `graph_weight: f64` on `RetrievalSettings`, validated alongside `vector_weight`/`bm25_weight`.
- `engine/src/generation/mod.rs` — `GenerationRequest.graph_weight: f64` (new field, defaulted in `::new()`).
- `engine/src/generation/openrouter.rs` — `execute_one_call` reads `request.graph_weight` (not a second, independently-sourced value) when calling `pack_evidence_and_graph_prompt`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Score-interleave graph facts with chunk evidence under a validated, configurable graph_weight — threaded through GenerationRequest so it reaches the real provider request</name>
  <read_first>rust-guidelines.md, engine/src/prompt.rs (pack_evidence_and_graph_prompt and the reserve-one-citable-chunk rule from Plan 02, EvidenceBlock, GraphFactBlock, EvidenceBlock.id assignment in from_candidate), engine/src/main.rs (query_rag, and Plan 02's early attempt_graph_augmentation wiring and GenerationRequest construction), engine/src/retrieval/mod.rs (RetrievalSettings's vector_weight/bm25_weight fields and validate() pattern, lines ~219-313), engine/src/generation/mod.rs (GenerationRequest, GenerationRequest::new, confirmed via grep to have no external literal `GenerationRequest { .. }` constructors outside its own `impl` block), engine/src/generation/openrouter.rs (execute_one_call's existing call to pack_evidence_prompt / Plan 02's pack_evidence_and_graph_prompt, ~line 358)</read_first>
  <files>engine/src/prompt.rs, engine/src/main.rs, engine/src/retrieval/mod.rs, engine/src/generation/mod.rs, engine/src/generation/openrouter.rs, config/config.example.toml</files>
  <behavior>
    - Test 1: given one chunk EvidenceBlock with a high fused score and one GraphFactBlock with a low normalized score, the packed prompt places the chunk evidence block ahead of the graph fact (order reflects normalized+weighted score, not "chunk evidence always first" beyond the single reserved top block).
    - Test 2: given the reverse (a high-scoring graph fact, a low-scoring SECOND chunk block, with a first chunk block already reserved per Plan 02's rule) and a token budget too small to fit both, the packed prompt includes the graph fact and excludes the low-scoring second chunk block — proving graph facts genuinely compete for the shared budget beyond the one reserved slot.
    - Test 3: setting `graph_weight` to `0.0` excludes all graph facts from the packed prompt regardless of their raw match score (mirrors the existing zero-weight-disables-source pattern already proven for `vector_weight`/`bm25_weight` in Phase 03 D-41-family tests).
    - Test 4 (corrected per REVIEWS.md MEDIUM, this round: "sequential citation-marker claim conflicts with current implementation" — confirmed via source read: `EvidenceBlock.id` is assigned once in `from_candidate` as `[{original_candidate_index + 1}]`, `prompt.rs` ~line 35-36, and the packer never renumbers it, `prompt.rs` ~line 226-243; reserving the highest-scoring block first can legitimately move it ahead of a lower-index block in packed order, so "sequential in packed order" is not actually true of this codebase's ID scheme): every packed chunk `EvidenceBlock` keeps its STABLE, originally-assigned `[N]` marker (reflecting its rank in the original candidate list, fixed at construction) regardless of where score-interleaving places it in the packed sequence — markers are never renumbered/remapped during packing, and no two packed blocks can ever share a marker, since each was already globally unique before packing began. Graph facts never receive a `[N]` marker at any packed position (D-27 still holds under interleaving). This is a weaker, source-accurate claim than "sequential [1], [2]..." — do not implement ID remapping to try to force literal sequential numbering, since that would require also updating `resolve_citations`' marker-matching logic and risks a correctness regression for no behavioral benefit (the LLM is only ever shown the IDs actually present in its own rendered prompt, and cites by those, never by an assumed external sequence).
    - Test 5: `RetrievalSettings::validate()` rejects a non-finite, negative, or excessively large `graph_weight` with the same error shape already used for `vector_weight`/`bm25_weight` (REVIEWS.md MEDIUM: "graph_weight needs the same validation discipline as existing retrieval weights").
    - Test 6: the reserve-one-citable-chunk rule (Plan 02) still holds under interleaving — given exactly one chunk EvidenceBlock and a graph fact scored high enough that unbounded interleaving would exclude the chunk block, the packed prompt still includes that one chunk block.
    - Test 7 (new, REVIEWS.md HIGH this round: "graph_weight has no complete path to the actual provider request" — a real raw-HTTP-capture test, not only a `pack_evidence_and_graph_prompt`-level assertion): a full `query_rag` call, through the real `LancetServiceImpl` and a real local mock HTTP server capturing the outbound OpenRouter request body (mirroring Plan 02 Test 4's `captured_chat` pattern), run twice against the SAME fixture — one reserved top chunk block, one competing second chunk block, one graph fact, and a `max_prompt_tokens` deliberately sized (mirroring Test 2's scarcity setup above) so exactly one of {second chunk block, graph fact} fits after the reserved block — once with `effective_settings.retrieval.graph_weight = 1.0` (the graph fact's normalized+weighted score outranks the second chunk block, so the captured body's `messages[1].content` CONTAINS `<GRAPH_FACT` and lacks the second chunk block's text) and once with `graph_weight = 0.0` (the graph fact's weighted score drops to `0.0`, ranking below the second chunk block, so the captured body CONTAINS the second chunk block's text and does NOT contain `<GRAPH_FACT`) — proving the configured value reaches the real wire payload and actually changes what gets packed, not merely an intermediate `PackedEvidence` value main.rs might compute differently from what `openrouter.rs` actually sends.
  </behavior>
  <action>
    Add `graph_weight: f64` to `RetrievalSettings` (engine/src/retrieval/mod.rs) immediately after `bm25_weight`, `#[serde(default = "default_weight")]` reusing the existing `default_weight() -> f64 { 1.0 }` helper (do not add a second default-1.0 function). Add the same validation block `RetrievalSettings::validate()` already applies to `vector_weight`/`bm25_weight` (finite, `>= 0.0`, `<= MAX_SERVICE_RRF_WEIGHT`) for `graph_weight`, with an equivalent error message. Add `graph_weight: f64` to `RetrievalConfigSettings` (main.rs) mirroring `vector_weight`/`bm25_weight`'s exact field/default/threading pattern into `to_retrieval_settings()`. Add `graph_weight = 1.0` to `config/config.example.toml`'s `[engine.retrieval]` section (following the existing `# unit=unitless; range=finite 0.0..=16.0` comment convention already used for `vector_weight`/`bm25_weight`).

    **Thread `graph_weight` through `GenerationRequest` so it reaches the actual provider payload, not just an intermediate value main.rs computes locally (REVIEWS.md HIGH, this round — confirmed via source read: `GenerationRequest` currently has no weight field at all, `generation/mod.rs` ~line 375-381, and `openrouter.rs`'s `execute_one_call` calls the packer directly at ~line 358 using only `self.config`, which has no knowledge of `RetrievalSettings.graph_weight` at all — the two sites could silently diverge with no shared source of truth):** add `pub graph_weight: f64` to `GenerationRequest` (`generation/mod.rs`), defaulted to `1.0` inside `GenerationRequest::new(...)` (mirroring exactly how Plan 02 added `graph_facts: Vec::new()` — confirmed via grep that `GenerationRequest {` has no external literal-construction call sites anywhere in `engine/src`, only inside its own `impl` block, so this addition needs no other call-site changes). In `main.rs`'s `query_rag`, when constructing the `GenerationRequest` passed to `self.generator.generate(...)`, set `graph_weight: self.effective_settings.retrieval.graph_weight` explicitly. **Change `pack_evidence_and_graph_prompt`'s signature** (`prompt.rs`, Plan 02's function — same name, no rename, but it gains a new required parameter) to `pub fn pack_evidence_and_graph_prompt(question: &str, evidence: &[EvidenceBlock], graph_facts: &[GraphFactBlock], graph_weight: f64, max_prompt_tokens: usize, answer_token_budget: usize) -> Result<PackedEvidence, PromptAssemblyError>`. Update its TWO call sites so both read the SAME source: `main.rs`'s own pre-check call passes `self.effective_settings.retrieval.graph_weight` directly (same expression used to set `GenerationRequest.graph_weight` above — same field read twice, not two independently-maintained values); `openrouter.rs`'s `execute_one_call` passes `request.graph_weight` (the field on the `GenerationRequest` it was actually handed) — THIS is the call site whose packed prompt becomes the real outbound `messages[1].content`, so this is the value that provably reaches the wire per Test 7.

    Inside `pack_evidence_and_graph_prompt`, after reserving the single highest-scoring `EvidenceBlock` (Plan 02's existing rule, unchanged), min-max normalize the REMAINING `EvidenceBlock.score` values across the candidate set to `[0.0, 1.0]` (guard the degenerate all-equal-scores case — treat it as all-`1.0`, i.e. `if max == min { 1.0 } else { (score - min) / (max - min) }`), separately min-max normalize `GraphFactBlock`/`GraphFact.score` across the returned graph facts to `[0.0, 1.0]` using the SAME degenerate-case guard (Antigravity LOW, this round: "naive min-max division `(score - min) / (max - min)` will produce `NaN`" if all graph facts share one raw score, e.g. a single-fact result — apply the identical `max == min` check on this side too, not only the chunk side), multiply the graph side by the `graph_weight` parameter, merge both into one `Vec` sorted descending by (normalized, weighted) score, and pack sequentially into the existing token-budget loop. Chunk blocks keep their pre-assigned, stable `EvidenceBlock.id` unchanged regardless of packed position (see Test 4's corrected wording above — do not attempt to renumber markers into "sequential in packed order," which is not how `EvidenceBlock.id`/`resolve_citations` already work in this codebase); graph facts render via Plan 02's `<GRAPH_FACT>` delimiter with no marker, at whatever position their score places them. A `graph_weight` of exactly `0.0` drives every graph fact's weighted score to `0.0`, which — combined with the min-max-normalized chunk side never being forced below its own real range — means graph facts sort last and are the first candidates dropped once the token budget is exhausted; Test 7 below deliberately sizes its fixture so this manifests as a `<GRAPH_FACT`-absent captured body at `graph_weight = 0.0`.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked graph_facts_interleave_by_normalized_score && cargo test --manifest-path engine/Cargo.toml --locked graph_weight_zero_excludes_graph_facts && cargo test --manifest-path engine/Cargo.toml --locked packed_chunk_markers_stay_stable_under_interleaving && cargo test --manifest-path engine/Cargo.toml --locked graph_weight_validation_rejects_non_finite_negative_and_oversized && cargo test --manifest-path engine/Cargo.toml --locked reserve_one_citable_chunk_holds_under_interleaving && cargo test --manifest-path engine/Cargo.toml --locked graph_weight_reaches_actual_provider_request_body</automated>
  </verify>
  <done>Graph facts and chunk evidence (beyond the one reserved block) compete for the same token budget by normalized, weighted score; graph_weight = 0.0 provably excludes graph facts from the real outbound provider payload (not just an intermediate value); graph_weight is validated with the same discipline as the other retrieval weights; chunk evidence markers remain stable (not falsely claimed sequential) under interleaving.</done>
  <acceptance_criteria>
    - `grep -n "graph_weight" engine/src/main.rs engine/src/prompt.rs engine/src/retrieval/mod.rs engine/src/generation/mod.rs engine/src/generation/openrouter.rs config/config.example.toml` shows it threaded end to end (including through `GenerationRequest`) and validated.
    - `grep -n "pub graph_weight" engine/src/generation/mod.rs` matches on `GenerationRequest`.
    - `grep -n "request.graph_weight" engine/src/generation/openrouter.rs` matches inside `execute_one_call`'s call to `pack_evidence_and_graph_prompt` — the actual outbound-request code path reads the field on the request it was handed, not a separately-derived value.
    - The seven named tests pass under `cargo test --manifest-path engine/Cargo.toml --locked`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove graph_augmentation outcome tagging is observable through the full /rag/query handler for all three outcomes</name>
  <read_first>rust-guidelines.md, engine/src/main.rs (query_rag, Plan 02's attempt_graph_augmentation and its tracing-span recording), .planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-AI-SPEC.md (Section 6 Online Guardrails table, "Graph-augmentation failure-transparency tagging" row)</read_first>
  <files>engine/src/main.rs, engine/src/tests.rs</files>
  <behavior>
    - Test 1: a full `query_rag` call (through the real `LancetServiceImpl`, not calling `attempt_graph_augmentation` in isolation) whose seed vector-search finds a matching entity and whose traversal returns at least one neighbor records `graph_augmentation = "succeeded"` on the request's tracing span, captured via a test-only `tracing_subscriber::Layer` — this is the "operator-observable" proof REVIEWS.md asked for, not inferred from response shape alone.
    - Test 2: a full `query_rag` call whose seed vector-search finds no entity above `seed_match_min_score` records `graph_augmentation = "no_match_found"` on the span — and the query still returns a normal response (chunk-only evidence if any exists, or the NO_EVIDENCE early-return per Plan 02's Test 8).
    - Test 3: a full `query_rag` call under Plan 02's real forced-fault mechanism (deleted/corrupted LanceDB directory for `entities`/`entity_edges`) records `graph_augmentation = "attempted_and_failed"` on the span — and the query STILL returns successfully with chunk-only evidence (D-32), proving the tag is purely observational and never changes the response contract. This extends Plan 02's Test 6 (which proved the `Ok` response) with the tracing-capture assertion Plan 02 did not yet make.
  </behavior>
  <action>
    **Async-safe capture is load-bearing here, not incidental (REVIEWS.md's own concern about this test category, confirmed by re-checking: `engine/src/tests.rs` has zero pre-existing `tracing::subscriber`/`tracing_subscriber` usage — this is genuinely new test infrastructure, not a pattern to grep-and-match).** `tracing::subscriber::set_default(...)` installs a THREAD-LOCAL default subscriber; on the default `#[tokio::test]` multi-thread runtime flavor, `query_rag`'s `.await` points can resume on a different worker thread than the one that called `set_default`, silently losing the capture (the test would pass vacuously with an empty captured vec, or flake). Each of the three tests below MUST use `#[tokio::test(flavor = "current_thread")]` (not the bare `#[tokio::test]` default every other test in this file uses) so the entire call — `set_default` through `query_rag(...).await` returning — runs on one OS thread and the thread-local subscriber stays active throughout.

    Add a small test-only capture layer type in `engine/src/tests.rs`, e.g. `struct GraphAugmentationCaptureLayer { captured: Arc<Mutex<Vec<String>>> }`, implementing `tracing_subscriber::layer::Layer<S>` for a generic `S: tracing::Subscriber`. Implement `fn on_record(&self, _id: &tracing::span::Id, values: &tracing::span::Record<'_>, _ctx: tracing_subscriber::layer::Context<'_, S>)`: construct a local visitor implementing `tracing::field::Visit` whose `record_str(&mut self, field: &tracing::field::Field, value: &str)` (and, defensively, `record_debug` as a fallback) checks `field.name() == "graph_augmentation"` and, if so, pushes `value.to_string()` into the shared `Arc<Mutex<Vec<String>>>`; call `values.record(&mut visitor)`. Build the subscriber for each test via `tracing_subscriber::registry().with(GraphAugmentationCaptureLayer { captured: captured.clone() })`, install it with `let _guard = tracing::subscriber::set_default(subscriber);` (the returned guard's `Drop` resets the previous default — keep it alive for the whole `query_rag(...).await` call), then call `query_rag(...).await` directly (already inside the `current_thread`-flavored async test fn, no `with_default`/closure wrapper needed). After the call, assert the captured `Vec<String>` contains exactly the expected outcome string. This does not change `query_rag`'s production code (Plan 02 already emits the field via `tracing::Span::current().record(...)`) — it only adds the missing observability proof, now genuinely async-safe.
  </action>
  <verify>
    <automated>cargo test --manifest-path engine/Cargo.toml --locked graph_augmentation_succeeded_is_observable_end_to_end && cargo test --manifest-path engine/Cargo.toml --locked graph_augmentation_no_match_found_is_observable_end_to_end && cargo test --manifest-path engine/Cargo.toml --locked graph_augmentation_attempted_and_failed_is_observable_end_to_end</automated>
  </verify>
  <done>All three `graph_augmentation` outcomes are proven observable through the real tracing span of a full `/rag/query` call, not only through Plan 02's isolated-function tests; none of the three cases changes the response contract for callers.</done>
  <acceptance_criteria>
    - The three named tests pass, and none of them assert a non-200/non-Ok response for any of the three outcomes.
    - `grep -n "graph_augmentation" engine/src/main.rs` matches the field name recorded on the tracing span (already emitted by Plan 02; this task only proves it observable).
    - `grep -n "flavor = \"current_thread\"" engine/src/tests.rs` matches at least three occurrences (one per new test) — confirms the async-safety fix, not the bare `#[tokio::test]` default every other test in this file uses.
    - `grep -n "impl.*Layer" engine/src/tests.rs` and `grep -n "tracing::subscriber::set_default" engine/src/tests.rs` both match — confirms a real capture layer + thread-local guard, not a placeholder assertion.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graph_augmentation outcome tag → tracing/log output | Purely observational metadata; must not leak entity/relation row content (only the outcome enum + counts), consistent with D-33's error-message-stays-clean constraint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-13 | Information Disclosure | `graph_augmentation` tracing field | low | mitigate | The tag carries only the outcome enum and small counts (e.g. fact count), never entity names/relation text or error detail beyond kind — matches the existing `GraphSpikeError::Display` convention of never carrying row values |
| T-04.1-14 | Repudiation | Silent degradation of graph-augmentation value over time (Section 1b's "silent query-time degradation" known failure mode) | medium | mitigate | The three-way tag directly closes this — a sustained `attempted_and_failed` or `no_match_found` rate becomes visible to anyone inspecting logs/traces, now proven observable end-to-end by this plan; full alerting thresholds are Phase 6/OBS-01 scope |

No canon-referral items this plan.
</threat_model>

<verification>
- `cargo test --manifest-path engine/Cargo.toml --locked` passes in full.
- A manual `/rag/query` call against a corpus with no matching entities confirms the response is unchanged in shape from Plan 02/04 (D-32's contract is not broken by this plan's additions).
</verification>

<success_criteria>
Graph context and chunk evidence compete fairly for the shared token budget under a validated, configurable weight that provably reaches the real outbound provider request body (not merely an intermediate value), chunk evidence markers remain stable and correctly documented under interleaving, and every query's graph-augmentation attempt is genuinely operator-observable (proven via async-safe tracing capture, not just inferred) without changing the response contract for callers.
</success_criteria>

<output>
Create `.planning/phases/04.1-knowledge-graph-extraction-query-full-implementation/04.1-05-SUMMARY.md` when done.
</output>
