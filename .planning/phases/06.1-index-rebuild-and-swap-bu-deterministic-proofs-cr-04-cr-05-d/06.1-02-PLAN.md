---
phase: 06.1-index-rebuild-and-swap-bu-deterministic-proofs-cr-04-cr-05-d
plan: 02
type: execute
wave: 2
depends_on:
  - 06.1-01
files_modified:
  - scripts/test_phase02_live_evidence.py
  - scripts/phase02_live_evidence.py
autonomous: true
requirements:
  - RAG-03
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "DEBT-BU-01 closes via a deterministic test using a controlled/injected clock: matching challenge/evidence identity and issue times, exceeding only issued_at -> generated_at, asserting the dedicated complete-run-window error classification (D-07)."
    - "The BU-01 proof calls validate_evidence directly with an injected now datetime, completely decoupled from machine wall clock and CLI subprocess invocation (D-07)."
    - "DEBT-BU-02 closes via deterministic tests: caller-fixture SHA-256 and byte length preserved across success plus representative early and post-upload failures, using script-created temporary inputs only, with no live services (D-07)."
    - "All Python evidence verification tests run under python3 -O -I and pass with zero failures."
  prohibitions:
    - statement: "MUST NOT execute live engine, PostgreSQL, or OpenRouter services in BU-01 or BU-02 tests. All tests must be 100% deterministic and hermetic."
      status: resolved
      verification: test
      category: reproducibility
    - statement: "MUST NOT delete or truncate caller-owned sample files during verify-ingestion.sh execution in any terminal outcome (early failure, post-upload failure, or success)."
      status: resolved
      verification: test
      category: safety
  artifacts:
    - path: "scripts/test_phase02_live_evidence.py"
      provides: "Deterministic test suite containing controlled-clock validate_evidence tests for BU-01 and mock-backed three-scenario caller sample preservation tests for BU-02."
    - path: "scripts/phase02_live_evidence.py"
      provides: "Evidence validation logic supporting injectable now datetime."
  key_links:
    - from: "scripts/test_phase02_live_evidence.py"
      to: "scripts/phase02_live_evidence.py"
      via: "test_bu01 calls validate_evidence(evidence, challenge, now=controlled_clock)"
      pattern: "validate_evidence"
    - from: "scripts/test_phase02_live_evidence.py"
      to: "verify-ingestion.sh"
      via: "test_bu02 runs verify-ingestion.sh with mock gateway and caller sample file"
      pattern: "verify-ingestion.sh"
---

## Phase User Story

**As a** Lancet verification maintainer, **I want** deterministic, hermetic proofs for evidence window validation (BU-01) and caller-supplied fixture preservation across all failure and success modes (BU-02), **so that** verification evidence tests execute reproducibly with zero reliance on machine wall clock or live provider infrastructure.

<objective>
Implement deterministic unit and integration test proofs in Python for DEBT-BU-01 (controlled-clock evidence window verification) and DEBT-BU-02 (caller-fixture SHA-256 and byte preservation across success, early failure, and post-upload failure).

Purpose: Satisfies the Phase 6.1 D-07 requirement by converting timing-sensitive and live-run evidence checks into fast, deterministic, hermetic tests that run without external dependencies or wall-clock drift.
Output: Extended test suite in `scripts/test_phase02_live_evidence.py` closing DEBT-BU-01 and DEBT-BU-02.
</objective>

<execution_context>
@D:/Repos/lancet/.claude/gsd-core/workflows/execute-plan.md
@D:/Repos/lancet/.claude/gsd-core/templates/summary.md
@D:/Repos/lancet/.claude/gsd-core/references/planner-preconditions.md
@D:/Repos/lancet/.claude/gsd-core/references/planner-reversibility.md
@D:/Repos/lancet/.claude/gsd-core/references/planner-mvp-mode.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-observability-evaluation-polish/06-CONTEXT.md
@.planning/phases/06.1-index-rebuild-and-swap-bu-deterministic-proofs-cr-04-cr-05-d/06.1-RESEARCH.md
@.planning/phases/06.1-index-rebuild-and-swap-bu-deterministic-proofs-cr-04-cr-05-d/06.1-VALIDATION.md
@scripts/phase02_live_evidence.py
@scripts/test_phase02_live_evidence.py
@verify-ingestion.sh
</context>

<scope_rationale>
Two focused tasks in Python test infrastructure:
Task 1 delivers the controlled-clock proof for DEBT-BU-01 by importing `validate_evidence` directly and asserting exact error classifications with synthetic UTC timestamps.
Task 2 delivers the three-scenario caller-fixture preservation proof for DEBT-BU-02 using a local loopback mock HTTP server with SHA-256 and byte-length equality assertions across success, early failure, and post-upload failure.
</scope_rationale>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: DEBT-BU-01 deterministic controlled-clock proof</name>
  <reversibility rating="reversible">Adds standalone deterministic unit tests to scripts/test_phase02_live_evidence.py with zero runtime impact.</reversibility>
  <read_first>06-CONTEXT.md D-07; .planning/phases/06.1-index-rebuild-and-swap-bu-deterministic-proofs-cr-04-cr-05-d/06.1-RESEARCH.md § Summary (workstream 2), § Common Pitfalls (Pitfall 3 - Controlled clock), § Code Examples (injectable clock); scripts/phase02_live_evidence.py lines 130-140 and 332-378; scripts/test_phase02_live_evidence.py</read_first>
  <files>scripts/test_phase02_live_evidence.py, scripts/phase02_live_evidence.py</files>
  <behavior>
    - validate_evidence is called directly in a unit test with synthetic timestamps and an injected now datetime.
    - When challenge.issued_at = "2026-01-01T00:00:00Z", run_started_at = "2026-01-01T00:05:00Z" (within 35m start delay window), generated_at = "2026-01-01T00:36:00Z" (exceeds 35m MAX_RUN_WINDOW), and now = "2026-01-01T00:40:00Z" (within 30m staleness), validate_evidence raises ValidationError with exact message "complete run window exceeded".
    - The classification specifically distinguishes "complete run window exceeded" from "run start delay exceeded", "evidence.generated_at is stale", and "evidence timestamps are not ordered".
    - A corresponding valid case with generated_at = "2026-01-01T00:30:00Z" succeeds under the same injected clock.
  </behavior>
  <action>In scripts/test_phase02_live_evidence.py, import validate_evidence, ValidationError, parse_timestamp, and UTC from phase02_live_evidence.

Implement test_bu01_deterministic_controlled_clock_run_window_exceeded:
Construct a valid base challenge dictionary and base evidence dictionary with matching challenge_id, corpus_fingerprint, and command metadata.
Set issued_at to dt.datetime(2026, 1, 1, 0, 0, 0, tzinfo=UTC).
Set run_started_at to dt.datetime(2026, 1, 1, 0, 5, 0, tzinfo=UTC) (5-minute delta, well within the 35-minute start delay limit).
Set generated_at to dt.datetime(2026, 1, 1, 0, 36, 0, tzinfo=UTC) (36-minute delta, strictly exceeding MAX_RUN_WINDOW of 35 minutes).
Set now to dt.datetime(2026, 1, 1, 0, 40, 0, tzinfo=UTC) (4 minutes after generation, well within MAX_EVIDENCE_AGE of 30 minutes).
Assert with self.assertRaisesRegex(ValidationError, r"^complete run window exceeded$") that validate_evidence(evidence, challenge, now=now) raises ValidationError with that exact message.

Also implement test_bu01_deterministic_controlled_clock_within_window_succeeds:
Set generated_at to dt.datetime(2026, 1, 1, 0, 30, 0, tzinfo=UTC) and verify validate_evidence returns the validated evidence dictionary cleanly without error.</action>
  <acceptance_criteria>
    - scripts/test_phase02_live_evidence.py contains test_bu01_deterministic_controlled_clock_run_window_exceeded and test_bu01_deterministic_controlled_clock_within_window_succeeds.
    - Tests invoke validate_evidence directly with explicit now parameter and synthetic timestamps.
    - The test asserts exact error message "complete run window exceeded".
    - python3 -O -I scripts/test_phase02_live_evidence.py -k test_bu01 passes cleanly.
  </acceptance_criteria>
  <verify>
    <automated>python3 -O -I scripts/test_phase02_live_evidence.py -k test_bu01</automated>
  </verify>
  <done>DEBT-BU-01 is proven closed via deterministic controlled-clock unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DEBT-BU-02 caller-fixture preservation proof across success, early failure, and post-upload failure</name>
  <reversibility rating="reversible">Extends scripts/test_phase02_live_evidence.py with mock HTTP server testing for verify-ingestion.sh caller fixture preservation.</reversibility>
  <read_first>06-CONTEXT.md D-07; .planning/phases/06.1-index-rebuild-and-swap-bu-deterministic-proofs-cr-04-cr-05-d/06.1-RESEARCH.md § Common Pitfalls (Pitfall 4 - Three scenarios), § Assumptions Log (A4); verify-ingestion.sh lines 45-60 and 145-160; scripts/test_phase02_live_evidence.py lines 540-660</read_first>
  <files>scripts/test_phase02_live_evidence.py</files>
  <behavior>
    - A caller-supplied fixture file (passed as --sample / sample_owned=false) has its SHA-256 hash and byte length recorded prior to verify-ingestion.sh execution.
    - In scenario 1 (early failure: gateway unreachable / immediate curl error), verify-ingestion.sh exits nonzero, and the caller sample file remains intact with identical SHA-256 and byte length.
    - In scenario 2 (post-upload failure: upload succeeds with job_id, but job polling returns status "failed"), verify-ingestion.sh exits nonzero, and the caller sample file remains intact with identical SHA-256 and byte length.
    - In scenario 3 (success: upload succeeds and polling returns status "completed"), verify-ingestion.sh completes its upload phase, and the caller sample file remains intact with identical SHA-256 and byte length.
    - All three scenarios execute deterministically on loopback without live gateway/engine/database processes.
  </behavior>
  <action>In scripts/test_phase02_live_evidence.py, implement helper classes and test cases for DEBT-BU-02:
Create a lightweight local HTTP mock server (using http.server.HTTPServer and http.server.BaseHTTPRequestHandler in a background thread) that can be configured to simulate:
1. Early failure (no server listening on gateway port or immediate 500 error).
2. Post-upload failure: responds to POST /documents with 200 {"job_id":"job-fail-1"} and to GET /documents/job-fail-1 with 200 {"status":"failed","error":"chunking error"}.
3. Success: responds to POST /documents with 200 {"job_id":"job-ok-1"} and to GET /documents/job-ok-1 with 200 {"status":"completed"}.

For each scenario, create a temporary caller fixture file with distinct test content, compute its initial hashlib.sha256(content).hexdigest() and len(content), run verify-ingestion.sh with --sample pointing to the temporary file and GATEWAY_URL pointing to the mock server, and in a finally block:
1. Assert os.path.exists(sample_path) is True.
2. Read the file bytes and assert hashlib.sha256(final_bytes).hexdigest() equals initial_hash.
3. Assert len(final_bytes) equals initial_len.

Combine existing test_caller_sample_preservation_on_early_failure into this comprehensive suite under test_bu02_caller_fixture_preservation_early_failure, test_bu02_caller_fixture_preservation_post_upload_failure, and test_bu02_caller_fixture_preservation_success.
Run the complete Python test suite to ensure all tests pass.</action>
  <acceptance_criteria>
    - scripts/test_phase02_live_evidence.py contains named tests covering all three scenarios: early failure, post-upload failure, and success.
    - Every scenario asserts that the caller sample file exists, its byte length is preserved, and its SHA-256 digest is unchanged.
    - Zero live external processes (OpenRouter, live engine) are contacted.
    - python3 -O -I scripts/test_phase02_live_evidence.py passes with 0 failures and 0 errors.
  </acceptance_criteria>
  <verify>
    <automated>python3 -O -I scripts/test_phase02_live_evidence.py -k test_bu02</automated>
    <automated>python3 -O -I scripts/test_phase02_live_evidence.py</automated>
  </verify>
  <done>DEBT-BU-02 is proven closed across success, early failure, and post-upload failure with byte and SHA-256 equality assertions.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols and paths created or modified by this plan.

**Created or modified by this plan**
- `scripts/test_phase02_live_evidence.py`:
  - `test_bu01_deterministic_controlled_clock_run_window_exceeded`
  - `test_bu01_deterministic_controlled_clock_within_window_succeeds`
  - `test_bu02_caller_fixture_preservation_early_failure`
  - `test_bu02_caller_fixture_preservation_post_upload_failure`
  - `test_bu02_caller_fixture_preservation_success`
  - Local loopback mock HTTP server for hermetic gateway protocol simulation.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification Script -> Caller File System | verify-ingestion.sh receives path to user-owned fixture; must not modify or remove caller-owned files under any failure condition. |
| Time Evaluation -> Evidence Acceptance | validate_evidence evaluates evidence timestamps; must enforce exact run windows without allowing clock drift or ordering bypass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.1-02-01 | Tampering | Fixture file corruption/deletion on failure | medium | mitigate | D-07 / BU-02: verify-ingestion.sh cleanup traps guard sample_owned flag; tests assert SHA-256 and byte length preservation across success, early failure, and post-upload failure. |
| T-06.1-02-02 | Repudiation / Timing Attack | Stale or overlong evidence accepted due to clock drift | medium | mitigate | D-07 / BU-01: validate_evidence strictly checks generated_at - issued_at <= MAX_RUN_WINDOW; deterministic tests prove exact failure classification with injected timestamps. |

</threat_model>

<verification>
- `python3 -O -I scripts/test_phase02_live_evidence.py` exits 0 with all test cases passing.
- `bash verify-live-evidence.sh --self-test` (or python3 scripts/phase02_live_evidence.py self-test) passes.
</verification>

<success_criteria>
- DEBT-BU-01 is verified via controlled-clock unit tests asserting the exact complete-run-window classification.
- DEBT-BU-02 is verified across success, early failure, and post-upload failure with byte and SHA-256 hash preservation.
- Zero live services or wall-clock dependencies are required for evidence verification tests.
</success_criteria>

<output>
Create `.planning/phases/06.1-index-rebuild-and-swap-bu-deterministic-proofs-cr-04-cr-05-d/06.1-02-SUMMARY.md` upon completion, detailing the test cases added, the mock server structure, and the verification output.
</output>
