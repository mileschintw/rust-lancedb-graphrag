---
phase: 6.1
slug: index-rebuild-and-swap-bu-deterministic-proofs-cr-04-cr-05-d
status: ready
nyquist_compliant: true
wave_0_complete: false
created: 2026-08-23
---

# Phase 6.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded by `/gsd-plan-phase 6.1` from `06.1-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework (Rust)** | Built-in `cargo test` + `#[tokio::test]` (tokio `~1.53`, `test-util` feature for paused-clock proofs) |
| **Framework (Python, BU-01/BU-02)** | Built-in `unittest`, invoked via `python3 -O -I scripts/test_phase02_live_evidence.py` |
| **Framework (Go)** | Built-in `go test` (CR-04/CR-05 verification only) |
| **Config file** | none — cargo/go/unittest conventions |
| **Quick run command (Rust)** | `cargo test --manifest-path engine/Cargo.toml --lib` |
| **Quick run command (Python)** | `python3 -O -I scripts/test_phase02_live_evidence.py` |
| **Full suite command** | `cargo test --manifest-path engine/Cargo.toml --locked && (cd gateway && go test ./...)` |
| **Build command** | `cargo build --manifest-path engine/Cargo.toml && (cd gateway && go build ./...)` |
| **Supplementary gates** | `cargo clippy --manifest-path engine/Cargo.toml -- -D warnings`, `cargo fmt --check`, `go vet ./...` |
| **Estimated runtime** | ~30s incremental per target; full gate under a few minutes |

**Baseline (measured 2026-08-23):**
- Rust `lib.rs`: 357 cases
- Rust `inspect_lancedb.rs`: 18 cases
- Rust `config_startup.rs`: 17 cases
- Rust Total: 392 cases
- Go `gateway`: 80 cases

---

## Sampling Rate

- **After every task commit:** Run focused command (`cargo test --manifest-path engine/Cargo.toml --lib -- <focused>` for engine work, `python3 -O -I scripts/test_phase02_live_evidence.py -k <TestName>` for BU-01/BU-02 work).
- **After every plan wave:** Run `cargo test --manifest-path engine/Cargo.toml --locked && (cd gateway && go test ./...)` plus `python3 -O -I scripts/test_phase02_live_evidence.py`.
- **Before `/gsd-verify-work`:** Full suite green + clippy + fmt + go vet.
- **Max feedback latency:** 30 seconds (quick run), full gate at wave boundaries.

> **D-85: there is no CI.** These commands *are* the verification path.

---

## Per-Requirement Verification Map

| Req ID | Behavior | Test Type | Automated Command | File Exists | Status |
|---|---|---|---|---|---|
| RAG-03 / D-20 | Successful post-ingest BM25 rebuild swaps into `Bm25IndexStore`; query after swap sees new docs | unit (`#[tokio::test]`) | `cargo test --manifest-path engine/Cargo.toml --lib -- rebuild_swap_updates_bm25` | ❌ W0 | ⬜ pending |
| RAG-03 / D-21 | Query concurrent with swap returns results from exactly one generation | unit | `cargo test --manifest-path engine/Cargo.toml --lib -- rebuild_swap_generation_atomicity` | ❌ W0 | ⬜ pending |
| RAG-03 / D-22 | In-flight query never blocks on rebuild in progress | unit | `cargo test --manifest-path engine/Cargo.toml --lib -- query_never_blocks_on_rebuild` | ❌ W0 | ⬜ pending |
| RAG-03 / D-23 | Burst of N document completions inside debounce window produces exactly 1 rebuild call | unit | `cargo test --manifest-path engine/Cargo.toml --lib -- debounce_coalesces_burst` | ❌ W0 | ⬜ pending |
| RAG-03 / D-24 | Dense & BM25 candidates in `RetrievalSnapshot` always agree on generation; mismatch rejected/falls back | unit | `cargo test --manifest-path engine/Cargo.toml --lib -- generation_agreement_dense_bm25` | ❌ W0 | ⬜ pending |
| RAG-03 / D-25 (startup) | Failed initial BM25 build stops engine before readiness | integration (subprocess) | `cargo test --manifest-path engine/Cargo.toml --test config_startup -- initial_bm25_failure_blocks_readiness` | ✅ (`config_startup.rs:339`) | ⬜ pending |
| RAG-03 / D-25 (post-ingest) | Failed post-ingest rebuild keeps previous gen serving, emits `INDEX_REBUILD_FAILED`, error log | unit | `cargo test --manifest-path engine/Cargo.toml --lib -- rebuild_failure_degrades_not_fails` | ❌ W0 | ⬜ pending |
| RAG-03 / D-26 | Corpus generation at startup equals corpus-wide max of `staged_documents.generation` | unit | `cargo test --manifest-path engine/Cargo.toml --lib -- startup_generation_derived_from_lancedb` | ❌ W0 | ⬜ pending |
| D-84 (debounce key) | Present-but-invalid `LANCET_ENGINE__WORKFLOW__REBUILD_DEBOUNCE_MS` fails startup closed | integration (subprocess) | `cargo test --manifest-path engine/Cargo.toml --test config_startup -- rebuild_debounce_ms_invalid_env_fails_closed` | ❌ W0 | ⬜ pending |
| Notice Reachability | `IndexRebuildFailed`/`IndexStale`/`IndexGenerationMismatch` reservations updated to real emission | static/unit | `cargo test --manifest-path engine/Cargo.toml --lib -- test_notice_published_enum_reachability_or_reservation` | ✅ (`workflow_phase5.rs:3758`) | ⬜ pending |
| D-07 / DEBT-BU-01 | Controlled-clock proof: matching identity/issue times, only `issued_at->generated_at` exceeded, exact "complete run window exceeded" message | unit (Python) | `python3 -O -I scripts/test_phase02_live_evidence.py -k test_bu01_deterministic_run_window` | ❌ W0 | ⬜ pending |
| D-07 / DEBT-BU-02 | Caller-fixture SHA-256/bytes preserved across success, early failure, post-upload failure without live services | unit/integration (Python) | `python3 -O -I scripts/test_phase02_live_evidence.py -k test_bu02_caller_fixture_preservation` | ❌ W0 | ⬜ pending |
| D-06 / DEBT-CR-04 / CR-05 | Loopback guardrail holds; no trigger fired; re-acceptance recorded | regression + docs | `cd gateway && go test ./... -run TestGatewayAddressIsLoopback` | ✅ (`gateway/main_test.go:1788`) | ⬜ pending |

---

## Wave 0 Requirements

- [ ] Shared `{bm25, generation, nodes_version}` swap-unit shape defined in `engine/src/workflow/ports.rs` / `service.rs`.
- [ ] Seeded-LanceDB test fixture helper for `staged_documents` table with multi-document generation distribution.
- [ ] Counted/observable rebuild-invocation seam for the D-23 debounce-coalescing test.
- [ ] Local loopback-bound fake HTTP responder / shim for BU-02 success and post-upload failure scenarios.
- [ ] Real emission sites for `NOTICE_CODE_INDEX_REBUILD_FAILED`, `NOTICE_CODE_INDEX_STALE`, and `NOTICE_CODE_INDEX_GENERATION_MISMATCH`.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|---|---|---|---|
| D-06 SC6/SC7 Re-acceptance Review | DEBT-CR-04 / DEBT-CR-05 | Local-only architecture review, no code changes | Document review findings in plan summary / phase summary confirming loopback bind and lack of external triggers |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** ready

